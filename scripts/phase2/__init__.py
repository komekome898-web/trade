"""Phase-2 research unit runners (docs/PHASE2_SPEC.md).

One module per research unit; each is a standalone script whose core
functions are importable so tests/ can exercise them on synthetic data.
"""
