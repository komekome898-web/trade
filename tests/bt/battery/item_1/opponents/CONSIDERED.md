# 項目 1「データと時刻」— 動かせなかった候補の検討表

場面係が書く(委任文 §3「動かせない候補の検討と再現」・場面集の規則 6・9)。この表は `tests/bt/battery/item_1/gen_considered.py` が作る。

- 候補 = REQUIREMENTS.md §3 が名を挙げた 13 件(§3.2 の機械抽出 54・73・74・87、§3.3 の grep の当たり 23・12・55・121・20・105・61・3・21)。
  §3.4 の番号の並び「5・7」は、§3.3 が名を付けた候補(L584 の Backtesting.py = 台帳 55、L614 の QSTrader = 台帳 121)と読んだ(台帳の 5 は Lean CLI、7 は Superalgos で、§3 のどこにも名が出ない)。「その他」は数えられないので含めていない(リードに聞くこと)。
- 「動かせた候補」= 隔離した venv で動き、その観点の場面で「結果なし」以外を 1 つ以上返した候補(survey_results/opp_*.tsv から機械で数えた)。導入はできたが、その観点の場面を 1 つも取れなかった候補は、その観点では表の行に載せる(規則 4 の「候補ごとの欠け」)。
- 段: どの観点にも無い(REQUIREMENTS.md §3.1。`段(機構)`・`段(既定)` は約定の模型の観点だけの列)。だから「段が低い」を理由にしたスキップは 1 件も無い。
- 実装の列の <venvs> は `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad/bt/venvs`、「…」は `lib/python3.11/site-packages`。
- 「持たないと確認した」の根拠は、導入済みの道具では adapter が実際に呼んだ結果(survey_results の表)と導入済みのコードの行、74 は SCAN の書き写しの行。

### 観点 V1 全資産の読み込み

動かせた候補: 1 件(54 finmarketpy)

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応)。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V1 の場面が全部「結果なし」(adapter の理由: PySystemtrade の CSV の読み口(csvFuturesContractPriceData)は <銘柄>_<限月>.csv の先物の足を 1 列の日付の索引で読む。約定・気配の行、時刻の単位・時間帯、date と time の 2 列の口が無い。重複は結合のときに黙って落とす(futures_per_co) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行)。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V1 の場面が全部「結果なし」(adapter の理由: pybotters にファイルを読む口が無い(取引所への HTTP / WebSocket の通信と、受けた通知を貯める DataStore だけ。read_csv・csv.reader の当たりは 0 件)。時刻の文字列は変換せずそのまま持つ(SCAN 2788 行)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V1 の場面が全部「結果なし」(adapter の理由: VnPy(本体と vnpy_ctastrategy)にファイルを読む口が無い(read_csv・csv.reader の当たりは 0 件。CSV の取り込みは別の部品 vnpy_datamanager で、調査報告に無く導入していない)) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V1 の場面が全部「結果なし」(adapter の理由: Qlib の配布物に CSV の市場データを読む口が無い(CSV から Qlib の形に直す scripts/dump_bin.py はリポジトリの側にあり配布物に入っていない。配布物の read_csv の当たりは銘柄の一覧・暦・注文の表・予測の表だけ)) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V1 の場面が全部「結果なし」(adapter の理由: hftbacktest のファイルの読み口は取引所・業者ごとに形が固定の変換器(tardis.convert は Tardis の trades / incremental_book_L2 の CSV)で、列の対応・時刻の単位・時間帯の引数が無い。検査(data/validation.py の correct_even) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ)。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V1 の場面が全部「結果なし」(adapter の理由: backtesting.py に市場データのファイルを読む口が無い(Backtest はメモリ上の OHLC の DataFrame を取る。read_csv の当たりは同梱の例のデータを読む test/__init__.py だけ)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行)。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V1 の場面が全部「結果なし」(adapter の理由: barter にファイルの市場データを読む口が無い(エンジンは型つきの MarketEvent の列を受ける。barter-data の正規化は取引所の WebSocket の通知に掛かる。grep で csv::Reader・ReaderBuilder の当たりは 0 件)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData)。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V1 の場面が全部「結果なし」(adapter の理由: vectorbt に市場データのファイルを読む口が無い(grep -rn 'read_csv/class CSVData' vectorbt/ は 0 件。data の型は Yahoo・Binance・CCXT・Alpaca からの取得と合成だけ)) |
| 74 ml-quant-trading | 入力は重みと収益の行列(SCAN 5885 行「weights : np.ndarray [T, N]」「returns : np.ndarray [T, N]」)で、ファイルの読み口の記載なし | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 入力は重みと収益の行列(SCAN 5885 行「weights : np.ndarray [T, N]」「returns : np.ndarray [T, N]」)で、ファイルの読み口の記載なし。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行)。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V1 の場面が全部「結果なし」(adapter の理由: PyTrendFollow に任意の CSV の市場データを読む口が無い(データは自前の取得器から自前の保管へ。grep で read_csv・csv.reader の当たりは 0 件)) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行)。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V1 の場面が全部「結果なし」(adapter の理由: slippage の読み口(io.load_bars・load_orders)は列が固定(足は symbol,timestamp,open,high,low,close,volume。io.py:44)で、場面のファイルの列(symbol の列が無い・名前が違う)を渡す引数が無い。約定を読む口は無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「宣言(spec)だけで資産の違うファイルを共通の事象にする」口が無い: CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V1 の場面が全部「結果なし」(adapter の理由: QSTrader の読み口 CSVDailyBarDataSource は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。列の対応・日中の時刻・単位・時間帯の引数が無い) |

### 観点 V2 時刻の単位の正規化

動かせた候補: 0 件

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 日付の索引 1 列と日付の書式だけ(csv_futures_contract_prices.py 17-24 行)。数の単位・時間帯・2 列の口なし。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V2 の場面が全部「結果なし」(adapter の理由: PySystemtrade の CSV の読み口(csvFuturesContractPriceData)は <銘柄>_<限月>.csv の先物の足を 1 列の日付の索引で読む。約定・気配の行、時刻の単位・時間帯、date と time の 2 列の口が無い。重複は結合のときに黙って落とす(futures_per_co) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 時刻の文字列を変換しない(SCAN 2788 行)。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V2 の場面が全部「結果なし」(adapter の理由: pybotters にファイルを読む口が無い(取引所への HTTP / WebSocket の通信と、受けた通知を貯める DataStore だけ。read_csv・csv.reader の当たりは 0 件)。時刻の文字列は変換せずそのまま持つ(SCAN 2788 行)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: ファイルの時刻を読む口が無い(V1 の行)。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V2 の場面が全部「結果なし」(adapter の理由: VnPy(本体と vnpy_ctastrategy)にファイルを読む口が無い(read_csv・csv.reader の当たりは 0 件。CSV の取り込みは別の部品 vnpy_datamanager で、調査報告に無く導入していない)) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: ファイルの時刻を読む口が無い(V1 の行)。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V2 の場面が全部「結果なし」(adapter の理由: Qlib の配布物に CSV の市場データを読む口が無い(CSV から Qlib の形に直す scripts/dump_bin.py はリポジトリの側にあり配布物に入っていない。配布物の read_csv の当たりは銘柄の一覧・暦・注文の表・予測の表だけ)) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 時刻は変換器ごとの固定(tardis.py 56 行以下はマイクロ秒の整数)。単位・時間帯の引数なし。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V2 の場面が全部「結果なし」(adapter の理由: hftbacktest のファイルの読み口は取引所・業者ごとに形が固定の変換器(tardis.convert は Tardis の trades / incremental_book_L2 の CSV)で、列の対応・時刻の単位・時間帯の引数が無い。検査(data/validation.py の correct_even) |
| 54 finmarketpy | findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行) | 実装のコードで確かめた(<venvs>/item_0/finmarketpy/…/findatapy/market/ioengine.py 1080・1108-1130 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 時刻は最初の列だけ・日付の文字列の型だけ("dukascopy" は秒まで、既定は DD/MM/YYYY。ioengine.py 1108-1121 行)。数の単位・時間帯の引数なし。`run_battery.py --target opp_finmarketpy` の出力 survey_results/opp_finmarketpy.tsv の V2 の場面が全部「結果なし」(adapter の理由: read_csv_data_frame は全列を float32 にするので、文字列の列(売買の向き)を持つ約定の形を読めない) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: ファイルの時刻を読む口が無い(V1 の行)。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V2 の場面が全部「結果なし」(adapter の理由: backtesting.py に市場データのファイルを読む口が無い(Backtest はメモリ上の OHLC の DataFrame を取る。read_csv の当たりは同梱の例のデータを読む test/__init__.py だけ)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 時刻の口は秒(f64)とミリ秒の 4 関数だけ(barter-integration/src/serde/de/util.rs 20・32・44・56 行)。マイクロ・ナノ・時間帯の口なし。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V2 の場面が全部「結果なし」(adapter の理由: barter にファイルの市場データを読む口が無い(エンジンは型つきの MarketEvent の列を受ける。barter-data の正規化は取引所の WebSocket の通知に掛かる。grep で csv::Reader・ReaderBuilder の当たりは 0 件)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: ファイルの時刻を読む口が無い(V1 の行)。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V2 の場面が全部「結果なし」(adapter の理由: vectorbt に市場データのファイルを読む口が無い(grep -rn 'read_csv/class CSVData' vectorbt/ は 0 件。data の型は Yahoo・Binance・CCXT・Alpaca からの取得と合成だけ)) |
| 74 ml-quant-trading | 粒度は日(SCAN 5886 行「単位は日で、足より細かい入力の口が無い」)。時刻の単位の正規化の記載なし | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 粒度は日(SCAN 5886 行「単位は日で、足より細かい入力の口が無い」)。時刻の単位の正規化の記載なし。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: ファイルの時刻を読む口が無い(V1 の行)。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V2 の場面が全部「結果なし」(adapter の理由: PyTrendFollow に任意の CSV の市場データを読む口が無い(データは自前の取得器から自前の保管へ。grep で read_csv・csv.reader の当たりは 0 件)) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 時刻は ISO だけ(io.py 133 行 datetime.fromisoformat)。秒・ミリ・マイクロ・ナノの口と時間帯の引数なし。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V2 の場面が全部「結果なし」(adapter の理由: slippage の読み口(io.load_bars・load_orders)は列が固定(足は symbol,timestamp,open,high,low,close,volume。io.py:44)で、場面のファイルの列(symbol の列が無い・名前が違う)を渡す引数が無い。約定を読む口は無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「秒・ミリ・マイクロ・ナノ・ISO の同じ瞬間を同じ UTC のナノ秒に」の口が無い: 日足の Date 列だけ(daily_bar_csv.py 35 行以下)。日中の時刻・単位・時間帯の口なし。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V2 の場面が全部「結果なし」(adapter の理由: QSTrader の読み口 CSVDailyBarDataSource は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。列の対応・日中の時刻・単位・時間帯の引数が無い) |

### 観点 V3 重複・逆行・欠落・世代間の食い違いの検出

動かせた候補: 1 件(54 finmarketpy)

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複は結合のときに黙って落とす(sysdata/futures/futures_per_contract_prices.py 381 行 index.duplicated(keep="first"))。報せる口なし。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V3 の場面が全部「結果なし」(adapter の理由: PySystemtrade の CSV の読み口(csvFuturesContractPriceData)は <銘柄>_<限月>.csv の先物の足を 1 列の日付の索引で読む。約定・気配の行、時刻の単位・時間帯、date と time の 2 列の口が無い。重複は結合のときに黙って落とす(futures_per_co) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V3 の場面が全部「結果なし」(adapter の理由: pybotters にファイルを読む口が無い(取引所への HTTP / WebSocket の通信と、受けた通知を貯める DataStore だけ。read_csv・csv.reader の当たりは 0 件)。時刻の文字列は変換せずそのまま持つ(SCAN 2788 行)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V3 の場面が全部「結果なし」(adapter の理由: VnPy(本体と vnpy_ctastrategy)にファイルを読む口が無い(read_csv・csv.reader の当たりは 0 件。CSV の取り込みは別の部品 vnpy_datamanager で、調査報告に無く導入していない)) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 読み口が無い(V1 の行)。data の重複の当たりは列の重複(data/cache.py 923 行)だけ。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V3 の場面が全部「結果なし」(adapter の理由: Qlib の配布物に CSV の市場データを読む口が無い(CSV から Qlib の形に直す scripts/dump_bin.py はリポジトリの側にあり配布物に入っていない。配布物の read_csv の当たりは銘柄の一覧・暦・注文の表・予測の表だけ)) |
| 23 hftbacktest | 事象の順の検査 validate_event_order(exch_ts が戻れば ValueError。報告は無い)(<venvs>/item_0/hftbacktest/…/hftbacktest/data/validation.py 139-152 行) | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/validation.py 139-152 行) | 再現した | 道具の読み口がこの場面の形を取れない(取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし)ため、検査の機構だけを一次資料(導入済みのコード)どおりに書き直し、opponents/repro_23_event_order.py として全場面に通した(survey_results/opp_repro_23.tsv)。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V3 の場面が全部「結果なし」(adapter の理由: hftbacktest のファイルの読み口は取引所・業者ごとに形が固定の変換器(tardis.convert は Tardis の trades / incremental_book_L2 の CSV)で、列の対応・時刻の単位・時間帯の引数が無い。検査(data/validation.py の correct_even) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 索引が増えているかを見るだけ(backtesting.py 1252 行 is_monotonic_increasing)。重複・欠落・世代を報せる口なし。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V3 の場面が全部「結果なし」(adapter の理由: backtesting.py に市場データのファイルを読む口が無い(Backtest はメモリ上の OHLC の DataFrame を取る。read_csv の当たりは同梱の例のデータを読む test/__init__.py だけ)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V3 の場面が全部「結果なし」(adapter の理由: barter にファイルの市場データを読む口が無い(エンジンは型つきの MarketEvent の列を受ける。barter-data の正規化は取引所の WebSocket の通知に掛かる。grep で csv::Reader・ReaderBuilder の当たりは 0 件)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V3 の場面が全部「結果なし」(adapter の理由: vectorbt に市場データのファイルを読む口が無い(grep -rn 'read_csv/class CSVData' vectorbt/ は 0 件。data の型は Yahoo・Binance・CCXT・Alpaca からの取得と合成だけ)) |
| 74 ml-quant-trading | 計算の本体は行列の演算だけ(SCAN 5885 行)。重複・逆行・欠落・世代の検査の記載なし | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 計算の本体は行列の演算だけ(SCAN 5885 行)。重複・逆行・欠落・世代の検査の記載なし。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V3 の場面が全部「結果なし」(adapter の理由: PyTrendFollow に任意の CSV の市場データを読む口が無い(データは自前の取得器から自前の保管へ。grep で read_csv・csv.reader の当たりは 0 件)) |
| 105 DaniyalMlk/slippage | 足の系列 BarSeries(時刻で並べ替え、同じ時刻の足があれば ValidationError。報告は無い)(<venvs>/item_0/c105/…/slippage/series.py 35-41 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/series.py 35-41 行) | 再現した | 道具の読み口がこの場面の形を取れない(読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行))ため、検査の機構だけを一次資料(導入済みのコード)どおりに書き直し、opponents/repro_105_duplicate_bars.py として全場面に通した(survey_results/opp_repro_105.tsv)。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V3 の場面が全部「結果なし」(adapter の理由: slippage の読み口(io.load_bars・load_orders)は列が固定(足は symbol,timestamp,open,high,low,close,volume。io.py:44)で、場面のファイルの列(symbol の列が無い・名前が違う)を渡す引数が無い。約定を読む口は無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「仕込んだ異常を種類と時刻で報せる」口が無い: 重複・逆行・欠落・世代の食い違いを報せる口が無い(読み口の行は V1 の行)。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V3 の場面が全部「結果なし」(adapter の理由: QSTrader の読み口 CSVDailyBarDataSource は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。列の対応・日中の時刻・単位・時間帯の引数が無い) |

### 観点 V4 sha256 の記録

動かせた候補: 0 件

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V4 の場面が全部「結果なし」(adapter の理由: PySystemtrade の CSV の読み口(csvFuturesContractPriceData)は <銘柄>_<限月>.csv の先物の足を 1 列の日付の索引で読む。約定・気配の行、時刻の単位・時間帯、date と time の 2 列の口が無い。重複は結合のときに黙って落とす(futures_per_co) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: hashlib の当たりは取引所への署名(pybotters/helpers/hyperliquid.py 39・281 行)だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V4 の場面が全部「結果なし」(adapter の理由: pybotters にファイルを読む口が無い(取引所への HTTP / WebSocket の通信と、受けた通知を貯める DataStore だけ。read_csv・csv.reader の当たりは 0 件)。時刻の文字列は変換せずそのまま持つ(SCAN 2788 行)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V4 の場面が全部「結果なし」(adapter の理由: VnPy(本体と vnpy_ctastrategy)にファイルを読む口が無い(read_csv・csv.reader の当たりは 0 件。CSV の取り込みは別の部品 vnpy_datamanager で、調査報告に無く導入していない)) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: hashlib の当たりは文字列の md5(キャッシュの名前、qlib/utils/__init__.py 274 行)だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V4 の場面が全部「結果なし」(adapter の理由: Qlib の配布物に CSV の市場データを読む口が無い(CSV から Qlib の形に直す scripts/dump_bin.py はリポジトリの側にあり配布物に入っていない。配布物の read_csv の当たりは銘柄の一覧・暦・注文の表・予測の表だけ)) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V4 の場面が全部「結果なし」(adapter の理由: hftbacktest のファイルの読み口は取引所・業者ごとに形が固定の変換器(tardis.convert は Tardis の trades / incremental_book_L2 の CSV)で、列の対応・時刻の単位・時間帯の引数が無い。検査(data/validation.py の correct_even) |
| 54 finmarketpy | findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行) | 実装のコードで確かめた(<venvs>/item_0/finmarketpy/…/findatapy/market/ioengine.py 1080・1108-1130 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_finmarketpy` の出力 survey_results/opp_finmarketpy.tsv の V4 の場面が全部「結果なし」(adapter の理由: finmarketpy / findatapy に読んだファイルの sha256 を記録する口が無い) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V4 の場面が全部「結果なし」(adapter の理由: backtesting.py に市場データのファイルを読む口が無い(Backtest はメモリ上の OHLC の DataFrame を取る。read_csv の当たりは同梱の例のデータを読む test/__init__.py だけ)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: sha の当たりは署名つきの要求の例(barter-integration/examples/signed_get_request.rs)だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V4 の場面が全部「結果なし」(adapter の理由: barter にファイルの市場データを読む口が無い(エンジンは型つきの MarketEvent の列を受ける。barter-data の正規化は取引所の WebSocket の通知に掛かる。grep で csv::Reader・ReaderBuilder の当たりは 0 件)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V4 の場面が全部「結果なし」(adapter の理由: vectorbt に市場データのファイルを読む口が無い(grep -rn 'read_csv/class CSVData' vectorbt/ は 0 件。data の型は Yahoo・Binance・CCXT・Alpaca からの取得と合成だけ)) |
| 74 ml-quant-trading | 書き写し(SCAN 5885・5886・5888 行)に読んだデータのハッシュを記録する記載なし | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 書き写し(SCAN 5885・5886・5888 行)に読んだデータのハッシュを記録する記載なし。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V4 の場面が全部「結果なし」(adapter の理由: PyTrendFollow に任意の CSV の市場データを読む口が無い(データは自前の取得器から自前の保管へ。grep で read_csv・csv.reader の当たりは 0 件)) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V4 の場面が全部「結果なし」(adapter の理由: slippage の読み口(io.load_bars・load_orders)は列が固定(足は symbol,timestamp,open,high,low,close,volume。io.py:44)で、場面のファイルの列(symbol の列が無い・名前が違う)を渡す引数が無い。約定を読む口は無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「読んだファイルごとの sha256 を実行の記録に残す」口が無い: grep -rlnE 'sha256/hashlib' の当たり 0 ファイル。読み口は V1 の行のとおり。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V4 の場面が全部「結果なし」(adapter の理由: QSTrader の読み口 CSVDailyBarDataSource は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。列の対応・日中の時刻・単位・時間帯の引数が無い) |

### 観点 V5 合成・中間物・封印区間を拒む許可一覧

動かせた候補: 0 件

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: 読み口は datapath の下を名前の規則で読む(csv_futures_contract_prices.py 27 行以下)。読み口は V1 の行のとおり。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V5 の場面が全部「結果なし」(adapter の理由: PySystemtrade の CSV の読み口(csvFuturesContractPriceData)は <銘柄>_<限月>.csv の先物の足を 1 列の日付の索引で読む。約定・気配の行、時刻の単位・時間帯、date と time の 2 列の口が無い。重複は結合のときに黙って落とす(futures_per_co) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V5 の場面が全部「結果なし」(adapter の理由: pybotters にファイルを読む口が無い(取引所への HTTP / WebSocket の通信と、受けた通知を貯める DataStore だけ。read_csv・csv.reader の当たりは 0 件)。時刻の文字列は変換せずそのまま持つ(SCAN 2788 行)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V5 の場面が全部「結果なし」(adapter の理由: VnPy(本体と vnpy_ctastrategy)にファイルを読む口が無い(read_csv・csv.reader の当たりは 0 件。CSV の取り込みは別の部品 vnpy_datamanager で、調査報告に無く導入していない)) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V5 の場面が全部「結果なし」(adapter の理由: Qlib の配布物に CSV の市場データを読む口が無い(CSV から Qlib の形に直す scripts/dump_bin.py はリポジトリの側にあり配布物に入っていない。配布物の read_csv の当たりは銘柄の一覧・暦・注文の表・予測の表だけ)) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V5 の場面が全部「結果なし」(adapter の理由: hftbacktest のファイルの読み口は取引所・業者ごとに形が固定の変換器(tardis.convert は Tardis の trades / incremental_book_L2 の CSV)で、列の対応・時刻の単位・時間帯の引数が無い。検査(data/validation.py の correct_even) |
| 54 finmarketpy | findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行) | 実装のコードで確かめた(<venvs>/item_0/finmarketpy/…/findatapy/market/ioengine.py 1080・1108-1130 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_finmarketpy` の出力 survey_results/opp_finmarketpy.tsv の V5 の場面が全部「結果なし」(adapter の理由: read_csv_data_frame は全列を float32 にするので、文字列の列(売買の向き)を持つ約定の形を読めない) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V5 の場面が全部「結果なし」(adapter の理由: backtesting.py に市場データのファイルを読む口が無い(Backtest はメモリ上の OHLC の DataFrame を取る。read_csv の当たりは同梱の例のデータを読む test/__init__.py だけ)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V5 の場面が全部「結果なし」(adapter の理由: barter にファイルの市場データを読む口が無い(エンジンは型つきの MarketEvent の列を受ける。barter-data の正規化は取引所の WebSocket の通知に掛かる。grep で csv::Reader・ReaderBuilder の当たりは 0 件)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V5 の場面が全部「結果なし」(adapter の理由: vectorbt に市場データのファイルを読む口が無い(grep -rn 'read_csv/class CSVData' vectorbt/ は 0 件。data の型は Yahoo・Binance・CCXT・Alpaca からの取得と合成だけ)) |
| 74 ml-quant-trading | 書き写し(SCAN 5885・5886 行)にデータのパスを拒む許可一覧の記載なし | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 書き写し(SCAN 5885・5886 行)にデータのパスを拒む許可一覧の記載なし。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V5 の場面が全部「結果なし」(adapter の理由: PyTrendFollow に任意の CSV の市場データを読む口が無い(データは自前の取得器から自前の保管へ。grep で read_csv・csv.reader の当たりは 0 件)) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: パスを拒む語(allowlist・whitelist・qa_・o3c・phase2・sealed)の当たりは無関係の箇所だけ。読み口は V1 の行のとおり。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V5 の場面が全部「結果なし」(adapter の理由: slippage の読み口(io.load_bars・load_orders)は列が固定(足は symbol,timestamp,open,high,low,close,volume。io.py:44)で、場面のファイルの列(symbol の列が無い・名前が違う)を渡す引数が無い。約定を読む口は無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「合成・中間物・封印区間のパスを構造で拒む」口が無い: 読み口は csv_dir の下の CSV を全部読む(daily_bar_csv.py 44-54 行 os.listdir)。読み口は V1 の行のとおり。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V5 の場面が全部「結果なし」(adapter の理由: QSTrader の読み口 CSVDailyBarDataSource は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。列の対応・日中の時刻・単位・時間帯の引数が無い) |

### 観点 V6 JPX 個別株の分割・併合・上場廃止・コード変更の調整と、生存者の偏りを避ける銘柄集合

動かせた候補: 1 件(21 Qlib)

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 先物の道具(SCAN 3168〜3254 行「同梱の先物の価格データが 252 銘柄」)で、個別株の分割・併合・上場廃止・コード変更の当たり 0 件。扱う入力は 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応)。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V6 の場面が全部「結果なし」(adapter の理由: PySystemtrade は先物の道具で、個別株の分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行)。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V6 の場面が全部「結果なし」(adapter の理由: pybotters に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V6 の場面が全部「結果なし」(adapter の理由: VnPy に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V6 の場面が全部「結果なし」(adapter の理由: hftbacktest に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い(ティックと板の模擬器)) |
| 54 finmarketpy | findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行) | 実装のコードで確かめた(<venvs>/item_0/finmarketpy/…/findatapy/market/ioengine.py 1080・1108-1130 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行)。`run_battery.py --target opp_finmarketpy` の出力 survey_results/opp_finmarketpy.tsv の V6 の場面が全部「結果なし」(adapter の理由: finmarketpy / findatapy に分割・併合の事象から値段を調整する口と、日付ごとの銘柄集合の口が無い) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ)。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V6 の場面が全部「結果なし」(adapter の理由: backtesting.py に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い(1 銘柄の OHLC だけを取る)) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行)。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V6 の場面が全部「結果なし」(adapter の理由: barter に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い(暗号資産の道具)) |
| 73 vectorbt | 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData) | 実装のコードで確かめた(<venvs>/item_1/vectorbt/…/vectorbt/data/custom.py) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 調整の種類を渡す引数は Alpaca からの取得(data/custom.py 838 行、鍵が要る)で、取得先が調整する。分割の事象から自前で調整する口・日付ごとの銘柄集合の口なし。扱う入力は 市場データのファイルの読み口なし(grep read_csv・class CSVData 0 件。data の型は合成と取得だけ: data/custom.py 29 行 SyntheticData・183 行 YFData・301 行 BinanceData・556 行 CCXTData・759 行 AlpacaData)。`run_battery.py --target opp_vectorbt` の出力 survey_results/opp_vectorbt.tsv の V6 の場面が全部「結果なし」(adapter の理由: vectorbt に分割・併合・コード変更の調整と、日付ごとの銘柄集合の口が無い(grep -rni 'split/delist/universe' の当たりはデータの分け方と列の名前だけ)) |
| 74 ml-quant-trading | 生存者の偏りなどの前提は文書に列挙するだけで実装していない(SCAN 5888 行「足しなさい」の一覧、5944 行「前提を報告の必須項目として一覧にしている運用」) | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 生存者の偏りなどの前提は文書に列挙するだけで実装していない(SCAN 5888 行「足しなさい」の一覧、5944 行「前提を報告の必須項目として一覧にしている運用」)。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 先物の道具(config/instruments.py 24 行 point_value「on the futures contract」、config/spots.py 3 行)で、個別株の分割・併合・上場廃止・コード変更の当たり 0 件。扱う入力は 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行)。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V6 の場面が全部「結果なし」(adapter の理由: PyTrendFollow は先物の連続足を扱い、個別株の分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 分割・併合・上場廃止・コード変更・生存者の当たり 0 件(grep -rniE の語 delist・survivor・corporate action・stock split・reverse split)。扱う入力は 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行)。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V6 の場面が全部「結果なし」(adapter の理由: slippage に分割・併合・コード変更の調整と日付ごとの銘柄集合の口が無い(執行費用の分析の道具)) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「分割・併合・コード変更の調整と、日付ごとの上場銘柄の集合」の口が無い: 調整はファイルの Adj Close の比を掛けるだけ(daily_bar_csv.py 134-161 行)。DynamicUniverse は入る日だけで除く口が無い(asset/universe/dynamic.py 9-10 行「does not currently support removal of assets」)。コード変更の口なし。扱う入力は CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume)。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V6 の場面が全部「結果なし」(adapter の理由: QSTrader の調整はファイルの Adj Close の比を掛けるだけで分割・併合の事象から計算しない。DynamicUniverse は銘柄ごとの入る日だけを持ち、上場廃止(除く日)を渡す口が無い(dynamic.py の docstring 9-10 行「does not currently support r) |

### 観点 V7 足のベクトル化の近道(同じ足で事象駆動と一致)

動かせた候補: 1 件(73 vectorbt)

| 候補 | 機構 | 実装 | 判断 | 理由 |
|---|---|---|---|---|
| 3 PySystemtrade | 先物の足の CSV の読み口 csvFuturesContractPriceData + ConfigCsvFuturesPrices(日付の索引 1 列・日付の書式・列の対応) | 実装のコードで確かめた(<venvs>/item_0/_dl/c3/sysdata/csv/csv_futures_contract_prices.py 17-27 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 模擬は pandas のベクトル化の 1 経路(SCAN 9401 行の節)。`run_battery.py --target opp_pysystemtrade` の出力 survey_results/opp_pysystemtrade.tsv の V7 の場面が全部「結果なし」(adapter の理由: PySystemtrade の模擬は pandas のベクトル化の 1 経路だけで、突き合わせる事象駆動の経路が無い) |
| 12 pybotters | HTTP / WebSocket の通信と DataStore だけ。ファイルの読み口なし(grep read_csv・csv.reader 0 件)。時刻の文字列は変換せずに持つ(SCAN 2788 行) | 実装のコードで確かめた(<venvs>/item_0/pybotters/…/pybotters) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 模擬の経路が無い(通信の道具。SCAN 6871 行の節)。`run_battery.py --target opp_pybotters` の出力 survey_results/opp_pybotters.tsv の V7 の場面が全部「結果なし」(adapter の理由: pybotters に模擬の経路が無い(通信の道具)) |
| 20 VnPy | 本体と vnpy_ctastrategy にファイルの読み口なし(grep read_csv・csv.reader 0 件)。足は BarGenerator.update_tick(vnpy/trader/utility.py 166・204 行)の 1 本道 | 実装のコードで確かめた(<venvs>/item_0/vnpy/…/vnpy/trader/utility.py 166・204 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 足づくり BarGenerator.update_tick(utility.py 204 行)と CTA の検証は 1 本ずつの 1 経路。`run_battery.py --target opp_vnpy` の出力 survey_results/opp_vnpy.tsv の V7 の場面が全部「結果なし」(adapter の理由: VnPy の足づくり(BarGenerator.update_tick)と CTA の検証は 1 本ずつの事象駆動の経路だけで、突き合わせる近道(ベクトル化)の経路が無い) |
| 21 Qlib | 配布物に CSV の読み口なし(read_csv の当たりは銘柄の一覧 file_storage.py 208 行・暦 utils/__init__.py 602 行・注文・予測の表)。CSV から自前の形に直す dump_bin.py はリポジトリの scripts/ にあり配布物に無い | 実装のコードで確かめた(<venvs>/item_1/qlib/…/qlib/data/storage/file_storage.py 208 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 検証は qlib.backtest の 1 経路(qlib/backtest/__init__.py 217 行 backtest)。`run_battery.py --target opp_qlib` の出力 survey_results/opp_qlib.tsv の V7 の場面が全部「結果なし」(adapter の理由: Qlib に同じ足を事象駆動と近道の 2 つの経路で回して突き合わせる口が無い(検証は qlib.backtest の 1 経路)) |
| 23 hftbacktest | 取引所・業者ごとに形が固定の変換器(data/utils/tardis.py 56 行 convert は Tardis の trades / incremental_book_L2 の CSV)。列の対応・単位・時間帯の引数なし | 実装のコードで確かめた(<venvs>/item_0/hftbacktest/…/hftbacktest/data/utils/tardis.py 56 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: ティックごとの事象駆動の 1 経路(SCAN 6906 行の節)。`run_battery.py --target opp_hftbacktest` の出力 survey_results/opp_hftbacktest.tsv の V7 の場面が全部「結果なし」(adapter の理由: hftbacktest の模擬はティックごとの事象駆動の 1 経路だけで、近道(ベクトル化)の経路も約定から足を作る口も無い) |
| 54 finmarketpy | findatapy の IOEngine.read_csv_data_frame(ioengine.py 1080 行。最初の列を時刻の索引、ほかの列を float32 = 1130 行、時刻の読み方は日付の文字列の型だけ = 1108-1121 行) | 実装のコードで確かめた(<venvs>/item_0/finmarketpy/…/findatapy/market/ioengine.py 1080・1108-1130 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 信号の表と値の表の演算の 1 経路(SCAN 7851 行「区分1-イベント駆動 は付けない」)。`run_battery.py --target opp_finmarketpy` の出力 survey_results/opp_finmarketpy.tsv の V7 の場面が全部「結果なし」(adapter の理由: finmarketpy の検証はベクトル化の 1 経路(信号の表と値の表の演算)だけで、突き合わせる事象駆動の経路が無い) |
| 55 backtesting.py | 読み口なし。Backtest(backtesting.py 1111 行)はメモリ上の OHLC の DataFrame を取る(read_csv の当たりは同梱の例の test/__init__.py だけ) | 実装のコードで確かめた(<venvs>/item_0/backtesting/…/backtesting/backtesting.py 1111 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: Strategy.next を足ごとに呼ぶ 1 経路(backtesting.py 1271 行 run)。`run_battery.py --target opp_backtesting` の出力 survey_results/opp_backtesting.tsv の V7 の場面が全部「結果なし」(adapter の理由: backtesting.py の模擬は Strategy.next を足ごとに呼ぶ 1 経路だけで、突き合わせる近道(ベクトル化)の経路が無い) |
| 61 barter-rs | ファイルの読み口なし(grep csv::Reader・ReaderBuilder 0 件)。型つきの MarketEvent の列を受ける。Candle は close_time: DateTime<Utc>(SCAN 6303 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c61) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 事象駆動の 1 経路(SCAN 6926 行の節、型つきの MarketEvent の列を受ける)。`run_battery.py --target opp_barter` の出力 survey_results/opp_barter.tsv の V7 の場面が全部「結果なし」(adapter の理由: barter の模擬は事象駆動の 1 経路だけで、近道(ベクトル化)の経路が無い) |
| 74 ml-quant-trading | 「Vectorised backtest engine.」で時間の繰り返しが 1 つも無い(SCAN 5885 行)= 突き合わせる事象駆動の経路が無い | (a) の書き写しだけ(道具台帳 §3 の 11 件。導入も一次資料の読みもしない) | 持たないと確認した | 「Vectorised backtest engine.」で時間の繰り返しが 1 つも無い(SCAN 5885 行)= 突き合わせる事象駆動の経路が無い。導入も実行もしない(委任文 §4) |
| 87 PyTrendFollow | 任意の CSV の読み口なし(grep read_csv・DictReader・csv.reader 0 件。データは IB と Quandl からの取得を自前で保管: config/currencies.py 9-12 行) | 実装のコードで確かめた(<venvs>/item_0/_dl/c87) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: accountCurve(trading/accountcurve.py 12 行)のベクトル化の 1 経路。`run_battery.py --target opp_pytrendfollow` の出力 survey_results/opp_pytrendfollow.tsv の V7 の場面が全部「結果なし」(adapter の理由: PyTrendFollow の損益の計算(accountCurve)はベクトル化の 1 経路だけで、突き合わせる事象駆動の経路が無い) |
| 105 DaniyalMlk/slippage | 読み口の列が固定(足 = symbol,timestamp,open,high,low,close,volume、io.py 44 行。欠けると InputError、57-59 行)。時刻は datetime.fromisoformat(133 行) | 実装のコードで確かめた(<venvs>/item_0/c105/…/slippage/io.py 44・57-59・126-149 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 足の系列から執行費用を出す道具で、模擬の 2 経路が無い。`run_battery.py --target opp_slippage` の出力 survey_results/opp_slippage.tsv の V7 の場面が全部「結果なし」(adapter の理由: slippage に同じ足を 2 つの経路で回して突き合わせる口が無い) |
| 121 mhallsmoore/qstrader | CSVDailyBarDataSource(qstrader/data/daily_bar_csv.py 35・44-54 行)は <銘柄>.csv の日足で列が固定(Date, Open, High, Low, Close, Adj Close, Volume) | 実装のコードで確かめた(<venvs>/item_0/qstrader/…/qstrader/data/daily_bar_csv.py 35・44-54 行) | 持たないと確認した | 場面が測る「同じ足を事象駆動と近道の 2 経路で回して一致させる」口が無い: 日程で駆動する 1 経路(qstrader/trading/backtest.py 25 行 BacktestTradingSession・368 行 run、simulation/daily_bday.py 11 行)。`run_battery.py --target opp_qstrader` の出力 survey_results/opp_qstrader.tsv の V7 の場面が全部「結果なし」(adapter の理由: QSTrader の模擬は日程で駆動する 1 経路だけで、近道(ベクトル化)の経路が無い) |

<!-- 集計ここから -->

## 集計(`scripts/check_bt_considered.py --write` が書く。手で直さない)

| 観点 | 動かせた | 動かせない(検討表の行) | 動かせない割合 | 再現した | 持たないと確認した | スキップ | 再現できない |
|---|---|---|---|---|---|---|---|
| 観点 V1 全資産の読み込み | 1 | 12 | 92% | 0 | 12 | 0 | 0 |
| 観点 V2 時刻の単位の正規化 | 0 | 13 | 100% | 0 | 13 | 0 | 0 |
| 観点 V3 重複・逆行・欠落・世代間の食い違いの検出 | 1 | 12 | 92% | 2 | 10 | 0 | 0 |
| 観点 V4 sha256 の記録 | 0 | 13 | 100% | 0 | 13 | 0 | 0 |
| 観点 V5 合成・中間物・封印区間を拒む許可一覧 | 0 | 13 | 100% | 0 | 13 | 0 | 0 |
| 観点 V6 JPX 個別株の分割・併合・上場廃止・コード変更の調整と、生存者の偏りを避ける銘柄集合 | 1 | 12 | 92% | 0 | 12 | 0 | 0 |
| 観点 V7 足のベクトル化の近道(同じ足で事象駆動と一致) | 1 | 12 | 92% | 0 | 12 | 0 | 0 |
| 計(観点ごとの延べ) | 4 | 87 | 96% | 2 | 85 | 0 | 0 |

再現もできなかった候補(圧倒の判定では「その候補とは比べていない」と候補名を付けて報告する):

- なし

<!-- 集計ここまで -->
