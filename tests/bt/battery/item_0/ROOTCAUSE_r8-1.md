# 場面集への指摘ごとの根本原因と正の定義(項目 0、第 r8-1 回の直しの前の定義、3 回目、場面係、2026-09-24)

委任文: `docs/DATA/delegations/20260923_backtest_env_prompt.md`。起動文の指紋は `9ef8d697ad0a`。作業木の委任文の指紋・ファイルの行の数と、読んだ範囲(4 本の `sed` で先頭から末尾まで)は `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_readlog.txt`。作業木の HEAD は `acc6d26`(版の名指し)。このファイルがコードの振る舞いと文書の中身について述べることは、全部この版を読んだものである。
合意した完了の形(委任文 §0 の逐語): 「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」
当てた決まり(逐語は委任文): §3「場面集」の「直す前に指摘ごとの根本原因と、その指摘の族に対する**正の定義**(何を対象自身の振る舞いと数え、何を数えないか。場所や形の一覧ではなく、どの形にも当たる 1 段落の規則)を書き、**その定義だけを監査役に先に通してから**コードを直す」、§3「周回の数え方と止める条件」4、§3「動かせない候補の検討と再現」、§3「要件と判定の固定」、§3「提出前の吟味」(最初に作る役の文)、リードの設計 `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/round_7/LEAD_DESIGN.md` §2・§3・§5・§7、リードの注記(L-438 の条件 (1)〜(4))、CLAUDE.md §0.2 O-7 と §3「数え直しは範囲を切らずに打ち…」。

この回の入力: 直しの前の定義の 2 回目の版(このファイルの 2 回目の版)への監査役(定義)の指摘 dr8-1-2 の 4 件(逐語は起動文)= [止める] dr8-1-2-1・dr8-1-2-2・dr8-1-2-3 / [直す] dr8-1-2-4。1 回目の版は `git show 3e42e8a:tests/bt/battery/item_0/ROOTCAUSE_r8-1.md`。2 回目の版はコミットされていなかったので、書き換える前に `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_prev_version_2.md` に写した(写す前と後の sha256: `…_prev_sha.txt`)。監査役が挙げた行番号は 2 回目の版の行番号で、この版の行番号ではない。

**この段ではコードも場面集の他のファイルも直していない**(起動文「まだコードを直さない」、L-433「場当たり的な修正をするな」)。書いたのはこのファイルだけ。

**このファイル自身が正の定義 0・B・D の当てる範囲に入る。**場面の中身(正の定義 B の意味)は書かず、場面 id と `scenes.py` の `Scene` の欄名で指す。確かめに打ったコマンドの出力は `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_*`(`<scratchpad>` = `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad`)に置き、ここでは path で指す(出力の値を文に写さない)。

## 用語の表(正の定義 D をこのファイルに当てたもの。1 つの語に 1 つの意味)

語の範囲(正の定義 D)の中の別の意味は、範囲を切らずに引いた出力 `…_term_tan-i.txt`・`…_term_senses_old.txt`・`…_term_newname.txt`・`…_term_newname2.txt`・`…_term_new_allverdicts.txt` にある(最後のものが範囲の文書に項目 0 の VERDICTS の全部を入れた版。それより前のものは VERDICTS を run7 だけ入れていた)。

| 語 | このファイルでの意味 | 範囲の中の別の意味と、このファイルでの避け方 |
|---|---|---|
| 対象 | 場面集に通して比べるエンジン(新実装・当方の現状・調査結果の側の各道具・再現・試金石)。委任文 §3「行 = 対象」の意味 | 2 回目の版は正の定義の当てる先にも使った。当てる先は「当てる範囲」と書く |
| 設定つき対象 | 対象 × 設定の値(正の定義 A)。範囲の中の出現 0 件を確かめて新しく名付けた | 2 回目の版の「走らせる単位」を廃した |
| 設定 / 設定の操作 / 設定の値 | 正の定義 A (1) の意味 | 2 回目の版の「設定の操作の列」の「列」は使わない(「列」は試験の列と要件の表の N 列目に使われる) |
| 候補 | 道具台帳の候補(委任文の意味) | 対象 × 設定の値には使わない |
| 時刻の単位 | 要件 P0-2 の語(秒・ミリ・ISO 文字列の類) | 範囲の中で「単位」は数量・研究の区切り(CLAUDE.md)にも使われる。このファイルでは裸の「単位」を使わない |
| 事象の型 / 型の組 | 要件 §1 の事象の型 / 1 回の実行に入れる事象の型の組み合わせ(L-438 の語) | Python の型は「class」と書く。要件 P0-4 の「型エラー」は要件の逐語の複合語としてだけ引く |
| 見る道 | 正の定義 C の軸の 1 つ | ほかの意味で裸の「道」を使わない(LEAN の購読から `OnData` までのコードは「購読から `OnData` までのコード」と書く。正の定義 A (3) は「届き方」と書く) |
| 升目 | 軸の値を 1 つずつ選んだ組み合わせ 1 つ(正の定義 A の試験の格子、正の定義 C の観点の範囲)。範囲の中の出現 0 件を確かめて使った | 2 回目の版は正の定義 C で「組」と書いた。「組」は型の組と盲検の組に使われる |
| N 行・ファイルの行 / 一次資料の行 / 表の行 | ファイルの行(番号つきか「ファイルの行」)/ 一次資料のコードの行 / 比較の表の行 | 裸の「行」を使わない。「実行」は別の語 |
| 元 | 正の定義 B の意味 | ほかの意味で使わない |
| 場面の定義 / 正の定義 / 直しの前の定義 | `scenes.py` の `Scene` の値 / このファイルの定義 0・A・B・C・D / 台本の役の段の名(起動文の語。「1 回目」「2 回目」はその段の回) | 裸の「定義」を使わない。「監査役(定義)」は役の名。動詞「定義する」は「意味を決めて書く」 |
| runner の出力 | `survey_results/` の中身 | 2 回目の版の「今の記録」を替えた(「実行記録」は委任文で項目 8 の語) |
| 範囲 / 当てる範囲 | 何かを当てる・覆う・比べる先の全部の集まり(当てる範囲・観点の範囲・語の範囲) / 規則・正の定義・検査・試験を当てる先の全部(正の定義 0) | 「当てる範囲」は範囲の中の出現 0 件を確かめて使った |
| 定型の文 | 試験が作り直して一致を確かめる、決まった書き方の文(正の定義 B (ii))。範囲の中の出現 0 件を確かめて使った | 2 回目の版の「決まった形の文」を替えた(「形」は委任文で「場所や形の一覧」の意味。「書式」はデータの書式に使われる) |
| 形 | 同じ欠陥が現れた具体の姿(委任文「どの形にも当たる」の意味) | ほかの意味で使わない(「完了の形」はオーナーと委任文の逐語の句としてだけ引く) |
| 行う(動詞)/ 列べる(動詞)/ 組む(動詞) | 行う / 全部を書き出す / 組み立てる。名詞の「行」「列」「組」とは別の語 | — |
| 仕事 / 届き方 / 作り手 / 受け渡し | 正の定義 A の (2) / (3) / 試験の軸 | 場面集のほかのファイルの「仕事」「受け渡し」の出現は直しの段で読む(正の定義 D の直す所) |
| 母語のコード | 対象の配布物のうち Python でないコード(C++・Rust の類)。場面集の過去の周と同じ意味 | — |
| 差し込む口 / 公開の入口 | 要件 P0-7 の口 / 利用者が呼ぶ対象の公開の関数 | 2 回目の版の「入力の口」は使わない(「データの購読」と書く) |

## 0. 族の読み(repeat_of。数えるのは台本と監査役で、場面係は読みを書くだけ)

| 指摘 | 監査役の repeat_of | 場面係の読み(族) | 読みの根拠 |
|---|---|---|---|
| i0-r7-03 | i0-r6-04 | 帰属の族(br6-1-1 → br6-2-1 → br6-3-2 の続き) | どれも「場面集の側が組んだ物を対象の振る舞いとして数えた」。値 → class の実体 → 戦略の外の呼び出し → 設定、と形が変わっただけ |
| i0-r7-04 | null | 書き写した文が元とずれる族(前例 0#5-1・i0-r3-11) | どれも「元から手で書き写した文が、元が変わっても照らされない」 |
| i0-r7-05 | null | 観点の範囲を要件の名詞から列べていない族(前例 i0-r3-10・i0-r3-03) | どれも「観点の範囲を、書ける物と思いついた物から作った」 |
| dr8-1-1-1 | null | 書き写した文の族 | 正の定義 B の当てる範囲を例で書き、正の定義 B を書いたファイル自身が外れた |
| dr8-1-1-2 | null | 観点の範囲の族 | 正の定義 C を一部の観点にだけ当てた |
| dr8-1-1-3 | null | 帰属の族 | 「設定」を手元の例の時点で書いた |
| dr8-1-1-4 | null | 帰属の族 | 表の行と対象の区切りを書かなかった |
| dr8-1-2-1 | dr8-1-1-1 | 書き写した文の族(監査役の読みと同じ) | 場面の中身を語の一覧で定義し、場面の集まりについての文(ある欄を持つ場面の数)が外れた。自己検査の範囲も同じ語の一覧から作った |
| dr8-1-2-2 | null | 語の使い回しの族(O-7、既往 I-004。項目 0 の場面集では初出。委任文の監査に同じ形の前例 = `docs/AUDITOR/VERDICTS/2026-09-23_backtest_env_prompt.md` 448-456 行「語を 1 か所だけ言い換え、同じ語を使う他の箇所を見なかった」) | 1 つの語を言い換え、新しい語の範囲の中の出現を見なかった |
| dr8-1-2-3 | i0-r7-03 | 帰属の族(監査役の読みと同じ) | 正の定義 A が認めた由来が、それを確かめる試験の軸から漏れた |
| dr8-1-2-4 | null | 監査役の読みは null。場面係の読みでは、dr8-1-1-2 と同じ「方法を一部にだけ当てた」形 | 「直す所」の小見出しを正の定義 A・B にだけ置き、C に置かなかった |

## 1. 2 回目と 3 回目の 8 件に共通の根本原因

**当てる範囲を、書き出した全部ではなく、手元の例・思い出した物・探す語の一覧から作った。**2 回目の版は、1 回目の 4 件の共通原因を「正の定義を 4 つの物((a) このファイル (b) 7 観点 (c) 実行の全部の時点 (d) 表の行の全部)に当てていなかった」と読み、その 4 つにだけ当てた。**この直し自体が「当てる先の一覧」**で、一覧の外に 4 件が残った: 場面の中身を語の一覧で決め、自己検査もその語で走査した(dr8-1-2-1)/ 言い換えた語だけを O-7 に照らし、新しい語と他の語を照らさなかった(dr8-1-2-2)/ 試験の軸を思い出した作り手で作った(dr8-1-2-3)/ 小見出しを思い出した正の定義にだけ置いた(dr8-1-2-4)。LEAD_DESIGN.md §2「扱う場合の一覧を書き、一覧の外を批評家が突く」と同じ形が、正の定義を書く段と、その直しを書く段で 2 回続いた。よって今回は、当てる範囲そのものの規則を正の定義 0 として書き、正の定義 A〜D はこれに従う。

## 2. 正の定義 0: 当てる範囲(dr8-1-2-1・dr8-1-2-3・dr8-1-2-4 と dr8-1-1-1・dr8-1-1-2 に共通)

### 確かめたこと

- 2 回目の版の自己検査は、場面 id の正規表現と、語の一覧(事象の型の名前・「件」「期待」)の 2 本の grep で走査していた。一覧に「場面」「型」「type_plan」は無い(`<scratchpad>/bt/r8-1/item0_r8-1-2_scenekeeper_def_selfcheck.txt`)。
- 2 回目の版の正の定義 A〜C の節の小見出しは、正の定義ごとに違っていた(C に「直す所」が無い。2 回目の版の見出しの書き出し: `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_prev_headings.txt`)。

### なぜ起きたか

§1 のとおり。加えて、「全部に当てた」と書く前に、書き出した数と当てた数を並べる手順が無かった。2 回目の版の §5 の「1 行ずつ読んだ」は、走査したファイルの行の数を示す出力を持たない申告だった。

### 前の回で外した根本原因

無し。正の定義 0 はこの回に新しく書いた(1 回目・2 回目の版に当たる正の定義は無い)。1 回目・2 回目の版がこの規則を持たなかったことは、上の「なぜ起きたか」と §1 に書いた。

### 正の定義 0(監査役に先に通す段落)

**当てる範囲**: 場面係が規則・正の定義・検査・試験を当てる範囲は、当てる先の全部を機械で書き出した物である(ファイルなら全行、文書なら全部の節とその小見出し、正の定義なら全部の選択肢と全部の「数えない物」に「どれにも当たらない物」を足した物、用語なら文書が意味を持たせた全部の語の全部の出現、観点なら観点ごとの升目の全部。括弧の中は例で、例に無い当てる先も「全部を書き出す」に従う)。当てる範囲を、指摘に出た物・思い出した物・探す語の一覧・実装の場合分けから作らない。それらは書き出した物に付ける印に使ってよいが、当てる範囲を狭めない。当てた結果は書き出した物の 1 つずつに持ち(ファイルなら行ごとの判断)、書き出した数と結果の数が等しいことを同じ出力に示す。等しいことを示さない出力を根拠に「全部に当てた」「1 行ずつ読んだ」と書かない。当てる範囲を切ったときは、切ったことと理由を書き、切った外に当てたとは書かない。

リードの答えとの突き合わせ: リードの答えに無く、場面係が足した(§1 の根本原因から)。CLAUDE.md §3「数え直しは範囲を切らずに打ち…範囲を…切ったら、切ったことを結果に書く」を、数えることから「当てること」全般に広げたもの。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: §7 の自己検査で、全行の判断・用語の全部の出現・全部の節の小見出しを出力に書き出し、数を並べた。
- 正の定義 A の試験の軸(§3 の直す所)・正の定義 B の検査(§4)・正の定義 C の表(§5)・正の定義 D の出現の書き出し(§6)を、それぞれ書き出した全部から作る。
- 場面集の検査と自己検査の道具の全部: 走査の範囲が語の一覧で狭まっていないかを、道具ごとに読んで直す(範囲は `git ls-files tests/bt/battery/item_0` の中の検査と試験の全部)。

## 3. 正の定義 A: 帰属の族(i0-r7-03・dr8-1-1-3・dr8-1-1-4・dr8-1-2-3)

### 確かめたこと

- LEAN の事象の型の決め方(一次資料、版 `856327ff`、第 r7-1 回に取った写し `<scratchpad>/bt/item0_r7-1_scenekeeper_DataManager.cs`): `Add(Symbol symbol, Resolution? resolution = null, … List<Tuple<Type, TickType>> subscriptionDataTypes = null, …)`(602-613 行)、`var dataTypes = subscriptionDataTypes;`(616 行)、`dataTypes = LookupSubscriptionConfigDataTypes(symbol.SecurityType, resolution ?? _algorithm.UniverseSettings.Resolution, symbol.IsCanonical());`(626 行)、`.Select(tickType => new Tuple<Type, TickType>(LeanData.GetDataType(resolution, tickType), tickType))`(768 行)。`QCAlgorithm.cs` 3069-3083 行の `AddSecurity<T>` は `subscriptionDataTypes` を渡さない。この読みでは、1 回の `AddCryptoFuture` の約定の事象の型は解像度 1 つから決まる。`LeanData.GetDataType` の中身、同じ銘柄に解像度を変えて 2 回 `AddCryptoFuture` を呼べるか、実行の途中に公開の API で購読を足せるかは**未確認**(読んでいない)。
- 再現の組み立て方: `opponents/repro_lean52.py` の `_sources` は、購読の型の組を `Scene.input` から作り、`repro_engines/lean52.py` の `data_manager_add` に型の組を渡す(一次資料 616 行の、`subscriptionDataTypes` が null でない側)。`data_manager_add` の注釈は利用者の呼び方を 626 行の側と書き、呼び出しと食い違う。再現のコードに解像度の語があるかの grep: `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_checks.txt`。
- 型の組の決め方の確かめ(式は `run_battery.py` の `target_types` と同じ): `<scratchpad>/bt/r8-1/item0_r8-1-2_scenekeeper_def_types.py` とその出力 `…_types.out`。
- 新実装と当方の現状の adapter に、利用者が選ぶ設定の値(解像度・購読・並びの引数)があるかの grep: `…_checks.txt`。

### なぜ起きたか(i0-r7-03)

1. **再現が書き直すコードの始まりを、利用者の公開の入口ではなく「購読ができた後」に置いた。**第 r6-3 回の再現は LEAN の購読から `OnData` までのコードを書き直し、購読を作る所(`AddCryptoFuture` から `LookupSubscriptionConfigDataTypes` まで)は場面集の側が `Scene.input` から組んだ。第 r7-1 回の i0-r6-04 の直しは、再現が引いていた一次資料の行の中の条件にだけ「囲む条件も 1 対 1」を当てた。
2. **「対象の型」を、設定の違う実行の和で決める規則にしていた。**runner は実行ごとの設定を書き残しも照合もしない(`run_battery.py` の `target_types` と `run_for_types`)。その和を `type_plan` の欄を持つ場面の入力に使うと、1 つの設定では作れない型の組を求め、adapter・再現がそれを組めば対象の振る舞いとして採点される。
3. **第 r6-3 回の帰属の検め(DEFINITIONS.md「判定」の「出所の検め」の (b))が物だけを見て、設定を見ていなかった。**

### 前の回で外した根本原因

- **dr8-1-1-3**(1 回目): 「設定」を時点で書いた。設定を対象に帰す理由(利用者が公開の手段で行った操作であること)で書かなかった。
- **dr8-1-1-4**(1 回目): 表の行と対象の区切りを書かず、寄せる相手が道具の間か設定の間かを決めていなかった。
- **dr8-1-2-3**(2 回目): 正の定義 A の本文は (3) で母語のコードの由来を認めていたのに、「直す所」の試験の格子の軸は思い出した作り手 4 つで書いた。軸を正の定義 A の文から作らなかった(正の定義 0 の違反)。同じ根で、格子には (2) 仕事の軸も無く、「一次資料の行に当たらない再現の工夫」「場面集の側が呼んだ変換」も作り手に無かった。さらに 2 回目の「直す所」は「戦略の呼び出しの外で実行の途中に行った操作を持つ実行は採点しない」と書いたが、この規則は正の定義 A の本文に無い(本文が決めるのは、操作が公開の手段か・操作を決めた材料が何か)。直す所を正の定義の文から作らず記憶から書くと、正の定義に無い規則が足され、正の定義にある由来が落ちる。

### 正の定義 A(監査役に先に通す段落)

**対象の振る舞いと数える物**: 場面の結果として対象に帰すのは、次の 3 つが全部成り立つ実行で出た物だけである。**(1) 設定**: その実行の中で利用者が対象の公開の手段で行った設定の操作の全部を、行った順に並べた物であること(データの購読・事象の型の組・解像度・同時刻の並びの引数・差し込む模型と口座を作り、足し、替える操作。実行の開始前に限らず、実行の途中に公開の API で購読や事象の型を足し替える操作も入る)。場面集の側は、操作ごとに利用者のどの公開の呼び出しかを対象の配布物のコードの行(再現なら一次資料の行)で示し、いつ(開始前か、どの戦略の呼び出しの中か、ほかの時点か)行ったかを書き残す。どの操作も、実行の前に利用者が知りうる物(場面が名指す銘柄と事象の型・その設定つき対象の設定の値・対象の公開の既定)と、その操作までに戦略が (3) の届き方で受けた物だけから決まること。公開の手段では 1 つの実行の中で続けて行えない操作を寄せ集めた物は、1 つの設定ではない。**(2) 仕事**: 場面が測る仕事(事象に型を付ける・時刻を変換する・並べる・過去を読ませる・通知を作る・模型を呼ぶ)を、対象の配布物のコード(母語のコードを含む)が行ったこと。再現では、利用者の公開の入口から戦略の呼び出しまでのコードの全部を一次資料の行に 1 対 1 で書き直した物(書き直した文を囲む条件を含む。書き直さない条件は、場面で成り立たない理由を一次資料の行で示す)が行ったこと。場面集の側が行ってよいのは、場面の入力の値を対象の公開の入口が受け取る値に写すこと、公開の手段を呼ぶこと、公開の差し込む口に利用者の実装を渡すこと、返ってきた物を場面の語に写すことだけである。**(3) 届き方**: 戦略が受けた物は、戦略の 1 回の呼び出しが始まってから戻るまで(コルーチンなら最初に入ってから終わるまで)の間に、対象の配布物のコード(母語のコードを含む)が戦略に引数として渡した物かその中にあった物、または戦略がその間に対象の関数を呼んで返ってきた物であり、その物を作ったのも対象の配布物のコードであること。**数えない物**: adapter・再現の外側・再現のうち一次資料の行に当たらない工夫が作った物(`dict`・関数・対象の class の実体を含む)、公開の手段で作れない設定か操作の寄せ集めの下で出た結果、まだ届いていない入力の時刻・順・件数に合わせて決めた操作の下で出た結果(並べる仕事を場面集の側がしたことになる)、戦略の呼び出しの外で対象の関数を呼んで得た物、戦略が自分で貯めた値、場面集の側が呼んだ変換の値、そして (1)〜(3) のどれにも当たると示せない物(既定は数えない)。これらで決まった場面は採点しない(結果なし、理由を残す)。**設定つき対象と表の行**: 設定つき対象 = 対象 × 設定の値(設定の操作のうち、場面の入力が決めない部分 = 利用者が選ぶ値の組み合わせ。場面の入力が決める遅れの値・差し込むダミーでは分けない)。1 つの設定つき対象の場面は全部同じ設定の値で走らせ、設定つき対象ごとに runner の出力に別々に残し、どの対象のどの設定の値かを materials に書く。事象の型を測らない観点の場面が使う「対象の型」は、その設定つき対象の p3 の結果だけから決め、設定つき対象の違う結果の和にしない(1 つの場面の入力と結果は、1 つの設定つき対象の実行から出る)。表の行「調査結果の側」は、委任文 §3 どおり場面ごとに、走らせた設定つき対象の最良を寄せる。これは場面ごとに設定つき対象の間から選ぶことで、どのセルも 1 つの設定つき対象の実際の実行の結果であり、表の行は 1 つの道具・1 つの設定の振る舞いを名乗らない。新実装・当方の現状・試金石の表の行は、それぞれ実行の前に決めた 1 つの設定つき対象だけから作る。この正の定義は新実装・当方の現状・調査結果の側・再現のすべてに同じく当てる。**この正の定義を確かめる試験の入力の空間は、この段落の選択肢と「数えない物」の全部に「どれにも当たらない」値を足して作り(正の定義 0)、期待はこの段落の条件だけから出す。**

リードの答えとの突き合わせ: §7-4 を (1) に、§7-5 を「設定つき対象と表の行」に書いた。§7-5 の「候補」の語は、道具台帳の候補と分けるため「設定つき対象」と書いた(意味は §7-5 の「対象 × 設定」と同じ)。**リードの答えに無く場面係が足したもの**: (i) 操作を決めてよい材料の条件、(ii) 設定つき対象を分けるのは利用者が選ぶ値だけ、(iii) 新実装・当方の現状・試金石の表の行は 1 つの設定つき対象だけ、(iv) 作り手の条件((3) の最後の文。br6-2-1 の「対象の class を adapter が組み立てる」形を、受け渡しの条件だけでは閉じられないため)、(v) どれにも当たると示せない物は数えない、(vi) 試験の入力の空間の作り方(最後の文)。(iv)〜(vi) は今回足した。

### 当てたときに直す所(この段では直さない。直しの段で試験を先に書く)

- `repro_lean52.py` の `_sources` と `lean52.data_manager_add`: 購読を `Scene.input` から組むのをやめ、利用者の公開の入口(`AddCryptoFuture` の解像度)から `LookupSubscriptionConfigDataTypes` の式どおりに型の組を作る。解像度ごとに別の設定つき対象として走らせる。未確認の 3 点は (b) で一次資料を読んで決め、読めなければ理由を書く。
- runner: 設定つき対象ごとに走らせて残す。`target_types` を設定つき対象ごとにする。adapter に設定の操作(公開の呼び出し・一次資料の行か配布物のコードの行・時点・決めた材料)を名乗らせ、正の定義 A (1) を満たさない実行は採点しない。時点は書き残すだけで、時点だけでは採点から外さない(2 回目の「直す所」の時点の規則は正の定義 A に無いので取り下げた)。
- `adapters/common.py` の `read`・`attr`・`iterate`・`aiterate` と runner の `delivered`: 戦略の呼び出しの中で読まれたことと、物の作り手を書き残す。呼び出しの外で読んだ物に `returned_by` を付けない(br6-3-2)。
- DEFINITIONS.md の「共通の決まり」の事象の型の段落と「出所の検め」の (b) の文: 正の定義 A の選択肢を 1 つのデータ(場面集の中の 1 つのモジュール)に書き、DEFINITIONS.md の文と試験の格子の軸を**両方ともそのデータから作る**(正の定義 A の文と試験の軸が別々に書かれてずれる余地を無くす)。
- 敵対者の試験(LEAD_DESIGN.md §3.3 を場面集に当てる)。軸は正の定義 A の文の節ごとに作り、各軸に「どれにも当たらない」を足す:

| 正の定義 A の節 | 軸 | 値 |
|---|---|---|
| (1) | 操作の出所 | 配布物のコードの行か一次資料の行で示せる公開の呼び出し / 示せない操作 / どれにも当たらない |
| (1) | 操作の時点 | 開始前 / 戦略の呼び出しの中 / ほかの時点 / どれにも当たらない |
| (1) | 操作を決めた材料 | 場面が名指す銘柄と事象の型 / 設定つき対象の設定の値 / 対象の公開の既定 / 戦略が (3) で受けた物 / まだ届いていない入力 / どれにも当たらない |
| (1) | 1 つの実行で続けて行えるか | 行える / 行えない寄せ集め / どれにも当たらない |
| (2) | 測る仕事をしたコード | 配布物のコード(Python)/ 母語のコード / 一次資料の行に 1 対 1 の再現 / 一次資料の行に当たらない再現の工夫 / 場面集の側の許された 4 つ / 場面集の側のそれ以外 / 戦略 / どれにも当たらない |
| (3) | 物の作り手 | 配布物のコード(Python)/ 母語のコード / adapter / 再現の外側 / 一次資料の行に当たらない再現の工夫 / 場面集の側が呼んだ変換 / 戦略 / どれにも当たらない |
| (3) | 受け渡し | 配布物のコードが引数で渡した / その中にあった / 母語のコードが渡した / 戦略が対象の関数を呼んで返ってきた / 戦略が自分で貯めた / 場面集の側が渡した / どれにも当たらない |
| (3) | 受けた時 | 戦略の 1 回の呼び出しの中(コルーチンは最初に入ってから終わるまで)/ 呼び出しの外 / どれにも当たらない |
| 設定つき対象と表の行 | 型の組の出所 | 同じ設定つき対象の p3 / 違う設定つき対象の p3 の和 / どれにも当たらない |
| 設定つき対象と表の行 | 表の行 | 調査結果の側 / 新実装 / 当方の現状 / 試金石 / どれにも当たらない |

  全部の升目で偽の対象を作り、runner が「対象の物」と数えるのが正の定義 A の条件を全部満たす升目だけであることを確かめる。期待は正の定義 A の条件だけから出す(runner の場合分けから作らない)。この表の軸の値は正の定義 A の文から書き出したもので、正の定義 A の文そのものが落とした由来はこの試験でも拾えない(限界。批評家・監査役が正の定義 A を読んで確かめる)。直しの段では、上の「1 つのデータ」から軸を作り、この表を手で持たない。
- 表への影響は**未測定**。直しの段で設定つき対象ごとに走らせ直して出す(ここに見込みを書かない。正の定義 B)。

## 4. 正の定義 B: 書き写した文が元とずれる族(i0-r7-04・dr8-1-1-1・dr8-1-2-1)

### 確かめたこと

- 場面集の追跡されたファイルの一覧: `<scratchpad>/bt/r8-1/item0_r8-1-2_scenekeeper_def_files.txt`。場面 id の出現の数え(範囲 = `git ls-files tests/bt/battery/item_0` の全部、CLAUDE.md §3 の 3 つ): `…_sceneid_counts.txt`。
- 生成物の中の手で書いた文: `gen_definitions.py` の 26 行の文字列(場面 id と観点ごとの場面の数を手で書いた文)は DEFINITIONS.md に写る。`test_battery_item0.py` の `test_definitions_in_sync_with_scenes` は DEFINITIONS.md と生成器の出力の一致だけを見る。`scenes.py` の値と並べた出力: `…_gen_prose_check.txt`(今食い違っているかは判定していない。照らす機械が無いこと自体が穴)。
- 2 回目の版の自己検査の grep の語(`…_selfcheck.txt`)と、監査役が挙げた 2 回目の版 37 行の文(`…_prev_version_2.md` の 37 行)。

### なぜ起きたか(i0-r7-04)

1. **場面の中身を、手で書く文書に書き写していた。**0#5-1 の直し(DEFINITIONS.md を `scenes.py` から作る)は DEFINITIONS.md の値の部分だけを閉じ、検討表・再現の注釈・生成器の手の文には当てなかった。
2. **同じ根の全箇所を、思い出した場所の一覧で探した。**同じ漏れが `opponents/repro_lean52.py` の docstring にもある。
3. **古い文を落とす機械が 1 つの書き方の文しか見ない**(`test_no_stale_best_claim_in_the_review_table`)。

### 前の回で外した根本原因

- **dr8-1-1-1**(1 回目): 当てる範囲を例で書き、ROOTCAUSE*.md が外れた。正の定義を自分の成果物に当てなかった。生成物を丸ごと書いてよい所にした。
- **dr8-1-2-1**(2 回目): **「場面の中身」を語の一覧(入力の型と件数・期待・対象ごとの結果)で定義した。**一覧は 1 つの場面の欄の値を名指し、場面の集まりについての文(ある欄を持つ場面の数)は一覧の外になった。2 回目の版 37 行は `type_plan` の欄を持つ場面の数を自然文で書いており、`scenes.py` が変われば黙って偽になるのに、一覧のどの語にも当たらなかった。**自己検査の範囲も同じ語の一覧から作った**ので、同じファイルの行が走査から漏れ、それでも「1 行ずつ読んだ」と書いた(正の定義 0 の違反)。同じ根で、2 回目の版はコマンドの出力を文に写していた所がほかにもある(`…_prev_version_2.md` の 36・38・143 行の「→」の後の値、70 行の件数、104 行の DEFINITIONS.md の文の写し、76 行の場面の数。この版では全部 path に替えた)。

### 正の定義 B(監査役に先に通す段落)

**場面集の文が場面について述べてよいこと**: 当てる範囲は場面係が書く全部の文 = 場面集の置き場所(`git ls-files tests/bt/battery/item_<番号>` と、まだ追跡されていない書いた物)の全ファイルの全行(ROOTCAUSE*.md・検討表・README の類・コードの注釈と docstring・生成器の中に手で書いた文を含み、ファイルの種類・名前・書いた周で除かない)と、場面係の返り値である。**元**は、場面集の中で値が変わりうる物 = 場面の定義(`scenes.py` の値)と runner の出力(`survey_results/`)、およびそれらから機械で作った物である。**場面の中身**は、真偽が元の今の値に依る言明の全部であり、どの語で書かれているかでは決めない: 1 つの場面の欄の値(入力・期待・結果)だけでなく、場面の集まりについての言明(ある欄を持つ場面の数と id、観点ごとの場面の数、対象ごとの結果の数、「どの場面も…」)を含む。見分け方は「元の値が変わったら、その文は偽になりうるか」で、なりうる文は場面の中身である。場面の中身を文で持ってよいのは、(i) 元の値から機械で作り、元と食い違えば試験が落ちる部分(生成器に手で書いた文は、生成物に写ってもこれに当たらない)と、(ii) 試験がその文の名指す場面 id の今の元(版を固定した文ならその版の元)から作り直して一致を確かめる定型の文、だけである。それ以外の文は場面の中身を述べず、場面 id と欄(版を固定するなら git の版)を名指す参照だけを持つ。照らせない書き方の場面の中身は、正しくても、過去の周の文書の中でも置かない(過去の周の文書は、その周の版を固定した参照に直す)。コマンドの出力は、打った版と一緒に場面集の外のファイルに置き、文はその path を名指し、出力の値を文に写さない。機構の読み・一次資料の行・判断とその理由・場面集のコードの振る舞い(関数名で指し、読んだ版を書く)は場面の中身ではない。この正の定義を当てる検査は、当てる範囲の全行を行ごとの判断で覆い(正の定義 0)、探す語の一覧で当てる範囲を狭めない。場面の定義を変えたときの同じ根の探しも、場面集の全ファイルの全行について行う。

リードの答えとの突き合わせ: §7-6(ROOTCAUSE*.md を含める。場面 id と欄の参照で書く)。**場面係が足したもの**: 場面の中身を真偽の依り方で決めること(今回)、元を「値が変わりうる物」と書いたこと(今回)、出力の値を文に写さないこと(今回。2 回目は「path を名指す」までで、値を写すことを禁じていなかった)。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: §7 の自己検査(全行の判断)。
- 場面集のほかの全ファイル(過去の ROOTCAUSE*.md・検討表・adapter と再現の注釈・runner と試験の注釈を含む全部): 全行を行ごとの判断で読み、場面の中身を述べる文を、版を固定した参照・今の元への参照・試験が照らす定型の文のどれかに直す。
- `gen_definitions.py` の手で書いた文: 場面 id の一覧と観点ごとの場面の数を、`scenes.py` の値から作る式に置き換える。
- 機械の試験: 場面集の手で書く全ファイルの全行を書き出し、場面 id・`Scene` の欄名・数字を持つファイルの行に印を付け、印の付いたファイルの行は定型の文に当たって照らしが通るか、判断の出力に「参照だけ」と書かれているかのどちらかでなければ落とす(既定は落とす)。印の無いファイルの行も判断の出力に載り、判断の数とファイルの行の数が等しくなければ落とす。**限界**: 文の真偽が元に依るかは機械では決まらない。機械が保証するのは、全行が判断の出力に載ることと、印の付いたファイルの行が既定で落ちることまでで、判断の中身は批評家・監査役が読んで確かめる。

## 5. 正の定義 C: 観点の範囲を要件の名詞から列べていない族(i0-r7-05・dr8-1-1-2・dr8-1-2-4)

### 確かめたこと

- 通知の遅れの試し(批評家の probe を、直しの前の定義の 1 回目の段で走らせ直した): `<scratchpad>/bt/r8-1/item0_r8-1_scenekeeper_def_probe_notice.out`。
- DEFINITIONS.md の「場面にしていない観点・側面」の節の手で書いた項目(`…_checks.txt` の grep、版 `acc6d26`)。どれも升目を列べずに書いた申告である。
- `Scene` の欄(`…_checks.txt`)に、場面がどの事象をどの見る道で見るかの欄は無い。

### なぜ起きたか(i0-r7-05)

1. **観点の範囲を、`Scene.input` に書ける事象の種類から作った。**要件の名詞(§1 の事象の型・戦略の API)の全部から作る手順が無く、`Scene.input` に書けない事象(対象が注文から作る事象)と、事象の配達以外の見る道が抜けた。
2. **見る道を列べる手順が無かった**(`Scene` に欄が無い)。

### 前の回で外した根本原因

- **dr8-1-1-2**(1 回目): 正の定義 C を指摘の観点と思い出した観点にだけ当て、軸を要件の名詞から作る規則が無かった。
- **dr8-1-2-4**(2 回目): 「直す所」の小見出しを正の定義 A・B にだけ置いた。各節の小見出しを書き出して並べる手順が無く、書いた順に思いついた小見出しを置いた(正の定義 0 の違反)。C の直しが実際に行われるかが、正の定義 C の本文の 1 文だけに委ねられていた。この版では、正の定義 0〜D の 5 つの節が同じ小見出しを持つ(§7 の自己検査の見出しの出力)。

### 正の定義 C(監査役に先に通す段落)

**観点の場面が覆う範囲**: 7 観点(P0-1〜P0-7)の全部に同じ方法で当てる。升目の軸は固定した要件の名詞から作る: **事象** = §1 の事象の型(約定・板の写真・板の差分・足・資金調達・清算・時計・注文の通知。通知は §1 の語どおり受付・拒否・約定の 3 つに分ける)、**見る道** = §1 の戦略の API の名詞と「見られない」の語から、戦略の呼び出しに届く物 / 発注の呼び出しがその場で返す物 / 取消の呼び出しがその場で返す物 / 戦略が対象の公開の手段で読む物(読む物は、事象そのものと、事象から対象が作る物(注文の状態・約定済みの数量・未決の数の類)に分ける)、加えて、その観点の §2 の文が事象と見る道のほかに列べて名指す名詞の類(P0-2 の時刻の単位、P0-7 の差し込む口)。観点の文(§2 の 2 列目)が限らない軸は、全部がその観点の範囲に入る。観点ごとに範囲の全部の升目を列べ、各升目を「場面にした(場面 id)」か「場面にしない(理由)」のどちらかにする。場面にするかは、その観点の固定した測り方(§2 の 3 列目)の文の語で決め、場面にしない理由には外れる語を引く。測り方の文の語で中か外かが決まらない升目は場面係が決めず、リードの決定に従い(理由に決定の出所を書く)、決定の無い升目はリードに上げる(要件と観点の読みを直すのはリード = 委任文 §3「要件と判定の固定」)。この表は DEFINITIONS.md の「場面にしていない観点・側面」として、場面の定義と升目の軸から機械で作り、範囲の升目が 1 つでも表に無ければ試験が落ちる(升目を列べない申告は置かない)。場面は、その升目について観点の性質だけで結果が決まり、ほかの観点が測るもの(事象の型の有無・差し込む口の有無)で決まらないように作る: 遅れは公開の設定の値でも差し込む口でもよく、事象の型が足りない対象は第 r7-1 回の型の規則(型が最低の数に足りない対象は対応なし、理由に p3 の結果)に従う。

リードの答えとの突き合わせ: §7-7(7 観点全部を事象 × 見る道で列べ、表を機械で作る)、§7-2(通知・時計の升目は場面にしない。理由の文はリードの文)。**リードの答えの「事象 8 種 × 見る道 3 種」より細かく分けた所**が 2 つある: 通知を 3 つに(§1 の語が受付・拒否・約定の 3 つの名詞)、見る道の 2 つ目と 3 つ目を 2 つずつに(§1 が発注と取消を別の名詞で書き、§2 の P0-4 の測り方の語「事象を読もうとする」が読む物のうち事象を名指すため)。分けると升目が増えるだけで、測り方は変わらない。P0-4 の「実行時エラーか型エラー」は結果の側の語で、軸にしない。2 回目の版からの変更は語だけ(「組」→「升目」、「P0-2 の単位」→「P0-2 の時刻の単位」。正の定義 D)。

### 当てたときに直す所(この段では直さない。直しの段で試験を先に書く)

- `scenes.py` の `Scene`: 場面が覆う升目(観点・事象・見る道・観点の追加の名詞)の欄を足し、全部の場面にその値を書く。表の「場面にした」はこの欄から作る(手で場面 id を書かない。正の定義 B)。
- 升目の軸: 要件 §1・§2 の名詞を、要件のファイルの行番号つきで 1 つのデータ(場面集の中の 1 つのモジュール)に書き、DEFINITIONS.md の表と試験の両方をそこから作る。
- 場面にしない升目の理由: 測り方の文の外れる語の引用か、リードの決定の出所(LEAD_DESIGN.md §7-2 ほか)を、升目ごとに同じデータに書く(判断とその理由なので正の定義 B の場面の中身ではない)。決定の無い升目は「リードに聞くこと」に上げ、その升目は表で「未決」と出す。
- `gen_definitions.py`: DEFINITIONS.md の「場面にしていない観点・側面」の節を、上のデータと `Scene` の新しい欄から作る。今の手で書いた項目を消す。
- 試験: (a) 7 観点それぞれの軸の直積の全部の升目が表にちょうど 1 回ずつ出ること、(b) 「場面にした」升目の場面 id の新しい欄がその升目を含むこと、(c) 「場面にしない」升目の理由が、引いた語をその観点の測り方の文に含むか、リードの決定の出所を持つこと。敵対者の試験(先に書く): 升目を 1 つ消したデータ・欄に無い升目を名乗る場面・引いた語が測り方の文に無い理由を作り、どれも (a)〜(c) で落ちることを確かめる。軸の直積は試験の中で要件のデータから独立に作り、生成器の出力から作らない。
- 「場面にする」と決まった升目の場面の追加: 表を作ってから決める(ここに見込みを書かない。正の定義 B)。
- 測り方の文の字面で中か外かが決まらない升目の読み(2 回目の版では C の節の別の小見出しに置いていた)は、§8 の 2・3 に移した。リードの答えを受けてから、上のデータの理由に書く。

## 6. 正の定義 D: 語の意味の族(dr8-1-2-2)

### 確かめたこと

- 「単位」の出現(範囲 = 場面集の全ファイル + 要件・委任文・リードの設計・CLAUDE.md): `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_term_tan-i.txt`。範囲の中で「単位」は、少なくとも、時刻の単位(要件 P0-2 の逐語)、数量(委任文 §4「1 単位買い」)、研究の区切り(CLAUDE.md「単位の着手前に」ほか)、2 回目の版の「走らせる単位」の 4 つの意味を持つ(ここに挙げたのは固定した文書と 2 回目の版だけ。場面集のほかのファイルの出現は出力のファイルにあり、ここでは述べない = 正の定義 B)。
- 2 回目の版の中で意味を持たせた語のほかの意味: `…_term_senses_old.txt`(「対象」「道」「組」「形」「列」「記録」「定義」「型」にも 2 つ以上の意味があった)。
- この版で新しく使った語(設定つき対象・升目・当てる範囲・定型)の範囲の中の出現: `…_term_newname.txt`・`…_term_newname2.txt`。範囲を項目 0 の VERDICTS の全部(run2〜run7)に広げて打ち直した出力: `…_term_new_allverdicts.txt`(`…_term_newname*.txt` の範囲の文書は VERDICTS run7 だけだった)。

### なぜ起きたか

1. **O-7 の照らしを、言い換える前の語(候補)にだけ当てた。**新しい語(単位)の範囲の中の出現を引かなかった。委任文の監査の前例(VERDICTS prompt 448-456 行)と同じ形である。
2. **文書が意味を持たせた語を書き出す手順が無かった。**そのため「単位」以外の語(行・道・組・型ほか)の使い回しも、照らす手順の外にあった(正の定義 0 の違反)。
3. 2 回目の版の §5 の吟味の表の行「O-7」は、配慮した事実だけを書き、照らしたコマンドと出力を持たなかった(O-3)。

### 前の回で外した根本原因

無し。正の定義 D はこの回に新しく書いた(1 回目・2 回目の版に当たる正の定義は無い)。2 回目の版の O-7 の照らし方の誤りは、上の「なぜ起きたか」に書いた。

### 正の定義 D(監査役に先に通す段落)

**語の意味**: 場面係が書く文(正の定義 B の当てる範囲と同じ)では、1 つの語は 1 つの意味だけを持つ。語の意味を比べる範囲は、その文と一緒に読まれる文書の全部 = 場面集の全ファイル・固定した要件・委任文・リードの設計・項目 0 の VERDICTS・CLAUDE.md である。範囲の中ですでに意味を持つ語(要件と委任文の語)は、その意味でだけ使う。新しく意味を持たせる語は、範囲の中で別の意味の出現が無いことを、範囲を切らずに引いたコマンドと出力で確かめてから使う。範囲の中で 2 つ以上の意味を持つ語は裸で使わず、意味を決める修飾をいつも付ける(例: 表の行 / 一次資料の行、時刻の単位)。語に意味を持たせる文書は、意味を持たせた全部の語を 1 つの用語の表に書き、表の語の全部の出現を書き出して(正の定義 0)、出現ごとに表の意味で使っているかを確かめる。語を言い換えたときは、言い換えた後の語の範囲の中の全部の出現も確かめる(1 か所の言い換えが新しい衝突を作る = dr8-1-2-2)。

リードの答えとの突き合わせ: リードの答えに無く、場面係が足した(CLAUDE.md §0.2 O-7 を場面集の文に当てる手順として)。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: 冒頭の用語の表と、§7 の自己検査の出現の書き出し。
- 場面集のほかの全ファイル: 意味を持たせた語(DEFINITIONS.md の語と、このファイルの用語の表の語)の全部の出現を書き出し、2 つ以上の意味の出現に修飾を付ける。DEFINITIONS.md・`scenes.py`・`gen_definitions.py` の「単位」の出現の書き出しは `…_checks.txt`(版 `acc6d26`)にある。直しの段で出現ごとに読み、要件の逐語はそのまま引き、ほかの意味の側を言い換える。
- 機械: 出現の書き出しまで(ファイルごとの数と合計、CLAUDE.md §3 のやり方)。意味の判断は読む者がする(限界)。

## 7. 提出前の吟味(委任文 §3、最初に作る役の文: 要件・規則・観点を読み直し、厳しい監査役・批評家なら何を止めるかを観点ごとに列べて潰す)

読み直したもの: 固定した要件(REQUIREMENTS.md §1・§2)、場面集の規則 1〜9、委任文 §3 の全部の段落、LEAD_DESIGN.md §2〜§7、起動文の dr8-1-2 の 4 件の逐語、2 回目の版。

**自己検査(正の定義 0・B・D をこのファイルに当てた)**: コマンドと出力は `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_selfcheck.txt`。中身: (1) このファイルの全行を行番号つきで書き出し、場面 id・`Scene` の欄名・数字・場面の語に印を付け、全行に判断を付けた(判断は `…_selfcheck_judgments.tsv`、判断の数とファイルの行の数を並べた)。(2) 用語の表の語と、2 回目の版で使い回していた語の全部の出現を行番号つきで書き出し、出現ごとに読んだ。(3) 全部の節の見出しを書き出し、正の定義 0〜D の 5 つの節の小見出しが同じことを並べた。

| 厳しい監査役が止めそうなこと | 潰し方 |
|---|---|
| dr8-1-2-1 の再発(このファイルが場面の中身を書く・自己検査の範囲が語の一覧) | 場面の中身を真偽の依り方で定義し直し、全行に判断を付けた。場面の集まりについての文も書かない。出力の値を文に写した所は全部 path に替えた |
| dr8-1-2-2 の再発(新しい語が別の意味と衝突) | 用語の表を置き、新しい語 4 つの範囲の中の出現を引いた。使い回していた語(対象・道・組・形・列・記録・定義・型・行・単位)は修飾か別の語に替え、全部の出現を自己検査で書き出した |
| dr8-1-2-3 の再発(試験の軸が正の定義の由来を落とす) | 軸を正の定義 A の節ごとに書き出し、各軸に「どれにも当たらない」を足した。直しの段では正の定義 A の文と軸を 1 つのデータから作る。正の定義 A の文そのものの落としは拾えない限界を書いた |
| dr8-1-2-4 の再発(方法を一部にだけ当てる) | 5 つの節に同じ小見出しを置き、自己検査で並べた |
| 直す所に正の定義に無い規則を足す(2 回目の時点の規則) | 取り下げたことを §3 に書いた。直す所は正の定義の文から作る |
| 正の定義 0 自体が「当てる先の一覧」になっている | 規則は「全部を機械で書き出す」で、括弧の中は例と明記した。例に無い当てる先も規則に従う |
| 正の定義 B の「偽になりうるか」は機械で決まらない | 限界として書いた。機械が保証すること(全行が判断に載る・印の付いたファイルの行は既定で落ちる)と、読む者が確かめることを分けた |
| 正の定義 A (3) に作り手の条件を足したのは要件を後から厳しくする | 要件(REQUIREMENTS.md)も判定も変えていない。変えたのは場面集の帰属の規則で、br6-2-1(adapter が対象の class を組み立てる)を閉じる条件。足したことを「場面係が足したもの」に書いた |
| 正の定義 A が新実装に有利に偏る | 新実装の表の行は 1 つの設定つき対象だけで、場面ごとの寄せは調査結果の側だけ = 新実装が勝ちにくい向き。当方の現状の adapter に利用者が選ぶ設定の値があるかは grep の出力(`…_checks.txt`)で確かめた |
| 正の定義 C の直しで要件を広げる | 場面にするかは測り方の文の語で決め、決まらない升目はリードに上げる。リードの §7-2 の決定に従う |
| 断定(O-2・O-3) | LEAN の未読の 3 点と表への影響は「未確認」「未測定」と書いた。確かめたことには出力のファイルを付けた |
| repeat_of の読みを付けずに返す(L-438 (1)) | §0 の表に 11 件とも読みと根拠を書いた |
| 候補 35 の件を埋もれさせる(L-438 (3)) | §8 の 1 に独立の項目として残した |

この段で回した試験: 無し(コードも場面集も変えていない)。回したのは確かめのコマンドだけ(上の出力のファイル)。

## 8. リードに聞くこと

1. **候補 35 の権限の分類器の拒否(br6-3-1、L-438 (3) の独立の項目)**: リードの答え §7-1「受け取った。場面係はこれ以上試さない。リードが run の止まった時機に試す」を受けた。場面係は試さない。検討表では「動かなかった」の側に置いたまま。
2. **§7-2 で「場面にしない」と決まった升目の理由の語**(2 回目から持ち越し): P0-1・P0-2・P0-4 の通知と時計の升目は、場面係には測り方の文のどの語で外になるかを示せない(P0-1 の「投入」は P0-3 の測り方が通知と時計を含む事象の型に使う語 / P0-2 の 2 列目は「全事象の時刻」/ P0-4 の「事象を読もうとする」は通知と時計の事象も名指す)。P0-5 は「入力を作り」、i0-r7-05 の升目(約定の通知 × 事象から対象が作る物を読む)は P0-4 の「事象を読もうとする」の外と読める(批評家とリードの読みと同じ)。外れる語を教えてほしい。答えが無ければ、理由の欄には §7-2 の文と出所(LEAD_DESIGN.md §7-2)を書き、語は「リードの決定」と書く。
3. **測り方の文が見る道を名指さない観点(P0-2「核が保持する値」・P0-3「保持するか」・P0-7「動くか」)の升目**(2 回目から持ち越し): (a) 対象の持つ見る道のうち 1 つで測れば足りる(ほかの見る道の升目は「1 つの見る道で測れる。全部を求めると固定した測り方より厳しくなる」を理由に場面にしない)か、(b) 対象の持つ全部の見る道で測る(場面が増え、読む手段を持たない対象はその場面が対応なしになる)か。場面係の読みは (a) だが、字面では決まらないので決めていない。
4. **正の定義 0 と D はリードの答えに無く、場面係が足した**(§1 の根本原因と O-7 から)。項目 0 の場面集の規則として置いてよいか。監査役(定義)が通しても、リードが要らないと決めれば外す。
