# 場面集への指摘ごとの根本原因と正の定義(項目 0、第 r8-1 回の直しの前の定義、4 回目、場面係、2026-09-24)

委任文: `docs/DATA/delegations/20260923_backtest_env_prompt.md`。起動文の指紋は `9ef8d697ad0a`。作業木の委任文の指紋と、読んだ範囲は `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_readlog.txt`。作業木の HEAD は `bc5a6d7`(版の名指し)。このファイルがコードの振る舞いと文書の中身について述べることは、この版を読んで書いた(版の違う物を読んだ所は、その所に版を書いた)。`<scratchpad>` = `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad`。
合意した完了の形(委任文 §0 の逐語): 「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」

**切片まで書き出して当てた決まり**(正の定義 0 の意味。切片ごとの判断と、切片の数と判断の数が等しいことの出力を持つもの):
- リードの注記(起動文、L-438、オーナー承認)の (1)〜(4) と、`docs/OWNER_LOG.md` の L-438 の行の案の文面 (1)(2): 切片と、切片ごとにこのファイルのどこに書いたかの判断は `…_l438.txt`。
- 固定した要件 §1 の要件の表の行(4 列目)と §2 の要件の表の各行(2 列目・3 列目): 正の定義 C の升目の軸を作った。切片と判断は `…_axes_c.txt`。
- リードの答え `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/round_7/LEAD_DESIGN.md` §7.1 の 8・9: 文が短いので切片に切らず、逐語をこのファイルの §7 と各「当てたときに直す所」の表の見出しに置いた(当てた所は `…_l438.txt` の末尾に並べた)。

**読み直した決まり**(切片まで書き出していない。この段の成果物は根本原因と正の定義で、下の決まりの切片ごとの対応を持つ段ではないため範囲を切った。これらについて「当てた」とは書かない): 委任文 §3「場面集」「周回の数え方と止める条件」「動かせない候補の検討と再現」「要件と判定の固定」「提出前の吟味」、LEAD_DESIGN.md §2〜§7、CLAUDE.md §0.2 O-7・O-10 と §3「数え直しは範囲を切らずに打ち…」、場面集の規則 1〜9。

この回の入力: 直しの前の定義の 3 回目の版への監査役(定義)の指摘 dr8-1-3 の 6 件(逐語は起動文)= [止める] dr8-1-3-1・dr8-1-3-2 / [直す] dr8-1-3-3・dr8-1-3-4 / [聞く] dr8-1-3-5・dr8-1-3-6。[聞く] の 2 件にはリードが LEAD_DESIGN.md §7.1 の 8・9 で答えている。3 回目の版は書き換える前に `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_prev_version_3.md` に写した(写す前と後の sha256: `…_prev_sha.txt`。HEAD `bc5a6d7` の版とも sha256 が同じ: `…_readlog.txt`)。1 回目の版は `git show 3e42e8a:tests/bt/battery/item_0/ROOTCAUSE_r8-1.md`、2 回目の版は `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_prev_version_2.md`。監査役が挙げたファイルの行の番号は 3 回目の版のもので、この版のものではない。

**この段ではコードも場面集の他のファイルも直していない**(起動文「まだコードを直さない」、L-433「場当たり的な修正をするな」)。書いたのはこのファイルだけ。

**このファイル自身が正の定義 0・B・D の当てる範囲に入る。**場面の中身(正の定義 B の意味)は書かず、場面 id と `scenes.py` の `Scene` の欄名で指す。確かめに打ったコマンドの出力は `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_*`(3 回目までの出力は `item0_r8-1-3_…`・`item0_r8-1-2_…`・`item0_r8-1_…`)に置き、ここでは path で指す。

## 用語の表(正の定義 D をこのファイルに当てたもの。1 つの語に 1 つの意味)

区分は正の定義 D の 3 つ(要件・委任文の語 / 範囲の中の既存の語 / 新しい語)。語ごとの範囲の中の出現(ファイルごとの数)と、新しい語と「母語のコード」の出現ごとの判断は `…_terms.txt`。このファイルの中の出現の書き出しと判断は `…_selfcheck.txt` の (2)。

| 語 | このファイルでの意味 | 区分と出所 | 範囲の中の別の意味と、このファイルでの避け方 |
|---|---|---|---|
| 対象 | 場面集に通して比べるエンジン(新実装・当方の現状・調査結果の側の各道具・再現・試金石) | 委任文の語(§3「行 = 対象」) | 2 回目までのこのファイルは当てる先の意味にも使った。当てる先は「当てる先」「当てる範囲」と書く |
| 設定つき対象 | 対象 × 設定の値(正の定義 A) | 新しい語(3 回目) | 範囲の中の出現はこのファイルだけ(`…_terms.txt`) |
| 設定 / 設定の操作 / 設定の値 | 正の定義 A (1) の意味 | 範囲の中の既存の語(LEAD_DESIGN.md §7 の 4・5) | 範囲の中では設定のファイル(`config/` の類)と設定値(CLAUDE.md A-12)にも使われる。その意味では「設定のファイル」と書き、裸の「設定」は正の定義 A の意味だけにする |
| 候補 | 道具台帳の候補(委任文の意味) | 委任文の語 | LEAD_DESIGN.md §7 の 5 は対象 × 設定を「候補」と呼び、LEAD_DESIGN.md §7.1 の 8 は出しうる指摘を「指摘の候補」と呼ぶ。このファイルでは前者を「設定つき対象」、後者を「指摘の候補」と書き、裸の「候補」は道具台帳の候補だけにする |
| 時刻の単位 | 要件 P0-2 の語(秒・ミリ・ISO 文字列の類) | 要件の語 | 裸の「単位」は数量(委任文 §4「1 単位買い」)と研究の区切り(CLAUDE.md)にも使われる。裸の「単位」を使わない |
| 事象の型 / 型の組 | 要件 §1 の事象の型 / 1 回の実行に入れる事象の型の組み合わせ | 要件の語 / L-438 の語 | Python の型は「class」と書く。要件 P0-4 の「型エラー」は要件の逐語の複合語としてだけ引く。「組」は盲検の組(委任文)にも使われるので裸の「組」を使わない |
| 設定つき対象の持つ型 | 設定つき対象の同じ実行の p3 の場面のうち、正しさが「正解と一致」の場面の事象の型(正の定義 A「型の組」) | 範囲の中の既存の語「持つ型」に修飾を付けた物。意味は L-438 (2)(「対象の持つ型から規則で決める」「値が不一致の型は「持つ」と数えない」)と LEAD_DESIGN.md §4 の br7-1-2 | 範囲の中の「持つ型」は 2 つの意味を持つ: 対象の配布物にある class(DEFINITIONS.md の「対象の配布物が持つ型」)と、場面に使う対象の型(ROOTCAUSE_r7-1.md・LEAD_DESIGN.md・委任文 §0)。出現ごとの判断は `…_terms.txt`。このファイルでは裸の「持つ型」を使わず「設定つき対象の持つ型」と書く。L-438 の逐語の引用だけはそのまま引く |
| 見る道 | 正の定義 C の軸の 1 つ | 範囲の中の既存の語(このファイルの 2・3 回目の版) | 裸の「道」を使わない(LEAN の購読から `OnData` までのコードは「購読から `OnData` までのコード」と書く) |
| 升目 | 軸の値を 1 つずつ選んだ組み合わせ 1 つ(正の定義 C の観点の範囲、各正の定義の試験の格子) | 新しい語(3 回目) | 範囲の中の出現はこのファイルだけ(`…_terms.txt`) |
| ファイルの行・全行 / 一次資料の行 / 表の行 | ファイルの行(番号つきか「ファイルの行」。「全行」はファイルの全部の行)/ 一次資料のコードの行 / 表(比較の表・要件の表・このファイルの表)の横の 1 段(「要件の表の行」「用語の表の行」のように表の名を付ける。比較の表の行は委任文どおり対象を名指す) | 委任文の語 | 裸の「行」を使わない。「実行」は別の語 |
| 表の列 / N 列目 | 表の縦の 1 欄 | 委任文の語(§3「列 = 場面」) | 範囲の中で「列」は配達の列・試験の列(LEAD_DESIGN.md §3.2)と受けた事象の並び(`scenes.py` の `TYPE_RULE` の「受けた列」)にも使われる。このファイルでは表の縦の欄だけに使い、「文字列」は別の語 |
| 元 | 正の定義 B の意味 | 新しい語(2 回目) | 範囲の中で「元」は「元に戻す」(委任文 §2 項目 14)の類の一般語にも使われる。このファイルでは一般語の「元」を使わない |
| 場面の定義 / 正の定義 / 直しの前の定義 | `scenes.py` の `Scene` の値 / このファイルの正の定義 0・A・B・C・D / 台本の役の段の名(「1 回目」〜「4 回目」はその段の回) | 委任文の語(正の定義 = 委任文 §3)/ 起動文の語 | 裸の「定義」を使わない。「監査役(定義)」は役の名 |
| runner の出力 | `survey_results/` の中身 | このファイルで名付けた語(2 回目) | 「実行記録」は委任文で項目 8 の語 |
| 範囲 / 当てる範囲 / 当てる先 | 覆う先の全部の集まり(観点の範囲・語の範囲)/ 規則・正の定義・検査・試験を当てる先の全部(正の定義 0)/ そのうちの 1 つ | 「当てる範囲」は新しい語(3 回目)。「範囲」は一般語 | 「範囲を切る」は CLAUDE.md §3 の意味(数える範囲を狭める)と同じ |
| 定型の文 | 試験が作り直して一致を確かめる、決まった書き方の文(正の定義 B (ii)) | 新しい語(3 回目) | 範囲の中の出現はこのファイルだけ(`…_terms.txt`) |
| 形 | 同じ欠陥が現れた具体の姿(委任文「どの形にも当たる」) | 委任文の語 | 「完了の形」はオーナーと委任文の逐語の句としてだけ引く |
| 行う / 列べる / 組む(動詞) | 行う / 全部を書き出す / 組み立てる | 一般の動詞 | 名詞の「行」「列」「組」とは別の語 |
| 仕事 / 届き方 / 作り手 / 受け渡し | 正の定義 A の (2) / (3) / 試験の軸の名 | このファイルで名付けた語(3 回目) | 場面集のほかのファイルの「仕事」「受け渡し」の出現は直しの段で読む(正の定義 D の直す所) |
| 母語のコード | 対象の配布物のうち Python でないコード(DEFINITIONS.md の「対象の母語(C・C++ など)」と同じ意味) | 範囲の中の既存の語(DEFINITIONS.md・`gen_definitions.py`・`run_battery.py`・ROOTCAUSE_r6-2.md・ROOTCAUSE_r6-3.md・VERDICTS run7) | 範囲の中の出現の全部と、出現ごとの判断は `…_terms.txt`。判断が「別の意味」の出現があれば、このファイルでは修飾を付けて書く |
| 差し込む口 / 公開の入口 | 要件 P0-7 の口 / 利用者が呼ぶ対象の公開の関数 | 要件の語 / このファイルで名付けた語(3 回目) | 「入力の口」は使わない(「データの購読」と書く) |
| 書き出す文 / 切片 | 一覧(軸・値・条件・区分)を作るもとにする文 / 書き出す文を区切りの字で切った 1 片 | 新しい語(この版) | 範囲の中の出現は `…_terms.txt` |
| 暫定表 | 手で列べ、直しの段で機械で書き出した物に置き換える表 | 範囲の中の既存の語(LEAD_DESIGN.md §7.1 の 9) | 範囲の中の出現は `…_terms.txt` |
| 切片まで書き出して当てた / 読み直した | 決まりの文を切片まで書き出し、切片ごとの判断と数の等しさを出力に持つもの / それ以外で読んだもの | このファイルで名付けた句(この版、正の定義 0) | 動詞「当てる」だけのときは一般の意味(規則を何かに適用する) |

## 0. 族の読み(repeat_of。数えるのは台本と監査役で、場面係は読みを書くだけ)

| 指摘 | 監査役の repeat_of | 場面係の読み(族) | 読みの根拠 |
|---|---|---|---|
| i0-r7-03 | i0-r6-04 | 帰属の族(br6-1-1 → br6-2-1 → br6-3-2 の続き) | どれも「場面集の側が組んだ物を対象の振る舞いとして数えた」。値 → class の実体 → 戦略の外の呼び出し → 設定、と形が変わっただけ |
| i0-r7-04 | null | 書き写した文が元とずれる族(前例 0#5-1・i0-r3-11) | どれも「元から手で書き写した文が、元が変わっても照らされない」 |
| i0-r7-05 | null | 観点の範囲を要件の名詞から列べていない族(前例 i0-r3-10・i0-r3-03) | どれも「観点の範囲を、書ける物と思いついた物から作った」 |
| dr8-1-1-1 | null | 書き写した文の族 | 正の定義 B の当てる範囲を例で書き、正の定義 B を書いたファイル自身が外れた |
| dr8-1-1-2 | null | 観点の範囲の族 | 正の定義 C を一部の観点にだけ当てた |
| dr8-1-1-3 | null | 帰属の族 | 「設定」を手持ちの例の時点で書いた |
| dr8-1-1-4 | null | 帰属の族 | 表の行と対象の区切りを書かなかった |
| dr8-1-2-1 | dr8-1-1-1 | 書き写した文の族 | 場面の中身を語の一覧で決め、場面の集まりについての文が外れた。自己検査の範囲も同じ語の一覧から作った |
| dr8-1-2-2 | null | 語の使い回しの族(O-7、既往 I-004。委任文の監査に同じ形の前例 = `docs/AUDITOR/VERDICTS/2026-09-23_backtest_env_prompt.md` 448-456 行) | 1 つの語を言い換え、新しい語の範囲の中の出現を見なかった |
| dr8-1-2-3 | i0-r7-03 | 帰属の族 | 正の定義 A が認めた由来が、それを確かめる試験の軸から漏れた |
| dr8-1-2-4 | null | 場面係の読みでは dr8-1-1-2 と同じ「方法を一部にだけ当てた」形 | 「直す所」の小見出しを正の定義 A・B にだけ置いた |
| dr8-1-3-1 | null | 場面係の読みでは、正の定義 0 の「等しいことを示さない出力を根拠に全部に当てたと書かない」に反する形。前例は dr8-1-2-1 の中の「1 行ずつ読んだ」の申告(裏付けの出力が無い申告) | 決まり (1)〜(4) を当てたと申告し、決まりの文を切片まで書き出していなかった |
| dr8-1-3-2 | dr8-1-1-2 | 観点の範囲の族(監査役の読みと同じ) | 升目の軸を、要件の文を切片まで書き出さずに手で列べた |
| dr8-1-3-3 | dr8-1-2-4 | 「方法を一部にだけ当てた」形(監査役の読みと同じ) | 敵対者の試験の表を、指摘に出た正の定義 A にだけ置いた |
| dr8-1-3-4 | dr8-1-2-2 | 語の使い回しの族(監査役の読みと同じ) | 用語の表の 1 つの語に、正の定義 D の区分と出現の確かめを当てていなかった |
| dr8-1-3-5 | null | 族なし(O-10 との関係を問う [聞く])。リードの答え LEAD_DESIGN.md §7.1 の 8 | — |
| dr8-1-3-6 | dr8-1-2-3 | 帰属の族の試験の軸(監査役の読みと同じ)。リードの答え LEAD_DESIGN.md §7.1 の 9 | 試験の軸を、正の定義 A の文から機械で書き出さずに手で列べた |

## 1. 3 回目の版への 6 件に共通の根本原因

**正の定義 0 は「当てる先」の側だけを書き出させ、「一覧を作るもとにした文」と「当てたと申告する決まり」の側を書き出させていなかった。**3 回目の版は、升目の軸(要件の文から)・L-438 の条件(注記の文から)・敵対者の試験の軸(正の定義 A の文から)・用語の区分(正の定義 D の文から)の 4 つを、どれも書き出す文を切片まで書き出さずに、読んだ記憶から手で列べた。そのうえで冒頭に「条件 (1)〜(4) を当てた」と書いた。正の定義 0 は「等しいことを示さない出力を根拠に『全部に当てた』と書かない」と決めていたのに、3 回目の版の自己検査(§7)は正の定義 0 の括弧の中の例(ファイルの全行・節の小見出し・用語の出現)だけを走査し、このファイルの中の「当てた」「列べた」の申告を裏付けの出力と照らさなかった。**自己検査の範囲を正の定義 0 の例から作った**ことは、正の定義 0 が禁じた「当てる範囲を…思い出した物から作る」そのものである。加えて、敵対者の試験の表を正の定義 A にだけ置いたのは、前の回の指摘(dr8-1-2-3)が A の試験の軸を名指したからで、方法を当てる先を指摘から作った(dr8-1-3-3)。

## 2. 正の定義 0: 当てる範囲(dr8-1-3-1・dr8-1-3-2・dr8-1-3-3・dr8-1-3-6 と、3 回目までの dr8-1-2-1・dr8-1-2-3・dr8-1-2-4・dr8-1-1-1・dr8-1-1-2 に共通)

### 確かめたこと

- 3 回目の版の自己検査の道具が走査した物: `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_selfcheck.py`(ファイルの全行の印・語の出現・見出しの 3 つ)。このファイルの中の申告の文を裏付けの出力と照らす手順は無い。
- 3 回目の版の冒頭の「当てた決まり」の文(`…_prev_version_3.md` の 5 行)と、L-438 の条件の切片を 3 回目の版の中で探した出力: `…_l438.txt` の「3 回目の版」の欄。

### なぜ起きたか

§1 のとおり。

### 前の回で外した根本原因

- **3 回目**: 正の定義 0 を当てる先(ファイル・文書・用語・観点)の側にだけ書いた。一覧を作るもとにした文(要件・L-438・正の定義の文)と、「当てた」と申告する決まりを当てる先に入れなかった。自己検査の範囲を正の定義 0 の括弧の中の例から作った。
- 1 回目・2 回目の版には正の定義 0 に当たる正の定義が無い。

### 正の定義 0(監査役に先に通す段落)

**当てる範囲**: 場面係が規則・正の定義・検査・試験を当てる範囲は、当てる先の全部を機械で書き出した物である(ファイルなら全行、文書なら全部の節とその小見出し、正の定義なら全部の選択肢と全部の「数えない物」に「どれにも当たらない物」を足した物、用語なら文書が意味を持たせた全部の語の全部の出現、観点なら観点ごとの升目の全部。括弧の中は例で、例に無い当てる先も「全部を書き出す」に従う)。**一覧(軸・値・条件・区分・試験の格子)を文から作るときは、その書き出す文も当てる先であり、書き出す文を切片(区切りの字で切った 1 片)まで機械で書き出し、切片ごとに、一覧のどこに入れたか・入れない理由を持つ。一覧を手で持たない。**決まりを「当てた」と申告してよいのは、その決まりの文を切片まで書き出し、切片ごとに当てた所(ファイルの行)か当てない理由を持ち、切片の数と判断の数が等しいことを出力で示したものだけである。そうでない決まりは「読み直した」と書き、「当てた」と書かない。文書の中の「当てた」「列べた」「全部」の申告も当てる先であり、自己検査はファイルの行ごとの判断の中で、申告の行ごとに裏付けの出力の path を持つ。当てる範囲を、指摘に出た物・思い出した物・探す語の一覧・実装の場合分け・この段落の括弧の中の例から作らない。それらは書き出した物に付ける印に使ってよいが、当てる範囲を狭めない。当てた結果は書き出した物の 1 つずつに持ち(ファイルなら行ごとの判断)、書き出した数と結果の数が等しいことを同じ出力に示す。等しいことを示さない出力を根拠に「全部に当てた」「1 行ずつ読んだ」と書かない。当てる範囲を切ったときは、切ったことと理由を書き、切った外に当てたとは書かない。

リードの答えとの突き合わせ: リードの答えに無く、場面係が足した。CLAUDE.md §3「数え直しは範囲を切らずに打ち…範囲を…切ったら、切ったことを結果に書く」を、数えることから「当てること」全般に広げたもの。この版では、書き出す文と申告の側を足した(dr8-1-3-1・dr8-1-3-2・dr8-1-3-6)。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: 冒頭の決まりを「切片まで書き出して当てた決まり」と「読み直した決まり」に分けた。切片まで書き出したのは L-438(`…_l438.txt`)と要件 §1・§2(`…_axes_c.txt`)。§7 の自己検査はファイルの行ごとの判断に「申告の行か / 裏付けの path」の欄を持つ(`…_selfcheck.txt`)。
- 正の定義 A〜D の「当てたときに直す所」の敵対者の試験: 軸を正の定義の文の切片から機械で書き出す(この版の表は暫定表。下の表の見出しの 1 文)。
- 場面集の検査と自己検査の道具の全部: 走査の範囲が語の一覧・例で狭まっていないかを、道具ごとに読んで直す(範囲は `git ls-files tests/bt/battery/item_0` の中の検査と試験の全部)。

敵対者の試験(**手で列べた暫定表。直しの段で正の定義 0 の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**。この 1 文はリードの答え §7.1 の 9 の文の「正の定義 A」を「正の定義 0」に替えた物。替えたのは場面係):

| 正の定義 0 の文 | 軸 | 値 |
|---|---|---|
| 当てる範囲 | 当てる先の種類 | ファイルの全行 / 文書の節と小見出し / 正の定義の選択肢と数えない物 / 用語の出現 / 観点の升目 / 書き出す文の切片 / 当てたと申告する決まりの切片 / 文書の中の申告の行 / どれにも当たらない |
| 当てる範囲を…から作らない | 書き出し方 | 機械で全部 / 指摘に出た物 / 思い出した物 / 探す語の一覧 / 実装の場合分け / 括弧の中の例 / どれにも当たらない |
| 書き出した数と結果の数 | 結果の持ち方 | 1 つずつに 1 つ / 結果の無い物がある / 書き出した物に無い結果がある / 同じ物に 2 つ / どれにも当たらない |
| 「当てた」と申告してよい | 申告 | 「当てた」+ 等しい出力 / 「当てた」+ 等しくない出力 / 「当てた」+ 出力なし / 「読み直した」/ 範囲を切った + 理由 / 範囲を切った + 理由なし / どれにも当たらない |

  期待は正の定義 0 の文だけから出す: 通るのは、書き出し方が「機械で全部」、結果の持ち方が「1 つずつに 1 つ」、申告が「当てた + 等しい出力」「読み直した」「範囲を切った + 理由」のどれかの升目だけ。検査の道具(自己検査・場面集の検査)ごとに全部の升目の偽の入力を作り、道具がこの期待どおりに通し・落とすことを確かめる。

## 3. 正の定義 A: 帰属の族(i0-r7-03・dr8-1-1-3・dr8-1-1-4・dr8-1-2-3・dr8-1-3-6)と型の組(dr8-1-3-1、L-438 (2))

### 確かめたこと

- LEAN の事象の型の決め方(一次資料、版 `856327ff`、第 r7-1 回に取った写し `<scratchpad>/bt/item0_r7-1_scenekeeper_DataManager.cs`): `Add(Symbol symbol, Resolution? resolution = null, … List<Tuple<Type, TickType>> subscriptionDataTypes = null, …)`(602-613 行)、`var dataTypes = subscriptionDataTypes;`(616 行)、`dataTypes = LookupSubscriptionConfigDataTypes(symbol.SecurityType, resolution ?? _algorithm.UniverseSettings.Resolution, symbol.IsCanonical());`(626 行)、`.Select(tickType => new Tuple<Type, TickType>(LeanData.GetDataType(resolution, tickType), tickType))`(768 行)。`QCAlgorithm.cs` 3069-3083 行の `AddSecurity<T>` は `subscriptionDataTypes` を渡さない。この読みでは、1 回の `AddCryptoFuture` の約定の事象の型は解像度 1 つから決まる。`LeanData.GetDataType` の中身、同じ銘柄に解像度を変えて 2 回 `AddCryptoFuture` を呼べるか、実行の途中に公開の API で購読を足せるかは**未確認**(読んでいない)。
- 再現の組み立て方: `opponents/repro_lean52.py` の `_sources` は、購読の型の組を `Scene.input` から作り、`repro_engines/lean52.py` の `data_manager_add` に型の組を渡す(一次資料 616 行の、`subscriptionDataTypes` が null でない側)。再現のコードに解像度の語があるかの grep: `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_checks.txt`。
- 型の組の決め方の今のコード(版 `bc5a6d7`): `run_battery.py` の `target_types` と `correctness`、`scenes.py` の `TYPE_RULE`、表の注記の 1 文と materials の記録の有無の grep: `…_typerule_checks.txt`。`target_types` は p3 の場面の結果を状態(`ok`)と受けた型の名で見ており、`correctness` の正しさ(「正解と一致」)を見ていない。`TYPE_RULE` の文も「届けた型」で書いている。L-438 (2) の「値が不一致の型は「持つ」と数えない」を今のコードも DEFINITIONS.md の文も満たしていない。表の注記の 1 文は、場面集・項目 0 の文書の中では LEAD_DESIGN.md にだけある。
- 新実装と当方の現状の adapter に、利用者が選ぶ設定の値(解像度・購読・並びの引数)があるかの grep: `…_checks.txt`(3 回目の出力)。

### なぜ起きたか(i0-r7-03)

1. **再現が書き直すコードの始まりを、利用者の公開の入口ではなく「購読ができた後」に置いた。**第 r6-3 回の再現は LEAN の購読から `OnData` までのコードを書き直し、購読を作る所(`AddCryptoFuture` から `LookupSubscriptionConfigDataTypes` まで)は場面集の側が `Scene.input` から組んだ。第 r7-1 回の i0-r6-04 の直しは、再現が引いていた一次資料の行の中の条件にだけ「囲む条件も 1 対 1」を当てた。
2. **「対象の型」を、設定の違う実行の和で決める規則にしていた。**runner は実行ごとの設定を書き残しも照合もしない(`run_battery.py` の `target_types` と `run_for_types`)。その和を `type_plan` の欄を持つ場面の入力に使うと、1 つの設定では作れない型の組を求め、adapter・再現がそれを組めば対象の振る舞いとして採点される。
3. **第 r6-3 回の帰属の検め(DEFINITIONS.md「判定」の「出所の検め」の (b))が物だけを見て、設定を見ていなかった。**

### なぜ起きたか(dr8-1-3-1)

1. **L-438 (2) の文を切片に切らずに、「型の組は 1 つの設定つき対象の p3 から決める」の 1 点に縮めて読んだ。**3 回目の版は L-438 (2) を、リードの答え §7 の 3・5(型の組を 1 つの設定に限る)の側からだけ読み、L-438 (2) の 4 つの条件(規則と式の文・materials の記録・表の注記の 1 文・値が不一致の型を数えない)を正の定義 A に入れなかった。正の定義 A を帰属の族の正の定義として書き始めたので、帰属と別の条件(記録・注記・設定つき対象の持つ型の数え方)が正の定義の外に落ちた。
2. **「当てた」と申告する前に、決まりの切片と書いた所を並べる手順が無かった**(§1、正の定義 0)。
3. 今のコードの `target_types` が設定つき対象の持つ型に当たる物を状態と型の名で決めていること(`…_typerule_checks.txt`)は、第 r7-1 回に L-438 より前に作った規則のままで、L-438 のあとに照らし直していなかった。

### 前の回で外した根本原因

- **dr8-1-1-3**(1 回目): 「設定」を時点で書いた。設定を対象に帰す理由(利用者が公開の手段で行った操作であること)で書かなかった。
- **dr8-1-1-4**(1 回目): 表の行と対象の区切りを書かず、寄せる相手が道具の間か設定の間かを決めていなかった。
- **dr8-1-2-3**(2 回目): 正の定義 A の本文は (3) で母語のコードの由来を認めていたのに、「直す所」の試験の格子の軸は思い出した作り手 4 つで書いた。
- **dr8-1-3-6・dr8-1-3-1**(3 回目): 試験の軸を正の定義 A の文から機械で書き出さずに手で列べた(リードの答え §7.1 の 9 で、直しの前の定義の段では暫定表として認められた)。L-438 (2) の条件を正の定義 A に入れなかった(上の「なぜ起きたか(dr8-1-3-1)」)。

### 正の定義 A(監査役に先に通す段落)

**対象の振る舞いと数える物**: 場面の結果として対象に帰すのは、次の 3 つが全部成り立つ実行で出た物だけである。**(1) 設定**: その実行の中で利用者が対象の公開の手段で行った設定の操作の全部を、行った順に並べた物であること(データの購読・事象の型の組・解像度・同時刻の並びの引数・差し込む模型と口座を作り、足し、替える操作。実行の開始前に限らず、実行の途中に公開の API で購読や事象の型を足し替える操作も入る)。場面集の側は、操作ごとに利用者のどの公開の呼び出しかを対象の配布物のコードの行(再現なら一次資料の行)で示し、いつ(開始前か、どの戦略の呼び出しの中か、ほかの時点か)行ったかを書き残す。どの操作も、実行の前に利用者が知りうる物(場面が名指す銘柄と事象の型・その設定つき対象の設定の値・対象の公開の既定)と、その操作までに戦略が (3) の届き方で受けた物だけから決まること。公開の手段では 1 つの実行の中で続けて行えない操作を寄せ集めた物は、1 つの設定ではない。**(2) 仕事**: 場面が測る仕事(事象に型を付ける・時刻を変換する・並べる・過去を読ませる・通知を作る・模型を呼ぶ)を、対象の配布物のコード(母語のコードを含む)が行ったこと。再現では、利用者の公開の入口から戦略の呼び出しまでのコードの全部を一次資料の行に 1 対 1 で書き直した物(書き直した文を囲む条件を含む。書き直さない条件は、場面で成り立たない理由を一次資料の行で示す)が行ったこと。場面集の側が行ってよいのは、場面の入力の値を対象の公開の入口が受け取る値に写すこと、公開の手段を呼ぶこと、公開の差し込む口に利用者の実装を渡すこと、返ってきた物を場面の語に写すことだけである。**(3) 届き方**: 戦略が受けた物は、戦略の 1 回の呼び出しが始まってから戻るまで(コルーチンなら最初に入ってから終わるまで)の間に、対象の配布物のコード(母語のコードを含む)が戦略に引数として渡した物かその中にあった物、または戦略がその間に対象の関数を呼んで返ってきた物であり、その物を作ったのも対象の配布物のコードであること。**数えない物**: adapter・再現の外側・再現のうち一次資料の行に当たらない工夫が作った物(`dict`・関数・対象の class の実体を含む)、公開の手段で作れない設定か操作の寄せ集めの下で出た結果、まだ届いていない入力の時刻・順・件数に合わせて決めた操作の下で出た結果(並べる仕事を場面集の側がしたことになる)、戦略の呼び出しの外で対象の関数を呼んで得た物、戦略が自分で貯めた値、場面集の側が呼んだ変換の値、そして (1)〜(3) のどれにも当たると示せない物(既定は数えない)。これらで決まった場面は採点しない(結果なし、理由を残す)。**設定つき対象と表の行**: 設定つき対象 = 対象 × 設定の値(設定の操作のうち、場面の入力が決めない部分 = 利用者が選ぶ値の組み合わせ。場面の入力が決める遅れの値・差し込むダミーでは分けない)。1 つの設定つき対象の場面は全部同じ設定の値で走らせ、設定つき対象ごとに runner の出力に別々に残し、どの対象のどの設定の値かを materials に書く。表の行「調査結果の側」は、委任文 §3 どおり場面ごとに、走らせた設定つき対象の最良を寄せる。これは場面ごとに設定つき対象の間から選ぶことで、どのセルも 1 つの設定つき対象の実際の実行の結果であり、表の行は 1 つの道具・1 つの設定の振る舞いを名乗らない。新実装・当方の現状・試金石の表の行は、それぞれ実行の前に決めた 1 つの設定つき対象だけから作る。**型の組(L-438 (2))**: 事象の型を測らない観点の場面(`Scene.type_plan` の欄を持つ場面)の入力の型の組は、設定つき対象ごとに、その設定つき対象の持つ型から規則で決める。設定つき対象の持つ型 = その設定つき対象の同じ実行の p3 の場面のうち、出所の検め(この正の定義の (1)〜(3))を通って採点された正しさが「正解と一致」の場面の事象の型である。値が不一致の型(その型の p3 の場面の正しさが「正解と一致」でない型。届いたが値が違う型、対応なし・結果なしの型を含む)は持つと数えない。設定つき対象の違う実行の結果の和にもしない(1 つの場面の入力と結果は、1 つの設定つき対象の実行から出る)。設定つき対象の持つ型から型の組を決める規則と、型の組から期待を出す式は、DEFINITIONS.md に、対象を見る前の文として書く(規則と式は対象の出力を読まない)。設定つき対象ごとに選んだ型の組は materials に記録する。審査員が読める表の注記に「型の組は対象の持つ型から規則で決めた」と 1 文書く。この正の定義は新実装・当方の現状・調査結果の側・再現のすべてに同じく当てる。**この正の定義を確かめる試験の入力の空間は、この段落の選択肢と「数えない物」の全部に「どれにも当たらない」値を足して作り(正の定義 0)、期待はこの段落の条件だけから出す。**

リードの答えとの突き合わせ: §7-4 を (1) に、§7-5 を「設定つき対象と表の行」に、L-438 (2) と LEAD_DESIGN.md §4(br7-1-1 の条件 (a)(b)、br7-1-2 の「数えない」)を「型の組」に書いた。§7-5 の「候補」の語は、道具台帳の候補と分けるため「設定つき対象」と書いた。**リードの答えに無く場面係が足したもの**: (i) 操作を決めてよい物の条件、(ii) 設定つき対象を分けるのは利用者が選ぶ値だけ、(iii) 新実装・当方の現状・試金石の表の行は 1 つの設定つき対象だけ、(iv) 作り手の条件((3) の最後の文)、(v) どれにも当たると示せない物は数えない、(vi) 試験の入力の空間の作り方(最後の文)、(vii) 設定つき対象の持つ型の「正解と一致」を、出所の検めを通って採点された正しさで決めること(L-438 の「値が不一致」を、正しさの欄の 4 つの値のうち「正解と一致」以外の全部と読んだ。対応なし・結果なしの型を設定つき対象の持つ型に数えないのは場面係の読み。この版で足した)。

L-438 の切片ごとに、このファイルのどこに書いたかと、切片の数と判断の数が等しいことの出力: `…_l438.txt`。

### 当てたときに直す所(この段では直さない。直しの段で試験を先に書く)

- `repro_lean52.py` の `_sources` と `lean52.data_manager_add`: 購読を `Scene.input` から組むのをやめ、利用者の公開の入口(`AddCryptoFuture` の解像度)から `LookupSubscriptionConfigDataTypes` の式どおりに型の組を作る。解像度ごとに別の設定つき対象として走らせる。未確認の 3 点は (b) で一次資料を読んで決め、読めなければ理由を書く。
- runner: 設定つき対象ごとに走らせて残す。adapter に設定の操作(公開の呼び出し・一次資料の行か配布物のコードの行・時点・決めた物)を名乗らせ、正の定義 A (1) を満たさない実行は採点しない。時点は書き残すだけで、時点だけでは採点から外さない。
- runner の `target_types`: 設定つき対象の持つ型を、同じ設定つき対象の p3 の場面の採点された正しさ(`correctness` の「正解と一致」)から決める(今の、状態と型の名で決める書き方を替える)。`run_target` の順は p3 の場面を先に採点してから `type_plan` の欄を持つ場面を走らせる。
- `scenes.py` の `TYPE_RULE` と DEFINITIONS.md の文: 設定つき対象の持つ型の決め方を正の定義 A の「型の組」の文に替える(対象を見る前の文)。
- materials: 資料係の表を作るスクリプトが、設定つき対象ごとの型の組を runner の出力から写した記録を残す(資料係の持ち物なので、場面係は runner の出力に設定つき対象ごとの型の組の欄を置き、資料係への申し送りに書く)。
- 表の注記: 「型の組は対象の持つ型から規則で決めた」の 1 文を、表を作るスクリプトが注記に必ず出し、試験がその 1 文の有無を見る(資料係への申し送り。場面集の側は、場面集の試験で申し送りの文を持つ)。
- `adapters/common.py` の `read`・`attr`・`iterate`・`aiterate` と runner の `delivered`: 戦略の呼び出しの中で読まれたことと、物の作り手を書き残す。呼び出しの外で読んだ物に `returned_by` を付けない(br6-3-2)。
- DEFINITIONS.md の「共通の決まり」の事象の型の段落と「出所の検め」の (b) の文: 正の定義 A の選択肢を 1 つのデータ(場面集の中の 1 つのモジュール)に書き、DEFINITIONS.md の文と試験の格子の軸を**両方ともそのデータから作る**。
- 表への影響は**未測定**。直しの段で設定つき対象ごとに走らせ直して出す(ここに見込みを書かない。正の定義 B)。

敵対者の試験(**手で列べた暫定表。直しの段で正の定義 A の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**。リードの答え §7.1 の 9 の逐語):

| 正の定義 A の節 | 軸 | 値 |
|---|---|---|
| (1) | 操作の出所 | 配布物のコードの行か一次資料の行で示せる公開の呼び出し / 示せない操作 / どれにも当たらない |
| (1) | 操作の時点 | 開始前 / 戦略の呼び出しの中 / ほかの時点 / どれにも当たらない |
| (1) | 操作を決めた物 | 場面が名指す銘柄と事象の型 / 設定つき対象の設定の値 / 対象の公開の既定 / 戦略が (3) で受けた物 / まだ届いていない入力 / どれにも当たらない |
| (1) | 1 つの実行で続けて行えるか | 行える / 行えない寄せ集め / どれにも当たらない |
| (2) | 測る仕事をしたコード | 配布物のコード(Python)/ 母語のコード / 一次資料の行に 1 対 1 の再現 / 一次資料の行に当たらない再現の工夫 / 場面集の側の許された 4 つ / 場面集の側のそれ以外 / 戦略 / どれにも当たらない |
| (3) | 物の作り手 | 配布物のコード(Python)/ 母語のコード / adapter / 再現の外側 / 一次資料の行に当たらない再現の工夫 / 場面集の側が呼んだ変換 / 戦略 / どれにも当たらない |
| (3) | 受け渡し | 配布物のコードが引数で渡した / その中にあった / 母語のコードが渡した / 戦略が対象の関数を呼んで返ってきた / 戦略が自分で貯めた / 場面集の側が渡した / どれにも当たらない |
| (3) | 受けた時 | 戦略の 1 回の呼び出しの中(コルーチンは最初に入ってから終わるまで)/ 呼び出しの外 / どれにも当たらない |
| 設定つき対象と表の行 | 表の行 | 調査結果の側 / 新実装 / 当方の現状 / 試金石 / どれにも当たらない |
| 型の組 | p3 の場面の設定つき対象 | 同じ設定つき対象 / 違う設定つき対象 / 設定つき対象の違う結果の和 / どれにも当たらない |
| 型の組 | p3 の場面の正しさ | 正解と一致 / 対応なし / 不一致 / 結果なし / 出所の検めで採点しない / どれにも当たらない |
| 型の組 | 規則と式の置き場所 | DEFINITIONS.md の対象を見る前の文 / 対象の出力を読む規則 / 書いていない / どれにも当たらない |
| 型の組 | 記録と注記 | materials の記録と表の注記の 1 文の両方 / 片方だけ / どちらも無い / どれにも当たらない |

  全部の升目で偽の対象を作り、runner が「対象の物」と数え、設定つき対象の持つ型に数えるのが正の定義 A の条件を全部満たす升目だけであることを確かめる。期待は正の定義 A の条件だけから出す(runner の場合分けから作らない)。正の定義 A の文そのものが落とした由来はこの試験でも拾えない(限界。批評家・監査役が正の定義 A を読んで確かめる)。

## 4. 正の定義 B: 書き写した文が元とずれる族(i0-r7-04・dr8-1-1-1・dr8-1-2-1)

### 確かめたこと

- 場面集の追跡されたファイルの一覧: `<scratchpad>/bt/r8-1/item0_r8-1-2_scenekeeper_def_files.txt`。場面 id の出現の数え(範囲 = `git ls-files tests/bt/battery/item_0` の全部、CLAUDE.md §3 のやり方): `…_sceneid_counts.txt`。
- 生成物の中の手で書いた文: `gen_definitions.py` の 26 行の文字列(場面 id と観点ごとの場面の数を手で書いた文)は DEFINITIONS.md に写る。`test_battery_item0.py` の `test_definitions_in_sync_with_scenes` は DEFINITIONS.md と生成器の出力の一致だけを見る。`scenes.py` の値と並べた出力: `…_gen_prose_check.txt`(今食い違っているかは判定していない。照らす機械が無いこと自体が穴)。
- 2 回目の版の自己検査の grep の語(`…_selfcheck.txt`、2 回目)と、監査役が挙げた 2 回目の版の文(`…_prev_version_2.md` の 37 行)。

### なぜ起きたか(i0-r7-04)

1. **場面の中身を、手で書く文書に書き写していた。**0#5-1 の直し(DEFINITIONS.md を `scenes.py` から作る)は DEFINITIONS.md の値の部分だけを閉じ、検討表・再現の注釈・生成器の手の文には当てなかった。
2. **同じ根の全箇所を、思い出した場所の一覧で探した。**同じ漏れが `opponents/repro_lean52.py` の docstring にもある。
3. **古い文を落とす機械が 1 つの書き方の文しか見ない**(`test_no_stale_best_claim_in_the_review_table`)。

### 前の回で外した根本原因

- **dr8-1-1-1**(1 回目): 当てる範囲を例で書き、ROOTCAUSE*.md が外れた。正の定義を自分の成果物に当てなかった。生成物を丸ごと書いてよい所にした。
- **dr8-1-2-1**(2 回目): 「場面の中身」を語の一覧で決め、場面の集まりについての文(ある欄を持つ場面の数)が一覧の外になった。自己検査の範囲も同じ語の一覧から作った。
- 3 回目の版: 正の定義 B の本文への指摘は無い。「当てたときに直す所」に敵対者の試験の表が無かった(dr8-1-3-3。この版で暫定表を置いた)。

### 正の定義 B(監査役に先に通す段落)

**場面集の文が場面について述べてよいこと**: 当てる範囲は場面係が書く全部の文 = 場面集の置き場所(`git ls-files tests/bt/battery/item_<番号>` と、まだ追跡されていない書いた物)の全ファイルの全行(ROOTCAUSE*.md・検討表・README の類・コードの注釈と docstring・生成器の中に手で書いた文を含み、ファイルの種類・名前・書いた周で除かない)と、場面係の返り値である。**元**は、場面集の中で値が変わりうる物 = 場面の定義(`scenes.py` の値)と runner の出力(`survey_results/`)、およびそれらから機械で作った物である。**場面の中身**は、真偽が元の今の値に依る言明の全部であり、どの語で書かれているかでは決めない: 1 つの場面の欄の値(入力・期待・結果)だけでなく、場面の集まりについての言明(ある欄を持つ場面の数と id、観点ごとの場面の数、対象ごとの結果の数、「どの場面も…」)を含む。見分け方は「元の値が変わったら、その文は偽になりうるか」で、なりうる文は場面の中身である。場面の中身を文で持ってよいのは、(i) 元の値から機械で作り、元と食い違えば試験が落ちる部分(生成器に手で書いた文は、生成物に写ってもこれに当たらない)と、(ii) 試験がその文の名指す場面 id の今の元(版を固定した文ならその版の元)から作り直して一致を確かめる定型の文、だけである。それ以外の文は場面の中身を述べず、場面 id と欄(版を固定するなら git の版)を名指す参照だけを持つ。照らせない書き方の場面の中身は、正しくても、過去の周の文書の中でも置かない(過去の周の文書は、その周の版を固定した参照に直す)。コマンドの出力は、打った版と一緒に場面集の外のファイルに置き、文はその path を名指し、出力の値を文に写さない。機構の読み・一次資料の行・判断とその理由・場面集のコードの振る舞い(関数名で指し、読んだ版を書く)は場面の中身ではない。この正の定義を当てる検査は、当てる範囲の全行を行ごとの判断で覆い(正の定義 0)、探す語の一覧で当てる範囲を狭めない。場面の定義を変えたときの同じ根の探しも、場面集の全ファイルの全行について行う。

リードの答えとの突き合わせ: §7-6(ROOTCAUSE*.md を含める。場面 id と欄の参照で書く)。**場面係が足したもの**: 場面の中身を真偽の依り方で決めること、元を「値が変わりうる物」と書いたこと、出力の値を文に写さないこと(いずれも 3 回目)。この版で本文は変えていない。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: §7 の自己検査(全行の判断)。
- 場面集のほかの全ファイル(過去の ROOTCAUSE*.md・検討表・adapter と再現の注釈・runner と試験の注釈を含む全部): 全行を行ごとの判断で読み、場面の中身を述べる文を、版を固定した参照・今の元への参照・試験が照らす定型の文のどれかに直す。
- `gen_definitions.py` の手で書いた文: 場面 id の一覧と観点ごとの場面の数を、`scenes.py` の値から作る式に置き換える。
- 機械の試験: 場面集の手で書く全ファイルの全行を書き出し、場面 id・`Scene` の欄名・数字を持つファイルの行に印を付け、印の付いたファイルの行は定型の文に当たって照らしが通るか、判断の出力に「参照だけ」と書かれているかのどちらかでなければ落とす(既定は落とす)。印の無いファイルの行も判断の出力に載り、判断の数とファイルの行の数が等しくなければ落とす。**限界**: 文の真偽が元に依るかは機械では決まらない。機械が保証するのは、全行が判断の出力に載ることと、印の付いたファイルの行が既定で落ちることまでで、判断の中身は批評家・監査役が読んで確かめる。

敵対者の試験(**手で列べた暫定表。直しの段で正の定義 B の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**。リードの答え §7.1 の 9 の文の「正の定義 A」を「正の定義 B」に替えた物。替えたのは場面係):

| 正の定義 B の文 | 軸 | 値 |
|---|---|---|
| 当てる範囲 | 文の置き場所 | ROOTCAUSE*.md / 検討表 / README の類 / コードの注釈と docstring / 生成器の中に手で書いた文 / 生成物 / まだ追跡されていない書いた物 / 場面係の返り値 / どれにも当たらない |
| 場面の中身 | 文の述べる物 | 1 つの場面の欄の値 / 場面の集まり(数・id・「どの場面も…」)/ 機構の読み / 一次資料の行 / 判断とその理由 / 場面集のコードの振る舞い / コマンドの出力の値の写し / どれにも当たらない |
| 文で持ってよい | 持ち方 | (i) 元から機械で作り試験が照らす / (ii) 定型の文で試験が照らす / 参照だけ(場面 id と欄、版)/ 照らせない書き方 / どれにも当たらない |
| 元 | 元の版 | 今の元 / 版を固定した元 / 版を書かない / どれにも当たらない |
| 当てる範囲の全行 | 判断の持ち方 | 全行に 1 つずつ / 判断の無いファイルの行がある / どれにも当たらない |

  期待は正の定義 B の文だけから出す: 落ちるのは、文の述べる物が「1 つの場面の欄の値」「場面の集まり」「どれにも当たらない」で持ち方が「照らせない書き方」「どれにも当たらない」の升目と、「コマンドの出力の値の写し」の升目と、判断の持ち方が「判断の無いファイルの行がある」の升目。機械の試験に全部の升目の偽のファイルを渡し、この期待どおりに通し・落とすことを確かめる。文の真偽が元に依るかの判断そのものは機械で決まらない(上の限界)。

## 5. 正の定義 C: 観点の範囲を要件の名詞から列べていない族(i0-r7-05・dr8-1-1-2・dr8-1-2-4・dr8-1-3-2)

### 確かめたこと

- 升目の軸の書き出し(この版): 固定した要件 §1 の要件の表の行(4 列目)と §2 の要件の表の各行(2 列目・3 列目)を区切りの字で切片に切り、切片ごとに判断(事象 / 見る道 / 観点の追加の軸 / 軸にしない + 理由)を付け、判断から軸を作り、下の軸の表と一致することを確かめた出力: `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_axes_c.txt`(道具は同じ置き場所の `…_axes_c.py`・`…_axes_c_judgments.tsv`・`item0_r8_1_4_segs.py`)。観点ごとの升目の数もこの出力にある。
- 通知の遅れの試し(批評家の probe を、直しの前の定義の 1 回目の段で走らせ直した): `<scratchpad>/bt/r8-1/item0_r8-1_scenekeeper_def_probe_notice.out`。
- DEFINITIONS.md の「場面にしていない観点・側面」の節の手で書いた項目(`…_checks.txt` の grep、3 回目の出力)。どれも升目を列べずに書いた申告である。
- `Scene` の欄(`…_checks.txt`)に、場面がどの事象をどの見る道で見るかの欄は無い。

### なぜ起きたか(i0-r7-05)

1. **観点の範囲を、`Scene.input` に書ける事象の種類から作った。**要件の名詞(§1 の事象の型・戦略の API)の全部から作る手順が無く、`Scene.input` に書けない事象(対象が注文から作る事象)と、事象の配達以外の見る道が抜けた。
2. **見る道を列べる手順が無かった**(`Scene` に欄が無い)。

### 前の回で外した根本原因

- **dr8-1-1-2**(1 回目): 正の定義 C を指摘の観点と思い出した観点にだけ当て、軸を要件の名詞から作る規則が無かった。
- **dr8-1-2-4**(2 回目): 「直す所」の小見出しを正の定義 A・B にだけ置いた。
- **dr8-1-3-2**(3 回目): 軸を「要件の名詞から作る」と書きながら、要件の文を切片まで書き出さず、読んだ記憶から列べた(§1、正の定義 0)。その結果、要件に名詞の無い分け方(戦略が読む物を「事象そのもの」と「事象から対象が作る物」に分ける)が軸に入っていた。この版の書き出しでは、その分け方の出所になる切片が無い(`…_axes_c.txt` の判断の全部)。

### 正の定義 C(監査役に先に通す段落)

**観点の場面が覆う範囲**: 7 観点(P0-1〜P0-7)の全部に同じ方法で当てる。升目の軸は、固定した要件の文から機械で書き出す: 書き出す文は §1 の要件の表の行(4 列目)と §2 の要件の表の各行(2 列目・3 列目)の全部で、これを区切りの字で切片に切り、切片の 1 つずつに次のどれか 1 つの判断を付ける。**事象** = §1 の事象の型と、各観点の文が名指す事象の型(通知は §1 の語どおり受付・拒否・約定の 3 つ)。**見る道** = 戦略の API の名(事象ごとの呼び出し・発注・取消)と、戦略が読む動作を名指す語から作る、戦略の呼び出しに届く物 / 発注の呼び出しがその場で返す物 / 取消の呼び出しがその場で返す物 / 戦略が対象の公開の手段で読む物。**観点の追加の軸** = その観点の文が事象と見る道のほかに列べて名指す名詞(P0-2 の時刻の単位、P0-7 の差し込む口の類)。**軸にしない** = 列べた要素をまとめる名・観点の性質そのもの・測り方の手順と結果の側の語・作りの形の名(場面集の規則 2)・文の繋ぎの類で、判断に理由を書く。軸と値は判断からだけ作り、判断の無い切片・切片に無い値があれば試験が落ちる。観点の文(§2 の 2 列目)が限らない軸は、全部がその観点の範囲に入る。観点ごとに範囲の全部の升目を列べ、各升目を「場面にした(場面 id)」か「場面にしない(理由)」のどちらかにする。場面にするかは、その観点の固定した測り方(§2 の 3 列目)の文の語で決め、場面にしない理由には外れる語を引く。測り方の文の語で中か外かが決まらない升目は場面係が決めず、リードの決定に従い(理由に決定の出所を書く)、決定の無い升目はリードに上げる(要件と観点の読みを直すのはリード = 委任文 §3「要件と判定の固定」)。この表は DEFINITIONS.md の「場面にしていない観点・側面」として、場面の定義と升目の軸から機械で作り、範囲の升目が 1 つでも表に無ければ試験が落ちる(升目を列べない申告は置かない)。場面は、その升目について観点の性質だけで結果が決まり、ほかの観点が測るもの(事象の型の有無・差し込む口の有無)で決まらないように作る: 遅れは公開の設定の値でも差し込む口でもよく、事象の型が足りない対象は正の定義 A の「型の組」の規則(設定つき対象の持つ型が最低の数に足りない設定つき対象は対応なし、理由に p3 の結果)に従う。

今の書き出しで出た軸(判断から作った物。下の表の各行を `…_axes_c.py` が判断と照らし、一致を `…_axes_c.txt` に出す。軸の値の出所の切片の番号は `…_axes_c_judgments.tsv`):

| 印 | 軸 | 値 |
|---|---|---|
| C軸 | 事象 | 約定 |
| C軸 | 事象 | 板の写真 |
| C軸 | 事象 | 板の差分 |
| C軸 | 事象 | 足 |
| C軸 | 事象 | 資金調達 |
| C軸 | 事象 | 清算 |
| C軸 | 事象 | 時計 |
| C軸 | 事象 | 注文の受付の通知 |
| C軸 | 事象 | 注文の拒否の通知 |
| C軸 | 事象 | 注文の約定の通知 |
| C軸 | 見る道 | 戦略の呼び出しに届く物 |
| C軸 | 見る道 | 発注の呼び出しがその場で返す物 |
| C軸 | 見る道 | 取消の呼び出しがその場で返す物 |
| C軸 | 見る道 | 戦略が対象の公開の手段で読む物 |
| C軸 | P0-2 の 時刻の単位 | int64 ナノ秒 |
| C軸 | P0-2 の 時刻の単位 | 秒 |
| C軸 | P0-2 の 時刻の単位 | ミリ |
| C軸 | P0-2 の 時刻の単位 | ISO 文字列 |
| C軸 | P0-7 の 差し込む口 | 約定模型 |
| C軸 | P0-7 の 差し込む口 | 遅延模型 |
| C軸 | P0-7 の 差し込む口 | 費用 |
| C軸 | P0-7 の 差し込む口 | 口座 |

リードの答えとの突き合わせ: §7-7(7 観点全部を事象 × 見る道で列べ、表を機械で作る)、§7-2(通知・時計の升目は場面にしない。理由の文はリードの文)。**リードの答えの「事象 8 種 × 見る道 3 種」と違う所**: 事象は通知を 3 つに分けた(§1 の切片が受付・拒否・約定の通知の 3 つ)。見る道は 4 つ(戦略の API の 3 つの名の切片と、P0-4 の 3 列目の「読もうとする」の切片から)。§1 の「見られない」と P0-4 の 2 列目の「見えない」は、見る道の全部に掛かる動詞と判断し、見る道の値にしなかった(判断は `…_axes_c_judgments.tsv`)。3 回目の版の「読む物を 2 つに分ける」は、出所の切片が無いので外した: 分けていた 2 つの物は、どちらも「戦略が対象の公開の手段で読む物」の升目に入り、升目から外れる物は無い(i0-r7-05 の升目 = 約定の通知 × 事象から対象が作る物を読む、は「注文の約定の通知 × 戦略が対象の公開の手段で読む物」の升目になる)。

### 当てたときに直す所(この段では直さない。直しの段で試験を先に書く)

- `scenes.py` の `Scene`: 場面が覆う升目(観点・事象・見る道・観点の追加の軸)の欄を足し、全部の場面にその値を書く。表の「場面にした」はこの欄から作る(手で場面 id を書かない。正の定義 B)。
- 升目の軸: この版の書き出しの道具(`…_axes_c.py` と判断の TSV)を場面集の中の 1 つのモジュールに移し、要件のファイルの行と列と切片の番号つきで軸を作る。DEFINITIONS.md の表と試験の両方をそこから作る。要件のファイルが変われば切片が変わり、判断の無い切片で試験が落ちる。
- 場面にしない升目の理由: 測り方の文の外れる語の引用か、リードの決定の出所(LEAD_DESIGN.md §7 の 2 ほか)を、升目ごとに同じデータに書く(判断とその理由なので正の定義 B の場面の中身ではない)。決定の無い升目は「リードに聞くこと」に上げ、その升目は表で「未決」と出す。
- `gen_definitions.py`: DEFINITIONS.md の「場面にしていない観点・側面」の節を、上のデータと `Scene` の新しい欄から作る。今の手で書いた項目を消す。
- 試験: (a) 7 観点それぞれの軸の直積の全部の升目が表にちょうど 1 回ずつ出ること、(b) 「場面にした」升目の場面 id の新しい欄がその升目を含むこと、(c) 「場面にしない」升目の理由が、引いた語をその観点の測り方の文に含むか、リードの決定の出所を持つこと、(d) 要件の文の切片の全部に判断が 1 つずつあること。軸の直積は試験の中で要件のデータから独立に作り、生成器の出力から作らない。
- 「場面にする」と決まった升目の場面の追加: 表を作ってから決める(ここに見込みを書かない。正の定義 B)。
- 測り方の文の字面で中か外かが決まらない升目の読みは、§8 の 2・3。リードの答えを受けてから、上のデータの理由に書く。

敵対者の試験(**手で列べた暫定表。直しの段で正の定義 C の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**。リードの答え §7.1 の 9 の文の「正の定義 A」を「正の定義 C」に替えた物。替えたのは場面係):

| 正の定義 C の文 | 軸 | 値 |
|---|---|---|
| 切片の判断 | 切片と判断 | 全部の切片に 1 つずつ / 判断の無い切片 / 切片に無い判断 / 1 つの切片に 2 つ / どれにも当たらない |
| 軸と値は判断からだけ作る | 値の出所 | 判断から作った / 判断に無い値を足した / 判断にある値を消した / どれにも当たらない |
| 観点ごとに範囲の全部の升目 | 升目の表での出方 | ちょうど 1 回 / 表に無い / 2 回出る / 範囲の外の升目が出る / どれにも当たらない |
| 場面にした(場面 id) | 場面の欄 | 欄がその升目を含む / 含まない / 欄が無い / どれにも当たらない |
| 場面にしない(理由) | 理由 | 引いた語が測り方の文にある / 引いた語が測り方の文に無い / リードの決定の出所がある / 出所が無い / 未決 / どれにも当たらない |

  期待は正の定義 C の文だけから出す: 通るのは、切片と判断が「全部の切片に 1 つずつ」、値の出所が「判断から作った」、升目が「ちょうど 1 回」で、場面にした升目は欄が升目を含み、場面にしない升目は「引いた語が測り方の文にある」「リードの決定の出所がある」「未決」のどれかの升目だけ。試験 (a)〜(d) に全部の升目の偽のデータを渡し、この期待どおりに通し・落とすことを確かめる。

## 6. 正の定義 D: 語の意味の族(dr8-1-2-2・dr8-1-3-4)

### 確かめたこと

- 「単位」の出現(範囲 = 場面集の全ファイル + 要件・委任文・リードの設計・CLAUDE.md): `<scratchpad>/bt/r8-1/item0_r8-1-3_scenekeeper_def_term_tan-i.txt`。
- 2 回目の版の中で意味を持たせた語のほかの意味: `…_term_senses_old.txt`。
- この版の用語の表の全部の語の、範囲の中の出現(ファイルごとの数)と、新しい語と「母語のコード」の出現ごとの判断: `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_terms.txt`。
- 3 回目の版の用語の表のうち、3 列目が空(「—」)だった語: `…_prev_version_3.md` の用語の表(「母語のコード」と「行う / 列べる / 組む」の 2 つ)。

### なぜ起きたか(dr8-1-2-2)

1. **O-7 の照らしを、言い換える前の語(候補)にだけ当てた。**新しい語(単位)の範囲の中の出現を引かなかった。
2. **文書が意味を持たせた語を書き出す手順が無かった。**
3. 2 回目の版の §5 の吟味の表の「O-7」は、配慮した事実だけを書き、照らしたコマンドと出力を持たなかった(O-3)。

### なぜ起きたか(dr8-1-3-4)

1. **正の定義 D の語の区分が 2 つ(要件と委任文の語 / 新しい語)で、範囲の中のほかの文書(場面集の過去の周・VERDICTS・リードの設計)ですでに意味を持つ語の区分が無かった。**「母語のコード」は場面集の過去の周の語で、どちらの区分にも入らず、区分ごとの確かめ(意味を守る / 出現が無いことを確かめる)のどちらも当たらなかった。
2. **用語の表の行ごとに区分と確かめの出力を持たせる欄が無かった。**3 列目は「別の意味があると知っていた語」にだけ書き、用語の表の行の全部に正の定義 D を当てたかを数えていなかった(正の定義 0 の違反)。

### 前の回で外した根本原因

- **dr8-1-3-4**(3 回目): 上の「なぜ起きたか(dr8-1-3-4)」。正の定義 D の本文の区分の穴なので、この版で本文を直した(下の段落の「範囲の中ですでに意味を持つ語」)。

### 正の定義 D(監査役に先に通す段落)

**語の意味**: 場面係が書く文(正の定義 B の当てる範囲と同じ)では、1 つの語は 1 つの意味だけを持つ。語の意味を比べる範囲は、その文と一緒に読まれる文書の全部 = 場面集の全ファイル・固定した要件・委任文・リードの設計・項目 0 の VERDICTS・CLAUDE.md である。語は次の 3 つの区分のどれか 1 つに入れる。**要件・委任文の語**はその意味でだけ使う。**範囲の中の既存の語**(範囲の中のほかの文書 = 場面集の過去の周・リードの設計・VERDICTS・CLAUDE.md で、すでに意味を持つ語)は、範囲の中の全部の出現を書き出して出現ごとにどの意味かを判断し、意味が 1 つならその意味でだけ使い、2 つ以上なら修飾を付ける。**新しい語**は、範囲の中で別の意味の出現が無いことを、範囲を切らずに引いたコマンドと出力で確かめてから使う。範囲の中で 2 つ以上の意味を持つ語は裸で使わず、意味を決める修飾をいつも付ける(例: 表の行 / 一次資料の行、時刻の単位)。語に意味を持たせる文書は、意味を持たせた全部の語を 1 つの用語の表に書き、表の行ごとに区分と、区分の確かめの出力の path を持つ(区分の無い表の行・出力の無い表の行を置かない)。表の語の全部の出現を書き出して(正の定義 0)、出現ごとに表の意味で使っているかを判断する。語を言い換えたときは、言い換えた後の語の範囲の中の全部の出現も確かめる(1 か所の言い換えが新しい衝突を作る = dr8-1-2-2)。

リードの答えとの突き合わせ: リードの答えに無く、場面係が足した(CLAUDE.md §0.2 O-7 を場面集の文に当てる手順として)。この版で足したもの: 区分を 3 つにしたこと(範囲の中の既存の語)、表の行ごとに区分と出力の path を持つこと(dr8-1-3-4)。

### 当てたときに直す所(この段では直さない。このファイルだけは今回の版で当てた)

- このファイル: 冒頭の用語の表に区分の列を足し、全部の表の行に区分と出力の path を書いた。表の全部の語の範囲の中の出現は `…_terms.txt`、このファイルの中の出現と出現ごとの判断は `…_selfcheck.txt` の (2)。
- 場面集のほかの全ファイル: 意味を持たせた語(DEFINITIONS.md の語と、このファイルの用語の表の語)の全部の出現を書き出し、出現ごとに判断し、2 つ以上の意味の出現に修飾を付ける。DEFINITIONS.md・`scenes.py`・`gen_definitions.py` の「単位」の出現の書き出しは `…_checks.txt`(3 回目の出力)にある。要件の逐語はそのまま引き、ほかの意味の側を言い換える。
- 機械: 出現の書き出しと、出現ごとの判断の表(語・ファイル・ファイルの行・判断した意味)を持ち、判断の無い出現・用語の表に無い意味の判断・区分の無い用語の表の行があれば落とす。意味の判断そのものは読む者がする(限界)。

敵対者の試験(**手で列べた暫定表。直しの段で正の定義 D の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**。リードの答え §7.1 の 9 の文の「正の定義 A」を「正の定義 D」に替えた物。替えたのは場面係):

| 正の定義 D の文 | 軸 | 値 |
|---|---|---|
| 3 つの区分 | 用語の表の行の区分 | 要件・委任文の語 / 範囲の中の既存の語 / 新しい語 / 区分なし / どれにも当たらない |
| 区分の確かめの出力 | 表の行の出力 | path があり出力が区分の確かめを持つ / path が無い / どれにも当たらない |
| 出現ごとに判断 | 出現の判断 | 表の意味 / 別の意味で修飾あり / 別の意味で修飾なし / 判断なし / どれにも当たらない |
| 新しい語 | 範囲の中の別の意味 | 0 件の出力あり / 別の意味の出現あり / 出力なし / どれにも当たらない |
| 言い換え | 言い換えた後の語 | 言い換えなし / 全部の出現を確かめた / 確かめていない / どれにも当たらない |

  期待は正の定義 D の文だけから出す: 落ちるのは、区分なし・path が無い・別の意味で修飾なし・判断なし・新しい語の別の意味の出現ありか出力なし・言い換えて確かめていない、と「どれにも当たらない」の升目。出現の判断の表の検査に全部の升目の偽の入力を渡し、この期待どおりに落とすことを確かめる。出現の意味の判断そのものは機械で決まらない(限界)。

## 7. 提出前の吟味(委任文 §3、最初に作る役の文。書き方はリードの答え LEAD_DESIGN.md §7.1 の 8 のとおり、指摘の候補と何をしたかの 2 列だけにし、判定の語を書かない)

読み直したもの: 固定した要件(REQUIREMENTS.md §1・§2)、場面集の規則 1〜9、委任文 §3 の段落、LEAD_DESIGN.md §2〜§7.1、起動文の dr8-1-3 の 6 件の逐語とリードの注記、3 回目の版。

**自己検査(正の定義 0・B・D をこのファイルに当てた)**: コマンドと出力は `<scratchpad>/bt/r8-1/item0_r8-1-4_scenekeeper_def_selfcheck.txt`。中身: (1) このファイルの全行を書き出し、場面 id・`Scene` の欄名・数字・場面の語に印を付け、全行に判断を付け(判断は `…_selfcheck_judgments.tsv`)、判断の数とファイルの行の数を並べた。判断には「申告の行か / 裏付けの path」の欄がある。(2) 用語の表の語と、範囲の中で 2 つ以上の意味を持つ裸の語の、このファイルの中の全部の出現を書き出し、出現ごとの判断(決まった書き方に当たる出現と、手で判断した出現)と、出現の数と判断の数を並べた。(3) 全部の節の見出しと、正の定義 0〜D の 5 つの節の小見出しを並べた。(4) `…_l438.txt`・`…_axes_c.txt` を走らせ直した。

| 厳しい監査役・批評家が [止める] にしうる指摘の候補 | 何をしたか(ファイルの行、コマンドと出力) |
|---|---|
| dr8-1-3-1 の再発(決まりを当てたと書き、切片の対応が無い) | 冒頭の決まりを 2 つに分けた(このファイルの 6-11 行)。L-438 を切片に切り、切片ごとに書いた所を判断にした: `…_l438.txt` |
| dr8-1-3-2 の再発(軸を手で列べる) | 要件 §1・§2 を切片に切り、切片ごとに判断を付け、判断から軸を作り、§5 の軸の表と照らした: `…_axes_c.txt` |
| dr8-1-3-3 の再発(方法を一部にだけ当てる) | 正の定義 0・A・B・C・D の 5 つの「当てたときに直す所」に同じ欄の並びの暫定表(見出しの 1 文・軸・値・期待)を置いた。小見出しと表の見出しの並び: `…_selfcheck.txt` の (3) |
| dr8-1-3-4 の再発(用語の表の行に区分と確かめが無い) | 正の定義 D の区分を 3 つにし、用語の表の行の全部に区分と出力の path を書いた: `…_terms.txt`、`…_selfcheck.txt` の (2) |
| dr8-1-3-5(O-10) | この表を 2 列にし、判定の語を書かない形にした(リードの答え §7.1 の 8)。このファイルの中の判定の語の出現: `…_selfcheck.txt` の (5) |
| dr8-1-3-6(試験の軸が手の列べ) | 5 つの表の見出しに、リードの答え §7.1 の 9 の 1 文(A は逐語、ほかは語を替えた物)を置いた |
| 正の定義 C の見る道の数がリードの答え(3 種)と違う | 違う所と出所の切片を §5 の「リードの答えとの突き合わせ」に書いた。3 回目の版から外した分け方の升目の行き先も書いた |
| 正の定義 A の「設定つき対象の持つ型」を「正解と一致」に限ると、対応なし・結果なしの型の扱いが L-438 の字面より広い | 今のコードとの違いを `…_typerule_checks.txt` で確かめ、§3 の「場面係が足したもの」(vii) と §8 の 5 に書いた |
| 正の定義 A の型の組の条件のうち、materials と表の注記は資料係の持ち物 | 場面集の側でできること(runner の出力の欄、場面集の試験が持つ申し送りの文)と、資料係への申し送りを分けて §3 の直す所に書いた。§8 の 6 でリードに聞く |
| 未読・未測定を断定する(O-2・O-3) | LEAN の未読の 3 点を「未確認」、表への影響を「未測定」と書いた。今のコードについての文には `…_typerule_checks.txt` を付けた |
| repeat_of の読みを付けずに返す(L-438 (1)) | §0 の表に dr8-1-3 の 6 件の読みと根拠を足した |
| 候補 35 の件を埋もれさせる(L-438 (3)) | §8 の 1 に独立の項目として残した |

この段で回した試験: 無し(コードも場面集も変えていない)。回したのは確かめのコマンドだけ(上の出力のファイル)。

## 8. リードに聞くこと

1. **候補 35 の権限の分類器の拒否(br6-3-1、L-438 (3) の独立の項目)**: リードの答え §7 の 1「受け取った。場面係はこれ以上試さない。リードが run の止まった時機に試す」を受けた。場面係は試さない。検討表では「動かなかった」の側に置いたまま。
2. **§7 の 2 で「場面にしない」と決まった升目の理由の語**(2 回目から持ち越し): P0-1・P0-2・P0-4 の通知と時計の升目は、場面係には測り方の文のどの語で外になるかを示せない(P0-1 の「投入」は P0-3 の測り方が通知と時計を含む事象の型に使う語 / P0-2 の 2 列目は「全事象の時刻」/ P0-4 の「事象を読もうとする」は通知と時計の事象も名指す)。外れる語を教えてほしい。答えが無ければ、理由の欄には §7 の 2 の文と出所(LEAD_DESIGN.md §7 の 2)を書き、語は「リードの決定」と書く。
3. **測り方の文が見る道を名指さない観点(P0-2「核が保持する値」・P0-3「保持するか」・P0-7「動くか」)の升目**(2 回目から持ち越し): (a) 対象の持つ見る道のうち 1 つで測れば足りるか、(b) 対象の持つ全部の見る道で測るか。場面係の読みは (a) だが、字面では決まらないので決めていない。
4. **正の定義 0 と D はリードの答えに無く、場面係が足した**。項目 0 の場面集の規則として置いてよいか。この版で正の定義 0 に「書き出す文と申告の側」を、正の定義 D に「範囲の中の既存の語」の区分を足した。
5. **設定つき対象の持つ型の「値が不一致」の読み**: L-438 の文面は「値が不一致の型は「持つ」と数えない」。場面係は、p3 の場面の正しさが「正解と一致」でない型の全部(不一致・対応なし・結果なし・出所の検めで採点しない)を設定つき対象の持つ型に数えない、と読んだ(§3 の (vii))。今のコードは、出所の検め(`checked`)を通した p3 の結果の状態が `ok` でない型をすでに数えない(`…_typerule_checks.txt` の `target_types` と `run_target`)。よって今のコードとの違いは、状態が `ok` で正しさが「不一致」の型だけである。L-438 の字面が名指すのもこの型で、対応なし・結果なしの型を数えないのは今のコードと同じ。この読みでよいか。
6. **型の組の記録と表の注記(L-438 (2) の 2 つ目と 3 つ目)の持ち主**: materials と表を作るのは資料係の持ち物(委任文 §3「比較の表」)。場面係は runner の出力に設定つき対象ごとの型の組の欄を置き、資料係への申し送りと場面集の試験を書く。資料係の台本の文(表の注記に 1 文を必ず出す)を足すのはリードか。
