#!/usr/bin/env python3
"""Render scenes.py into DEFINITIONS.md (the one document of scene
definitions). `python3 gen_definitions.py` writes it; `--check` exits 1 if
the file on disk differs (the pytest in this directory runs the check)."""
from __future__ import annotations

import json
import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent
sys.path.insert(0, str(HERE))
import scenes  # noqa: E402

# The six positive definitions of round r8-1, frozen here by LEAD_DESIGN.md section 8.2 item 5 (one paragraph each).
# 0, A, B, D and E are the paragraphs of ROOTCAUSE_r8-1.md, character for character (test_battery_item0.py checks it);
# C replaces only the sentences LEAD_DESIGN.md section 8.2 item 1 withdrew (the cells' verdicts and how they were decided).
FROZEN_DEFINITIONS = {
    '0': '**当てる範囲**: 場面係が規則・正の定義・検査・試験を当てる範囲は、当てる先の全部を機械で書き出した物である(ファイルならファイルの全部の行、文書なら全部の節とその小見出し、正の定義なら全部の選択肢と全部の「数えない物」に「どれにも当たらない物」を足した物、用語なら文書が意味を持たせた全部の語の全部の出現、観点なら観点ごとの升目の全部。括弧の中は例で、例に無い当てる先も「全部を書き出す」に従う)。一覧(軸・値・条件・区分・試験の格子)を文から作るときは、その書き出す文も当てる先であり、書き出す文を切片(区切りの字で切った 1 片)まで機械で書き出し、切片ごとに、一覧のどこに入れたか・入れない理由を持つ。一覧を手で持たない。決まりを「当てた」と申告してよいのは、その決まりの文を切片まで書き出し、切片ごとに当てた所(ファイルの行)か当てない理由を持ち、切片の数と判断の数が等しいことを出力で示したものだけである。そうでない決まりは「切片まで書き出さずに読み直した」と書き、「当てた」と書かない。**緩める決定**(義務を外す・遅らせる・弱める許可と例外。「認める」「暫定」「当てない」「場面にしない」の類)は向きが逆で、当てる先は、その決定の文を切片まで書き出して、切片が名指す物だけである。名指さない物には当てず、義務の側を当てる。名指さない物に当てたいときは、その判断をリードに上げ、答えが来るまで義務の側を当てる。決定の文の語を替えた物は、その決定の引用でも、その決定でもない。文書の中の「当てた」「列べた」「全部」の申告も当てる先であり、自己検査は、ファイルの全部の行に「申告か / 申告ならその裏付けの名指し」の判断と「緩める決定を当てたか / 当てたなら決定の切片とその名指す当てる先」の判断を持つ(語の印は判断を助ける印で、判断を付けるファイルの行を印の付いた物に狭めない)。**裏付けの名指し**(出力の path と、その中の項の名)は、自己検査が機械で引き当てる: path の物が在り、名指した項の見出しが出力に在ること。出力の中身をこの文書に手で書き写さない(項の見出しを名指すか、道具が差し込み自己検査が出力と照らす)。当てる範囲を、指摘に出た物・思い出した物・探す語の一覧・実装の場合分け・この段落の括弧の中の例から作らない。それらは書き出した物に付ける印に使ってよいが、当てる範囲を狭めない。当てた結果は書き出した物の 1 つずつに持ち(ファイルならファイルの行ごとの判断)、書き出した数と結果の数が等しいことを同じ出力に示す。等しいことを示さない出力を根拠に「全部に当てた」「1 行ずつ読んだ」と書かない。当てる範囲を切ったときは、切ったことと理由を書き、切った外に当てたとは書かない。',
    'A': '**対象の振る舞いと数える物**: 場面の結果として対象に帰すのは、次の 3 つが全部成り立つ実行で出た物だけである。**(1) 設定の操作の列**: その実行の中で利用者が対象の公開の手段で行った設定の操作の全部を、行った順に並べた物であること(設定の操作 = データの購読・事象の型の組・解像度・同時刻の並びの引数・差し込む模型と口座を作り、足し、替える操作。実行の開始前に限らず、実行の途中に公開の API で購読や事象の型を足し替える操作も入る)。場面集の側は、設定の操作ごとに利用者のどの公開の呼び出しかを対象の配布物のコードのファイルの行(再現なら一次資料の行)で示し、いつ(開始前か、どの戦略の呼び出しの中か、ほかの時点か)行ったかを書き残す。どの設定の操作も、実行の前に利用者が知りうる物(場面が名指す銘柄と事象の型・その設定つき対象の選ぶ値・対象の公開の既定)と、その操作までに戦略が (3) の届き方で受けた物だけから決まること。公開の手段では 1 つの実行の中で続けて行えない設定の操作を寄せ集めた物は、1 つの設定の操作の列ではない。**(2) 測る仕事**: 場面が測る仕事(事象に型を付ける・時刻を変換する・並べる・過去を読ませる・通知を作る・模型を呼ぶ)を、対象の配布物のコード(母語のコードを含む)が行ったこと。再現では、利用者の公開の入口から戦略の呼び出しまでのコードの全部を一次資料の行に 1 対 1 で書き直した物(書き直した文を囲む条件を含む。書き直さない条件は、場面で成り立たない理由を一次資料の行で示す)が行ったこと。場面集の側が行ってよいのは、場面の入力の値を対象の公開の入口が受け取る値に写すこと、公開の手段を呼ぶこと、公開の差し込む口に利用者の実装を渡すこと、返ってきた物を場面の語に写すことだけである。**(3) 届き方**: 戦略が受けた物は、戦略の 1 回の呼び出しが始まってから戻るまで(コルーチンなら最初に入ってから終わるまで)の間に、対象の配布物のコード(母語のコードを含む)が戦略に引数として渡した物かその中にあった物、または戦略がその間に対象の関数を呼んで返ってきた物であり、その物を作ったのも対象の配布物のコードであること。**数えない物**: adapter・再現の外側・再現のうち一次資料の行に当たらない工夫が作った物(`dict`・関数・対象の class の実体を含む)、公開の手段で作れない設定の操作の列か設定の操作の寄せ集めの下で出た結果、まだ届いていない入力の時刻・順・件数に合わせて決めた設定の操作の下で出た結果(測る仕事のうち並べることを、場面集の側がしたことになる)、戦略の呼び出しの外で対象の関数を呼んで得た物、戦略が自分で貯めた値、場面集の側が呼んだ変換の値、そして (1)〜(3) のどれにも当たると示せない物(既定は数えない)。これらで決まった場面は採点しない(結果なし、理由を残す)。**設定つき対象と比較の表の行**: 設定つき対象 = 対象 × 選ぶ値(選ぶ値 = 設定の操作のうち場面の入力が決めない部分、つまり利用者が選ぶ値の全部の組み合わせ。場面の入力が決める遅れの値・差し込むダミーでは分けない)。1 つの設定つき対象の場面は全部同じ選ぶ値で走らせ、設定つき対象ごとに runner の出力に別々に残し、どの対象のどの選ぶ値かを materials に書く。比較の表の行「調査結果の側」は、委任文 §3 どおり場面ごとに、走らせた設定つき対象の最良を寄せる。これは場面ごとに設定つき対象の間から選ぶことで、どのセルも 1 つの設定つき対象の実際の実行の結果であり、比較の表の行は 1 つの道具・1 つの選ぶ値の振る舞いを名乗らない。新実装・当方の現状・試金石の比較の表の行は、それぞれ実行の前に決めた 1 つの設定つき対象だけから作る。**型の組(L-438 (2))**: リードの注記の L-438 (2) が名指す「P0-1・P0-5 の 4 場面」の入力の型の組は、設定つき対象ごとに、その設定つき対象の持つ型から規則で決める。この 4 場面は `scenes.py` の `Scene.type_plan` の欄を持つ場面として機械で書き出し、試験が、書き出した場面の観点が全部 P0-1 か P0-5 であることと、場面の数が L-438 (2) の「4」であることを確かめる(外れれば試験が落ち、場面係はリードに上げる)。4 場面の外の場面の入力の型の組を対象から決めることは、L-438 (2) が名指さないので行わない(正の定義 0 の緩める決定)。設定つき対象の持つ型 = その設定つき対象の同じ実行の p3 の場面のうち、出所の検め(この正の定義の (1)〜(3))を通って採点された正しさが「正解と一致」の場面の事象の型である。値が不一致の型(その型の p3 の場面の正しさが「正解と一致」でない型。届いたが値が違う型、対応なし・結果なしの型を含む)は持つと数えない。設定つき対象の違う実行の結果の和にもしない(1 つの場面の入力と結果は、1 つの設定つき対象の実行から出る)。設定つき対象の持つ型が 4 場面の規則の求める最低の数に足りない設定つき対象は、その場面を対応なしにし、理由に p3 の結果を名指す。設定つき対象の持つ型から型の組を決める規則と、型の組から期待を出す式は、DEFINITIONS.md に、対象を見る前の文として書く(規則と式は対象の出力を読まない)。設定つき対象ごとに選んだ型の組は materials に記録する。審査員が読める表の注記に「型の組は対象の持つ型から規則で決めた」と 1 文書く。この正の定義は新実装・当方の現状・調査結果の側・再現・試金石のすべてに同じく当てる。この正の定義を確かめる試験の入力の空間は、この段落を切片まで書き出して作った軸の全部の値に「どれにも当たらない」値を足した升目の全部であり(正の定義 0)、期待はこの段落の条件だけから出す。',
    'B': '**場面集の文が場面について述べてよいこと**: 当てる範囲は場面係が書く全部の文 = 場面集の置き場所(`git ls-files tests/bt/battery/item_<番号>` と、まだ追跡されていない書いた物)の全ファイルの全部の行(ROOTCAUSE*.md・検討表・README の類・コードの注釈と docstring・生成器の中に手で書いた文を含み、ファイルの種類・名前・書いた周で除かない)と、場面係の返り値である。**照合元**は、場面集の中で値が変わりうる物 = 場面の定義(`scenes.py` の値)と runner の出力(`survey_results/`)、およびそれらから機械で作った物である。**場面の中身**は、真偽が照合元の今の値に依る言明の全部であり、どの語で書かれているかでは決めない: 1 つの場面の欄の値(入力・期待・結果)だけでなく、場面の集まりについての言明(ある欄を持つ場面の数と id、観点ごとの場面の数、対象ごとの結果の数、「どの場面も…」)を含む。見分け方は「照合元の値が変わったら、その文は偽になりうるか」で、なりうる文は場面の中身である。場面の中身を文で持ってよいのは、(i) 照合元の値から機械で作り、照合元と食い違えば試験が落ちる部分(生成器に手で書いた文は、生成物に写ってもこれに当たらない)と、(ii) 試験がその文の名指す場面 id の今の照合元(版を固定した文ならその版の照合元)から作り直して一致を確かめる定型の文、だけである。それ以外の文は場面の中身を述べず、場面 id と欄(版を固定するなら git の版)を名指す参照だけを持つ。照らせない書き方の場面の中身は、正しくても、過去の周の文書の中でも置かない(過去の周の文書は、その周の版を固定した参照に直す)。コマンドの出力は、打った版と一緒に場面集の外のファイルに置き、文はその path を名指し、出力の値を文に写さない。機構の読み・一次資料の行・判断とその理由・場面集のコードの振る舞い(関数名で指し、読んだ版を書く)は場面の中身ではない。この正の定義を当てる検査は、当てる範囲の全部のファイルの行をファイルの行ごとの判断で覆い(正の定義 0)、探す語の一覧で当てる範囲を狭めない。場面の定義を変えたときの同じ根の探しも、場面集の全ファイルの全部の行について行う。',
    'C': '**観点の範囲と場面**: 7 観点(P0-1〜P0-7)の全部に同じ方法で当てる。升目の軸は、固定した要件の文から機械で書き出す: 書き出す文は §1 の要件の表の行(4 列目)と §2 の要件の表の各行(2 列目・3 列目)の全部で、これを区切りの字で切片に切り、切片の 1 つずつに次のどれか 1 つの判断を付ける。**事象の軸** = §1 の事象の型と、各観点の文が名指す事象の型(通知は §1 の語どおり受付・拒否・約定の 3 つ)。**見る道** = 戦略の API の名(事象ごとの呼び出し・発注・取消)と、戦略が読む動作を名指す語から作る、戦略の呼び出しに届く物 / 発注の呼び出しがその場で返す物 / 取消の呼び出しがその場で返す物 / 戦略が対象の公開の手段で読む物。**観点の追加の軸** = その観点の文が事象と見る道のほかに列べて名指す名詞(P0-2 の時刻の単位、P0-7 の差し込む口の類)。**軸にしない** = 列べた要素をまとめる名・観点の性質そのもの・測り方の手順と結果の側の語・作りの形の名(場面集の規則 2)・文の繋ぎの類で、判断に理由を書く。軸と値は判断からだけ作り、判断の無い切片・切片に無い値があれば試験が落ちる。観点の文(§2 の 2 列目)が限らない軸は、全部がその観点の範囲に入る。観点ごとに観点の範囲の全部の升目を列べ、各升目の判断は、その升目を `covers` に持つ同じ観点の場面があれば「場面にした(場面 id)」、無ければ「測っていない」(括弧に、その升目を数える場面が無い理由を、場面の宣言と入力から機械で書く、第 r16-1 回)の 2 つのどちらかで、`covers` から機械で決める(LEAD_DESIGN.md §8.2 の 1)。`covers` は、場面の宣言(`scenes.py` の `COVERS`)の升目のうち、下の「第 r13-1 回の正の定義」が数える升目(宣言し、かつ事象の型が場面の入力から機械で出る升目。`scenes.covers_of`)で、宣言した升目が場面の測る物と合うかは批評家が読んで確かめ、升目の判断のために要件の文を切片に切って名指しを決める機械は置かない(同じ §8.2 の 1。宣言だけで数えていた文は、批評家 i0-r11-02 を受けた LEAD_DESIGN.md §9.2 の 34 のリードの決定で第 r13-1 回に替えた)。表には §8.2 の 2 の 1 文「この表は測っていない範囲の記録である。要件を広げるかはオーナーの判断で、項目 0 の通過のあとに『欠けているもの』の批評家の経路で上げる。」を置く。この表は DEFINITIONS.md の「場面にしていない観点・側面」として、場面の定義と升目の軸から機械で作り、観点の範囲の升目が 1 つでも表に無ければ試験が落ちる(升目を列べない申告は置かない)。場面は、その升目について観点の性質だけで結果が決まり、ほかの観点が測るもの(事象の型の有無・差し込む口の有無)で決まらないように作る: 遅れは公開の手段で決める値でも差し込む口でもよい。入力の型の組を設定つき対象の持つ型から決める方法は、L-438 (2) が 4 場面にだけ与えた緩める決定なので、升目の場面には当てない。升目の場面で事象の型の有無をどう扱うかはリードに上げ、答えが来るまで、その扱いに依る升目の場面は足さない(場面を足すのは直しの段)。',
    'D': '**語の意味**: 場面係が書く文(正の定義 B の当てる範囲と同じ)が意味を持たせた語(文の中で「=」「:」で意味を書いた語、太字で名付けた語、用語の表の語)は、1 つの意味だけを持つ。語の意味を比べる範囲(比べる範囲)は、その文と一緒に読まれる文書の全部 = 場面集の全ファイル・固定した要件・委任文・リードの設計・項目 0 の VERDICTS・CLAUDE.md である。語は次の 3 つの区分のどれか 1 つに入れる。**要件・委任文の語**は、要件か委任文がその意味を持たせたファイルの行を引き、その意味でだけ使う。**比べる範囲の中の既存の語**(比べる範囲のこのファイルの外で、すでに使われている語)と**新しい語**(比べる範囲のこのファイルの外に出現が無い語)は、比べる範囲のこのファイルの外の出現を範囲を切らずに書き出し、次のどちらかを出力で示す: 意味は 1 つ = 全部の出現に出現ごとの判断があり、全部がこのファイルの意味である(全称の言明なので全部の出現が要る。新しい語は出現が無いことがこれに当たる)/ 意味は 2 つ以上 = このファイルの意味と違う意味の出現を 1 つ以上ファイルの行で示す(存在の言明なので 1 つで足りる)。意味が 2 つ以上の語は裸で使わず、意味を決める修飾を付けた語を、修飾つきの語としてもう一度この区分に入れて確かめる。用語の表の表の行は、区分(3 つのどれか 1 つの名)と、確かめた結果(「意味を持たせたファイルの行: …」「外の出現なし」「意味は 1 つ」「意味は 2 つ以上: 違う意味の出現のファイルの行」のどれか 1 つで始める。続けてこのファイルでの書き方を書いてよい)と、出力の path を持つ。結果の欄に手順の文を置かない。自己検査は、この文書が意味を持たせた語を機械で書き出して(太字の語・「=」「:」で意味を書いた語)全部が用語の表にあることと、用語の表の全部の表の行の区分と結果の頭の語が出力と同じことと、表の全部の語と裸で使わない語のこのファイルの中の出現に出現ごとの判断があることを示す。語を言い換えたときは、言い換えた後の語も同じ確かめに入れる。文書が意味を持たせていない一般の語は、この正の定義の当てる先の外である(当てる範囲を切った理由: 一般の語の全部は書き出せても、出現ごとの意味の判断の数が文書の語の数になり、この段で判断を持てない。限界として書く)。',
    'E': '**提出前の吟味の記録が持ってよい物**: 場面係の提出前の吟味の記録(委任文 §3「提出前の吟味」、直しの前の定義では ROOTCAUSE のファイル)は、表の列を「指摘の候補」と「何をしたか」の 2 つだけにする(リードの答え LEAD_DESIGN.md §7.1 の 8)。指摘の候補の表の列は、(a) 機械で書き出した物 = このファイルの §0 の表の指摘の id の全部を 1 件ずつ、と (b) 場面係が足した候補(候補の文の頭に「場面係が足した候補:」と書く)からなる。(a) を場面係が選んで減らさない。何をしたかの表の列は、変えた所の名指し(このファイルの節の番号かファイルの行)・打ったコマンドと出力の path・リードに上げた項目の番号と、その所で行った動作(置いた・足した・言い換えた・上げた の類)だけを持つ。どちらの表の列にも、提出前の吟味の判定の語(候補が片付いたか・通るか・残る指摘の数の見込みを述べる語)を置かない。提出前の吟味の判定の語の有無は、自己検査が記録の全部のファイルの行に判断を付けた出力で示す(語の一覧で探す検査を当てる範囲にしない。正の定義 0)。この記録が CLAUDE.md §0.2 表 A の O-10(オーナーの逐語のある禁止)に触れるかを決めるのは場面係ではない: 委任文 §3 と起動文は記録を書くことを求め、リードの答えは書き方の条件を付けたが、オーナーの逐語のある禁止に対する例外を決めるのはオーナーである(CLAUDE.md §8 の権限の順位)。場面係はこの関係を「リードに聞くこと」に上げ、答えが来るまでは委任文と起動文の指示どおり記録を上の形で書き、「O-10 に触れない」「解消した」と書かない。(b) の候補を選ぶことと、何をしたかの表の列を書くことに場面係の選びが残ることは、この形では消えない(限界)。',
}

# Round r13-1: the lead's answer F, verbatim (LEAD_DESIGN.md section 9.2 item 32; item 34 adds it to the definitions'
# section). test_battery_r13_claims.py checks it against LEAD_DESIGN.md character for character.
LEAD_F = '**正の定義 F(升目を「場面にした」と数える物)**: 升目 (e, v, x) を場面 s が「場面にした」と数えるのは、s が見る道と追加の軸の組 (v, x) を宣言し、かつ型 e が s の**入力から機械で出る**ときだけである。市場の事象の型(約定・板の写真・板の差分・足・資金調達・清算)は、scenes.py が s の入力に載せた事象(`events` と `streams` の中身)の型の欄そのもの(`type_plan` の場面は runner が全 6 型を持つ対象のために作る組 = `for_target_types(s, TYPE_ORDER)` の入力の型の欄。型の欄の無い事象と、事象を持たない場面からは型を出さない。adapter が代えた型は数えない)。これは批評家 i0-r11-02 の神託 `_kinds(scene)`(`test_i0r11_covers_within_scene_input.py` 38-49 行)と同じ計算である(監査 52 回目の 52-3 で直した)。時計・注文の通知(受付/拒否/約定)・取消の通知は、s の入力の戦略の頼みがその型を作らせる頼みであるときだけ(時計の頼み → 時計、発注 → 注文の通知、取消 → 取消の通知)。手書きの宣言・adapter が書く値・対象の出力・「正解がその事象を名指すか」「その道を通さなければ正解にならないか」の意味の読みは使わない。判断は機械で 2 値。(v, x) が s の測る物と合うか、s の入力がその道を実際に呼ぶかは批評家が読む(§8.5 の 24)。'

# Round r13-1 (critic i0-r11-02): the scene keeper's positive definition, ROOTCAUSE_r13-1.md section 3, character for
# character (test_battery_item0.py checks it), as the definitions auditor passed it. LEAD_DESIGN.md section 9.2 item 34
# has it added to the definitions' section; it replaces definition C's sentence on how `covers` is decided.
R13_DEFINITION = '**升目を「場面にした」と数える物**: 升目 (e, v, x) を場面 s が「場面にした」と数えるのは、s がその升目 (e, v, x) を宣言し、かつ型 e が s の入力から機械で出るときだけである。宣言と入力の両方が要り、どちらか一方だけでは数えない(入力は s が宣言しない升目を足せない)。入力から機械で出る型は次の 2 つだけである。市場の事象の型(約定・板の写真・板の差分・足・資金調達・清算)は、scenes.py が s の入力に載せた事象(`events` と `streams` の中身)の型の欄そのもので、型の欄 1 つが型 1 つを出す(`type_plan` の場面は `for_target_types(s, TYPE_ORDER)` の入力の型の欄。批評家 i0-r11-02 の神託 `_kinds(scene)` と同じ計算。型の欄の無い事象と、事象を持たない場面からは型を出さず、adapter が代えた型は数えない)。市場の事象の型では入力が型そのものを決めるので、宣言は入力に無い型の升目を足せない。時計・注文の受付の通知・注文の拒否の通知・注文の約定の通知・取消の通知は、s の入力が欄として持つ戦略の頼みから出る: 時計の頼みは時計を、発注の頼みは注文の受付・拒否・約定の通知の 3 つを一緒に、取消の頼みは取消の通知を出す。取消の通知は要件 §1 の事象の型の外で、升目の表の事象の軸に入れず、`scenes.py` の `OUTSIDE_AXES` の区分の軸の外の升目として別に出す。入力の文(`strategy` の文など)を読んで頼みを決めない。発注の頼みについて入力が機械で決めるのは 3 つの通知が一緒に出ることまでで、3 つのうち s がどれを測るかは入力から機械では決まらない(決めるには、下で使わないとした対象の出力・runner の記録・「正解がその事象を名指すか」を読むことになる)。よって注文の 3 つの通知の升目では、機械が決めるのは「発注の頼みを持たない s の宣言は数えない」までで、3 つのどれを数えるかは s の宣言だけが決める。宣言の e が入力から出ない升目は数えず、そういう宣言があれば試験が落ちる(黙って捨てない)。adapter が書く値・対象の出力・runner が残す記録・「正解がその事象を名指すか」「その道を通さなければ正解にならないか」の意味の読みは使わない。判断は機械で 2 値。宣言した升目が s の測る物と合うか(注文の 3 つの通知のどれを数えるかの宣言を含む)と、s の入力がその道を実際に呼ぶかは批評家が読む(LEAD_DESIGN.md §8.5 の 24)。この定義は升目の表と `covers` だけに当て、場面の正解・採点・比較の表のセルを変えない。'

HEAD = """# 項目 0「核」の場面集 — 定義

**この文書は `scenes.py` から `gen_definitions.py` で作る。手で直さない**(`test_battery_item0.py` が食い違いを落とす)。
固定した要件: `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/REQUIREMENTS.md`。観点は §2 の P0-1〜P0-7。

## 共通の決まり(すべての場面の入力の一部)

- 時刻は 1970-01-01T00:00:00Z からのナノ秒の整数。`T0` = 1,700,006,400,000,000,000(2023-11-15T00:00:00Z、水曜日の 0 時)、`S` = 1 秒 = 1,000,000,000、`DAY` = 1 日 = 86,400 秒。
- 事象の間隔は、時刻そのものを測る場面(P0-2、P0-5 の同時刻、ミリ秒の遅延の場面)を除いて 1 日。暦の上の日足しか受けない対象でも、それ以外の場面を測れるようにするため。
- 事象は `kind`(trade = 約定 / book_snapshot = 板の写真 / book_delta = 板の差分 / bar = 足 / funding = 資金調達 / liquidation = 清算)と `ts_ns`(取引所の時刻)を持つ。`recv_ns`(当方が受け取れる最初の時刻)は書いていなければ `ts_ns` と同じ。足の `ts_ns` は足が閉じた時刻。
- 入力に `"any_type": true` がある場面は、事象の型以外を測る場面である。約定を受けない対象では、各約定を、同じ時刻(受け取れる時刻も同じ)で 始値=高値=安値=終値=その約定の価格 の足に代えてよい(代えたことを記録に書く)。
- **型の組を設定つき対象の持つ型から決める場面(L-438 (2)、第 r8-1 回)**: {L438}。この {N438} 場面だけが入力に `type_rule` を持つ。入力の事象の型の組は、下の「設定の操作と設定つき対象」の設定つき対象ごとに、その設定つき対象の持つ型から規則(各場面の入力の `type_rule`)で runner が決め、期待は型の組から式(各場面の「正解の出し方」)で出す。規則と式は対象の出力を読まない。定義に載せた入力と期待は 6 種を全部持つ設定つき対象のもの。ほかの場面の入力の型の組は対象から決めない(場面の入力に書いた型、または入力の note が名指す選び方)。
- **設定の操作と設定つき対象(第 r8-1 回)**: 場面の結果を対象に帰すのは、その実行の中で利用者が対象の公開の手段で行った設定の操作(データの購読・事象の型の組・解像度・同時刻の並びの引数・差し込む模型と口座を作り、足し、替える操作。実行の途中の操作も入る)の列の下で出た物だけである。どの設定の操作も、実行の前に利用者が知りうる物(場面が名指す銘柄と事象の型・その設定つき対象の選ぶ値・対象の公開の既定)と、その操作までに戦略が受けた物だけから決まる。まだ届いていない入力の値・時刻・順・件数に合わせて決めた設定(例: 場面の事象の最初と最後の時刻に合わせた実行の窓)の下で出た結果は採点しない(結果なし、理由を残す)。設定つき対象 = 対象 × 選ぶ値(設定の操作のうち場面の入力が決めない部分、つまり利用者が選ぶ値の全部の組み合わせ)。1 つの設定つき対象の場面は全部同じ選ぶ値で走らせ、設定つき対象ごとに別々の結果にする。比較の表の「調査結果の側」は場面ごとに走らせた設定つき対象の最良を寄せる(どのセルも 1 つの設定つき対象の実際の実行の結果)。
- 入力の `requests`(第 r13-1 回)は、場面の戦略が対象に出す頼みの種類の欄: `timer` = 時計で起こすことを頼む / `place` = 発注 / `cancel` = 取消。値は同じ入力の `strategy` の文が書く頼みと同じで、新実装の adapter の戦略(`adapters/new_impl.py`)が実際に出した頼みと等しいことを `test_battery_r13_claims.py` が照らす。升目の表で、時計・注文の受付/拒否/約定の通知・取消の通知の升目を数えるのに使う(下の「第 r13-1 回の正の定義」)。場面の正解・採点は変えない。
- 当方の注文が埋まる場面では、市場の約定は 1 件 100 単位(注文は 1 単位)。対象の出来高の上限で結果が決まらないようにするため。
- 対象に事象を渡す側は、場面が書いた順・まとまりのまま渡す。並べ替え・結合・間引きをしない(それを測っている)。
- 「受け取った値」は、戦略が呼び出しの中でその場で記録した値。実行の後に結果から読んだ値ではない(場面が別に書いたときを除く)。
- 対象の公開の手段と公開の差し込み口だけを使う。対象のコードを書き換えない・実行中に差し替えない。
- **場面が測るもの(事象の型・時刻の変換・過去の読み出し・並び)は、対象の配布物のコードが出したものだけを数える。**対象に事象を渡す側と場面の戦略が書いてよいのは、(1) 対象の公開の手段を呼ぶこと、(2) 対象が利用者の実装を受けると公開している差し込み口に渡すダミー(P0-7 の模型・口座、時計を起こす条件)、(3) 対象が返したものを場面の語に写すこと(型の名前を `kind` に、状態を通知の種類に)、だけ。事象を渡す側が作った型(対象の基の class の子、欄を足した基の class、型の印を付けた辞書や関数)で事象を運ばない。戦略が自分で貯めた値、渡す側が自分で呼んだ変換で答えない。対象がその型・手段を持たないときは「対応なし」(何を確かめたかを書く)。

## 判定(資料係の表の 2 つの欄。対象の側は判定しない)

- **正しさ**: 正解と一致 / 対応なし(対象が明示的に拒否した・その口が無いことを実際に呼んで確かめた)/ 不一致(黙って違う結果を返した)/ 結果なし(例外で結果が出なかった)。期待が辞書のときは、期待に書いた鍵がすべて同じ値で出ていれば一致(出力の余分な鍵は見ない)。
- **採点に使う値を出力から作る場面**(「採点に使う値」の行がある場面): 対象の側(adapter)は観測したもの(届いた列・試しの結果・対象が明記した規則とその出所)だけを出し、採点に使う値は runner が書いたとおりに作る。対象の側が「できた」と書いた値では採点しない(場面集の規則 1)。
- **出所の検め**(採点の前。runner が行う): 戦略が受け取った事象の列を返す場面では、受け取った 1 件ごとの物を、p2-iso-* では ISO の文字列を読んだ対象の関数を、p4-visible-at-step では件数と終値を読んだ読み出しを、p4-future-read-attempt では手段ごとの試しを、対象の側が出所として返す。**出所は、場面集の共通の部品(`adapters/common.py`)が物から作った記録だけを受ける**(手で書いた名前・辞書は採点しない)。**「対象の物」と数えるのは、出所のファイルが対象の配布物の置き場所の中にあると示せたときだけ**(置き場所は runner が対象ごとに持つ表 `TARGET_DISTS` から、その対象の環境の import の探索で決める。場面集の名前でないことからは決めない)。示し方は、**型**と**届いた道**の 2 つを別々に示す(型は「対象がその型を持つ」ことしか示さず、「この物を対象が戦略に届けた」ことは示さないため。第 r6-3 回): (a) 型: 物の型を定義したファイル(numpy の行はその dtype、札の付いた物は札の型か札の定数を持つモジュールも)が置き場所にある。`dict`・関数・`datetime`・`DataFrame` のような誰でも作れる型の物は、場面集の側が対象に渡した物・場面集の側が持っている物と同じ物でないこと(対象が作った物)で示す。(b) 届いた道(型を問わず、対象の class の物にも当てる): **対象のコードが戦略の呼び出しに引数として渡した**か、**そうして渡した物の中にあった**(場面集の側の物の属性を通る道は数えない)か、**対象の母語(C・C++ など)のコードが戦略を呼んで渡した**(runner が場面ごとに `sys.setprofile` で走っている母語の関数を記録する)か、**戦略が対象の関数を呼んで返ってきた**(`common.read`・property の読み `common.attr`・反復 `common.iterate`/`common.aiterate`。戦略の呼び出しの中の読みに限る: 読んだ時点の呼び出しの列で、場面集の側の一番外の枠を呼んだのが対象のコードか対象の母語のコードであるか、利用者が自分の回しで対象を動かす対象では adapter の `common.user_loop_call` の塊の中であることを `common.call_context` が記録し、runner がそれを見る。実行が終わってから読んだ物は数えない。第 r8-1 回)か、のどれかが置き場所のコードである。場面の入力を adapter が対象の class で組んで対象に渡し、対象が届けた物は数える(どの対象も場面の入力は adapter が対象の入口の形に直して渡す。記録には `input` と残す)。戦略の呼び出しの中で場面集の側が対象の class を組んだ物・adapter が持つだけで対象に渡していない物は数えない。(c) 読み出しと試しは、戦略の呼び出しの中で行い((b) の読みと同じ記録。第 r8-1 回)、その間に対象のコードが走った(`sys.setprofile` で記録)か、読んだ物が (a)(b) で対象の物か、手段が対象のコードの関数・method、(d) 翻訳した道具(Rust・Go・C・C++)は driver が物から名付けた型・関数の名前が対象の名前の頭で始まる。出所が無い・示せない・場面集の側の型・1 つの型に 2 つの `kind` を写した・読み出しと答えが食い違う・名指し方の一覧が欠ける・名指しの試しの例外が場面集の側の `raise` 文で起きた、のどれかなら、その場面は採点せず「結果なし」にし、理由を残す。
- **再現**: 2 回の実行で同じ / 2 回で違う / 結果なし。
- 良い順(場面集の規則 5): 正解と一致 > 対応なし > 不一致 > 結果なし。正しさが同じなら「2 回の実行で同じ」が上。

## 資料係への申し送り(第 r8-1 回。正の定義 A)

- runner の出力(`survey_results/<設定つき対象>.tsv`)は設定つき対象ごとに 1 つ。ファイルの名の `@` の後ろと `choose` の表の列が選ぶ値、`settings_1` の表の列がその実行の設定の操作の列、`types_1` の表の列が型の組を対象から決める {N438} 場面({L438})でその設定つき対象の持つ型から選んだ型の組。資料係の表を作るスクリプトは、設定つき対象ごとの選ぶ値と型の組をこれらの表の列から materials に写す。
- 比較の表の行「調査結果の側」は、場面ごとに、走らせた設定つき対象の最良を寄せる。新実装・当方の現状・試金石の比較の表の行は、実行の前に決めた 1 つの設定つき対象だけから作る。
- 審査員が読める表の注記に、次の 1 文を必ず出す: 「型の組は対象の持つ型から規則で決めた」。
- runner の出力には、第 r13-1 回から `types_in`・`types_added`・`requests` の表の列がある(LEAD_DESIGN.md §9.2 の 33。記録で、升目の表の判断には使わない)。`types_in` = 対象に入った入力の市場の事象の型(runner が渡した入力の型のうち、戦略に届いた列を返す場面では届いた型、そのほかの場面では adapter が共通の部品 `as_bar`・`substitute` で代えた型を除き、設定つき対象の持つ型(同じ実行の p3 の採点、L-438 (2))に限った型。runner が adapter を呼ばなかった場面は空)、`types_added` = 戦略に届いた列にあって、渡した入力に無い型(adapter が代えた型)、`requests` = その実行で戦略が対象に出した頼みの種類(adapter が共通の部品 `request` で記録した物。記録するのは新実装・当方の現状・試金石の adapter だけで、ほかは null)。資料係の表の注記に、設定つき対象ごとの「場面にしたが、この対象には型が入らなかった升目」と「頼みの記録が無く入ったかを決められない升目」を `grid_c.not_entered` で機械で出す(`python3 grid_c.py <runner の出力>…` が設定つき対象ごとに注記の 1 行を出す。道具の名は出さない)。

## 正の定義 0〜E(第 r8-1 回、凍結)

{FROZEN}

## 数える物の選択肢(正の定義 A)

{DEF_A}

## 場面にしていない観点・側面

{GRID}
"""


def fmt(v) -> str:
    return "`" + json.dumps(v, ensure_ascii=False) + "`"


def fmt_input(inp) -> str:
    """The input as JSON; round r16-1 (ROOTCAUSE_r16-1.md section 2, root 5): a float `time` also shows the exact
    decimal of the value the float holds (the JSON shows the shortest repr, which may hold other digits)."""
    out = fmt(inp)
    if isinstance(inp, dict) and type(inp.get("time")) is float:
        from decimal import Decimal
        out += f"(float が正確に持つ値: time = {Decimal(inp['time'])})"
    return out


def named_lines(vp: str) -> list[str]:
    """The lines of one viewpoint: each value its text names with the scenes whose input gives it (grid_c.named_coverage),
    then the extra values its scenes' inputs give that its text does not name, with the reason (scenes.OUTSIDE_EXTRA)."""
    import grid_c
    rows = [r for r in grid_c.named_coverage(scenes.SCENES) if r["viewpoint"] == vp]
    out = [f"- {r['axis']} {r['value']}: " + (", ".join(f"`{i}`" for i in r["scenes"]) or "無し(場面が無い)") for r in rows]
    axes = {ax for (v, ax) in grid_c.axes()[2] if v == vp}
    named = {(r["axis"], r["value"]) for r in rows}
    extra: dict = {}
    for s in scenes.SCENES:
        if s.viewpoint == vp:
            for ax, val in sorted(scenes.extra_values_of(s)):
                if ax in axes and (ax, val) not in named:
                    extra.setdefault((ax, val), []).append(s.id)
    for (ax, val), ids in extra.items():
        out.append(f"- 軸の外の値 {ax} {val}({scenes.OUTSIDE_EXTRA[val]}): " + ", ".join(f"`{i}`" for i in ids))
    return out or ["- 観点の文は事象・追加の軸の値を名指さない"]


def grid_section() -> str:
    """The table of every cell of every viewpoint's range (positive definition C), made by grid_c.py: two verdicts
    only, from the scenes' `covers` (LEAD_DESIGN.md section 8.2 items 1-2; since round r13-1 `scenes.covers_of`)."""
    assert not [(s.id, scenes.covers_problems(s)) for s in scenes.SCENES if scenes.covers_problems(s)]
    import grid_c
    rows = grid_c.table(scenes.SCENES)
    assert not grid_c.problems(scenes.SCENES, rows)
    ev, see, extra = grid_c.axes()
    done, not_measured = grid_c.VERDICTS
    lines = [grid_c.MEANING, "",
             "観点ごとの範囲の升目の全部を、固定した要件(REQUIREMENTS.md)の §1 の要件の表の行と §2 の要件の表の各行の文を切片に切って"
             "書き出した軸(`grid_c.py`・`grid_c_judgments.tsv`)から作った。升目 = 事象 × 見る道(× その観点の追加の軸)。"
             f"各升目の判断は、その升目を `covers` に持つ同じ観点の場面があれば「{done}」(場面 id を書く)、無ければ「{not_measured}」の"
             " 2 つのどちらかで、`covers` から機械で決めた(LEAD_DESIGN.md §8.2 の 1)。「{not_measured}」の括弧は、その升目を数える場面が無い理由を"
             f"場面の宣言と入力から機械で書く(`grid_c.why`、第 r16-1 回、批評家 i0-r15-06): 「{grid_c.WHY_NO_DECLARATION}」/"
             f"「{grid_c.WHY_TYPE_NOT_FROM_INPUT}」。`covers` は、場面の宣言(`scenes.py` の `COVERS`)の升目のうち、"
             "事象の型が場面の入力から機械で出る升目だけである(第 r13-1 回の正の定義、`scenes.covers_of`): 市場の事象の型は入力の事象の型の欄"
             "(型を設定つき対象の持つ型から決める 4 場面は、6 種を全部持つ対象のために作る入力の型の欄)、時計・注文の受付/拒否/約定の通知・取消の通知は"
             "入力の `requests` の欄から出る。型の欄の無い事象と事象を持たない場面からは型を出さず、adapter が代えた型は数えない。"
             "注文の受付・拒否・約定の通知の升目では、機械が検めるのは発注の頼み(`requests` の `place`)の有無までで、3 つのどれを数えるかは場面の宣言が決め、"
             "批評家が読む。宣言の型が入力から出ない升目があれば試験が落ちる。設定つき対象ごとに、この表で場面にした升目のうち入らなかった型の升目は、"
             "runner の記録から資料係が表の注記に出す(上の「資料係への申し送り」)。",
             "",
             "観点ごとの見出しの下の一覧は、その観点の場面のうち、どの升目にも当たらない場面(見出しの「升目に当たらない場面」の数)と、升目に当たるが"
             "型の欄の無い事象を含む場面を、場面の入力から機械で区分して出す(`grid_c.unplaced`、第 r15-1 回、批評家 i0-r14-06): "
             f"「{grid_c.NO_EVENT}」= 入力に事象も型も無い、「{grid_c.TYPE_NOT_IN_INPUT}」= 入力に事象はあるが型の欄が無い(型は対象の側で決まる)、"
             f"「{grid_c.NO_CELL_DECLARED}」= 入力から型は出るが、場面の宣言にその型の升目が無い(読む物か見る道が升目の軸に無い。"
             f"宣言が合うかは批評家が読む)、「{grid_c.HAS_UNTYPED}」。一覧の場面はその観点を測っているが、升目の判断は上の 2 つの値のままで、"
             "一覧は判断を変えない。場面が何を測るかは、下の観点ごとの節の場面の定義にある。",
             "",
             "- `requests` の記録(adapter のクラスの `records_requests = True`、`grid_c.request_recording`。第 r15-1 回): "
             + grid_c.request_recording_sentence(grid_c.request_recording())
             + "。記録しない設定つき対象では、資料係の注記は時計と注文の通知の升目を「入った」と数えず「記録なし」と分けて出す。",
             "",
             f"- 事象の軸({len(ev)}): " + " / ".join(ev),
             f"- 見る道({len(see)}): " + " / ".join(see)]
    for (vp, ax), vals in extra.items():
        lines.append(f"- {vp} の {ax}({len(vals)}): " + " / ".join(vals))
    listed = grid_c.unplaced(scenes.SCENES)
    by_id = {s.id: s for s in scenes.SCENES}
    kind_jp = {"value": "値の場面", "capability": "能力の場面"}
    for vp in grid_c.VIEWPOINTS:
        mine = [r for r in rows if r["viewpoint"] == vp]
        n_done = sum(1 for r in mine if r["verdict"] == done)
        un = [x for x in listed if x["viewpoint"] == vp]
        n_un = sum(1 for x in un if x["class"] in grid_c.UNPLACED_CLASSES)
        items = [f"- `{x['scene']}`({kind_jp[by_id[x['scene']].kind]}): {x['class']}" for x in un] or ["- 無し"]
        lines += ["", f"### {vp}(升目 {len(mine)}: {done} {n_done} / {not_measured} {len(mine) - n_done}"
                      f" / 升目に当たらない場面 {n_un})", "",
                  f"固定した測り方: 「{grid_c.measure_text(vp)}」", "",
                  "升目の表に全部は現れない場面(`grid_c.unplaced`。升目の判断は変えない):", "", *items, "",
                  "観点の文が名指す値ごとの場面(`grid_c.named_coverage`、第 r16-1 回。事象は入力の型、追加の軸は入力の欄から機械で出す):",
                  "", *named_lines(vp), "",
                  "| 事象 | 見る道 | 追加の軸 | 判断 | 場面 |", "|---|---|---|---|---|"]
        for r in mine:
            what = ", ".join(f"`{i}`" for i in r["scenes"]) or "—"
            lines.append(f"| {r['event']} | {r['see']} | {r['extra'] or '—'} | {grid_c.shown(r)} | {what} |")
    out_axes = sorted({c[0] for s in scenes.SCENES for c in s.covers} - set(ev))
    if out_axes:
        lines.append("")
    for k in out_axes:
        lines.append(f"- 軸の外の升目を覆う場面がある: {k}({scenes.OUTSIDE_AXES[k]})。場面: "
                     + ", ".join(f"`{s.id}`" for s in scenes.SCENES if any(c[0] == k for c in s.covers)))
    return "\n".join(lines)


def frozen_section() -> str:
    """The six positive definitions, one paragraph each (LEAD_DESIGN.md section 8.2 item 5)."""
    lines = ["第 r8-1 回に監査役(定義)が通した 6 つの正の定義を、リードの設計(LEAD_DESIGN.md §8.2 の 5)によりここに 1 定義 1 段落で置いて"
             "凍結した。0・A・B・D・E は ROOTCAUSE_r8-1.md の段落と 1 字も変えていない(`test_battery_item0.py` が照らす)。C は"
             " LEAD_DESIGN.md §8.2 の 1 が撤回して置き換えた文(升目の判断の値と、その決め方)だけを替え、第 r13-1 回に、その中の `covers` の決め方の文を"
             " LEAD_DESIGN.md §9.2 の 34 で下の「第 r13-1 回の正の定義」を指す文に替えた。段落の中の「このファイル」は、"
             "その正の定義を当てる ROOTCAUSE のファイル(第 r8-1 回は ROOTCAUSE_r8-1.md)を指す。"]
    for k in "0ABCDE":
        lines += ["", f"正の定義 {k}: " + FROZEN_DEFINITIONS[k]]
    lines += ["", "リードの答え F(LEAD_DESIGN.md §9.2 の 32、逐語。批評家 i0-r11-02 へのリードの答えで、§9.2 の 34 がこの節に足すとした物): "
              + LEAD_F,
              "", "第 r13-1 回の正の定義(F を規則として前の起動の定義の段で場面係が書き、監査役(定義) r13-1-2 が [止める] 0 で"
              "通した文。ROOTCAUSE_r13-1.md §3 と 1 字も変えていない(`test_battery_r13_claims.py` が照らす)。F から変えた点は同じファイルの"
              "「リードに聞くこと」。升目の表はこの文のとおり `scenes.input_types`・`scenes.covers_of` が機械で作る): " + R13_DEFINITION]
    return "\n".join(lines)


def def_a_section() -> str:
    """The options of positive definition A (FROZEN_DEFINITIONS above), from the one data both this text and the test grid
    use (`def_axes/jA.tsv` through def_grids.derive; test_battery_def_grids.py runs the grid)."""
    import def_grids
    axes, pairs, problems = def_grids.derive("A")
    assert not problems, problems
    lines = ["場面の結果を対象に帰すかの決まり(上の正の定義 A)を、その段落の切片ごとの判断(`def_axes/jA.tsv`)から"
             "`def_grids.py` が書き出した軸と値。「通る」の値だけからなり、下の条件を全部満たす実行の結果だけを対象に帰す。"
             "同じ物から `test_battery_def_grids.py` が試験の升目を作り、機械(runner と `adapters/common.py`)の答えと照らす。", ""]
    for ax, vals in axes.items():
        lines.append(f"- {ax}: " + " / ".join(f"{v}({e})" for v, e in vals))
    if pairs:
        lines += ["", "条件(満たさなければ落ちる):", ""]
        for a, av, b, bv in pairs:
            lines.append(f"- {a} が {'・'.join(av)} ならば、{b} は {'・'.join(bv)}")
    return "\n".join(lines)


def render() -> str:
    out = [HEAD.replace("{L438}", "・".join(scenes.L438_2_SCENES)).replace("{N438}", str(len(scenes.L438_2_SCENES)))
           .replace("{FROZEN}", frozen_section()).replace("{DEF_A}", def_a_section()).replace("{GRID}", grid_section())]
    for vp, title in scenes.VIEWPOINTS.items():
        ss = [s for s in scenes.SCENES if s.viewpoint == vp]
        n_val = sum(1 for s in ss if s.kind == "value")
        out.append(f"\n## {vp} {title}\n\n場面 {len(ss)} 件(値の場面 {n_val} / 能力の場面 {len(ss) - n_val})\n")
        for s in ss:
            kind = "値の場面" if s.kind == "value" else "能力の場面"
            out.append(f"\n### `{s.id}` — {s.title}({kind})\n")
            out.append(f"- **入力**: {fmt_input(s.input)}")
            out.append(f"- **期待(正解)**: {fmt(s.expected)}")
            out.append(f"- **正解の出し方**: {s.derivation}")
            out.append(f"- **何を測るか**: {s.measures}")
            if s.graded_from:
                out.append(f"- **採点に使う値**: {s.graded_from}")
    out.append(f"\n## 件数\n\n場面 {len(scenes.SCENES)} 件。\n")
    return "\n".join(out)


def main() -> int:
    path = HERE / "DEFINITIONS.md"
    text = render()
    if "--check" in sys.argv:
        if path.read_text(encoding="utf-8") != text:
            print("DEFINITIONS.md differs from scenes.py; run gen_definitions.py")
            return 1
        print("OK")
        return 0
    path.write_text(text, encoding="utf-8")
    print(f"wrote {path} ({len(scenes.SCENES)} scenes)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
