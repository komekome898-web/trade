# 場面の定義 -- 項目0「核」(場面係、2026-09-23)

このファイルは `scenes.py` から自動生成される(`gen_definitions.py`)。場面を変えるときは `scenes.py` を編集してこのスクリプトを再実行すること -- 手で書き足さない(2文書が食い違う最大の原因)。

委任文 `docs/DATA/delegations/20260923_backtest_env_prompt.md` §3「場面集」の定義どおり、**既知解の場面**(合成の入力 + エンジンを見ずに手計算・閉じた式で出した正解。出し方を明記)と、**能力の場面**(「X ができるか」を実際に呼んで試す。単一の正解は無い)の2種類のみを置く。**道具の名前は書かない**(場面はどの対象に対しても同一)。

全 27 場面(観点別の内訳は下の見出し順)。

## 観点1: 事象駆動アーキテクチャ

### `v1-event_driven-cap` -- 1件ずつ事象を渡して駆動できるか(能力の場面)

- **入力**: `{'events': [{'kind': 'bar', 'ts_ns': 1700000000000000000, 'close': 100.0}, {'kind': 'bar', 'ts_ns': 1700000060000000000, 'close': 101.0}, {'kind': 'bar', 'ts_ns': 1700000120000000000, 'close': 102.0}, {'kind': 'bar', 'ts_ns': 1700000180000000000, 'close': 103.0}, {'kind': 'bar', 'ts_ns': 1700000240000000000, 'close': 104.0}]}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 対象へ5件の合成事象を1件ずつ(逐次)供給する呼び口があるか。一括の配列/DataFrameでしか受け付けられない場合は「持たない」。戦略コードが1事象ごとに1回呼ばれた回数を数える(5であれば逐次駆動、1であれば一括のベクトル処理とみなす)。

## 観点2: 事象型の網羅性

### `v2-fill-cap` -- 事象型「約定」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'fill', 'sample': {'ts_ns': 1700000000123456789, 'price': 5012345.0, 'qty': 0.01, 'side': 'buy'}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「約定」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-fill-known` -- 事象型「約定」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'fill', 'sample': {'ts_ns': 1700000000123456789, 'price': 5012345.0, 'qty': 0.01, 'side': 'buy'}}`
- **期待**: `{'ts_ns': 1700000000123456789, 'price': 5012345.0, 'qty': 0.01, 'side': 'buy'}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「約定」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-book_snapshot-cap` -- 事象型「板の写真」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'book_snapshot', 'sample': {'ts_ns': 1700000000123456789, 'bids': [[5012000.0, 0.5]], 'asks': [[5012500.0, 0.4]]}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「板の写真」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-book_snapshot-known` -- 事象型「板の写真」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'book_snapshot', 'sample': {'ts_ns': 1700000000123456789, 'bids': [[5012000.0, 0.5]], 'asks': [[5012500.0, 0.4]]}}`
- **期待**: `{'ts_ns': 1700000000123456789, 'bids': [[5012000.0, 0.5]], 'asks': [[5012500.0, 0.4]]}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「板の写真」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-book_delta-cap` -- 事象型「板の差分」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'book_delta', 'sample': {'ts_ns': 1700000000123456789, 'side': 'bid', 'price': 5012000.0, 'size': 0.3}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「板の差分」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-book_delta-known` -- 事象型「板の差分」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'book_delta', 'sample': {'ts_ns': 1700000000123456789, 'side': 'bid', 'price': 5012000.0, 'size': 0.3}}`
- **期待**: `{'ts_ns': 1700000000123456789, 'side': 'bid', 'price': 5012000.0, 'size': 0.3}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「板の差分」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-bar-cap` -- 事象型「足」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'bar', 'sample': {'ts_ns': 1700000000000000000, 'open': 100.0, 'high': 101.0, 'low': 99.0, 'close': 100.5, 'volume': 12.0}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「足」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-bar-known` -- 事象型「足」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'bar', 'sample': {'ts_ns': 1700000000000000000, 'open': 100.0, 'high': 101.0, 'low': 99.0, 'close': 100.5, 'volume': 12.0}}`
- **期待**: `{'ts_ns': 1700000000000000000, 'open': 100.0, 'high': 101.0, 'low': 99.0, 'close': 100.5, 'volume': 12.0}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「足」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-funding-cap` -- 事象型「資金調達」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'funding', 'sample': {'ts_ns': 1700000000000000000, 'rate': 0.0001}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「資金調達」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-funding-known` -- 事象型「資金調達」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'funding', 'sample': {'ts_ns': 1700000000000000000, 'rate': 0.0001}}`
- **期待**: `{'ts_ns': 1700000000000000000, 'rate': 0.0001}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「資金調達」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-liquidation-cap` -- 事象型「清算」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'liquidation', 'sample': {'ts_ns': 1700000000123456789, 'price': 4990000.0, 'qty': 0.2, 'side': 'sell'}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「清算」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-liquidation-known` -- 事象型「清算」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'liquidation', 'sample': {'ts_ns': 1700000000123456789, 'price': 4990000.0, 'qty': 0.2, 'side': 'sell'}}`
- **期待**: `{'ts_ns': 1700000000123456789, 'price': 4990000.0, 'qty': 0.2, 'side': 'sell'}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「清算」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-clock-cap` -- 事象型「時計」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'clock', 'sample': {'ts_ns': 1700000000000000000}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「時計」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-clock-known` -- 事象型「時計」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'clock', 'sample': {'ts_ns': 1700000000000000000}}`
- **期待**: `{'ts_ns': 1700000000000000000}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「時計」型の主要フィールドが欠落・丸め・変質なく保持されるか。

### `v2-order_notice-cap` -- 事象型「注文の受付/拒否/約定の通知」を扱えるか(能力の場面)

- **入力**: `{'event_type': 'order_notice', 'sample': {'ts_ns': 1700000000123456789, 'notice': 'accepted', 'order_id': 'synthetic-1'}}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 「注文の受付/拒否/約定の通知」型の事象を、実装のコードで確かめられる形で受理・処理できるか(持つ/持たない)。

### `v2-order_notice-known` -- 事象型「注文の受付/拒否/約定の通知」の値がそのまま往復するか(既知解の場面)

- **入力**: `{'event_type': 'order_notice', 'sample': {'ts_ns': 1700000000123456789, 'notice': 'accepted', 'order_id': 'synthetic-1'}}`
- **期待**: `{'ts_ns': 1700000000123456789, 'notice': 'accepted', 'order_id': 'synthetic-1'}`
- **正解の出し方**: 合成の1件をそのまま投入し、対象が保持/報告する値が入力と完全一致するかを見る。期待値は入力そのもの(恒等写像)なので手計算は不要 -- 一致しなければ、その型を値の損失なく保持できていないことを意味する。
- **何を測るか**: 「注文の受付/拒否/約定の通知」型の主要フィールドが欠落・丸め・変質なく保持されるか。

## 観点3: 時刻の精度

### `v3-precision-known` -- ISO8601(ナノ秒つき)の時刻を int64 UTC ナノ秒へ変換した値(既知解の場面)

- **入力**: `{'iso': '2024-01-01T00:00:00.123456789Z'}`
- **期待**: `1704067200123456789`
- **正解の出し方**: 2024-01-01 00:00:00 UTC は 1970-01-01 からのうるう年を数えて 19723 日 (1972,76,80,...,2020 の 13 回のうるう日を含む)。19723*86400=1,704,067,200 秒。小数部 .123456789 秒を ns に換算して加算: 1,704,067,200,123,456,789 ns。`python3 -c "import datetime as dt; print(int(dt.datetime(2024,1,1,tzinfo=dt.timezone.utc).timestamp()*1_000_000_000)+123456789)"` でも同じ値が出ることを確認済み(1704067200123456789)。
- **何を測るか**: ナノ秒精度のタイムスタンプを、丸めずに int64 UTC ナノ秒として保持できるか。

### `v3-precision-cap` -- 内部の時刻表現の型を申告できるか(能力の場面)

- **入力**: `{}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 対象が内部で時刻をどの型(int64 ns / float 秒 / その他)で持つかを、実装のコードまたは機械可読の契約から申告できるか。

## 観点4: ルックアヘッド防止の構造化

### `v4-lookahead-known` -- 時刻 t 時点で見える最大 close は bar[t].close を超えない(既知解の場面)

- **入力**: `{'bars': [{'ts_ns': 1700000000000000000, 'close': 100.0}, {'ts_ns': 1700000060000000000, 'close': 101.0}, {'ts_ns': 1700000120000000000, 'close': 102.0}, {'ts_ns': 1700000180000000000, 'close': 103.0}, {'ts_ns': 1700000240000000000, 'close': 104.0}, {'ts_ns': 1700000300000000000, 'close': 105.0}], 'probe_index': 3}`
- **期待**: `{'max_visible_close': 103.0, 'visible_count': 4}`
- **正解の出し方**: 入力は close が 100,101,102,103,104,105 と単調増加する 6 本の合成足。probe_index=3 の時点で「受け取れた時刻 <= 今」を満たすのは bars[0..3] の4本のみ(100,101,102,103)なので、見えてよい close の最大値は 103.0、件数は4。105 や 104 が見えていれば未来の事象が漏れている。
- **何を測るか**: 戦略側から観測できる事象が、探査時刻以前に受け取れたものだけに構造的に制限されているか。

### `v4-lookahead-cap` -- 将来事象への直接アクセス手段の有無(能力の場面)

- **入力**: `{'bars': [{'ts_ns': 1700000000000000000, 'close': 100.0}, {'ts_ns': 1700000060000000000, 'close': 101.0}, {'ts_ns': 1700000120000000000, 'close': 102.0}, {'ts_ns': 1700000180000000000, 'close': 103.0}, {'ts_ns': 1700000240000000000, 'close': 104.0}, {'ts_ns': 1700000300000000000, 'close': 105.0}], 'probe_index': 3}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 戦略コードから「今」より先の事象を取得できる公開の手段(索引アクセス・全件配列の直接参照など)が構造上そもそも存在しないか(存在しない=良い)。

## 観点5: 同時刻事象の決定的順序

### `v5-order-known` -- 同一タイムスタンプの事象列を2回処理して同じ順序が出るか(既知解の場面)

- **入力**: `{'events': [{'kind': 'fill', 'ts_ns': 1700000000000000000, 'tag': 'A'}, {'kind': 'bar', 'ts_ns': 1700000000000000000, 'tag': 'B'}, {'kind': 'order_notice', 'ts_ns': 1700000000000000000, 'tag': 'C'}, {'kind': 'clock', 'ts_ns': 1700000000000000000, 'tag': 'D'}]}`
- **期待**: `{'run1_equals_run2': True}`
- **正解の出し方**: 「決定的」の定義そのものが closed-form の正解を与える: 同じ入力を2回処理して得られる出力順序が一致することが、決定的順序規則を持つことの必要条件。一致しない場合、regardless of どんな規則を採用していても、その規則が実装に反映されていない(非決定的)と判定できる。
- **何を測るか**: 同一タイムスタンプの複数事象を処理したとき、出力順序が実行のたびに再現するか。

### `v5-order-cap` -- 同時刻の並びの規則が明記されているか(能力の場面)

- **入力**: `{'events': [{'kind': 'fill', 'ts_ns': 1700000000000000000, 'tag': 'A'}, {'kind': 'bar', 'ts_ns': 1700000000000000000, 'tag': 'B'}, {'kind': 'order_notice', 'ts_ns': 1700000000000000000, 'tag': 'C'}, {'kind': 'clock', 'ts_ns': 1700000000000000000, 'tag': 'D'}]}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 同一タイムスタンプの事象順序を決める規則を、対象が明記(実装のコードまたは契約)しているか。

## 観点6: 戦略 API の完全性

### `v6-order_lifecycle-known` -- 注文を1件発注して1件取消すと未決注文数が0に戻るか(既知解の場面)

- **入力**: `{'order': {'side': 'buy', 'qty': 1.0}, 'action': 'place_then_cancel'}`
- **期待**: `{'open_orders_after': 0}`
- **正解の出し方**: 発注1件・取消1件という単純な操作列なので、操作後の未決注文数は代数的に 1 - 1 = 0 。これはどんな注文管理の実装であっても成り立つべき不変条件。
- **何を測るか**: 発注APIと取消APIが対になって機能するか(戦略が明示的に呼べる形であるか)。

### `v6-api_surface-cap` -- 事象ごとのコールバック・発注・取消の3種の呼び口(能力の場面)

- **入力**: `{}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 戦略が呼べる公開APIとして (a) 事象ごとのコールバック (b) 発注 (c) 取消 の3種をそれぞれ持つか(0〜3の数)。

## 観点7: 拡張口(モジュール性)

### `v7-cost_swap-known` -- 費用模型をゼロコスト版に差し替えると手数料が0になるか(既知解の場面)

- **入力**: `{'order': {'side': 'buy', 'qty': 1.0, 'price': 100.0}, 'cost_model': 'zero'}`
- **期待**: `{'fee': 0.0}`
- **正解の出し方**: 「ゼロコスト模型」という定義そのものから手数料は恒等的に0。核を書き換えずにこの模型へ差し替えられれば、既定の非ゼロ手数料が0に変わって出力されるはず。差し替え口が無い実装は、この既知解を再現できない(対応なし、または既定値のまま)。
- **何を測るか**: 核のコードを書き換えずに費用模型を差し替えられるか(構造としての拡張口の有無を、値の変化で確認する)。

### `v7-extension_points-cap` -- 約定模型・遅延模型・費用・口座の4口(能力の場面)

- **入力**: `{}`
- **期待**: (無し -- 能力の場面。対象の実測をそのまま記録する)
- **何を測るか**: 約定模型・遅延模型・費用・口座の4要素それぞれについて、核を書き換えずに差し替え可能な口を持つか(0〜4の数)。
