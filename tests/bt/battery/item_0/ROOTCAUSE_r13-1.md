# 場面集への指摘ごとの根本原因と正の定義(項目 0、第 r13-1 回の直しの前の定義、場面係、第 2 版、2026-09-25)

合意した完了の形(オーナー逐語、L-405): 「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」

この回の場面集への指摘は [止める] 1 件(i0-r11-02)。委任文 §3「場面集」により、直す前に根本原因とこの指摘の族の正の定義をこのファイルに書き、その定義だけを監査役(定義)に先に通す。この族へのリードの答えは正の定義 F(LEAD_DESIGN.md §9.2 の 32。起動文の lead_notes に逐語)で、場面係は F を規則として §3 の定義を書いた。第 1 版の定義に監査役(定義)が [止める] 1 件(AUD-1)・[聞く] 1 件(AUD-2)を出したので、この第 2 版で §2.3・§2.4 を足し、§3・§4・§5・§6・§7・§8 を書き直した。F の内容を変えた点は §8.1(同じ文を返り値の `lead_answer_changes` に返す)。この段ではコード・場面・DEFINITIONS.md を変えていない。場面集の側で書いたのは、このファイルと、このファイルの行と語の判断(`line_judgments.tsv`・`term_judgments.tsv`。正の定義 B・D の試験が求める)だけである。確かめに打ったコマンドの出力は `<S>` = `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad/bt/` に置き(各出力の先頭に打った版とコマンドがある。場面集と批評家の試験のファイルは版 c2f5573 と 115112d で同じ = `<S>item0_r13-1_scenekeeper_def1_versions.txt`)、ここでは path と項の見出しで指す(正の定義 B)。

## 0. 対応表(CLAUDE.md §0.1。右は原文の逐語)

| やること | 原文の該当語(逐語) |
|---|---|
| 指摘ごとに根本原因を書く | 起動文「指摘 1 件ごとに「なぜ起きたか(根本原因)」と、この指摘の族に対する**正の定義**…を書く」 |
| 族の正の定義を 1 段落で書き、場所や形の一覧にしない | 起動文「場所や形の一覧ではなく、どの形にも当たる 1 段落の規則」 |
| F を規則として自分の定義を書く | 起動文「この族へのリードの答えは次の正の定義 F…で、場面係はこれを規則として自分の定義を書く」 |
| 監査役(定義)の指摘 AUD-1・AUD-2 を受けて定義を書き直す | 起動文「場面集…に監査役が次の指摘を出した(逐語)」「前の定義(監査役が [止める] を出した)」 |
| F の内容を変えた点と理由を §8.1 に書き、同じ文を `lead_answer_changes` に返す | 起動文「F の内容を変える必要があると読むなら、変えた点と理由を ROOTCAUSE_r13-1.md の節「リードに聞くこと」に書き、同じ文を返り値の lead_answer_changes に返す」 |
| 新しい一般規則・手続き・登録簿・用語の表を足さない | 起動文「定義は指摘の族への 1 段落で、新しい一般規則・手続き・登録簿・用語の表を足さない(足したい規則は「リードに聞くこと」に書いて止める」 |
| コードを直さない | 起動文「**まだコードを直さない。**」 |
| 直しの段で行うことを §6 に書く(§9.2 の 33・34・36、ROOTCAUSE_r12-1.md の見出し、批評家の試験) | 起動文「直しは §9.2 の 33・34・36 のとおり。ROOTCAUSE_r12-1.md は「採らなかった案(第 12 周の定義 3 版)」と見出しを付けて残す。批評家の試験 tests/bt/critic/item_0/test_i0r11_covers_within_scene_input.py(13 件)を変えずに通す。」 |
| 候補 35 を同じ 3 手で走らせ、検討表の表の行を直す(直しの段) | 起動文 (3)「候補 35 はリードが走らせた(§9.2 の 36)。場面係が同じ 3 手で走らせ、検討表の行を直す。」 |
| このファイルの行と語の判断を足す | DEFINITIONS.md 正の定義 B「この正の定義を当てる検査は、当てる範囲の全部のファイルの行をファイルの行ごとの判断で覆い」+ 正の定義 D「表の全部の語と裸で使わない語のこのファイルの中の出現に出現ごとの判断があることを示す」 |
| 提出前の吟味の記録をこのファイルに書く | 起動文「直しの前の定義なら ROOTCAUSE のファイルに書く」 |

## 1. 指摘の一覧(正の定義 E の (a) の元)

| id | 格 | 相手 | repeat_of(指摘の値) | 族の読み(場面係の読み。数えるのは台本と監査役) |
|---|---|---|---|---|
| i0-r11-02 | [止める] | 場面集 | null | 升目の表の「場面にした」が、場面の入力より広い宣言(`covers`)から出る族 |
| AUD-1 | [止める] | 定義(第 1 版) | null | 第 1 版の §4 が「宣言は升目を足せない」を全部の軸について書いたが、注文の 3 つの通知の升目では入力が発注の頼みの有無までしか決めず、3 つのどれを数えるかは宣言が決める(指摘の文は、これを i0-r11-02 の族のこの形と書く) |
| AUD-2 | [聞く] | 定義(第 1 版) | null | 第 1 版の §3 の「取消の頼み → 取消の通知」が、取消の通知が要件 §1 の事象の型の外(`OUTSIDE_AXES` の区分)であることを書いていない |

i0-r11-02 の逐語は起動文と `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/round_11/CRITIC.md` の「i0-r11-02」の節、AUD-1・AUD-2 の逐語は起動文(id は起動文の逐語のまま)。第 12 周の定義の段(r12-1-1〜3)への監査役(定義)の指摘は、リードの戻しで族の鎖が 0 からになった(LEAD_DESIGN.md §9.2 の 32、L-438)ので、この表に入れない。

## 2. なぜ起きたか

### 2.1 i0-r11-02(場面集の作り)

- **根 1(「走らせうる」を「測った」として宣言した)**: `scenes.py` の `COVERS` の注(版 c2f5573 の 554-559 行)は、型を対象に任せる場面は走らせうる市場の事象の型の全部を覆う、と決めた。場面の作り(型を対象に選ばせる・代えを許す)は正の定義 C の「ほかの観点が測るもので決まらないように作る」に従ったもので、それ自体は変えない。だが「どの型でも走る」は「その型が入力にある」ではない。宣言の決まりが前者を後者として升目に書いた。
- **根 2(同じ事実の元を 2 つ持ち、一方から他方を作らず、照らしもしなかった)**: 場面が何を入力に持つかの元は `scenes.py` の入力である。`covers` はそれと別に手で書いた一覧で、第 r11-1 回に升目の判断を `covers` だけから決める形にした(LEAD_DESIGN.md §8.2 の 1)とき、`covers` を入力と照らす機械も試験も置かなかった。第 r11-1 回の升目の試験は表を `covers` と照らすだけで、神託の元と実装の元が同じ `covers` だった。
- **根 3(宣言を、測る物と照らさずに置いた)**: 正の定義 C は「場面の測る物と `covers` が合うかは批評家が読んで確かめ」と書き、場面係は `covers` を書くときにその読みを自分では当てなかった。
- **根 4(知っていた幅を、直さずに文で説明して上げた)**: 第 r11-1 回の場面係は、`covers` が場面の測る物より広いことを提出前の吟味の指摘の候補に挙げ(ROOTCAUSE_r11-1.md §7)、宣言の仕方を 1 文足してリードに上げた(同 §8)。表の値は誤ったまま審査員とオーナーへ渡る所に出た。
- **なぜ前の回の試験で見つけられなかったか**: 根 2 のとおり、試験の入力の空間を `covers` から作っていた。入力から型を独立に出す神託が無かった。批評家の再現: `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== reproduce the critic's count (H, market axis)」(時計の升目は市場の事象の型でないので数えていない。批評家の数との差はこの 1 件)と、`<S>item0_r13-1_scenekeeper_def1_pytest_before.txt`。

### 2.2 リードの答え F を字のまま計算すると、同じ向きの升目が出る(§3 で F を 2 点変えた根)

- **根 5(型と見る道の組を、どこにも持たせていない)**: 升目は (e, v, x) の 3 つ組で、どの型をどの見る道で見るかの組は場面の性質である。入力は型の集まりだけを、(v, x) の宣言は見る道と追加の軸の集まりだけを持ち、組を持たない。F は e を入力から、(v, x) を宣言から別々に取るので、升目は両者の積になる。入力に 2 つ以上の型(市場の事象と、頼みから出る型)を持ち、2 つ以上の (v, x) を宣言した場面では、場面が組にしていない型と見る道の升目が「場面にした」になる。字のまま計算した結果: `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== F(発注→3 通知)」の「broadened」の下の出力の行と、`<S>item0_r13-1_scenekeeper_def1_lists.txt` の項「== F」(判断は同じで、場面の列が広がる升目)。向きは i0-r11-02 と同じ(測っていない範囲を狭く見せる、通過の報告に有利な向き)。
- **根 6(神託の外を、誰も見ない)**: 批評家 i0-r11-02 の神託は、升目の市場の事象の型が入力にあるかだけを見る。積の広がりは型が入力にある升目で起きるので、神託に見えない(`<S>item0_r13-1_scenekeeper_def1_subset.txt` の項「== critic oracle」: F の積でも神託に落ちる場面が無い)。F の「(v, x) が s の測る物と合うかは批評家が読む」でも止まらない。広がりの元になる (v, x) は、同じ場面の別の型には正しい宣言なので、批評家は (v, x) を外せない。組を決める物は、意味の読み(F が禁じる)を除けば場面の宣言しか無い。
- **根 7(発注の頼みから出る 3 つの通知)**: F の括弧は「注文の通知(受付/拒否/約定)」で、発注の頼みは 3 つの通知の型を出す。3 つのうちどれが起きるかは、頼みと市場と口座の組み合わせの帰結で、それを入力から決めるのは F が禁じる意味の読みである。機械が確かめられるのは「発注の頼みがある」までで、3 つのうちどれを測るかは場面の宣言にしか無い。F の積の形ではこれも根 5 の広がりになる(`<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== F(発注→3 通知)」の P0-3・P0-6・P0-7 の通知の升目)。
- **根 8(頼みを読む欄が決まっていない)**: F は判断を機械で 2 値とするが、戦略の頼みを入力のどの欄から読むかを決めていない。版 c2f5573 の入力で頼みの種類を値として持つ欄は `<S>item0_r13-1_scenekeeper_def1_requests.txt` の項「== keys that name a request kind as data (not free text) in any scene input」、発注・取消の頼みの在りかは同じ出力の項「== per scene: input keys other than events/streams, and the raw strategy / timer_at_ns fields」。文を語で探す読み方は、言い換えや別の文を指す書き方で黙って外れうる(`…_measure.txt` の項「== per scene」の頼みの列は、測るためだけに語で探した物で、判断の元にしない)。

### 2.3 AUD-1(第 1 版の §4 が、注文の 3 つの通知の升目でも宣言は升目を足せないと言い切った)

- **根 9(言明を軸ごとに照らさずに言い切った)**: 第 1 版の §4 の §8.5 の 24 の表の行は、市場の事象の型の升目で測った結果(`<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== I(宣言∩入力, 発注→3 通知)」の broadened)から書き、同じ文書の根 7 と第 1 版の §3 の最後から 2 つ目の文が書いた注文の 3 つの通知の升目と照らさずに、全部の軸について言い切った。言明の範囲(どの軸で確かめたか)を書かなかった。
- **根 10(入力から出る物の細かさと、事象の軸の値の細かさが違う)**: 市場の事象は事象ごとに型の欄を持つので、事象の軸の値 1 つずつが入力から出る。戦略の頼みは頼みの種類を持つだけで、時計の頼みは事象の軸の値 1 つに、取消の頼みは軸の外の値 1 つに当たるが、発注の頼み 1 つは事象の軸の値 3 つ(注文の受付・拒否・約定の通知)に一緒に当たる(`<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (1) event-axis values of the grid and the input-derived group each belongs to」)。第 1 版の §3 は「型 e が入力から出る」を値 1 つずつの言明として書き、発注の頼みを「3 つの通知が出る」とだけ書いたので、3 つとも「入力から出る」を満たし、3 つのどれを数えるかを宣言だけが決めることが定義の本文の数え方から見えなかった。§4 の言い切りを止める文が定義に無かった。
- **根 11(3 つの間の選びを決める元が、F が使わないとした物にしか無い)**: 発注の頼みから受付・拒否・約定のどの通知が起きるかは、頼みと市場と口座を対象の約定の模型と口座が処理した帰結である。入力から機械で出すには、対象の出力か runner の記録を読むか、正解がその通知を名指すかを読むことになり、どれも F が使わないとした物である。だからこの 3 つの間の選びは、F の中では機械で閉じられない。実測: 宣言の写しで、入力に無い市場の事象の型を足した升目は第 1 版の数え方で数えられず、注文の通知を発注の頼みから一緒に出る別の通知に替えた升目は数えられる(`<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (3) swap probe on copies of the declarations (definition 1 applied to the copy)」)。第 1 版の数え方で注文の 3 つの通知の升目のうち宣言が選んでいる升目と、その場面は同じ出力の項「== (2) definition 1's counted cells on the order-notice group (viewpoint, event, see, extra) -> scenes」。

### 2.4 AUD-2(取消の通知が要件の事象の軸の外であることを、第 1 版の §3 が書いていない)

- **根 12(F の括弧の字から写し、今の場面集の区分を定義に持ち込まなかった)**: 第 1 版の §3 の頼みの対応は F の括弧(「取消 → 取消の通知」)から写した。F は升目の表の升目を数える物で、取消の通知は要件 §1 の事象の型の外なので、F の字だけでは区分が見えない。版 115112d の場面集のコードでは、升目の表の事象の軸は要件の文の判断からだけ作られ(grid_c.py の関数 axes)、軸に無い `covers` の値は gen_definitions.py の関数 grid_section が `scenes.py` の `OUTSIDE_AXES` の理由と一緒に DEFINITIONS.md の軸の外の行に出す。取消の通知がそのどこにあるかは `<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (4) where the cancel notice sits」。直しの段で `covers_of` を §3 の文だけから書き直すと、取消の通知を要件の事象の軸の値と同じ扱いにする書き方を止める文が、第 1 版の定義に無かった。

## 3. この指摘の族の正の定義

**升目を「場面にした」と数える物**: 升目 (e, v, x) を場面 s が「場面にした」と数えるのは、s がその升目 (e, v, x) を宣言し、かつ型 e が s の入力から機械で出るときだけである。宣言と入力の両方が要り、どちらか一方だけでは数えない(入力は s が宣言しない升目を足せない)。入力から機械で出る型は次の 2 つだけである。市場の事象の型(約定・板の写真・板の差分・足・資金調達・清算)は、scenes.py が s の入力に載せた事象(`events` と `streams` の中身)の型の欄そのもので、型の欄 1 つが型 1 つを出す(`type_plan` の場面は `for_target_types(s, TYPE_ORDER)` の入力の型の欄。批評家 i0-r11-02 の神託 `_kinds(scene)` と同じ計算。型の欄の無い事象と、事象を持たない場面からは型を出さず、adapter が代えた型は数えない)。市場の事象の型では入力が型そのものを決めるので、宣言は入力に無い型の升目を足せない。時計・注文の受付の通知・注文の拒否の通知・注文の約定の通知・取消の通知は、s の入力が欄として持つ戦略の頼みから出る: 時計の頼みは時計を、発注の頼みは注文の受付・拒否・約定の通知の 3 つを一緒に、取消の頼みは取消の通知を出す。取消の通知は要件 §1 の事象の型の外で、升目の表の事象の軸に入れず、`scenes.py` の `OUTSIDE_AXES` の区分の軸の外の升目として別に出す。入力の文(`strategy` の文など)を読んで頼みを決めない。発注の頼みについて入力が機械で決めるのは 3 つの通知が一緒に出ることまでで、3 つのうち s がどれを測るかは入力から機械では決まらない(決めるには、下で使わないとした対象の出力・runner の記録・「正解がその事象を名指すか」を読むことになる)。よって注文の 3 つの通知の升目では、機械が決めるのは「発注の頼みを持たない s の宣言は数えない」までで、3 つのどれを数えるかは s の宣言だけが決める。宣言の e が入力から出ない升目は数えず、そういう宣言があれば試験が落ちる(黙って捨てない)。adapter が書く値・対象の出力・runner が残す記録・「正解がその事象を名指すか」「その道を通さなければ正解にならないか」の意味の読みは使わない。判断は機械で 2 値。宣言した升目が s の測る物と合うか(注文の 3 つの通知のどれを数えるかの宣言を含む)と、s の入力がその道を実際に呼ぶかは批評家が読む(LEAD_DESIGN.md §8.5 の 24)。この定義は升目の表と `covers` だけに当て、場面の正解・採点・比較の表のセルを変えない。

## 4. 定義と、固定した規則・リードの答えとの関係(監査役(定義)の検め LEAD_DESIGN.md §8.2 の 4 の 2 つ目のため)

| 固定した物 | 関係(場面係の読み) |
|---|---|
| 正の定義 F(LEAD_DESIGN.md §9.2 の 32) | 市場の事象の型の計算(神託 `_kinds` と同じ)・型の欄の無い事象と事象を持たない場面・adapter の代え・頼みから出る型の対応・使わない物・機械で 2 値・批評家が読む物は F の文のとおり。変えたのは 2 点(§8.1): (1) 数える升目を「(v, x) の宣言 × 入力から出る型」の積から「宣言した升目 (e, v, x) のうち e が入力から出るもの」に、(2) 頼みを「入力の頼み」から「入力が欄として持つ頼み」に。注文の 3 つの通知の間の選びは、F では積で 3 つとも数える形で、§3 では宣言が選び批評家が読む形にした(§8.1 の 1 の最後の文)。取消の通知を事象の軸の外に置く文は、F の内容を変えず、版 115112d の区分を保つ文である。使わない物に「runner が残す記録」を足したのは §9.2 の 33 の「表の判断には使わない」の文による |
| LEAD_DESIGN.md §8.5 の 24(`covers` の事象の軸は入力に実際に含まれる型から機械で出す) | 市場の事象の型の升目では、数える升目の e は入力の型の欄から機械で出た型で、宣言は入力に無い型の升目を足せない(`<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (3) swap probe on copies of the declarations (definition 1 applied to the copy)」の最初の出力の行)。時計の頼みと取消の頼みは頼み 1 つが値 1 つ(時計は事象の軸の値、取消の通知は軸の外の値)に当たるので、頼みの欄が機械で決める。注文の 3 つの通知の升目では、機械が閉じるのは発注の頼みの有無までで、3 つのどれを数えるかは宣言が決め、批評家が読む(同じ出力の項「== (2) definition 1's counted cells on the order-notice group (viewpoint, event, see, extra) -> scenes」と、項「== (3) …」の 2 つ目からの出力の行)。24 の「入力に実際に含まれる型から機械で出す」を、この 3 つの間の選びでは満たしていない(§2.3 の根 11。§8.2 の 4 に上げた) |
| 同 §9.2 の 33(記録は表の判断に使わない。`types_in` ⊆ F で出した型の試験) | 表の判断に記録を使わない。試験は §6 の 3 のとおり、頼みの欄にも同じ形の試験を置く |
| 同 §9.2 の 34(`COVERS` の事象の手書きの一覧を消し、(v, x) の宣言だけを残し、`covers_of(s)` を F で計算) | §8.1 の 1 により「`COVERS` を升目の宣言として残し、入力から出ない型の宣言を消し、`covers_of(s)` を §3 で計算する」に替わる(§6 の 1) |
| 同 §8.6 の 30(p6-place-then-cancel の見る道が合うかは批評家の読み) | §5.1 のとおり、批評家の読みに任せ、場面係は直しの段でこの場面の宣言を変えない |
| 凍結した正の定義 C「`covers` は場面の宣言で、場面の測る物と `covers` が合うかは批評家が読んで確かめ」 | 直しの段で §3 の文に替える(§9.2 の 34 がリードの決定として替えることを決めた)。批評家が読む物は §3 の最後から 2 つ目の文 |
| 凍結した正の定義 C の事象の軸の文「§1 の事象の型と、各観点の文が名指す事象の型(通知は §1 の語どおり受付・拒否・約定の 3 つ)」 | 取消の通知を事象の軸に入れない。§3 の取消の頼みの文は、`OUTSIDE_AXES` の区分と、gen_definitions.py の関数 grid_section が作る DEFINITIONS.md の軸の外の行を保つ(版 115112d で読んだ。`<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (4) where the cancel notice sits」) |
| 凍結した正の定義 C「この表は…場面の定義と升目の軸から機械で作り」 | 表は scenes.py の宣言と入力だけから作り、記録に依らないので、場面集の指紋は対象の結果で変わらない(§9.2 の 34 の L-435 の文と同じ) |
| 同 §8.2 の 1(升目の判断は機械で 2 値) | 2 値のまま。注文の 3 つの通知の升目にも第 3 の値を足さない(宣言が選ぶことは §6 の 2 の表の前の文に書く) |
| 凍結した正の定義 A の型の組(L-438 (2)) | `type_plan` の場面の入力と採点は変えない。e は F どおり `for_target_types(s, TYPE_ORDER)` の入力から出す。対象ごとに入らなかった型の升目は §9.2 の 33 の注記で出る |
| 場面集の規則 2(振る舞いを試し、作りの形を試さない) | 場面の採点は変えない |
| CLAUDE.md §0.2 O-7(用語の意味を変えて使い回さない) | 表の判断の値「場面にした」の決め方が「`covers` に持つ」から §3 に替わる。直しの段で DEFINITIONS.md の決め方の文(正の定義 C)を §3 に替え、今の版の文書では 1 つの決め方だけにする。過去の周の文書の「場面にした」は、その周の版の決め方のまま |

## 5. 定義を計算した結果(直しの段の範囲)

- §3 の形で計算した結果は `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== I(宣言∩入力, 発注→3 通知)」(今の `covers` と比べて広がる升目の数と、狭まる升目の出力の行)と、`<S>item0_r13-1_scenekeeper_def1_lists.txt` の項「== I」。第 2 版の §3 は数える升目を第 1 版から変えていない(変えたのは、注文の 3 つの通知の升目で宣言が選ぶことと取消の通知の区分を、定義の文に書いたことだけ)。狭まる升目は批評家の数え(`<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== reproduce the critic's count (H, market axis)」)と同じ升目で、広がる升目は無い(`<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== I(宣言∩入力, 発注→3 通知)」の「broadened」)。§3 の升目が F の升目の部分集合であることは `<S>item0_r13-1_scenekeeper_def1_subset.txt` の項「== I within F, per scene」。
- 注文の 3 つの通知の升目のうち、宣言が選んでいる升目と場面は `<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (2) definition 1's counted cells on the order-notice group (viewpoint, event, see, extra) -> scenes」。機械はこれらの升目で発注の頼みの有無だけを検め、3 つのどれかは批評家が読む。
- 頼みから出る型は、この計算では頼みを文の語で探して代えた(§2.2 の根 8)。直しの段で頼みの欄を足したあと、欄から計算し直す。
- P0-2 の升目: §3 と F のどちらでも、型の欄の無い事象と事象を持たない場面からは型を出さないので、P0-2 の升目の判断は `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== I…」の観点ごとの数のとおりになる。これは F の文が決めたことで、場面係は条件を足さない。

### 5.1 批評家が「人の読みで残る点」として名前を挙げた場面

- 場面と升目: p6-place-then-cancel の `covers` の升目(版 c2f5573 の `COVERS` の鍵「p6-place-then-cancel」)。
- §3 で決まる部分: その升目の e が注文の 3 つの通知のどれかなら、機械が決めるのは入力の発注の頼みの有無までで、3 つのどれを数えるかはこの場面の宣言が決める。宣言した升目が場面の測る物と合うかは、LEAD_DESIGN.md §8.6 の 30 のとおり批評家の読みに任せる。場面係は直しの段でこの場面の宣言を変えない。

## 6. 直しの段で変える作り(定義が監査役(定義)を通ったあとに行う。この段では行っていない)

1. **`scenes.py`**: `COVERS` を場面ごとの升目 (e, v, x) の宣言として残し、入力から出ない型の宣言(§5 の狭まる升目)を消す。`covers_of(s)` を §3 で計算する関数にし、`SCENES` の `covers` をそれにする。取消の通知の宣言は `OUTSIDE_AXES` の鍵と同じ字のまま残し、升目の表の事象の軸に入れない。注(554-559 行)を §3 の文に替える。戦略の頼みを持つ場面の入力に、頼みの種類を値として持つ欄を足す(値は文の頼みと同じ。場面の測る物・正解・採点は変えない)。
2. **表**(`grid_c.py`・`gen_definitions.py`・DEFINITIONS.md): 升目の判断を `covers_of` から作る。DEFINITIONS.md の正の定義 C の「`covers` は場面の宣言で」の文を §3 に替え、§3 を正の定義の節に足す(凍結の変更はリードの決定 LEAD_DESIGN.md §9.2 の 34)。gen_definitions.py の関数 grid_section の表の前の文(「`covers` は場面の宣言で…」)を §3 に合わせ、注文の 3 つの通知の升目では機械が発注の頼みの有無までを検め、3 つのどれを数えるかは宣言が決めて批評家が読むことを書く。凍結の段落を照らす場面係の試験(`test_battery_item0.py` の正の定義 C を照らす試験)を、替えた文に合わせて書き直し、書き直したことと理由を記録に書く(場面集の規則 8 の後半)。
3. **runner**(§9.2 の 33): 場面ごと・設定つき対象ごとに `types_in` と `requests` を記録に残す(表の判断には使わない)。試験: 記録の `types_in` ⊆ §3 で入力から出た型、記録の `requests` ⊆ 入力の頼みの欄。資料係の表の注記に、対象ごとの「場面にしたが、この対象には型が入らなかった升目」を機械で出す。
4. **試験を先に書く**(委任文 §3「提出前の吟味」(6)): §3 の文だけから書いた神託と `covers_of` を、入力を変えた写しの格子で照らす(事象の型の欄を消す・事象を空にする・`type_plan` の組を変える・頼みの欄を足す・消す・宣言に入力から出ない型を足す・宣言の (v, x) を変える・取消の頼みの欄を足す・消す、の組み合わせ)。宣言に入力から出ない型があれば落ちることと、取消の通知の升目が升目の表の事象の軸に入らず軸の外の区分に出ることを確かめる。列に入れない物(宣言した升目が測る物と合うか、注文の 3 つの通知のどれを数えるかの宣言 = 批評家の読み。写しで宣言の通知を 3 つの中の別の通知に替えても機械は落とさないこと)を試験のファイルに書く。
5. **批評家の試験** `tests/bt/critic/item_0/test_i0r11_covers_within_scene_input.py` を変えずに通す(直しの前は `<S>item0_r13-1_scenekeeper_def2_pytest_before.txt` の末尾の数)。
6. `ROOTCAUSE_r12-1.md` に「採らなかった案(第 12 周の定義 3 版)」の見出しを付けて残す。
7. **候補 35**(§9.2 の 36): リードが打った 3 手を同じく打って走らせ、検討表の表の行を直し(結果が「対応なし」でも記録に残す)、`python3 scripts/check_bt_considered.py <検討表> --write` を誤り 0 件にする。
8. 変えたファイルの行と語の判断を `line_judgments.tsv`・`term_judgments.tsv` に足し、出現の無い判断を消す。mutant と場面集の試験と全試験を回す。

## 7. 提出前の吟味(委任文 §3。書き方は DEFINITIONS.md の正の定義 E)

切片まで書き出さずに読み直したもの: 委任文(全 165 行)、固定した要件 REQUIREMENTS.md、委任文 §3 の場面集の規則 1〜9・「提出前の吟味」・「場面集」・「格付けの基準」、LEAD_DESIGN.md の §8・§9(版 115112d)、DEFINITIONS.md の正の定義 0〜E、指摘 i0-r11-02 の逐語と批評家の試験、監査役(定義)の指摘 AUD-1・AUD-2 の逐語、`scenes.py` の `COVERS`・`OUTSIDE_AXES`・`for_target_types`、`grid_c.py`、gen_definitions.py の関数 grid_section、`line_marks.py`・`term_marks.py`。

| 指摘の候補 | 何をしたか |
|---|---|
| i0-r11-02 | §2.1 に根 1〜4 を書いた。§3 に定義を置いた。批評家の数えを出力 `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== reproduce the critic's count (H, market axis)」で作り直した。直しの作りを §6 に置いた |
| AUD-1 | 出力 `<S>item0_r13-1_scenekeeper_def2_axes.txt` を作った(項「== (1) …」「== (2) …」「== (3) …」)。§2.3 に根 9〜11 を書いた。§3 に、市場の事象の型では宣言が入力に無い型の升目を足せないこと、注文の 3 つの通知の升目では機械が決めるのは発注の頼みの有無までで 3 つのどれを数えるかは宣言が決めること、を分けて置いた。§4 の F の表の行と §8.5 の 24 の表の行を書き分けた。§5 の 2 つ目・§5.1・§6 の 2・4 に足した。§8.1 の 1 に文を足した。§8.2 の 4 に上げた |
| AUD-2 | 出力 `<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (4) where the cancel notice sits」を作った。§2.4 に根 12 を書いた。§3 に取消の通知が要件 §1 の事象の型の外で `OUTSIDE_AXES` の区分に置くことを足した。§4 に正の定義 C の事象の軸の表の行を足した。§6 の 1・4 に足した |
| 場面係が足した候補: F を字のまま計算すると、型と見る道の積で i0-r11-02 と同じ向きの升目が出る | 出力 `…_measure.txt` の項「== F(発注→3 通知)」・`…_lists.txt` の項「== F」・`…_subset.txt` を作った。§2.2 の根 5〜7 に書いた。§3 の数え方を替えた。§8.1 の 1 に上げた |
| 場面係が足した候補: F は頼みを読む欄を決めておらず、文を語で探す読み方は黙って外れうる | 出力 `…_requests.txt` を作った。§2.2 の根 8 に書いた。§3 に「欄として持つ頼み」と置いた。§6 の 1・3 に欄と試験を置いた。§8.1 の 2 に上げた |
| 場面係が足した候補: 第 2 版の §3・§4・§5 に、第 1 版の §4 と同じく軸を書かずに全部の軸について言い切る文が残る | §3・§4・§5 の言明を、市場の事象の型・時計・注文の 3 つの通知・取消の通知に分けて照らし直した(出力 `<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (1) …」)。§4 の §8.5 の 24 の表の行と §8.2 の 1 の表の行、§5 の 1 つ目の文を書き分けた |
| 場面係が足した候補: 注文の 3 つの通知の間の選びを、F が使わないとした物(正解が名指す通知・対象の出力・記録)で機械に閉じる | 採らなかった。§2.3 の根 11 に理由を書いた。§8.2 の 4 に上げた |
| 場面係が足した候補: 注文の 3 つの通知の升目を、宣言が選ぶので「測っていない」にする | 採らなかった(場面が測る升目を測っていない側に振る)。§3 で宣言と批評家の読みに置いた。§8.2 の 4 に上げた |
| 場面係が足した候補: 直しの段で足す頼みの欄を adapter が読まず、欄が宣言の 2 つ目の元になる(根 2 と同じ形) | §6 の 3 の試験(記録の `requests` ⊆ 欄)が捕まえるのは欄より多く頼む向きだけであることを §8.2 の 5 に上げた |
| 場面係が足した候補: 宣言を残すことで、宣言が入力を超える形(根 2)が戻る | §3 に「宣言の e が入力から出ない升目は数えず、そういう宣言があれば試験が落ちる」と置いた。§6 の 4 に試験を置いた。注文の 3 つの通知の間ではこの試験が届かないことを §3 と §6 の 4 に書いた |
| 場面係が足した候補: 宣言が測る物より狭い形は、機械が見ない | §3 の最後から 2 つ目の文で批評家の読みの側に置いた(F と同じ) |
| 場面係が足した候補: P0-2 の升目の判断が F の文だけで決まる所を、場面係が条件で変える | §5 の 4 つ目で、条件を足さないと書いた |
| 場面係が足した候補: 定義が新しい一般規則・手続き・登録簿・用語の表を足す | §3 を 1 段落にした。太字で名付けた語を足していない。足した語「欄として持つ頼み」を §8.1 の 2 に上げた |
| 場面係が足した候補: 表の判断の値「場面にした」の決め方が替わる(O-7) | §4 の表の最後の表の行に書いた。§6 の 2 に DEFINITIONS.md の文の置き換えを置いた |
| 場面係が足した候補: 表が記録に依って場面集の指紋が周ごとに変わる(L-435) | §3 で記録を使わない物の側に置いた。§4 の表に書いた |
| 場面係が足した候補: このファイルに場面の中身を書く(正の定義 B) | 数と型と頼みの在りかと、宣言が選ぶ升目は出力の path と項の見出しで指した。版を 115112d に固定した(出力 `…_def1_versions.txt`、`…_def2_axes.txt` の先頭の行) |
| 場面係が足した候補: このファイルが名指す出力の項の見出しが出力に無い | 自己検査を作って走らせた(出力 `<S>item0_r13-1_scenekeeper_def2_cites.txt`) |
| 場面係が足した候補: 批評家が名前を挙げて残した場面への答えが無い | §5.1 に置いた |
| 場面係が足した候補: 第 12 周の定義の段の指摘を族の鎖に数える | §1 の表の下に、鎖が 0 からであることと出所を書いた |
| 場面係が足した候補: このファイルの行と語の判断が無く、場面集の試験が落ちる | 判断を足し、出現の無い判断を消した。場面集の試験と批評家の試験を走らせた(出力 `<S>item0_r13-1_scenekeeper_def2_judge.txt`・`<S>item0_r13-1_scenekeeper_def2_pytest_after.txt`) |
| 場面係が足した候補: この段でコードを変える | 出力 `<S>item0_r13-1_scenekeeper_def2_status.txt` を作った。場面係が書いたのは ROOTCAUSE_r13-1.md・`line_judgments.tsv`・`term_judgments.tsv` だけで、同じ出力のほかの path は場面係が書いた物ではない |
| 場面係が足した候補: 候補 35・ROOTCAUSE_r12-1.md の見出しを直しの段から落とす | §6 の 6・7 に置いた |

## 8. リードに聞くこと

### 8.1 正の定義 F の内容を変えた点(返り値の `lead_answer_changes` と同じ文)

1. 升目の数え方を、F の「s が (v, x) を宣言し、かつ型 e が s の入力から機械で出る」(型と見る道を別々に取った積)から、「s が升目 (e, v, x) を宣言し、かつ型 e が s の入力から機械で出る」(宣言と入力の両方が要る)に変えた。理由: 升目は型と見る道と追加の軸の 3 つ組で、どの型をどの見る道で見るかの組は、入力にも (v, x) の宣言にも無い。F を字のまま計算すると、入力に 2 つ以上の型(市場の事象と、頼みから出る型)を持ち 2 つ以上の (v, x) を宣言した場面で、場面が組にしていない型と見る道の升目が「場面にした」になる(版 c2f5573 の出力 `<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== F(発注→3 通知)」の broadened の下の出力の行と、`<S>item0_r13-1_scenekeeper_def1_lists.txt` の項「== F」。`<S>` = `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad/bt/`)。向きは i0-r11-02 と同じ(測っていない範囲を狭く見せる)。批評家の神託は型が入力にあるかだけを見るので積の広がりを捕まえず(`<S>item0_r13-1_scenekeeper_def1_subset.txt` の項「== critic oracle」)、批評家が (v, x) を読んでも、その (v, x) は同じ場面の別の型には正しい宣言なので外せない。意味の読みを使わずに組を持つのは場面の宣言だけである。変えた形の升目は F の升目の部分集合で、F が数えない升目を足さない(`<S>item0_r13-1_scenekeeper_def1_subset.txt` の項「== I within F, per scene」、`<S>item0_r13-1_scenekeeper_def1_measure.txt` の項「== I(宣言∩入力, 発注→3 通知)」の broadened)。これに伴い、F の「手書きの宣言…は使わない」は「手書きの宣言は升目の上限としてだけ使い、宣言だけでは数えない。宣言の型が入力から出なければ試験が落ちる」に替わり、§9.2 の 34 の「`COVERS` の事象の手書きの一覧を消し、(v, x) の宣言だけを残し」は「`COVERS` を升目 (e, v, x) の宣言として残し、入力から出ない型の宣言を消し、`covers_of(s)` を宣言と入力の両方から計算する」に替わる。ただし「宣言は上限としてだけ使う」が成り立つのは、入力が値 1 つずつを決める型(市場の事象の型・時計・軸の外の取消の通知)だけである。発注の頼みは注文の受付・拒否・約定の通知の 3 つを一緒に出し、3 つのどれを s が測るかは入力から機械で決まらない(決めるには F が使わないとした対象の出力・記録・正解の名指しを読むことになる)ので、注文の 3 つの通知の升目では、機械が決めるのは発注の頼みの有無までで、3 つのどれを数えるかは宣言だけが決め、批評家が読む(`<S>item0_r13-1_scenekeeper_def2_axes.txt` の項「== (3) swap probe on copies of the declarations (definition 1 applied to the copy)」)。F の積の形では、この 3 つを発注の頼みを持つ場面の全部の (v, x) について 3 つとも数える。
2. 戦略の頼みの読み方を、F の「s の入力の戦略の頼み」から「s の入力が欄として持つ戦略の頼み(入力の文を読んで頼みを決めない)」に変えた。理由: F は判断を機械で 2 値とするが、頼みを入力のどの欄から読むかを決めていない。版 c2f5573 の入力で頼みの種類を値として持つ欄は `<S>item0_r13-1_scenekeeper_def1_requests.txt` の項「== keys that name a request kind as data (not free text) in any scene input」のとおりで、発注・取消の頼みの在りかは同じ出力の項「== per scene: input keys other than events/streams, and the raw strategy / timer_at_ns fields」のとおりである。文を語で探す読み方は、言い換えや別の文を指す書き方で黙って外れうる。直しの段で、戦略の頼みを持つ場面の入力に頼みの種類を値として持つ欄を足し(場面の測る物・正解・採点は変えない)、runner の記録 `requests`(§9.2 の 33)が欄の中にあることを試験で確かめる。

### 8.2 そのほか

3. **§7 の記録と O-10(正の定義 E)**: §7 は正の定義 E の書き方(表の列 2 つ・提出前の吟味の判定の語を置かない)で書いた。O-10 に触れるかを決めるのは場面係ではない(正の定義 E)。
4. **注文の 3 つの通知の間の選び(AUD-1)**: §3 では、注文の 3 つの通知の升目で 3 つのどれを数えるかを宣言が決め、批評家が読む。LEAD_DESIGN.md §8.5 の 24 の「入力に実際に含まれる型から機械で出す」を、この 3 つの間の選びでは満たしていない。F の中で機械に閉じる元は無い(§2.3 の根 11): 閉じるには、F が使わないとした物(例: 正解の欄が名指す通知の種類)のどれかを使う決定が要り、場面係は決めない。逆に 3 つの升目を「測っていない」に振るのは、場面が測る升目を測っていない側に置くので採らなかった。この扱いでよいか、閉じる決定をするかを聞く。答えが来るまで §3 のとおりにする。
5. **頼みの欄の向き(§6 の 1・3)**: 直しの段で足す頼みの欄は今は無いので、今の adapter はどれもそれを読まない(版 115112d の adapters/new_impl.py では、場面ごとの関数 scene_p3_notice_accepted などが頼みを手で書く。ほかの adapter の頼み方は読んでいない)。§6 の 3 の試験(記録の `requests` ⊆ 欄)は、戦略が欄より多く頼む向きを捕まえるが、欄が戦略の頼まない頼みを持つ向き(欄が宣言の 2 つ目の元になり、升目を広く見せる向き)は捕まえない。この向きを試験で見るなら記録と欄の逆向きの照らし(例: 新実装の記録で欄 ⊆ `requests`)を足す決定が要り、規則を足すことになるので場面係は決めない。答えが来るまで、§3 の最後から 2 つ目の文(s の入力がその道を実際に呼ぶかは批評家が読む)の読みに置く。
