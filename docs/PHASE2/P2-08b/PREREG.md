# P2-08b Binance 先行の秒スケール捕捉(メイン BOT champion の秒足版)— 事前登録(第 3 稿 2026-09-07、設計監査 2 回目の要修正 6 件を反映、3 回目待ち)

## 仮説(`docs/PHASE2_TRIAGE.md` #28b、オーナー指示 L-011)
Binance の秒スケールの値動きは bitFlyer FX_BTC_JPY に有限の時間で伝播する。その伝播が完了する前に追随する taker 取引に、
実測費用後で正となる窓(閾値・レイテンシ・保有)が存在し、かつ日次の円換算がサーバ費を上回るか。

**旧棄却との差(問いの差別化)**: 旧研究は 1 窓の断面で閾値 × 遅延の面を走査し棄却した(数値はリードのみ保持、実装者・監査者には開示しない)。本単位が新たに問うのは
(i) 分足 P2-08 で見えた「Binance を先読みすると正」が分内のどの秒に存在するか(分内分解)、(ii) 条件の向き(低ボラと高ボラの両側を同じ格子で)、
(iii) 伝播時間分布の長期変化(2019〜2026)。閉じる条件は §第 1 層に数値で書く。(i)〜(iii) のいずれも出せなければ「重複・判定不能」で閉じる。

## 格子セル
市場 = bitFlyer Crypto CFD(FX_BTC_JPY)/ 執行様式 = taker(成行)/ 時間スケール = 秒 / 情報源 = 他市場(Binance BTCUSDT 現物 aggTrades。無期限・Bybit は対照)

## 調達票と盲点監査
`PROCUREMENT.md`(2026-09-06 実測)、`BLINDSPOT_AUDIT.md`(13 指摘)、`DESIGN_AUDIT_1.md`(8 件)。要点:
| 必要データ | 経路 | 到達確認 | 欠けと対処 |
|---|---|---|---|
| bitFlyer 約定(ms / μs) | REST 31 日スナップショット(`executions_FX_BTC_JPY_31d_20260823.csv.gz`、ms)+ WS 記録(`paper_logs/tape/executions_*.csv.gz`、μs)→ 正規化 `bitflyer_executions_us_20260723_20260906/` | 連続窓 2026-07-23 12:09 .. 08-25 18:29(33.3 日)、断片込み 44.5 / 45.3 日 | 長期は無い。Tardis(有料、2019-08-30〜)は無料サンプル(毎月 1 日)を診断 (iii) に使う。購入は §最終評価の引き金 |
| Binance BTCUSDT 現物 **aggTrades**(信号、取引所刻印) | data.binance.vision 日次 zip → `binance_BTCUSDT_aggTrades_20260723_20260906/` | 2017-08-17〜 全日 200 | 1s klines は信号に**使わない**(同一秒内の先行を潰す。監査 1 回目 実測 #5)。長期(2017〜)は診断 (iii) 専用 |
| 対照の信号源 | Binance USDT 無期限 aggTrades(2019-12〜)、Bybit 公開 csv | 到達 200 | 対照 5 のみ(探索面に入れない) |
| 円市場の長期代理 | bitbank `/btc_jpy/transactions/YYYYMMDD`(キー不要、2017-03〜、ms) | 全日 200 | 診断 (iii) の計測器。**2026-08-23 以降は読まない**(封印期間の円市場を含むため) |
| 板厚・サーバ費 | `paper_logs/tape/board_top5_*`、WS 板、`constants.yaml` | 板厚上限 0.02 BTC・サーバ費 33〜50 円/日(旧報告、`assumed` 扱い) | 実装時に再測し `constants.yaml` に `measured`(確認日つき)で登録してから判定に使う |
Tardis 由来は利用規約 9.2 により **`data/tardis/`(gitignore 域)** に置き、封印台帳にはパスと MD5 のみ。Tardis の 2026-09-01 サンプルは封印期間なので読まない。

## データと封印
- 主系列(primary)= bitFlyer 約定 2026-07-23 12:09 .. 2026-09-06(REST ms + WS μs。重なり 08-20〜08-23 で id 突合)。
- **封印台帳に登録するファイル(一意)**: `executions_FX_BTC_JPY_31d_20260823.csv.gz`、`paper_logs/tape/executions_YYYYMMDD.csv.gz`(08-20〜09-06)、`bitflyer_executions_us_20260723_20260906/*.csv.gz`(正規化日次)、
  `binance_BTCUSDT_aggTrades_20260723_20260906/*.csv.gz`、`binance_BTCUSDT_1s_20260723_20260906/*.csv.gz`、`binance_um_BTCUSDT_aggTrades_20260723_20260906/*.csv.gz`。ticker・board_top5 は登録しない(信号にも判定にも使わない。板厚の実測のみ、封印外の 07-23〜08-22 に限る)。
  `sealed.py` は μs epoch(`ts_us`)を解釈できるよう拡張済み(2026-09-07、テスト付き)。`load_diagnostic(path, cutoff)` は**新規実装**(既存に無い)。
  **封印台帳は作成済み**(2026-09-07、154 ファイル、`--seal-from 2026-08-23`、forward_start 2026-09-07)。Binance の本体データは容量のため git 管理外(`docs/DATA_LOSS_REGISTER.md` #15)だが、
  SEALED.json にパスと MD5 を登録済みで改変検知は効く。再取得は `scripts/fetch_binance_vision.py --kind aggTrades` で MD5 突合できる。
- **封印**: 主系列の暦 70/30 floor = **2026-08-23 00:00 UTC**(`phase2_seal.py --unit P2-08b --seal-from 2026-08-23`、REST 31 日・テープ日次・Binance 日次 07-23〜09-06・正規化済み日次を全て登録)。
  開発セット = 07-23 .. 08-22(REST 由来、単一刻印 ms)、train = 07-23 .. 08-12(21 日)、val = 08-13 .. 08-22(10 日)。封印 = 08-23 .. 09-06(WS 由来 μs)+ フォワード。
  開発 → 封印で計測系が REST → WS に変わる: 重なり 08-20〜23 で両系の結果差(欠け率・価格)を既知欠陥 (2) として報告し、最終評価に併記。
- 診断専用系列(Tardis サンプル日 2019-09-01 .. 2026-08-01、bitbank 2017-03 .. 2026-08-22、Binance 長期 2017-08 .. 2026-08-22)は **封印外だが境界 08-23 以降を含まない**よう日付で切る。
  構造: 主指標モジュールは `load_unsealed(path, "P2-08b")` 経由で SEALED.json 登録ファイルしか開けない。診断モジュールは別関数 `load_diagnostic(path, cutoff="2026-08-23")` で cutoff 以降の行を落とす。
- 既知欠陥: (1) 刻印の混在(REST ms / WS μs / Tardis 受信 / Binance 取引所刻印 ms→2025 年から μs)→ UTC μs に正規化、出所列を持つ。
  (2) WS の欠け 0.6〜1.9%(時間分布とバースト集中の有無を報告)。(3) 日次メンテ窓 UTC 18:59〜19:12 と 5 分超の無約定は除外し件数報告。
  (4) 約定の無い時間は「観測なし」(信号判定は Binance 側のみ、bitFlyer 側は次の約定で評価)。(5) 自宅 PC の時計(手順 P7 の同期が前提、ticker は使わない)。
  (6) **取引所間のずれ δ_clock(報告のみ、補正しない)**: 開発セットで aggTrades × bitFlyer 約定の交差相関を 10 ms 刻み(±2 秒)で日別に取り、嵐日を除いた日別中央値を報告する。
  監査 2 回目の実測ではピーク +110〜170 ms(中央値 120)で、これは時計差ではなく伝播 + 反応時間を含む量。補正すれば先行を消すので**信号や約定の時刻には適用しない**。λ 感度の解釈にのみ使う。
  (7) 同一刻印の束(約定の 58.8%): 約定価格の規則は §第 2 層で一意化。(8) 事象の重複: 発火後 max(w, 5) 秒は再発火なし、建玉中も再発火なし(件数を除去前後で報告)。
  (9) 封印部分の実効日数は 13.7 日(WS の穴 2 か所を除く)。(10) Tardis 2026-08-01 サンプルは REST 31 日と行数一致(17,194、監査 2 回目)= 同じ約定集合。
- 開示: リードは旧研究の数値を知っている。実装者・監査者は `docs/` の旧報告と `src/bot/strategy/` を読まない(盲検)。

## 第 1 層: 伝播の計測(取引なし、存在の層)
- 事象 = Binance aggTrades から作る直近 w 秒のリターン |r_B| ≥ s(s ∈ {10, 20, 30} bps、w ∈ {1, 2, 5} 秒)。t0 = 事象を確定させた aggTrade の取引所刻印。重複規則は既知欠陥 (8)。
- 応答 A(τ1, τ2) = bitFlyer の t0+τ1 以降の最初の約定価格から t0+τ2 以降の最初の約定価格へのリターン(bps、r_B の符号に揃える)。
- **主量 = A(τ, τ+5)**(τ から 5 秒保有した場合の粗リターン、第 2 層の保有と同一)、τ ∈ {0.1, 0.2, 0.5, 1, 2, 5, 10, 30} 秒。併記 = A(0, τ)/|r_B|(織り込み率)。
  95% CI は事象を日でクラスタしたブートストラップ 2,000 回、seed 20260907。
- **実用窓 τ*** = A(τ, τ+5) の CI 下限が往復の保守費用(§コスト)を超える最大の τ(該当する τ が無ければ τ* = 0)。τ* < λ(0.1 秒)なら第 2 層は形式的に実行するが反証文が成立(§最終評価)。
- 条件分析(標準 §6): ボラ三分位(60 秒実現ボラ、train で境界固定)× 主量。**(ii) の判定文**: 低ボラ − 高ボラ の A(0.1, 5.1) の差について、CI がゼロを含まず |差| ≥ MDE →「候補(向き = 符号)」、CI がゼロを含み幅 < 2 × MDE →「差なし」、それ以外 →「判定不能」(`state_split` の規則)。
- **(i) 分内分解**: P2-08 の分足信号(k=15/thr 0.4、k=30/thr 0.8 の 2 点。開発 31 日で発火分は約 1,451 / 632)が発火した分について、A(0,5)・A(5,15)・A(15,60) を**絶対 bps の CI**で出す(割合は分母 ≈ 0 で不成立、監査 2 回目)。
  判定文: A(0,5) の CI がゼロを含まず正 →「最初の 5 秒に存在」、A(5,15) または A(15,60) のみ正 →「5 秒より後に存在」、すべて CI ゼロ含む →「判定不能」。
- **(iii) 長期変化**(標準 §5): Tardis サンプル日(2019-09〜2026-08、毎月 1 日、最大 84 日)で年別に A(0.1, 5.1) と τ* を出す。単位 U = 年、窓 W = 12 サンプル日、ブロック長 = 1 日、
  傾き(bps/年)と傾きの MDE、判定文は §5.7 の規則。「縮小していない」= 傾きの CI 上限 > 0。年別 n が 12 日分しか無いので MDE を先に出し、MDE > |傾き| なら「判定不能」。
  bitbank 9 年でも同じ表(円市場の代理、判定に使わない)。監査 2 回目の見込み: 傾き MDE は s=10 と (20,5) で 0.26〜0.66 bps/年(判定可能)、他は ≥ 1.5 bps/年(判定不能見込み)。標本日の Binance aggTrades 84 本は調達票に追加(データ準備で取得)。

## 第 2 層: 取引可能性(探索面、事前に固定)
| パラメータ | 候補 | 備考 |
|---|---|---|
| s(bps) | 10, 20, 30 | 発火閾値 |
| w(秒) | 1, 2, 5 | 信号窓 |
| λ(ms) | 100 固定(判定)、50 / 500 は感度 | 受信 p50 61ms + 判断 + REST 46ms ≈ 110ms(既存実測) |
| 手仕舞い | t0 + λ + 5 秒以降の最初の約定で成行(固定)。保護ストップなし | |
N0 = 3 × 3 = **9**。サイズ 0.01 BTC。**監査 2 回目の実測(aggTrades 29 日 × REST、重複除去・約定規則適用後)**: 開発 n は s=10 で 257〜674、s=20 で 28〜88、s=30 で 9〜28(約定規則による減少 0〜7.8%)。
最終評価に必要な n_X = (2.8016 σ / 2.0)² への到達は s=10 で 6〜27 日、**s ≥ 20 の 6 構成は 185〜9,045 日 = 全て 180 日超**。
→ **s ≥ 20 の 6 構成は着手前の棄却候補(頻度不足)で、実質の探索面は s=10 の 3 構成**。9 構成すべて実行し帰無も 9 で取るが、最終評価の対象になり得るのは到達見込み ≤ 180 日の構成のみ。反復 0 で実測し確定。
**約定価格の規則(一意)**: 建玉 = t0 + λ 以降の最初の bitFlyer 約定の刻印束のうち、買いなら束内最高値・売りなら束内最安値(不利側)+ 片道費用。
その約定が t0 + λ + 1 秒より後なら「約定なし」で取引しない(件数報告)。手仕舞い = t0 + λ + 5 秒以降の最初の束の不利側価格。待ちが 30 秒を超える取引は除外(件数報告)。
**主指標** = val の保守費用後・1 取引平均 net(bps)と CI(日クラスタ・ブートストラップ 2,000 回)。**選択統計量はこれ一本**。日次円換算 = 取引数/日 × 0.01 BTC × 価格 × net は経済の停止条件と最終バーにのみ使う。
**経済の停止条件(主検定より前)**: val 最良構成について**開発セット 31 日全体**の日次円の点推定を、(a) 全日、(b) 日次円が最大の 1 日を除外、の 2 通りで出し(val 10 日のみでは嵐日 08-19/20 で符号が反転する、監査 2 回目)、
**両方が < 100 円/日**(サーバ費 50 円/日 × 2)なら封印を開けずに棄却。片方のみなら「嵐日依存」と明記して続行。
帰無の構成(累計 N 通りの最良): Binance の秒リターンを日ブロックで置換して事象を作り直し(bitFlyer 側はそのまま)、9 構成の最良、2,000 回、95 点(帰無 A = 1 取引平均、帰無 B = 日次円)。
反復の梯子(2 段で終了): 反復 1 = ボラ三分位(9 × 3、N = 36)。反復 2 = なし。停止規則: 反復 1 の val 改善 < 選ばれたセルの MDE で早期停止。

## コスト定数(`config/constants.yaml`、`assumed` は判定に使用不可)
| キー | 値 | source_type | 状態 |
|---|---|---|---|
| bitflyer_fx_btc_jpy.realized_taker_one_way_bps | [1.0, 1.3] | measured | 登録済み |
| bitflyer_fx_btc_jpy.burst_factor_1s(新規) | 事象時刻 ±1 秒の実効半スプレッド ÷ 無条件(構成別) | measured(実装時、確認日つき) | 反復 0 で登録 |
| bitflyer_fx_btc_jpy.top_depth_btc_p50(新規) | tape の最良気配数量の中央値 | measured | 反復 0 で登録 |
| infra.server_cost_jpy_per_day(新規) | 33〜50 | measured(クラウド東京の料金ページ URL と確認日) | 反復 0 で登録 |
保守 = 片道 1.3bps × バースト係数 × 2 + 資金調達(決済時刻またぎのみ)。楽観 = 片道 1.0 × 2。判定は保守。

## 指標と分母
分母 = 開発セット 31 日の取引数(構成ごと、重複除去後)。σ・MDE は反復 0 で実測して台帳に確定(上の見込みを置き換える)。事象数も除去前後で報告。

## 対照(反事実)
1. 全時刻無作為(同じ保有、1,000 回)。2. 符号シャッフル(1,000 回)。3. Binance 秒リターンの日ブロック置換(帰無と同じ)。4. 先行なし: bitFlyer 自身の秒リターンで同じ規則。
5. 信号源の置換: Binance USDT 無期限、Bybit 現物。6. 診断: (a) λ 感度 50 / 100 / 500 ms(δ_clock 込み)、(b) WS 欠けを REST で補った場合の差(重なり 08-20〜23)、(c) 束内の最良側価格を採った場合の差(執行の上限)。

## 停止規則
経済の停止条件 → 反復 1 で終了。反復 8 回の一般規則は使わない。

## 最終評価
- 対象 = val 最良構成 1 点(選択統計量 = val 1 取引平均 net)。構成ごとに **n_X = (2.8016 × σ / 2.0)²**(MDE を 2.0bps にするための取引数、σ は開発セット実測)を反復 0 で表にし、
  封印 + フォワードで **n ≥ n_X かつ暦 ≥ 14 日** に到達した時点で 1 回評価。到達判定は **Binance 側の事象数だけを数える count-only スクリプト**(bitFlyer 側の結果を読まない、実行ログを `UNSEAL_LOG` に残す)で行う。
  到達見込みが 180 日超なら「判定不能・頻度不足」で閉じる。
- バー: 帰無 A・B の 95 点超、かつ保守費用後の 1 取引平均の CI が正、かつ日次円の点推定 > 100 円/日。
- 反証文: 「第 1 層で τ* < λ(0.1 秒)」または「経済の停止条件に該当」または「最終評価で 1 取引平均の CI がゼロを含む」なら本仮説は棄却。
- 引き金(資源化): 第 1 層で τ* ≥ 0.5 秒かつ (iii) で縮小していない場合のみ、Tardis 購入($350/月、四半期払い)を週次要約の判断項目に載せる。

## 実行可能性
フォワードは PC の WS 記録(fetch_all 所有、share_logs 06:30 JST)に依存。既存手順 P7(PC 時計の NTP 同期)の確認報告を前提条件とする。

## 既知正解テスト(凍結条件)
手作りの Binance aggTrades 列と bitFlyer 約定列(60 秒、同一刻印束・約定なし区間・メンテ窓を含む)で、(a) 事象の確定時刻と重複除去、(b) λ 後の最初の束の不利側価格での建玉と「約定なし」判定、
(c) 5 秒後の手仕舞いと待ち上限、(d) A(τ1, τ2) の計算、(e) δ_clock の適用、を手計算 5 件と一致させる。
