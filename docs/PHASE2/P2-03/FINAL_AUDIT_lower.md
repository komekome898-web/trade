# P2-03 最終評価 下位監査(独立再実装、blind)

自分の数値は先に `FINAL_AUDIT_lower_own_numbers.json` に書き出してから
`RESULTS.md`/`RUN.json` を開いた(手順どおり)。読んだファイル: PROTOCOL.md,
PREREG.md, ITER.md, schema/jpx_etf_daily.json, config/constants.yaml,
src/bot/research/sealed.py, src/bot/research/overnight.py,
scripts/data_quality.py(split_candidate 部分のみ)、その後 RESULTS.md /
RUN.json。p2_03_final.py / p2_03_run.py は開いていない(数値一致度から
再現性は確認済みと判断し、budget を検証優先に使った)。

## 自分の数値 vs 報告値(除外後、封印期間 1,102 行 → 端点規則 1,101 ペア)

| 系列 | n(自分/報告) | 楽観平均 bps(自分/報告) | 保守平均 bps(自分/報告) |
|---|---|---|---|
| 1306.T | 1,098 / 1,098 | 7.4950 / 7.4950 | -4.2711 / -4.2711 |
| 1591.T | 1,099 / 1,099 | 12.1776 / 12.1776 | 7.2888 / 7.2888 |
| 2516.T | 1,099 / 1,099 | 5.8972 / 5.8972 | -30.3352 / -30.3352 |
| 1321.T | 1,099 / 1,099 | 7.4697 / 7.4697 | 3.0264 / 3.0264 |

CI(ブロック20・2,000回・seed 20260906)も全系列で小数点以下まで一致
(例: 1591 保守 [1.3446, 13.3629] 自分 = [1.345, 13.363] 報告)。帰無95点
(符号シャッフル・グロス)も一致(1306: 5.186/5.187、1591: 4.910/4.924注、
2516: 6.353/6.278注)。※自分は sign_shuffle_null を n=2,000 で回したため
報告(n=1,000、PREREG §対照1どおり)と僅差だが結論(超えた/超えない)は
全系列で一致。**検証結果: 再現(headline は全て一致、結論も同一)。**

## 欠陥規則(封印期間内のみ)

- 規則1(誤プリント連): 1306.T 2026-03-30/31(k=2, c0=2026-03-27 close
  382.70円、復帰行2026-04-01 close 389.20円、+1.70%)を独立に検出 —
  **要求どおり捕まえた**。他3系列は規則1該当0。
- 規則2(split_candidate): 1306.T の2026-03-30が独立実装でも発火(比率
  ×0.098、翌日持続)。これは規則1の連の一部で、報告書§3も同じ重複を
  明記しており除外ペア数に影響なし(規則1が既に3ペア除外)。
- 規則3(null): 1591/2516/1321 に各1行(2025-10-24、既知欠陥どおり)、
  1306は0。規則5(幽霊行)は封印期間内では全系列0件(幽霊行はいずれも
  開発セット側)。
- 除外ペア数: 1306=3、他3系列=各2。n=1,098/1,099/1,099/1,099で一致。

## 1591(JPX400)の重点確認

独立計算で保守CI = **[1.345, 13.363] bps、ゼロを含まない**(平均
7.289bps)。帰無95点(4.91〜4.92bps)も超過、1321と符号一致(共に正)。
**3条件すべて満たすとの報告は独立に確認できた。**

## バー・反証文・保留区分(自分の数値から評価)

- バー(3条件同時): 1591=満たす、1306・2516=満たさない(1306はバー2で
  CIがゼロを跨ぐ、2516はバー1・2とも未達)— 報告と一致。
- 反証文(存在): ゼロを含む銘柄=2516の1/3のみ→棄却条件(2件以上)未達
  →「存在する」は棄却されない — 一致。
- 反証文(取引可能): 1306はCIがゼロを含み該当、2516はCIが完全に負
  (ゼロを含まない)ため文言上は非該当だが実質「取引不能」がより強く
  確定している点は報告書も§13の逸脱記録で同様に注記済み — 一致。
- 保留区分「存在するが取引不能」: 1306のみ該当(楽観正・保守非正)。
  2516は既に保守側が有意に負なので「取引不能」はより確定的、1591は
  該当なし(保守側も正)。

## MD5・封印記録の確認

対象4系列CSVのmd5sumを独立に計算し、SEALED.json・RUN.json双方の値と
一致(4/4)。UNSEAL_LOG.jsonlは全32行が unit="P2-03" のみ(P2-01/P2-02
への漏洩なし)。ただし報告書§13が述べる「3回×8ファイル=24行」に対し
実ログは32行(24行+8行)。8行のうち4行は本監査自身の load_sealed
クロスチェック。残り4行(主系列4本のみ、本監査実行の約1.6秒前)は
RESULTS.mdの説明と対応が取れず出所不明 — unit漏洩ではないが軽微な
記録上の欠落として指摘する(判定は変えない)。

## 前提の誤り

なし(既知欠陥・定数の出所は事前登録どおりで、独立計算でも矛盾は
見つからなかった)。2516の帰無95点(6.35bps)が実測平均(5.90bps)を
上回る点はPREREG自身が「検出力不足」と事前に警告済みで、齟齬ではない。

## 総合判定

**再現**(全 headline 数値が報告値と一致し、結論も同一)。読み順の推奨:
PREREG.md → ITER.md → 本監査 → RUN.json/RESULTS.md → (上位監査があれば)
その報告、の順で読むこと。
