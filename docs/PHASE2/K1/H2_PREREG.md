# H2a — ヒゲ先端の無効化(損切り)を外し、反対シグナルだけで決済する(事前登録 + 2 × 2 の読み方)

**オーナー決定 2026-09-10**: 第 2 周 = H2(L-072)、形は **H2a**(L-073)。
**オーナー提案(L-072)**: ① H2a 単独(効果の確認)と ② H1 + H2a(掛け合わせに相乗効果があるか)の 2 つを出す。
**この文書を書き終えてから実装を走らせる。** 書いていないことは決まっていない。

**守るもの**: 帰無・MDE・判定バーは作らない(L-056)/ 経費は引かない(L-048)/ 建玉状態は外さない / 引き算をしない(交互作用は「並べた 4 マス」から読者が計算できる形で出し、
それ自体を判定に使わない)/ **封印は開けない** / 数字は生成 / 区間はセルごとに種を固定 / 原典と**並べる**。

---

## 0. ループ台帳(`H1_PREREG.md` §0 の続き。周回数は多重性に算入)

| 周 | 機構仮説 | 何を変えるか | 出所 | データ | 状態 |
|---|---|---|---|---|---|
| 1 | 実体 ≥ ヒゲ の足は実体の戻りが後続を決める | 向きを実体と逆に(H1) | L-070 | 探索区間 + Binance | 第 10 部 |
| **2** | **ヒゲ先端の損切りは、戻りが終わる前に閉じている**(第 7 部・第 9 部 §9.2-5) | **無効化の枝を外す。決済は反対シグナル(弱い = 決済のみ / 強い = ドテン)だけ**(H2a) | L-072 / L-073 | **同じデータ** | **実施済み → `RESULT.md` 第 11 部**(① = 第 7・8 部と一致 234/234 × 2。② 5 分・両方: BitMEX +3.15\* / Binance −0.60。交互作用は「両方」で 103/108 セル正、大きさは標準誤差の桁)|

**第 2 周の機構仮説は H2a の 1 つ。** ② は新しい仮説ではなく、**同じ H2a を第 1 周の土台(H1)に載せたときに効きが変わるか**を読む腕である。
判定は設計ごとに一度、封印かフレッシュデータで。**第 2 周の測定も in-sample の確認であり、判定ではない。**

## 1. 意図マップ追補

| # | 意図 | 出所 | 実装 | 印 | 備考 |
|---|---|---|---|---|---|
| I-8(無効化ライン) | 反発の根拠が壊れたら降りる(ヒゲ先端の無効化) | 原典 | **本周では外す**(`simulate(..., use_invalid=False)`) | **✕(意図的に外す)** | **カツオの意図(I-8)を外す変更**である。オーナー承認 L-073。外す理由は第 7 部の測定(損切りで終わる取引の中央値が負、外すと平均が +5.7〜+9.2 bp)。**「損切りを外せばよい」と読まない**(裾が広がる)|
| I-4 / I-5 | 強弱で決済とドテンを分ける / 建玉が無ければ弱いでも建てる | 原典 | 変更なし | ○ | 反対シグナルの決済はそのまま。無効化が無いので**建玉は反対シグナルが来るまで持ち続ける** |
| I-12(H1) | 実体 ≥ ヒゲ の足は実体を逆張り | L-070 | ② の腕でだけ有効 | ○ | ① では無効 |

## 2. 規則(H2a: 1 つだけ変える)

```
simulate(bars, sigs, keep=None, use_invalid=True):
    行動対象でない足:
        if use_invalid and 建玉あり and 足の色が建玉と反対 and 終値がラインを割った → 決済(invalidated)
        # use_invalid=False なら何もしない(建玉は続く。lcline も見ない)
    行動対象の足: 原典どおり(同方向 = ラインだけ更新 / 反対 = 決済、強いならドテン / 無し = 新規)
```

- これは第 7 部 `measure_katsuo_exit_ablation.py` の `opposite_only` **そのもの**である。**再現ゲート**: ① の 234 セルが `exit_ablation.json` の `opposite_only|…` と n・平均で一致すること(区間は種の付け方が違うので一致しない。第 7 部の注記どおり)
- `use_invalid=True`(既定)の出力は原典と同一(テストで固定)
- 期間末に残った建玉は取引に数えない(原典と同じ)

## 3. 2 × 2 の設計と測るもの

| マス | シグナル | 決済 | 出力 | 状態 |
|---|---|---|---|---|
| 原典 | 原典 | 原典 | `effect.json` | 第 2 部・第 8 部 |
| H1 | `flip_body=True` | 原典 | `effect_flipbody.json` | 第 10 部 |
| **① H2a** | 原典 | `use_invalid=False` | **`effect_noinval.json`** | 本周 |
| **② H1 + H2a** | `flip_body=True` | `use_invalid=False` | **`effect_flip_noinval.json`** | 本周 |

- 機構だけを測る(シグナル単体は決済を持たないので H2a で変わらない)。族は第 2 部と同じ 234 セル、両取引所
- 出す表(`render_k1_h2.py`、生成物): (a) 4 マス横並び 足 × 門 13 × 強さ 3(平均・区間・sd・p05・p50・p95・保有中央値・n)(b) **交互作用** = (②) − (H1) − (①) + (原典) を「両方」「弱い」「強い」の全セルで(符号と大きさ。区間は出さない = 判定に使わない)(c) 門 `s19/b24` の年別 4 マス(d) 決済理由の内訳
- 分位(p05 等)を出すために `measure_katsuo_effect.py` の出力に分位を足す(既存 JSON の n・平均・区間は変えない)

## 4. 見込み(事前に書く。**経費前、in-sample**)

| 量 | 見込み | 根拠 |
|---|---|---|
| ① H2a 単独 | 第 7・8 部の `opposite_only` **そのもの**。5 分・弱い: BitMEX +6.93、Binance +0.29。5 分・両方: BitMEX +0.34、Binance −3.54。sd 1.4 倍・p05 1.9 倍・保有 2 倍 | 表 Q / 第 7 部(再現ゲート) |
| ② の基準線(足し算) | 「両方」5 分: BitMEX +0.34 + (+0.89 − (−1.18)) = **+2.41**、Binance −3.54 + (−1.77 − (−3.93)) = **−1.38**。**これは交互作用ゼロのときの値**であって見込みではない | 第 10 部の H1 の上がり幅を ① に足す算術 |
| ② の交互作用の符号 | **書かない(見込み無し)**。考えられる向きは両方ある: (+) 反転した足は新しい無効化ラインが入口に近く(H1_PREREG §5)、それを外すと反転した足の取り分が増える / (−) 損切りが無いと反転した足も反対シグナルまで持ち続け、実体の戻りが終わった後の動きを抱える | 【測っていない】 |
| 「強い」(① で) | 原典より改善するが負のまま(BitMEX 5 分 −5.32 → −1.98、Binance −6.42 → −7.11 = 悪化) | 表 Q |
| `b-` の門 | ② は H1 で反転が起きないので ① と一致 | H1_PREREG §2 |

## 5. 前提(バイアスの向きつき)

| 前提 | 向き | 出所 |
|---|---|---|
| 損切りを外すと裾が広がる(sd 1.4 倍、p05 1.9 倍) | 平均は上がるが**分散改善ではない**。採用の議論には分散の情報が要る(research-protocol §9) | 第 7 部の実測 |
| 保有が伸びる(中央値 2 倍)ので、年をまたぐ建玉・期末の未決済が増える | 取引数がわずかに減る | 実装の帰結 |
| 交互作用を「両方」で読む | 「弱い」「強い」の枝は建玉機械の状態が違うので、足し算が成り立つ保証はもともと無い。交互作用は**参考量** | 設計 |

## 6. 出さないもの

判定・帰無・MDE・バー・経費・封印・H2b(保有上限)・H3〜H5。**規則は 1 つ**(無効化を外す)。
