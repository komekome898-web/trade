# K1 独立監査(層 3)の全件トリアージ — 29 件

**監査の実施**: 2026-09-09。事前登録・意図マップ・洗い出し・原典コード・準備測定**だけ**を渡し、
`OWNER_LOG` / `INCIDENTS` / `DISCUSSIONS` は**渡していない**(「どこが処理済みか」を読むと
監査人がリードと同じ視点になるため)。設計は `PREFLIGHT.md`。

**判定: K1 は実行しない。** 層 3 の不合格条件は「重大 1 件」で、**重大 10 件**が出た。

## 0. 全体の総括

| | 件数 |
|---|---|
| **認める** | **25** |
| **一部認める**(重大度や射程を修正して受け入れ) | **4** |
| **否認する** | **0** |

**直接検証した 9 件はすべて再現した**(#1 #2 #3 #10 #11 #13 #20 #24 #29)。
残りは文書・コードの読解で確認できるもので、読んで認めた。

> **監査の途中で 3 件が既に直っていた**: 層 2(私の 3 つの読み)が同じセッションで
> #5-c(2.49 の出所)・#28(帰無 1,000 回の根拠)・#16-b の一部(足の欠測率)を先に拾い、
> 監査が読んだのはその**修正前**の版だった。**層 2 が無価値だったわけではない**が、
> **重大 10 件は 1 件も拾えていない**(→ §3)。

## 1. 重大(10 件)— **すべて認める**

### #1 §5.2 の 3 列中 2 列(`sd_all` / `sd_sig`)に再現手段が無い 【認・検証済】

**検証**: `dispersion.json` のキーは `sd_trade_bp` 系のみ。**`sd_all` / `sd_sig` は 1 つも無い。**
`measure_katsuo_dispersion.py` もこの 2 つを計算しない。

**何が悪いか**: 表の見出しに **【事実、…2026-09-09 に全面差し替え】**と書き、直下に
「再現: `scripts/measure_katsuo_dispersion.py`」と添えた。**しかし差し替えたのは 3 列中 1 列だけ。**
残る 2 列は I-005 で「壊れていた」と認定した時期の値がそのまま残っている。
**I-005 の訂正そのものが不完全で、しかも「全面」と称していた。** I-004 と同じ「出所の誤表示」。

`sd_sig(h)` は §A.1 で **§4.3 保有曲線の検出力の基礎**に指定されているので、
誤っていれば §4.3 の設計根拠が消える。

**対処**: `sd_all` / `sd_sig` を測定スクリプトに実装して測り直す。
測るまで表から**削除**する(残すと「測ったことになっている」状態が続く)。

### #2 「38,609 → 3,475 と 9 割減る」も壊れた測定の残骸 【認・検証済】

**検証**(2019Q1・15 分足): **`both` 44.6% 残 / `invalid` 9.4% 残**。
「9 割減る」は **`invalid` と一致**。

**何が悪いか**: この数字は「**建玉の重複を許す**」という設計判断の**唯一の根拠**で、
そこから「だから §6 の巡回シフト帰無という複雑な機構が要る」という連鎖が伸びている。
**連鎖の起点が壊れていた。** 正しくは約 56% 減(MDE ×1.5)であって ×3.2 ではない。

**§4.1-a で「旧値は `invalid` と一致した」と自分で書いた指紋が、同じ文書の別の場所にもう 1 つ残っていた。**
I-005 の訂正で、私は「数字が載っている表」だけを直し、**本文に埋まった数字を探さなかった**。

**対処**: 実測し直して §5.2 を書き換える。**重複を許す設計の是非を、正しい数字で再検討する**
(56% 減なら「検出力を捨てているだけ」とは言い切れない)。

### #3 絶対 USD の門は、探索区間では長さの門ではなく**価格水準フィルタ**【認・検証済】

**検証**(15 分足、シグナル残存率):

| 期間 | BTC 中央値 | $10 | $19 | $24 | $40 |
|---|---|---|---|---|---|
| 2017-01 | $906 | 5.7% | **1.5%** | 0.8% | 0.4% |
| 2018-01 | $12,791 | 99.9% | **99.0%** | 97.3% | 82.4% |
| 2019-06 | $8,867 | 87.9% | **62.0%** | 50.4% | 30.8% |

**何が悪いか**: 同じ「$19 の門」が、2018-01 では**「門なし」と 99% 同一のセル**、
2017-01 では**98.5% を落とす**。**同じ水準が年ごとに別物**である。

さらに: `KATSUO_PARAMETER_INVENTORY.md` は「**19 ドルは 2019 年の BTC 1 万ドルに較正された値で、
そのまま他年に持ち込めない**」と**自分で書いている**。事前登録はその文書を §3 で参照しながら、
この一文に触れずに絶対門を族に入れた。**自分の出典が禁じたことをしていた。**

封印区間 2020-2021 は BTC $5k→$69k なので、**探索で選んだ絶対門は判定区間でほぼ無効化され、
OOS が同じ設計の検定にならない。**

**対処**: **枠組みの変更なのでオーナーに諮る。** 私の案は
「絶対 USD をやめ、**価格に対する比率**(bp 建て)に置き換える」。19 ドル / BTC 1 万ドル ≈ 19 bp。
これなら年をまたいで同じ意味になり、原典の較正値の意図も保てる。

### #4 「主要な関心」の軸 × 3 年符号一致 = 機構と無関係に必ず陰性 【認】

$19 の門は 2017 年のシグナルの 98.5% を落とすので、2017 年単独の n は 10² のオーダー。
`sd_trade ≈ 92.8 bp` なら 2017 年の MDE は 20 bp 超で、**符号はコイン投げ**。

**§5.3 条件 3(3 年で符号が揃う)を全 162 セルに一律に課すと、絶対門のセル群は
「2017 年に n が無い」という理由だけで落ちる。** これは「エッジが無い」ではなく
「その年に測れていない」であり、**CLAUDE.md §5.2 が禁じた偽陰性そのもの**。

**対処**: #3 を直せば大半が解消する。加えて **条件 3 を「年ごとの n / MDE と併せて評価する」**
と明記する(年次 MDE が効果量より大きい年は、符号一致の要求から外し、**その事実を報告する**)。

### #5 §5.2 と §5.3 が MDE の役割について正面から矛盾 【認】

- §5.2:「**MDE は『合否の線』ではなく『陰性の射程』になった**」
- §5.3 条件 2:「`mean(r) > MDE`」= **合否の線として使っている**

**同じ文書内で、判定に使わないと宣言した量を判定条件に据えていた。**
L-048 でバーを削除したとき、**§5.2 の役割宣言だけ書き換えて §5.3 の条件 2 を残した。**

**さらに監査が新しく指摘した点**: 条件 1 は帰無の 95 点(≈ 1.645 sd 相当)、条件 2 は 2.49 sd。
**条件 2 が条件 1 を常に包含するので、多重性対策として掲げた条件 1 が死に条件になる。**
どちらが実効的なバーなのかを、私は計算していなかった。

**対処**: 条件 2 を削除する。MDE は**陰性の射程**としてのみ使う(§5.2 の宣言どおり)。
判定は条件 1(帰無の 95 点)と条件 3(符号一致)。**実効バーが 1 本になることを明記する。**

### #6 「MDE を書いていない事前登録は実行しない」と言いながら MDE の数値が無い 【認】

§5.2 は MDE を「帰無分布から出す」= **実行後にしか決まらない**と定めた。
その結果、事前登録の時点で書かれた MDE は §A.2 の例示 1 つだけで、
しかもその例示は **§5.2 が「使わない」と明記した t 公式**で計算されている(#27 と同根)。

規約 §4.1 の「**欲しい効果 < MDE なら実行しない**」という**事前の停止判断ができない**。

**対処**: **帰無を先に走らせて MDE を確定させ、それを事前登録に書いてから本測定に入る**
(帰無はシグナルとリターンの対応を壊した世界なので、**効果量を見ずに MDE だけ得られる**)。
これで「実行後にしか決まらない」と「事前に書く」が両立する。**手順として登録する。**

### #7 §6 の帰無が自分自身と矛盾。かつ改訂後の主指標に対して未定義 【認】

**矛盾**(すべて同じ文書):

| 箇所 | 何をずらすと書いてあるか |
|---|---|
| §6 第 1 項 | シグナルの**発生時刻**を巡回シフト |
| §6 第 4 項 | **リターン系列**ごと巡回シフト |
| §A.3 | リターンの時系列を丸ごとずらす。**シグナルの発生時刻は動かさない** |

**より重い問題**: L-046 で主指標を固定 `h` から**経路依存の決済**
(終値 vs `lcprice`、反対シグナルの時刻)に変えたのに、**§6 を追随させていない。**
リターン系列を巡回シフトすると価格経路が別物になるので、**シフト後の世界では
`lcprice` に対する無効化判定も、絶対門の通過判定も定義されない。**
「保有区間の重なりも自己相関も帰無側に保存される」は**固定 `h` の主張**であって、
決済が内生的な今の主指標には成立しない。

**帰無が定義できなければ、条件 1 も MDE も計算できない。** 本単位の統計的土台そのもの。

**対処**: **枠組みの変更なのでオーナーに諮る。** 私の案は
**「シグナル系列の方を巡回シフトする」**(価格経路は本物のまま動かさない)。
こうすれば決済ルールは実データの価格に対して定義されたまま働き、
壊れるのは「シグナルと価格の対応」だけになる。日内周期・自己相関も保存される。
**シグナル系列をずらす版で、保有の重なり構造が保存されるかは要検証**(検証項目として登録)。

### #8 検出力を測ったのは 162 セルのうち 6 セルだけ 【認】

§5.2 の表は**足 6 水準のみ**。**門 9 水準・強さ 3 水準は掛けられていない。**
門をかけると n は最大 98.5% 落ち(#3)、強さで分ければさらに約半分。
最悪セル(60 分 × $40 × 弱い)の n は 10³ を割り、MDE は §A.2 の例示より 1 桁以上悪い。

**「6 水準のうち 3 つが未測定のまま完成と称していた」(I-005)を訂正したのは足の軸だけで、
同じ穴が隣の 2 軸に残っていた。** オーナーの問い「30 分足はなぜ外れているのか」で
発覚した欠陥が、**隣の軸で再発している。**

**対処**: **162 セル全部の n と MDE を出す。** これが本来の「網羅性」。
層 1 の C6 も**族の全軸**を見るように直す(現状は足の軸しか見ていない)。

### #9 §7 のアブレーション 3〜7 が多重性に算入されていない 【認】

族 162 の外で、`int()` 2 通り × 決済 4 通り × 打ち切り 3 通り × 測り方 2 通り ×
無効化ライン 4 通り = **最大 192 通り**が同じ探索データ上を走る。合計の探索面は最大 162×192。

§4.3 の保有曲線には「**これは記述であって選択には使わない**」と封じを書いたのに、
**アブレーションには同じ封じが無い。** 「どれが効いているか」を見た後に主指標の定義を
動かせる余地が残り、**帰無(162 の最大統計量)がその探索を吸収しない。**

**対処**: アブレーションにも §4.3 と同じ封じを明記する —
**「アブレーションは記述であって選択には使わない。主指標の定義はアブレーションの結果で動かさない」**。
動かしたくなったら**それは次の周回**であり、周回数として多重性に算入する(規約 §0.3)。

### #10 意図マップ I-8 の ○ が原典の条件を取り落としている 【認・検証済】

**検証**(原典 `katsuo_v03.py` 490-535 行):

| 足 | 建玉 | 無効化を見るか |
|---|---|---|
| 陽線 | 売り(`mybtc < 0`) | **見る** |
| 陽線 | 買い(`mybtc > 0`) | **見ない**(「上がれ...!」) |
| 陰線 | 買い(`mybtc > 0`) | **見る** |
| 陰線 | 売り(`mybtc < 0`) | **見ない**(「下がれ...!」) |
| 同値 | 任意 | **見ない** |

**原典は、足の色が建玉と反対のときしか無効化を見ていない。**
意図マップ I-8 は「`signal == 0` の足で `Close` が `lcprice` を抜いたら `order_exit`」と書き、
**色の条件を落として ○ を付けている。**

**これは意図マップの存在意義そのものへの打撃である。** 意図マップは
「機構の構築が戦略意図を反映していないケースを先に潰す」ためにあり、
**○ の誤判定は、潰したつもりで潰していない状態**を作る。

さらに `measure_katsuo_dispersion.py` は色を見ずに判定しており、
**文書にも原典にも無い第 3 の挙動**になっている(15 分足で取引の 0.7% が変わる)。
§7 アブレーション 7 が「意図が不明な 2 点を**両方**測る」と宣言した列挙に、
**この 3 通り目が入っていない** — 存在を誰も知らないまま「両論併記した」と書かれていた。

**対処**: (1) 意図マップ I-8 を **○ → △** に格下げして色条件を書く。
(2) **他の ○ 判定を全部、原典と突き合わせ直す**(I-1 / I-3 / I-4 / I-5 / I-9)。
1 件誤判定があったなら、他も疑う。(3) 測定スクリプトを原典に合わせる。
(4) アブレーション 7 に 3 通り目を追加する。

## 2. 中(13 件)

### #11 `iqr` が効果の符号を漏らしている 【**一部認める** — 重大度を修正】

**認める部分**: `measure_katsuo_dispersion.py` の docstring は
「**`mean(r)` は計算しない。効果の符号も大きさも、ここでは一切見ない**」と書いているが、
`iqr_trade_bp` を出力しており、**符号は読めてしまう**
(検証: `15:invalid` = **[−86.7, −17.8]** — 四分位が両方とも負)。**docstring は端的に虚偽。**

**修正する部分**: 監査は「封印の建て付けとしては重大に近い」とするが、**封印破りではない。**
`iqr` は**探索区間 2017-2019** のもので、そこは**見てよい区間**である
(§2: 探索区間は「ここだけで全部決める」)。封印は 2020-2021 で、そちらには触れていない。

**したがってこれは「区間の侵害」ではなく「記述の虚偽」** — I-004 と同じ型の、
**出所・性質の誤表示**である。分類を変えて受け入れる。

**対処**: docstring を事実に合わせる(「探索区間の分布を見る。判定区間には触れない」)。
`iqr` は保持する(ばらつきの形を見るのに要る)。

### #12 「上限到達 0.0〜0.1%」が自分の表と食い違う 【認】

§4.1-a と §10 は「0.0〜0.1%」と書くが、§5.2 の表と `dispersion.json` は
**1 分足 0.7%、3 分足 0.2%**。7 倍の過小申告。

**原因は §4.1-a がまた 5/15/60 分の 3 点しか載せていないこと** —
「論証の都合で 3 点を選んだ」と**自ら反省した箇所が、その反省を書いた文書の中で直っていない。**

**対処**: 6 水準全部を §4.1-a に載せ、§10 を「0.0〜0.7%」に直す。

### #13 アブレーション 6 は代数的に恒等 【**一部認める** — 位置づけを変更】

**検証**: 2019Q1・15 分足 8,640 本で、原典の測り分けと `High − max(O,C)` の**差は 0 件**。
`KATSUO_PARAMETER_INVENTORY.md` が既に「どちらも一致する」と書いていた。

**認める**: 「原典の凝った測り方に意味があるか」を問うアブレーションとしては**空虚**。
必ず「差なし」を返す。**7 種と数えたのは実質 6 種**(数え間違いを訂正した節で、また数えが狂っていた)。

**変更**: **廃止ではなく §8 のサニティに移す。** 「私の実装が原典の測り分けを正しく写しているか」の
**実装検査としては有効**(差が 0 でなければ実装バグ)。アブレーションは **6 種**に、サニティは **6 種**になる。

### #14 前提表(バイアスの向き)が存在しない 【認】

CLAUDE.md §5.2 は「**事前登録の前提表にバイアスの向きを明記する**」と規定しているが、
K1 に前提表が無い。とくに **§4.2 の「建値 = シグナル足の終値」は、原典が最良気配に
post only で置いていた**(買いなら bid・売りなら ask = **終値より有利な側**)のと別物で、
この代理の向きが書かれていない。`fold()` の欠測落とし、`entry <= 0` の除外、末尾切り捨ても同様。

**対処**: 前提表を新設し、各前提に**採択寄り / 棄却寄り**の向きを書く。
**規約に明記があるのに従っていなかった**ので、層 1 に「前提表の存在」の検査を足す。

### #15 サニティ 1(先読み)が §4.1-a の実測と両立しない 【認】

「シグナルを 1 本後ろにずらすと効果が消えるか」だが、**保有中央値は 2 本**なので、
1 本ずらしたトレードは元と保有区間の大半が重なる。**真のエッジがあっても効果は消えない。**
「消えなかった = 先読み」と読むと**正常な実装を先読みと誤認する**。
しかも「消える」の閾値が事前登録されていない。

**対処**: 検査を**「1 本ずらす」から「保有の 95 パーセンタイル(10 本)ずらす」に変える**か、
**シグナルを未来方向に 1 本ずらした版(本来なら効果が出るはずがない)と比較する**形に変える。
**閾値を数値で事前登録する。** 設計し直しが要るので、対処案は再設計後に確定する。

### #16 サニティ 3 と 5 は落ちようがない 【認】

- **サニティ 3**: 「欠測を残した場合」の**「残す」手続きが定義されていない**。
  前足の終値で埋めるなら `o=h=l=c` → `candle=0` → `signal=0` でシグナル集合は不変。
- **サニティ 5**: バグ A(建玉上限)・B(ドテン枚数)は**建玉管理と発注枚数の話**だが、
  §4.2 の測定は**建玉も枚数も持たない**。**直すべき対象が測定に存在しない**ので永久に合格する。

**私は読み A でサニティ 5 を「非該当」と自分で書きながら、
「非該当な検査は検査ではない」に進まなかった。** 一歩手前で止まっていた。

**対処**: サニティ 3 は「残す」手続きを定義する。サニティ 5 は**削除**し、
代わりに **#13 から移してくる実装検査**を置く。

### #17 原典 v03 は 1 度も新規建玉を建てられない 【**一部認める** — 既知の部分と新しい部分を分ける】

**既知**: バグ A(`abs(mybtc) >= sizemax` の条件が逆で、建玉 0 では新規が入らない)は
意図マップ §5-A と洗い出し §5-A に記載済みで、「意図された `<` で検証する」と合意済み(L-042)。

**新しい**: その**帰結**を書いていなかった —
**「意図マップの ○/△/✕ は、実行されたことのないコードに対する判定である」**。
オーナーが記憶している挙動・成績は v03 のものではありえない(v01/v02 は所在不明)。
意図マップの目的が「実装が意図を反映しているかを先に潰す」ことなら、
**突き合わせ相手が実際に走っていた版でない**以上、その目的は達成されていない。

**対処**: この帰結を**射程(§10)と意図マップの冒頭に明記**する。
消せない制約なので、**制約として書く**。

### #18 `bigbeardline = 24` の 2 段構造が族に復元されていない 【認】

洗い出しは「`beardline`(そもそもシグナルか)と `bigbeardline`(大ヒゲ)の **2 段**」
「強さの基準がヒゲ絶対長 → ヒゲ+足の色に移った途中」と読解しているのに、
族は 19 と 24 を**単一の門の 2 水準として並列**に置いている。**復元されているのは 1 段だけ。**

結果、**「強さの基準がヒゲ長だったか色だったか」という洗い出しの中心的な推定が検定されない。**

**対処**: **枠組みなのでオーナーに諮る。** 案: 強さの軸に
「**色で切る(原典 v03)/ ヒゲ長 2 段で切る(19 で足切り・24 以上を強い)/ 両方**」を入れる。
族が大きくなるので、代わりに何を削るかも併せて出す。

### #19 長さの門の適用点が定義されていない 【認】

門をどこにかけるか未指定: (i) 勝った側のヒゲだけか両ヒゲか、(ii) `int(top) > int(under)` の
**前か後か**、(iii) `top > abs(candle)` との論理順。**(ii) だけで、門が
「シグナル判定の一部」なのか「シグナル後のフィルタ」なのかが変わり、結果が変わる。**

**A-1(無効化をシグナル足でも見るか)と同じ型** — 条項が定めていない選択を実装が黙って決める。
**A-1 を見つけた読み A が、隣にある同じ型を見落とした。**

**対処**: §3 に明記する。私の既定案は「**勝った側のヒゲに、シグナル判定の後にかける**」
(= フィルタ)。原典が未配線なので原典からは決まらず、**選択であることを明記**する。

### #20 ボラ比の門は実測ではほとんど門になっていない 【認・検証済】

**検証**(15 分足、シグナル残存率):

| 期間 | 0.5 | 1.0 | 1.5 | 2.0 |
|---|---|---|---|---|
| 2017-01 | 94.3% | 63.1% | 38.9% | 24.2% |
| 2018-01 | 96.0% | 61.0% | 27.6% | 13.3% |
| 2019-06 | 95.5% | 63.9% | 35.6% | 19.4% |

**0.5 はほぼ「門なし」**(94〜96% 通過)。9 水準は「門の強さ」の等間隔グリッドになっていない。

加えて: 「20 本」の出所が無い。**`|C−O|` の中央値は実体長であって実現ボラティリティではなく**、
名前が事実と違う。しかもシグナル条件が既に `beard > |C−O|` なので、
**比 1.0 の門はシグナル条件のほぼ再掲**になる。

**対処**: 水準を**残存率で等間隔になるように取り直す**(例: 残存 80/60/40/20% の点)。
名前を「実現ボラ」から「**直前 20 本の実体長の中央値**」に直す(#14 の記号・名称の一貫性)。

### #21 帰無の最大統計量が片側のみ 【認】

族には「弱い(上ヒゲ陽線・下ヒゲ陰線)」のセルがあり、原典の 4 分岐がこの向きで撃っていたことが
誤りだった場合、そのセルの `mean(r)` は**大きな負**になる。**それは機構についての情報**だが、
最大統計量だけを見る検定では拾われない。

**「なぜ」を出すのが本単位の成果物である以上、片側だけを見る設計は成果物を半分捨てる。**

**対処**: **両側にする**(`|mean(r)|` の最大の帰無分布)。バーが上がるので**事前に決める**。
#5 の 2.49(片側)も両側に合わせて直す。

### #22 原典が読んでいた足が確定足か形成中の足か未解決 【認】

原典は `fetch_current(1)`(足確定の 1 秒後)で `data[-1]` を取る。
洗い出しは「**1 秒後だと始まったばかりの足を掴む可能性がある**」と自分で書いている。

意図マップ §3-c は「終値で見る = 確定した足の形だけを信じる」を **○(取り下げ)** と判定し、
L-044 のオーナー回答を根拠にしている。**しかし `data[-1]` が形成中の足なら、
v03 は確定足を見ていなかったことになり、○ の根拠が崩れる。**

**#10 と同じ型**(○ の判定が原典の挙動と合っていない)。
§7 の未確認 2 点に数えられていない **3 点目**。

**対処**: 未確認事項として §7 に追加する。cryptowat.ch は終了しているので
**当時の API 挙動は再現できない**。したがって**確定足で測ることを選択として明記**し、
射程に「原典が確定足を見ていたかは確認不能」と書く。

### #23 §5.2「読み取れること」の因果主張が自分の表で支えられていない 【認】

「カツオが短期売買なのは**シグナルが頻繁に出るため**であって、損切りが速いからではない」と書いたが、
シグナル率は 1 分 10.9% → 60 分 47.5% と **4.4 倍**違うのに、`both` の保有中央値は
3 本 → 2 本と**1 本しか動かない**。頻度で説明するなら 1 分足の保有はもっと長くなるはず。

実際に効いているのは **`opposite` 分岐の存在**(1 分足で単独なら中央値 7 本、`both` で 3 本)で、
それは「頻度」ではなく「**反対シグナルという決済条件**」である。

**「なぜ」の材料として報告に載る予定の読みが、根拠の飛躍を含んでいた。**

**対処**: 記述を「決済条件のうち**反対シグナルが保有長を支配している**」に直す。
**因果の言い切りをやめ、観察の記述にする。**

## 3. 軽微(6 件)— **すべて認める**

| # | 内容 | 対処 |
|---|---|---|
| **#24** | 意図マップ I-1 の行番号 173 / 184 は条件式ではなく `signal = 1` / `signal = -1`(条件は 169/172/180/183)。参照先が `get_candle_first`(初回のみ)で、ループを回す `get_candle`(243/246/254/257)ではない【**検証済**】 | 行番号を `get_candle` 側に統一 |
| **#25** | `capped` が「上限 96 本到達」と「データが尽きた」を同じカウンタで数えている | 分離する。報告義務のある数字なので |
| **#26** | 保有本数の分単位換算(「2 本(10 分)」)は欠測があるので厳密でない | A-2 と同根。換算に「欠測を除く」と注記 |
| **#27** | §A.2 の MDE 例示が、§5.2 が「使わない」と定めた t 公式で計算されている | 帰無から出す手順(#6)に合わせて書き直す |
| **#28** | 帰無 1,000 回に出所が無い。1,000 反復では p < 0.001 を表現できない | **層 2 の B-1 で対処済み**(ブートストラップ標準誤差 → 必要なら 10,000 回)。p の下限を明記 |
| **#29** | バグ C の記述が不完全。`fetch_current` の `minute % 15 == 0` も 15 分固定【**検証済**】 | 意図マップ §5-C に追記 |

## 4. なぜ層 1 と層 2 は重大 10 件を 1 件も拾えなかったか

**これが今回の一番重い発見である。**

| 漏らした型 | 該当 | なぜ漏れたか |
|---|---|---|
| **宣言と運用の矛盾**(経費以外) | #5 #7 | 層 1 の C3 は「経費」の語だけを見ていた。**「§X で A と宣言 → §Y で not-A」という一般形になっていない** |
| **再現手段の実在** | #1 | 「再現コマンドを書いたか」しか見ておらず、**そのコマンドが実際にその数字を出すか**を見ていない(`sd_trade` だけ照合していた) |
| **網羅性の軸** | #8 #12 | C6 は**足の軸しか見ていない**。族は 3 軸ある |
| **○ の再検証** | #10 #22 | 読み A は「スクリプト ↔ 事前登録」を突き合わせたが、**「意図マップの ○ ↔ 原典」を突き合わせなかった** |
| **壊れた測定の残骸** | #2 #12 | 読み B は数値を表にしたが、**「その値は訂正前の時期のものか」を問わなかった**。I-005 の訂正で**表だけ直して本文を探さなかった** |
| **一歩手前で止まる** | #16 #19 | 読み A は「サニティ 5 は非該当」「A-1 は未定義」と**書きながら**、「非該当な検査は検査ではない」「隣の #19 も同じ型では」に進まなかった |

**共通の構造**: 層 1・層 2 は**私が既に気づいていた型**しか捕まえられない。
検査は過去の事故から導出したので、**過去の事故の形をしたものしか見えない。**

**層 3(独立監査)だけが、私の視点の外を見た。** 価値の大半はここから出た。

**したがって層 3 は「最後の念のため」ではなく、実質的な主検査である。**
層 1・層 2 は**層 3 に渡す前の粗取り**と位置づけ直す。

---

# K1 独立監査(層 3)2 回目の全件トリアージ — 37 件

**実施**: 2026-09-09。1 回目と同じく、事前登録・意図マップ・洗い出し・原典・準備測定**だけ**を
渡し、`OWNER_LOG` / `INCIDENTS` / `DISCUSSIONS` / 1 回目のトリアージ / `PREFLIGHT.md` は**渡していない**。

**判定: K1 は実行しない。** 不合格条件は「重大 1 件」で、**重大 18 件**が出た。

| | 1 回目 | **2 回目** |
|---|---|---|
| 指摘 | 29 | **37** |
| 重大 | 10 | **18** |
| 認める | 25 | **34** |
| 一部認める | 4 | **3** |
| 否認 | 0 | **0** |

**直したのに増えた。** その理由は L-054 に記録した(検証より速く断定を書く)。
本トリアージは、**その対策(数値の生成化・計算の評価・必須項目)を入れた後**に書いている。

---

## 0. 直接検証したもの(6 件。**すべて再現した**)

| # | 主張 | 検証 |
|---|---|---|
| **1** | 「条件 2 が条件 1 を包含する」は逆 | 198 セルの最大の 95 点 = **両側 3.65 sd**、旧条件 2 = 2.49 sd。**条件 1 の方が厳しい** |
| **8** | §7 の「無効化のみで中央値 7〜8 本」は旧値 | 測定は **18 本**(色条件あり)/ 15 本(なし) |
| **9** | §4.1-a の保有分布は門なしセルの数字 | `s-/b-` と**完全一致**(5 分 中央 2 / p95 14 / 到達 0.09%)。`s19/b24` は **中央 3 / p95 33 / 1.22%** |
| **10** | 上限到達率の射程が過小 | 族全体の最大は **13.9%**(`1分/s30/b-/weak`)、10% 超が **6 セル** |
| **15** | `s30/b24` は縮退ではない | `(w≥30 かつ w>実体) または (w≥24)` ⇔ `(w≥24)` 単独。**`s-/b24` =(w>実体)または(w≥24)とは別物** |
| **22** | 探索面「144 通り」 | 族外だけで【計算: 2*3*2*4 = 48】通り、全体で【計算: 4*3*3*2*3*2*4 = 1728】通り |

---

## 1. 重大(18 件)

### #1 判定条件を 1 本削った根拠が逆【認】

**私は「私はこれを計算していなかった」と書きながら、今回も計算していなかった。**

- 条件 1 = 族 198 セルの**最大** `|mean(r)|` が帰無の 95 点を超えること。
  独立なら 1 セルあたり `0.95^(1/198)` → **両側 3.65 sd**
- 旧条件 2 = `mean(r) > MDE = 2.49 sd`
- **2.49 < 3.65 なので、条件 1 の方が厳しい。** 「条件 2 が条件 1 を死なせていた」は成り立たない

**ただし条件 2 の削除自体は維持する。** 理由が変わる:
削除の正しい理由は「**§5.2 が MDE を『陰性の射程』と宣言しているのに、判定の線に使っていた**」
という**文書内の矛盾**であって、包含関係ではない。**行動は同じ、根拠は差し替える。**

→ **C10 の対象**: この種の主張は今後 `【計算: 式 = 値】` を義務づける。

### #2 `MDE = 2.80 × (帰無分布の sd)` が定義不能、かつ実効バーと桁が合わない【認】

**指摘のとおり、どちらの読み方でも壊れている**:

- 「帰無分布」= **族の最大の分布**なら、0 中心ではないので `2.80 × sd` は意味を持たない
- 「セルごとの帰無 `mean(r)` の sd」なら多重性を含まないので、
  **MDE(2.80 sd)< 陽性バー(3.65 sd)** になり、その帯の効果は
  「陽性でない」かつ「MDE 超だから陰性」と**両方に落ちる**

**原因**: MDE を**実際に使う判定規則ではなく、単一検定の一般式**から作っていた。

**対処**: **MDE は実際の判定規則から導く。**
判定は「族の最大が帰無の 95 点を超える」なので、そのセルが単独で判定を通す確率が 80% になる効果量が MDE:

```
MDE(セル) = (帰無の族最大 95 点)+ 0.84 × (そのセルの帰無 mean(r) の sd)
```

桁感で **≈ 3.65 + 0.84 = 4.49 sd 相当**【計算: 3.65+0.84 = 4.49】。
**バーより必ず大きくなる**ので、「陽性でないが陰性」の帯は消える。

### #3 帰無 N2 が実データのセルと交換可能でない【認・最も重い】

監査が **N2 を実装して実データと比較した**(2019・15 分・`s19/b24`):

| | strong の n | weak の n | `sd_trade` | 保有中央値 |
|---|---|---|---|---|
| 実データ | 3,137 | 4,931 | 104.2 bp | 3 |
| N2 世界 | 4,044〜4,069 | 3,999〜4,024 | 92.7〜95.8 bp | 2 |

**原因は私の設計そのもの**: 強弱は「シグナルの向きと足の色が一致するか」で決まるので、
**向きをコイン投げにすると強弱が必ず 50/50 になる**。実データは 37〜43%。
さらに `lcprice` を反対側の先端に付け替えると、**負けた側の(短い)ヒゲ先端**になる場合が生じ、
無効化が早く効いて保有が縮み `sd_trade` が 9〜11% 小さくなる。

**帰無分布が実データより狭い → 95 点のバーが低い → 偽陽性方向。**
§6.2 の「壊さないもの: 保有区間の重なり」も §A.3 の「壊れるのは向きの対応だけ」も、
**実測で成立していない。**

**私は帰無を作り直したのに、作り直した帰無を一度も走らせなかった。**
規約 §1.1「準備測定も測定である」に自分で違反している。

**対処**: 帰無を**もう一度**設計し直す。**枠組みなのでオーナーに諮る**(→ §4)。

### #4 「同じ置換世界」の共有単位が未定義【認】

「族 198 セル全部を同じ置換世界で計算」と書いたが、コイン投げを
**足ごとに引くのか / セルごとに引くのか**を書いていない。
門が違えばシグナル集合が違い、足が違えばバーそのものが別物なので、**自明に定義できない**。

- セルごとに独立に引く → セル間の相関が消えて**バーが上がる**(偽陰性方向)
- 足ごとに 1 回引いて全門で共有 → **バーが下がる**(偽陽性方向)

**手続きの未記載部分だけでバーが動く。** #3 と併せて設計し直す。

### #5 サニティ 2(符号反転)は無意味か必ず落ちるかのどちらか【認】

`signal` を反転すると決済も変わる(反対シグナル判定・色条件・`lcprice` の側が
すべて `s` に依存)。**正しく反転すれば絶対値は一致しない。**
一致するのは最後の掛け算だけ裏返した場合で、それは恒等式の確認にすぎない。

**§8 の前口上「落ちない検査は検査ではない」を、差し替えた新しい検査自身が破っていた。**

**対処**: 統計的なサニティから外し、**単体テストにする**
(`s = +1 / −1` で `r` の符号計算が対称であることを、合成データで直接確かめる)。

### #6 サニティ 1 の「10 本ずらす」が族の 7 割で足りない【認】

`hold_p95 > 10` のセルが **140 / 198**、`≥ 20` が **86**、`1分/s19/b-` 系は **96**(上限に張り付き)。
10 本は §4.1-a の 14/10/8 から取ったが、それは **#9 のとおり `s-/b-` セルだけ**の値だった。
**旧版を作り直した検査が、族の大半で同じ欠陥を持ったまま。**

### #7 サニティ 1 の推論が §4.3 の仮説と両立しない【認】

サニティ 1 は「10 本ずらしても効果が残れば先読み」と断定するが、
§4.3 は「**数十本かけて伸びるならトレンド転換の予測として筋が通る**」を生きた仮説にしている。
**数十本持続するエッジが実在すれば、ずらしても効果は残る。**
同じ観測が §4.3 では「機構の証拠」、§8-1 では「致命的バグ」と読まれる。

**#5 / #6 / #7 に共通する対処 — 統計で先読みを見るのをやめる**:

**先読みは「使ってはいけない情報を読んでいるか」であって、本来コードの性質**である。
統計的なずらし検査は、**先読みと持続性を原理的に区別できない**。

→ **コード側の検査に変える**: シグナル関数が**その足の `o,h,l,c` 以外を読まない**ことを、
合成データで直接確かめる(未来の足を書き換えても出力が変わらないこと)。
**これは必ず落ちうるし、先読み以外の理由では落ちない。**
ずらしたときの効果の減り方は、**先読みの検査ではなく §4.3 の記述**として報告する。

### #8 §7 アブレーション 5 の数値が §5.2 と矛盾【認・検証済】

「無効化のみで中央値 **7〜8 本**」→ 実測 **18 本**。I-005 の訂正が §5.2 の表だけに入り、
**§7 の本文に入っていなかった**。「表ではなく本文に旧値が残る」型の**3 回目**。

→ **C8 の対象**: この数値をテンプレートの参照にする。**手で書ける場所を無くす。**

### #9 §4.1-a の保有分布表が門なしセルの数字【認・検証済】

表には門の指定が無く、「正しい主指標での保有分布」とだけ書いてあった。
そこから「**上限 96 本はほぼ効いていない**」と結論し、**§4.3 の読み方と §8 サニティ 1 の
「10 本」がこの表を前提にしていた**。1 セルの数字が 3 箇所に波及していた。

→ **C8 の対象**(参照がセル名を含むので、指定漏れが起こせなくなる)。

### #10 上限到達率の射程が族の実測と違う【認・検証済】

「60 分足 0.0% 〜 1 分足 6.2%」は `s19/b24` 系列のみ。族全体では**最大 13.9%**。
**旧版が「5/15/60 の 3 点しか見ていない」ことを反省した箇所で、今度は門 11 通りのうち 1 通りしか見ていない。**

→ **C8 の対象**(`{{stat.max_capped_share}}` / `{{stat.argmax_capped_share}}` で生成する)。

### #11 §4.2 に色条件が書かれていないが、実装は既定で色条件つき【認】

§4.2 は「判定するのは `signal == 0` の足だけ」としか書いていない。
「**かつ足の色が建玉と反対**」が抜けている。**A-1(条項に無い選択を実装が決めていた)を
修正した、まさにその節で、別の未記載条件が残っていた。**

### #12 測定に建玉状態が無いため、○ の I-4 / I-5 が黙って外れている【認】

**§8 で「§4.2 の測定は建玉も枚数も持たない」と自分で書いておきながら**、
意図マップの I-4(強い=ドテン / 弱い=決済で止める)と I-5(建玉が無ければ弱いシグナルでも建てる)が
**両方とも ○ で、どちらも「反対建玉があるか」に依存する**ことに気づいていなかった。

**「外してよいのは ＋ だけ。○ と △ は外さない」に、また違反している**(L-046 と同型、別項目)。
§8.5 の前提表にも無い。

**対処**: 建玉状態を持つ測定に変えるか、**○ を外していることを射程と前提表に明記**するか。
**枠組みなのでオーナーに諮る**(→ §4)。

### #13 判定区間の判定規則が事前登録されていない【認】

段 2 は「一度だけ実行し、そのまま報告」としか書いていない。
**閾値・帰無の再実行の有無・見るセル・多重性・判定区間の MDE — どれも無い。**
**封印の目的は「事前に決めた線で一度だけ測る」ことなのに、線が無い。**

→ **C9 が現在この不足を検出している**(層 1 が返している 3 件のうちの 1 つ)。

### #14 §3.0 / §3.2 / §7 の【実測】に再現手段が無い【認】

年別内訳・残存率・適用点の等価性・`int()` の 0.9% — **どれも
`measure_katsuo_dispersion.py` は計算していない**。私が対話中に使い捨ての
スクリプトで測り、**コミットしなかった**。

**§5.2 の同じ欠陥(1 回目 #1)を「最大の欠陥」として直したと書きながら、
自分が新しく書いた 3 つの節に同じものを作っていた。**

**対処**: これらを `scripts/measure_katsuo_gates.py`(新規)に実装し、
出力を `gates.json` に落として**テンプレートの参照にする**。

### #15 `s30/b24` の除外理由が誤り【認・検証済】

包含は正しいが、残る `(w ≥ 24)` 単独は**他のどのセルとも違う固有の設定**で、
しかも**オーナーが確定させた「実足の長さに関わらず単体で長いヒゲ」を単独で測れる唯一のセル**。
**§3.0 が最も強調している仮説を直接検定できる設定を落としていた。**

**対処**: `b` 単独のセルを族に**戻す**(名前は `s-none/b24` ではなく **`bonly/b24`** のように
「小門の枝を使わない」ことが読める形にする)。族の大きさが変わる。

### #16 `int()` の丸めがドル建てのまま残っている【認】

**§3.2 は「同じドルの門が年によって別物になる」を理由に門を bp にしたのに、
向きを決める `int(top) > int(under)` はドル建てのまま**残した。
BTC $1,000 の $1 は 100 bp、$10,000 なら 10 bp。

監査の実測(意図どおりの規則、15 分足):

| 年 | シグナル | `int()` の有無で判定が変わる | 割合 |
|---|---|---|---|
| 2017 | 13,946 | 200 | **1.43%** |
| 2018 | 11,901 | 32 | 0.27% |
| 2019 | 8,068 | 14 | **0.17%** |

**年較差 8 倍。** §7 の「0.9% の差で結論が動くなら脆い」という論法は、
**値が固定である前提**に立っていたが、固定ではない。しかも 0.9% 自体が
**v03 規則・2019 年のみ**の測定で、事前登録する規則でも期間でもない。

### #17 信頼区間の算出方法がどこにも無い【認】

「効果量を bp と**信頼区間**で書く」を報告義務・④ への引き渡し本体としながら、
**t 公式は自ら否認し、帰無は族の最大統計量しか作らない。**
建玉重複を許す設計なので素朴な i.i.d. の区間は幅を過小評価する。

→ **C9 が現在この不足を検出している。**

**対処案**: **ブロックブートストラップ**(保有の重なりより長いブロックで時系列を再標本化)。
ブロック長は保有の 95 パーセンタイルから決め、**セルごとに**出す。

### #18 「1,000 → 10,000 に増やす」がデータ依存の停止規則で、段順と矛盾【認】

「**実測の最大が**誤差の範囲に入るなら 10,000 回に増やす」— 判定の要否が実測を見てから決まる。
ところが §8.6 は**段 0(帰無のみ、本物を見ない)→ 承認 → 段 1(本物)**と定めている。
**段 0 では実測の最大が無いので、この規則は実行できない。**
順序を守れば規則が実行できず、規則を実行すれば段 0 の目的が崩れる。
加えて閾値付近だけ試行を増やすのは**境界例で有意水準を歪める**。

**対処**: **段 0 で無条件に 10,000 回走らせる**(実測を見ずに決まる)。
1,000 回で足りるかを実測依存で判断する規則を**削除**する。

---

## 2. 中(12 件)

| # | 内容 | 認否 | 対処 |
|---|---|---|---|
| **19** | 門が入口だけでなく**決済規則も変えている**(無効化は `signal == 0` の足で走るので、門を厳しくすると発火機会が増える)。族の軸は「どのシグナルを撃つか」と宣言しているが、出口も動かしている | **認** | §3.1 と §8.5 前提表に明記。アブレーション 1・2 の解釈に「入口の選別と出口の変化が交絡する」と併記 |
| **20** | `mean(r)` を §4.3 が**別の量**に流用(§A.1 は「§4.2 の決済で切った 1 取引」と唯一定義しているが、§4.3 は `h` の関数として使う) | **認** | §4.3 の量に別の記号を与える(`mean_h(r)`)。**I-004 の再発**。用語節を作る改訂の中で起きた |
| **21** | §A.2 の MDE の例示がセル指定なし。しかも `38,609` は §5.2 が「壊れた測定の残骸」の文脈で使う数と同じ | **認** | 例示を `{{cell:...}}` 参照にする(C8) |
| **22** | §7 の探索面「144 通り」が誤り | **認・検証済** | 族外 48 / 全体 1,728 に訂正し `【計算】` を付ける(C10) |
| **23** | サニティ 3 は `n` しか見ないので目的の異常を検出できない(埋めた足は `signal=0` になるので `n` は定義上ほぼ変わらない) | **認** | 見る量を `mean(r)`・保有本数・無効化の発火位置に変える |
| **24** | サニティ 4 はサニティではなく**判定条件の重複** | **認** | §8 から外す(§5.3 条件 2 に一本化)。**サニティは「計算が壊れていないか」だけ** |
| **25** | サニティ 6 に数値の合格条件が無い(「大きく動けば」) | **認** | 「48 本と 96 本で `mean(r)` の差が MDE 未満」を条件にする |
| **26** | §6.4 の N1 プラセボが**書かれたとおりには実行できない**(向きが指定されていない) | **認** | #3 の帰無の再設計に含める。→ **C9 が現在検出中** |
| **27** | §5.3 条件 2 の**年ごとの MDE を作る手順が無い** | **認** | 段 0 の帰無を**年別にも**回す(計算量 3 倍)。事前に登録する |
| **28** | §8.5 前提表に、結果を左右する選択が 6 つ入っていない(建玉状態を持たない / 強弱が入口だけをフィルタする / 門が決済も変える / 無効化の色条件 / bp の分母が終値 / `int()` がドル建て) | **認** | 全部追加。**前提表は「載っていない前提は向きも書けない」ので、抜けが致命的** |
| **29** | 「44.6% 残 / MDE ×1.5」は保有中央値 2〜3 本のセルの数字で、`1分/s30/b-`(中央値 7 本)には効かない | **認** | 族全体で測り直して参照にする(C8) |
| **30** | §4.1-a の 254.9 と §5.2 の 363.0 が**同じ量で食い違う**(門の建て方が違うが、どちらの表にも門が書いていない)。§10 にはさらに第 3 の値がある | **認** | I-005 の診断そのものが再現できない。**診断を bp 建てで測り直し**、3 つの値の由来を 1 つに統一 |

---

## 3. 軽微(7 件)— **すべて認める**

| # | 内容 | 対処 |
|---|---|---|
| **31** | §3.1 に「実現ボラ」の名前が残っている(§7 で「実体長の中央値」に直すと宣言した) | 直す。**「2 か所に書けば片方だけ更新される」の実例**(§A.1 が自ら警告している型) |
| **32** | 打ち切り上限の感度が §4.2 / §7 / §8 の**3 箇所で別の場所に置かれている** | §8 サニティ 6 に一本化 |
| **33** | bp 換算の分母が**終値**であることがどこにも書いていない | §3.2 と前提表に明記(60 分足では向きによって門が非対称になる) |
| **34** | 「族 198」を独立試行のように書いている(`both` = strong ∪ weak なので **66 セルは他の 2 セルから決まる**) | §A.3 の「198 回サイコロを振る」を書き換える |
| **35** | `sd_sig(2)` の対象集合が `sd_trade` の対象集合とわずかにずれる | 揃える |
| **36** | §4.3 は h = 1…96 の曲線を報告すると宣言しているが、測ってあるのは `sd_sig(2)` の 1 点だけ | 曲線に必要な検出力の材料を段 0 で測る |
| **37** | 日次ファイルが無ければ黙って飛ばす(§1 は「欠けなし」と主張) | 検査を実装(現時点で 1,826 日すべて存在することは確認済み) |

---

## 4. オーナーに諮る枠組み — **2 件**

残りは私の側で直せるが、次の 2 つは**枠組み**なので判断を仰ぐ。

### 枠組み A: 帰無をどう作り直すか(#3 / #4 / #26)

**N2(向きのランダム化)は実測で失格した。** 強弱の比率が 37〜43% → 50% に動き、
`lcprice` の付け替えで保有が縮む。**帰無の方が実データより狭くなり、バーが下がる = 偽陽性方向。**

候補:

| 案 | 中身 | 難点 |
|---|---|---|
| **N4 ブロック順序入替**(推奨) | 価格系列を**日単位のブロック**に切り、日の順序をランダムに入れ替える。シグナルは入れ替え後の系列から**計算し直す** | 日をまたぐ保有が壊れる(保有 95% が 1 日以内のセルなら影響小)。日内周期は保存、日をまたぐ自己相関は壊れる |
| **N5 別銘柄・別期間** | 同じ規則を**関係のない期間**に当てる | 「エッジが無い世界」ではなく「別の世界」なので、帰無として厳密でない |
| N2 の修正 | 強弱の比率と `lcprice` を実データに合わせて補正する | **補正の妥当性を検証する手段が無い**。私の設計ミスを設計で覆うことになる |

**私の推奨は N4。** 「シグナルを入れ替え後の系列から計算し直す」ので、
**強弱の比率も保有構造も自然に実データと同じ分布になる**(#3 の失敗が構造的に起きない)。

### 枠組み B: 建玉状態を持つか(#12)

意図マップで **○** と判定した I-4(ドテン)/ I-5(建玉が無ければ弱いシグナルでも建てる)は、
**建玉状態が無いと表現できない**。いまの測定は持っていない。

| 案 | 中身 | 代償 |
|---|---|---|
| **B1** | **建玉状態を持たせる**(建玉 1 単位、ドテンあり) | n が約 45% に減る(MDE ×1.5)。ただし**○ を外さずに済む** |
| **B2(推奨)** | 持たせない。**○ を 2 つ外していることを射程と前提表に明記** | 「意図どおりの機構を測った」とは言えなくなる。**射程が狭まる** |

**私が B2 を推す理由**: 本単位の問い(§0)は「**足の形から後続リターンが予測できるか**」で、
建玉管理は**執行の話(層 ②)**である。ただし **L-046 で「○ を外すな」と自分で書いた**ので、
**外すなら明示的にオーナーの承認が要る**。黙って外すのが最悪である。

---

## 5. 直す順序

1. **枠組み A・B のオーナー判断**(帰無の作り直し / 建玉状態)
2. **#14 の測定を実装**(`measure_katsuo_gates.py`)— 参照にできる形にしないと C8 が通らない
3. **#13 / #17 / #26 の規則を書く**(現在 C9 が検出中の 3 件)
4. 残りの重大・中・軽微
5. **層 1 → 層 2 → 層 3(3 回目)**

**層 3 は 3 回目も新しいものを出す前提でいる。** 前回「出なくなるまで」という基準を
置いていなかったので、ここで置く: **重大 0 件が 2 回続くまで実行しない。**
