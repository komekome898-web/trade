# 第 5 周 — (a') 出口の遅れを決済理由別に分ける / (c)-iii 「強い」の残りの負の理由を層別で探す(事前登録)

**オーナー決定 2026-09-10(L-080)**: 「(a’) と(c) のⅲをお願いします。」 **(b) H2b は見送り**(オーナーとリードが一致: 保有上限は「なぜ」から導かれた機構仮説ではなく、裾の形を変えるパラメータ。資本・リスクの目的は流れの ③〜④ で決まる)。
**リードは設計と結果の読みだけ。実装・実行・表・数値抽出は下位モデルに委任する**(L-080、トークン節約)。
**この文書を書き終えてから実装を走らせる。** 書いていないことは決まっていない。

**守るもの**: 帰無・MDE・判定バーは作らない(L-056)/ 経費は引かない(L-048)/ 建玉状態は外さない / 引き算をしない(層を並べる)/ **封印は開けない**(`end <= 2019-12-31` の assert)/ 数字は生成 / 区間はセルごとに種を固定 / 平均と並べて**取引数と年ごとの総損益**を出す(L-075)。

---

## 0. ループ台帳(第 1〜4 周は `H1_PREREG.md` / `H2_PREREG.md` / `H3_PREREG.md` / `H3_DECOMP_PREREG.md`)

| 周 | 機構仮説 | 何をするか | 出所 | データ | 状態 |
|---|---|---|---|---|---|
| 1〜4 | H1 / H2a / H3 / H3 の分解 | 第 10〜13 部 | L-070〜L-079 | 探索区間 + Binance | 完了 |
| **5** | **(新しい機構仮説ではない。理解の続き)** (a') 出口の遅れの利得は損切りの遅れから来るか(第 13 部 §13.5-2 (ii))/ (c)-iii H1 の後に「強い」に残る足(ヒゲ > 実体)の負は**どこで**生じているか(第 13 部 §13.5-5、L-078) | (a') 第 13 部の同じ取引を決済理由で分ける / (c)-iii 残りの強いの取引を層別し、入口からの値動きの経路を見る | L-080 | **同じデータ**(5 周目) | **実施済み → `RESULT.md` 第 14 部**(再現ゲート 234/234 × 4。(a') 出口の遅れの主成分は損切りの遅れ(BitMEX 5 分・弱い +3.42、反対弱い +0.83)。(c)-iii 持ち続けた経路は BitMEX ≈ 0 / Binance −1〜−2、機構の負は決済の非対称(損切り 6 割 × −50、ドテン 4 割 × +50〜+75)。見込み 9 件中 合う 1・半分 3・外れ 5) |

多重性は 5 周分に算入する。判定は設計ごとに一度、封印かフレッシュデータで。

## 1. (a') 規則

第 13 部と**同じ取引・同じ 4 通りの値付け**(EE / DE / ED / DD、`measure_katsuo_delay_decomp.reprice`)を、決済理由 {`invalidated`, `opposite_weak`, `reversed`} で分けて集計する。
土台 2(原典 / H1+H2a。後者は `invalidated` が無い)× 234 セル × 2 取引所。セル × 理由ごとに: n、EE の平均、入口の遅れ = mean(DE − EE)、出口の遅れ = mean(ED − EE)、年別 (n, EE, DE, ED の平均)。
**検査**: 理由別の (n × 分解量) の和 / n 全体 = 第 13 部の分解量(`delay_decomp*.json` の `decomp_bp`)と 1e-3 で一致すること(全セル)。

## 2. (c)-iii 規則

対象 = **H1 の土台の「強い」の腕**(`signals(flip_body=True)` で `strength == "strong"` のまま残る足 = ヒゲ > 実体、`keep="strong"`)。無効化あり(H1)と無し(H1+H2a)の 2 土台。**門は `s19/b24` のみ**(本文で一貫して参照している門。族全体は第 10〜13 部で測定済み)、足 6、両取引所。
各取引に付ける層:

| 層 | 定義 | 出所 |
|---|---|---|
| 向き | 買い / 売り | 建玉の符号 |
| ヒゲ/実体比 | r = w / \|実体\|。ビン (1, 1.5], (1.5, 2], (2, 3], (3, ∞)、実体 0 は ∞ のビン | 第 5 部の層別と同じ量(w = 勝った側のヒゲ、`int()` 切り捨て後) |
| ヒゲ長 | w の bp、ビン [24, 40), [40, 80), [80, ∞) | 大門 b24 を通る足なので 24 以上 |
| 局所ボラ | 入口足の直前 100 本の \|close/close−1\| 平均の三分位(年ごとではなく全期間で三分位) | 第 9 部 D6 と同じ定義(`measure_katsuo_robustness.py` を再利用) |
| 年 | 入口の年 | — |
| 保有 | 保有本数のビン [1, 3), [3, 10), [10, 30), [30, ∞) | — |
| 決済理由 | invalidated / opposite_weak / reversed | — |

各層で: n、平均 bp、区間(日ブロック 200 回、種 = セル名 × 層)、総損益(n × 平均)。
**経路**: 入口足の終値から h ∈ {1, 2, 3, 5, 10, 20, 50} 本後の終値までの符号付きリターン(建玉の向き × 値動き、**決済を無視して持ち続けた場合**)の平均と n。全体と、決済理由別・局所ボラ別に出す。取引の実際のリターン(機構)と並べる。

## 3. 出力(生成物)

- `measure_katsuo_round5.py`(1 回の読み込みで (a') と (c)-iii を両方出す。`--flip-body` / `--no-invalidation` / `--source`)→ `round5_exit_reason{tag}.json` と `round5_residual_strong{tag}.json`(`tag` は第 13 部と同じ: `""` / `_flip_noinval`、(c)-iii は `_flipbody` / `_flip_noinval`)。BitMEX の実行は単独で(メモリ)
- `render_k1_round5.py` → `ROUND5_TABLES.md`: (a') 表 M-a 門 `s19/b24` 理由別の分解(足 × 強さ)、表 M-b 全 13 門の理由別 出口の遅れ(強さ「両方」と「弱い」)/ (c)-iii 表 M-c 層別(向き・比・ヒゲ長・ボラ・年・保有・理由)、表 M-d 経路
- テスト: (a') 理由別の和 = 第 13 部の分解量 / (c)-iii 経路の h = 保有 での値が、決済が終値で行われた取引のリターンと一致(反対シグナル決済は終値なので一致するはず。無効化も終値)/ 層の割り当てが定義どおり(合成足)

## 4. 見込み(事前に書く。経費前、in-sample)

| 量 | 見込み | 根拠 |
|---|---|---|
| (a') 出口の遅れ、`invalidated` | **正で最大**(BitMEX 5 分・弱い +3 以上。全体 +2.04 のうち無効化 22〜35% の取引が大半を担う) | 第 13 部 §13.5-2 (ii): 損切りは局所の極値に近い終値で閉じる |
| (a') 出口の遅れ、`opposite_weak` | 正、小さい(+0.5〜+1.5) | §13.5-2 (i): 次の 1 本はヒゲの側 |
| (a') 出口の遅れ、`reversed` | ≈ 0 か負 | ドテンの足は強い足で、次の 1 本の向きは (i) と第 12 部の観測が食い違う |
| (a') 入口の遅れ | 理由によらず同じ符号(入口は決済理由を知らない)。差があれば**保有の長さ**を通じた選択 | 設計 |
| (c)-iii 経路 | Binance の残りの強いは h=1〜3 で小さく負(第 10 部 −1〜−2)、**h=10〜20 で負が広がる**(機構の −5.29、保有中央値 14)。BitMEX は ≈ 0 のまま | 第 10 部 §10.2、第 12 部 |
| (c)-iii 比 | r が 1 に近いビン (1, 1.5] が最も負(実体の戻りの続き)、r > 3 は ≈ 0 か正 | 第 5・9 部 |
| (c)-iii 局所ボラ | 高ボラ三分位に負が集中 | 第 9 部 D6 |
| (c)-iii 年 | Binance 2021 に集中 | 第 12 部 |
| (c)-iii 決済理由 | `reversed`(ドテン)で閉じた取引が最も負 | ドテンは反対の強いシグナル = 実体の戻りが完了した後 |

## 5. 前提(バイアスの向きつき)

| 前提 | 向き | 出所 |
|---|---|---|
| 門 `s19/b24` だけ | 他の門で違う形なら見えない。族全体は第 10〜13 部で見ているので、本周は理由の所在を探す診断 | 設計 |
| 三分位は全期間 | 年でボラ水準が違うので、高ボラ三分位は 2017〜2018・2021 に偏る(年の層と混ざる) | 第 9 部 D6 と同じ |
| 経費なし | 層 ④ | L-048 |

## 6. 出さないもの

判定・帰無・MDE・バー・経費・封印・新しい機構仮説・H2b・(c)-i / (c)-ii(残りを使わない / 逆にする)の実行。**機構は変えない**。
