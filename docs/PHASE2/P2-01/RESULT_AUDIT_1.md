# P2-01 反復1(四半期ロール規則)監査 — 下位ティア・盲検

読了順序: 1) PROTOCOL.md 2) PREREG.md(凍結版) 3) ITER.md(反復1定義) 4) RESULT_AUDIT_0.md
5) src/bot/research/overnight.py 6) sealed.py 7) schema/n225f_225labo.json
8) backtest_data/phase2_sealed/P2-01/SEALED.json 9) config/constants.yaml(grep)
10) backtest_data/audit_fetch_JPX_n225f_months_20260906/README.md と page.html(grep照合)
— ここまでで自分の数値を RESULT_AUDIT_1_own_numbers.json に確定。その後のみ
11) iter1_20260906/RESULTS.md 12) 同RUN.json 13) p2_01_run.py(grepのみ)。
KNOWLEDGE*・他PREREG/RESEARCH_REPORT・他P2単位・DESIGN_AUDIT・SURVEY・judge_*は未読。

再利用関数: `overnight_returns(kind="simple")`、`drop_glitches`(閾値0.10既定)、`mean_t`、
`block_bootstrap_ci`(block=20, n_boot=2000, seed=20260906)、`sign_shuffle_null`。
データは `load_unsealed(path, "P2-01")` のみ(day/night 2ファイル)。

## JPX限月引用の照合
README.mdが引用する「３月、６月、９月、12月のうち19の限月」「第2金曜日の前営業日を取引最終日」
の2文をpage.htmlにgrep照合 → **完全一致**(バイト数38,397も一致)。四半期限月のみという
反復1の前提は一次資料で裏付けられる。

## ロール隣接規則の再現(重要な発見)
PREREG文面「{SQ日、その前営業日}を含むペアとし、±2営業日には広げない」を字義通り(両端が
SQ日と前営業日の1組のみ)で実装すると53件(四半期)/157件(月次)しか出ず、台帳の159/471と
不一致。**「SQ日または前営業日を端点に含む全ペア」(前後1ペアずつ計3ペア/回)**と読み替えて
初めて159/471を再現(53×3=159、157×3=471)。実装コードは未読のままこの読み替えに到達。

## 数値対照表(bps/ペア、n はペア数)
| 項目 | 自分 | 実装者 | 判定 |
|---|---|---|---|
| 端点規則後 n | 3,240 | 3,240 | 再現 |
| 誤プリント件数 | 3 | 3 | 再現 |
| ロール隣接(四半期) | 159 | 159 | 再現 |
| 主集合 n | 3,078 | 3,078 | 再現 |
| cost_bps 中央値/平均 | 7.9118/8.8829 | 7.91/8.88 | 再現 |
| 主指標1 平均(保守後) | -4.587011884903 | -4.587011884903 | 再現(ビット一致) |
| 主指標1 CI95 | [-8.5487,-0.9241] | [-8.5487,-0.9241] | 再現(ビット一致) |
| グロス平均 | 4.295851803265 | 4.295851803265 | 再現(ビット一致) |
| 主指標2 平均(夜間−日中) | 4.871841842386 | 4.871841842386 | 再現(ビット一致) |
| 主指標2 CI95 | [0.3124,9.2192] | [0.3124,9.2192] | 再現(ビット一致) |
| シャッフル帰無95点(グロス平均) | 3.140129997354 | 3.140129997354 | 再現(ビット一致) |
| train/val 平均(保守後) | -5.5696/-2.2787 | -5.570/-2.279 | 再現 |

## 個別判定・総合
全項目「再現」。着手前関門(グロス14.9bps)はグロス平均4.30bpsで大幅未達。主指標1のCIは0を
含まない負(反証条件の一つに合致=夜間プレミアム消失)、主指標2のCIは[0.31,9.22]bpsで0を跨がず
正だが着手前関門8.07bpsには届かない。反復0(月次、n=2,768, r_net -3.96bps)からの差分もITER.md
記載と整合(n+310、r_net -0.63bps)。**総合判定: 再現**。

## 封印データ漏洩チェック
RUN.jsonのinputs md5(day/night)はSEALED.json記載値と一致。`seal_record_md5`もSEALED.json実
ファイルmd5と一致。`UNSEAL_LOG.jsonl`・`UNSEAL_APPROVED`は存在せず、スクリプトgrepで
`load_sealed`・`bars_1min`・`paper_logs`の実使用なし(`load_unsealed`2箇所のみ)。**漏洩なし**。

## 発見された欠陥
- 中: PREREG本文の印付け規則の文面は1ペアのみを示唆するように読めるが、実際の集計は「SQ日/
  前営業日を端点に含む前後3ペア」でなければ台帳の159/471件と一致しない。文面だけでは第三者が
  独立再現できず(台帳の期待値159を手がかりに読み替えて到達。手がかりが無ければ再計算不能に
  なっていた可能性が高い)。PREREG本文の明確化を推奨。
- 軽微: 上記ギャップは文書表現の問題のみで、実装(反復0・反復1とも)自体の一貫性に問題はない。
- 重大な欠陥・恣意的逸脱・不開示の逸脱は見つからず。
