# P2-01 設計監査 4(盲検・第 4 稿の確認パス)— 2026-09-06

対象: `docs/PHASE2/P2-01/PREREG.md`(第 4 稿)。基準 = `PHASE2_TEMPLATES.md` §4。
読んだのは PREREG・DESIGN_AUDIT_3・TEMPLATES §4・`src/bot/research/overnight.py`・封印外データ(`load_unsealed` 経由)のみ。
禁止ファイル(KNOWLEDGE / RESEARCH_REPORT / 上位 PREREG_* / AUDIT_2026-09 / SURVEY / research_*.py / judge_*.py /
paper_on1.py / P2-02・P2-03)は開いていない。

## 1. 監査 3 の最小修正 4 件

| # | 項目 | 判定 | 本監査の実測・確認 |
|---|---|---|---|
| i | σ・MDE・着手前関門を実測値に置換 | **解決** | 独立に再計算: 月次規則で除外 471 / n=2,769、σ(r_night)=**113.18**、SE 2.151、MDE **6.026**;差 σ=**151.55**、MDE **8.069**。費用は中央値 7.907 / 平均 8.886 / 範囲 4.54〜17.33bps、関門 8.886+6.026=**14.91bps**。PREREG の 113.18 / 151.55 / 6.03 / 8.07 / 8.89 / 14.9 と全て一致。 |
| ii | 夜間行の整列規則と検証方法の登録 | **解決** | 取引日慣行・ペア (t,t+1) ↔ 夜間行 t+1・水準比較による検証・恒等式(telescoping)ゆえ整合性テストは検証にならない旨、いずれも §データと対照 3 の両方に明記。 |
| iii | 夜間不在の 5 ペアを日付ペアで列挙 | **解決** | 列挙された 5 ペアは本監査の独立抽出と完全一致(下記)。「5 日を含むペア」という不精確な表現は母集団定義から消えている。 |
| iv | glitch 閾値の適用対象を明記 | **解決** | `drop_glitches` は `df["r"]` を素で `|r| > threshold` 比較(`overnight.py` L87-101、既定 `GLITCH_ABS_LOG_RET_DEFAULT = 0.10`)。PREREG は「本単位(kind="simple")では **|単純収益| > 0.10** として効く」と実装どおりに登録。 |

## 2. 整列チェックの自前再実行(1 回)

開発セット(日中 3,241 行 2007-09-19..2020-12-18、ペア 3,240)で再計算:

- median |night_open(t+1) − close_day(t)| = **20 円**、median |night_open(t+1) − close_day(t+1)| = **110 円**。
  PREREG が登録した検証条件(前者 < 後者)は成立。整列は日付 t+1 の夜間行で確定。
- 夜間行の被覆 **3,235 / 3,240** ペア。欠落は次の 5 ペアのみで、PREREG の列挙と一字一句一致:
  (2007-12-28→2008-01-04)、(2008-01-04→01-07)、(2008-12-30→2009-01-05)、(2009-01-05→01-06)、(2016-07-15→07-19)。

## 3. 文言により新たに生じた問題

- **N5(軽微・内部矛盾)** データ表の夜間日次の行に「対照 3 と**整合性テスト**に使う」が残存(§データ)。本文 §リターン定義・対照 3 では
  「整合性テストは恒等式なので使わない」と登録済みで、記述が食い違う。データ表側の「整合性テスト」の語を削るだけで解消。
- **N6(軽微)** 副指標の一覧に「夜間セッション**無しの 5 日**を除いた場合」という旧表現が 1 箇所残る(§指標と分母)。母集団定義は
  ペア列挙に直っているので実害は無いが、「上記 5 ペア」に揃えるのが望ましい。
- **N7(軽微・欠落)** 対照 3 の分母が未記載。整列規則が確定した結果、(b) レグを持つのは 3,235 ペア(上記 5 ペアは (b) が存在しない)で、
  主指標の 3,240 / 2,769 とは分母が異なる。対照 3 の n をどちらで報告するかを一文で決めておくと最終評価での揺れが無い。
- これら 3 件はいずれも数値・判定・分母の実体に影響せず、凍結を妨げない(凍結後の誤読防止として推奨する程度)。

## 4. 判定(TEMPLATES §4 の 10 問)

| 問 | 判定 |
|---|---|
| Q1 仮説文 | 可 |
| Q2 分母・n・MDE | **可**(実測と完全一致。監査 3 の要修正は解消) |
| Q3 封印の一致と非重複 | 可(端点規則は対照 3 にも適用) |
| Q4 コスト定数 | 可(全て primary_document、bps 換算は元本連動) |
| Q5 対照 3 種 | **可**(整列規則の登録により条件充足。N7 は分母の明記のみ) |
| Q6 探索面 N0 | 可(N0 = 1、反復の加算規則も明記) |
| Q7 停止規則 | 可 |
| Q8 既知欠陥への対処 | **可**(夜間不在をペアで列挙。N6 は表現の残り) |
| Q9 既知正解テスト | 可(simple 経路の事前確認が条件として登録済み) |
| Q10 反証文 | 可(閾値は訂正後の値と整合) |

**総合判定: 可 — 凍結可**。監査 3 の最小修正 4 件はすべて解決し、独立再計算で数値も一致した。
新規指摘 N5〜N7 は軽微な文言・分母表記の整理で、凍結の条件としない(凍結時に併せて直せば足りる)。
