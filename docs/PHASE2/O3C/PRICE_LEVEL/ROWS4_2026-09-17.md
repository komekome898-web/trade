# O-3c「4 行」の結果(2026-09-17、委任先の実装)

設計: `DESIGN_2026-09-17.md`。全件走行: `FULL_2026-09-17.md`(以下「FULL」)。
追加 (a)(b)(c): `EXT_2026-09-17.md`(以下「EXT」)。3 件目の一覧: `MISSING_2026-09-17.md`(以下「MISSING」)。
測定器の検証: `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-14.md`(以下「REPORT」)。

**観測表のみ。判定(予測できる/できない、当たる/当たらない、使える/使えない、有効/無効)は書かない。**
各主張に 事実 / 推定 / 仮定 の印を付ける。用語は前の文書と同じ(ノード / 空白 / 束 / 帯 / W / b)。

## 0.1 対応表

**【リード注記、監査 8】「行 N の文面」はいずれもリードの返答 053 の表の左列(委任文面)で、
オーナーはそれを L-192「4 行全部進めてください」で承認した。文面自体はオーナー自身の語ではない。**

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 行 1: 清算の重複行を一意化してから `build_cascades` に掛ける | L-192「**4 行全部進めてください**」/ 行 1 の文面「**重複行を一意化(全列一致の行を 1 件に)してから、測定器の束ね `build_cascades` を使う**」(リードの返答 053 の表の左列。オーナーが L-192 で承認) |
| 行 1 で数える統計量(束の数・束あたり件数の分布・束の長さの分布・幅 0 の束) | **(該当語なし)**。リードの委任文面の指定(監査 6) |
| 行 2: 「幅 0 の断片」「タイ」を一意化した後に数え直す | L-192「**4 行全部進めてください**」/ 行 2 の文面「**3 件目の一覧 §4・§12 の「幅 0 の断片」「タイ」の数え直しは、一意化した後に行う**」(リードの返答 053 の表の左列。オーナーが L-192 で承認) |
| 行 2 の内訳(タイの件数 / タイの経路 / 幅 0 の断片の件数) | REPORT §0.0 からの引用(**リードの文書。オーナーの逐語ではない**。監査 8) |
| 行 3: 帯の位置(56 / 70bp)を前半 236 日で決め直し、後半 236 日だけで数える | L-192「**4 行全部進めてください**」/ 行 3 の文面「**(b) の 56 / 70bp を前半 236 日で決め、後半 236 日で数える形に置き直す**」(リードの返答 053 の表の左列。オーナーが L-192 で承認) |
| 行 4: W = 8h と 24h を 2 列で並べる | L-192「**4 行全部進めてください**」/ 行 4 の文面「**次の表は W = 8h と 24h の 2 本を並べて出す**」(リードの返答 053 の表の左列。オーナーが L-192 で承認) |
| 大もとの問い | `docs/PHASE2/O3C/OWNER_INTENT_2026-09-12.md` §1(L-139)「**価格毎の約定履歴の積み上げからどの価格帯で清算が起きるかの予測できるか、できるならエントリーや利確に活かせるか**」 |
| 一意化の既定を `False` のままにし、新しい測定だけが明示的に `dedup=True` を渡す | **(該当語なし)**。委任先の判断(§6 の 1) |
| 一意化の判定を**生の CSV 行の全 10 列一致**で行う | **(該当語なし)**。委任先の判断(§6 の 2) |
| タイを数えるときの価格系列の粒度を **60 秒バー**、反応窓(地平線)を **15 分**にする | **(該当語なし)**。リードの指示の具体化(委任の文面「**合成データの検証で使った値(REPORT §0.0 参照。バー 60 秒、地平線 15 分が既定だった)を仮定として使い**」)。**仮定**である |
| 上に加えて**約定そのまま**(aggTrades をそのまま)の粒度でも数える | **(該当語なし)**。委任先の判断(§6 の 4) |
| タイの経路を「時刻が同じ」= 時間分解能 /「時刻は違うが価格が同じ」で分ける | **(該当語なし)**。委任先の判断(委任の文面「**分け方の定義を報告に書き、コードで数える**」)。§3.2。**【リード訂正、監査 1】経路名を「価格の刻み」から「価格が同じ(時刻は違う)」に改めた(合成データの `quantize_prices` 格子と同じ機構だという根拠が無いため)** |
| 「幅 0 の断片」を `end_ms == start_ms` の束とする | **(該当語なし)**。委任先の判断(委任の文面「**REPORT §0.0 の B 行の意味に合わせる。合わせ方を報告に書く**」)。§3.3 |
| 多重度 4 の 13 群がなぜ生じるかを調べる | **(該当語なし)**。リードが監査 4 を受けて実測(§2.1) |
| 分位のずれ(MISSING §4 との 1% 未満の差)の原因を特定する | **(該当語なし)**。リードが監査 7 を受けて実測(§2.3) |
| 前半 / 後半を**日付順のファイル 236 本ずつ**で割る(暦日の半分ではない) | **(該当語なし)**。委任先の判断(§6 の 6) |
| 帯の位置を `offset = −(dist_node_bp の side 別中央値) / 1e4` を小数 4 桁に丸めて置く | **(該当語なし)**。前回(EXT)と同じ取り方に揃えた。委任先の判断(§6 の 7) |
| `o3c_price_level_ext.py` に `--fit-days` / `--dedup-liq` を足す(別スクリプトにしない) | **(該当語なし)**。委任の文面「**`--fit-days` / `--eval-days` のような引数を足すか、別スクリプトにする(どちらにしたかを書く)**」に対する選択 |
| W = 8h の「全期間で決めて全期間で数えた」列も作る(前回は W = 24h しか無いため) | **(該当語なし)**。委任先の判断(§6 の 8) |

## 1. 走らせたもの(事実)

根 = 本体 `backtest_data/binance_cm_o3c_20260913/` + 補遺 `..._supp_20260917/` をシンボリック
リンクで重ねたディレクトリ(作り方 FULL §1)。日 = `liquidationSnapshot` の zip 名 472 日。
以下 `<merged_root>` と書く。

```
# 行 1
python3 scripts/o3c_rows4.py cascades --data-root <merged_root> \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/cascades
# 行 2
python3 scripts/o3c_rows4.py ties --data-root <merged_root> \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/ties
# 行 4 の (a)(一意化後の「束の最初の 1 件」の表)を W = 24h / 8h で
python3 scripts/o3c_price_level_ext.py bundle --days <472 日> --data-root <merged_root> \
    --dedup-liq --window-hours 24 --out-dir backtest_data/o3c_price_level_rows4_20260917/bundle_w24
python3 scripts/o3c_price_level_ext.py bundle --days <472 日> --data-root <merged_root> \
    --dedup-liq --window-hours 8  --out-dir backtest_data/o3c_price_level_rows4_20260917/bundle_w8
# 行 3 + 行 4(前半 236 日で帯を決め、後半 236 日だけで数える)
python3 scripts/o3c_price_level_ext.py band --days <後半 236 日> --fit-days <前半 236 日> \
    --data-root <merged_root> --dedup-liq --window-hours 24 \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/band_split_w24
python3 scripts/o3c_price_level_ext.py band --days <後半 236 日> --fit-days <前半 236 日> \
    --data-root <merged_root> --dedup-liq --window-hours 8 \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/band_split_w8
# 比較のための「全期間で決めて全期間で数えた」列(W = 8h は前回が無いので作った)
python3 scripts/o3c_price_level_ext.py band --days <472 日> --fit-days <472 日> \
    --data-root <merged_root> --dedup-liq --window-hours 24 \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/band_full_w24
python3 scripts/o3c_price_level_ext.py band --days <472 日> --fit-days <472 日> \
    --data-root <merged_root> --dedup-liq --window-hours 8 \
    --out-dir backtest_data/o3c_price_level_rows4_20260917/band_full_w8
```

出力(すべて `table.csv` + `summary.json` + `MD5SUMS`。`MD5SUMS` のパスに `./` は付けていない):

**【リード訂正、監査 3】**下の列は `table.csv` の最終行ではなく、**各スクリプトが実行終了時に
標準出力へ出す集計行**である(`scripts/o3c_price_level_ext.py:675-701` の f-string など)。
列名を「最後の 1 行(そのまま)」から改めた。

| 出力 | 行(ヘッダ込み) | 所要 | 標準出力の集計行(そのまま) |
|---|---|---|---|
| `.../cascades/table.csv` | 57 | 2.1 秒 | `一意 53398 件 / 束 22036 個 / 幅 0 の束 12436` |
| `.../ties/table.csv` | 22,037 | 86.7 秒 | (標準出力の集計行は保存していない。値は `ties/summary.json` から §3.1 に転記) |
| `.../bundle_w24/table.csv` | 44,081 | 475.7 秒 | `行 44080(清算 22040 / 対照 22040) 束 22040` |
| `.../bundle_w8/table.csv` | 44,081 | 528.3 秒 | `行 44080(清算 22040 / 対照 22040) 束 22040` |
| `.../band_split_w24/table.csv` | 11,327 | 276.1 秒 | `行 11326(時点 5663)` |
| `.../band_split_w8/table.csv` | 11,327 | 297.0 秒 | `行 11326(時点 5663)` |
| `.../band_full_w24/table.csv` | 22,653 | 360.0 秒 | `行 22652(時点 11326)` |
| `.../band_full_w8/table.csv` | 22,653 | 414.9 秒 | `行 22652(時点 11326)` |

`bundle` / `band` は 4 本ずつ並走させたので、所要は単独走行より長い(既定の 1 走行は FULL で 388 秒)。

## 2. 行 1: 一意化 →(束ね)`build_cascades`

逐語: 「**重複行を一意化(全列一致の行を 1 件に)してから、測定器の束ね `build_cascades` を使う**」

### 2.1 一意化そのもの(事実、`cascades/summary.json: dedup`)

| | 値 |
|---|---|
| 入力の行数 | **106,822** |
| 一意化後 | **53,398** |
| 落とした行 | 53,424(50.01%) |
| 多重度の分布 | 2 が 53,385 群 / 4 が 13 群。**多重度 1(1 回しか現れない中身)は 0 群** |

EXT §2.0 がリードの実測として書いた値(一意 53,398、多重度 2 が 53,385 群・4 が 13 群)と**一致した**(事実)。
判定は**生の CSV の全 10 列一致**で行っている(§6 の 2)。

**多重度 4 の 13 群がなぜ生じるかを調べた(監査 4。リード実測)。**472 日を 1 ファイルずつ数えたところ、
多重度 4 の 13 群は**すべて 2023-09-21 の 1 ファイルに集中**している。そのファイルは 196 行(ヘッダ抜き)
= 多重度 2 が 72 群 + 多重度 4 が 13 群。**残り 471 ファイルには多重度 4 は 1 件も無い(事実)。**
補遺 `backtest_data/binance_cm_o3c_supp_20260917/` に `liquidationSnapshot` は含まれない(`ls` で確認)ので、
「本体+補遺の重ね方による二重読み込み」ではない(事実)。**取引所側で 2023-09-21 に何が起きて 1 件の
清算が 4 回記録されたかは未確認(一次資料に当たっていない)。**事実として言えるのは、
多重度 4 が 1 日に集中していることだけ。コマンドと出力:

```
$ PYTHONPATH=src python3 -  (各 zip を読み、全列一致で groupby(size)==4 の群を数えた)
BTCUSD_PERP-liquidationSnapshot-2023-09-21.zip rows 196 mult dist {2: 72, 4: 13}
total files: 472 files with mult4: 1
```

### 2.2 `build_cascades`(`gap_ms` = 60,000ms 既定)の結果(事実、`cascades/table.csv`)

| | 一意化前(raw) | 一意化後(dedup) |
|---|---|---|
| 清算の件数 | 106,822 | **53,398** |
| 束の数 | **22,036** | **22,036**(変わらない) |
| 束あたり件数 = 1 | 0 | **12,436** |
| 束あたり件数 = 2 | 12,427 | 4,034 |
| 束あたり件数 3〜5 | 4,043 | 3,699 |
| 束あたり件数 6〜 | 5,566 | 1,867 |
| 束あたり件数 q50 / q75 / q90 / q95 / q99 / 最大 | 2 / 6 / 10 / 14 / 32 / 244 | **1 / 3 / 5 / 7 / 16 / 122** |
| 束あたり件数の平均 | 4.848 | **2.423** |
| `total_size`(= Σ qty × price)の合計 | 1,045,566,877,989.2 | **522,701,712,914.2** |
| 束の向き long / short / mixed | 12,458 / 9,397 / 181 | 同左(変わらない) |

**束の長さ(end − start、ms)の分布は一意化で 1 件も動かない(事実)。**重複行は同一 ms なので、
束の境界にも端の時刻にも効かないため。

| 束の長さ | 件数(raw = dedup) |
|---|---|
| **= 0(幅 0 の束)** | **12,436(56.43%)** |
| 1〜999ms | 15 |
| 1〜10 秒 | 2,624 |
| 10〜60 秒 | 5,013 |
| 60 秒〜10 分 | 1,948 |
| 10 分以上 | 0 |
| 分位 q50 / q75 / q90 / q95 / q99 / 最大(ms) | 0 / 20,019 / 56,897.5 / 83,011.25 / 151,872.25 / **593,960** |

**束の数が EXT §2.1 の 22,040 と 4 個ずれる(事実)。**EXT の (a) は
`o3c_price_level_table.py: bundle_ranges` を**日(ファイル)ごとに**掛けており、
日をまたぐ束が切れる。ここは `build_cascades` を全期間 1 本の列に掛けているので、
**日をまたぐ束 4 個がつながって 22,036 になる。**どちらも `gap_ms` = 60,000ms で
定義は同じ(`bundle_ranges` の docstring が `build_cascades` と同じ規則だと明記している)。
**ずれの原因は束ね方ではなく、掛ける単位(日ごと / 全期間)である。**

### 2.3 清算の間隔の分布(MISSING §4 の数え直し。事実)

MISSING §4 は 6 日標本で間隔分布を出し、「中央値 0ms」「60,000ms 以下 78.9%(819/1,038)」
「0ms が 522/1,038 = 50.3%」と書いて、重複を落とすと 0ms はほぼ消える**見込み(未再計算)**としていた。
**数え直した(全 472 日と、MISSING と同じ 6 日の両方。6 日の行は MISSING と同じく日ごとに区切って数えた)。**

| | 6 日・一意化前 | 6 日・一意化後 | 472 日・一意化前 | 472 日・一意化後 |
|---|---|---|---|---|
| 間隔の件数 | 1,038 | 516 | 106,821 | 53,397 |
| 最小 (ms) | 0 | **996** | 0 | **902** |
| q25 | 0 | 3,843.25 | 0 | 3,009 |
| **q50** | **0** | **33,842.5** | **0** | **27,921** |
| q75 | 33,086.25 | 389,484 | 27,880 | 320,032 |
| q90 | 758,614.6 | 2,601,061.5 | 562,017 | 1,873,882 |
| q99 | 7,219,892.59 | 8,699,010.8 | 7,905,829.6 | 11,765,063.04 |
| 最大 | 28,374,082 | 28,374,082 | 187,806,964 | 187,806,964 |
| 間隔 0ms | **522(50.29%)** | **0(0%)** | **53,424(50.01%)** | **0(0%)** |
| 間隔 ≤ 60,000ms | 819(78.90%) | 297(57.56%) | 84,786(79.37%) | 31,362(58.73%) |

- **6 日・一意化前の「間隔の件数 1,038」「0ms が 522」「≤60,000ms が 819」は MISSING §4 の値と
  完全に一致した(事実)。**最大 28,374,082ms も一致。**中間の分位は 1% 未満ずれる**
  (q75 33,086.25 対 MISSING の 33,451、q90 758,614.6 対 778,986、q99 7,219,892.59 対 7,272,874)。
  **原因を特定した(監査 7。リード実測)。**MISSING §4 は `np.percentile` の `higher`(= `inverted_cdf`)型で、
  本書は既定の `linear`(内挿)型を使っていた。同じ 6 日・同じ 1,038 件の間隔を `method="higher"` で
  取ると 33,451 / 778,986 / 7,272,874 と 3 つとも MISSING の値に一致した。**データも件数も同じで、
  分位の型の違いだけである(事実)。**この違いは前半/後半の枠組みには影響しない(データと件数が同じため)。
  コマンドと出力:

```
$ PYTHONPATH=src python3 -  (6 日の zip を読み、日ごとに np.diff(np.sort(time)) を連結 → np.percentile(x, q, method=...))
linear     [33086.25, 758614.600000002, 7219892.589999983]
higher     [33451.0, 778986.0, 7272874.0]
inverted_cdf [33451.0, 778986.0, 7272874.0]
```
- **一意化すると間隔 0ms は 1 件も残らない(事実)。**MISSING §4 の「見込み」と §14 の 2 件目
  (「同時刻の複数レコードの数え方が未定義」)は、これで数えられた。
- **一意化しても `gap_ms` = 60,000ms 以下の間隔が過半(58.7%)である(事実)。**
  MISSING §4 の 78.9% は重複込みの値で、一意化すると下がる。

## 3. 行 2: 前提条件 3 つの数え直し(一意化後)

逐語(行 2 の文面。リードの返答 053 の表の左列で、オーナーは L-192 で承認した): 「**3 件目の一覧
§4・§12 の「幅 0 の断片」「タイ」の数え直しは、一意化した後に行う**」
**以下の「前提条件 3 つ」は REPORT §0.0(委任元の文書)からの引用であり、オーナーの逐語ではない
(監査 8)。**REPORT §0.0 の前提条件 3 つ: 「**タイの件数を数える / タイの経路(価格の刻み / 時間分解能)を分ける /
幅 0 の断片の件数を数える**」

対象 = §2 の一意化後の束 **22,036 個**。価格系列は aggTrades から作る。

### 3.1 タイの件数(事実、`ties/summary.json`)

**タイ** = `attach_internal_direction` が付ける粗い内部方向 `internal_bp` がちょうど 0 の行
(= 起点価格と終点価格が等しい行)。
起点(anchor)は既定 `before` と `after_shift` の両方で数えた。反応窓は 15 分(**仮定**)。

| 粒度 | 起点 | 行 | **タイ** | **タイ率** | 欠測 | 除外率 |
|---|---|---|---|---|---|---|
| 60 秒バー(**仮定**) | `before`(既定) | 22,036 | **17,309** | **0.7855** | 0 | 0.0 |
| 60 秒バー | `after_shift` | 22,036 | **133** | **0.0060** | 0 | 0.0 |
| 約定そのまま | `before`(既定) | 22,036 | **12,592** | **0.5714** | 0 | 0.0 |
| 約定そのまま | `after_shift` | 22,036 | **10,154** | **0.4608** | 0 | 0.0 |

- 上の「タイ」は `reversal_scores(tie_policy="keep")` が返す `n_tie` / `tie_rate` で、
  **自前に起点・終点の点を引き直して数えた件数と 1 件も違わない**(スクリプト内の `assert` で確認。事実)。
- **欠測は 4 通りとも 0 件**(起点も 15 分後の価格も全部引けた。事実)。
  REPORT の射程 J(群ごとの欠測率)は、この実群については 0 である(事実)。**対照群は測っていない**(§7)。
- **60 秒バー・既定の起点でのタイ率 0.7855 は、`liq_response.py` 365〜367 行が
  「タイは実カスケードで 8 割」と書いた水準と同じ桁である(事実)。**
  その記述は 2026-09-13 の観測(一意化前)なので、**同じ数ではない**(事実)。

### 3.2 タイの経路(事実)

**分け方(原文に無い。委任先が置いた定義。コードは `scripts/o3c_rows4.py: classify`)**:
タイと判定した行だけを、起点の点と終点の点の**時刻**で 2 つに分ける。

- **時間分解能** (`time_resolution`): 起点の点と終点の点の**時刻が同じ** = その粒度では
  窓が 1 点に潰れていて、窓の内側で時刻が 1 つも進んでいない(委任の文面「**窓の内側に点が無い・バーが同じ**」)。
- **価格が同じ(時刻は違う)**(コード上の変数名は `price_tick`): **時刻は違う**のに価格が等しい =
  その間に価格が動いていない(委任の文面「**起点と終点の価格が同じ約定価格 = 価格が動いていない**」)。

**【リード訂正、監査 1】**変数名 `price_tick` は合成データの検証(REPORT §1.4.3)の「価格の刻み」
(`quantize_prices` による 40bp 格子への量子化。**人工の粗い格子**)から取った名だが、実データの
約定価格は人工的な格子ではなく**実際の約定価格そのもの**である。**両者が同じ機構を指すという
根拠は本文に無く、確かめていない(仮定ではなく未確認)。**そのため経路の表示名は「価格が同じ
(時刻は違う)」に改め、変数名 `price_tick` とは分けて読む。REPORT の射程 E との対応は、
「タイが時刻の一致ではなく価格の一致から生じる」という**分け方の型**が同じであることだけを指し、
発生機構が同一だとは主張しない。

2 つは排他で、タイの全件がどちらかに入る(事実。件数の和がタイの件数に一致)。

| 粒度 | 起点 | タイ | 時間分解能 | 価格が同じ(時刻は違う) | その割合 |
|---|---|---|---|---|---|
| 60 秒バー | `before` | 17,309 | **17,307** | **2** | 0.0001 |
| 60 秒バー | `after_shift` | 133 | 99 | 34 | 0.2556 |
| 約定そのまま | `before` | 12,592 | **12,436** | **156** | 0.0124 |
| 約定そのまま | `after_shift` | 10,154 | **8,898** | **1,256** | 0.1237 |

REPORT の射程 E は「価格の刻みで作ったタイ(合成データ、`quantize_prices` の 40bp 格子)に対する
`after_shift` は測っていない(09-16・09-17 の 8 行は時間分解能の経路だけ。**価格の刻みの経路は
0 件**)」と書いていた。**実データでは『価格が同じ(時刻は違う)』の経路が 0 件ではない(事実)**:
約定そのままの `after_shift` で 1,256 件(タイの 12.4%)、60 秒バーの `after_shift` で 34 件(同 25.6%)。
**これが REPORT の E 行の「価格の刻み」と同じ機構かは確かめていない(未確認)。**
**これは件数の観測であって、`after_shift` がその経路で正しく測れるかどうかは測っていない**(§7)。

### 3.3 幅 0 の断片(事実)

**合わせ方(委任の文面「**REPORT §0.0 の B 行の意味に合わせる。合わせ方を報告に書く**」)**:
REPORT §0.0 の B 行は「**幅 0 の断片の 0 埋め**(`GAP_MS`=60 秒でカスケードが分割される場面)」で、
合成データでは 1 本のカスケードが `GAP_MS` で切られて `start == end` の断片になり、
その断片の内部方向が 0 で埋まることを指していた(`REPORT_2026-09-17_anchor.md` の
「実カスケードが `GAP_MS` で幅 0 の断片 93 行に分割され 0 埋めされる」)。
**ここでは `end_ms == start_ms` の束をそう数えた**(行 1 の「幅 0 の束」と同じ定義)。
**違い**: 合成では 1 本の長いカスケードが分割されてできた断片で、実データでは分割の由来を問わず
「束が 1 つの ms に収まっている」ものを数えている(**この 2 つが同じ集合だとは確かめていない**)。

| | 件数 | 割合 |
|---|---|---|
| **幅 0 の束(一意化後)** | **12,436** | **0.5643** |
| うち一意 1 件だけの束 | 12,436 | 1.0000 |
| 幅が正の束 | 9,600 | 0.4357 |

**幅 0 の束 12,436 と、一意化後に束あたり件数 = 1 の束 12,436 は同じ集合である(事実)。**
一意化後は「同じ ms に 2 件以上の別の清算」が 1 件も無いため(§2.3 で間隔 0ms が 0 件)。

幅 0 かどうかで分けたタイ率(事実):

| 粒度 | 起点 | 幅 0 の束のタイ率 | 幅が正の束のタイ率 |
|---|---|---|---|
| 60 秒バー | `before` | **1.0000**(12,436/12,436) | 0.5076(4,873/9,600) |
| 60 秒バー | `after_shift` | 0.0104(129) | 0.0004(4) |
| 約定そのまま | `before` | **1.0000**(12,436/12,436) | 0.0163(156/9,600) |
| 約定そのまま | `after_shift` | 0.8046(10,006) | 0.0154(148/9,600) |

- **既定の起点では、幅 0 の束は粒度によらず必ずタイになる(事実)。**起点 `at_or_before(start_ms)` と
  終点 `at_or_before(end_ms)` が、`start_ms == end_ms` なので同じ点を指すため。
- **約定そのまま・`after_shift` でも幅 0 の束の 80.5% がタイのままである(事実)。**

幅 0 かどうか × 経路(`ties/table.csv` から数えた。事実。列名「価格が同じ」は §3.2 の訂正後の
表示名で、コード上の変数名 `price_tick` と同じ実体を指す):

| 粒度 / 起点 | 幅 0 の束 12,436 | 幅が正の束 9,600 |
|---|---|---|
| 60 秒バー / `before` | 時間分解能 12,436 | 時間分解能 4,871 / 価格が同じ 2 / 非タイ 4,727 |
| 60 秒バー / `after_shift` | 時間分解能 99 / 価格が同じ 30 / 非タイ 12,307 | 価格が同じ 4 / 非タイ 9,596 |
| 約定そのまま / `before` | 時間分解能 12,436 | 価格が同じ 156 / 非タイ 9,444 |
| 約定そのまま / `after_shift` | **時間分解能 8,898 / 価格が同じ 1,108** / 非タイ 2,430 | 価格が同じ 148 / 非タイ 9,452 |

- **約定そのまま・`after_shift` の「時間分解能」8,898 件は、全部が幅 0 の束である(事実)。**
  幅 0 の束で起点 `at_or_before(t)` と終点 `at_or_after(t)` が同じ時刻に落ちるのは、
  **その ms に約定がある**場合である(**推定**。約定の時刻と清算の時刻の突き合わせは別に測っていない)。
  残る 1,108 件は時刻が違うのに価格が同じ = 「価格が同じ(時刻は違う)」の経路(事実)。

## 4. 行 3: (b) の置き直し(前半で決め、後半で数える)

逐語: 「**(b) の 56 / 70bp を前半 236 日で決め、後半 236 日で数える形に置き直す**」

### 4.1 期間の割り方(事実)

`liquidationSnapshot` の zip がある 472 日を日付順に並べ、**ファイル 236 本ずつ**に割った。

| | 最初の日 | 最後の日 | 日数 |
|---|---|---|---|
| 前半(帯を決める側) | **2023-06-25** | **2024-02-18** | 236 |
| 後半(数える側) | **2024-02-19** | **2024-10-14** | 236 |

**暦日の半分ではない(事実)**: 清算の zip が無い日(`aggTrades` はある 2023-09-09 / 2023-09-23 /
2024-06-01 の 3 日)を数えていないので、境界は暦日の中点からずれる。

### 4.2 前半 236 日で決めた帯の位置(事実、`band_split_*/summary.json: params.fit`)

取り方は前回(EXT §1 の (b))と同じ: 清算行の `dist_node_bp` の side 別**中央値**を採り、
`offset = −中央値 / 1e4` を小数 4 桁に丸める。違うのは**期間**(前回は全 472 日)と、
**一意化した清算行を使う**ことだけ。前半の清算行は 22,775(SELL 13,350 / BUY 9,425)。

| | 前回(全 472 日、一意化なし。EXT §1) | 前半 236 日 W = 24h | 前半 236 日 W = 8h |
|---|---|---|---|
| SELL の `dist_node_bp` 中央値 | +56.4bp | **+45.06bp** | **+47.22bp** |
| BUY の `dist_node_bp` 中央値 | −70.2bp | **−71.45bp** | **−59.59bp** |
| SELL の offset | −0.0056 | **−0.0045** | **−0.0047** |
| BUY の offset | +0.0070 | **+0.0071** | **+0.0060** |

**SELL は前半だけで決めると 56.4 → 45.06bp(W=24h)に動く(事実)。BUY はほぼ動かない(−70.2 → −71.45)。**

### 4.3 後半 236 日で数えた帯の指標(事実)

帯 = ノードの代表価格 ×(1 + offset)が入るビンの ±1 ビン(前回と同じ)。
時点 = 各正時、行 = 時点 × side。後半 236 日 × 24 = 5,664 のうち **5,663**(窓が空の 1 点を飛ばした)。
全期間の列は 472 日 × 24 = 11,328 のうち **11,326**(窓が空の 2 点を飛ばした)。

**W = 8h と W = 24h の 2 本を並べる(行 4)。**「全期間 / 全期間」は §4.4 の走行(一意化して
全 472 日で決め、全 472 日で数えたもの)である。

| | **W = 24h** 前半で決め / 後半で数える | **W = 24h** 全期間 / 全期間 | **W = 8h** 前半で決め / 後半で数える | **W = 8h** 全期間 / 全期間 |
|---|---|---|---|---|
| 帯の offset(SELL / BUY) | −0.0045 / +0.0071 | −0.0056 / +0.0070 | −0.0047 / +0.0060 | −0.0056 / +0.0061 |
| **SELL** | | | | |
| 清算のあった時点 | 2,730 / 5,663 | 4,876 / 11,326 | 2,730 / 5,663 | 4,876 / 11,326 |
| 清算件数(一意化後) | 19,474 | 32,824 | 19,474 | 32,824 |
| うち帯に入った件数 | 4,275 | 8,219 | 4,673 | 8,690 |
| **`in_band_rate`** | **0.2195** | **0.2504** | **0.2400** | **0.2647** |
| `band_coverage_wtd` | 0.2009 | 0.1969 | 0.2208 | 0.1998 |
| `band_coverage_log_wtd` | 0.2017 | 0.1978 | 0.2218 | 0.2007 |
| `trade_in_band_rate_wtd` | 0.2187 | 0.2408 | 0.2410 | 0.2445 |
| `trade_in_band_rate_pooled` | 0.1978 | 0.1877 | 0.1935 | 0.1663 |
| `band_coverage_mean` | 0.2121 | 0.1999 | 0.2168 | 0.1764 |
| **BUY** | | | | |
| 清算のあった時点 | 2,312 / 5,663 | 4,077 / 11,326 | 2,312 / 5,663 | 4,077 / 11,326 |
| 清算件数(一意化後) | 11,147 | 20,572 | 11,147 | 20,572 |
| うち帯に入った件数 | 2,702 | 5,082 | 3,097 | 5,902 |
| **`in_band_rate`** | **0.2424** | **0.2470** | **0.2778** | **0.2869** |
| `band_coverage_wtd` | 0.1947 | 0.1924 | 0.1954 | 0.1884 |
| `band_coverage_log_wtd` | 0.1934 | 0.1912 | 0.1943 | 0.1874 |
| `trade_in_band_rate_wtd` | 0.2357 | 0.2339 | 0.2473 | 0.2506 |
| `trade_in_band_rate_pooled` | 0.1872 | 0.1821 | 0.1869 | 0.1792 |
| `band_coverage_mean` | 0.1950 | 0.1867 | 0.1899 | 0.1725 |

**3 つ(または 4 つ)を並べただけである。どれが大きいから当たる・当たらない、という判定は書かない。**

### 4.4 一意化して走らせ直した「全期間 / 全期間」と、前回(EXT §3)との突き合わせ(事実)

`band_full_w24`(一意化あり、全 472 日で決め、全 472 日で数える)の帯の位置は
**SELL −0.0056 / BUY +0.0070 で、前回(EXT)と同じ値になった**(中央値は SELL +56.35bp /
BUY −70.26bp。EXT が使った +56.4 / −70.2 と 4 桁の丸めで一致)。その上で:

| | EXT §3(一意化なし) | `band_full_w24`(一意化あり) | 差 |
|---|---|---|---|
| SELL `in_band_rate` | 0.2504 | **0.250396** | 無し |
| SELL `band_coverage_wtd` | 0.1970 | 0.196948 | 無し(EXT の表記 0.1970 は 0.196948 の丸め方の違い) |
| SELL `band_coverage_log_wtd` | 0.1978 | 0.197831 | 無し |
| SELL `trade_in_band_rate_wtd` | 0.2408 | 0.240773 | 無し |
| SELL `trade_in_band_rate_pooled` | 0.1877 | 0.187694 | 無し |
| SELL `band_coverage_mean` | 0.1999 | 0.199939 | 無し |
| BUY `in_band_rate` | 0.2470 | 0.247035 | 無し |
| BUY 他の 5 つ | 0.1924 / 0.1912 / 0.2339 / 0.1821 / 0.1867 | 0.192430 / 0.191177 / 0.233940 / 0.182079 / 0.186734 | 無し |
| SELL 清算件数 | 65,660(2 重) | **32,824** | 半分より **6 件少ない** |
| BUY 清算件数 | 41,158(2 重) | **20,572** | 半分より **7 件少ない** |

**割合は一意化で 1 つも変わらない(事実)。**件数だけが半分になる。
**ちょうど半分ではなく 13 件少ない**のは、多重度 4 の 13 群(52 行 → 13 行)があるため(§2.1)。

### 4.5 時点ごとの分位(事実)

| 列 | W=24h 後半 SELL q5/q25/**q50**/q75/q95 | W=24h 後半 BUY | W=8h 後半 SELL | W=8h 後半 BUY |
|---|---|---|---|---|
| `in_band_rate`(清算のあった時点のみ) | 0.00/0.00/**0.00**/0.667/1.00 | 0.00/0.00/**0.00**/0.571/1.00 | 0.00/0.00/**0.089**/0.667/1.00 | 0.00/0.00/**0.00**/0.733/1.00 |
| `band_coverage` | 0.143/0.177/**0.208**/0.241/0.310 | 0.090/0.166/**0.196**/0.230/0.287 | 0.00/0.175/**0.222**/0.272/0.346 | 0.00/0.150/**0.202**/0.251/0.334 |
| `trade_in_band_rate` | 0.00/0.00/**0.00**/0.390/0.955 | 0.00/0.00/**0.00**/0.343/0.925 | 0.00/0.00/**0.00**/0.356/0.863 | 0.00/0.00/**0.00**/0.333/0.851 |
| `n_band_bins`(帯のビン数) | 4/6/**8**/11/17 | 同左 | 3/4/**6**/7/11 | 同左 |
| `n_profile_bins`(範囲のビン数) | 15/27/**39**/55/89 | 同左 | 8/14/**21**/31/55 | 同左 |
| `n_nodes` | 2/3/**4**/6/9 | 同左 | 1/2/**3**/4/6 | 同左 |

**W = 8h では `band_coverage` の q5 が 0.000 になる(事実)。**窓が短い時点では
帯がプロファイルの価格範囲の外に出て、範囲内に 1 ビンも残らないことがある
(`band_coverage` は範囲の外に出た帯を切り落とす。EXT §5 の 3)。

## 5. 行 4: W = 8h と 24h を並べる

逐語: 「**次の表は W = 8h と 24h の 2 本を並べて出す**」

W に依存する表は 2 つある。**帯の表は §4.3(すでに 2 列で並べた)。**もう 1 つが下の
「(a) 束の最初の 1 件の表を一意化後で出し直したもの」である。b は 0.1% のまま。

### 5.1 束の最初の 1 件の表(一意化後、事実)

行 = 22,040 の清算行(束の最初の 1 件)+ 22,040 の対照行。
**束の数 22,040 は日ごとの束ね(EXT と同じ)**であり、§2 の 22,036(全期間 1 本)とは
4 個違う(§2.2 の説明)。

| | **W = 24h** 清算 / 対照 | **W = 8h** 清算 / 対照 |
|---|---|---|
| 行数 | 22,040 / 22,040 | 22,040 / 22,040 |
| `bin_pct` 中央値 | **50.00** / 76.56 | **35.71** / 75.00 |
| `dist_node_bp` 中央値 | +0.87 / −1.07 | +3.35 / −1.33 |
| `\|dist_node_bp\|` 中央値 | 47.70 / 17.77 | 39.80 / 12.89 |
| `dist_gap_bp` 中央値 | −1.69 / +14.52 | −1.41 / +3.50 |
| `\|dist_gap_bp\|` 中央値 | 65.75 / 84.85 | 37.65 / 56.11 |
| `dist_vwap_bp` 中央値 | −2.28 / −8.28 | +4.37 / −6.07 |
| `bin_pct == 0` の割合 | 0.0559 / 0.0003 | 0.1261 / 0.0024 |
| `bin_pct ≤ 10` の割合 | 0.1663 / 0.0048 | 0.2605 / 0.0093 |

side 別(清算行の中央値、事実):

| | 件数 | `bin_pct` W=24h / W=8h | `dist_node_bp` W=24h / W=8h | `dist_gap_bp` W=24h / W=8h | `dist_vwap_bp` W=24h / W=8h |
|---|---|---|---|---|---|
| SELL | 12,566 | 53.13 / 40.00 | **+23.52** / **+29.66** | −16.53 / −6.21 | +48.57 / +46.23 |
| BUY | 9,474 | 44.57 / 29.76 | **−35.77** / **−38.05** | +6.21 / +3.21 | −74.57 / −59.57 |

束の大きさ(一意化後の `bundle_n_events_dedup`)で分けた(事実):

| | 件数 | `bin_pct` 中央値 W=24h / W=8h | `\|dist_node_bp\|` W=24h / W=8h | `\|dist_gap_bp\|` W=24h / W=8h | `bin_pct ≤ 10` W=24h / W=8h |
|---|---|---|---|---|---|
| 一意 1 件の束 | 12,439 | 56.62 / 45.00 | 38.80 / 32.91 | 74.87 / 48.05 | 0.0988 / 0.1835 |
| 一意 2 件以上の束 | 9,601 | **38.64** / **22.22** | 62.24 / 51.16 | 49.13 / 19.07 | **0.2537** / **0.3604** |

**この表の W = 24h の数値は、EXT §2.2・§2.3 の「束の最初」の数値と 1 つも違わない(事実)。**
一意化は「どの行が束の最初か」を変えないため(重複行は同一 ms で、束の最初の行の中身も同じ)。
変わったのは `bundle_n_events` 列だけで、一意化後は `bundle_n_events == bundle_n_events_dedup`
(= 1 が 12,439 / ≥ 2 が 9,601 / 最大 122)になった。**EXT §2.1 の「`bundle_n_events == 1` は 0 件」は
一意化前の列の話で、一意化後は 12,439 件である(事実)。**

## 6. 実装で置いた判断(該当語なしのものは全部ここに書く)

1. **`load_binance_cm_liquidations` の既定を変えていない(該当語なし)。**
   `dedup=False` が既定で、従来の呼び出し(`scripts/measure_liq_response.py` /
   `run_o3c_stage0.py` / `explore_o3c_oi_axis.py` / `src/bot/research/liq_bands.py`)の
   出す数は 1 つも変わらない。**理由**: 既定を変えると、それらが黙って別の数を出すようになるため。
   新しい測定は明示的に `dedup=True`(スクリプトでは `--dedup-liq`)を渡している。
   落とした件数が要るときは `load_binance_cm_liquidations_with_dedup_stats` を使う。
2. **一意化の判定は生の CSV 行の全 10 列一致(該当語なし)。**`LiquidationEvent` は 5 列しか
   持たないので、そちらで一意化すると `order_type` などが違う行まで同じ扱いになりうる。
   `o3c_price_level_table.py` 側(`--dedup-liq`)も同じく全 10 列を読んで落としている。
3. **多重度の分布を返す(該当語なし)。**`DedupStats`(`n_in` / `n_out` / `multiplicity`)。
   `drop_rate` は 0 件のとき **NaN**(0.0 を返して「重複なし」に見せない。`read_rows` と同じ約束)。
4. **タイを 60 秒バーだけでなく「約定そのまま」でも数えた(該当語なし)。**
   委任の文面は 60 秒バーを仮定として使えと書いており、それは §3 の主表にしてある。
   約定そのままを足したのは、**タイが仮定した粒度の産物かどうかがこの 1 本では分からない**ため。
   **日を減らしていない**(委任の文面「**日を減らさず、粒度を粗くする側で対処し**」)。
   約定そのままは日ごとに切って(前後の日を必要な分だけ足して)処理した。
5. **タイの経路の分け方(該当語なし)。**§3.2 に定義を書いた。時刻が同じ = 時間分解能、
   時刻は違うが価格が同じ = 「価格が同じ(時刻は違う)」。判定は「まず価格が等しいか(タイか)」を
   見てから分ける。
6. **前半 / 後半を zip のファイル 236 本ずつで割った(該当語なし)。**逐語は「前半 236 日 /
   後半 236 日」なので日数で割った。暦日で割ると 236 / 236 にならない(清算の zip が無い日が 3 日ある)。
7. **帯の位置は `offset = −中央値 / 1e4` を小数 4 桁に丸めた(該当語なし)。**前回(EXT)が
   +56.4 → −0.0056、−70.2 → +0.0070 と 4 桁に丸めていたので揃えた。丸めの差は最大 0.5bp で、
   ビンの幅(10bp)より小さい。
8. **W = 8h の「全期間で決めて全期間で数えた」列を作った(該当語なし)。**前回(EXT §3)は
   W = 24h でしか走らせていないので、W = 8h を並べる相手が無い。同じ作り方で作った。
9. **`o3c_price_level_ext.py` に `--fit-days` と `--dedup-liq` を足した(別スクリプトにしなかった)。**
   `--fit-days` を渡すと、その日だけで `dist_node_bp` の side 別中央値を取って offset を決め、
   `--days`(測る側)に当てる。使った日・中央値・offset は `summary.json: params.fit` に全部入る。
10. **`load_liquidations(path, dedup=)` を足したが既定は `False`(該当語なし)。**
    `o3c_price_level_table.py` の既存の出力(`o3c_price_level_full_20260917*` など)は変わらない。
11. **`process_day` の `note` に `liq_rows_used` と `dedup_liq` を足した(該当語なし)。**
    `liq_rows_in_file` は従来どおり**一意化前**の行数を指す(前の走行と比べられるように)。
12. **対照行の引き方は一切変えていない(該当語なし)。**束の最初の時刻から ±5 分以上、
    種は `f"{seed}|{day}"`。一意化しても清算時刻の集合は変わらないので、対照点も変わらない。
13. **行 1 で数える統計量(束の数・束あたり件数の分布・束の長さの分布・幅 0 の束)は
    委任文面の指定である(該当語なし。監査 6)。**オーナー逐語(L-192「4 行全部進めてください」/
    行 1 の文面)は「一意化してから `build_cascades` を使う」という手順しか指定しておらず、
    どの統計量を出すかまでは書いていない。§0.1 に独立の行として分けた。

## 7. 測っていないもの(射程)

- **対照群のタイ率を測っていない。**§3 は実群(清算の束)だけである。REPORT の射程 J
  (群ごとの欠測率)と、§0.0 の A 行が書いた「群間で非対称に発生する」の**非対称の相手側**は
  この表には無い。
- **タイが直ったかどうかは測っていない。**§3 は**件数と経路**だけで、`after_shift` が
  「価格が同じ(時刻は違う)」の経路で正しい反応 bp を返すか(REPORT の射程 E)は測っていない。
  合成データでの合否(REPORT §0.0)を実データに当て直してもいない。
- **粒度は 60 秒バーと約定そのままの 2 通りだけ。**1 秒バーは作っていない。
  反応窓(地平線)は 15 分の 1 通りだけ(**仮定**)。`GAP_MS` は 60,000ms の 1 通りだけ。
- **束ね方を変えた表が無い。**MISSING §4 の「`gap_ms` の妥当性」は、§2.3 の間隔分布を
  出したところまでで、**束ね方の決定はしていない**(決定は枠組みとしてオーナーに見せる)。
- **§3.3 の「幅 0 の断片」が REPORT §0.0 の B 行と同じ集合だとは確かめていない**(§3.3 の「違い」)。
- **帯は ±1 ビン・正時・先の窓 1 時間の 1 通りだけ。**`in_band_rate` は件数の割合で、
  **数量で重み付けていない。**規模別の表も無い。
- **前半 / 後半の割り方は 1 通りだけ**(日付順の 236 / 236)。他の割り方・複数の折り返しは測っていない。
  **後半 236 日で数えたのは 1 回だけで、これを繰り返していない。**
- FULL §6・EXT §7 の射程(対照の引き方は一様の 1 通り、窓が短い日を混ぜたまま、建玉を見ていない、
  bitFlyer 側を見ていない)はそのまま残っている。
- **予測の当否・エントリーや利確への含意は測っていない**(設計 §4)。
- **帯の位置(offset)が前半 / 後半・四半期で動く「なぜ」の原因は測っていない(監査 5)。**
  §9 に候補を並べたが、どれも検証していない。

## 8. 一意化が前の文書の数字をどう変えるか(前の文書は書き換えていない)

**委任先はこの報告を書いた時点で前の文書を 1 文字も編集していない。**下は「どこがどう変わるか」の一覧。
**【リード、検収後】**この一覧に従い、リードが前の文書 4 本に注記を入れた(元の値は消していない): `FULL_2026-09-17.md` §2(21 行目)/ `EXT_2026-09-17.md` §2.1・§3・§4(76・119・153 行目)/ `MISSING_2026-09-17.md` §4・§12・§14 の 2 件目(79・185・209 行目)/ `REPORT_2026-09-14.md` §0.0 の A・B・E 行(44・45・48 行目)。

| 文書・箇所 | 書いてある値 | 一意化後 | 変わるか |
|---|---|---|---|
| FULL §2 | 清算行 **106,822** | **53,398** | **件数だけ半分。**分位・中央値・割合は変わらない(各行が等しく 2 倍なので)。**ただし多重度 4 の 13 群(52 行 → 13 行)があるので、厳密には「全部が 2 倍」ではない**(該当は 106,822 行中 52 行 = 0.05%)。FULL §2 の追記はこの 13 群も書いている |
| FULL §2 | 対照行 106,821 | **未測定** | 対照は清算行と同数引くので、一意化した表を作り直せば 53,398 前後になるはず(**推定**)。**本書は FULL の表(清算 1 件ずつ)を作り直していない**(作り直したのは束の表と帯の表だけ) |
| FULL §2 | side SELL 65,664 / BUY 41,158 | **未測定**(FULL の表を作り直していない) | **ちょうど半分ではない。**参考: 同じ 472 日を一意化して数えた帯の走行の清算件数は **SELL 32,824 / BUY 20,572**(§4.4)で、半分より 6 件・7 件少ない(多重度 4 の 13 群のぶん)。**帯の走行は窓が空の 2 時点を飛ばしているので、FULL の表の件数とは一致しない**(事実) |
| EXT §2.0 | 一意 53,398、多重度 2 が 53,385 群・4 が 13 群 | **同じ** | 一致した(§2.1) |
| EXT §2.1 | 束 **22,040**、`bundle_n_events == 1` は **0 件** | 束 22,040(日ごと)/ **22,036**(全期間)、`== 1` は **12,439** | 束の数は一意化では変わらない。**22,036 との差 4 は掛ける単位の違い**(§2.2) |
| EXT §2.2・§2.3 の数値 | `bin_pct` 中央値 50.00 ほか | **1 つも変わらない** | 一意化は「どの行が束の最初か」を変えない(§5.1 で実測) |
| EXT §3 | 清算件数(行、2 重のまま)SELL 65,660 / BUY 41,158 | **SELL 32,824 / BUY 20,572**(実測、§4.4) | 件数はほぼ半分(半分より 6・7 件少ない)。**`in_band_rate` などの割合は 1 つも変わらない**(§4.4 で実測) |
| EXT §4(監査 2 で漏れを指摘された行) | 5 走行(既定 / W=8h / W=72h / b=0.05% / b=0.25%)とも清算行 **106,822**・対照行 **106,821** | **未測定**(53,398 前後になるはず。**推定**。5 走行の表は作り直していない) | 分位・中央値は多重度 2 の行では変わらず、多重度 4 の 13 群(52 行、106,822 中 0.05%)のぶんだけ動きうる(**推定**) |
| MISSING §4 | 間隔の中央値 **0ms**、0ms が 50.3%、≤60,000ms が 78.9% | 中央値 **27,921ms**(472 日)/ **33,842.5ms**(6 日)、0ms が **0%**、≤60,000ms が **58.7%**(472 日)/ **57.6%**(6 日) | **大きく変わる。**§2.3 |
| MISSING §14 の 2 件目 | 「同時刻の複数レコードの数え方が未定義」→【解決 2026-09-17】数え方 = 全列一致の行を 1 件に、`build_cascades` は「使う前に一意化が要る(未実装)」 | **実装した**(`dedup_exact_rows` / `load_binance_cm_liquidations(dedup=True)`)。一意化後は同一 ms の別レコードが 0 件 | 「未実装」が「実装済み」に変わる |
| MISSING §12 | 前提条件 3 つは「未実行」、専用の集計処理が「無い」 | **3 つとも数えた**(§3)。集計は `scripts/o3c_rows4.py` | 「未実行」が「実行済み」に変わる |
| REPORT §0.0 の A 行 | 「09-13 の観測ではタイは実カスケードで 8 割(実データで数え直す)」 | **数え直した**: 60 秒バー・既定の起点で **0.7855** | 数え直しが済んだ。**同じ数ではない**(09-13 は一意化前) |
| REPORT §0.0 の B・E 行 | 幅 0 の断片 / 価格の刻みの経路は「測っていない」 | 幅 0 の束 **12,436(56.4%)**、「価格が同じ(時刻は違う)」の経路が 0 件ではない(§3.2) | 件数は数えた。**E 行の「価格の刻み」と同じ機構かは確かめていない(未確認)。直ったかも測っていない**(§7) |
| `liq_response.py` 365〜367 行の注記 | 「タイは実カスケードで 8 割、窓の長いプラセボで 0%」 | 実群 0.7855(60 秒バー・既定の起点)。**プラセボ側は測っていない** | 実群だけ数え直した |

## 9. なぜ帯の位置が前半と後半で違うか(候補。判定なし)(監査 5)

逐語には無い節(該当語なし)。§4.2 で「SELL の offset が前半 236 日だけで決めると相対で約 20%
動くのに BUY はほぼ動かない」ことを実測したが、その「なぜ」を §6・§7 のどちらにも書いていなかった
(監査役の指摘 5)。リードが `o3c_price_level_full_20260917{,_w8}/table.csv` の清算行(`kind == liq`)
から、前半/後半(境界 2024-02-18 | 2024-02-19)と四半期ごとの `dist_node_bp` の side 別中央値を
数えた(事実。**一意化前の FULL の表から数えている**。各行が等しく 2 倍なので中央値は一意化で
変わらない。多重度 4 の 52 行だけ例外で、106,822 中 0.05%)。

| | SELL 前半 | SELL 後半 | BUY 前半 | BUY 後半 |
|---|---|---|---|---|
| W=24h | **+45.09** | **+69.21** | **−71.35** | **−69.03** |
| W=8h | **+47.26** | **+64.33** | **−59.57** | **−61.82** |

四半期別(`dist_node_bp` 中央値、BUY / SELL):

| 四半期 | W=24h BUY / SELL | W=8h BUY / SELL |
|---|---|---|
| 2023Q2 | −42.9 / 55.3 | −58.2 / 46.1 |
| 2023Q3 | −56.6 / 46.5 | −50.2 / 46.5 |
| 2023Q4 | −76.9 / 37.6 | −62.3 / 44.2 |
| 2024Q1 | −81.2 / 47.3 | −68.6 / 50.9 |
| 2024Q2 | −58.1 / 73.4 | −55.5 / 66.7 |
| 2024Q3 | −69.8 / 79.4 | −61.6 / 71.0 |
| 2024Q4 | −46.3 / 75.9 | −50.2 / 71.1 |

**観測(事実)**: SELL の中央値は 2024Q2 以降に 37〜47 → 73〜79 へ上がる。BUY は 2023Q4〜2024Q1 に
−77〜−81 まで下がってから戻る(単調ではない)。前半/後半の 2 分割で見ると SELL だけが動いて
見えるが、四半期で見ると両側とも動いている。

**候補(推定。どれも検証していない。判定はしない)**:
1. `dist_node_bp` は bp 単位なので、その時点のプロファイルの価格範囲(値幅)が広い時期ほど
   絶対値が大きくなりうる。
2. side の差は、SELL 清算(ロングの清算 = 下落局面)と BUY 清算(ショートの清算 = 上昇局面)で
   局面ごとの値幅が異なる可能性がある。
3. 帯の位置を単一の定数(offset)で置くという枠組み自体が時期に依存している可能性がある(観測)。

**どの候補が正しいかは判定しない。原因は測っていない**(§7 に注記)。
