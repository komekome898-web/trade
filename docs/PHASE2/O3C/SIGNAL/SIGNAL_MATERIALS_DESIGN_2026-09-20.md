# 清算を起点とした値動きの予測可能性 — 材料の選定(続く / 止まるを分ける量を、両側同じ手順で選ぶ)の設計(2026-09-20)

**探索段**(research-protocol §0.0)。選定は**前半 228 日だけ**で行う(後半は Jev の較正と方策の測定に取っておく)。判定語(陽性 / 陰性 / 有意 / 差あり / 検出されず)は使わない。
経緯: 続く / 止まるの単位の材料 14 個は連鎖 30 本の目視の印象から選び手順が無かった(L-281「**材料の選定が雑で思いつきの域を出ていない**」)。止まる側だけ手順を置くのは片手落ち(L-284)。Jev に渡す状態に止まる側の情報が少ない(L-280)。この設計はその置き換え。

## 用語

| 記号 | 意味 |
|---|---|
| 1 件目 / 2 件目以降 | ts の前 60 秒に同じ側の清算が無い / 有る(ts で分かる) |
| 事後の位置 | 探索段 5 の束(gap 60)の位置: 1 件目は 単発(止まる)/ 多件の最初(続く)、2 件目以降は 途中(続く)/ 多件の最後(止まる)。束の外は除く。**選定にだけ使う**(判断には使わない) |
| 2 組 | 組 A = 1 件目: 単発 対 多件の最初。組 B = 2 件目以降: 途中 対 多件の最後 |
| 分かれ方 | 止まる側から 1 件・続く側から 1 件を無作為に取ったとき、止まる側の値の方が大きい確率(順位だけで決まる。0.5 = 分かれない。0 か 1 に近いほど分かれる)。**同値は 0.5 と数える。**欠測は両側から除き、欠測の割合を併記 |
| 候補 | 機構 → 量 → なぜ最後のプリントの前に動くはずか、の 3 行が書ける量。**すべて ts 以前の約定・清算・5 分値だけで計算する(p₀ は使わない)。値段の基準点は常に「その時刻の直前の約定」(`at_or_before(t − 1)`)** |
| 連鎖(ts 基準) | 同じ側のプリントを、直前のプリントから 60 秒以内なら繋いだもの。**ts だけで決まる**(探索段 5 の束と同じ規則だが、束は終わりを未来で決めるのに対し、ここでは「ここまで」だけを使う)。連鎖の開始 = その連鎖の最初のプリントの ts。1 件目のプリントでは連鎖 = 自分だけ = 連鎖の内側の量(F3・F4・A3・A5)は**欠測**(組 A の候補にしない) |

## 0.1 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 止まる側の材料を足す | 「**止まる側の材料を足すべきであるのは明白**」(L-281) |
| 思いつきの列挙をやめ、手順で選ぶ | 「**材料の選定が雑で思いつきの域を出ていない**」(L-281) |
| 両側を同じ手順で選び直す(既存 14 個も同じ枠で書き直し、同じ基準でふるう) | 「**止まる側の材料選びと続く側の材料選びに差がうまれていない？**」(L-284) |
| 1 件目と 2 件目以降の 2 組で分ける | 「**なぜ「最初」はない？**」(L-285) |
| 分布(分位)で見る、平均は出さない | 「**平均で計算したらわからなくなって当然**」(L-260) |
| 境目は前半の表を全部見てから置く(後半に触れない・理由を書く・候補の数を数える) | 「**境目の決め方は結果から決められる？**」(L-289) |
| 未来を見ていないことが条件(窓の長さは揃えなくてよい) | 「**未来を見ていなければ問題ないと思うがどうか？**」(L-283) |
| Jev への問いは 1 問のまま | 「**清算を検知→jevでこの清算は続くか判断**」(L-262)、「**未来を見ていなければ問題ないと思うがどうか？**」(L-283) |
| 選んだ材料で 5,000 件の較正を取り直す | **リードの選択**(§3 の 5。材料が変われば較正も変わるため。原文に該当語なし) |
| 機構の枠 = 燃料 / 吸収 の 2 つ、分かれ方の数の定義、候補の一覧、実物読みの件数(各組 30 + 30)と抽出の規則(同じ日から 2 件以上取らない) | **リードの選択**(§1・§2・§3・§4 に理由) |
| 全体の進め方 | 「**進めてください**」(L-290) |

## 1. 機構の枠(リードの分解。測った根拠ではない、L-282)

清算 = 強制の成行。次の同じ側の清算が 60 秒以内に起きる条件は (a) **燃料**: 次の清算水準が届く範囲に残っている、(b) **吸収されない**: 押す力(強制の成行 + 同じ向きの能動的な流れ)が、受ける側の指値と反対向きの能動的な流れに吸収されない。反対側の動き出しは (b) の中身。文脈(その時点までに分かる長い窓の量)は機構ではないが、L-283 により候補に含める(未来を見なければよい)。

## 2. 候補(機構 → 量 → なぜ最後のプリントの前に動くはずか。既存の材料は番号を残す)

| # | 機構 | 量(ts 以前だけ) | なぜ止まる前に動くはずか |
|---|---|---|---|
| 8 | 燃料 | この先 5 / 10 / 20 bp 以内の建玉の量(既存。被覆外は欠測) | 先に清算される建玉が無ければ、値段が届いても次は来ない |
| 5' | 燃料 | 次の清算水準までの距離(既存 5 を「先」だけに直す。通り過ぎた水準は除く) | 遠いほど 60 秒で届かない |
| F3 | 燃料 | 連鎖(ts 基準、用語表)でここまでに清算された想定元本の累計(自分を含む) | 燃料は使うほど減る |
| F4 | 燃料 | 連鎖の開始の直前に確定していた 5 分値の建玉と、ts の直前に確定していた 5 分値の建玉の差(metrics 欠測 99 日は欠測。被覆外は欠測) | 建玉が減った分だけ残りの燃料が減る |
| 3 / 12 | 燃料・吸収 | 想定元本、直前との比、連鎖の最大との比(既存) | 大きな 1 件は燃料を一度に使い、受け手の指値も一度に消す |
| 9 / 13 | 吸収 | 直前 5 秒の成行の偏り、5 秒 − 30 秒(既存) | 反対向きの成行が勝ち始めると押されない |
| A3 | 吸収 | 連鎖の開始の直前の約定(最初のプリントの `at_or_before(ts₁ − 1)`)から、今の ts の直前の約定(`at_or_before(ts − 1)`)までの値動き(bp、清算の向きが正)÷ F3 = 想定元本 1 単位あたりの値動き。**p₀ は使わない。1 件目は欠測** | 受け手が増えると同じ量で動かなくなる |
| A4 | 吸収 | 直前 5 秒の値動き ÷ 直前 5 秒の成行の量(同じ向き) | 同上、短い窓 |
| A5 | 吸収 | 直前の同じ側のプリントの ts の直前の約定から今の ts の直前の約定までの間で、清算の向きと逆への最大の戻り(bp。基準は直前のプリントの ts 直前の約定。**p₀ は使わない。1 件目は欠測**) | 戻りが出ていれば受け手が出ている |
| A6 | 吸収 | 清算の向きの極値(直近 60 秒)を最後に更新してからの秒数 | 更新が止まれば押す力が尽きている |
| A9 | 吸収 | 直前 60 秒の反対側の清算の件数と想定元本 | 反対側の清算は値段が逆へ動いた証拠 |
| 1 / 2 / 14 / 15 | 吸収・文脈 | 直前 60 秒の同じ側の件数と経過秒、間隔の比、約定件数、10 秒 ÷ 60 秒の変位(既存) | 連鎖の勢いと密度。止まる前に間隔が伸び、変位が鈍る |
| 11 | 吸収 | 想定元本 ÷ 直前 60 秒の値幅(既存) | 値幅に対して大きい 1 件は吸収されにくい |
| 6 / 10 | 文脈 | 時刻帯、建玉の傾き・資金調達率(既存) | 参加者の多さ、混み具合 |
| C3 | 文脈 | その日の(ts までの)清算の向きの極値からの距離(bp) | 極値に近いほど既に伸びている |
| C4 | 文脈 | 直前 5 分の実現変動 ÷ 直前 1 時間の実現変動 | 荒れの局面か |

外すもの: 4(1 件目で p₀ の関数、反証者 6 致命 5)。前段の材料は 1〜6・8〜15 の 14 個(7 は最初から欠番 = 板の厚みは測らない)。**14 − 4 = 既存 13、新規 9、候補 22 本。**組 A(1 件目)では連鎖の内側の量 F3・F4・A3・A5 が欠測なので、組 A の候補は 18 本、組 B は 22 本。**候補の数は多重性に数える。**

## 3. 手順

1. 委任先が 22 本を全 52,000 プリントで計算する(後半の値は後で Jev に渡すため。**表は前半だけ**)。
2. 前半で、組 A・組 B それぞれ、候補ごとに 2 群の分位(p10 / p25 / p50 / p75 / p90)・件数・欠測の割合・分かれ方の数を出し、分かれ方の |0.5 からの距離| で並べ替える(表 = 組 A 18 + 組 B 22 = 40 行)。
3. **リードが並べ替えた表を見て境目を置き、理由を書く**(後半に触れない)。切れ目が無ければ「分かれる候補は無い」がその組の結果。
4. 実物読み(**前半だけ**から抜く): 組 A から 単発 30 + 多件の最初 30、組 B から 途中 30 + 多件の最後 30 を乱数(種 20260920。**同じ日から 2 件以上取らない** = 前回の 30 本と同じ規則、リードの選択)で抜き、各プリントの候補の値と直前 60 秒の清算の列・値段の列を並べる。リードが読み、候補に無いものが見えたら §2 に戻して足す(足した理由を機構の行で書く)。**足した候補は前半で同じ表に加え、手順 3 の境目は表全体で置き直す**(置き直した場合は前の境目と理由も残す)。
5. 残った材料で Jev の状態を組み直し、後半 5,000 件(同じ選定)の較正を取り直す(別の委任)。「わからない」の帯は 0.01 刻みの較正から置く(L-277・L-278。参照は L-279 の議論を受けて設計に書く)。

## 4. 走らせる前に決めること(リードの決定)

- 分かれ方の数 = 順位の重なりの確率(裾に引かれないため。平均の差は使わない)。
- 実物読みは各組 30 + 30(前回の 30 本と同じ規模)。
- 表の行数 = 組 A 18 + 組 B 22 = 40(+ 実物読み 120 件 × 1 行 = 120 行の一覧)。多重性: **行** 2,583 + 160、**反証者のセル** 1,167(この設計では反証者はまだ無いので据え置き。反証者レビューの後に同じ数え方で足す)、候補 22 本を「見た候補の数」として別に数える。
- テスト: 候補が ts 以後を使わない(ts 以後の約定・清算・5 分値を消した入力で同じ値、**p₀ に当たる約定の価格を実際に別の値に書き換えた入力でも同じ値** = 反証者 6 致命 5 の再発防止。リードが試験のコードを読んで p₀ が可変になっていることを確かめ、確かめたことを結果に書く)/ 組の分け方 / 分かれ方の数(合成の 2 群で手計算と一致)/ 前半だけで表を作る / 判定語なし / `paper_logs/` を開かない。
- 反証者レビューは境目を置いた後、較正の前に。
- `jev_design.py` の判定(設計の段): この文書は材料の選定で「測る量」は分かれ方の数の 1 つなので rank-observables は掛けない。**新候補を含めた rank-covariates は、方策の模擬の設計を材料の確定後に書き直すときに再実行する**(そこで意図と量の対を並べる)。

## 5. 結果(リード、2026-09-20。手順 3・4 の記録。後半には触れていない)

### 5.1 引き取りの検査(委任先 `MATERIALS_DELEGATE_REPORT_2026-09-20.md`)

- `md5sum -c backtest_data/o3c_signal_materials_20260920/MD5SUMS`(リポジトリ直下で): 7 件すべて OK。
- `PYTHONPATH=src python -m pytest tests/test_o3c_signal_materials.py`: `10 passed, 1 warning in 3.27s`(警告は `DtypeWarning`、混在型の列の読み込みで値には関係しない)。委任先の全スイート末尾行 `2701 passed, 5 skipped, 1 warning in 457.22s`。
- **p₀ が可変になっていることをリードが試験のコードで確かめた**(§4 の約束): `tests/test_o3c_signal_materials.py` の `_synthetic_trades(..., p0_differs=True)` が `times >= ts` の最初の約定の価格に `+12_345.0` を足し、`assert prices_c[at_ts] != prices_a[at_ts]` で実際に変わったことを確かめてから、5' と新規 9 本が元の値と一致することを見ている。
- 40 行の独立再計算(リード、`scipy.stats.rankdata` の平均順位で U 統計量 → 順位確率、p50 も比較): 組 A 18 行・組 B 22 行とも不一致 0(許容 1e-6)。
- A9 の独立再計算: 探索段 5 の `rows_prints.csv.gz`(`kind == print`、52,000 件)で「直前 60 秒に反対側の清算がある」割合 = 0.0095、委任先の `cand_A9_count > 0` の割合 = 0.0095(一致)。A9 は 99% が 0 なので順位確率がほぼ 0.5 に固定される(表の 0.5000 は「分かれない」ではなく「ほとんど同値」)。
- `scripts/jev_report_intake.py check … --kind implementation`: 印 6 件(必須項目「仕様に無い判断」の見出し語の不一致 1 件 = 報告は「(5) 設計に無い判断」の見出しで全 15 件を書いている / 出所なしの数値 5 件 = いずれも報告内で実測した数)。差し戻し無し。
- 委任先の設計に無い判断で、リードが後で使うもの: (5)-3 **5' の基準は `p_pre`**(既存 5 は `p_liq`)/ (5)-10 A9 は想定元本を候補にし件数は列に残した / (5)-11 **12 は組 A で構造的に欠測 100%**(1 件目では分母が無い)/ (5)-14 6 は 4 帯を 0〜3 に数値化。

### 5.2 境目(手順 3。前半の表だけから。根拠の数字は `docs/DATA/probes/20260920_o3c_materials_cut.{py,log}`)

**規則(両組同じ、L-284)**: |分かれ方 − 0.5| の降順に並べ、隣り合う落差のうち **最上位 1 本の直下の落差を除いて最大の落差** の所に置く。理由: 最上位の 14(直前 60 秒の約定件数)は両組で孤立していて(直下の落差 0.0471 / 0.0304 が最大)、そこで切ると材料 1 本になり、それは前段で測った「実質 1 本の規則」と同じ形に戻る。順位確率の SE(交換可能のとき)は組 A 0.0062 / 組 B 0.0056 で、境目の落差はその 3 倍前後。**この規則はリードの選択**で、オーナーの逐語には無い(L-289「境目の決め方は結果から決められる？」に対する具体化)。

| 組 | 境目 | 落差 | 境目の上(残す) | 境目のすぐ下 |
|---|---|---|---|---|
| A(単発 対 多件の最初) | C4(0.0738)と 15(0.0523)の間 | 0.0215 | **14, 8, A6, C3, C4**(5 本) | 15, 9, 11(0.043〜0.052) |
| B(途中 対 多件の最後) | A6(0.0879)と 2(0.0697)の間 | 0.0182 | **14, 1, A6**(3 本) | 2, C3, F3, 8, 15, C4, 12, 11(0.048〜0.070 に 8 本が密集。次の落差 0.0135 まで下げるとこの 8 本が入る) |

向き(分かれ方 = 止まる側の値の方が大きい確率): 組 A では 14 が 0.35(単発は約定が少ない)、8 が 0.60(単発の先に建玉が多い)、A6 が 0.59(単発は極値の更新から時間が経っている)、C3 が 0.58(単発は日の極値から遠い)、C4 が 0.43(単発は直前 5 分の変動が相対的に小さい)。組 B では 14 が 0.63(最後は約定が多い)、1 が 0.60(最後は直前 60 秒の同じ側が多い)、A6 が 0.41(最後は極値の更新が新しい)。**組 A と組 B で同じ候補の向きが逆になっているものがある**(14: 0.35 と 0.63、A6: 0.59 と 0.41)。1 件目と 2 件目以降で「止まる側」の意味が違う(単発 = 連鎖が始まらない、最後 = 連鎖が終わる)ので、Jev の状態にはこの向きの違いを説明で渡さず、値と位置(1 件目か否か)だけを渡す。

構造的に 0.5 の行(境目の判断に使わない): 組 A の 1(1 件目は定義上 0)、A9(両組、99% が 0)、12(組 A、欠測 100%)。5p は欠測 70〜92% なので、順位確率は「先の水準が被覆内にある場合だけ」の数。

**この境目は暫定。** 手順 4 で候補を 2 本足したので(§5.4)、足した候補を含めた表全体で置き直す(§3 の 4 の規定)。置き直したら、この境目と理由はここに残す。

### 5.3 実物読み(手順 4。120 件、`docs/DATA/probes/20260920_o3c_materials_read.md`。リードが全件読んだ)

読んだ形: 候補 22 本の値・直前 60 秒の清算の列・直前 60 秒の 1 秒刻みの値段(bp、清算の向きが正、基準 = ts 直前の約定)。候補に無いもので見えたのは次の 2 つ。

1. **ts 直前までに出ていた戻りの大きさ。** 60 秒の値段の列が p_pre を超えて清算の向きに進んでいた(列の最大が正)プリントが、多件の最後 30 件のうち 10 件(#94 +1.4、#95 +1.2、#96 +1.2、#102 +3.6、#105 +6.1、#106 +21.3、#108 +2.5、#111 +1.8、#115 +5.7、#120 +5.5 bp)、途中 30 件のうち 5 件(#66、#73、#79、#86、#88)、1 件目 60 件のうち 13 件(単発 #11、#16、#17、#19、#20、#21、#30、多件の最初 #33、#49、#54、#56、#57、#60)。これは「受け手が出て値段が戻った後に清算が刷られた」形で、A5(直前のプリントからの戻り)と同じ機構だが A5 は 1 件目で欠測、A6 は「極値の更新からの秒数」で大きさを持たない。件数の比は読んだ 120 件の中の数で、分かれ方の測定ではない(表で測る)。
2. **直前数秒の大きな 1 件。** ts の 1〜3 秒前に、自分より桁違いに大きい同じ側の清算がある(#82 3,024 万、#94 4,949 万、#106 934 万、#111 2,045 万、#115 1,253 万、#116 705 万、#99 1,944 万)。3(直前との比)と 12(連鎖の最大との比)は「自分 ÷ 相手」の比で、直前に燃料がどれだけ一度に使われたかの量そのものは F3(連鎖の累計)にしか無く、F3 は長い連鎖では開始からの総量に埋もれる。

見えたが足さないもの: 最後の 1 秒の跳ね(#56 −19.5 → +8.0、#106 −2.5 → +21.3、#89 −9.1 → −16.9)は 1 の「戻り」の中に含まれる(列の最大が跳ねの先端になる)。

### 5.4 足す候補(手順 4。§2 の表に足す行。理由は機構の行に書く)

| # | 機構 | 量(ts 以前だけ) | なぜ止まる前に動くはずか |
|---|---|---|---|
| R1 | 吸収 | 直前 60 秒 `[ts − 60,000 ms, ts − 1 ms]` の約定の価格の、清算の向きの最大 `max sign × (p − p_pre) / p_pre × 1e4`(bp)。正なら「p_pre より先まで行って戻っている」、0 以下なら戻り無し(p_pre が 60 秒の極値)。**全位置で定義**(1 件目でも) | A5 と同じ(戻りが出ていれば受け手が出ている)。A5 が欠測の 1 件目にも効く形。A6 は秒数、R1 は大きさ |
| F5 | 燃料 | 直前 10 秒 `[ts − 10,000 ms, ts − 1 ms]` の同じ側の清算の想定元本の合計(自分を含まない)。窓 10 秒は候補 15 の短い窓と同じ(新しい数を増やさない)。**1 件目は定義上 0 なので組 A では測らない**(F3 と同じ扱い) | 直前に燃料を一度に大きく使った後は残りが減る。F3(連鎖の累計)との違いは「直前の使い方の速さ」 |

候補の数: 22 → **24**(組 A 19 本、組 B 24 本、表 43 行)。多重性の「見た候補の数」も 24 に更新する。再計算は委任(`docs/DATA/delegations/20260920_o3c_signal_materials2_prompt.md`)。**再計算の表が出るまで後半には触れない。**

### 5.5 境目の置き直し(候補 24 本の表全体で。§3 の 4 の規定。数字は `docs/DATA/probes/20260920_o3c_materials_cut_v2.log`)

規則は §5.2 と同じ(最上位 1 本の直下を除いた最大の落差)。**境目は動かなかった。**

| 組 | 足した候補の位置 | 最大の落差(最上位の直下を除く) | 境目の上(残す) |
|---|---|---|---|
| A(19 本) | R1 = 0.466(\|d\| 0.034、11 の下) | C4(0.074)と 15(0.052)の間 0.0215(変わらず) | **14, 8, A6, C3, C4** |
| B(24 本) | F5 = 0.560(\|d\| 0.060、8 の下)/ R1 = 0.514(\|d\| 0.014) | A6(0.088)と 2(0.070)の間 0.0182(変わらず) | **14, 1, A6** |

組 B では 0.048〜0.070 に 9 本(2, C3, F3, 8, F5, 15, C4, 12, 11)が密集し、燃料側(8, F3, F5)はすべてこの層にある。次の落差(11 と A3 の間、0.0135)まで下げるとこの 9 本が入る。規則どおり置くので入れない。§5.2 の暫定の境目と理由はそのまま残す。

引き取り検査の状態: この置き直しは委任先の表(`screen_A.csv` 19 行 / `screen_B.csv` 24 行、ディスク上)から取った。委任先の報告書 `MATERIALS2_DELEGATE_REPORT_2026-09-20.md` はまだ届いておらず、MD5・既存 22 列の完全一致・43 行の独立再計算・R1/F5 の試験は**未検査**。検査で数が変わればここを直す。

## 6. Jev に渡す情報の組み直し(L-298〜L-300。2026-09-20。**この体制の手引き `docs/JEV.md` に照らして書き直した版**。オーナーの返答待ち)

**根拠**: `docs/JEV.md` §2(できないこと)・§3(第三者の落とし穴)・§4(この体制の規則)・§7(点検表)・§10(判断の中で使う型)、逐語集 `docs/DISCUSSIONS/2026-09-19_jev_review_inventories/D_study_notes.md` §C・§D・§I(ベンダーの既知の失敗 9 種)。前の版(d8c3775)は公式文書だけを読んで書き、**手引きの「数値の近さが分からない」「文字どおりに読む」を見落としていた。**

### 6.1 手引きに照らした、いまの渡し方の問題

| 手引き | いまの渡し方(`jev_state_for_print`) | 何が起きるか |
|---|---|---|
| §2「数えられない・数値の近さが分からない」、失敗 #2「Math and Numbers: keep the arithmetic in code」、「16 進の色より英語の色名の方が当たる」 | 材料 14 本を生の数(`notional_over_60s_range: 37317.17` など)で渡す | Jev は数の大小・近さを判断できない。数の列は「形」として読まれるだけ |
| 失敗 #5「判断に関係ない内容が増えると精度が落ちる」、§4-2「文書丸ごとではなく code が切り出した断片」 | 1 秒刻みの値段 61 点をそのまま渡す(入力の半分近く) | 気を散らす材料。どこが答えを動かしたか分からなくなる |
| §1「数秒で判断できる直感的な判定」、§3「問いは、目に見える具体的な状況を書く」、§4-3「1 判断 1 問、肯定形、criteria は具体例つき」 | 問い 1 つ(「60 秒以内に同じ側の清算が来るか」)、criteria は抽象語(momentum / exhausted) | 燃料・押す力・受け手の判断が 1 つの答えに隠れ、0.4〜0.6 に圧縮される |
| §4-4「数える・比べる・日付は code。Jev には意味の判断だけ」、§10「code が判断の材料を作り、Jev が小さな判定を返し、code がそれで選ぶ」 | 数値の比較(大きいか、近いか、増えたか)を Jev に任せている | 判断の材料を作る仕事を code がしていない |
| §4-7「数値の結果(検出率など)をリポジトリに残さない(MCA §2.3(f))」 | **Jev の的中率・較正の表を `backtest_data/…/jev/*.csv`、`docs/DATA/probes/*jev*.log`、結果記録に書いてコミットしている** | **体制の規則に反している(§6.4)** |

### 6.2 材料の基準と 24 本の扱い(オーナー承認 L-309「**その基準と案で進めてください**」。基準の言葉は L-308「**jevが判断しやすいような言葉かどうかが、材料の判断基準になるのでは？**」)

**基準(3 つ)**: (1) トレーダーが板を見て言う英語の事実として書けるか / (2) 問いの criteria の機構の言葉(燃料が届く範囲に残っているか・押す力が続いているか・吸収されているか)につながるか / (3) 値が動くか。前半の 2 組の表は (3) の確認に使う。境目は置かない。

| 扱い | 材料 | 理由 |
|---|---|---|
| 残す(そのまま事実に) | 1、2、3、6、9・13、14、F3、A5、A6、A9、C3、C4、R1、F5、10 | 3 条件を満たす |
| 1 つの文にまとめる | 8 と 5' | 同じ「燃料が届く範囲にあるか」の事実 |
| 比を捨てて元の事実にする | 11 → 60 秒の値幅 / 15 → 10 秒と 60 秒の変位 / A3 → 連鎖の開始からの値動き / A4 → 5 秒の値動き | 比は規則のための数で、言葉にならない |
| 落とす | F4、12 | F4 は定義される行の 92% が 0。12 は 3 の「直前との比」と F5 で言い尽くされる |

**渡し方 = トレーダーが言う短い英文の列**(項目名と値の対ではない)。数は「among the largest fifth of prints」のような前半の五分位の言葉と、秒・bp・% の整数だけ。生の実数は書かない。1 件目には連鎖の行を書かない(「not applicable」も書かない)。欠測は "unknown"。文の型(code の定数。委任先が実装、リードが 20 件読んで確かめる):

| 文 | 元の材料 | 型(SELL の清算の例。BUY は low → high、sells → buys、below → above) |
|---|---|---|
| 1 | 3、6 | `SELL liquidation, among the {largest fifth / second-largest fifth / middle fifth / second-smallest fifth / smallest fifth} of prints, at {12-18} UTC.` |
| 2 | 1、F3 | `It is the {n}th same-side liquidation of a cascade that began {X} seconds ago; the amount liquidated so far is among the {…} fifth of cascades.` 1 件目: `No same-side liquidation in the last 60 seconds.` |
| 3 | 1、2、3 | `The last two same-side liquidations were {a} and {b} seconds ago; the gaps are getting {shorter / longer / about the same}. This print is {larger / smaller / about the same} than the previous one.`(連鎖の内側だけ。2 件目は「the previous same-side liquidation was {a} seconds ago」) |
| 4 | F5 | `Same-side liquidations in the last 10 seconds: among the {…} fifth (of prints that had any).` / `none in the last 10 seconds.` |
| 5 | A9 | `No opposite-side liquidation in the last 60 seconds.` / `{k} opposite-side liquidation(s) in the last 60 seconds, the last {t} seconds ago.` |
| 6 | A6、R1 | `Price made a new 60-second low {t} seconds ago and has {not pulled back / pulled back {p} bp since}.` |
| 7 | 15、11 | `Over the last 60 seconds price fell {m60} bp (range {r} bp); over the last 10 seconds, {m10} bp.` |
| 8 | A5 | `Since the previous same-side liquidation, the largest pullback was {p} bp.`(連鎖の内側だけ) |
| 9 | A3 | `Since the cascade began, price has fallen {m} bp.`(連鎖の内側だけ) |
| 10 | 9、13、A4 | `Taker flow in the last 5 seconds: {pct}% sells; over 30 seconds: {pct30}% ({more one-sided now / about the same / less one-sided now}). Price moved {m5} bp in the last 5 seconds.` |
| 11 | 8、5' | `{No open interest / Open interest among the {…} fifth} within 20 bp below the current price; within 5 bp: {none / some} (coverage: yes). Nearest liquidation level below: {d} bp away / none within the mapped range.` 被覆外: `Open interest below the current price: coverage unknown.` |
| 12 | 10 | `Open interest over the last hour: {falling fast / falling / flat / rising / rising fast}. Funding: {positive / negative / zero}.` |
| 13 | C3、C4、14 | `Price is {at the day's low / very close to / close to / some distance from / far from} the day's low. Volatility now vs the last hour: {much calmer / calmer / similar / busier / much busier}. Trading activity in the last 60 seconds: among the {quietest / … / busiest} fifth.` |

帯の境界は前半だけの五分位(定数 1 か所。現在の値は `docs/DATA/probes/20260920_o3c_jev_state_proto.log`)。**「largest fifth」などの言葉は前半の分布に対する相対の位置で、後半でも同じ境界を使う。**

### 6.3 問い(1 つ。L-303)

| 鍵 | 問い(英文で送る) | criteria |
|---|---|---|
| `next_print_within_60s` | Will another same-side liquidation print occur within the next 60 seconds? | yes: there is still open interest or a liquidation level within reach of the recent move, and the facts about the new extreme, the pullback and the taker flow show the push still going / no: nothing within reach, or the pullback, the taker flow and opposite-side liquidations show the other side absorbing the forced flow |

code 側: 確率を後半で較正して 3 択に使う。**比較の相手**: code の規則(直前 60 秒の清算の有無など)が前段で 63〜66% 当てていた。Jev がこれを上回らなければ判断に置く意味は無い。閾値は code の定数、後半で測ってから置く。

### 6.5 下見(前半 200 件)で渡し方を比べてから、後半に一度だけ(L-309 で承認)

- 対象: 前半 200 件(前回の下見と同じ層化抽出、種 20260920)。後半には触れない。
- 組: V1 = §6.2 の英文の列 + §6.3 の criteria / V2 = 同じ英文の列、criteria 無し(手引き §3「criteria あり / なしを自分のデータで試す」)/ V3 = 生の数の state(前段の形から 61 点の列だけ落としたもの。比較の基準)。呼び出し 200 × 3 = 600。
- 出す表(**数値は `data/jev/` にだけ**。オーナーには会話で見せる): 答えの 0.05 刻みの分布 / 前半のラベルとの一致(前半は選ぶ側なので使ってよい)/ code の規則(直前 60 秒の清算の有無)との一致 / 1 回あたりの入力トークン。**state の実物 10 件**(単発 3・多件の最初 3・途中 2・最後 2)を並べる(state は入力で、性能の数値ではないのでリポジトリに置く)。
- 決め方: V1 と V2 のどちらを使うかは前半の一致で決め、理由を書く。V3 より良くなければ、渡し方をここで直す(後半に進まない)。
- そのあと: 選んだ形で後半 5,000 件(前回と同じ抽出)に一度だけ送り、0.01 刻みの較正と規則との比較。数値は `data/jev/`。

#### 6.5.1 実装の引き取りでリードが直した点(2026-09-20、下見の再実行の前)

委任先(`scripts/o3c_jev_state.py`、試験 25 件)の実装をリードが読み、3 件の実物を出力させて確かめた。直した点(**直す前の呼び出し 146 回は `data/jev/state_preview/answers_partial_oldcriteria_*.jsonl` に退避し、集計に使わない**):
1. **V1 の問いが前段の古い criteria(momentum / exhausted)のままだった** → 設計 §6.3 の問いと criteria(オーナー承認 L-309)に置き換え、答えは組ごとの問いの鍵で読むように直した。V3(生の数)は前段の問いのまま(比較の基準)。
2. **符号つきの bp が向きを誤読させる**(SELL の清算で「over the last 10 seconds, +4.6 bp」= 下に 4.6 bp なのに上に読める)→ 型 7・10 は「it fell 4.6 bp」「Price fell 14.7 bp」のように向きを語で書く。試験 2 件も同じ語に直した。
3. 「the gaps are getting about the same」→「the gaps are about the same」。

委任先の設計に無い判断で、リードが定数として認めたもの(設計の型に幅の指定が無かった): 間隔・規模の「同程度」の幅 = 比 1 ± 0.15 / 成行の偏りの変化「変わらない」の幅 = 0.10 / 戻り 0 とみなす閾値 = 0.05 bp / 日の極値「at」= 0.05 bp 以内、それ以外の 4 段は正の値の四分位 / 建玉の傾きの 5 段 = 前半の五分位 / A3 の文(型 9)は比の復元ではなく価格の経路から直接計算(CSV の丸めで 0 に潰れるため)/ V3 には `assert_clean` を掛けない(材料の変数名が誤検出されるため。値は数値だけで秘密は入り得ない)。

### 6.4 体制の規則違反(リードの発見、2026-09-20。オーナーに報告)

`docs/JEV.md` §4-7「数値の結果(検出率など)もリポジトリに残さない(契約 MCA §2.3(f): publish benchmarks or performance information about the Services)」に対し、リードは Jev の的中率・較正の表と数値を次にコミットしている: `backtest_data/o3c_signal_continue_20260920/jev/`(q3〜q7_jev.csv、tables_jev.md、summary_jev.json)、`docs/DATA/probes/20260920_o3c_signal_continue_jev_*.log`、`docs/DATA/probes/20260920_jev_continue_call_probe.log`、`SIGNAL_CONTINUE_RESULT_2026-09-20.md` §1・§3、`CONTINUE_JEV_RUN_NOTE_2026-09-20.md`、`CONTINUE_DELEGATE_REPORT_2026-09-20.md`、`REFUTER_REVIEW6_2026-09-20.md`。手引きの規則はリードが書いたもので、リード自身が破った。リポジトリが公開かどうかは未確認(この環境から GitHub の設定を読んでいない)。**オーナーの決定(L-310「一旦そのままでいいです」)**: 既にコミットした分はそのまま(履歴の書き換えもしない)。以後は (a) だけ。以下は決定前の選択肢: (a) 以後は Jev の性能の数値を `data/jev/`(gitignore 済)にだけ書き、リポジトリには手順と件数だけ残す / (b) 既にコミットした数値を消す(履歴には残る。履歴の書き換えはオーナーの指示があるときだけ)。

## 7. V4 と後半の一度きりの測定(オーナー指示 L-323「**全てやってください。**」。対象 = L-319〜L-322 のリードの案すべて)

### 7.1 決定表(L-319 で作り直した版。データの向き = 前半全件(1 件目 9,069 / 連鎖の中 12,769)、Jev の向き = 下見 200 件)

規則: 前半で「続いた側の値が大きい確率」が 0.5 から 0.03 以上離れていれば「データが動く」。動くもののうち、Jev が同じ向き → 残す / 逆向き → 逆で与える(事実の文は事実のまま、criteria に「どちらが続く側か」を書く)/ Jev が動かしていない → 残して向きを criteria に書く。動かないものは、Jev が動かしていれば「邪魔」、いなければ「無関係」として文を外す。

| 場面 | 残す | 逆で与える(criteria に向き) | 残して向きを criteria に | 外す |
|---|---|---|---|---|
| 1 件目 | 約定件数 | 先の建玉(少ないほど始まる)、日の極値からの距離(近いほど始まる)、荒れの比(荒いほど始まる)、元本 ÷ 値幅(小さいほど始まる) | 極値からの秒数(新しいほど)、極値からの戻り(大きいほど)、成行の偏り 5 秒(一方向なほど始まらない)、10 秒 ÷ 60 秒(大きいほど) | 偏りの変化、想定元本、建玉の傾き、時刻帯、反対側の清算(99% が none) |
| 連鎖の中 | 直前 10 秒清算額、同じ側の件数、累計清算額、約定件数、極値からの秒数、荒れの比、10 秒 ÷ 60 秒、元本 ÷ 値幅 | 日の極値からの距離(近いほど続く) | 先の建玉(多い側の五分位だけ止まる)、間隔の比(伸びると止まる) | 成行の偏り、偏りの変化、直前からの戻り、極値からの戻り、建玉の傾き、想定元本、時刻帯 |

「外す」は state からその文を消す。「逆で与える」「向きを criteria に」は、criteria を 1 件目と連鎖の中で別に書く(位置は code が知っている)。

### 7.2 1 件目に足す事実(L-320 (2)。「新しい値段の領域に入る局面か」の枠)

- N1: この清算の価格が、その日の(ts までの)極値を清算の向きに超えているか(超えている / 何 bp 手前か)。C3 の「距離 0」を言葉にしたもの。
- N2: 直前 5 分の値幅の中でいまどこにいるか(清算の向きの端にいる / 中 / 逆の端)。
- **前半で分かれるかを先に確かめる**(§7.1 と同じ規則、|確率 − 0.5| ≥ 0.03)。分かれなければ V4 に入れない。分かれ方の表の行は 2 本増える(多重性に数える)。

### 7.3 V4 の問いと criteria(1 件目 / 連鎖の中で別)

1 件目: instructions は同じ(60 秒以内に同じ側の次の清算が来るか)。criteria yes: 「the tape is busy and volatility has picked up versus the last hour; price is at or breaking the day's extreme and has just set a fresh extreme; little or no open interest is mapped just ahead (price is entering territory where positions have not been built); the print is small relative to the recent range」/ no: 「a quiet tape, calm volatility, price well inside the day's range, a large amount of open interest mapped just ahead, or an extremely one-sided last 5 seconds on a quiet tape (an isolated forced print)」。
連鎖の中: yes: 「same-side liquidations are coming faster and larger (more in the last 10 seconds and in the cascade so far), the tape is busy and volatility rising, price keeps setting fresh extremes, near the day's extreme, gaps between prints not lengthening」/ no: 「gaps between prints are lengthening, the last extreme was set many seconds ago, price is well inside the day's range, or a large amount of open interest sits just ahead(前半では多い側の五分位だけが止まる側)」。

### 7.4 code の比較相手 = 前半で固定した logistic

- 入力: §7.1 の「残す」「逆で与える」「向きを criteria に」の材料(1 件目 9 本 + N1・N2 が分かれれば追加 / 連鎖の中 11 本)を前半の五分位の順位(0〜1、欠測 0.5)にしたもの。前半全件で当てはめ、係数を `config/o3c_signal_logit_<場面>.yaml` に固定する。後半では読むだけ。
- 前半での 5-fold の out-of-fold で順位分離・的中・閾値ごとの適合率を出す(事前の見積もり)。

### 7.5 後半 5,000 件(一度だけ)

- 対象: 前段と同じ選定(`backtest_data/o3c_signal_continue_20260920/jev/selection_manifest.json` の 5,000 件)。Jev V4(接続の使い回し、`jev-1.13.0`)と logistic を同じ件に当てる。呼び出し 5,000。
- 出す表(数値は `data/jev/` にだけ): 1 件目 / 連鎖の中 それぞれの 0.01 刻みの較正(件数 / 実際に続いた割合)、順位分離、閾値ごとの適合率と件数、Jev と logistic の一致、Jev の遅延(p50 / p90 / p99)、入力トークン。
- 決め方(事前): **較正の良い方を方策の模擬に使う**。「良い」= 0.05 刻みの帯で |実際の割合 − 帯の中央| の件数重み平均が小さい方。同点(差 0.01 以内)なら遅延の短い code。「わからない」の帯は、選んだ方の 0.01 刻みの較正で、基準率と 2 SE で区別できない連続した帯(L-277 の規則)。
- **反証者レビューを後半の前に**(§4 の順序)。委任先は後半の段に入る前に止まって渡す。
- **段 1 の結果を受けたリードの決定(2026-09-20、後半に触れる前)**: 段 2 で後半に当てる Jev の形は **V1**(英文 + §6.3 の criteria)にする。理由: 前半 900 件で V4 は V1 より良くなかった(数値は `data/jev/v4/preview_tables.md`。1 件目は両方とも半々の近く、連鎖の中は V1 の方が上)。前半で形を選ぶのは §6.5 の規定の範囲。logistic は §7.4 のとおり前半で固定した係数(`config/o3c_signal_logit_{first,chain}.yaml`)を読むだけ。委任先が推測で埋めた 2 点(連鎖の中で A3・A9 を「外す」側に置いた / N2 の定義)は **logistic の入力にも及ぶ**(反証者 7 C2 で訂正。前の文「段 2 に影響しない」は誤り): 連鎖の中の入力は §7.1 の 11 本(A3 は事実に分けたので入れない、A9 は連鎖の中で 99% が none)で委任先の並びと一致、1 件目には N2 が入る。どちらもリードが認める。

### 7.5.1 反証者レビュー 7(致命 3・直すべき 4・示唆 2)を受けた直し(段 2 の前。`REFUTER_REVIEW7_2026-09-20.md`)

- **C1(点質量の材料で五分位の順位が潰れる)**: 前半の五分位の切り値で順位にする方式をやめ、**前半の経験分布の中央順位**にする。材料の値 x の順位 = (前半で x 未満の件数 + x 以下の件数) / (2 × 前半の件数)。0 が 84% を占める「先の建玉」なら 0 は 0.42 の順位、正の値はその上に並ぶ。前半の並べた値を `backtest_data/o3c_signal_materials_20260920/logit_ecdf_{first,chain}.npz` に固定し、後半ではそこから引くだけ(**C3 の「固定した切り値を後半に当てる経路が無い」も同時に直る**)。logistic は同じ入力で前半に当てはめ直し、係数を書き直す。前半の out-of-fold もやり直す(数値は `data/jev/v4/`)。
- **R1 の同種の退化**(直すべき 1)も同じ方式で直る。
- **§7.5 の「較正の良さ」の実装と少数帯**(直すべき 2): 帯は [0,0.05) … [0.95,1.0) の固定 20 本。件数 20 未満の帯は平均から外す(外した帯を表に書く)。同点(差 0.01 以内)は遅延の短い code。**後半を見る前にここで固定する。**
- **層化の歪み**(直すべき 3): 5,000 件の選定は側 × 材料 1 の 3 群の層化で、1 件目 対 連鎖の中の比が自然の 41.5 : 58.5 でなく 33.3 : 66.7 になる。較正は 1 件目 / 連鎖の中で別に取るので位置ごとの較正は歪まない。全体の数は出さない(出すなら自然比で重み付けし、その旨を書く)。
- **StateBuilder が帯を毎回再計算**(直すべき 4): `config/o3c_jev_state_bands.yaml` を読んで使うように直す(後半で前半の帯をそのまま使うため)。
- **示唆**: Jev の形の選択回数 = 2 回(下見 200 件で V1/V2/V3 → V1、前半 900 件で V1/V4 → V1)、延べ 4 形。多重性に「形の選択 2 回」として数える。決定表由来の判断(残す / 外す / 逆)は 1 件目 13 + 連鎖の中 18 = 31 の判断で、これも多重性に数える(段 2 では V1 を使うので Jev 側には効かず、logistic の入力の選択にだけ効く)。

### 7.6 遅延の項目(L-321。方策の模擬の設計に足す)

方策の模擬の入力に、検知の遅れ(取引所の配信 → 手元)、state 作成、判断(code ≈ 0 / Jev = 実測の分布。この環境の実測 2026-09-20: 接続を張り直す包みで 1 回 0.40 秒(中央値)、`keep_alive=True` で 0.22 秒。TLS の接続だけで 0.17〜0.39 秒だったため。オーナーの PC からは未測定)、発注の 4 つを足し、合計の遅れ d で入る値段を `at_or_after(t₀ + d)` に置く。d は 0.5 / 1 / 2 秒の 3 通り。検知の遅れと state 作成は PAPER の bot の時刻印から測る(未測定。測るまでは 0.5 / 1 / 2 秒の外側の値として扱う)。前半の実測(§ L-321): +1 秒で逆張りの取り分 0.3 bp、+3 秒で 1.5 bp が消える。
