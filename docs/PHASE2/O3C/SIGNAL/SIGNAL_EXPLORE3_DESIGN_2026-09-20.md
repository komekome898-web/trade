# 清算を起点とした値動きの予測可能性 — 探索段 3 の設計(清算の前の価格を基準に、行き過ぎか減衰か、2026-09-20、L-247)

> **位置付け(オーナー逐語 L-243)**: 「**燃料が尽きる尽きないに関わらず、値動きの予想ができるなら戦略のシグナルとしての価値があるので、清算を起点にした研究という位置付けで問いを立て直してください。**」
> **経緯**: 探索段 2(`SIGNAL_EXPLORE2_RESULT_2026-09-19.md`)で残った問いは 1 つ = 清算の直後 60 秒の数 bp の戻りが「**行き過ぎの修正**」(値段が本来の水準を越えて動き、戻る = 予想の材料)か「**一時的インパクトの減衰**」(掃きで動いた分の一部が戻るだけ = 掃いた側の費用)か。分けるには**清算の前の価格**が要る(反証者レビュー 2 の翻訳 (a))。

## 用語(この文書と報告で使う記号。オーナーの問い L-247 の直前「h ってなに？」への答えを固定する)

| 記号 | 意味 |
|---|---|
| 束 | gap 秒以内に続いた清算をひとつにまとめたもの(1 周目の `build_cascades`、gap 60 秒を主)。`start_ms` = 最初の清算の時刻、`end_ms` = 最後の清算の時刻 |
| 基準(p_pre) | **束の最初の清算より前の最後の約定の価格**(`at_or_before(start_ms − 1)`。規則は時刻だけで決まり、価格を見ない)。清算が始まる前の水準 |
| 終端(p_end) | 束の最後の清算の直後の約定の価格(探索段 1・2 の Δ = 0 の起点と同じ) |
| h | **起点から何秒後の価格を見るかの時間幅**。この段の起点は終端(`end_ms`)。h = 60 秒 / 5 分 / 15 分 |
| r_pre(h) | 基準から h 後の価格までの符号付き変化(bp)。符号は清算の向きで付け、**負 = 基準より清算と逆の側**、正 = 基準より清算の側 |
| sweep(列名 `sweep_bp`) | 基準から終端までの符号付き変化(bp)= 掃きの大きさ。定義上 正(清算の向き)になるはず(実測で確かめる)。本文の「sweep」と yaml・列名の `sweep_bp` は同じ量(監査 1 回目・指摘 10) |
| r_end(h) | 終端から h 後の価格までの符号付き変化(bp)。探索段 1・2 の r(Δ = 0, h) と同じ量(監査 1 回目・指摘 2) |
| 前の束の間隔(`prev_bundle_gap_s`) | この束の `start_ms` − 同じ日・同じ gap の直前の束の `end_ms`(秒)。その日の最初の束は NaN |
| 次の束の間隔(`next_bundle_gap_s`) | 同じ日の次の束の `start_ms` − この束の `end_ms`(秒)。未来の量 |
| 戻り率 f(h) | (終端 − h 後の価格)/(終端 − 基準)。0 = 戻っていない、1 = 基準まで戻った、**> 1 = 基準を越えた(行き過ぎの修正)**、< 0 = 清算の向きへ続いた |
| Δ | 探索段 2 の記号(起点を終端から Δ 秒離す)。この段では使わない |

## 0.1 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 清算の前の価格を基準に、行き過ぎか減衰かを 456 日で測る | 「**値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認**」(原文 §1)、「**自前記録使ったら判定できなくなるからダメ**」(L-246) |
| 基準の取り方(最初の清算の直前の約定)、h = 60 秒 / 5 分 / 15 分、後続の清算は条件付けせず全体と「180 秒以上」を併記 | 「**異論ないので進めてください**」(L-247。空の行として見せた上での承認) |
| 自前記録は判定まで触らない。判定は一度だけ | 「**自前記録使ったら判定できなくなるからダメ**」(L-246) |
| 設計の段で `jev_design.py` の 2 表を出して貼る | 「**設計の段階でjevの判定(もしくはその代替となるもの)がないと事前登録させない**」(L-242) |
| 機構(燃料切れ)を判定から外す | 「**燃料が尽きる尽きないに関わらず**」(L-243) |
| 用語(h・Δ・r)の定義を冒頭に置く | 「**h ってなに？**」(2026-09-20) |
| 「基準を越えた / 基準と終端の間」の 2 区分(行き過ぎの修正 / 減衰) | L-247 の直前にオーナーへ見せた文「**h 後の価格が基準を清算と逆の側に越えていれば行き過ぎの修正、基準と束の終わりの間にあれば減衰**」(リードの文。これを見た上での「異論ないので進めてください」) |
| 3 つ目の区分(f < 0 = 終端より先へ続いた)・区分を戻り率 f(比)で置くこと・基準が 5 分以内に無ければ NaN・**対照 (a) 掃き合わせ** | **(該当語なし)**— リードの具体化(監査 1 回目・指摘 1)。清算側の行はこの決定に依らない。この行は 2026-09-20 に委任の前にオーナーへ見せた(返答待ち)。**リードの決定として、返答を待たずに委任した**: 清算側の行はこの決定に依らず、探索は判定ではなく(456 日の 4 度目の読み)、対照 (a) は外せる(監査 3 回目・指摘 8。§0.1「そこだけ聞く」は満たし、「止まる」は満たしていない)。**これはこの行だけの判断で、§0.1 の例外運用にはしない**(監査 4 回目・指摘 1)。次からは、右が空の行は返答が来るまでその行に依る作業を委任しない。この判断の是非はオーナーが決める(報告の冒頭に再掲する)。オーナーの異論があれば対照 (a) は報告から外す。外した場合、F2 の 3 区分は清算側だけの分布になり(対照 (b) は r_end だけ)、問い F2「h 後の価格は基準のどちら側か」は清算側の分布だけで答える。対照 (a) は「同じ大きさの値動きが清算なしで起きたとき、どれだけ戻るか」の参照であり、設計の組み直しは要らない(監査 2 回目・指摘 6) |
| SE の日クラスタ・gap の併記・多重性の引き継ぎ・反証者レビュー・テスト | `research-protocol` §0.0 / §11 の固定手順。「**スキルの承認をもって聞かずに当ててよい**」(L-179) |

## 1. 意図と実装の突き合わせ(§0.5。`jev_design.py` はこの表の逐語を読む)

| # | 原文の意図(逐語) | この設計の要素 | 印 | 備考 |
|---|---|---|---|---|
| S-1 | 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243) | 基準を越えて戻る(行き過ぎの修正)か、基準までしか戻らない(減衰)かを、束の後の h で数える。越えるなら「基準より先まで動く」という予想になる | **○** | |
| S-2 | 「**清算を起点にした研究という位置付け**」(L-243) | 起点 = 束(gap 60 主、30 / 180 併記)。基準は束の直前 | **○** | |
| S-3 | 「**清算の監視による値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認し、戦略へ落とし込む材料を見つける**」(原文 §1) | 「上がりすぎ下がりすぎ」= 掃きが基準を越えて動き、その後基準を越えて戻る形。戻り率 f(h) の分布と、f > 1 の割合 | **○** | この段の中心 |
| S-4 | 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1) | f(h) の符号の分岐(f < 0 = 清算の向きへ続く = トレンド、f > 1 = 転換)を h ごとに数える。分ける条件(Q4)は外 | **△** | |
| S-5 | 「**みる時間軸 可能な限り多く**」(原文 §1) | h = 60 秒 / 5 分 / 15 分(L-247 で承認した 3 本)。1 時間以上は探索段 2 で検出力不足と分かっているので置かない | **△** | 3 本に限る理由を書いた |
| S-6 | 「**bitFlyerの価格相関**」(原文 §1) | 同じ基準・同じ h の bitFlyer の値を併記(1 分足、窓ずれを明記) | **△** | 併記まで |
| S-7 | 「**価格毎の約定履歴の積み上げからどの価格帯で清算が起きるかの予測できるか**」(原文 §1) | 測らない(Q6) | **✕** | 次の単位 |
| S-8 | 「**できるならエントリーや利確に活かせるか**」(原文 §1) | 規則にはしない。f(h) の分位は出す | **△** | 費用は判定に使わない(A-8) |
| S-9 | 「**海外取引所の高レバ市場**」(原文 §1) | Binance COIN-M BTCUSD_PERP | **○** | |

## 2. 問い

| # | 問い | 見る形 |
|---|---|---|
| F1 | 掃きの大きさ: sweep(基準 → 終端)は何 bp か | sweep の平均・中央値・分位(全体、側別、束の件数別)。負(定義と逆)の束の割合と、その正体(基準の約定が束の中の約定か、等) |
| F2 | **行き過ぎか減衰か**: h 後の価格は基準のどちら側か | 各 h で、**基準を越えた(f > 1)/ 基準と終端の間(0 ≤ f ≤ 1)/ 終端より先へ続いた(f < 0)** の 3 区分の割合(清算側)と、r_pre(h) の平均 ± 日クラスタ SE、f(h) の中央値と分位。対照は §4 |
| F3 | 越えた幅: f > 1 の束で、基準をどれだけ越えたか(bp)の分布 | 分位。sweep との比(越えた幅 / sweep) |
| F4 | 前後の清算の扱い | 全体と「次の束まで 180 秒以上」(結果依存の条件付け = コライダーと明記)と「前の束から 180 秒以上」(過去の条件付け。基準への直前の束の混入を除く)を**併記**する。条件付けで F2 の 3 区分がどう動くかを見る |
| F5 | 属性との関係 | 探索段 1 と同じ 10 属性 30 群で、f > 1 の割合と r_pre を h ごとに出す。**sweep の大きさ**(F1)を属性に足す(11 属性 33 群)。含む日数と欠測の割合を列に置く |

**答えない問い**: Q4(分岐を分ける条件)、Q5(bitFlyer との相関の判定。併記だけ)、Q6(価格帯の事前予測)、1 時間以上の h。

## 3. 候補(`jev_design.py` に渡すもの)

- 候補の観測量: `config/jev_design_examples/signal3_observables.yaml`(8 件)。
- 対照で合わせる候補の変数: `config/jev_design_examples/signal3_covariates.yaml`(**13 件** = 探索段 1 の属性 10(側・時刻帯・連続 8 = `dist_node_bp` / `dist_vwap_bp` / `bin_pct` / `doi_pre_1h` / `implied_leverage` / `bundle_n_events_dedup` / `bundle_total_notional` / `bundle_width_ms`)+ `sweep_bp` + `next_bundle_gap_s`(未来の量。合わせるに入っても対照の作り方には使えない)+ `baseline_lag_ms`)。前版は `dist_node_bp` と `doi_pre_1h` を欠いた 11 件で、これはリードの見落とし(監査 3 回目・指摘 3)。足して再実行した。

## 4. 対照

清算の無い時刻には「基準 → 終端」に当たる掃きが無いので、r_pre と f は対照で定義できない。対照は 2 つの形で置き、加えて対照そのものの自己点検(F0)を出す:

- **(a) 掃き合わせ**(手順は機械的に一意。監査 1 回目・指摘 4): 束 b(日 d、側 s、幅 W_b = `end_ms − start_ms`、sweep S_b > 0)ごとに、
  1. 候補時刻 t = その日の 00:00:00 UTC から 10 秒刻み(8,640 点)。区間 [t − W_b − 1 ms, t + 900 秒] が同じ gap のどの束の [`start_ms`, `end_ms`] とも重ならないものだけ残す。
  2. 候補の掃き c_t = (`at_or_after(t)` の価格 − `at_or_before(t − W_b − 1)` の価格)(bp)× sgn(s)。sgn(SELL) = −1、sgn(BUY) = +1(清算側の sweep と同じ符号規則)。
  3. S_b の 10 分位帯に c_t が入る候補のうち、|c_t − S_b| が最小のものを取る。同値なら早い t。**分位帯の切り方**(監査 2 回目・指摘 4): 同じ gap の S_b > 0 の束の S_b について `numpy.quantile(…, [0.1, …, 0.9])`(既定の線形補間)で切り値 q_1 … q_9 を出し、q_10 = +∞ とし、**帯 1 = (0, q_1)(0 を含まない)、帯 k ≥ 2 = [q_{k−1}, q_k)(下限を含み上限を含まない)**。S_b の帯もこの規則で決める(S_b > 0 なので必ずどこかの帯に入る)。**c_t ≤ 0 は帯 1 の下限 0 を含まないのでどの帯にも入らず、候補にならない**(監査 3 回目・指摘 1 で「下回る」の誤りを直した)。切り値が同値で帯が空になる場合も、帯は正常でもその日の生存候補(手順 1 の重なり除外と手順 4 の置換なしを経た後)が 1 つもその帯に落ちない場合も、その束は「対照 (a) なし」(監査 3 回目・指摘 2)。
  4. 置換なし: 束を `start_ms` の順に処理し、既に取った候補の区間 [t − W_b − 1 ms, t + 900 秒] と重なる候補は使わない。取れなければその束は「対照 (a) なし」。
  5. 対照 (a) の基準 = `at_or_before(t − W_b − 1)`、終端 = `at_or_after(t)`、h 後 = `at_or_before(終端の時刻 + h)`(清算側と同じ非対称の取り方)。f と r_pre・r_end を清算側と同じ式で出す。
  **窓の幅は束ごとの W_b であり、中央値は使わない**(gap ごとに違う中央値を流用する曖昧さを消す)。価格を見て選ぶ規則なので、**対照 (a) は「同じ大きさの値動きの後」の比較であり反事実ではない。**
- **(b) 無条件**: 対照 (i) 日集約版と (ii) の h 後の符号付き変化 r_end(探索段 2 と同じ)。基準が無いので r_pre と f は無く、**F2 の 3 区分は清算側と対照 (a) だけ**(監査 1 回目・指摘 5)。
- **F0 自己点検**(監査 1 回目・指摘 6・8。探索段 2 の E0 に当たる): 清算 / 対照 (a) / (b)-i / (b)-ii の 4 種それぞれで、基準の遅れ(求めた時刻 − 見つかった約定の時刻)・終端の遅れ(見つかった約定 − 求めた時刻)・h 後の遅れ(h − 実際の間隔)の分位(p50 / p90 / p99 / 最大)を行として出し、h 後の価格が終端と同じ(約定なし)割合は h 後の遅れの行の**列**に置く(行は増えない。監査 2 回目・指摘 1)。対照 (a) は加えて |c_t − S_b| の分位と、取れなかった束の数と割合を gap ごとに出す。**「構造でそろう」とは書かない。そろうかは F0 で測る。**

## 5. 設計の段の判定(`jev_design.py`。オーナー指示 L-242)

コマンド(2026-09-20、リードの実測): `PYTHONPATH=src:scripts python3 scripts/jev_design.py rank-observables --purpose-from docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md --intent S-1,S-3,S-4 --observables config/jev_design_examples/signal3_observables.yaml --md …` と `rank-covariates --quantity "…" --covariates config/jev_design_examples/signal3_covariates.yaml --md …` を **2 回**(判定の量 = `retrace_fraction`(主。F2 が報告する量)と `r_pre`(代理)。監査 3 回目・指摘 4: 前版は代理の r_pre だけで走らせていた)。確率は `data/jev/design/signal3_*.json`(リポジトリに入れない)。

## 設計の段の判定(jev_design、2026-09-19、モデル jev-1.13.0)

- 設計文書: `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md`
- 候補ファイル: `config/jev_design_examples/signal3_observables.yaml`
- 意図: S-1, S-3, S-4

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### S-1 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `retrace_fraction` | 直接(3 以上) |
| 2 | `overshoot_share` | 直接(3 以上) |
| 3 | `r_pre` | 代理(2 台) |
| 4 | `r_end` | 代理(2 台) |
| 5 | `time_to_baseline` | 代理(2 台) |
| 6 | `mfe_mae_pre` | 無関係(1 以下) |
| 7 | `bf_r_pre` | 無関係(1 以下) |
| 8 | `sweep_bp` | 無関係(1 以下) |

### S-3 「**清算の監視による値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認し、戦略へ落とし込む材料を見つける**」(原文 §1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `retrace_fraction` | 直接(3 以上) |
| 2 | `overshoot_share` | 直接(3 以上) |
| 3 | `r_pre` | 代理(2 台) |
| 4 | `r_end` | 代理(2 台) |
| 5 | `mfe_mae_pre` | 代理(2 台) |
| 6 | `time_to_baseline` | 無関係(1 以下) |
| 7 | `bf_r_pre` | 無関係(1 以下) |
| 8 | `sweep_bp` | 無関係(1 以下) |

### S-4 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1)(印 △)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `retrace_fraction` | 代理(2 台) |
| 2 | `overshoot_share` | 代理(2 台) |
| 3 | `r_end` | 代理(2 台) |
| 4 | `r_pre` | 代理(2 台) |
| 5 | `time_to_baseline` | 無関係(1 以下) |
| 6 | `mfe_mae_pre` | 無関係(1 以下) |
| 7 | `bf_r_pre` | 無関係(1 以下) |
| 8 | `sweep_bp` | 無関係(1 以下) |

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: retrace_fraction: (p_end - p_h) / (p_end - p_pre), the fraction of the sweep (pre-cascade baseline to last liquidation fill) that has been retraced h seconds after the bundle's last liquidation; > 1 means the price crossed back beyond the baseline (overshoot corrected), 0..1 means partial decay, < 0 means the move continued
- 変数ファイル: `config/jev_design_examples/signal3_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `sweep_bp` | 合わせなくてよい |
| 2 | `side` | 合わせなくてよい |
| 3 | `baseline_lag_ms` | 合わせなくてよい |
| 4 | `bin_pct` | 合わせなくてよい |
| 5 | `bundle_width_ms` | 合わせなくてよい |
| 6 | `dist_node_bp` | 合わせなくてよい |
| 7 | `next_bundle_gap_s` | 合わせなくてよい |
| 8 | `bundle_total_notional` | 合わせなくてよい |
| 9 | `dist_vwap_bp` | 合わせなくてよい |
| 10 | `bundle_n_events_dedup` | 合わせなくてよい |
| 11 | `implied_leverage` | 合わせなくてよい |
| 12 | `doi_pre_1h` | 合わせなくてよい |
| 13 | `time_of_day` | 合わせなくてよい |

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: r_pre: signed price change in basis points from the pre-cascade baseline (last trade before the bundle's first liquidation) to h seconds after the bundle's last liquidation; negative = price is on the side opposite to the cascade direction relative to the baseline
- 変数ファイル: `config/jev_design_examples/signal3_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `side` | 合わせなくてよい |
| 2 | `sweep_bp` | 合わせなくてよい |
| 3 | `baseline_lag_ms` | 合わせなくてよい |
| 4 | `bundle_width_ms` | 合わせなくてよい |
| 5 | `next_bundle_gap_s` | 合わせなくてよい |
| 6 | `bin_pct` | 合わせなくてよい |
| 7 | `bundle_n_events_dedup` | 合わせなくてよい |
| 8 | `bundle_total_notional` | 合わせなくてよい |
| 9 | `dist_node_bp` | 合わせなくてよい |
| 10 | `implied_leverage` | 合わせなくてよい |
| 11 | `dist_vwap_bp` | 合わせなくてよい |
| 12 | `time_of_day` | 合わせなくてよい |
| 13 | `doi_pre_1h` | 合わせなくてよい |

**リードの読み**: (i) S-1・S-3 で戻り率 f(h)(`retrace_fraction`)と 3 区分の割合(`overshoot_share`)が 1・2 位「直接」、S-4 でも 1・2 位。**主な量はこの 2 つ**(F2)。r_pre と r_end(探索段 1・2 の量)は代理として併記し、探索段 2 との一致(r_end の Δ = 0・h = 60 秒 = −6.80)を確認に使う。sweep は目的の量ではなく F1 の記述と F5 の属性(「無関係」でよい)。(ii) 合わせる変数は 13 件とも、判定の量が `retrace_fraction` でも `r_pre` でも「合わせなくてよい」。`retrace_fraction` で 1 位(`r_pre` で 2 位)の `sweep_bp` は、対照 (a) の選び方(§4 手順 3)が同じ大きさの値動きを比較の定義に入れる。**対照 (a) は Jev の表から導いたものではない**(監査 4 回目・指摘 2): `rank-covariates` が答えるのは「清算側と対照の比較を公平にするために、その変数をそろえる必要があるか」で、13 件とも「合わせなくてよい」= 対照 (b) の無条件比較に sweep 合わせは要らない、という答え。対照 (a) は公平さのための合わせではなく、別の問い「清算なしで同じ大きさの値動きが起きたとき、どれだけ戻るか」を置くためのもので、リードの設計判断(§0.1 の空の行)。Jev は判断しない(道具の注記のとおり)。**実際にそろうかは F0(|c_t − S_b| の分位)で測る**(監査 1 回目・指摘 6。「構造でそろう」の断定は取り下げ)。`next_bundle_gap_s`(5 位)は未来の量なので対照の作り方には使わず、F4 の併記で扱う。**Jev の閾値の較正は未測定**(探索段 2 の設計と同じ注記)。

## 6. データと段

- 入力: `backtest_data/binance_cm_o3c_20260913`(約定 475 日・清算 472 日)。対象日は探索段 1・2 と同じ 456 日(1 周目の走行の `per_day` の日の集合)。**18 日**は暦の日数 474(最初の日〜最後の日)− 456 で、清算 472 日との差(16)ではない(出所: `backtest_data/o3c_signal_explore2_20260919/summary.json` の「暦の日数 474」「欠けた日の数 18」、欠けた日の一覧つき。監査 4 回目・指摘 4)。欠けた日はそのまま(1 周目と同じ日の集合で比べるため)。束と対照は 1 周目の表の行(`gap60_w8` 主、`gap30_w8` / `gap180_w8` 併記)をそのまま使い、**基準(`at_or_before(start_ms − 1)`)、終端(`at_or_after(end_ms)`)、h 後の価格(`at_or_before(終端の時刻 + h)`)を約定から取る**。探索段 2 の道具 `scripts/o3c_signal_explore2.py` の読み込みと日クラスタ SE をそのまま使う。
- bitFlyer: 1 分足(`bf_*` の経路)。基準は `start_ms` を含む足の 1 本前の終値(足の開始が起点より後になる窓ずれを表の見出しに書く)。
- **この段も探索段**(research-protocol §0.0)。456 日は **4 度目の読み**。判定語は付けない。判定は自前記録(触っていない、L-246)で一度だけ。
- 出力(行数は委任先に数え直しを求める。監査 1 回目・指摘 3 で「31 群」の誤記を直した):
  - **F0**(自己点検): 4 種 × (基準の遅れ + 終端の遅れ + h 後の遅れ 3) = 20 行 × gap 3 本 = 60 行、対照 (a) の合わせ具合 1 行 × gap 3 本 = 3 行。**計 63 行。**
  - **F1**(sweep の分布): 全体・側 2・束の件数 3 = 6 行 + 両側混在で除外した束の割合 1 行 = 7 行 × gap 3 本 = **21 行**。
  - **F2**: h 3 × {清算 全体、清算 次の束まで 180 秒以上、清算 前の束から 180 秒以上、対照 (a)、対照 (b)-i、対照 (b)-ii} = 18 行 × gap 3 本 = **54 行**。列: 群 / h / n / 含む日数 / r_end 平均 ± 日クラスタ SE ± naive SE と日で等重みの平均(6 種すべて)/ r_pre 平均 ± 日クラスタ SE ± naive SE と日で等重みの平均(清算と (a))/ f > 1 の割合・0 ≤ f ≤ 1 の割合・f < 0 の割合・f の中央値と分位(清算と (a)。(b) の行は「—」)/ h 後の価格が終端と同じ割合(6 種すべて。3 区分の読みに添える)。
  - **F3**(越えた幅): h 3 × gap 3 本 = **9 行**(監査 2 回目・指摘 3 で gap 60 限定をやめた)。
  - **F5**(属性): 33 群(10 属性 30 群 + sweep の 3 分位)× h 3 = **99 行**(gap 60 だけ)。**gap 30 / 180 を F5 で測らない理由**: 10 属性 30 群の切り値は探索段 1 が gap 60 の表で切ったもので(探索段 2 の E4 も gap 60 だけ)、gap ごとに切り直すと群が別物になり探索段 1・2 と並べられない。sweep の 3 分位はこの gap 60 の表に足す列であり、同じ表に置くために gap 60 だけ(sweep 自体の gap 30 / 180 の分布は F1 にある)。gap 30 / 180 の属性は**本単位では測らない**(監査 2 回目・指摘 3。縮めた範囲をここに書く)。**sweep の 3 分位の切り方**(監査 3 回目・指摘 5): gap 60 の清算側で S_b > 0 の束を母集団に、探索段 2 の道具の `tertile_cuts`(`nanpercentile` の 33.3 / 66.7、線形補間)で lo / hi を切り、`tertile_mask` と同じ規則(下 = S_b ≤ lo、中 = lo < S_b ≤ hi、上 = S_b > hi)で分ける。S_b ≤ 0 の束はどの群にも入らず、欠測の割合の列に数える。列に「含む日数」「その属性が欠測で群に入らない束の割合」を置く(反証者 2 の #10)。
  - **合計 246 行**(63 + 21 + 54 + 9 + 99)。`tables.md` + csv + 行データ(`rows_gap*.csv.gz`。清算・対照 (a)・(b) の全行、`prev_bundle_gap_s` / `next_bundle_gap_s` / `baseline_lag_ms` / `sweep_bp` / 各 h の r_pre・r_end・f を含む)+ `summary.json` + `MD5SUMS`。
- **自前記録は読まない**(テストで測る)。

## 7. 走らせる前に決めること(リードの決定、L-247 で承認)

- 基準 = 束の最初の清算より前の最後の約定(`at_or_before(start_ms − 1, 300_000)`。5 分以内に無ければ NaN)。基準の約定と最初の清算の間隔 `baseline_lag_ms` を行に残し、分布を出す(基準が束の中の約定になっていないことの確認)。**直前の束からの混入**(監査 1 回目・指摘 9): `prev_bundle_gap_s` を行に残し、F1 の各行に「基準の約定の時刻が直前の束の `end_ms` 以下である束の数」と「前の束から 300 秒未満の束の割合」の**列**を置く(行は増えない。監査 2 回目・指摘 2)。F2 に「前の束から 180 秒以上」の部分集合を併記する(過去で条件付けるのでコライダーではない。次の束の条件付けとは区別して書く)。
- h = 60 秒 / 5 分 / 15 分。
- 戻り率 f(h) の 3 区分: f > 1(基準を越えた = 行き過ぎの修正)/ 0 ≤ f ≤ 1(減衰)/ f < 0(続いた)。sweep ≤ 0 の束(定義と逆)は f を定義せず、数を報告する。
- 対照 (a) の掃き合わせ: §4 の手順 1〜5(10 秒刻みの候補、束ごとの幅 W_b、同じ 10 分位帯の中で |c_t − S_b| 最小、置換なし)。取れない束の数と割合を F0 に出す。
- 後続の清算: 条件付けせず全体を主、「次の束まで 180 秒以上」(結果依存、コライダー)と「前の束から 180 秒以上」(過去の条件付け)を併記。
- **3 区分の読み方**(反証者 2 の #4): h は 60 秒以上に限り、3 区分は h 後の価格が終端と同じ(約定なし)割合を横に添えて読む。群の間で 3 区分を比べるときは、その割合が同じ桁であることを先に見る。
- **清算行の重複**(反証者 2 の #7): **既に測ってある**(`docs/DATA.md` §2 の行「Binance COIN-M `liquidationSnapshot` の重複行」、2026-09-17 リード実測、`ACTION_LOG` 053: 472 日・106,822 行のうち一意 53,398、多重度 2 が 53,385 群・4 が 13 群)。反証者 2 の「未確認」はこの記録を見ずに書かれたもので、設計の前版もそれを引き継いだ(監査 2 回目・指摘 5)。この段では委任先に同じ数え方(全列一致)で再計算させ、`summary.json` に多重度の内訳を残して `DATA.md` の値と一致するかを書く。束は 1 周目の重複除去済みの行(`bundle_n_events_dedup`)を使う。
- **両側混在の束**(反証者 2 の #3): 除外した割合を F1 に gap ごとに出す。
- SE は日でクラスタ(探索段 2 の道具 `mean_se_cluster` の式)。**naive SE**(母標準偏差 / √n。同じ関数が返す)も併記し、クラスタ化で何倍になるかが読めるようにする。日で等重みの点推定を併記(探索段 2 の反証者 #6)。(監査 3 回目・指摘 6・7: 委任文にあって設計に無かった語をここに置いた)
- gap: 60 主、30 / 180 併記(固定しない)。
- 多重性: 探索段なので検定しない。行数 246 を、探索段 1 の 552・探索段 2 の 318・反証者の約 90 に足して次の事前登録に引き継ぐ。**(2026-09-20 測定後の追記、報告の監査 3 回目・指摘 2)**: 報告では行とセルを分け、(i) 表の行数 552 + 318 + 246 = 1,116 行、(ii) 反証者のセル数 58 + 156 + 224 = 438 セル(反証者 1 を新たに算入。「約 90」は実測 156)とした。会計を増やす向きの変更で、この式を減らすものではない。
- 実装: `scripts/o3c_signal_explore3.py`(委任)。テスト: r_end(h = 60 秒)が探索段 2 の Δ = 0 と一致 / 基準が `start_ms` より前 / 3 区分の和が 1 / sweep ≤ 0 の扱い / 対照 (a) の帯が合っていて区間が束と重ならない / 対照 (a) が置換なし / `paper_logs/` を開かない。
- 読みの前に反証者レビュー(research-protocol §11)。報告の冒頭に用語の表とオーナーの目的(逐語)への答え。

## 8. 反証者レビュー 2 の「直すべき」10 件との突合せ(監査 1 回目・指摘 7)

| # | 反証者 2 の項目 | この段での扱い |
|---|---|---|
| 1 | 対照 (i) の 1 対 1 規則 | 扱う: 対照 (b)-i は日集約版だけ(探索段 2 の報告と同じ) |
| 2 | bitFlyer の窓 | 扱う: §6 の見出しに窓ずれを書く |
| 3 | 両側混在の束の除外率 | 扱う: F1 に gap ごとの割合を出す(§7) |
| 4 | 短い h の 3 区分を群間で比べない | 扱う: h ≥ 60 秒だけ。約定なしの割合を横に添える(§7) |
| 5 | h ≥ 1 時間の「0」を「不明」と書く | 扱わない: この段に 1 時間以上の h は無い(§2「答えない問い」) |
| 6 | 日で等重みの点推定 | 扱う: §7 |
| 7 | 清算行の 2 倍重複(3 日しか未確認) | 扱う: 472 日の多重度を `summary.json` に出す(§7) |
| 8 | 対照 (i) で落とす行の時刻の偏り | 扱わない: この段の対照 (b)-i は日集約版だけで、1 対 1 の組み方が落とす行は存在しない。行が落ちるのは対照 (a) の取れなかった束だけで、それは F0 に数と割合を出す(監査 2 回目・指摘 7。前版の「落とした行数を `summary.json` に」は取り下げ) |
| 9 | 引き継ぐ周回数 | 扱う: §7 の多重性(552 + 318 + 約 90 + 246。**2026-09-20 追記**: 報告では行 1,116 とセル 438 に分けた。§7 の追記) |
| 10 | `doi_pre_1h` / `implied_leverage` の欠測日の偏り | 扱う: F5 に「含む日数」「欠測で群に入らない束の割合」の列(§6) |
