# 反証者レビュー9 — 「値段の続き」を判断の対象にした方策(①)+ 全部逆張りの最適化(②)段1→段2前 2026-09-21

対象: 設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md`(全文)、段1 報告
`docs/PHASE2/O3C/SIGNAL/VALUE_STAGE1_REPORT_2026-09-21.md`(全文、(8) リードの検収を含む)、
道具 `scripts/o3c_signal_value.py`・`scripts/o3c_bitflyer_spread.py`、試験
`tests/test_o3c_signal_value.py`、出力 `backtest_data/o3c_signal_value_20260921/`
(`stage1_firsthalf/`・`spread/`・npz)、係数 `config/o3c_signal_logit_value_{first,chain}.yaml`、
再利用元 `scripts/o3c_signal_policy.py`・`scripts/o3c_signal_logit.py`、委任文
`docs/DATA/delegations/20260921_o3c_signal_value_prompt.md`。

**守った制約**: 判定語(陽性 / 陰性 / 有意 / 差あり / 検出されず / 支持 / 棄却)は 1 つも使っていない。
`half == "後半"` の行も後半の出力も一度も開いていない(費用の道具が読む後半の標本日の時刻は、
設計 §4 と報告 (5)-1 が開示している既存の読みで、このレビューでは新たに開いていない)。
`paper_logs/` は開いていない。Jev は呼んでいない。コードも出力ファイルも書き換えていない
(このレビュー文書だけを書いた)。モデル名は書いていない。コミット・押し出しはしていない。
**再計算はすべて自分でコマンドを打ち、出力を貼った**(末尾に一覧)。

---

## 致命

### 致命-1: 段2 の実装が、設計 Q4・Q1 併記・Q5b・Q0 の要求の大半を出さない。しかも連鎖 1 本ごとの行を出さないので後から作れない

設計 §2 の Q4(61 行目)の逐語:

> 「並べる 6 本: ① 3 択 A / ① 3 択 B / ②opt / 全部逆張り(素)/ **前段の 3 択(清算)A** / 完全な判断(値段)A。
> 連鎖ごとの分布、**日ごとの合計の分布(負の日の割合、上位 5 日の寄与、下位 5 日)**、位置別、**大きさ別**、**側別**、
> **連鎖ごとの対差の分布**(① 3 択 A − ②opt / ① 3 択 A − 素 / ②opt − 素)」

Q1 は「併記(段 2、11 度目に含める): 前段の 5,000 件(後半)での較正表」、Q5b は「後半 81 本は段 2」
「(b) … 後半 2,000 本全部」、Q0 は「後半 2,000 本の `bundle_id` が前段と同一であること(MD5)」。
Q2 は参照として「**完全な判断(清算)と前段の 3 択(清算)を費用込みで再集計**」。

`scripts/o3c_signal_value.py: run_stage2`(1387〜1459 行)が出すのは 1437 行の `tables` だけである:

```
$ sed -n '1437,1445p' scripts/o3c_signal_value.py
    tables = [
        ("dist_table.csv", dist_rows, "後半 2,000 本 連鎖ごとの損益分布"),
        ("position_breakdown.csv", pos_rows, "後半 2,000 本 位置別の内訳"),
        ("judge_counts.csv", sp.build_judge_count_table(built["judge_counts"]),
         "後半 2,000 本 判断の件数"),
        ("action_counts.csv", sp.build_action_count_table(built["action_counts"]),
         "後半 2,000 本 行動の件数"),
        ("opt_cascades.csv", opt_rows, "後半 2,000 本 ②opt の連鎖ごとの損益"),
    ]
```

`sp.build_dist_table`(`scripts/o3c_signal_policy.py` 711〜718 行)は 方策 × 型 × 遅れ × 0 含む/含まないの
4 重ループだけで、**側・大きさ・日の層別を持たない**:

```
$ sed -n '711,718p' scripts/o3c_signal_policy.py
def build_dist_table(cdf: pd.DataFrame) -> list:
    rows = []
    for policy in ALL_POLICIES:
        for ptype in (TYPE_A, TYPE_B):
            for delay in DELAYS_S:
                for inc0 in (True, False):
                    rows.append(dist_stats(cdf, policy, ptype, delay, inc0))
    return rows
```

出ないものを列挙する(いずれも段2 の出力ファイルに存在しない):

| 設計の要求 | 段2 の実装 |
|---|---|
| 連鎖ごとの対差(① 3 択 A − ②opt / ① − 素 / ②opt − 素) | 無い。②opt は `opt_cascades.csv`(`bundle_id`・`day`・`pnl_bp`・`出口の理由` の 4 列)にしか出ず、① と 素 は連鎖 1 本ごとの行が**どこにも出ない**ので対差を作れない |
| 日ごとの合計の分布(負の日の割合・上位 5 日・下位 5 日) | 無い |
| 側別 | 無い |
| 大きさ別 | 無い |
| 前段の 3 択(清算)A | 無い(`judgments_for_policy_value` は値段のラベルの帯・閾値しか持たず、清算のラベルの係数 `config/o3c_signal_logit_{first,chain}.yaml` を読む経路が `run_stage2` に無い) |
| 完全な判断(清算) | 同上 |
| Q1 併記(後半 5,000 件の較正表) | 無い |
| Q5b(a) 後半 81 本 / (b) 後半 2,000 本の 1 分の始値 | 無い(`run_q5b` / `pnl_next_minute_open` は `run_stage1` からしか呼ばれない) |
| Q0 後半 2,000 本の `bundle_id` が前段と同一(MD5) | 無い(`select_stage2_cascades` を呼ぶだけで突き合わせない) |

段1 も同じ形で、`stage1_firsthalf/` に連鎖 1 本ごと・プリント 1 行ごとのファイルが無い
(実測: `ls` の 16 ファイルは表と `summary.json`・`grid_selected.json`・`MD5SUMS` だけ)。
**前段(`o3c_signal_policy_20260920/stage1_firsthalf/`)は `cascades.csv.gz` と `prints_policy.csv.gz` を
出していたので、反証者レビュー8 は連鎖単位の突き合わせができた。この単位はその出力を落としている。**

**なぜ段2 の前に直す必要があるか**: 後半は 11 度目の一度きりの読みで、上の表の量はどれも
「後半 2,000 本の連鎖 1 本ごとの損益」から作る。段2 が連鎖ごとの行を残さずに走ると、
これらを後から作るには**後半をもう一度読む**しかない(前段の Q6 の道筋と同じ理屈で、
反証者レビュー8 致命-3 が「一度だけ」の趣旨に反すると書いた形)。

**対応(段2 の前)**: `run_stage2` に (i) 連鎖 1 本ごとの行(`cascades.csv.gz` 相当。`bundle_id`・`day`・
`side`・`連鎖の大きさ`・方策・型・遅れ・`pnl_bp`・`建玉の回数`・`保有秒`・`入った`・`欠測`)と
②opt の同じ列、(ii) 清算のラベルの 2 本(前段の 3 択 A・完全な判断(清算))、(iii) Q1 併記・Q5b・
Q0 の MD5 を足す。表の形(対差・日ごと・側別・大きさ別)は (i) があれば後から作れるので、
最低限 (i)(ii)(iii) を段2 の実行前に入れること。

**再計算**: 該当なし(存在しない出力は再計算できない。欠落の確認コマンドを上に示した)。

---

### 致命-2: ① の帯と基準率を作った母集団に、方策が一度も通らない 1,041 件(4.8%)が混ざっている。連鎖の中だけで較正し直すと、連鎖内プリントの 5.6% で判断が変わる

`scripts/o3c_signal_value.py: build_scene_frames`(200〜212 行)は `rows_materials` の前半**全行**
(21,838 件)を `cand_1` で 1 件目 / 連鎖の中に割り、`fit_value_logit` → `calibration_table` が
その母集団で基準率と帯を決める。一方、方策を通す連鎖は
`sp.cascades_from_prints`(`scripts/o3c_signal_policy.py` 350〜355 行)が `groupby("bundle_id")` で作る。
**`bundle_id` が欠損している行は pandas の `groupby` が黙って落とす。**

```
$ python3 -c "... v.load_stage1_prints() / v.cascades_from_prints(...) ..."
前半プリント 21838 bundle_id 欠損 1041
連鎖 8931 連鎖に入るプリントの合計 20797
bundle_pos の内訳(前半 materials) {'最後': 8931, '途中': 7968, '最初': 3898, '束の外': 1041}
束の外 1041 のうち cand_1==0: 138  cand_1>0: 903
最初 3898 値段ラベル割合 0.737
最後 8931 値段ラベル割合 0.1982
束の外 1041 値段ラベル割合 0.9232
途中 7968 値段ラベル割合 0.7974
```

つまり:

- 判断を**作る**母集団は 21,838 件、判断を**使う**母集団は 20,797 件。差の 1,041 件(4.8%)は
  探索段 4 の束に入らないプリントで、値段のラベルの割合が **0.9232** と他(0.198 / 0.737 / 0.797)から
  大きく外れている。
- 報告 (5)-15(594 行目)と (8) の検収は、このうち `cand_1 == 0` の **138 件しか見ていない**。
  残りの 903 件は「連鎖の中」の母集団に入っており、報告のどこにも出てこない。
- 報告 (3) の Q0 表と設計 §用語は「前半 プリント 21,838 / 連鎖 8,931」と書くが、
  **21,838 件のうち 1,041 件がどの連鎖にも入らない**ことは書かれていない。

較正を「実際に方策が通るプリント」だけでやり直すと、基準率と帯が動く:

```
$ python3 /tmp/.../chk_pop.py
連鎖に入るプリント 20797
[1件目] 全件 n=9069 基準率=0.4415 帯=(0.42, 0.52) / 連鎖内だけ n=8931 基準率=0.4343 帯=(0.38, 0.52)
   連鎖内のプリントで判断が変わる件数: 682 / 8931
[連鎖の中] 全件 n=12769 基準率=0.6229 帯=(0.6, 0.68) / 連鎖内だけ n=11866 基準率=0.5999 帯=(0.58, 0.68)
   連鎖内のプリントで判断が変わる件数: 476 / 11866
```

合わせて **1,158 / 20,797 = 5.6%** のプリントで 3 択の判断が変わる。向きは一方向で、
今の帯(下端 0.42 / 0.60)は連鎖内だけで作った帯(0.38 / 0.58)より下端が高いので、
**「止まる」= 逆張りに寄る**。2 値(基準率)の閾値も 0.4415 → 0.4343、0.6229 → 0.5999 と動く。

**なぜ段2 の前に直す必要があるか**: 帯と基準率は段1 で凍結し、`run_stage2` が
`summary.json` から読むだけの入力である(1393〜1396 行)。後半を一度通したあとにこの母集団を
直すと、段2 をもう一度走らせることになる。

**対応**: (a) 帯・基準率を「連鎖に入るプリントだけ」で作り直すか、(b) 今の母集団のまま進めるなら、
設計と報告に「帯は方策が通らない 1,041 件(値段のラベル割合 0.9232)を含む母集団で作った」と
書き、上の 5.6% を併記すること。どちらを選ぶかはリード / オーナーの判断だが、**黙って進めない。**
併せて `cascades_from_prints` に「落とした行数」を出す 1 行を足すこと(今は無言で落ちる)。

---

### 致命-3: ②opt の出口(最後 + 300 秒)は建玉の重なりと保有時間を増やすが、段2 の出力には保有秒も建玉の時刻も残らないので、露出を揃えた読みが後からできない

報告 (4)-2 の機構 4(500 行目)の逐語:

> 「**出口を「連鎖の終わり」から「最後のプリント + 300 秒」に伸ばすと取り分が増える**(+1,925 → +4,609 bp)」

この 2 つは**露出が違う**。入る位置を 3 件目以降に固定し、出口だけを変えて建玉の時間を数え直した:

```
$ python3 /tmp/.../chk_overlap.py
素(1件目から・連鎖の終わり): 本数 8931 / 同時建玉の最大 1 / 合計保有 187.4 時間
選んだ組(3件目以降・最後+300秒): 本数 2284 / 同時建玉の最大 4 / 合計保有 204.5 時間
3件目以降・連鎖の終わり: 本数 2284 / 同時建玉の最大 1 / 合計保有 52.2 時間
```

- 総収支は 1,925 → 4,609 bp(2.4 倍)だが、合計保有時間は 52.2 → 204.5 時間(3.9 倍)。
  **保有 1 時間あたりでは 36.9 → 22.5 bp/h** と逆向きに動く。
- 出口を伸ばすと同時建玉が最大 4 本になる(素と「3 件目以降・連鎖の終わり」は最大 1 本)。
  設計 §用語の「想定元本は連鎖 1 本 = 1 単位」で総収支を足すと、**1 単位の資本では取れない収支**を
  足していることになる。同じ側だけでも最大 4 本(SELL)重なる。
- Q4 は ①(出口 = 連鎖の終わり、1 本あたり平均 75.6 秒)と ②opt(同 322 秒)を同じ表に並べる。
  **露出を揃える列が設計にも実装にも無い。**

`run_stage2` の `opt_rows`(1414〜1435 行)は `bundle_id`・`day`・`pnl_bp`・`出口の理由` の 4 列だけで、
`simulate_reverse_entry` が返す `保有秒`・`入りの約定時刻`・`出の約定時刻` を捨てている
(`scripts/o3c_signal_value.py` 1431〜1434 行)。① 側も連鎖ごとの行が出ない(致命-1)。
したがって後半を通したあとに「保有時間あたり」「同時建玉の本数」で読み直すことができない。

**対応**: 段2 の ②opt の行に `保有秒`・`入りの約定時刻`・`出の約定時刻`・`建玉の回数` を足し、
① 側も連鎖ごとの行(致命-1 の (i))に `保有秒` を含める。Q4 の表に「合計保有時間」と
「保有 1 時間あたりの収支」を足すか、少なくとも段2 の報告で露出が 4 倍違うことを書くこと。

---

## 直すべき

### D-1: 実効スプレッド c が符号付きの中央値で、報告 (8) の決定 9「負の対は費用の過小評価には働かない」は実測と食い違う

報告 (5)-9(576 行目)と (8) の決定 9(705 行目)の逐語:

> 「9 | 符号付きのまま採用。ただし c は正の中央値(1.787)で、**負の対は相場が動いている瞬間の逆の対 = 費用の過小評価には働かない**(p75 3.717 を併記する理由)」

`scripts/o3c_bitflyer_spread.py` 121〜127 行は `(p_buy - p_sell) / mid * 1e4` を符号付きで返し、
207〜208 行がその**符号付きの分布の中央値**を c にしている。負の対を含めれば中央値は下がる:

```
$ python3 /tmp/.../chk_spread.py
清算直後・直前だけ(符号付き、実装) n= 3489 p25/p50/p75/p90= [0.3744, 1.7869, 3.7171, 5.9828] 負の割合= 0.1161
清算直後・直前だけ(絶対値)         n= 3489 p25/p50/p75/p90= [0.64, 1.9176, 3.7996, 6.1184] 負の割合= 0.0
清算直後・正の対だけ               n= 2994 p25/p50/p75/p90= [0.934, 2.2574, 4.1445, 6.3612] 負の割合= 0.0
```

c は 1.7869(実装)/ 1.9176(絶対値)/ 2.2574(正の対だけ)。**負の対を含めることで中央値は 0.13 bp
(7.3%)下がっている**ので、「過小評価には働かない」は成り立っていない。板を叩いて払う費用は
向きによらず正なので、どの定義を採るかは選択だが、**選択の理由を実測と食い違わない文にすること。**
(なお実装値 1.7869239… は報告・`summary.json` と完全に一致した = 再計算 1。)

### D-2: 「清算直後」の窓が直前の 1 件の清算しか見ておらず、窓に入る対の 26% を落としている

`mask_post_liquidation`(`scripts/o3c_bitflyer_spread.py` 130〜144 行)は
`searchsorted(..., "right") - 1` で**直前の清算 1 件だけ**を見る。連鎖のように清算が 1 秒以内に
続くと、前の清算の [t₀+1, t₀+5] に入る対でも、より新しい清算からの差が 1 秒未満だと落ちる。
設計 Q5 の文は「Binance の清算直後(t₀ + 1〜5 秒)」で、どれかの清算の窓に入るかを言っている。

```
$ python3 /tmp/.../chk_spread.py（日ごとの行、抜粋）
2023-08-01: 対 3393 / 直前だけ 200 / どれかの窓 282
2024-05-01: 対 15119 / 直前だけ 854 / どれかの窓 1204
清算直後・直前だけ(符号付き、実装)   n= 3489 p50= 1.7869 p75= 3.7171
清算直後・どれかの窓(符号付き)       n= 4712 p50= 1.8497 p75= 3.8334
```

落ちている 1,223 対(26%)は「より新しい清算から 1 秒未満」の対、つまり清算の直後の最も荒い瞬間で、
拾うと c は 1.7869 → 1.8497 に上がる。大きさは 0.06 bp と小さいが、**向きは費用を小さく見せる側**である。
併せて、bitFlyer 側は取引所時刻(報告 (5)-8)、清算側は Binance の時刻で、両者の時計合わせは
**未測定**である(1〜5 秒の窓はこのずれと同じ桁になり得る)。

### D-3: 「遅れ d より短い到達は取れない」が実測と食い違う(到達 < 1 秒の 78.4% は、遅れ 1 秒の約定価格からなお 5 bp 以上動く)

報告 277 行目の逐語:

> 「遅れ d = 1 秒では、値段のラベルが 1 のプリントのうち **21.3%** は約定する前に 5 bp に達している
> (連鎖の中では 27.1%)。設計 §Q1 の「**遅れ d より短い到達は取れない**」はこの割合。」

到達が 1 秒より短いプリントについて、**遅れ 1 秒の約定価格から** t₀+60 秒までの最大順行を測った:

```
$ python3 /tmp/.../chk_ttt.py
値段のラベル=1 のプリント 11958
到達 < 1 秒: n=2546 (21.3%) / 遅れ 1 秒の約定価格から t0+60 秒までの最大順行 bp:
             p25=6.53 中央値=20.43 p75=53.65 / なお 5 bp 以上取れる割合=0.784
到達 >= 1 秒: n=9412 (78.7%) / 同 p25=8.44 中央値=13.64 p75=24.30 / なお 5 bp 以上取れる割合=0.963
```

21.3% は報告の値をそのまま再現した。しかしその 2,546 件のうち **78.4%** は、1 秒遅れて約定した
値段からでもまだ 5 bp 以上順行する(中央値 20.4 bp)。**「到達が d より早い」= 「取れない」ではない。**
損益の模擬は最初から ts + d の約定価格で測っているので数値に誤りはないが、
この一文を段2 の「なぜ」に持ち込むと読み違える。設計 §Q1 の括弧書きと報告 277 行を直すこと。

### D-4: 格子 189 組のうち 18 組は構造的に空(材料 12 が 1 件目で全欠測)。分位を切った母集団の記述も実際と違う

`condition_cuts`(466〜475 行)は前半のプリント全件から分位を取るが、材料 12 は 1 件目では全欠測である:

```
$ python3 -c "各材料の有限値の数と分位"
材料12 mat12_notional_over_max_recent_print n= 12769 p75= 1.98342 p90= 11.341327
材料14 mat14_trade_count_60s               n= 21838 p75= 965.0   p90= 1988.3
材料15 mat15_burst_ratio_10s_over_60s      n= 21825 p75= 0.711458 p90= 0.932031
```

材料 12 の有限値は 12,769 件 = 連鎖の中の件数ちょうど。したがって「1 件目から × 材料 12」の組は
入りのプリントが必ず欠測で、1 本も入らない:

```
$ python3 -c "grid.csv の 入った本数 == 0 を数える"
入った本数 0 の組 18 / 189
     入る位置      入る条件        出口 損切り
9   1件目から  材料12≥p75  連鎖の終わり+d  なし
18  1件目から  材料12≥p90  連鎖の終わり+d  なし
中央値_入った本のみ が NaN の組 18
```

(2 条件 × 出口 3 × 損切り 3 = 18 組。`入った本数 0` / `総収支 0` / 中央値 NaN。)

- 報告 Q3(281 行目)の「格子 189 組すべてが `grid.csv`。**欠測 … は全 189 組で 0**」は、
  「欠測」列(価格が引けなかった連鎖)としては正しいが、**18 組が 1 本も入っていないこと**を書いていない。
  実際に測れた組は 171 組である。多重性 189 は安全側なので算入はそのままでよいが、
  「189 組を測った」とは書かないこと。
- 報告 (5)-5(566 行目)「**前半のプリント全件(21,838 件)で切った**」は材料 12 について正しくない
  (12,769 件で切っている)。材料 15 も 21,825 件。

### D-5: 約定の遅れが出力に残っていない。Q5b(a) の bitFlyer 側は中央値 1.29 秒・p90 11.9 秒・最大 254.6 秒遅れて約定しており、「経路差」に約定時刻の差が混ざる

`price_at_or_after` の許容は `STALENESS_MS = 300_000`(300 秒)である。報告は繰り返し「欠測 0」と
書くが、300 秒の許容の下での欠測 0 は「約定が近くにある」ことを意味しない。標本日 16 日に掛かる
前半 285 本について、入り(1 件目 + 1 秒)と出(最後 + 60 秒 + 1 秒)の目標時刻と実際の約定時刻の差を測った:

```
$ python3 /tmp/.../chk_lag.py
標本日に掛かる前半の連鎖 285
Binance : n=570 約定の遅れ(ms) p50=440  p75=1167 p90=2770  p99=8908  最大=27887  / 1 秒超=0.277 / 10 秒超=0.005
bitFlyer: n=564 約定の遅れ(ms) p50=1290 p75=4890 p90=11870 p99=43977 最大=254630 / 1 秒超=0.560 / 10 秒超=0.121
```

- Q5b(a) は「入る・出る・損切りの約定を bitFlyer の約定の `at_or_after` に置き換えた損益」を
  Binance の損益と対にするが、bitFlyer 側は**中央値で 1.29 秒、10 本に 1 本は 11.9 秒以上**遅れて
  約定している。対差は「場所の差」と「約定時刻の差」の和で、分けられていない。
  報告 (5) の限界に「経路差は Q5b で下見しただけ」とはあるが、**この遅れの分布は報告に無い。**
- 主の損益(Binance)も、名目の遅れ 1 秒に加えて中央値 0.44 秒の約定待ちがある。
  遅れ 0.5 / 1 / 2 秒の比較はこの分だけ潰れる。
- 段2 の前に、① と ②opt の連鎖ごとの行に「入りの約定時刻 − 目標時刻」を足すか、
  少なくとも段1 の数字を報告に載せること(致命-1 の (i) と同じ 1 列で済む)。

### D-6: 段2 の 2 つの穴 — materials に print_id が無いときに無言で通る / 日ごとの約定のキャッシュが解放されない

(a) 段1 の `load_value_probs_for_cascades`(828 行)は materials に無い `print_id` があれば止まる:

```
        raise RuntimeError(f"[止め] materials に無い print_id: {sorted(missing)[:5]}")
```

`run_stage2`(1405〜1408 行)は同じ絞り込みを自分で書いていて、**この検査が無い**。
materials に無いプリントがあると確率が引けず、`judge_3way_with_band` が「わからない」を返すので、
一度きりの後半でその連鎖は静かに「入らない」に落ちる。段1 と同じ停止を入れること。

(b) `js.StateBuilder._trades_for_day`(`scripts/o3c_jev_state.py` 622〜627 行)の `_window_cache` は
上限も追い出しも無い。段1 は `front_half_day_pass` が日ごとに `clear()` しているが、
`run_stage2` は `compute_value_logit_probs(sb, df)` を 1 回呼ぶだけで解放しない。後半 228 日ぶんが
そのまま残る:

```
$ python3 -c "StateBuilder._trades_for_day の配列の大きさ"
2023-07-01 約定 51108 配列 0.8 MB
2023-10-24 約定 474234 配列 7.6 MB
2024-01-11 約定 454753 配列 7.3 MB
3 日平均 5.2 MB → 228 日分をキャッシュすると 1.2 GB
```

`_raw_trade_cache` も同様に溜まるので実際はこれより大きい。一度きりの実行が記憶不足で落ちると
やり直しが 2 度目の読みになるので、段2 でも日ごとに `clear()` すること。

### D-7: Q5b(a) の ① 3 択 A の「どちらも 0 の連鎖」は 25 本(8.9%)。報告の「未測定」を埋めた

報告 371 行目の逐語:

> 「この行の対差の中央値 0.000 には、その「どちらも 0」の連鎖が混ざっている
> (281 本のうち何本かは数えていない = **未測定**)。」

数えた:

```
$ python3 /tmp/.../chk_q5b.py
①3択A の対 281 本 / うち両方とも入らない(対差が構造的に 0)= 25 本 (8.9%) / 入った対 256 本
```

25 本は「わからない」だけで終わった連鎖で、bitFlyer 側も Binance 側も損益 0 = 対差 0。
中央値 0.000 はこの 25 本だけでは決まらない(256 本が残る)が、**報告に 25 / 281 と書けば
「未測定」を残さずに済む。** 併せて `paired_diff_row`(1259 行付近)の「符号が変わった本数」は
`np.sign(0) != np.sign(x)` も 1 と数えるので、0 を含む行では意味が変わる点を注記すること。

---

## 示唆

### S-1: 5 分割 OOF が行の無作為分割で、同じ日・同じ連鎖のプリントが訓練側と試験側に跨る。順位分離はほぼ動かないが、帯は 1 段動く

`lg.kfold_indices`(`scripts/o3c_signal_logit.py` 174〜178 行)は行番号を混ぜて 5 等分する。
連鎖の中のプリントは数十秒の間隔で並び、材料もラベルの窓も重なるので、同じ連鎖の別のプリントが
訓練側に入る。日ブロックで切り直して測った(連鎖の中。材料に N2 が要らないので独立に走る):

```
$ python3 /tmp/.../chk_chain.py
順位分離 0.6708 基準率 0.6229 的中 0.652
帯 (0.6, 0.68) 基準率 0.6229
日ブロック5分割 順位分離 0.6697
日ブロックの帯 (0.58, 0.66)
```

順位分離は 0.6708 → 0.6697(0.0011)で、跨ぎの効きは小さい。**ただし帯は (0.60, 0.68) → (0.58, 0.66) と
1 段(0.02)動く。** 帯は段2 に凍結して持ち込む量なので、致命-2 と合わせて「帯は較正の作り方に
0.02 の幅で動く」ことを段2 の報告に書くとよい。

### S-2: 連鎖の中で「止まる」に区分された帯のうち 5 帯・2,567 件(20.1%)は、実際の続く割合が 0.5 を超えている

`calib_chain.csv` の 0.50–0.60 の 5 帯:

| 帯 | 件数 | 続く割合 | 区分 |
|---|---|---|---|
| 0.50–0.52 | 491 | 0.5784 | 止まる |
| 0.52–0.54 | 552 | 0.5344 | 止まる |
| 0.54–0.56 | 541 | 0.5416 | 止まる |
| 0.56–0.58 | 503 | 0.5507 | 止まる |
| 0.58–0.60 | 480 | 0.5667 | 止まる |

合計 2,567 件 = 連鎖の中 12,769 件の 20.1%(`chk_pop.py` の出力
「「止まる」の帯のうち 続く割合>0.5 の帯: 5 帯 / 件数 2567」)。
帯は**基準率(0.6229)との差**で切るので、基準率が 0.5 から離れているほど、
「止まる」と区分された帯でも絶対的には続く方が多くなる。一方で行動(逆張り)の損益は
**絶対的な向き**で決まる。設計は 2 値 0.5 の方策を並べているのでこの違いは測れる形にはなっているが、
Q6 の「なぜ」でこの点を明示しないと「3 択が 2 値 0.5 に負けた / 勝った」の理由を取り違える。
1 件目(基準率 0.4415)では同じ帯は 0 件だった。

### S-3: 「完全な判断 = 上限」は前段の用語表から引き継がれているが、この単位の実装は正しく「上限ではない」と書いている

設計 §用語の 10 行目は「連鎖 / 判断 / 行動 / 型 A・B / 完全な判断 | 前段 `SIGNAL_POLICY_DESIGN` の
用語表と同じ」とだけ書いており、その用語表は「完全な判断 … = 上限」である
(反証者レビュー8 致命-2 が、連鎖単位では他方策を下回る本が 9〜35 / 200 あることを実測している)。
`scripts/o3c_signal_value.py` の `judge_perfect_value`(356〜362 行)の docstring は
「事後の**値段のラベル**をそのまま判断に使う参照点(**損益の上限ではない**)」と正しく書いている。
段2 の報告で「上限」の語を使わないこと(用語表を引き継ぐなら注記を付ける)。

### S-4: 費用の窓 [t₀+1 秒, t₀+5 秒] は遅れ 1 秒に合わせた 1 通りで、遅れ 0.5 / 2 秒にも同じ c を使う

`POST_LIQ_LO_MS = 1_000`(`o3c_bitflyer_spread.py` 66 行)。設計 Q5 もそう書いている。
遅れ 3 通りを対等に扱う設計(§Q2)に対して費用は 1 通りなので、遅れの効きを読むときに
「費用は遅れに依らない」という仮定が入っていることを報告に書くとよい。

### S-5: 段1 で確かめられたもの(問題なし)

- **① の当てはめは独立に完全再現できた。** N2 を素の `StateBuilder`(窓は前 400 秒・後ろ 0)で
  計算し直しても、道具が差し込む窓(前後 400 秒)で計算した係数と**最大差 0.0** で一致した
  (道具のコメント「後ろに伸びた分は結果に入らない」が実測で裏付いた)。
- **`dist_table.csv` は 200 本を一から回して全セル一致**(費用なし・費用あり(主の c)とも 1,260 / 1,260)。
- **格子・選ぶ規則・分位・費用の c・行数・MD5** がすべて一致(下の一覧)。
- 報告 (4)-2 の機構 5 が引く清算のラベルの数(連鎖の中 基準率 0.6875 / 順位分離 0.6884)は、
  `data/jev/v4/logit_firsthalf.md` がこの実行環境に無いので出所は確かめられないが、
  **同じ前半・同じ材料で当てはめ直して同じ値が出た**(再計算 8)。
- 費用 = c × 建玉の回数、ドテン 1 回 = c は設計どおり(`apply_cost` 921〜926 行、
  `simulate_cascade` の `n_entries` がドテンで +1、試験
  `test_cost_is_c_times_number_of_entries_and_a_flip_costs_one_c`)。
- ② の未来の不使用: 判断・条件は入りのプリントの材料(ts 以前)、入り・出・損切りの約定は
  すべて `price_at_or_after(… + delay_ms)`(`simulate_reverse_entry` 487〜530 行)。
  出口「最後のプリント + 300 秒」は、連鎖の終わり(最後 + 60 秒)を過ぎて初めて確定する時刻より
  後なので、実時間で決められる。「3 件目以降」も、その 3 件目の時点で数えられる。
- `stage1` の前半だけの門(`assert_front_half_only` / `require_front_half`、152〜170 行)と、
  段2 が走っていないこと(`stage2_secondhalf/` が無い)を確認した。

### S-6: このレビューが見ていない範囲

- 材料(`cand_*`)の生成そのもの(`o3c_signal_materials.py` / `o3c_jev_state.py`)は再監査していない。
  N2 だけ、素の `StateBuilder` で計算し直して当てはめが一致することを確かめた。
- 後半の行・後半の出力・`paper_logs/` は一度も開いていない。したがって後半にも
  `bundle_id` 欠損のプリントがどれだけあるか(致命-2 の後半側)は**未測定**である。
- `run_stage2` は走っていないので、実装レベルの検査は読み合わせだけ(致命-1・D-6)。
- `data/jev/v4/logit_firsthalf.md` はこの環境に無い(`ls data/jev` に v4 が無い)。報告 (4)-2 の
  機構 5 が引く数はそこからの引用で、出所そのものは**未確認**(値は再計算で一致した)。

---

## 再計算の一覧(すべて自分で打ち、出力は本文に貼った)

| # | 何を | コマンド(要旨) | 結果 |
|---|---|---|---|
| 1 | 費用 c と分位 | `chk_spread.py`(16 標本日の tardis を読み直し、対を作り直す) | 清算直後プール n=3,489 / p50 **1.7869** / p75 **3.7171** → 報告・`summary.json` と一致。日ごとの対の数も 16 日すべて一致 |
| 2 | 費用の定義の感度 | 同上 | 絶対値 p50 1.9176 / 正の対だけ 2.2574 / どれかの窓 1.8497(n=4,712)→ D-1・D-2 |
| 3 | ① 連鎖の中の当てはめ | `chk_chain.py`(`build_scene_frames` → `run_scene`) | 件数 12,769 / 基準率 **0.6229** / 順位分離 **0.6708** / 的中 0.652 / 帯 **(0.60, 0.68)** → 報告と一致 |
| 4 | ① 1 件目の当てはめ(N2 を独立に計算) | `chk_first.py`(素の `StateBuilder` で N2 → `run_scene`) | 件数 9,069 / 基準率 **0.4415** / 順位分離 **0.6689** / 的中 0.6314 / 適合率 0.680(1,642)・0.805(256)・0.571(7) / 帯 **(0.42, 0.52)** / 保存された係数との最大差 **0.0** → 報告と一致 |
| 5 | 帯の母集団 | `chk_pop.py` | 連鎖内だけで較正すると 1 件目 0.4343・(0.38, 0.52)、連鎖の中 0.5999・(0.58, 0.68)。判断が変わるプリント 682 + 476 = 1,158 / 20,797 → 致命-2 |
| 6 | 日ブロック 5 分割 | `chk_chain.py` | 順位分離 0.6697 / 帯 (0.58, 0.66) → S-1 |
| 7 | 前半 200 本の `dist_table` | `chk_dist.py`(200 本を抜き直し、確率・判断・状態機械を一から回す) | cascade_rows 8,400 / 欠測 0。`dist_table.csv` と **費用なし 1,260 / 1,260 セル一致、費用あり(主の c) 1,260 / 1,260 セル一致**。200 本の内訳 単発 122 / 2 件 33 / 3 件以上 45 も一致 |
| 8 | 清算のラベルの当てはめ(機構 5 の引用値) | `run_scene(label_col="label_60")`(連鎖の中) | 基準率 **0.6875** / 順位分離 **0.6884** → 報告 (4)-2 機構 5 と一致 |
| 9 | ② の格子と選ぶ規則 | `grid.csv` に (a)(b) を当て直す | 規則を満たす組 **30 / 189**、最良は「3 件目以降 / なし / 最後+300 秒 / なし」総収支 **4,609.355648**、前 2,454.20 / 後 2,155.15、入った本 2,284。0 含みでは **0 / 189**。欠測の合計 0 → 報告と一致 |
| 10 | 格子の空の組 | `grid.csv` の 入った本数 == 0 | **18 / 189**(1 件目から × 材料 12)→ D-4 |
| 11 | 入る条件の分位 | 前半のプリントから p75 / p90 を取り直す | 材料12 1.98342 / 11.341327(有限値 12,769)、材料14 965.0 / 1988.3(21,838)、材料15 0.711458 / 0.932031(21,825)→ `summary.json` と一致。母集団の記述だけ食い違う(D-4) |
| 12 | 建玉の重なりと保有時間 | `chk_overlap.py` | 素 最大 1 本・187.4 時間 / 選んだ組 最大 4 本・204.5 時間 / 3 件目以降+連鎖の終わり 最大 1 本・52.2 時間 → 致命-3 |
| 13 | 約定の遅れ | `chk_lag.py` | Binance p50 440 ms / p90 2,770 ms、bitFlyer p50 1,290 ms / p90 11,870 ms / 最大 254,630 ms → D-5 |
| 14 | 5 bp 到達後の残り | `chk_ttt.py` | 到達 < 1 秒 2,546 件(**21.3%**、報告と一致)。そのうち遅れ 1 秒の約定価格からなお 5 bp 以上動くのは **78.4%**(中央値 20.43 bp)→ D-3 |
| 15 | Q5b の「どちらも 0」 | `chk_q5b.py` | ① 3 択 A の対 281 本 / 両方とも入らない **25 本(8.9%)** → D-7 |
| 16 | 束の外のプリント | `load_stage1_prints` と `cascades_from_prints` を突き合わせ | 前半 21,838 件のうち `bundle_id` 欠損 **1,041 件**、連鎖に入るのは 20,797 件。束の外の値段ラベル割合 **0.9232** → 致命-2 |
| 17 | 行数・数値セル数・MD5 | 出力を数え直し、`MD5SUMS` と入力の MD5 を照合 | 12 表の合計 **783**(内訳も報告と一致)、数値セル数 7,482、`MD5SUMS` 全件一致、入力 3 本の MD5 も一致 |

使った道具は `/tmp/claude-0/.../scratchpad/` に置いた(リポジトリには入れていない)。
出力ファイル・コード・設定はいずれも書き換えていない(17 の MD5 照合が全件一致していることが裏付け)。

---

# 再点検(直しの後)2026-09-21

見たもの: 報告 `VALUE_STAGE1_REPORT_2026-09-21.md` の **(9) 反証者 9 への処置**、
`git diff HEAD -- scripts tests`(`o3c_signal_value.py` +977 / `o3c_bitflyer_spread.py` +59 /
`tests/test_o3c_signal_value.py` +410)、出力
`backtest_data/o3c_signal_value_20260921/`(`stage1_firsthalf/` の新しい `grid.csv`・
`cascades.csv.gz`・`exposure.csv`、`spread/`、`stage2_dryrun/`)。

守った制約は前回と同じ: **`half == "後半"` の行も後半の出力も開いていない**
(`backtest_data/o3c_signal_policy_20260920/stage2_secondhalf/cascades.csv.gz` は
`Path.exists()` で在ることだけを見た。中身は読んでいない)。`paper_logs/` は開いていない。
Jev は呼んでいない。判定語は使っていない。コード・出力は書き換えていない
(3 つの `MD5SUMS` を最後に照合し、全件一致)。モデル名は書いていない。コミット・押し出しはしていない。

---

## (1) 致命 1〜3・D-1〜D-7 の処置を 1 件ずつ

### 致命-1(段2 が設計 Q0・Q1 併記・Q2・Q4・Q5b を出さない / 連鎖 1 本ごとの行が無い)→ **大部分は確かめた。Q5b(a) だけ確かめられない(下の「残る致命」)**

**確かめた**: 段1 に `cascades.csv.gz` が出た(18 列・**65,697 行**)。内訳も合う:

```
$ python3 -c "cascades.csv.gz の方策ごとの行数"
方策
logistic_3択 / logistic_2値0.5 / logistic_2値基準率 / 規則 / 完全な判断 / 全部逆張り / 全部順張り  各 3600
素(1件目から・連鎖の終わり・損切りなし) 26793
3件目以降だけ・連鎖の終わり・損切りなし / ②opt(選んだ組)  各 6852
入った行 21731 / うち 入りの約定時刻_ms が空 0 / 出の約定時刻_ms が空 0
```

(7 方策 × 200 本 × 3 遅れ × 2 型 × 3 費用 = 25,200、8,931 × 3 = 26,793、2,284 × 3 = 6,852 が 2 通り。
合計 65,697。)

**この行だけから `dist_table.csv` を組み直せることを確かめた**(監査可能性の要):

```
$ python3 -c "cascades.csv.gz → sp.build_dist_table → dist_table.csv と突き合わせ"
数の列 13 1e-6 以内で一致 3276 / 3276  最大差 7.500000007709673e-07
文字の列 ['費用', '方策', '型'] 一致 756 / 756
```

(食い違いは CSV の 6 桁丸めだけ。)さらに **200 本を一から回し直した**結果も表と完全一致:

```
$ python3 /tmp/.../chk2_dist.py
一から回した dist_table の一致セル 4032 / 4032 (67s)
judge_counts 一致 True
```

段2 は `--dry-run`(合成データ 2 本)で全部の表が出ることを確かめた:

```
$ ls backtest_data/o3c_signal_value_20260921/stage2_dryrun/
q0_selfcheck.csv calib_5000.csv cascades.csv.gz dist_table.csv judge_counts.csv
action_counts.csv position_breakdown.csv q4_main.csv q4_pair_diff.csv q4_by_day.csv
q4_by_side.csv q4_by_size.csv q4_by_position.csv q4_exposure.csv q5b_secs_secondhalf.csv
summary.json tables.md MD5SUMS
行数の内訳: dist_table 288 / position_breakdown 144 / q4_main 6 / q4_pair_diff 3 /
            q4_by_day 6 / q4_by_side 11 / q4_by_size 11 / q4_by_position 6 / q4_exposure 6
dist_table の方策 = 9 本(値段 7 + 前段の3択(清算) + 完全な判断(清算))
```

`q4_main.csv` は設計 Q4 の 6 本(① 3 択 A / ① 3 択 B / ②opt / 全部逆張り(素)/
前段の 3 択(清算)A / 完全な判断(値段)A)が並び、`q4_pair_diff.csv` は 3 本の対差を出した。
Q0 の `bundle_id` の MD5 照合は `run_stage2`(2046〜2064 行)にあり、一致しなければ例外で止まる。
前段の出力は在る(`Path.exists()` = True。中身は見ていない)。

**確かめられない**: Q1 併記(`calib_5000.csv`)と Q5b(`q5b_secs_secondhalf.csv`)は
dry-run で **0 バイト**(この 2 つのブロックは `if not dry_run:` の中で、合成では 1 行も通らない)。
試験にもこの 2 ブロックを通すものは無い。**段2 で初めて走る経路が 2 つ残っている。**
加えて Q5b(a) の母集団が設計と違う(下の「残る致命」)。

### 致命-2(帯・基準率の母集団に 1,041 件が混ざる)→ **確かめた**

母集団・基準率・順位分離・帯・係数を**独立に計算し直して全部一致した**
(N2 は素の `StateBuilder` で計算し直し、日ブロック分割は docstring から自分で書き直した):

```
$ python3 /tmp/.../chk2_fit.py
母集団: 1件目 8931 連鎖の中 11866 束の外 1041
束の外 値段ラベル割合 0.923151 日数 31
[1件目]   n=8931  基準率=0.434330 順位分離=0.6608 的中=0.6284 帯=(0.38, 0.5)  わからない=2189 (24.510%) 分割の日数=[46,46,46,45,45]
   保存された係数との最大差 0.0 / 母集団の記載: bundle_id のあるプリントだけ(束の外は入れない) / 分割: 日でブロックした 5 分割
[連鎖の中] n=11866 基準率=0.599865 順位分離=0.6466 的中=0.6307 帯=(0.56, 0.66) わからない=2830 (23.850%) 分割の日数=[45,45,44,44,44]
   保存された係数との最大差 0.0
```

`cascades_from_prints` の落とした行数の表示も出る(このレビューの実行ログに
`[束] cascades_from_prints: 入力 21838 行のうち bundle_id 欠損 1041 行を落とした
(連鎖に入るのは 20797 行、連鎖 8931 本)` が出た)。Q0 に束の外の件数・ラベル割合・
想定元本・日数が載った。

(9) が新しく書いた「束の外はどれも束から 60 秒以上離れた孤立したプリント」も**実測で一致**した:

```
$ python3 -c "束の外の各プリントから最も近い同じ側の束のプリントまでの差"
SELL 束の外 607 最小 60854 ms 中央値 202088 ms
BUY  束の外 434 最小 61159 ms 中央値 318426 ms
全体 最小 60854 ms / 60 秒以内の件数 0
2023-10-16: その日のプリント 258 うち束の外 137 / 束の外の時刻帯 13:21:58 - 13:54:26
```

### 致命-3(②opt の露出が残らない)→ **確かめた**

```
$ python3 -c "exposure.csv"
                      通り  入った本数        総収支_bp   合計保有時間_時間  保有1時間あたりの収支_bp/h  同時建玉の最大_本       平均保有秒
0  素(1件目から・連鎖の終わり・損切りなし)   8931 -13086.907342  188.553054        -69.407029          1   76.003918
1   3件目以降だけ・連鎖の終わり・損切りなし   2284   1429.216332   52.497250         27.224594          1   82.745227
2             ②opt(選んだ組)   2284   4113.496964  204.956053         20.070141          4  323.048069
```

前回このレビューが自分で数えた値(52.2 h / 204.5 h / 同時建玉 1 本・4 本)と同じ向き・同じ桁で、
差は約定時刻を使うか名目の時刻を使うかの分だけである。`grid.csv` に露出の 3 列
(`合計保有時間_時間` / `保有1時間あたりの収支_bp/h` / `同時建玉の最大_本`)、`knob_contrib.csv` にも
同じ 3 列、`cascades.csv.gz` に `保有秒`・`入りの約定時刻_ms`・`出の約定時刻_ms` が入った。
段2 の `q4_exposure.csv` も dry-run で出た。**選ぶ規則は変えていない**(露出は出すだけ)ことも
`select_combo`(diff に変更なし)で確かめた。

### D-1(c が符号付きの中央値)→ **確かめた**

```
$ python3 /tmp/.../chk2_spread.py
清算直後の対(プール) 4712
絶対値 p25/p50/p75/p90 = [0.6997, 2.004, 3.9228, 6.3649]
参考 符号付き p50 = 1.8497  正の対だけ p50 = 2.3186
負の対の割合 0.114
```

`summary.json` の `主のc_清算直後プールの絶対値の中央値_bp = 2.004024923346042` /
`併記_..._p75 = 3.922816` / 参考 2 列も同じ。報告 (8) の決定 9 は (9) で取り下げられている。

### D-2(清算直後の窓が直前 1 件だけ)→ **確かめた**

`mask_post_liquidation` を自分で別の式で書き直し(`searchsorted` を使わず 1 対ずつ判定)、
**16 標本日すべてで実装と完全一致**した(`assert` が全日通った)。対の数は 3,489 → **4,712**。

### D-3(「遅れ d より短い到達は取れない」)→ **確かめた**

```
$ python3 -c "time_to_target.csv"
全体   到達1秒未満の件数 2546 / なお5bp以上の割合 0.783975 / 最大順行の中央値 20.425372
1件目  389  / 0.668380 / 10.929733
連鎖の中 2157 / 0.804822 / 22.193221
到達1秒以上: 全体 9412 / 0.963238 / 13.643459
```

前回このレビューが独立に測った 2,546 件・78.4%・中央値 20.43 bp と一致。報告 320 行で
当該の一文が取り下げられていることも確かめた。

### D-4(18 組が構造的に空 / 分位の母集団)→ **確かめた**

```
$ python3 -c "grid.csv"
空の組 18 | 空の理由の例: 構造的に空(材料12≥p75 の材料 `mat12_notional_over_max_recent_print` が
「1件目から」の入りのプリントで全欠測。届く連鎖 8931 本すべて)
Q0: 入る条件の分位を切った件数(材料12) 11866/20797 /(材料14) 20797/20797 /(材料15) 20784/20797
Q0: 格子で 1 本も入らなかった組 18/189
```

分位の母集団が帯と同じ 20,797 件に揃い、材料ごとの有限値の件数が Q0 に出た。

### D-5(約定の遅れ)→ **確かめた**

`q5b_secs_firsthalf.csv` が 5 行 → **12 行**になり、約定の遅れの分位 4 行と
「bitFlyer の遅れ ≤ 5 秒の対だけ」3 行が加わった:

```
約定の遅れ(目標時刻からの差、全部逆張り(素)):
  Binance_入り  n=281 p50=301  p75=830  p90=1848 p99=7500  最大=9556   1秒超=0.185 5秒超=0.043
  Binance_出    n=281 p50=622  p75=1507 p90=3308 p99=9980  最大=27887  1秒超=0.374 5秒超=0.046
  bitFlyer_入り n=281 p50=821  p75=2977 p90=7422 p99=37751 最大=75365  1秒超=0.445 5秒超=0.174
遅れ ≤ 5 秒の対だけ: 全部逆張り(素) 281→172 対(対差の中央値 −1.111 → −1.239)、
                     ①3択A 282→173 対、②opt 77→56 対
```

前回このレビューが測った値(Binance 入り+出をまとめて p50 440 ms、bitFlyer p50 1,290 ms)と
同じ水準である。

### D-6(段2 の materials の検査 / 日ごとのキャッシュ)→ **(a) 確かめた / (b) 一部だけ**

(a) `require_materials_cover`(1057〜1065 行)が段1・段2 の両方から呼ばれている
(`run_stage2` 2069 行)。試験 `test_require_materials_cover_stops_on_a_missing_print_id` あり。

(b) `add_n2_by_day` が日ごとに `_window_cache` / `_raw_trade_cache` を空にし、
`compute_value_probs_for_frame` が段2 でそれを使う。**ただし参照 2 本の確率を引く
`sp.stage2.compute_logit_probs`(`scripts/o3c_signal_stage2.py` 108〜112 行)は
`lg.add_n2_column(sb, df_first)` を 1 回で呼ぶので、その呼び出しの中では日ごとの解放が効かない**
(前後で `clear()` しているだけ)。下の (3) N-3 に書く。

### D-7(Q5b の「どちらも 0」)→ **確かめた**

`q5b_secs_firsthalf.csv` に `どちらも0の本数` 列が付き、①3択A = **50 / 282**。
独立に回し直して一致した:

```
$ python3 /tmp/.../chk2_q5b.py
帯 {'1件目': (0.38, 0.5), '連鎖の中': (0.56, 0.66)} 基準率 {'1件目': 0.4343..., '連鎖の中': 0.5998...}
①3択A の対 282 / どちらも0 50
```

(直す前の 25 / 281 から増えたのは帯が広がって「わからない」が増えたためで、報告 (9) の説明と合う。)

### 試験と整合

```
$ PYTHONPATH=src python -m pytest tests/test_o3c_signal_value.py
53 passed in 22.22s
```

新しい試験は Q0 の指紋・前段の `bundle_id`・dry-run・対差・参照 2 本・Q4 の表・日ブロック分割・
露出・絶対値の分位・どれかの窓・materials の検査・キャッシュの解放を覆っている。

---

## (2) 段2 の道筋(`run_stage2`)の点検

### 出るもの(dry-run と code で確かめた)

| 設計 | 段2 の実装 | 確かめ方 |
|---|---|---|
| Q0 `bundle_id` の MD5 | あり。一致しなければ **例外で止まる**(2046〜2064 行) | code。dry-run では通らない |
| Q1 併記(5,000 件の較正) | あり(`calib_5000.csv`。前半で凍結した係数を当てるだけ) | code のみ(dry-run では 0 バイト) |
| Q2 7 方策 × 型 × 遅れ 3 × 費用 3 + 参照 2 本 | あり(`dist_table.csv` 288 行 = (7×2 + 2×1) × 3 遅れ × 2 × 3 費用) | dry-run |
| Q4 6 本の並置・対差・日ごと・側別・大きさ別・位置別・露出 | あり(`q4_*.csv` 7 ファイル) | dry-run |
| Q5b (a) 秒単位 / (b) 1 分の始値 | あり。ただし **(a) の母集団が設計と違う**(下) | code |
| 連鎖 1 本ごとの行 | あり(`cascades.csv.gz`、②opt と参照 2 本を含む) | dry-run(291 行) |

### 未来を使う箇所

**見つからなかった。** 判断に使う量はすべて段1 で凍結したものを読むだけである:
`bands` / `base_rates` / `cuts` / `chosen` は `stage1_firsthalf/summary.json` から読む(2030〜2033 行)、
確率は `compute_value_logit_probs`(`lg.load_ecdf_npz` + `apply_frozen_rank` + `load_beta` だけで
`build_ecdf` / `newton_logistic` を呼ばない)、参照の清算のラベルは
`sp.stage2.compute_logit_probs`(同じく `apply_frozen_rank` だけ)と前段の帯 `sp.BAND_*`。
Q1 併記の `calibration_table` は後半のラベルを**読むが帯を作り直して使わない**(表に出すだけで、
`judgments_for_policy_value` には渡らない)。事後のラベルを使う 2 本(完全な判断(値段)・
完全な判断(清算))は設計が参照として並べているものである。

### 残る致命(1 件)

**致命-4(新): 段2 の Q5b(a) の母集団が「後半 2,000 本 ∩ 標本日」に狭まっている。設計は
「標本日 16 日に掛かる連鎖(後半 81 本)」。段1 は設計どおり全連鎖で回している。**

```
$ grep -n "q5b_rows = run_q5b" scripts/o3c_signal_value.py
1578:    q5b_rows = run_q5b(all_cascades, chosen, prob_of, pos_of, bands,      ← 段1: 前半の全連鎖 8,931 本 → 285 本
2173:        q5b_rows = run_q5b(picked, chosen, prob_of, pos_of, bands, base_rates,   ← 段2: 抜いた 2,000 本だけ
```

`run_q5b` の中(1705 行)は `target = {bid: pr for bid, pr in all_cascades.items() if 標本日}` なので、
渡した集合がそのまま母集団になる。設計 §0.1 と §2 Q5b の逐語:

> 「bitFlyer の約定がある標本日 16 日に掛かる連鎖(**後半 81 本**・前半 285 本)だけ」
> 「**前半 285 本は段 1**(委任先が道具を確かめる)、**後半 81 本は段 2**(11 度目に含める)」

前半の 285 本は「前半の全連鎖 8,931 本 ∩ 標本日」で、実測で 285 と一致している(段1 の
`(母集団)` 行)。同じ数え方なら後半の 81 本は「後半の全連鎖 12,267 本 ∩ 標本日」である。
段2 は 12,267 本のうち 2,000 本しか渡さないので、**推定**では 81 × 2,000 / 12,267 ≈ **13 本**
(後半の行を読んでいないので実測ではない = 未測定)。13 対の対差の分布は 4 分位を置く意味が薄く、
**後半は一度きりなので後から 81 本に戻せない**(戻すには 2 度目の読みになる)。

報告 (9) は「設計 §Q5b は (a) を 81 本と見積もるが、実際の本数は段2 を走らせるまで分からない
(未測定)」と書いているが、**本数が分からないのではなく母集団を変えている。**
直し方は 1 行(段2 でも後半の全連鎖から `target` を作って `run_q5b` に渡す)で、設計が
Q5b を 11 度目の読みに含めている以上、読みの回数は増えない。

**これが段2 の前に残っている唯一の致命である。**

### 後から作り直せない欠け(致命ではないが、段2 の前に決めておくと安い)

- **`cascades.csv.gz` を書くのが最後**(2179 行)。順番は 方策 → 参照 → ②opt → Q4 → **Q1 併記** →
  **Q5b** → 書き出し。段2 で初めて走る 2 ブロック(上の (1) 致命-1)のどちらかで落ちると、
  連鎖 1 本ごとの行を含めて**何も残らない**。段1 は Q5b の前に書いている(1581 行)ので、
  段2 も同じ順にするだけで済む。
- **① の行の約定の遅れ**は、`cascades.csv.gz` に絶対時刻(`入りの約定時刻_ms`)しか無く、
  目標時刻(プリントの `ts_ms` + 遅れ)が同じ表に無いので、後から遅れを引き算できない
  (`rows_continue` を引き直すと 2 度目の読みになる)。Q5b の遅れの表は標本日の連鎖だけを覆う。
  `cascades.csv.gz` に `入りの目標時刻_ms` を 1 列足せば足りる。

---

## (3) 直しで新しく入った誤り

### N-1(直すべき): Q4 の 並置と対差の分母が線ごとに違う。②opt は「入った連鎖だけ」で数えている

dry-run の実測:

```
q4_main.csv:   ①3択A n=2 / ①3択B n=2 / 全部逆張り(素) n=2 / 前段の3択(清算)A n=2 /
               完全な判断(値段)A n=2 / ②opt n=1
q4_pair_diff.csv: ①3択A − ②opt 対の数 1 /  ①3択A − 全部逆張り(素) 対の数 2 /
                  ②opt − 全部逆張り(素) 対の数 1
```

`build_pair_diff_rows` は `set(A) ∩ set(B)` の `bundle_id` で対にするので、**②opt が入らなかった
連鎖は対から落ちる**。① 側は入らなかった連鎖も損益 0 の行を持つ(`simulate_cascade` は
`total_pnl` を 0 のまま返す)。段1 の格子では ②opt が入るのは 2,284 / 8,931 = 25.6% なので、
後半でも対差は 2,000 本ではなく**その 4 分の 1 ほど**で出ることになる。
`q4_main.csv` も同じで、②opt の 中央値・負の割合・日等重み平均だけが「入った連鎖」の上の量になる
(合計は 0 を足しても変わらないので比べられる)。

設計 Q4 の逐語は「**連鎖ごとの対差の分布**(① 3 択 A − ②opt / …)」で、母集団を書いていない。
どちらの読みも成り立つが、**線ごとに分母が違う表を 1 枚で並べると読み違える。**
`cascades.csv.gz` に ① の行が 2,000 本ぶん入るので、②opt の無い連鎖を 0 として作り直すことは
後からできる(だから致命ではない)。段2 の前に、どちらを主にするか決めて両方出すのが安い。

### N-2(直すべき): `sp.stage2.compute_logit_probs` は日ごとの解放が効かない(D-6(b) の穴)

```
$ sed -n '108,112p' scripts/o3c_signal_stage2.py
    if len(df_first):
        df_first = lg.add_n2_column(sb, df_first)
```

`run_stage2` は前後で `sb._window_cache.clear()` / `_raw_trade_cache.clear()` を呼ぶが、
この 1 回の呼び出しの中で後半 228 日ぶんが溜まる。大きさの見積もり(前回の実測 1 日平均 5.2 MB
× 228 日 ≈ 1.2 GB、生のキャッシュを足して約 2.5 GB)に対して、この環境の空きは

```
$ free -m
Mem: total 16095 / available 15300
```

なので落ちる見込みは小さい。**直すなら `compute_value_probs_for_frame` と同じく日ごとに回すだけ**で、
段2 の前に 1 行で済む。

### N-3(直すべき): `add_n2_by_day` が 2 回定義されている

```
$ grep -n "^def add_n2_by_day" scripts/o3c_signal_value.py
253:def add_n2_by_day(sb, df_first: pd.DataFrame) -> pd.DataFrame:
1961:def add_n2_by_day(sb, df_first: pd.DataFrame) -> pd.DataFrame:
```

中身は同じ(どちらも日ごとに両方のキャッシュを空にする)ので**挙動は変わらない**が、
後の定義が前を上書きしており、片方だけ直すと食い違う。

### N-4(直すべき): 到達時間の表の母集団だけ 21,838 件のまま(帯の母集団 20,797 件と揃っていない)

`time_to_target_table` は `_recheck_rows_for_day` の行から作られ、それは `prints_df`
(前半の全プリント 21,838 件)を回る。帯・基準率は 20,797 件に揃えたのに、Q1 の到達時間だけ
束の外 1,041 件を含む。束の外の値段のラベルの割合は 0.9231 なので、
**「値段のラベルが 1 の 11,958 件」のうち約 961 件(8.0%、推定 = 1,041 × 0.9231)は
方策が一度も通らないプリントである。** 数値そのものは前と同じで誤りではないが、
「① が取れる / 取れない」の読みに使うなら母集団を揃えるか、注記が要る。

### N-5(示唆): 露出の 2 つの時計が違う

`exposure_of` の `合計保有時間` は `保有秒`(状態機械が持つ、**目標時刻**ベース)、
`同時建玉の最大` は `入りの約定時刻_ms`〜`出の約定時刻_ms`(**約定時刻**ベース)。
実測すると両者はずれる:

```
$ python3 -c "logistic_3択 型A 遅れ1・入った行"
span >= 保有秒 の割合 0.563953...  中央値 span 60.6355 秒  中央値 保有秒 60.0 秒
```

(約定の遅れが入りと出で違うため。)`entry_exit_fill_times` の docstring は型 A の建て直しで
区間に穴が空くことを注記しているが、**2 つの時計の違いは書かれていない。**
`入りの約定時刻_ms` が空の行は 0 件だったので、区間の復元そのものは効いている。

### N-6(示唆): `cascades.csv.gz` の `出口の理由` が ① の行では常に「連鎖の終わり+d」

```
$ python3 -c "①の行の出口の理由"
{'連鎖の終わり+d': 2400}
```

`run_policies`(1189・1216 行)が固定文字列を入れており、型 A が連鎖の途中で決済した行も
「連鎖の終わり+d」になる。②opt の行は `simulate_reverse_entry` の本当の理由(損切り / 出口 3 種)が入る。
列の意味が方策によって違う。

### N-7(示唆): 直しの効きの内訳(順位分離)

(9) は順位分離の低下を「致命-2 + S-1(日ブロック)」とまとめているが、実測では**ほぼ全部が
母集団の直し**である(連鎖の中):

```
束込み・行の無作為 5 分割      0.6708
束の中だけ・行の無作為 5 分割  0.6476   ← 束の外 903 件を外した分 −0.0232
束の中だけ・日ブロック 5 分割  0.6466   ← 日ブロックの分 −0.0010
```

前回のレビュー S-1 が「跨ぎの効きは小さい(0.0011)」と測った値と同じで、
**直す前の 0.6708 を押し上げていたのは、値段のラベルの割合が 0.9231 の束の外 903 件だった。**

### 新しく入った誤りは見つからなかった箇所(c の絶対値化・窓の全対・日ブロック・束の除外・露出の列)

- **c の絶対値化**: `_dist_row` の p25/p50/p75/p90 が `np.abs(vals)` になり、符号付きと正の対だけの
  中央値が参考列として残った。`read_cost` が拾うプールの行も絶対値である(試験
  `test_read_cost_returns_the_absolute_quantiles` あり)。独立の再計算と一致。
- **窓の全対**: 自分で別の式を書いて 16 日すべてで実装と一致(`assert` が全日通った)。
- **日ブロック分割**: docstring から書き直した実装で 順位分離・帯・わからないの割合が全部一致。
  同じ日が訓練側と試験側に跨らないことは試験
  `test_day_block_folds_never_split_a_day_and_are_deterministic` が見ている。
- **束の除外**: `build_scene_frames` の戻り値が 3 つに増え、呼び出し側(1499 行)と試験(708 行)の
  両方が直っている。`notional` 列を足した併合で列名の衝突は起きていない(`outside["notional"]` が
  引けている)。
- **露出の列**: `max_concurrent` は半開区間で端が触るだけなら 1 と数える(試験あり)。
  `grid.csv`・`knob_contrib.csv`・`exposure.csv`・`q4_exposure.csv` の 4 か所に入った。
- **選ばれた組と総収支**: 費用の差だけで説明できることを算術で確かめた。
  ②opt 4,609.355648 − 2,284 × (2.004025 − 1.786924) = 4,609.355648 − 495.859 = **4,113.497**
  (`grid.csv` の 4,113.496964 と一致)。素 −11,147.978 − 8,931 × 0.217101 = **−13,086.91**
  (`exposure.csv` の −13,086.907342 と一致)。**帯の直しは ② に触れない**(② は logistic を使わない)
  ことの裏付けでもある。

---

## 再点検の再計算の一覧

| # | 何を | 結果 |
|---|---|---|
| R-1 | 費用 c(絶対値・どれかの窓) | 対 4,712 / 絶対値 p50 **2.0040** / p75 **3.9228** / 参考 符号付き 1.8497・正だけ 2.3186 → 出力と一致。窓の判定は自分の別実装が 16 日すべてで一致 |
| R-2 | ① の当てはめ(母集団・日ブロック) | 1 件目 8,931 / 0.434330 / 0.6608 / (0.38, 0.50) / わからない 24.51%、連鎖の中 11,866 / 0.599865 / 0.6466 / (0.56, 0.66) / 23.85%、**保存された係数との最大差 0.0** |
| R-3 | 順位分離の内訳 | 0.6708 →(束の外を外す)0.6476 →(日ブロック)0.6466 |
| R-4 | 束の外 1,041 件 | ラベル割合 0.923151 / 31 日 / 最も近い束まで最小 60,854 ms・60 秒以内 0 件 / 2023-10-16 は 258 件中 137 件が 13:21:58〜13:54:26 |
| R-5 | `dist_table.csv` を 200 本から一から回し直し | **4,032 / 4,032 セル一致**、`judge_counts.csv` も一致 |
| R-6 | `cascades.csv.gz` から `dist_table.csv` を組み直し | 数の列 **3,276 / 3,276** が 1e-6 以内(最大差 7.5e-7 = CSV の 6 桁丸め)、文字の列 756 / 756 |
| R-7 | Q5b の「どちらも 0」 | ①3択A **282 対 / 50 本** → 出力と一致 |
| R-8 | 到達時間 | 到達 < 1 秒 2,546 件・なお 5 bp 以上 78.4%・中央値 20.43 bp → 出力と一致(前回の独立測定とも一致) |
| R-9 | 露出の算術 | ②opt 4,113.497 / 素 −13,086.91 が c の差だけで説明できる |
| R-10 | 約定時刻の復元 | 入った 21,731 行すべてで `入りの約定時刻_ms` / `出の約定時刻_ms` が埋まっている。span と 保有秒 のずれは 43.6% |
| R-11 | 試験 | `53 passed in 22.22s` |
| R-12 | 出力の無改変 | `stage1_firsthalf` / `spread` / `stage2_dryrun` の `MD5SUMS` が全件一致 |

**残る致命: 1 件**(致命-4 = 段2 の Q5b(a) の母集団)。
直すべき: 4 件(N-1〜N-4)+ 段2 の書き出しの順と `入りの目標時刻_ms` の 1 列。示唆: 3 件(N-5〜N-7)。
