# 清算を起点とした値動きの予測可能性 — 続く / 止まるの単位の設計(2026-09-20。§3 の材料は連鎖 30 本を読んで確定した)

**探索段**(research-protocol §0.0)。456 日は **7 度目の読み**。判定語(陽性 / 陰性 / 有意 / 差あり / 検出されず)は使わない。判定は自前記録(触らない、L-246)で一度だけ。
前段: `SIGNAL_EXPLORE5_RESULT_2026-09-20.md`(層 × h の平均の表しか作っておらず「続く / 止まる」を分ける材料探しに届いていない = L-260)。

## 用語(記号の式と、日常語の説明)

| 記号 | 意味 |
|---|---|
| プリント / ts / t₀ / p₀ | 探索段 5 と同じ。清算 1 件、その時刻、ts 以後の最初の約定の時刻(= 判断の時点)と価格 |
| **続く(ラベル)** | このプリントの後、**同じ側の次のプリントが 60 秒以内**に来る(1 / 0)。30 秒・120 秒も併記。**判断の対象であって層ではない** |
| 基準率 | 何も見ずに「続く」と言ったときの的中率 = 続くの割合(探索段 5 の H1 で 58.5%、60 秒) |
| 材料 | t₀ に手元にある数字(§3)。ts より後の約定・清算は使わない(p₀ だけは使う) |
| 判断 | 材料から「続く」か「止まる」かを返すもの。**規則**(前半の日で作る)と **Jev**(状態を渡して確率を返させる)の 2 通り |
| 2 枝の損益 | 続く → 清算の向きに入る、止まる → 逆に入る。入る時点 t₀ + 1 / 3 / 5 秒、出る時点 入ってから 30 / 60 / 300 秒。1 件あたり bp(符号は判断の向きが正)。費用は含めない(A-8) |
| 前半 / 後半 | 456 日を日付順に 228 日ずつ。切り値・規則は前半だけで作り、**的中率と損益は後半だけで測る** |

## 0. これまでの結果 × この設計

| 前段の結果 | この設計での扱い |
|---|---|
| 続いた清算の後は清算の向きへ(多件の最初 +5.2 bp)、止まった清算の後は逆へ(単発 −4.0、最後 −10.5)。1 件目 −0.01、2 件目以降 −2.5(探索段 5、L-259) | 判断が当たれば両枝に取り分がある。**測るのは判断の的中率と、判断に条件付けた 2 枝の損益**。r(h) の層別平均は作らない |
| t₀ で分かる量(d・直前の間隔)で「最後」を当てにいくと平均が 0 に寄る(探索段 5 致命 5) | 平均で潰さない: 判断ごとに分布と 1 件あたり損益(L-260)。材料も「続いた / 止まった」の分布で見る |
| 切り値を 456 日全部で切ると 20〜28% が帯を変える(探索段 5 直すべき 11) | 前半で作って後半で測る(L-263) |
| k の層は価格水準の層だった(探索段 5 致命 2) | k はティック数で持つ。単独の層にしない |
| 対照 (c') は静かな区間に偏る(探索段 5 致命 3) | 基準は**基準率**と「全プリントに同じ枝を取った場合の損益」。対照は作り直す(Q7: 同じ勢い・同じ成行の偏りの清算の無い時刻に同じ判断を当てる。取れた割合を出す) |

## 0.1 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 問いを「この清算は続くか」の判断に置き、判断に条件付けた 2 枝の損益を測る | 「**清算を検知→jevでこの清算は続くか判断→続くなら乗る、続かないなら逆張り**」(L-262) |
| Jev に判断させ、規則と同じ清算で比べる | 同上、「**こんなときこそjevの使い道やろ**」(L-238) |
| 平均で潰さず、判断ごとの分布と 1 件あたりの損益で出す | 「**平均で計算したらわからなくなって当然**」(L-260) |
| 運用のシグナルとしての価値を測る(入る・出る時点、的中率、基準率) | 「**実際の運用で使うシグナルとしての価値が見出せる結果**」(L-262) |
| 材料に 8〜11 を足す(具体はリードの選択) | 「**その7つで足りているか検討しましたか**」(L-262) |
| X = 60 秒(30 / 120 併記) | 「**その空の行もそれでいいです**」(L-263。中身は L-262 への返答でリードが示した空の行) |
| 前半 228 日で作って後半 228 日で測る | 同上(L-263) |
| 連鎖 30 本を実物で読んでから材料を確定する | 同上(L-263) |
| bitFlyer との経路差は別の単位で測る | 同上(L-263) |
| **監査 1 回目の指摘 9 で足した対照 Q7**(同じ勢い・同じ成行の偏りの清算の無い時刻に同じ判断を当てる) | **(該当語なし)**(リードの決定。研究の規律 §4.2「対照群が作れるなら対照群を使う」による。納品時にオーナーに明示し、退けられれば外す) |
| **読んで足したもの**: 材料 12〜15(§3.0 の 3・4・6・8)と結果の列「値段の続き」(§3.2) | 上の「連鎖 30 本を実物で読んでから材料を確定する」の行に依る(L-263)。**具体はリードの選択で、原文に該当語は無い。納品時にオーナーに明示し、退けられれば外す** |
| 自前記録は判定まで触らない | 「**自前記録使ったら判定できなくなるからダメ**」(L-246) |
| SE の日クラスタ・多重性・反証者・テスト | `research-protocol` の固定手順(L-179) |

## 1. 意図と実装の突き合わせ(§0.5)

| # | 原文の意図(逐語) | この設計の要素 | 印 |
|---|---|---|---|
| C-1 | 「**清算を検知→jevでこの清算は続くか判断**」(L-262) | ラベル = 同じ側の次の清算が 60 秒以内。判断 = 規則と Jev、t₀ の材料だけ | ○ |
| C-2 | 「**続くなら乗る、続かないなら逆張り**」(L-262) | 2 枝の損益を、判断に条件付けて 1 件あたりの分布で出す(Q4) | ○ |
| C-3 | 「**実際の運用で使うシグナルとしての価値**」(L-262) | 入る時点 t₀ + 1 / 3 / 5 秒、出る時点 30 / 60 / 300 秒、後半だけで測る、日ごとの損益(Q4・Q5)。**bitFlyer で取れるかは別の単位**(S-6) | △ |
| C-4 | 「**平均で計算したらわからなくなって当然**」(L-260) | 判断ごと・材料ごとの分布(中央値・四分位・負の割合)。層別平均の表は作らない | ○ |
| S-1 | 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243) | Q3〜Q5 | ○ |
| S-3 | 「**値段の上がりすぎ下がりすぎ**」(原文 §1) | 「止まる」の判断 = 上がりすぎ下がりすぎの終わり。Q4 の止まる枝 | ○ |
| S-4 | 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1) | 続く = トレンド、止まる = 転換。ラベルそのもの | ○ |
| S-6 | 「**bitFlyerの価格相関**」 | 本単位では測らない(456 日分の bitFlyer 約定が無い。2026-08-20 以降で別の単位) | ✕ |
| S-8 | 「**エントリーや利確に活かせるか**」 | 入る・出る時点の格子で損益を出す。費用は含めない(A-8) | △ |
| S-9 | 「**海外取引所の高レバ市場**」(原文 §1) | Binance COIN-M BTCUSD_PERP | ○ |

単位のマスター `docs/PHASE2/O3C/INTENT_MAP.md` との対応: C-1〜C-4・S-1・S-3・S-4 = I-01(清算で過伸・転換を捉える)、S-6 = I-04、S-8 = I-08 の後半(前半「どの価格帯で清算が起きるかの予測」= I-08 前半は本単位では扱わない)、S-9 = I-03(Binance COIN-M = 海外の高レバ市場、○)。**本単位で扱わないもの**: I-02(位置づけ、検定単位にしない)、I-05(時間軸: h は 30 / 60 / 300 秒の 3 本に絞る = 探索段 5 の 7 本から縮めた。理由: 判断の対象が 60 秒のラベルで、15 分は別の問い。**縮めたことをここに書く**)、I-06(BitMEX、前提不成立)、I-07 の bitFlyer 側、I-09・I-10(機構仮説ではない)。

## 2. 問い(Q0〜Q6)

| # | 問い | 見る形 |
|---|---|---|
| Q0 | 自己点検 | プリント数、ラベルの割合(前半 / 後半、側ごと、30 / 60 / 120 秒)、材料ごとの欠測数、p₀ と h 後の遅れの分位、前半と後半の日数・清算数・価格帯 |
| Q1 | 材料ごとに「続いた / 止まった」の分布は違うか | 材料 1 つ × ラベル 2 群: 分位(p10 / p25 / p50 / p75 / p90)と、材料の 10 分位帯ごとの続く割合(前半で切る)。**平均の r は出さない** |
| Q2 | 規則 | 前半で、材料の 10 分位帯ごとの続く割合から「続く」と言う帯を決める(割合 > 基準率の帯)。単一の材料の規則を材料ごとに 1 本、上位 3 材料の組の規則を 1 本。後半での的中率・適合率・再現率(基準率つき) |
| Q3 | Jev | 後半の各プリント(または 5,000 件の層化無作為、§8)について、t₀ の状態(直前 60 秒の清算の列、直前 60 秒の値段の列、材料の値。**すべて §3 の規則どおり ts 以前だけ。p₀ と ts 以後の約定・清算は渡さない**)を state に渡し、「この後 60 秒以内に同じ側の清算が続くか」の確率を返させる。的中率・適合率・再現率、確率の 10 帯ごとの実際の続く割合(較正)、規則との一致率 |
| Q4 | 2 枝の損益 | 判断(規則 / Jev / 全部続く / 全部止まる / 完全な判断 = 事後のラベル)× 入る時点 3 × 出る時点 3: 1 件あたり損益の中央値・四分位・負の割合・日等重み平均・日クラスタ SE。**続くと言った件と止まると言った件を別々に** |
| Q5 | 日ごと | Q4 の主格子(t₀ + 1 秒 → 60 秒)の日ごとの合計の分布(負の日の割合、上位 5 日の寄与) |
| Q6 | 完全な判断の上限 | 事後のラベルで分けた損益(診断)。**Q4 と同じ列**(中央値・四分位・負の割合・日等重み平均・日クラスタ SE)。平均だけの行は作らない |
| Q7 | 清算の無い時刻に同じ判断を当てる(対照) | §4 のとおり(向き = 直前 10 秒の変位の符号、ラベル = 値段の続き)。規則 15 + Jev × 枝 2、主格子(t₀ + 1 秒 → 60 秒)、Q4 と同じ列 + 取れた割合 |

## 3. 材料(t₀ に手元にある数字)

### 3.0 連鎖 30 本を実物で読んで見えたこと(`docs/DATA/probes/20260920_o3c_cascade_read.md`、続いた 15 / 止まった 15、乱数の種 20260920。リードが読んだ。**30 本の目視であって測定ではない**。ここから材料を足し、Q1 で測る)

1. **清算のプリントは値段をほとんど動かさない**(k はティック 0〜1 が大半。30 本 79 件中、2 ティック以上は 12 件)。値段が動いて清算が起きる。連鎖 = 値段の一方向の動きで、プリントはその目印。**「続く」≈「値段の動きが続く」**。
2. **プリントの間で値段が動く**(直前のプリントから +5〜+27 bp)。続いた連鎖は最後のプリントまで新しい高値(安値)を作り続け、最後の後に戻る(15 本中 14 本が 60 秒後に負、−1〜−41 bp)。止まった 15 本の 60 秒後は −11〜+6 で小さい。
3. **大きな 1 件が連鎖の終わり近くに出る**ことが多い(#1 は 8 件目 37.5M USD が最後、#3 は 5.7M が最後、#14 は 234M・30M が 2・3 件目で終わり、#23 は 139M が 5 件目で次が最後)。単発にも大きいもの(#9 24.4M、#18 14.8M)がある。→ **この 1 件の想定元本 ÷ 直前 60 秒の最大の 1 件**を材料に足す(12)。
4. **直前 5 秒の成行の偏りは、最初のプリントでは続いた側も止まった側も清算の向きに寄っている**(±0.8〜1.0)ので、最初の 1 件では分けない。**連鎖の中では偏りが 0 や逆に向かい、最後のプリントの前後で弱い**(#1: +1.0 → +0.5 → +0.3 → −0.04、#2: +1.0 → −0.08 → −0.11、#21: +0.84 → +0.25 → +0.06)。→ 5 秒と 30 秒の偏りの差(13)。
5. **プリントの間隔は連鎖の山に向かって縮む**(#1: 4 → 50 → 41 → 3 → 2.6 → 8.6 → 7.6 秒、#12: 1 → 37 → 3 → 7 → 2 → 1 → 1 秒)。1〜3 秒刻みの塊が来て、その塊の最後が山。→ 直前 2 つの間隔の比(2)を残す。
6. **直前 60 秒の約定件数**は続いた側に多い(続いた 15 本の中央値 327 件、止まった 15 本 215 件。目視の数字)。→ 材料に足す(14)。
7. **m_60・d・dist_node_bp は最初の 1 件では両群が重なる**(m_60 続いた 2.9〜60、止まった 3.0〜48)。`dist_node_bp` は連鎖が進むほど大きくなる = **通り過ぎた水準からの距離**で、「この先の水準」ではない。建玉の量(8)は 30 本の最初のプリントで 25 本が被覆内、うち 5 / 10 / 20 bp 以内に量が入るのは 3 / 8 / 14 本で、続いた側と止まった側の両方に 0 がある。**材料としては薄い**が、測ってから外す。**欠測と 0 の扱い**: 被覆外は欠測(Q1 では「被覆外」の群を 1 つ別に立て、規則には使わない)。被覆内の 0 は 0 の帯として数える(補完しない)。Jev の state には被覆外を `null` で渡し、0 は 0 で渡す。
8. **読んだ範囲の限界**(L-264・L-265): 30 本は乱数で抽出した(規則: 3 件以上の束から 15 本、単発から 15 本。人の目では選んでいない)。**ちょうど 2 件の束(3,906 本)は抽出の規則の対象外**で、読んでいない。測定には全 52,000 件(単発 12,029 件を含む)が入る。
9. 値段の 1 秒刻みの列で、**最後のプリントの直前 5〜10 秒に値段が一気に走る**(#1 は 90〜116 秒で +12 → +41 bp、#12 は 48〜52 秒で +22 → +67)。→ 直前 10 秒の変位 ÷ 直前 60 秒の変位(15)。

### 3.1 材料の一覧(番号は L-262 の 1〜11 を引き継ぐ。12〜15 は 3.0 で足した)

| # | 材料 | 何を見るか | 出所 |
|---|---|---|---|
| 1 | 直前 60 秒の同じ側の件数、最初の 1 件からの経過秒 | 清算の列 | rows(d を 60 秒窓で作り直す) |
| 2 | 直前 2 つの間隔の比(縮んでいるか) | 清算の列 | rows |
| 3 | 想定元本、直前のプリントとの比 | 清算の列 | rows(notional) |
| 4 | 連鎖の最初からの bp、直前のプリントの後の戻り bp | 約定 | 約定 |
| 5 | 次の清算水準までの距離 dist_node_bp(3.0 の 7 の但し書きつき) | 建玉の分布 | rows |
| 6 | 時刻帯(UTC 6 時間 × 4) | ts | rows |
| 8 | この先 5 / 10 / 20 bp 以内の建玉の量(枚) | 建玉の分布(`oid` の対数ビン) | 委任先の `oi_band_amounts` を道具に入れる |
| 9 | 直前 5 秒の成行の偏り | 約定(is_buyer_maker) | 約定 |
| 10 | 建玉の傾き: 5 分値の建玉の変化率、taker のロングショート比、直近の資金調達率 | metrics + fundingRate(月次 zip 19 本、列 `last_funding_rate`) | 委任先が確認済み |
| 11 | 想定元本 ÷ 直前 60 秒の値幅(bp) | 清算 + 約定 | 約定 |
| 12 | この 1 件の想定元本 ÷ 直前 60 秒の同じ側の最大の 1 件(無ければ欠測) | 清算の列 | rows |
| 13 | 成行の偏り(5 秒)− 成行の偏り(30 秒) | 約定 | 約定 |
| 14 | 直前 60 秒の約定件数 | 約定 | 約定 |
| 15 | 直前 10 秒の変位 ÷ 直前 60 秒の変位(清算の向きが正) | 約定 | 約定 |

7(板の厚み)は測らない(データ無し)。k のティック数と側も列に持つ(層にはしない)。**すべて ts 以前の約定・清算・5 分値だけで計算する**(p₀ は使わない = 判断は ts + 数百 ms で出せる。損益の入る時点は t₀ 基準)。

### 3.2 結果の列(併記、判断の対象ではない)
- ラベル(60 秒、主)、30 秒、120 秒。
- **値段の続き**(3.0 の 1 を受けて足した): t₀ から 60 秒以内に、清算の向きへ p₀ から 5 bp 以上進むか(1 / 0)。取り分は値段で決まるので、清算のラベルと並べる。

## 4. 基準(対照の代わり)

基準率(続くの割合)、全プリントに「続く」枝 / 「止まる」枝を取った損益、完全な判断の損益(上限)。探索段 5 の対照 (c')(同じ m の清算の無い時刻)は作らない(致命 3 の偏り)。代わりに **Q7**: 同じ規則と同じ Jev を、**清算の無い時刻**に当てる。定義(曖昧さを残さない):
- 候補 = 各プリントと同じ日の 10 秒刻みの時刻で、**前後 15 分にどちらの側の清算も無い**もの。
- 合わせる帯 = 材料 15(直前 10 秒 ÷ 60 秒の変位)と材料 9(直前 5 秒の成行の偏り)の、**前半で切った 10 分位帯**が両方ともプリントと同じもの。1 プリントに 1 件、置換なし。**その日に無ければ欠測**(日をまたがない)。取れた割合と、帯ごとの取れなかった数を出す(探索段 5 致命 3 の再発防止。帯 9・10 の取れた割合が 50% を下回れば、その帯の Q7 は読まない)。
- **向き**(清算が無いので「清算の向き」は無い)= **直前 10 秒の変位の符号**(勢いの向き。清算のプリントでは m(60) > 0 が 99.4% = 勢いの向きと清算の向きはほぼ同じ、探索段 5)。材料の符号付きの値・ラベル(「続く」= その向きへ 60 秒以内に清算が来るか、ではなく **その向きへ p₀ 相当の価格から 5 bp 以上進むか** = 「値段の続き」と同じ定義)・損益の符号はすべてこの向きで取る。**Q7 の「続く」のラベルは清算のラベルではなく値段の続きである**(清算の無い時刻に清算のラベルは定義できない)。プリント側も同じ「値段の続き」で並べて比べる。
- なぜ材料 15 と 9 で合わせるか: 3.0 で見えたのは「値段が動いて清算が起きる」= 清算の瞬間は勢いと成行の偏りが強い瞬間である。Q7 が測りたいのは「同じ勢い・同じ成行の偏りの瞬間に、清算があることが何か足しているか」なので、この 2 つを合わせ、他は合わせない(合わせるほど取れなくなる = 致命 3)。他の材料の分布の差は Q7 の表に併記する。
- **清算があることが、勢いの続きに何か足しているか**を直接見る。

## 5. 設計の段の判定(`jev_design.py`)— 2026-09-20 リードの実測

コマンド: `PYTHONPATH=src:scripts python3 scripts/jev_design.py rank-observables --purpose-from <この文書> --intent C-1,C-2,C-3,C-4,S-1,S-3,S-4 --observables config/jev_design_examples/signal6_observables.yaml --md …` と `rank-covariates --quantity "<続くのラベル>" / "<続くと言って乗った損益>" --covariates config/jev_design_examples/signal6_covariates.yaml --md …`(2 回)。確率は `data/jev/design/`(リポジトリに入れない)。**Jev は決めない。順位と語だけで、選ぶのはリード。**表の右端の列はリードが足した日本語(材料の番号は §3.1)。

リードの読み: C-1・C-2 で「直接」になったのは ラベル・2 枝の損益・判断に条件付けた損益。**「材料ごとの分布」(Q1)は C-4(平均で潰さない)でだけ直接で、C-1 では無関係**と出た = 材料の分布は判断の材料探しであって判断そのものではない、という位置づけで矛盾しない。rank-covariates の「合わせなくてよい」は「対照で合わせる変数か」への答え。**Q7(監査 1 回目の指摘 9 で足した対照)は材料 15 と 9 を合わせる**が、それは Jev の語に反して合わせるのではなく、Q7 の目的(同じ勢いの瞬間を作る)から来る(§4 に理由)。Q7 を足した後に rank-observables を再実行した(下の 2 つ目のブロック、`control_same_momentum` を候補に足した)。**順位だけを使う**: ラベルに対しては 1 件数と経過秒・5 距離・4 連鎖の最初からの bp・2 間隔の比・15 の順、損益に対しては 4・k と側・5・15・11 の順。

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 設計文書: `docs/PHASE2/O3C/SIGNAL/SIGNAL_CONTINUE_DESIGN_2026-09-20.md`
- 候補ファイル: `config/jev_design_examples/signal6_observables.yaml`
- 意図: C-1, C-2, C-3, C-4, S-1, S-3, S-4

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### C-1 「**清算を検知→jevでこの清算は続くか判断**」(L-262)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `continuation_label` | 直接(3 以上) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 2 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率(後半)対 基準率と規則 |
| 3 | `calibration_of_probability` | 代理(2 台) | 確率の帯ごとの実際の続く割合(較正) |
| 4 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |
| 5 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 6 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 7 | `payoff_conditional_on_decision` | 無関係(1 以下) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 8 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 9 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |

### C-2 「**続くなら乗る、続かないなら逆張り**」(L-262)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `two_branch_pnl_per_print` | 直接(3 以上) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 2 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 3 | `continuation_label` | 直接(3 以上) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 4 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率(後半)対 基準率と規則 |
| 5 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 6 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 7 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |
| 8 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 9 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |

### C-3 「**実際の運用で使うシグナルとしての価値**」(L-262)(印 △)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 2 | `two_branch_pnl_per_print` | 代理(2 台) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 3 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率(後半)対 基準率と規則 |
| 4 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 5 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 6 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |
| 7 | `continuation_label` | 無関係(1 以下) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 8 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 9 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |

### C-4 「**平均で計算したらわからなくなって当然**」(L-260)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 代理(2 台) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 2 | `material_distribution_by_outcome` | 代理(2 台) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |
| 3 | `two_branch_pnl_per_print` | 代理(2 台) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 4 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 5 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 6 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率(後半)対 基準率と規則 |
| 7 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |
| 8 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 9 | `continuation_label` | 無関係(1 以下) | 続くのラベル(同じ側の次の清算が 60 秒以内) |

### S-1 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 2 | `two_branch_pnl_per_print` | 直接(3 以上) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 3 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率(後半)対 基準率と規則 |
| 4 | `price_move_conditional_on_true_label` | 代理(2 台) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 5 | `rule_hit_rate_vs_base` | 代理(2 台) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 6 | `continuation_label` | 代理(2 台) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 7 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |
| 8 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 9 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |

### S-3 「**値段の上がりすぎ下がりすぎ**」(原文 §1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 2 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |
| 3 | `payoff_conditional_on_decision` | 無関係(1 以下) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 4 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 5 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 6 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率(後半)対 基準率と規則 |
| 7 | `continuation_label` | 無関係(1 以下) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 8 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 9 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |

### S-4 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `continuation_label` | 代理(2 台) | 続くのラベル(同じ側の次の清算が 60 秒以内) |
| 2 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率(後半)対 基準率と規則 |
| 3 | `price_move_conditional_on_true_label` | 代理(2 台) | 事後のラベルで分けた値動き(診断。完全な判断の上限) |
| 4 | `payoff_conditional_on_decision` | 無関係(1 以下) | 判断に条件付けた損益の分布(続くと言った件 / 止まると言った件を別々に) |
| 5 | `calibration_of_probability` | 無関係(1 以下) | 確率の帯ごとの実際の続く割合(較正) |
| 6 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝のロジックの 1 件あたり損益(分布と日ごと) |
| 7 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率(前半で作り後半で測る)対 基準率 |
| 8 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの h 秒後の平均と中央値(探索段 5 と同じ、基準) |
| 9 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの、続いた / 止まったの分布(値動きの平均は取らない) |



## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: whether another same-side liquidation print occurs within 60 seconds after this print (the continuation label the decision must predict at the print time)
- 判定の量(日本語): このプリントの後 60 秒以内に同じ側の次の清算が来るか(判断が当てるべき「続く」のラベル)
- 変数ファイル: `config/jev_design_examples/signal6_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `same_side_count_60s_and_elapsed` | 合わせなくてよい | 1 直前 60 秒の同じ側の件数と経過秒 |
| 2 | `distance_to_liquidation_node` | 合わせなくてよい | 5 dist_node_bp |
| 3 | `move_since_cascade_start_and_bounce` | 合わせなくてよい | 4 連鎖の最初からの bp と直前の後の戻り |
| 4 | `interval_ratio_last_two` | 合わせなくてよい | 2 直前 2 つの間隔の比 |
| 5 | `print_sweep_ticks_and_side` | 合わせなくてよい | k のティック数と側(列、層にしない) |
| 6 | `burst_ratio_10s_over_60s` | 合わせなくてよい | 15 直前 10 秒 ÷ 60 秒の変位 |
| 7 | `notional_over_60s_range` | 合わせなくてよい | 11 想定元本 ÷ 直前 60 秒の値幅 |
| 8 | `open_interest_mass_ahead` | 合わせなくてよい | 8 この先 5/10/20 bp の建玉の量 |
| 9 | `notional_and_ratio_to_previous` | 合わせなくてよい | 3 想定元本と直前との比 |
| 10 | `notional_over_max_recent_print` | 合わせなくてよい | 12 想定元本 ÷ 直前 60 秒の最大の 1 件 |
| 11 | `taker_imbalance_5s` | 合わせなくてよい | 9 直前 5 秒の成行の偏り |
| 12 | `trade_count_60s` | 合わせなくてよい | 14 直前 60 秒の約定件数 |
| 13 | `taker_imbalance_trend` | 合わせなくてよい | 13 偏り 5 秒 − 30 秒 |
| 14 | `oi_slope_and_funding` | 合わせなくてよい | 10 建玉の傾き・資金調達率 |
| 15 | `time_of_day_band` | 合わせなくてよい | 6 時刻帯 |



## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: the signed price move over the 60 seconds after the print in the liquidation direction, i.e. the payoff of going with the liquidation when the decision says continue
- 判定の量(日本語): プリントの後 60 秒の、清算の向きへの符号付き値動き(判断が「続く」と言って乗ったときの取り分)
- 変数ファイル: `config/jev_design_examples/signal6_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `move_since_cascade_start_and_bounce` | 合わせなくてよい | 4 連鎖の最初からの bp と直前の後の戻り |
| 2 | `print_sweep_ticks_and_side` | 合わせなくてよい | k のティック数と側(列、層にしない) |
| 3 | `distance_to_liquidation_node` | 合わせなくてよい | 5 dist_node_bp |
| 4 | `burst_ratio_10s_over_60s` | 合わせなくてよい | 15 直前 10 秒 ÷ 60 秒の変位 |
| 5 | `notional_over_60s_range` | 合わせなくてよい | 11 想定元本 ÷ 直前 60 秒の値幅 |
| 6 | `same_side_count_60s_and_elapsed` | 合わせなくてよい | 1 直前 60 秒の同じ側の件数と経過秒 |
| 7 | `open_interest_mass_ahead` | 合わせなくてよい | 8 この先 5/10/20 bp の建玉の量 |
| 8 | `taker_imbalance_5s` | 合わせなくてよい | 9 直前 5 秒の成行の偏り |
| 9 | `notional_and_ratio_to_previous` | 合わせなくてよい | 3 想定元本と直前との比 |
| 10 | `taker_imbalance_trend` | 合わせなくてよい | 13 偏り 5 秒 − 30 秒 |
| 11 | `trade_count_60s` | 合わせなくてよい | 14 直前 60 秒の約定件数 |
| 12 | `interval_ratio_last_two` | 合わせなくてよい | 2 直前 2 つの間隔の比 |
| 13 | `notional_over_max_recent_print` | 合わせなくてよい | 12 想定元本 ÷ 直前 60 秒の最大の 1 件 |
| 14 | `oi_slope_and_funding` | 合わせなくてよい | 10 建玉の傾き・資金調達率 |
| 15 | `time_of_day_band` | 合わせなくてよい | 6 時刻帯 |



### Q7 を足した後の再実行(2026-09-20、2 回目。候補に `control_same_momentum` を足した)

リードの読み: Jev は Q7 の対照をどの意図にも「無関係」と置いた。**対照は意図を直接測る量ではなく、測った量が清算に固有かを確かめる部品**なので、この語は矛盾しない(研究の規律 §4.2 により対照を置く)。上位の量(ラベル・2 枝の損益・判断に条件付けた損益)は 1 回目と同じ。

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 設計文書: `docs/PHASE2/O3C/SIGNAL/SIGNAL_CONTINUE_DESIGN_2026-09-20.md`
- 候補ファイル: `config/jev_design_examples/signal6_observables.yaml`
- 意図: C-1, C-2, C-3, C-4, S-1, S-3, S-4

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### C-1 「**清算を検知→jevでこの清算は続くか判断**」(L-262)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `continuation_label` | 直接(3 以上) | 続くのラベル |
| 2 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率 対 基準率と規則 |
| 3 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 4 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 5 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |
| 6 | `payoff_conditional_on_decision` | 無関係(1 以下) | 判断に条件付けた損益の分布 |
| 7 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝の 1 件あたり損益 |
| 8 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(上限) |
| 9 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |
| 10 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |

### C-2 「**続くなら乗る、続かないなら逆張り**」(L-262)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `two_branch_pnl_per_print` | 直接(3 以上) | 2 枝の 1 件あたり損益 |
| 2 | `continuation_label` | 直接(3 以上) | 続くのラベル |
| 3 | `payoff_conditional_on_decision` | 代理(2 台) | 判断に条件付けた損益の分布 |
| 4 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率 対 基準率と規則 |
| 5 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(上限) |
| 6 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 7 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 8 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |
| 9 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 10 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |

### C-3 「**実際の運用で使うシグナルとしての価値**」(L-262)(印 △)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布 |
| 2 | `two_branch_pnl_per_print` | 代理(2 台) | 2 枝の 1 件あたり損益 |
| 3 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率 対 基準率と規則 |
| 4 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |
| 5 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 6 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(上限) |
| 7 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 8 | `continuation_label` | 無関係(1 以下) | 続くのラベル |
| 9 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 10 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |

### C-4 「**平均で計算したらわからなくなって当然**」(L-260)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布 |
| 2 | `material_distribution_by_outcome` | 代理(2 台) | 材料ごとの続いた / 止まったの分布 |
| 3 | `two_branch_pnl_per_print` | 代理(2 台) | 2 枝の 1 件あたり損益 |
| 4 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(上限) |
| 5 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率 対 基準率と規則 |
| 6 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 7 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 8 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 9 | `continuation_label` | 無関係(1 以下) | 続くのラベル |
| 10 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |

### S-1 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `payoff_conditional_on_decision` | 直接(3 以上) | 判断に条件付けた損益の分布 |
| 2 | `two_branch_pnl_per_print` | 代理(2 台) | 2 枝の 1 件あたり損益 |
| 3 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率 対 基準率と規則 |
| 4 | `price_move_conditional_on_true_label` | 代理(2 台) | 事後のラベルで分けた値動き(上限) |
| 5 | `rule_hit_rate_vs_base` | 代理(2 台) | 規則の的中率 対 基準率 |
| 6 | `continuation_label` | 代理(2 台) | 続くのラベル |
| 7 | `calibration_of_probability` | 代理(2 台) | 確率の較正 |
| 8 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |
| 9 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 10 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |

### S-3 「**値段の上がりすぎ下がりすぎ**」(原文 §1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `price_move_conditional_on_true_label` | 無関係(1 以下) | 事後のラベルで分けた値動き(上限) |
| 2 | `payoff_conditional_on_decision` | 無関係(1 以下) | 判断に条件付けた損益の分布 |
| 3 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |
| 4 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 5 | `continuation_label` | 無関係(1 以下) | 続くのラベル |
| 6 | `jev_hit_rate_vs_base` | 無関係(1 以下) | Jev の的中率 対 基準率と規則 |
| 7 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝の 1 件あたり損益 |
| 8 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 9 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 10 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |

### S-4 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `continuation_label` | 代理(2 台) | 続くのラベル |
| 2 | `price_move_conditional_on_true_label` | 代理(2 台) | 事後のラベルで分けた値動き(上限) |
| 3 | `jev_hit_rate_vs_base` | 代理(2 台) | Jev の的中率 対 基準率と規則 |
| 4 | `payoff_conditional_on_decision` | 代理(2 台) | 判断に条件付けた損益の分布 |
| 5 | `calibration_of_probability` | 無関係(1 以下) | 確率の較正 |
| 6 | `two_branch_pnl_per_print` | 無関係(1 以下) | 2 枝の 1 件あたり損益 |
| 7 | `rule_hit_rate_vs_base` | 無関係(1 以下) | 規則の的中率 対 基準率 |
| 8 | `forward_move_all_prints` | 無関係(1 以下) | 全プリントの平均と中央値(基準) |
| 9 | `material_distribution_by_outcome` | 無関係(1 以下) | 材料ごとの続いた / 止まったの分布 |
| 10 | `control_same_momentum` | 無関係(1 以下) | Q7 対照: 同じ勢いの清算の無い時刻に同じ判断 |



## 6. データと段

- 456 日(2023-06-27〜2024-10-12)、Binance COIN-M BTCUSD_PERP の清算 52,000 件(実測: 探索段 5 の `summary.json`「プリント数」と `rows_prints.csv.gz` の kind == print の行数。リード再計算 `20260920_o3c_signal_explore5_verify.log` 2 行目)+ 約定(475 zip)+ metrics(379 zip)+ 資金調達率(19 zip、委任先の実測 `20260920_o3c_cascade_read.md` §4)。前半 228 日 = 作る、後半 228 日 = 測る。
- 段 1: 材料の列を作る(探索段 5 の行データに列を足す)。段 2: Q1・Q2(前半で作り後半で測る)。段 3: Jev(Q3。まず 200 件で呼び出しと形を確かめ、上限を測ってから本数を決める)。段 4: Q4〜Q6。
- 未来を使わない検査: 材料の列は ts 以後の約定・清算を消した入力でも同じ値(p₀ だけは残す)。

## 7. 走らせる前に決めること(リードの決定)

- ラベル X = 60 秒(主)、30 / 120 を併記。同じ側だけ。
- 判断の閾値: 規則は前半の帯の続く割合が基準率を超えるか。Jev は確率 0.5(較正の表で他の閾値も示す)。
- 損益の価格 = 約定価格(`at_or_before`、穴 300 秒)。入る時点 = t₀ + 1 / 3 / 5 秒。出る時点 = **入る時点から** 30 / 60 / 300 秒後(保有期間。用語表と同じ)。
- Jev の呼び出し: 合成の状態で 3 回呼んだ下見(`docs/DATA/probes/20260920_jev_continue_call_probe.log`)= 1 回 0.4〜0.6 秒、入力 524 トークン。**200 件の下見は前半のプリントから層化無作為に取る**(後半のラベルに触れない)。**分岐の基準(事前)**: 200 件を 2 件/秒で送って 429 が 0 回、かつ API の応答か口座の表示で残り呼び出し数が 26,000 件以上 → 後半の全件。どちらかが満たされなければ後半から層化無作為 5,000 件(側 × 前半の材料 1 の 3 群で層化)。決めた根拠を報告に書く。
- 多重性: 行とセルを分けて引き継ぐ(探索段 5 まで: 行 2,149、セル 996、事後の切り 2)。**この単位の行の見積もり ≈ 470**(材料は 14 = §3.1 の 1〜6・8〜15): Q0 約 30、Q1 材料 14 × (10 帯 + 分位 2 群) = 168、Q2 規則 15(材料 14 + 組 1)× 3(30 / 60 / 120 秒)= 45、Q3 約 30(的中・較正 10 帯・一致)、Q4 判断 18(規則 15 + 全部続く + 全部止まる + 完全)× 入る 3 × 出る 3 × 枝 2 = 324 → **主格子(t₀ + 1 秒 → 60 秒)だけ全判断 = 36 行、他の格子は組の規則と完全な判断だけ = 2 × 8 × 2 = 32 行**、計 68、Q5 18、Q6 は Q4 に含む、Q7 規則 15 + Jev 1 = 16 × 枝 2 = 32 + 取れた割合の帯 10 = 42。合計 30 + 168 + 45 + 30 + 68 + 18 + 42 = **401**。委任先が数え直す。委任先が `tables.md` の数値セル数を数えて `summary.json` に書く。反証者のセルはリードが同じ数え方(`20260920_o3c_signal_explore3_refuter_cells.log`)で数える。
- テスト: ラベルの計算 / 前半・後半の分割 / 材料が未来を使わない / 2 枝の損益の符号 / `paper_logs/` を開かない / 判定語なし。
- 反証者レビュー(§11)を読みの前に。報告の冒頭に用語表と目的への答え。
- **△・✕ の扱い(§5.2)**: C-3(運用のシグナルとしての価値)と S-8 は bitFlyer で測っていない(S-6 ✕)ので、この単位の結果は「Binance で判断が当たるか・取り分があるか」までで、**運用の価値については報告に「不明(bitFlyer の経路差を測るまで)」と書く**。陰性とは書かない。
