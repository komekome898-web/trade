# 材料の選定(続き、R1・F5 を足す)— 委任の報告(2026-09-20)

委任文: `docs/DATA/delegations/20260920_o3c_signal_materials2_prompt.md`
設計: `docs/PHASE2/O3C/SIGNAL/SIGNAL_MATERIALS_DESIGN_2026-09-20.md`(§5.4 が足す 2 本の定義)
前回の委任文: `docs/DATA/delegations/20260920_o3c_signal_materials_prompt.md`
前回の報告: `docs/PHASE2/O3C/SIGNAL/MATERIALS_DELEGATE_REPORT_2026-09-20.md`
道具: `scripts/o3c_signal_materials.py`(既存に R1・F5 を追加)
試験: `tests/test_o3c_signal_materials.py`(既存に (8)〜(11) を追加)
出力: `backtest_data/o3c_signal_materials_20260920/`
実物読み: `docs/DATA/probes/20260920_o3c_materials_read.md`(上書き)

**探索段。判定語は使っていない(`check_no_banned` が `tables.md`・`summary.json`・実物読み
markdown の書き出し直前に検査、通過)。`paper_logs/` は開いていない。**

---

## (1) 設計 §0.1 の対応表(そのまま写し)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 止まる側の材料を足す | 「**止まる側の材料を足すべきであるのは明白**」(L-281) |
| 思いつきの列挙をやめ、手順で選ぶ | 「**材料の選定が雑で思いつきの域を出ていない**」(L-281) |
| 両側を同じ手順で選び直す(既存 14 個も同じ枠で書き直し、同じ基準でふるう) | 「**止まる側の材料選びと続く側の材料選びに差がうまれていない？**」(L-284) |
| 1 件目と 2 件目以降の 2 組で分ける | 「**なぜ「最初」はない？**」(L-285) |
| 分布(分位)で見る、平均は出さない | 「**平均で計算したらわからなくなって当然**」(L-260) |
| 境目は前半の表を全部見てから置く(後半に触れない・理由を書く・候補の数を数える) | 「**境目の決め方は結果から決められる？**」(L-289) |
| 未来を見ていないことが条件(窓の長さは揃えなくてよい) | 「**未来を見ていなければ問題ないと思うがどうか？**」(L-283) |
| Jev への問いは 1 問のまま | 「**清算を検知→jevでこの清算は続くか判断**」(L-262)、「**未来を見ていなければ問題ないと思うがどうか？**」(L-283) |
| 選んだ材料で 5,000 件の較正を取り直す | **リードの選択**(§3 の 5。材料が変われば較正も変わるため。原文に該当語なし) |
| 機構の枠 = 燃料 / 吸収 の 2 つ、分かれ方の数の定義、候補の一覧、実物読みの件数(各組 30 + 30)と抽出の規則(同じ日から 2 件以上取らない) | **リードの選択**(§1・§2・§3・§4 に理由) |
| 全体の進め方 | 「**進めてください**」(L-290) |

右列が空の行は無かった(前回と同じ設計文書の §0.1 をそのまま使っている)。擦り合わせ不要のまま
着手した。**足す 2 本(R1・F5)の定義は設計 §5.4 に明記されており、設計自身の中に食い違いは
見つからなかった。**

---

## (0) 行数・数値セル数

- `rows_materials.csv.gz`: **52,000 行**(前回と同じ母集団、`kind == "print"` の全件)。列は
  **35 列**(前回 33 列 + `cand_R1`・`cand_F5` の 2 列)。
- `tables.md` の数値セル数(`count_numeric_cells`): **700**(前回 652。組 A に R1 の 1 行、
  組 B に R1・F5 の 2 行が増えたぶん)。
- 表(`screen_A.csv` + `screen_B.csv`)の行数: **43 行**(組 A 19 + 組 B 24。委任文どおり)。
- 実物読み: **120 件**(前回と同じ 120 件、候補の値の表に R1・F5 の 2 行を追加)。

---

## (2) 実行したコマンドと出力の要所

```
$ nohup env PYTHONPATH=src:scripts python3 scripts/o3c_signal_materials.py --progress-every 20 > materials2_run.log 2>&1 &
日数 456(前半 228 / 後半 228)、プリント 52000 件
  段1 20/456 日 (2023-07-16) 経過 8s
  …(456 日、日ごとの chunk。再開可能 — `chunks/<day>.csv.gz` が既にあればスキップ)
  段1 440/456 日 (2024-09-26) 経過 251s
rows_materials 行数 = 52000
chunk の中間ファイルを 456 個消した
1b88b2483444a88e369706076e3927e9  rows_materials.csv.gz
06326f6c3a23552c4a39b1ebb1ecac2d  screen_A.csv
c8abde3f240c21403104c88151ad9dfd  screen_B.csv
9edd21f4a6ba87a382d9eeaa27175bd4  tables.md
283b32d9d6ebfccc0895604ccef2e12f  summary.json
50d6aa01e7429eab7df4568821696bbf  docs/DATA/probes/20260920_o3c_materials_read.md
完了 298s -> backtest_data/o3c_signal_materials_20260920
```

この道具は背景プロセスで走らせ(1 時間前後かかりうる想定だったが、実測 298 秒で完了)、
ログ `materials2_run.log` を `until grep -q '完了' … || ! pgrep …; do sleep 30; done` で
見張った。日ごとの chunk(`chunks/<day>.csv.gz`)は既に計算済みの日をスキップする作りなので、
途中で止めても再開できる(今回は中断なしで完走)。

既存 22 本の列の値が変わっていないことの確認(列ごとの完全一致、MD5 ではない):

```
$ python3 compare_cols.py rows_materials_v1.csv rows_materials_v2.csv
old shape (52000, 33) new shape (52000, 35)
added cols: ['cand_F5', 'cand_R1']
removed cols: []
ALL COMMON COLUMNS MATCH EXACTLY (列ごとの完全一致)
```

実物読み 120 件の並びと print_id が前回と一致することの確認:

```
$ diff probe_ids_v1.txt probe_ids_v2.txt && echo "並び・print_id 完全一致"
並び・print_id 完全一致
```

実物読み 120 件の「清算の列」「1 秒刻みの値段の列」が前回と変わっていないことの確認
(python で各件の該当ブロックを抜き出して比較):

```
120 120
mismatches: 0 / 120
```

全スイート:

```
$ PYTHONPATH=src python -m pytest
…
2704 passed, 5 skipped, 1 warning in 460.62s (0:07:40)
```

---

## (3) 自己点検

### R1・F5 の欠測・0 の割合(全 52,000 行、両半期、`summary.json` + 実測)

| 候補 | 欠測割合 | 0 の割合(全体) | 備考 |
|---|---|---|---|
| R1 | 0.0000 | 0.3502 | 負の値は 0 件(52,000 件中 0)。定義上「p_pre 自身が直前 60 秒の窓に入る限り 0 以上」になるはずで、実測もそのとおりだった |
| F5 | 0.0000 | 0.6198 | 組 A(1 件目、21,198 件)は **全件が 0**(0 件が非 0)。組 B(28,241 件)では 36.11% が 0(直前 10 秒に同じ側の清算が無い) |

R1 が「欠測 0.0000・負の値 0 件」なのは、設計 §5.4 の注記「正なら…戻っている、0 以下なら
戻り無し(p_pre が 60 秒の極値)」のとおり、**直前 60 秒に約定さえあれば p_pre 自身が窓の
中に必ず含まれる(p_pre は `ts−1` 以前で最新の約定なので、窓内に他の約定があればその約定
より新しいか同じ時刻になるため)**ので、最大値は理論上 0 以上にしかならない。52,000 件
全件で負の値が出なかったことを実測で確認した(上表)。

F5 が組 A で全件 0 になるのは設計どおり(「1 件目は定義上 0」)。組 A の 21,198 件を
`group == "A"` で絞って `cand_F5 != 0` の件数を数えると **0 件**だった(実測、下記 (6) 参照)。

### 既存 22 本の列の完全一致(列ごと、MD5 ではない)

前回の `rows_materials.csv.gz`(v1、33 列)と今回(v2、35 列)を `print_id` で突き合わせ、
共通の 33 列(`print_id` を含む識別子・メタ列 10 + 既存候補 22 + `cand_A9_count`)を
1 列ずつ比較した。**52,000 行 × 32 の非識別子列すべてで不一致 0 件。**新規に追加されたのは
`cand_F5`・`cand_R1` の 2 列だけで、既存列の削除は無い。

### 実物読み 120 件の一致

- 並び・`print_id`: 前回と**完全一致**(120 件とも同じ順序・同じ ID)。
- 各件の「直前 60 秒の清算(相対秒・側・想定元本)」表と「直前 60 秒の 1 秒刻みの値段」の
  コードブロック: 120 件すべてで前回と**完全一致**(不一致 0 件)。
- 各件の「候補の値」表に `R1`・`F5` の 2 行が新たに追加されている(既存 22 本の値の行は
  そのまま)。

---

## (4) 組 A・組 B の表(並べ替え済み、43 行)

### 組 A(|分かれ方 − 0.5| の降順、19 候補)

| 候補 | 単発_n | 単発_欠測 | 多件の最初_n | 多件の最初_欠測 | 分かれ方の数 | \|分かれ方−0.5\| |
|---|---|---|---|---|---|---|
| 14 | 5033 | 0.0000 | 3898 | 0.0000 | 0.3536 | 0.1464 |
| 8 | 5033 | 0.0076 | 3898 | 0.0110 | 0.5993 | 0.0993 |
| A6 | 5033 | 0.0000 | 3898 | 0.0000 | 0.5888 | 0.0888 |
| C3 | 5033 | 0.0000 | 3898 | 0.0000 | 0.5824 | 0.0824 |
| C4 | 5033 | 0.0000 | 3898 | 0.0000 | 0.4262 | 0.0738 |
| 15 | 5033 | 0.0020 | 3898 | 0.0005 | 0.4477 | 0.0523 |
| 9 | 5033 | 0.0079 | 3898 | 0.0033 | 0.5478 | 0.0478 |
| 11 | 5033 | 0.0000 | 3898 | 0.0000 | 0.5426 | 0.0426 |
| **R1** | 5033 | 0.0000 | 3898 | 0.0000 | 0.4664 | 0.0336 |
| 5p | 5033 | 0.6986 | 3898 | 0.8248 | 0.4683 | 0.0317 |
| 6 | 5033 | 0.0000 | 3898 | 0.0000 | 0.4789 | 0.0211 |
| A4 | 5033 | 0.0185 | 3898 | 0.0064 | 0.5209 | 0.0209 |
| 13 | 5033 | 0.0079 | 3898 | 0.0033 | 0.4877 | 0.0123 |
| 2 | 5033 | 0.0004 | 3898 | 0.0003 | 0.5108 | 0.0108 |
| 10 | 5033 | 0.0004 | 3898 | 0.0000 | 0.4912 | 0.0088 |
| 3 | 5033 | 0.0002 | 3898 | 0.0003 | 0.5019 | 0.0019 |
| 1 | 5033 | 0.0000 | 3898 | 0.0000 | 0.5000 | 0.0000 |
| A9 | 5033 | 0.0000 | 3898 | 0.0000 | 0.5000 | 0.0000 |
| 12 | 5033 | 1.0000 | 3898 | 1.0000 | (空) | (空) |

### 組 B(|分かれ方 − 0.5| の降順、24 候補)

| 候補 | 途中_n | 途中_欠測 | 多件の最後_n | 多件の最後_欠測 | 分かれ方の数 | \|分かれ方−0.5\| |
|---|---|---|---|---|---|---|
| 14 | 7968 | 0.0000 | 3898 | 0.0000 | 0.6292 | 0.1292 |
| 1 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5988 | 0.0988 |
| A6 | 7968 | 0.0000 | 3898 | 0.0000 | 0.4121 | 0.0879 |
| 2 | 7968 | 0.0000 | 3898 | 0.0003 | 0.4303 | 0.0697 |
| C3 | 7968 | 0.0000 | 3898 | 0.0000 | 0.4310 | 0.0690 |
| F3 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5682 | 0.0682 |
| 8 | 7968 | 0.0158 | 3898 | 0.0110 | 0.4379 | 0.0621 |
| **F5** | 7968 | 0.0000 | 3898 | 0.0000 | 0.5604 | 0.0604 |
| 15 | 7968 | 0.0001 | 3898 | 0.0000 | 0.5581 | 0.0581 |
| C4 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5527 | 0.0527 |
| 12 | 7968 | 0.0000 | 3898 | 0.0000 | 0.4503 | 0.0497 |
| 11 | 7968 | 0.0000 | 3898 | 0.0000 | 0.4524 | 0.0476 |
| A3 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5341 | 0.0341 |
| 5p | 7968 | 0.9174 | 3898 | 0.8463 | 0.5322 | 0.0322 |
| 13 | 7968 | 0.0001 | 3898 | 0.0013 | 0.5295 | 0.0295 |
| A5 | 7968 | 0.0001 | 3898 | 0.0008 | 0.5219 | 0.0219 |
| 6 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5215 | 0.0215 |
| 10 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5166 | 0.0166 |
| **R1** | 7968 | 0.0000 | 3898 | 0.0000 | 0.5142 | 0.0142 |
| 3 | 7968 | 0.0000 | 3898 | 0.0000 | 0.4859 | 0.0141 |
| A4 | 7968 | 0.0010 | 3898 | 0.0051 | 0.5057 | 0.0057 |
| 9 | 7968 | 0.0001 | 3898 | 0.0013 | 0.5029 | 0.0029 |
| F4 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5024 | 0.0024 |
| A9 | 7968 | 0.0000 | 3898 | 0.0000 | 0.5000 | 0.0000 |

分位(p10〜p90)は `screen_A.csv`・`screen_B.csv`・`tables.md` に全列ある(この報告には
n・欠測・分かれ方の数だけを再掲した)。**境目はここでは置いていない — リードが置く。**

---

## (5) 設計に無い判断(全部)

前回の委任(`MATERIALS_DELEGATE_REPORT_2026-09-20.md` の (5))で列挙した 15 件は、
委任文の指示どおりそのまま引き継ぐ(以下、番号は前回と同じ):

1. 5' の窓・ビン幅は既存材料 5 と同じ 8 時間・対数ビン 0.1%。
2. 5' の「先」の判定は既存 5 と同じ「出来高上位 10% のビン」から `sign*(center−p_ref)>0`
   を満たすものだけに絞り、最も近いものを採る。
3. 5' の基準価格は用語表どおり `p_pre = at_or_before(ts−1)`(既存 5 は `p_liq`)。
4. F3 の「連鎖(ts 基準)」は既存の `burst_start_ts` を使わず独立実装。
5. F4 の差の符号は `ts の直前 − 連鎖開始の直前`(正 = 増加)。
6. F4/A3 の「直前に確定していた」は `latest_at_or_before(t, v, ts_ms)`(時刻そのものを含む)。
7. A4 の分母は清算の側と同じ方向の taker 出来高(数量ベース)。
8. A5 の基準時刻の範囲は `path_extreme_bp` の `(lo_ms, hi_ms]`。
9. A6 の「更新」は狭義の更新(同値では更新済みとみなさない)。
10. A9 は想定元本を候補にし、件数は `cand_A9_count` に残すがスコアには数えない。
11. 候補 12 が組 A で構造的に 100% 欠測(F3 と同じ集合)。
12. ts 基準の「1 件目」と `bundle_pos` 由来の「1 件目」が 399 件(0.77%)食い違うが、
    その全件が「束の外」で組 A・組 B の表には影響しない。
13. `--stage resummarize` を道具に追加(入力を書き換えない)。
14. 候補 6(時刻帯)を出現順に 0〜3 の整数へ数値化。
15. 実物読みの 1 秒刻み値段の出力形式を `sec:bp` のコードブロックにした。

**この委任(続き)で新たに生じた判断**:

16. **R1 の窓境界の実装**: 設計は `[ts − 60,000, ts − 1]`(両端含む)と書いているが、
    既存の `path_extreme_bp(times, prices, lo_ms, hi_ms, p_ref, sign)` は
    `(lo_ms, hi_ms]`(下端は含まない、上端は含む)という規約なので、
    `lo_ms = ts − 60,000 − 1`、`hi_ms = ts − 1` として呼び出すことで
    `[ts−60,000, ts−1]` の両端を含む窓にした。
17. **R1 の符号は `s`(清算の向きそのまま)**: 隣接する A5 は `path_extreme_bp` を
    `-s`(清算と逆向き)で呼んでいるが、R1 は設計 §5.4 の「sign は清算の向き」の
    定義どおり `s` を渡した(A5 と混同しないよう関数呼び出しの直前にコメントで明記)。
18. **F5 の「自分を含まない」の実装**: 既に道具にあった `opposite_side_window_sum`
    (A9 用)と同じ規約 — `hi = ts` に `searchsorted(..., side="left")` を使うことで
    自分自身(ts にちょうど一致する行)を除外する — を、同じ側の合計を取る
    `same_side_window_sum` という新しい関数に流用した。設計は「自分を含まない」としか
    書いておらず、除外の実装方法(境界の付け方)はリードが選んだ。
19. **F5 は連鎖ではなく窓そのもので計算**: F3 が連鎖(ts 基準、直近の間隔 60 秒以内なら
    繋ぐ)の累計であるのに対し、F5 は連鎖を経由せず「直前 10 秒」という固定窓で直接
    同じ側の想定元本を合計した(設計の定義どおり。10 秒 < 60 秒〈連鎖の閾値〉なので、
    窓内の同じ側の清算は必ず同じ連鎖に属するが、実装上は連鎖の変数〈`chain_cum`〉を
    使わず `side_prefix` から直接引いた)。
20. **組 A から F5 を除く方法**: 既存の `CHAIN_INNER`(F3・F4・A3・A5 を組 A の候補から
    除く集合)に `F5` を追加して除いた。F5 は「連鎖の内側」ではなく「1 件目では定義上
    0」という別の理由での除外だが、除外先の集合・コードパスは既存のものを再利用した
    (コメントで理由の違いを明記)。
21. **実物読み markdown は差分パッチではなく全体を再生成**: `--skip-probes` を使わず
    道具を通常どおり実行したので、`docs/DATA/probes/20260920_o3c_materials_read.md`
    は(同じ乱数の種・同じ抽出規則で)丸ごと作り直された。結果として 120 件の並び・
    `print_id`・清算の列・値段の列は前回と完全一致し、候補の値の表にだけ `R1`・`F5`
    の 2 行が増えた(上記 (0)(3) で実測して確認)。
22. **道具は `--stage all`(単一起動)で走らせた**: 前回は `--stage resummarize` で
    集計だけの再生成もできたが、今回は R1・F5 の計算に約定の再読み込みが要るため、
    `rows`(日ごとの chunk)から通しで走らせる必要があった。chunk は日ごとに再開可能
    なので、10 分どころか 298 秒で完走したが、要求どおり日ごとの chunk の形は保った。
23. **R1・F5 の窓定数名**: `R1_WINDOW_MS = 60_000`、`F5_WINDOW_MS = 10_000` として
    既存の `A4_WINDOW_MS` 等と同じ命名規則に揃えた(設計の値そのものは変えていない)。

設計との食い違いは見つからなかった(§5.4 の 2 本の定義と、既存道具の関数の入出力規約との
橋渡しだけが判断を要した)。

---

## (6) サニティ

### 未来を使わない検査

`test_new_candidates_do_not_use_data_after_ts_or_p0` のループに `R1`・`F5` を追加し、
(a) `ts` より後の約定価格を全部変えた入力、(b) 「p₀ に当たる約定」の価格だけを書き換えた
入力の 2 通りで、既存 9 本・5' に加えて `R1`・`F5` も元の値と一致することを確認した
(`tests/test_o3c_signal_materials.py::test_new_candidates_do_not_use_data_after_ts_or_p0`、
green)。

### R1 の手計算(`test_r1_hand_computed`)

合成の約定列(3 点)で、(a) 直前に `p_pre` より先まで行って戻っているケース
(`@101 → @99 → @100`)で最大値が `(101−100)/100×1e4 = 100bp` と一致すること、
(b) 戻りが無いケース(単調増加で `p_pre` 自身が窓内の最大)で値が `≤ 0`(実測は厳密に
`0.0`)になること、(c) `p_pre` は staleness(300 秒)以内で拾えるが直前 60 秒の窓には
約定が無いケース(100 秒前の 1 件だけ)で欠測になること、の 3 通りを確認した。

### F5 の手計算(`test_f5_hand_computed`)

合成の清算列(5 件)で、窓の下端 `ts − 10,000` ちょうどの同じ側の清算は含み、
`ts − 10,001`(境界の外)は含まないこと、自分自身(`ts`)は含まないこと、反対側
(`opp_in`)は数えないこと、を確認した(`cnt == 2`、`amt == 200,000 + 300,000`)。
同じ側の清算が窓に無いケースでは `0.0`(欠測ではない)になることも確認した。

### 組 A・組 B のメンバーシップ(`test_f5_excluded_from_group_a_only`)

`"F5" not in CAND_GROUP_A`・`"F5" in CAND_NAMES`・`"R1" in CAND_GROUP_A` を確認した。
実測の `screen_A.csv`(19 行、`F5` 無し)・`screen_B.csv`(24 行、`F5` 有り)とも整合する
(上記 (4) の表)。

### R1 が理論上 0 以上になることの実測確認

52,000 行全件で `cand_R1 < 0` の件数を数えたところ **0 件**だった(§(3) 参照)。設計の
注記(「0 以下なら戻り無し」)と実装(`p_pre` 自身が窓に含まれる限り最小値は 0)が
整合していることの裏付けとして記録する。

### 再実行の決定性

道具は前回と同じ乱数の種(20260920)・同じ抽出規則で走らせたため、実物読み 120 件の
並び・`print_id`・清算列・値段列が前回と完全一致した(§(3) 参照。今回は `--stage
resummarize` は使わず `--stage all` を通しで実行したので、MD5 一致ではなく内容の
突き合わせで決定性を確認した)。

---

## (7) 限界

- **R1 の値の解釈**: 理論上ほぼ必ず 0 以上になる構造(§(6))のため、分布の半分近く
  (組 A で 35.0%、組 B で 27.0%)が厳密に `0.0` になる。これは「戻りが全く無い」
  プリントの割合であり、`5p` のような欠測とは性質が違う(実測して初めて分かった量的な
  性質で、実装のバグではない)。
- **F5 の「0」の二義性**: `F5 == 0` は「直前 10 秒に同じ側の清算が無かった」ことを
  意味するが、値だけからは「燃料が枯れている」のか「そもそも連鎖がまだ始まっていない
  (組 A に相当する状況)」のかを区別できない(組 B だけに限っても 36.11% が 0)。
- **既存の限界(前回の報告 (7) から引き続き有効、今回変えていない)**:
  5' の量子化(対数ビン幅 0.1% が局所変動より粗い場合の敏感さ)、読み込み窓
  `BACK_MS`(8h + 300s)を超える連鎖での F3・A3・A5・F4 の欠測可能性(件数は未測定)、
  候補 12 の組 A での 100% 欠測、A9 の「件数」情報がスコアに数えられていないこと、
  aggTrades の欠けている日(2023-09-25、2024-06-11、2024-06-12。今回の `stage_notes.json`
  でも同じ 3 日)。
- **境目・反証者レビュー・較正の取り直しはこの委任の範囲外**(設計 §3 の手順 3〜5、
  リードの仕事)。ここでは R1・F5 を足した表を渡すところまで。

---

## (8) 作ったファイル

- `scripts/o3c_signal_materials.py`(更新、R1・F5 の計算を追加、933 行)
- `tests/test_o3c_signal_materials.py`(更新、(8)〜(11) の試験を追加、452 行)
- `backtest_data/o3c_signal_materials_20260920/rows_materials.csv.gz`(更新、24 本の
  候補列 = 35 列、52,000 行)
- `backtest_data/o3c_signal_materials_20260920/screen_A.csv`(更新、19 行)
- `backtest_data/o3c_signal_materials_20260920/screen_B.csv`(更新、24 行)
- `backtest_data/o3c_signal_materials_20260920/screen_A_v1.csv`(前回の版、新規保存)
- `backtest_data/o3c_signal_materials_20260920/screen_B_v1.csv`(前回の版、新規保存)
- `backtest_data/o3c_signal_materials_20260920/tables.md`(更新)
- `backtest_data/o3c_signal_materials_20260920/tables_v1.md`(前回の版、新規保存)
- `backtest_data/o3c_signal_materials_20260920/summary.json`(更新)
- `backtest_data/o3c_signal_materials_20260920/stage_notes.json`(更新、内容は前回と同じ
  欠損 3 日)
- `backtest_data/o3c_signal_materials_20260920/MD5SUMS`(更新)
- `docs/DATA/probes/20260920_o3c_materials_read.md`(上書き、候補の値の表に R1・F5 の
  2 行を追加)
- 本報告 `docs/PHASE2/O3C/SIGNAL/MATERIALS2_DELEGATE_REPORT_2026-09-20.md`

他のファイルは変更していない。コミット・プッシュはしていない。

---

## (9) テストの末尾行

```
2704 passed, 5 skipped, 1 warning in 460.62s (0:07:40)
```

(`PYTHONPATH=src python -m pytest`、`-q` を足していない。`tests/test_o3c_signal_materials.py`
単体では 13 passed、2 skipped。)
