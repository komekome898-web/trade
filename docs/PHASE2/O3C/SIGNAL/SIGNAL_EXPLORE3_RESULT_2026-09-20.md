# 清算を起点とした値動きの予測可能性 — 探索段 3 の結果(清算の前の価格を基準に、行き過ぎか減衰か、2026-09-20)

**これは探索段の結果である**(research-protocol §0.0)。456 日は 1 周目・探索段 1・2 に続く **4 度目の読み**。判定語は付けない。判定は自前記録(触っていない、L-246)で一度だけ。
設計 `SIGNAL_EXPLORE3_DESIGN_2026-09-20.md`(監査 4 回)/ 道具 `scripts/o3c_signal_explore3.py` / 出力 `backtest_data/o3c_signal_explore3_20260920/`(246 行、コミット 0235cb6)/ 委任先の報告 `EXPLORE3_DELEGATE_REPORT_2026-09-20.md` / 反証者レビュー `REFUTER_REVIEW3_2026-09-20.md`(致命 6 件、全文)。**リードの再計算はすべて一致**(`docs/DATA/probes/20260920_o3c_signal_explore3_verify.log`、`…_refuter_verify.log`)。

## 用語(設計の用語表と同じ。意味を変えていない)

| 記号 | 意味 |
|---|---|
| 束 | gap 秒以内に続いた清算をひとつにまとめたもの(gap 60 が主)。`start_ms` = 最初の清算、`end_ms` = 最後の清算 |
| 基準(p_pre) | 束の最初の清算より前の最後の約定の価格(`at_or_before(start_ms − 1)`)。実測の間隔は中央値 96 ms、p90 911 ms(F1) |
| 終端(p_end) | 束の最後の清算の直後の約定(探索段 1・2 の起点と同じ) |
| h | 起点(終端)から何秒後の価格を見るかの時間幅。60 秒 / 5 分 / 15 分 |
| sweep | 基準 → 終端の符号付き変化(bp)。正 = 清算の向き |
| r_pre(h) | 基準 → h 後の符号付き変化(bp)。**負 = 基準より清算と逆の側** |
| r_end(h) | 終端 → h 後の符号付き変化(bp)。探索段 1・2 の r(Δ = 0, h) と同じ量 |
| f(h) | 戻り率 (p_end − p_h)/(p_end − p_pre)。1 = 基準まで戻った、> 1 = 越えた、< 0 = 続いた。sweep ≤ 0 では定義しない |

## 0.1 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 清算の前の価格を基準に、行き過ぎか減衰かを 456 日で測る | 「**値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認**」(原文 §1)、「**自前記録使ったら判定できなくなるからダメ**」(L-246) |
| 基準の取り方・h = 60 秒 / 5 分 / 15 分・後続の清算は条件付けせず全体と「180 秒以上」を併記・「基準を越えれば行き過ぎの修正、基準と終端の間なら減衰」 | 「**異論ないので進めてください**」(L-247。空の行として見せた上での承認) |
| 3 つ目の区分(f < 0)・f を比で置く・基準 5 分の NaN・**対照 (a) 掃き合わせ** | **(該当語なし)**— リードの具体化。2026-09-20 に見せた(返答待ち)が、**返答を待たずに委任した**(リードの決定。清算側の行は依らない)。異論があれば対照 (a) は報告から外す |
| 用語の定義を冒頭に置く | 「**h ってなに？**」(2026-09-20) |

## 1. オーナーの目的への答え(推定。反証者の致命 6 件を通したあと)

原文 §1「**清算の監視による値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認し、戦略へ落とし込む材料を見つける**」、L-243「**燃料が尽きる尽きないに関わらず、値動きの予想ができるなら戦略のシグナルとしての価値がある**」。

**問い**: 清算直後 60 秒の数 bp の戻り(探索段 2、−6.8 bp)は「行き過ぎの修正」(基準を越えて戻る)か「一時的インパクトの減衰」(基準までしか戻らない)か。

**答え**: **表の見出しの数(f > 1 が 50.1%、f の中央値 1.006、r_pre(60 秒)−0.12 ± 0.10)はどれも読めない。**掃きの大きさが違う束の混合比が作った数である(致命 1・2・3)。掃きの大きさで切り直すと、2 つの別のものが同時に見える:

1. **掃きが大きい束(sweep 上位 3 分位 > 8.08 bp、n = 4,743、掃き平均 26.8 bp)では「減衰」の形。**戻り率の中央値は 60 秒 0.514 → 5 分 0.622 → 15 分 0.734、f > 1 は 23.5 / 36.9 / 43.7%。基準からは 15 分たっても清算の側に **+11.3 ± 1.1 bp** 残る(F5・反証者、リード一致)。基準を越えて戻る形はこの群からは出ない。
2. **掃きがほぼ無い束では、掃きと無関係な数 bp の逆行。**sweep = 0(p_end = p_pre、4,468 束 = 21.1%)で 60 秒後 **−2.85 ± 0.14 bp**、sweep < 0(2,501 束)で **−6.06 ± 0.21 bp**(r_end。リード一致)。減衰すべき掃きが無いところで清算の反対側へ動いている。**設計の 2 分法はこの 3 つ目の形を受け取る箱を持っていなかった。**

**どちらが「予想の材料」かは、この表では決まらない = 不明**(陰性ではない。反証者の総括「支えない。否定もしない」と同じ)。

**なぜそうなるか(推定、次で測る)**: 基準が最初の清算の **96 ms 前**(中央値)にある。清算を引き起こした値動きはそれより前に起きているので、単発の束では「掃き」がその値動きを含まず(sweep = 0 が 21%)、束の後の逆行が「掃きの戻り」として測れない。**基準をもっと前(60 秒・5 分前)に置いた測定が要る。**

## 2. 反証者レビュー 3(逐語の要所。全文は `REFUTER_REVIEW3_2026-09-20.md`)

> **致命 1** 「f > 1 の割合 50%」は、分母が微小な束の産物である。… sweep > 0 の束 14,229 のうち、**23.9%(3,400 束)は掃きが値幅 1 ティックちょうど** … 幅 0(単発)かつ掃きが 1 ティックの束 3,350 件では、f の中央値は h=60 秒で **157.0** … sweep > 20 bp では f > 1 = **0.1175**、f 中央値 **0.420**。
> **致命 2** r_pre(60 秒)≈ 0 は「基準まで戻る」ではなく、大きい掃きと小さい掃きの打ち消しである。… sweep の 10 分位で −7.08 〜 **+24.39 bp**。振れ幅は 31 bp。
> **致命 3** 3 区分の母集団(14,229)と r_pre / r_end の母集団(21,198)が違い、除いた束は逆向きに偏っている。sweep > 0 の r_pre(60 秒)**+1.889 ± 0.147**、sweep ≤ 0 は **−4.233 ± 0.131**。
> **致命 4** 対照 (a) は、比較したい大きい清算をちょうど落としている。取れなかった 2,766 束の sweep 中央値 **18.95 bp**(取れた側 1.40)、r_end(60 秒)**−15.57**。… 対照 (a) の 45.0% は幅 W_b = 0 の束が相手。… 清算 − 対照の差(同じ 11,463 束で −6.385 対 −0.642)は母集団・掃きの帯・実経過のどれで揃えても残る(ここは私の反証が通らなかった点)。
> **致命 5** 「次の束まで 180 秒以上」の部分集合は、反転した束だけを選んでいる。… A のみ(次が遠い・前が近い)4,073 束 r_pre(300 秒)**−6.97**、B のみ **+11.43**。効きは 18.4 bp。… 「前の束から 180 秒以上」は全体とほぼ同じ(−0.124 → +0.056)。
> **致命 6** bitFlyer の列は h によらず +11 bp で一定 — r_pre ではなく「ずれた窓の掃き」である。基準は起点より平均 29.3 秒前 … h 後の位置については何も言えない。

リードの再計算(gap 60、`rows_gap60.csv.gz` から独立): sweep = 0 4,468 / < 0 2,501、sweep > 0 の r_pre(60) +1.889 ± 0.147、≤ 0 −4.233 ± 0.131、sweep > 20 の f > 1 0.1175・f 中央値 0.420、A のみ / B のみ 4,073 ずつ(r_pre(300) −6.968 / +11.425)、対照 (a) 取れなかった束の sweep 中央値 18.951、bf_r_pre(60/300/900) +10.979 / +11.008 / +11.260 — **すべて一致**。

## 3. 表の要所(事実。gap 60、出所は `f2_retrace.csv` / `f5_attributes.csv` / 反証者の計算 = リード一致)

| 群 | n | r_pre 60 秒 | r_pre 5 分 | r_pre 15 分 | f 中央値(60 秒) | f > 1(60 秒) |
|---|---|---|---|---|---|---|
| 清算 全体(表の行、読めない) | 21,198 | −0.12 ± 0.10 | −0.39 ± 0.22 | −0.75 ± 0.42 | 1.006 | 0.501 |
| sweep 下位 3 分位(≤ 0.20 bp) | 4,743 | −4.21 ± 0.14 | −4.33 ± 0.30 | −5.41 ± 0.60 | — | 0.682 |
| sweep 中位 3 分位(0.20〜8.08) | 4,743 | −2.84 ± 0.17 | −2.87 ± 0.34 | −2.75 ± 0.64 | — | 0.585 |
| sweep 上位 3 分位(> 8.08) | 4,743 | +12.72 ± 0.36 | +11.73 ± 0.58 | +11.31 ± 1.11 | 0.514 | 0.235 |
| sweep = 0(f 無し) | 4,468 | −2.85 ± 0.14 | −2.96 ± 0.36 | — | — | — |
| 対照 (a)(掃き合わせ、小さい掃き側だけの参照) | 11,463 | +4.67 ± 0.11 | +3.44 ± 0.16 | +2.66 ± 0.21 | 0.042 | 0.298 |

r_end(60 秒)は清算 全体 −6.80 ± 0.17 で探索段 2 の Δ = 0 と小数 6 桁まで同じ(試験)。対照 (b)-i −0.01 ± 0.01、(b)-ii −0.27 ± 0.05。

## 4. 取り下げるもの・使えないもの

- 「f > 1 が半分」「r_pre ≈ 0 = 基準まで戻る」「次の束が来なければ基準を越える(5 分 −4.96)」は**読まない**(致命 1・2・5)。
- 対照 (a) は「同じ大きさの値動きの後の戻り」の参照として、掃きが小さい側にだけ使える(致命 4)。
- bitFlyer の列(`bf_r_pre_*`)は h 後の位置を測っていない(致命 6)。他会場の裏取りには窓を合わせた再計算が要る。
- gap 180 の「次 / 前 180 秒以上」の 3 行は同じ集合(反証者、次の束の間隔 p1 = 186 秒)。

## 5. 射程・多重性(事実)

- 456 日、Binance COIN-M BTCUSD_PERP、4 度目の読み。表 246 行 + 反証者の切り直し約 60 セル。次の事前登録に引き継ぐ周回数: 552 + 318 + 約 90 + 246 + 約 60。
- 測っていないもの: 基準をもっと前に置いた掃き / 掃きゼロの束の逆行の文脈(直近の値動き)/ bitFlyer の窓を合わせた値 / 1 時間以上の h / 両側混在の束(gap 60 で 0.84%)。
- 清算行の多重度(反証者 2 の #7)は 472 日で `docs/DATA.md` の実測と一致(委任先の `summary.json`)。

## 6. 次の単位(リードの案。着手前に §0.1 の表を出す)

1. **基準を清算の前へ引く**(60 秒前・5 分前の 2 本)。清算を引き起こした値動きを掃きに含め、sweep = 0 の 21% を無くす。既存の行データでは足りず、新しい走行が要る(約定の読み 5 度目)。
2. 3 区分と f の分位を掃きの 3 分位ごとに出し直す(既存の行データ。走行不要)。
3. bitFlyer の基準を Binance と同じ時刻に合わせる。

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 清算の前の値動きを含める基準(60 秒前・5 分前)で、掃きゼロの束の逆行が「清算前の値動きの戻り」か「清算そのものの後の逆行」かを分ける | 「**値段の上がりすぎ下がりすぎ … を捉えることができるか確認**」(原文 §1)、「**なぜそうなるのかの理解 → 改良 → 再検証**」(L-020) |
| 基準の位置(60 秒前・5 分前)、456 日で 5 度目の読み | **(該当語なし)**— リードの案 |
