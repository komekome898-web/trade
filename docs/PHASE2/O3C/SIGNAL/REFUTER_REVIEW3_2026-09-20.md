# 反証者レビュー(research-protocol §11)— O3C/SIGNAL 探索段 3 の数表(2026-09-20)

読んだもの: `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md`(用語表を先に)/ `scripts/o3c_signal_explore3.py` / `backtest_data/o3c_signal_explore3_20260920/`(`tables.md` 246 行・`f0`〜`f5` の 5 csv・`rows_gap{30,60,180}.csv.gz`・`summary.json`・`stage_notes.json`・`MD5SUMS`)/ `docs/PHASE2/O3C/SIGNAL/EXPLORE3_DELEGATE_REPORT_2026-09-20.md` の (5) と (7) / `docs/PHASE2/O3C/SIGNAL/REFUTER_REVIEW2_2026-09-19.md` / 1 周目の表 `backtest_data/o3c_reaction_20260918_full/gap60_w8/table.csv` / 生の約定 `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/`(5 日ぶん)。**リードの読み・報告は渡されていない(まだ書かれていない)。`paper_logs/` は開いていない。**

「計算」と印を付けた値は、上の行データ・1 周目の表・生の約定 zip を pandas / numpy で自分で集計し直したものである(日クラスタ SE の式は道具の `mean_se_cluster` と同じ)。印の無い値は `tables.md` / `summary.json` / 委任先の報告からの引用である。作業ファイルはスクラッチパッドに置き、リポジトリには何も書いていない(このファイル 1 本を除く)。探索段なので判定語(陽性 / 陰性 / 有意)は 1 つも使わない。

---

## 0. 先に、再計算が一致したこと(問い 1 への答え)

**表の中心の量は設計の式どおりに計算されており、行データからの再計算と一致する。**

- 生の約定 zip から独立に基準・終端・h 後を引き直した(5 日 = 2023-06-27 / 2023-11-03 / 2024-03-13 / 2024-07-21 / 2024-10-12、gap 60 の清算 219 束)。`p_pre` / `p_end` は**全行で一致(差 0)**、`t_pre_ms` / `t_end_ms` も**全行一致**。`sweep_bp` / `r_pre` / `r_end` の最大差は 5.0×10⁻⁷(csv の小数 6 桁の丸め幅)。f の有限件数と f > 1 の件数も日ごとに一致(計算)。
- 21,198 束全体でも恒等式が成り立つ(計算): `r_pre(h) = sweep + (p_end/p_pre)·r_end(h)` の最大差 1.5×10⁻⁶、`f(h) = (p_end − p_h)/(p_end − p_pre)` の再計算との最大相対差 3.2×10⁻⁵。**sweep > 0 の束では f > 1 と r_pre < 0 が完全に同値(不一致 0 件 / 14,229)** — f は r_pre を sweep で割ったものの裏返し(`r_pre = sweep·(1 − f)`)であって、独立な量ではない。
- F2 の gap 60 の 6 行(清算 全体 / 対照 (a) の 3 つの h)を行データから作り直し、**r_end 平均・日クラスタ SE・naive SE・日等重み平均・f の中央値と p25/p75・3 区分の割合・n・含む日数が、`tables.md` の値と小数 4 桁まで一致した**(計算)。F1 の gap 60 全体の行、F3 の gap 60・h=60 の行も一致(計算、下の §3 に数値)。
- 対照 (a) の実装も設計どおり(計算): `sweep_bp` と `c_t` の差は全行 0、`c_t` の帯と相手の `S_b` の帯は **11,463 / 11,463 で一致**、`c_t ≤ 0` の行は 0 件、候補時刻は全行が 10 秒格子(`ctrl_t_ms % 10000 == 0`)。帯の切り値も sweep > 0 の 10 分位から再計算して `summary.json` と一致。F5 の sweep 3 分位の切り値も再計算して 0.200939 / 8.076017 = 表の 0.2009 / 8.0760。

**計算の欠陥は見つからなかった。以下の致命は、すべて「その数字をどう読むか」の欠陥である。**

---

## 致命(表の読みを変えるもの)— 6 件

### 致命 1 「f > 1 の割合 50%」は、分母が微小な束の産物である(問い 3)

gap 60 の sweep > 0 の束 14,229 のうち、**23.9%(3,400 束)は掃きが値幅 1 ティックちょうど**、27.4%(3,901 束)は 3 ティック以内である(計算、`|p_end − p_pre| / 0.1`)。sweep > 0 の p10 は 0.0167 bp = 1 ティック、p25 は 0.043 bp。f はこの 0.04 bp を分母にする。

sweep の下限で切ると、3 区分は次のように動く(計算、gap 60、h = 60 秒):

| 切り方 | n | f > 1 | 0 ≤ f ≤ 1 | f < 0 | f 中央値 | r_pre 平均(bp) |
|---|---|---|---|---|---|---|
| sweep > 0(表の行) | 14,229 | **0.5009** | 0.2675 | 0.2316 | **1.006** | −0.12(母集団は 21,198) |
| sweep > 0.05 bp | 10,563 | 0.4340 | 0.3570 | 0.2090 | 0.760 | +4.04 |
| sweep > 1 bp | 8,254 | 0.3728 | 0.4502 | 0.1770 | 0.666 | +6.27 |
| sweep > 5 bp | 6,088 | 0.2939 | 0.5522 | 0.1539 | 0.570 | +9.59 |
| sweep > 20 bp | 2,170 | **0.1175** | 0.7829 | 0.0995 | **0.420** | +24.02 |

設計の F5 の sweep 3 分位でも同じ(表の値、私の再計算と一致): Q1(sweep ≤ 0.2009)で f > 1 = **0.6821**、Q3(sweep > 8.076)で **0.2353**。

いちばん極端な部分集合: **幅 0(単発)かつ掃きが 1 ティックの束 3,350 件(sweep > 0 の 23.5%)**では、f の中央値は h=60 秒で **157.0**、h=300 秒で 193.5、h=900 秒で 287.5(計算)。この群の「0 ≤ f ≤ 1」は 0.9% しかない。**f は 0.017 bp の分母に対して数 bp の分子を割っているだけで、「基準を越えた」という語が指すはずの現象を測っていない。**

**読みへの影響**: 「f > 1 の割合がちょうど半分」「f の中央値が 1.006」は、**掃きがほぼ無い束と大きい束の混合比が半々だったという事実**であって、「半分の清算で行き過ぎの修正が起きる」ではない。F2 の 3 区分の 3 列と f の分位は、**掃きの大きさで切らずに読んではならない。**

### 致命 2 r_pre(60 秒)≈ 0 は「基準まで戻る」ではなく、大きい掃きと小さい掃きの打ち消しである(問い 4)

gap 60 全体の r_pre(60 秒) = −0.1238 ± 0.1038 は、sweep の 10 分位に分けると次のようになる(計算、日クラスタ SE):

| sweep の帯(bp) | n | sweep 平均 | r_pre(60 秒) | r_pre(300 秒) |
|---|---|---|---|---|
| (−13.34, −0.0156] | 2,120 | −0.756 | **−7.084 ± 0.234** | −6.574 ± 0.526 |
| (−0.0156, 0] | 4,849 | −0.001 | −2.986 ± 0.134 | −3.283 ± 0.346 |
| (0, 0.017] | 1,510 | 0.015 | −4.233 ± 0.215 | −3.983 ± 0.555 |
| (0.017, 0.0475] | 2,120 | 0.028 | −4.418 ± 0.214 | −4.520 ± 0.493 |
| (0.0475, 0.707] | 2,120 | 0.244 | −3.659 ± 0.230 | −4.417 ± 0.528 |
| (0.707, 4.394] | 2,119 | 2.419 | −3.475 ± 0.213 | −3.648 ± 0.501 |
| (4.394, 9.644] | 2,120 | 6.814 | −1.009 ± 0.237 | −0.747 ± 0.507 |
| (9.644, 20.409] | 2,120 | 14.133 | +3.864 ± 0.303 | +3.599 ± 0.541 |
| (20.409, 314.76] | 2,120 | 43.813 | **+24.385 ± 0.593** | +22.708 ± 1.007 |

**振れ幅は 31 bp である。**全体の −0.12 は、この 9 本を件数で重み付けして足したらたまたま 0 の近くに来た、というだけの数である。**「r_pre ≈ 0 だから基準まで戻る」と読むことはできない。**掃きが大きいほど基準の清算側に残り(上位 10 分位で +24 bp)、掃きが小さいほど基準の反対側へ抜ける(−3 〜 −7 bp)。

### 致命 3 3 区分の母集団(14,229)と r_pre / r_end の母集団(21,198)が違い、除いた束は逆向きに偏っている(問い 2)

`sweep ≤ 0` として f を定義しなかった束は gap 60 で **6,969 件 = 32.9%**(内訳は計算: sweep = 0 がちょうど 4,468 件 = 21.1%、sweep < 0 が 2,501 件 = 11.8%)。この除外は無作為ではない(計算、日クラスタ SE):

| 群 | n | r_pre(60 秒) | r_pre(300 秒) | r_end(60 秒) |
|---|---|---|---|---|
| sweep > 0(f が定義される = 3 区分の母集団) | 14,229 | **+1.889 ± 0.147** | +1.511 ± 0.272 | −8.171 ± 0.203 |
| sweep ≤ 0(f が NaN = 3 区分から落ちる) | 6,969 | **−4.233 ± 0.131** | −4.284 ± 0.297 | −4.002 ± 0.127 |
| 全体(表の「清算 全体」の行) | 21,198 | −0.124 ± 0.104 | −0.394 ± 0.223 | −6.800 ± 0.171 |

gap 30・gap 180 でも同じ向き(計算): gap 30 が +1.648 / −3.032、gap 180 が +6.313 / −4.406。

**F2 の 1 行の中で、`r_pre 平均` の列は 21,198 束、`f > 1 の割合` の列は 14,229 束の値である。**そして 2 つの母集団の r_pre は**符号が逆**(+1.89 と −4.23)。委任先は (7)-4 で母数の違いを書いているが、表は同じ行に並べているので、並べて読むと「r_pre はほぼ 0、でも半分は基準を越えている」という、**どちらの母集団にも当てはまらない文**ができる。3 区分を読むときは、その母集団の r_pre は **+1.89 bp(基準の清算側)**である。

### 致命 4 対照 (a) は、比較したい大きい清算をちょうど落としている(問い 5)

**(4-a) 取れなかった 19.4%(2,766 束)は大きい方に強く偏る**(計算):

| | 取れた 11,463 | 取れなかった 2,766 |
|---|---|---|
| sweep 中央値 / 平均(bp) | **1.403 / 6.107** | **18.951 / 26.397** |
| 束の幅 `bundle_width_ms` 中央値 | 2,984 | 22,999 |
| `bundle_n_events_dedup` 中央値 | 2 | 4 |
| r_end(60 秒) 平均(bp) | −6.385 | **−15.574** |
| r_pre(60 秒) 平均(bp) | −0.275 | **+10.855** |

対照 (a) が付く束の sweep 中央値は 1.40 bp。**対照を作れなかったのは、掃きが大きく・長く・清算の数が多い束、つまり「行き過ぎか減衰か」を分けられる唯一の群である。**この脱落は帯(候補の c_t がその大きさまで届かない)と重なり除外で決まるので、構造的に大きい掃きを落とす。

**(4-b) 対照 (a) の 45.0%(5,155 / 11,463)は幅 W_b = 0 の束が相手で、名目の窓は 1 ms である**(計算)。この群の対照は r_end(60 秒) = **−0.0122 ± 0.0887**、f の中央値 0.0000 — 事実上「無条件の次の 1 分」と同じである。「同じ大きさの値動きの後の戻り」の参照としての中身は薄い。

**(4-c) −6.80 対 −0.64 は母集団が違う 2 行の比較である。**表の「清算 全体」は 21,198 行、「対照 (a)」は 11,463 行。**同じ 11,463 束に揃えて並べ直すと**(計算):

| h | 清算(対照が取れた 11,463 束) | 対照 (a) 11,463 |
|---|---|---|
| 60 秒 | r_end **−6.385 ± 0.129** / r_pre −0.275 / f > 1 0.5404 | r_end **−0.642 ± 0.068** / r_pre +4.675 / f > 1 0.2979 |
| 300 秒 | r_end −6.616 ± 0.225 / f > 1 0.5478 | r_end −1.875 ± 0.146 / f > 1 0.4051 |
| 900 秒 | r_end −7.145 ± 0.487 / f > 1 0.5621 | r_end −2.655 ± 0.199 / f > 1 0.4515 |

**差は母集団の入れ替えでは消えない**(−6.80 → −6.385)。掃きの大きさの帯をそろえても消えない(計算、h=60 秒の r_end): S ∈ (0, 0.05] 清算 −4.155 / 対照 −0.180、(0.05, 1] −4.056 / +0.163、(1, 5] −5.781 / +0.109、(5, 20] −8.486 / −1.139、(20, ∞) −14.839 / −4.464。

**(4-d) それでも「価格で選んだこと」の影響は残る、ただし向きは差を**小さく**する側である。**対照の候補は c_t(価格から作る量)で取るので、「その向きへ動いた直後」を選んでいる。もしこの選び方が平均回帰を生むなら、**対照の側に戻りが出る**はずで、清算 − 対照の差は縮む。実測では対照の戻りは ≈ 0 なので、この経路は清算側の戻りを作り出してはいない。ただし**窓の実経過がそろっていない**(問い 9 と同じ): W_b = 0 の束で、実際に経過した時間(`t_end_ms − t_pre_ms`)の中央値は**清算 239 ms 対 対照 (a) 2,423 ms**(計算)。実経過で層別しても対照はどの層でも ≈ 0(計算、h=60 秒の r_end: [0,300) ms −0.206 ± 0.420 / [300,1200) −0.037 ± 0.242 / [1200,∞) +0.016 ± 0.094)、清算は同じ層で −4.993 / −3.004 / −1.557 なので、**実経過のずれで差が説明されることはない**(ここは私の反証が通らなかった点として書く)。

**読みへの影響**: 対照 (a) は「掃きが小さい束についてだけ」の参照である。**大きい掃きの束(行き過ぎか減衰かが判定できる唯一の群)について、対照 (a) は 19.4% を落としており、落とし方が結果と相関している。**表の −6.80 対 −0.64 をそのまま「清算の後だけ戻る」と読むのは、行の母集団の違いを見落とすことになる。

### 致命 5 「次の束まで 180 秒以上」の部分集合は、反転した束だけを選んでいる(問い 6)

2 つの部分集合の n が gap 60 でどちらも 15,145 なのは偶然ではない。**束 i の `next_bundle_gap_s` は束 i+1 の `prev_bundle_gap_s` と同じ量である**から、「次が 180 秒以上」の集合 A と「前が 180 秒以上」の集合 B は、**同じ束の対の前側と後側**であり、件数は必ず一致する(計算で確認: A ∩ B = 11,072、A のみ 4,073、B のみ 4,073)。

食い違いは、入れ替わる 4,073 束だけから来る(計算、r_pre、日クラスタ SE):

| 群 | n | r_pre(60 秒) | r_pre(300 秒) | sweep 平均 |
|---|---|---|---|---|
| 全体 | 21,198 | −0.124 ± 0.104 | −0.394 ± 0.223 | 6.671 |
| A = 次まで 180 秒以上 | 15,145 | −1.628 ± 0.114 | −4.964 ± 0.208 | 6.015 |
| B = 前から 180 秒以上 | 15,145 | +0.056 ± 0.124 | −0.017 ± 0.223 | 5.869 |
| A ∩ B | 11,072 | −1.295 ± 0.125 | −4.226 ± 0.224 | 5.380 |
| **A のみ**(次が遠い・前が近い) | 4,073 | **−2.531 ± 0.251** | **−6.968 ± 0.502** | 7.743 |
| **B のみ**(前が遠い・次が近い) | 4,073 | **+3.729 ± 0.244** | **+11.425 ± 0.522** | 7.198 |

**説明**: 「次の束が 180 秒以内に来る」= その 180 秒の間に清算がもう一度起きた、である。値段が清算の向きへ動き続ければ次の清算が誘発されるので、**続伸した束は A から落ち、B に残る。**だから B のみで r_pre = +3.73(清算の側に残っている)、A のみで −2.53(反対側へ抜けた)。**A(= 表の「次の束まで 180 秒以上」)は、続伸しなかった束を選別した集合である。**この行の f > 1 = 0.5565(h=60 秒)・0.6072(h=300 秒)は「反転が起きやすい」ことの根拠にならない。表の見出しに「結果依存の条件付け」と書いてあるのは正しいが、**どちらへ何 bp 効くかは書かれていない。効きは h=300 秒で 18.4 bp(A のみ −6.97 対 B のみ +11.43)である。**

一方 **B(過去の条件付け)は全体とほとんど同じ**(−0.124 → +0.056)。これは、基準に直前の束が混入している束を落としても表の値が動かないことを示している(設計 §7 が確かめたかったこと)。**設計の狙いどおりに効いたのは B の方で、A の差はコライダーの効きそのものである。**

なお gap 180 では、束化の規則から**次の束との間隔が構造上 180 秒を超える**(計算: 「next ≥ 180」の割合 0.9699、next_gap の p1 = 186 秒)ので、F2 の 3 行(全体 / A / B)は同じ集合をほぼ 3 回書いている。gap 180 の 3 行の差は情報を持たない。

### 致命 6 bitFlyer の列は h によらず +11 bp で一定 — r_pre ではなく「ずれた窓の掃き」である(問い 10)

gap 60 の清算側(計算、日クラスタ SE):

| h | `bf_r_pre` 平均(bp) | Binance の `r_pre` 平均(bp) | `bf_r_pre` の SD | 相関 | NaN |
|---|---|---|---|---|---|
| 60 秒 | **+10.979 ± 0.394** | −0.124 | 22.55 | 0.615 | 755 |
| 300 秒 | **+11.008 ± 0.484** | −0.394 | 33.63 | 0.793 | 903 |
| 900 秒 | **+11.260 ± 0.708** | −0.752 | 50.58 | 0.870 | 1,135 |

**平均が h で動かない(+10.98 → +11.01 → +11.26)のに SD は倍以上になる。**これは「h 後の位置」を測っている量の振る舞いではない。基準の足は `start_ms` を含む足の 1 本前の終値なので、**基準は起点より平均 29.3 秒(p90 53 秒)前である**(計算、`t_pre_ms % 60000`)。つまり `bf_r_pre` = (清算前 30 秒のドリフト)+(掃き)+(h 後の戻り)であり、Binance の掃き平均 6.67 bp と合わせると **+11 bp はほぼ全部が前の 2 項**である。

**読みへの影響**: `bf_r_pre` と `r_pre` を同じ表に並べると、**符号が逆の 2 つの量が同じ名前で並ぶ**(+10.98 対 −0.12)。「bitFlyer では基準を越えて戻っていない」とは読めない。窓を合わせない限り、この列から言えるのは「1 分足では清算の前後 1 分の間に清算の向きへ 11 bp 動いている」だけで、**h 後の位置については何も言えない。**(反証者 2 の #2 で「窓を書く」ことは扱われているが、書かれたのは注記であって、+11 bp が窓のずれで説明できることは測られていない。)

---

## 致命ではないが直すべきもの

1. **F1 の「前の束から 300 秒未満の割合」の分母を書く。**表の 0.3818 は NaN(その日の最初の束 456 件)を分母から除いた値で、21,198 を分母にすると 0.3736 になる(計算)。列の見出しに分母がない。
2. **F3 の「越えた幅 / sweep」は f − 1 そのもので、新しい情報が無い。**(計算で確認: 再計算した p50 13.3542 は F2 の f と同じ分布の裾。)しかも致命 1 と同じ分母支配で、p75 = 170 倍・p90 = 497 倍。「越えた幅」の bp の方(p50 6.27 bp)だけが読める量である。
3. **「sweep ≤ 0 の束」という書き方が、その 64%(4,468 / 6,969)が「1 ティックも動いていない束」(p_end = p_pre)であることを隠している**(計算)。「定義と逆」という語は sweep < 0 の 2,501 件にしか当てはまらない。
4. **F5 の sweep 3 分位の「属性が欠測で群に入らない束の割合 0.3288」は、欠測ではなく sweep ≤ 0 である。**他の属性(`doi_pre_1h` 0.2856 など)と同じ列に置くと、同じ種類の欠落に見える。
5. **gap ごとの比較には掃きの構成比の差を添える。**sweep > 5 bp の束の割合は gap 60 で 28.7%、gap 180 で 39.1%(計算)。この差だけで表頭の値はかなり動く(問い 7 の答え、下の §「問い 7」)。
6. **日クラスタ SE と naive SE の比を読むときの注意**(問い 8 は下に)。
7. **対照 (a) の候補は両側混在の束の区間を避けていない**(委任先 J-9)。gap 180 では混在が 2.87%(447 件)あるので、対照の候補時刻がそこに落ちうる。効きは未測定。
8. **多重性の 246 行は、致命 1 の処置(掃きの大きさで切り直して 3 区分を読む)を入れると増える。**次の事前登録に引き継ぐ数は、切り直した行数を足してから書くべきである。

---

## 残りの問いへの答え

### 問い 7 gap 180 の r_pre(60 秒) = +3.28 と gap 60 の −0.12 は整合するか

**整合する。差はほぼ全部が掃きの大きさの構成比である。**同じ sweep の帯で並べると(計算、r_pre(60 秒)):

| sweep の帯(bp) | gap 60 構成比 / r_pre(60) | gap 180 構成比 / r_pre(60) |
|---|---|---|
| ≤ 0 | 0.3288 / −4.233 | 0.2834 / −4.405 |
| (0, 0.05] | 0.1729 / −4.315 | 0.1435 / −4.366 |
| (0.05, 1] | 0.1089 / −3.938 | 0.0911 / −4.028 |
| (1, 5] | 0.1022 / −3.049 | 0.0908 / −3.056 |
| (5, 20] | 0.1848 / +1.597 | 0.2162 / +2.858 |
| (20, ∞) | 0.1024 / **+24.024** | 0.1751 / **+29.571** |

**帯ごとの値はほぼ同じで、gap 180 は上の 2 帯の構成比が 28.7% → 39.1% に増えているだけである。**「gap 180 では基準を越えていない」は機構の違いではなく、大きい掃きの束が多いことの言い換えである。

h の起点(終端)の問題は、h = 300 秒の方に出る。gap 180 の h=300 秒は**どの帯でも** gap 60 より負に寄る(≤0 帯で −6.872 対 −4.284、(5,20] 帯で −1.148 対 +1.639)。gap 180 の束は実経過の中央値が 2,504 ms(gap 60 は 1,046 ms、計算)で終端が遅く、**gap 60 なら「次の束」として現れる続伸が gap 180 では束の中に吸収される。**致命 5 の A / B の話と同じ機構が、束化の幅の側から効いている。

### 問い 8 日クラスタ SE と日等重み平均の食い違い(−0.12 対 −0.34)

**これは SE の話ではなく、点推定の重み付けの話である**(SE の式は平均を動かさない)。日等重み平均は束の多い日を軽くする。**その日の束の数とその日の平均 sweep の相関は +0.506、その日の平均 r_pre との相関は +0.156**(計算、456 日)。束が多い日 = 掃きが大きい日 = r_pre が正に寄る日なので、日等重みにすると平均が下がる。

実数(計算): 束の数の上位 10%(46 日、5,182 束)の行等重み平均 r_pre(60 秒) = **+0.241**、下位 50%(228 日、5,696 束)= **−0.563**。日ごとの平均 r_pre の分位は p5 −3.393 / p50 −0.469 / p95 +3.304 — **日によって符号が両側に振れる。**食い違いの 0.22 bp は、この日間のばらつきと束数の偏りの積であって、どちらかが正しいという話ではない。**どちらの数も、致命 2 のとおり掃きの大きさで切らないと意味を持たない。**

### 問い 9 F0 の遅れは 3 区分の比較に効くか

**h 後の遅れ(h=60 秒): 清算 p50 421 ms / p90 2,367 ms、対照 (a) p50 774 ms / p90 4,787 ms**(表の値)。60 秒の窓に対して 0.4 秒対 0.8 秒の差なので、h 後の位置の比較にはほとんど効かない(差は実効 h の 0.6%)。

**効くのは基準と終端の遅れの方である。**清算の基準の遅れ p50 は 95 ms・終端の遅れ p50 は 0 ms(= 終端は清算の約定そのもの)、対照 (a) は 812 ms と 715 ms。**結果として、名目で同じ幅 W_b の窓が、実際には清算で中央値 1,046 ms、対照 (a) で 9,445 ms の経過になる**(計算、全行)。W_b = 0 に限ると 239 ms 対 2,423 ms(計算)。**「同じ大きさの値動き」は大きさだけ合っていて、速さが 10 倍違う。**

ただし致命 4-(d) のとおり、実経過で層別しても対照側は 3 層とも ≈ 0(−0.206 / −0.037 / +0.016)で、清算側は −4.993 / −3.004 / −1.557 なので、**この遅れの差で清算 − 対照の差が説明されることはない**(測った。これは私の反証が通らなかった点である)。

### 問い 11 反証者レビュー 2 の「直すべき」10 件の扱い

| # | 反証者 2 の項目 | 設計 §8 の宣言 | 私が数表で確かめた扱い |
|---|---|---|---|
| 1 | 対照 (i) の 1 対 1 規則 | 扱う(日集約版のみ) | **扱われた。**対照 (b)-i は日集約で、1 対 1 の組は無い(`summary.json` の「対照 (b)-i」の定義、行数 21,378 = 1 周目の一様行の全数) |
| 2 | bitFlyer の窓 | 扱う(見出しに窓ずれを書く) | **注記は書かれたが、効きは測られていない。**→ 致命 6(実際のずれは平均 29.3 秒、+11 bp はほぼ全部これ) |
| 3 | 両側混在の除外率 | 扱う(F1 に gap ごと) | **扱われた**(0.0084 / 0.0032 / 0.0287)。ただし対照 (a) の候補はこの区間を避けていない(直すべき 7) |
| 4 | 短い h の 3 区分を群間で比べない | 扱う(h ≥ 60 秒・約定なしの割合を添える) | **扱われた。**h 後が終端と同じ割合は清算 0.0000、対照 (a) 0.0001、(b)-i 0.0009(表)— 桁がそろっている。この経路はもう効いていない |
| 5 | h ≥ 1 時間の「0」を「不明」と書く | 扱わない(この段に 1 時間以上の h が無い) | **妥当**(h は 60 / 300 / 900 秒のみ) |
| 6 | 日で等重みの点推定 | 扱う | **扱われた**(6 種すべてに列がある)。読み方は問い 8 |
| 7 | 清算行の 2 倍重複(3 日しか未確認) | 扱う(472 日を `summary.json` に) | **扱われた。**472 日・106,822 行 → 一意 53,398、多重度 2 が 53,385 群・4 が 13 群、`DATA.md` と 5 項目とも一致(`summary.json`)。私は再計算していない(生の zip を 472 日ぶん開き直していない = **未確認**) |
| 8 | 対照 (i) で落とす行の時刻の偏り | 扱わない(日集約版なので落ちる行が無い) | **妥当。**ただし**落ちる行は対照 (a) に移っており、そこでは偏りが大きい**(致命 4-a)。反証者 2 の #8 の懸念は消えたのではなく、対照 (a) に移った |
| 9 | 引き継ぐ周回数 | 扱う(552 + 318 + 約 90 + 246) | **扱われた**(設計 §7)。直すべき 8 の但し書きつき |
| 10 | `doi_pre_1h` / `implied_leverage` の欠測 | 扱う(F5 に「含む日数」「欠測の割合」の列) | **扱われた**(0.2856 / 0.3796、`implied_leverage` Q1 は 273 日)。ただし同じ列に sweep 3 分位の 0.3288 が入っていて意味が違う(直すべき 4) |

**扱われていないもの: #2 の実効(致命 6)。形を変えて残ったもの: #8(対照 (a) の脱落、致命 4-a)。**

---

## 使ったコマンド(「計算」と印を付けた値の出所)

すべて `python3` のヒアドキュメント(pandas / numpy)。作業用の pickle と読み込み関数は
`/tmp/claude-0/-home-user-trade/220780c0-d897-5de0-a902-2af69538ba02/scratchpad/ref3/` に置いた
(リポジトリには何も書いていない、コミットもしていない)。

1. 行の読み込み: `pd.read_csv("backtest_data/o3c_signal_explore3_20260920/rows_gap{30,60,180}.csv.gz")` → pickle。
2. **生の約定からの独立再計算**: `zipfile` + `pd.read_csv` で `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/BTCUSD_PERP-aggTrades-<日>.zip` を前後 1 日ぶん読み、自前の `searchsorted` で `at_or_before(start_ms − 1)` / `at_or_after(end_ms)` / `at_or_before(t_end + h*1000)`(許容 300,000 ms)を引き、`sweep = s(p_end − p_pre)/p_pre·1e4`、`r_pre = s(p_h − p_pre)/p_pre·1e4`、`r_end = s(p_h − p_end)/p_end·1e4`、`f = (p_end − p_h)/(p_end − p_pre)` を計算。束は `backtest_data/o3c_reaction_20260918_full/gap60_w8/table.csv` の `kind=='liq'` の `start_ms` / `end_ms` / `side`。5 日 = 2023-06-27 / 2023-11-03 / 2024-03-13 / 2024-07-21 / 2024-10-12。
3. 恒等式の検査(全 21,198 束): `r_pre − (sweep + (p_end/p_pre)·r_end)`、`f` の再構成、`(f > 1) != (r_pre < 0)` の件数。
4. 日クラスタ SE: `m=mean(v)`、`s_d=Σ_{i∈d}(v_i−m)`、`SE=sqrt(Σ_d s_d²)/n × sqrt(G/(G−1))`(道具の `mean_se_cluster` と同じ式)。naive SE = 母標準偏差 / √n。日等重み平均 = 日ごとの平均の単純平均。
5. F2 / F1 / F3 の再現: gap 60 の清算 全体・対照 (a) の 3 つの h について、上の式で平均・SE・f の分位・3 区分を出して `tables.md` と突き合わせ(小数 4 桁まで一致)。
6. ティック数: `round(|p_end − p_pre| / 0.1)`(BTCUSD_PERP の呼値 0.1 USD)。1 日ぶんの連続プリントの変化幅は `|diff(p)|/p·1e4`(2024-03-13: p50 0.0273 bp、1 ティック = 0.0139 bp @ 72,000)。
7. 掃きの分位・下限での切り直し: `np.nanpercentile(sweep[sweep>0], …)`、`sweep > 閾値` での 3 区分と f の中央値。
8. 対照 (a) の突き合わせ: `matched_liq_id` で清算行に結合し、`bundle_width_ms` を 1 周目の表から取る。帯は `summary.json` の切り値に `np.searchsorted(cuts, v, 'right')`。実経過は `t_end_ms − t_pre_ms`。
9. 部分集合の構造: `next_bundle_gap_s >= 180` と `prev_bundle_gap_s >= 180` の集合演算(A ∩ B、A のみ、B のみ)。
10. bitFlyer: `bf_r_pre_{60,300,900}` の日クラスタ平均・SD・`r_pre` との相関、基準の足のずれ `t_pre_ms % 60000`。
11. 日の重み: `groupby(day)` の束数・平均 r_pre・平均 sweep の相関、上位 10% / 下位 50% の日の比較。

---

## 総括

**数表は「予想の材料がある」を、いまの形では支えない。支えないが、否定もしない。**

1. **表の 3 つの見出しの数(f > 1 が 50%、f の中央値 1.006、r_pre ≈ 0)は、どれも掃きの大きさの混合比の産物である。**掃きが 1 ティック以下の束が sweep > 0 の 24%、0.2 bp 以下が 33% を占め、そこでは f が 80 〜 287 になる(計算)。**3 つの数はすべて、掃きの大きさで切って読み直さないと意味を持たない。**

2. **切り直すと、「行き過ぎの修正」と「一時的インパクトの減衰」が区別できる唯一の群 — 掃きが大きい束 — では、減衰の側が支持される。**gap 60 の sweep 上位 3 分位(> 8.076 bp、n = 4,743、掃きの平均 26.83 bp)で(計算):

   | h | f 中央値 | f > 1 の割合 | r_pre 平均(bp) | 集計の戻り率 `1 − r_pre平均/sweep平均` |
   |---|---|---|---|---|
   | 60 秒 | **0.514** | 0.2353 | +12.718 ± 0.365 | **0.526** |
   | 300 秒 | **0.622** | 0.3694 | +11.729 ± 0.578 | **0.563** |
   | 900 秒 | **0.734** | 0.4366 | +11.305 ± 1.106 | **0.579** |

   **15 分たっても掃きの半分強しか戻らず、基準の清算側に 11 bp 残ったままである。**「基準を越えて戻る」形は、この群からは出てこない。中位 3 分位(掃き平均 3.28 bp)でも r_pre は −2.8 bp で頭打ち(h=60 / 300 / 900 で −2.841 / −2.869 / −2.754)になり、**戻りの絶対量が掃きの大きさとほとんど無関係に数 bp で止まる**ことが、この 2 群に共通して見える。

3. **一方で、「減衰」でも説明できないものが残っている。**掃きがゼロの束(p_end = p_pre、4,468 件 = 21.1%)の r_end(60 秒)は **−2.848 ± 0.136**、掃きが逆向きの束(2,501 件)は **−6.064 ± 0.205**(計算)。**減衰すべき掃きが無いところでも、清算の反対側へ数 bp 動いている。**掃きが 1 ティックの束(3,400 件)でも −4.412 ± 0.157 で、これは 1 ティック(0.014 bp)の 300 倍以上であり、呼値や気配の跳ね返りでは説明できない大きさである(計算、連続プリントの変化幅 p50 = 0.027 bp)。同じ大きさで揃えた対照 (a) の同じ帯は −0.180 / +0.163(計算)。

   **つまり設計の 2 分法(行き過ぎの修正 / 一時的インパクトの減衰)は、どちらも「掃きが原因で動いた分が戻る」という形をしていて、この 3 番目の形 — 掃きの大きさに依らない数 bp の逆行 — を受け取る箱を持っていない。**この段の表からは、**掃きが大きい束では減衰(戻りは半分強で止まる)、掃きが小さい束では掃きと無関係な数 bp の逆行**、という 2 つの別々のものが同時に見えている、としか言えない。**どちらが「予想の材料」かは、この表では決まらない。**

4. **部分集合の行(「次の束まで 180 秒以上」)は使えない。**これは続伸しなかった束を選んだ集合で、入れ替わる 4,073 束の r_pre(300 秒)が −6.97 対 +11.43(計算)= 18.4 bp の効きがある。**この行の f > 1 = 0.6072 を「条件を付ければ反転が増える」と読むことはできない。**「前の束から 180 秒以上」は全体とほぼ同じで、基準への直前の束の混入が表を動かしていないことだけを示している。

5. **対照は、比較したい群について弱い。**対照 (a) は大きい掃きの束の 19.4% を落とし(落ちた方は sweep 中央値 18.95 bp、r_end −15.57 bp)、残った対照の 45% は名目 1 ms 窓である。清算 − 対照の差自体は母集団・掃きの帯・実経過のどれで揃えても残る(計算)が、**「同じ大きさの値動きの後の戻り」の参照として読めるのは掃きが小さい側だけである。**

6. **bitFlyer の列は、このままでは r_pre として読めない**(致命 6)。他会場での裏取りには、窓を合わせた再計算が要る。

**次に測ると読みが決まるもの**(順に): (i) 3 区分と f の分位を**掃きの 3 分位ごとに**出し直す(既存の行データでできる。新しい走行は不要)、(ii) 掃きゼロ・掃き 1 ティックの束の逆行が、束の外の文脈(その時点の直近の値動き)で説明できるか、(iii) bitFlyer の基準を Binance と同じ時刻に合わせた `bf_r_pre`。
