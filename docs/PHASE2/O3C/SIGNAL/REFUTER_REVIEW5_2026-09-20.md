# 反証者レビュー 5 — 探索段 5 の数表(2026-09-20)

> 立場: `research-protocol` §11 の反証者。リードの読みも報告も渡されていない(まだ書かれていない)ので、
> **数表そのものを「清算の後の値動きに予想の材料がある」という読みに反対する側から**検査した。
> 読んだもの: 設計 `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md` / 道具 `scripts/o3c_signal_explore5.py` /
> 試験 `tests/test_o3c_signal_explore5.py` / 出力 `backtest_data/o3c_signal_explore5_20260920/`
> (`tables.md` 535 行・`h0`〜`h6` の csv・`rows_prints.csv.gz`・`summary.json`・`stage_notes.json`)/
> 委任先の報告 `EXPLORE5_DELEGATE_REPORT_2026-09-20.md` の (5)(6)(7) /
> 文脈として `REFUTER_REVIEW4_2026-09-20.md` と `SIGNAL_EXPLORE4_RESULT_2026-09-20.md`。
> `paper_logs/` は開いていない。
> **探索段なので判定の語は使わない。**
> 数値に「計算」と書いたものは、行データ `rows_prints.csv.gz` と清算の生の zip から
> 反証者が自分で計算し直した値である。末尾に使ったコマンドと出力を置く。
>
> **用語**(設計の用語表と同じ): プリント = 清算 1 件。t₀ = p₀(清算の時刻 `ts` 以後の最初の約定)の約定の時刻
> = 「その時点で分かる」の境界。m(T) = `ts` の T 秒前から `ts` 直前までの、清算の向きへの値動き(bp)。
> k = `ts` 直前 → p₀ の符号付き変化(bp)= このプリント 1 件の掃き。d = 直前 5 分の同じ側のプリント数。
> r(h) = p₀ → t₀ + h 秒の符号付き変化(bp、**負 = 清算と逆へ戻った**)。
> 束の位置 = 探索段 4 の gap 60 の束の中での位置(未来の情報を使う量なので H0 の診断だけ)。

## 0. 先に、再計算が一致したこと(問い 1 への答え)

行データだけを入力に、SE・日等重み平均・分位・帯・3 分位を**委任先の道具を import せずに**組み直し、
出力の csv の数値セルと突き合わせた。**532 行 / 9,444 セルで、一致しないセルは 0 件**(計算)。

| 表 | 再計算した範囲 | 結果 |
|---|---|---|
| H1 | **全 7 行 × 21 列**(平均・日クラスタ SE・naive SE・比・日等重み平均・中央値・p25/p75・r<0 の割合と母数・次の清算 60/120 秒の割合と母数・対照 (b) の 3 列) | 全一致 |
| H2 | **全 140 行**(T 2 × 帯 10 × h 7)。帯 1 / 5 / 10 を含む | 全一致 |
| H3 | **全 36 行**(k 群 4 × m 3 分位 3 × h 3) | 全一致 |
| H4 | **全 60 行**(対照 (c')、T 2 × 帯 10 × h 3) | 全一致 |
| H5 | **全 27 行** | 全一致 |
| H6 | **全 162 行**(側 2 + 想定元本 3 + 時刻帯 4 + `bin_pct` 3 + `\|dist_node_bp\|` 3 + `implied_leverage` 3 = 18 群 × m 3 × h 3) | 全一致 |
| H0 診断 | 束の位置 21 行(n・t₀ 基準の平均・ts 基準の平均・2 つの起点で値が違う行・中央値) | 全一致。「最後」h=60 = **−6.800366**(t₀ 基準)/ **−6.802065**(ts 基準) |
| H0 起点の取り方 | 7 行 | 全一致 |
| H0 対照 (c') | 38 行(取れなかった割合・帯別・中央値・相関 −0.7890 / −0.7534・\|c_t − m_p\| の分位・c_t・k_ctrl) | 全一致 |
| H0 遅れ・分布・欠測 | 11 + 12 + 11 行(p₀ の遅れ・m の窓の遅れ・h 後の遅れ・m/k/d の分位・欠測 11 種) | 全一致 |
| 重複除去 | 清算の zip **472 本を生から数え直し** | **106,822 行 → 全列一致で 53,398 件**(表と一致)。日を跨ぐ全列一致は **0 組**(委任先が「未確認」と書いた点を測った) |
| セル数 | `tables.md` の行数と数値セル数 | **535 行 / 11,059 セル**(`summary.json` と一致)。表の見出し行は 7 |

**一致しない行は 0 件である。**以下の指摘は、値が間違っているという指摘ではなく、
**その値をどう読めるかの指摘である。**

---

## 致命(表の読みを変えるもの)— 6 件

### 致命 1 H1 の h=60 の平均 −0.3962 bp は、5 日と 2,561 件の「束の外」が作った値で、中心ではない

**事実(計算)**。h=60 の平均への日ごとの寄与(Σ_{i∈日} r_i / 52,000)を大きい順に並べる:

| 量 | h=60 | h=300 | h=900 |
|---|---|---|---|
| 全プリントの平均 | **−0.3962** | −2.9970 | −4.0581 |
| 上位 5 日の寄与の和 | **+0.9202**(全体平均の −232%) | +0.4473(−14.9%) | −0.0743(1.8%) |
| 上位 10 日の寄与の和 | +0.8793(−222%) | +0.1800(−6.0%) | −0.1791(4.4%) |
| 日の平均が負の日 | 332 / 456(72.8%) | 365 / 456(80.0%) | 365 / 456(80.0%) |
| 日の平均の中央値 | **−1.5581** | −4.2819 | −6.6750 |
| 日等重み平均(表の列) | −1.2903 | −4.0045 | −6.2806 |
| プリントの中央値(表の列) | −1.3766 | −4.0258 | −6.5397 |

h=60 の寄与の上位は `2023-08-17`(+0.6003、その日 408 件、日の平均 **+76.51 bp**)、
`2023-10-16`(+0.1575)、`2023-10-23`(+0.1503)、`2024-01-03`(+0.0710)、`2024-01-09`(−0.0588)。

さらに、**探索段 4 の束に入らない 2,561 件(4.9%、75 日)**だけで h=60 の平均に **+0.9697** を寄せている
(その群の r(60) 平均 **+19.6884**、中央値 +5.3342)。この 2,561 件を外すと全プリントの平均は
**−1.4366** になり、日等重み平均・中央値と同じ桁に揃う。

| h | 全部 | 束の外を除く | 寄与上位 5 日を除く | 両方除く |
|---|---|---|---|---|
| 60 | −0.3962 | **−1.4366** | −1.3571 | −1.3675 |
| 300 | −2.9970 | −2.9442 | −3.6181 | −2.8801 |
| 900 | −4.0581 | −2.6429 | −4.1408 | −3.2745 |

裾の効き方(h=60、計算): r>0 は 23,187 件で平均 +16.791、r<0 は 28,703 件で平均 −14.282、r=0 は 110 件。
上下 1% を落とすと平均は −1.2231、上下 5% を落とすと −1.4797。

**読みへの影響**: 表は各行の**左から 6 列目に平均**を、**18 列目に中央値**を置いている。
平均の列だけを見ると h=60 は「ほぼ 0」と読め、中央値・日等重み平均だけを見ると「−1.3 bp」と読める。
**この 2 つは 3 倍以上違い、どちらを読むかで結論が変わる。**
h=60 について言えるのは「日を等しく扱えば中心は −1.3 〜 −1.6 bp、プリントを等しく扱えば 5 日で符号が動く」までである。

### 致命 2 H3 の k の群のうち K+1 と K+2 は「掃きの大きさ」ではなく「その日の BTC の価格水準」で切った層である

**事実(計算)**。k の分位は p10 = −0.0777 / p25 = 0.0000 / **p50 = 0.0145** / p75 = 0.0336 / p90 = 0.2701 bp。
k = 0 ちょうどが 13,969 件(26.9%)、k < 0 が 11,008 件(21.2%)。
k をティック数(k × 10⁻⁴ × p₀ / 0.1 USD)に直すと分位は −4 / 0 / **1** / 1 / 14 / 117 枚である。
**中央値のプリントの掃きは、ちょうど 1 ティックである。**

k > 0 の 3 分位の切り値 0.022345 / 0.086693 bp は、1 ティック(0.1 USD)がその値になる価格
**44,760 USD / 11,534 USD** と同じ意味しか持たない。群ごとの内訳(計算):

| 群 | n | ちょうど 1 ティックの割合 | p₀ の中央値 | 多い月 |
|---|---|---|---|---|
| K+1(k ≤ 0.0223) | 9,008 | **1.000**(9,008 / 9,008) | 63,043 USD | 2024-03 / 2024-04 / 2024-08 |
| K+2(0.0223 < k ≤ 0.0867) | 9,007 | 0.719 | 38,054 USD | 2023-12 / 2024-01 / 2023-11 |
| K+3(k > 0.0867) | 9,008 | 0.000 | 58,628 USD | 2024-03 / 2024-08 / 2024-04 |

**K+1 は 1 件残らず「1 ティック」の掃きである。**K+1 と K+2 の違いは掃きの大きさではなく、
BTC が 44,760 USD より上だったか下だったかである。

**読みへの影響**: H3 の 36 行のうち 18 行(K+1・K+2)は、設計が意図した「掃きの層」ではなく暦の層である。
探索段 4 の致命 2(「逆行の大きさは掃きにほぼ等しい」)を受けて k を層にした設計の狙いは、
**プリント 1 件の単位では成り立たない**。束の掃きとプリントの掃きは別の量である。
K0(k ≤ 0)の 24,977 件のうち 13,969 件は k = 0 ちょうどで、これも「掃きが小さい」ではなく
「同じティックで約定した」である。

### 致命 3 対照 (c') は「同じ m の、清算が無い時刻」ではなく「15 分間清算が 1 件も無い静かな区間」で、対で引くとプリントのほうが戻りが小さい

**構造(事実)**: 候補時刻は `[t − T, t + 900 秒]` に**どの側のプリントも無い**時刻だけである。
一方プリント側は **58.47%** が 60 秒以内に同じ側の次の清算を持つ(H1 の列)。
つまり対照は定義により「15 分以上清算が来ない区間」だけから取られている。

偏りの実測(計算、すべて表の値と一致):

| 量 | T = 10 | T = 60 |
|---|---|---|
| 取れた割合 | 18,943 / 52,000 = 0.3643 | 16,943 / 52,000 = 0.3258 |
| 帯 9 / 帯 10 が取れなかった割合 | 0.9790 / 0.9971 | 0.9904 / 0.9998 |
| 日のプリント数と 1 件あたり取れた対照の数の相関 | −0.7890 | −0.7534 |
| 取れた側 / 取れなかった側の m の中央値 | 4.086 / 11.765 | 8.871 / 25.221 |
| 取れた側 / 取れなかった側の r(60) の平均 | −0.1067 / −0.5620 | +0.1024 / −0.6371 |

**時刻の偏り(計算、表に無い)**: プリントは UTC 12–18 に 20,497 件(39.4%)、UTC 06–12 は 8,989 件(17.3%)。
対照はほぼ一様(T=10 で UTC 18–24 4,913 / UTC 00–06 4,884 / UTC 06–12 4,670 / UTC 12–18 4,476)。
**ばらつきも半分である**(対になった部分集合で、プリントの r(60) の標準偏差 13.95 に対し対照 6.15、
IQR 10.58 に対し 6.02。T=10)。

**対で引いた差(計算、表に無い。日クラスタ SE)**: 対照が取れたプリントとその対照だけを、同じ行で引く。

| T | h | 対のプリント 平均 | 対照 平均 | **差(プリント − 対照)** | 差の日クラスタ SE | 差の中央値 |
|---|---|---|---|---|---|---|
| 10 | 60 | −0.1067 | −0.2734 | **+0.1667** | 0.1266 | −0.4508 |
| 10 | 300 | −0.5227 | −1.3280 | **+0.8053** | 0.3913 | −0.9688 |
| 10 | 900 | −0.8269 | −1.8548 | **+1.0279** | 0.6761 | −1.9180 |
| 60 | 60 | +0.1024 | −0.8885 | **+0.9910** | 0.1322 | +0.0514 |
| 60 | 300 | −0.3869 | −1.9785 | **+1.5916** | 0.3645 | −0.2550 |
| 60 | 900 | −0.9217 | −2.4756 | **+1.5539** | 0.5948 | −1.2591 |

**6 本すべて符号が同じで、清算のあったほうが戻りが小さい。**r < 0 の割合も T=60 では
プリント 0.5321 / 対照 0.5383(h=60)、0.5598 / 0.5667(h=300)と対照のほうが高い。
(中央値の差は T=10 で逆を向く。平均と中央値が食い違うのは致命 1 と同じ裾の効きである。)

**読みへの影響**: H4 を H2 の横に置いて「清算があると余分に戻る」と読むことはできない。
H2 の帯の行は 5,200 件すべて、H4 の同じ帯の行は対照が取れた側だけで、**母集団が違う**。
同じ行に揃えて引くと、向きはむしろ逆である。
帯 9・10 を読まないという設計の約束(§4)は守られているが、**読めないのは帯 9・10 だけではない。**

### 致命 4 H0 の診断の 3 群は排他ではなく、12,029 件が「最初」と「最後」の両方に入っている。「最後」の −6.80 は 2 つの別の群の混合である

**事実(計算)**: 行データの `bundle_pos` の実数は 最後 21,198 / 途中 19,072 / **最初 9,169** / 束の外 2,561
(合計 52,000)。表の「最初 21,198」は 9,169 に**単発の束 12,029 件**を足した数で、
その 12,029 件は「最後」の 21,198 件にも入っている(委任先の D-15 のとおり)。
**3 群の n の合計は 61,468 で、束に入るプリント 49,439 件を 12,029 件ぶん超えている。**

排他に分け直すと(計算、日クラスタ SE):

| 群 | n | r(60) | 中央値 | r(900) |
|---|---|---|---|---|
| 多件の束の**最初**(単発を除く) | 9,169 | **+5.182 ± 0.226** | +3.548 | +4.332 ± 0.735 |
| 途中 | 19,072 | +1.343 ± 0.358 | +1.520 | −0.675 ± 2.382 |
| **単発の束**(最初かつ最後) | 12,029 | **−3.971 ± 0.113** | −2.798 | −4.660 ± 0.345 |
| 多件の束の**最後**(単発を除く) | 9,169 | **−10.512 ± 0.258** | −7.634 | −11.065 ± 0.642 |
| 束の外 | 2,561 | +19.688 ± 12.885 | +5.334 | −31.377 ± 10.206 |

**表の「最後」の −6.800366 は、−3.971 と −10.512 の 1 : 1 に近い混合である。**
表の「最初」の行は **−0.0120**(t₀ 基準)/ 中央値 −0.6348 であって、+5.18 ではない。
**問い 6 が前提に置いた「最初 +5.18」は `tables.md` にも csv にも無い値で、
上の表の「多件の束の最初(9,169 件)」とだけ一致する**(計算、`grep` で全出力を探して 0 件)。
この行き違い自体が、表の 3 群が排他でないことから来ている。

### 致命 5 t₀ で分かる量では「最後」に当たる層を作れず、当てにいくと逆行が消える

「最後」であるかはプリントの時点では分からない(設計の用語表もそう書いている)。
**t₀ で分かる量でどれだけ当てられるか**を測った(計算、表に無い)。
「最後」の割合は全 52,000 件の中で **0.4077**、束に入る 49,439 件の中で 0.4288。

| t₀ で分かる層 | n | 「最後」の割合 | r(60) 平均 | r(60) 中央値 |
|---|---|---|---|---|
| d = 0 | 14,041(27.0%) | **0.6183** | **+0.148 ± 0.122** | −0.489 |
| d = 1〜2 | 14,382 | 0.4600 | −0.0385 | −1.0050 |
| d = 3〜5 | 10,332 | 0.3274 | −0.9257 | −1.9378 |
| d ≥ 3 | 23,577(45.3%) | 0.2502 | −0.938 ± 1.509 | −2.987 |
| 直前の同じ側のプリントから 600 秒超 | 11,353(21.8%) | **0.6427** | **+0.132 ± 0.128** | −0.394 |
| 直前の同じ側から 10 秒以内 | 19,769 | 0.2676 | −0.9102 | −3.0158 |
| 直前の同じ側から 3,600 秒超 | 5,064 | **0.7109** | +0.0588 | −0.4868 |

**「最後」をいちばん当てられる層(d = 0)の r(60) は +0.148 ± 0.122 で、−6.8 bp の向きですらない。**
理由は分解すると出る(計算):

| d = 0 の内訳 | n | r(60) |
|---|---|---|
| 単発の束 | 8,682 | **−3.238 ± 0.115** |
| 多件の束の最初 | 5,164 | **+5.633 ± 0.250** |
| 束の外 | 195 | +5.633 ± 3.910 |

**d = 0 は「これが最後の清算だった」場合(−3.24)と「これから束が始まる」場合(+5.63)を
1 : 0.6 で混ぜており、t₀ ではこの 2 つを区別できない。**
探索段 4 の −6.8 bp に近づく層は、この表の中の t₀ で作れる層には無い。

### 致命 6 H1 の 7 行のうち、h=60 は日クラスタ SE の幅の中にあり、いちばん短い h=1 は符号が逆である

**事実(表の値、計算で一致)**:

| h | 平均 | 日クラスタ SE | \|平均\| / SE | 2 SE の幅 | 中央値 | 日等重み平均 | 日/naive |
|---|---|---|---|---|---|---|---|
| 1 | **+0.1486** | 0.0323 | 4.60 | ±0.06 | −0.000 | +0.078 | 1.51 |
| 5 | −0.0642 | 0.1185 | 0.54 | ±0.24 | −0.348 | −0.281 | 2.42 |
| 10 | −0.2608 | 0.1915 | 1.36 | ±0.38 | −0.622 | −0.572 | 2.86 |
| 30 | −0.4794 | 0.4008 | 1.20 | ±0.80 | −0.965 | −1.046 | 4.01 |
| 60 | −0.3962 | 0.6810 | **0.58** | ±1.36 | −1.377 | −1.290 | **4.66** |
| 300 | −2.9970 | 0.7577 | 3.96 | ±1.52 | −4.026 | −4.004 | 3.24 |
| 900 | −4.0581 | 1.0660 | 3.81 | ±2.13 | −6.540 | −6.281 | 3.45 |

**平均の大きさが日クラスタ SE の 2 倍を超えるのは h=1・h=300・h=900 の 3 本だけで、
そのうち h=1 は清算の向きへ +0.1486 と符号が逆である。**
探索段 4 が中心に置いた h=60 は、プリントを起点にすると幅の中に入る。

**しかもこの「2 SE」の篩は、清算が 1 件も無い対照でも通る**(計算):
H4(対照 (c')、60 行)のうち **50 行**が「平均が −2 SE より負」を満たす。
H2 は 140 行中 42 行、H3 は 36 行中 16 行、H5 は 27 行中 6 行、H6 は 162 行中 60 行である
(日等重み平均で見ると H4 50 / H2 90 / H3 24 / H5 19 / H6 126)。
**清算の無い時刻を並べても同じ篩を通るので、この篩だけでは清算に固有のものを選り分けられない。**

---

## 直すべき — 11 件

1. **平均の列と中央値の列の並び。** 致命 1 のとおり、同じ行の平均と中央値が h=60 で 3 倍以上違う。
   現在は平均が 6 列目、中央値が 18 列目にある。中央値と日等重み平均を平均の隣に置く。
2. **束の位置の 3 群を排他にする。** 単発の束 12,029 件を別の群にして、n の合計が 52,000 を超えないようにする
   (今は 61,468)。`bundle_pos_single` の列はあるので、表の側の数え方だけの問題である。
3. **帯の定義の文言。** 設計 §4・`summary.json`「m の帯の規則」は「帯 k = [q_{k−1}, q_k)」と書くが、
   実装 `band_of_m` は `searchsorted(..., side="right")` なので **(q_{k−1}, q_k]** である。
   実害は 0 件(m がちょうど切り値に等しいプリントは T=10・T=60 とも 0 件。計算)。文言を実装に合わせる。
4. **k を bp だけで出さない。** 致命 2 のとおり、k はティック数に直さないと読めない。
   H0 の k の分位に「ティック数」の行を、H3 の各行に「ちょうど 1 ティックの割合」と「p₀ の中央値」を添える。
5. **H4 に「対になったプリント」の同じ行を並べる。** 今の H4 は H2 と母集団が違うので、横に置くと読み違える
   (致命 3)。差と差の日クラスタ SE を同じ行に出す。
6. **H1 の「次の清算」の列は h に依らない。** 7 行すべてに 0.5847 / 0.6528 と母数が繰り返し入っている
   (7 行 × 4 列 = 28 セル)。1 行に畳むか、h ごとに意味のある量(`ts` 起点ではなく t₀ + h 起点)にする。
7. **h 後の価格の遅れを表に出す。** `at_or_before(t₀ + h·1000, 300_000)` は最大 300 秒まで古い約定を返す。
   実測の最大は h=60 で 40,807 ms(**実際の窓 19 秒**)、h=300 で 67,630 ms、h=900 で 71,162 ms。
   遅れが 5 秒を超える行は h=60 で 730 件、h=300 で 1,352 件、h=900 で 1,983 件、
   30 秒を超える行は 1 / 9 / 22 件(計算)。「実際の窓の長さ」の分位を H0 に足す。
8. **重複除去の多重度を出す。** 生の zip を数え直すと、全列一致の組は **53,385 組がちょうど 2 行、
   13 組が 4 行**(計算)。4 行の組を 1 件にしているので、**最大 13 件のプリントを落としている可能性がある**
   (4 行が「2 件 × 2 重」なのか「1 件 × 4 重」なのかは**未確認**)。
   委任先が D-4 で「未確認」とした日を跨ぐ全列一致は、472 本全部で **0 組**(計算)。
9. **対照 (b) は側で打ち消し合う。** 日の一様行の生の r に「その日の各プリントの側の符号」を掛ける規則なので、
   同じ日に SELL と BUY が混ざると相殺する。h=60 で SELL 側 −0.0722 / BUY 側 +0.0659 → 混ぜて **−0.0192**
   (計算)。h=900 は SELL +0.9150 / BUY +1.1127 → +0.9908。
   `control_b_i` は 21,378 行 / 456 日 = 1 日あたり 46.88 行で **`side` 列が空**(符号なしの生の値)である。
   「無条件の基準がほぼ 0」と読めてしまうが、その 0 は構造から来ている。
10. **`implied_leverage` の層だけ期間が違う。** 欠測 18,539 件(35.7%)、有る側は 361 日 / 456 日(計算)。
    有る側の r(60) 平均 −0.4442 / 無い側 −0.3096。H6 のこの 3 群は他の 15 群と同じ期間の層ではない。
11. **帯と 3 分位の切り値に、t₀ には無い情報が入っている。** 456 日全部の m で切っているので、
    運用時にその帯に入れることはできない。前半(2024-02-01 より前)と後半で切り直すと、
    **帯が変わるプリントは T=10 で 10,617 件(20.4%)、T=60 で 14,381 件(27.7%)**(計算)。
    属性の 3 分位(想定元本・`bin_pct`・`|dist_node_bp|`・`implied_leverage`)も同じである。

---

## 示唆(判定ではない)— 7 件

1. **この段が前段から消したのは「終端の定義が未来を使う」ことで、値そのものはほぼ動いていない。**
   H0 の診断は 21 行とも探索段 4 と同じ束を使っており、「最後」の r(60) は −6.800366 で一致した。
   **一致が保証しているのは、約定の読み込み・`at_or_before`/`at_or_after`・符号・束の集合が探索段 4 と同じであることだけである。**
   保証していないもの: 2,561 件の「束の外」の扱い(探索段 4 には無い群)、「最初」「途中」の分け方
   (探索段 4 は終端しか作っていない)、m・k・d の層、対照 (c')、t₀ 基準への統一の効き
   (H0「起点の取り方」で 2 つの起点が違う行は h=1 で 4,993 件、h=900 で 2,249 件、平均の差は 10⁻³ bp 台)。
2. **プリントを起点にすると、束の位置ごとの値が符号ごと入れ替わる**(致命 4 の表)。
   +5.18 / +1.34 / −3.97 / −10.51 / +19.69 は、どれも t₀ では選べない群の値である。
   **「清算の後の値動き」と一口に言える形は、この表の中には無い。**
3. **平均で読めるのは h=300 と h=900 だけで、それも清算に固有とは並べられていない。**
   h=300 は −2.997 ± 0.758、上位 5 日を除くと −3.618、束の外を除くと −2.944 と頑健である。
   ただし対で取った対照との差は h=300 で **+0.805 ± 0.391**(T=10)/ **+1.592 ± 0.365**(T=60)で、
   清算のあったほうが戻りが小さい(致命 3)。
4. **執行の視点(問い 13)。** r の中央値 −1.3766 bp(h=60)は、清算と逆向きに p₀ で入って t₀ + 60 秒で出る
   取引の粗利の代理である。**費用はこの単位では測らない(CLAUDE.md A-8)ので、費用で足切りはしない。**
   書いておくべきことは 2 つ。(a) **p₀ も h 後の価格も約定価格であって板ではない。**
   p₀ は「清算の時刻以後の最初の約定」なので、多くの場合**清算された側の反対を取った誰かの約定そのもの**である。
   その値段で入れる主体が居るかはこの表では測っていない。
   (b) **入る時点を遅らせた場合の値は表に無いので計算した**(計算):

   | 起点 | h=60 | h=300 | h=900 |
   |---|---|---|---|
   | p₀(表の値) | −0.3962(中央値 −1.3766) | −2.9970(−4.0258) | −4.0581(−6.5397) |
   | 1 秒遅れ | **−0.5448**(中央値 −1.3774) | −3.1456(−4.0672) | −4.2066(−6.5910) |
   | 5 秒遅れ | −0.3320(−0.9977) | −2.9328(−3.7420) | −3.9939(−6.2177) |
   | 10 秒遅れ | −0.1354(−0.7195) | −2.7362(−3.4556) | −3.7973(−5.9444) |

   1 秒遅らせると値は**大きくなる**(r(1) の平均が +0.1486 = 最初の 1 秒は清算の向きへ進むため)。
   **つまりこの表の量は「p₀ でしか取れないから読めない」形ではない。**遅れの効きは ±0.2 bp 程度である。
5. **問い 14(対照 (b)との比較)の答え。** `control_b_i` 21,378 行は「同じ日の無作為時刻」ではなく
   **1 周目の日ごとの一様行**(1 日あたり 46.88 行、`side` 空)である。生の(符号なしの)平均は
   h=60 で +0.0683、h=300 で +0.4295、h=900 で +0.1165、標準偏差は 6.00 / 12.28 / 26.07(計算)。
   プリントの側の符号を掛けると直すべき 9 のとおり相殺して 0 に寄る。
   **側の符号を掛けずに「無条件のばらつき」としてだけ読むなら使える**(h=60 の標準偏差 6.00 に対し、
   プリントの r(60) の標準偏差はこの表では出ていないが、対の部分集合で 13.95。計算)。
6. **重なりのため、n は独立な観測の数ではない。** t₀ を昇順に並べて重ならない窓だけを取ると、
   h=60 で 23,715 件(45.6%)、h=300 で 15,473 件(29.8%)、h=900 で **10,676 件(20.5%)**である(計算)。
   日クラスタ SE は日の外の相関しか吸わないので、中央値・p25/p75・r<0 の割合の列には
   **重なりに対する手当てが一切無い**(表にもそう書かれていない)。
7. **反証条件(問い 10)。「予想の材料がある」と言うために、この表のどの値がどうなっていれば足りないか。**
   反証者としては、次の 4 つが**同時に**揃わなければ足りないと考える。
   (a) **t₀ だけで作れる層**(m・k・d・側・時刻帯・属性のいずれか。帯の切り値も過去だけで作る)1 つの中で、
   **日等重み平均と中央値の両方**が、その層の日クラスタ SE の 2 倍を超えて同じ向きであること。
   (b) その層を**寄与上位 5 日と「束の外」2,561 件を除いても**符号と桁が変わらないこと(致命 1)。
   (c) **対で取った対照との差**が同じ向きであること(今は 6 本すべて逆向き。致命 3)。
   (d) 層の切り値を**前半だけで切って後半に当てて**も同じ向きであること(直すべき 11)。
   **(a) だけでは足りない。**清算が 1 件も無い対照 (c') の 60 行のうち 50 行が (a) の前半を通るからである(致命 6)。
   現在の表で (a)〜(d) を全部通る層は、反証者が数えた範囲では **0 件**である。
   なお **MDE は書けない**(この段は検定をしていないので、検出力の計算もしていない)。
   **MDE を書けない以上、どの層についても「無い」とは書けない**(CLAUDE.md §5.2)。

---

## 問いへの直接の答え(数値つき)

| # | 問い | 答え |
|---|---|---|
| 1 | 再計算の一致 | **532 行 / 9,444 セル、一致しない行 0 件**(§0)。H1 全 7 行・H0 診断 21 行・H2 全 140 行・H3 全 36 行・H4 全 60 行・H5 全 27 行・H6 全 162 行 |
| 2 | 分母 | 率の分母はすべて「その群のプリント数」で、行データと一致(§0)。ただし**帯の割り当てに 456 日全部の分位を使うので、運用時にその群に入れることはできない**(直すべき 11、20.4% / 27.7% が帯を変える)。「束の外」2,561 件は**探索段 4 の gap 60 の束のどれにも当たらなかったプリント**で、75 日に散り、SELL 1,765 / BUY 796、最大は 2024-08-05 の 288 件。r(60) 平均 +19.69(致命 1・4) |
| 3 | 対照 | (c') は同じ問いへの反事実になっていない(致命 3)。取れた割合 36.4% / 32.6% は静かな日・静かな時刻・小さい m に偏り(相関 −0.789 / −0.753、時刻帯はほぼ一様に対しプリントは UTC 12–18 が 39.4%)、対で引くと 6 本とも向きが逆。(b) 日集約は**側の符号で相殺する**ので「無条件の基準」として読めない(直すべき 9) |
| 4 | 情報の境界 | 構造の検査は通る(計算): t₀ < ts の行 0、t_pre ≥ ts の行 0、t₀ − ts が次の同じ側の清算までの間隔を超える行 0。m・d は `ts` 基準で過去だけ。**k は p₀ を使うので t₀ まで待って初めて分かる量**で、p₀ の遅れが 0 でないプリントは 11,909 件(22.9%)、1 秒超は 625 件(1.2%)、最大 20,469 ms。r_t0 と r の差(h=1 で 4,993 行)は、この遅れのぶん窓がずれることの現れで、平均への効きは 10⁻³ bp 台。**層の割り当てに 456 日全部の分位を使う点が、境界の外にある唯一の実害である**(直すべき 11) |
| 5 | 相対/絶対 | 致命 1。上位 5 日の寄与 +0.9202(平均の −232%)、2023-08-17 だけで +0.6003。日の平均が負の日 72.8%、日の平均の中央値 −1.5581 |
| 6 | 定義の副作用 | 致命 4・5。表の「最初」は −0.0120(問いの +5.18 は**単発を除いた 9,169 件**の値で表に無い)。t₀ で分かる最良の代理 d = 0 は「最後」を 61.8% 当てるが、その層の r(60) は **+0.148 ± 0.122**(単発 −3.238 と 多件の最初 +5.633 の混合) |
| 7 | データ妥当性 | 重複除去 106,822 → 53,398 を生から再現(§0)。多重度は 2 行が 53,385 組・4 行が 13 組、日を跨ぐ一致 0 組(直すべき 8)。遅れは p₀ − ts が p50 0 / p90 125 / 最大 20,469 ms、h 後は最大 71,162 ms(直すべき 7)。欠測は `implied_leverage` 18,539 件以外すべて 0(表と一致) |
| 8 | 選択の汚染 | 帯・3 分位を全期間で切っている(直すべき 11、20.4% / 27.7%)。H6 の `implied_leverage` は 361 日分(直すべき 10)。k の群は期間そのものになっている(致命 2) |
| 9 | 一貫性 | 引用の T が 2 つ混ざっている。**T=10 帯 10**: h=300 −18.1405 ± 3.3417 / h=60 −1.2759 ± 4.7780。**T=60 帯 10**: h=300 −17.1107 ± 5.2468 / h=60 **+0.1920 ± 6.6491**(問いの「+0.19(SE 6.6)」はこちら)。どちらの T でも**中央値は h とともに単調に負へ進む**(T=60 帯 10 で h=1 +0.218 → h=60 −12.515 → h=300 −22.770 → h=900 −28.704)。食い違っているのは平均だけで、致命 1 と同じ裾の効きである。帯 10 は 5,200 件・n は同じでも日クラスタ SE が h=60 で 6.65 と最大になる |
| 10 | 反証条件 | 示唆 7。(a)〜(d) を全部通る層は現在 **0 件**。(a) だけなら対照 (c') の 60 行中 50 行が通る |
| 11 | 行数とセル数 | `tables.md` **535 行 / 数値セル 11,059**(`summary.json` と一致、反証者が同じ数え方で再現)。表の見出し行 7。**反証者がこのレビューで新しく切ったセルは末尾に数えて書いた** |
| 12 | 探索段 4 との突き合わせ | 示唆 1。−6.800366 の一致が保証するのは約定の読み込み・`at_or_before`/`at_or_after`・符号・束の集合が同じであることだけ。「束の外」2,561 件・「最初」「途中」の分け方・m/k/d の層・対照は保証されていない |
| 13 | 執行の視点 | 示唆 4。**費用の語で足切りはしない。**p₀ も h 後も約定価格(板ではない)で、p₀ は清算の約定そのものである。1 / 5 / 10 秒遅らせた値を計算して置いた(効きは ±0.2 bp 程度) |
| 14 | (c') 以外の対照 | 示唆 5。`control_b_i` は「無作為時刻」ではなく 1 周目の日ごとの一様行(1 日 46.88 行、`side` 空)。生の平均 +0.0683 / +0.4295 / +0.1165、標準偏差 6.00 / 12.28 / 26.07 |
| 15 | 直すべき点と示唆 | 上の 2 節に分けて書いた(直すべき 11 件 = 道具と設計の食い違い・表の作り。示唆 7 件 = 次の単位への材料) |

---

## 使ったコマンドと出力

再計算の本体は `docs/DATA/probes/20260920_o3c_signal_explore5_refuter.py`、
その出力は同名の `.log` に置いた。

```
$ cd /home/user/trade && python3 docs/DATA/probes/20260920_o3c_signal_explore5_refuter.py \
    > docs/DATA/probes/20260920_o3c_signal_explore5_refuter.log 2>&1
$ head -6 docs/DATA/probes/20260920_o3c_signal_explore5_refuter.log
[行データ] print=52000 control_c=35886 control_b_i=21378 日数=456
[切り値] m10 = [1.967588, 3.527995, 4.938159, 6.402933, 7.992376, 10.006686, 12.700654, 16.767302, 25.549409]
[切り値] m60 = [5.904325, 8.999551, 11.886451, 14.946111, 18.358878, 22.496027, 27.951066, 35.806904, 50.836735]
[切り値] m60 3分位 = [12.883745, 25.86029]  k>0 3分位 = [0.022345, 0.086693]

[突き合わせ] 行 532 / セル 9444 / 一致しないセル 0
```

```
$ tail -3 docs/DATA/probes/20260920_o3c_signal_explore5_refuter.log
  zip=472 生の行=106822 全列一致で 1 件=53398
  多重度の分布(件数)= {2: 53385, 4: 13}
  日を跨いで全列一致する組=0
```

セル数の数え直し(委任先と同じ数え方 = `|` で始まる表の行から区切り行と見出し行を除き、
`float()` に通ったセルを数える。関数は同じプローブの `count_cells`):

```
$ python3 -c "<count_cells を読み込んで呼ぶ>"
backtest_data/o3c_signal_explore5_20260920/tables.md            (535, 7, 11059)
docs/PHASE2/O3C/SIGNAL/REFUTER_REVIEW5_2026-09-20.md            (73, 11, 76)
# (表の行, 見出し行, float() に通ったセル)
```

「+5.18」が出力に無いことの確認:

```
$ grep -rn "5\.18" backtest_data/o3c_signal_explore5_20260920/tables.md \
    backtest_data/o3c_signal_explore5_20260920/*.csv
  (束の位置の行には 1 件も無い。当たるのは H6 の別の列の -5.1803 / -5.1824 など)
```

---

## 件数

- **致命 6 件**(表の読みを変えるもの)
- **直すべき 11 件**(道具の欠陥・設計と道具の食い違い・表の作り)
- **示唆 7 件**(次の単位への材料。判定ではない)

### 反証者がこのレビューで新しく切ったセル

この文書の表は **73 行 / 見出し 11 行**(**この数え方の表そのものは除いて数えた**。入れると自分を数えることになる)。
数値セルの数は、数え方で 3 通りになる(計算):

| 数え方 | セル |
|---|---|
| 委任先と同じ厳密な数え方(`float()` がそのまま通るセル) | 76 |
| 太字の `**` と全角マイナス `−` を直してから `float()` に掛ける | 175 |
| **数を 1 つ以上含むセル**(`±` の入ったセルも 1 と数える) | **281**(数そのものは 590) |

**多重性に引き継ぐときは 281 を使う**(委任先の 11,059 と同じ「セル」の単位に揃えるため。
厳密な数え方の 76 は、反証者が全角マイナスと太字を使ったせいで小さく出るだけで、実体ではない)。
うち H0〜H6 の値をそのまま写したセル(致命 6 の H1 の表、致命 3 の偏りの表の一部、
問いへの答えの表)も含む。**出力の表に無い群を新しく切ったのは、日ごとの寄与・除外後の平均・
裾を落とした平均・k のティック数・対で引いた差・排他の束の位置・t₀ の層(d と直前の間隔)・
d = 0 の内訳・遅れて入る起点・h 後の遅れの閾値・対照の時刻帯とばらつき・前後半の切り直し・
重ならない窓・2 SE の篩の 14 種類である。**
