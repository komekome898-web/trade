# 清算を起点とした値動きの予測可能性 — 探索段 4 の設計(逆行側と続伸側を同じ束の集合で並べる、2026-09-20、L-253)

> **位置付け(オーナー逐語)**: 「**その向きで設計を書いてください**」(L-253)。向き = L-252 の表(これまでの結果 × 設計の要素)を §0 に置き、**逆行側**(前の値動きで分ける、L-251)と**続伸側**(掃きの大きさ・次の清算の発生)を**同じ束の集合で並べて測る**。原文 §1.1「**トレンド転換が起きるかまたはそれがトレンドになるのか**」の両側を 1 つの表で見る。
> **この段も探索段**(research-protocol §0.0)。456 日は **5 度目の読み**。判定語は付けない。判定は自前記録(触っていない、L-246)で一度だけ。

## 用語(記号の式と、日常語の説明と例。L-248「掃きってなに？」の規則)

| 記号 | 意味 |
|---|---|
| 束 | gap 秒以内に続いた清算をひとつにまとめたもの(gap 60 が主)。`start_ms` = 最初の清算、`end_ms` = 最後の清算 |
| 基準(p_pre) | 束の最初の清算の直前の約定の価格(探索段 3 と同じ)。`baseline_lag_ms` = `start_ms` − 基準の約定の時刻 の中央値 96 ms(一次の出所: 探索段 3 の `f1_sweep.csv` gap 60 全体。F0 の「基準の遅れ」p50 = 95 は `baseline_lag_ms − 1` の定義(委任先の J-6)なので 1 ms 違う) |
| 終端(p_end) | 束の最後の清算の直後の約定の価格(探索段 1〜3 の起点) |
| **前の値動き m(T)** | **清算が起きる前に、値段がどれだけ清算の向きへ動いていたか。**式: 最初の清算の T 秒前の価格(`at_or_before(start_ms − T·1000)`)から基準までの符号付き変化(bp)。正 = 清算の向き。主の表では T = 60 秒 / 5 分、G0 の「T の下見」では T = 10 秒 / 30 秒 / 60 秒 / 5 分 / 15 分の 5 値(L-255)。例: ロング清算の 60 秒前が 60,060、直前が 60,000 なら m(60) = +10 bp |
| 掃き(sweep) | 清算の成行注文が板を掃いて動かした分 = 基準 → 終端(bp)。正 = 清算の向き(探索段 3 と同じ) |
| **後の逆行 r_end(h)** | 終端から h 秒後までの符号付き変化(bp)。**負 = 清算と逆へ戻った**、正 = 清算の向きへ続いた(探索段 1〜3 の r(Δ = 0, h) と同じ量) |
| **続伸** | 清算のあとも清算の向きへ動き続けること。2 つの形で数える: (i) 次の束(同じ gap)が 120 秒以内に始まる割合(探索段 2 で「+8.6 bp 側」を分けた境界)、(ii) r_end(h) > 0 の割合 |
| **戻り率 g(T, h)** | 後の戻りが、T 秒前の水準に対してどこまで戻ったか。式: (p_end − p_h)/(p_end − p_T)。1 = T 秒前の水準まで戻った、> 1 = 越えた、< 0 = 続いた。分母 = m(T) + 掃き が 0 以下なら定義しない(数を報告) |
| h | 起点(終端)から何秒後の価格を見るか。1 / 5 / 10 / 30 / 60 秒(時間の形)と 60 秒 / 5 分 / 15 分(位置) |
| 日の状態 | その日の束の数(gap 60)の 3 分位。束が多い日ほど掃きが大きく r_pre が正に寄る(探索段 3 反証者、相関 +0.51) |

## 0. これまでの結果 × この設計の要素(L-252 の表。右が「扱わない」なら理由を書く)

| これまでに分かったこと(出所) | この設計の要素 |
|---|---|
| 清算直後 60 秒で数 bp 逆行(1 周目 −6.8 bp、探索段 1・2) | G1・G2 の r_end(60 秒)。同じ量 |
| 逆行は起点の跳ねではなく約 60 秒かけて溜まる(1 秒 −0.57 = 8%、探索段 2) | **G3 時間の形**: h = 1 / 5 / 10 / 30 / 60 秒の溜まり方を m の分位ごとに出す。(A) 前の値動きの戻りなら m ≈ 0 の層で溜まらないはず |
| 次の束が 120 秒以内に来る 17% は +8.6 bp、来ない 83% は −1.7(探索段 2)/ 次の束の有無で ±18 bp(探索段 3 致命 5) | **G2 続伸側**: 次の束が 120 秒以内に来る割合と r_end > 0 の割合を、掃き × m の層ごとに出す。条件付けではなく**発生率として測る** |
| 掃きが大きい束は 15 分たっても清算の側に +11 bp 残る(探索段 3) | G2 の掃き上位層 × h 15 分。g(T, h) で T 秒前の水準に対しても出す |
| 掃きゼロ・逆向きの束でも −2.85 / −6.06 bp 逆行(探索段 3) | **G1 逆行側**: m(T) の 10 分位で層別し、m ≈ 0 の層に逆行が残るかを見る(L-251) |
| 対照 (a) は幅ゼロの束を 1 ms の窓に合わせ、大きい掃きの 19.4% を落とした(探索段 3 致命 4) | **G4 対照**: 窓の長さを T に固定した m 合わせ(§4)。幅ゼロの問題が無い。落ちた束の数と側を出す |
| 「次の束まで 180 秒以上」は続伸しなかった束の選別(探索段 3 致命 5) | 未来で条件付けしない。過去の条件付け「前の束から T + 60 秒以上」だけ併記 |
| 属性(束の大きさ・薄さ・レバレッジ・側)で大きさが変わる(1 周目、探索段 1) | **G6**: 5 属性 × m 3 分位。**残す基準**: 探索段 1 の報告が「1 分の値の順序を作る」と名指しした属性(「**束が大きい・遠い・薄い・低レバレッジ・SELL ほど 1 分の値が大きい**」`SIGNAL_EXPLORE_RESULT_2026-09-19.md` §0)= `bundle_n_events_dedup`・`dist_node_bp`・`bin_pct`・`implied_leverage`・`side`。**外す 5 属性**(本単位では測らない): `dist_vwap_bp`・`doi_pre_1h`・`bundle_total_notional`・`bundle_width_ms`(探索段 1 で `bundle_n_events_dedup` の下位と同一の 12,029 行)・`time_of_day`。前版の「4 属性」は `dist_node_bp` を落としていた(監査 2 回目・指摘 4) |
| 束が多い日は掃きが大きく r_pre が正に寄る(相関 +0.51、探索段 3 反証者) | **G5 日の状態**: 日の束数 3 分位 × m 3 分位で G1 を出す |
| gap は撹乱パラメータではなく条件付けの事象(探索段 2) | gap 60 主。30 / 180 は G1 の T = 60・h = 60 だけ併記 |
| bitFlyer は窓を合わせないと読めない(探索段 1・3) | **G7**: Binance と bitFlyer を**同じ 1 分の格子**に置く(両方とも足の終値、基準 = start_ms を含む足の 1 本前、h = 5 / 15 分)。約定ベースとの比較はしない |
| 1 時間以上の h は検出力不足で不明(探索段 2、MDE 2.5〜7.7 bp) | h ≤ 15 分に限る |
| 3 区分の母集団と r_pre の母集団が違った(探索段 3 致命 3) | すべての表で、割合の母数の列を別に置く |

## 0.1 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 逆行側と続伸側を同じ束の集合で並べて測る設計を書く | 「**その向きで設計を書いてください**」(L-253) |
| これまでの結果 × 設計の要素の表を §0 に置く | 「**その向きで設計を書いてください**」(L-253。「向き」= L-252 の問い「**今までの測定結果の分析を踏まえた上での設計？**」への答えとして見せた表を §0 に置くこと) |
| 前の値動き m で層別・同じ大きさの値動きの対照・T 秒前を基準にした戻り率 | 「**その向きで設計を書いてください**」(L-253。「向き」に含む「逆行側 = 前の値動き m で分ける(L-251)」。L-251「**2.はどうやって分けるの？**」は問いで、3 本はそれへの答えとして見せたもの。承認は L-253) |
| 転換と続伸の両側を測る | 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1) |
| 456 日の公開アーカイブで探索、自前記録は判定まで触らない | 「**自前記録使ったら判定できなくなるからダメ**」(L-246) |
| 用語表に日常語の説明と例 | 「**掃きってなに？**」(L-248)、「**h ってなに？**」 |
| T: 主の表(G1〜G7)は 60 秒 / 5 分の 2 本。G0 に T = 10 秒 / 30 秒 / 60 秒 / 5 分 / 15 分の 5 本で m の分布と後の逆行との相関を出し、次の段で T を決め直す | 「**その案で設計に書いてください**」(L-255。「**Tはなぜその二つを選んだの？**」L-254 への答え = 60 秒は根拠あり(gap 60 の構造・後の逆行の時間幅)、5 分は丸い数、として見せた案) |
| m の層 = 10 分位(G1)/ 3 分位(G2〜G6)、続伸の境界 = 120 秒、対照の作り方(§4)、日の状態 = 日の束数の 3 分位、G6 の属性 5 つ、**G7 = Binance と bitFlyer を同じ 1 分の格子に置く**、g の分母の下限(§7) | 「**残る空の行もそれでいいです**」(L-256。この行を空の行として見せた上での承認) |
| SE の日クラスタ・gap 併記・多重性・反証者・テスト | `research-protocol` の固定手順(L-179「**スキルの承認をもって聞かずに当ててよい**」) |

**空の行は無くなった**(T は L-255、残りは L-256 で承認)。委任は監査が通ってから(L-250 の約束どおり、返答のあと)。

## 1. 意図と実装の突き合わせ(§0.5)

| # | 原文の意図(逐語) | この設計の要素 | 印 |
|---|---|---|---|
| S-1 | 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243) | 束の直後に、m と掃きから逆行 / 続伸のどちらが起きやすいかを層ごとに数える(G1・G2) | ○ |
| S-2 | 「**清算を起点にした研究という位置付け**」(L-243) | 起点 = 束(gap 60 主)。前の値動き m も束の最初の清算を起点に遡って測る | ○ |
| S-3 | 「**値段の上がりすぎ下がりすぎ … を捉えることができるか**」(原文 §1) | 上がりすぎ = m が大きい層。そこで逆行が大きいか(G1)、T 秒前の水準を越えるか(g) | ○ |
| S-4 | 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1) | 同じ層で、逆行の割合と続伸の割合(次の束 120 秒以内、r_end > 0)を並べる(G2) | **○**(探索段 3 では △) |
| S-5 | 「**みる時間軸 可能な限り多く**」 | h = 1 秒〜15 分の 8 本。1 時間以上は検出力不足で置かない(理由を書いた) | △ |
| S-6 | 「**bitFlyerの価格相関**」 | G7 同じ格子で併記 | △ |
| S-7 | 「**どの価格帯で清算が起きるかの予測**」 | 測らない | ✕ |
| S-8 | 「**エントリーや利確に活かせるか**」 | 規則にしない。層ごとの分位は出す。費用は使わない(A-8) | △ |
| S-9 | 「**海外取引所の高レバ市場**」(原文 §1) | Binance COIN-M BTCUSD_PERP(前段と同じ) | ○ |

## 2. 問い(G0〜G7)

| # | 問い | 見る形 |
|---|---|---|
| G0 | 自己点検と T の下見 | m の窓の実経過(T 秒前の約定の遅れ)、基準・終端・h 後の遅れの分位、m の分布(負の割合 = 清算の前に逆へ動いていた束)。**T の下見(L-255)**: T = 10 秒 / 30 秒 / 60 秒 / 5 分 / 15 分の 5 本で、m(T) の分位(p10 / p50 / p90)、m ≤ 0 の割合、m(T) と r_end(60 秒)の相関(束の行、日ごとの平均の 2 通り)を 1 行ずつ = 5 行(gap 60)。主の表は T = 60 秒 / 5 分のまま。5 本の結果から次の段で T を決め直す(この段では選ばない) |
| G1 | 逆行側: 後の逆行は前の値動き m に比例するか、m ≈ 0 でも残るか | T 2 × m 10 分位 × h(60 秒 / 5 分 / 15 分): r_end 平均 ± 日クラスタ SE、g(T, h) の中央値と 3 区分(g > 1 / 0 ≤ g ≤ 1 / g < 0、母数の列つき) |
| G2 | 続伸側: 掃きが大きい束は続くか | 掃き 3 分位 × m 3 分位 × T 2 × h 3: r_end、次の束が 120 秒以内に来る割合(**清算側の層の間でだけ比べる。対照には無い量**)、r_end > 0 の割合、g |
| G3 | 時間の形 | m 3 分位 × T 2 × h(1 / 5 / 10 / 30 / 60 秒): r_end。溜まり方が m で変わるか |
| G4 | 対照: 清算なしで同じ m の値動きの後はどう動くか | G1 の gap 60 の部分と同じ形(T 2 × m 10 × h 3。**gap 30 / 180 は作らない** = 対照の生成を gap ごとに 3 回走らせないため。監査 1 回目・指摘 10)(対照 (c)、§4): r_end・g・続伸 (ii) r_end > 0 の割合。**続伸 (i) は出さない**(重なり除外で構造上 0。§5 のリードの読み)。落ちた束の数と側 |
| G5 | 日の状態 | 日の束数 3 分位 × m 3 分位 × h 3(T = 60): r_end、続伸の割合 |
| G6 | 属性 | 5 属性(束の件数 3・ノードからの距離 3・薄さ 3・レバレッジ 3・側 2 = 14 群。§0 に残す基準と外す 5 属性)× m 3 分位 × h 3(T = 60): r_end、続伸の割合、含む日数、欠測の割合 |
| G7 | bitFlyer | 同じ 1 分格子で Binance と bitFlyer の r(5 分 / 15 分)を m 3 分位 × T 2 で並べる |

**答えない問い**: 1 時間以上の h、価格帯の予測(S-7)、bitFlyer との相関の判定、規則化。

## 3. 候補(`jev_design.py`)

- 観測量: `config/jev_design_examples/signal4_observables.yaml`(8 件)。
- 対照で合わせる変数: `config/jev_design_examples/signal4_covariates.yaml`(**16 件** = 探索段 3 の 13 件すべて + `m_pre_T` + `prev_bundle_gap_s` + `bundles_per_day`)。前版は 12 件で、探索段 3 の `baseline_lag_ms` / `bundle_total_notional` / `dist_vwap_bp` / `doi_pre_1h` を黙って落としていた(監査 1 回目・指摘 5。前段の監査 3 回目・指摘 3 と同じ型の誤り)。足して再実行した。

## 4. 対照

- **(c) 前の値動き合わせ(窓の長さ固定)**: 束 b(日 d、側 s、m_b = m(T))ごとに、候補時刻 t = その日の 00:00 UTC から 10 秒刻み。区間 [t − T 秒, t + 900 秒] が同じ gap のどの束の [`start_ms`, `end_ms`] とも重ならないものだけ残す。候補の値動き c_t = s × (p(`at_or_before(t, 300_000)`) − p(`at_or_before(t − T·1000, 300_000)`))/p(`at_or_before(t − T·1000, 300_000)`) × 10⁴(どちらか引けなければ候補にしない)。対照の終端は `at_or_after(t, 300_000)`、h 後は `at_or_before(終端 + h·1000, 300_000)`。m_b の 10 分位帯(同じ gap の全束の m で切る。`numpy.quantile` 線形補間、帯 k = [q_{k−1}, q_k)、両端は −∞ / +∞。**m は負も含むので 0 を含まない帯は作らない**)に c_t が入る候補のうち |c_t − m_b| 最小、同値なら早い t、置換なし(取った区間と重なる候補は使わない)。**処理順**: 日を昇順、日の中では束を `start_ms` 昇順に処理する(探索段 3 の対照 (a) と同じ。監査 2 回目・指摘 2)。取れなければ「対照 (c) なし」で数と側を報告。**窓の長さは T で固定なので、探索段 3 の対照 (a) の幅ゼロの問題は起きない。**価格で選ぶので反事実ではない(同じ大きさの値動きの後の比較)。
- **(b) 無条件**: 探索段 2・3 の対照 (i) 日集約版の r_end(h) を併記。
- G0 に対照 (c) の |c_t − m_b| の分位、取れなかった束の数と側、**取れなかった束の m(T) 10 分位別の割合と、取れた束と取れなかった束の m・掃き・r_end(60 秒)の中央値**(致命 4-a と同型の偏りを見る。監査 1 回目・指摘 8)、実経過の分位を出す。「そろう」とは書かず G0 で測る。

## 5. 設計の段の判定(`jev_design.py`、L-242)

コマンド(2026-09-20、リードの実測): `PYTHONPATH=src:scripts python3 scripts/jev_design.py rank-observables --purpose-from <この文書> --intent S-1,S-3,S-4 --observables config/jev_design_examples/signal4_observables.yaml --md …` と `rank-covariates --quantity "<r_end の定義>" --covariates config/jev_design_examples/signal4_covariates.yaml --md …`(判定の量 = r_end(h) と 続伸の割合 の 2 回)。確率は `data/jev/design/`(リポジトリに入れない)。表は下に貼る。

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 設計文書: `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE4_DESIGN_2026-09-20.md`
- 候補ファイル: `config/jev_design_examples/signal4_observables.yaml`
- 意図: S-1, S-3, S-4

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### S-1 「**値動きの予想ができるなら戦略のシグナルとしての価値がある**」(L-243)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `reversal_by_premove` | 代理(2 台) |
| 2 | `time_shape_by_premove` | 代理(2 台) |
| 3 | `retrace_to_T` | 代理(2 台) |
| 4 | `continuation_return_share` | 代理(2 台) |
| 5 | `residual_reversal_at_zero_premove` | 代理(2 台) |
| 6 | `continuation_rate` | 無関係(1 以下) |
| 7 | `day_state_split` | 無関係(1 以下) |
| 8 | `bf_same_grid` | 無関係(1 以下) |

### S-3 「**値段の上がりすぎ下がりすぎ … を捉えることができるか**」(原文 §1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `reversal_by_premove` | 代理(2 台) |
| 2 | `retrace_to_T` | 代理(2 台) |
| 3 | `time_shape_by_premove` | 代理(2 台) |
| 4 | `continuation_return_share` | 無関係(1 以下) |
| 5 | `residual_reversal_at_zero_premove` | 無関係(1 以下) |
| 6 | `continuation_rate` | 無関係(1 以下) |
| 7 | `day_state_split` | 無関係(1 以下) |
| 8 | `bf_same_grid` | 無関係(1 以下) |

### S-4 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」(原文 §1.1)(印 ○)

| 順位 | 量 | 語 |
|---|---|---|
| 1 | `reversal_by_premove` | 代理(2 台) |
| 2 | `continuation_return_share` | 代理(2 台) |
| 3 | `time_shape_by_premove` | 無関係(1 以下) |
| 4 | `retrace_to_T` | 無関係(1 以下) |
| 5 | `continuation_rate` | 無関係(1 以下) |
| 6 | `residual_reversal_at_zero_premove` | 無関係(1 以下) |
| 7 | `day_state_split` | 無関係(1 以下) |
| 8 | `bf_same_grid` | 無関係(1 以下) |

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: r_end(h): signed price change in basis points from the first trade after the bundle's last liquidation to h seconds later; negative = price moved back against the liquidation direction (reversal), positive = continued in the liquidation direction
- 変数ファイル: `config/jev_design_examples/signal4_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `side` | 合わせなくてよい |
| 2 | `baseline_lag_ms` | 合わせなくてよい |
| 3 | `sweep_bp` | 合わせなくてよい |
| 4 | `m_pre_T` | 合わせなくてよい |
| 5 | `prev_bundle_gap_s` | 合わせなくてよい |
| 6 | `bin_pct` | 合わせなくてよい |
| 7 | `bundle_width_ms` | 合わせなくてよい |
| 8 | `dist_node_bp` | 合わせなくてよい |
| 9 | `next_bundle_gap_s` | 合わせなくてよい |
| 10 | `bundle_n_events_dedup` | 合わせなくてよい |
| 11 | `dist_vwap_bp` | 合わせなくてよい |
| 12 | `implied_leverage` | 合わせなくてよい |
| 13 | `bundle_total_notional` | 合わせなくてよい |
| 14 | `bundles_per_day` | 合わせなくてよい |
| 15 | `doi_pre_1h` | 合わせなくてよい |
| 16 | `time_of_day` | 合わせなくてよい |

## 設計の段の判定(jev_design、2026-09-20、モデル jev-1.13.0)

- 判定の量: continuation_rate: share of liquidation bundles that are followed by another liquidation bundle (same side or either side) within 120 seconds after the bundle's last liquidation
- 変数ファイル: `config/jev_design_examples/signal4_covariates.yaml`
- 意図: (この段は量と変数の対なので意図の番号は無い)

この道具は判定の中身(どの量を選ぶか)を決めない。出すのは順位と語だけで、選ぶのはリードである。確率と期待値は json にだけ残す。

### 対照で合わせる変数

| 順位 | 変数 | 語 |
|---|---|---|
| 1 | `next_bundle_gap_s` | 合わせる |
| 2 | `prev_bundle_gap_s` | 合わせなくてよい |
| 3 | `bundles_per_day` | 合わせなくてよい |
| 4 | `bin_pct` | 合わせなくてよい |
| 5 | `bundle_n_events_dedup` | 合わせなくてよい |
| 6 | `m_pre_T` | 合わせなくてよい |
| 7 | `bundle_width_ms` | 合わせなくてよい |
| 8 | `implied_leverage` | 合わせなくてよい |
| 9 | `sweep_bp` | 合わせなくてよい |
| 10 | `time_of_day` | 合わせなくてよい |
| 11 | `baseline_lag_ms` | 合わせなくてよい |
| 12 | `bundle_total_notional` | 合わせなくてよい |
| 13 | `side` | 合わせなくてよい |
| 14 | `dist_node_bp` | 合わせなくてよい |
| 15 | `dist_vwap_bp` | 合わせなくてよい |
| 16 | `doi_pre_1h` | 合わせなくてよい |

**リードの読み**: (i) 観測量は 8 件とも「直接(3 以上)」に届かず、`reversal_by_premove`(m で層別した後の逆行)が S-1・S-3・S-4 のすべてで 1 位「代理」。**主な量は G1 の r_end(h) を m で層別したもの。**`continuation_return_share`(r_end > 0 の割合)は S-4 で 2 位「代理」、`continuation_rate`(次の束が 120 秒以内)は 3 つとも「無関係」。**続伸の (i) 次の束の発生率は、道具の順位では低いが、原文 §1.1「トレンドになるのか」の測れる形として G2 に残す(リードの選択。道具は判断しない)。**`day_state_split`・`bf_same_grid` は「無関係」= 意図に直接は答えない補助の表(§0 の理由で置く)。(ii) 合わせる変数: 判定の量が r_end(h) なら 16 件とも「合わせなくてよい」。判定の量が続伸の割合なら `next_bundle_gap_s` が「合わせる」— これは続伸 (i) の定義そのもの(未来の量)で、対照 (c) は重なり除外の規則で [t − T, t + 900 秒] に束が無い時刻だけを取るため、**対照 (c) の「次の束が 120 秒以内」は構造上 0 になり、続伸 (i) は対照と比べられない。**G4 は r_end と g と続伸 (ii) だけを出し、続伸 (i) は G2 の清算側の層(掃き × m)の間でだけ比べる(§2 の G2 と G4 にそう書く)。**Jev の閾値の較正は未測定**(前段と同じ注記)。

## 6. データと段

- 入力: 探索段 3 と同じ(1 周目の表の束と対照 (i)、約定 zip、bitFlyer 1 分足)。対象日 456 日(欠けた 18 日 = 暦 474 − 456、探索段 2 の `summary.json`)。**5 度目の読み。**
- 道具: `scripts/o3c_signal_explore4.py`(探索段 3 の道具の読み込み・SE・gzip・chunk を再利用)。**`paper_logs/` は読まない**(テストで測る)。
- 出力(行数は委任先に数え直しを求める): G0 約 40 行 + T の下見 5 行 / G1: T 2 × m 10 × h 3 = 60 行(gap 60)+ gap 30・180 の T = 60・h = 60 = 20 行 → 80 行 / G2: 3 × 3 × 2 × 3 = 54 行 / G3: 3 × 2 × 5 = 30 行 / G4: 60 行 / G5: 3 × 3 × 3 = 27 行 / G6: 14 群 × m 3 × h 3 = 126 行 / G7: m 3 分位 × T 2 × 会場 2 × h 2(5 分 / 15 分)= 24 行。**合計約 446 行**(G0 と T の下見を除き 401)。`tables.md` + csv + 行データ(`rows_gap*.csv.gz`。m_60 / m_300 / 掃き / r_end 各 h / g / 次の束の間隔 / 対照 (c) の c_t と t)+ `summary.json` + `MD5SUMS`。
- **すべての割合の列に母数の列を添える**(致命 3 の再発防止)。

## 7. 走らせる前に決めること(リードの決定。空の行は §0.1)

- T = 60 秒 / 5 分(主の表)。根拠: 60 秒は gap 60 の束の直前 60 秒に同じ gap の清算が構造上無く、後の逆行が約 60 秒で溜まるのと同じ物差し(探索段 2)。5 分は一桁長い窓を 1 本置くための丸い数で、データから出していない(L-254)。**G0 で T = 10 秒 / 30 秒 / 60 秒 / 5 分 / 15 分を下見し、次の段で決め直す**(L-255)。T 秒前の価格は `at_or_before(start_ms − T·1000, 300_000)`、無ければ NaN(数を報告)。
- m の層: G1 は 10 分位(gap 60 の全束の m で切る、負を含む)。G2〜G7 は 3 分位。掃きの層は探索段 3 の切り値(0.2009 / 8.0760 bp)をそのまま使う(並べるため)。
- **g の分母**(監査 1 回目・指摘 7。致命 1 の再発防止): 分母 = p_end − p_T(= m(T) + 掃き に相当)。G0 に分母の分位と「分母 < 1 bp の束の割合」を T ごとに出す。g の中央値と 3 区分は、**分母 > 0 の全束**と**分母 ≥ 1 bp の束**の 2 通りを並べ、母数の列を添える。分母 ≤ 0 は定義しない。
- 続伸: (i) 次の束(同じ gap、同じ日)の `start_ms` − この束の `end_ms` ≤ 120 秒の割合、(ii) r_end(h) > 0 の割合。どちらも母数つき。**未来で条件付けした部分集合は作らない。**
- 過去の条件付け: 「前の束から T + 60 秒以上」の部分集合を G1 に併記(m の窓に前の束が入らない束)。
- h: 1 / 5 / 10 / 30 / 60 秒(G3)、60 秒 / 5 分 / 15 分(他)。
- SE: 日クラスタ + naive + 日等重み(探索段 3 と同じ)。gap 60 主、30 / 180 は G1 の一部だけ。
- 多重性: 探索段なので検定しない。行(約 446)とセルは単位を分けて引き継ぐ(探索段 3 の報告 §5)。
- テスト: r_end(60 秒)が探索段 3 の全体 −6.800 と一致 / T 秒前の約定が `start_ms − T` 以前 / g の 3 区分の和が 1 / 対照 (c) の帯・重なり・置換なし / `paper_logs/` を開かない / 判定語なし。
- 読みの前に反証者レビュー(`research-protocol` §11。この文書の節番号ではない)。報告の冒頭に用語表と目的への答え。

## 8. 反証者レビュー 3 の突合せ

| # | 反証者 3 の項目 | この段での扱い |
|---|---|---|
| 致命 1 | f の分母が微小 | g の分母は m + 掃き(T 秒の値動き)。分母 ≤ 0 は定義せず数を報告。層別は m で行う |
| 致命 2 | r_pre ≈ 0 は打ち消し | 全体の行は出すが読まない。層別の行を主にする |
| 致命 3 | 母集団の違い | 割合の列に母数を添える |
| 致命 4 | 対照 (a) が大きい掃きを落とす | 対照 (c) は窓固定。落ちた束の数と側を G0 に |
| 致命 5 | 未来の条件付け | 作らない。続伸は発生率として測る |
| 致命 6 | bitFlyer の窓 | G7 は同じ格子 |
| 直すべき 1〜8 | 1 分母 / 2 F3 の重複 / 3 sweep ≤ 0 の書き方 / 4 欠測列の意味 / 5 gap の構成比 / 6 SE の読み / 7 混在束 / 8 多重性 | 1 母数の列 / 2 F3 相当は置かない / 3 「掃きなし(p_end = p_pre)」と「掃きが逆」を分けて数える / 4 欠測列を「属性の欠測」と「定義されない」に分ける / 5 gap 併記は G1 の一部だけで、掃きの構成比を添える / 6 日クラスタ SE と naive SE の比の列を置く / 7 混在束は対照 (c) の重なり除外に含める / 8 多重性は行とセルを分けて引き継ぐ |
