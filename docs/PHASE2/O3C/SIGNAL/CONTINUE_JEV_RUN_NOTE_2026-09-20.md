# 続く / 止まるの単位 — Jev を後半 5,000 件に呼んだ走行の記録(リード、2026-09-20)

委任文 `docs/DATA/delegations/20260920_o3c_signal_continue_jev_prompt.md` の実行記録。委任先(下位モデル)は道具 `scripts/o3c_signal_continue_jev.py`・試験 `tests/test_o3c_signal_continue_jev.py`(7 件)・選定 `jev/selection_manifest.json` を作り、呼び出しを背景プロセスで始めたところでセッションが切れた(995 / 8,644 件の時点。委任先は「完了していない仕事を完了した体裁の報告書にしたくない」として報告書を書かなかった)。**残りはリードが待機の背景ジョブで見張り、切り離した背景プロセスが 8,644 件を呼び終えた 2026-09-20 09:26 UTC に `--stage tables` で表を作った。**委任先の報告書は無く、この記録がその代わり。

## 0.1 対応表(委任文 §0 の写し)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| Jev に「この清算は続くか」を判断させ、後半で的中率・較正・2 枝の損益を測る | 「**清算を検知→jevでこの清算は続くか判断→続くなら乗る、続かないなら逆張り**」(L-262)、「**こんなときこそjevの使い道やろ**」(L-238) |
| 件数 = 後半から層化無作為 5,000 件 | 設計 §7 の分岐の基準(429 と残り呼び出し数を確かめられない → 5,000 件の枝)。リードの決定 |

**この走行は L-269 の指摘の前に設計された「全プリントで固定時間」の形の一部**であり、2 枝の損益(Q4)はその筋違いの形で測られている。1 件ごとの的中率と較正(Q3)はその形に依らないので、方策の模擬(`SIGNAL_POLICY_DESIGN`)の「わからない」の閾値を決める入力として使う。

## 事実(`backtest_data/o3c_signal_continue_20260920/jev/`、リード再計算 `docs/DATA/probes/20260920_o3c_signal_continue_jev_verify.log` と一致)

- 呼び出し: プリント 5,000(側 × 材料 1 の 3 群 = 各 833〜834)+ Q7 の対照候補 3,644。エラー 0。遅延 p50 0.41 秒 / 最大 1.37 秒。入力トークン p50 1,174。確率そのものは `data/jev/continue/`(リポジトリ外)にだけ残る。
- **5,000 件の基準率(続く割合、60 秒)= 0.6056**(層化のため後半全体の 0.5846 とは違う。層ごとに等しく抜いたので材料 1 ≥ 1 の割合が母集団より高い)。
- **的中率(閾値 0.5、60 秒)= 0.5994、適合率 0.7086、再現率 0.5750**、続くと言った件 2,457。30 秒: 0.6012、120 秒: 0.5846。閾値 0.6: 的中 0.4834・適合 0.8211、0.7: 的中 0.4034・適合 0.9091(n = 55)。
- **較正(帯 = Jev の確率 0.1 刻み、実際に続いた割合)**: [0.2,0.3) 6 件 0.167 / [0.3,0.4) 436 件 0.475 / [0.4,0.5) 2,101 件 0.514 / [0.5,0.6) 1,764 件 0.664 / [0.6,0.7) 638 件 0.813 / [0.7,0.8) 55 件 0.909。0.2 未満と 0.8 以上は 0 件。**単調**で、上の帯ほど実際に続く。
- **L-275 の規則で「わからない」の帯を決めると: 件数のある 6 帯はすべて基準率 0.6056 と 2 SE で区別できる**([0.4,0.5) は 0.514 ± 0.022 で下、[0.5,0.6) は 0.664 ± 0.023 で上)。**わからない = 件数 0 の帯だけ**([0,0.2) と [0.8,1.0))。止まる = 確率 < 0.5、続く = 確率 ≥ 0.5。
- 規則(材料 1 ≥ 1)との一致率 0.7252(rule_14 0.679、rule_combo 0.703)。
- 固定の形の 2 枝の損益(Q4、t₀ + 1 秒 → 60 秒、後半、中央値): Jev 0.5 の続く枝 −1.22 bp(n 2,457、負 52.8%)/ 止まる枝 +0.48(n 2,543)。5 分保有: −6.68 / +2.33。閾値 0.7 の続く枝 −15.3(n 55)。**規則と同じ形(続く枝が負)**。
- Q7(清算の無い時刻の対照候補 3,644 件に同じ問い): Jev が「続く」と言った割合 **1.5%**(プリント側の同じ件では 45.0%)。Jev は清算の無い時刻をほぼ「止まる」と読む。損益は ±0.1 bp(前段の反証者 6 致命 3 のとおり荒さが違うので並べない)。

## 限界
- 429 の回数はクライアントの内部再試行のため数えられない(遅延の最大 1.37 秒から再試行の痕跡は薄い。未確認)。
- Q4 は筋違いの形(L-269)。方策の模擬で測り直す。
- 較正の基準率は層化した 5,000 件のもの。方策の模擬の Q1 はこの値を使う(設計どおり)。
