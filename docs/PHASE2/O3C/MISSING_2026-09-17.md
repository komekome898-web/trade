# O-3c 足りないものの一覧(出典・入手方法・代替案つき)

2026-09-17、調査班(research-squad モード1・調達調査)による。**戦略・データの当否は判定しない。**
各主張に事実 / 推定 / 仮定の印を付ける。生ログ: `docs/DATA/probes/20260917_o3c_missing.log`。

## 0.1 対応表(着手前の擦り合わせ)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| O-3c の「進行する上で足りないこと、足りないと予想されることは何か」を調べる | 「**進行する上で足りないこと、足りないと予想されることは何か**」(`docs/PHASE2/O3C/OWNER_INTENT_2026-09-12.md` §1) |
| 成果物を「足りないものの一覧(出典・入手方法・代替案つき)1 本」にする | 「**3 件目の成果物は「足りないものの一覧(出典・入手方法・代替案つき)1 本」としてください。**」(L-188) |
| 各項目に「何が足りないか / なぜ要るか / いま手元にあるもの / 出典 / 入手方法(経路 3 つ以上)/ 代替案 / 印」の形式を当てる | (該当語なし) — リードの依頼文が指定した出力形式(オーナー原文には形式の指定はない。L-188 は「一覧」「出典・入手方法・代替案つき」までを指定) |
| 個別 14 項目(1 OI・2 清算規模・3 分岐・4 束ね方・5 約定単位の価格系列・6 bitFlyer 価格・7 bitFlyer 清算・8 欠落 3 日・9 レバレッジ分布・10 volume at price・11 読み出し層検証・12 前提条件 3 つ・13 他取引所・14 候補外の発見)を挙げて実物で確かめる | (該当語なし) — リードが挙げた候補項目そのもの。原文の対応語は「①事前に煮詰めておきたいこと(価格毎の約定履歴の積み上げから清算の価格帯を予測)」「②必要データ収集の手段と管理の徹底方法」「③足りないこと・足りないと予想されること」の 3 行だけで、14 項目への分割はリードの具体化 |
| `research-squad` モード1の固定手順(経路3つ以上・生ログ保存・DATA.md 追記案)を適用する | 「**スキルの承認をもって聞かずに当ててよい**」(L-179。研究班の固定手順への適用可) |

**右が空の行(4 行目)がある。** 個別 14 項目そのものはオーナー原文に無く、リードが具体化したものである。ここでは項目名を変えず、実物で確かめた結果として書く。**なぜここで聞かなかったか(リード)**: L-188 が決めたのは成果物の**形**(一覧、出典・入手方法・代替案つき)で、一覧の**中身**は調べて出てくるものである。何を足りないと見るかをオーナーに先に書いてもらうのは §0.1 の「仕様を書き下ろしてもらわない」に当たるので、リードの列挙で作り、**足りない項目があればオーナーが足す**形にした。この判断は返答に明記する。

### 項目の分類と、「入手方法」「代替案」の意味

「足りないもの」は 4 種に分ける: **データ**(手元に無い)/ **計算**(データは有る、集計が無い)/ **実行**(コードもデータも有る、走らせていない)/ **手順**(意図と実装の突き合わせ、または判断の置き所)。データ以外の項目では、**入手方法 = 何を誰が実行すれば埋まるか**、**代替案 = 実行しない場合に何で代えるか(無ければ「無し」と書く)**と読み替える。「該当なし」「不要」とは書かない(監査役の指摘 13 を受けて改めた)。

| 項 | 分類 | 項 | 分類 |
|---|---|---|---|
| 1 建玉の履歴 | データ | 8 欠落 3 日 | データ |
| 2 清算の規模の推移 | 計算 | 9 レバレッジ分布 | データ(列は有るが空) |
| 3 分岐の観測 | 実行 | 10 volume at price | 計算 |
| 4 束ね方 | 手順(判断の置き所) | 11 読み出し層・対照窓 | 手順 |
| 5 約定単位の価格系列 | **足りていた**(候補として調べ、無かった) | 12 前提条件 3 つ | 実行(集計処理は書く必要あり) |
| 6 bitFlyer 価格 | データ(標本日 17 日だけ有る) | 13 他取引所 | データ(価格系列側) |
| 7 bitFlyer 清算 | データ | 14 候補外の発見 | 混在(各項に印) |

「zip N 個」の数え方(監査役の指摘 4): 本文書では **zip = 本体ファイルの数**(`.CHECKSUM` は別に同数ある)。metrics 379 / liquidationSnapshot 472 / aggTrades 475。

---

## 1. 燃料の軸: 建玉(OI)の履歴

- **足りないもの**: Binance COIN-M BTCUSD_PERP の建玉(`sum_open_interest`)が、O-3c 窓(2023-06-25〜2024-10-14)のうち **99 日**(単発2日 + `2024-03-04`〜`2024-06-08` の連続 97 日 = 窓の約 2 割)で欠測。
- **なぜ要るか**: 原文「燃料がつきる」の直接の代理変数。
- **いま手元にあるもの(実測)**:
  - `backtest_data/binance_cm_o3c_20260913/metrics/BTCUSD_PERP/` — zip 379 個(`.CHECKSUM` も 379、`ls` の合計 758)。1 日 288 行 = 5 分刻み(`create_time` が 00:00:00, 00:05:00, …, 23:55:00。2023-06-25 で実測)。列 `create_time, symbol, sum_open_interest, sum_open_interest_value, count_toptrader_long_short_ratio, sum_toptrader_long_short_ratio, count_long_short_ratio, sum_taker_long_short_vol_ratio`(実測ヘッダ)。
  - `backtest_data/auto_okx_open_interest_5m_20260905`〜`20260915`(9 日分、自前記録継続中)。
  - **試行して不可(この環境)**: Binance REST `openInterestHist` → HTTP 451(下記 1.)。調査班は「30 日分しか遡れない」と書いたが一次資料の URL を添えていない(**未確認**。リード検収で印を改めた)。
- **出典**: `docs/OWNER_STATUS.md:35-46`(97 日欠測の実測)、`backtest_data/binance_cm_o3c_20260913/README.md`。
- **入手方法(経路)**:
  1. **この環境**: Binance REST `fapi.binance.com/futures/data/openInterestHist` → **実測 HTTP 451**(地域制限)。ログ済み。
  2. **オーナー PC**: 未確認。地域制限が同じ IP 帯かは未測定。
  3. **公開アーカイブ**: `data.binance.vision` の `metrics` は当該 99 日について 404(`docs/OWNER_STATUS.md:44` 実測、2026-09-14)。**この経路では埋まらない。**
  4. **第三者(有料)**: Coinalyze(`api.coinalyze.net`)、CoinGlass(`open-api-v4.coinglass.com`)。**両方プローブした**: Coinalyze は `liquidation-history` で HTTP 401(鍵必須、既知)。CoinGlass の正しいエンドポイント(`docs.coinglass.com/reference/oi-ohlc-histroy` を WebSearch で特定)`/api/futures/open-interest/history` を実測 → **HTTP 200 だが本文 `{"code":"401","msg":"API key missing."}`**(鍵必須)。CryptoQuant・Amberdata はトップページ到達のみ確認(HTTP 200)、API の鍵無し無料枠の有無は未確認。
  5. **有料**: 上記 4 系はいずれも鍵が要る(価格・プランは未確認)。
- **代替案**: OKX の自前 5 分建玉記録(9 日、継続中)を時間軸の異なる代理として使う。ただし取引所が違うため Binance の窓の穴を直接は埋めない。**穴の 99 日については代替が無い**(この環境・公開アーカイブとも不可。有料鍵は未取得)。
- **印**: 事実(欠測日数・アーカイブ 404・プローブ結果)/ 推定(有料経路の可否)。

## 2. 燃料の軸: 清算の規模の推移(カスケード内の件数・数量の時系列)

- **足りないもの**: **データではなく計算**。`liquidationSnapshot`(Binance COIN-M、472/478 日)は 1 件ごとの清算オーダーを持っており、`src/bot/research/liq_response.py: build_cascades` がこれを時間で束ねて `n_events` / `total_size` を返す実装が**既にある**(実測、上記コード読み)。
- **なぜ要るか**: 原文「燃料がつきる」の強度(1 回のカスケードがどれだけ大きいか)。
- **いま手元にあるもの**: `backtest_data/binance_cm_o3c_20260913/liquidationSnapshot/BTCUSD_PERP/` zip 472 個(`.CHECKSUM` も 472、`ls` の合計 944)。
- **出典**: `src/bot/research/liq_response.py:187-249`(`Cascade` / `build_cascades` / `_finalize_cascade`)。
- **入手方法**: 計算。`build_cascades` を 472 日全件に掛けて `n_events` / `total_size` の時系列を出す(下位モデルに委任できる)。
- **代替案**: 件数だけ(`n_events`)で規模の代理にする(数量 `total_size` を落とす)。
- **印**: 事実(コード・zip 件数を実測)。

## 3. 分岐の観測(戻った件 / 続いた件の分布)

- **足りないもの**: **データではなく「実データに対する実行」**。方向判定(`_direction_of`)・転換スコア(`reversal_scores` / `compute_reversal`)・除外率の突き合わせ(`ReversalExclusionImbalance`)は**コードとして実装済み**(実測、上記コード読み)。ただし `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-14.md` §0.0 の結論は「**実データでは一度も測っていない**」(140 行目)。
- **なぜ要るか**: 原文「トレンド転換が起きるか、またはそれがトレンドになるのか」— この分岐そのもの(`OWNER_INTENT_2026-09-12.md` §1.1)。
- **いま手元にあるもの**: 測定器(コード)は完成。実データでの実行結果は 0 件(合成データのみ実行済み)。ただし転換スコアの**符号ごとの分布・件数**(戻った件 / 続いた件)を出す処理は測定器に無く、平均 1 本に潰す形(`REPORT_2026-09-14.md` §0.0 の L 行、返答 048)。
- **出典**: `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-14.md:140-141`。
- **入手方法**: 実行。前提条件 3 つ(§12)を数えてから、`anchor="after_shift"` と約定単位の価格で実データに掛け、1 件ごとの行を出し、符号ごとに数える(集計は測定器の外側に足す)。
- **代替案**: 無し(分岐は原文の中核なので、平均だけで代えない)。
- **印**: 事実(REPORT の逐語)。

## 4. 束ね方(`gap_ms`=60 秒固定、1 回の清算連鎖の定義)

- **足りないもの**: **手順(判断の置き所)**: 実データの清算の間隔分布に基づく `gap_ms` の妥当性の検討。現状 `build_cascades` の既定値はコード docstring で「判断の置き所」と明記されており(`liq_response.py:220-222`)、実データで検討されていない。下のサンプル計算は「足りない」と言うための確認であって、束ね方の決定ではない(決定は枠組みとしてオーナーに見せる、`CLAUDE.md` §0.2 B の A-13)。
- **なぜ要るか**: 何を「1 回の清算連鎖」と呼ぶかで燃料切れの単位が変わる。
- **いま手元にあるもの(実測、今回サンプル計算)**: `liquidationSnapshot` 6 日分(`2023-06-25/26`, `2024-02-19/20`, `2024-10-13/14`)、イベント間隔 1,038 件の分布 = 最小 0ms・25%点 0ms・中央値 0ms・75%点 33,451ms・90%点 778,986ms・99%点 7,272,874ms・最大 28,374,082ms。**60,000ms 以下の割合 = 78.9%(819/1,038)**。中央値が 0ms なのは同一 ms に複数の清算注文が記録される場面が多いため。**リード検収(2026-09-17)で同じ 6 ファイルから再計算し、上の 8 値すべて一致した。追加: 間隔 0ms が 522/1,038 = 50.3%**(同一 ms のレコードが半分。§14 の 2 件目の根拠)。**【訂正 2026-09-17、`PRICE_LEVEL/EXT_2026-09-17.md` §2.0】「同一 ms が半分」の正体は、1 件の清算が全列同じ行として 2 回入っていること(472 日で 106,822 行 → 一意 53,398。リード実測)。重複を落とすと 0ms の間隔はほぼ消える見込み(未再計算)。上の分位は重複込みの値。**
- **出典**: 今回の実測(生データは `backtest_data/binance_cm_o3c_20260913/liquidationSnapshot/BTCUSD_PERP/` の実在 zip。調査班の計算過程は保存されていないが、リードの再計算のコマンドと出力は `docs/AUDITOR/ACTION_LOG.md` 049 に置く)。
- **入手方法**: 実行。472 日全件の間隔分布を出し(下位モデルに委任できる)、それを見て束ね方の候補をオーナーに見せる。
- **代替案**: 60 秒固定のまま進め、幅 0 の断片(§12)を件数として併記する(束ね方を決めずに限界として書く)。
- **印**: 事実(6 日サンプルの実測値)/ 推定(472 日全体への一般化。サンプルは窓の始め・中間・終わりから2日ずつ取ったのみ)。

## 5. 約定単位の価格系列(両群に同じ系列)— `PriceSeries.from_trades` で読めるか【足りていた】

- **足りないもの**: **無い(データは実在し、コードは対応している)**。当初の疑問(「読めるか」)は解消。**候補として調べて「足りていた」項目**。一覧に残すのは、返答 048 で「約定単位の価格なら A・E・G は消える見込み(未確認)」と書いた前提のうち「読める」の部分だけがここで確認できたことを記録するため(A・E・G が実際に消えるかは §12 の前提条件を数えるまで未確認のまま)。
- **なぜ要るか**: `compute_reactions` の先読み防止(`at_or_before`)を使うには `(ts_ms, price)` 対の系列が必要。
- **いま手元にあるもの(実測)**: `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/` に **zip 475 個**(`.CHECKSUM` も 475、`ls` の合計 950)。1 ファイルの実際の中身を展開して確認: 列 `agg_trade_id,price,quantity,first_trade_id,last_trade_id,transact_time,is_buyer_maker`。`transact_time` はミリ秒(実測サンプル行 `1687651202018`)。**`PriceSeries.from_trades(rows)` が要求する `(ts_ms, price)` の形は `(transact_time, price)` 列をそのまま渡せば作れる**(コード読み: `liq_response.py:267-271`。実行はしていない=未実行だが、フォーマットの整合はコードと実データ両方を読んで確認)。
- **出典**: 実物 zip の展開・ヘッダ確認(このセッション内)、`liq_response.py:267-271`。
- **入手方法**: 不要(足りていた)。
- **代替案**: 1 分足(`from_ohlc_bars`)。ただしそれが A・E・G の原因(`REPORT_2026-09-14.md` §0.0)。
- **印**: 事実(zip 展開・ヘッダを実測)。

## 6. bitFlyer 側の価格(原文「bitFlyerの価格相関」)

- **足りないもの**: Binance COIN-M の窓(2023-06-25〜2024-10-14)と同じ期間の **bitFlyer FX_BTC_JPY の約定単位(tick)価格**。
- **なぜ要るか**: 原文「bitFlyerの価格相関」= 海外清算と bitFlyer 価格を突き合わせる際、1 分足では清算カスケード(数秒〜数十秒)の内部の動きを見られない。
- **いま手元にあるもの**: `docs/DATA.md:59` bitFlyer 1 分足(lightchart)は 2015-11〜2026-09 で **取得済**(2023-06〜2024-10 を含む)。**約定単位(tick)は、窓の全日分は無いが、各月 1 日の標本日なら有る**: `docs/DATA.md:67` の tardis 形式約定 `data/tardis/bitflyer_FX_BTC_JPY_trades/`(2019-09-01〜2026-09-01、各月 1 日、1 日 00:00〜23:59 のフルカバー、向き付き)。**窓 2023-06〜2024-10 に入る標本日は 17 日**(リード実測: `ls | grep -E '^FX_BTC_JPY_(2023(0[6-9]|1[0-2])|2024(0[1-9]|10))01' | wc -l` = 17。監査役の指摘 1 で拾った。調査班の初稿はこの行を見落として「手元に無い」と書いていた)。窓 478 日のうち 17 日(3.6%)。それ以外の約定スナップショット(`docs/DATA.md:66`・`68`・`69`)は最長 2026-07-23〜09-06 で、窓の期間はカバーしない。
- **出典**: `docs/DATA.md:59`・`66`〜`69`(調査班は `61-62` と書いていたが、その行は板・ticker・板 top5 の行。リード検収で行番号を直した)。
- **入手方法**:
  1. **この環境**: bitFlyer `getexecutions` は 31 日ロールオフ(`docs/DATA.md:260`「31 日(サーバが明示エラー)」。調査班の `137` は空行。リード検収で直した)。**過去(2023-06〜2024-10)の tick は公開 API からは取得不可**(31 日より前は消えている。方法: `getexecutions`、範囲: この環境の実測は `DATA.md:260`)。
  2. **オーナー PC**: bitFlyer の長期テープ保管の有無は未確認。
  3. **公開アーカイブ・API**: bitFlyer 公式の長期 tick アーカイブは**未確認**(`docs/DATA.md` に記載が無く、公式ドキュメントに言及が見当たらない、という 2 点だけ。「無い」とは書かない。監査役の指摘 3 を受けて改めた)。tardis(第三者、`data/tardis/` の出所)なら標本日以外も取れる可能性があるが、**有料なので提案しない**(`CLAUDE.md` §0.2 A-3)。
  4. **第三者**: `note.com/taiyo_pro/n/nbe13dc03bbe9`(1 時間足 OHLCV を CryptoCompare から収集、というブログ記事。tick ではなく 1 時間足であり、この用途には不足。WebSearch で確認、2026-09-17)。
  5. **有料**: 未確認。
- **代替案**: (a) 標本日 17 日だけ約定単位で見る(窓の 3.6%。カスケードが標本日に何件入るかは未集計)。(b) 1 分足(取得済)で代用し、突き合わせの分解能を「1 分」に落とす。清算カスケードが数秒〜数十秒で終わる点(§4 参照)を考えると、**1 分足では bitFlyer 側の反応の内部構造は見えない**という限界を明記して使う。
- **印**: 事実(1 分足の取得状況・31 日ロールオフ・標本日 17 日)/ 未確認(公式の長期 tick アーカイブの有無)。

## 7. bitFlyer の清算データ【閉じた: 集めない(L-190)】

**オーナー決定 L-190(2026-09-17)**: 「**3は不要(bitFlyerCFDのレバレッジは2倍、清算などほとんど起きない)**」。この項目は「集めない」で閉じる。下の記述は閉じる前の調査の記録。

- **足りないもの**: bitFlyer 自身が発する清算(強制ロスカット)イベント。
- **なぜ要るか**: 原文「bitFlyerの価格相関」を清算の一次データとして使う場合に必要(現状は海外清算 + bitFlyer 価格の組み合わせで代替)。
- **いま手元にあるもの**: 無し。
- **出典**: `docs/DATA.md:72`「**試行して不可**(bitFlyer Lightning の公開 API リファレンスと実測スキーマの両方で、約定に清算を識別するフィールドが存在しないことを確認。清算履歴の専用エンドポイントも見つからない)」2026-09-12、`docs/DATA/probes/20260912_o3c_supp_a_bitflyer_bitmex.log`。
- **入手方法(再プローブ)**:
  1. **この環境**: 今回 `lightning.bitflyer.com/docs` へ HEAD を再実測 → **HTTP 200**(到達は生きている。ページ自体に清算専用エンドポイントが無いことは 09-12 の実測どおりで変化なし)。
  2. **オーナー PC**: 未確認。
  3. **公開アーカイブ・API**: 同上、専用エンドポイント無し。
  4. **第三者**: 今回 WebSearch(`bitFlyer FX_BTC_JPY 強制決済 ロスカット 履歴 API OR データ 第三者`)を実行 → **bitFlyer の清算履歴を提供する第三者は見つからなかった**(検索結果は個人ブログの API 利用記事のみ)。
  5. **有料**: 未確認。
- **代替案**: bitFlyer の証拠金維持率・ロスカット閾値の**一次資料**(SFD FAQ 等)から、価格変化とロスカット発生の**理論上の関係式**を作り、価格系列だけから推定する案。ただし該当 FAQ ページ自体がこの環境から HTTP 403(WAF、`docs/DATA.md:74`)で読めておらず、**この代替案も一次資料未確認のまま**。
- **印**: 事実(再プローブの結果・WebSearch 結果)/ 仮定(代替案が機能する保証は無い)。

## 8. Binance 清算の欠落 3 日(2023-09-09 / 2023-09-23 / 2024-06-01)

- **足りないもの**: 上記 3 日の Binance COIN-M `liquidationSnapshot`。
- **なぜ要るか**: 「その日に清算が 0 件だった」か「取れていないだけ」かを埋めないと、燃料切れの日次頻度を誤って過小に見積もる。
- **いま手元にあるもの**: 無し(3 日とも公開アーカイブに実在しない)。
- **出典**: `docs/OWNER_STATUS.md:35-46`(2026-09-14 実測)。
- **入手方法(再プローブは 2023-09-09 の 1 日だけ今回実施。2023-09-23・2024-06-01 は 2026-09-14 の実測 `docs/OWNER_STATUS.md:37-44` の転記)**:
  1. **この環境**: `data.binance.vision` の UM(`BTCUSDT`)・CM(`BTCUSD_PERP`)両方の `2023-09-09` パスに GET → **両方 HTTP 404**(`NoSuchKey`、今回実測、生ログ 6〜7 行目)。CM 側の約定(aggTrades)は同日に実在することが既に確認済み(`docs/OWNER_STATUS.md`)なので、**「清算が0件だった」と「配信されていない」を区別する材料が無い**(未確認のまま)。
  2. **オーナー PC**: 未確認。
  3. **第三者**: Coinalyze(鍵必須、既知)、CoinGlass(鍵必須、今回再確認)。鍵を取れば当日の集計値は出るが、**Binance 発の生イベントそのものではない**。
  4. **有料**: 同上(鍵の価格・範囲は未確認)。
- **代替案**: 同日の自前 WS 記録は 2026-09-08 以降のみで、2023-09/2024-06 には遡れない。**この環境と公開アーカイブでは代替無し**(方法: `data.binance.vision` GET、自前記録の範囲確認)。**オーナー PC と有料鍵の経路は未確認のまま**なので「全経路で無い」とは書かない(監査役の指摘 2 を受けて改めた)。実務上の代替は、3 日を「欠測」として除外し、日次頻度の分母から外す(0 件と扱わない)。
- **印**: 事実(404 の実測)/ 推定(「0 件」か「未配信」かの判定は未確認)/ 未確認(オーナー PC・有料)。

## 9. レバレッジ分布・ロング/ショート比

- **足りないもの**: **列は存在するが空(実測)**。
- **なぜ要るか**: 原文①「価格毎の約定履歴の積み上げからどの価格帯で清算が起きるかの予測」に、建玉のレバレッジ構成があれば精度が上がる見込み。
- **いま手元にあるもの(実測)**: `metrics` の列 `count_toptrader_long_short_ratio` / `sum_toptrader_long_short_ratio` / `count_long_short_ratio` は、調査班のサンプル 6 日・1,728 行で**全て空欄(0.0%)**。**リード検収で 7 日(2023-06-25/26, 2024-01-02/03, 2024-10-12〜14)・2,016 行を再計算: 3 列とも 2,016 行全部が空欄、他の 5 列は空欄 0**(調査班は「6 日」と書いて 7 日を列挙していた。1,728 = 6 日 × 288 行なので、どの 6 日かは調査班の記述からは決まらない。リードの 7 日で置き換える)。一方 `sum_open_interest` / `sum_open_interest_value` / `sum_taker_long_short_vol_ratio` は**同サンプルで 100% 埋まっている**。**レバレッジ分布そのものの列は無い**(この API に無い)。
- **出典**: リード検収の再計算(コマンドと出力は `docs/AUDITOR/ACTION_LOG.md` 049)。
- **入手方法**:
  1. **この環境**: 上記 API で top-trader 比率が空である理由(BTCUSD_PERP がこの指標の対象外か、収集側の欠測か)は未確認。
  2. **オーナー PC**: 未確認。
  3. **公開アーカイブ**: 同じ空欄が全期間で続くかは、6 日サンプル以外は未確認。
  4. **第三者**: CoinGlass 等はロング/ショート比を提供する製品ページがあるが鍵必須(未確認、価格未取得)。
  5. **有料**: 未確認(鍵の価格・範囲とも)。
- **代替案**: **無し**(レバレッジ分布そのものを出す経路は、この環境・公開アーカイブでは見つかっていない。第三者は鍵未取得で未確認)。`sum_taker_long_short_vol_ratio`(取れている)は**別の量**(テイカーの売買比率)であり、代替ではない。使うなら「レバレッジ分布の代理」と呼ばず、別の軸として扱う(監査役の指摘 12 を受けて改めた)。
- **印**: 事実(6 日サンプルの空欄率の実測)/ 推定(全期間・原因未確認)。

## 10. 価格ごとの約定量(volume at price)

- **足りないもの**: **データではなく計算**。`aggTrades`(§5 で読める形と確認済み)から `price` と `quantity` を集計すれば作れる。
- **なぜ要るか**: 原文①そのもの(「価格毎の約定履歴の積み上げ」)。
- **いま手元にあるもの**: `aggTrades` 475 日分(§5)。集計コードは今回のリポジトリ内には専用のものが見当たらない(`src/bot/research/board.py` は板歩行コストの関数群で、volume-at-price の集計とは別物。板データではなく約定データからの集計になるため既存関数の転用は要検討)。
- **出典**: §5 と同じ実測。
- **入手方法**: 計算。`aggTrades` の `price` × `quantity` を価格帯ごとに積む処理を書く(1 件目(①)に着手するときに、枠組みをオーナーに見せてから)。
- **代替案**: 1 分足の出来高を終値の価格帯に積む(粗い代理。約定単位の情報を落とす)。
- **印**: 事実(データの存在)/ 推定(既存コードでの転用可否は未確認)。

## 11. 対照窓の作り方・読み出し層(`load_binance_cm_liquidations` / `load_gate_liquidations`)と清算 `side` の向きの検証

- **足りないもの**: **データではなく手順(意図と実装の突き合わせ = INTENT_MAP)**。
- **なぜ要るか**: `REPORT_2026-09-14.md` の射程表 F 行「読み出し層と清算 `side` の向き、対照窓の作り方 | **一度も検証を通っていない**」。
- **いま手元にあるもの**: `side` の向き判定自体は一次資料に基づく実測がある(Binance: 公式の force order 定義どおり。Gate: 傍証で決定 — `docs/DATA/surveys/O3C_VERIFY_LIQUIDATION_SIDE_2026-09-13.md`、`liq_response.py:34-42`・`156-165` のコメントに実測根拠が明記)。**INTENT_MAP による突き合わせ自体はまだ実行されていない**。
- **出典**: `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-14.md:49`(F 行)。
- **入手方法**: 手順。`INTENT_MAP.md`(`docs/PHASE2/O3C/` に既存)に読み出し層・`side`・対照窓の 3 行を足し、○/△/✕ を付ける(`.claude/skills/research-protocol` §0.5)。
- **代替案**: 無し(△・✕ が残ったまま出た陰性は不明になる、`CLAUDE.md` §5.2)。
- **印**: 事実(REPORT の逐語・コード内の根拠コメント)。

## 12. 実データに触る前の前提条件 3 つ(タイの件数 / タイの経路 / 幅 0 の断片の件数)

- **足りないもの**: **実行(集計処理を書いてから走らせる)**: 472 日全件に対する集計。今回サンプル 6 日で仮の値は出した(§4 のギャップ分布)が、全件では未実行。
- **なぜ要るか**: `REPORT_2026-09-14.md:59`「3 つとも数えるまで、実データの数字は読まない」という自らの前提条件。
- **いま手元にあるもの**: データ(`liquidationSnapshot` 472 日)は揃っている。集計コードは `build_cascades` を使えば得られる(§2・§4 参照)。**専用の集計スクリプトの有無(リード検収で確認)**: `scripts/` で `build_cascades` を呼ぶのは `measure_liq_response.py` / `run_o3c_stage0.py` / `explore_o3c_oi_axis.py` / `verify_liq_instrument.py` の 4 本。**タイの件数・タイの経路・幅 0 の断片を数える処理は 4 本のどれにも無い**(`tie` / `幅 0` / `zero-width` で検索、`verify_liq_instrument.py` は合成データ専用)。**書く必要がある。**
- **出典**: `REPORT_2026-09-14.md:59`、リード検収の検索(コマンドと出力は `ACTION_LOG` 049)。
- **入手方法**: 実行。3 つの数え方を書き(タイ = 起点価格と終点価格が同じ行、経路 = 価格の刻み / 時間分解能のどちらで同じになったか、幅 0 = 束ねた結果 1 件だけのカスケード)、472 日に掛ける(下位モデルに委任できる)。
- **代替案**: 無し(3 つを数えるまで実データの数字を読まない、と `REPORT_2026-09-14.md:59` に書いた)。
- **印**: 事実(前提条件がまだ数えられていないこと / 専用の集計処理が無いこと)。

## 13. 他の取引所の清算データと、その取引所の約定単位の価格

- **足りないもの**: 自前 WS 記録(binance_cm / bitmex / bybit / okx、2026-09-08〜09-16)に対応する**同一取引所・同一期間の約定単位価格系列**。
- **なぜ要るか**: 清算があっても価格系列が無ければ反応(bp)を測れない。
- **いま手元にあるもの(実測)**:
  - `paper_logs/liquidations/` に **binance_cm・bitmex・bybit・okx の 4 取引所、2026-09-08〜09-16(bitmex/bybit/okxは09-08開始、binance_cmは09-09開始)分の `.jsonl.gz` が実在**(実測 `ls`)。**破損ファイル `.trunc1.jsonl.gz` はリード検収の実測で 11 個**(binance_cm 09-11・09-12 / bitmex 09-09・09-11・09-12 / bybit 同 3 日 / okx 同 3 日)。調査班は「10 個で `docs/DATA.md:96` の 10 と一致」と書いたが、**一致していない**(台帳の 10 は 2026-09-11 の記載。差 1 個の原因は未確認)。`data/liquidations/` はこの環境には**存在しない**(実測、`No such file or directory`)。
  - Gate.io の清算 90 日(2026-06-10〜09-08、既知)。
  - 対応する価格系列: BitMEX は **1 分足**が 2026-09-08〜(`docs/PHASE2/O3C/DATA_AVAILABILITY.md:88`「清算(自前WS)+1分足」)で**揃う**。調査班の初稿は「1 秒足」と書いていたが、1 秒足は 2017〜2021 の加工済みアーカイブ(`:49`、`docs/DATA.md:97`)で WS 記録の期間とは重ならない(リード検収で直した)。**さらに BitMEX の XBTUSD 等は 2026-09-16 12:00 UTC に上場廃止・清算、取引所全体は 2026-09-23 閉鎖**(`DATA_AVAILABILITY.md:127-128`、`docs/DATA.md:283`)なので、**BitMEX の組は 09-08〜09-16 の約 8 日で打ち止め**。Bybit は 1 分足の終端(2026-08-31)が清算記録の開始(09-08)より前で**揃わない**。OKX・Binance USD-M も同ファイル §2a の記載どおり未確認/揃わない。Gate は 90 日分、清算・1 分粒度価格(mark_price)ともに揃う(既知)。
- **出典**: 今回の `ls` 実測、`docs/PHASE2/O3C/DATA_AVAILABILITY.md` §2a。
- **入手方法**(経路別。監査役の指摘 8 を受けて他の節と同じ厳密さに揃えた。**今回はプローブしていない** = `DATA_AVAILABILITY.md` §2a の転記):
  1. **この環境**: Bybit・OKX の 1 分足は公開 API から取得可(`DATA_AVAILABILITY.md:32`・`37`「取得可・未取得」)。約定単位は Bybit が公開アーカイブ(`:31`「2020-03-25〜、取得可・未取得」)、OKX は「正 URL 未特定」(`:36`)。
  2. **オーナー PC**: 未確認。
  3. **公開アーカイブ・API**: 上と同じ(Bybit 約定アーカイブ、両取引所の 1 分足)。
  4. **第三者・有料**: 未確認。
  つまり「過去に遡れない」のは**清算**のほう(WS のみ)で、**価格系列は遡って取れる経路がある**(1 分足・Bybit 約定)。調査班の初稿「過去に遡って埋める経路は無い」は価格側について誤りだった。
- **代替案**: Gate の組(90 日、価格・清算ともに揃う)を主対象にし、BitMEX は約 8 日で打ち止めの組として脇に置く。Bybit・OKX・Binance USD-M は「清算だけ記録が進んでいる。価格を公開経路で取るまで測れない」区分として保留する。
- **印**: 事実(ファイルの実在・件数)/ 推定(取引所ごとの揃う/揃わない判定と経路は既存文書の転記であり今回は個別に再検算していない)。

## 14. リード(調査班)が実物を見て「足りないと予想される」もの(候補外の発見)

- **1件目**: `metrics` の `count_toptrader_long_short_ratio` 系 3 列が**全て空**(§9)。原文①のレバレッジ分布の直接的な材料は、この API には無い。**印: 事実**(6 日サンプル実測。全期間での再確認は未実施)。
- **2件目**: `liquidationSnapshot` の生の時間間隔は**中央値 0ms**(同一 ms の複数注文が多い、§4)。カスケードの束ね方(`gap_ms`)の議論の前に、「同時刻の複数レコードをどう数えるか」自体が未定義。**印: 事実**(6 日サンプル実測)/ 推定(全期間での一般化)。**【解決 2026-09-17】同一 ms の正体は完全一致の重複行(1 件 = 2 行。`EXT_2026-09-17.md` §2.0)。数え方 = 全列一致の行を 1 件にする。`build_cascades` の `n_events` / `total_size` も重複込みで 2 倍になるので、使う前に一意化が要る(未実装)。**
- **3件目**: `paper_logs/liquidations/` の破損ファイル(`.trunc1.jsonl.gz`)は**回収待ちのまま**で、**実測 11 個**(`docs/DATA.md:96` の「10 ファイル」より 1 個多い。台帳は 09-11 の記載。リード検収で訂正)。回収されないと自前 WS 記録の期間に穴が残る。**印: 事実**(件数)/ 未確認(差 1 個の原因)。
- **4件目**: bitFlyer の tick データ(§6)を埋める代替案(一次資料からの理論推定)も、その一次資料(SFD FAQ)自体がこの環境から読めない(§7 の代替案参照)。**「代替案の代替案が無い」二重の不足**。**印: 事実**(既存プローブの転記)。

---

## `docs/DATA.md` への追記案(**マージ済み、2026-09-17**。リードが検収後の値で §2 の海外暗号資産の表に 5 行足し、96 行目の破損 10 → 11 を直した)

| 資産 | 所在 | 範囲 | 状態 | 最終確認日 | プローブのログ | 使った単位 |
|---|---|---|---|---|---|---|
| CoinGlass 建玉履歴(`open-interest/history`) | `https://open-api-v4.coinglass.com/api/futures/open-interest/history` | — | 試行して不可(HTTP 200、本文 `{"code":"401","msg":"API key missing."}`。方法: GET、実測 2026-09-17) | 2026-09-17 | `docs/DATA/probes/20260917_o3c_missing.log` | O-3c(未使用) |
| Binance COIN-M metrics のレバレッジ関連列(`count_toptrader_long_short_ratio` 等) | `backtest_data/binance_cm_o3c_20260913/metrics/BTCUSD_PERP/` | サンプル 7 日・2,016 行で 3 列とも全て空欄(リード再計算) | 取得済(列自体は空。データではなく欠損。全期間は未確認) | 2026-09-17 | 本報告(実測、`ACTION_LOG` 049) | O-3c(未使用) |
| Binance COIN-M aggTrades の読み込み可否(`PriceSeries.from_trades` 用) | `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/` | 475/478日 | 取得済(列 `transact_time,price` を直接読める形と実測確認) | 2026-09-17 | 本報告(実測、専用ログ未保存) | O-3c(未使用) |
| 自前WS清算記録(paper_logs)の取引所別・破損ファイル件数 | `paper_logs/liquidations/*.jsonl.gz` | binance_cm/bitmex/bybit/okx、2026-09-08〜09-16、破損 **11** 件 | 取得済(台帳の 10 件と**不一致**、リード実測 11 件) | 2026-09-17 | 本報告(実測、`ACTION_LOG` 049) | O-3c(未使用) |
| Binance liquidationSnapshot 欠落3日の再プローブ | `data.binance.vision`(UM/CM 両方、2023-09-09) | 該当なし | 試行して不可(両方 HTTP 404、方法: GET、実測 2026-09-17) | 2026-09-17 | `docs/DATA/probes/20260917_o3c_missing.log` | O-3c(未使用) |

## 予算・完了状況

実時間 約20分、生ログ 9行(`docs/DATA/probes/20260917_o3c_missing.log`)。1〜14 の全項目を実物確認まで完了。**未完了は無い。**

## リードの検収(2026-09-17、`research-squad` §6)

生ログ 9 行は実在し、本文の HTTP コード(451 / 404×3 / 401 / 200 本文 401 / 200×3)と一致した。再計算・再確認した項目と結果(コマンドと出力は `docs/AUDITOR/ACTION_LOG.md` 049):

| 項 | 調査班の記述 | リードの実測 | 処置 |
|---|---|---|---|
| §1・§2・§5 zip 件数 | metrics 758 / 清算 944 / aggTrades 950(各 ×2) | 758 / 944 / 950、zip 単独 379 / 472 / 475 | 一致 |
| §1 OKX 自前建玉 | 9 日分(09-05〜09-15) | ディレクトリ 9 個(0905〜0915) | 一致 |
| §1 `openInterestHist` の遡り 30 日 | 「一次資料上の制約」 | 一次資料の URL 無し | **未確認に改めた** |
| §4 間隔分布 8 値・78.9% | 6 日・1,038 件 | 同じ 6 ファイルで全 8 値と 819/1,038 が一致。0ms は 522(50.3%) | 一致、0ms の割合を追記 |
| §5 aggTrades のヘッダ | 7 列、`transact_time` ms | 同じ 7 列、先頭行 `1687651202018` | 一致 |
| §6 出典の行番号 | `DATA.md:61-62`・`137` | 61-62 は板の行、137 は空行。正しくは 59・66〜69・260 | **直した** |
| §8 欠落 3 日の aggTrades | 実在(既確認の転記) | 3 日とも zip 実在 | 一致 |
| §9 サンプル | 「6 日」と書き 7 日を列挙、1,728 行 | 7 日・2,016 行で 3 列とも全部空欄 | **7 日の数値で置き換えた** |
| §12 専用スクリプトの有無 | 未確認 | `build_cascades` を呼ぶ 4 本にタイ・断片の集計は無い | **確認して書いた** |
| §13・§14-3 破損ファイル | 10 個、台帳と一致 | **11 個**、台帳(09-11)の 10 と不一致 | **直した** |
| §13 BitMEX の価格系列 | 「1 秒足が 09-08〜継続中」 | `DATA_AVAILABILITY.md:88` は **1 分足**。1 秒足は 2017〜2021。XBTUSD は 09-16 上場廃止、取引所は 09-23 閉鎖 | **直した** |
| 引用の行番号(`OWNER_STATUS.md:35-46`、`REPORT_2026-09-14.md:49・59・140-141`、`DATA_AVAILABILITY.md:88`、`liq_response.py:220-222・267-271・34-42・156-165`) | — | 全部その内容がある | 一致 |

**検収で直した箇所は本文中に「リード検収」と明記した。**調査班の記述で検収していないもの: §6 の第三者(note.com の記事)と §7 の WebSearch の結果(検索はこの環境で再実行していない)、§13 の取引所ごとの「揃う / 揃わない」(`DATA_AVAILABILITY.md` の転記のまま)。

**納品前の監査(`owner-auditor`、2026-09-17)**: 止める 2・直す 7・聞く 5 = 14 件。直した 11、答えた 3(6・7・10)。全文と各件の応答は `docs/AUDITOR/VERDICTS/2026-09-17_missing.md`。監査で拾われた検収の漏れ: tardis の bitFlyer 約定(標本日 17 日、`DATA.md:67`)を「無い」と書いていた(§6)、§13 の「遡れない」は価格側について誤りだった、§8 の「代替無し」は未確認の経路を残したままだった。
