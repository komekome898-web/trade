# P2-02 J-REIT ETF 夜間プレミアム(1343)— 事前登録(第 2 稿 2026-09-06、設計監査 1 回目の致命 4 件を反映、2 回目待ち)

## 仮説(`docs/PHASE2_TRIAGE.md` §4 の転記のみ)
J-REIT ETF でも先物と同様の夜間プレミアムが存在し、取引可能な形で捕捉できるか。

## 格子セル
市場 = JPX 現物 ETF(1343、NEXT FUNDS 東証 REIT 指数連動)/ 執行様式 = 板寄せ(引成で買い、翌寄成で売り)/ 時間スケール = 日 / 情報源 = 自市場の価格のみ

## データ
| ファイル | 内容 | 期間 | 封印(`backtest_data/phase2_sealed/P2-02/SEALED.json`) |
|---|---|---|---|
| `reit_onr_20260904/etf_1343_daily.csv` | 1343 日足(主系列) | 2008-09-16 .. 2026-09-03 | 2021-04-13 以降 |
| `jpx_etf_daily_20260905/1343.T.csv` | 同銘柄の同一出所コピー(Yahoo) | 2011-09-05 .. 2026-09-04 | 同。コピーの同一性確認のみ(独立検証には使えない) |
| `reit_onr_20260904/etf_1343_dividends.csv` | 分配金(権利落ち日、額) | 2013-02-10 .. 2026-08-10 | 同。権利落ち日をまたぐ夜間リターンの補正に使用 |
| `reit_onr_20260904/etf_1321_daily.csv` | 日経 225 ETF(「先物と同様」の比較用) | 2009-01-05 .. 2026-09-03 | 同 |
| `reit_onr_20260904/reit_index_daily.csv` | 東証 REIT 指数(300 行のみ) | 2025-06-16 .. 2026-09-03 | 全期間封印(フォワード側でのみ使用: ETF と指数の乖離の実測) |
| `paper_logs/onr_ledger.csv` | フォワードのペーパー台帳(現在 0 行) | 2026-09-06 以降 | フォワード |

- 既知欠陥(監査 1 回目の実測で更新): (1) `etf_1343_daily.csv` に **open = 0 の行が 4 日**(2009-07-01, 07-03, 07-27, 08-06)あり、
  そのまま使うと −100% の偽リターンが入る(σ が 375bps に化ける)。(2) 上場直後の出来高 0 の行は 71 行(暦日で 111 日)。
  (3) 分配金の権利落ち日は価格が機械的に下がる。(4) `1343.T.csv`(Yahoo)と `etf_1343_daily.csv` は**同一の出所**(重なり 3,687 日で差分 0)
  なので相互検証の力は無く、コピーの同一性確認にのみ使う。(5) 2024-11-05 に引け板寄せが 15:00 → 15:30 に変更(封印期間内)。
  schema/reit_onr.json の既知欠陥は (1)(2) を 1343 についても記載するよう更新する。
- **欠陥の扱い(事前規則、評価時に目で選ばない)**: open または close が 0・空・`extreme_return` の 1 日往復に該当する行を含むペアは
  全期間で機械的に除外して件数を報告。出来高 0 の行は除外しない(印のみ)。除外前後の両方を報告し、判定は除外後。
- **開発セット** = 2009-01-05 .. 2021-04-12(規則適用後 n ≈ 3,015 ペア)。**train** = 2009-01-05 .. 2017-12-31、**val** = 2018-01-01 .. 2021-04-12。
  端点規則: ペアは両日が封印外のときだけ開発セット、最終評価も両日が封印期間内のペアのみ。
- 読み手は `bot.research.sealed.load_unsealed(path, "P2-02")` で読む。

## リターンの定義
- 夜間リターン r_night(t) = open(t+1) / close(t) − 1。権利落ち日が t+1 の場合は分配金額を open(t+1) に加算した値で計算し、補正前も併記。
- 日中リターン r_day(t) = close(t) / open(t) − 1(対照)。
- 「先物と同様」の判定: 同じ定義で計算した日経 225 ETF(1321)の r_night と符号が一致し、双方の 95% CI が正であること。
- 「取引可能な形で捕捉できる」の判定: 保守コスト(下記)後の平均が正で 95% CI が正、かつ**ドローダウン基準**: 開発セット全期間の
  累積ネット損益(1 口固定、保守コスト後)の最大ドローダウン(単一実現値)が、年率換算した平均ネット利益の 3 倍以内。3 倍は事前に置く
  慎重側の規則(資本の 3 年分の期待利益を失う局面を許容しない)。ブロック・ブートストラップ(20 営業日)で最大ドローダウンの分布の
  中央値も併記する(判定は単一実現値)。

## コスト定数(`config/constants.yaml`、`assumed` は使用不可)
| キー | 値 | source_type |
|---|---|---|
| jpx_cash_equity.sor_commission_yen | 0 円(2026-05-18 約定分から) | primary_document |
| jpx_cash_equity.non_sor_fee_yen_by_notional | 55 / 99 / 115 / 275 円(約定代金帯別、2026-05-18 以前の水準) | primary_document |
| jpx_cash_equity.etf_tick_size_yen_1000_3000_band | 1 円 | primary_document |
| jpx_cash_equity.etf_spread_bps | null(未計測) | 使用禁止 |

- **費用の適用規則(致命の対処)**: 収益率は制度に依存しない価格変化率なので、判定は「今この戦略を運用したら」の**現行制度**
  (SOR 手数料 0 円)で行う。過去期間に当時の手数料を遡及適用した値は**感度として併記**する(55 円/約定を想定元本 10 万円 = 約 52 口で
  約 5.5bps/片側、1 口だけなら 290bps/片側 = 取引不能)。したがって想定ロットも事前に固定する: **約定代金 10 万円相当(約 50 口)**。
- 1343 の現値は約 1,900 円なので呼値 1 円 = 約 5bps。板寄せはスプレッドを払わないが丸めが片側ごとに入り得る。
  費用後の評価は「楽観: 手数料 0 + 丸め 0」「保守: 手数料 0 + 1 ティック/片側(約 10bps/往復)」の 2 本立て、判定は保守側。
- フォワードでは `reit_index_daily` と ETF の寄付き・引けの乖離を実測し(ペーパー台帳の `gap_bps`)、最終評価の後に丸め仮定を実測値に
  置き換える(反復として台帳に記録)。最終評価の前に封印データを費用計測のために開かない。

## 指標と分母
- 主指標: 保守コスト後の平均夜間リターン(bps/日)と 95% CI。分母 = 開発セットの営業日ペア、n ≈ 3,000。
- 副指標: 夜間 − 日中の差と CI、Sharpe(年率)、最大ドローダウン、勝率、権利落ち補正の有無での差、1321 との相関と残差。
- MDE(α 0.05、検出力 0.8、監査 1 回目の実測): 欠陥規則適用後 σ = 88bps、n ≈ 3,015 → SE 1.6bps → **MDE ≈ 4.5bps/日**。
  保守コスト 10bps/往復を上回るには平均 ≥ 14.5bps/日が必要(MDE の 3 倍)。届かない場合は「存在するが取引不能」として棄却側に分類する。

## 対照(反事実)
1. シャッフル(符号の無作為化 1,000 回)。
2. 全時刻無作為 + 20 日実現ボラ三分位内の無作為保有(診断のみ)。
3. 符号反転(夜間売り持ち)。
4. 日経 225 ETF(1321)を同時に同じ手順で評価し、REIT 固有か市場全体かを分ける。

## 探索面
主検定の自由パラメータなし(N0 = 1)。診断(ボラ三分位 3、曜日 5、月 12、権利落ち前後 2)は選択に使わない。

## 停止規則
直近 3 反復の val 主指標の改善 < MDE、または反復 8 回。

## 最終評価(封印データ、1 回のみ)
- 封印期間 2021-04-13 .. 2026-09-03 とフォワード。
- バー: 累計 N の帰無 95 点超、かつ保守コスト後の平均夜間リターンの 95% CI が正、かつ 1321 と符号一致。
- 反証文: 封印期間で「保守コスト後の平均夜間リターンの 95% CI がゼロを含む」なら「取引可能な形で捕捉できる」は棄却。
  楽観コストでのみ正の場合は「存在するが取引不能(丸め・スプレッド次第)」として保留、引き金 = スプレッドの実測。

## 執行様式のパイプライン既知正解テスト
型 = 日次 / 板寄せ。合格記録 = `backtest_data/qa_pipeline_daily_20260905/RESULTS.md`。**分配金補正**は既存テストに無いので、手計算した
極小ケース(権利落ち日をまたぐペア 3 件)をパイプラインテストに追加し、合格を確認してから実行する。

## 開示
リードは過去の研究結果を記憶している。本書は転記された仮説文のみを起点に書き、過去レポート・判定・数値は参照していない。
