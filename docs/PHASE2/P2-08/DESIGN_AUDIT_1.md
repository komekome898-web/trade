# P2-08 設計監査(第1稿、盲検・下位)

対象: PREREG.md 第1稿(データ構築中)。総合判定: **要修正**(致命0、要修正6項目、着手前に解消)。

## 定型10問

1. 仮説文の数値・過去判定混入: なし。転記のみ。→ 可
2. 分母/n見込み/MDE: 記載あるがσ(取引リターン)が「実装時に実測、見込み30〜60bps」のまま凍結案 → 見込みレンジが2倍あり、MDEが8〜17bpsとぶれる。開発セット(封印外、train)で現行構成のσを直ちに実測し単一値でMDEを確定してから凍結すべき。→ 要修正
3. 封印とSEALED.json一致: データ構築中でSEALED.json未生成のため現時点で検証不能。生成後に本項のみ再監査要。→ 保留(致命ではない)
4. コスト定数がmeasured/primary_documentか: constants.yaml確認、全てmeasuredまたはprimary_document(assumedなし)。ただしfunding_swap_daily_pctは3日サンプルのみで薄い。→ 可(注記)
5. 対照3種が仮説の反事実か: 4種登録(全時刻無作為、符号シャッフル、先行系列ブロック置換、先行なし対照)。1〜3は仮説の直接帰無、4は「他市場寄与の分離」用の比較ベースラインで役割が異なる → 呼称を分けるとよいが妥当。→ 可
6. 探索面N0: 3×3×3=27、正しく数えられている。ただし反復の梯子は反復1(実現ボラ三分位)・反復2(セッション三分位)の2つしか状態変数を定めておらず、停止規則の上限(8反復)まで埋まっていない。反復3以降に事後選択の余地が残る。→ 要修正(反復3〜8の状態変数、または「未定義なら反復2で打ち切る」旨を明記)
7. 停止規則と反復予算: 記載あり(直近3反復val改善<MDE、または8回)。MDEが2番と連動して未確定のため運用基準が定まらない。→ 要修正(2と同一原因)
8. 既知欠陥への対処: gap>5分の除外は明記。一方、`candles_fx_btc_jpy`のffillバグ(メンテ窓/ゼロ出来高バーが前バーのOHLCをコピー、2026-09-05以前はsyntheticフラグ無し)への言及がなく、次足始値執行モデルに直接混入しうる。仕様§5の「妥当性マスク適用前後の両方を報告」もPREREGに明記がない。→ 要修正
9. 執行様式の既知正解テスト: 汎用taker/分(qa_pipeline_taker_20260905)は合格済みだが、本単位固有の「先行シグナル→次足始値→ストップ」植え込みテストは「実行前に追加」と書かれているのみで未実施。仕様§5は実施後でないと当該様式の研究単位を走らせない規定。→ 要修正(実行前必須、現状未完了)
10. 反証文の具体性: 「フォワードn≥200で保守コスト後平均の95%CIがゼロを含むなら棄却」は具体的。→ 可

## 追加7問

(1) forward-onlyシール設計は妥当(bitFlyerが約45日しかなく70/30では封印2週間で検出力不足)。到達日数を実測: Binance 210日系列(1分足)でk=30・close(t)/close(t−30)−1の建玉シミュレーション(exit=0.05%)では、thr=0.8%(現行)で210日886件≒4.2件/日→**n=200到達に約47暦日**。thr=0.4%なら約13日、thr=1.2%なら約131日。val選定後の最良構成が高thr側に寄ると到達が数か月に伸びうるため、PREREGに構成別の到達日数見込み表を明記すべき。→ 要修正
(2) 執行モデル(次足始値+実測片道費用、ストップは高安判定・次足始値約定)自体は明確。ただし戦略は相場が動いた瞬間に発注するため、16日tapeの無条件平均コスト(spread中央値1.9bps等)は静穏時主体でバースト時を過小評価しうる。「執行差」(PAPER実約定とモデルの差)は明記済みだが、直前1分の|m(t)|三分位などバースト条件付きの実現コスト分割測定が無い。→ 要修正(条件付きコスト測定を追加)
(3) best-of-Nの帰無構成が未記載。仕様§4/テンプレ§5にある「同じ置換世界から全統計量を計算して最大を取る」手順を、対照1〜4のどれ(または束ね方)で作るか、各反復で何回re-runするかが書かれていない。→ 要修正
(4) 対照1〜4は反事実として妥当(5参照)。→ 可
(5) 代理価格禁止(Binance×FXでbitFlyer欠測期間を埋めない)に同意。SFD等のCFD固有乖離がコストと同オーダーなら代理は前提の誤りになるため、Binance系列を信号統計限定に留める設計は妥当。→ 可
(6) MDE誠実性: (2)参照。σを見込みレンジのまま凍結するのは誠実さに欠ける。開発セットで即時実測し単一値化してから凍結すること。→ 要修正
(7) 新規の問題点: (a) ffillのsyntheticバー混入(2026-09-05以前は検出不能)、(b) 反復3〜8の状態変数未定義、(c) スワップ按分の基準時刻(UTC0時かbitFlyer実ロールオーバー時刻か)未記載、(d) 0.01BTC固定サイズなので複利端数問題は無いが明記した方がよい、(e) UTCセッション区分(0-8/8-16/16-24)は固定式でDST影響は無いが「東京/欧州/米国」ラベルは実地時間帯とズレる時期がある(誤解防止のため注記推奨)、(f) 分足重複3405分の「正しい方を採る」具体的解消手順が未確定。致命は無いが(a)(b)(c)は要修正。

## データ節(構築完了時に必須で追記する項目)

- 実ファイルパス・MD5・SEALED.json生成日時、凍結日(`--seal-from`)の具体的日付
- 三点(bitFlyer分足×Binance分足×約定)の実連続窓の確定値(G2棚卸しでは最長28日、PREREGの想定45日との差の説明)
- Binance 1分足データセットの`schema/*.json`定義が現状無い(`schema/binance_daily.json`は日次のみ)→ 新設または追記要
- 分足重複3,405分の解消方法と採用ソースの決定根拠
- forward_start以降の実際の取引可能日数見込み(追加問(1)の到達日数と整合させる)
- ffill/synthetic既知欠陥の妥当性マスク適用前後、両方の取引数・指標

## 総合判定

致命: 0。要修正: 定型2,6,7,8,9 + 追加(1)(2)(3)(6)(7a-c)。可: 定型1,4,5,10 + 追加(4)(5)。保留: 定型3(データ節参照)。
→ 現段階では実行着手不可。上記修正(特にMDE/σの実測、本単位固有の既知正解テスト、バースト条件付きコスト、反復3〜8の状態変数)を反映した第2稿を、データ節完成後に再監査すること。
