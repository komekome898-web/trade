# 第 4 の方針(群で対称かつ向きを保つタイ方針)は在るか — 測った結果(2026-09-16)

> 委任先(下位モデル、研究・実装)の報告をリードがそのまま保存した(委任先の Write が拒否されたため)。
> 検収: リードが差分(3 ファイル、+453/−3)・証拠ログ(`evidence_2026-09-16/15_第4の方針.log` 172 行)・`pytest -k liq` 89 件通過を実物で確認した。

**実データにはまだ触っていない。ここにあるのは全部合成データの結果である。**
土台は `REPORT_2026-09-14.md` §0 の 4 と §1.4.4、および `evidence_2026-09-14/14`。

## 0. §0.1 の対応表(着手前に出すもの)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 測定器の検証を終わらせるために、残っていた改良点を測る | 「**先に測定器の検証を終わらせてください。これは監査の検証でもあるので結論が出ない限り次に進めません。**」L-167 |
| 第 4 の方針の検討をその検証に含める | 「**1. 含める**」L-181 |
| 着手する | 「**進めてください**」L-183 |
| 反転側と継続側の**両方**で測る | 「**トレンド転換が起きるかまたはそれがトレンドになるのか**」`docs/PHASE2/O3C/OWNER_INTENT_2026-09-12.md` §1.1 |
| 問いを「群で対称に向きを決め直す方法があるか」に固定する | **(該当語なし)** — リードの依頼文(`REPORT_2026-09-14.md` §0 の 4 の逐語)から |
| 必要条件 (a)〜(d)・合否 ±3.0bp・8 行の場面・候補は最大 3 つ・判定は候補ごと一度だけ | **(該当語なし)** — リードの依頼文の事前登録 |
| `TiePolicy` に方針を足す / 既存 3 方針は変えない / 表に列を足す / evidence を `15_第4の方針.log` に保存 | **(該当語なし)** — リードの依頼文の実装指示 |
| コミットも押し出しもしない | **(該当語なし)** — リードの依頼文 |

## 1. 候補 3 つ — 向きの情報源と、(a)〜(d) の充足

| 候補 | **どの情報から向きを取るか(1 行)** |
|---|---|
| `widen` | 粗い系列そのもの。タイ行の起点の点から **K 点前**と、終点の点から **K 点後**(同数)の価格差の符号。**K=1 固定**(潰れたタイを解ける最小の幅。大きくするほど反応窓を余計に取り込むので最小を取る) |
| `ohlc` | **OHLC バーの、起点を含むバーの始値 → 終点を含むバーの終値**。窓が 1 本に収まる行はそのバーの始値→終値 |
| `symfine` | **間引く前の細かい価格系列**を**両群に**当てて読んだ起点→終点(= 対称な `refine`) |

- **(a) 群で対称**: 3 つとも入力は「両群に同じ系列/バー列」と行の `start_ms`/`end_ms` だけ。実群だけが持つ `first_price`/`last_price` は**読まない**(`refine` との違いはここだけ)。実測: 8 行すべてで**対照群の除外率の差 = 0.000**。単体テストでも実群と対照群に同じ値が付くことを固定した(新規 7 件)。
- **(b) 落とさない**: 8 行すべてで**対照群 n_used/n_rows = 300/300**。列が無い・NaN・符号 0 の行は `keep` と同じく 0 のまま残す(テストで固定)。
- **(c) 0 に潰さない**: タイ行の `n_tie_resolved` は下表の脚注のとおり(13.7% の場面で 41/41)。
- **(d) `bp_*` を読まない**: 3 つとも行の `bp_*` を参照しない。**ただし `widen` と `ohlc` は `end_ms` より後の価格を見るので、反応窓と時間的に重なる**(§5 射程 2)。`symfine` は窓の外を一切見ないので、この重なりは無い。

## 2. 8 行 × (既存 3 方針 + 候補 3 つ)

**この 8 行と、左の 4 列(向きを与えた推定器 / keep / refine / drop)は `evidence_2026-09-14/14` と同じ値である**(打ち直して一致を確認。比較の土台なので変えていない)。値は**符号つき**、括弧内は**仕込み(±20.0bp)からのずれ**。許容 ±3.0bp。

| # | 場面(長さ / バー) | 向き | n_tie | 向きを与えた | `keep` | `refine` | `drop` | **`widen`** | **`ohlc`** | **`symfine`** |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 5〜45秒 / 60秒 | 反転 | 41/300 (13.7%) | +14.319 (5.68) | **+17.189 (2.81)** | +14.319 (5.68) | +19.910 (0.09) | +14.319 (5.68) | +14.319 (5.68) | +14.319 (5.68) |
| 2 | 5〜45秒 / 60秒 | 継続 | 41/300 (13.7%) | −25.421 (5.42) | **−17.345 (2.66)** | −25.421 (5.42) | −20.090 (0.09) | −25.421 (5.42) | −25.421 (5.42) | −25.421 (5.42) |
| 3 | 60〜120秒 / 60秒 | 反転 | 167/390 (42.8%) | +5.350 (14.65) | +8.630 (11.37) | +7.103 (12.90) | +15.094 (4.91) | +5.350 (14.65) | +10.146 (9.85) | +8.650 (11.35) |
| 4 | 60〜120秒 / 60秒 | 継続 | 167/390 (42.8%) | −33.072 (13.07) | −13.941 (6.06) | **−20.144 (0.14)** | −24.382 (4.38) | −33.072 (13.07) | **−17.101 (2.90)** | −16.046 (3.95) |
| 5 | 5〜45秒 / 300秒 | 反転 | 300/300 (100%) | −23.868 (43.87) | +0.000 (20.00) | −23.868 (43.87) | nan (n_used=0) | −22.684 (42.68) | −23.868 (43.87) | −23.868 (43.87) |
| 6 | 5〜45秒 / 300秒 | 継続 | 300/300 (100%) | −52.077 (32.08) | +0.000 (20.00) | −52.077 (32.08) | nan (n_used=0) | −32.888 (12.89) | −52.077 (32.08) | −52.077 (32.08) |
| 7 | 1〜10秒 / 60秒 | 反転 | 0 (0%) | +19.929 (0.07) | +19.929 | +19.929 | +19.929 | +19.929 (0.07) | +19.929 (0.07) | +19.929 (0.07) |
| 8 | 1〜10秒 / 60秒 | 継続 | 0 (0%) | −20.071 (0.07) | −20.071 | −20.071 | −20.071 | −20.071 (0.07) | −20.071 (0.07) | −20.071 (0.07) |

**対照群の n_used / n_rows と除外率の差**: 候補 3 つは **8 行すべてで 300/300、差 0.000**。比較のため `drop`: 行 3・4 で実群 223/390(42.8% 除外)対 対照 300/300(0%)= **差 0.428**(上限 0.10 超)、行 5・6 は両群 0/300。**候補は (b) を満たし、`drop` は満たさない。**
`n_tie_resolved`: 行 1・2 = 3 候補とも 41/41、行 3・4 = `widen` 167 / `ohlc` 167 / **`symfine` 22**、行 5・6 = `widen` 299 / `ohlc` 300 / `symfine` 300。

**タイ率の表記を訂正する**: 09-14 の報告と `evidence_14` は行 3・4 を「**55.7%**」と書いたが、実際は **167/390 = 42.8%** である。分母に投入 N(300)を使い、**実カスケードが 60 秒超のギャップで分割されて n_rows が 390 になっていること**を落としていた。**場面そのもの(種・つまみ)は同じ**なので左 4 列の値は 1 つも変わらない。訂正は分母だけである。

## 3. 合否(候補ごとに一度だけ)

| 候補 | 判定 | 外れた場面 | 0% での退行 |
|---|---|---|---|
| `widen` | **不合格** | 行 1〜6 の**全部**(最悪 42.68bp) | 無し(既存と差 0.000) |
| `ohlc` | **不合格** | 行 1・2・3・5・6(行 4 だけ許容内) | 無し(差 0.000) |
| `symfine` | **不合格** | 行 1〜6 の**全部**(最悪 43.87bp) | 無し(差 0.000) |

**3 つとも不合格である。**「惜しい」とは書かない。なお **`keep` は行 1・2 では許容内**(2.81 / 2.66)で、**候補 3 つはそこで `keep` より悪い**。

## 4. なぜそうなるのか

**タイの経路は 8 行すべて「時間分解能」である。価格の刻みの経路は 0 件**(この 8 行は `quantize_prices` を通しておらず、価格を丸めていない)。実測ではタイ行は全件「**起点と終点が同じ点を引いた**」= `at_or_before(start)` と `at_or_before(end)` が同一の点に潰れた行である。

**落ちる場所は「向き」ではなく「反応 bp」である。** `tie_policy` が決められるのは符号 ∈ {−1,0,+1} だけで、**`bp_*` は既に決まっている**。タイ行では起点価格(`anchor_price` = `at_or_before(end_ms)`)が**カスケードの手前の点**に戻るので、`bp` にカスケード自身の値動きが丸ごと混ざる。実測のタイ行の `|bp|` 平均:

| 場面 | 反転側 | 継続側 |
|---|---|---|
| 5〜45秒 / 60秒 | 21.642 | **59.094** |
| 60〜120秒 / 60秒 | 17.654 | **44.677** |
| 5〜45秒 / 300秒 | 24.101 | **52.077** |

継続側で膨らむのは、内部の動き(15〜60bp)と反応(−20bp)が**同符号で足し算になる**ため。向きを正しく当てても score は ±この値なので、**仕込み ±20 には戻らない。** これが 3 候補が `refine` と同じ壁に当たる理由である(行 1・2 では 3 候補とも `refine` と**まったく同じ値** — 同じ 41 行に同じ符号を付けるから)。

**「どんな符号の付け方でも届かない外側」を測った**(スクリプトが毎行印字する `どんなタイ方針でも届く範囲`。タイ行の符号を 1 行ずつ自由に選んだときの平均の可動域):

| # | 届く範囲 | 許容帯 | 重なるか |
|---|---|---|---|
| 1 | [+14.231, +20.147] | [+17.0, +23.0] | 重なる |
| 2 | [−25.421, −9.268] | [−23.0, −17.0] | 重なる |
| 3 | **[+1.071, +16.190]** | **[+17.0, +23.0]** | **重ならない** |
| 4 | [−33.072, +5.189] | [−23.0, −17.0] | 重なる |
| 5 | [−24.101, +24.101] | [+17.0, +23.0] | 重なる |
| 6 | [−52.077, +52.077] | [−23.0, −17.0] | 重なる |

**行 3 は重ならない。**符号だけを変える方針は、どう作ってもこの場面では許容に入らない。**これは 3 候補の陰性ではなく、方針の作り方全体に対する陰性である。**理由は `drop` の値が示す: **非タイ行だけでも +15.094 で 4.91bp 過小**で、タイ行をどう扱っても 300 行ぶんの平均を +17.0 まで持ち上げられない。

**場面ごとの落ち方**:
- **行 1・2(13.7%)**: 3 候補とも 41 行全部を**正しい向き**で解決するが、その 41 行の `bp` が汚れているので `refine` と同じ値に落ちる。**向きを 0 に潰す `keep` だけが許容内**という逆転。
- **行 3・4(42.8%)**: 実群だけ n_rows=390(分割)、**タイ 167 のうち 117 は `start==end` の 1 件束**で窓の幅が 0 — **どんな系列を当てても内部方向が定義できない**。だから `symfine` は 22/167 しか解けない。`widen` と `ohlc` は窓の外を見るので 167 全部を解くが、そのぶん反応窓の冒頭を取り込む。**この場面では対照群のタイが 0 件**で、タイの発生自体が群間で非対称である。
- **行 5・6(100%)**: 非タイ行が 1 行も無い。`ohlc`・`symfine` は正しい向きを付けるが −23.868 / −52.077(反転側は**符号が逆**)。`widen` だけ違う値になるのは終点側 1 点先まで見る幅が 300 秒あって見る範囲が違うためだが、**その分離は測っていない**。

## 5. 射程(測っていないこと)

1. **全部合成データ。実データでは一度も測っていない。**
2. **`widen` と `ohlc` は `end_ms` より後の価格を見るので、反応窓 [end, end+15分] と時間的に重なる。** `bp_*` の値そのものは読まないが、**反応窓の冒頭の値動きを向きの決定に使っている**。合成では重なりは 1 バー(60 秒 = 反応の 6.7% / 300 秒 = 33%)。**実データでこの重なりが無害かは測っていない。** `symfine` にはこの重なりが無い。
3. **`symfine` に要る入力(間引く前の系列が「対照群にも」あること)が実データで手元にあるかは測っていない。** この環境の在庫も、オーナー PC の在庫も見ていない。**「無い」とは書かない。**
4. **候補は 3 つだけで、符号の付け方を尽くしてはいない。**ただし §4 の「届く範囲」は符号の付け方すべてを覆うので、**行 3 についてだけは方針全体に対する陰性**である。残り 7 行は「この 3 候補が落ちた」までしか言えない。
5. **起点価格(anchor)自体を取り直す案は測っていない。**事前登録が「`TiePolicy` に足す」に範囲を切っているので範囲外。**§4 の実測からは、残る方向はここだけである**(汚れているのは向きではなく `bp` なので)。これは測っていない推論であり、事実ではない。
6. `widen` の **K=1 以外は測っていない**。地平線は **15 分のみ**(1 / 5 / 60 分は測っていない)。
7. **終了コードには合流させていない。**新しい表は測定として印字するだけで、`overall_pass` は 09-14 と同じ 3 項のまま(反転側の終了コードは 0 で変わらない)。

## 6. 打ったコマンドと出力の在処

- **`docs/PHASE2/INSTRUMENT_VERIFY/evidence_2026-09-16/15_第4の方針.log`**(172 行、4 本ぶん):
  1. `PYTHONPATH=src python3 scripts/verify_liq_instrument.py`(終了コード 0。新しい節が §2 の 8 行と §4 の「届く範囲」を出す)
  2. タイの経路とタイ行の `bp` の内訳(§4 の 2 つの表の出典)
  3. `PYTHONPATH=src python -m pytest -q tests/ -k "liq"` → **89 passed, 1814 deselected**(**集計行は `-q` 無しで打ち直して生ログ末尾に残した**: `89 passed, 1814 deselected in 6.38s`。監査役の [直す] への処置、リード 2026-09-16)
  4. フルスイート `PYTHONPATH=src python -m pytest tests/` → **1902 passed, 1 skipped in 255.16s**(収集 1903 件、うち今回の新規 7 件。委任先が `-q` 無しで測り直した値)

## 8. 監査役(`owner-auditor`)への答え(2026-09-16。逐語と全文は `docs/AUDITOR/VERDICTS/2026-09-16_policy4_report.md`)

**[止める] 1 — 許容 ±3.0bp の出所と、第 4 の方針の合否に転用した根拠**: 直した。
- 出所: `scripts/verify_liq_instrument.py` の `TOLERANCE_BP = 3.0`。**リードが 2026-09-14 に置いた値(分類: 仮定)**。理由はコードの注記のとおり「N=300 なら SEM ≈ 0.23bp、3bp はその約 13 倍で、健全な合成データは確実に通し、条件 1 の変異試験(効果を半分 = 誤差 10bp)は確実に落とす」ための分離幅。
- 転用の根拠: **既存 3 方針(`keep` / `refine` / `drop`)の合否が同じ値で測られている**(§0 の表の 4 方針の許容 ±3.0bp)。第 4 の方針だけ別のバーにすると比較できない。だから同じ値を、候補を作る前に固定した。
- **この値に判定が依存するか(感度)**: 依存しない。理由は行 3(60〜120 秒 / 60 秒バー、反転)で、3 候補の仕込みからのずれが **14.65 / 9.85 / 11.35bp**(§2)、かつ符号の付け方すべての到達範囲の上端が **+16.190**(§4)なので、**許容を ±3.0 から ±3.8 未満のどこに置いても行 3 は外れ、候補ごとの最悪値で見れば ±9.8 まで広げても不合格のまま**。`symfine` 行 4 の 3.954bp は境界に近いが、合否は「3 場面すべて」なので行 3 で決まる。

**[直す] 2 — pytest の集計行**: 直した(上の §6 の 3、生ログ末尾に `-q` 無しの集計行を追加)。

**[聞く] 3 — 枠組み(必要条件 (a)〜(d)・許容・場面・候補数・判定 1 回)をオーナーに見せたか(A-13)**: 一部は見せた、一部は見せていない。
- 見せた: 場面と許容と判定の対象。L-180 への返答(2026-09-16、監査済み)に「**合成データで、仕込んだ ±20bp をタイ 55.7% の場面でも許容 ±3bp 内で取り出せるかを測ります**」と書き、オーナーはその上で L-181「**1. 含める**」と決めた。
- 見せていない: 必要条件 (a)〜(d) の書き方、候補の上限 3、判定 1 回。これはリードが委任の依頼文で固定した。**A-13 はリードが導いた規則(§0.2 B)であり、L-179 の「承認済み常設スキルの固定手順」でもない。**見せずに進めた事実をここに書く。**結果への影響**: 行 3 は到達範囲の測定が符号の付け方すべてを覆うので、候補数の上限は結論を狭めていない。残り 7 行は「3 候補が落ちた」まで。

**[聞く] 4 — `widen` / `ohlc` は (d) を実質的に破っていないか**: 形式上は満たし(反応 `bp_*` の値を読まない)、実質は境界にある(`end_ms` より後の価格 = 反応窓の冒頭を向きの決定に使う)。**事前登録の (d) の文言は「反応(`bp_*`)を向きの決定に使わない」で、窓の重なりまでは禁じていなかった。**判定への影響: 2 候補とも**不合格**なので結論は変わらない。**もし合格していたら、この重なりを理由に採用できなかった。**その基準(反応窓と重なる情報を向きに使わない)を、次に同種の候補を作るときは (d) に明記する。合成データで重なりが値をどちらへ動かしたかは測っていない(射程 2 のまま)。

**[聞く] 5 — 候補の上限 3 の根拠**: リードの判断で、根拠は 2 つ。(i) 予算: 候補 1 つにつき実装・テスト・8 場面の測定が要り、上位モデルの週間使用率は 09-12 報告値 90%(自主上限 70%、L-002)で、下位モデルへの委任でも 1 回の委任に収める大きさにした。(ii) 網羅性の代替: 候補を増やす代わりに「符号の付け方すべての到達範囲」を測り、行 3 では上限が結論を狭めないことを示した。**「これ以上は仮説として意味が薄い」という判断ではない。**残り 7 行については上限が効いており、射程 4 に書いたとおり。

## 7. 実装の変更点(最小 diff、コミットも押し出しもしていない)

- `src/bot/research/liq_response.py` — `TiePolicy` に `widen` / `ohlc` / `symfine` を追加、列名 3 つと `TIE_DIRECTION_KEYS`、`attach_widened_direction` / `attach_bar_direction` / `attach_symmetric_fine_direction` を新設、`reversal_scores` に 4 行の分岐。**既存 3 方針の挙動は 1 行も変えていない**(既存テスト全通過)。
- `scripts/verify_liq_instrument.py` — `build_dense` に `dur_range` 引数(既定は従来値)、`to_ohlc_bars` / `reachable_mean_range` / `run_policy4_table` を新設、`main` から呼び出し。**既存の列と行はそのまま**(`evidence_14` と一致を実測で確認)。
- `tests/test_liq_response.py` — 新規 7 件((a)〜(d) をそれぞれ固定)。

## 結論(1 行)

**「群で対称かつ向きを保つ第 4 の方針」は、事前登録のバーでは 3 候補とも不合格。**しかも行 3 については「**符号だけを変えるどんな方針でも届かない**」ことを到達範囲の実測で示した。タイ行で壊れているのは向きではなく**起点価格(anchor)経由の反応 bp** であり、`tie_policy` の枠内では直せない。残る方向は anchor の取り直しだが、それは今回の事前登録の範囲外で**測っていない**。
