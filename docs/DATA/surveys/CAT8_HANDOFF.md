# 道具サーベイ 区分 8 の引き継ぎ書(2026-09-23、L-416 / L-417)

この会話(区分 8 の会話)のリードは、この文書を最初に全部読んでから始める。**この文書は引き継ぎの材料であって、オーナーの指示そのものではない。**オーナーの指示は下の逐語だけ。

## 1. オーナーの指示(逐語)

- L-416: 「**明らかに監査からの指摘の修正に手こずっているので、区分8を今のワークフローと並行して始めておきたい。区分1の教訓を活かして、調査して資源化まで進めてほしい。一気に終わらせようとせずに手順はきちんと守ること、今のワークフローとの競合に注意すること、これらを守って進められますか？**」
- L-417: 「**案1の別の会話で調査を進めたい。引き継げますか？**」
- 案 1 の文面(リードが出し、オーナーが選んだもの): 「区分 8 は別の会話(新しいクラウドの会話)で行い、別のブランチに押し出す。区分 8 が終わったら、リードがこのブランチに取り込む。」

完了の形(オーナーの語): 「**調査して資源化まで進めてほしい**」。

## 2. 区分 8 のテーマ(委任文の逐語)

委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`。8 区分共通。監査済み)§2 の 8 番目:

> 8. 検証・再現・品質(別実装との突き合わせ・データ品質・ルックアヘッド検出・リプレイ)

区分の完了の条件も委任文 §2 のとおり(「新しい計画が新しい候補を 1 件も出さなくなったとき、その区分は完了(それまで完了と書かない)」)。

## 3. 手順(守るもの)

**`.claude/skills/research-squad/SURVEY.md`(区分 1 の振り返りから作った手順書。オーナー指示 L-403 でスキル化)を全部読み、その順に進める。**要点:
1. **1 回目の前に設計票を書き、監査役に通す**(§1: 完了の形を述語で・母集合に未分類も数える / 誰の語か / 要素の印の述語 / 尺度 / 台帳の列 / 生ログの形 / 数え直しの道具)。設計票は、起動の前にオーナーに見せる。
2. **台帳を先に作り、番号は台帳でだけ振る**(§8 の 2)。区分 1 の台帳 `docs/DATA/tools_catalog.tsv` を伸ばすか、区分 8 用の台帳を作るかは設計票で決める。
3. **数え直しの道具を最初に置く**(`scripts/recount_scan_cat1.py` を区分に合わせる)。受け入れ検査は被覆を測ってから使う。
4. **1 回ずつ起動し、毎回リードが検収する**(§3 の順)。1 回の予算は調査班 25 万トークン・25 分。**Jev の検査を毎回当てる**(当てないなら理由を書く)。
5. **監査役は中身の回と区分の締めだけ**。区分を閉じる前に全体受け取り監査を 1 回。
6. **資源化**: 区分 1 の資源(`docs/DATA/TOOLS_CATALOG.md`・`tools_catalog.tsv`・`scripts/build_tools_catalog.py`)にならい、索引と機械で引ける台帳を作る。区分 8 で得た手順の教訓があれば `SURVEY.md` に足す(足す前に監査役)。

オーナーの語「**一気に終わらせようとせずに**」: 1 回の起動で区分を終わらせようとしない。回ごとに検収し、区分の完了は委任文 §2 の条件で決める。

## 4. 最初の候補(区分 1 の台帳から区分 8 に回されたもの、5 件)

`docs/DATA/tools_catalog.tsv` の `他の区分へ` の列に 8 がある行(2026-09-23 に数えた):
62 `qf-lib`(区分 3・5・8)/ 70 `PineForge`(区分 8)/ 92 `prediction-market-backtester`(区分 8)/ 101 `akurkar07/OrderBook`(区分 8)/ 117 `Exegy`(区分 3・8)。
これは出発点で、委任文 §3 の検索計画(6 本・検索と X の経路)で候補を足す。

## 5. 並行して走っているもの(競合に注意)

もとの会話(ブランチ `claude/jev-eval-001-production-fq5o7w`)で、**バックテスト環境の刷新の Workflow** が走っている(委任文 `docs/DATA/delegations/20260923_backtest_env_prompt.md`)。競合を避けるため:
- **区分 8 の会話はブランチ `claude/tools-survey-cat8` にだけ押し出す。**もとのブランチには押し出さない。取り込みはもとの会話のリードが行う。
- **書いてよいのは区分 8 の資料だけ**: 報告書・生ログ・台帳・索引・区分 8 の検収の記録・道具の数え直しの script・`SURVEY.md` への追記・`OWNER_LOG` / `OWNER_STATUS`。**`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/`・バックテストの委任文には触らない。**
- **`OWNER_LOG` の番号は L-500 から振る**(もとの会話の番号と重ならないように。もとの会話は L-417 まで使った)。`OWNER_STATUS` には区分 8 の欄を 1 つだけ足し、他の欄は書き換えない(取り込みのときの衝突を小さくする)。
- 外部の道具を入れる場所は `<scratchpad>/cat8/venvs/`。
- 区分 8 の結果(ルックアヘッド検出・別実装との突き合わせの道具など)が、バックテスト環境の項目 8・11 に効きそうでも、**もとの会話の作業に手を入れない**。気づいたことは区分 8 の報告と `OWNER_STATUS` の区分 8 の欄に書く。

## 6. 規則の要点(`CLAUDE.md` が全部。とくに区分 1 で破ったもの)

- 着手前に §0.1 の対応表を出す。右が空の行があれば `/owner-options` で案を出して止まる(全面停止)。
- オーナーの発言は、その回のうちに `OWNER_LOG` と `OWNER_STATUS` に書く(区分 1 で 2 件漏らした)。
- 監査役の出力は、その場でオーナーに逐語で出す(O-4)。監査役の `[聞く]` を調べずにオーナーへ上げない(L-402)。
- 「無い・取れない」には範囲と方法を書く。判定(使える / 使えない)は書かない。
- 区分 1 で繰り返したリードの誤り: 名指しされた箇所だけを手で直して他と食い違わせる / 段落を行末まで置き換えて同じ行の文を消す。**直したら毎回、語を grep で全部見て、前の版との差分で消えた文が無いかを確かめる。**
