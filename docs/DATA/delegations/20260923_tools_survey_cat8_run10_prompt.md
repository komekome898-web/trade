# 道具サーベイ 区分 8 — 10 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 10 回目**を担当します。

**この回の終わり方**: §2 の 1〜6 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 10 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run10.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**ただし (a) の「文書の全部」と、この起動文 §2 の N の定義(リポジトリならファイル一覧の全件)が違うときは、**広いほう(この起動文 §2)を当てる**。
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` を全文(とくに §3 と、末尾の「監査 47 回目」)。**9 回目は、(1) README の無いリポジトリを除外し、README だけを読んで `なし` と書いた、(2) 「再確認した」と書いた手が生ログに無かった、(3) 同梱の別のライブラリの機能を候補の機能として、問いに出さずに値・段を決めた、(4) 乱数の種を段 5 の (ア) に数えた。同じ誤りをしない。**
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。**この回の出発点は台帳の値**(9 回目の報告の表の値ではない。検収と監査 47 回目で台帳を直した)。

## 1. この回の位置づけ

この回は 6 つ: 8-016 の残り(§2 の 1)/ 8-005 `Exegy`(§2 の 2)/ 辿る一覧(§2 の 3、1〜9 回目は手付かず)/ 成熟度の枠組みの原典(§2 の 4)/ 8-011 E6 の段(§2 の 5)/ 8-014 の `performance_metrics` の確かめ(§2 の 6、値・段は決めない)。

**この回に回さないもの(この回で手を付けない)**: 上に無いほかの行(8-010 E3b と 8-014 E3b・E6 は、オーナーの答え L-508・L-509 でリードが台帳を決めた)。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

**`なし` を書くときの手順(この回の全部の `なし` に当てる。4〜8 回目の差し戻しの対策)**:
- **問いに出した行は、値も段も `未判別` にする**(決めてから問いに出さない。8 回目まで 4 回、この規則を破った)。問いの行には、`[値・段の問い]` か `[それ以外の問い]` のどちらかを必ず書く(今後の追い方など、値・段を決めずにおく理由でない問いは `[それ以外の問い]`)。**`[それ以外の問い]` の行は、§7 の 2 の検査の「問いに出した行は `未判別`」の対象外**になる。どちらも書いていない問いの行は検査が止める。**問いにしかけて取り下げた判断は、取り下げた理由と行を「判断に迷った点と問い」の節に書く**(黙って問いから外さない)。
- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは、道具の機能として数えない。
- N を 2 つ以上の一覧の和で作るときは、一覧ごとの件数と生ログの行、和を書く。
- 根拠の欄で「確かめた」「再確認した」と書くときは、その手の行を 7 列目に書き、確かめた文を「…」で引く(9 回目: 引いた行がほかの手だった)。
- **オーナーが決めた述語の読み(L-508「1〜4全て承認します」・L-509「b」)**: 候補が同梱・呼び出している**別のライブラリ**の機能は、候補の機能に**数える**。LLM への指示文(プロンプト)に書かれた検出・検証の指示は、候補の機能に**数える**。重複をはじく処理は E2 の「重複の検出」に**数える**。1 回だけの固定の学習・評価の分け方は E3b に**数える**。この 4 つは問いに出さずに当てる。
- 乱数の種(`seed`)は段 5 の (ア)「判定の基準(許容誤差・閾値・検出の条件)」に当たらない。
- 7 列目に書く行は、**その要素の語で検索した手**の行にする(ほかの要素の検索の手や、一覧を取っただけの手を引かない)。検索の語には、設計票 §3 のその要素の名前を日本語と英語で入れる。
- 検索の対象の一覧(除外したあとのファイルの一覧)は scratchpad の下に置き、その件数と先頭・末尾の数行を生ログに印字する手を打つ(`wc -l`・`head`・`tail`)。
- 段 4・5 を付けるときは、その条件(段 4 = 枠の外のものに掛かる、段 5 = (ア)(イ))に当たる逐語を、7 列目の行の手の出力に出す。逐語が無ければ段は `未判別`。
- 候補の一次資料の**ファイル一覧・頁の一覧を全件で取る**。`cat8_step.py` の既定は出力を 3,000 字で切るので、一覧を取る手には `--keep 200000` を付ける。一覧は生ログに全件が残るようにする。
- **N は、その候補の一次資料の一覧を取った手の出力の件数そのもの**(リポジトリならファイル一覧の全件、サイトなら文書・ヘルプ・ブログの全部の頁の一覧)。検索した範囲の件数ではない(5 回目の差し戻し: `*.py` だけ・`.rs` だけ・ヘルプの 1 分類だけを N にしていた)。M は、その N 件のうち実際に読んだ・検索の対象に入れた件数(検索の手の対象の指定 `--include` などがあれば、それで当たる件数を数える)。除外してよいのは、文字を検索しても意味の無いもの(画像・ロックファイル・バイナリ)だけで、除外は**ファイル名と理由を全部**書く(テスト用のデータ・設定ファイル・スクリプトは除外しない)。
- `なし` の行の根拠の欄に、**`一覧 N 件 / 読んだ M 件`** の形(この字面のまま)と、一覧を取った生ログの行(`20260923_tools_8_run10.log:<行>`)を書く。**N と M が違うなら `なし` と書かない**(`未判別` のまま、読み残しの名前を書く)。§7 の 2 の検査がこの形・N = M(数として)・生ログがこの回のもので行が実在することを機械で見る(この回に触らない行の根拠は `台帳の値のまま(<何>回目の節)` と書けば検査の対象外)。一覧から外すもの(画像・生成物など、文書でもソースでもないもの)は、外した名前と理由を書く。
- 「主要な」「全体で」のような語で範囲をぼかさない。
- **引く文は生ログに印字する**(6 回目の差し戻し): 根拠に「…」で引く文は、`印`・`なし`、`一次資料`・`実測` の別を問わず、その文を含む行が、根拠に引いた生ログの手の出力に出ているようにする(`curl -o` でファイルに逃がしたら、`grep -n` や `sed -n` でその行を生ログに印字する手を続けて打ち、その手の行を引く)。取った本文は scratchpad の下(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下)に置き、`/tmp` の直下に置かない。§7 の 2 の検査が、「…」の逐語(8 字以上)の先頭 30 字が引いた生ログの手の出力にあるかを見る。**生ログの行は、文の中に書かず、決まった欄にだけ書く**(§7 の 2 の検査がこの欄の形を機械で見る。監査 34〜39 回目で、文の中から行の番号を読み当てる検査は何度直しても読み落としが見つかったので、読み当てるのをやめた): (1) **「要素と段」の表は 7 列**にする: `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目「生ログの行」には、`docs/DATA/probes/20260923_tools_8_run10.log:10` か `docs/DATA/probes/20260923_tools_8_run10.log:10-20` を**空白で区切って並べるだけ**(ほかの字・注記・読点・カンマを入れない)。値が `印` か `なし` の行は空にしない。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし(ほかの文を足さない)、値と段は台帳と同じにする。7 列目は空でよい(値・段が台帳と違う行や、6 列目にほかの文がある行は「触った行」として 7 列目の検査を受ける)。6 列目「根拠」は自由な文でよい(番号を書いても検査は読まない。行は 7 列目に書く)。引いた文「…」は 7 列目の行の手の出力に出ていること。(2) **知見の表の「根拠」の欄**には、生ログの参照(上と同じ形)・URL・日付(`2026-09-24`)・「取得日」だけを書く(説明は「知見」の欄に書く)。
- **ノートの語は実際のコマンドの語と同じにする**(6 回目: ノートに「防ぐ」「同じ結果」と書いたのに、コマンドには無かった)。検索の語は、コマンドの正規表現に日本語と英語で入れる(要素の名前を含む)。
- **WebFetch は一次資料に使わない**(要約を返す道具なので、委任文 §4.1 の「検索結果の要約」に当たる)。まず `curl` で生の HTML を取る。JavaScript で描く頁で本文が HTML に無いときは、`scripts/cat8_render.js`(この環境の Chromium で頁を開いて本文をそのまま出す。`playwright install` は打たない)を `cat8_step.py` 経由で打つ: `NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run10.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`(`rc` と秒が数字で入る。ブラウザーは自分では通信せず、道具の Node の側が証明書を通常どおり検証して取ったものを頁に渡す。リードが 2026-09-24 に `https://fxreplay.com` で HTTP 200 と本文、証明書の誤りのある頁(名前の不一致・自己署名・期限切れ)で取得が止まることを確かめた)。本文が取れずに止まったら、その頁は `未判別` のまま、止まった理由を書く。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない**(道具は本文の欠けを全部は数えられない。監査 25〜27 回目で、何も数えられないまま本文が欠ける型が 3 つ見つかった)。`なし` は、生の HTML・文書のファイル・ソースなど、全部を取ったと件数で示せる一次資料で書く。描画でしか読めない頁しか無い要素は `未判別` のまま、読めなかった頁の名前を書く。§7 の 2 の検査は、`なし` の根拠に引いた生ログの手が `cat8_render.js` なら止める。出力の `INCOMPLETE` の行と最後の `INCOMPLETE 合計 <N> 件` は、`印` の根拠に使うときに一緒に写す。**描画した頁を `印` の根拠にするときの条件(全部を満たす)**: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く(語だけを拾わない)/ (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

段は設計票 §4.1 の逐語のとおりに決める: 「**段は下から積み上げで決める**: その要素について、段 1 から順に条件を満たすかを見て、**条件を満たす一番上の段**を付ける。上の段の条件の一部だけを満たしても、その段は付けない」。各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引き、7 列目の行の手の出力に出して示す。段 1 の条件に当たる原文も無ければ `未判別`。

1. 8-016 `Exactpro の reconciliation testing`: E3a・E3b・E5 と、委任文 §4.0 の表。9 回目に `ecosyste.ms` で取った th2-net の 178 件の名前と `size` の一覧を生ログに印字する。**取得の量に上限を置く**(委任文 §6 の 6「本体の一括ダウンロード(数百 MB 以上)はしない」、§10 の禁止の並びにも「本体の一括ダウンロード」がある): 1 件ずつ、`scripts/cat8_repo_fetch.sh th2-net/<名前> /tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/th2/<名前>` を `cat8_step.py --keep 200000` 経由で打つ(`--keep` を付けないと出力が 3,000 文字で切られ、`skipped:` の名前が落ちる。1 MB を超える中身は取らずに、取った分だけを展開する。普通の `git clone` や `git checkout` をすると、除いた中身を git が後から取りに行くので使わない。リードが 2026-09-24 に 3 件で打って確かめた: `th2-net/viewer`(取った量 27.8 MB・ファイル 25,999 件・取らなかったもの 14 件・展開 25,985 件)、`th2-net/.github`(113 件・取らなかったもの 0 件・展開 113 件)、`th2-net/th2-common-py`(127 件・0 件・127 件)。件数が合わない・取得に失敗したなどで道具が 4・5・6 以外の 0 でない終了コードで止まったリポジトリは「取れなかった」に数える)。出力の `files_in_tree(N)` がそのリポジトリの N、`skipped:` の行が取らなかったファイル。検索は展開したファイルに `grep -rIl`(`.git` を除く)で行い、**そのリポジトリの置き場を消してから次へ進む**。出力の `downloaded_bytes`(通信で取った量)の和を 1 件ごとに生ログに残し、和が 300 MB に達したらそこで取得をやめる(委任文 §6 の 6 は「ダウンロード」の量の規則なので、通信で取った量で数える。委任文は「数百 MB」とだけ書き、**300 という数はリードが決めた**。1 件ずつ消してから次へ進むので、置き場に同時にあるのは 1 件だけ)。**和は道具が数える**: 道具は置き場の親(`cat8/venvs/th2/`)の `.cat8_downloaded_total` に取った量の和を足していき、和が 300 MB 以上なら取らずに終了コード 5 で止まる(`downloaded_total_before`・`downloaded_total_after` の行が出る)。消すのは各リポジトリの置き場だけにし、このファイルは回の終わりまで消さない。終了コード 5 が出たら、残りのリポジトリは打たずに、名前を全部「上限で取らなかった」として書き、値は `未判別`、オーナーの PC から取る手順を書く。**止めるのは 8-016 の取得だけで、この回のほかの項目へは進む**(§0 の「途中で止めない」は回全体のこと)。**上限はもう 1 つある(掛かる量が違う)**: 道具は 1 件ごとに、展開する前に展開後の大きさ `expected_checkout_bytes` を手元の中身から計算し、**1 件の展開後が 500 MB を超えるなら展開せずに終了コード 4 で止まる**(500 もリードが決めた数)。展開したあとは、実物の大きさ `checked_out_bytes` を出し、計算と合わなければ終了コード 1 で止まる。終了コード 4 で止まったリポジトリは「大きさで止めた」として名前と `expected_checkout_bytes` を書き、値は `未判別` のまま残す。経路の問題ではないので、下の「オーナーの PC から取る手順」は書かない(リードが次の回の扱いを決める)。`lfs_pointer:` の行のファイル(Git LFS の置き換えの小さな文字列で、中身ではない)は `skipped:` と同じに扱う。**8-016 の N・除外・読んでいない件数は、文で数えずに** `python3 scripts/cat8_fetch_tally.py docs/DATA/probes/20260923_tools_8_run10.log --prefix th2-net/` の出力から取り、この手は `cat8_step.py --keep 2000000` 経由で打って生ログに残す(生ログのその手の最後の行が `== 集計の終わり(誤り N 件)` でなければ出力が切られているので、`--keep` を大きくして打ち直す。和の行は出力の最初と最後の 2 か所に出る。N = 終了コード 0 の N の和。この道具が 0 件でない数の誤りを出したら、`なし` は書かない)。リードは検収で同じ道具を打ち直して数え合わせる。`skipped:` のファイルは、画像・バイナリ・ロックファイル・ソースマップ(`.map`)なら除外として名前と理由を書き、それ以外は「読んでいない」に数える(M < N なら `なし` は書けない)。取れなかったリポジトリ(終了コード 4・5・6 以外で止まったもの)と読んでいないファイルがあれば、名前を全部書き、値は `未判別` のまま、オーナーの PC から取る手順(`gh repo list th2-net --limit 500` と `git clone --depth 1` の繰り返し)を書く。
   - **取得の道具の終了コードの読み分け(まとめ)**: 0 = 取れた / 4 = 1 件の展開が 500 MB を超えるので展開しなかった(「大きさで止めた」、次のリポジトリへ進む)/ 5 = 取った量の和が 300 MB に達したので取らなかった(残りは打たずに「上限で取らなかった」)/ 6 = 置き場が絶対パスでない・和のファイル `.cat8_downloaded_total` が数でない(**直さずに 8-016 の取得をそこでやめ**、そのことと残りの名前を書く。リードが直す)/ それ以外(1・128 など)= 取れなかった。取得に失敗したときも、取った分は和に足される。**どの終了コードでも、止めるのは 8-016 の取得だけで、この回のほかの項目へは進む**(§0 の「途中で止めない」は回全体のこと)。
2. 8-005 `Exegy`(1 回目から `登録が要る`): 登録なしで取れる一次資料(製品の頁・データシート・公開の文書・PDF)を全部取り、E1a〜E6 を決められる範囲で決める。登録が要る部分は、委任文 §5-6 のとおり、オーナーが辿れる手順を書く。状態は設計票 §2 の語で書く。
3. **辿る一覧**(2 回目の検収 §4 の後半): dev.to の reconciliation tools の記事 / X の `aiwithjainam` の投稿のリポジトリ列挙 / zenn の仮説検証フレームワークの記事 / quantreo・algorier・fortraders の look-ahead bias の解説記事 / 1 回目の検索計画 7 の結果に出たライブラリ。それぞれを開き、**出てくる道具の名前を全部**、この回の節の `### 辿る一覧から出た名前` の小節に 1 行ずつ、ハイフンで始め、名前をバッククォートで囲み、続けて「 — 」で区切って URL(一覧に書かれていたもの。無ければ「URL なし」)と出た一覧の名前を書く(例: 行頭がハイフン・空白・バッククォートで囲んだ名前)。各一覧の最初に「一覧: <一覧の名前> — 開いた URL — `docs/DATA/probes/20260923_tools_8_run10.log:N`」の 1 行を書く(生ログの行は、この形で行の最後に 1 つだけ書く。§7 の 2 の検査が、この形があるか・その行が生ログにあるかを見る)。**候補の一覧には載せない**(候補の一覧に載せると「要素と段」の 8 行が要るため。リードが台帳に `未着手` で足し、次の回に回す = 監査 11 回目の指摘 4)。**書かない名前があれば、書かない理由を 1 つずつ書く**(道具でない・既に台帳にある、など。専用かどうか・有名かどうかは理由にしない = 委任文 §10 の当否判定)。
4. 成熟度の枠組み(Testing Maturity Model)の原典: 原典(論文か書籍か公式の文書)を探して、段ごとの定義を読み、知見の表に段ごとの定義の逐語と出典を書く(2 回目は解説の頁の静的な部分しか取れなかった)。取れなければ、試した手段と見つかった原典の書誌を書く。
5. 8-011 `arXiv:2512.12924`: E6 の段(9 回目は「再確認した」手が生ログに無かった)。`hdt/stats.py` の各関数を生ログに印字し、段の条件(合否を自動で出すか)に当たる原文で決める。
6. 8-014 `Fincept Terminal`: 台帳の `未判別` は E3a の値・段と E1b の段。(a) `skfolio_validation.py` の `performance_metrics` が実際にどこかで計算・使用されているかを、同じファイルの `validation_history`(9 回目の知見 #5)と同じ手順で確かめて知見の表に書き、その結果で E1b の段を決める(使われていなければ、その属性を段の根拠にしない)。(b) E3a を、上の述語の読み(LLM への指示文も機能に数える)で当て、値と段を決める(9 回目の報告で見つかった `subagents.py` 150 行「- Identify lookahead bias and survivorship bias」など。段は §2 の段の条件どおり下から積み上げる)。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 10 回目の実行(2026-09-24)` の追記だけ**。1〜9 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run10.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` を除いたもの全部(`### 辿る一覧から出た名前` は置く = §2 の 3)。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表(7 列。§2 の冒頭)は、8-001〜8-016 の 16 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(10 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log docs/DATA/probes/20260923_tools_8_run6.log docs/DATA/probes/20260923_tools_8_run7.log docs/DATA/probes/20260923_tools_8_run8.log docs/DATA/probes/20260923_tools_8_run9.log docs/DATA/probes/20260923_tools_8_run10.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 10` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件` と、引いた文が生ログにあるかもここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run10.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: §2 の 6 項目の状態(辿る一覧は一覧ごとに出た名前の数)(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」の一覧 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ **描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧**(候補・要素・読めなかった頁)/ 生ログの行数。
6. 「区分は完了」とは書かない。
