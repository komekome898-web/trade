# 道具サーベイ 区分 8 — 3 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 3 回目**を担当します。

**この回の終わり方(有界)**: 次のどちらか早い方で終わる。(a) 期限(§6。開始から 20 分)が来て `cat8_step.py` が終了コード 3 で止まった / (b) §2 の 1〜3 をすべて済ませた。どちらでも §7 を済ませてから返す。**それ以外の理由で止めない**(止めたら、止めた理由を生ログの時刻と一緒に `### 予算` に書く)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの(先に書いておく)**: この回の節(`## 区分8 — 3 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260924_tools_8_run3.log`。返答には §2 の 1〜3 の進み具合、深掘りした候補の番号と状態、辿る一覧から書いた名前の数、3 つの検査の合計行、問いの一覧、生ログの行数を書く(§7)。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `46258eebcd93`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**この回もそのまま当てる**(生ログは `cat8_step.py` で・知見の表の形・`[深掘り]` の付け方・必ず置く小節・§4.0 の表を空にしない)。生ログの名前だけ、この回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6(線引き・書き換えの範囲・禁止・申し送り)。
6. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run2.md` を全文(とくに §2 の表、§5、§10 問いへの答え、§11 差し戻し)。
7. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。

## 1. この回の位置づけ

2 回目は 1 回目の訂正を済ませたが、残りの候補 8-009〜8-016 の深掘りと辿る一覧は、期限の 14 分前に止めて手付かずだった(検収 §2)。また「なし」の主張の誤りが 2 回続いた(1 回目 `qf-lib` の再現、2 回目 PineForge のルックアヘッド。どちらも読んだ範囲が狭かった = 検収 §5)。**この回は、残りの候補の深掘りと辿る一覧を先にする**(委任文 §2「前回の残りの候補名があれば、まずそれを深掘りする」)。1 回で区分を終わらせようとしない(オーナー逐語「**一気に終わらせようとせずに**」L-416)。

## 2. やること(この順)

1. **残りの候補の深掘り**: 台帳の 8-009〜8-016 を**番号順**に。深掘りした道具は委任文 §4.0 の表(語彙のすべての項目)と §4 の全列、「要素と段」の表 8 行を書く。論文(8-010・8-011)は道具ではないが候補として扱い、§4.0 の項目で当たらないもの(版・週 DL 数など)は `該当なし(論文)` と書き、論文が示す実装・コード・データの所在があればそれを書く。深掘りできなかった候補は「浅い(何が未確認か)」か `未着手` で残す。
2. **PineForge(8-002)の E3a・E3b**: 検収 §5 のとおり、リポジトリの `docs/pages/mtf.md` 143-153 行(「`lookahead` and `gaps` must be off … Violations raise at run-time with a precise diagnostic」)を開き、設計票 §3 の E3a・E3b の述語に当てる(どちらにも当たりうる。当たる方に `印`、段を §4.1 で)。あわせて、PineForge の文書のほかの箇所(ファイル一覧を打って `docs/` の下を全部)に E3a・E3b に当たる記述があるかを、下の「`なし` と書くための条件」で確かめる。
3. **辿る一覧**(2 回目の検収 §11 の (4)): dev.to の reconciliation tools の記事 / X の `aiwithjainam` の投稿のリポジトリ列挙 / zenn の仮説検証フレームワークの記事 / quantreo・algorier・fortraders の look-ahead bias の解説記事 / 1 回目の検索計画 7 の結果に出たライブラリ(1 回目の生ログ 97・103・106・112・198 行付近に出た場所がある)。2 回目の起動文 §2 の 4 の形で `### 辿る一覧から出た名前` に書く(候補の一覧には載せない。各一覧の最初に「一覧: <一覧の名前> — 開いた URL — 生ログの行」の 1 行)。書かない名前は 1 つずつ理由を書く(専用かどうか・有名かどうかは理由にしない)。

**`なし` と書くための条件**(この回のすべての要素に当てる。検収 §5 で決めた述語): 次の (a)(b) の両方を満たしたときだけ `なし` と書き、満たせなければ `未判別` と書く。
- (a) 読んだ範囲 = その候補の一次資料の**全部の文書**。リポジトリなら `ungh.cc/repos/<所有者>/<登録名>/files/<既定枝>` でファイル一覧を打ち、README に加えて `docs/`・`wiki`・API の文書など文書の全部を読む(読んだ文書の名前を生ログに残す)。論文なら本文(PDF か HTML)の全部。サイトならその製品の文書の頁の全部。
- (b) 検索の語に、**要素の名前そのもの**(日本語と英語。例として決めない。E3 なら「ルックアヘッド」と `lookahead`・`look-ahead` の両方の綴り、E4 なら「リプレイ」と `replay` など)を必ず入れる。打った語を生ログに残す。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 3 回目の実行(2026-09-24)` の追記だけ**。1 回目・2 回目の節は書き換えない)と `docs/DATA/probes/20260924_tools_8_run3.log` の 2 つだけ(追補 §1。生ログの名前は追補の `20260923_tools_8_run<N>.log` の形から日付だけ変わる)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` を除いたもの全部。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない(「この環境から不可」と書いた項目があればその試した手段と第 2 経路(オーナー PC)のコマンド、無ければ「この回は『この環境から不可』と書いた項目なし」)。PineForge の E3a・E3b の当て直しは `### PineForge の当て直し` の小節に「2 回目の報告の行番号 → 直した値と根拠」の形で書く。候補の一覧と「要素と段」の表には 8-001〜8-016 の全行を**今の値で**載せる(8-002 の E3a・E3b は、台帳ではリードが `未判別` に直してある)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**25 万トークン・25 分**を上限の目安にする(追補 §5)。1 回目は 32.5 万、2 回目は 29.1 万トークンで、どちらも超えた。**最初の手で期限を出す**: `python3 scripts/cat8_step.py --log docs/DATA/probes/20260924_tools_8_run3.log --method budget --target 期限 --note 期限 -- date -u -d '+20 min' +%FT%TZ`。**以後の `cat8_step.py` にはすべて `--deadline <その時刻>` を付ける。**期限を過ぎると道具が手を打たずに終了コード 3 で止まるので、止まったら §7 へ進む(§7 の検査は `cat8_step.py` を通さずに打つ)。**期限の前に自分から止めない**(2 回目は期限の 14 分前に止めていた)。

## 7. 返す前に(3 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260924_tools_8_run3.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 3` を 0 件にする(最後の出力の全文を同じ節に貼る。1 行目の「読んだもの」の行数が、この回の表の行数と合っていることも見る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260924_tools_8_run3.log --require-deadline` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(1 回目・2 回目の節から消えた行が無い。出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: §2 の 1〜3 それぞれの進み具合(済み / 途中 / 未着手。途中・未着手なら止めた時刻と理由)/ 深掘りした候補の番号と状態 / PineForge の E3a・E3b の値と段 / 辿る一覧から書いた名前の数(一覧ごと)と、書かなかった名前の数と理由 / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに)**/ 生ログの行数。
6. 「区分は完了」とは書かない。
