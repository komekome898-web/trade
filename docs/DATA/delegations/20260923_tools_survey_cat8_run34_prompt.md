# 道具サーベイ 区分 8 — 34 回目の起動文(2026-09-26)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 34 回目**を担当します。この起動文は、それだけで読めるように書いてあります(ほかの回の起動文は読まなくてよい)。

**この回の終わり方**: §2 の全部を済ませてから返す。期限は無い。途中で止めない(オーナーの逐語 L-506「**期限を指示した覚えがありません。中途半端で止まらないでください。**」)。予算で止めない(オーナーの決定 L-507「**案A**」)。どうしても決められない要素は `なし` を書かずに `未判別` のまま残し、何を試して何が足りなかったかを書く。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節 `## 区分8 — 34 回目の実行(2026-09-26)` と生ログ `docs/DATA/probes/20260923_tools_8_run34.log`。返答の中身は §7。

## 0. 最初に読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語(§3)と段(§4.1)は設計票の逐語で当てる。**
4. 報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 33 回目の実行(2026-09-26)` の節(45344 行目の見出しから末尾まで。末尾の「リードの追記(検収、監査 130 回目)」を含む)と、生ログ `docs/DATA/probes/20260923_tools_8_run33.log`。**この回の出発点**(8-035 Apache Spark を初めて開いた回)。小節・表の形はこの節に合わせる(末尾の「リードの追記」はリードが書くもので、調査班は置かない)。中身は写さない。案 B の記録の形は 32 回目の節の知見 8・9 を見本にする。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする。
5. 21〜33 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run21.md`〜`..._run33.md` の処置の節(§4)。**33 回目は全文**。**21・24・26・28 回目の調査班は §6-1 の導入前の検査の一部を導入・実行のあとに行った。31 回目は生ログに無い `pip` の手で、§6-1 の前にソースから wheel を 18 個作り(第三者の作りの手順を動かした)、導入で入った依存の中身を開かなかった。**述語の対象に届かない根拠で `印` を付けた(22 回目は E4、24 回目は E2、26・28 回目は E2 と E4、31 回目は E2)。24 回目に別の配布物を N に入れた。26・31 回目に scratchpad の外に書いた(31 回目は `/tmp` 直下に 36 個と、システムの Python の下に `.pyc` 111 個)。29 回目に探す手を `head -25` で打ち切ったまま「探し尽くした」と書いた。29 回目に、検索の要約だけで宣伝詐欺の兆候を `実測` にしかけた。30 回目に、ディレクトリを選ぶ手の出力を生ログに出さなかった。**26・28・31 回目に、案 B の記録(読んだ範囲・当たりの行の数)が足りないまま `未判別` にした(文書の題を読んだところで止まり、本文を読む頁を選ばなかった)。31 回目に、ファイルの表の理由を同じ文で約 40 ファイルに当て、当たった行に理由と合わないものがあった。32 回目に、`pip list`・`pip check` の手に `PIP_CACHE_DIR` を付けず `/root/.cache/pip` に書き、`curl -o /tmp/...` で `/tmp` の直下に書き、報告の下書きの断片を `/tmp/nul` に残した。32 回目に、§7 の検査の出力を 1 つしか貼らなかった。33 回目は `pip` の手の全部に `TMPDIR`・`PIP_CACHE_DIR` を付けず、`cat8_step.py` を通さない殻の手で `/tmp/blocklines.txt` を作り、E4 に「時刻順に再生することを明言する逐語はこの回では見つからず」と書きながら `印` を付けた(28 回目の Kafka の E4 は `未判別` なのに「28回目Kafkaと同じ当て方」と書いた)。**この回は同じことをしない。
6. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値。**

## 1. この回の範囲

- **8-035 `Apache Spark`** の **E4 だけ**。台帳の値: E1a 印 段 4・E1b 印 段 2・E2 印 段 3・E3a 未判別・E3b 印 段 3・E4 未判別・E5 印 段 2・E6 未判別、状態 `浅い`。E4 は 33 回目に `印`・段 3 で出たが、述語の「記録した市場データを時刻順に再生して」に届く逐語が無いので受け取っていない(33 回目の検収 §2 の 1)。この回は、E4 の `印` を探し尽くし、見つからなければ `未判別` のまま案 B の記録を書く。**33 回目に読んだ頁(`docs/streaming/structured-streaming-kafka-integration.md` など)も、この回に `cat8_step.py` で読み直して生ログに出したうえで、案 B の「読んだ範囲」に入れる**(33 回目の検収 §4 の処置 6。根拠と 7 列目に引くのはこの回の生ログの行だけ。§2.4 の「生ログはこの回のものだけを根拠にする」のとおり)。そのうえで、文書の全頁の道と題に E4 の語の組を機械的に当てて選んだ頁の本文を全部読み、ソースの全ディレクトリにも同じ語を当てて選んだディレクトリの中を読み、§1 の「当たりの量の上限」の見積もりの手を打つ。**見積もりが 500 行以下なら `cat8_search.py` を打って当たりを全部判定し、`なし` もありうる**(500 行を超えれば `未判別` のまま案 B の記録)。
- **E4 の当て方**: 述語の逐語は「**記録した市場データを時刻順に再生して、戦略・執行・計算を再実行する**」。時刻順の再生を言う逐語が要る(22・23 回目の検収 §4)。汎用の「data」を流し直すこと、到着順・offset の順に読み直すことを言う逐語だけでは当たらない(26 回目 Debezium・28 回目 Kafka・33 回目 Spark と同じ当て方)。原文に無い「時刻順に」を言い換えで足さない。
- **数えるもの・N**: 33 回目と同じ(公式のリポジトリ `apache/spark` の中の本体だけ。`venvs/8-035/src/` と一覧 `venvs/8-035/list.txt`、33 回目の生ログ 1220 行の `listed=24454`)。取り直さない。**新しく何も取ってこない・入れない・動かさない**(読むだけ)。
- **当たりの量の上限(500 行)**: `印` が見つからずに全件の検索に進む要素は、**`cat8_search.py` を打つ前に**、同じ一覧・同じ語で当たりの行の数だけを見積もる手を `cat8_step.py` 経由で打つ(一覧の全部のファイルに、`grep -I -c -i -E` で、§2.4 の要素ごとの語の組と同じ語(`cat8_search.py` に渡すのと同じ正規表現)を当て、ファイルごとの数を足した合計を印字する手。件数だけを出す手で、`なし` の根拠にはならない)。**見積もりが 500 行を超える要素には `cat8_search.py` を打たず**、`なし` を書かずに `未判別` にして、見積もりの手の行と行の数を書く(その要素だけの回を立てる)。`cat8_search.py` を打った手の当たりは、検査が全部の判定を求めるため。500 行以下なら `cat8_search.py` を打ち、当たりを全部判定する。
  - 500 の根拠(仮定。12〜19 回目の観察から): 当たりが数十〜数百行の要素では、1 行・1 ファイルごとに違う理由が書けていた(17 回目の AI Trading Lab 40 行、18 回目の行ごとの判定など)。560 行(17 回目の行の表)と 2,998 行(16 回目)では、同じ文の当てはめになった。境目を実測したわけではない。
- **探す手を打ち切らない**(29 回目の誤り)。`head`・`tail` で当たりを切らない。当たりが多いときは、当たりの行の全部を scratchpad の下のファイルに書き、`wc -l` で数を出し、`sed -n` で区切って全部読む(読んだ区切りの手を全部生ログに出す)。`cat8_step.py` の `--keep` が出力を切ったら、その手を根拠にしない。ソースの一部だけを読むときは、ディレクトリを要素の語の一覧で機械的に選ぶ(`find <src> -type d` の全部と、`grep -i -E '<語>'` で選んだ一覧を、**両方とも出力ごと**生ログに出す。30 回目は選んだ一覧の出力を出さなかった)。
- **`印` を先に探す(§2.3)ことがこの回の中心**。大きいリポジトリでは、要素の多くで見積もりが 500 行を超える見込みが高い(参考。値の根拠にはしない: 同じ区分の OpenClaw のリポジトリで、リードが同じ語の組を `grep -rIci -E` で全ファイルに当てて足した当たりは E1a 42,161 行・E1b 35,611 行・E2 233,208 行・E3a 2,037 行・E3b 469 行・E4 213,009 行・E6 496,179 行。`docs/` だけなら E1a 2,545 行・E3a 133 行・E4 7,038 行。20 回目の検収 §7)。その要素は `印` を探し尽くしてから、見積もりの手を打って `未判別` にする。`印` を探すときは、文書の目次・API の一覧・機能の一覧の頁を先に読む。
- **案 B の記録(オーナーの決定 L-519「案B」)**: `印` を探し尽くしても見つからない要素は `未判別`(不明。無いという意味ではない)のまま残し、知見の表に「読んだ範囲」(文書の目次の行数、題を読んだ頁の数、本文を読んだ頁の道の一覧、読んだソースのディレクトリ)と「当たりの行の数」(見積もりの手の合計。**数えるのは候補の公式のリポジトリと文書だけ**)を書く。選んだ文:「印を探し尽くしても見つからない要素は `未判別`(不明。無いという意味ではない)のまま残す。完了の条件を「残り 0 件」から「未判別の要素には、印を探した範囲と当たりの行の数が書いてある」に変える」。「探し尽くす」の範囲の決め方(リードの決定): 文書の目次の全部と、文書の全頁の題(front matter の `title` と最初の見出し)を 1 行ずつ印字して全部読み、要素に関わりうる頁の本文を読む。ソースは最上位のディレクトリの一覧と、関わりうるディレクトリの中のファイルの名前の一覧を出して、関わりうるファイルを読む。この範囲の限界(題に出ない機能の記述は読み落としうる)を「読んだ範囲」の行に 1 文で書く。 **文書の頁の選び方(32 回目から。31 回目の検収 §4 の処置 4)**: 「関わりうる頁」を調査班が選ばない。文書の全頁(`doc/source` の下の全部の `.rst`・`.md`・`.ipynb`)の道と題を 1 行ずつ出したものに、要素ごとの語の組(§2.4 と同じ正規表現)を `grep -i -E` で当て、当たった頁は**全部本文を読む**。選んだ頁の一覧を**出力ごと**生ログに出し、読んだ頁ごとに手の行を出す。ソースのディレクトリも同じく、`find <src> -type d` の全部と、要素の語を `grep -i -E` で当てて選んだ一覧を出力ごと生ログに出す。
- **この回に手を付けないもの**: ほかの 40 行と、8-035 の E4 以外の 7 要素(台帳の値のまま)。

## 2. やること

### 2.1 述語と段(設計票の逐語。短く写す)

- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは数えない。**候補が自分自身の版・ランタイムを試験する仕組み(候補の開発のための試験・CI・`#[test]`・`assert!` など)は `印` に数えない**(オーナーの決定 L-516「**数えません**」)。
- **E1a の述語の括弧の例**: 「差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較」。どれも、同じものを出すはずの 2 つを照らし合わせるもの。戦略どうしの成績を並べて比べる機能は E1a に当たらない(18 回目の検収 §7)。生のデータどうし(例: ティックと足、取引所どうし)の食い違いを見つける機能は E2 の「取引所間(情報源間)の食い違い」の側で、E1a ではない(19 回目の検収 §3)。ただし、計算の出力(損益・指標・約定の再計算など)を参照値(取引所の明細・別の計算の結果など)と照らして差を出す機能は、E1a の「実装と参照値」に当たる。
- **E1b の読み(14 回目から同じ。22・23 回目の検収 §4)**: 利用者が定義(どの列を・どの窓で・どの指標を)を書き、計算の中身は道具が持つもの(例: 14 回目の Great Expectations の `ColumnMean`、23 回目の dbt の MetricFlow)は、道具の計算に数える。計算の中身を利用者がコードで全部書くもの(例: 22 回目の dbt のモデル)は、道具で動かす利用者のコードで、道具の計算に数えない。
- **E4 の読み(22・23 回目の検収 §4)**: 述語の「**記録した市場データを時刻順に再生して**」を逐語で当てる。バッチを独立に・同時に走らせる再処理や、単なる再実行・再試行は、時刻順の再生を言う逐語が無ければ当たらない。原文に無い「時刻順に」を言い換えで足さない(22 回目の誤り)。
- **オーナーが承認した 4 つの問い**(L-508「1〜4全て承認します」・L-509「b」。承認は個別の問いへのもので、**ほかの候補へ同じ形で当てるのはリードの当て方**。問いに出さずに当てる): 候補の配布物に同梱された別のライブラリの機能は候補の機能に数える(承認された問いの文:「8-014 に同梱の `skfolio`・`scipy` の機能を Fincept の機能に数える」) / LLM への指示文に書かれた検出・検証の指示は数える / 重複をはじく処理は E2 の「重複の検出」に数える / 1 回だけの固定の学習・評価の分け方は E3b に数える(L-508「1〜4全て承認します」・L-509「b」)。乱数の種(`seed`)は段 5 の (ア) に当たらない。
- 設計票 §3 の述語の文を 1 語ずつ当てる。E2 は「時系列・約定・板・足・参照データについて…検出するか報告する機能」、E4 は「記録した市場データを時刻順に再生して…」、E6 は「E1a〜E5 の述語のどれにも当たらないもの」。E6 に置く前に、その機能が E1a〜E5 のどれかに当たらないかを書く(17 回目の AlgoNetwork の E6「independent verification」は E1a と重なりうるので `未判別` に戻った)。
- 段は §4.1 のとおり下から積み上げる。段 4 は「その要素の対象のすべて」を外から持ち込めることを言う逐語が要る(一部だけなら段 3)。段 5 は (ア)(イ) の両方の逐語が要る。**段は道具の側の記述だけで決め、当方が買った・動かしたかでは決めない。**各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引いて示す(段 4・5 は 8 字以上の逐語が要る。検査がこれを見る)。
- **`印` を付けた要素ごとに、設計票 §4.2 の表の「記録する軸」を知見の表に原文(逐語)と URL で書く**(E1a・E1b = 突き合わせの単位・許容誤差を指定できるか / E2 = 検出する異常の種類・直すか報告だけか / E3a・E3b = 方式 / E4 = 再生の粒度・遅延を入れられるか・時計の扱い / E5 = 何を固定・比較するか(乱数の種・依存・データの版・出力の差分)/ E6 = その機能の原文(何を確かめる機能か)・その機能が名指す対象の原文 / 全要素 = 方式・入力・外から持ち込める対象・(ア)・(イ)・自動で判定するか)。原文に無い軸は `記載なし(読んだ箇所: …)` と書く(追補 §4)。20 回目は、これを書き落とした。

### 2.2 問いの書き方

- 問いに出した行は、値も段も `未判別` にする(決めてから問いに出さない)。`### 判断に迷った点と問い` の番号付きの行には、全部 `[値・段の問い]` か `[それ以外の問い]` を付ける(付けないと検査が止める)。値・段を決めずにおく理由でない問いは `[それ以外の問い]`。

### 2.3 `印` を先に探す

各要素について、まず候補の文書・README・ソースから、述語に当たる機能を言う逐語を探し、`grep -n` や `sed -n` で生ログに印字して引く(`印` には全件の検索は要らない)。行を生ログに出すのは、読むと決めたファイルの範囲だけ。ソース全体への `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI`・`grep -rcI`)にする。`印` を探す段では `なし` を結論しない。

### 2.4 `なし` は全件の検索からだけ

- 一覧は `scripts/cat8_mklist.py` で作り、検索は `scripts/cat8_search.py` だけで打つ(どちらも `scripts/cat8_step.py` 経由。`--candidate` にその候補の台帳の番号。`--` のあとにパイプ・`;`・リダイレクトを付けない)。形は 20 回目の生ログ(`docs/DATA/probes/20260923_tools_8_run20.log`)の手をそのまま見本にする。
- **E2 の語の組の 7 つ目(`食い違い・discrepan・inconsisten・cross-source`)は 32 回目から足した**(リードの決定。述語の 7 種のうち「取引所間(情報源間)の食い違い」に当たる語が 11 回目からの組に無かった。監査 127 回目の指摘 4)。語を足すのは当たりを増やす向きだけに効く。
- 検索の語には、要素ごとに次の組の**語を全部**、この字のとおりに入れる: E1a = 突き合わせ・reconcil / cross-check・比較・compar / diff・差分・一致・mismatch、E1b = 突き合わせ・reconcil / 別実装・reference implementation・参照実装・cross-check / compar・比較・diff・差分、E2 = 欠損・missing・gap・欠け / 重複・duplicat / 順序・out of order・monoton・sort / 外れ値・outlier・anomal / 時刻のずれ・clock・skew・timestamp / 型・範囲・schema・range・valid / 食い違い・discrepan・inconsisten・cross-source、E3a = ルックアヘッド・look-ahead / survivorship・生存者 / leak・リーク・未来 / point-in-time・as-of・時点、E3b = ルックアヘッド・look-ahead / embargo / purge / point-in-time・as-of・時点、E4 = リプレイ・再生・replay / record・記録・capture / playback・再実行・rerun、E5 = 再現・reproduc / seed・乱数 / deterministic・決定的 / snapshot・golden・pin・lock・版の固定、E6 = 検証・verif・validat / 品質・quality / test・assert・check。**打つ前に語に誤字が無いかを見る**(誤字の手を打つと、その手の当たりも全部判定が要る)。
- `なし` の根拠の欄に `一覧 N 件 / 読んだ M 件` と `当たり F ファイル / H 行` をこの字のとおりに書く。N と M は cat8_mklist.py の `listed=` の和。7 列目には、その要素の検索の手の行を全部書く(長い出力の中の離れた行を引くときは、引用した行も書く)。
- **当たった行は全部判定する。この回は、ファイルの表 (甲) だけを使う**(決まりで分ける `cat8_classify.py`(乙)は使わない。18・19 回目に、決まりが語ごとの形になり、候補の機能を言う行まで取っていた)。
  - ファイルの表 `### 当たりの判定`: `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |`。**理由は、そのファイルの当たった行を読んで、当たった語と、その語がそこで何を指すかを書く。**
  - 行の表・ファイルの表には、同じ文を繰り返さない(語尾や語順を変えただけの同じ内容も、同じ理由とみなす。リードが検収で読む)。同じ文を何行・何ファイルにも当てた `なし` は受け取られない。当たりが多くて 1 つずつ書けないなら、§1 の「500 行」の規則で `未判別` にする。
- 生ログはこの回のものだけを根拠にする(ほかの回の検索の結果を引かない)。

### 2.5 §4.0 の表と、最初の手・最後の手

8-035 の §4.0 の表は書かない(この回は §4.0 を変えない。33 回目の節の値のまま)。

**最初の手**(生ログの 1 つ目の手)は、`python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run34.log --sandbox /tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/8-035/sb34 --method setup --target 開始 --note 開始の時刻と置き場所 -- 'date -u +%Y-%m-%dT%H:%M:%SZ; ls /tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/8-035; ls -ld /root/.m2 /root/.gradle; du -sh /root/.cache/pip'`。**`--sandbox` は道具が以後の同じ生ログの手の全部に一時・キャッシュの置き場所の環境変数を付けるもの**(33 回目の検収 §4 の処置 1。以後の手に `--sandbox` を書かなくてよい。見出しの行に `[sandbox …]` が出ることを確かめる)。**`cat8_step.py` を通さない殻の手を打たない**(ファイルを読むだけの `Read` の道具は別。33 回目は殻の手で `/tmp/blocklines.txt` を作った)。**最後の手**(検索が全部終わり、§7 の検査の前)は、`find /tmp -maxdepth 1 -newermt '<開始の時刻>'` と `find /root -xdev -newermt '<開始の時刻>' -not -path '/root/.claude/*' -type f` を打って出力ごと生ログに出す(出たら、そのファイルを移さず消さずに、名前と作った手を報告に書く)。

### 2.6 大きさ・届かないとき・取り方

- 生ログと報告は GitHub に押し出すので、どちらも 90 MB 以内に保つ(`wc -c` を検索の手の前後に `cat8_step.py` で打つ)。
- この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書き、「存在しない」とは書かない。
- 取った本文・一時のファイル・自分で書いた手順の小さいスクリプトは、全部 `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/8-035/` の下に置く(`/tmp` の直下に置かない。31 回目は 36 個、32 回目は 2 個を置いた。**報告の節の下書きもここに書く**。検査 `check-elements` がこれを数える)。
- **WebFetch は一次資料に使わない**(要約を返すため)。まず `curl` で生の HTML を取る。JavaScript で描く頁は `scripts/cat8_render.js` を `cat8_step.py` 経由で打つ(`NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run34.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`)。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない。**`印` の根拠にするのは、次の全部を満たすときだけ: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く / (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(この回の節の追記だけ。1〜33 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run34.log` の 2 つだけ。scratchpad 以外にファイルを作らない。生ログはすべて `scripts/cat8_step.py` で書く。
- 台帳には書かない。`scripts/` の下のファイルは書き換えない。区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。外部の道具を入れる場所は `.../scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

33 回目の節と同じ小節を置く(`### 決まりの一覧`・`### 当たりの判定(行ごと)` は、この回は (乙) を使わないので置かない)(`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」)。検査が形を見る所:
- 「要素と段」の表は 7 列 `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目には `docs/DATA/probes/20260923_tools_8_run34.log:10` か `...:10-20` を空白で区切って並べるだけ(ほかの字を入れない)。値が `印` か `なし` の行は 7 列目を空にしない。引いた文「…」は 7 列目の行の手の出力に出ていること。
- この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし、値と段は台帳と同じにし、7 列目は空でよい。
- 知見の表の「根拠」の欄には、生ログの参照(上と同じ形)・URL・日付(`2026-09-26`)・「取得日」だけを書く(説明は「知見」の欄に書く)。**候補の一覧と「要素と段」の表(7 列)は、8-001〜8-041 の 41 行全部を載せる**(この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする)。変えた要素は候補の一覧の説明に全部書く。

## 5. 禁止

委任文 §10 のすべて。commit も push もしない。モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。

## 7. 返す前に

(**1〜4 の出力を全部貼る**。32 回目は 1 しか貼らなかった。リードは検収で打ち直して一致するかを見る。)
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md` に、`docs/DATA/probes/20260923_tools_8_run1.log` から `docs/DATA/probes/20260923_tools_8_run34.log` までの 34 本の生ログを並べて打つ。誤検出は閉じずに行と理由を書く。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 34` を 0 件にする(最後の出力を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run34.log` を 0 件にする(同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0(同じ節に貼る)。
5. 返答には次をこの順で書く: 候補ごと・要素ごとの `印`・`なし`・`未判別`(`未判別` なら何が足りないか)/ この回の `なし` の全部の「一覧 N 件 / 読んだ M 件」/ 当たりが 500 行を超えたために `未判別` にした要素と、その当たりの行・ファイルの数 / §4.0 で `未確認` のまま残した項目と理由 / 4 つの検査の最後の合計行 / 判断に迷った点と問い(決めずに) / 描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧(候補・要素・読めなかった頁)/ 生ログと報告の大きさ(最後の `wc -c`)。「区分は完了」とは書かない。
