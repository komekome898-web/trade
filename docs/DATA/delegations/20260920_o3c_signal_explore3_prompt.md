あなたは本リポジトリの研究・実装エージェントです。探索段 3 の道具 `scripts/o3c_signal_explore3.py` とテストを作り、走らせ、数表を出してください。設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md` から逸脱しないこと。

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計(全文。用語表を先に)/ 探索段 2 の道具 `scripts/o3c_signal_explore2.py`(1 周目の表と約定の読み込み・`PriceSeries` の `at_or_before` / `at_or_after`・日クラスタ SE・対照 (i) の日集約と (ii) の符号・gzip の決定的な書き出し・`check_no_banned`・chunk の再開)と `tests/test_o3c_signal_explore2.py` / `scripts/o3c_reaction.py` の `p_liq`(束の最初の清算の価格)と `start_ms` / `end_ms` の意味 / `docs/PHASE2/O3C/SIGNAL/REFUTER_REVIEW2_2026-09-19.md`(この段が分けようとしている問い = 翻訳 (a))。

【データ】探索段 2 と同じ: 1 周目の表 `backtest_data/o3c_reaction_20260918_full/{gap60_w8,gap30_w8,gap180_w8}/table.csv`(無ければ `.gz` を展開して MD5 照合)の束(`cascade_id`、`start_ms`、`end_ms`、`side`、`p_liq`、属性列)と対照 (i)(ii) の行、約定 `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/*.zip`。bitFlyer は探索段 2 と同じ 1 分足。**`paper_logs/` は開かない**(L-246)。

【量(設計の用語表)】基準 p_pre = `at_or_before(start_ms − 1, 300_000)`(無ければ NaN)。終端 p_end = `at_or_after(end_ms, 300_000)`(探索段 2 の Δ = 0 の起点と同じ)。h 後 = `at_or_before(終端の時刻 + h*1000, 300_000)`、h ∈ {60, 300, 900} 秒。符号 s = SELL: −1、BUY: +1(探索段 1・2 と同じ向き: 負 = 清算と逆)。sweep = s × (p_end − p_pre)/p_pre × 10⁴(清算の向きが正になる定義。実測で負の束の数を報告)。r_pre(h) = s × (p_h − p_pre)/p_pre × 10⁴。r_end(h) = s × (p_h − p_end)/p_end × 10⁴(探索段 2 の r(0,h) と同じ量 → 一致の確認に使う)。戻り率 f(h) = (p_end − p_h)/(p_end − p_pre)(sweep > 0 の束だけ定義。sweep ≤ 0 は NaN にして数を報告)。3 区分: f > 1 / 0 ≤ f ≤ 1 / f < 0。`baseline_lag_ms` = start_ms − 基準の約定の時刻。`next_bundle_gap_s` = 次の束の start_ms − この束の end_ms(同じ gap の表の中で、日をまたぐ次の束も含む。最後の束は NaN)。`prev_bundle_gap_s` = この束の start_ms − 直前の束の end_ms(同じ gap、日をまたぐ直前も含む。最初の束は NaN)。`baseline_in_prev_bundle` = 基準の約定の時刻 ≤ 直前の束の end_ms(真偽)。

【対照】設計 §4 の手順 1〜5 をそのまま実装する(要約しない)。(a) 掃き合わせ: 束 b(日 d、側 s、幅 W_b = end_ms − start_ms、sweep S_b > 0)ごとに、候補時刻 t = その日の 00:00:00 UTC から 10 秒刻み(8,640 点)のうち、区間 [t − W_b − 1 ms, t + 900 秒] が同じ gap のどの束の [start_ms, end_ms] とも重ならないもの。候補の掃き c_t = s × (p(at_or_after(t)) − p(at_or_before(t − W_b − 1)))/p(at_or_before(t − W_b − 1)) × 10⁴(清算側の sweep と同じ符号規則、同じ許容 300,000 ms)。S_b の 10 分位帯に c_t が入る候補のうち |c_t − S_b| 最小のものを取り、同値なら早い t。分位帯: 同じ gap の S_b > 0 の S_b を `numpy.quantile(…, [0.1,…,0.9])`(既定の線形補間)で切り、q_10 = +∞、帯 1 = (0, q_1)(0 を含まない)、帯 k ≥ 2 = [q_{k−1}, q_k)(下限含む・上限含まない)。S_b の帯も同じ規則。c_t ≤ 0 はどの帯にも入らず候補にならない。帯が空でも、その日の生存候補が帯に 1 つも落ちなくても「対照 (a) なし」。置換なし: 束を start_ms の順に処理し、既に取った候補の区間 [t − W_b − 1 ms, t + 900 秒] と重なる候補は使わない。取れなければ「対照 (a) なし」で、数と割合を gap ごとに報告。対照 (a) の基準 = at_or_before(t − W_b − 1)、終端 = at_or_after(t)、h 後 = at_or_before(終端の時刻 + h)(清算側と同じ非対称)。f・r_pre・r_end を清算側と同じ式で出す。対照 (ii) の時刻は使わない。(b) 無条件: 対照 (i)(探索段 2 の日集約版)と (ii) の r_end(h) だけ(基準が無いので r_pre と f は無い)。

【F0 自己点検】清算 / 対照 (a) / (b)-i / (b)-ii の 4 種それぞれで、基準の遅れ(求めた時刻 − 見つかった約定の時刻)・終端の遅れ(見つかった約定の時刻 − 求めた時刻)・h 後の遅れ(h×1000 − 実際の間隔)の p50 / p90 / p99 / 最大と、h 後の価格が終端と同じ(約定なし)割合。対照 (a) は加えて |c_t − S_b| の p50 / p90 / p99 / 最大と、取れなかった束の数と割合。(b) には基準が無いので基準の遅れの行は「—」。

【固定値】gap 60 主、30 / 180 併記。SE は日でクラスタ(探索段 2 の `mean_se_cluster` と同じ式)、naive SE(母標準偏差 / √n、同じ関数が返す)も併記(設計 §7)。日で等重みの点推定を併記。前後の清算は全体を主、「次の束まで 180 秒以上」(見出しに「結果依存の条件付け」と書く)と「前の束から 180 秒以上」(過去の条件付け)を併記。両側混在で除外した束の割合を gap ごとに F1 に出す。清算行の重複: 472 日すべてで、清算ファイルの全列が同じ行の多重度(1 / 2 / 3 以上)を数え、summary.json に内訳を残し、`docs/DATA.md` §2 の既存の実測(106,822 行 → 一意 53,398、多重度 2 が 53,385 群・4 が 13 群)と一致するかを報告に書く(束は 1 周目の重複除去済みの行を使う)。

【出力】`backtest_data/o3c_signal_explore3_20260920/`: `rows_gap{60,30,180}.csv.gz`(行 = 束または対照、列 = kind, cascade_id, day, side, t_pre_ms, t_end_ms, p_pre, p_end, baseline_lag_ms, end_lag_ms, sweep_bp, prev_bundle_gap_s, next_bundle_gap_s, baseline_in_prev_bundle, r_pre_60/300/900, r_end_60/300/900, f_60/300/900, h_lag_ms_60/300/900, bf_r_pre_60/300/900, 対照 (a) は matched_liq_id と c_t)、`f0_selfcheck.csv`(gap 3 × 4 種 × {基準の遅れ, 終端の遅れ, h 後の遅れ 3} = 60 行 + 対照 (a) の合わせ具合 gap 3 行 = 63 行)、`f1_sweep.csv`(gap 3 × {全体, SELL, BUY, 束の件数 3 分位, 両側混在の除外} = 21 行: 平均・中央値・p10/p90・sweep ≤ 0 の数・baseline_lag_ms の中央値と p90・基準が直前の束の end_ms 以下の束の数・前の束から 300 秒未満の割合)、`f2_retrace.csv`(gap 3 × h 3 × {清算 全体, 清算 次の束まで 180 秒以上, 清算 前の束から 180 秒以上, 対照 (a), 対照 (b)-i, 対照 (b)-ii} = 54 行: n・含む日数・r_end 平均 ± 日クラスタ SE ± naive SE と日等重みの平均(6 種)・r_pre 平均 ± 日クラスタ SE ± naive SE と日等重みの平均(清算と (a)。(b) は「—」)・f の中央値・p25/p75・3 区分の割合(清算と (a)。(b) は「—」)・h 後の価格が終端と同じ割合(6 種))、`f3_overshoot.csv`(gap 3 × h 3 = 9 行: f > 1 の束の、基準を越えた幅 bp の p10/p25/p50/p75/p90 と sweep との比)、`f5_attributes.csv`(gap 60 だけ。理由は設計 §6、33 群(探索段 2 と同じ 10 属性 30 群の切り値 + sweep の 3 分位。sweep の 3 分位は gap 60 の清算側で S_b > 0 の束を母集団に、探索段 2 の `tertile_cuts` / `tertile_mask` と同じ規則で切る。S_b ≤ 0 は欠測として数える)× h 3 = 99 行: f > 1 の割合・r_pre 平均 ± 日クラスタ SE・n・含む日数・その属性が欠測で群に入らない束の割合)、`tables.md`、`summary.json`(入力の MD5、日数と欠けた日、行数、sweep ≤ 0 の数、対照 (a) の採否の数、清算行の多重度の内訳と DATA.md との一致、実行時刻、パラメータ)、`MD5SUMS`。**判定語(差あり / 検出されず / 陽性 / 陰性)を出力にもログにも書かない。**

【テスト】`tests/test_o3c_signal_explore3.py`: (1) r_end(60) の gap60 清算側の平均が探索段 2 の Δ = 0・h = 60 の −6.800(`backtest_data/o3c_signal_explore2_20260919/e1_delta.csv`)と小数 2 桁で一致(実データ。長ければ 5 日分で行ごとに一致)。(2) 基準の約定の時刻 < start_ms。(3) 3 区分の和が 1(sweep > 0 の束で)。(4) sweep ≤ 0 の束は f が NaN で数が summary に出る。(5) 対照 (a) の c_t が相手の sweep の分位帯に入り、区間が束と重ならず、置換なし(区間が互いに重ならない)。(6) `paper_logs/` を開かない(探索段 2 と同じモック)。(7) 日クラスタ SE が探索段 2 の式と一致。既存スイート `PYTHONPATH=src python -m pytest` を全部通す(件数を報告)。

【長時間処理】10 分を超えるなら切り離して chunk で書き、再開できる形に(探索段 2 と同じ)。監視は回数上限つき。

【必須報告】(0) 表の行数の数え直し(設計 §6 は 246 行: F0 63 + F1 21 + F2 54 + F3 9 + F5 99。違えばどこか)/ (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所(数値は csv から写す)/ (3) r_end(60) が探索段 2 と一致した証拠 / (4) F0〜F5 の表(`tables.md`)/ (5) 設計に無い判断を全部 / (6) サニティ(先読み 0、再実行の決定性、欠測の数、baseline_lag_ms の分布)/ (7) 限界。**読み(なぜ)は書かない。**

【制約】read-only・冪等・ネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名を書かない。Do not commit. Do not push。報告は `docs/PHASE2/O3C/SIGNAL/EXPLORE3_DELEGATE_REPORT_2026-09-20.md` に書く。
