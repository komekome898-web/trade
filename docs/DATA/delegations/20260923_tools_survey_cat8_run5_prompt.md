# 道具サーベイ 区分 8 — 5 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 5 回目**を担当します。

**この回の終わり方**: §2 の 1〜9 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 5 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run5.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run4.md` を全文(とくに §7 の答えと、末尾の「監査 21 回目」の指摘とリードの処置)。**4 回目の `なし` のうち 4 候補分が、条件 (a) の「全部」を満たさずに差し戻された。同じ誤りをしない。**
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。**この回の出発点は台帳の値**(4 回目の報告の表の値ではない。監査 21 回目で台帳を直した)。

## 1. この回の位置づけ

台帳の残り(設計票 §2 の「残り」= 状態が `未着手`・`判別に一次資料が要る`・`浅い` の行)は 9 行: 8-003・8-006・8-007・8-008・8-010・8-012・8-013・8-014・8-016。この回はこの 9 行を全部、`深掘り` の条件まで進める。

**この回に回さないもの(6 回目以降。この回で手を付けない)**: 8-001・8-002・8-004・8-005・8-009・8-011・8-015 の `なし` の当て直し / E5 の段の当て直し(8-002。台帳で `未判別` に戻した)/ 辿る一覧 / 成熟度の枠組みの原典 / `akurkar07/OrderBook` の E6。ただし 8-003 は残りの 9 行に入るので、8-003 の E5 の段はこの回で当て直す(§2 の 1)。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

**`なし` を書くときの手順(この回の全部の `なし` に当てる。4 回目の差し戻しの対策)**:
- 候補の一次資料の**ファイル一覧・頁の一覧を全件で取る**。`cat8_step.py` の既定は出力を 3,000 字で切るので、一覧を取る手には `--keep 200000` を付ける。一覧は生ログに全件が残るようにする。
- `なし` の行の根拠の欄に、**`一覧 N 件 / 読んだ M 件`** の形(この字面のまま)と、一覧を取った生ログの行(`20260923_tools_8_run5.log:<行>`)を書く。**N と M が違うなら `なし` と書かない**(`未判別` のまま、読み残しの名前を書く)。§7 の 2 の検査がこの形・N = M(数として)・生ログがこの回のもので行が実在することを機械で見る(この回に触らない行の根拠は `台帳の値のまま(<何>回目の節)` と書けば検査の対象外)。一覧から外すもの(画像・生成物など、文書でもソースでもないもの)は、外した名前と理由を書く。
- 「主要な」「全体で」のような語で範囲をぼかさない。
- **WebFetch は一次資料に使わない**(要約を返す道具なので、委任文 §4.1 の「検索結果の要約」に当たる)。まず `curl` で生の HTML を取る。JavaScript で描く頁で本文が HTML に無いときは、`scripts/cat8_render.js`(この環境の Chromium で頁を開いて本文をそのまま出す。`playwright install` は打たない)を `cat8_step.py` 経由で打つ: `NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run5.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`(`rc` と秒が数字で入る。ブラウザーは自分では通信せず、道具の Node の側が証明書を通常どおり検証して取ったものを頁に渡す。リードが 2026-09-24 に `https://fxreplay.com` で HTTP 200 と本文、証明書の誤りのある頁(名前の不一致・自己署名・期限切れ)で取得が止まることを確かめた)。本文が取れずに止まったら、その頁は `未判別` のまま、止まった理由を書く。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない**(道具は本文の欠けを全部は数えられない。監査 25〜27 回目で、何も数えられないまま本文が欠ける型が 3 つ見つかった)。`なし` は、生の HTML・文書のファイル・ソースなど、全部を取ったと件数で示せる一次資料で書く。描画でしか読めない頁しか無い要素は `未判別` のまま、読めなかった頁の名前を書く。§7 の 2 の検査は、`なし` の根拠に引いた生ログの手が `cat8_render.js` なら止める。出力の `INCOMPLETE` の行と最後の `INCOMPLETE 合計 <N> 件` は、`印` の根拠に使うときに一緒に写す。

1. 8-003 `prediction-market-backtester`: E3a・E3b・E6 を、上の手順で当て直す(`tests/` の全ファイルを含む。4 回目は 7 本しか検索していなかった)。E5 の段 5 を、設計票 §4.1 の (ア)(イ) の原文で当て直す(乱数の種は (ア) の「判定の基準」に当たらない = 4 回目の検収の監査 21 回目の処置 8)。
2. 8-006 `freqtrade`: E3b を当て直す(`docs/` の全ファイル。4 回目は 5 本しか検索していなかった)。
3. 8-007 `backtrex`: E3a を当て直す(Documentation の全頁。4 回目は 8 頁中 5 頁)。
4. 8-008 `FX Replay`: E1a・E2・E3a・E3b・E5・E6 を、Chromium で取った本文で当て直す(4 回目は WebFetch の要約だった)。
5. 8-010 `arXiv:2603.20319`: E1b と E2(台帳で両方 `未判別`)。コードが未公開なら、論文の本文だけで E1b・E2 の述語に当たる原文があるか決める(`なし` は上の手順で)。論文が示すほかの所在(補足資料・データの置き場)も開く。届かなければ `判別に一次資料が要る` のまま、試した手段を全部書く。
6. 8-012 `VectorBT`: E4 を設計票 §3 の述語の逐語(「**記録した市場データを時刻順に再生して、戦略・執行・計算を再実行する**」)で、公式の文書の原文に当てる。**時刻順に処理することを書いた原文があるときだけ `印`**(配列をまとめて計算すること自体は、それだけでは当てない。述語を広げない)。原文が無ければ上の手順で `なし`、読みに迷えば `未判別` のまま問いに出す。E5 の段を (ア)(イ) の原文で当て直す。
7. 8-013 `rusty-bot`(`yasstake/rbot`): Rust の本体を読み、E2(`download_gap` など)・E5 を決める。
8. 8-014 `Fincept Terminal`: §4.0 の表の `未確認` を、一次資料か実測で埋める(過半を `一次資料` か `実測` にする)。E1a の段(6 つのエンジンの結果を突き合わせる記述があるか)、E5(作りが決定的なだけでは E5 の機能ではない。再現を確かめる・固定すると書いた原文があるときだけ `印`)。
9. 8-016 `Exactpro の reconciliation testing`: **範囲を縮めない**。候補は「Exactpro の reconciliation testing」の全体。公開の OSS(th2)の関係するリポジトリを一覧で全部挙げ、E2〜E6 に関わるものを読み切る。商用の部分は委任文 §5-6(登録なしで取れるものを全部取り、オーナーが辿れる手順を書く)。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 5 回目の実行(2026-09-24)` の追記だけ**。1〜4 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run5.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` と `### 辿る一覧から出た名前` を除いたもの全部。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表は、8-001〜8-016 の 16 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(5 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 5` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件` もここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run5.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: 9 件の各候補の状態(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」の一覧 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ 生ログの行数。
6. 「区分は完了」とは書かない。
