あなたは本リポジトリの研究・実装エージェントです。探索段 2 の道具 `scripts/o3c_signal_explore2.py` とテストを作り、走らせ、数表を出してください。設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE2_DESIGN_2026-09-19.md` から逸脱しないこと。

【対応表(CLAUDE.md §0.1。着手前に自分の言葉で書き直さず、設計 §0.1 の表をそのまま写し、右列が空になる作業があればそこで止めて報告する)】
設計 §0.1 の表を報告の冒頭に写す。

【読了必須】設計(全文)/ `scripts/o3c_reaction.py` の `build_cascades` の呼び出し・`_anchor_chunks_trades`・対照 (i)(`control_uniform`、`allowed_intervals` + `MATCH_GRID_MS`)と (ii)(`control_matched`)の作り方・`bf_*` 列の作り方・`compute_reactions(anchor="after_shift")` / `src/bot/research/liq_response.py` の `PriceSeries.at_or_after` と `compute_reactions` / 探索段 1 の道具 `scripts/o3c_signal_explore.py`(出力の形と日クラスタ SE の式は `docs/DATA/probes/20260919_o3c_signal_refuter_verify.py` にある)/ `docs/PHASE2/O3C/SIGNAL/REFUTER_REVIEW_2026-09-19.md`(この道具が分離すべき致命 3 件)。

【データ】`backtest_data/binance_cm_o3c_20260913`(約定 `aggTrades/BTCUSD_PERP`、清算 `liquidationSnapshot/BTCUSD_PERP`、`metrics`)。対象日 = 1 周目の走行 `backtest_data/o3c_reaction_20260918_full/gap60_w8/summary.json` の `per_day[].day`(456 日)。束 = 1 周目の主表 `backtest_data/o3c_reaction_20260918_full/gap60_w8/table.csv` の清算行(`cascade_id`、`start_ms`、`end_ms`、`side`、属性列)をそのまま使い(束を作り直さない = 同じ集合)、対照 (ii) も同じ表の `control_matched` 行(`matched_liq_id` で相手の側を引く)、対照 (i) は同じ表の `control_uniform` 行(束に紐づかず `side` 空。符号は設計 §4 の規則 = 日ごとに liq 行(mixed を除く)を `start_ms` 昇順、一様行を `time_ms` 昇順に並べ、i 番目どうしを 1 対 1 に組む。一様行が余る日(mixed の束がある 75 日)は末尾の余りを落とす(合計 180 行になるはず。日ごとの数を `summary.json` に書く)。参考として「日ごとの一様行の平均 × 束の符号」も併記し、Δ = 0・h = 14400 秒で探索段 1 の +10.7 bp が再現するか確認する)。gap 30 / 180 の感度は `gap30_w8` / `gap180_w8` の表の束と対照(gap 60 の束を流用しない)。**起点と h 後の価格だけを約定から新しく取る。**bitFlyer は 1 周目の `bf_*` 列を作った経路と同じデータで同じ規則(データの在処は `o3c_reaction.py` を読んで確かめ、無ければ「無い」と範囲と方法を書く)。**`paper_logs/` は開かない**(オーナー逐語 L-246「自前記録使ったら判定できなくなるからダメ」)。

【量】設計 §2。起点 = 束の `end_ms` + Δ 秒 **以後**の最初の約定(`at_or_after(end_ms + Δ*1000, 300_000)`、無ければ NaN)。対照の行も同じ規則(対照の時刻 + Δ)。h 後の価格 = `at_or_before(起点の時刻 + h*1000, 300_000)`。`r(Δ, h)` = (h 後 − 起点) / 起点 × 10⁴ × 符号(SELL: −1、BUY: +1 = 1 周目の `reactdir` と同じ向き。負 = 反転)。`bounce(Δ)` = Δ = 0 の起点 → Δ の起点の符号付き変化(bp)。`anchor_lag_ms` = 起点の時刻 − (end_ms + Δ*1000) を行ごとに残す。

【固定値(設計 §7)】Δ ∈ {0, 1, 5, 10, 30, 60} 秒 / h ∈ {1, 5, 10, 30, 60, 300, 900, 3600, 14400, 28800} 秒(10 本)/ gap 60 主、30・180 感度 / SE は日でクラスタ(式は上のプローブ)、naive も併記 / 反転の割合は r<0 / r=0 / r>0 の 3 つ。

【出力】`backtest_data/o3c_signal_explore2_20260919/`: `rows_gap60.csv.gz`(行 = 束または対照 × Δ、列 = kind, cascade_id, day, side, Δ, anchor_ts_ms, anchor_lag_ms, bounce, r_h ×9, bf_r_h ×9)、同じく gap30 / gap180、`e0_anchor_lag.csv`(Δ 6 × kind 3 = 18 行: lag==0 の割合・中央値・p90・NaN の数)、`e1_delta.csv`(Δ 6 × {清算 全体, 清算 対照(ii)付き, 清算 付かない, 対照(i), 対照(ii)} = 30 行、h=60 秒: 平均・日クラスタ SE・naive SE・n・3 区分の割合・bounce の平均・bitFlyer の平均)、`e2_shape.csv`(Δ ∈ {0,10,60} × h 10 本 × kind 3 = 90 行)、`e3_gap.csv`(gap 3 本 × Δ 6 本 × E1 と同じ 5 行 = 90 行、h=60。gap 60 は E1 の再掲)、`e4_attributes.csv`(探索段 1 と同じ 10 属性 30 群(3 分位 × 8 + 側 2 + 時刻帯 4。切り値は `scripts/o3c_signal_explore.py` と同じ)× Δ ∈ {0,10,60} = 90 行、清算側 h=60、n、含む日数、「束の幅と件数の下位は同一行」の注記列)、`tables.md`(全部を表に)、`summary.json`(入力の MD5、日数、**欠けた日の列挙**(456 日は暦 474 日のうち 18 日欠ける: 2023-09-09 / 09-23 / 09-25、2024-02-15〜02-26、2024-06-01 / 06-11 / 06-12 — 自分で数え直して一致を確かめる)、行数、欠測、実行時刻、パラメータ、**清算行の重複の確認**: 生の `liquidationSnapshot` の 3 日分(例 2023-06-27 / 2024-01-15 / 2024-10-01)で raw の行数・全列一致の重複の数・ローダ `load_binance_cm_liquidations_with_dedup_stats` が同じ行を 2 つの経路から読んでいないか(zip 内のファイル数、CHECKSUM ファイルの扱い)を調べて書く。分からなければ「未確認」と書く)、`MD5SUMS`。**判定語(差あり / 検出されず / 陽性 / 陰性)を出力にもログにも書かない。**

【テスト】`tests/test_o3c_signal_explore2.py`: (1) Δ = 0 の gap60 の清算側 h=60 秒の平均が 1 周目の `bp_1m_reactdir` の平均 −6.800(n 21,198)と小数 2 桁で一致(合成データではなく実データで、実行時間が長ければ 5 日分に絞って表の同じ行と一致させる)。(2) 対照の行にも同じ Δ が当たる(anchor_ts_ms ≥ 対照時刻 + Δ*1000)。(3) 起点の後 5 分に約定が無ければ NaN。(4) `paper_logs/` を開かない(モックで `open`/`Path.glob` を監視)。(5) 日クラスタ SE がプローブの式と一致。(6) 3 区分の割合の和が 1。(7) 対照 (i) の符号が設計 §4 の規則どおりで、mixed を含む日で末尾の余りが落ちる(合成の小さな表で。実データでは落ちた数が 180)。(8) E0 の出力に Δ × kind の 18 行があり、Δ = 0 の清算側 lag==0 の割合が 1 周目の表の値 0.7949 と一致する。既存スイート `PYTHONPATH=src python -m pytest` を全部通す(件数を報告)。

【長時間処理】10 分を超えるなら `setsid nohup … > <scratchpad>/run.log 2>&1 < /dev/null &` で切り離し、日ごとの chunk を書いて再開できる形にする。監視は回数上限つき。

【必須報告】(0) 表の行数の数え直し(設計 §7 は 318 行: E0 18 + E1 30 + E2 90 + E3 90 + E4 90。違えばどこか)/ (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所(数値は csv から写す。出所を書く)/ (3) Δ = 0 が探索段 1 と一致した証拠 / (4) E1〜E4 の表(`tables.md`)/ (5) 1 周目と定義が違う点があれば全部 / (6) サニティ(先読み 0 = 起点の選び方に未来の価格を使っていない、再実行の決定性、欠測の数)/ (7) 限界。**読み(なぜ)は書かない**(探索段の読みはリードと反証者が行う)。

【制約】read-only・冪等・ネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名を書かない。Do not commit. Do not push.
