あなたは本リポジトリの研究・実装エージェントです。「材料の選定」の道具 `scripts/o3c_signal_materials.py` とテストを作り、走らせ、前半 228 日の 2 組の表を出してください。**出力はすべて日本語。判定語(陽性/陰性/有意/差あり/検出されず/支持/棄却)を書かない。`paper_logs/` を開かない。後半の表は作らない(後半の候補の値は行データに出すだけ)。**

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_MATERIALS_DESIGN_2026-09-20.md`(全文。§2 の候補 22 本の定義と §3 の手順を先に)/ 前段の道具 `scripts/o3c_signal_continue.py`(既存 13 本の材料の計算・約定の読み込み・成行の偏り・建玉の帯 = **そのまま流用**)/ 行データ `backtest_data/o3c_signal_continue_20260920/rows_continue.csv.gz`(既存の材料の値・ts・side・half)/ 探索段 5 の `backtest_data/o3c_signal_explore5_20260920/rows_prints.csv.gz`(`bundle_pos`・`bundle_pos_single` = 事後の位置)/ 5 分値 `backtest_data/binance_cm_o3c_20260913/metrics/BTCUSD_PERP/`、約定 `.../aggTrades/BTCUSD_PERP/*.zip`。

【候補 22 本(設計 §2)】既存 13 本(1, 2, 3, 5', 6, 8, 9, 10, 11, 12, 13, 14, 15。**4 は外す**)は行データの列を再利用してよいが、5' は「先」だけに作り直す(探索段 5 の `oid` の対数ビンから、清算の向きの先にある最も近い水準までの距離。無ければ欠測)。新規 9 本(F3, F4, A3, A4, A5, A6, A9, C3, C4)は設計の定義どおり **ts 以前の約定・清算・5 分値だけ**で計算する(p₀ を使わない。5 分値は ts より前に確定した最新の値)。連鎖 = ts 基準で「同じ側の直前の間隔が 60 秒以内なら繋ぐ」(前段の (5)-6 と同じ)。

【2 組(設計の用語表)】事後の位置は探索段 5 の `bundle_pos` と `bundle_pos_single` から: 1 件目 = 単発(止まる)/ 多件の最初(続く)、2 件目以降 = 途中(続く)/ 多件の最後(止まる)。束の外は除く。**事後の位置は表を作るときにだけ使う。**

【表(前半だけ)】組 A・組 B × 候補 22 本 = 44 行。列: 候補 / 組 / 止まる側の n・欠測の割合・p10・p25・p50・p75・p90 / 続く側の同じ列 / **分かれ方の数** = 止まる側から 1 件、続く側から 1 件を無作為に取ったとき止まる側の値の方が大きい確率(順位で計算。同値は 0.5 と数える。欠測は両側から除く)/ |分かれ方 − 0.5|。**|分かれ方 − 0.5| の降順に並べる**(組ごと)。平均の列は作らない。

【実物読みの抽出(設計 §3 の 4)】組 A から 単発 30 + 多件の最初 30、組 B から 途中 30 + 多件の最後 30 を乱数(種 20260920、前半、日を跨いで重ならないよう 1 日 1 件まで)で抜き、各プリントについて 候補 22 本の値と、直前 60 秒の清算の列(相対秒・側・想定元本)と、直前 60 秒の 1 秒刻みの値段(bp、ts 直前 = 0、清算の向きが正)を markdown に出す(`docs/DATA/probes/20260920_o3c_materials_read.md`)。

【出力】`backtest_data/o3c_signal_materials_20260920/`: `rows_materials.csv.gz`(全 52,000 プリント × 候補 22 本 + ts・side・half・事後の位置)、`screen_A.csv`・`screen_B.csv`(上の表)、`tables.md`、`summary.json`(行数、数値セル数、欠測、入力の MD5、連鎖の定義)、`MD5SUMS`。

【テスト】`tests/test_o3c_signal_materials.py`: (1) 候補 22 本が ts 以後を使わない(ts 以後の約定・清算・5 分値を消した合成入力で同じ値、p₀ を変えても同じ)/ (2) 2 組の分け方(合成の位置で 4 群 → 2 組)/ (3) 分かれ方の数(合成の 2 群で手計算と一致、同値 0.5、欠測の除外)/ (4) 表が前半だけから作られる(後半の値を変えても表が変わらない)/ (5) 5' が「先」だけ / (6) 判定語が `tables.md` に無い / (7) `paper_logs/` を開かない。全スイート `PYTHONPATH=src python -m pytest`(`-q` を足さない)の末尾行。

【必須報告】`docs/PHASE2/O3C/SIGNAL/MATERIALS_DELEGATE_REPORT_2026-09-20.md`: (0) 行数と数値セル数 / (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) 自己点検(候補ごとの欠測、連鎖の数、2 組の件数)/ (4) 組 A・組 B の表(並べ替え済み)/ (5) 設計に無い判断(全部。設計の中の食い違いを見つけたら止めて報告)/ (6) サニティ(未来を使わない検査、再実行の決定性)/ (7) 限界 / (8) 作ったファイル / (9) テストの末尾行。**境目は置かない(リードが置く)。**

【制約】read-only(入力を書き換えない)・冪等・ネットワークなし。10 分を超えるなら日ごとの chunk で再開できる形に。既定値を変えるなら列挙して理由を書く。モデル名を書かない。**Do not commit. Do not push.** 他のファイルを変更しない。
