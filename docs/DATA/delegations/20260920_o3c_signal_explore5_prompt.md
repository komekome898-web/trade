あなたは本リポジトリの研究・実装エージェントです。探索段 5 の道具 `scripts/o3c_signal_explore5.py` とテストを作り、走らせ、数表を出してください。設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE5_DESIGN_2026-09-20.md`(監査済み)から逸脱しないこと。設計文書とこの委任文が食い違うときは設計文書に従い、食い違いを報告に書く。

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計(全文。用語表と §0 を先に)/ 探索段 4 の道具 `scripts/o3c_signal_explore4.py`(約定の読み込み・`at_or_before` / `at_or_after`・日クラスタ SE と naive SE と日等重み・対照 (c) の候補格子と置換なし・gzip の決定的な書き出し・`check_no_banned`・chunk の再開)とテスト、探索段 4 の委任先の報告 `EXPLORE4_DELEGATE_REPORT_2026-09-20.md` の (5)(同じ細部は同じに決める)、反証者レビュー `REFUTER_REVIEW4_2026-09-20.md`(致命 1〜6 = この段が避けるべきもの)、`docs/DATA.md` §2(清算行の重複: 全列一致で 1 件、472 日で 53,398 件)。

【データ】清算 `backtest_data/binance_cm_o3c_20260913/liquidationSnapshot/BTCUSD_PERP/`(全列一致の重複を 1 件に)、約定 `aggTrades/BTCUSD_PERP/*.zip`。対象日 = 探索段 4 と同じ 456 日(`backtest_data/o3c_signal_explore4_20260920/summary.json` の日の集合)。束の位置(H0 の診断だけ)は探索段 4 の `rows_gap60.csv.gz` の束(`cascade_id`・`start_ms`・`end_ms`)からプリントを 最初 / 途中 / 最後 に当てる(単発の束は「最初」と「最後」の両方に数え、その旨を列に)。**`paper_logs/` は開かない**(L-246)。

【量(設計の用語表。式はそこから写す)】プリント = 清算 1 件、`ts`(ms)。起点 p₀ = `at_or_after(ts, 300_000)`、直前 = `at_or_before(ts − 1, 300_000)`。m(T) = s × (直前 − p(`at_or_before(ts − T·1000, 300_000)`))/p(T 秒前) × 10⁴、T ∈ {10, 60}。k = s × (p₀ − 直前)/直前 × 10⁴。d = [ts − 300,000, ts) の同じ側のプリント数。r(h) = s × (p_h − p₀)/p₀ × 10⁴、h ∈ {1, 5, 10, 30, 60, 300, 900}。次の清算(結果)= (ts, ts + 60,000] / (ts, ts + 120,000] に同じ側のプリントがある割合。s = SELL: −1、BUY: +1。

【対照】(c') 設計 §4 をそのまま実装する(10 秒刻みの候補、区間 [t − T 秒, t + 900 秒] にどの側のプリントも無い、c_t は許容 300,000 ms で両端が引けるもの、m の 10 分位帯は 456 日の全プリントの m(負を含む、両端 ±∞)、|c_t − m_p| 最小・同値は早い t、置換なし、処理順は日の昇順 → ts の昇順、対照の起点 = `at_or_after(t, 300_000)`)。(b) 1 周目の対照 (i) 日集約版(束の表の一様行)の r(h): 日ごとの一様行の平均 × その日の各プリントの側の符号。対照には次の清算の列を置かない。

【層は t₀ 以前だけ】起点 t₀ = p₀ の約定の時刻(`ts` 以後の最初の約定)。H1〜H6 の層の列(m・k・d・側・想定元本・時刻帯・付け直せた属性)は t₀ 以前のデータだけで作る(p₀ 自身は含む)。テストで、t₀ より後の約定と清算を消した入力で層の列を作り直しても同じ値になることを測る。p₀ − ts の遅れの分位を H0 に出す。

【属性の付け直し(H6)】`bin_pct`・`dist_node_bp`・`implied_leverage` の 3 つを、1 周目 `scripts/o3c_reaction.py` の定義(W = 8 時間)で、**ts より前のデータだけ**からプリントごとに作れるかを確かめる。作れたものは H6 に 3 分位で足し、作れないものは報告に「本単位では測らない」と理由を書く。`implied_leverage` は metrics(建玉)を使う定義なら、ts より前の直近の値だけを使う。

【表(設計 §2・§6。行数は数え直す)】H0(自己点検: プリント数の前後、遅れの分位、m ≤ 0 の割合、k = 0 の割合、d の分布、束の位置 × h 7 の診断 21 行、対照 (c') の採否と m の帯別の割合、取れた / 取れなかった側の中央値、|c_t − m| の分位、日のプリント数と 1 件あたり取れた対照の数の相関、対照の変位の中央値)/ H1(h 7)/ H2(T 2 × m 10 × h 7 = 140。各行に k の平均と中央値)/ H3(k 4 群 × m 3 × h 3 = 36)/ H4(対照 (c')、T 2 × m 10 × h 3 = 60。k と次の清算の列は無し、c_t の平均と中央値)/ H5(d {0, 1〜2, 3 以上} × m 3 × h 3 = 27)/ H6(属性ごとの周辺の群: 側 2 + 想定元本 3 + 時刻帯 4 = 9 群 + 付け直せた属性の 3 分位ずつ。各群 × m 3 × h 3 = 81 以上)。**すべての平均に中央値・p25/p75・日クラスタ SE・naive SE・比の列、すべての割合に母数の列。**H2 の各行に k の平均と中央値。

【出力】`backtest_data/o3c_signal_explore5_20260920/`: `rows_prints.csv.gz`(全プリントと対照の行: ts・side・p0・m_10・m_60・k・d・bundle_pos・r 各 h・遅れ・次の清算の間隔(結果)・属性・対照の c_t と t)、`h0_selfcheck.csv`〜`h6_attributes.csv`、`tables.md`、`summary.json`(入力の MD5、日数、プリント数(重複除去の前後)、行数、**tables.md の数値セル数**、対照の採否、実行時刻、パラメータ)、`MD5SUMS`。chunk の中間ファイルは最後に消す。**判定語(差あり / 検出されず / 陽性 / 陰性 / 有意)を出力にもログにも書かない。**

【テスト】`tests/test_o3c_signal_explore5.py`: (1) プリント数 53,398(472 日、重複除去後)/ (2) H0 診断「最後」の r(60) が探索段 4 の gap 60 清算 全体 −6.800366 と小数 4 桁で一致(束の終端 = 最後のプリントの直後の約定なので同じ量)/ (3) 層の列が ts より後を使わない(上記)/ (4) 対照 (c') の帯・重なり・置換なし・処理順 / (5) 中央値の列がある / (6) `paper_logs/` を開かない(探索段 4 と同じモック)/ (7) 判定語なし / (8) 表の行数。既存スイート `PYTHONPATH=src python -m pytest` を全部通す(件数を報告。`-q` を手で足さない)。

【長時間処理】10 分を超えるなら chunk で再開できる形に(探索段 4 と同じ)。監視は回数上限つき。

【必須報告】(0) 表の行数の数え直し(設計 §6 は約 412 行。違えばどこか)と数値セル数 / (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) H0 診断「最後」が探索段 4 と一致した証拠 / (4) H0〜H6 の表(`tables.md`)/ (5) 設計に無い判断を全部 / (6) サニティ(層が未来を使わない、再実行の決定性、欠測の数、遅れの分布)/ (7) 限界。**読み(なぜ)は書かない。**各主張に 事実 / 推定 / 仮定 の印。

【制約】read-only・冪等・ネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名を書かない。Do not commit. Do not push。報告は `docs/PHASE2/O3C/SIGNAL/EXPLORE5_DELEGATE_REPORT_2026-09-20.md` に日本語で書く。最後の返答は報告のパスと、行数の数え直しの結果、テスト件数の 3 点だけでよい。
