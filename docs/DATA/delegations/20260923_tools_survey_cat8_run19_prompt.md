# 道具サーベイ 区分 8 — 19 回目の起動文(2026-09-26)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 19 回目**を担当します。この起動文は、それだけで読めるように書いてあります(ほかの回の起動文は読まなくてよい)。

**この回の終わり方**: §2 の全部を済ませてから返す。期限は無い。途中で止めない。予算で止めない(オーナーの決定 L-507「**案A**」)。どうしても決められない要素は `なし` を書かずに `未判別` のまま残し、何を試して何が足りなかったかを書く。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節 `## 区分8 — 19 回目の実行(2026-09-26)` と生ログ `docs/DATA/probes/20260923_tools_8_run19.log`。返答の中身は §7。

## 0. 最初に読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語(§3)と段(§4.1)は設計票の逐語で当てる。**
4. 報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 18 回目の実行(2026-09-25)` の節(35575 行目から)。**この回の節の小節・表の形は、この節を見本にする**(この節は台帳の検査 `check-elements` を 0 件で通っている)。**8-029 の出発点は 17 回目の節**(33568 行目から)の 8-029 の行と、生ログ `docs/DATA/probes/20260923_tools_8_run17.log`。
5. 18 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md` を全文(とくに §2「決まりが語ごとの決まりだった」と §7。E1a の述語の括弧の例の読み)。17 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md` の 8-029 の所。
6. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値。**

## 1. この回の範囲

- **8-029 `MetaTrader の Strategy Tester`** だけ。値の `未判別` = E1a・E3a・E5(E1b・E2・E3b・E4・E6 は 17 回目に `印`。台帳の値のまま)。17 回目に `未確認` だった §4.0 の項目も全部試す(§2.5)。
- **N**: 候補は MetaTrader の端末の機能 Strategy Tester なので、述語は Strategy Tester の機能に当て、N はそれを含む製品の公式の文書・ヘルプ・ブログの全部の頁にする(広いほう): MetaTrader 5 の端末のヘルプの全頁と、MQL5 の言語の文書(リファレンス)の全頁(どちらも販売元の公式の文書)。一覧はサイトの地図・目次から全件で取り、どこを N にしたか(URL と件数)を書く。全部を取れない・大きさの上限(§2.6)を超えるなら、その要素は `なし` を書かずに `未判別` にし、試した手段と量を書く。
- **この回に手を付けないもの**: ほかの 40 行(台帳の値のまま書く)。8-038 `NinjaTrader` は 20 回目に回す。

## 2. やること

### 2.1 述語と段(設計票の逐語。短く写す)

- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは数えない。**候補が自分自身の版・ランタイムを試験する仕組み(候補の開発のための試験・CI・`#[test]`・`assert!` など)は `印` に数えない**(オーナーの決定 L-516「**数えません**」)。
- **E1a の述語の括弧の例**: 「差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較」。どれも、同じものを出すはずの 2 つを照らし合わせるもの。戦略どうしの成績を並べて比べる機能は E1a に当たらない(18 回目の検収 §7)。
- **オーナーが決めた述語の読み**(問いに出さずに当てる): 候補が同梱・呼び出している別のライブラリの機能は候補の機能に数える / LLM への指示文に書かれた検出・検証の指示は数える / 重複をはじく処理は E2 の「重複の検出」に数える / 1 回だけの固定の学習・評価の分け方は E3b に数える(L-508「1〜4全て承認します」・L-509「b」)。乱数の種(`seed`)は段 5 の (ア) に当たらない。
- 設計票 §3 の述語の文を 1 語ずつ当てる。E2 は「時系列・約定・板・足・参照データについて…検出するか報告する機能」、E4 は「記録した市場データを時刻順に再生して…」、E6 は「E1a〜E5 の述語のどれにも当たらないもの」。E6 に置く前に、その機能が E1a〜E5 のどれかに当たらないかを書く(17 回目の AlgoNetwork の E6「independent verification」は E1a と重なりうるので `未判別` に戻った)。
- 段は §4.1 のとおり下から積み上げる。段 4 は「その要素の対象のすべて」を外から持ち込めることを言う逐語が要る(一部だけなら段 3)。段 5 は (ア)(イ) の両方の逐語が要る。**段は道具の側の記述だけで決め、当方が買った・動かしたかでは決めない。**各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引いて示す(段 4・5 は 8 字以上の逐語が要る。検査がこれを見る)。

### 2.2 問いの書き方

- 問いに出した行は、値も段も `未判別` にする(決めてから問いに出さない)。`### 判断に迷った点と問い` の番号付きの行には、全部 `[値・段の問い]` か `[それ以外の問い]` を付ける(付けないと検査が止める)。値・段を決めずにおく理由でない問いは `[それ以外の問い]`。

### 2.3 `印` を先に探す

各要素について、まず候補の文書・README・ソースから、述語に当たる機能を言う逐語を探し、`grep -n` や `sed -n` で生ログに印字して引く(`印` には全件の検索は要らない)。行を生ログに出すのは、読むと決めたファイルの範囲だけ。ソース全体への `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI`・`grep -rcI`)にする。`印` を探す段では `なし` を結論しない。

### 2.4 `なし` は全件の検索からだけ

- 一覧は `scripts/cat8_mklist.py` で作り、検索は `scripts/cat8_search.py` だけで打つ(どちらも `scripts/cat8_step.py` 経由。`--candidate` にその候補の台帳の番号。`--` のあとにパイプ・`;`・リダイレクトを付けない)。形は 17 回目の生ログの手をそのまま見本にする。
- 検索の語には、要素ごとに次の組の**語を全部**、この字のとおりに入れる: E1a = 突き合わせ・reconcil / cross-check・比較・compar / diff・差分・一致・mismatch、E1b = 突き合わせ・reconcil / 別実装・reference implementation・参照実装・cross-check / compar・比較・diff・差分、E2 = 欠損・missing・gap・欠け / 重複・duplicat / 順序・out of order・monoton・sort / 外れ値・outlier・anomal / 時刻のずれ・clock・skew・timestamp / 型・範囲・schema・range・valid、E3a = ルックアヘッド・look-ahead / survivorship・生存者 / leak・リーク・未来 / point-in-time・as-of・時点、E3b = ルックアヘッド・look-ahead / embargo / purge / point-in-time・as-of・時点、E6 = 検証・verif・validat / 品質・quality / test・assert・check。**打つ前に語に誤字が無いかを見る**(誤字の手を打つと、その手の当たりも全部判定が要る)。
- `なし` の根拠の欄に `一覧 N 件 / 読んだ M 件` と `当たり F ファイル / H 行` をこの字のとおりに書く。N と M は cat8_mklist.py の `listed=` の和。7 列目には、その要素の検索の手の行を全部書く(長い出力の中の離れた行を引くときは、引用した行も書く)。
- **当たった行は全部判定する**。やり方は 2 つ:
  - (甲) ファイルの表 `### 当たりの判定`: `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |`。**理由は、そのファイルの当たった行を読んで、当たった語と、その語がそこで何を指すかを書く。**
  - (乙) 決まりで分ける: `scripts/cat8_classify.py`(使い方は 17 回目の生ログの手と、道具の説明 `sed -n 1,60p scripts/cat8_classify.py`)。決まりは当たった行を読んでから書く。決まりに当たらない行は全部 `### 当たりの判定(行ごと)` に 1 行ずつ理由を書き、決まりは `### 決まりの一覧` に全部載せる。**2 つの表の形(検査が見る)**:
    - `### 決まりの一覧`: `| 検索の手の行 | 決まりの id | 正規表現 | 理由 | 取った行の数 | 取った行のあるファイルの数 |`。決まりを 1 つ 1 行で全部載せる。1 列目は `docs/DATA/probes/20260923_tools_8_run19.log:<検索の手の見出しの行>`(**cat8_search.py の手**の `--- ` の行の番号。cat8_classify.py の手の行ではない)。3〜6 列目は cat8_classify.py の出力の字と数のとおり(正規表現の中の `|` は `\|` と書く。書き忘れると列の数が 6 でなくなり検査が止める。取ったファイルの数は、その決まりの `  file` の行の数)。表と出力が合わない決まりがあると、その手は (乙) として数えられない。
    - `### 当たりの判定(行ごと)`: `| ファイルの道と行 | 当たった行の字 | その語がその行で何を指し、なぜ述語に当たらないか |`。1 列目は `unmatched` の行に出た `<道>:<行>` の字のとおり(道に `|` があれば `\|`)。
    - cat8_classify.py は `cat8_step.py` 経由で、`--keep` は出力の最後の行 `cat8_classify: complete ...` が生ログに残る大きさにする(例 `--keep 50000000`)。最後の行が残らなかった(切れた・落ちた・決まりが広すぎて止まった)分類の手が 1 つでもある検索の手は、(乙) として数えられない(そのときは (甲) の表に全部書くか、同じコマンドのまま cat8_search.py を打ち直して新しい検索の手で分ける)。決まりを見直して同じ検索の手に打ち直してよく、数えるのは最後の 1 回。一度でも `unmatched` に出た行は、打ち直した回で決まりに取られても行の表に理由つきで残す。
  - **決まりの正規表現には、検索した語の前後の字(その行の文脈)を入れる**。検索した語に 1 字足しただけの決まり(`.valid|valid.` など)や、語そのものを取る決まり(`compar\w+`・`differ\w*` など)は書かない(18 回目の決まりがこの形で、候補の機能を言う行まで取っていた)。その語が出る行が本当に全部同じ意味かは、全部を読まないと分からないので、読み切れないなら決まりにせず (甲) でファイルごとに読んで書く。リードは検収で決まりの一覧を読み、候補の機能が書かれうる頁・ファイルの当たりを自分で読んで確かめる。
  - **同じ理由を 2 行以上に書きたくなったら、それは決まりにする**(`cat8_classify.py` の規則として書き、生ログに残す)。行の表・ファイルの表には、同じ文を繰り返さない(語尾や語順を変えただけの同じ内容も、同じ理由とみなす。リードが検収で読む)。同じ文を何行・何ファイルにも当てた `なし` は受け取られない(12・13・14・16・17 回目に同じ理由で差し戻した)。決まりにすれば、まとめて判断したことが生ログに残り、リードと監査役が決まりと例の行を読める。
  - 検索した語が `def`・`class`・`fn`・`const`・`let` などに続く名前の中にある行は、道具が決まりに取らせず必ず 1 行ずつの判定に出す。その行は、定義された名前と、その行が何をするかを書いて判定する。
- 生ログはこの回のものだけを根拠にする(17 回目の検索の結果を引かない)。自己試験の根拠(L-516)しか無い要素は、全件の検索で、自己試験のほかに述語に当たる行が無いかを確かめる。自己試験の行も、決まり(例: Rust の `#[test]` の下の行・`assert!` の行を「候補自身の試験で、L-516 により数えない」とする決まり)で分けてよい。その決まりが本当に試験の行だけを取っているかは、決まりの見本の 10 行で確かめられるように書く。

### 2.5 §4.0 の表

8-029 について、委任文 §4.0 の表の項目を書く(17 回目の値を出発点にし、17 回目に `未確認` だった 11 項目は全部この回に試す)。「4軸4_向上」のように、道具を当方の環境に組み込まないと測れない項目は「サーベイの外(組み込みの作業が要る)」と書いてよい(18 回目の検収 §6 の 3)。`未確認` のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたものと、試すのに当方の鍵・口座・登録・購入が要るか外へ送信するもの(そう言う逐語を引く)だけ。「時間が掛かる」「時間内に到達しなかった」「優先しなかった」は理由にしない(17 回目はこの理由で 45 件を残した)。

### 2.6 大きさ・届かないとき・取り方

- 生ログと報告は GitHub に押し出すので、どちらも 90 MB 以内に保つ(`wc -c` を検索の手の前後に `cat8_step.py` で打つ)。
- この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書き、「存在しない」とは書かない。
- 取った本文は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下に置く(`/tmp` の直下に置かない)。
- **WebFetch は一次資料に使わない**(要約を返すため)。まず `curl` で生の HTML を取る。JavaScript で描く頁は `scripts/cat8_render.js` を `cat8_step.py` 経由で打つ(`NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run19.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`)。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない。**`印` の根拠にするのは、次の全部を満たすときだけ: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く / (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(この回の節の追記だけ。1〜18 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run19.log` の 2 つだけ。scratchpad 以外にファイルを作らない。生ログはすべて `scripts/cat8_step.py` で書く。
- 台帳には書かない。`scripts/` の下のファイルは書き換えない。区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。外部の道具を入れる場所は `.../scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

18 回目の節と同じ小節を置く(`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」)。検査が形を見る所:
- 「要素と段」の表は 7 列 `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目には `docs/DATA/probes/20260923_tools_8_run19.log:10` か `...:10-20` を空白で区切って並べるだけ(ほかの字を入れない)。値が `印` か `なし` の行は 7 列目を空にしない。引いた文「…」は 7 列目の行の手の出力に出ていること。
- この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし、値と段は台帳と同じにし、7 列目は空でよい。
- 知見の表の「根拠」の欄には、生ログの参照(上と同じ形)・URL・日付(`2026-09-26`)・「取得日」だけを書く(説明は「知見」の欄に書く)。**候補の一覧と「要素と段」の表(7 列)は、8-001〜8-041 の 41 行全部を載せる**(この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする)。変えた要素は候補の一覧の説明に全部書く。

## 5. 禁止

委任文 §10 のすべて。commit も push もしない。モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。

## 7. 返す前に

1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md` に、`docs/DATA/probes/20260923_tools_8_run1.log` から `docs/DATA/probes/20260923_tools_8_run19.log` までの 19 本の生ログを並べて打つ。誤検出は閉じずに行と理由を書く。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 19` を 0 件にする(最後の出力を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run19.log` を 0 件にする(同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0(同じ節に貼る)。
5. 返答には次をこの順で書く: 候補ごと・要素ごとの `印`・`なし`・`未判別`(`未判別` なら何が足りないか)/ この回の `なし` の全部の「一覧 N 件 / 読んだ M 件」と、判定に (甲)(乙) のどちらを使ったか / (乙) の手ごとの決まりの数と、1 行ずつ判定した行の数 / §4.0 で `未確認` のまま残した項目と理由 / 4 つの検査の最後の合計行 / 判断に迷った点と問い(決めずに) / 描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧(候補・要素・読めなかった頁)/ 生ログと報告の大きさ(最後の `wc -c`)。「区分は完了」とは書かない。
