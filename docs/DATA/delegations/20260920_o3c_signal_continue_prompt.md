あなたは本リポジトリの研究・実装エージェントです。「続く / 止まるの単位」の道具 `scripts/o3c_signal_continue.py` とテストを作り、走らせ、数表を出してください。**出力はすべて日本語**(コード内のコメントは日本語、識別子は英語でよい)。**判定語(陽性/陰性/有意/差あり/検出されず/支持/棄却)を書かない。`paper_logs/` を開かない。**

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_CONTINUE_DESIGN_2026-09-20.md`(全文。用語表・§3・§7 を先に)/ 探索段 5 の道具 `scripts/o3c_signal_explore5.py`(約定の読み込み・`at_or_before` / `at_or_after`・日クラスタ SE・行データの列。**再利用する**)/ 連鎖 30 本の抽出 `docs/DATA/probes/20260920_o3c_cascade_read.py`(成行の偏り・建玉の量 `oi_band_amounts`・直前 60 秒の値幅の計算を**そのまま流用**)/ Jev の呼び出し `scripts/jev/client.py` と下見 `docs/DATA/probes/20260920_jev_continue_call_probe.py`(criteria は辞書)/ `docs/DATA/probes/20260920_o3c_signal_explore5_refuter_verify.py`(日クラスタ SE の式)。

【データ】探索段 5 の行データ `backtest_data/o3c_signal_explore5_20260920/rows_prints.csv.gz`(kind == "print" の 52,000 件を母集団にする。ts・side・p0・t0_ms・notional・dist_node_bp・oi_covered・bundle_id はここから)、約定 `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/*.zip`、5 分値 `.../metrics/BTCUSD_PERP/`、資金調達率 `.../fundingRate/BTCUSD_PERP/*.zip`(月次、列 calc_time, funding_interval_hours, last_funding_rate)。

【前半 / 後半】456 日を日付順に 228 日ずつ。**切り値・規則・Jev の較正の帯は前半だけで作る。的中率・損益・較正の表は後半だけで出す。**前半で作った切り値は `summary.json` に書く。

【ラベル】続く(60 秒、主)= ts の後 (ts, ts + 60_000] に同じ側のプリントがある。30 秒・120 秒も列に持つ。「値段の続き」= t₀ から 60 秒以内に清算の向きへ p₀ から 5 bp 以上進む(結果の列、判断の対象ではない)。

【材料(設計 §3.1 の 1〜15、7 は無し)】**すべて ts 以前の約定・清算・5 分値・資金調達率だけで計算する。p₀ を使わない。**定義は設計の表のとおり。材料 8 は被覆外 = 欠測(別群)、被覆内の 0 = 0。材料 12 は直前 60 秒に同じ側のプリントが無ければ欠測。k のティック数と側は列に持つ(層にしない)。

【問い(設計 §2 Q0〜Q7。行数は数え直す。設計の見積もりは約 480 行)】
- Q0 自己点検: プリント数、ラベルの割合(前半 / 後半 × 側 × 30 / 60 / 120 秒)、材料ごとの欠測数、p₀ と h 後の遅れの分位、前半と後半の日数・清算数・価格帯。
- Q1 材料 × ラベル: 材料ごとに、続いた群 / 止まった群の分位(p10 / p25 / p50 / p75 / p90)と、**前半で切った 10 分位帯**ごとの続く割合(前半・後半を別の列に)。**値動きの平均は出さない。**
- Q2 規則: 前半で、材料の帯ごとの続く割合が基準率(前半の続くの割合)を超える帯を「続く」と言う規則を材料ごとに 1 本、上位 3 材料(前半の帯ごとの割合の広がりが大きい順)の組(3 材料の帯の続く割合の平均が基準率を超えるか)の規則を 1 本。後半で 的中率・適合率・再現率(基準率つき)、30 / 60 / 120 秒のラベルそれぞれ。
- Q3 Jev: **この委任では前半から層化無作為(側 × 材料 1 の 3 群)に 200 件だけ呼ぶ**(モデル `jev-1.13.0`、`JevClient(log_dir="data/jev/continue")`。state = 直前 60 秒の清算の列(相対秒・想定元本・側)・直前 60 秒の 1 秒刻みの値段(bp、ts 直前の約定 = 0、清算の向きが正)・材料の値(被覆外は null)。**p₀ と ts 以後のものは渡さない**。問い = noul 1 問、criteria は辞書(下見の文をそのまま使う)。2 件/秒で送り、429 の回数・遅延の分位・入力トークンの分位・返った確率の 10 帯の件数を報告する。**確率そのものは `data/jev/` にだけ書く(リポジトリに入れない)。後半には呼ばない。**後半への呼び出しは、リードが設計 §7 の分岐の基準で件数を決めてから、別の委任で行う。
- Q4 2 枝の損益: 判断 = 規則(材料ごと + 組)/ 全部続く / 全部止まる / 完全な判断(事後のラベル)× 入る時点 t₀ + 1 / 3 / 5 秒 × 出る時点 = 入ってから 30 / 60 / 300 秒 × 枝(続くと言った件 / 止まると言った件を**別々に**): 1 件あたり損益(bp、判断の向きが正、費用なし)の中央値・p25 / p75・負の割合・日等重み平均・日クラスタ SE・n・含む日数。**後半だけ。**
- Q5 日ごと: Q4 の主格子(t₀ + 1 秒 → 60 秒)の判断ごとに、日ごとの合計の分布(負の日の割合、上位 5 日の寄与の和 ÷ 全体)。
- Q6 完全な判断の上限: Q4 と同じ列。平均だけの行を作らない。
- Q7 清算の無い時刻に同じ規則を当てる: 設計 §4 の作り方(同じ日、材料 15 と 9 の帯(前半で切った 10 分位)を合わせて 1 件、10 秒刻みの候補、置換なし、前後 15 分にどの側の清算も無い)。規則ごとに「続く」と言った割合と、Q4 と同じ列の損益。取れた割合と帯ごとの取れなかった数。Jev は Q3 の 200 件と同数の 200 件だけ(前半)。

【出力】`backtest_data/o3c_signal_continue_20260920/`: `rows_continue.csv.gz`(全プリントの行: ts・side・t0・ラベル 3 本・値段の続き・材料 1〜15・k ティック・前半/後半・損益の格子 3 × 3)、`q0`〜`q7` の csv、`tables.md`(全表、見出しに Q 番号)、`summary.json`(行数の内訳、**`tables.md` の数値セル数**、前半の切り値、欠測、実行時刻、入力の MD5)、`MD5SUMS`。

【テスト】`tests/test_o3c_signal_continue.py`: (1) ラベルの計算(合成の清算列で 60 秒の境界の内外)/ (2) 前半・後半の分割が 228 / 228 日で切り値が前半だけから出る(後半の値を変えても切り値が変わらない)/ (3) 材料が未来を使わない(ts 以後の約定・清算を消した入力で材料 1〜15 が同じ値。p₀ を変えても同じ)/ (4) 2 枝の損益の符号(続くと言って上がれば正、止まると言って下がれば正)/ (5) 出る時点が入る時点からの保有期間 / (6) Q7 の候補が前後 15 分に清算を持たない / (7) `paper_logs/` を開かない(ソースに文字列が無い)/ (8) 判定語が `tables.md` に無い / (9) Jev の state に p₀ と ts 以後の値が入らない(合成データで)。

【長時間処理】10 分を超えるなら日ごとの chunk で再開できる形に(探索段 5 と同じ)。監視は回数上限つき。全スイート `PYTHONPATH=src python -m pytest`(`-q` を足さない)の末尾行を報告する。

【必須報告】`docs/PHASE2/O3C/SIGNAL/CONTINUE_DELEGATE_REPORT_2026-09-20.md` に: (0) 表の行数の数え直しと数値セル数 / (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) Q0 の自己点検 / (4) Q1〜Q7 の表(tables.md から) / (5) 設計に無い判断(全部。設計の中の食い違いを見つけたら止めて報告) / (6) サニティ(未来を使わない検査の結果、遅れ、欠測、再実行の決定性)/ (7) Jev の 200 件の下見(429 の回数・遅延・トークン・確率の帯の件数。確率そのものは書かない)/ (8) 限界(全部書く)/ (9) 作ったファイル / (10) テストの末尾行。

【制約】read-only(入力を書き換えない)・冪等・Jev 以外のネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名(Claude 等)を書かない。**Do not commit. Do not push.** 他のファイルを変更しない。
