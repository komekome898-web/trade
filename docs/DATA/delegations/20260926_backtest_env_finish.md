# 委任文: バックテスト環境の仕上げ(項目 4 を「信用できる測定器」として完成させ、旧エンジンを置き換える)

2026-09-26、オーナー決定 L-464(逐語): 「**進めろ ただし10時間は長すぎる。3時間以内に終わるように精度を落とさず全速力でやれ。完了した時か3時間後か重大な判断が必要になった時に呼べ**」。
親の委任文は `docs/DATA/delegations/20260925_backtest_env_prompt.md`(指紋 388d55cdeb32)。**この文書は親の委任文の上に重ねる仕上げの指示で、食い違えばこの文書が優先する**(§2 の批評家の射程と §1 の終わる条件)。それ以外(記録の規則・§4 の安全の規則・提出前の吟味・場面集の規則 1〜9)は親の委任文のとおり。

## 0. 対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 提案(L-462 の回)の終わる条件 (1)〜(4) を実行する | 「**進めろ**」 |
| 上限を 3 時間にし、役を並列にして縮める | 「**3時間以内に終わるように精度を落とさず全速力でやれ**」 |
| 終わる条件を緩めない(検査を薄くしない) | 「**精度を落とさず**」 |
| 呼ぶのは完了・3 時間後・重大な判断のときだけ | 「**完了した時か3時間後か重大な判断が必要になった時に呼べ**」 |
| 経緯(信用できる測定器)に沿う: 旧エンジンを置き換え、例外を作らない | L-453「**測定器の不具合やバックテストの条件付けに初歩的なミスが発覚し測定結果が信用できないことが判明**」、L-407「**新しいエンジンはそもそも旧エンジンの完全上位互換になっていないと意味がない**」 |

## 1. 終わる条件(全部満たしたら完了。上限 = 起動から 3 時間、2026-09-26 14:57 UTC まで。上限に達したら止めて、残りを逐語で報告する)

1. **正しさ・安全・統合の欠陥 0**: 第 2 周の批評家の [止める] のうち次の 6 件を直し、批評家の試験 `tests/bt/critic/item_4/` と場面集 `tests/bt/battery/item_4/` が全部通る。
   - i4-r2-02: 時間切れの足で範囲の逆指値を始値の時間切れより先に取る(場面集の規則 R-O1 と矛盾)。**直し方の決め**: R-O1 の順(始値の出来事が先)を正とし、R-H3 の「同じ足で先に見るのは逆指値だけ」は**始値より後の範囲の中での順**と読み直す(場面係が規則の文と正解を直す。作業者が spec と bar_rules を直す)。
   - i4-r2-03: 実データの出所の判定が再圧縮・展開・切り出しで破れる。**直し方の決め**: 出所は宣言でもファイルの中身の同一性でもなく、**行の中身**で決める(読んだ行の (時刻, 値) の並びが、この環境の市場データのフォルダのファイルから読んだ行の並びと一致するか = データ層の read を通した比較)。一致すれば実データ。合成は種つきの生成器からだけ作れる口にし、その種と生成器の版を実行記録に残す。
   - i4-r2-05: 研究の実行の事前登録の hash が形だけ。**直し方の決め**: 事前登録は**ファイル**として受け取り(項目 3 の `repro/runner.py` と同じ形)、その sha256 を計算して実行記録に残す。文字列の hash は受けない。
   - i4-r2-06: 統合の口が項目 2 の執行の模型を繋いでいない。**直し方の決め**: `pipeline.py` の約定・遅延・費用・口座を項目 2 の差し込み口(venue・LatencyModel・ScheduleCostModel・MarginAccount)に置き換え、自前の FirstObservedFill・_Latency・_Fee・NullAccount は消す。楽観・悲観の幅と遅延の分布と証拠金が実行記録に出ること。
   - i4-r2-08: maker の規則 2 つ(マスク False の合図・待っている建ての指値と同じ向きの合図)が規則の文に無い。**直し方の決め**: 場面係が規則の文(R-M・R-E)を足し、場面を置く。値の決めは「マスク False の合図は指値を置かない(取り逃しに数えない)」「同じ向きの合図は指値を置き直さない(古い指値を残す)」= 独立の参照の役の決めと同じ側にする(D2・D3 を消す)。作業者は spec をその文に合わせる(legacy は旧のまま)。
   - i4-r2-01: 参照が独立でない。→ 条件 2 で埋める。
2. **独立の参照が場面集の規則を全部持つ**: 参照実装の役(**核 `src/bot/bt/core/` と新エンジン `src/bot/bt/compat/`・`pipeline.py`・`bar_rules.py` を開かない**)が、場面集の規則の文 `tests/bt/battery/item_4/DEFINITIONS.md` の「足の模型の仕様」(R-T・R-C・R-A・R-M・R-P・R-X・R-W・R-H・R-E・R-S・R-O)だけから `src/bot/bt/reference/bar_sim.py` を書き直し(数量・合図の手仕舞い・率の水準・持ち越し・構造的な逆指値・スプレッドと滑りを含む)、作業者の格子の試験 `tests/bt/item_4/test_i4_engine_vs_independent_bar_sim.py` を全ての規則に広げて、新エンジン(spec)と**全升で一致**する。違いが出たら新エンジンの欠陥として作業者が直す(参照を新エンジンに合わせない)。作業者が書いた `bar_rules.py` は消し、場面集の I4-1 の場面は `bar_sim` を参照にする。
3. **旧エンジンの置き換え**: `src/bot/backtest/engine.py`・`metrics.py`・`walk_forward.py` の中身を互換の口(`src/bot/bt/compat/`)への委譲に置き換え、旧の golden(`tests/bt/compat/golden/`)と旧の試験 8 本(`tests/test_backtest*.py` ほか、`tests/bt/compat/test_old_tests_against_compat.py` が名指す 8 本)を**変えずに**回して全部通る。旧を import する研究のスクリプト 9 本ほかは触らない(L-406「2.案1」)。**例外は作らない**: 旧が計算していた不正な足(高値 < 安値・NaN)は、互換の口の手前に「旧と同じ数を出す層」を置いて旧と同じ結果を返す(核の契約は変えない)。置き換えたら全試験を回す(`PYTHONPATH=src python -m pytest -p no:cacheprovider -o tmp_path_retention_policy=none --basetemp=<私有>`)。1 つでも落ちたら戻して報告する。
4. **監査役(通過)の [止める] 0**(親の委任文の監査役の役のまま)。

## 2. 批評家の射程(この仕上げで固定。親の委任文の格付けの基準より優先)

- [止める] にするのは **正しさ(場面集の正解・規則の文・独立の参照と食い違う)・安全(実データ・事前登録・封印の規則を迂回できる)・統合(部品が繋がっていない、試験が落ちる)** の 3 種だけ。
- 検討表の言い回し・調査の相手の数(PineForge を含む)・委任文と台本の文の一致・報告の書き方は **[直す] 以下**にし、周を止めない。
- 批評家は核も読む(統合の周)。核の欠陥は target=実装、fix_files に核の path。
- 批評家の試験は `tests/bt/critic/item_4/` に足す(落ちる試験 + 対照)。

## 3. 役と並列(時間を縮めるため 3 役を同時に起こす。上限 3 時間の内訳: 直しの段 90 分 → 批評 30 分 → 置き換えと監査 40 分。余りは戻しに使う)

| 役 | 持ち物 | 読んでよい物 | してはいけないこと |
|---|---|---|---|
| 作業者 | `src/bot/bt/compat/`・`src/bot/bt/pipeline.py`・`src/bot/bt/reference/bar_rules.py`(消す)・`tests/bt/item_4/`・`tests/bt/compat/`・(条件 3 の段で)`src/bot/backtest/` | 全部 | 場面集を変えない。核の契約を変えるなら契約の版に書く。git commit / push をしない |
| 場面係 | `tests/bt/battery/item_4/`(規則の文・場面・正解・adapter・検討表) | 全部 | `src/` を変えない。場面だけの特別扱いをしない |
| 参照実装の役 | `src/bot/bt/reference/bar_sim.py`・`SPEC.md`・`tests/bt/item_4/reference/` | `DEFINITIONS.md` の規則の文・親の委任文 §2 の項目 4 の行・要件 `docs/DISCUSSIONS/2026-09-23_backtest_env/item_4/REQUIREMENTS.md` **だけ** | 核・新エンジン・bar_rules・作業者の試験を開かない(開いたと分かる成果物は [止める]) |
| 批評家 | `tests/bt/critic/item_4/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/item_4/round_3/CRITIC.md` | 全部 | §2 の射程の外を [止める] にしない |
| 監査役(通過) | 記録 | 全部 | — |

- 全ての役: pytest は私有の `--basetemp=<scratchpad>/bt/<役>_basetemp` と `-p no:cacheprovider -o tmp_path_retention_policy=none` で回す(同じ時刻に別の役の pytest が一時フォルダを消した実測、run 14)。一時ファイルは `/tmp/claude-0/-home-user-trade/17c10364-8019-48da-af27-038caa7b187a/scratchpad/bt/` の下に役の名を付けて置く。
- 全ての役: 直す前に「直す前に落ちる」記録を `docs/DISCUSSIONS/2026-09-23_backtest_env/item_4/round_3/materials/<役>/` に写す(親の委任文の記録の規則)。
- 全ての役: 返す前に親の委任文の「提出前の吟味」を行い、返り値に (a) 変えたファイル、(b) 試験のコマンドと末尾の行、(c) 満たせなかった条件と理由、(d) リードに聞くこと、を書く。**時間の上限に届きそうなら、途中でも (a)〜(d) を書いて返す**(黙って続けない)。
- 全ての役: オーナーの目に触れる文(記録・返り値)は日本語。コードとコメントは英語。モデル名を書かない。

## 4. 周回の数え方と止める条件

- 直しの段は 1 回。批評家の [止める](§2 の射程)が出たら**戻しを 1 回だけ**(作業者 / 場面係 / 参照実装の役のうち target の役に、指摘の逐語を渡して直させる)。2 回目の批評で [止める] が残れば止めて報告する(上限に達したのと同じ扱い)。
- 上限 3 時間に達したら、走っている役に (a)〜(d) を返させ、終わる条件 1〜4 のどれが残ったかを逐語で報告する。

## 5. 記録

- 監査役の出力・批評家の出力・各役の返り値は逐語で `docs/AUDITOR/VERDICTS/2026-09-26_backtest_env_finish.md` に写す(O-4)。
- 通過の報告には「持ち越し」「測っていない範囲」を逐語で載せる。
