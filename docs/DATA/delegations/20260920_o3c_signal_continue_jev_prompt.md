# 委任: 続く / 止まるの単位 — Jev を後半に呼ぶ(Q3・Q4・Q5・Q7 の Jev の行を埋める。2026-09-20)

## 0. 対応表(CLAUDE.md §0.1)
| やること | オーナーの原文の該当語(逐語) |
|---|---|
| Jev に「この清算は続くか」を判断させ、後半で的中率・較正・2 枝の損益を測る | 「**清算を検知→jevでこの清算は続くか判断→続くなら乗る、続かないなら逆張り**」(L-262)、「**こんなときこそjevの使い道やろ**」(L-238) |
| 件数 = 後半から層化無作為 5,000 件 | 設計 §7 の分岐の基準(事前): 200 件の下見で 429 は直接数えられず(クライアントが内部で再試行)、残り呼び出し数も API から取れない = 「429 が 0 回かつ残り 26,000 件以上」を確かめられない → **5,000 件の枝**。リードの決定(2026-09-20) |
| Q7 の Jev = 同じ 5,000 件のうち対照の候補が取れたプリントの候補に当てる | 設計 §2 Q7(「Q7 の Jev も後半だけ・別の委任」) |

**判定語を書かない。paper_logs/ を開かない。確率そのものはリポジトリに入るファイルに書かない(`data/jev/continue/` にだけ)。**

## 1. 読むもの
設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_CONTINUE_DESIGN_2026-09-20.md`(§2 Q3・Q4・Q7、§7)/ 道具 `scripts/o3c_signal_continue.py`(下見 200 件の呼び出しの実装をそのまま使う: state の作り方・noul の問い・criteria・2 件/秒)/ 行データ `backtest_data/o3c_signal_continue_20260920/rows_continue.csv.gz` / 委任先の報告 `docs/PHASE2/O3C/SIGNAL/CONTINUE_DELEGATE_REPORT_2026-09-20.md` の (7)。

## 2. やること
1. 後半(`half == "後半"`)のプリントから、側 × 材料 1 の 3 群(0 / 1〜2 / 3 以上)で層化無作為に 5,000 件(乱数の種 20260920。各層 ≈ 833)。**前半には呼ばない。**
2. 各件に下見と同じ state(ts 以前だけ。p₀ と ts 以後は渡さない)で Jev(`jev-1.13.0`)を 1 問(noul)呼ぶ。2 件/秒。`JevClient(log_dir="data/jev/continue")`。エラーは件数と種類を数えて報告(再試行後も失敗した件は欠測)。
3. 同じ 5,000 件のうち Q7 の対照候補(`q7_matched_print_id` で結ばれた `kind == q7_candidate` の行)があるものについて、候補の state(向き = 直前 10 秒の変位の符号、ts 相当 = 候補の時刻)で同じ問いを呼ぶ。
4. 表(**既存の csv・tables.md・summary.json は変更しない**。新しいディレクトリ `backtest_data/o3c_signal_continue_20260920/jev/` に書く):
   - `q3_jev.csv`: 閾値 0.5 の的中率・適合率・再現率(30 / 60 / 120 秒のラベル、基準率つき)、確率の 10 帯ごとの件数と実際の続く割合(較正)、規則(rule_combo・rule_1・rule_14)との一致率、層ごとの n。
   - `q4_jev.csv`: Jev の判断(閾値 0.5)× 入る 3 × 出る 3 × 枝 2(既存の Q4 と同じ列。主格子は全格子でよい = Jev 1 判断 × 9 格子 × 2 枝 = 18 行)。閾値 0.3 / 0.7 の行も併記(主格子だけ、4 行)。
   - `q5_jev.csv`: 主格子の日ごとの合計の分布(負の日の割合、上位 5 日の寄与)。
   - `q7_jev.csv`: 対照候補に対する「続く」と言った割合と、主格子の損益(枝 2、既存の Q7 と同じ列)。プリント側の同じ 5,000 件の値も並べる。
   - `tables_jev.md`(上の表の全文)、`summary_jev.json`(件数・層・エラー・遅延の分位・入力トークンの分位・確率の帯の件数。**確率そのものは書かない**)、`MD5SUMS`。
5. テスト: `tests/test_o3c_signal_continue_jev.py` に (a) 層化の件数 (b) state に p₀ と ts 以後の値が入らない(合成) (c) 閾値の判断の符号 (d) 前半のプリントが入らない、の 4 件。全スイートの末尾行。
6. 報告 `docs/PHASE2/O3C/SIGNAL/CONTINUE_JEV_DELEGATE_REPORT_2026-09-20.md`: (0) 対応表の写し (1) 実行したコマンド (2) 表 (3) 設計に無い判断(全部) (4) サニティ(エラー・遅延・トークン・欠測) (5) 限界 (6) 作ったファイル (7) テストの末尾行。**日本語。コミットしない。他のファイルを変更しない。**
