あなたは本リポジトリの研究・実装エージェントです。「材料の選定」の道具 `scripts/o3c_signal_materials.py` に候補 2 本(R1・F5)を足し、全 52,000 プリントで計算して、前半 228 日の 2 組の表(組 A 19 本 + 組 B 24 本 = 43 行)を作り直してください。**出力はすべて日本語。判定語(陽性/陰性/有意/差あり/検出されず/支持/棄却)を書かない。`paper_logs/` を開かない。後半の表は作らない(後半の候補の値は行データに出すだけ)。境目は置かない(リードが置く)。**

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_MATERIALS_DESIGN_2026-09-20.md`(全文。**§5.4 が足す 2 本の定義**、§5.2 が暫定の境目 — 境目には触らない)/ 前回の委任文 `docs/DATA/delegations/20260920_o3c_signal_materials_prompt.md`(規約はすべて同じ)/ 前回の報告 `docs/PHASE2/O3C/SIGNAL/MATERIALS_DELEGATE_REPORT_2026-09-20.md`(特に (5) 設計に無い判断 15 件 — 同じ判断をそのまま引き継ぐ)/ 道具 `scripts/o3c_signal_materials.py` と試験 `tests/test_o3c_signal_materials.py`。

【足す候補 2 本(設計 §5.4 の定義どおり)】
- R1(吸収、全位置): `[ts − 60,000 ms, ts − 1 ms]` の約定の価格 p について `max sign × (p − p_pre) / p_pre × 1e4`(bp)。`p_pre = at_or_before(ts − 1)`、sign は清算の向き(BUY 清算 = 上が正)。約定が無ければ欠測。**p₀ を使わない。**
- F5(燃料、組 B だけ): `[ts − 10,000 ms, ts − 1 ms]` の同じ側の清算(自分を含まない)の想定元本の合計。同じ側の清算が無ければ 0。1 件目(組 A)では定義上 0 なので、**組 A の表には入れない**(F3 と同じ扱い。行データには全行の値を出す)。

【やり方】既存の 22 本は再計算せず `rows_materials.csv.gz` の列をそのまま使ってよい(2 本だけ足して突き合わせる)。ただし **R1 は約定の読み込みが要る**ので、前回と同じ chunk(日ごと、再開可能)で回す。出力先は同じ `backtest_data/o3c_signal_materials_20260920/`: `rows_materials.csv.gz` を **24 本の列に更新**(`cand_R1`・`cand_F5` を追加。既存の列は値を変えない — 変わっていないことを MD5 ではなく列ごとの完全一致で確かめて報告)、`screen_A.csv`(19 行)・`screen_B.csv`(24 行)・`tables.md`・`summary.json`・`MD5SUMS` を作り直す。前回の表は `screen_A_v1.csv`・`screen_B_v1.csv`・`tables_v1.md` として残す(置き直しの前後を残すため。設計 §3 の 4)。

【実物読み】前回と同じ 120 件(種 20260920、同じ抽出)について、候補の値の表に R1・F5 の行を足した版を `docs/DATA/probes/20260920_o3c_materials_read.md` に**上書き**する(清算の列・値段の列はそのまま)。120 件の並びと print_id が前回と一致することを報告に書く。

【テスト】`tests/test_o3c_signal_materials.py` に足す: (8) R1・F5 が ts 以後を使わない(既存の (1) と同じ 2 通り = ts 以後の価格を変えた入力・**p₀ に当たる約定の価格を書き換えた入力**で同じ値)/ (9) R1 の手計算(合成の約定列で最大の bp が一致、戻り無しのとき ≤ 0、約定無しで欠測)/ (10) F5 の手計算(10 秒の窓の境界 `ts − 10,000` を含み `ts` を含まない、自分を含まない、反対側を数えない)/ (11) 組 A の表に F5 が無く、組 B にある。全スイート `PYTHONPATH=src python -m pytest`(`-q` を足さない)の末尾行。

【必須報告】`docs/PHASE2/O3C/SIGNAL/MATERIALS2_DELEGATE_REPORT_2026-09-20.md`: (0) 行数と数値セル数 / (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) 自己点検(R1・F5 の欠測と 0 の割合、既存 22 列の完全一致、120 件の一致)/ (4) 組 A・組 B の表(並べ替え済み、43 行)/ (5) 設計に無い判断(全部)/ (6) サニティ / (7) 限界 / (8) 作ったファイル / (9) テストの末尾行。

【制約】read-only(入力の約定・5 分値・前段の行データを書き換えない)・冪等・ネットワークなし。既定値を変えるなら列挙して理由を書く。モデル名を書かない。**Do not commit. Do not push.** 上に書いたファイル以外を変更しない。
