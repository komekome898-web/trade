あなたは本リポジトリの研究・実装エージェントです。「方策の模擬」の道具 `scripts/o3c_signal_policy.py` とテストを作り、走らせ、数表を出してください。**出力はすべて日本語。判定語(陽性/陰性/有意/差あり/検出されず/支持/棄却)を書かない。`paper_logs/` を開かない。確率そのものはリポジトリに入るファイルに書かない(`data/jev/policy/` にだけ)。**

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_POLICY_DESIGN_2026-09-20.md`(全文。用語表・§2・§7 を先に)/ 前段の道具 `scripts/o3c_signal_continue.py`(材料・state・Jev の問い・`at_or_after`)と `scripts/o3c_signal_continue_jev.py`(冪等な呼び出しと再開。**そのまま流用**)/ 前段の行データ `backtest_data/o3c_signal_continue_20260920/rows_continue.csv.gz`(材料・ts・p0・half)/ 探索段 5 の `backtest_data/o3c_signal_explore5_20260920/rows_prints.csv.gz`(`bundle_id`・`bundle_pos`・`bundle_pos_single`)/ 5,000 件の較正 `backtest_data/o3c_signal_continue_20260920/jev/q3_jev.csv`(確率の 10 帯 × 実際の続く割合)と選定 `.../jev/selection_manifest.json`(除外する 5,000 件の print_id)。

【段 1: 閾値】設計 §2 Q1 の規則(L-275: 帯の実際の続く割合 ± 2 SE(二項)が基準率(= 5,000 件の較正サンプル自身の続く割合。数え直して `summary.json` に書く)を含む帯 = わからない、幅ごと上 = 続く、下 = 止まる、件数 0 の帯 = わからない)で帯を決める。決めた帯を `summary.json` と報告に書く。

【段 2: 連鎖の抜き出し】後半の連鎖(`bundle_id`、gap 60、同じ側)のうち、5,000 件のプリントを 1 件でも含む連鎖を除き、残りから 2,000 本を無作為に(種 20260920)。内訳(単発 / 2 件 / 3 件以上)を報告。

【段 3: Jev】抜いた 2,000 本の全プリントに、前段と同じ state(ts 以前だけ、p₀ と ts 以後は渡さない。**材料 4 は state から外す** = 反証者 6 致命 5)・同じ問い(noul、同じ criteria)で呼ぶ(`jev-1.13.0`、2 件/秒、`JevClient(log_dir="data/jev/policy")`、冪等な再開、バックグラウンドで回し回数上限つきで監視)。エラーは件数を数え、再試行後も失敗した件は「わからない」扱い(件数を報告)。

【段 4: 方策の模擬】設計の用語表の状態機械をそのまま実装する。方策 = 完全な判断 / Jev(段 1 の帯)/ 規則(材料 1 ≥ 1 → 続く、0 → 止まる。「わからない」は無い)/ 基準 (i) 最初で順張り → 終わりまで / (ii) 最初で逆張り → 終わりまで。型 A(合わない → 決済して次のプリントまで建玉なし。同じプリントで新規は開かない)と型 B(合わない → 決済と同時に反対向きの新規 = 新しい判断に合う側)。9 通りの行動は設計の用語表のとおり。規則は 2 値(わからない無し)。遅れ 1 秒(主)/ 3 秒。価格は「時刻 + 遅れ」以後の最初の約定(`at_or_after`、穴 300 秒。引けなければその連鎖は欠測 = 件数を報告)。出口 = 最後のプリント + 60 秒。損益 = 連鎖 1 本の全建玉の bp の合計(建玉の向きが正、費用なし)。**判断には ts 以前の情報だけを使い、連鎖の終わりの時刻は出口にだけ使う。**

【表(設計 §2 Q0〜Q6。行数は数え直す。設計の見積もりは 132 行)】Q2 の列: 中央値・p25 / p75・負の割合・日等重み平均・日クラスタ SE(連鎖の日 = 最初のプリントの日)・n・建玉の回数の平均・保有秒の中央値・「入らなかった」割合。「入らなかった」連鎖を 0 として含める表と含めない表の両方。Q5 の位置は探索段 5 の `bundle_pos`(事後の診断)。Q6 は乱数(種 20260920)で 10 本を抜き、各プリントの判断・行動・約定価格・損益を 1 行ずつ。

【出力】`backtest_data/o3c_signal_policy_20260920/`: `cascades.csv.gz`(2,000 本 × 方策ごとの損益・回数・保有秒)、`prints_policy.csv.gz`(全プリントの判断 3 択(確率は書かない)・行動・価格)、`q0`〜`q6` の csv、`tables.md`、`summary.json`(行数の内訳、数値セル数、閾値の帯、除外した連鎖の数、Jev の呼び出し数・エラー・遅延とトークンの分位、入力の MD5)、`MD5SUMS`。

【テスト】`tests/test_o3c_signal_policy.py`: (1) 状態機械: 建玉なし / 順張り / 逆張り × 止まる / 続く / わからない の 9 通りの行動が設計の表どおり(型 A と B)/ (2) 出口の時刻 = 最後のプリント + 60 秒 / (3) 価格が ts + 遅れ以後 / (4) 閾値の規則(合成の較正表 3 通り)/ (5) 5,000 件を含む連鎖が除外される / (6) 完全な判断が連鎖の道筋で計算される(合成の 3 件の連鎖で手計算と一致)/ (7) state に p₀ と ts 以後が入らない / (8) `paper_logs/` を開かない / (9) 判定語が `tables.md` に無い。全スイート `PYTHONPATH=src python -m pytest`(`-q` を足さない)の末尾行。

【必須報告】`docs/PHASE2/O3C/SIGNAL/POLICY_DELEGATE_REPORT_2026-09-20.md`: (0) 行数の数え直しと数値セル数 / (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) Q0 / (4) Q1〜Q6 の表 / (5) 設計に無い判断(全部。設計の中の食い違いを見つけたら止めて報告)/ (6) サニティ(未来を使わない検査、遅れ、欠測、再実行の決定性)/ (7) 限界(全部)/ (8) 作ったファイル / (9) テストの末尾行。

【制約】read-only(入力を書き換えない)・冪等・Jev 以外のネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名を書かない。**Do not commit. Do not push.** 他のファイルを変更しない。
