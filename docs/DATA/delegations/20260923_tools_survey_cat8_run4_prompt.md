# 道具サーベイ 区分 8 — 4 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 4 回目**を担当します。

**この回の終わり方**: §2 の 1〜12 の**全部を済ませてから**返す。期限は無い。**途中で止めない**(オーナー逐語 L-506「**期限を指示した覚えがありません。中途半端で止まらないでください。**」)。追補 §5 は L-506 を受けて「予算で途中で止めない」に改めた(監査を通して指紋を記録した版)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 4 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run4.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `490f328717a8`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**この回もそのまま当てる。ただし `--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6(線引き・書き換えの範囲・禁止・申し送り)。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての要素に当てる。**
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run3.md` を全文(とくに §5、§7 問いへの答え)。
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。

## 1. この回の位置づけ

1〜3 回目は、リードが置いた 20 分の期限で、どの回も割り当ての途中で止まっていた(期限はオーナーの指示ではなかった = L-506)。この回は期限を外し、**残りの候補 12 件(設計票 §2 の「残り」= 状態が `未着手`・`判別に一次資料が要る`・`浅い` の行。8-003・8-006・8-007・8-008・8-009〜8-016)を、全部深掘りし終えてから返す。**辿る一覧と、3 回目の起動文の記録に回した 4 件のうち 8-007 以外に関わるものは、次の回(5 回目)に割り当てる。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

1. 8-009 `nicferrari/backtester`: 3 回目に 8 列は確定した。§4.0 の表(語彙の全項目)を書き、E2 は検索の語に要素の名前そのもの(「データ品質」と `data quality`)を足して当て直す(3 回目の検収 §5)。
2. 8-010 `arXiv:2603.20319`: 論文の**本文**(PDF か HTML)を全部読む。道具ではないので、§4.0 の項目で当たらないもの(版・週 DL 数など)は `該当なし(論文)`。論文が示す実装・コード・データの所在があれば書き、その所在の一次資料も開く。E1a と E6 は 3 回目の検収 §7 の 2 のとおり、要素ごとの原文で決める。
3. 8-011 `arXiv:2512.12924`: 同上。
4. 8-012 `VectorBT`: README に加えて公式の文書のサイトの頁を読む(`なし` と書くための条件 (a))。
5. 8-013 `rusty-bot`: 3 回目の検収 §7 の 6 のとおり、名前で検索して(GitHub・crates.io など)見つかった候補を、X の投稿が指すものと同じか確かめてから書く。確かめられなければ `判別に一次資料が要る` のまま、試した手段を全部書く。
6. 8-014 `Fincept Terminal`: README に加えて、README が示す別の文書(マニュアルなど)を開く。
7. 8-015 `TradingView のリプレイ機能`: 公式の文書(ヘルプの頁など)を開く。登録が要る部分は委任文 §5-6 の手順(登録なしで取れるものを全部取り、オーナーが辿れる手順を書く)。
8. 8-016 `Exactpro の reconciliation testing`: 公式の文書(製品の頁・文書)を開く。同上。
9. 8-003 `prediction-market-backtester`: 1 回目の報告の `未判別` の列を、`なし` と書くための条件で当て直す(ファイル一覧を取り、文書を全部読む)。
10. 8-006 `freqtrade`: 同上。公式の文書のサイトの頁も読む。
11. 8-007 `backtrex`: 同上。README が示す比較のページ(3 回目の起動文の記録の表の 1 件)も開く。
12. 8-008 `FX Replay`: 同上。公式の文書・ヘルプの頁を開く。登録が要る部分は委任文 §5-6 の手順。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 4 回目の実行(2026-09-24)` の追記だけ**。1〜3 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run4.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` と `### 辿る一覧から出た名前` を除いたもの全部。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。候補の一覧と「要素と段」の表には 8-001〜8-016 の全行を**今の値で**載せる(8-001〜8-008 は台帳の値のまま写す)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから `cat8_ledger.py check` の参考の 1 行を取る)。

## 7. 返す前に(4 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 4` を 0 件にする(最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run4.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: 12 件の各候補の状態(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ 5 回目に回したもの(辿る一覧と、`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run4_prompt.md` の「5 回目に回すもの」の表のうち 8-007 の比較のページ以外)にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに)**/ 生ログの行数。
6. 「区分は完了」とは書かない。
