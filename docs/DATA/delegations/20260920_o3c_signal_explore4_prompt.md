あなたは本リポジトリの研究・実装エージェントです。探索段 4 の道具 `scripts/o3c_signal_explore4.py` とテストを作り、走らせ、数表を出してください。設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE4_DESIGN_2026-09-20.md`(監査済み)から逸脱しないこと。設計文書とこの委任文が食い違うときは設計文書に従い、食い違いを報告に書く。

【対応表(CLAUDE.md §0.1)】設計 §0.1 の表を報告の冒頭にそのまま写す。右列が空になる作業が出たらそこで止めて報告する(勝手に決めない)。

【読了必須】設計(全文。用語表と §0 を先に)/ 探索段 3 の道具 `scripts/o3c_signal_explore3.py`(1 周目の表と約定の読み込み・`at_or_before` / `at_or_after`・日クラスタ SE と naive SE と日等重み・対照 (i) の日集約・対照 (a) の候補格子と置換なしの実装・gzip の決定的な書き出し・`check_no_banned`・chunk の再開)と `tests/test_o3c_signal_explore3.py` / 探索段 3 の報告 `EXPLORE3_DELEGATE_REPORT_2026-09-20.md` の (5) 設計に無い判断(同じ細部は同じに決める)/ 反証者レビュー `REFUTER_REVIEW3_2026-09-20.md`(致命 1〜6 = この段が避けるべきもの)。

【データ】探索段 3 と同じ: 1 周目の表 `backtest_data/o3c_reaction_20260918_full/{gap60_w8,gap30_w8,gap180_w8}/table.csv`(束と対照 (i))、約定 `backtest_data/binance_cm_o3c_20260913/aggTrades/BTCUSD_PERP/*.zip`、bitFlyer 1 分足(探索段 3 と同じ経路)、Binance の 1 分足は約定から同じ格子で作る(G7)。**`paper_logs/` は開かない**(L-246)。

【量(設計の用語表。式はそこから写す)】基準 p_pre / 終端 p_end / 掃き sweep_bp は探索段 3 と同じ。前の値動き m(T) = s × (p_pre − p(`at_or_before(start_ms − T·1000, 300_000)`))/p(T 秒前) × 10⁴、T ∈ {60, 300}(主)と {10, 30, 60, 300, 900}(G0 の下見)。後の逆行 r_end(h) = s × (p_h − p_end)/p_end × 10⁴、h ∈ {1, 5, 10, 30, 60, 300, 900} 秒。戻り率 g(T, h) = (p_end − p_h)/(p_end − p_T)、分母 ≤ 0 は NaN(数を報告)。続伸 (i) = 次の束(同じ gap・同じ日)の start_ms − end_ms ≤ 120 秒、(ii) = r_end(h) > 0。`prev_bundle_gap_s` / `next_bundle_gap_s` は探索段 3 と同じ(同じ日の中)。日の束数 = その日の gap 60 の束の数。

【対照】(c) 前の値動き合わせ: 設計 §4 の手順をそのまま実装する(10 秒刻みの候補、区間 [t − T 秒, t + 900 秒] が束と重ならない、c_t は許容 300,000 ms で両端が引けるもの、m の 10 分位帯(全束の m、負を含む、両端 ±∞)、|c_t − m_b| 最小・同値は早い t、置換なし、処理順は日の昇順 → start_ms の昇順)。取れなければ「対照 (c) なし」。(b) 探索段 3 と同じ対照 (i) 日集約版の r_end。対照 (c) では続伸 (i) を出さない(構造上 0)。

【表(設計 §2・§6。行数は数え直す)】G0 自己点検(遅れの分位・m の分布と負の割合・T の下見 5 行(m の p10/p50/p90、m ≤ 0 の割合、m と r_end(60) の相関を束の行と日ごとの平均の 2 通り)・g の分母の分位と 1 bp 未満の割合・対照 (c) の |c_t − m_b| の分位、取れなかった束の数と側と m の 10 分位別の割合、取れた / 取れなかった束の m・掃き・r_end(60) の中央値)/ G1(T 2 × m 10 分位 × h 3、gap 60。gap 30・180 は T = 60・h = 60 だけ)/ G2(掃き 3 分位(切り値 0.2009 / 8.0760)× m 3 分位 × T 2 × h 3)/ G3(m 3 分位 × T 2 × h 1/5/10/30/60)/ G4(対照 (c)、G1 の gap 60 と同じ形)/ G5(日の束数 3 分位 × m 3 分位 × h 3、T = 60)/ G6(5 属性 14 群 × m 3 分位 × h 3、T = 60。切り値は探索段 2 の E4 と同じ)/ G7(m 3 分位 × T 2 × 会場 2 × h {300, 900}、同じ 1 分格子)。**すべての割合の列に母数の列を添える。**g の中央値と 3 区分は「分母 > 0」と「分母 ≥ 1 bp」の 2 通り。SE は日クラスタ + naive + 日等重み、日クラスタ / naive の比の列。「前の束から T + 60 秒以上」の部分集合を G1 に併記。未来で条件付けした部分集合は作らない。

【出力】`backtest_data/o3c_signal_explore4_20260920/`: `rows_gap{60,30,180}.csv.gz`(束と対照の全行。m_60 / m_300 / m_10 / m_30 / m_900 / sweep_bp / r_end 各 h / g_60_* / g_300_* / prev・next_bundle_gap_s / 日の束数 / 対照 (c) の c_t と t / 遅れの列)、`g0_selfcheck.csv`〜`g7_bitflyer.csv`、`tables.md`、`summary.json`(入力の MD5、日数、行数、NaN の数、対照 (c) の採否、実行時刻、パラメータ)、`MD5SUMS`。chunk の中間ファイルは最後に消す。**判定語(差あり / 検出されず / 陽性 / 陰性 / 有意)を出力にもログにも書かない。**

【テスト】`tests/test_o3c_signal_explore4.py`: (1) r_end(60) の gap 60 清算 全体の平均が探索段 3 の −6.800366(`f2_retrace.csv`)と小数 4 桁で一致 / (2) T 秒前の約定の時刻 ≤ start_ms − T·1000 / (3) g の 3 区分の和が 1(分母 > 0 で)/ (4) 対照 (c) の c_t が相手の m の帯に入り、区間が束と重ならず、置換なし、処理順どおり / (5) `paper_logs/` を開かない(探索段 3 と同じモック)/ (6) 判定語なし / (7) 表の行数。既存スイート `PYTHONPATH=src python -m pytest` を全部通す(件数を報告。`-q` を手で足さない)。

【長時間処理】10 分を超えるなら chunk で再開できる形に(探索段 3 と同じ)。監視は回数上限つき。

【必須報告】(0) 表の行数の数え直し(設計 §6 は約 446 行。違えばどこか)/ (1) 設計 §0.1 の表 / (2) 実行したコマンドと出力の要所 / (3) r_end(60) が探索段 3 と一致した証拠 / (4) G0〜G7 の表(`tables.md`)/ (5) 設計に無い判断を全部 / (6) サニティ(先読み 0、再実行の決定性、欠測の数、m の窓の実経過)/ (7) 限界。**読み(なぜ)は書かない。**各主張に 事実 / 推定 / 仮定 の印。

【制約】read-only・冪等・ネットワークなし。既定値を変えるなら列挙して理由を書く。最小 diff。モデル名を書かない。Do not commit. Do not push。報告は `docs/PHASE2/O3C/SIGNAL/EXPLORE4_DELEGATE_REPORT_2026-09-20.md` に日本語で書く。最後の返答は報告のパスと、行数の数え直しの結果、テスト件数の 3 点だけでよい。
