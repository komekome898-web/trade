# 道具サーベイ 区分 8 — 6 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 6 回目**を担当します。

**この回の終わり方**: §2 の 1〜6 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 6 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run6.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5.md` を全文(とくに §2 の `なし` の数え直しの表と、末尾の「監査 30 回目」の指摘とリードの処置)。**5 回目の `なし` 11 件のうち 10 件が、「一覧の件数 N」を検索した範囲で数えていたために差し戻された。同じ誤りをしない。**
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。**この回の出発点は台帳の値**(5 回目の報告の表の値ではない。検収と監査 30 回目で台帳を直した)。

## 1. この回の位置づけ

台帳の残り(設計票 §2 の「残り」= 状態が `未着手`・`判別に一次資料が要る`・`浅い` の行)は 6 行: 8-003・8-006・8-007・8-008・8-013・8-016。この回はこの 6 行を全部、`深掘り` の条件まで進める。

**この回に回さないもの(7 回目以降。この回で手を付けない)**: `深掘り` の行(8-001・8-002・8-009・8-010・8-011・8-012・8-014・8-015)と 8-004・8-005 の `なし` の当て直し / 段が `未判別` の行の当て直し(この回の 6 行を除く)/ 辿る一覧 / 成熟度の枠組みの原典 / `akurkar07/OrderBook` の E6。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

**`なし` を書くときの手順(この回の全部の `なし` に当てる。4 回目の差し戻しの対策)**:
- 候補の一次資料の**ファイル一覧・頁の一覧を全件で取る**。`cat8_step.py` の既定は出力を 3,000 字で切るので、一覧を取る手には `--keep 200000` を付ける。一覧は生ログに全件が残るようにする。
- **N は、その候補の一次資料の一覧を取った手の出力の件数そのもの**(リポジトリならファイル一覧の全件、サイトなら文書・ヘルプ・ブログの全部の頁の一覧)。検索した範囲の件数ではない(5 回目の差し戻し: `*.py` だけ・`.rs` だけ・ヘルプの 1 分類だけを N にしていた)。M は、その N 件のうち実際に読んだ・検索の対象に入れた件数(検索の手の対象の指定 `--include` などがあれば、それで当たる件数を数える)。除外してよいのは、文字を検索しても意味の無いもの(画像・ロックファイル・バイナリ)だけで、除外は**ファイル名と理由を全部**書く(テスト用のデータ・設定ファイル・スクリプトは除外しない)。
- `なし` の行の根拠の欄に、**`一覧 N 件 / 読んだ M 件`** の形(この字面のまま)と、一覧を取った生ログの行(`20260923_tools_8_run6.log:<行>`)を書く。**N と M が違うなら `なし` と書かない**(`未判別` のまま、読み残しの名前を書く)。§7 の 2 の検査がこの形・N = M(数として)・生ログがこの回のもので行が実在することを機械で見る(この回に触らない行の根拠は `台帳の値のまま(<何>回目の節)` と書けば検査の対象外)。一覧から外すもの(画像・生成物など、文書でもソースでもないもの)は、外した名前と理由を書く。
- 「主要な」「全体で」のような語で範囲をぼかさない。
- **WebFetch は一次資料に使わない**(要約を返す道具なので、委任文 §4.1 の「検索結果の要約」に当たる)。まず `curl` で生の HTML を取る。JavaScript で描く頁で本文が HTML に無いときは、`scripts/cat8_render.js`(この環境の Chromium で頁を開いて本文をそのまま出す。`playwright install` は打たない)を `cat8_step.py` 経由で打つ: `NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run6.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`(`rc` と秒が数字で入る。ブラウザーは自分では通信せず、道具の Node の側が証明書を通常どおり検証して取ったものを頁に渡す。リードが 2026-09-24 に `https://fxreplay.com` で HTTP 200 と本文、証明書の誤りのある頁(名前の不一致・自己署名・期限切れ)で取得が止まることを確かめた)。本文が取れずに止まったら、その頁は `未判別` のまま、止まった理由を書く。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない**(道具は本文の欠けを全部は数えられない。監査 25〜27 回目で、何も数えられないまま本文が欠ける型が 3 つ見つかった)。`なし` は、生の HTML・文書のファイル・ソースなど、全部を取ったと件数で示せる一次資料で書く。描画でしか読めない頁しか無い要素は `未判別` のまま、読めなかった頁の名前を書く。§7 の 2 の検査は、`なし` の根拠に引いた生ログの手が `cat8_render.js` なら止める。出力の `INCOMPLETE` の行と最後の `INCOMPLETE 合計 <N> 件` は、`印` の根拠に使うときに一緒に写す。**描画した頁を `印` の根拠にするときの条件(全部を満たす)**: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く(語だけを拾わない)/ (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

1. 8-003 `prediction-market-backtester`: E3a・E3b・E6 を、上の手順で当て直す(ファイル一覧 99 件が N。5 回目は検索の手が `*.py`・`*.md`・`*.yaml` だけで 88 件)。E5 の段を、設計票 §4.1 の (ア)(イ) の逐語に当たる原文で当て直す(`DATA_SHA256` を (ア) と読むかは述語の読みの問いなので、逐語に当たると言えなければ段は `未判別` のまま問いに出す)。
2. 8-006 `freqtrade`: E3b を当て直す。`startup_candle_count` は指標の暖機(過去の足の不足)の仕組みで、E3b の述語(その時点で知り得ない情報が入らないようにする)に当たると書いた原文が無ければ根拠にしない。FreqAI の `shuffle=False` などは、原文が時系列の学習と検証の間の漏れを防ぐと書いているかで決める。`なし` にするなら `docs/` の全ファイルと README が N。
3. 8-007 `backtrex`: E3a を当て直す。N は Documentation の全頁と、ブログの全記事(発見の出典のブログを含む)。
4. 8-008 `FX Replay`: E1a・E2・E3a・E3b・E5・E6 を当て直す。まず Support Center の**全分類の全記事**の一覧を `curl` で取り、記事の本文も `curl` で取れるかを試す(取れれば、それが `なし` の根拠になる)。描画でしか読めない記事は `印` の根拠にだけ使う。
5. 8-013 `rusty-bot`(`yasstake/rbot`): E3a・E5 を当て直す。N はリポジトリのファイル一覧の全件(5 回目は `.rs` の 75 件だけ)。
6. 8-016 `Exactpro の reconciliation testing`: **範囲を縮めない**。th2-net の組織のリポジトリの一覧を全件で取る(5 回目は 100 件で頭打ち。`https://github.com/orgs/th2-net/repositories?page=2` などの HTML を `curl` で試す)。E2 の段(値は `印`。段 5 の (イ) を「イベントの永続化と閲覧」で読んでよいかは述語の読みの問いなので、逐語に当たると言えなければ `未判別`)、E3a・E3b・E4・E5・E6 を読み切る。商用の部分は委任文 §5-6。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 6 回目の実行(2026-09-24)` の追記だけ**。1〜5 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run6.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` と `### 辿る一覧から出た名前` を除いたもの全部。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表は、8-001〜8-016 の 16 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(6 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log docs/DATA/probes/20260923_tools_8_run6.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 6` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件` もここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run6.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: 6 件の各候補の状態(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」の一覧 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ **描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧**(候補・要素・読めなかった頁)/ 生ログの行数。
6. 「区分は完了」とは書かない。
