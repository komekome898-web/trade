# 道具サーベイ 区分 8 — 25 回目の起動文(2026-09-26)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 25 回目**を担当します。この起動文は、それだけで読めるように書いてあります(ほかの回の起動文は読まなくてよい)。

**この回の終わり方**: §2 の全部を済ませてから返す。期限は無い。途中で止めない。予算で止めない(オーナーの決定 L-507「**案A**」)。どうしても決められない要素は `なし` を書かずに `未判別` のまま残し、何を試して何が足りなかったかを書く。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節 `## 区分8 — 25 回目の実行(2026-09-26)` と生ログ `docs/DATA/probes/20260923_tools_8_run25.log`。返答の中身は §7。

## 0. 最初に読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語(§3)と段(§4.1)は設計票の逐語で当てる。**
4. 報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 24 回目の実行(2026-09-26)` の節(39661 行目から末尾まで。末尾の「リードの追記」を含む)と、生ログ `docs/DATA/probes/20260923_tools_8_run24.log`。**この回の出発点**(8-033 Prefect を初めて開いた回)。小節・表の形はこの節に合わせる。中身は写さない。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする。
5. 24 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run24.md` を全文(§3 の監査 111 回目の指摘 8 件と §4 の処置。**この回はその処置 4・5・7 を済ませる回**)。
6. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値。**

## 1. この回の範囲

- **8-033 `Prefect`** の 1 行だけ。台帳の値: E3a なし・E3b なし・E5 印 段 4・E6 印 段 3・E1a・E1b・E2・E4 は `未判別`、状態 `浅い`。この回にやるのは次の 3 つだけ:
  1. **当たりの行の数を本体だけで数え直す(24 回目の検収 §4 の処置 4)**。24 回目の一覧には、起動文が N から外すと書いた別の配布物 `src/integrations/*`(19 件、別の PyPI の配布物)が入っていた。`venvs/8-033/src/` の Prefect のリポジトリから `src/integrations/` の下を除いた一覧を `cat8_mklist.py` で作り(除いた数を書く)、E1a・E1b・E2・E4 について見積もりの手(§1 の 500 行の規則の手)を打つ。E1a・E1b・E4 の案 B の記録の「当たりの行の数」を、この数に直す。
  2. **E2 の `印` を探し尽くす**。24 回目の `印`(ワークフローの引数の型の検査)は、E2 の述語の対象「**時系列・約定・板・足・参照データについて**」に当たることを言う逐語が無いので受け取っていない(24 回目の検収 §4 の処置 5)。Prefect の本体の機能で、この列挙のデータ(時系列・約定・板・足・参照データ)について、欠け・重複・順序の乱れ・時刻のずれ・外れ値・型や範囲の違反・情報源間の食い違いを検出するか報告する機能を言う逐語を探す。範囲は 24 回目の E1a・E1b・E4 と同じ決め方(文書の目次の全部・文書の全頁の題・ソースの一覧から、関わりうる頁とファイルを読む)。見つからなければ `未判別` のまま、案 B の記録(読んだ範囲と、1 の当たりの行の数)を書く。見積もりが 500 行以下なら、§2.4 の全件の検索を打って当たりを全部判定する(`なし` を書けるのはそのときだけ)。
  3. **§4.0 の `未確認` の 3 件を試す(24 回目の検収 §4 の処置 7)**: 登録の要否(商用版の登録の頁の入力の項目。JS で描く頁なので `scripts/cat8_render.js` を §2.6 の手順で打つ。登録はしない)/ 宣伝詐欺の兆候(公式のサイト・料金の頁・規約の頁を `curl` で取って読む。個々の投稿を取らずに検索の要約だけで決めたなら、根拠の種類は `推定` と書く)/ 時刻の扱い(公式の文書のタイムゾーン・スケジュールの頁を読む。`venvs/8-033/run/` の中の既に入れた環境で、合成の小さい例を 1 つ動かしてよい。新しく何も入れない)。
- **E2 の当て方**: E2 の述語の逐語は「時系列・約定・板・足・参照データについて、**欠け・重複・順序の乱れ・時刻のずれ・外れ値・型や範囲の違反・取引所間(情報源間)の食い違い**のどれか 1 つ以上を**検出するか報告する**機能」。対象がこの列挙のデータであることを言う逐語が要る(16 回目の OpenClaw、24 回目の Prefect の引数の型と同じ当て方)。合成の値に `price` などの名前を付けて動かしても、述語の対象を示す一次資料にはならない。
- **数えるもの**: Prefect の公式のリポジトリの中の本体だけ(`src/integrations/*` の別の配布物は数えない。オーナーが承認したのは「8-014 に**同梱**の `skfolio`・`scipy` の機能を Fincept の機能に数える」だけ。L-508・L-509)。
- **N**: 24 回目と同じリポジトリと文書(`venvs/8-033/src/`)から `src/integrations/` を除いたもの。取り直さない。新しく何かを入れない。
- **当たりの量の上限(500 行)**: `印` が見つからずに全件の検索に進む要素は、**`cat8_search.py` を打つ前に**、同じ一覧・同じ語で当たりの行の数だけを見積もる手を `cat8_step.py` 経由で打つ(一覧の全部のファイルに、`grep -I -c -i -E` で、§2.4 の要素ごとの語の組と同じ語(`cat8_search.py` に渡すのと同じ正規表現)を当て、ファイルごとの数を足した合計を印字する手。件数だけを出す手で、`なし` の根拠にはならない)。**見積もりが 500 行を超える要素には `cat8_search.py` を打たず**、`なし` を書かずに `未判別` にして、見積もりの手の行と行の数を書く(その要素だけの回を立てる)。`cat8_search.py` を打った手の当たりは、検査が全部の判定を求めるため。500 行以下なら `cat8_search.py` を打ち、当たりを全部判定する。
  - 500 の根拠(仮定。12〜19 回目の観察から): 当たりが数十〜数百行の要素では、1 行・1 ファイルごとに違う理由が書けていた(17 回目の AI Trading Lab 40 行、18 回目の行ごとの判定など)。560 行(17 回目の行の表)と 2,998 行(16 回目)では、同じ文の当てはめになった。境目を実測したわけではない。
- **この回に手を付けないもの**: ほかの 40 行と、8-033 の E3a・E3b・E5・E6(台帳の値のまま)。E1a・E1b・E4 は、1 の数の直しと案 B の記録の直しだけ(値は `未判別` のまま。`印` を新しく探し直さない)。

## 2. やること

### 2.1 述語と段(設計票の逐語。短く写す)

- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは数えない。**候補が自分自身の版・ランタイムを試験する仕組み(候補の開発のための試験・CI・`#[test]`・`assert!` など)は `印` に数えない**(オーナーの決定 L-516「**数えません**」)。
- **E1a の述語の括弧の例**: 「差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較」。どれも、同じものを出すはずの 2 つを照らし合わせるもの。戦略どうしの成績を並べて比べる機能は E1a に当たらない(18 回目の検収 §7)。生のデータどうし(例: ティックと足、取引所どうし)の食い違いを見つける機能は E2 の「取引所間(情報源間)の食い違い」の側で、E1a ではない(19 回目の検収 §3)。ただし、計算の出力(損益・指標・約定の再計算など)を参照値(取引所の明細・別の計算の結果など)と照らして差を出す機能は、E1a の「実装と参照値」に当たる。
- **オーナーが承認した 4 つの問い**(L-508「1〜4全て承認します」・L-509「b」。承認は個別の問いへのもので、**ほかの候補へ同じ形で当てるのはリードの当て方**。問いに出さずに当てる): 候補の配布物に同梱された別のライブラリの機能は候補の機能に数える(承認された問いの文:「8-014 に同梱の `skfolio`・`scipy` の機能を Fincept の機能に数える」) / LLM への指示文に書かれた検出・検証の指示は数える / 重複をはじく処理は E2 の「重複の検出」に数える / 1 回だけの固定の学習・評価の分け方は E3b に数える(L-508「1〜4全て承認します」・L-509「b」)。乱数の種(`seed`)は段 5 の (ア) に当たらない。
- 設計票 §3 の述語の文を 1 語ずつ当てる。E2 は「時系列・約定・板・足・参照データについて…検出するか報告する機能」、E4 は「記録した市場データを時刻順に再生して…」、E6 は「E1a〜E5 の述語のどれにも当たらないもの」。E6 に置く前に、その機能が E1a〜E5 のどれかに当たらないかを書く(17 回目の AlgoNetwork の E6「independent verification」は E1a と重なりうるので `未判別` に戻った)。
- 段は §4.1 のとおり下から積み上げる。段 4 は「その要素の対象のすべて」を外から持ち込めることを言う逐語が要る(一部だけなら段 3)。段 5 は (ア)(イ) の両方の逐語が要る。**段は道具の側の記述だけで決め、当方が買った・動かしたかでは決めない。**各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引いて示す(段 4・5 は 8 字以上の逐語が要る。検査がこれを見る)。
- **`印` を付けた要素ごとに、設計票 §4.2 の表の「記録する軸」を知見の表に原文(逐語)と URL で書く**(E1a・E1b = 突き合わせの単位・許容誤差を指定できるか / E2 = 検出する異常の種類・直すか報告だけか / E3a・E3b = 方式 / E4 = 再生の粒度・遅延を入れられるか・時計の扱い / E5 = 何を固定・比較するか(乱数の種・依存・データの版・出力の差分)/ E6 = その機能の原文(何を確かめる機能か)・その機能が名指す対象の原文 / 全要素 = 方式・入力・外から持ち込める対象・(ア)・(イ)・自動で判定するか)。原文に無い軸は `記載なし(読んだ箇所: …)` と書く(追補 §4)。20 回目は、これを書き落とした。

### 2.2 問いの書き方

- 問いに出した行は、値も段も `未判別` にする(決めてから問いに出さない)。`### 判断に迷った点と問い` の番号付きの行には、全部 `[値・段の問い]` か `[それ以外の問い]` を付ける(付けないと検査が止める)。値・段を決めずにおく理由でない問いは `[それ以外の問い]`。

### 2.3 `印` を先に探す

各要素について、まず候補の文書・README・ソースから、述語に当たる機能を言う逐語を探し、`grep -n` や `sed -n` で生ログに印字して引く(`印` には全件の検索は要らない)。行を生ログに出すのは、読むと決めたファイルの範囲だけ。ソース全体への `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI`・`grep -rcI`)にする。`印` を探す段では `なし` を結論しない。

### 2.4 `なし` は全件の検索からだけ

**この回に使うのは、§1 の 2 で E2 の見積もりが 500 行以下だったときだけ**。

- 一覧は `scripts/cat8_mklist.py` で作り、検索は `scripts/cat8_search.py` だけで打つ(どちらも `scripts/cat8_step.py` 経由。`--candidate` にその候補の台帳の番号。`--` のあとにパイプ・`;`・リダイレクトを付けない)。形は 20 回目の生ログ(`docs/DATA/probes/20260923_tools_8_run20.log`)の手をそのまま見本にする。
- 検索の語には、要素ごとに次の組の**語を全部**、この字のとおりに入れる: E1a = 突き合わせ・reconcil / cross-check・比較・compar / diff・差分・一致・mismatch、E1b = 突き合わせ・reconcil / 別実装・reference implementation・参照実装・cross-check / compar・比較・diff・差分、E2 = 欠損・missing・gap・欠け / 重複・duplicat / 順序・out of order・monoton・sort / 外れ値・outlier・anomal / 時刻のずれ・clock・skew・timestamp / 型・範囲・schema・range・valid、E3a = ルックアヘッド・look-ahead / survivorship・生存者 / leak・リーク・未来 / point-in-time・as-of・時点、E3b = ルックアヘッド・look-ahead / embargo / purge / point-in-time・as-of・時点、E4 = リプレイ・再生・replay / record・記録・capture / playback・再実行・rerun、E5 = 再現・reproduc / seed・乱数 / deterministic・決定的 / snapshot・golden・pin・lock・版の固定、E6 = 検証・verif・validat / 品質・quality / test・assert・check。**打つ前に語に誤字が無いかを見る**(誤字の手を打つと、その手の当たりも全部判定が要る)。
- `なし` の根拠の欄に `一覧 N 件 / 読んだ M 件` と `当たり F ファイル / H 行` をこの字のとおりに書く。N と M は cat8_mklist.py の `listed=` の和。7 列目には、その要素の検索の手の行を全部書く(長い出力の中の離れた行を引くときは、引用した行も書く)。
- **当たった行は全部判定する。この回は、ファイルの表 (甲) だけを使う**(決まりで分ける `cat8_classify.py`(乙)は使わない。18・19 回目に、決まりが語ごとの形になり、候補の機能を言う行まで取っていた)。
  - ファイルの表 `### 当たりの判定`: `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |`。**理由は、そのファイルの当たった行を読んで、当たった語と、その語がそこで何を指すかを書く。**
  - 行の表・ファイルの表には、同じ文を繰り返さない(語尾や語順を変えただけの同じ内容も、同じ理由とみなす。リードが検収で読む)。同じ文を何行・何ファイルにも当てた `なし` は受け取られない。当たりが多くて 1 つずつ書けないなら、§1 の「500 行」の規則で `未判別` にする。
- 生ログはこの回のものだけを根拠にする(ほかの回の検索の結果を引かない)。

### 2.5 §4.0 の表

8-033 の §4.0 の表は、この回に変えた行(§1 の 3 の 3 項目)だけを書く(ほかの 40 行は 24 回目の節の値のまま)。`未確認` のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたものと、試すのに当方の鍵・口座・登録・購入が要るか外へ送信するもの(そう言う逐語を引く)だけ。「この回では実施していない」「読んでいない」は理由にしない。

### 2.6 大きさ・届かないとき・取り方

- 生ログと報告は GitHub に押し出すので、どちらも 90 MB 以内に保つ(`wc -c` を検索の手の前後に `cat8_step.py` で打つ)。
- この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書き、「存在しない」とは書かない。
- 取った本文は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下に置く(`/tmp` の直下に置かない)。
- **WebFetch は一次資料に使わない**(要約を返すため)。まず `curl` で生の HTML を取る。JavaScript で描く頁は `scripts/cat8_render.js` を `cat8_step.py` 経由で打つ(`NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run25.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`)。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない。**`印` の根拠にするのは、次の全部を満たすときだけ: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く / (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(この回の節の追記だけ。1〜24 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run25.log` の 2 つだけ。scratchpad 以外にファイルを作らない。生ログはすべて `scripts/cat8_step.py` で書く。
- 台帳には書かない。`scripts/` の下のファイルは書き換えない。区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。外部の道具を入れる場所は `.../scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

24 回目の節と同じ小節を置く(`### 決まりの一覧`・`### 当たりの判定(行ごと)` は、この回は (乙) を使わないので置かない)(`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」)。検査が形を見る所:
- 「要素と段」の表は 7 列 `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目には `docs/DATA/probes/20260923_tools_8_run25.log:10` か `...:10-20` を空白で区切って並べるだけ(ほかの字を入れない)。値が `印` か `なし` の行は 7 列目を空にしない。引いた文「…」は 7 列目の行の手の出力に出ていること。
- この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし、値と段は台帳と同じにし、7 列目は空でよい。
- 知見の表の「根拠」の欄には、生ログの参照(上と同じ形)・URL・日付(`2026-09-26`)・「取得日」だけを書く(説明は「知見」の欄に書く)。**候補の一覧と「要素と段」の表(7 列)は、8-001〜8-041 の 41 行全部を載せる**(この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする)。変えた要素は候補の一覧の説明に全部書く。

## 5. 禁止

委任文 §10 のすべて。commit も push もしない。モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。

## 7. 返す前に

1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md` に、`docs/DATA/probes/20260923_tools_8_run1.log` から `docs/DATA/probes/20260923_tools_8_run25.log` までの 25 本の生ログを並べて打つ。誤検出は閉じずに行と理由を書く。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 25` を 0 件にする(最後の出力を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run25.log` を 0 件にする(同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0(同じ節に貼る)。
5. 返答には次をこの順で書く: 候補ごと・要素ごとの `印`・`なし`・`未判別`(`未判別` なら何が足りないか)/ この回の `なし` の全部の「一覧 N 件 / 読んだ M 件」/ 当たりが 500 行を超えたために `未判別` にした要素と、その当たりの行・ファイルの数 / §4.0 で `未確認` のまま残した項目と理由 / 4 つの検査の最後の合計行 / 判断に迷った点と問い(決めずに) / 描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧(候補・要素・読めなかった頁)/ 生ログと報告の大きさ(最後の `wc -c`)。「区分は完了」とは書かない。
