# 道具サーベイ 区分 8 の追補(2026-09-23)

**これは委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)の追補である。**区分 8 の実行だけに当たる。
**ここに書いていないことは、すべてもとの委任文のとおり。**ここに書いたことが、もとの委任文の同じ箇所と食い違うときは、区分 8 ではここが勝つ。
設計の本体(完了の形・状態の語・要素の印の述語・記録する軸)は設計票 `docs/DATA/surveys/CAT8_DESIGN.md` にある。**調査班は起動の最初に、もとの委任文 → この追補 → 設計票の §2〜§5 の順に全部読む。**

出所: 設計票の監査 1 回目の指摘 1・2(委任文 §6-5 が書き込み先を 2 つに限っているのに、それを起動文で上書きしようとしていた)。上書きを起動文でせず、この追補を監査に通して指紋を記録する。

## 1. 書き込み先(委任文 §5-5・§6-5・§9・§11 の**書き込み先の部分だけ**を置き換える)

**§9 の他の規定(必須の小節 6 つ・§4.0 の表・受け入れ検査の出力の節・X の投稿の書き方・一行候補)と、§5-5 の 1 手ずつ残す規則はそのまま。**§11 の「1 回の実行の完了」の `SCAN_2026-09-21_tools.md` は、区分 8 では下の報告書 `SCAN_2026-09-23_tools_cat8.md` と読み替える(揃えるべき中身はそのまま)。

**調査班が**区分 8 で**書いてよいのは次の 2 つだけ**(台帳・索引・検収の記録などはリードが書く。調査班は書かない)。scratchpad 以外にファイルを作らない。
- 報告書: `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(区分 8 の節を追記する。節の見出しは `## 区分8 — N 回目の実行(YYYY-MM-DD)`)
- 生ログ: `docs/DATA/probes/20260923_tools_8_run<N>.log`(N はリードが起動文で指定する回の番号)

**`docs/DATA/SCAN_2026-09-21_tools.md`・`docs/DATA/probes/20260921_tools_*.log`・区分 1 の台帳と索引・台帳 `docs/DATA/tools_catalog_cat8.tsv` には書かない。**
外部の道具を入れる場所は、リードが起動文で指定する scratchpad の `cat8/venvs/` の下だけ(委任文 §5-2 の隔離はそのまま)。

## 2. 台帳と番号(区分 8 で新しく足す規則)

- 台帳 `docs/DATA/tools_catalog_cat8.tsv` は**読むだけ**。書くのはリードで、番号を振るのは `scripts/cat8_ledger.py` だけ。調査班は `cat8_ledger.py` を打たない。
- 報告の候補の一覧では、**台帳にある候補は `8-NNN` の番号で引き**(名前は台帳の `名前` 列と 1 文字も変えない)、**台帳に無い候補は番号を付けず行頭に `新` と書き、名前と発見の出典の URL を添える。**自分で番号を作らない。
- 台帳にあるかどうかは、名前と URL の両方で台帳を検索してから決める(`grep -i` で。打ったコマンドを生ログに残す)。

## 3. 生ログの形(委任文 §5-5 に足す)

冒頭に `=== ` で始まる行(回・指定・申し送り)。1 手ごとに次の 3 つをこの順で書く。**この形以外の手を書かない**(区分 1 では 27 本の生ログが 5 通りの形に流れ、検査が読めない手が出た):
```
--- <UTC の ISO 時刻> method=<語> target=<語> rc=<終了コード> time_s=<秒> note=<文>
$ <打ったコマンド>
<出力(先頭 200 文字以上)>
```

## 4. 要素の印と、記録する軸(委任文 §4 の列に足す)

- 深掘りした候補と浅い候補の両方について、設計票 §3 の 8 列(`E1a` `E1b` `E2` `E3a` `E3b` `E4` `E5` `E6`)の値を `印` / `なし` / `未判別` のどれかで書き、`印` と `なし` には根拠(委任文 §4.1 の印と、一次資料の URL か生ログの行)を付ける。**述語は設計票 §3 の逐語で当てる。自分で言い換えない。**
- 状態は設計票 §2 の 7 語のどれかで書く(`未着手` / `判別に一次資料が要る` / `浅い` / `深掘り` / `危険で導入停止` / `登録が要る` / `区分8の要素なし`)。定義は設計票 §2 の逐語。
- `印` を付けた要素ごとに、**設計票 §4.1 の段**(`1`〜`5` / `未判別`)を書く。段は下から積み上げで、条件を満たす一番上の段。根拠(一次資料の URL と、その段の条件に当たる原文)を付ける。**段の条件は設計票 §4.1 の逐語で当てる。**段で並べられない機能が出たら、段を曲げずに問いとして報告の末尾に出す。
- `印` を付けた要素ごとに、知見の表に設計票 §4.2 の表の「記録する軸」を**原文(逐語)と URL** で書く。原文に無い軸は `記載なし(読んだ箇所: …)` と書く。**当方の位置を物差しにしない。**
- 述語や状態の語で判断に迷ったら、決めずに問いとして報告の末尾に列挙する(リードが決める)。

## 5. 予算(委任文 §7 の「1 回 5 万トークン・20 分」を置き換える)

1 回あたり **25 万トークン・25 分**を上限の目安にする(出所: `.claude/skills/research-squad/SURVEY.md` §2。区分 1 の実測は中央値 20.8 万トークン・17.7 分で、5 万・20 分は区分サーベイに当てていなかった)。超えそうなら中断して「未完了」と書く。中断の前に書くものは委任文 §7 のとおり。

## 6. 受け入れ検査(委任文 §12 の読み替え)

コマンドは `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run<N>.log`。他は委任文 §12 のとおり(0 件になるまで直す / 誤検出は閉じずに渡す / 最後の出力を全文貼る)。

## 7. 完了の形(定義は設計票 §2 の表にある。ここはそれを調査班の側から引いたもの)

- **1 回の実行の完了** = 設計票 §2 の表の「1 回の実行」の行の (a)(b)。つまり、委任文 §11 の「1 回の実行の完了」(報告の在処は §1 の読み替えのとおり `SCAN_2026-09-23_tools_cat8.md`)に加えて、報告の候補の一覧の全行が「台帳の `8-NNN`」か「`新`(名前と URL つき)」のどちらかで書かれ、全行に §4 の 8 列と状態が書かれ、`印` の要素には段が書かれていること。同じ行の (c)(台帳の検査)はリードが検収で打つもので、調査班の返す前の条件ではない。
- **区分 8 の完了**は調査班が決めない。設計票 §2 の述語(残り = 0 行 かつ 最後の新しい検索計画 6 本が新しい行を 1 行も足さない)で、リードが台帳から機械で数えて決める。**調査班は「区分は完了」と書かない**(書いてよいのは「この回で新しい候補は `新` が N 行」まで)。
