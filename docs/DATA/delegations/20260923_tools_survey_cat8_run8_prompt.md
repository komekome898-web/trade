# 道具サーベイ 区分 8 — 8 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 8 回目**を担当します。

**この回の終わり方**: §2 の 1〜10 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 8 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run8.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**ただし (a) の「文書の全部」と、この起動文 §2 の N の定義(リポジトリならファイル一覧の全件)が違うときは、**広いほう(この起動文 §2)を当てる**。
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7.md` を全文(とくに §2・§3 と、末尾の「監査 42 回目」)。**7 回目は、(1) 問いに出した行の値・段を決めていた(5 か所。3 回目の同じ誤り)、(2) 7 列目に引いた手がその要素の検索になっていなかった(8-002 の E6 は E2 の検索の手、8-004 の E6 は E6 の語を検索していない)、(3) 段 4 の「枠の外」を逐語でなく推論で付けた、(4) 検索の対象の一覧を `/tmp` の直下に置いて生ログに中身を残さなかった。同じ誤りをしない。**
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。16 行)。**この回の出発点は台帳の値**(7 回目の報告の表の値ではない。検収と監査 42 回目で台帳を直した)。

## 1. この回の位置づけ

台帳の残り(設計票 §2 の「残り」)は 3 行: 8-002・8-013・8-016。この回はこの 3 行を `深掘り` の条件まで進め(§2 の 1〜3)、8-004 の `未判別` 2 つを決め(§2 の 4)、1〜5 回目に書いたまま数え直していない `なし` の残り(8-009・8-010・8-011・8-012・8-014・8-015)を当て直す(§2 の 5〜10)。

**この回に回さないもの(9 回目以降。この回で手を付けない)**: 段が `未判別` の行の当て直し(この回の §2 で当てる行を除く。8-003 E5・8-011 E5・8-012 E5・8-014 E1b/E3b/E4・8-007 E6)/ 8-005 / 辿る一覧 / 成熟度の枠組みの原典。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

**`なし` を書くときの手順(この回の全部の `なし` に当てる。4〜7 回目の差し戻しの対策)**:
- **問いに出した行は、値も段も `未判別` にする**(決めてから問いに出さない。7 回目まで 3 回、この規則を破った)。
- 7 列目に書く行は、**その要素の語で検索した手**の行にする(ほかの要素の検索の手や、一覧を取っただけの手を引かない)。検索の語には、設計票 §3 のその要素の名前を日本語と英語で入れる。
- 検索の対象の一覧(除外したあとのファイルの一覧)は scratchpad の下に置き、その件数と先頭・末尾の数行を生ログに印字する手を打つ(`wc -l`・`head`・`tail`)。
- 段 4・5 を付けるときは、その条件(段 4 = 枠の外のものに掛かる、段 5 = (ア)(イ))に当たる逐語を、7 列目の行の手の出力に出す。逐語が無ければ段は `未判別`。
- 候補の一次資料の**ファイル一覧・頁の一覧を全件で取る**。`cat8_step.py` の既定は出力を 3,000 字で切るので、一覧を取る手には `--keep 200000` を付ける。一覧は生ログに全件が残るようにする。
- **N は、その候補の一次資料の一覧を取った手の出力の件数そのもの**(リポジトリならファイル一覧の全件、サイトなら文書・ヘルプ・ブログの全部の頁の一覧)。検索した範囲の件数ではない(5 回目の差し戻し: `*.py` だけ・`.rs` だけ・ヘルプの 1 分類だけを N にしていた)。M は、その N 件のうち実際に読んだ・検索の対象に入れた件数(検索の手の対象の指定 `--include` などがあれば、それで当たる件数を数える)。除外してよいのは、文字を検索しても意味の無いもの(画像・ロックファイル・バイナリ)だけで、除外は**ファイル名と理由を全部**書く(テスト用のデータ・設定ファイル・スクリプトは除外しない)。
- `なし` の行の根拠の欄に、**`一覧 N 件 / 読んだ M 件`** の形(この字面のまま)と、一覧を取った生ログの行(`20260923_tools_8_run8.log:<行>`)を書く。**N と M が違うなら `なし` と書かない**(`未判別` のまま、読み残しの名前を書く)。§7 の 2 の検査がこの形・N = M(数として)・生ログがこの回のもので行が実在することを機械で見る(この回に触らない行の根拠は `台帳の値のまま(<何>回目の節)` と書けば検査の対象外)。一覧から外すもの(画像・生成物など、文書でもソースでもないもの)は、外した名前と理由を書く。
- 「主要な」「全体で」のような語で範囲をぼかさない。
- **引く文は生ログに印字する**(6 回目の差し戻し): 根拠に「…」で引く文は、`印`・`なし`、`一次資料`・`実測` の別を問わず、その文を含む行が、根拠に引いた生ログの手の出力に出ているようにする(`curl -o` でファイルに逃がしたら、`grep -n` や `sed -n` でその行を生ログに印字する手を続けて打ち、その手の行を引く)。取った本文は scratchpad の下(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下)に置き、`/tmp` の直下に置かない。§7 の 2 の検査が、「…」の逐語(8 字以上)の先頭 30 字が引いた生ログの手の出力にあるかを見る。**生ログの行は、文の中に書かず、決まった欄にだけ書く**(§7 の 2 の検査がこの欄の形を機械で見る。監査 34〜39 回目で、文の中から行の番号を読み当てる検査は何度直しても読み落としが見つかったので、読み当てるのをやめた): (1) **「要素と段」の表は 7 列**にする: `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目「生ログの行」には、`docs/DATA/probes/20260923_tools_8_run8.log:10` か `docs/DATA/probes/20260923_tools_8_run8.log:10-20` を**空白で区切って並べるだけ**(ほかの字・注記・読点・カンマを入れない)。値が `印` か `なし` の行は空にしない。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし(ほかの文を足さない)、値と段は台帳と同じにする。7 列目は空でよい(値・段が台帳と違う行や、6 列目にほかの文がある行は「触った行」として 7 列目の検査を受ける)。6 列目「根拠」は自由な文でよい(番号を書いても検査は読まない。行は 7 列目に書く)。引いた文「…」は 7 列目の行の手の出力に出ていること。(2) **知見の表の「根拠」の欄**には、生ログの参照(上と同じ形)・URL・日付(`2026-09-24`)・「取得日」だけを書く(説明は「知見」の欄に書く)。
- **ノートの語は実際のコマンドの語と同じにする**(6 回目: ノートに「防ぐ」「同じ結果」と書いたのに、コマンドには無かった)。検索の語は、コマンドの正規表現に日本語と英語で入れる(要素の名前を含む)。
- **WebFetch は一次資料に使わない**(要約を返す道具なので、委任文 §4.1 の「検索結果の要約」に当たる)。まず `curl` で生の HTML を取る。JavaScript で描く頁で本文が HTML に無いときは、`scripts/cat8_render.js`(この環境の Chromium で頁を開いて本文をそのまま出す。`playwright install` は打たない)を `cat8_step.py` 経由で打つ: `NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run8.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`(`rc` と秒が数字で入る。ブラウザーは自分では通信せず、道具の Node の側が証明書を通常どおり検証して取ったものを頁に渡す。リードが 2026-09-24 に `https://fxreplay.com` で HTTP 200 と本文、証明書の誤りのある頁(名前の不一致・自己署名・期限切れ)で取得が止まることを確かめた)。本文が取れずに止まったら、その頁は `未判別` のまま、止まった理由を書く。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない**(道具は本文の欠けを全部は数えられない。監査 25〜27 回目で、何も数えられないまま本文が欠ける型が 3 つ見つかった)。`なし` は、生の HTML・文書のファイル・ソースなど、全部を取ったと件数で示せる一次資料で書く。描画でしか読めない頁しか無い要素は `未判別` のまま、読めなかった頁の名前を書く。§7 の 2 の検査は、`なし` の根拠に引いた生ログの手が `cat8_render.js` なら止める。出力の `INCOMPLETE` の行と最後の `INCOMPLETE 合計 <N> 件` は、`印` の根拠に使うときに一緒に写す。**描画した頁を `印` の根拠にするときの条件(全部を満たす)**: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く(語だけを拾わない)/ (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

1. 8-002 `PineForge`: E6 を E6 の語(検証・品質・verify・validat・quality など)で検索し直して決める(7 回目は E2 の検索の手を引いていた)。E2 の段を、§4.1 の段の条件の逐語で決める(段 5 の (ア) の逐語が無ければ 4 か `未判別`)。
2. 8-013 `rusty-bot`(`yasstake/rbot`): E6 を決める。`validate_db_by_date` の実装を、リポジトリの全ファイルと、README が示すほかのリポジトリ・クレートで探す(CLAUDE.md §0.2 A-11: 「使われていない」を目で見て判断しない)。実装が見つかり、何を確かめるかが原文で分かれば述語に当てる。見つからなければ `未判別` のまま、探した場所を全部書く。
3. 8-016 `Exactpro の reconciliation testing`: **範囲を縮めない**。7 回目に集めた部品の名前(Maven Central の 73 件ほか)のリポジトリを個別に開き、E3a・E3b・E5 と、E2・E6 の段を決める。組織の全部の一覧が取れなければ `なし` は書けない(`未判別` のまま、開いたリポジトリの件数と名前・試した手段を全部書く)。段 4 の「枠の外」は、そのリポジトリの一次資料の逐語で支える。
4. 8-004 `akurkar07/OrderBook`: **導入・実行・`git clone` をしない**(ファイルを `curl` で 1 本ずつ取って `grep`・`sed` で読むだけ。7 回目に取った 17 本を使ってよい)。E2: README の「Duplicate IDs are rejected」「Orders require a valid buy/sell side and non-zero quantity」は、板への入力の**重複**と**型や範囲の違反**を検出する記述で、E2 の述語(「時系列・約定・**板**・足・参照データについて、…**重複**…**型や範囲の違反**…のどれか 1 つ以上を検出するか報告する」)に当たる(リードの読み。7 回目の問い 1 への答え)。この読みで値と段を決める。E6: E6 の語で 17 本全部を検索して決める。
5. 8-009 `nicferrari/backtester`: `なし` の E1a・E2・E3a・E3b・E5・E6 を上の手順で当て直す(N はファイル一覧の全件)。
6. 8-010 `arXiv:2603.20319`: `なし` の E3a・E3b・E4・E6 を当て直す(N は論文の本文の全部の節と、補足資料。論文は PDF か HTML を取り、本文を生ログに印字してから検索する)。
7. 8-011 `arXiv:2512.12924`: `なし` の E1a・E3a・E6 を当て直す(N は論文の本文と、実装のリポジトリのファイル一覧の全件)。
8. 8-012 `VectorBT`: `なし` の E1a・E3a・E6 を当て直す(N はリポジトリのファイル一覧の全件と、公式の文書のサイトの全頁)。
9. 8-014 `Fincept Terminal`: `なし` の E3a・E6 を当て直す(N はリポジトリのファイル一覧の全件と、マニュアルの全頁)。
10. 8-015 `TradingView のリプレイ機能`: `なし` の E1a・E1b・E2・E3a・E5・E6 を当て直す(N は公式のヘルプのリプレイに関わる頁の全部。頁の一覧を取った手を生ログに残す。描画でしか読めない頁は `なし` の根拠にしない)。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 8 回目の実行(2026-09-24)` の追記だけ**。1〜7 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run8.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` と `### 辿る一覧から出た名前` を除いたもの全部。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表(7 列。§2 の冒頭)は、8-001〜8-016 の 16 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(8 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log docs/DATA/probes/20260923_tools_8_run6.log docs/DATA/probes/20260923_tools_8_run7.log docs/DATA/probes/20260923_tools_8_run8.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 8` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件` と、引いた文が生ログにあるかもここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run8.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: 10 項目の各候補の状態(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」の一覧 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ **描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧**(候補・要素・読めなかった頁)/ 生ログの行数。
6. 「区分は完了」とは書かない。
