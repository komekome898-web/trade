# 道具サーベイ 区分 8 — 13 回目の起動文(2026-09-24)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 13 回目**を担当します。

**この回の終わり方**: §2 の 1〜2 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 13 回目の実行(2026-09-24)`)と生ログ `docs/DATA/probes/20260923_tools_8_run13.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**ただし (a) の「文書の全部」と、この起動文 §2 の N の定義(リポジトリならファイル一覧の全件)が違うときは、**広いほう(この起動文 §2)を当てる**。
7. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run12.md` を全文(とくに §2 と、末尾の「監査 73 回目」)。**12 回目は、(1) 当たりの表の理由の 224 行が「「<語>」の使用箇所」だけで、語が何を指すかを言っていなかった、(2) 8-025 の `なし` が、自分の当たりの表にある突き合わせの試験を「examples の利用者のサンプルコード」と書いて外した(試験は `tests/` にあった)、(3) 段 4 を候補自身の例の設定ファイルや README の一般の説明からの外挿で付けた、(4) 同じ一覧を 2 回検索して N・M を 2 倍にした(検査の作りの欠陥。13 回目からは検査が同じ一覧を 1 回だけ数える)、(5) §4.0 の語彙を 41 項目と書いた(43 項目)。同じ誤りをしない。**
8. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値**(12 回目の報告の表の値ではない。検収・監査 73 回目で台帳を直した)。

## 1. この回の位置づけ

この回は 2 つ: 12 回目の `なし` の手直し(§2 の 1)/ 8-016〜8-028 のほかの `未判別` と §4.0 の表(§2 の 2)。

**この回に回さないもの(この回で手を付けない)**: 8-029〜8-041(14 回目に回す。L-416「**一気に終わらせようとせずに**」)/ 8-016 の E3b・12 回目に受け取った `なし`(8-019 AutoHedge・8-027 Quantreo)は、台帳の値のまま書く / 上に無いほかの行。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

**`なし` を書くときの手順(この回の全部の `なし` に当てる。4〜8 回目の差し戻しの対策)**:
- **問いに出した行は、値も段も `未判別` にする**(決めてから問いに出さない。8 回目まで 4 回、この規則を破った)。問いの行には、`[値・段の問い]` か `[それ以外の問い]` のどちらかを必ず書く(今後の追い方など、値・段を決めずにおく理由でない問いは `[それ以外の問い]`)。**`[それ以外の問い]` の行は、§7 の 2 の検査の「問いに出した行は `未判別`」の対象外**になる。どちらも書いていない問いの行は検査が止める。**問いにしかけて取り下げた判断は、取り下げた理由と行を「判断に迷った点と問い」の節に書く**(黙って問いから外さない)。
- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは、道具の機能として数えない。
- N を 2 つ以上の一覧の和で作るときは、一覧ごとの件数と生ログの行、和を書く。
- 根拠の欄で「確かめた」「再確認した」と書くときは、その手の行を 7 列目に書き、確かめた文を「…」で引く(9 回目: 引いた行がほかの手だった)。
- **オーナーが決めた述語の読み(L-508「1〜4全て承認します」・L-509「b」)**: 候補が同梱・呼び出している**別のライブラリ**の機能は、候補の機能に**数える**。LLM への指示文(プロンプト)に書かれた検出・検証の指示は、候補の機能に**数える**。重複をはじく処理は E2 の「重複の検出」に**数える**。1 回だけの固定の学習・評価の分け方は E3b に**数える**。この 4 つは問いに出さずに当てる。**段の読みのオーナーの答え(L-511「5 は 34、6 は 3、7 は 4」・L-512「5は4です」)は、その 3 つのセルの答え**(8-005 E4 = 段 4 / 8-014 E3a = 段 3 / 8-014 E1b = 段 4。台帳に当ててある)。一般の規則ではない(問いの文も答えもセルごとだった = 監査 64 回目の指摘 4)。ほかの候補で同じ形の境界(道具から取り出したファイルを別の道具で使えるか / LLM に指示して出させる判断 / 広く使われるライブラリの形に縛られた引数)に当たったら、段は `未判別` にして `[値・段の問い]` で出し、前例として L-511・L-512 の番号を書く。
- 乱数の種(`seed`)は段 5 の (ア)「判定の基準(許容誤差・閾値・検出の条件)」に当たらない。
- 7 列目に書く行は、**その要素の語で検索した手**の行にする(ほかの要素の検索の手や、一覧を取っただけの手を引かない)。検索の語には、設計票 §3 のその要素の名前を日本語と英語で入れる。
- 検索の対象の一覧(除外したあとのファイルの一覧)は scratchpad の下に置き、その件数と先頭・末尾の数行を生ログに印字する手を打つ(`wc -l`・`head`・`tail`)。
- 段 4・5 を付けるときは、その条件(段 4 = 枠の外のものに掛かる、段 5 = (ア)(イ))に当たる逐語を、7 列目の行の手の出力に出す。逐語が無ければ段は `未判別`。
- 候補の一次資料の**ファイル一覧・頁の一覧を全件で取る**。`cat8_step.py` の既定は出力を 3,000 字で切るので、一覧を取る手には `--keep 200000` を付ける。一覧は生ログに全件が残るようにする。
- **N は、その候補の一次資料の一覧を取った手の出力の件数そのもの**(**`なし` の N と M は、除外のあとの件数 = cat8_mklist.py の `listed=` の和**。除外・取らなかったものは cat8_mklist.py の出力に名前と理由が出る。§2 の 1 の (a))(リポジトリならファイル一覧の全件、サイトなら文書・ヘルプ・ブログの全部の頁の一覧)。検索した範囲の件数ではない(5 回目の差し戻し: `*.py` だけ・`.rs` だけ・ヘルプの 1 分類だけを N にしていた)。M は、その N 件のうち実際に読んだ・検索の対象に入れた件数(検索の手の対象の指定 `--include` などがあれば、それで当たる件数を数える)。除外してよいのは、文字を検索しても意味の無いもの(画像・ロックファイル・バイナリ)だけで、除外は**ファイル名と理由を全部**書く(テスト用のデータ・設定ファイル・スクリプトは除外しない)。
- `なし` の行の根拠の欄に、**`一覧 N 件 / 読んだ M 件`** の形(この字面のまま)と、一覧を取った生ログの行(`20260923_tools_8_run13.log:<行>`)を書く。**N と M が違うなら `なし` と書かない**(`未判別` のまま、読み残しの名前を書く)。§7 の 2 の検査がこの形・N = M(数として)・生ログがこの回のもので行が実在することを機械で見る(この回に触らない行の根拠は `台帳の値のまま(<何>回目の節)` と書けば検査の対象外)。一覧から外すもの(画像・生成物など、文書でもソースでもないもの)は、外した名前と理由を書く。
- 「主要な」「全体で」のような語で範囲をぼかさない。
- **引く文は生ログに印字する**(6 回目の差し戻し): 根拠に「…」で引く文は、`印`・`なし`、`一次資料`・`実測` の別を問わず、その文を含む行が、根拠に引いた生ログの手の出力に出ているようにする(`curl -o` でファイルに逃がしたら、`grep -n` や `sed -n` でその行を生ログに印字する手を続けて打ち、その手の行を引く)。取った本文は scratchpad の下(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下)に置き、`/tmp` の直下に置かない。§7 の 2 の検査が、「…」の逐語(8 字以上)の先頭 30 字が引いた生ログの手の出力にあるかを見る。**生ログの行は、文の中に書かず、決まった欄にだけ書く**(§7 の 2 の検査がこの欄の形を機械で見る。監査 34〜39 回目で、文の中から行の番号を読み当てる検査は何度直しても読み落としが見つかったので、読み当てるのをやめた): (1) **「要素と段」の表は 7 列**にする: `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目「生ログの行」には、`docs/DATA/probes/20260923_tools_8_run13.log:10` か `docs/DATA/probes/20260923_tools_8_run13.log:10-20` を**空白で区切って並べるだけ**(ほかの字・注記・読点・カンマを入れない)。値が `印` か `なし` の行は空にしない。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし(ほかの文を足さない)、値と段は台帳と同じにする。7 列目は空でよい(値・段が台帳と違う行や、6 列目にほかの文がある行は「触った行」として 7 列目の検査を受ける)。6 列目「根拠」は自由な文でよい(番号を書いても検査は読まない。行は 7 列目に書く)。引いた文「…」は 7 列目の行の手の出力に出ていること。(2) **知見の表の「根拠」の欄**には、生ログの参照(上と同じ形)・URL・日付(`2026-09-24`)・「取得日」だけを書く(説明は「知見」の欄に書く)。
- **ノートの語は実際のコマンドの語と同じにする**(6 回目: ノートに「防ぐ」「同じ結果」と書いたのに、コマンドには無かった)。検索の語は、コマンドの正規表現に日本語と英語で入れる(要素の名前を含む)。
- **WebFetch は一次資料に使わない**(要約を返す道具なので、委任文 §4.1 の「検索結果の要約」に当たる)。まず `curl` で生の HTML を取る。JavaScript で描く頁で本文が HTML に無いときは、`scripts/cat8_render.js`(この環境の Chromium で頁を開いて本文をそのまま出す。`playwright install` は打たない)を `cat8_step.py` 経由で打つ: `NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run13.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`(`rc` と秒が数字で入る。ブラウザーは自分では通信せず、道具の Node の側が証明書を通常どおり検証して取ったものを頁に渡す。リードが 2026-09-24 に `https://fxreplay.com` で HTTP 200 と本文、証明書の誤りのある頁(名前の不一致・自己署名・期限切れ)で取得が止まることを確かめた)。本文が取れずに止まったら、その頁は `未判別` のまま、止まった理由を書く。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない**(道具は本文の欠けを全部は数えられない。監査 25〜27 回目で、何も数えられないまま本文が欠ける型が 3 つ見つかった)。`なし` は、生の HTML・文書のファイル・ソースなど、全部を取ったと件数で示せる一次資料で書く。描画でしか読めない頁しか無い要素は `未判別` のまま、読めなかった頁の名前を書く。§7 の 2 の検査は、`なし` の根拠に引いた生ログの手が `cat8_render.js` なら止める。出力の `INCOMPLETE` の行と最後の `INCOMPLETE 合計 <N> 件` は、`印` の根拠に使うときに一緒に写す。**描画した頁を `印` の根拠にするときの条件(全部を満たす)**: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く(語だけを拾わない)/ (ii) その文が本文(`HTTP` の行の下)か、URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(広告・計測・チャットなど第三者の埋め込みの frame の文は使わない)/ (iii) その手の出力に `INCOMPLETE text-changed-after-wait` の行が無い(あれば、同じ頁をもう一度描画して、引く文が 2 回とも同じに出ることを確かめる)。満たさなければ `未判別`。

段は設計票 §4.1 の逐語のとおりに決める: 「**段は下から積み上げで決める**: その要素について、段 1 から順に条件を満たすかを見て、**条件を満たす一番上の段**を付ける。上の段の条件の一部だけを満たしても、その段は付けない」。各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引き、7 列目の行の手の出力に出して示す。段 1 の条件に当たる原文も無ければ `未判別`。

1. **12 回目の `なし` の手直し**(監査 73 回目: 当たりの表の理由が「「<語>」の使用箇所(例: …)」だけで、その語が何を指すかを言っていない行が 224 行あり、それに依る `なし` を台帳で `未判別` に戻した = 12 回目の検収の末尾)。対象の要素: 8-016 の E3a・E5 / 8-021 `Qlib` の E1a / 8-022 `FinGPT` の E3a・E3b・E4 / 8-023 `Backtrader` の E1a・E2・E3a・E3b / 8-025 `FinanceToolkit` の E1a・E3a・E3b・E4・E5。**この回の生ログで検索を打ち直す**(`なし` の 7 列目はこの回の生ログの手を引く決まりなので、12 回目の手は引けない)。検索の語は 12 回目と同じでよい(語の組の語を全部含むこと)。当たりの表は、12 回目の表の理由を写してよいが、**理由が語の意味を言っていない行は読み直して書き直す**。**8-025 は `tests/performance/test_performance_model.py`(自分の計算を独立の NumPy の実装と突き合わせ、乱数の種を固定する試験)を E1a・E5 に当てる**。候補自身の試験の中の突き合わせ・固定を候補の機能に数えるかは、12 回目にほかの候補(FinGPT・Backtrader・Lean)で数えた扱いと揃える(揃えられないと思ったら値・段を `未判別` にして `[値・段の問い]` で出す)。8-016 の取り直しは下の手順(10〜12 回目と同じ)。委任文 §4.0 の表は 10 回目のものを使ってよい。**取り始める前に**、10 回目の和のファイル `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/th2/.cat8_downloaded_total` があれば、その中身を `cat` で生ログに印字してから `rm -f` で消す手を打つ(300 MB の和はこの回の分で数える)。取得の手順は下のとおり(10 回目と同じ)。9 回目に `ecosyste.ms` で取った th2-net の 178 件の名前と `size` の一覧を生ログに印字する。**取得の量に上限を置く**(委任文 §6 の 6「本体の一括ダウンロード(数百 MB 以上)はしない」、§10 の禁止の並びにも「本体の一括ダウンロード」がある): 1 件ずつ、`scripts/cat8_repo_fetch.sh th2-net/<名前> /tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/th2/<名前>` を `cat8_step.py --keep 200000` 経由で打つ(`--keep` を付けないと出力が 3,000 文字で切られ、`skipped:` の名前が落ちる。1 MB を超える中身は取らずに、取った分だけを展開する。普通の `git clone` や `git checkout` をすると、除いた中身を git が後から取りに行くので使わない。リードが 2026-09-24 に 3 件で打って確かめた: `th2-net/viewer`(取った量 27.8 MB・ファイル 25,999 件・取らなかったもの 14 件・展開 25,985 件)、`th2-net/.github`(113 件・取らなかったもの 0 件・展開 113 件)、`th2-net/th2-common-py`(127 件・0 件・127 件)。件数が合わない・取得に失敗したなどで道具が 4・5・6 以外の 0 でない終了コードで止まったリポジトリは「取れなかった」に数える)。出力の `files_in_tree(N)` がそのリポジトリの N、`skipped:` の行が取らなかったファイル。検索は展開したファイルに `grep -rIl`(`.git` を除く)で行い、**そのリポジトリの置き場を消してから次へ進む**。出力の `downloaded_bytes`(通信で取った量)の和を 1 件ごとに生ログに残し、和が 300 MB に達したらそこで取得をやめる(委任文 §6 の 6 は「ダウンロード」の量の規則なので、通信で取った量で数える。委任文は「数百 MB」とだけ書き、**300 という数はリードが決めた**。1 件ずつ消してから次へ進むので、置き場に同時にあるのは 1 件だけ)。**和は道具が数える**: 道具は置き場の親(`cat8/venvs/th2/`)の `.cat8_downloaded_total` に取った量の和を足していき、和が 300 MB 以上なら取らずに終了コード 5 で止まる(`downloaded_total_before`・`downloaded_total_after` の行が出る)。消すのは各リポジトリの置き場だけにし、このファイルは回の終わりまで消さない。終了コード 5 が出たら、残りのリポジトリは打たずに、名前を全部「上限で取らなかった」として書き、値は `未判別`、オーナーの PC から取る手順を書く。**止めるのは 8-016 の取得だけで、この回のほかの項目へは進む**(§0 の「途中で止めない」は回全体のこと)。**上限はもう 1 つある(掛かる量が違う)**: 道具は 1 件ごとに、展開する前に展開後の大きさ `expected_checkout_bytes` を手元の中身から計算し、**1 件の展開後が 500 MB を超えるなら展開せずに終了コード 4 で止まる**(500 もリードが決めた数)。展開したあとは、実物の大きさ `checked_out_bytes` を出し、計算と合わなければ終了コード 1 で止まる。終了コード 4 で止まったリポジトリは「大きさで止めた」として名前と `expected_checkout_bytes` を書き、値は `未判別` のまま残す。経路の問題ではないので、下の「オーナーの PC から取る手順」は書かない(リードが次の回の扱いを決める)。`lfs_pointer:` の行のファイル(Git LFS の置き換えの小さな文字列で、中身ではない)は `skipped:` と同じに扱う。**8-016 の N・除外・読んでいない件数は、文で数えずに** `python3 scripts/cat8_fetch_tally.py docs/DATA/probes/20260923_tools_8_run13.log --prefix th2-net/` の出力から取り、この手は `cat8_step.py --keep 2000000` 経由で打って生ログに残す(生ログのその手の最後の行が `== 集計の終わり(誤り N 件)` でなければ出力が切られているので、`--keep` を大きくして打ち直す。和の行は出力の最初と最後の 2 か所に出る。N = 終了コード 0 の N の和。この道具が 0 件でない数の誤りを出したら、`なし` は書かない)。リードは検収で同じ道具を打ち直して数え合わせる。`skipped:` のファイルは、画像・バイナリ・ロックファイル・ソースマップ(`.map`)なら除外として名前と理由を書き、それ以外は「読んでいない」に数える(M < N なら `なし` は書けない)。取れなかったリポジトリ(終了コード 4・5・6 以外で止まったもの)と読んでいないファイルがあれば、名前を全部書き、値は `未判別` のまま、オーナーの PC から取る手順(`gh repo list th2-net --limit 500` と `git clone --depth 1` の繰り返し)を書く。
   - **取得の道具の終了コードの読み分け(まとめ)**: 0 = 取れた / 4 = 1 件の展開が 500 MB を超えるので展開しなかった(「大きさで止めた」、次のリポジトリへ進む)/ 5 = 取った量の和が 300 MB に達したので取らなかった(残りは打たずに「上限で取らなかった」)/ 6 = 置き場が絶対パスでない・和のファイル `.cat8_downloaded_total` が数でない(**直さずに 8-016 の取得をそこでやめ**、そのことと残りの名前を書く。リードが直す)/ それ以外(1・128 など)= 取れなかった。取得に失敗したときも、取った分は和に足される。**どの終了コードでも、止めるのは 8-016 の取得だけで、この回のほかの項目へは進む**(§0 の「途中で止めない」は回全体のこと)。
   - **検索の手の決まり(この回の全部の `なし` に当てる。監査 63 回目)**: (a) **`なし` の根拠にする検索は、`scripts/cat8_search.py` だけで打つ**(監査 63〜65 回目: 間引く書き方を並べて禁じても、並べていない書き方で切れた)。形は `python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run13.log --keep 2000000 --method grep --target <名前> --note <語> -- python3 scripts/cat8_search.py --candidate <台帳の番号 8-NNN> --list <検索するファイルの一覧> --pattern '<正規表現>' --ignore-case` の 1 つだけ(`--` のあとに `bash -c` やパイプ・`;`・リダイレクトを付けない)。この道具は一覧のファイルを全部読み、当たった行を全部出し(400 字を超える行は当たった所の前後 200 字と行の長さを出す)、最後に `cat8_search: complete files=N read=N files_with_hits=F hits=H` を出す。**検索するファイルの一覧は `scripts/cat8_mklist.py` で作る**(手で作った一覧・一部だけの一覧は §7 の 2 の検査が止める = 監査 66 回目)。形は `... -- python3 scripts/cat8_mklist.py --candidate <台帳の番号 8-NNN> --root <置き場> --out <一覧> [--exclude '<置き場からの道>=<理由>' ...] [--add <別の経路で取ったファイル> ...] [--absent '<道>=<理由>' ...]`(これも `cat8_step.py` 経由)。置き場の全ファイル(git の作業木なら `git ls-files`)から `--exclude` の分だけを外し(外してよいのは画像・バイナリ・ロックファイル・ソースマップ。理由を書く)、`--add` の分を足す。取得の道具が 1 MB を超えて取らなかったファイル(`skipped:`)は、別の経路で取って**置き場の同じ道に置き**、その道を `--add` するか、除外の種類なら `--absent '<道>=<理由>'` で名前と理由を残す。**どちらにもしていないファイルがあると、この道具は止まる**(取得の道具の記録 `.git/cat8_missing` を読む。§2 の 2 の候補を同じ取得の道具で取ったときも同じ)。一覧は NUL 区切りで書かれ、終わりの行に一覧の `sha=` が出る。**一覧を作ったあとに書き換えない**(検索の道具も一覧の `sha=` を出し、§7 の 2 の検査が突き合わせる)。置き場の下にシンボリックリンクのディレクトリがあると止まる(中身を置き場に写すか、`--add` する)。**N と M は、この道具の `listed=` の和**(除外のあとの件数。除外・無いものの名前と理由は、この道具の出力に出る)。バイナリや符号化の分からないファイルを一覧に残すと、検索の道具が `INCOMPLETE` と出して止まる(`--exclude` で外すか、読めるようにする)。リポジトリごとに打ってよい(8-016 は 1 件ずつ消すので、1 件ごとに、E3a・E5 の全部の語を 1 つの正規表現に入れて打つ。E3b は 11 回目で受け取ったので、この回は E3b の行を `台帳の値のまま(11 回目の節)` と書く)。**`--candidate` にはその候補の台帳の番号を書く**(§7 の 2 の検査は、`なし` の行の候補の番号と、7 列目の検索の手・その一覧を作った手の番号が同じかを見る。178 件の生ログの行を候補の間で取り違えて引くのを止める = 監査 68 回目)。**`なし` の 7 列目には、検索に使った cat8_search.py の手を全部書く**(§7 の 2 の検査は、7 列目の cat8_search.py の手の `files=` の和が根拠の「読んだ M 件」と同じか、終わりの行があるか、生ログで切られていないか、ほかの検索(grep・rg など)を引いていないかを見る)。1 MB を超えて取らなかったファイルを別の経路で取ったときも、そのファイルを一覧に入れて cat8_search.py で検索する。 (b) **この回に打った cat8_search.py の手の、当たった行のあるファイルは全部**(`なし` の 7 列目に引いた手だけでなく、引かなかった手も)、この回の節の `### 当たりの判定` の小節の表 `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |` に 1 行ずつ書く(道は cat8_search.py の出力の「ファイルごとの当たった行の数」に出たとおりの字。`印` の根拠になった当たりなら、理由の欄にその旨を書く)。理由の欄は空にしない。**理由には、当たった語と、その語がその行で何を指しているかを書く**(「一般的な記述中の語」「「<語>」の使用箇所(例: …)」のような文だけにしない = 監査 70 回目の指摘 4・監査 73 回目の指摘 3)。当たったのが同梱の別のライブラリのファイルなら、L-509(同梱の別のライブラリの機能は候補の機能に数える)に照らして、その語がそのライブラリの検証・品質の機能を指すかも書く。道に `|` があれば `\|` と書く。**当たった行のあるファイルは 1 つも落とさない**。`なし` の根拠の欄には、`一覧 N 件 / 読んだ M 件` に続けて **`当たり F ファイル / H 行`**(7 列目の cat8_search.py の手の `files_with_hits` と `hits` の和)を書く。§7 の 2 の検査は、この数が道具の出力の和と同じか、当たった行のあるファイルの道が `### 当たりの判定` の表に理由つきで全部あるかを見る(理由の中身はリードの検収と監査で読む)。7 列目の手は重複を数えない。(c) 検索の語は、要素の名前だけでなく**設計票 §3 の述語の文の語**を日本語と英語で入れる。最低でも、要素ごとに次の組の**語を全部**、ここに書いた字のとおりに(`look-ahead`・`point-in-time` のように区切りは 1 文字で)、`なし` の根拠に引く手の `$` の行に入れる(§7 の 2 の検査が、組の中の語を 1 つずつ見る。11 回目は組から 1 語で満たし、英語の `leak` を探さなかった。§2 の 2 の候補の全要素にも当たる): E1a = 突き合わせ・reconcil / cross-check・比較・compar / diff・差分・一致・mismatch、E1b = 突き合わせ・reconcil / 別実装・reference implementation・参照実装・cross-check / compar・比較・diff・差分、E2 = 欠損・missing・gap・欠け / 重複・duplicat / 順序・out of order・monoton・sort / 外れ値・outlier・anomal / 時刻のずれ・clock・skew・timestamp / 型・範囲・schema・range・valid、E3a = ルックアヘッド・look-ahead / survivorship・生存者 / leak・リーク・未来 / point-in-time・as-of・時点、E3b = ルックアヘッド・look-ahead / embargo / purge / point-in-time・as-of・時点、E4 = リプレイ・再生・replay / record・記録・capture / playback・再実行・rerun、E5 = 再現・reproduc / seed・乱数 / deterministic・決定的 / snapshot・golden・pin・lock・版の固定、E6 = 検証・verif・validat / 品質・quality / test・assert・check。**§7 の 2 の検査は、(a) の形と、(c) の語の組が 7 列目の cat8_search.py の手の `$` の行に無いことを止める。**同梱の別のライブラリ(`node_modules` など)に当たった行も、L-509 の読みで候補の機能に数えるかを 1 件ずつ見る(当たった語が述語と別の意味 = 構文解析の先読みなど、ならその理由を書く)。
2. **ほかの `未判別` と §4.0 の表**(12 回目の検収・監査 73 回目): (a) **段の `未判別`**: 8-018 `Vibe-Trading` の E1a・E1b・E2・E3b・E4・E5・E6 / 8-019 `AutoHedge` の E1b / 8-021 `Qlib` の E5(例の設定ファイルの `seed: 0` からの外挿だった)/ 8-023 `Backtrader` の E1b / 8-024 `Lean` の E1b・E4(README の一般の説明だけだった)/ 8-025 `FinanceToolkit` の E1b・E2 / 8-027 `Quantreo library` の E3b。段 4 は「その要素の対象のすべてを、その道具の外から持ち込んでも掛けられる」を言う逐語、段 5 はそれに加えて (ア)(イ) を言う逐語を、候補の使い方の文書・API の文書・ソースから引く。候補自身の例の設定ファイルや README の一般の説明からの外挿で段 4 にしない。(b) **値の `未判別`**: 8-017 `Great Expectations` の E1b・E3a・E3b・E4・E5 / 8-020 `OpenBB Terminal` の E1a・E1b・E2・E3a・E3b・E4・E6 / 8-024 `Lean` の E1a / 8-026 `OpenClaw` の全部 / 8-028 `AlgoBuild` の全部。12 回目は Great Expectations・Lean の検索の出力が 8,000,000 字で切れ、OpenBB は当たりを読み切らなかった。**大きい候補は、一覧を部分(ディレクトリごとなど)に分けて cat8_mklist.py で作り、部分ごとに検索する**(`なし` の 7 列目には部分の検索の手を全部書く。部分の `listed=` の和が N・M)。8-026 は取得の道具が展開後 560,058,585 バイトで終了コード 4 だった。この回に限り、8-026 は `CAT8_MAX_CHECKOUT_BYTES=734003200`(700 MB。リードが決めた数)を付けて取り直してよい。8-028 は、登録なしで読める文書・ヘルプ・API の頁を探し、無ければ試した手段を全部書く(登録すれば読めることを一次資料で示せれば状態を `登録が要る` にする)。(c) **§4.0 の表**: 値に `未判別` が無い候補(この回の終わりの時点)について、委任文 §4.0 の表(43 項目)を**その候補について**書き、`深掘り` の条件まで進める(12 回目は 8-019・8-021〜8-023・8-025・8-027 の表を書かなかった = 起動文の書き漏れ)。8-018 の §4.0 の表は 12 回目に書いた。(d) 8-022 `FinGPT` の E2 の `印` の根拠になった当たりの行(`finogrid/agents/audit_governance/agent.py`)には、当たりの表の理由の欄にその旨を書く。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 13 回目の実行(2026-09-24)` の追記だけ**。1〜9 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run13.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログはすべてこれで書く)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` を除いたもの全部(`### 辿る一覧から出た名前` は置かない。この回は辿る一覧を開かない。深掘りした候補の一次資料に別の道具の名前が出ても候補の一覧には載せず、`### 判断に迷った点と問い` に `[それ以外の問い]` で名前と出典を書く)。`### 当たりの判定` の小節を置く(§2 の 1 の (b))。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表(7 列。§2 の冒頭)は、8-001〜8-041 の 41 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(13 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log docs/DATA/probes/20260923_tools_8_run6.log docs/DATA/probes/20260923_tools_8_run7.log docs/DATA/probes/20260923_tools_8_run8.log docs/DATA/probes/20260923_tools_8_run9.log docs/DATA/probes/20260923_tools_8_run10.log docs/DATA/probes/20260923_tools_8_run11.log docs/DATA/probes/20260923_tools_8_run12.log docs/DATA/probes/20260923_tools_8_run13.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 13` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件` と、引いた文が生ログにあるかもここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run13.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: §2 の 2 項目の状態(候補ごと)(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」の一覧 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ **描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧**(候補・要素・読めなかった頁)/ 生ログの行数。
6. 「区分は完了」とは書かない。
