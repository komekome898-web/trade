あなたは本リポジトリの研究・実装エージェントです。オーナー指示 L-355「1で計算し直してみて」・L-356「利確の基準を入り値基準にして」により、① 3 択 A の**順張りの枝の出口だけ**を「入り値から 5 bp で利確」に替えた変種を作り、前半の全連鎖と後半の同じ 2,000 本で損益を出してください。Do not commit. Do not push。

【読了必須】`docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_RESULT_2026-09-21.md`(§7 まで。§7 が今回の動機)/ `docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md`(用語・§2〜§4)/ `scripts/o3c_signal_value.py`(段 1・段 2・`from-rows` の道筋、方策の一覧、出力の列)/ `scripts/o3c_signal_policy.py`(`simulate_cascade`・`price_at_or_after`・`judge_3way_from_prob`)/ `tests/test_o3c_signal_value.py` / `docs/PHASE2/O3C/SIGNAL/VALUE_STAGE2_REPORT_2026-09-21.md`(段 2 の実行の事実。**既存の行は変えない**)。

【定義】方策名 `logistic_3択(順張り利確5bp・入り値基準)`、型 A、遅れ 3 通り(主 1 秒)、費用 3 通り(なし / 主の c / p75。段 1 の `summary.json` の値)。判断・帯・係数・「わからない」・逆張りの枝は `logistic_3択` と同じ。違いは順張りの建玉の出口だけ:
- 順張りで入った建玉は、**入りの約定価格 p_in** から清算の向きに 5 bp 進んだ価格(p_in × (1 + 5e-4) が買い建玉、p_in × (1 − 5e-4) が売り建玉)に**最初に達した約定(入りの約定より後)**で決済する。約定価格はその約定の値段、`出口の理由` = `利確`。
- 届かないまま次の清算のプリントが来たら従来の 3 択(止まる → 決済 / 続く → ホールド / わからない → ホールド)。ホールド中も利確の目標は**元の入り値のまま**(建玉は 1 つ)。連鎖の終わり + d で強制決済(従来どおり)。
- 未来を使わない(利確の約定は入りの約定より後。ts + d の規則は従来どおり)。逆張りの建玉は変えない。

【実行】(1) 前半: 全連鎖 8,931 本に `logistic_3択`(従来)・新しい変種・`完全な判断` の 3 本を通す(前半は読みに数えない)。出力 `backtest_data/o3c_signal_value_20260921/stage1_tp/`。(2) 後半: 同じ 2,000 本(種 20260920、`stage2_secondhalf/cascades.csv.gz` の bundle_id 集合)に**新しい変種だけを 1 回**通し、行を `stage2_secondhalf/cascades.csv.gz` と `prints_policy.csv.gz` に**追加**する(既存の行は 1 行も変えない = 追加前の既存行の MD5 と追加後に既存行だけ抜き出した MD5 が同じことを確認して報告に書く。`MD5SUMS` は作り直す)。**これは後半の 12 度目の読み**(オーナー指示 L-355)として報告に明記。結果を見てから定義を変えない。

【出すもの】前半・後半それぞれ、`logistic_3択` / 変種 / `完全な判断` の 3 本を並べて: 連鎖 1 本の分布(合計・中央値・p25/p75・負の割合・日等重み平均)、最初の行動別(順張り / 逆張り / 入らない)の合計と中央値、**順張りの建玉の出口の理由の内訳**(利確 / 次の清算で決済 / 連鎖の終わり)と各々の本数・損益、利確までの秒の分位(p25/p50/p75)、露出(合計保有時間・bp/h・同時建玉の最大)、対差(変種 − 従来)の中央値と合計(同じ bundle_id で対にする)。表は `q_tp_*.csv` として保存し MD5SUMS に入れる。

【試験】`tests/test_o3c_signal_value.py` に足す: 合成の連鎖で (a) 利確の約定が入りの約定より後で、目標価格に最初に達した約定であること (b) 目標は入り値基準で、ホールド後も更新されないこと (c) 届かない場合は従来の道筋(次の清算で 3 択 / 連鎖の終わり)と一致すること (d) 逆張りの建玉の損益が `logistic_3択` と一致すること (e) 既存の行が変わらないこと。全スイート `PYTHONPATH=src python -m pytest`(`-q` を足さない)を通す。

【報告】`docs/PHASE2/O3C/SIGNAL/VALUE_STAGE2_REPORT_2026-09-21.md` の末尾に「## (6) 順張り利確 5 bp(入り値基準)の変種(L-355・L-356、12 度目の読み)」を足す: 実行したコマンドと出力(全部、開始・終了時刻)、上の表、なぜ(§0.2 の 3 問: 利確で順張りの枝はどう変わったか、外れた側の損はどうなったか、それでも素に届かないなら何が食っているか)、限界。判定語(陽性 / 陰性 / 有意 / 差あり / 検出されず / 支持 / 棄却)を使わない。オーナーに見える文は日本語。コード・コメント・文書にモデル名を書かない。最小 diff。終わったら、前半・後半の 3 本の合計と中央値、順張りの出口の内訳、対差の中央値、試験の件数を短く返してください。
