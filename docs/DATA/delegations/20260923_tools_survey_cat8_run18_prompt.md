# 道具サーベイ 区分 8 — 18 回目の起動文(2026-09-25)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 18 回目**を担当します。この起動文は、それだけで読めるように書いてあります(ほかの回の起動文は読まなくてよい)。

**この回の終わり方**: §2 の全部を済ませてから返す。期限は無い。途中で止めない。予算で止めない(オーナーの決定 L-507「**案A**」)。どうしても決められない要素は `なし` を書かずに `未判別` のまま残し、何を試して何が足りなかったかを書く。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節 `## 区分8 — 18 回目の実行(2026-09-25)` と生ログ `docs/DATA/probes/20260923_tools_8_run18.log`。返答の中身は §7。

## 0. 最初に読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語(§3)と段(§4.1)は設計票の逐語で当てる。**
4. 報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 17 回目の実行(2026-09-25)` の節(33568 行目から)。**この回の節の小節・表の形は、この節を見本にする**(この節は台帳の検査 `check-elements` を 0 件で通っている)。この節の 8-037・8-041 の行と生ログ `docs/DATA/probes/20260923_tools_8_run17.log` も、この回の出発点として読む。
5. 17 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md` を全文(なぜ 8-037・8-041 の要素が `未判別` に戻ったか)。
6. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値。**

## 1. この回の範囲

- **8-037 `AlgoNetwork`**: 値の `未判別` = E1a・E2・E3a・E3b・E6。N は `algorier.com` の文書・ヘルプ・ブログの全部の頁(17 回目の 68 頁。一覧の取り方も 17 回目と同じでよいが、この回に取り直して生ログに残す)。
- **8-041 `Oryon`**: 値の `未判別` = E6。段の `未判別` = E1b(値は `印`。Oryon は特徴量・指標の計算のライブラリで、E1b の述語「当方の計算(約定・損益・指標・特徴量・板の再構成)と同じ種類の出力を、当方と別の実装で出す計算」に当たる。段は §4.1 の逐語で決める)。リポジトリ `https://github.com/lucasinglese/oryon` は `scripts/cat8_repo_fetch.sh` でこの回に取り直す。
- **この回に手を付けないもの**: ほかの 39 行(台帳の値のまま書く)。

## 2. やること

### 2.1 述語と段(設計票の逐語。短く写す)

- 述語は**候補(道具)の機能**に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは数えない。**候補が自分自身の版・ランタイムを試験する仕組み(候補の開発のための試験・CI・`#[test]`・`assert!` など)は `印` に数えない**(オーナーの決定 L-516「**数えません**」)。
- **オーナーが決めた述語の読み**(問いに出さずに当てる): 候補が同梱・呼び出している別のライブラリの機能は候補の機能に数える / LLM への指示文に書かれた検出・検証の指示は数える / 重複をはじく処理は E2 の「重複の検出」に数える / 1 回だけの固定の学習・評価の分け方は E3b に数える(L-508「1〜4全て承認します」・L-509「b」)。乱数の種(`seed`)は段 5 の (ア) に当たらない。
- 設計票 §3 の述語の文を 1 語ずつ当てる。E2 は「時系列・約定・板・足・参照データについて…検出するか報告する機能」、E4 は「記録した市場データを時刻順に再生して…」、E6 は「E1a〜E5 の述語のどれにも当たらないもの」。E6 に置く前に、その機能が E1a〜E5 のどれかに当たらないかを書く(17 回目の AlgoNetwork の E6「independent verification」は E1a と重なりうるので `未判別` に戻った)。
- 段は §4.1 のとおり下から積み上げる。段 4 は「その要素の対象のすべて」を外から持ち込めることを言う逐語が要る(一部だけなら段 3)。段 5 は (ア)(イ) の両方の逐語が要る。**段は道具の側の記述だけで決め、当方が買った・動かしたかでは決めない。**各段の条件を満たすことは、その条件に当たる原文を「…」で 6 列目に引いて示す(段 4・5 は 8 字以上の逐語が要る。検査がこれを見る)。

### 2.2 問いの書き方

- 問いに出した行は、値も段も `未判別` にする(決めてから問いに出さない)。`### 判断に迷った点と問い` の番号付きの行には、全部 `[値・段の問い]` か `[それ以外の問い]` を付ける(付けないと検査が止める)。値・段を決めずにおく理由でない問いは `[それ以外の問い]`。

### 2.3 `印` を先に探す

各要素について、まず候補の文書・README・ソースから、述語に当たる機能を言う逐語を探し、`grep -n` や `sed -n` で生ログに印字して引く(`印` には全件の検索は要らない)。行を生ログに出すのは、読むと決めたファイルの範囲だけ。ソース全体への `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI`・`grep -rcI`)にする。`印` を探す段では `なし` を結論しない。

### 2.4 `なし` は全件の検索からだけ

- 一覧は `scripts/cat8_mklist.py` で作り、検索は `scripts/cat8_search.py` だけで打つ(どちらも `scripts/cat8_step.py` 経由。`--candidate` にその候補の台帳の番号。`--` のあとにパイプ・`;`・リダイレクトを付けない)。形は 17 回目の生ログの手をそのまま見本にする。
- 検索の語には、要素ごとに次の組の**語を全部**、この字のとおりに入れる: E1a = 突き合わせ・reconcil / cross-check・比較・compar / diff・差分・一致・mismatch、E1b = 突き合わせ・reconcil / 別実装・reference implementation・参照実装・cross-check / compar・比較・diff・差分、E2 = 欠損・missing・gap・欠け / 重複・duplicat / 順序・out of order・monoton・sort / 外れ値・outlier・anomal / 時刻のずれ・clock・skew・timestamp / 型・範囲・schema・range・valid、E3a = ルックアヘッド・look-ahead / survivorship・生存者 / leak・リーク・未来 / point-in-time・as-of・時点、E3b = ルックアヘッド・look-ahead / embargo / purge / point-in-time・as-of・時点、E6 = 検証・verif・validat / 品質・quality / test・assert・check。**打つ前に語に誤字が無いかを見る**(誤字の手を打つと、その手の当たりも全部判定が要る)。
- `なし` の根拠の欄に `一覧 N 件 / 読んだ M 件` と `当たり F ファイル / H 行` をこの字のとおりに書く。N と M は cat8_mklist.py の `listed=` の和。7 列目には、その要素の検索の手の行を全部書く(長い出力の中の離れた行を引くときは、引用した行も書く)。
- **当たった行は全部判定する**。やり方は 2 つ:
  - (甲) ファイルの表 `### 当たりの判定`: `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |`。**理由は、そのファイルの当たった行を読んで、当たった語と、その語がそこで何を指すかを書く。**
  - (乙) 決まりで分ける: `scripts/cat8_classify.py`(使い方は 17 回目の生ログの手と、道具の説明 `sed -n 1,60p scripts/cat8_classify.py`)。決まりは当たった行を読んでから書く。決まりに当たらない行は全部 `### 当たりの判定(行ごと)` に 1 行ずつ理由を書き、決まりは `### 決まりの一覧` に全部載せる。
  - **同じ理由を 2 行以上に書きたくなったら、それは決まりにする**(`cat8_classify.py` の規則として書き、生ログに残す)。行の表・ファイルの表には、同じ文を繰り返さない(語尾や語順を変えただけの同じ内容も、同じ理由とみなす。リードが検収で読む)。同じ文を何行・何ファイルにも当てた `なし` は受け取られない(12・13・14・16・17 回目に同じ理由で差し戻した)。決まりにすれば、まとめて判断したことが生ログに残り、リードと監査役が決まりと例の行を読める。
  - 検索した語が `def`・`class`・`fn`・`const`・`let` などに続く名前の中にある行は、道具が決まりに取らせず必ず 1 行ずつの判定に出す。その行は、定義された名前と、その行が何をするかを書いて判定する。
- Oryon の E6 は、17 回目の検索の結果を使わず、この回に検索から打ち直す(生ログはこの回のものだけを根拠にする)。自己試験の根拠(L-516)しか無い要素は、全件の検索で、自己試験のほかに述語に当たる行が無いかを確かめる。自己試験の行も、決まり(例: Rust の `#[test]` の下の行・`assert!` の行を「候補自身の試験で、L-516 により数えない」とする決まり)で分けてよい。その決まりが本当に試験の行だけを取っているかは、決まりの見本の 10 行で確かめられるように書く。

### 2.5 §4.0 の表

2 候補について、委任文 §4.0 の表の項目を書く(17 回目の値を出発点にし、17 回目に `未確認` だった項目は全部この回に試す)。`未確認` のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたものと、試すのに当方の鍵・口座・登録・購入が要るか外へ送信するもの(そう言う逐語を引く)だけ。「時間が掛かる」「時間内に到達しなかった」「優先しなかった」は理由にしない(17 回目はこの理由で 45 件を残した)。

### 2.6 大きさ・届かないとき・取り方

- 生ログと報告は GitHub に押し出すので、どちらも 90 MB 以内に保つ(`wc -c` を検索の手の前後に `cat8_step.py` で打つ)。
- この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書き、「存在しない」とは書かない。
- 取った本文は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の下に置く(`/tmp` の直下に置かない)。
- **WebFetch は一次資料に使わない**(要約を返すため)。まず `curl` で生の HTML を取る。JavaScript で描く頁は `scripts/cat8_render.js` を `cat8_step.py` 経由で打つ(`NODE_PATH=/opt/node22/lib/node_modules NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run18.log --method 公式 --target <名前> --note <何を取るか> --keep 200000 -- node scripts/cat8_render.js <URL> 4000`)。**描画した頁は `印` の根拠にだけ使い、`なし` の根拠には使わない。**`印` の根拠にするのは、次の全部を満たすときだけ: (i) 述語に当たる文を、前後の否定・条件を含めて 1 文まるごと逐語で引く / (ii) その文が本文(`HTTP` の行の下)か、候補自身のドメインの `--- frame <URL>` の下にある / (iii) 出力に `INCOMPLETE text-changed-after-wait` が無い(あれば、もう一度描画して 2 回とも同じに出ることを確かめる)。
- **AlgoNetwork の N の中の別の製品**: `algorier.com` には同じ会社の別の製品 AlgoBuild(台帳の 8-028)の頁がある。AlgoBuild の頁の機能の記述は、AlgoNetwork の `印` の根拠にしない(当たりの判定では「別の製品 AlgoBuild の機能」と書いてよい)。
- 取得の道具 `scripts/cat8_repo_fetch.sh` の終了コード: 0 = 取れた / 4 = 展開が 500 MB を超えるので展開しなかった / 5 = 取った量の和が 300 MB に達した / 6 = 置き場が絶対パスでない・和のファイルが数でない(直さずにその候補の取得をやめてリードに渡す)/ それ以外 = 取れなかった。取り始める前に、置き場の親の `.cat8_downloaded_total` があれば `cat` で生ログに印字してから `rm -f` で消す。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(この回の節の追記だけ。1〜17 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run18.log` の 2 つだけ。scratchpad 以外にファイルを作らない。生ログはすべて `scripts/cat8_step.py` で書く。
- 台帳には書かない。`scripts/` の下のファイルは書き換えない。区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。外部の道具を入れる場所は `.../scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

17 回目の節と同じ小節を置く(`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」)。検査が形を見る所:
- 「要素と段」の表は 7 列 `| 道具 | 要素 | 値 | 段 | 根拠の種類 | 根拠 | 生ログの行 |`。7 列目には `docs/DATA/probes/20260923_tools_8_run18.log:10` か `...:10-20` を空白で区切って並べるだけ(ほかの字を入れない)。値が `印` か `なし` の行は 7 列目を空にしない。引いた文「…」は 7 列目の行の手の出力に出ていること。
- この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` **だけ**にし、値と段は台帳と同じにし、7 列目は空でよい。
- 知見の表の「根拠」の欄には、生ログの参照(上と同じ形)・URL・日付(`2026-09-25`)・「取得日」だけを書く(説明は「知見」の欄に書く)。**候補の一覧と「要素と段」の表(7 列)は、8-001〜8-041 の 41 行全部を載せる**(この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにし、値と段は台帳と同じにする)。変えた要素は候補の一覧の説明に全部書く。

## 5. 禁止

委任文 §10 のすべて。commit も push もしない。モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。

## 7. 返す前に

1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md` に、`docs/DATA/probes/20260923_tools_8_run1.log` から `docs/DATA/probes/20260923_tools_8_run18.log` までの 18 本の生ログを並べて打つ。誤検出は閉じずに行と理由を書く。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 18` を 0 件にする(最後の出力を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run18.log` を 0 件にする(同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0(同じ節に貼る)。
5. 返答には次をこの順で書く: 候補ごと・要素ごとの `印`・`なし`・`未判別`(`未判別` なら何が足りないか)/ この回の `なし` の全部の「一覧 N 件 / 読んだ M 件」と、判定に (甲)(乙) のどちらを使ったか / (乙) の手ごとの決まりの数と、1 行ずつ判定した行の数 / §4.0 で `未確認` のまま残した項目と理由 / 4 つの検査の最後の合計行 / 判断に迷った点と問い(決めずに) / 生ログと報告の大きさ(最後の `wc -c`)。「区分は完了」とは書かない。
