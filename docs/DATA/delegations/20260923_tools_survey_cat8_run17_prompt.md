# 道具サーベイ 区分 8 — 17 回目の起動文(2026-09-25)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 17 回目**を担当します。

**この回の終わり方**: §2 の 1〜3 の**全部を済ませてから**返す。期限は無い。**途中で止めない。予算で止めない**(追補 §5。オーナーの決定 L-507「**案A**」)。どうしても済ませられない候補があったら(一次資料に届かない・登録が要るなど)、その候補の状態を設計票 §2 の語で書き、試した手段を全部書いて次の候補へ進む(止まらない)。**当たりが多くて判定を終えられない要素は、`なし` を書かずに `未判別` のまま残し、どの検索の手の何行が残ったかを書く**(決まりを広げて片付けない。監査 78 回目の指摘 4)。次の回を自分で始めない。新しい検索計画は打たない。

**返すもの**: この回の節(`## 区分8 — 17 回目の実行(2026-09-25)`)と生ログ `docs/DATA/probes/20260923_tools_8_run17.log`。返答の中身は §7 の 5。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `2c178ba75341`)を全文。追補は委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 2 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`(指紋 `fbd510552276`)の §3(記録の形)。**`--deadline` は付けない。**生ログの名前はこの回のものに読み替える。
5. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5(候補の一覧の行の形・「要素と段」の表)と §3〜§6。
6. 3 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run3_prompt.md`(指紋 `1ff8dd5b2d7e`)の §2 の「`なし` と書くための条件」。**この回のすべての `なし` に当てる。**ただし (a) の「文書の全部」と、14 回目の起動文 §2 の N の定義(リポジトリならファイル一覧の全件)が違うときは、**広いほう(14 回目の起動文 §2)を当てる**。
7. **14 回目の起動文** `docs/DATA/delegations/20260923_tools_survey_cat8_run14_prompt.md`(指紋 `9ee8e55478d6`)の §2 を全文(冒頭の「`なし` を書くときの手順」・段の決め方・§2 の 1 の「取得の道具の終了コードの読み分け」と「検索の手の決まり」の (a)(b)(c))。**この回もそのまま当てる。**生ログの名前・回の番号は、この回のもの(`20260923_tools_8_run17.log`・`--round 17`)に読み替える。**§2 の 1 の (b) だけは、この起動文 §2 の 2 で置き換える。**
8. **15 回目の起動文** `docs/DATA/delegations/20260923_tools_survey_cat8_run15_prompt.md`(印 `20260923_tools_survey_cat8_run15_prompt.md@1d21e6c15a85`)の §2 の 2(当たりの判定の (甲)(乙))と §2 の 3(§4.0 の実測)を全文。**この回もそのまま当てる**(生ログの名前・回の番号はこの回のものに読み替える)。**前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run15.md` を全文(とくに §2 と「監査 86 回目」)。**15 回目は、(1) 候補の一覧の説明文に「引き続き未確認」と書いたまま、§4.0 の表ではその項目を埋めていた(同じ節の中で食い違った)。同じ誤りをしない。(2) 検収で、cat8_classify.py が、行の中の検索語の一部にしか決まりが重ならなくても行ごと取っていたことが分かり、道具を直した(行を取るのは、行の中の検索語が全部どれかの決まりに重なるときだけ)。この回はその直した道具を使う。**16 回目の検収** `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run16.md` を全文(とくに §2 と「監査 89 回目」の処置)。**16 回目は、(1) `DEF_KW` で `unmatched` に出た 2,998 行に、語の系統ごとの 11 種類の型の文を当てた(`なし` は受け取っていない)。行の表の理由は、その行を読んで、その行の字(定義された名前と、その行が何をするか)を入れて書く。(2) 古い検索の手の当たりのファイル全部に「打ち直した手と同じ内容」と同じ文を当てた。古い手のファイルの表の理由も、そのファイルの当たった行を読んで書く(打ち直す前に、検索の語に誤字が無いかを確かめて、古い手を作らない)。(3) 設計票 §3 の述語の対象の語(E2 の「時系列・約定・板・足・参照データ」、E4 の「記録した市場データ」)に当たらないものを `印` にした。述語の文を 1 語ずつ当てる。**14 回目の検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run14.md` も全文(とくに §2 と、末尾の「監査 77 回目」の逐語と処置)。**14 回目は、(1) 起動文が「ファイルごとに当たった行を読んで書く」と書いたのに、分類の決まりを Python のスクリプトにして機械的に当てはめた(この作り方の `なし` は受け取っていない)、(2) 段 5 の (イ)「結果を保存して次の実行と比べられる」を、次の実行と比べることを言っていない文(「review and rendering into Data Docs」)で付けた、(3) 突き合わせの対象が候補自身の型に限られると自分で書きながら段 5 を付けた(段 4 は「対象のすべて」を外から持ち込めること)、(4) この環境のプロキシの `CONNECT tunnel failed, response 502` を「DNS 到達不可・存在しない」と書いた。同じ誤りをしない。**
9. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。41 行)。**この回の出発点は台帳の値**。

## 1. この回の位置づけ

この回は **5 候補**: 8-029 `MetaTrader の Strategy Tester`・8-036 `AI Trading Lab`・8-037 `AlgoNetwork`・8-038 `NinjaTrader`・8-041 `Oryon`(§2 の 1)と、その §4.0 の表(§2 の 3)。

**この回に回さないもの(この回で手を付けない)**: 8-026 `OpenClaw`(18 回目)/ 8-030 `dbt`・8-031 `Debezium`・8-032 `Apache Kafka`・8-033 `Prefect`・8-034 `Pandas`・8-035 `Apache Spark`・8-039 `NumPy`・8-040 `SciPy`(19 回目以降。どれも大きいリポジトリで、18 回目の結果を見てから、1 回に入れる数をリードが決める。L-416「**一気に終わらせようとせずに**」)/ 上に無いほかの行(台帳の値のまま書く)。

## 2. やること(番号順。全部済ませる)

各候補について、委任文 §4.0 の表(語彙のすべての項目)と委任文 §4 の全列、「要素と段」の表 8 行、知見の表(設計票 §4.2 の記録する軸)を書く。**各候補を設計票 §2 の `深掘り` の条件まで進める**(委任文 §4.0 の「深掘り」の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い)。

1. **5 候補の `未着手`(E1a〜E6 の全部)**: 台帳の「発見の出典」の記事と、候補の公式の一次資料(製品の文書・ヘルプ・API の頁、GitHub のリポジトリ)が同じものを指すかを、両方の文を生ログに印字して突き合わせてから始める(違えば、そのことを書いて `判別に一次資料が要る` にする)。
   - 8-029 `MetaTrader の Strategy Tester`・8-038 `NinjaTrader`・8-036 `AI Trading Lab`・8-037 `AlgoNetwork` は、製品の公式の文書・ヘルプ・API の頁を取る(`curl` で生の HTML。JavaScript で描く頁は `scripts/cat8_render.js`。条件は 14 回目の起動文 §2 の冒頭)。**`なし` は、文書の頁の一覧を全件で取り(サイトの地図 `sitemap.xml`・目次の頁など、件数で示せる一覧)、その全部を cat8_search.py で検索したときだけ書く**(14 回目の起動文 §2 の N の定義: サイトなら文書・ヘルプ・ブログの全部の頁)。**N の範囲(監査 90 回目の指摘 4・91 回目の指摘 1・3)**: N は 14 回目の起動文 §2 の定義のとおり、候補の公式の文書・ヘルプ・ブログの全部の頁で、この回に新しく外すものは無い。候補が大きな製品の一つの機能のとき(8-029 は MetaTrader の端末の機能 Strategy Tester)は、述語は候補(Strategy Tester)の機能に当て、N はその機能を含む製品の文書・ヘルプ・ブログの全部の頁にする(広いほう。Strategy Tester の頁だけにしない)。どこを N にしたか(サイトの地図・目次の URL と件数)を書く。頁の一覧が取れない・全部を取れないときは `なし` を書かずに `未判別` にし、試した手段を全部書く。登録しないと読めないことを一次資料で示せれば `登録が要る` にする。**この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書く**(監査 77 回目の指摘 3)。
   - **一般的な名前(監査 90 回目の指摘 5)**: `AI Trading Lab`・`AlgoNetwork` のように一般的な名前の候補は、同じ名前の別の製品・会社が無いかを検索し、発見の出典の記事の説明(何をする製品か・誰が出しているか)と、一次資料の説明が同じものを指すことを、両方の文を生ログに印字して示す。どれか決められなければ `判別に一次資料が要る` にし、見つかった同じ名前のものを全部書く。
   - 8-041 `Oryon`(`https://github.com/lucasinglese/oryon`)は `scripts/cat8_repo_fetch.sh` で取る(14 回目の起動文 §2 の 1 と同じ置き場・同じ和のファイルの扱い)。
   - **進め方と生ログの大きさ(16 回目と同じ。5 候補の全部に当てる)**: 生ログと報告は GitHub に押し出すので、1 つのファイルが 100 MB を超えてはならない(GitHub が受け取らない。取得元がサイトでもリポジトリでも同じ)。そこで次の順で進める。
     1. **まず要素ごとに `印` の根拠を探す**(全件の検索より先に。大きい全件の検索を先に打つと、90 MB を使い切って、`印` で済んだはずの要素まで `未判別` になる)。候補の文書・README・ソースから、述語に当たる機能を言う逐語を、`grep -n` や `sed -n` で生ログに印字して引く(`印` は全件の検索を要さない。14 回目の起動文 §2 の冒頭の「引く文は生ログに印字する」のとおり)。段も同じ手で決める。**`印` の条件は緩めない**: 述語は候補(道具)の機能に当て、道具で検証される 1 つの戦略の作り・例のコードの作りは、道具の機能として数えない(14 回目の起動文 §2 の冒頭の逐語)。**設計票 §3 の述語の文を 1 語ずつ当てる**(対象の語 = E2 の「時系列・約定・板・足・参照データ」、E4 の「記録した市場データ」など。16 回目の誤り 3)。**候補が自分自身の版・ランタイムを試験する仕組み**(候補の開発のための試験・CI)は、候補の機能に数えるかが設計票に書かれていない(16 回目の検収 §6。区分の完了のときにオーナーに示す問い)。これに当たる根拠しか無い要素は、**根拠を普通の `印` と同じように全部集める**(述語に当たる逐語・段とその逐語・7 列目の生ログの行)。そのうえで値と段だけを `未判別` にし、`[値・段の問い]` で出して、その行に「自己試験の根拠: 数えるなら `印`・段 N」と書く(オーナーの答えが出たらリードがそのまま当てられるように。根拠を集めずに `未判別` にしない = 監査 90 回目の指摘 1)。その要素の知見の表の軸(設計票 §4.2)も、`印` のときと同じに書く(監査 91 回目の指摘 4)。**その要素も次の 2 の全件の検索に進める**。全件の検索で自己試験の根拠のほかに `印` の根拠が見つかれば `印` にする。見つからなければ、値は `未判別` のまま、「自己試験の根拠のほかは当たりが全部述語に当たらない(一覧 N 件 / 読んだ M 件)」と書く(答えが「数えない」なら、リードがその全件の検索で `なし` を当てる。`なし` とは書かない = 監査 91 回目の指摘 2)。量が多いことは `印` を早く決める理由にならない。**長い出力の中の離れた行を引くときは、手の見出しの行に加えて、引用した文の行も 7 列目に書く**(監査 89 回目の指摘 3)。どこを読んで `印` を探したか(読んだディレクトリ・文書の名前)を、要素ごとに知見の表か `### 判断に迷った点と問い` に書く。**探し方の範囲(監査 88 回目の指摘 1)**: README と文書のディレクトリ(`docs/` など)の目次をまず読む。ソース全体に打つ `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI` や `grep -rcI`)にする。行そのものを生ログに出すのは、読むと決めたファイルの `sed -n '<始め>,<終わり>p'` か `grep -n` だけにする(`印` を探す段で、全件の検索と同じ量を生ログに出さない)。**`印` を探す段では `なし` を結論しない**。`なし` は次の 2 の全件の検索からだけ出す。`印` が見つからない要素だけが次の 2 に進む。リードは検収で、生ログの手の並び(`印` を探す手が、その要素の全件の検索より前にあるか)を見る。
     2. **`なし` を書く要素だけ**、cat8_search.py の全件の検索を打つ(14 回目の起動文 §2 の 1 の (a)(c)。語の組の語を全部入れる)。当たりは (甲) か (乙) で全部判定する。
     3. **生ログ `docs/DATA/probes/20260923_tools_8_run17.log` を 90 MB 以内に保つ**。検索の手を打つ前後に `wc -c` をその生ログに打って、大きさを生ログに残す。次の検索で 90 MB を超えそうなら、その要素は `なし` を書かずに `未判別` のまま残し、どの要素の検索が何行・何バイトになりそうだったか(見積もりの手の出力)を書く(量を理由に `なし` にしない。量を理由に決まりを広げない)。報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(いま約 8 MB)も 90 MB 以内に保つ(`### 当たりの判定(行ごと)` の行が多いと大きくなる)。大きさは、**cat8_search.py・cat8_classify.py の手を打つたびにその前後で、また報告の `### 当たりの判定` の表に行を書き足すたびにその前後で**(監査 88 回目の指摘 2)、2 つのファイルを 1 つの手でまとめて測る: `python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run17.log --method run --target size --note 大きさ -- wc -c docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run17.log`。
   - **段の決め方の注意(監査 77 回目の指摘 1・2)**: 段 4 は、設計票 §4.1 の「その要素の対象のすべて」を外から持ち込めることを言う逐語が要る。対象が候補自身の型・候補自身の口座などに限られると書くなら、段は 3 以下にする(「対象の一部だけが外から持ち込めるものは段 3」)。段 5 の (イ) は「結果を保存して次の実行と比べられる」ことを言う逐語が要る。保存するだけ・見せるだけの文では (イ) にしない。生ログのノート欄は自分の説明なので、逐語の代わりにならない。
   - **「届かない・無い」を書くとき(監査 77 回目の指摘 3)**: 生ログの出力の字をそのまま書き、どの経路で何回試したかを書く。この環境のプロキシの失敗(`CONNECT tunnel failed` など)は「この環境のプロキシ経由で届かなかった」と書き、「存在しない」「DNS で届かない」とは書かない。

2. **当たりの判定(14 回目の起動文 §2 の 1 の (b) をこれで置き換える)**: この回に打った cat8_search.py の手の、当たった行は全部、次の**どちらか**で判定する。
   - (甲) **ファイルの表**: 14 回目の起動文 §2 の 1 の (b) のとおり、`### 当たりの判定` の表 `| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |` に、当たった行のあるファイルを 1 行ずつ書く。理由は、そのファイルの当たった行を読んで書く。
   - (乙) **決まりで分ける**: 当たりが多い手は、`scripts/cat8_classify.py` を `cat8_step.py` 経由で打って、決まりで分けてよい。形は `python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run17.log --keep 2000000 --method run --target <名前> --note <どの検索の手を分けるか> -- python3 scripts/cat8_classify.py --log docs/DATA/probes/20260923_tools_8_run17.log --step <その検索の手の見出しの行の番号> --rules <決まりのファイル> --candidate <台帳の番号 8-NNN>` の 1 つだけ(パイプ・`;`・リダイレクトを付けない)。`--keep` は、出力の最後の行 `cat8_classify: complete ...` が生ログに残る大きさにする。**最後の行が生ログに残らなかった(切れた・落ちた)cat8_classify.py の手が 1 つでもある検索の手は、§7 の 2 の検査が (乙) として数えない**(切れた出力は全部が見えないので、その検索の手は、あとで同じ手に打ち直しても決まりでは分けられない = 監査 78 回目の指摘 2・83 回目の指摘 1・2)。そうなったら、その検索の手の当たりを全部 (甲) の表に書くか、**同じコマンドのまま cat8_search.py を打ち直して新しい検索の手を作り、大きい `--keep`(たとえば `--keep 50000000`)で cat8_classify.py を打つ**(新しい検索の手の見出しの行で `--step` を指す)。新しい検索の手が (乙) で数えられれば、§7 の 2 の検査は、`$` の行が 1 字も違わず、終わりの行も 1 字も違わない(`list=`・`files=`・`hits=`・`sha=` が同じ)古い検索の手を、それで済んだとする(同じ一覧のファイルで打ち直す。一覧を作り直したり書き換えたりした手は同じとしない)。`なし` の 7 列目には、古い手か新しい手の**どちらか 1 つ**を書く(両方を書くと、同じ一覧を 2 回数えることになるので §7 の 2 の検査が 1 回だけ数える。15 回目以降は一覧の `sha=` でも見る = 監査 84 回目の指摘 1・2)。**決まりを見直して同じ検索の手に打ち直してよい。§7 の 2 の検査は、同じ検索の手の cat8_classify.py の手のうち最後の 1 回だけを数える**(前の回の手は数えない = 監査 80 回目の指摘 1)。「同じ検索の手」とは、cat8_classify.py の `--step` が指す**同じ cat8_search.py の手**(生ログの同じ `--- ` の行)のことである。cat8_search.py を打ち直すと別の手になり、その手の当たりは全部 (甲) か (乙) で新しく扱う(監査 81 回目の指摘 3)。`### 決まりの一覧` は最後の 1 回の出力に合わせる。**一度でも `unmatched` に出た行は、打ち直した回で決まりに取られても、`### 当たりの判定(行ごと)` の表に理由つきで残す**(§7 の 2 の検査が、どの回の `unmatched` の行も表にあるかを見る。表に、どの回の `unmatched` にも出ていない行や、同じ行が 2 つあると止める = 監査 81 回目の指摘 1・4)。打ち直したら、何を見直したかを `### 決まりの一覧` の小節の表の下に 1 手 1 行で書く(監査 81 回目の指摘 2。**これは機械では見ない。リードと監査役が生ログと突き合わせて読む** = 監査 82 回目の指摘 3)。
     - 決まりのファイルは scratchpad の下に置き、1 行に 1 つ、タブ区切りで `<決まりの id>` `<当たった行の字に当てる正規表現>` `<その語がそういう行で何を指し、なぜ述語に当たらないか>` を書く。**決まりは、検索の手の出力(当たった行)を読んでから書く。**検索の語の前後の字を決まりに入れる(例: 色名なら `(dark|pale|light)?goldenrod`)。
     - この道具は、空の字に当たる決まり(`.*` など)と、検索した語だけに当たる決まり(`golden`・`\w` など)を止める。また、決まりが検索した語を「覆う」のは、決まりの当たりがその語の当たりと重なるときだけで、**行を取るのは、行の中の検索した語が全部、どれかの決まりに覆われるときだけ**にする(15 回目の検収で直した)。同じ行の別の語に当たる決まり(例 `the`)は何も覆わない。
     - 検索した語が、道具の `DEF_KW` の語(`def`・`class`・`function`・`fn`・`func`・`struct`・`interface`・`impl`・`const`・`let`・`var`・`val`・`type`・`typedef`・`enum`・`trait`・`module`・`namespace`・`macro`・`record`・`object`・`sub`・`proc`・`method`・`export`)に続く名前の中にある行は、どの決まりにも取らせず必ず `unmatched` に出る(監査 78 回目の指摘 1・79 回目の指摘 1)。**この語の無い定義(Python の `replay_helper = 1`・YAML の鍵など)は守られない**ので、そういう行を決まりに入れない。
     - 出力には、決まりごとの取った行の数・当たりに占める割合・ファイルごとの取った行の数・取った行から均等に選んだ 10 行と、どの決まりにも当たらない行の全部(`unmatched<タブ><道>:<行><タブ><字>`)が出る。**決まりの中身と出力は生ログに残り、リードと監査役が決まりと例の行を読む**(この道具は、検索した語に、ありふれた前後の字をわざと付けた決まりと、定義でない行に偽りの理由を付けた狭い決まりと、`DEF_KW` の語の無い定義を取る決まりを止められない)。**(乙) を打った手ごとに、この回の節の `### 決まりの一覧` の小節に表 `| 検索の手の行 | 決まりの id | 正規表現 | 理由 | 取った行の数 | 取った行のあるファイルの数 |` を書き、決まりを 1 つ 1 行で全部載せる**(監査 78 回目の指摘 5)。1 列目は `docs/DATA/probes/20260923_tools_8_run17.log:<検索の手の見出しの行>`(**cat8_search.py の手**の `--- ` の行の番号。cat8_classify.py の手の見出しの行ではない。例: 検索の手の見出しが 1200 行目、分類の手の見出しが 1310 行目なら、1 列目は `docs/DATA/probes/20260923_tools_8_run17.log:1200` = 監査 80 回目の指摘 5)、3〜6 列目は cat8_classify.py の出力の字と数のとおり(各列の字をバッククォートで囲んでもよい。正規表現の中の `|` は `\|` と書く。書き忘れると列の数が 6 でなくなり、§7 の 2 の検査が止める。取ったファイルの数は、その決まりの `  file` の行の数)。**§7 の 2 の検査が、この表と生ログの出力を突き合わせ、無い・違う決まりがあれば、その手を (乙) として数えない**(監査 79 回目の指摘 3)。
     - **どの決まりにも当たらない行は、全部**、この回の節の `### 当たりの判定(行ごと)` の小節の表 `| ファイルの道と行 | 当たった行の字 | その語がその行で何を指し、なぜ述語に当たらないか |` に 1 行ずつ書く。1 列目は `unmatched` の行に出た `<道>:<行>` の字のとおりに書く(道に `|` があれば `\|`)。理由はその行を読んで書く。
     - `印` の根拠になる行は、決まりに入れず、`unmatched` に出して行の表に「`印` の根拠」と書く。
   - 同じ検索の手を、(甲) と (乙) の両方で扱ってよい。§7 の 2 の検査は、(乙) を打った検索の手については、`unmatched` の行が全部行の表に理由つきであれば、ファイルの表を求めない。(乙) を打っていない検索の手の当たりは、全部 (甲) の表に要る。
   - **スクリプトを自分で書いて分類しない**(14 回目の誤り 1)。分けるのは cat8_classify.py だけ。scratchpad にスクリプトを置いて打った分類は、この回の `なし` の根拠にしない。
   - 当たりの数・`当たり F ファイル / H 行` の書き方は、14 回目の起動文 §2 の 1 の (b) のとおり。

3. **§4.0 の表**: 5 候補について、委任文 §4.0 の表(43 項目)を書き、`深掘り` の条件まで進める。**全部の項目をこの回に試す**(15 回目の起動文 §2 の 3 と同じ)。`未確認` のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたもの(導入が失敗した・一次資料に届かなかった)と、試すのに当方の鍵・口座・登録が要るか外へ送信するもの(そう言う一次資料の逐語を引く)だけ。「時間が掛かる」「優先しなかった」は残す理由にしない。導入は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下の隔離環境だけに入れる。最小実行は、外へ送信しないもの(当方のデータ・鍵を使わない、公開の例のデータ)にする。打てなかった項目は、何を試して何が出たかを生ログの行を添えて書く。

## 3. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 17 回目の実行(2026-09-25)` の追記だけ**。1〜16 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run17.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` と `check` 以外を打たない。`scripts/cat8_step.py`・`cat8_repo_fetch.sh`・`cat8_mklist.py`・`cat8_search.py`・`cat8_classify.py`・`cat8_fetch_tally.py`・`cat8_render.js` は打ってよい(生ログはすべて cat8_step.py で書く)。`scripts/` の下のファイルは書き換えない。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ。

## 4. この回の節に置くもの

2 回目の起動文 §3 の「必ず置く小節」から `### 1 回目の訂正` を除いたもの全部(`### 辿る一覧から出た名前` は置かない。この回は辿る一覧を開かない。深掘りした候補の一次資料に別の道具の名前が出ても候補の一覧には載せず、`### 判断に迷った点と問い` に `[それ以外の問い]` で名前と出典を書く)。`### 当たりの判定` の小節と、(乙) を使ったなら `### 当たりの判定(行ごと)` と `### 決まりの一覧` の小節を置く(§2 の 2)。`### 検索計画` には「この回は新しい検索計画を打たない(委任文 §2)」と書く。`### 代替経路` の本文の 1 行目は空にしない。**候補の一覧と「要素と段」の表(7 列。14 回目の起動文 §2 の冒頭)は、8-001〜8-041 の 41 行全部を、この回の値で載せる**(この回に触らない行は台帳の値)。候補の一覧の各行の変更点の説明には、変えた要素を全部書く(表の変更と食い違わせない)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**この回は予算で止めない**(追補 §5、L-507)。報告の `### 予算` には「この回は予算で止めない(追補 §5)」とだけ書く。時刻は書かない(リードが検収で生ログから取る)。

## 7. 返す前に(17 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log docs/DATA/probes/20260923_tools_8_run3.log docs/DATA/probes/20260923_tools_8_run4.log docs/DATA/probes/20260923_tools_8_run5.log docs/DATA/probes/20260923_tools_8_run6.log docs/DATA/probes/20260923_tools_8_run7.log docs/DATA/probes/20260923_tools_8_run8.log docs/DATA/probes/20260923_tools_8_run9.log docs/DATA/probes/20260923_tools_8_run10.log docs/DATA/probes/20260923_tools_8_run11.log docs/DATA/probes/20260923_tools_8_run12.log docs/DATA/probes/20260923_tools_8_run13.log docs/DATA/probes/20260923_tools_8_run14.log docs/DATA/probes/20260923_tools_8_run15.log docs/DATA/probes/20260923_tools_8_run16.log docs/DATA/probes/20260923_tools_8_run17.log` を 0 件にする。誤検出は閉じずに行と理由を書いて渡す。最後の出力の全文を `### 受け入れ検査の出力` に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 17` を 0 件にする(`なし` の `一覧 N 件 / 読んだ M 件`・引いた文が生ログにあるか・当たりの判定の表(ファイルの表と行の表)もここで見る。最後の出力の全文を同じ節に貼る)。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run17.log` を 0 件にする(最後の出力を同じ節に貼る)。
4. `git diff -U0 HEAD -- docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` が 0 であること(出力を同じ節に貼る)。
5. 返答には、次をこの順で書く: §2 の 1・3 の状態(候補ごと・要素ごとに、`印`・`なし`・`未判別` と、`未判別` なら何が足りないか)/ 生ログの大きさ(最後の `wc -c`)/ §4.0 の表で `未確認` のまま残した項目の全部と、その理由(`深掘り` にできなかった候補は、何が足りないかと試した手段)/ この回に書いた `なし` の全部について「一覧の件数 N / 読んだ件数 M」と、判定に (甲)(乙) のどちらを使ったか / (乙) を使った手ごとの、決まりの数と `unmatched` の行の数 / §1 の「この回に回さないもの」にこの回で手を付けていないこと / 4 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに。決めずに出した問いの対象の行は `未判別` のままにする)**/ **描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧**(候補・要素・読めなかった頁)/ 生ログの行数。
6. 「区分は完了」とは書かない。
