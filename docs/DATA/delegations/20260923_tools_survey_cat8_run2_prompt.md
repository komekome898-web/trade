# 道具サーベイ 区分 8 — 2 回目の起動文(2026-09-23)

あなたは本リポジトリの調査班です。**区分 8(検証・再現・品質)の 2 回目**を担当します。

## 0. 最初に全部読むもの(この順)

1. 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)を全文。
2. 区分 8 の追補 `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `46258eebcd93`)を全文。追補に書いたことは、委任文の同じ箇所より優先する。
3. 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` の §2〜§5 を全文。**述語・段の条件は設計票の逐語で当てる。**
4. 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md`(指紋 `0ac68ca101f9`)の §2 の 3・5、§3〜§6。**この回もそのまま当てる**(候補の一覧の行の形・「要素と段」の表・線引き・書き換えの範囲・禁止・申し送り)。書き込み先と生ログの名前だけは、下の §4 のとおり 2 回目のものに読み替える。
5. **前回のリードの検収** `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run1.md` を全文(とくに §4 落とされた候補、§5〜§7、§11 問いへの答え、§12、§13 差し戻し、§14 監査)。**前回の問いへの答えは §11 にある。**
6. 台帳 `docs/DATA/tools_catalog_cat8.tsv`(読むだけ。今は 8-001〜8-016 の 16 行)。

**この回の終わり方(有界)**: 次のどちらか早い方で終わる。(a) §2 の 1〜4 を順に進めて予算(§6)に達した / (b) §2 の 1〜4 をすべて済ませた。どちらでも §7 を済ませてから返す。**次の回を自分で始めない。新しい検索計画は打たない**(委任文 §2「前回の残りの候補名があれば、まずそれを深掘りする(検索計画は打ち直さない)」)。

## 1. この回の位置づけ

1 回目は 8 件を調べたが、リードの検収で一部を受け取り、次を差し戻した(検収 §13 の (1)〜(7)): 検索の結果に出た候補 8 件を一覧から落としていた(リードが台帳に 8-009〜8-016 で足した)/ `qf-lib` の「再現の機能は無い」が誤りだった / 生ログが 1 手ずつ記録されていなかった / 知見の表が受け入れ検査に読まれない形だった / ほか。**この回は、まず 1 回目の訂正を済ませ、次に残りの候補を番号順に深掘りする。**1 回で区分を終わらせようとしない(オーナー逐語「**一気に終わらせようとせずに**」L-416)。

## 2. やること(この順。予算に達したら、そこで止めて §7 へ)

1. **1 回目の訂正**(検収 §13 の (2)(3)(6)(7) と §8・§11)。対象は 8-001〜8-008 の 8 件。
   - 1 回目の「要素と段」の表で値が `なし` の **21 行すべて**(`awk '/^### 要素と段/,/^### 知見/' docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '| なし |'` で出る行)を、原典で確かめ直す。**どこを読んだか(ファイルと行、または URL と節)と打ったコマンドを生ログに残す。**読んで機能が無いと書けたら `なし` のまま根拠を書き直し、機能があれば `印` に直し、読み切れなければ `未判別`。
   - `qf-lib`(8-001)の E5 と E2: 検収 §5・§11 の 1 の原典の箇所を読み、述語を当て直す。E2 は、導入済みの `qflib_venv` の `qf_lib` パッケージ**全体**を、E2 の述語の 7 種(欠け・重複・順序の乱れ・時刻のずれ・外れ値・型や範囲の違反・情報源間の食い違い)に当たる語(例として決めない。7 種それぞれについて自分で語を選び、選んだ語を生ログに残す)で検索し、当たった関数を読んで述語を当てる。**7 種のどれにも当たる関数が無いと読めたら `なし`(検索した範囲と語を根拠に書く)、読み切れなければ `未判別`。**`qf-lib` の状態(深掘りかどうか)も設計票 §2 の定義で当て直す。
   - `prediction-market-backtester`(8-003)の README の `make setup` の節の「optionally verifies `DATA_SHA256`」を、E2・E6 の述語に当てる。
   - PineForge(8-002)の E4: 検収 §11 の 4 のとおり、E4 の述語に当たる再生の原文を示す。示せなければ `未判別`。
   - 出典の表の全行に取得日を書く。報告の「312 戦略」「413 件」などの数に出所(URL と行)を書く。
   - 「この環境から不可」と書いた項目があれば、委任文 §5-1 の試した手段の一覧と、**第 2 経路(オーナー PC)でそのまま打てるコマンド**を書く。
   - 1 回目の 4 軸で印が `推定` の行は、何から外挿したかを根拠に書く(書けなければ `未確認`)(検収 §11 の 6)。
   - 1 回目の報告で判定を言っている文(検収 §8 の「判定を言う文」、報告 467 行)は、判定ではなく問いの形に直して、この回の問いの一覧に移す。
   - **1 回目の §4.0 の表で値が変わる項目**(少なくとも `qf-lib` の `再現性`。ほかに訂正で変わった項目があればすべて)は、この回の `### 4.0 機械可読の表` に**新しい値で**書き、根拠を付ける(1 回目の表の古い値と食い違ったまま残さない = 監査 11 回目の指摘 2)。
   - **書き方**: 1 回目の節は書き換えない。この回の節に `### 1 回目の訂正` を作り、直した行を「1 回目の報告の行番号 → 直した値と根拠」の形で全部書く。そのうえで、この回の「候補の一覧」と「要素と段」の表に 8-001〜8-008 も**今の値で**載せる(台帳はリードがこの回の値で書き直す)。
2. **段と突き合わせる外部の枠組み**: 1 回目に検索の要約だけで引いた Testing Maturity Model(生ログ 1 回目の 108 行付近)の**一次資料の本文**を開き、各段の名前と定義を逐語で書く(URL と取得日)。**本文を読まずに「」で引かない**(1 回目は検索の要約を「」で引いていた = 検収 §8)。段の比較はリードがする。調査班は段を変えない。
3. **残りの候補の深掘り**: 台帳の 8-009〜8-016 を**番号順**に。深掘りした道具は委任文 §4.0 の表(語彙のすべての項目)と §4 の全列、「要素と段」の表 8 行を書く。論文(8-010・8-011)は道具ではないが候補として扱い、§4.0 の項目で当たらないもの(版・週 DL 数など)は `該当なし(論文)` と書き、論文が示す実装・コード・データの所在があればそれを書く。深掘りできなかった候補は「浅い(何が未確認か)」か `未着手` で残す。
4. **辿る一覧**(検収 §4 の後半): dev.to の reconciliation tools の記事 / X の `aiwithjainam` の投稿のリポジトリ列挙 / zenn の仮説検証フレームワークの記事 / quantreo・algorier・fortraders の look-ahead bias の解説記事 / 1 回目の検索計画 7 の結果に出たライブラリ。それぞれを開き、**出てくる道具の名前を全部**、この回の節の `### 辿る一覧から出た名前` の小節に `- \`<名前>\` — <URL(一覧に書かれていたもの。無ければ「URL なし」)> — <出た一覧>` の形で書く。**候補の一覧には載せない**(候補の一覧に載せると「要素と段」の 8 行が要るため。リードが台帳に `未着手` で足し、次の回に回す = 監査 11 回目の指摘 4)。**書かない名前があれば、書かない理由を 1 つずつ書く**(道具でない・既に台帳にある、など。専用かどうか・有名かどうかは理由にしない = 委任文 §10 の当否判定)。

## 3. 記録の形(1 回目との違い)

- **生ログは、手を打つたびに `scripts/cat8_step.py` を通して書く**(検収 §7)。シェルのコマンドは `python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run2.log --method run --target <語> --note <文> -- <コマンド>`。WebSearch・WebFetch・x_fetch などシェルの外の道具は、返ってきた結果(題名と URL の一覧・取得した本文の先頭)を**要約せずに**標準入力に流して `--manual "<打った問い>"` で書く。**生ログを手で書かない。あとからまとめて書かない。**
- **知見の表の形は委任文のものに揃える**: 見出し `| # | 知見 | 印 | 根拠 |`。`#` は数字、印は委任文 §4.1 の 5 語。道具・要素・記録する軸(設計票 §4.2)は「知見」の列に「`<道具>` / `<要素>` / `<軸>`: 」に続けて原文(逐語)で書く。根拠は URL と取得日か生ログの行(検収 §2・§12)。
- **候補の一覧**: 状態が `深掘り` の行には必ず行頭に `[深掘り]` を付ける。付けない行を `深掘り` にしない(検収 §11 の 2)。注記と行を食い違わせない。
- **この回の節に必ず置く小節**(受け入れ検査 K3 が見る 6 つ + この回のもの): `### 検索計画`(「この回は新しい検索計画を打たない(委任文 §2)」と書く)/ `### 出典` / `### 知見` / `### 候補の一覧` / `### 要素と段` / `### ツール1件ごとの表` / `### 4.0 機械可読の表` / `### 1 回目の訂正` / `### 辿る一覧から出た名前` / `### 予算` / `### 受け入れ検査の出力` / `### 判断に迷った点と問い`。
- **`### 4.0 機械可読の表` を空にしない**: 受け入れ検査 `check_scan_report.py` の `scope()` は、§4.0 の表の行が 1 行も無い節を**丸ごと検査の外**にする(監査 11 回目の指摘 1。そうなると「0 件」は「読まれずに 0 件」になる)。§2 の 1 の訂正で変わった項目を必ずこの表に書くので、少なくとも 1 行はある。

## 4. 書き換えの範囲

- 書いてよいのは `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(**この回の節 `## 区分8 — 2 回目の実行(2026-09-23)` の追記だけ**。1 回目の節は書き換えない)と `docs/DATA/probes/20260923_tools_8_run2.log` の 2 つだけ(追補 §1)。scratchpad 以外にファイルを作らない。
- 台帳には書かない。`scripts/cat8_ledger.py` は読むだけの `check-elements` 以外を打たない。`scripts/cat8_step.py` は打ってよい(生ログに書く道具)。
- 区分 1 の報告・台帳・索引、`src/bot/bt/`・`tests/bt/`・`docs/DISCUSSIONS/2026-09-23_backtest_env/` には触らない。
- 外部の道具を入れる場所は `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/` の下だけ(1 回目の `qflib_venv` はそこにある。使ってよい)。

## 5. 禁止

委任文 §10 のすべて。**commit も push もしない。**モデル名を書かない。オーナーに見える文はすべて日本語。

## 6. 予算

**25 万トークン・25 分**を上限の目安にする(追補 §5)。1 回目は 32.5 万トークンで約 3 割超えた。トークンの数はあなたから見えないので、**時間で区切る**(監査 11 回目の指摘 6): 始めに `python3 scripts/cat8_step.py … --method budget --target 開始 --note 開始時刻 -- date -u +%FT%TZ` を打ち、§2 の 2・3・4 に入る前と、§2 の 3 で候補を 1 件始める前にも同じように打つ。**開始から 20 分を過ぎていたら、次の段にも次の候補にも入らずに §7 へ進む。**§2 の 1 と 2 を先に済ませ、3 は番号順に進める。中断するときは委任文 §7 のもの(候補の一覧の全部 / 深掘り済み・浅い・未着手の印 / 残りの候補名)を必ず書く。報告に、この回のおおよその消費(自分で分かる範囲)を書く。

## 7. 返す前に(2 回目の完了の形)

完了の形は設計票 §2 の表の「1 回の実行」の行の (a)(b)(追補 §7)。つまり:
1. `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run1.log docs/DATA/probes/20260923_tools_8_run2.log` を打ち、当たった件数を 0 にする。誤検出だと思っても自分で閉じず、行と理由を書いて渡す。最後に打った出力の全文を、この回の「受け入れ検査の出力」の節に貼る。
2. `python3 scripts/cat8_ledger.py check-elements docs/DATA/SCAN_2026-09-23_tools_cat8.md --round 2` を打ち、当たった件数を 0 にする(最後の出力の全文を同じ節に貼る)。この回の候補の一覧の全行が `(8-NNN)` か `(新)` で、全行に状態と「要素と段」の表の 8 行があり、値が `印` の要素には段が書かれていること。
3. `python3 scripts/cat8_ledger.py check "" docs/DATA/probes/20260923_tools_8_run2.log` を打ち、生ログの形が 0 件であること(最後の出力を同じ節に貼る)。
4. `git diff -U0 docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep '^-[^-]' | wc -l` を打ち、**1 回目の節から消えた行が 0 行**であることを確かめる(出力を同じ節に貼る。0 でなければ直す = 監査 11 回目の指摘 7)。
5. 返答には、次をこの順で書く: §2 の 1〜4 それぞれの進み具合(済み / 途中 / 未着手)/ 1 回目の訂正の件数(`なし` の 21 行のうち、`なし` のまま・`印` に直した・`未判別` に直した、の数と番号)/ 候補の数(台帳にあったもの・`新`・状態ごと)/ 3 つの検査の最後の出力の合計行 / **判断に迷った点と問いの一覧(決めずに)**/ 生ログの行数。
6. 「区分は完了」とは書かない。
