あなたは本リポジトリの調査班です(`.claude/skills/research-squad` モード 2「市場・環境調査(scan)」の固定手順に従う)。テーマは「**トレードに使えるあらゆるツール**」。オーナー指示(2026-09-21、L-377)「**私がXのオススメ欄を見るだけでたくさんトレードのツールは出てくる。それが今の環境以下である保証は調べていないのでないはず。バックテスト用に限らず、調査班にありとあらゆるツールを調べさせろ。**」

【対応表(CLAUDE.md §0.1)】
| やること | オーナーの原文の該当語(逐語) |
|---|---|
| バックテストに限らず、トレードに使えるツールを幅広く集める | 「**バックテスト用に限らず、調査班にありとあらゆるツールを調べさせろ**」 |
| X(旧 Twitter)を発見の経路に入れる | 「**私がXのオススメ欄を見るだけでたくさんトレードのツールは出てくる**」 |
| 「今の環境以下」と決めつけず、比べるための事実を集める(当否は判定しない) | 「**それが今の環境以下である保証は調べていないのでないはず**」 |
| 無料で使えるものに限る(有料は名前・料金・鍵の要否だけ記録) | オーナー規則 L-124(有料インフラを提案しない)。逐語は「**サーバー借りるのは収益算段が立ってからやって何回言ったらわかんねん**」 |
| 分類 8 区分 × 1 回の実行で 1 区分(予算 = スキルの固定値: 5 万トークン・20 分) | (該当語なし。スキルの固定手順による区切り。L-179「スキルの承認をもって聞かずに当ててよい」) |
| 各ツールを §5.5 の 4 軸で記述する(道具として入れられるか / 当方に無い情報 / 当方に無い視点 / 既存の成果を向上できるか) | (該当語なし。`delegated-study` §5.5 の固定の型) |

【担当する区分】この実行では区分 __N__(リードが起動時に指定)を担当する。区分の一覧:
1. バックテスト・シミュレーション(足単位 / ティック単位 / 板の待ち行列(指値の埋まり方)を持つもの / イベント駆動 / ベクトル化。例の名前は挙げないので自分で集める)
2. 執行・bot の枠組み(取引所接続、注文管理、紙運用 = paper、リスク管理。国内取引所(bitFlyer・GMO・bitbank)対応の有無を列に持つ)
3. データ(約定・板・清算・建玉・資金調達率・オプション。無料の履歴とリアルタイム、保存形式、地域制限)
4. 研究・特徴量・統計(時系列、機械学習、多重比較・事前登録の道具、ウォークフォワード)
5. 監視・可視化・記録(ダッシュボード、通知、取引日誌、実験の追跡)
6. LLM・AI エージェントを使うトレード(判断・調査・コード生成の枠組み。無料で試せるもの)
7. スクリーニング・スキャン・アラート(価格・出来高・清算・ニュース・X の流れ)
8. 検証・再現・品質(別実装との突き合わせ、データ品質の検査、ルックアヘッド検出、リプレイ)

【手順(スキル §4 の固定手順 + 追加)】
1. **検索計画を先に書く**: 検索幅 3 段(狭い / 中間 / 広い)× 日本語 / 英語 = 最大 6 クエリ。**発見の経路は 3 つ**: (a) `WebSearch` 一般、(b) X(`x-research` の手順: `WebSearch` で `site:x.com <語>` を語を変えて 3 回以上 → `python3 scripts/x_fetch.py <URL>` で本文を逐語で取る。1 秒 1 件以下)、(c) GitHub / PyPI / 公式サイト(星の数・最終更新日・ライセンスを一次資料から)。
2. 出典ごとに URL・取得方法・取得日(UTC)を記録。X の投稿は URL・著者・日時・本文(逐語)・反応数を記録し、個人の連絡先は写さない。
3. **ツール 1 件ごとに次の列を埋める**(埋まらない列は「未確認」と書く。空欄にしない): 名前 / 種別 / 何ができるか(一次資料の逐語 1〜2 行) / 料金(無料の範囲・有料の境目) / 鍵・ログインの要否 / ライセンス / 言語・動作環境 / 対応取引所(国内の有無) / 最終更新日と活動(星・コミット) / **この環境からの到達(HEAD/GET のプローブ 1 件、HTTP コード。生ログ `docs/DATA/probes/20260921_tools_<区分>.log`)** / §5.5 の 4 軸(道具として入れられるか / 当方に無い情報が取れるか / 当方に無い視点で分析できるか / 既存の成果を向上できるか。**4 軸それぞれに 事実 / 推定 / 仮定 の印**)/ 出典。
4. **今の環境との比較の材料**を列に持つ(判定はしない): 当方の対応物(`src/bot/backtest/engine.py` / `scripts/o3c_signal_*.py` / `scripts/record_*.py` / `scripts/dashboard.py` など。無ければ「無し」)と、その対応物に**無い機能**を一次資料の逐語で 1 行。「今の環境で十分」「不要」という語は書かない(判定はリード)。
5. 知見ごとに 事実 / 推定 / 仮定 の印と「このプロジェクトへの含意」列(無ければ「無し」。憶測で作らない)。
6. 出力は `docs/DATA/SCAN_2026-09-21_tools.md` に区分ごとの節を**追記**する形(スキル §4 のテンプレート + 上のツールの表)。`STRATEGY_IDEAS.md` / `DATA.md` 向けの一行候補は末尾に提案として(マージしない)。
7. 予算(5 万トークン・20 分)に達したら中断し「未完了(どこまで)」と書く。件数の目標は置かない(見つかった分を全部書く。少なく見せない)。

【禁止】戦略・ツールの当否判定(「使える / 使えない / 今の環境以下」)を書かない / 事前登録の変更 / 自動マージ / 大きい一括ダウンロード(プローブは HEAD/GET 数件・小サイズ)/ 鍵・ログイン・課金の経路(有料は名前と料金の記録だけ)/ 秘密情報を書かない / モデル名を書かない / Do not commit. Do not push.

【完了の形】担当区分について、検索計画・出典表・ツールの表(全列)・知見の表・生ログ・候補の一行、が `SCAN_2026-09-21_tools.md` に揃っていること。到達不能は「この環境から不可(方法・HTTP コード)」+ 第 2 経路(オーナー PC)を必ず書く。
