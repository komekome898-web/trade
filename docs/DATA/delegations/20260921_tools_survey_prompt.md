あなたは本リポジトリの調査班です(`.claude/skills/research-squad` モード 2「市場・環境調査(scan)」の固定手順に従う)。テーマは「**トレードに使えるあらゆるツール**」。オーナー指示(2026-09-21、L-377)「**私がXのオススメ欄を見るだけでたくさんトレードのツールは出てくる。それが今の環境以下である保証は調べていないのでないはず。バックテスト用に限らず、調査班にありとあらゆるツールを調べさせろ。**」/ L-378「**無料に限るってだけだと、まともに調査せず有料の文字が見えただけで排除して無料で使える手段や用途を見逃す。逆に無料ってだけで採用して実はAPI課金性ですって使い始めてから言うんやろ。ちゃんと調べさせる気のない委任文出すなよ**」

【対応表(CLAUDE.md §0.1)】
| やること | オーナーの原文の該当語(逐語) |
|---|---|
| バックテストに限らず、トレードに使えるツールを幅広く集める | 「**バックテスト用に限らず、調査班にありとあらゆるツールを調べさせろ**」 |
| X(旧 Twitter)を発見の経路に入れ、実際に使っている人の投稿も集める | 「**私がXのオススメ欄を見るだけでたくさんトレードのツールは出てくる**」 |
| 「今の環境以下」と決めつけず、比べるための事実(当方に無い機能を一次資料の逐語で)を集める。当否はリードとオーナー | 「**それが今の環境以下である保証は調べていないのでないはず**」 |
| **料金で足切りしない。**有料の語が見えても外さず、無料で使える範囲・用途を調べる。逆に「無料」の語だけで済ませず、当方の用途で実際に使ったときに課金が発生する条件(API 課金・従量・鍵・依存する有料データ)を一次資料で確かめる | 「**無料に限るってだけだと、まともに調査せず有料の文字が見えただけで排除して無料で使える手段や用途を見逃す。逆に無料ってだけで採用して実はAPI課金性ですって使い始めてから言うんやろ**」 |
| 可能なものはこの環境で実際に入れて動かす(導入 → 読み込み → 最小の実行)。動かした記録を残す | 「**ちゃんと調べさせる気のない委任文出すなよ**」(調べる = 記述ではなく実行) |
| 8 区分 × 1 回の実行で 1 区分(予算 = スキルの固定値: 5 万トークン・20 分。足りなければ「未完了(どこまで)」で続きを次の実行に回す) | (該当語なし。スキルの固定手順による区切り。L-179「スキルの承認をもって聞かずに当ててよい」) |
| 各ツールを §5.5 の 4 軸で記述する | (該当語なし。`delegated-study` §5.5 の固定の型) |

【当方の具体的な用途(比較の物差し。これに対して「使えるか」ではなく「何がどう変わるか」を事実で書く)】
U1 清算の連鎖の模擬: Binance の清算プリント(456 日、53,398 件)と約定(aggTrades)で、清算ごとに 3 択の判断 → 成行で入る / 指値で利確 / 次の清算で決済 or ドテン / 連鎖の終わりで決済、を遅れ d 付きで再生し、連鎖 1 本ごとの損益・保有時間・同時建玉を出す(今は `scripts/o3c_signal_value.py` の手書き)。
U2 指値の埋まり方: ティック単位で「同じ値段の先行注文・板の厚み」を考えて指値が埋まるかを模擬する(今は「目標価格に最初に達した約定で埋まった」の楽観)。
U3 bitFlyer FX_BTC_JPY(CFD)での実行: 板・約定の記録(今は `scripts/record_*.py` の WS 記録)、paper 運用(今は `scripts/run_paper.py`)、実弾は未承認。
U4 費用の実測: 実効スプレッド・滑り・遅れを実データから測る(今は `scripts/o3c_bitflyer_spread.py`)。
U5 別実装との突き合わせ(再現ゲート)とルックアヘッドの検出(今は無い)。
U6 データの調達と品質(清算・板・建玉・資金調達率の無料の履歴。今は `docs/DATA.md` の台帳と `scripts/data_quality.py`)。
U7 実験の追跡と多重性の台帳(今は文書の手書き)。
U8 監視・通知(今は `scripts/dashboard.py` と Discord 通知)。

【担当する区分】この実行では区分 __N__(リードが起動時に指定)を担当する。
1. バックテスト・シミュレーション(足単位 / ティック単位 / 板の待ち行列(指値の埋まり方)を持つもの / イベント駆動 / ベクトル化)
2. 執行・bot の枠組み(取引所接続、注文管理、paper、リスク管理。国内取引所(bitFlyer・GMO・bitbank)対応の有無)
3. データ(約定・板・清算・建玉・資金調達率・オプション。履歴とリアルタイム、保存形式、地域制限)
4. 研究・特徴量・統計(時系列、機械学習、多重比較・事前登録、ウォークフォワード)
5. 監視・可視化・記録(ダッシュボード、通知、取引日誌、実験の追跡)
6. LLM・AI エージェントを使うトレード(判断・調査・コード生成の枠組み)
7. スクリーニング・スキャン・アラート(価格・出来高・清算・ニュース・X の流れ)
8. 検証・再現・品質(別実装との突き合わせ、データ品質、ルックアヘッド検出、リプレイ)

【手順(スキル §4 の固定手順 + 追加)】
1. **検索計画を先に書く**: 幅 3 段(狭い / 中間 / 広い)× 日本語 / 英語 = 最大 6 クエリ。発見の経路 3 つ: (a) `WebSearch` 一般、(b) **X**(`x-research`: `WebSearch` で `site:x.com <語>` を語を変えて 3 回以上 → `python3 scripts/x_fetch.py <URL>` で本文を逐語で。**ツール名だけでなく「実際に使っている人が何をどう使っているか」の投稿を集める**。1 秒 1 件以下)、(c) GitHub / PyPI / 公式サイト(一次資料)。
2. 出典ごとに URL・取得方法・取得日(UTC)。X の投稿は URL・著者・日時・本文(逐語)・反応数。個人の連絡先は写さない。
3. **ツール 1 件ごとに次の列を埋める。埋まらない列は「未確認(何を試したか)」と書き、空欄にしない**:
   - 名前 / 種別 / 何ができるか(一次資料の逐語 1〜3 行) / 言語・動作環境 / ライセンス / 最終更新日と活動(星・コミット・issue の反応)/ 対応取引所(国内の有無)/ 出典
   - **料金の構造(一次資料 = 料金ページ・利用規約・API 文書の逐語 + URL + 取得日)**: 無料の範囲の**具体的な上限**(呼び出し回数 / 期間 / 履歴の深さ / 機能の制限)/ **課金が始まる条件**(従量・鍵・席・データの購読)/ **隠れた依存**(動かすのに別の有料データ・鍵・クラウドが要るか)/ 当方の用途 U1〜U8 のうち該当するもので**実際に回したときに課金が発生するか**(例: 456 日分の清算をこの道具に入れる・1 か月 paper 運用する、で何が起きるか)。**「無料」「有料」の 1 語で済ませない。**有料の部分がある道具も外さず、無料で使える範囲と用途を書く。
   - **この環境で動かした記録**(オープンソースで導入できるもの): `pip install`(または clone)→ 読み込み → **最小の実行(合成データか当方の小さいサンプルで 1 回)**。コマンド・出力・所要時間・失敗なら理由を `docs/DATA/probes/20260921_tools_<区分>.log` に残す。導入できない(鍵・地域・容量)ときは何で止まったかを書く。実行は小さく(1 件数秒〜数分、ネットワークは小さい HEAD/GET と導入だけ)。
   - **当方との比較の材料(判定はしない)**: 該当する用途 U1〜U8 / 当方の対応物 / **対応物に無い機能**を一次資料の逐語で / 当方の対応物にあってその道具に無いもの。「不要」「今の環境で十分」「今の環境以下」の語は書かない(判定はリードとオーナー)。
   - §5.5 の 4 軸(道具として入れられるか / 当方に無い情報が取れるか / 当方に無い視点で分析できるか / 既存の成果を向上できるか)。**4 軸それぞれに 事実 / 推定 / 仮定 の印。**
   - 危険(保守が止まっている・ライセンスの制約・地域制限・鍵の露出・秘密情報の送信先)。
4. 知見ごとに 事実 / 推定 / 仮定 の印と「このプロジェクトへの含意」列(無ければ「無し」。憶測で作らない)。
5. 出力は `docs/DATA/SCAN_2026-09-21_tools.md` に区分ごとの節を追記(スキル §4 のテンプレート + 上の表)。`STRATEGY_IDEAS.md` / `DATA.md` 向けの一行候補は末尾に提案として(マージしない)。
6. 予算(5 万トークン・20 分)に達したら中断し「未完了(どこまで・残りの候補名)」と書く。**件数の目標は置かない。見つかった分を全部書き、少なく見せない。**深さが足りない候補は「浅い(何が未確認か)」と印を付ける(浅いものを深いものと同じ表に混ぜない)。

【禁止】ツールの当否判定(「使える / 使えない / 今の環境以下 / 不要」)/ 料金の語だけで候補を外す / 「無料」の語だけで料金の調査を終える / 事前登録の変更 / 自動マージ / 大きい一括ダウンロード / 鍵・ログイン・課金の経路を**使う**こと(記録はする)/ 秘密情報を書く / モデル名を書く / Do not commit. Do not push.

【完了の形】担当区分について、検索計画・出典表・ツールの表(全列。料金の構造と動かした記録を含む)・知見の表・生ログ・候補の一行が `SCAN_2026-09-21_tools.md` に揃っていること。到達不能は「この環境から不可(方法・HTTP コード)」+ 第 2 経路(オーナー PC)。
