# 委任: 方策の模擬(状態機械)— 判断 = 前半で固定した logistic の 3 択、出口 = 連鎖の終わり、遅れ 3 通り(2026-09-20)

## 0. 対応表(CLAUDE.md §0.1)
| やること | オーナーの原文の該当語(逐語) |
|---|---|
| 各プリントで 3 択(止まる → 逆張り / 続く → 順張り / わからない → 入らない)、建玉があれば次のプリントで 決済(またはドテン)/ ホールド / ホールド | 「**・止まると読んで逆張りで入る ・続くと読んで順張りで入る ・どちらかわからないので入らない**」「**・止まると読んで決済(またはドテン) ・続くと読んでホールド ・わからないのでホールド**」(L-269)、L-274 |
| 出口 = 連鎖の終わり(最後のプリント + 60 秒) | 「**連鎖が終わるまで持つなら筋は通りますがどうですか？**」(L-272) |
| 判断は code の logistic(較正の良い方) | 「**codeでの判断が勝てるならjevを使う意味はありません**」(L-321)、「**全てやってください。**」(L-323) |
| 遅れを 4 項目の和で置き、3 通りで測る | 「**判断→注文のタイムロスは後々に成績直結します**」(L-321) |
| 平均で潰さず分布で出す | 「**平均で計算したらわからなくなって当然やろ**」(L-260) |

**出力はすべて日本語(code の文字列は英語)。判定語(陽性/陰性/有意/差あり/検出されず/支持/棄却)を書かない。`paper_logs/` を開かない。Jev を呼ばない。この委任は 2 段: 段 1 = 実装 + **前半**の連鎖 200 本での動作確認(後半に触れない)で止まって渡す。段 2(後半 2,000 本)はリードが反証者レビューのあとに別に指示する。Do not commit. Do not push。**

## 1. 読むもの
設計 `docs/PHASE2/O3C/SIGNAL/SIGNAL_POLICY_DESIGN_2026-09-20.md`(**改訂 2 が優先**。用語表の 9 通りの行動、§1 の意図の対応)、`SIGNAL_MATERIALS_DESIGN_2026-09-20.md` §7.4〜§7.7、`scripts/o3c_signal_logit.py`(`apply_frozen_rank`、定数と npz)、`scripts/o3c_signal_materials.py` / `backtest_data/o3c_signal_materials_20260920/rows_materials.csv.gz`(24 本の材料、`cand_1`、`bundle_id`)、`backtest_data/o3c_signal_continue_20260920/rows_continue.csv.gz`(`label_60`、`p0`、ts)、探索段 5 の `rows_prints.csv.gz`(連鎖 = `bundle_id`、`bundle_pos`)、約定の読み込みは `scripts/o3c_signal_continue.py` を流用(`price_at_or_after`)。作りかけの `scripts/o3c_signal_policy.py`・`tests/test_o3c_signal_policy.py`(前の委任が途中で止まったもの。使えるなら使い、使わないなら消さずに残す)。

## 2. 作るもの(段 1)
1. `scripts/o3c_signal_policy.py`: 連鎖 1 本(同じ側のプリントの列、間隔 60 秒以内で繋ぐ = 用語表)を入力に、各プリントで (a) 位置(1 件目か)を code で決め、(b) logistic の確率を `apply_frozen_rank` で出し、(c) 改訂 2 の帯で 3 択、(d) 用語表の 9 通りの行動を状態機械で適用、(e) 入る / 出る / 決済 / ドテンの値段を `at_or_after(時刻 + d)` で取り、連鎖 1 本の損益(bp、清算の向きが正ではなく**建玉の向きで符号**)を返す。方策 = 改訂 2 R2.3 の 9 通り(logistic 3 択 × A/B、logistic 2 値 × A/B、規則 × A/B、全部逆張り、全部順張り、完全な判断)。遅れ d = 0.5 / 1 / 2 秒。出口 = 最後のプリント + 60 秒。
2. 表(改訂 2 R2.5): 連鎖ごとの損益の分布(中央値・p25・p75・負の割合・日等重み平均・日クラスタ SE)を方策 × 型 × 遅れ で。「わからない」で入らなかった連鎖を 0 で含める表と含めない表。位置別の内訳。3 択の件数と行動の件数。**段 1 では前半の連鎖 200 本(種 20260920、5,000 件の較正サンプルは後半なので重ならない)だけで作り、`backtest_data/o3c_signal_policy_20260920/stage1_firsthalf/` に置く。**
3. 試験 `tests/test_o3c_signal_policy.py`: 9 通りの行動が用語表どおり(合成の連鎖で 1 本ずつ)/ 型 A は同じプリントで新規を開かない、型 B はドテン / 出口が最後のプリント + 60 秒 / 遅れ d で `at_or_after` が動く / 帯の 3 択が改訂 2 の値 / 判断が ts 以前だけを使う(ts 以後の約定・清算を変えても判断が同じ。p₀ を書き換えても同じ)/ 完全な判断は事後のラベルだけを使う / 損益の符号が建玉の向き / `paper_logs/` を開かない / 判定語なし。
4. 報告 `docs/PHASE2/O3C/SIGNAL/POLICY_STAGE1_REPORT_2026-09-20.md`: (0) 行数・数値セル数 / (1) §0 の表 / (2) コマンド / (3) 合成の連鎖 3 本の状態機械の道筋(プリントごとの判断・行動・建玉・値段)/ (4) 前半 200 本の表 / (5) 設計に無い判断(全部)/ (6) サニティ / (7) 限界 / (8) 作ったファイル / (9) テストの末尾行(全スイート、`-q` 無し)。

## 3. 制約
read-only(入力を書き換えない)・冪等・後半に触れない(段 1)。既定値を変えるなら列挙して理由を書く。モデル名を書かない。上に書いたファイル以外を変更しない。設計の帯・行動の表を変えたくなったら止めて報告する。
