# 道具の台帳 — 索引(区分 1: バックテスト・シミュレーション)

**何か**: 道具サーベイ区分 1(2026-09-21〜23、34 回の実行)の結果を、後から引ける形にした資源。オーナー指示 L-403「**まずは調査結果を資源化し保管してください**」。
**本体は 2 つ**: この索引(分かったことと引き方)と、台帳 `docs/DATA/tools_catalog.tsv`(候補 1 件 = 1 行、116 行)。
**台帳は手で書いていない。**報告書から `python3 scripts/build_tools_catalog.py` で作る。数はすべて台帳か `scripts/recount_scan_cat1.py` の出力から取った(2026-09-23)。

---

## 1. 区分 1 はどこまで来たか

**完了の形**: 「6 要素それぞれについて、一次資料に到達した候補が尽きること」— **リードが書いた案 A' の本文を、オーナーが「案 A' で」と選んだもの**(L-398。オーナーの逐語ではない)。

| 数え方 | 値 |
|---|---|
| 一次資料に未到達 | **0 件** |
| 最後の記載が「判別に一次資料が要る」のまま(機械で数えた) | **0 件**(32 回目の締めで見落とした 8 件を 33 回目で処置) |
| 6 要素のどれかに印がある候補 | 84 件(台帳 116 行のうち、32 件は区分 1 の印が無く他の区分へ回したもの・一覧など) |

| 要素 | 足 | ティック | 板の待ち行列 | イベント駆動 | ベクトル化 | 市場影響と約定の模型 |
|---|---|---|---|---|---|---|
| 印を持つ候補 | 55 | 19 | 19 | 19 | 4 | 52 |

**尽きていない 4 件**(理由つきの別立て): 14 `Bot18`(npm の登録は取得済み、実装の中身だけが公開物として見えない)/ 22 `Lean CLI`(検証の機関が取得していない Docker 画像の中)/ 11 `OctoBot` の 5 要素(模擬を回す必要があり、深掘り禁止の範囲の外)/ 49 `GFT Backtest Software`(入口が登録の内側。**登録すれば届く可能性は否定していない**。登録はオーナーが決めること)。

---

## 2. 当方の bot に効くこと

### 2.1 指値の埋まり方の尺度(段)と当方の位置

| 段 | 何を見て埋めるか | 当方 |
|---|---|---|
| 0 | 相場に「埋まるか」を問わない(必ず埋まる) | |
| 1 | 価格が跨いだら埋まる | **`src/bot/backtest/engine.py`**(45〜53 行で自ら「楽観的」と書いている) |
| 2 | 次の足の値と比べ、未約定を残す | |
| 3 | 埋まりを決める到着が事象になっている | |
| 4 | 約定の出来高を読んで、埋まる量を決める | |
| 5 | 自分の注文が列のどこに居るかを追う | **`scripts/qa/maker_fill_ref.py`**(FIFO)。ただし**研究の実行経路が呼んでいない** |
| 6 | 市場影響の関数を持つ | |

段の表は 52 行(`区分1-市場影響と約定の模型` の印を持つ候補と同じ集合)。**段(機構) = 差し替えれば届く高さ、段(既定) = そのまま使ったときの高さ。この 2 つが違うのは 52 件中 14 件**(2・4・6・15・18・21・33・37・38・53・65・91・92・97)。

### 2.2 先行注文の取り消しの扱い — 5 つの立場(当方は 95 の側)

| 候補 | 立場 | 効く時点 |
|---|---|---|
| 95 `polymarket-fill-model` | 何もしない。逐語「Cancellations ahead of us are invisible, so we never move up the queue」 | — |
| 57 `WonderTrader`(執行の機関だけ) | 差した瞬間に 1 回、平均の取消率で先行量を割り引く。**既定は 0** | 注文を出すとき |
| 104 `LimitOrderBook-MatchingEngine` | 取り消しが起きた時点で順位が繰り上がる | 取り消しのとき |
| 98 `limitOrderBook` | 取り消した注文に印を付けて列に残す(繰り上がらず、埋まりもしない) | 通り過ぎるとき |
| 90 `PredictionMarketBench` | 板の写真の量が先行量を下回ったら、先行量をその量まで下げる。**既定の型は 95 と同じ** | 写真のとき |

取り消しの扱いを持つのは 15 件(23・37・38・57・65・90・91・96・97・98・99・101・102・103・104)。**当方の `maker_fill_ref.py` は何もしない側。**

### 2.3 遅延 — 当方に無い軸

注文から約定までの遅延の模型を持つのは 8 件(15・23・33・37・63・91・92・98)。33 は送信と取り消しで別々の遅延を持つ。**当方はどちらも持たない。**

### 2.4 「持っていると書いてあるのに効いていない」— 4 つの形

| 形 | 例 |
|---|---|
| 1. 機構はあるが、検証の実行経路が呼ばない | 107 `sigc`・15 `BacktestingCore`(97KB の検証本体が 12KB の滑りと影響の板に一度も触らない)・20 `VnPy` |
| 2. 呼ぶが、既定では無効 | 4 `PyBroker`(`slippage_model=None`)・**21 `Qlib`(`impact_cost: float = 0.0`。原典自身が「A recommended value is 0.1.」)**・33(`ZeroSlippageModel`)・2 `Backtrader`・91・92・57(`_cancelrate(0)`) |
| 3. 当方の `engine.py` | 呼んでいるが、模型そのものが段 1(自ら「楽観的」と明記) |
| 4. 文書が名乗る機構が、実装に無い | **90: README は「pro-rata fill allocation」と書くが、実装の `.py` 16 本にその語は 0 件で、実際は FIFO** |

**道具を選ぶときは段(既定)と実装を見る。**README や段(機構)だけで選ぶと、当方と同じ楽観を持ち込む。

---

## 3. 安全側 — 取り込むと当方の作業者に指示を出しうる経路(11 件)

| 型 | 件数 | 候補 |
|---|---|---|
| 作業者向けの手引きの同梱 | 8 | 44・74・120・41・58 `nautilus_trader`・19 `Jesse`・112・111(`SKILL.md` など 91 本) |
| 指示の文章の同梱 | 1 | 97 |
| 外部送信の口 | 1 | 51 `QUANTAXIS`(作者のサーバーへ口座名・銘柄・向き・値段・数量を送る `requests.post`、既定は無効) |
| 宣伝・詐欺の兆候 | 1 | 118(「guaranteeing risk-free alpha generation」「94% accuracy」) |

**どれも中身を読まず、従っていない。候補からも外していない**(止めるのは「この環境への導入と実行」だけ)。**使う段で必ず確かめる。**

**登録の付け替え 3 件**: `robcarver17/pysystemtrade` → `pst-group/pysystemtrade` / `Ziplime/ziplime` → `Limex-com/ziplime` / `QUANTAXIS/QUANTAXIS` → `yutiansut/QUANTAXIS`。**うち 2 件は `code=200` のまま中身だけ別の登録**(返ってきた `repo` の欄を読まないと気づかない)。

---

## 4. 次の区分への引き継ぎ

台帳の `他の区分へ` の列が、**次の区分の最初の候補一覧**になる(75 件)。

| 区分 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|
| 回された候補の数 | 49 | 14 | 12 | 9 | 14 | 3 | 5 |

区分 2(執行・bot の枠組み)は 2026-09-22 に 1 回起動して L-384 で止めたまま。**次の区分の進め方は `.claude/skills/research-squad/SURVEY.md`**(区分 1 の振り返りから作った手順書)。

---

## 5. 未処置(完了の形には響かない精度の直し。述語で決まる)

| 何 | 決め方 |
|---|---|
| 約定の列を時刻つきで当てる列の模型(95・90 など)に段 3 を足すか | 段 3 の述語「埋まりを決める到着が事象になっていること」で当て直す |
| 100・109 の `区分1-板の待ち行列` の印 | 述語「自分の注文が列のどこに居るかを追う実装」に照らすと外れる見込み(100 は照合の本体が空、109 は注文を入れる口が無い) |
| 段 4 が「提示量を読んで量を決める」を含むか(97・90・63) | 段 4 の定義が提示量について言っていない。定義を締めてから当て直す |
| 65 の既定の機関 | 未確認 |

---

## 6. 引き方

- **台帳の列**: 番号 / 名前 / 到達 / 6 要素(○ = 印あり、× = 打ち消し)/ 段(機構)・段(既定)/ 遅延・取り消しの扱い(判定語: 在る・該当なし・該当なし(別の意味)・未確認・持たない)/ 他の区分へ / 安全側の所見 / 尽きていない理由 / **報告書の最後の記載の行**(根拠はここから引く)/ 段の表の行 / 遅延(原文)・取り消しの扱い(原文)。
- **作り直す**: `python3 scripts/build_tools_catalog.py`(`--check` で台帳が古くないかを見る)。
- **数え直す**: `python3 scripts/recount_scan_cat1.py`。**受け入れ検査**: `python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-21_tools.md docs/DATA/probes/20260922_tools_1_run*.log`。
- **根拠の本体**: `docs/DATA/SCAN_2026-09-21_tools.md`(1〜34 回目の節)/ 生ログ `docs/DATA/probes/20260921_tools_1.log` と `20260922_tools_1_run{2..34}.log` / 各回の検収と全体監査の受け答え `docs/AUDITOR/VERDICTS/2026-09-22_tools_scan_cat1_*.md` / 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)。
