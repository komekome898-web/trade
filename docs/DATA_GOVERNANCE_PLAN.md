# データ取扱いの厳重管理(課題登録 2026-09-05、オーナー指示)

## 0. 経緯(オーナーの指摘)

- 監査 R で「主張の 210 日窓に対しスナップショットの重なりは 21 日しかない」(部分消失)が判明した。
- これ以前にも、オーナーがログを送付した直後に「板は 0.2 日分のみ」とリードが答え、実際には 14 日分あった
  (研究側が古いローカルコピーを読んでいた)ことが数回あった。**データの取扱いが雑**だった。
- データは全ての元になる資源であり、監査の結果で消失・部分消失が「未検証」への格下げを生んでいる。
- 以前に全棄却した「データ取得に関するツイート」(位置分布チェッカー、Freqtrade、暗号資産 MCP 群)の判断も、
  この見直しで覆る可能性がある。

## 1. 監査で判明したデータ問題(台帳から)

| 問題 | 例 |
|---|---|
| 消失 | 逆選択の壁の元テープ(第 x 報、31 日制限)、OKX OI/L-S の 08-23 以前、R14 の再生母集団 |
| 部分消失 | Binance 1 分足の 210 日窓(backtest_data には 21 日、data/ に 210 日が未スナップショットで残っていた) |
| 古いコピーの参照 | 研究チェックアウトの data/ws・logs/bot.jsonl(共有 paper_logs より古い)|
| 列の意味の取り違え | ベニュー約定ファイルの `t`(収集時刻)と `ts`(約定時刻)|
| 品質の未点検 | Yahoo の JPX ETF 日足の誤プリント 15 日、bitstamp 2011-12 の出来高ゼロ日、USDJPY の ask 列の合成疑い、
  再接続直後の交差板、メンテ窓の合成バー |
| 窓の固定 | 2005-14 FX イベント・ティックが E±600s 固定で対照が作れない |
| 出所の欠落 | GMO 手数料 0.4bps、JPX ETF のコスト定数、1311 の売買代金 |

## 2. 設計(監査完了後に実装。実装前に本書で凍結)

1. **受領台帳(intake ledger)**: オーナーが送るログ・外部取得・スナップショットは全て `data/INTAKE.jsonl` に
   1 行(時刻、経路、ファイル、行数、期間、MD5、送付者)。`share_logs.bat` と `fetch_*` が自動追記。
2. **単一の真実**: 研究側は必ず `paper_logs/`(共有)を読む。`shared_or_local` を全読み手に強制し、
   ローカルが新しい場合のみローカル。読み手はどのコピーを読んだかを出力に明記(監査の「Files read」と同じ)。
3. **鮮度と網羅の表示**: ダッシュボードの「データ蓄積」に、各系列の**期間・行数・最終更新・ギャップ**を
   受領台帳から表示。リードの報告に出す件数は台帳の値のみ(手計算禁止)。
4. **スナップショット義務**: 判定・報告に使ったデータは `backtest_data/<name>_<date>/` に MD5 付きで
   保存してからでなければ結論を書けない(消失は「未検証」への格下げ規則を恒久化)。
5. **品質ゲート**: 取り込み時に自動チェック(重複、欠測、交差板、メンテ窓、極端リターン、列の型と意味の
   宣言ファイル `schema.json`)。`t`/`ts` のような列意味は schema に必ず書く。
6. **出所ファイル**: 定数(手数料、スプレッド、ティック、乗数)は `config/constants.yaml` に値・単位・
   `source_type`(primary_document/measured/assumed)・一次資料 URL(該当時)・確認日・実測なら測定
   スクリプト/レポートと期間を並記する。値だけの定数を禁止。ローダーと `require_source()`(assumed/
   deprecated な定数を判定文脈で使おうとすると例外)は `src/bot/constants.py`、スキーマ検証は
   `tests/test_constants.py`。**実装済み**(2026-09-05)。既存 `config/products.yaml` 等の値は変更せず、
   出所の記録のみをこのファイルに追加している。
7. **保持期限の先回り**: bitFlyer 31 日、OKX 30 日など期限付きソースは、期限の半分で自動スナップショット。
8. **外部ツールの再評価**: 位置分布チェッカー、Freqtrade のデータ層、暗号資産 MCP 群を「データ取得・保持」の
   観点で再評価する(以前の棄却理由が戦略性能だった項目は、データ基盤としての価値を別途判定)。

## 3. 位置づけ

監査(`AUDIT_PLAN_2026-09.md`)完了後、最初に着手する基盤課題。順位付けの結果に関わらず実施する。

## 4. スナップショットのバイト完全一致(2026-09-05、SNAPSHOT_VERIFY.json ok:false の調査)

- **症状**: オーナー PC で `verify_snapshots.py` が `backtest_data/` 配下 21 unit・99 ファイルの
  MD5 不一致を報告(`ok: false`)。一方 `.gz` は 1 件も不一致がなく、台帳(`data/INTAKE_latest.json`)
  とのクロスチェックも 0 件不一致。
- **原因**: データ破損ではなく **git の autocrlf** による改行コード変換。MD5SUMS は本 VM(Linux、LF)
  で封印されるが、オーナーの Windows チェックアウトでは `.md`/`.json`/`.txt`/`.yaml`/`.py`/`.csv`
  などテキストファイルが checkout 時に LF→CRLF へ書き換えられる。バイナリ扱いの `.gz` は対象外の
  ため一致していた。99 件の不一致は全てテキスト系ファイルで説明がつく。
- **対処**:
  1. `scripts/verify_snapshots.py`: 不一致ファイルを、内容を LF→CRLF・CRLF→LF の両方向に正規化した
     md5 が MD5SUMS の記録値と一致するかで再判定し、一致すれば `line_ending_only`(`ok: true`、
     `mismatches`/`total_mismatches`・全体の `ok` 判定からは除外)として `line_ending_mismatches` に
     分離して記録する。正規化しても一致しない実質的な内容変更は従来どおり `mismatch` のまま。
  2. リポジトリ直下に `.gitattributes` を追加し、`backtest_data/** -text` と `paper_logs/** -text`
     を宣言。以後どの OS でチェックアウトしても改行変換が起きず、スナップショットはバイト完全一致に
     なる。**データファイルへの変更は一切行っていない**(git の改行変換設定のみ)。
  3. 既存のオーナー PC の Windows チェックアウトは、`.gitattributes` 適用後もその場のファイルが
     自動で書き戻るわけではない(git は変更のあったファイルだけ再正規化する)。実務上は次回
     `restart_all.bat`(`git pull`)や当該ファイルへの何らかの書き込みで解消される想定。
     `share_logs.bat` は `paper_logs/` に**追記するだけ**(既存ファイルを触らない)ため、この変更が
     既存の共有ログに影響することはない。
- テスト: `tests/test_verify_snapshots.py`
  (`test_line_ending_only_mismatch_is_not_a_real_mismatch`,
  `test_line_ending_normalization_does_not_mask_real_content_changes`)。
