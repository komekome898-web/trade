# Jev の手引き(この体制での使い方。2026-09-19、L-218「徹底的に調べてjevのスペシャリストになれ」)

一次資料の逐語集は `docs/DISCUSSIONS/2026-09-19_jev_review_inventories/D_study_notes.md`(説明書の全頁・料理本 18 本・評価集 4 件・ブログ・SDK・第三者のリポジトリ 4 件・X の投稿 19 件)。この手引きはそれをこの体制の言葉に直したもの。**規則は §4、着手前の点検表は §7。**

## 1. Jev とは何か(3 行)

文章と「状態」を渡すと、**あらかじめ決めた答えの集合の中から、確率つきで答えを返す**モデル。文章を書かない、理由を説明しない、考えを積み重ねない。ベンダーの言葉で「数秒で判断できる直感的な判定(System One)」を、コードから関数のように呼ぶための部品。確率は「0.8 と答えた集団の 8 割が当たる」ように訓練されている(校正)。**1 件の答えが正しい保証ではない。**

問いは 3 種類だけ。**はい/いいえ(noul: はいの確率)**、**選択(choice: 各選択肢の確率と確信度)**、**段階(score: 段階の確率と加重平均)**。1 要求に問いを何個でも並べられ、同時に評価され、互いの答えは見えない。

## 2. できないこと(ベンダー自身の一覧を、この体制の言葉で)

| ベンダーの記載 | この体制で意味すること |
|---|---|
| 文字どおりに読む。否定・二重否定・多段の間接は落ちる | 問いは 1 判断 1 問、肯定形、「反する」を聞かない(「守っている」を聞いて 1 − 確率 は code で) |
| 数えられない・数値の近さが分からない・段階の値を数値の補間に使うな | 件数・平均×件数・bp の差・行数は **code**。Jev には「どの文が主張か」だけ聞く |
| 日付を順序として読めない | 期限・窓・順序は code |
| 判断に関係ない内容が増えると精度が落ちる | 文書丸ごとを送らない。code が切り出した断片の対を送る(昼の失敗の主因) |
| 状態を敵対的として扱わない | **判定される側(リード)が state を書かない。**code が生の成果物・返答・記録から組む |
| はい/いいえ と 選択 の確率は互いに換算できない。否定形の問いの和は 1 にならない | しきい値は問いごとに測る。別の問いのしきい値を持ち回らない |
| 英語が主。日本語は「扱えるが同等ではない、自分の内容で試せ」 | 実測: 断片に切った状態なら日本語でも英語とほぼ同じ。文書丸ごとの日本語は未測定 |
| 文字列を生成できない | 自由な値が要る場面(検索語・要約)は別のモデル。Jev は「候補から選ぶ」まで |

## 3. 第三者が実測して書き残した落とし穴(公式に無いもの)

- **「問いが間違っていた。しきい値ではない」**(Foadsf/jev-for-engineers): 「改訂履歴から来た値か」と聞いたら、同じ値が 2 箇所にあって正解を捨てた。しきい値を動かす前に問いを疑う。
- **「判断モデルに算術をさせたら、確信度 0.04〜0.28 で『止める』と出て、成功に見えた」**(同): 無知の答えが安全側に倒れて成功に見える。数値の検査は code に移した。
- **「しきい値は勘だった。0.70 は実測した確信度の塊の真ん中にあり、明白な例を 0.01 差で落とした」**(同): しきい値は自分のデータで測ってから置く(0.60 / 0.85 に直した)。
- **はい/いいえ には確信度が無い。**確信度で門を作ると**一度も鳴らない門**になる(同)。0.5 が最大の無知、両端が確信。
- **別名 `jev-latest` は黙って中身が変わり、しきい値が全部ずれる**(bender #37)。版付き ID を固定する(この体制は `jev-1.13.0`)。
- **答えを位置で読むと、順序が変わったときに黙って食い違う**(bender #27)。答えは必ず問いの名前(鍵)で読む(この体制の `client.py` は鍵で読む)。
- **創業者の言葉**: 「コードベース丸ごと『安全か』と聞ける。だがうまくいかない。Jev は小さく分解した問いを組み合わせる方が向く」。
- **段階の使い方**(entity_alignment): 段階の 3 水準をそのまま 3 つの行動にすると「合わせるしきい値が無い」設計になる。
- **落ちた文の見つけ方**(autoformat): 「同じ段落か」と聞くと全部 yes になり、「前の行の文の途中から始まるか」と聞くと分かれた。**問いは、目に見える具体的な状況を書く。**

## 4. この体制の規則(全部、ベンダーの規定と実測から)

1. **Jev は判定しない。**印と確率を出すだけ。判断は LLM(リード・監査役)とオーナー(L-218)。「止める / 通す」の語を出力に置かない。
2. **state は code が組む。**判定される側が書かない。文書丸ごとではなく、code が切り出した **2 つの断片の対**(主張と根拠 / 定義と使用箇所 / 射程と判定バー / 意図マップの ○ と測定対象 / 原文と行動 / 使える経路と確かめた経路)。項目名を付けた object にする。
3. **問いは 1 判断 1 問、肯定形、criteria は具体例つき、英語**(断片は日本語のまま)。「存在するか」と「条件を満たすか」は別の問いに分け、同じ要求に並べる(投機的な fan-out)。code が組み合わせる。
4. **数える・比べる・日付は code。**Jev には「どれが主張か」「意味が同じか」「矛盾するか」だけ。
5. **しきい値は自分のデータで測ってから定数として 1 箇所に置き、結果を見てから動かさない。**まず「要確認 0.35」1 つ。行動の重さでしきい値を変えてよいが、それは code の分岐であって Jev の判断ではない。
6. **版は `jev-1.13.0` に固定。**応答の `model` を毎回記録し、変わったら止まって報告。SDK は使わず薄い `scripts/jev/client.py`(公式 SDK は 1 週間に破壊的変更が 2 回)。
7. **送るものは `redact` + `assert_clean` を通したものだけ。**鍵・生の会話記録・`paper_logs/`・口座と注文のログは送らない。送った本文はリポジトリに残さない、数値の結果(検出率など)もリポジトリに残さない(契約 MCA §2.3(f))。
8. **取引の経路には置かない。**LIVE・Kill Switch・リスク上限・注文状態・資本・口座・git の保護。
9. **沈黙を「異常なし」と読ませない。**届かなければ「未到達」を表示する(Stop フック `jev_notice.sh`、表示だけ、何も止めない)。

## 5. いま動いているもの

| 何 | 場所 | 状態 |
|---|---|---|
| 包み(要求・記録・再試行)| `scripts/jev/client.py` | 版固定、鍵で答えを読む、本文を記録しない |
| 伏せ字と検査 | `scripts/jev/redact.py` | 5 型、1 件でも残れば送らない |
| 監査の前段 | `scripts/jev_check.py audit <成果物>` | 対 6 種、要確認の印のみ。存在の判定を追加中 |
| 表示だけの Stop フック | `.claude/hooks/jev_notice.sh` | 変わった docs の Markdown に前段を当て要約を表示 |
| 評価集合 | `docs/AUDITOR/before/` × `answers/`(22 組)| 訂正前の成果物とオーナーの訂正 |
| X の調査経路 | `.claude/skills/x-research/`、`scripts/x_fetch.py` | 発見 = WebSearch、本文 = fxtwitter |
| 返答の検査(U12)| `scripts/jev_reply.py` + `config/jev_routes.yaml` | 取れない × 経路一覧、対応表の左右。Stop フックの表示に出る |
| 調査の検証・仕分け(U5・U6)| `scripts/jev_survey.py verify / triage` | 主張 × 引用、出典の仕分けと順位(重みは code)|
| 行動記録の検査(U14)| `scripts/jev_trace_export.py --review` | 手ごとの道具の列 + 引用した指示。観測のみ |
| 委任のモデル選定(U8)| `scripts/jev_delegate.py plan / calibrate` + `config/jev_delegation_tiers.yaml` | 課題の分類 → 段は code の規則表 |
| 指摘の振り分け・採点(U10・U11)| `scripts/jev_audit_eval.py route-findings / score-findings` | スクリプト化候補・既知解との同型 / 捕捉の候補 |
| 受領検査(U2)| `scripts/jev_report_intake.py check` | 必須項目・なぜ・判定の語・出所なしの数値・範囲逸脱 |
| 空白の地図(U1)| `scripts/jev_ideas.py map` | 族 × 市場の件数。過去の結果は入れない |
| 監査ループの検出(U9)| `scripts/jev_audit_loop.py rounds` | 前の巡と同じ指摘の連鎖の長さ |
| 発言の種別・事故の同型(U13・U16)| `scripts/jev_owner_log.py classify / calibrate / incident` | 台帳の種別との一致、I-番号の候補 |
| 通知の仕分け・告知の分類・② の事後検査(U15・U17・U18)| `scripts/jev_ops.py notify / announce / decision-review` | 表示と記録のみ。注文には触れない |

## 5.5 手順への組み込み(段 1、オーナー承認 2026-09-19、L-225)

- `owner-audit`: 監査役を呼ぶ前に `jev_check.py audit`。印の箇所(パスと行番号)を添える。監査役は全件のまま。
- `delegated-study` §1.5: 委任の前に `jev_delegate.py plan`(段の候補、完了の形、承認の要否)。§6: 受領時に `jev_report_intake.py check`。
- `research-squad` §6-0: `jev_survey.py verify / triage` と `jev_report_intake.py check --kind survey`。
- 段 2(運用しながら): しきい値の校正、弱点 3 件(行動記録の証拠の対・完了した保守・台帳の種別の語彙)。段 3(監査役の再開時): 前段の効果の測定 → 省略の判断。

## 6. 次に足すもの(優先順)

1. ~~「これで全部」型の対~~ → 実装したが KA-17 / KA-18 は捕まらなかった(2026-09-19 実測)。**理由: その 2 件の失敗は「確かめた」「全部」と書いた文ではなく、根拠なく確定として書かれた数値(「16 件で確定」)だった。文に手がかりが無い失敗は文の対では捕まらない。**KA-18 型は code の数え上げ(その数値の出所の集合を数える)、KA-17 型(読まずに判定した)は行動記録の検査(判定した対象を実際に開いたか)の仕事。対の型は残す(他の文書では鳴る)。
2. 意図マップの対を本番の事前登録(同じディレクトリに `INTENT_MAP.md`)で動かす — KA-06 型。
3. 調査班の報告に「主張 と 一次資料の引用」の対(引用検証の型、料理本 E.1 と同じ)。
4. リードの返答に「取れない・無い」の文 と リポジトリに固定した経路一覧 の対(監査に盛り込んだ後、L-217)。
5. 行動記録に、ベンダーの評価集「Agent Trace Observability」の問い(取り消せない操作の許可・最後の返答の主張に証拠があるか・どの手で最初に狂ったか)を当てる — 評価集合が 3 件しか無いので観測のみ。

## 7. 問いを 1 つ足す前の点検表

- [ ] その判断は code で完全に決まらないか(決まるなら Jev に聞かない)
- [ ] state は code が組み、判断に要る事実が全部入っているか(Jev は外を知らない)
- [ ] 問いは 1 判断か、肯定形か、criteria に具体例があるか、「存在」と「条件」を分けたか
- [ ] 答えの集合に「どれでもない」があるか
- [ ] 確率の読み方を決めたか(noul は はい の確率、0.5 は無知。choice は確信度も)
- [ ] しきい値は測ってから置いたか、定数は 1 箇所か
- [ ] 版は固定か、応答の `model` を記録するか
- [ ] 伏せ字を通すか、数値の結果をリポジトリに書かないか
- [ ] 印が付いたあと誰が判断するか書いたか(Jev ではない)

## 8. 共通部品としての用途の地図(2026-09-19、L-220「これだけを検討して満足しないでください…有効活用方法を見出してください」)

前提: 目標は「トレードによる実用的な収益基盤」で、今は ① 自動取引 bot と ② 自律 AI トレーダーの知見づくり(`docs/PROJECT_GOAL.md`)。**Jev が効くのは「文章や記録に対する、答えの集合が決まった、数秒の判断」が体制の中で大量に・繰り返し・人か LLM が遅く高くやっている場所**。取引の経路(注文・リスク・資本)には置かない(§4-8)。数える・計算する場所にも置かない(§4-4)。

| # | 用途 | 出所 | code が組む state | Jev に聞くこと(答えの集合) | 印を見て動く人 | 価値 / 手間 / 正解ラベル |
|---|---|---|---|---|---|---|
| U1 | **戦略案の在庫の「空白の地図」** | リード | `STRATEGY_IDEAS.md` の案 1 件ずつ + `DATA.md` の在庫名 | 状態変数の族(価格由来 / 出来高・フロー / 建玉・清算 / 板 / 外部・暦 / 複合)、必要データが在庫にあるか、市場(暗号 / FX / 株)、実装の重さ(3 段)、既存の案と同型か | リード → オーナー(「片っ端から検証」の順番の材料。**過去の結果は使わない = A-5 を守る**。§5.5 の「状態変数の系統の偏り」がここで見える) | 高 / 中 / 地図なので不要(抜き取りで読む) |
| U2 | **委任先の報告の受領検査** | リード | 報告の節ごとの本文 + 必須報告 7 項目の見出しの有無(code) | 「なぜ」の節が機構を述べているか(結果の言い直しでないか)/ 判定の語(採用・棄却)を書いていないか / 指示の範囲を広げていないか / 主張に実行結果が添えてあるか | リード(差し戻すか、自分で読むか) | 高(上位モデルの読みを減らす)/ 中 / 過去の差し戻し記録(ACTION_LOG)|
| U3 | 事前登録と意図マップの整合 | 済(`jev_check`) | 対(射程 × 判定バー、○ × 測定対象) | 矛盾するか | リード | — |
| U4 | 陰性 / 不明 / 陽性の分類 | 済(code) | — | — | — | Jev 不要 |
| U5 | **調査結果の仕分けと順位付け**(オーナーの例) | **オーナー** | 出典の行 + 問い + 在庫名 | 一次 / 二次 / 共同体 / 宣伝、関連、在庫と重複、要プローブ → 順位は code の重み | リード(上位だけ精読。検索幅は狭めない) | 高 / 小 / 実装中(1) |
| U6 | 主張 × 一次資料の引用の検証 | リード | 主張 + 引用 + 出典の記載 | 裏付け / 矛盾 / 何も言っていない、経路と方法の記載 | リード(検収) | 高 / 小 / 実装中(1) |
| U7 | 調査の幅の検査 | リード | 検索計画の表(狭い / 中間 / 広い)| 3 つの語が本当に幅の違う検索か | リード | 低 / 小 / — |
| U8 | **委任時のモデル選定**(オーナーの例) | **オーナー** | 委任文 + 読むファイルの数と大きさ + 触る経路(保護パス・不可逆)| 課題の種類(実装 / 測定・実行 / 抽出・棚卸し / 判断 / 執筆)、複数ファイルの推論が要るか、スクリプトで済むか(→ モデル不要)、オーナー承認が要るか → モデルは code が `DELEGATION.md` §7 の表で決める | リード(既定を Jev の印で上書きしない。**Jev が選ぶのではなく、規則の表を当てるための分類**) | 高(上位モデル 90% の事故 I-007 の対策)/ 中 / 過去の委任 50 件超の記録 |
| U9 | **監査の無限ループの検出**(I-011) | リード | 各巡の指摘 と 前の巡の指摘 の対 | 同じ指摘の繰り返しか(entity alignment の型)→ 回数は code | リード(打ち切りは規則 L-200 で決める。Jev は「同じ指摘が N 回目」を表示するだけ) | 高(10 時間の事故)/ 中 / 段 A の 10 巡の記録 |
| U10 | **監査役の指摘の振り分け** | リード | 指摘 1 件 + 既知解の一覧 | code で決定的に検査できるか(→ `IMPROVEMENT.md` 手順 5 のスクリプト化へ)、どの既知解 KA と同型か | リード・監査役の改良の週次 | 高 / 中 / VERDICTS 32 件 |
| U11 | **盲検評価の採点(捕捉 / 部分 / 見逃し)** | リード | 監査役の出力 + オーナーの訂正(answers/) | 監査役の指摘はオーナーの訂正と同じ欠陥を指しているか(3 段) | リード(**自己採点の禁止 O-10 を機械で解く**。TREND.md の捕捉率が中立になる) | 高 / 小 / EVAL_2026-09-11b のリードの採点と比べられる |
| U12 | リードの返答の検査 | 実装中(2) | 取れない × 経路一覧、対応表の左右 | 全経路を確かめたか、左は右の範囲か、自分でできることを頼んでいないか | リードとオーナー(表示だけ) | 高 / 小 / 実装中 |
| U13 | オーナー発言の種別と紐付け | リード | 発言 + 状態板の「待っている項目」 | 種別(指示 / 決定 / 質問 / 報告 / 指摘)、どの待ち項目への答えか | リード(§7 の記録、I-001 の型の防止)| 中 / 小 / OWNER_LOG 220 行 |
| U14 | 行動記録の検査 | 実装中(3) | 手ごとの道具の列 + 最後の返答 + 保護操作の一覧 | 承認なしの保護操作、主張に証拠、最初に狂った手 | リード・オーナー(観測のみ、評価集合 3 件) | 中 / 中 / 薄い |
| U15 | 運用の通知の仕分け(Discord・監視ログ) | リード | 通知の文面・見慣れない例外の文 | 見るだけ / 記録 / 至急、既知の型のどれか | オーナー・リード(**注文経路ではない**。PAPER の今は低、LIVE で上がる) | 中 / 小 / paper_logs |
| U16 | 事故の同型判定 | リード | 新しい事故の記述 + I-001〜012 | どの過去の事故と同型か(資料 §11「過去 incident と同型 → 全件監査」の引き金) | リード | 中 / 小 / INCIDENTS 12 件 |
| U17 | 取引所の告知・外部の文章の分類(② に向けて) | リード | 取引所のステータス告知・メンテ情報・ニュースの見出し | 停止 / 縮退 / 通常、相場に関わる種類 | **表示・記録のみ**(注文の判断には使わない)| 中(後で高)/ 中 / 未収集 |
| U18 | 自律 AI トレーダーの判断の事後検査(②) | リード | 各周期の提案した行動 + 根拠 + 方針の文 | 方針との整合、根拠の有無(行動記録の検査と同じ型) | リード・オーナー(事後のみ、注文を止めない)| 後で高 / 中 / 未着手 |

**私が薦める順**(1〜3 の後): U11 → U10(正解ラベルが揃っていて、自己採点の問題を機械で解き、週次の監査役改良に直結)→ U8(予算の事故の対策。過去の委任記録で校正できる)→ U2(上位モデルの読みを減らす)→ U1(「片っ端から検証」の順番の材料。空白が見える)→ U9(10 時間の事故の再発防止)→ U13・U16 → U15・U17。**U17・U18 は ② の段で戻ってくる。**

**共通の作り**: すべて `jev_decide("<用途>/<問い集合>", state)` の形(資料 §22)で、問い集合は `scripts/jev/schemas.py` に用途ごとに置き、しきい値は各スクリプト先頭の定数、印は表示だけ、判断は人か LLM。

## 9. 事例: 問いと測定の照合(2026-09-19、L-238「こんなときこそjevの使い道やろ」)

段 A の 1 周目で、判定の量(W 時間の VWAP へ戻り切ったか)が目的(I-1「清算の監視による値段の上がりすぎ下がりすぎやトレンド転換を捉えることができるか確認」)を直接測っておらず、対照が出発点の距離を合わせていない交絡を見落とし、報告が「逆でした」と真逆になった(記録 L-231〜L-237、`VERDICTS/2026-09-19_reaction_report_r3.md`)。5,000 行の事前登録と 12 巡の監査(形式)は捕まえなかった。

同じ事例を、目的の逐語・判定の量の定義・対照の定義・同じ出力にある別の量・報告の結論、を名前付きの欄で Jev に渡し、6 問(量は目的を直接測るか / 量は出発点の性質で決まり対照が揃えていないか / 別の量の方が直接か / 結論は目的に答えているか / その測定で「逆」と言えるか / 機構以外の原因を挙げているか)を送った(1 回、入力 1,213 トークン)。**設計の量・対照・結論の 3 点すべてに印が付いた**(確率は `data/jev/probe/20260919_L238_purpose_checks.txt`。数値はリポジトリに入れない)。

学んだこと: この照合は中身(交絡・△・特定の欠陥)を指定しない。**「この量はこの問いに答えるか」「この差は他の何で作れるか」を毎回、設計と読みの段で問う**ので、次の未知の欠陥にも効く。前段 `jev_check.py` の `intent_vs_measured` は `INTENT_MAP.md` の ○ 行しか見ず、今日の設計(§8 の表)では対が 0 件で黙っていた。→ 対の種類 `purpose_vs_quantity` / `control_vs_quantity` / `conclusion_vs_purpose` / `other_cause_named` / `direction_supported` を足す(実装 2026-09-19、委任)。Jev は判定しない。印を読んで考えるのはリード。

## 10. 印より前: 判断の中で使う(2026-09-19、L-241「印をつけるための道具としてしか使ってないのでは？」)

§9 までの使い方は事後の検査(印)である。Jev の本来の型は「code が判断の材料を作り、Jev が小さな判定を返し、code がそれで選ぶ・並べる」で、研究の判断そのものに入れられる。今日の事例で 2 つ試した(スクリプト `docs/DATA/probes/20260919_jev_select_rank_probe.py`、確率は `data/jev/probe/20260919_L241_select_rank.json`、2 回の送信で入力 2,905 トークン):

1. **判定の量を選ぶ(score)**: 設計が列挙した観測量 8 系統をそれぞれ「目的(逐語)をどれだけ直接表すか」の 5 段階で点数化。**清算の向きに符号を付けた価格変化が最上位、VWAP への戻り到達は下から 3 番目(「弱い代理: 値が別のもので決まる」の段に集中)。** 設計の段でこれを出していれば、1 周目の判定の量は変わっていた。
2. **対照で合わせる変数を選ぶ(noul の同時送信)**: 表の列(距離・薄さ・建玉の減り・側・時刻・束の大きさ)ごとに「判定の量の値がこの変数で構造的に決まるか」を問う。**VWAP からの距離だけが 0.5 を超え、実際に合わせていた薄さは最下位近く。** 対照の設計の段でこれを出していれば、交絡は走る前に見えていた。

同じ型で置ける判断(未実装): 読みの段で「差を作る機構以外の原因」の候補(構成の違い・定義・標本の選び方・符号・欠損・局面)を点数化して上から潰す / 意図マップの ○△✕ を文書から生成する(意図 × 実装の節 → choice: 直接 / 代理 / 無し)/ 監査役に強く問わせる項目を成果物ごとに選ぶ。いずれも Jev は選択肢の点数を返すだけで、決めるのは code の規則とリード。
