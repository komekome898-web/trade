# 再開計画(2026-09-10 木 08:00 JST のトークン枠リセット以降)

作成 2026-09-08。**この 1 枚だけ読めば再開できる**ように書く(文脈の要約で会話が失われても、状態板 `docs/OWNER_STATUS.md`・
台帳 `docs/OWNER_LOG.md`・本書の 3 つが真実)。印: 事実 = 記録・実測、推定 = 推論。

## 0. 再開時の前提確認(最初の 5 分)
1. `git pull`(オーナー PC の share_logs が毎日 06:30 JST に push している)。
2. `docs/OWNER_STATUS.md` を読む(オーナー側の進捗。指示を出す前に必ず)。
3. 週間トークン枠を確認。**2026-09-07 時点で全モデル 81% / Fable 100% = 自主上限 70% を超過(1 回目)**。
   `docs/DELEGATION.md` §6 の自動停止基準は「超過 2 回」。リセット後の新しい週で、再び 70% に近づいたら重い工程を止める。【事実】

## 1. 優先順位(この順で進める)

### A. P2-08b(秒スケール、着手中) — 最優先
準備は完了済み。残りは以下の順。
1. **設計監査 4 回目**(盲検、Opus)。第 4 稿 `docs/PHASE2/P2-08b/PREREG.md` に対し、
   `DESIGN_AUDIT_3.md` の要修正(大 3 / 小 6)が反映されたかの対応表を作らせる。前 3 回と同じ形式。
2. **凍結** → 既知正解テスト(手計算 5 件、PREREG 末尾の (a)〜(e))。
3. **反復 0**: 第 1 層(伝播の計測 A(τ, τ+5)、τ 8 点)→ 候補集合 C の確定 → 第 2 層(9 構成)→ 帰無(C 限定・n<30 除外・t 標準化、2,000 抽選)。
4. **結果監査**(両層、盲検)→ 判定提案 → オーナー判定。
- 既に用意済み【事実】: 封印台帳 156 ファイル(境界 2026-08-23、forward_start 2026-09-07)、
  Binance 現物 aggTrades 45 日 + 1s + 無期限、標本日 85 日、bitbank 109 日、bitFlyer 約定の μs 正規化 46 日、
  `load_diagnostic` と μs epoch 対応(テスト付き)、`scripts/fetch_binance_vision.py --kind aggTrades --market {spot,um}`。
- 注意: Binance の本体データは容量のため git 管理外(`DATA_LOSS_REGISTER` #15)。**コンテナが再生成されていたら再取得が必要**
  (`scripts/fetch_binance_vision.py`、MD5 は SEALED.json にある。所要 1〜2 時間)。最初に `ls backtest_data/binance_BTCUSDT_aggTrades_20260723_20260906/*.csv.gz | wc -l` で 45 を確認する。

### B. #70 ポジショニングの調達(A と並行でよい。軽い)
`data.binance.vision/data/futures/um/` から:
- `daily/metrics/BTCUSDT/`(2021-01-05〜、1 日 289 行、5 分刻み)= **建玉・上位トレーダーの偏り・taker 比率**
- `monthly/fundingRate/BTCUSDT/`(2020-01〜)= 現在 66 日しかない資金調達履歴の置き換え
- `daily/bookDepth/BTCUSDT/`(1 日 34,561 行)は**抜き取り**(重い。嵐日と平常日を各 20 日程度)
`scripts/fetch_binance_vision.py` に `--kind metrics/fundingRate/bookDepth` を足すのが最短。スナップショットは
`backtest_data/binance_um_metrics_20210101_2026MMDD/` 等。取込台帳とスキーマも登録。

### C. 嵐予兆 第 2 段(**9/19 ごろゲートが開く**)
2026-09-04T12:50Z から 15 日分のテープが要件。運用点 0.771 は凍結済みで、**一度だけ判定**する。
判定は AUC バー + 有用性バー(適合率 ≥ 3× 基礎率・再現率 ≥ 30%・F1 ≥ ベンチマーク)。
B が終わっていれば、**建玉・taker 比率を特徴量に足した新単位**を別途立てられる(既存の第 2 段は改造しない)。

### D. #70 単位化 → E. #69 オンチェーン日次 → F. P2-06(NT 倍率)
- D: (a) 嵐予兆の特徴量追加、(b) 資源 R11(champion の低ボラ三分位)の条件付けをポジショニングに置換。
- E: Coin Metrics 無料日次 3,964 日で測り切る(MDE は不利。上限を確定して閉じるのが目的)。
- F: **データは 2026-09-07 に揃った**(`topixf_225labo_20260907` / `mini_topixf_225labo_20260907`、日足 2001/2008〜、1 分足 2026)。
  日経側は `n225f_225labo_20260828`。NT 倍率 中央値 12.10 で整合確認済み。着手可能。

### G. 小物(いつでも)
- ダッシュボードに鮮度・心拍の欠落表示(I-002 の未実装分。Prometheus の代替)。
- `.git` が 1.9GB。オーナー PC の pull が重い。整理案を次の週次要約に出す。

## 2. 期限のあるもの(時間切れで失われる)

| 項目 | 期限 | 状態 |
|---|---|---|
| **WS テープの穴 2 か所の REST 補填**(08-25 18:29→08-26 02:55、08-27 17:57→08-28 04:19) | **2026-09-25 / 09-27**(31 日制限、N-002) | **2026-09-08 に実施**(§3) |
| 1591(JPX400 ETF)の板寄せ実約定測定 | 2027-01-27(分割前) | オーナー側 P1 待ち |
| 嵐予兆 第 2 段のゲート | 2026-09-19 ごろ開く | PC の記録継続が前提 |
| 225Labo の更新版 | 四半期ごと | 2026-09-07 取得済み |
| Binance Vision / Coin Metrics / bitbank | **期限なし**(恒久アーカイブ) | 急がない |

## 3. 2026-09-08 に前倒しで実施したこと
bitFlyer の公開約定は **31 日で消える**(N-002)。本日時点の到達最古は **2026-08-08T05:26**(実測)。
WS テープには記録の断絶が 2 か所あり(合計約 19 時間)、**それらは今なら REST で埋められるが 9/25〜9/27 で不可能になる**。
そこで 31 日ぶんの約定スナップショットを取得した(`scripts/fetch_deep.py FX_BTC_JPY --days 31`)。
副次的な利得: REST と WS の重なりが 4 日 → **19 日**に伸びるので、P2-08b の既知欠陥 (2)(WS の欠け 0.56〜2.81%)の
定量が大幅に強くなる。取得後は P2-08b の封印台帳に追加し、`ITER.md` にデータ追加として記録すること(**中身は読まない**)。

## 4. オーナー側の残件(急がない)
- **P1**: 入金 → 1348.T を 1 口 SOR で購入 → 先物口座 → kabuステーション API → `.env`。板寄せ測定の前提。
- **P7**: 完了(2026-09-07、階層 2 / NICT / ポーリング 1024s)。週 1 回 `w32tm /query /status /verbose` で位相オフセット確認。
- **P3**: TOPIX 分は完了。日経 225 の更新は四半期ごと。
