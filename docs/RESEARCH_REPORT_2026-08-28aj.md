# 第35報(aj): FX-S4判定 — 初撃方向再開は後方新鮮データで再現せず。棄却

日付: 2026-08-28。PREREG_fx_s4_judgment.md の一発判定。
スクリプト: `scripts/fetch_fx_calendar_2005_2014.py` / `build_fx_event_library_2005_2014.py` /
`research_fx_s4_judgment.py`(S4実装の6セルを import — 再定義ゼロ、再現ゲートで
S4印字値と完全一致、独立2プロセスでバイト一致)。
データ: **2005〜2014 の320イベント(一次資料率100%、ルール推定ゼロ)**、
Dukascopy ティック319窓・59万ティック → `backtest_data/fx_event_ticks_2005_2014/` に恒久保存。

## 1. 判定表(3バー全FAIL)

| バー | 実測(主2セル = judgment m=5 / m=20、コードが事前指名) | 判定 |
|---|---|---|
| ①機構の再現(同符号・t≥2・台地) | グロス **−0.55 / −0.66 bps**(参照 +3.32/+3.15)— **符号逆転**、t −0.25/−0.20、台地1/4 | **FAIL** |
| ②当時実測コスト込みで正 | −1.81 / −2.06 | FAIL |
| ③今日コスト地図で正 | グロスが負のため不問 | FAIL |

**PREREG §5 の3行目に該当: S4 の「全6セル正(+0.8〜+3.0bps)」は掘り出しと確定。棄却。**
格下げ通過は発火していない(グロス自体が負)。

## 2. 中身

- 参照側(2015-26)の主柱だった **NFP(+3.95, t=+4.52)が 2005-14 では −0.56** に消える。
  年次分解は正負がランダムに揺れ、特定時代に機構が住んでいる形跡なし。
- 初撃自体は当時も大きい(NFP |E→E+5s| 中央値 25.6 vs 15-26 の 17.7)—
  **「初撃はあったが、その後の方向再開は無かった」**。
- 当時の実効往復コストは 0.84〜2.08bps(中央値1.57 = 今日の1.8倍)— ただし本判定では
  コストは決定要因ではない(グロス負)。
- カレンダー品質: BLSアーカイブ(2013年のシャットダウン延期まで反映)+ Fed一次資料、
  FOMC時刻は17件逐語+63件を一次資料で確定した時代規則。E推定はカレンダーと
  ≤60秒93%で整合。

## 3. 限界

2005-06 はティック密度が低い(0.13 ticks/s)が、当該2年の取引は36件で判定を動かさない。
FOMCアーム(n=25)は時刻精度・値動きとも最弱 — 落としても①は通らない。

## 4. 帰結

- **FXパイプラインはこれで10系統すべて棄却**。係属だった GBP/JPY 頑健性・スリッページ
  ライブ実測は対象戦略の消滅により凍結。
- 横展開第1弾(FX)は「コスト床1/9でも、測った効果はやはり床の下か、幻」で決着。
  KNOWLEDGE_FX §3 の原則(棄却は自動転移しないが、検定はする)が正しく機能した —
  S4の候補は転移先で死んだのではなく、**元の発見自体が選択バイアスだった**ことを
  後方新鮮データが暴いた。
- 第2弾 = 国内株式(クロスセクショナル — 全プロジェクトを通じて唯一未検証の戦略クラス)
  の机上調査へ。
