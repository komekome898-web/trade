# インシデント台帳(追記のみ)

| ID | 日付 | 事象 | 原因 | 再発防止 |
|---|---|---|---|---|
| I-001 | 2026-09-06 | オーナーが証券口座開設の進捗(現物口座開設済み、先物口座の前提の入金待ち)をリードの指示に従って進め報告していたのに、リードがそれを記録せず、後日「口座開設から始めてください」と誤った指示を出した | オーナーへの指示と報告が会話にしか無く、文脈の要約(コンパクション)で失われた。状態を持つ文書が無かった | `docs/OWNER_LOG.md`(追記台帳)、`docs/OWNER_STATUS.md`(状態板)、`docs/OWNER_PROCEDURES.md`(手順書)、`/owner-procedure` スキル、CLAUDE.md §7 の記録義務(その回のうちに追記、指示前に状態板を読む) |
| I-002 | 2026-09-06 | オーナー PC の保持期限スナップショット(`scripts/retention_snapshot.py` が `backtest_data/auto_<source>_<date>/` に作成、例: `auto_bitflyer_executions_20260905/` の `candles_FX_BTC_JPY.csv`・`executions_*.csv.gz`・`ticker_*.csv.gz`・`board_top5_*.csv.gz`・`manifest.json`・`MD5SUMS`。PC の台帳 `paper_logs/INTAKE_latest.json` には作成が記録済み、15日スナップショットあたり約60MB)が push されず、研究環境(VM)側には一度も届いていなかった | `deploy/share_logs.bat` の `git add` が `paper_logs` のみを対象にしており、`backtest_data/auto_*` を一度も add していなかった。DATA_QA_CHECKLIST item 4 は「PC 上に作成された」ことだけを確認しており、「共有された」ことまでは確認していなかった | (1) `deploy/share_logs.bat` を修正: `git add paper_logs` の直後に `for /d %%D in (backtest_data\auto_*) do git add "%%D"` を追加し `backtest_data/auto_*` も commit 対象にした(コピーのみ・無人実行の挙動は不変)。(2) ミミックツリーでの検証手順を `docs/DATA_QA_CHECKLIST.md` item 11 として追記(1回目で全ファイル commit・2回目で空コミットなしを確認、bat の cmd 構文も静的点検済み)。(3) 提案: ダッシュボード/README に「PC 側台帳(`INTAKE_latest.json` のスナップショット一覧)」と「環境側(git に commit 済みの `backtest_data/auto_*` 一覧)」の差分表示を追加し、次回以降は同種の未共有を自動検知できるようにする(未実装、別途着手) |
| I-003 | 2026-09-09 | 清算記録器が**最初の切断で落ちた**。加えて `restart_all.bat` が「再起動した」と表示しながら**記録器だけ古いコードのまま走り続け**、`check_liq_recorder.bat` は python プロセスを **2 つ**検出した。さらに binance_um は**接続済みのまま 12 時間 0 件** | 3 つ別々の原因が重なった。(1) **cp932**: Windows の既定コンソールは cp932 で、ログ行の em ダッシュ `\u2014` が `UnicodeEncodeError` になった。しかもそれが**切断処理の中のログ行**だったため、切断のたびに死ぬ。同じ文字は他に約 40 スクリプトにある。 (2) **停止リストの漏れ**: `stop_all.bat` と `restart_all.bat` の停止確認リストに `record_liquidations.py` が入っておらず、`start_all.bat` の :launch にだけ入っていた。停止されない → 起動ガードが「既に動いている」と判断 → 新しいコードで起動し直されない。 (3) **二重起動を防ぐものが無かった**: 起動ガードはプロセス走査だけで、停止漏れやタイミングで擦り抜けうる。記録器自身は何も持っていなかった。**なお pid が 2 つだったことが二重書き込みを意味するとは限らない** — ログの「記録開始」は 1 行しかないので、venv の起動スタブがもう 1 つの python.exe として見えているだけの可能性が高い。**未確認**(確認手段を `check_liq_recorder.bat` に追加した) | (1) 起動側で **`PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8:replace`**(`start_all.bat` / `fetch_all.bat`)。約 40 スクリプトを一度に無害化する。記録器と到達確認は自前でも `sys.stdout.reconfigure(errors="replace")` を持つ(手動起動でも死なないように)。 (2) 停止リストに追加し、**「start_all が起動するものは stop_all が止め、restart_all が確認する」をテストで固定**(3 つの bat を突き合わせる)。これが今回を捕まえる仕掛け。 (3) 記録器に**排他鍵**(`data/liquidations.lock`)。取れなければ**起動しない**。常駐なので鍵の時刻を 45 秒ごとに更新し、180 秒古い鍵は死んだプロセスのものとして奪う(クラッシュ後に永久に起動できなくなるのを防ぐ)。強制終了した側の責任として `stop_all.bat` が鍵を消す。 (4) `asyncio.gather(..., return_exceptions=True)`: 1 取引所の異常で他の記録まで畳まない。 (5) binance_um には**対照ストリーム**(`btcusdt@aggTrade`)を追加し、「静かなだけ」と「データが流れていない」を判定で区別する。テスト 7 件追加 |
