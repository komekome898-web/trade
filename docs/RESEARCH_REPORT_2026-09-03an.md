# 第39報(an): C2 レーダー窓内サブセット — 判定到達、棄却

日付: 2026-09-03。`scripts/judge_gates.py` G4a(composite.yaml `radar_window.gate` の凍結バー:
サブセット n≥15・ネット≥+0.15%/取引・maxDD<10%・日クラスタCIが0を除外)。
データ: オーナーPC共有の `paper_logs/bot.jsonl`(champion ペーパー、決済済み59取引)。
注: リード側の判定ハーネスが古いローカルコピーを読んでいたため到達を9日見逃した。
恒久修正済み(`gates.shared_or_local`: 共有コピーが新しければそちらを読む)。

## 1. 判定

| 項目 | 実測 |
|---|---|
| 窓内(12:30–15:00 UTC)サブセット n | **21**(59取引中) |
| 中央値ネット | **−0.375%/取引**(バー +0.15%) |
| 勝率 / 実現損益 | 23.8% / −6,796円 |
| 日クラスタ95%CI(11日) | **[−0.404, −0.094]** — 0を**負側に**除外 |

**FAIL。** 窓内はむしろ全体(−0.164%/取引)より悪い。「レーダー窓が champion の負けを反転させる」
仮説は、正確には逆で、窓内(嵐時計)では順張りシグナルの逆選択が増幅されていた。

## 2. 帰結

- `radar_window` モジュールは OFF のまま恒久閉鎖。C3(ロングオンリー)は第22報で棄却済み。
  これで **champion のサブセット救済案は全滅**。
- champion ペーパーの継続理由「C2判定まで収集継続」は消滅。以後は bitFlyer 側の
  データ収集(板・約定テープ・OI・ベニュー)の運搬役としてのみ稼働(停止はオーナー判断)。
- bitFlyer 側の係属は S12(判定間近)・G6・G8・監視モードのみ。
