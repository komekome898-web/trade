# KNOWLEDGE.md — このプロジェクトが実測で確立した知識

出典記法: `b`〜`m` = `docs/RESEARCH_REPORT_2026-08-20{b..m}.md`(第2〜13報)、`n`〜`q` = `docs/RESEARCH_REPORT_2026-08-21{n..q}.md`(第14〜17報)、`r`/`s` = `docs/RESEARCH_REPORT_2026-08-22{r,s}.md`(第18・19報)、`u`/`v` = `docs/RESEARCH_REPORT_2026-08-22{u,v}.md`(第20・21報)、`w`/`x`/`y` = `docs/RESEARCH_REPORT_2026-08-25{w,x,y}.md`(第22〜24報)、`BT` = `docs/BACKTEST_REPORT_2026-08-20.md`(第1報)、`T` = 戦略トーナメント(`scripts/research_tournament.py` + 第13報予定)。

## 1. コスト実測(FX_BTC_JPY / Crypto CFD)

- 手数料は taker/maker とも **0%**。コストは実質「半スプレッド + スリッページ2bps」だけ (`config/products.yaml`, `docs/NOTE_CRYPTO_CFD_2026-08-20.md`)
- **taker 片道 ≒ 3.2bps**(半スプレッド1.18 + スリッページ2.0)、**往復 6.35bps** (e)
- **バースト時は 3.96bps**(1.96+2.0)、平穏時は 2.93bps(0.93+2.0)— **スプレッドはバースト時に約2倍** (i)
- 板記録での実測スプレッド 1.56〜2.22bps(mid比)は、30秒以下の全条件付きドリフトを上回る (f, g)
- maker 建ては手数料もスプレッドも払わないが、**逆選択で6〜9bps失う**(§2)。3つの独立研究で実測 (i, j, k)
- 現物(XRP_JPY 等)は taker 0.15%/片道 + スプレッド0.15% → **往復0.55%**。CFDの約10倍 (BT)
- 資金調達率は8時間毎決済(05/13/21 UTC)、実測平均 |rate| ≒0.017〜0.02%/8h (f)

## 2. 市場構造の法則

- **コストの壁** — 単一銘柄テクニカル(ema_cross / rsi_reversion / breakout、全時間足・パラメータ探索込み)は例外なくコスト未満。損失 ≒ 取引回数 × 往復コスト (BT, b)
- **消えた遅れ** — 分単位のリード・ラグは高流動性市場では裁定済み。薄いXRP_JPY は lag1 +0.176 だが厚い FX_BTC_JPY は **+0.019**(同時相関+0.878)。**コストが低い場所に遅れはなく、遅れがある場所はコストが高い** (b, c)
- **秒単位にだけ遅れが残る** — 1秒足では lag1 +0.11〜0.25、吸収に1〜2秒。ただし通常規模のイベントではコストに負け、閾値20bps級の激変時のみプラス (e)
- **予測可能な脚は取引不能な側** — ベーシス乖離を閉じるのは遅行する現物側であり、CFD自身は同方向にドリフトする。効率性は一貫した論理で貫かれている (f)
- **逆選択の壁・シグナル文脈の顔** — シグナル発火文脈では逆選択は約定率の低下として現れない。発火直後の成行フローは逆側指値を中央値1.6秒で**96%約定**させ、約定群はその後もシグナル方向へ 5分4.5bps・240分14.9bps 走り続ける。**約定の速さと確実さは優位性ではなく逆選択の強度そのものである**。また符号反転で移せるのはグロスのみで、コストと逆選択は反転しない(champion 反転の全11執行型で最良 +0.018%/取引 = バーの1/8.2) (x)
- **バースト面の法則(全面地図、約1,190セル)** — バーストは面のほぼ全域で**平均として継続**する(無条件の反転領域は900セル中3・全て非有意)。thr に単調、**地平600〜1800秒で最大化**(S9 の30分タッチと S10 の120秒地平は面の谷側の点だった)。中央値はほぼ全域ゼロ近傍 = 平均は右裾駆動。素の面でコスト線(往復7.92bps)を超える78セルの全量は**窓重複の幻**(非重複標本で +9.40→+1.46bps)。正直な標本で線の上に出るのは**時計窓内×エピソード先頭×20bps/30–60s×30分保有の交点のみ**(+23.71bps コスト前、CI [+7.92,+37.27]、n=40、1.44回/日)— 既知3法則(h時計・k継続・e閾値階段)の交点であり新法則ではない → S12 として係属登録。**旧「taker族はコスト線の下」(v,x)はこの交点についてのみ反証**。「雪崩は120秒で戻す」(x)は**TP約定群の選択後の性質**であり無条件には成立しない(訂正)。maker反転は逆選択の壁の第4の独立実測(約定群 全地平負/取り逃し群 正、差最大+32bps)で機構水準で閉鎖 (y)
- **リーダー追随族の死亡診断(機構水準)** — 1分スケールにリード・ラグは存在しない(同時相関+0.890、lag1 +0.016)= 分スケールの「Binance追随」は自己モメンタムと同一機構。210日で全25セル×5地平のドリフト −0.06〜+0.09% < コスト床0.079%、n≥400セルの taker 線超え **0.0%**(面の正領域は n が2桁の測定不能領域と一致、corr=−0.568)。出口では救えない: 必要勝負比5.55 対 実現2.29(n=898)、ストップ有無・時間切れとも210日全セル負。レジームが変えるのは**頻度だけ**(嵐日27.5倍)— 発火条件付きドリフトは嵐日でも悪くない。採用時 +0.19%(n=15)は同一分布の中央値的標本(P=0.52) (y)
- **逆選択の壁** — 指値は「価格が突き抜ける瞬間」にだけ約定する。逆張りでは敵(レンジ逆張り: 約定は置いた指値の0.86%のみ、約定後平均−6.5bps逆行 / アンカー乖離: 約定群+0.34 vs 取り逃し群+6.50、差6.2bps / スキャルパー: 差9.3bps)、順張りの出口では味方(TP指値は勝ちトレードを選んで約定、選択効果+33bps)。1分足スケールでも再現し**+88bps**と更に大きい(トーナメントC1)。**いずれもシグナルの符号は変えられない** (i, j, k, T)
- **レンジ端タッチは順張りイベント** — 穏やか相場の23,642件のバリアレースで **10bps戻り31.7% vs 10bps突破61.4%**。「ブレイクはフェードに勝る」 (k)。**レジーム条件付けでも生存**: vr下位四分位(深部静穏、duty 25%)は戻りを+6〜7pp動かすが最良でも34.5% vs 61.1% — コイントスにすら届かない (o)。**市場横断で生存**: USD/JPY 3.5年・コスト1/9でも非重複レース戻り40% vs BTC 34% — BTCの性質ではなく価格の性質 (s)
- **嵐は時計に支配される** — 予兆13仮説のうち合格は **UTC 12:30–15:00(米セッション開始帯)のみ**(リフト2.23・再現率0.33・n=234、月次1.84〜4.05で安定)。IV・OI・フローは時計の情報を超えず、DVOLを重ねるとリフトは**下がる** (h)
- **嵐の方向は事前予測不能** — 上下は49.3/50.7%で無偏。定義窓の内側を読むと「予測」に見えるのはトートロジーで、31分前読みではコイントスに崩壊 (i)
- **出口チューニングの限界** — 曲面に尾根はなく、fallback軸は隣接ステップで符号反転するノイズ。適応型TP(ボラ/シグナル強度/出来高連動)は定数の大TPと有意差なし=「特徴量に応じたTP」ではなく単に「大きいTP」を発見していただけ (k, l)
- **時計窓はタッチ継続を増幅する** — 30分極値タッチ後の順方向ドリフトは時計窓内 +6.81bps@30分 vs 全日 +1.15bps(n=821/7,157、bitFlyer 30日足)— レンジ端継続則(k)と嵐時計則(h)の**相互作用**の初の直接測定であり C2 の機構的裏付け。ただし平均は右裾駆動(中央値タッチはフェード側)で、往復taker 7.92bps に届かない — 候補はコスト線の下 (v)
- **両側フローレジームは実在し、makerが正になる唯一の場所** — 双方向takerフローの窓(W=30s: duty 11.5%、出来高の26%を運ぶ、嵐時計帯リフト1.85x)。テープの実現半スプレッドは1.169bps。maker捕獲は窓内外でほぼ一定(1.1〜1.4bps)で**内外差の全ては逆選択**: 窓内では逆選択が約5秒で飽和(5s→60s比1.0〜1.2)、窓外では倍増(1.8〜2.0)=ノイズフロー対情報フローの署名。理想化makerのネットは窓内のみ正(+0.38〜+0.76bps)、無条件maker(常時両建て)は負。窓後の前方ドリフトはむしろ窓内の方が大きい(3.7 vs 2.5bps)= 静穏レジームではなく**双方向レジーム**。上限は薄く、fill率f=100%で8〜13万円/日、f=1%で約1,300円/日 — **可否を決めるのはレジームではなく行列(f)**。なお1ロット建玉ではこの天井に機構的に届かない(戻り脚が収益ではなく費用になる、§3索引の高速サイクル行) (u)

## 3. 棄却済み仮説の索引

第11報時点で検証済み仮説40件 / 棄却36件(第12報でさらに出口候補27件を棄却)。系統別:

| 系統 | 内容 | 棄却理由 | 出典 |
|---|---|---|---|
| 単一銘柄テクニカル | ema_cross / rsi_reversion / breakout × 4時間足 × グリッド探索 | コスト前に優位性なし。「最良」は取引回数最少の構成 | BT |
| 通常規模のモメンタム追随 | XRP / BTC現物 / FX、taker・maker、閾値0.05〜0.25%の12構成 | 条件付きドリフト < 往復コスト | b, c |
| 長期モメンタム | 2年分 4h/日足の時系列モメンタム | train +1.7〜22%/取引 → val・OOS 全負(強気相場の丸暗記) | b |
| 出来高系 | 出来高スパイク後ドリフト / いなご(出来高急増追随) / 出来高加速 | 同一分内に織り込み済み。加速は嵐の冒頭検知で先行性なし | b, d, h |
| ヒゲ逆張り | 15分足、mult2.0 / 穏やか+指値 | 21日で合格 → 新鮮データで崩壊、構成の乗り換わり(過学習の兆候) | d→g, j |
| レンジ逆張り | 素 / 穏やか+指値+嵐損切り / 執行逆転(成行建て+指値TP) | 全執行極性で負(−3.30 / −9.12bps)。レンジ端は順張りイベント | d, j, k |
| アンカー乖離逆張り | v1(21日)/ v2(30日・事前登録)/ 穏やか+指値 | 生のフェード効果が往復コスト6.3bpsを超えない。退役 | g, j |
| ベーシス平均回帰 | CFD−現物乖離の極値フェード | 回帰は実在(半減期9.1分)だが閉じるのは取引不能な現物脚 | f |
| 常時型スキャル | 秒スケール thr=3〜5bps の高頻度回転 | 全窓でネット −2.6〜−6.1bps。回すほど確実に損 | e |
| 嵐の予兆(価格・出来高・IV・大口) | スクイーズ / 解放初動 / FR偏り / 吸収 / 合成 / DVOL 2種 / 平均約定サイズ | 最大リフト1.57(基準2.0未満)、合成は逆相関 | h |
| 嵐の方向予測 | モメンタム / フロー / 24時間レンジ位置ほか | 定義窓を外すとコイントス。レンジ位置は日次で−6.6bps | i |
| 時間帯の方向性 | 時刻別・曜日別ドリフト | 全てノイズフロア(1bps)未満 | f |
| 板不均衡の単独taker戦略 | 買い板優勢→上昇の直接執行 | 効果と符号は実在するが 0.29〜1.35bps < スプレッド2.22bps | g |
| スキャルパー武装閾値 | レーダー点灯中に閾値 10→8bps | 限界トレードが系統的に低質(−4.14bps) | i |
| 出口設計 | E1 / E3 / E4、固定曲面24セル、適応型TP 3方式 | E2を有意に上回る構成が存在しない。曲面はノイズ | k, l |
| メインBOT出口グリッド | exit_pct × 損切り × 利確 × 時間切れ 54構成 | 訓練最良構成が val・OOS 両方で現行に敗北 | k |
| 旧bot要素(ヒゲ無効化ストップ / vr予兆 / vrフィルタ) | katsuo/matilda 由来の3判定 | ヒゲストップは30日実データで勝つが val・OOS で敗北(21日合格→新鮮死のヒゲ族と同型をゲートが拒否)。vr はリフト≈1.00 の完全無情報。champion のエントリーは構造的に高vr側で matilda の静穏モードと非重複。**適用範囲の明確化(2026-08-25)**: 閾値は現行データの分位で走査済み(絶対閾値スケール非依存)だが、転写は**1分足/60分窓**であり matilda 固有の **5秒足/30秒窓**スケールは未検証。5秒スケールの再測はスプレッドMM研究(同じ時間スケール・同じ板データ)の副読み値として実施し、両側フローレジーム検知と直接比較する | n |
| バーストスキャルパー(本体) | Binance追随・秒スケール・maker建て・E2出口 | **ペーパー正式判定で棄却**: 86イベント net −3.83bps、95%CI [−9.2, −2.8] がゼロ未満。リプレイ2本の予測と三重一致 | p |
| TPのボラ連動 | 高ボラ帯ほど遠いTP(第12報 +8bps/9シグナル) | ライブ前方検証 n=77 で傾き−0.20bps に消滅。9シグナルの偶然と確定 | p |
| TP上限(maker TP +0.3/0.5/0.8%) | トーナメントC1。出口を指値TPに置換 | **機構レベルで棄却**: 選択効果+88bpsは実在するが、champion の期待値は1本の太い右裾で、どのTP上限もそこだけを切り落とす(出口効果−37bps 対 コスト削減+1.8bps) | T |
| 高速サイクルmakerスキャルパー(1ロット・両脚maker・TP2〜3bps) | T1バースト後回帰側 / T2両側窓内クオート、12セル×2約定モデル | **探索天井が負**(楽観的モデルでも EV −2.19〜−3.17bps、収益基準ゼロ通過): T1は壁の敵側(30s逆選択−12.9bps)、T2はエッジ寿命3秒を保有14〜32秒が食い潰す。abortがtaker費用を復活させ、無料化しても負。1ロットの往復は連続両側の天井に機構的に届かない(差≈捕獲1脚)。正式判定は未実施 — PREREG_fast_cycle.md 凍結、復活条件(実板 capture+adverse(5s)≥+0.38bps)のみ | u |
| **メインBOT champion(xborder_momentum k=30/thr0.8/exit0.05)** | Binance追随・分スケール・taker、0.5%保護ストップ | **ペーパー正式判定で棄却**(第22報): 30取引 net −0.148%/取引、CI [−0.243, +0.063] がバー+0.15%を排除。損失の全額はストップ束(7本×−0.60%)、シグナル決済21本は平均+0.01%=エントリ後ドリフト消滅。採用時 n=15 の +0.19〜0.36% は再現されず(ヒゲ族と同じ「薄いn合格→新鮮死」署名)。頻度も0.7→6.6回/日と10倍(レジーム差)。コストモデルの保守性(実測比2倍)を全額戻してもグロス≒−0.02% | w |
| C3 ロングオンリー(composite `long_only` モジュール) | champion のロング側のみ許可 | **判定で棄却**(第22報): サブセット n=17 net −0.013%、CI [−0.194, +0.196] が0を含む。トーナメント n=6 勝利は過学習兆候どおり新鮮死 | w |
| S10 雪崩追随(thr20/30bps・2秒窓・taker建て・E2出口) | 4セル(thr×TP{10,20})、レイテンシ1秒、スリッページ感度併記 | **実現可能性で棄却**: 意味ある n を持つ thr20 の2セルが前後半×両コスト仮定の4通り全て負(−0.5〜−12.8bps)。thr30 は n≤2。凍結トリガの頻度 0.65/0.15回/日で判定バー n≥30 に46/206日 = 到達不能。再開条件: WS駆動執行で実測レイテンシ<1秒(ただし t+0 でも +0.64bps)。新鮮データ不消費 | x |
| S11 シグナル反転フェード(champion 信号の逆側 maker 指値・TP{0.3,0.5}%×時間切れ{60,240}分・stop0.8%) | 「シグナル方向の成行が逆側指値を約定させ、旧損切り=新利確で収支反転」(オーナー提案) | **実現可能性で棄却(後半40%が4セル全負)**。前提「逆側指値は約定しやすい」は強く実証(約定率96%、遅延中央値1.6秒)されたが、それが逆選択の強度そのもの(約定群が240分で−14.9bps 走られる)。損失機構は勝ち0.5%切り・負け0.84%走りのサイズ非対称 = C1 右裾トートロジーの鏡像。反転算術の最良型でもバーの1/8.2。新鮮データ不消費 | x |
| 嵐時計ブラケット(S9: 窓内30分極値順張り・taker両側・stopのみ・TPなし) | X∈{10,15,20}bps × T∈{30,60}分の6セル、時計窓 12:30–15:00 限定 | **実現可能性で棄却(コスト負け)**: 窓内タッチ継続 +6.81bps@30分は実在するが往復7.92bps未満。全6セル負(−2.8〜−7.1bps)、凍結済み選択規則により新鮮データ不消費。maker再監査は符号法則により構造的に不可(突き抜け方向の指値は約定しない)— 残る再監査軸はイベントクラス絞り(→S10) | v |

## 4. 係属中の仮説と判定条件

- **スプレッドMM / 板不均衡フィルタ** — 板の連続記録が1〜2週間分(約0.5〜1GB)に達した時点で判定。検出力分析: |ρ|≈0.05級は1〜5秒で5〜6時間、30秒で35〜40時間の記録が必要 (f, g)。**判定は上記の高頻度/MMクラス基準を適用**(2026-08-22事前登録 — 板データ成熟前に固定)。前段研究完了(§2両側フロー則, u): 板研究の**第一の仕事は fill率 f の実測**(全てが 1/f で効く)。t≥2 に必要な板日数: f=5%で約6.5日、f=1%で約33日(W=30、sd 7.5bps基準)。収益レベルは前後半で2倍動くため**符号のみ引用**
- **OI系(OI急増 / OI増×静穏 / L-S比極値)** — OKXの履歴は約30日固定でページング不可と実証済み。自前の `data/oi_snapshots.csv` が30日(≒2,900行)に達した時点でフェーズC判定 (h)
- **約定後5分回転(maker両建て・TP 2〜3bps)** — 約定後5分の+1.1bps戻りは実在するが同一30日データからの生成のため自己汚染。**新鮮データ2週間分**で §5 の再監査基準を一度だけ適用 (j)
- **資金調達決済後ドリフト** — 13:00 UTC 決済後30分に −8.2bps(n=21, σ=20.9, t≈1.8)。**サンプル3倍**で再検定 (f)
- **C2 レーダー窓内エントリー限定**(`radar_window` モジュール、既定 OFF) — トーナメントは候補生成のみ(判定 n=2、210d代理で符号反転)。判定は champion のペーパー取引の**窓内部分集合**が §5 常設基準(ネット +0.15%/取引以上 かつ 最大DD 10%未満)を満たし、イベントクラスタCIが0を除外し、フレッシュデータで追認できることを示す docs/ のレポートを**オーナーが承認**した時点。**部分集合 n≥15 は §5 の全体基準「30取引」からの意図的な逸脱**(窓内で30取引に達するには約10か月かかり判定不能になるため)であり、§5 が15と定めているわけではない — レポートと同時にオーナーが逸脱ごと承認する (T)
- ~~C3 ロングオンリー~~ — **判定済み: 棄却**(2026-08-25 第22報、§3索引)。n=17 で CI が0を含み FAIL
- **高速サイクルmakerスキャルパー復活チェック**(`docs/PREREG_fast_cycle.md` 凍結済み)— **既定は実行しない**(探索天井が負、§3索引)。唯一の復活条件: 新鮮データの実板で T2 の capture+adverse(5s) **≥+0.38bps**(S7窓内理想下限)。この事前チェックはスプレッドMM研究の f 実測と同じ板データで走るため追加費用ゼロ。判定実行はオーナー承認制(PREREG §9)。日次シャープは年率換算(×√365)で凍結 (u)
- ~~S10 雪崩追随~~ — **探索済み: 実現可能性で棄却**(2026-08-25 第23報、§3索引)。v の頻度見積り(2.8回/日)は1分窓代理で、凍結された2秒窓では0.65回/日だった
- **S12 時計バースト30分保有**(`docs/PREREG_clock_burst.md` 凍結 2026-08-25)— W=60s・|Δmid|≥20bps・時計窓内発火のみ・フラット時のみ・taker建て・30分時間切れ決済のみ(TP/stopなし)。汚染面で4関門(台地・前後半・両側正・非重複)通過: コスト前+23.71bps、CI[+7.92,+37.27]、n=40、1.44回/日。**約1,190セル探索の生存者であり汚染面の数値は採用根拠にならない**。判定: フレッシュ(2026-08-25T12:00Z以降)テープで n≥30(見込み約21日)に達したら一度だけ、バー = ネット≥+5bps(実測スリッページ込み)・日クラスタt≥2/CI>0・maxDD≤1000bps。通過しても採用ではなくペーパー第2段(オーナー承認)へ (y)
- **壁板手前指値**(旧bot matilda 由来、docs/legacy/) — 大口壁板の直前に指値を置き、壁に先に流れを吸収させて逆選択(実測6〜9bps)を減らす執行仮説。板記録1〜2週間で判定: 板リプレイで「壁手前置き vs 単純タッチ置き」の約定条件付き期待値差 **≥+2bps** かつ 日次クラスタ t≥2.0 かつ 約定 **100件以上**で執行候補化(戦略ではなく執行オプション)。**パラメータのスケール再設計(2026-08-25、オーナー指摘による。板データ判定前の再指定)**: matilda の絶対値(壁=1BTC・2円手前)は2020年環境(BTC≒100万円・レバ4倍)の値であり今の板には存在しない(1BTC はベストレベル中央値 0.0182BTC の55倍、2円は当時0.2bpsに対し今0.0016bps)。判定は**絶対BTC・絶対円を使わず**、壁 = レベルサイズ ≥ k×(直近24hベストレベルサイズ中央値、バーンインで実測・以後24hローリング)、k ∈ {10, 30, 100} を族として凍結。手前オフセット = 1ティックを基本、2020年等価0.2bps(≒24円)を感度として併記。バー(≥+2bps・t≥2・n≥100)は不変

## 5. 検証済みの実務決定

- **スキャルパー出口 E2**(TP指値+10bps / 120秒で taker フォールバック)を採用。エッジ採用ではなく**分散改善**(中央値・勝率・sd・損失有界性で旧E0を支配)。ペーパー30イベントのカウントは切替時点から再スタート (k, l)
- **メインBOTの戦略切替もカウント再スタート** — `config/config.yaml: strategy.name` の変更(例 `xborder_momentum` → `composite`)は E2 と同じ先例に従い、ペーパー**30取引のカウントを切替時点から数え直す**(切替前後の取引を混ぜて基準を判定しない)。なお `data/overlay_state.json` は戦略に依らず蓄積されるため、リスクオーバーレイのブレーキは切替直後の1ティック目から有効になる
- **武装閾値の撤回** — `run_scalp_paper.py --thr-armed-bps` 既定を 8→10 に戻す。レーダーの武装状態はJSONL記録を継続し「同一閾値での窓内 vs 窓外」を検証 (i)
- **メインBOT出口は現行維持** — 54構成監査で事前登録の変更条件を満たさず。バックテストエンジンに決済理由(signal / stop_loss / take_profit / time_exit)を恒久記録。現行は実データの約2割が0.5%保護損切りで終了し、時間切れ(240本)は一度も発火していない (k)
- 事前登録の採用基準:

| 対象 | 基準 |
|---|---|
| メインBOT(大変動追随) | 判定済み: **棄却**(30取引・−0.148%/取引、CIがバーを排除、第22報)。バーは +0.15%/30取引/maxDD10%だった (c, d, g, w) |
| バーストスキャルパー | 判定済み: **棄却**(86イベント・−3.83bps、第16報)。バーは +5bps/30イベントだった (e, i, p) |
| レーダー(予兆) | リフト **2.0以上** かつ 再現率 **0.10以上** かつ 嵐 **30件以上** (h) |
| 再監査系(穏やか+指値) | 判定区間 **100取引以上** かつ ネット **+2bps/取引以上** かつ 日次クラスタ **t≥2.0** (j) |
| **高頻度/MMクラス**(目安 20回/日以上) | 1取引あたりの値幅バーは**課さない**(オーナー指摘 2026-08-22: 高頻度の本質は小単価×回転数×検証速度)。代わりに: ネットEV>0 を**クラスタ補正 t≥2.0**で立証(n≥300、微小エッジ可)かつ 日次ネット期待値 **+10bps/日以上**(実効サイズ換算)かつ 日次シャープ **≥1.0** かつ maxDD **≤10%** かつ 実効約定率・逆選択の実測報告。判定は板リプレイ→ペーパーの2段 |
| 共通手順 | Train+Val のみで選択 → OOS は一度だけ実行。フレッシュデータ追試に失敗した候補は落とす (g) |

基準未達の場合の正式な成果物は**却下レポート**であり、基準の緩和ではない。

## 6. データ資産と寿命

- **bitFlyer 公開約定履歴は直近31日まで**(90日バックフィルは不可能と実証)。フェッチャーは制限到達で優雅に打ち切る → **恒久保存は `backtest_data/` へのスナップショットが必須** (g)
- OKX の OI / L-S比履歴も約30日固定・ページング不可 (h)。Binance 先物(資金調達率・OI・清算)は HTTP 451 で地理的ブロック (e)
- Claude Code 開発環境は一時VMのため `data/` はセッション終了で消える。継続蓄積は自宅マシン側 (`docs/OPERATIONS.md` §6)
- 恒久スナップショット(`backtest_data/`): `candles_FX_BTC_JPY_20260820.csv`(21日)/ `candles_FX_BTC_JPY_30d_20260820.csv` / `candles_{BTC,ETH,XRP}_JPY_20260820.csv` / `flow_FX_BTC_JPY_20260820.csv` / `storm_events_20260820/`(嵐16件×2時間窓、796KB)/ `burst_events_20260820/`(平常バースト99窓×40分、1.5MB)/ Binance・bitbank の 1m・4h・1d / **`executions_FX_BTC_JPY_31d_20260823.csv.gz`(98.2万約定、2026-07-23〜08-23、10.8MB — S10探索テープ)+ `candles_FX_BTC_JPY_31d_20260823.csv.gz`** / `okx_btc_{oi,lsratio}_{5m,1h}_20260823.csv`(1hは30日分)
- **Binance 遡及取得の実証**(2026-08-23): `data.binance.vision` の日次アーカイブ(spot 1s klines・aggTrades)は直近日も約3か月前も HTTP 200 で到達可 — **保持期限なし、研究時の遡及取得で足りる**(先回り収集は不要)。注意: タイムスタンプは**マイクロ秒**(ミリ秒ではない)。一方 `api.binance.com`(REST)はこの環境から HTTP 451 で geo-block(fapi と同様)— 使えるのは静的アーカイブのみ
- データ収集の切り分け原則と係属研究→データ対応表: `docs/DATA_PLAN_2026-08-23.md`(今すぐ集める = 期限付きで消滅するものだけ)
- 蓄積中(`data/`): WS記録 `ws/*.jsonl.gz`(板・約定・ティッカー、実測523msg/分・約70MB/日)/ `spread_FX_BTC_JPY.csv`(5秒毎 bid/ask)/ `oi_snapshots.csv`(15分毎、OKX OI・L-S比・Deribit DVOL)/ `basis_1m.csv` / `funding_rate_history.csv`(66日・200決済)/ `scalp_paper.jsonl`(スキャルパー全判断)
