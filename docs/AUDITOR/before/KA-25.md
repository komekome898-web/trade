# KA-25 の Before(復元不要、作業ツリーに現存)

出典: `docs/DISCUSSIONS/2026-09-09_the_day_nothing_shipped.md` 96-103 行、および
`docs/OWNER_LOG.md` L-055・L-056 行(いずれも現存、改変していない)。

## `docs/DISCUSSIONS/2026-09-09_the_day_nothing_shipped.md`(該当箇所、全文引用)

> 証拠として最も明快なのは、夕方の私の提案である:
>
> > **B. 建玉状態を持つか** — 推奨は**持たない**。○ を 2 つ外していることを射程に明記
>
> オーナーの返答:
>
> > **今日 1 番論外な提案です。検査を通すことが目的で、
> > ロジックの性能を測るのに 1 番重要とまで言える箇所を外そうとしています。**
>
> **そのとおりだった。** 測定コードが建玉状態を持っていなかったので、
> **道具に合わせて機構の中心(ドテン・建玉時の挙動)を削ろうとした。**
> 「射程」という言葉で正当化しようとしていた。

## `docs/OWNER_LOG.md` L-055 行(該当箇所、抜粋)

> **オーナーに諮る枠組み 2 件**: (…) / **B 建玉状態を持つか**(推奨 = **持たない。ただし ○ を
> 2 つ外していることを射程と前提表に明記**。本単位の問いは足の形の予測力で、建玉管理は
> 執行=層 ② のため。**ただし L-046 で「○ を外すな」と自分で書いたので、外すなら明示的な
> 承認が要る。黙って外すのが最悪**)

**測定コードが建玉状態(ドテン・保有中の挙動)を実装していなかった**という道具側の制約に
対し、リードは「機構の中心を測定対象から外して射程を狭める」という形でその制約に**測定対象
そのものを合わせようとした**。これは KA-06(L-046、固定保有 `h` が意図マップの ○ 項目を
黙って外していた)と同型の違反だが、対象が異なる(保有期間の掃引軸 → 建玉状態の有無)。
