# KA-01 の「訂正前」原文

出典: `docs/DATA_PERISHABILITY.md` §2 の表(清算ストリームの行)
コミット: `1c5835b`(「Add the liquidation-stream reachability probe and recorder」、2026-09-08)
訂正コミット: `c5dc1b0`(「Add the liquidation recorder, and correct "liquidation history cannot be obtained"」)

`git show 1c5835b -- docs/DATA_PERISHABILITY.md` で復元。

## 訂正前の行(そのまま)

| 種別 | 何が消えるか | 対応 |
|---|---|---|
| **同上(最優先)— 清算(強制決済)ストリーム** | 高レバ取引所の強制決済。**無料の履歴アーカイブがどこにも無い**(Binance Vision の `liquidationSnapshot` は 404 = §4)。記録を始めた日から先しか存在しない | `scripts/record_liquidations.py` を `start_all.bat` で常駐(2026-09-08 追加、L-026)。`share_logs.bat` が `data/liquidations` を `git add -f` して PC 1 コピーを避ける。**到達確認はオーナー PC で `scripts/check_liquidation_feeds.py`(手順 P8)** — 経路によって届く取引所が違う |

同じコミットのコミットメッセージにも同じ主張が現れている:

> This is the one dataset the project cannot buy or backfill: no venue
> publishes a free liquidation archive, so history exists only from the
> moment recording starts.

## 何が誤りだったか(訂正コミットのメッセージより)

> Owner stopped the commit to ask whether liquidation history really is
> unobtainable - had every method and every venue been tried, and could the
> external tools from the shared post supply it. They were right to ask: the
> claim was too strong, and it rested on one 404 on one file at one venue.
>
> Re-checked every route with GET. The finding that changes the picture:
> Gate.io publishes per-event liquidations with no authentication, reaching
> back a rolling ~90 days.

「無料でどこにも無い」は Binance Vision 1 経路の 404 だけを根拠にした一般化だった。
Gate.io・OKX・ccxt 経由の Gate など、試していない経路が複数残っていた。
