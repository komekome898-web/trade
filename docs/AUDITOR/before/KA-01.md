# KA-01 抜粋

出典: `docs/DATA_PERISHABILITY.md` §2 の表(清算ストリームの行)

## 抜粋(そのまま)

| 種別 | 何が消えるか | 対応 |
|---|---|---|
| **同上(最優先)— 清算(強制決済)ストリーム** | 高レバ取引所の強制決済。**無料の履歴アーカイブがどこにも無い**(Binance Vision の `liquidationSnapshot` は 404 = §4)。記録を始めた日から先しか存在しない | `scripts/record_liquidations.py` を `start_all.bat` で常駐(2026-09-08 追加、L-026)。`share_logs.bat` が `data/liquidations` を `git add -f` して PC 1 コピーを避ける。**到達確認はオーナー PC で `scripts/check_liquidation_feeds.py`(手順 P8)** — 経路によって届く取引所が違う |

同時期の記述にも同じ主張が現れている:

> This is the one dataset the project cannot buy or backfill: no venue
> publishes a free liquidation archive, so history exists only from the
> moment recording starts.
