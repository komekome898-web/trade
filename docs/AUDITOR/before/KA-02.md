# KA-02 の「訂正前」原文

出典: `docs/DATA_SOURCES/LIQUIDATION_HISTORY_SURVEY.md`
コミット: `cc14f19`(100d7f4 の親。「Record the Coinalyze depth-by-interval measurement」までの版)
訂正コミット: `100d7f4`(「Record the owner-PC liquidation feed reachability, and add a Gate.io backfill」)

`git show 100d7f4^:docs/DATA_SOURCES/LIQUIDATION_HISTORY_SURVEY.md` で復元(全文はさらに長いので該当節のみ)。

## 訂正前の冒頭(但し書きが無い)

> **きっかけ**: リードが「清算の履歴は買えない。記録を始めた日から先しか残らない」と書いたのに対し、
> オーナーが「本当にあとから取得できないのか。全ての方法・取引所を試したのか。外部のツールで
> 取れるものはないのか」と差し戻した。全部当たり直した結果、リードの主張は言い過ぎだった。
>
> 方法: すべて GET(HEAD は使わない)。当セッションの経路からの実測なので、
> **地域制限は オーナー PC と異なりうる**。日付は 2026-09-08。

(この但し書き自体は L-027 の訂正で書かれたもの。しかし「オーナー PC と異なりうる」と
書きながら、下の「取れない」表は自分の経路の 451/403 をそのまま「取れない」と分類していた。)

## 訂正前の「取れない(確認済み)」表(該当行)

| 経路 | 結果 |
|---|---|
| Binance Vision `data/futures/um/daily/liquidationSnapshot/` | S3 の一覧 API で KeyCount=0(中身が空。経路と無関係) |
| **Binance REST** `fapi/v1/allForceOrders` | **451(当経路の地域制限)。仮に通っても提供は短期のみ** |
| Bybit 公開アーカイブ | 清算フラグが無い(経路と無関係) |
| BitMEX 公開約定アーカイブ | 清算フラグ無し(経路と無関係) |

Binance REST の行だけが「経路依存の 451」を「取れない」の確認済み表に同居させていた。
中身が無いことを確かめた行(Binance Vision・Bybit・BitMEX)と、経路のせいで届かなかった行
(Binance REST)が**区別されずに同じ表にあった**。

## 何が誤りだったか(訂正コミットのメッセージより)

> Owner ran procedure P8 step 1. All four venues are usable from their PC:
> Binance REST 200 (451 from the dev session's route), Bybit REST 200 (403
> there), and OKX's WebSocket connected and delivered 8 real liquidations in
> 60 seconds where it had been reset. The geo blocks were artefacts of a
> datacenter route, not properties of the venues.
