# 監査(owner-auditor)— 段 A 事前登録への **6 回目(走行前の再監査)18 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction_judge.py`(以下 JUDGE)、試験 2 本
(`tests/test_o3c_reaction_judge.py` / `tests/test_audit_gates_wired.py`)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `..._r2.md`(23 件 + ア〜エ)、3 回目は `..._r3.md`(23 件)、
4 回目は `..._r4.md`(22 件)、5 回目は `..._r5.md`(19 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 4(1〜4)/ 直す 10(5〜14)/ 聞く 4(15〜18)= 18 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない。§3 の実測)。
**`scripts/o3c_reaction.py` は 1 行も変えていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` は
1 行も触っていない。`regen_hook_manifest.sh` も走らせていない。**
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**別の委任先が作っている `backtest_data/binance_cm_o3c_supp_20260918/` と
`docs/DATA/probes/20260918_cm_metrics_missing_days.log` に触っていない。**
**コミットもプッシュもしていない。**

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 18 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r6.md` を新設(逐語を一字も変えずに貼り、18 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r5.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r6.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| JUDGE と試験を直す | リードの決定 1・2・4・5・8・9・12・13 の逐語(各件に引いた) |
| **`scripts/o3c_reaction.py` を変えない** | リードの委任文面「**`scripts/o3c_reaction.py`・`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/` は触らない(`regen_hook_manifest.sh` も走らせない)**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| **別の委任先の作業物に触らない** | リードの委任文面「**別の委任先が `backtest_data/binance_cm_o3c_supp_20260918/` と `docs/DATA/probes/20260918_cm_metrics_missing_days.log` を作っている最中なので触らない**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 標本 6 日での動作確認 / 15 の数え上げ / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 6 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**18 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 8 つ目の表(18 行)に明記した。**

**作業中に、原文に無い判断が必要になった点が 2 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書き、報告でも先に出す**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。
**そのうち 1 件(§4 の (1))は、リードが開封の前に `scripts/o3c_reaction.py` を
1 行直さないと走行が「[止め]」になる、という実害のある分岐である。**
**残り 17 件を進めるために作業は止めなかったが、報告の先頭で出す。**

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(6 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(5 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r5.md` + `scripts/o3c_reaction_judge.py` + テスト
>
> ## 監査(6 回目・走行前の再監査)— `owner-auditor` の返答
>
> 1. **[止める]** `/home/user/trade/scripts/o3c_reaction_judge.py:511-546,1281-1298` vs `/home/user/trade/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:2962,2977` — `check_run_params` が見るのは `mode` / `gap_ms` / `window_hours` / `days` の数の **4 つだけ**である。一方 §14.5 は「**`--mmr` を渡さないと `implied_leverage` 列が出ない…すると §4 の軸 E(6 群)が作れず、48 群が 42 群になり、§4.3 の 576 検定が 504 検定になって α が変わる**」と書き、決定 3 は「**`--mmr 0.004` を 6 本すべてに付ける**」と書く。**実測**(本監査、合成の走行で `implied_leverage` を空にして `main` を呼んだ): `params` の検査・サニティ #14・`check_axis_kinds`・48 群・576 行の**すべてを通過し、終了コード 0** で `judgment_576.csv` が書かれ、**E 群の 72 行が黙って「不明(n < 30)」になった**(`control_axis_kinds` は列が空でも `inherit` を返すので固定と一致する)。`summary.json` の `params` には `mmr` / `seed` / `bin_pct` / `match_order` も入っている(**実測**: 標本 4 本とも `mmr=0.004 / seed=1 / bin_pct=0.1 / match_order=table`)。**凍結した入力のうち、事前登録が「付けないと 576 が壊れる」と書いた `--mmr` と「主指標に効く」と書いた `--seed`(:2625-2627)を、この機械が見ていないのはなぜか。開封は一度きりである**(P-3)
>
> 2. **[止める]** PREREG:2614-2615,2630 vs :2707,2754 — §14.1 の走行コマンドは `--approval L-199` を固定で書き、関門は「**`docs/OWNER_LOG.md` に行頭 `| L-199 |` の行が実在しないと非 0 で止まる**」だけを見る。一方 §14.4 の 4 は「**オーナーの応答(L 番号)を得てから走らせる**」「**応答の L 番号: (まだ無い)**」と書く。**L-199 は「1 = a、9 = a」への応答であって、§13 の 8 件と待つもの 7 件への応答ではない。****報告への応答が来ていなくても、L-199 のまま §14.1 は通る。応答の L 番号を `--approval` に差し替えるのか、L-199 のまま走らせるのかは、どこに書かれているか**(P-8)
>
> 3. **[止める]** PREREG:3122-3127,3135-3141 vs `scripts/o3c_reaction.py:179-219` — §14.6 は、開封後に読みの検査(サニティ #14 / 軸の作り方)が破れたときの対処を「**その走行を調べて直す**」と書く。判定区間 456 日は既に開いた後なので、「直す」は `--mode full` の再走行を含みうるが、`check_approval` は**承認行の実在しか見ず、開けた回数を見ない**(**実測**: 関数に回数・既存出力の検査が無い)。§3 の「**一度だけ開ける**」を守る機械は無い。**開封後に検査が破れたとき、再走行してよいのか、してよいならそれを何本目の開封として §3.1 に数えるのかは、走行の前に決まっているか**(A-6、`research-protocol` §0「判定は設計ごとに一度だけ」)
>
> 4. **[止める]** PREREG:1892 vs judge:144-145,1341-1343 — §9.1 は定数化の効果として「**変えるにはファイルを書き換えるしかなく、差分に残る**」と書く。**実測**(本監査): 本ファイルを 1 行も書き換えずに、モジュールを import して `judge.REPS = 50` を代入してから `main()` を呼ぶと**表が全部書かれ、`summary.json` は `"reps": 50` と `"seed と reps の出所": "定数(引数では変えられない…)"` を同時に出した**(試験の `frozen()` と同じ経路)。実行時に `REPS == 2000` を確かめる行は無い。**「引数では変えられない」と「ファイルを書き換えるしかない」は同じ主張か。この 1 文は何を試した結果か**(O-3、A-6)
>
> 5. **[直す]** PREREG:2049 vs judge:915 — §10.1 の分岐は「**検出されず(MDE = X を必ず併記する。X はその行の MDE 列の値と単位)**」と書くが、実装は `f"検出されず(MDE = {_fmt(m)})"` で**単位を書かない**。§8.3 は単位が系統ごと(bp / 割合 / 枚 / 秒)であることを前提にしている。**この「単位」は誰がどこで付けるか**(P-2)
>
> 6. **[直す]** PREREG:1938 vs :2021,1934-1937 — 4 回目の指摘 2 で列名を `有効反復数` → `有限な複製の本数` に改めた経緯を書いたその節の中に、「**`summary.json` には最小の有効な本数と、指定した反復回数に満たないセルの数を出す**」が残っている。実装の鍵は `"有限な複製の本数" → "最小"` である(judge:1411-1420)。**同じ量に 2 つの名前があり、片方は自分が禁じた語「有効」を含む。これは 1 度だけ定義されているか**(P-2、O-7)
>
> 7. **[直す]** judge:943,948-952,1061 vs PREREG:2160,3170,2204 — 感度の表 `observation_only_<NAME>.csv` にも列見出し「**MDE(α=0.05/576)**」がそのまま出る。§10.2 は「**感度の行は 576 にも α にも F1 の読みにも 1 つも入らない**」と書く。**族に入らない行に族の α の名前を付けた列を出してよいか。読む人は 576 の α と同じものと読まないか**(P-2)
>
> 8. **[直す]** judge:1311-1314 vs :1323-1325 — サニティ #14 は 5 回目の指摘 2 で**標本の走行にも掛けた**が、`check_axis_kinds`(決定 3'')は判定 2 本と `--sens` だけで、**標本の走行には掛けていない**。標本側の群分けは判定走行で測った `g.control_axis` を使って行われる(judge:668-676)ので、標本側の列の在り方が違えば MDE の `p`・`s` の母集団が変わる。**「標本側でも帰属が決まる」という指摘 2 の理由は、軸の作り方の検査にも同じく当たらないか**(P-3)
>
> 9. **[直す]** PREREG:3095-3099,3102-3105 — §14.6 の `params` の表は標本の側に `mode` と日数しか要求せず、射程に「**「W = 24h の標本を `--sample-w8` に渡す」取り違えは、この機械では止まらない**」「**足すかはリードが決めること**」と書く。**実測**(本監査): 標本 4 本の `summary.json` の `params` には `window_hours` も `gap_ms` も入っている(gap60_w8 は `W=8.0 / gap_ms=60000`)。**要る値が在るのに検査しない理由は、道具の制約か、リードがまだ決めていないだけか**(P-3、P-14)
>
> 10. **[直す]** PREREG:2766,2762-2763 vs 本監査 — §14.4 は「**オーナーへの報告に入れるもの = 走行前の監査を何回受けたか(本版の時点で 5 回。25 件 / 23 件 + ア〜エ / 23 件 / 22 件 / 19 件)**」と書き、打ち切り条件を「**「止める」が 0 件になった回で打ち切る**」と書く。**本監査は 6 回目で、道具はまだ直りうる。オーナーに報告する回数はいつの時点の数か。報告の後にさらに監査を受けたら、その回数と「凍結した道具」の版はどこで更新されるか**(P-5、A-6)
>
> 11. **[直す]** PREREG:1695-1700 — §8.6 の読み方は「**`p` のこの誤差はそのまま MDE の誤差になる**」「**この誤差が効くのは「検出されず」の行に併記する MDE の大きさだけである**」を **事実** の印で書く。前者は `√(p(1−p))` の `p` についての感度を計算していない(0.05 の `p` の誤差が MDE を何 % 動かすかの数が無い)。後者について、MDE は分岐「**不明(MDE 未算出)**」と列「**MDEとの比較**」にも入る(:2023,2048)。**この 2 文の「事実」は式から出るか、それとも見立てか**(P-2、P-16)
>
> 12. **[直す]** judge:1379-1386 vs PREREG:2242-2251 — §10.3 は「**有限な複製の本数が 2,000 未満のセルは、本数と `1/√(2·n)` を併記して読む**」と書く。実装の一覧は `本数 < REPS` の行を全部入れるので、**群が空で本数 0 のセルも入り、`1/√(2·n)` が `None` になる**(**実測**: 本監査の合成走行で「満たないセル 72 / 最小 0」、その 72 行の判定はすべて「不明(n < 30)」)。**「本数が少ないセル」と「群が空で検定が成り立たないセル」を、読む人はこの一覧のどこで分けるか**(P-3)
>
> 13. **[直す]** judge:1102-1103,1363-1364 vs PREREG:2155-2160 — `f1_reading` は 12 セルでないとき「**読めない(12 セルのはずが N セル)**」という文字列を返すだけで、**そのまま `f1_reading.txt` と全ての表が書かれ、終了コードは 0 になる**。同じスクリプトの他の食い違い(#14 / `params` / 軸の作り方 / 群の数 / 576 行)はすべて「[止め]」で 1 ファイルも書かない。**F1 の 12 セルが揃わなかったときだけ表を出す設計は、事前登録のどこで決まっているか**(P-3)
>
> 14. **[直す]** PREREG:2137,2160 vs :1978 — §10.2 は感度 1 本を「**30 群 × 55 系統 = 1,650 行**」と書き、その 55 は「判定の 12 + 観測の 43」である。同じ §10 の項 1 は `observation_only.csv` を「**48 群 × 観測 43 系統 + W = 24h 側の 12 群 × 判定 12 系統 = 2,208**」と書く。**感度の表では「判定の 12 系統」が観測として入り、判定の表では入らない。同じ「判定の 12 系統」という語が 2 つの扱いを指していないか**(P-2)
>
> 15. **[聞く]** PREREG:2527-2536 vs :2718-2726 — §12 は「**1 周目で 472 日を使い切る。2 周目は新しい期間で測る**」「**2024-10-15〜2026-09-08 の間の在庫はこの環境で数えていない(未確認。公開アーカイブから取り直せるかも試していない)**」と書く。§14.4 の「待つもの」7 件にこれは入っていない。**2 周目の材料が在るかを 1 度も確かめないまま 472 日を使い切る判断を、開封の可否を決めるオーナーに出さなくてよいか**(P-1、P-7)
>
> 16. **[聞く]** PREREG:2718-2726 — §14.4 の「待つもの」は **7 件**(§13 の 8 件 / 凍結は 6 項目 / 関門なしの #13 / D の被覆 1.13 倍 / 台帳の作り直し / `WIRED` 未記載 / MDE の `p` の誤差)である。**リードが今回の返答でオーナーに出す一覧に、2 の「「凍結」が指すのは 6 項目だけである」が入っているか。7 件と報告の件数は一致しているか**(P-5)
>
> 17. **[聞く]** PREREG:2723 — §14.4 の待つもの 4 は「**主軸 D の被覆が 356/456 日なので、F1 の 12 セルの MDE が §8.3 の見込みの約 1.13 倍になること**」を**報告する事実**として書いており、オーナーに選ばせる選択肢(a / b)は事前登録のどこにも書かれていない。**オーナーに出す選択肢を作るなら、その選択肢は走行の前に事前登録へ書くべきものではないか(選択肢の中身次第で族・検出力の扱いが動く)**(A-13、P-9)
>
> 18. **[聞く]** PREREG:2756-2770 — 打ち切りの条件は「**走行前の監査は、監査役の返答の「止める」が 0 件になった回で打ち切る**」「**回数の上限は置かない**」である。本監査は「止める」を出しているので、処置 → 7 回目が要る形になる。**この条件の下で「凍結した道具」が確定するのはいつか。処置のたびに §13 の報告の中身(8 件)と待つもの(7 件)が増えるなら、オーナーへの報告は最後の監査の後に 1 回だけ出すのか、今回出すのか**(A-6、P-9)
>
> 判定: 止める
>

---

## 2. 18 件の処置

**行番号は処置後の PREREG / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**18 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] `check_run_params` が凍結した入力 4 つを見ていない

**処置: 直した。**リードの決定(逐語):
「**`check_run_params` は判定 2 本・感度 4 本について `mmr == 0.004`・`seed == 1`・
`bin_pct == 0.1`・`match_order == "table"` も検査し、違えば「[止め]」。§14.6 に書く。テスト。**」

- **答え(「なぜ見ていなかったか」)**: **理由は無い。見られる値を見ていなかっただけである。**
  **実測** 2026-09-18: `backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json` の
  `params` に `mmr: 0.004 / seed: 1 / bin_pct: 0.1 / match_order: "table"` が実在する。
- **JUDGE に定数 `MMR_FIXED` / `RUN_SEED_FIXED` / `BIN_PCT_FIXED` / `MATCH_ORDER_FIXED` を置き、
  `check_run_params(..., settings=True)` で突き合わせる**(JUDGE:180〜185、619〜628、665〜676)。
  **当てる先は判定 2 本と感度 4 本**(JUDGE:1467〜1500)。**標本には当てない**(理由は §14.6 に書いた)。
- **PREREG §14.6 の `params` の表に 4 つを足し、理由と監査の実測を書いた**(PREREG:3461〜3489)。
- **試験**: `test_the_frozen_run_settings_are_checked`(通る側 1 + 止まる側 5)/
  `test_the_frozen_run_settings_are_checked_on_the_sensitivity_runs_too`。

### 2.[止める] `--approval L-199` は報告への応答ではない

**処置: 直した。**リードの決定(逐語):
「**`--approval` の番号は L-199 ではなく、この報告(返答 062)へのオーナーの応答の L 番号にする。
§14.1 のコマンドを `--approval L-NNN`(NNN = §14.4 の「応答の L 番号」欄)に直す。
読みのスクリプトは事前登録の §14.4 の行「応答の L 番号: L-NNN」を読み
(欄が「(まだ無い)」なら「[止め]」)、判定 2 本の `summary.json` の `params.approval` が
それと一致しなければ「[止め]」。テスト(事前登録の行を一時ファイルで差し替えて検査。迂回旗なし)。**」

- **答え(「どこに書かれているか」)**: **どこにも書かれていなかった。**本版で決めた。
- **PREREG §14.1 / §14.2 / §14.4 の 2 のコマンドを `--approval L-NNN` に直した**
  (PREREG:2896〜2897、2899〜2911、2926、2953、3014〜3016)。
- **JUDGE に `read_approval_from_prereg()` を足した**(JUDGE:591〜616)。
  読む場所は `<--root>/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md` の
  **行の形ごと固定した欄** `**応答の L 番号**: **L-NNN**`(JUDGE:186〜193)。
  **欄が「(まだ無い…)」なら「[止め]」**(JUDGE:1456〜1465)。
  **判定 2 本の `params.approval` と突き合わせる**(JUDGE:677〜682、1467〜1472)。
- **迂回する旗は作っていない**(`--approval` という引数を JUDGE に作っていない。
  試験は `--root` を一時ディレクトリにして、そこへ事前登録の写しを置く)。
- **試験**: `test_the_approval_comes_from_the_prereg_field`(欄が空 / 番号違い / 一致 /
  事前登録が無い / 迂回旗が無いことの 5 つ)。
- **【委任の範囲外の 1 点。§4 の (1) に分けて書いた】**
  **`scripts/o3c_reaction.py` は `params` に `approval` を書いていない**(**実測**)。
  **リードが開封の前に 1 行足す必要がある。**本処置では触っていない(委任文面の禁止)。

### 3.[止める] 開封後に検査が破れたときに再走行してよいか

**処置: 直した(事前登録に書いた)。**リードの決定(逐語):
「**事前登録に書く: 開封後に読みの検査(#14 / 軸の作り方 / params)が破れたら、
`--mode full` の再走行はしない。破れた走行に依存する判定は「不明(検査で止まった)」として
結果に書き、1 周目はそこで終わる。再走行は新しい開封として §3.1 に数え、オーナーの決定が要る。**」

- **答え**: **走行の前には決まっていなかった。**本版で決めた(PREREG:3555〜3578)。
- **`check_approval` が開けた回数を見ていないことは監査役の実測のとおりである**
  (**本処置では `scripts/o3c_reaction.py` を読んだだけで、1 行も変えていない**)。
  **機械が無いので、規約で閉じた。**

### 4.[止める] 「引数では変えられない」と「ファイルを書き換えるしかない」は同じではない

**処置: 直した。**リードの決定(逐語):
「**`main()` の冒頭で `REPS == 2000` と `SEED == 1` をリテラルと突き合わせ、違えば「[止め]」
(モジュール属性の差し替えを止める)。summary の文面「引数では変えられない」を
「引数では変えられず、実行時にも 2000 / 1 と突き合わせる」に。
テストは monkeypatch をやめ、合成データで 2,000 回そのまま走らせる
(遅ければテストのデータを小さくする)。§9.1 の「ファイルを書き換えるしかなく、差分に残る」は
「実行時の突き合わせも通す必要がある」に直す。**」

- **答え(「この 1 文は何を試した結果か」)**: **何も試していなかった。**
  監査役の実測(`judge.REPS = 50` を代入して `main()` を呼ぶと表が全部書かれた)のとおりである。
- **`REPS_FROZEN_LITERAL = 2000` / `SEED_FROZEN_LITERAL = 1` を置き、`main()` の冒頭で
  突き合わせる**(JUDGE:163〜170、1406〜1414)。
- **`summary.json` の文面を直した**(JUDGE:「seed と reps の出所」= 「定数(引数では変えられず、
  実行時にも 2000 / 1 と突き合わせる)」/ 新しい鍵「反復回数と種の実行時の突き合わせ(決定 4'''')」)。
- **PREREG §9.1 の表に「実行時の突き合わせ」の行を足し、「ファイルを書き換えるしかなく、
  差分に残る」を「このファイルを書き換えて差分に残すだけでなく、実行時の突き合わせも
  通す必要がある」に直した**(PREREG:2008〜2009、2069〜2084)。
- **試験から `frozen(reps=500)` を消した。**試験は `REPS = 2000` / `SEED = 1` のまま走る。
  **合成データを小さくした**(既定の日数 60 → 40。1 日あたりの行数は 40 のまま)。
  **所要**: 本ファイル単体で **約 40 秒**(**実測**。前版は 500 回で約 10 秒だったので、
  2,000 回にすると 60 日のままでは約 48 秒だった)。
- **試験**: `test_reps_and_seed_are_checked_against_the_literals_at_run_time`
  (`REPS` / `SEED` を差し替えると終了コード 1 で 1 ファイルも書かれないことを測る)。
  **止まる側で属性を差し替える文脈管理子は `swapped()` に改名した**(用途を限った)。

### 5.[直す] 「検出されず(MDE = X)」に単位が無い

**処置: 直した。**リードの決定(逐語):
「**「検出されず(MDE = X 単位)」の単位を系統ごとの固定表(到達率 = 割合、bp 系 = bp、
秒 = 秒、枚 = 枚)から付ける。§10.1 に表を置く。テスト。**」

- **答え(「誰がどこで付けるか」)**: **決まっていなかった。**本版で固定表を置いた。
- **JUDGE に `_UNIT_TEMPLATES` / `MDE_UNITS` / `mde_unit()` を足し、`decide()` に `unit` を渡す**
  (JUDGE:279〜316、1016〜1040、1221〜1222)。
  **`main()` の冒頭で「系統に単位の無いものが無いか」を検査し、あれば「[止め]」**(JUDGE:1416〜1424)。
- **PREREG §10.1 に固定表を置いた**(PREREG:2185〜2205)。
  **判定の 576 行に出るのは到達率の 2 系統だけなので、判定の表の単位はすべて「割合」である。**
- **`BRANCHES` の語も「検出されず(MDE = X 単位)」に直した**(JUDGE:1012〜1013。PREREG:2858)。
- **試験**: `test_the_mde_carries_its_unit`(固定表に穴が無い / 純関数の文字列 /
  576 行の「検出されず」がすべて「割合)」で終わる)。

### 6.[直す] 「有効な本数」が残っていた

**処置: 直した。**リードの決定(逐語):
「**「有効な本数」の残りを「有限な複製の本数」に統一(全箇所。`grep -n '有効'` の結果を報告に貼る。
残ってよいのは禁止の記述だけ)。**」

- **PREREG:2057 の「最小の有効な本数」を「最小の有限な複製の本数」に直した**(PREREG:2051〜2058)。
- **残った「有効」の全行を報告に貼った**(§3 の 4)。**残っているのは
  (a) 禁止の記述、(b) リードの決定の**逐語**(`有効反復数` / 「有効な本数」)、
  (c)「有効数字 5 桁」という別の語、(d) 走査の節そのもの、の 4 種だけである。**
  **(b) は逐語なので変えない**(変えると `CLAUDE.md` §0.1「オーナーの原文を自分の語彙に
  置き換えて実行する」と同じ型になる。**リードの決定の逐語も同じ扱いにした**)。
- **走査の節の出力も貼り直した**(PREREG:3708〜3739。**打ち直して 2 回同じ数になるまで回した**)。

### 7.[直す] 感度の表に族の α の名前が付いた列がある

**処置: 直した。**リードの決定(逐語):
「**感度の表の MDE 列の見出しを「MDE(参考。族の α に入らない)」に変える。§10.2 に書く。**」

- **答え(「読む人は 576 の α と同じものと読まないか」)**: **読む。**列見出しを分けた。
- **JUDGE に `MDE_COL` / `SENS_MDE_COL` / `SENS_HEADER` / `to_sens_rows()` を足した**
  (JUDGE:1081〜1108、1564〜1567、1847)。**列の順と中身は変えていない。**
- **PREREG §10.2 に表を足した**(PREREG:2332〜2346)。**§14.6 の出力の表も直した**(PREREG:3602)。
- **試験**: `test_sens_writes_one_observation_only_table_per_run`(見出しが `SENS_HEADER` で、
  `MDE(α=0.05/576)` が無いこと)。

### 8.[直す] 軸の作り方の検査が標本に掛かっていない

**処置: 直した。**リードの決定(逐語):
「**`check_axis_kinds` を標本の走行にも掛ける。§11 に書く。テスト。**」

- **答え(「指摘 2 の理由は軸の作り方にも当たらないか」)**: **当たる。**同じ理由である。
- **JUDGE:1523〜1537 で `runs` + `samples` + `sens` + `sens_samples` の全部に掛ける。**
- **PREREG §11 の #14 の下に注を足した**(PREREG:2536〜2548)。
- **試験**: `test_axis_kinds_are_checked_on_the_sample_runs_too`
  (**標本の側だけを壊し、判定の走行が無傷でも止まることを測る**)。

### 9.[直す] 標本側の `params` の検査が 2 つだけ

**処置: 直した。**リードの決定(逐語):
「**標本側の params 検査に `window_hours` と `gap_ms` を足す(標本 4 本の summary.json に
値がある = 実測済み)。射程の「止まらない」の文を消す。テスト。**」

- **答え(「道具の制約か、まだ決めていないだけか」)**: **後者である。要る値が在るのに見ていなかった。**
- **JUDGE:1473〜1499 で標本にも `gap_sec` / `window_hours` を当てる**
  (判定の標本は gap 60 秒 × W 8h・24h、感度の標本は名前から読む)。
- **PREREG §14.6 の表を直し、「止まらない」「足すかはリードが決めること」の 3 文を消した**
  (PREREG:3462〜3469、3501〜3513)。
- **試験**: `test_the_sample_dirs_must_match_their_window_and_gap`
  (W の取り違え / gap 違い / 正しい組の 3 通り)。

### 10.[直す] オーナーに報告する監査の回数はいつの時点の数か

**処置: 直した。**リードの決定(逐語):
「**§14.4 の「オーナーに報告する回数」は「報告を出した時点の回数(6 回、処置済み)」と書き、
その後の監査の結果と処置は「開封の前にオーナーへ追加で報告する(§13 の表と待つものが
変わったときだけ)」と書く。**」

- **PREREG §14.4 の表に 2 行を足し、回数を 5 回 → 6 回に直した**(PREREG:3117〜3131)。
- **「凍結した道具」の版の欄(コミットのハッシュ)はそのまま残した**(開封の直前に書き写す)。

### 11.[直す] `p` の誤差が MDE に効く大きさと、効く場所

**処置: 直した。**リードの決定(逐語):
「**§8.6 の 2 文を直す: (a) p の誤差が MDE に効く大きさを式で出す
(MDE ∝ √(p(1−p)/n) なので、p の相対誤差 δ に対する MDE の相対変化 ≈ (1−2p)/(2(1−p)) · δ。
標本の p の値で数値を出す)。(b) 「効くのは検出されずだけ」→
「MDE の列・MDE との比較・不明(MDE 未算出)にも効く」に直す。印は「式から」= 事実、
数値 = 実測(標本)。**」

- **答え(「この 2 文の「事実」は式から出るか、見立てか」)**: **前者は見立て**(感度を計算していなかった)、
  **後者は誤り**(MDE は 4 か所に入る)。**両方直した。**
- **(a)** **式 `d ln f / d ln p = (1−2p)/(2(1−p))` を書き、標本の `p` と SE で数値を出した**
  (PREREG:1748〜1799。再現コマンドと 12 行の表つき)。
  **1 標準誤差あたりの MDE のずれは 0.52%〜7.53%**(**実測**)。
- **(b)** **MDE が入る 4 か所(MDE の列 / MDE との比較 / 不明(MDE 未算出)/ 検出されずの X)を
  列挙した**(PREREG:1801〜1816)。
- **印**: **式 = 事実(微分)、表の数値 = 実測(標本 6 日)。**

### 12.[直す] 本数の一覧に「群が空」のセルが混ざる

**処置: 直した。**リードの決定(逐語):
「**「有限な複製の本数」の一覧から n < 30 のセル(群が空を含む)を除き、
別の欄に「群が空 / n < 30 で検定していないセル数」を出す。§10.3 に書く。テスト。**」

- **答え(「読む人はどこで分けるか」)**: **分けられなかった。**欄を 2 つにした。
- **JUDGE:1596〜1610、1636〜1651**: 一覧は判定が「不明(n < 30)」「不明(n2 < 30)」の行を除き、
  **`n1` / `n2` の列を足した**。**除いた数は別の鍵「群が空 / n < 30 で検定していないセル数」に出す。**
  **標準出力にも出す**(JUDGE:1860〜1864)。
- **PREREG §10.3 に表を足した**(PREREG:2438〜2457)。
- **試験**: `test_low_reps_cells_are_listed_in_the_summary`(一覧の全行が n1 ≥ 30 かつ n2 ≥ 30 で
  `1/√(2·n)` が `None` でないこと / 除いた数が判定の語の数と一致すること)。

### 13.[直す] F1 の 12 セルが揃わなくても表が出る

**処置: 直した。**リードの決定(逐語):
「**`f1_reading` が 12 セルでないときは「[止め]」で 1 ファイルも書かない。§7.0 に書く。テスト。**」

- **答え(「事前登録のどこで決まっているか」)**: **どこにも決まっていなかった。**本版で決めた。
- **JUDGE:1569〜1580**: `len(cells) != 12` なら「[止め]」で終了コード 1(**表を書く前**)。
- **PREREG §7.0 に書いた**(PREREG:1196〜1212)。
- **試験**: `test_f1_must_have_twelve_cells_or_nothing_is_written`。

### 14.[直す] 「判定の 12 系統」が 2 つの扱いを指している

**処置: 直した。**リードの決定(逐語):
「**語を分ける: 感度の表に入る 12 系統は「判定の 12 系統と同じ観測量(感度では観測のみ)」と書き、
§10.2 の 1,650 行の内訳の文を「30 群 ×(観測 43 + 判定と同じ 12 = 55)系統」に直す。**」

- **答え**: **指していた**(`CLAUDE.md` §0.2 の O-7)。**呼び方を分けた。数は 1 つも変えていない。**
- **PREREG §10.2 の内訳の文と、§10.2 の表の行、§14.6 の出力の表を直した**
  (PREREG:2318〜2330、2172、3602)。

### 15.[聞く] 2 周目の材料を確かめずに 472 日を使い切ってよいか

**処置: 数えて事前登録に書き、待つものに足した。**リードの決定(逐語):
「**2 周目の材料の有無をファイルの有無だけで数える: `backtest_data/` と
`paper_logs/liquidations/` に 2024-10-15 以降の COIN-M 清算(`binance_cm_*.jsonl.gz` など)と
約定・metrics が何日分あるか(名前から日付を取る。中身は読まない)。
§12 に「在庫にある日数(名前ベース)」と「公開アーカイブからの取り直しは未確認」を書き、
§14.4 の待つものに 8 件目として足す。**」

- **答え(「出さなくてよいか」)**: **出す。**§14.4 の待つものの **ソ** に足した。
- **数え上げのコマンドと出力は PREREG §12(PREREG:2752〜2812)。**報告にも貼った(§3 の 5)。
- **結果(実測 2026-09-18、名前だけ)**: **COIN-M 清算 9 日(2026-09-09〜17)/
  COIN-M 約定 0 日 / COIN-M metrics 0 日。**
  **= 3 点セットが揃った日は 2024-10-15 以降 0 日。**
  **公開アーカイブからの取り直しとオーナー PC は未確認**(`CLAUDE.md` §5.2)。

### 16.[聞く] 待つものの一覧と報告の件数は一致しているか

**処置: 名前で書き直した。**リードの決定(逐語):
「**§14.4 の待つものの一覧を、リードの返答に載せる形(番号ではなく名前: ア〜オ = 報告 5 件、
カ〜ク = 答えが要る 3 件、ケ = D の欠け、コ = 関門なしの #13、サ = 台帳の作り直し、
シ = WIRED 未記載、ス = MDE の p の誤差、セ = 凍結は 6 項目、ソ = 2 周目の材料)に書き直す。**」

- **答え(「7 件と報告の件数は一致しているか」)**: **前版は一致していなかった**
  (待つもの 7 行のうち 1 行が §13 の 8 件をまとめていたので、オーナーに出す項目の数が
  7 とも 8 とも 14 とも読めた)。
- **PREREG §14.4 の表を **ア〜ソ の 15 行**に書き直した**(PREREG:3044〜3090。表は 3052〜3068)。
  **ア〜ク = §13 の「報告する変更」の 8 件(1 件 1 名)、ケ〜ソ = 残る 7 件。**
  **セ(凍結は 6 項目)も 1 行として入っている**(監査役の問いへの答え)。
- **【原文に無い判断・§4 の (2) に分けて書いた】**
  **「ア〜オ = 報告 5 件 / カ〜ク = 答えが要る 3 件」の分け方は、リードの返答 062 の側で決まる。**
  **本書は §13 の表の並び順のまま ア〜ク を振り、そのことを PREREG に明記した。**

### 17.[聞く] D の欠けの選択肢を事前登録に書くべきか

**処置: 選択肢にしない形で書いた。**リードの決定(逐語):
「**D の欠けは選択肢にしない。事実として報告し、取り直しの結果(別の委任先が試行中。
結果はリードが §8.6 に書き足す)を待たずに「取れた分は D の被覆に足す
(取り直した日の zip を読む経路はリードが決める)/ 取れなかった分は欠けのまま走らせる」と書く。
a / b の記述があれば消す。**」

- **答え**: **選択肢は作らない。**よって「選択肢を走行の前に事前登録へ書く」必要も生じない。
- **PREREG §14.4 の **ケ** に「事実として報告する。選択肢にしない」と書き、
  取り直しの扱い(取れた分は足す / 取れなかった分は欠けのまま走らせる)を明記した**
  (PREREG:3062、3077〜3089)。
- **§8.6 の 97 日の一覧の下にも同じ表を置き、「取り直しの結果(リードが書き足す)」の欄を
  空のまま作った**(PREREG:1906〜1918。**別の委任先が試行中で、結果はリードが書き足す**)。
- **a / b の記述は本書に無い**(**実測** 2026-09-18、**範囲は本ファイル 1 本**:
  `grep -c 'D の欠け'` = 7 / `grep -c '1.13 倍'` = 3。どの行にも a / b は無い)。

### 18.[聞く] 「凍結した道具」が確定するのはいつか / 報告は今回出すのか

**処置: 事前登録に書いた。**リードの決定(逐語):
「**報告は 6 回目の処置の後に出す(= 今回)。以後の監査で §13 の表か待つものが変わったときだけ、
開封前に追加で報告する。凍結した道具の版 = 開封のときのコミットのハッシュ(§14.4 の欄)。**」

- **答え**: **報告と打ち切りを分けた。**
  **報告 = 今回出す。打ち切り(開封してよい状態)= 「止める」が 0 件になった回。**
  **凍結した道具の版 = 開封のときのコミットのハッシュ**(§14.4 の欄。まだ空)。
- **PREREG §14.4 の打ち切りの表に 3 行を足した**(PREREG:3117〜3131)。

---

## 3. 実測(本処置で打ったもの)

### 1. 試験(3 本。`-q` 無し)

```
$ PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
........................................................................ [ 62%]
...........................................                              [100%]
115 passed in 41.25s
```

```
$ PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py --collect-only | tail -1
115 tests collected in 0.26s
```

**内訳**(**実測**): `test_o3c_reaction.py` 50 件(変えていない)/
`test_o3c_reaction_judge.py` **53 件**(45 → 53、+8)/ `test_audit_gates_wired.py` 12 件(中身を直した)。
**リポジトリ全体**(**実測** 2026-09-18):
`PYTHONPATH=src python -m pytest --collect-only | tail -1` = **`2097 tests collected in 1.46s`**(前版 2,089 件)。
`PYTHONPATH=src python -m pytest -q` = **終了コード 0(所要 4 分 22 秒)。落ちた試験は 0 件。**

### 2. §8.5 の再現(152 セル)

事前登録 §8.5 の 2 つ目の再現コマンド(本書の §8.3 の表を読んで計算し直し、1 セルずつ突き合わせる):

```
照合したセル: 152 / 一致 152 / 食い違い 0
```

**= §8.3 の MDE の表は本処置で 1 セルも動いていない**(**実測**)。

### 3. 標本 6 日での動作確認(試験用台帳・一時ディレクトリ)

**判定区間は 1 日も開けていない**(渡したのは `backtest_data/o3c_reaction_20260918_sample/` の
標本 6 日の表で、`summary.json` だけを判定の走行の形に差し替えた一時ディレクトリである。
**迂回する旗は使っていない**)。**通る側と止まる側の両方を測った。**

```
=== (1) 通る側: 標本 6 日をそのまま判定 2 本として渡す ===
判定の表: 576 行 / 観測のみの表: 2208 行 / 群 48
有限な複製の本数: 指定 2000 / 最小 1998 / 指定に満たないセル(検定したセルだけ) 24 / 群が空・n < 30 で検定していないセル 0
F1 の 12 セルの読み: 不明(差あり 0。内訳: 不明(n < 30) 0 / 不明(n2 < 30) 0 / 不明(t 未算出) 0 / 不明(MDE 未算出) 0 / 検出されず 12)
  -> 終了コード 0 / 出力ディレクトリ あり
  judgment_576.csv 行数: 576
  承認の L 番号: L-200
  reps / seed: 2000 / 1
  群が空 / n < 30 で検定していないセル数: 0
=== (2) 止まる側: params.mmr を落とす ===
[止め] 渡された走行が名前と合わない(params の検査)。表を 1 枚も書かずに終わる。
       - gap60_w8: params.mmr が None(要る値は 0.004)
       - gap60_w24: params.mmr が None(要る値は 0.004)
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (3) 止まる側: params.seed を 2 にする ===
[止め] 渡された走行が名前と合わない(params の検査)。表を 1 枚も書かずに終わる。
       - gap60_w8: params.seed が 2(要る値は 1)
       - gap60_w24: params.seed が 2(要る値は 1)
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (4) 止まる側: 事前登録の「応答の L 番号」の欄が空 ===
[止め] 事前登録 §14.4 の「応答の L 番号」が読めないので、表を 1 枚も書かずに終わる。
       事前登録 §14.4 の「応答の L 番号」の欄が埋まっていない(欄の値: '(まだ無い。)')
       オーナーの応答(L 番号)を §14.4 の欄に書き写してから走らせる。
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (5) 止まる側: 欄の L 番号と params.approval が違う ===
[止め] 渡された走行が名前と合わない(params の検査)。表を 1 枚も書かずに終わる。
       - gap60_w8: params.approval が 'L-200'(要る値は 'L-201' = 事前登録 §14.4 の「応答の L 番号」)
       - gap60_w24: params.approval が 'L-200'(要る値は 'L-201' = 事前登録 §14.4 の「応答の L 番号」)
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (6) 止まる側: REPS を 50 に差し替える ===
[止め] 反復回数と種が §9.1 で凍結した値と違う。表を 1 枚も書かずに終わる。
       REPS = 50(凍結した値は 2000) / SEED = 1(凍結した値は 1)
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (7) 止まる側: F1 の 12 セルが揃わない ===
[止め] F1 の 12 セル(§7.0)が 0 セルしか揃っていない。表を 1 枚も書かずに終わる。
       群 48 群に無い群 × 戻り到達 2 系統 × h 6 本 = 12 が要る。
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (8) 止まる側: 標本の W を取り違える(W=24h を --sample-w8 に) ===
[止め] 渡された走行が名前と合わない(params の検査)。表を 1 枚も書かずに終わる。
       - gap60_w8(標本): params.window_hours が 24.0(要る値は 8.0)
  -> 終了コード 1 / 出力ディレクトリ 無し
=== (9) 止まる側: 台帳の判定が「止める」 ===
[止め] 測定後・報告前の監査が通っていないので、表を 1 枚も書かずに終わる。
       単位: o3c_reaction_20260918 / 段階: 結果
  -> 終了コード 1 / 出力ディレクトリ 無し
```

**(1) で `--mode full` は 1 本も走っていない**(**実測**:
`ls backtest_data/o3c_reaction_20260918_full` = `No such file or directory`)。

### 4. 「有効」の走査(指摘 6。**範囲は 4 ファイル。リポジトリ全体は数えていない**)

```
$ grep -n '有効' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
66:  判定(予測できる / 使える / 有効)は 1 つも書かない。              ← 禁止の記述
216: …リードの決定「…列 `有効反復数` に出す…最小の有効反復数も出す」   ← リードの決定の逐語
286: | 6 | 「有効な本数」の残りを消す | …「「有効な本数」の残りを…」     ← リードの決定の逐語(本監査の 6)
1663:丸め: …系統 5・8 は有効数字 5 桁(末尾の 0 も書く)                 ← 別の語
2052:列名を `有効反復数` としていたが、…禁じた「有効」を含み、           ← 改名の経緯(逐語の引用)
2057:「「有効な本数」の残りを「有限な複製の本数」に統一(全箇所)」        ← リードの決定の逐語
2274:判定語(予測できる / 使える / 有効)はどの行にも書かない。            ← 禁止の記述
2283:差を書いてよいが、「判定」「有効」「使える」と書かない。             ← 禁止の記述
3687:判定語(予測できる / 使える / 有効)を、判定として書いた行は…       ← 禁止の記述
3701:for w in 予測できる 使える 有効 差なし 陰性; do                      ← 走査の節そのもの
3723:--- 有効 ---                                                         ← 走査の節そのもの
3749:…「禁止の文」か「別の語(有効数字)」か                               ← 走査の節そのもの
3751:内訳: 「有効」= 禁止の文 / 「有効数字 5 桁」という別の語 …           ← 走査の節そのもの

$ grep -n '有効' scripts/o3c_reaction_judge.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
scripts/o3c_reaction_judge.py:34:  判定語(予測できる / 使える / 有効)は出力に 1 つも書かない  ← 禁止の記述
scripts/o3c_reaction_judge.py:195: FORBIDDEN = ("予測できる", "使える", "有効", …)       ← 禁止の記述
tests/test_o3c_reaction_judge.py:13: 禁じた語(… / 有効 / …)が出力に 1 つも無いこと       ← 禁止の記述
```

**残っているのは (a) 禁止の記述、(b) リードの決定の逐語、(c)「有効数字」という別の語、
(d) 走査の節そのもの、の 4 種だけである**(**目視の分類**。上の右欄。
**この分類に機械は掛かっていない**)。**「有効な本数」を量の名前として使っている行は 0 である。**

### 5. 2 周目の材料の数え上げ(指摘 15。**名前だけ。中身は 1 バイトも読んでいない**)

```
$ python3 - <<'PY'
import re
from pathlib import Path
pat = re.compile(r'(20\d{2})-?(\d{2})-?(\d{2})')
cats = {}
for root in (Path("backtest_data"), Path("paper_logs/liquidations")):
    if not root.exists():
        continue
    for p in root.rglob("*"):
        if not p.is_file() or p.name.endswith(".CHECKSUM"):
            continue
        m = pat.search(p.name)
        if not m:
            continue
        d = f"{m.group(1)}-{m.group(2)}-{m.group(3)}"
        if d < "2024-10-15":
            continue
        low = p.name.lower()
        kind = ("清算" if ("liquidation" in low or low.startswith("binance_cm_2"))
                else "約定" if "aggtrades" in low
                else "metrics" if "metrics" in low else None)
        if kind is None:
            continue
        coinm = "BTCUSD_PERP" in p.name or "binance_cm" in low
        cats.setdefault((kind, "COIN-M" if coinm else "COIN-M 以外"), set()).add(d)
for k, v in sorted(cats.items()):
    v = sorted(v)
    print(k, len(v), "日分", v[0], "〜", v[-1])
PY
('清算', 'COIN-M') 9 日分 2026-09-09 〜 2026-09-17
('約定', 'COIN-M 以外') 66 日分 2024-11-01 〜 2026-09-05
```

**`('metrics', 'COIN-M')` と `('約定', 'COIN-M')` は 1 行も出ない = 0 日分。**
**内訳(ディレクトリ別。同じ走査を親ディレクトリで割ったもの)**:

| 種類 | 商品 | 日数 | 置き場所 |
|---|---|---|---|
| 清算 | COIN-M | **9 日**(2026-09-09〜17) | `paper_logs/liquidations/`(同じ 9 日の写しが `backtest_data/liquidations_repaired_*` にもある) |
| 約定 | COIN-M | **0 日** | — |
| metrics | COIN-M | **0 日** | — |
| 約定 | USD-M BTCUSDT(**別商品**) | 66 日(2024-11-01〜2026-09-05) | `backtest_data/binance_BTCUSDT_aggTrades_*` ほか |

**印**: **実測**(ファイル名だけを見た)。
**範囲と方法**: **`backtest_data/` と `paper_logs/liquidations/` の名前だけ。**
**公開アーカイブからの取り直しは試していない(未確認)。オーナー PC も見ていない(未確認)。**
**よって「2 周目の材料が無い」とは書かない。**

---

## 4. 原文に無い判断(2 件。黙って決めなかった)

### (1) `scripts/o3c_reaction.py` が `params.approval` を書いていない

**リードの決定 2 は「判定 2 本の `summary.json` の `params.approval` が
(事前登録の欄の L 番号と)一致しなければ「[止め]」」と書いている。**
**一方、委任文面は「`scripts/o3c_reaction.py` …は触らない」と書いている。**

**実測**(2026-09-18): `backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json` の
`params` の鍵は
`mode / days / window_hours / bin_pct / gap_ms / granularity / horizons_min / staleness_ms /
seed / mmr / side_price / control_gap_minutes / match_tolerance_pct / match_order /
data_root / metrics_root / bitflyer_dir / design` の 18 個で、**`approval` は無い。**
`scripts/o3c_reaction.py` の `run_table()` に渡している `params` の辞書にも入っていない
(`main()` の `a.approval` は `check_approval()` に渡るだけである)。

**選んだ側**: **リードの決定 2 をそのまま実装した**(`params.approval` が無ければ「[止め]」)。
**選ばなかった側**: 「`approval` が無いときは検査を飛ばす」(= 迂回の口を作る。
リードの決定の逐語「**迂回旗なし**」に反する)。

**帰結(リードの作業が 1 つ増える)**: **開封の前に、`scripts/o3c_reaction.py` の
`run_table()` が書く `params` に `"approval": <渡した L 番号>` を足す必要がある。**
**足さないまま読みを走らせると「[止め]」になる**(設計どおりだが、走行 6 本を
走らせた後で止まることになる)。**PREREG §14.1 にもこの 1 点を書いた**(PREREG:2942〜2954)。

### (2) 「ア〜オ = 報告 5 件 / カ〜ク = 答えが要る 3 件」の分け方

**リードの決定 16 は名前の割り当てを ケ〜ソ については 1 件ずつ書いているが、
ア〜ク については「報告 5 件」「答えが要る 3 件」という**群の説明**しか書いていない。**
**どの 3 件が「答えが要る」かは、リードの返答 062 の側で決まる。**

**選んだ側**: **§13 の「報告する変更」の表の並び順のまま ア〜ク を振った**(1 行目 = ア … 8 行目 = ク)。
**件数は 8 で、リードの決定の 5 + 3 と一致する。**
**選ばなかった側**: 委任先が「答えが要る 3 件」を推測して印を付ける(**推測でオーナーの
指示を実行する形になる** = `CLAUDE.md` §0.1)。

**PREREG にこの対応付けが委任先のものであることを明記した**(PREREG:3070〜3076)。
**リードの返答の分け方と食い違ったら、リードの返答が正で、PREREG のこの表を直す。**

---

## 5. していないこと(隠さずに書く)

- **`--mode full` を 1 本も走らせていない**(**実測**:
  `ls backtest_data/o3c_reaction_20260918_full` = `No such file or directory`)。
- **`scripts/o3c_reaction.py` を 1 行も変えていない**(§4 の (1) の 1 行はリードが足す)。
- **`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` を
  1 行も触っていない。`sh scripts/regen_hook_manifest.sh` も走らせていない。**
- **`backtest_data/binance_cm_o3c_supp_20260918/` と
  `docs/DATA/probes/20260918_cm_metrics_missing_days.log`(別の委任先の作業物)に触っていない。**
- **コミットもプッシュもしていない。**
- **`CLAUDE.md` §3 の「現在 2,041 件収集」は本処置で更新していない**(委任の範囲外。
  **実測の現在値は 2,097 件**)。**リードが判断すること。**
- **判定(予測できる / 使える / 有効)は本書に 1 つも書いていない。**
- **判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
