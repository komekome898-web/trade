# 検収: 道具サーベイ 区分 8 の 14 回目(2026-09-25、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 14 回目の実行(2026-09-25)` / 生ログ `docs/DATA/probes/20260923_tools_8_run14.log`(10,705 行)。届いたままのコミット c96b1e6。起動文の指紋 `9ee8e55478d6`。**実行環境の再起動で最初の調査班が 80 手で止まり(生ログはコミット 27f6a15 に保存)、立て直した調査班が同じ生ログに続けて書いた**。

## 0. 費用と時間

立て直した調査班 **621,152 トークン・3,045,008 ms(50.8 分)・道具 517 回**(最初の調査班の分は記録が失われた)。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(報告 + 生ログ 14 本)18 件(前の回からの K13 と同じもの。この回の新しいものは 0 と調査班の報告)/ `check-elements --round 14` 0 件(知見の表 69 行)/ `check "" <run14.log>` 0 件。

## 2. リードが見た点と直し

- **当たりの判定の理由の作り方(3 回目の同じ型)**: 起動文は「ファイルごとにそのファイルの当たった行を読んで書く(語ごとの決まりを先に立てて当てはめない)」と書いたが、調査班は「要素の語の組ごとの分類規則を Python のスクリプトとして組み、機械的に当てはめた。全件を 1 行ずつ読んではいない」と書いた(問い 2)。**起動文に反するので、この作り方で出した `なし` は受け取らない**: 台帳で 8-017 Great Expectations の E3a・E3b・E4、8-020 OpenBB の E3a を `未判別` に戻し、2 候補の状態を `判別に一次資料が要る` にした。
- **対策(リードの判断)**: 12〜14 回目と 3 回続けて、数千行の当たりを 1 行ずつ読むことを調査班ができずに、決まりを作って当てはめた。決まりで分けること自体は、決まりが行の字に当たるかを機械で見れば、決まりの取り違え(13 回目の色名 `darkgoldenrod` を試験の語とした型)は起きない。そこで `scripts/cat8_classify.py` を作った: 検索の手の当たった行を、書いた決まり(`<id>` `<行の字に当てる正規表現>` `<その行で語が何を指し、なぜ述語に当たらないか>`)に 1 行ずつ当て、決まりに当たらない行を全部 `unmatched` で出す。`check-elements` は 15 回目以降、`cat8_classify.py` を掛けた検索の手は、決まりに当たらない行が全部 `### 当たりの判定(行ごと)` の表に理由つきであれば、ファイルの表を求めない。決まりの中身(正規表現と理由)は生ログに全部残るので、監査で決まりそのものを読める。実測(リード、合成の生ログ): 決まり `goldenrod` は `color: darkgoldenrod` に当たり、「Recreate the golden samples」には当たらずに `unmatched` に出た。行の表が無いと止まり、あると通った。2〜14 回目の結果は変わらない。
- **範囲の未達**: 8-026 OpenClaw は、当たりが要素ごとに 1,351〜33,906 件で、検索と理由づけに手を付けなかった(「この回では終えられなかった」)。15 回目で上の道具を使う。
- 8-028 AlgoBuild を `登録が要る` にした(ダッシュボードがログインの頁、文書のサイトが DNS で届かない・404)。中身の監査で見る。
- OpenBB E1b の段 3(問い 3): OpenBB 自身が定義する `Data` の形は、広く使われるライブラリの形ではないので、段 3 のまま(L-514 の判断の 9・10 は広く使われるライブラリ・DataFrame の形)。

## 3. 台帳

`import --round 14`: 変わった所 26。§2 の直しを `set` で当てた。`check` 0 件。`recount`: 深掘り 22 / 判別に一次資料が要る 3(8-017・8-020・8-026)/ 登録が要る 2(8-005・8-028)/ 未着手 13 / 危険で導入停止 1。**残り 16 行**。

## 4. 判定

**一部受け取り**。新しい `印` と段(8-018 の 7 つの段、8-017 E1b 段 5 など)と 8-028 の `登録が要る` を中身の監査(77 回目)に通す。

## 5. 中身の監査(77 回目、owner-auditor)の逐語

> 監査 77 回目・指摘一覧(A: 道具サーベイ区分8・14回目の中身、B: cat8_classify.py / cat8_ledger.py の 15回目以降の組み込み)。
>
> ## A(区分8・14回目の中身)
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:17500` — `Vibe-Trading` E1a を段 5 とした根拠が、同じ節の知見表 #29(`:17274`「外から持ち込める対象: 記載なし…突き合わせの型 `AccountState`/`BinanceAccountSnapshot` は Vibe-Trading 自身の Binance USD-M 口座の型に限られる」)と矛盾していないか。設計票 §4.1 は「段5 = 段4 に加えて(ア)(イ)」「対象の一部だけが外から持ち込めるものは段3」と定めており、E1a の対象(突き合わせる2つの出力)が外部から持ち込めないと自分で書いていながら、その上に積む段5(ア・イ)を付けている。同じ回の中で `OpenBB Terminal` の `Data` 型(自身が定義するクラス)については「広く使われるライブラリの形式でない」として段3にとどめる判断をしている(`:19025` 問い3、リードもこれを追認)のに、Vibe-Trading の同型の制約(自身の口座型に限る)には同じ扱いをせず、問いにも出していない。段の付け方の一貫性が崩れている(P3・P9 型)。
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:17251,17493` — `Great Expectations` E1b を段5 とする根拠(イ:「結果を保存して次回実行と比較できる」)の引用元「A ValidationResultsStore manages Validation Results to ensure they are accessible via a Data Context for review and rendering into Data Docs」(生ログ `docs/DATA/probes/20260923_tools_8_run14.log:9437-9450`)には「次回実行との比較」を言う文言が無い(「review and rendering」とだけ)。生ログの手の note 欄「次回実行との比較の基盤」は調査班自身が付けた説明で、一次資料の逐語ではない。設計票 §4.1「(イ)結果を保存して次の実行と比べられるか」の条件に当たる逐語が無いまま(イ)を満たしたことにしており、段5 の根拠が弱い(P2・P10 型: 引用と主張の対応が崩れている)。
>
> 3. [直す/止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:17238` — `AlgoBuild` の「`docs.algorier.com` 等の独立した文書サイトは **DNS到達不可**・404で存在せず」という記述は、生ログ(`docs/DATA/probes/20260923_tools_8_run14.log:9270-9277`)の実際の出力「`curl: (56) CONNECT tunnel failed, response 502`」と食い違う。これはこの環境のプロキシ層での CONNECT 失敗であり、DNS解決の失敗を示す出力ではない(`api.algorier.com` だけが実際に 404 を返している)。P1(「取れない・無い・存在しない」は範囲と方法を明記して初めて言える)に照らし、この否定的主張は単一経路(このプロキシ経由)の失敗を「存在しない」に一般化している。`登録が要る` という状態自体は `dashboard.algorier.com/login` の到達(独立の一次資料)で別途支えられるが、この特定の一文は訂正が要る。
>
> 4. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:19026`(問い4)— `Lean` の §4.0 表が 43 項目中 23 項目(53.5%)で「過半」をぎりぎり満たすという、調査班自身が出した懸念(13 回目の 51.2% の前例と同種)に対し、検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run14.md` の §4 判定は明示的に答えていない(候補の一覧・処置一覧の中で暗黙に `深掘り` を受け取っているだけ)。13 回目の同型の問い(監査75回目の指摘5)には「答える(受け取る)。設計票§2の条件(過半)は満たす」と明記した先例があるが、今回の検収はその先例を明示的に適用したとは書いていない。
>
> 5. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:19027`(問い5)— OpenBB・GE の 43 項目中各13項目が `未確認`(導入可否・pip check・最小実行など)のまま残ることについて、次回に実測を割り当てるかの判断が検収に書かれていない(保留のまま)。
>
> ## B(cat8_classify.py / cat8_ledger.py の組み込み)
>
> 6. [止める] `scripts/cat8_classify.py` は、決まり(id・正規表現・理由)の広さ・具体性を一切検証しない(3列がすべて非空でありコンパイル可能な正規表現であることしか見ない)。実測: 合成の候補 8-999・E4 検索(`replay_market_data`という実際の実装行を含む4行)に対し、決まりを1本だけ `r1 \t .* \t general mention of the word, not the tool feature (catch-all, not read per line)` として `cat8_classify.py` を実行すると、真の陽性(実装の説明文)を含む4行すべてが `matched=4 unmatched=0` で通過した(終了コード0)。`scripts/cat8_ledger.py` の `classified_searches()`(:472-488)は `unmatched` 件数だけを見て、0件なら無条件にその検索手を `classified` 集合へ入れ、以後 `check-elements` はその手の当たり行を `### 当たりの判定` の表に載せる要求を完全に免除する(:707-711 の `if step_start(f, n) not in classified:` の分岐)。つまり「正直に作業した調査班」が悪意なく `.*` のような広すぎる決まりを1本書けば、真の陽性を含む大量の当たりが機械検査を一切通らずに `なし` の根拠として確定してしまう経路が残っている。歯止めは「決まりの中身は生ログに全部残るので監査で決まりそのものを読める」(検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run14.md:16`)という、監査役の目視・抜き取りにのみ依存しており、機械では止まらない(P10・A-11型: 目視・要約は原典再照合まで正しいと言えない、の裏返し)。OpenClaw(当たりが要素ごとに最大 33,906 件)のような大規模ケースでまさにこの道具を使う計画(`:19023` 問い1)であり、規模が大きいほど広い決まり1本で済ませたくなる誘因も、監査役が全件を読み切れない可能性も大きくなる。
>
> 7. [直す] 上の6に対する機械的な歯止め案が無い。例えば「1つの決まりが当たり行の一定割合(例: 5割・9割)以上を占めたら `check-elements` を止める」「理由の文中に、当たった行から実際に取った語を引用させ、その語が生ログの当該行に実在するかを機械で照合する」のような追加の検査を入れない限り、6 の経路は残ったままになる。少なくとも、この限界(6)を `docs/DATA/surveys/CAT8_DESIGN.md` か `cat8_classify.py` のdocstringに明記していない(今は「なぜ作ったか」だけが書かれ、「何を防げていないか」が書かれていない)。
>
> ## 補足(確認して問題が無かった点)
>
> - `cat8_classify.py` が unmatched 行を `### 当たりの判定(行ごと)` の表に理由つきで持たない場合に `check-elements` が止まること自体は、コード上正しく実装されている(未達の決まりを黙って落とす経路は無い)。
> - K13(18件、既存の理由文複写型)は「誤検出として閉じずにそのまま残す」と明記されており、A-19(調べずに誤警報と決めつける)には当たらない。
> - Great Expectations の `great-expectations/great_expectations` → `fivetran/great_expectations` の配布元一致は、ungh.cc の `id` が同一(組織移管であり別リポジトリではない)であることを生ログで確認できた。
> - OpenClaw(8-026)を取り直す前に発見出典記事と README の対象一致を確認した手順(起動文§2の1の要求どおり)は実行されている。

## 6. 処置(リード)

1. **直した**。報告の同じ節の知見 #29 が「外から持ち込める対象: 記載なし」と書いているので、設計票 §4.1 の段 4(「対象のすべて」を外から持ち込める)を満たさない。台帳 8-018 の 段_E1a を 5 → **3** にした。(ア)(イ)は §4.2 の軸に残っている。
2. **直した**。引用の文は「review and rendering into Data Docs」だけで、次の実行と比べることを言っていない。(イ)の逐語が無いので、台帳 8-017 の 段_E1b を 5 → **4** にした(段 4 の根拠は `add_dataframe_asset` の逐語、(ア)は min/max_value)。
3. **直した(報告は届いたままにし、ここで訂正する)**。生ログの出力は `CONNECT tunnel failed, response 502` で、この環境のプロキシでの失敗であり、DNS の失敗ではない。`docs.algorier.com`・`help.algorier.com`・`support.algorier.com` は**未確認(この環境のプロキシ経由で 1 回だけ試した)**で、「存在しない」とは言えない。`api.algorier.com` は 404。`登録が要る` は `dashboard.algorier.com/login` の到達(生ログ :9282-)で支えるので、状態は変えない。
4. **答える(受け取る)**。13 回目(監査 75 回目の指摘 5)と同じ扱いにする。設計票 §2 の条件は「過半」で、23/43 はこれを満たす。
5. **答える**。8-017・8-020 は 15 回目の割り当てに入れる。まだ `未確認` の §4.0 の項目のうち、この環境で打てるもの(導入可否・`pip check`・最小実行)は 15 回目に測らせる。
6・7. **直した(`scripts/cat8_classify.py`)**。
   - 空文字に当たる決まり(`.*`・`x?`)は止める。
   - 検索の型そのものが当たった字(検索した語)だけに当たる決まり(`golden`・`\w`・`e`)も止める。
   - 決まりが行を取るのは、決まりの当たりが検索した語の当たりと重なるときだけにした。同じ行の別の語に当たる決まり(`the`)では行を取らず、その行は `unmatched` に出る。
   - 決まりごとに、当たりに占める割合と最初の 3 行を出す。
   - 実測(リード、合成の生ログ 3 行): `.*`・`golden`・`\w`・`e` は終了コード 1。`the` は 0 行を取り、3 行とも `unmatched`。`(dark|pale|light)?goldenrod` は `darkgoldenrod` の 1 行だけを取った。`golden_file\(` は `golden_file(a)` の 1 行だけを取った。
   - **防げていないもの**(docstring に書いた): 検索した語に、ありふれた前後の字をわざと付けた決まり(例 `\bthe golden`)は通る。これは、出力された決まりと例の行を読む者しか捕まえられない。
   - 「1 つの決まりが当たりの一定割合を超えたら止める」案は入れなかった。理由: 色名のように、正しい決まりが当たりの大半を取ることがある。割合は出力に出すので、監査で見られる。
   - 2〜14 回目の結果は変わらない(`cat8_classify.py` を使った回はまだ無い)。
