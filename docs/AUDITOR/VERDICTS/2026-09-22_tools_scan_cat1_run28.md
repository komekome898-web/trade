# 区分 1 の 28 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 28 回目の実行」節(384 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run28.log`
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲・受け入れ検査(機械)

`git diff --numstat` = **384 行追加 / 0 行削除**。**過去の回の節は 1 行も触っていない。**
当方が全 27 本の生ログを渡して打った結果は 13 本すべて 0 件。**判定: 合。**

## 2. **最も重い指摘 — 完了の形の帰属を、当方が偽っていた**

調査班の指摘: 「`OWNER_LOG` L-398 でオーナーが発した語は『**区分1のテーマは何ですか？**』と『**案 A' で**』の 2 つで、
『**6 要素それぞれについて、一次資料に到達した候補が尽きること**』は**リードが書いた案 A' の本文**です。」

**当方が `OWNER_LOG` L-398 の実物を読んで確かめた。指摘は当たっている。**
`OWNER_LOG` 自体は正しく書かれている(オーナーの列に 2 つの逐語、完了の文はリードの対応の列)。
**誤っていたのは当方の検収と、当方がオーナーへ出した報告である。**当方は 26・27・28 回目の起動の指定と複数の検収で、
この文を「**オーナー逐語(L-398)**」として引いた。**オーナーが発した語ではない。**

これは §0.2 A-1(オーナーの原文を自分の語彙に置き換えて実行する)であり、
**L-162 でオーナーが名指しした型そのものである**。オーナー逐語:

> **私の指示でないものを私の指示とするのは嘘なのでやめる。経緯となった言葉を書く。**

**正しい書き方**:
> **区分 1 の完了 = 「§2 の 6 要素それぞれについて、一次資料に到達した候補が尽きること」(リードが書いた案 A' の本文)。**
> **オーナーはこの案を選んだ**(逐語「**案 A' で**」L-398)。**オーナーが発した語は「区分1のテーマは何ですか？」と「案 A' で」の 2 つ。**

**中身は変わらない**(オーナーが選んだ定義なので有効である)。**変わるのは、誰の語かということだけである。それが §0.2 の表が 2 つに分かれている理由そのものである。**
**次のオーナー報告で訂正する。**調査班が過去の節を書き換えず、要約でだけ書き分けたのは正しい扱い。

## 3. **受け入れ検査の射程 — 当方が毎回出していた「13 本すべて 0 件」は、報告書の半分についての数字だった**

調査班の指摘(K2 が節に当たる / `scope()` が表の無い節を外す)を受けて、当方が測った:

```
対象の節: 13  対象外の節: 15     ← 28 節中 15 節が、表を前提にした検査の対象外
```

外れているのは 1・2・14・15・18〜28 回目の節(深掘りが 0 件で §4.0 の表を持たないため)。
**当方は毎回「13 本すべて 0 件」とオーナーに書いてきたが、それは 13 節についての数字である。**

**そこで、外れている 15 節に K1・K2 を当てて測った**(表を前提にしない 2 本だけ当てられる):

| 検査 | 当たり | 中身 |
|---|---|---|
| K1 太字 | 14 件 | **10 件は符号の中の `*`**(`(trade_val / total_trade_val) ** 2` など Python の冪乗)。残り 4 件も同じ原因の「`**` の数が奇数」 |
| K2 括弧 | 14 件 | **逐語の中の括弧**(閉じるには逐語を書き換えるしかない) |

**28 件とも、逐語と符号を多く含む節に当てたことによる誤検出である。**
**つまり射程は狭いが、狭いことで欠陥を取り逃がした形跡は、K1・K2 については無い。**K4・K5・K9・K13 は表と突き合わせる検査なので、表の無い節には当てようがない(元の理由が正しい)。

→ **検査は直さない。**直すと誤検出が入る。**代わりに、射程を監査役へ渡す要約に明記する**(「13 本 0 件」は 28 節中 13 節についての数字であること)。
**`--all` で落ちる件も、監査の直前に道具を触らないという理由で、このまま監査役に渡して見てもらう。**

## 4. 調査班が挙げた 5 件 — リードの回答

**(1) 帰属 → §2 のとおり。当方の誤り。**

**(2)「呼ぶが既定では無効」は 5 件ではなく 7 件 → 調査班の列挙が正しい。**
2・4・33・21・91・92・57 の 7 件。26 回目の知見 20 が 33 番を数え落としていた。**27 回目が 57 番を「7 件目」と書いたほうが列挙と合う。**

**(3) `engine.py` の行の参照 → これは調査班の誤りである。2 つとも外れている。**
- **`CLAUDE.md` に `engine.py` の語は 0 件**(当方が `grep -c` で数えた)。その参照は **`CLAUDE.md` ではなく委任文**にある(`20260922_tools_survey_prompt.md:114` の §8 A)。
- **行番号 47・51 は正しい。**当方が原典を読んだところ、47 行が「`...reached in the queue, nor`」、51 行が「`unknown. Real fills also depend on queue position; this model grants the`」で、
  **委任文が引いた 2 つの文をちょうど担っている行である。**楽観の宣言が始まるのは 45 行だが、委任文は宣言の開始行ではなく引用した文の行を指している。
- **したがって直すものは無い。**調べずに「間違っている」と言わず、**出典を当方が引き当てられる形で渡してきた**ので、ここで片付いた。

**(4) `maker_fill_ref.py` の「呼んでいない」の射程 → 調査班の言い直しが正しい。**
「一度も呼ばれていない」ではなく「**研究の実行経路が呼んでいない**」(`src/` と `scripts/` で 0 件、試験と既知解の手書き導出では呼ばれている)。
**CLAUDE.md §5.2「棄却には射程を書く」のとおり。当方も以後この言い方にする。**

**(5)「当方より高い段に既定で居るのは 25 件」→ 導いた数であることを明記すること。**
36 − 段 0 の 2 − 段 1 の 7 − 尺度の外 2 = 25。**数え上げではなく引き算で出した数なので、式を添えて `導出` と印を付ける。**
印の無い数字は、後で誰かが数え直したときに食い違いの原因が追えない(§0.2 O-6)。

## 5. 安全側

導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。
**逐語 55 本を `grep -cF` で突き合わせ、当たり 0 件の 1 本も原典を取り直して引用の誤りでないことまで確かめている。**
`ungh.cc` の `code=000` を打ち直し、404 の欄に入れていない。**この回の引用の誤りは 0 件。**

## 6. 判定

**受け取る。**検査 13 本が 0 件、書き換えは追記のみ。
**調査班が当方の偽った帰属を見つけた(§2)。これは今回の一連で最も重い指摘である。**
**受け入れ検査の射程も当方が測り直し、「13 本 0 件」が報告書の半分についての数字だったことが分かった(§3)。**
**(3) は調査班の側が外れていたので、当方が原典で片付けた。差し戻す点は無い。**

## 7. 次の一手 — **区分 1 の全体を監査役にかける**

オーナー指示(L-396)の逐語: 「**区分1を完了まで進めて、その後区分1の全体受け取り監査をしてください**」。
**28 回目で表と定義が揃ったので、ここで監査役にかける。**

監査役に渡すときに**必ず添えるもの**(当方が隠すと監査にならない):
1. **完了の形の帰属**(§2)。**オーナーが発した語は 2 つだけで、完了の文はリードが書いたものである。**
2. **受け入れ検査の射程**(§3)。**「13 本すべて 0 件」は 28 節中 13 節についての数字である。**
3. **`--all` を渡すと道具が落ちること**(調査班が閉じずに渡してきたもの。監査の直前なので直していない)。
4. **別立ての 4 件**(14・22・11 の 5 要素・49)と、それぞれ「尽きた」に数えない理由。
5. **当方が 28 回のあいだに出した誤りの一覧**(19・21 の指定漏れ / 25 の候補 33 / 27 の機関の数と高頻度の段と逐語の読点 / 28 の帰属)。
