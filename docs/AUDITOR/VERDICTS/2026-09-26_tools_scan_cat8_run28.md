# 検収: 道具サーベイ 区分 8 の 28 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 28 回目の実行(2026-09-26)`(42046 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run28.log`(347,232 バイト)。届いたままのコミット d74df57。起動文の印 `20260923_tools_survey_cat8_run28_prompt.md@9b8225871207`。

## 0. 費用と時間・中断

1 度目の起動はコンテナの再起動で 10 手(公式のダウンロード頁を読んだところ)で止まった。その生ログは scratchpad の `cat8/run28_aborted/` に移し、作業のフォルダー `venvs/8-032/`(頁 3 つだけ)は消して、2 度目を空の生ログから始めた。2 度目: 調査班 464,586 トークン・2,207,284 ms(36.8 分)・道具 229 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 28` 0 件 / `check "" run28.log` 0 件 / 過去の節から消えた行 0。scratchpad の外: `/root/.m2` 無し、`/root/.gradle` は 2026-03-31 からのもの、`/root/.cache/pip` 728M のまま。Kafka・Java のプロセスは残っていない。

## 2. 台帳

`import --round 28`(変わった所 7): 8-032 は `未着手` → `浅い`、E1a 印/3・E2 印/3・E3a なし・E3b なし・E4 印/3・E5 印/2、E1b・E6 は `未判別`。**監査 120 回目の結果を見てから** `set 8-032 E2=未判別 段_E2=未判別 E4=未判別 段_E4=未判別`。`check` 0 件。

## 3. 中身の監査(120 回目、owner-auditor)の逐語

> # 監査 120 回目 — 道具サーベイ区分8・28回目(Apache Kafka)
>
> ## 決定的な検査(該当箇所ごと)で見つかったもの
>
> 1. [止める] `docs/DATA/probes/20260923_tools_8_run28.log:1907-1913`(動作環境確認、15:29:06)/`:1914-1935`(難読化確認、15:29:12) vs `:1742-1789`(最小実行、15:26:10〜15:27:35) — 起動文§2.5は「**最小実行の前に、委任文§6-1の導入前の検査を全部やる**」「**§6-1の結論の行を生ログに出してから、導入・実行の手を打つ**」と明記するが、実測では「動作環境確認」「難読化確認」という§6-1の必須項目(build.gradleのJavaバージョン要件・jarの難読化有無)が最小実行の**あと**に実行されている。さらに生ログ全体を`結論`で検索しても1件もヒットしない(実測)。これは起動文§0項目5がまさに名指しで「21・24・26回目の調査班は§6-1の導入前の検査の一部を導入・実行のあとに行った…この回は同じことをしない」と求めた誤りの再発ではないか。SCAN報告42080行(知見7)はこれらの手を§6-1の一部として時系列を明かさずに引用しているが、実行順の違反を報告本文のどこにも書いていない(隠れた形の未開示ではないか)(P3、起動文§2.5)。
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42082`(知見9・E2印/段3、および42391行の要素と段の表) — 述語(`docs/DATA/surveys/CAT8_DESIGN.md:54`)の対象は「**時系列・約定・板・足・参照データ**」。根拠に引く冪等プロデューサの重複検出とブローカーのメッセージ検証失敗率(CRC・マジックバイト・シーケンス番号)は、知見9自身が「**いずれもKafka自身のワイヤフォーマットに閉じた検査**」と書いており、これが「時系列・約定・板・足・参照データ」に当たることを言う逐語は無い。起動文§0項目5が「22・24・26回目に、述語の対象(**E2の列挙のデータ**)に届かない根拠で『印』を付けた」と名指しした誤りと同型ではないか。この回は`判断に迷った点と問い`にこの論点を出していない(P3)。
>
> 3. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42085`(知見12・E4印/段3/実測、42394行) — 述語(`CAT8_DESIGN.md:57`)は「**記録した市場データを時刻順に再生して**」。引用逐語(`core-concepts.md`「an ordered, replayable...sequence of immutable **data** records」、`app-reset-tool.md`「reprocess its **data** from scratch」)はいずれも汎用の「data」であり「市場データ」ではない。起動文§0項目5が「E4の記録した**市場データ**に届かない根拠で『印』を付けた」と名指しした誤りと同型ではないか(P3)。
>
> 4. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42750`(判断に迷った点1、E4の段) — 「対象のすべてを外から持ち込める(段4相当)と考えたが、26回目のDebeziumのE4(段3)との**整合を優先**し…保守的に段3のまま残した」とある。`CAT8_DESIGN.md`(89行目)「**段は道具の側の記述だけで決める**(当方が何を持っているかで決めない)」に照らすと、他候補との整合という道具の記述ではない理由で段を確定させているのではないか。さらに起動文§2.2「**値・段を決めずにおく理由でない問いは『それ以外の問い』**」に照らし、この問いは段そのものを決めかねる問いに見えるが`[それ以外の問い]`に分類され、段は`未判別`ではなく`3`のまま報告されている。この分類は妥当か。
>
> 5. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42751`(判断に迷った点2、E1a) — 「廃止予定(deprecated)の機能を印の根拠にしてよいか」という、印そのものの成否に関わりうる問いが`[それ以外の問い]`に分類され、42081行・42389行では`印・段3`が確定値として報告されている。項目4と同じ理由で、この分類は妥当か(起動文§2.2)。
>
> 6. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42476`〜`42588`付近(`Apache Kafka` E3a当たりの判定、甲の表・113ファイル) — 大多数の行で「ソフトウェアのメモリ/スレッド/接続/内部型の漏洩(leak)の意味で、その時点で知り得ない情報が入らないようにする機能(purged交差検証・embargo等)ではない」という**同一文**が繰り返されている(実測で数十行確認)。起動文§2.4「**行の表・ファイルの表には、同じ文を繰り返さない…同じ文を何行・何ファイルにも当てた『なし』は受け取られない。当たりが多くて1つずつ書けないなら、§1の『500行』の規則で『未判別』にする**」に反していないか。E3bの表(`42589`行〜)についても同じ確認が要るのではないか(P5、起動文§2.4)。
>
> ## 判断が要る問い
>
> 7. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42798`(受け入れ検査K13、「この回の新規追加は0件」) — K13の例(42799行〜)はいずれも表の「根拠」列(生ログ参照URLの複写)を対象にしており、上記6で指摘した「理由」列の同一文の繰り返しとは列が異なるように見える。K13が0件であることを、起動文§2.4の遵守の証拠として扱ってよいか(P8)。
>
> 8. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42086`(E5印/段2、`--random-seed`)と`:42088`(知見15、E6から除外した`VerifiableConsumer`/`VerifiableProducer`) — 両者はいずれもKafka自身の動作確認・計測に使う付属ツールである。L-516「候補が自分自身の版・ランタイムを試験する仕組みは…印に数えない」に照らし、`kafka-producer-perf-test.sh`だけをE5に印を付け、`VerifiableConsumer`/`Producer`を「Kafka自身のシステムテスト向け」としてE6から除外する境界線が本文に明記されているか。
>
> 9. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run28_prompt.md:20`(発見経緯「10回目の辿る一覧の記事の中のApache Kafka」)がSCAN報告の候補の一覧(`42125`行)・知見表に引き継がれていない。今後の索引化・資源化で発見経緯を追跡する必要はないか。
>
> **止める指摘の有無**: あり(1〜6の6件)。

## 4. 処置(リード、監査 120 回目)

1. **止める → 記録し、実測の値の扱いを変えた**。生ログで確かめた: 「動作環境確認」(15:29:06)と「難読化確認」(15:29:12)は最小実行(15:26:10〜15:27:35)のあと。§6-1 の結論の行は無い。難読化の確認の出力(1914〜1935 行)はクラスの名前が読める形で、止める所見は無い。**§6-1 の一部が実行のあとになったのは 21・24・26・28 回目の 4 回目**で、起動文の文(名指しを含む)では止まらない。そこで、この回から**実行のあとに §6-1 の手があった回の実測の値は、「§6-1 を済ませる前の導入による値」と記録する**(報告の節の「リードの追記」)。次の新しい候補の回からは、起動文に「実行のあとに §6-1 の手が 1 つでもあれば、その回の実測の値はリードが受け取らない(文書の値だけにする)」と書き、結果に効く形にする。
2. **止める → E2 は `未判別`**。知見 9 自身が「Kafka 自身のワイヤフォーマットに閉じた検査」と書くとおり、述語の列挙のデータを言う逐語が無い(24 回目 Prefect・26 回目 Debezium と同じ当て方)。
3. **止める → E4 は `未判別`**。引いた逐語は汎用の「data」で、「記録した市場データ」を言う逐語が無い(26 回目 Debezium・16 回目 OpenClaw と同じ当て方)。**区分全体に効く問いとして記録する**: 流すデータを利用者が決める汎用の道具(Kafka・Debezium・Prefect など)で、述語の「市場データ」「列挙のデータ」を逐語で求める当て方は、述語の逐語どおりだが、汎用の道具の機能を狭く数える側に働く(A-18 の向き)。区分 8 の完了の報告で、この当て方をオーナーに見せる(A-13)。値は `未判別` で、`なし` ではない。
4. **止める → 答える(E4 は `未判別` になったので段は無い)**。他の候補との整合で段を決めた点は、設計票 §4.1 の「段は道具の側の記述だけで決める」に反していた(調査班の誤り)。
5. **止める → 答える(E1a は `印`・段 3 のまま)**。廃止予定でも、この版の配布物に入っていて利用者がクラスタに掛けられる道具で、述語の「2 つ以上の実装(または実装と参照値)の出力を突き合わせて、差を出す機能」に当たる(同じものを出すはずのレプリカどうし)。問いの分類は `[値・段の問い]` が正しかったが、値は変えない。
6. **止める → 受け取る(リードが数えた)**。E3a の表の 113 行と E3b の表の 88 行は、全部の行がそのファイルの当たった行を逐語で引いている(`L<n>「…」` の形。`grep -c 'L[0-9]*「'` で 113・88 = 全行)。引いた行は行ごとに違い、そのあとの分類の文が繰り返しになっている。21 回目の検収 §4 の処置 3・24 回目の検収 §4 の処置 3 と同じ扱い。
7. **答える**。K13 は根拠の列を見る検査で、理由の列の繰り返しは見ていない。K13 の 0 件を §2.4 を守った証拠にはしない(処置 6 はリードが理由の列を読んで決めた)。
8. **答える**。`kafka-producer-perf-test.sh` は利用者が自分のクラスタの性能を測る道具で、`--random-seed` で入力を固定できる(E5)。`VerifiableConsumer`/`Producer` は、報告の知見 15 の逐語のとおり Kafka のシステム試験のための道具で、L-516 に当たる。境目は「利用者が自分の環境に掛ける道具か、Kafka の開発者が Kafka を試験する道具か」で、本文の知見 15 に書かれている。
9. **答える**。発見の経路は台帳の「発見の経路」「発見の出典」の列にある(8-032 の行)。報告の候補の一覧に写す必要は無い。

**8-032 の扱い**: `浅い`。`未判別` は E1b・E2・E4・E6。E2・E4 は案 B の記録が無い。E1b・E6 は「未読の個別頁」が残る(探し尽くしていない)。§4.0 に起動文が認めない理由の `未確認`(専用検索は未実施・未投入)が残る。**残りに数える。残り 7 のまま**。

## 5. これからの手

29 回目 = 8-032 Kafka の残り(E1b・E2・E4・E6 の `印` の探し尽くしと案 B の記録、§4.0 の残り。新しく入れない・動かさない)。そのあと Pandas・Spark・NumPy・SciPy・OpenClaw・MetaTrader。
