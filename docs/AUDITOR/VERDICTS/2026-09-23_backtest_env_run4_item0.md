# バックテスト環境 4 回目の起動(wf_97933da5-8af)項目 0: 場面集の監査 5 回で止まった記録とリードの処置(2026-09-23)

監査役の出力(逐語)と場面係の直しの記録は `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/battery/AUDIT.md`(`scripts/export_workflow_judges.py` で Workflow の記録から書き出したもの)。止まった理由(Workflow の返り値): 「場面集の監査で同じ未達の理由が 3 回続いた(L-407)」(能力の場面 v6・v7 が `hasattr`・`inspect.signature` で作りの形を試している)。

## リードが見つけたこと

- **3・4 回目の直しは場面係が作業をしなかった。**場面係は、起動した回のオーナーの発言「起動してますか？」を依頼とみなし、「直しは今回のスコープ外」と書いて返した(上の記録の「場面の直し:0#3」「場面の直し:0#4」)。リードは「この作業への問いなので食い違わない」と見立てて起動したが、外れた。同じ理由が 3 回続いたのは、この 2 回が直さなかったため。
- **予約した知らせの回に起動すると、委任先に依頼の枠が渡らない**(1 体の試験 `wf_e1ae193a-453`、返り値「(枠なし)」)。
- **場面係・資料係・批評家を Jev に通していなかった。**Jev の前段では 4 つの委任文(場面集の作成・直し・表・批評)とも「実装の段」= opus。オーナーが L-419「案1 3役ともJev (推奨)」を選んだ。
- **検討表の道具が、動かせた候補の行を「動かせない」側に数えていた**(2 回目の監査の [止める])。

## リードの処置

1. 起動・再開は予約した知らせ(send_later)の回で行う(OWNER_STATUS)。
2. 場面係・資料係(表)・批評家を opus にした(L-419)。要件の固定は Jev で「作業の段」= sonnet のまま。
3. 道具: 「動かせた候補: N 件(番号 名前, …)」を求め、その候補が検討表に載っていれば誤りにする(試験 11 件)。委任文の規則 9 も合わせた。
4. 能力の場面 v6・v7(作りの形を試す)、観点 4・5 の場面を動かせた 5 候補に通していない、ティック観点の「上位互換」の言い切り、の 3 つは場面係が直すもの。次の起動の場面集の監査と直しで扱う(場面係の作成の指示に、前の起動の記録としてこのファイルを渡す)。
5. 4 回目の場面集の監査 5 回は、次の起動の周の数に入れない。設計を変えて起動し直すたびに数え直してきた(1〜3 回目も同じ)。
