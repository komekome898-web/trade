# 監査記録: 探索段 5 の設計(`SIGNAL_EXPLORE5_DESIGN_2026-09-20.md`)

## 1 回目(2026-09-20、対象コミット 99709a0)

監査役の出力(逐語。[止める] 1 件):

> 1. [止める] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:74 と :200 — H3 の族の定義が矛盾している。§2(line 74)は「k 3 分位(k = 0 を含む。『k = 0』の数を列に)× m 3 分位 × h 3 = 27 行」と書き、k は 3 水準(0 を含む 3 分位、k=0 の件数は付随列)として積の式 3×3×3=27 を出している。ところが §7(line 200)は「3 分位は k > 0 のプリントで切り、k ≤ 0 は『k = 0 以下』の**群として別に数える**」と書いており、これだと k は「k>0 の 3 分位 + k≤0 の別群」= 4 水準になり、積は 4×3×3=36 になるはずである。「27 行」が正しい族の大きさなのか、k=0 以下が独立の行群を持つのかが本文から一意に決まらない。H3 は jev のランキングで S-1・S-3・S-4 いずれも 1 位(直接/代理)の主な量として位置付けられており、族の水準数の食い違いをこのまま委任すると、H3 の行数・セル数自体がぶれる(P2・P3、「族の大きさの記載(積の式)と実際の水準数が一致するか」)。
>
> 2. [直す] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:22-38(§0「これまでの結果×この設計の要素」表) — 探索段 4 の反証者レビュー(致命 6・直すべき 8)のうち、直すべき 1(g は r_end/den の決定関数で新しい情報を持たない)・直すべき 2(『分母 ≥ 1 bp』列は帯 1 以外で同値)・直すべき 5(gap 30/180 に掃きの構成比を添える約束が未達)の 3 件が、この §0 の表には行として現れていない。§8(line 219, 222)では扱われているが、§0 単体では 8 件中 3 件が抜けている。この設計文書のこの表が「探索段 4 の結果を漏れなく扱っているか」という観点では不完全(P3・P6)。
>
> 3. [直す] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:13 と :88 — 用語表の「その時点で分かる量」の定義(line 13)は「t₀ より後の情報は層の作り方に一切使わない」と明記する。一方 §4 の対照 (c') の手順(line 88)は、候補時刻 t の周りの区間 [t−T秒, t+900秒] に清算が無いことを確認して候補を選ぶ、すなわち **未来 900 秒を見る**。この未来参照は対照側の候補選定だけに限られ、実際の清算プリント側の層(m・k・d)には使われていないと読めるが、その例外であることが用語表の定義文自体には書かれていない。定義文だけを読むと「未来は一切使わない」との矛盾に見える(P2)。
>
> 4. [直す] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:84 と `config/jev_design_examples/signal5_covariates.yaml:8-11` — line 84 は候補変数 10 件のうち `next_print_gap_s` だけに「(未来の量)」と注記している。しかし yaml 側(covariates ファイル line 10)では `prints_per_day` の定義文にも "partly future within the day" と明記されており、この注記が設計文書の本文(line 84)には引き継がれていない。「`prints_per_day` は未来を含むと書いてあるか」という観点では、yaml にはあるが設計文書の地の文には無い。
>
> 5. [聞く] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:34 — H6 の行は「薄さ・レバレッジ・ノード距離は 1 周目の束の表の列」と 3 属性を挙げながら、「プリントに付け直せるもの(過去の窓だけで作れる bin_pct・dist_node_bp)を委任先に確かめさせ」と、確認対象を bin_pct・dist_node_bp の 2 つに絞っている。レバレッジ(implied_leverage)がここで理由なく外れているように読める。説明のない族の縮小でないか(P-13 / A-10 型)。
>
> 6. [聞く] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:186 — jev_design の S-3 向けランキング(line 116-126)では `forward_move_all_prints`(H1)は 4 位「無関係」。しかしリードの読み(line 186)は「主な量は H3(k×m)と H1(全プリント)、次に H2」として H1 を主要量に含めている。同じ行で `next_same_side_liquidation_rate` と `bundle_position_diagnostic` の「無関係」評価には個別の採否理由が書かれているのに対し、H1 が S-3 で上位に無いことについては(S-1 で「直接」だったという以外の)個別の理由が書かれていない。指示にある「判定に使う量が (i) の上位に無い…ときは、その理由が本文に書かれているか(無ければ [止める])」に照らして、理由の記載が薄い。
>
> 7. [直す] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:206-207(§7 多重性) — 「行(約403)とセルは単位を分けて引き継ぐ」と書きつつ、この段自身のセル数を数える・報告する計画がテスト一覧(line 207)に無い。探索段 4 の反証者レビュー(問い 12)は「行とセルの両方を数え直すべき」「引き継ぐなら独立に読める量を数え直すべき」と明記しており、この段はセル数の扱いを次段以降に先送りしたまま、行数のみを予告している。
>
> 8. [直す] `config/jev_design_examples/signal5_covariates.yaml:8-9` — `bin_pct`・`dist_node_bp` の定義文にある "preceding W hours" の `W` の具体的な値が、yaml にも `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md` 本文にも一度も定義されていない。「過去の窓だけで作れる」(§0 line 34)という主張の「窓」の長さが未定のまま委任先の確認に投げられている(P2、未定義の記号)。
>
> 9. [聞く] SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:71(H0(c)) — 探索段 4 の反証者致命 3 が最も強く出した診断(その日の束数と「その日 1 つあたり取れた対照の数」の相関 −0.796 = 静かな日への偏り)に相当する量が、この段の H0(c) の仕様(「取れなかった数と m の帯別の割合、取れた/取れなかった側の中央値」)には明示的に引き継がれていない。取れなかった割合は帯別に出るが、日の忙しさとの相関という形では出ないため、致命 3 の「静かな日への偏り」という論点がこの段で再現・確認されない可能性がある。

### リードの応答(1 件ずつ)

1. §7 のとおり k は 4 水準(k ≤ 0 の群 + k > 0 の 3 分位)。§2 の H3 を 36 行に直し、合計を約 412 行にした。
2. §0 に直すべき 1・2(比 g と分母の下限の列は置かない)と 5(gap が無いので該当なし、代わりに H2 に k の平均と中央値)の行を足した。
3. 用語表の定義に例外(対照 (c') の候補選定だけ未来 900 秒を見る = 対照側のみ)を書いた。
4. §3 の地の文に `prints_per_day` が日の中の未来を含み層には使わないことを書いた。
5. 理由なく外れていた。3 つとも(`implied_leverage` を含む)t₀ より前のデータで付け直せるかを委任先に確かめさせ、付けられたものは H6 に足すと書いた。
6. H1 を置く理由(層別しない基準の行 = H2・H3 の母集団そのもの、束の終端起点の −6.8 bp と同じ 456 日で並べる唯一の行、S-1 で「直接」)を §5 に書いた。
7. 委任先が `tables.md` の数値セル数を数えて `summary.json` に書き、反証者のセル数はリードが同じ数え方で数える、と §7 に足した。
8. W = 8 時間(1 周目の表 `gap60_w8` の窓)を用語表と yaml に書いた。
9. H0(c) に「その日のプリント数と 1 件あたり取れた対照の数の相関」(探索段 4 致命 3 の −0.796 に当たる量)と対照の掃き相当の中央値を足した。

## 2 回目(2026-09-20、対象コミット b19bce2 + L-258 の反映 2c891ee)

監査役の出力(逐語。[止める] 1 件):

> 1. [直す] `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:40` と `:76`、`:78` — §0:40(指摘2への対応)は「代わりに H2 の各行に k の平均と中央値を添える」と約束しているが、H2 自体の仕様(§2:76「T 2 × m 10 分位 × h 7: H1 と同じ列 = 140 行」)にはこの列が反映されていない(H1 の列定義(§2:75)にも k の統計は無い)。さらに H4(§2:78)は「H2 と同じ列」と定義しており、H2 に k の列が追加されるなら H4 にも伝播するはずだが、対照(c')の候補点には「このプリントの掃き k」が定義されない(k は実際の清算プリントの量、§用語表:15)。この列がどこで定義され、H4 にどう扱われるかが本文から一意に決まらない(P2・P3)。
>
> 2. [直す] `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:40` と `:219` — 同じ「H2 に k の統計を足す」という変更が、§0:40 では「k の平均と中央値」、§8:219(致命2の欄)では「k の平均」とだけ書かれ、中央値の有無が食い違っている。加えて §8:226(直すべき5の欄)はこの変更に触れず「該当なし」とだけ書いており、同一の設計要素が異なる指摘への対応として重複・不一致した形で書かれている(P2「一度出した用語の意味を変えて使い回す」に近い構造)。
>
> 3. [直す] `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:80` — H6 の行「側 2・このプリントの想定元本 3 分位・時刻帯 4(UTC 6 時間ごと)= 9 群」は、2×3×4=24 であり「9」と一致しない。本文中の他の全ての族(H2〜H5)は「×」で積を取っているのに対し、この「9」は 2+3+4=9(各属性を独立の周辺群として足し上げる)と解釈しないと合わない。この加算的な数え方は他のどの行にも使われておらず、説明もない。1 回目監査の指摘1(H3 の水準数の食い違い)と同型の「族の大きさの記載(積の式)と実際の水準数が一致するか」という問題がここに残っている(P3)。
>
> 4. [止める] `SIGNAL_EXPLORE5_DESIGN_2026-09-20.md:13`(用語表「その時点で分かる量」の定義)と `:12`・`:15`(起点 p₀・掃き k の定義)、および `:207`(未来の情報を使わない検査) — 用語表:13 は「起点 t₀ より前の約定…だけから作る量」「t₀ より後の情報は清算側の層の作り方に一切使わない。例外は対照(c')の候補時刻の選び方だけ」と、例外を対照側 1 つに限定している。しかし H3 の主軸である「このプリントの掃き k」(:15)は p₀(:12「`at_or_after(ts, 300_000)`」= t₀ 以後の約定)を使って作られており、定義上「t₀ より前」ではない。これは文書が明示した唯一の例外(対照の候補選定)の外にある、清算側の層(H3 の層そのもの)での未来情報の使用に見える。§7:207 の検査は「層の列を ts より後の約定・清算を消した入力で作り直しても同じ値になる」ことを測るとしているが、k は ts 以後の約定(p₀)が無ければ作れないため、この検査は文字通りには k に対して失敗するはずである。この段の中心的な設計原則(冒頭 line 4「その時点で分かる量だけで層別する」)と、族の主な量である H3 の実際の定義が矛盾していないか。

### リードの応答(1 件ずつ)

1. H2 の仕様に「その帯の k の平均と中央値」を足し、H4 は「k と次の清算の列は無し(対照には清算のプリントが無い)、代わりに c_t の平均と中央値」と書いた。委任文も同じ。
2. §8 の致命 2 を「平均と中央値」に、直すべき 5 を「同型の構成比は H2 の k の列で扱う」に直した。
3. H6 は属性ごとの周辺の群(足し算。探索段 4 の G6 と同じ数え方)であることを明記した。
4. 矛盾していた。起点 t₀ を「清算の時刻 `ts`」ではなく **p₀ の約定の時刻**(`ts` 以後の最初の約定 = 通常は清算の約定自身)に置き直し、「その時点で分かる量」を「t₀ 以前(p₀ 自身を含む)」に、検査を「t₀ より後を消した入力(p₀ は残す)」に直した。p₀ − `ts` の遅れは H0 に出す(探索段 3 の実測では中央値 0 ms、p90 170 ms)。
