# 監査記録: 道具サーベイ 区分 8 の 10 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md`(初版の指紋 `ad0962fb5bd4`)。9 回目の起動文(監査 45・46 回目を通した版)を元に、範囲を 8-016・8-005・辿る一覧・成熟度の枠組みの原典にし、9 回目の誤りの型への規則を足した。辿る一覧の指示は 2 回目の起動文 §2 の 4 の文をそのまま写した。

## この回に回さないもの

| 何 | 理由 |
|---|---|
| 8-010 E3b・8-014 E3a/E6 と E1b/E3b の段 | 述語の読みの問い。**2026-09-24 に決着(L-508・L-509、4 問とも数える)**: 8-010 E3b・8-014 E3b の段・E6 はリードが台帳を決めた。8-014 E3a の値・段と E1b の段は、問い以外の理由も残るので 10 回目の §2 の 6 で決める(監査 59 回目の指摘 3) |
| freqtrade の §4.0 の表の合わせ直し・8-015 の N の出所の書き直し | 資源化のとき |

## オーナーに聞いていた述語の読み(2026-09-24。L-508・L-509 で 4 問とも「数える」と決着)

1. 8-004: 重複した注文を「拒否する」機能を、E2 の「重複を検出する」に数えるか(8 回目にリードの読みで数えた)
2. 8-014: LLM に「先読みの偏りを見つけよ」と指示する仕組みを、E3a・E6 の「機能」に数えるか(監査 56 回目の指摘 1 による注: 台帳の 8-014 E6 を `未判別` にした理由は問い 3(同梱の `skfolio`・`scipy`)で、この問いではない。リードがこの問いに E6 を入れた理由は、記録からは確かめられない = 未確認)
3. 8-014: 候補が同梱・呼び出している別のライブラリ(`skfolio`・`scipy`)の機能を、候補の機能に数えるか
4. 8-010: 学習期間と評価期間を 1 回だけ分ける論文の作りを、E3b の「その時点で知り得ない情報が入らないようにする機能」に数えるか

## 監査 48 回目(対象 = bc8a144、起動文の指紋 `ad0962fb5bd4`)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件・[聞く] 2 件。確かめて不整合の無かった点: 9 回目の検収 §3・監査 47 回目の指摘 1・2・4・5 の処置、§2 への割り当て、2 桁の回の正規表現、読ませる文書の指紋):

> 1. [止める] `docs/DATA/tools_catalog_cat8.tsv` 12 行目(8-011)`段_E6` = `未判別` — Python で台帳の全行・全列(E1a〜E6 の値と段)を機械的に数え上げたところ、`未判別` が残るのは 8-005(全 16 セル)/ 8-010 E3b(値・段)/ 8-011 **段_E6 だけ**/ 8-014(E3a 値、E6 値・段、E1b・E3a・E3b の段)/ 8-016(E3a・E3b・E5 の値・段)の 5 行。このうち 8-005・8-016・辿る一覧・成熟度の枠組みは起動文 §2 の 1〜4 に、8-010 E3b と 8-014 の未判別は起動文 §1「この回に回さないもの」および検収記録の「この回に回さないもの」表・「オーナーに聞いている述語の読み」4 問に、それぞれ 1 回ずつ現れる。**8-011 段_E6 だけがどちらにも一度も現れない**(起動文 §1 の「ほかの行」という無名の受け皿でしか拾えない)。この未判別は監査 47 回目の指摘 1(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` 39・51 行、8-011 E6 段2 の「再確認した」根拠が別の手の生ログだった)を受けてリードが直したものだが、8-010/8-014 のように「述語の読みの問い(オーナー待ち)」ではなく、単に生ログを引き直せば決まる性質の未判別であるにもかかわらず、この回のどのタスクにも割り当てられていない(P3・P5 — 族の全水準の網羅性、数え上げの型)。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` 44 行(監査 47 回目の指摘 3、E1b 知見#8 `performance_metrics` の使用未確認)の扱い — 同 56 行「リードの処置」は指摘 2 と 3 をまとめて「2・3: 認める…台帳で 8-014 … E1b・E3b の段を `未判別` にした」としか書いておらず、指摘 3 固有の要求(`performance_metrics` が実際にどこかで計算・使用されているかを、同クラスの `validation_history`(知見#5)と同じ厳密さで確かめる)は、起動文のどこにも次に引き継ぐ具体的な作業として残っていない。8-014 は「述語の読みの問い」としてまとめて次回以降に回されており(起動文 §1・§2 の「オーナーに聞いている述語の読み」3)、再開時に「機能の帰属の問い」には答える設計だが、「使用の有無を確かめる」という別種の指摘が同じ棚に紛れて消えている。
>
> 3. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(§2 の 1、th2-net 178 件を `git clone --depth 1` で全件取得)— 実測: `https://repos.ecosyste.ms/.../owners/th2-net/repositories` を 3 頁とも取得し 178 件のメタデータの `size`(GitHub 報告値、KB)を合計すると約 323 MB。さらにサンプル 10 件(最大級 1 件 + 無作為抽出 9 件)を実際に `git clone --depth 1` したところ、合計約 421 MB(内訳: `th2-documentation` 45 MB、`viewer` **324 MB・ファイル数 25,999**(サブディレクトリ `webpack-starter` だけで 285 MB)、残り 8 件は 1〜42 MB)。GitHub の `size` メタデータは実際のクローン容量を大きく下回ることもある(`viewer` は報告 34 MB に対し実クローン 324 MB)。10/178 件のサンプルだけで既に数百 MB に達しており、178 件全件では合計が委任文 §10 で禁止された「本体の一括ダウンロード(数百MB以上)」(起動文 §5「委任文 §10 のすべて」で継承)を超える可能性が高いが、起動文にはこの禁止規則との整合(サイズの上限・事前のメタデータ確認による大型リポジトリの除外・分割取得など)についての記述が一切無い。「取れなければ理由を書いて進む」規則(§2 冒頭)は届かない・登録が要る場合のためのもので、容量超過そのものを止める条件にはなっていない。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` と `:60` の置き場所の不一致 — §2 の 1 は 178 件のクローン先を「scratchpad の下」とだけ指定するのに対し、§3(書き換えの範囲)は「外部の道具を入れる場所は…`cat8/venvs/` の下だけ」と限定している。読むだけでビルド・実行はしない git clone が「外部の道具」に当たるか(当たるなら `cat8/venvs/` の下に置くべきで §2 の 1 の指定と矛盾、当たらないなら §3 の一文がなぜ「道具」に限定されているかが書かれていない)が起動文からは判断できない。
>
> 5. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:52`(§2 の 3、辿る一覧)— 文面は 2 回目の起動文 §2 の 4(`docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:36`)をほぼそのまま写しており、「一覧: <一覧の名前> — 開いた URL — 生ログの行」という**生ログの行を文中に埋め込む**形式のまま残っている。しかし同じ起動文 §2 の一般規則(冒頭近く)は「生ログの行は、文の中に書かず、決まった欄にだけ書く(…監査 34〜39 回目で、文の中から行の番号を読み当てる検査は何度直しても読み落としが見つかったので、読み当てるのをやめた)」と明記し、「要素と段」表・知見表には 7 列目・専用欄を機械検査(`logcol_errors`・`quotes_in_log`)で強制している。ところが `scripts/cat8_ledger.py` の `check-elements`(458・466〜467・638〜639 行、実測で確認)は、辿る一覧の節については `- \`名前\`` で始まる行数 `n_trace` を数えるだけで、埋め込まれた「生ログの行」の書式・実在は一切検査しない。辿る一覧だけが、この回で唯一「文中引用」を許したまま新しい引用規律の外に置かれている。
>
> 6. [聞く] 辿る一覧で見つかった名前をこの回の候補の一覧に載せない扱い(起動文 §2 の 3「候補の一覧には載せない…リードが台帳に `未着手` で足し、次の回に回す」)について、`cat8_ledger.py` に辿る一覧の名前を台帳へ機械的に取り込む経路が無く(実測: `add`/`sync` のコードに `辿る一覧`・`trace` への参照なし。`check-elements` は件数 `n_trace` を報告するだけ)、台帳への追加は完全にリードの手作業に依存する。これは監査 11 回目の指摘 4(候補の一覧からの黙った脱落)と同型のリスクだが、今回も機械的な突合せ(この回の `n_trace` の件数と、次回検収での台帳への `未着手` 追加件数の一致を確認する手段)が用意されていない。

**リードの処置**:
1: 認める。§2 の 5 に 8-011 E6 の段を足した(`hdt/stats.py` の各関数を生ログに印字して決める)。
2: 認める。§2 の 6 に 8-014 の `performance_metrics` の使用の確かめを足した(値・段は決めない。オーナーの答えを待つ)。
3: 認める。§2 の 1 を、取得の量に上限を置く形に改めた: 1 件ずつ `git clone --depth 1 --filter=blob:limit=1m` で取り、全ファイルの一覧を `git ls-tree` で残し、検索し、**消してから次へ**。取った量の和を 1 件ごとに生ログに残し、300 MB を超えたら取得をやめる(委任文 §10 の「数百 MB 以上の一括ダウンロード」を超えない)。1 MB を超えて取れなかったファイルは、画像・バイナリの拡張子なら除外、それ以外は「読んでいない」に数える(M < N なら `なし` は書けない)。
4: 答える。取得物の置き場を `cat8/venvs/th2/` の下に揃えた(§3 の「外部の道具を入れる場所」と同じにした)。
5: 認める。辿る一覧の「一覧:」の行は、生ログの行を `docs/DATA/probes/20260923_tools_8_run10.log:N` の形で行の最後に 1 つだけ書くようにし、`check-elements` が 10 回目以降、その形があるか・その行が生ログにあるかを見るようにした。2〜9 回目の結果は変わらない(8 回目は届いた報告のまま 5 件、処理済み)。
6: 約束する。10 回目の検収で、辿る一覧から出た名前を 1 件ずつ台帳に `未着手` で足すか、足さない理由を書き、**節の名前の数(`n_trace`)と、足した数 + 足さない数の和が合うこと**を検収の記録に書く。
直した版(起動文の指紋 `7f7891408a83`)は監査 49 回目で確かめてから印を記録する。

## 監査 49 回目(対象 = 1d857fa、起動文の指紋 `7f7891408a83`)

監査役の出力の要点: 48 回目の指摘 1・2・4 の処置は実物で成り立つ(台帳の `未判別` 31 セルと起動文・記録が 1 回ずつ一致)/ 辿る一覧の「一覧:」の行の検査は、正しい行を通し、形の無い行・別の回・無い行を止める / 2〜9 回目は変わらない。指摘(逐語、[止める] 1 件・[直す] 1 件):

> 3. **[止める]** `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` — 300 MB 上限による `git clone --depth 1 --filter=blob:limit=1m` の手順を scratchpad で実際に打って検証したところ、**この手順は謳っている容量抑制を達成しない**。実測: `--filter=blob:limit=1m --no-checkout` で th2-net/viewer をクローンすると `.git` は 22M に収まり、`git ls-tree -r --name-only HEAD` は除外された 1MB超のファイル(`tsserver.js` 等)を含め 25,999 件を正しく列挙する(N への算入は設計どおり機能する)。しかし手順が指示する「検索し」を素直に実装する通常の `git checkout`(デフォルトの clone で自動実行される)を行うと、フィルタで除外されたはずの大きい blob が git のプロミサーリモート機構により **逐次オンデマンドで再取得され**、ディレクトリは 324M(9回目のサンプルで得た無フィルタのクローンと同じ大きさ)に膨れる。`git cat-file -s <除外blob>` のようにサイズだけ確認する操作でも同様にフルフェッチが起きることを確認した。手順の文面は「1 MB を超えて取れなかったファイル」の扱いを定義しているが、通常の checkout+grep のワークフローでは実際には「取れなかった」ファイルは発生しない(全部取れてしまう)ため、この条件分岐は事実上空になり、300 MB の累計上限は 178 件中の最初の大型リポジトリ(まさに監査48回目で指摘された `viewer`)だけで単独に到達しうる。つまり、監査48回目の指摘3が問題にした「178件全件では容量超過の可能性が高い」という懸念は、この修正後も本質的に解消されていない(フィルタは checkout を経ない場合にしか効かない)。この手順のまま10回目を実行すると、意図とは異なり数件(場合によっては1件)のリポジトリで打ち止めになるか、あるいは各リポジトリの「取った量」の記帳が「300MB上限を守った」という体裁だけを与え、実質的な抑制になっていない可能性がある。`--no-checkout` や `sparse-checkout`、あるいは 1MB 超のファイルを明示的に検索対象から外す(grep 前にファイルサイズを見て除外する)手順が指示に無い限り、この問題は10回目の実行中に再現する。
>
> 5. [概ね機能・軽微な穴あり] … **ただし**、正規表現 `r"^\s*[-*]?\s*一覧[::]"` の文字クラス `[::]` は半角コロンが重複しているだけで、全角コロン「一覧：」を含んでいない。半角コロンの代わりに全角コロンで書かれた「一覧：」行は `in_trace` の判定自体をすり抜け、参照の有無・妥当性を一切検査されずに通過することを scratchpad で実際に確認した(形の無い行のはずが検出されない)。日本語の文書中で全角コロンが使われる可能性は排除できない。severity は `直す`(現行のテンプレートは半角コロンで統一されているため即座に事故には繋がらないが、検査の抜け穴として残っている)。

(指摘 2・6 は「文面では確かめられない / 約束のみ」で、10 回目の検収で見る。)

**リードの処置**:
3: 認める(リードは `--filter` を付けただけで、展開のときに後から取りに行くことを確かめていなかった)。**取得の道具 `scripts/cat8_repo_fetch.sh` を作った**: `GIT_NO_LAZY_FETCH=1`(後から取りに行かない)で `git clone --depth 1 --filter=blob:limit=1m --no-checkout` し、`git rev-list --objects --missing=print` で取らなかった中身を拾い、`git ls-tree -r` の全ファイルを「取った / 取らなかった」に分けて件数と名前を出し、**取った分だけ**を `git checkout --pathspec-from-file` で展開する。実測(2026-09-24、最大の `th2-net/viewer`): 取った量 `downloaded_bytes: 22659953`(22.7 MB、`.git` は 26 MB)・`files_in_tree(N): 25999`・`blobs_not_downloaded(>1MB): 14`(`package-lock.json`・`webpack-starter/node_modules/@babel/parser/lib/index.js.map` ほか)。展開のあとも `.git` は 26 MB のまま(後から取りに行っていない)。起動文 §2 の 1 をこの道具を使う形に改め、普通の `git clone`・`git checkout` を使わないと書いた。取った量は `downloaded_bytes` の和で数える。
5: 認める。全角のコロン「：」も読むようにした(`一覧\s*[:：]`)。
直した版(起動文の指紋 `421daad8286f`)は監査 50 回目で確かめてから印を記録する。

## 監査 50 回目(対象 = 559c72c、起動文の指紋 `421daad8286f`)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件(処置の確認で、直す所は無い)):

> 監査 50 回目の結果。対象コミット 559c72c。scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit50/`、確認後に削除済み)で `scripts/cat8_repo_fetch.sh` を `python3 scripts/cat8_step.py ... -- sh scripts/cat8_repo_fetch.sh <owner/repo> <dest>` の形(起動文どおり)で実際に 3 件(`th2-net/viewer`・`th2-net/.github`・`th2-net/th2-common-py`)打った。
>
> 1. [止める] `scripts/cat8_repo_fetch.sh`(17行、`git ls-tree -r HEAD | awk 'NR==FNR{m[$1]=1;next} ... ' ../.missing -` の行) — **1 MB を超えるファイルが 1 つも無いリポジトリでは `files_in_tree(N)` が 0 になり、`git checkout` が一度も実行されない(取得したのに中身が 1 件も展開されない)ことを実機で確認した。** 実測: `th2-net/.github`(`downloaded_bytes: 90010`)と `th2-net/th2-common-py`(`downloaded_bytes: 99512`)の両方で `files_in_tree(N): 0`・`blobs_not_downloaded(>1MB): 0` と出力され、`find . -type f`(`.git` 除く)が実際に 0 件だった(手作業の `git ls-tree -r HEAD` はそれぞれ 113 件・複数件を正しく返す)。原因はこの環境の `awk`(`/usr/bin/awk` → `mawk 1.3.4`、`gawk` は未導入)の実装依存の挙動で、`NR==FNR{...; next}` パターンの 1 つ目の入力ファイル(`../.missing`)が **空**(= 1 MB 超のファイルが無い = `.missing` が 0 バイト)のとき、パイプで渡した 2 つ目の入力(`git ls-tree -r HEAD` の出力)が丸ごと読み飛ばされ、awk の出力が空になる。`/tmp` で最小再現(`awk 'NR==FNR{m[$1]=1;next}{print}' 空ファイル 非空ファイル` が 0 行を返す)も確認済み。`th2-net/viewer` だけがたまたま 1 MB 超のファイルを 14 件含んでいたため `.missing` が空でなく、この不具合を踏まなかった(監査 49 回目の確認がこの 1 件だけだったため見逃された)。178 件の大半は小さいリポジトリで 1 MB 超のファイルを含まない可能性が高く、その場合 **取得はできても検索対象が 0 件のまま、エラーも出さずに(`rc=0`)次のリポジトリへ進む**。P3(準備測定も測定である・族の全水準)・P10(目視でなく機械的検証)に相当。10 回目をこのまま起動すると、8-016 の `なし`/`印` の判定が「検索した結果何も無かった」ではなく「検索対象がそもそも 0 件だった」ことに基づく可能性が高い。
>
> 2. [止める] 同ファイル 15〜17 行(`downloaded_bytes` の定義)と `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(300 MB 上限の記述) — 検索の指摘1とは別に、**チェックアウトが実際に起きた `th2-net/viewer` 単体で、`downloaded_bytes`(`.git` の `du -sb`)が 22,659,953 バイト(22.7 MB)なのに対し、展開後の作業ツリーの実サイズは `du -sb .`(`.git` 込み)で約 150〜157 MB、`du -sh` では 241M と出た**(2 回の計測方法で数値が割れる点も含め実測を明記)。1 MB 未満のファイルが 25,985 件と非常に多く、個々は上限未満でも合計すると `.git` の圧縮後サイズよりも遥かに大きくなる。起動文 §2 の 1 が 300 MB の上限を `downloaded_bytes`(`.git` サイズ)の和で管理する設計になっているため、この管理方法では**実際に scratchpad に置かれる中身の総量を大きく過小評価する**(viewer 1 件だけで実ディスク消費が上限の半分〜8 割に達しうるのに、記帳上は 22.7 MB としか残らない)。監査 48・49 回目が問題にした「記帳した量と実際の消費量が食い違う」という型が、git のレイジーフェッチとは別の経路(圧縮率の差)で再演している。
>
> 3. [直す] `scripts/cat8_ledger.py:468`(全角コロン対応の正規表現) — 実際に `一覧: foo` / `一覧： foo` / `- 一覧: foo` / `一覧 : foo` の 4 通りで検査し、意図どおり全て拾うことを確認した。監査 49 回目の指摘 5 への処置はこの点で成り立つ。
>
> 4. 確認できたこと(指摘ではない): (a) `th2-net/viewer` の 1 件については、展開・`grep -rIl` のあとも `.git` は 26,238,585 バイトのまま増えなかった(監査 49 回目が問題にした「後から取りに行く」挙動は解消)。(b) `files_in_tree(N)` が正しく出た `viewer` では、値は `git ls-tree -r HEAD | wc -l`(25,999)と完全に一致し、`skipped:` の 14 件はシンボリックリンクも含めて集合として過不足なく一致した。(c) `.gitattributes` に `filter`/`lfs` の指定は無く、`.git/hooks/` は `.sample` のみでフックは有効化されておらず、`core.hooksPath` も未設定 — 実行した 3 件のいずれでもリポジトリの中身(フック・設定・ビルド)は実行されなかった。(d) `cat8_step.py` 経由の呼び方(`-- sh scripts/cat8_repo_fetch.sh <owner/repo> <dest>`)は 3 件とも機械的にそのまま打て、`rc=0` で記録された(が、指摘1のとおり `rc=0` は「正しく取れた」を意味しない)。
>
> **10 回目を起動してよい状態か**: 起動しない方がよい。指摘 1 は 178 件のうち大半に影響しうる機能不全(検索対象が黙って 0 件になる)で、`th2-net/viewer` 1 件だけの確認では検出できない型であり、CLAUDE.md §0.2 A-11(目視・ぱっと見での判定の禁止)・O-3(機構の挙動は実行したコマンドと出力で示す)の観点からも、この道具のまま回すと 8-016 の `なし`/`未判別` の判定が汚染される可能性が高い。直すべき点: (i) `awk` の当該行を空ファイルでも壊れない書き方(例: `mawk` の `next` 前提をやめる、または `gawk` を明示指定する、または awk を使わずに `join`/`python3` で置き換える)に直し、1 MB 超のファイルが 0 件のリポジトリでも再検証する。(ii) 300 MB 上限の算出方法(`downloaded_bytes` = `.git` サイズ)と実ディスク消費(展開後の作業ツリー)の乖離をどう扱うか(上限の対象を作業ツリー込みの実サイズに変えるか、乖離を許容する理由を明記するか)をオーナー/リードで決めてから起動文に反映する。

**リードの処置**:
1: 認める(リードは 1 MB を超えるファイルがある `viewer` 1 件でしか打っていなかった)。`scripts/cat8_repo_fetch.sh` の振り分けを `awk` から `python3` に替え、ファイルの一覧を NUL 区切りで読む(名前に空白・改行があっても割れない)。**件数が合わなければ(一覧が 0 件、または 取った + 取らなかった ≠ 一覧)道具は 0 でない終了コードで止まる**。展開のあとに `git ls-files` の件数が「取った」の件数と合わなければ、それでも止まる。打ち直した結果(2026-09-24、リード): `th2-net/.github` 一覧 113 件・取らなかった 0 件・展開 113 件 / `th2-net/th2-common-py` 127 件・0 件・127 件 / `th2-net/viewer` 25,999 件・14 件・25,985 件(取った量 27,791,910 バイト)。
2: 答える(リードの判断)。300 MB の上限は委任文 §6 の 6「本体の一括ダウンロード(数百 MB 以上)はしない」から引いた数で、この規則が見ているのは**通信で取る量**なので、上限は `downloaded_bytes` の和のままにする。展開したあとの大きさは、道具が `checked_out_bytes(without .git)` として 1 件ごとに出し、生ログに残る(`viewer` は 130,782,466 バイト)。**1 件ずつ消してから次へ進む**ので、置き場に同時にあるのはいつも 1 件だけで、178 件の展開の大きさは積み上がらない。起動文 §2 の 1 にこの 2 点を書いた。あわせて、起動文がこの規則を「委任文 §10」と引いていた誤り(数の入った文は §6 の 6 にある。§10 は禁止の並び)を直した。
3: 処置の確認として受け取る。
直した版(起動文の指紋 `1ebfbc2bb1d9`)は監査 51 回目で確かめてから印を記録する。

## 監査 51 回目(対象 = d8d1a0b、起動文の指紋 `1ebfbc2bb1d9`)

監査役の出力(逐語。[聞く] 2 件、[止める] 0 件):

> 監査 51 回目の結果。対象コミット d8d1a0b、起動文の指紋 `1ebfbc2bb1d9`(委任文の実測 sha256sum で確認)。
>
> 実測した内容: scratchpad(`.../scratchpad/audit51/`、確認後に `rm -rf` で削除済み)で `scripts/cat8_repo_fetch.sh` を起動文 §2 の 1 のとおり `python3 scripts/cat8_step.py --log ... -- sh scripts/cat8_repo_fetch.sh <owner/repo> <dest>` の形で 2 件打った。
>
> 1. `th2-net/.github`(1 MB 超のファイルが無いリポジトリ)→ `files_in_tree(N): 113`・`blobs_not_downloaded(>1MB): 0`・`checked_out_files: 113 (expected 113)`。展開後 `find -type f` も 113 件で一致。監査 50 回目の指摘 1(mawk の `NR==FNR` が空の `.missing` で 2 つ目の入力を丸ごと読み飛ばし、この型のリポジトリで `files_in_tree(N)=0`・展開 0 件になっていた不具合)は、python への置き換えで実際に解消していた。
> 2. `th2-net/viewer`(1 MB 超のファイルが 14 件あるリポジトリ)→ `files_in_tree(N): 25999`・`blobs_not_downloaded(>1MB): 14`・`checked_out_files: 25985 (expected 25985)`。`git ls-files` も 25985 件で一致(`find -type f` が 25937 件と少なく見えたのはシンボリックリンク 48 件を数えないためで、道具の不具合ではないことを `find -type l` で確認済み)。`downloaded_bytes: 27791910`(27.8 MB)、`checked_out_bytes(without .git): 130782466`(130.8 MB、約 4.7 倍)。起動文が書く「リードが 2026-09-24 に 3 件で打って確かめた」実測値(viewer: 取った量 27.8 MB・25,999 件・取らなかったもの 14 件・展開 25,985 件/ .github: 113/0/113)と完全に一致した。
>
> 以上より 50 回目の指摘 1 の処置は実物で成り立つ。
>
> ## 指摘
>
> 1. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` — 300 MB 上限を `downloaded_bytes`(通信で取った量)だけで管理し、`checked_out_bytes`(展開後の大きさ)は「1 件ごとに記録する」だけで、個々のリポジトリの展開後サイズそのものへの上限は無い。実測(viewer)では展開後が通信量の約 4.7 倍(27.8 MB → 130.8 MB)になっており、この比率がもし通信量の大きいリポジトリ(300 MB 上限にまだ余裕がある序盤の 1 件)で再現すれば、単発で GB 級の展開が scratchpad に一時的に発生しうる(1 件ずつ消すため累積はしないが、瞬間的なピークは記録も上限も無い)。委任文 §6 の 6 の文言(「ダウンロード」)を根拠に「通信量だけを管理すればよい」というリードの読みは字面としては成り立つが、監査 48〜50 回目が問題にしてきた懸念(取った量と実際に置かれる量の食い違い)の実質(ディスクを圧迫しうる量)には答えていない。この解釈(展開後サイズは記録のみで上限を設けない)でよいか、オーナー/リードで確認済みか(P16 — 棄却方向・安全側と逆の「対応した体裁だけで実質のリスクに答えていない」型に近い)。
>
> 2. [聞く] 同じ行 — 「委任文 §6 の 6 は『ダウンロード』の量の規則なので、通信で取った量で数える」という出所の引き直しは、実際に §6 の 6(「本体の一括ダウンロード(数百 MB 以上)はしない」)と §10(「本体の一括ダウンロード」、数値なし)の該当箇所と突き合わせて字面が一致することを確認した(委任文 96〜124 行、実測)。50 回目の指摘 2 が問題にした「委任文 §10 に上限の数値がある」という誤りは解消している。ただし §6 の 6 の趣旨(「時間と規模の上限」の節にある)が「通信量だけを指す」と読むか「置き場の規模全体を指す」と読むかは委任文自身が明記しておらず、リードの読みは一つの解釈でしかない(指摘 1 と同じ論点)。
>
> 以上の 2 点はいずれも「聞く」(判断が要る、実行して壊れることが確認できた欠陥ではない)。50 回目の指摘 1(空リポジトリ扱いの機能不全)・指摘 2 のうち「委任文 §10 に数がある」という誤引用は、いずれも実物・引用の両面で処置が成り立っていることを確認した。今回の直しで新たに生じた文書内の食い違い(§3 の置き場との不一致・古い回数値の残存など)は見つからなかった(th2/300 MB/downloaded_bytes/checked_out_bytes/一括ダウンロード の全出現箇所は §2 の 1 の 1 段落に閉じている)。
>
> **10 回目を起動してよい状態か**: 起動してよい(指摘 2 件はいずれも「聞く」であり、実行を止める実測上の欠陥ではない)。ただし指摘 1(展開後サイズの上限が無いこと)はオーナー/リードの確認を推奨する。

**リードの処置**:
1: 答える(規則の読みを主張するのをやめ、機械で塞いだ)。取得の道具に、展開する**前に**展開後の大きさを手元の中身(`git cat-file --batch-check` の大きさの和。後から取りに行かない)から計算する手を足し、`expected_checkout_bytes` として出す。500 MB(`CAT8_MAX_CHECKOUT_BYTES`)を超えるリポジトリは展開せずに終了コード 3 で止まる。実測(2026-09-24、リード): `th2-net/.github` 計算 212,001 = 展開後 212,001 バイト / `th2-net/viewer` 計算 130,782,466 = 展開後 130,782,466 バイト(計算と実物が一致)/ 上限を 1,000,000 にして viewer を打つと `rc=3`・展開 0 件。起動文 §2 の 1 に、止まったリポジトリは「取れなかった」に数え名前と大きさを書く、と足した。同時に置き場にあるのは 1 件だけなので、展開の山は 1 件あたり 500 MB を超えない。
2: 1 と同じ論点。通信量の上限(300 MB の和)と、展開の上限(1 件 500 MB)の両方を置いたので、どちらの読みでも規模は抑えられる。
直した版(起動文の指紋 `1a2e9ed11f63`)は監査 52 回目(道具の足した手だけを見る)で確かめてから印を記録する。


## 監査 52 回目(対象 = 8de5359、起動文の指紋 `1a2e9ed11f63`)

監査役の出力(逐語。[直す] 1 件・[聞く] 2 件、[止める] 0 件):

> 監査 52 回目。対象コミット 8de5359。実測(scratchpad `audit52/`、`rm -rf "${D:?}"` で削除済み): ローカルの合成リポジトリ(空白入りファイル名・改行入りファイル名・シンボリックリンク・1MB超ファイルを含む)を `file://` で用意し、`scripts/cat8_repo_fetch.sh` と同一ロジックのコピーで検証。加えて実物の GitHub(`th2-net/.github`・`th2-net/viewer`)にも `cat8_step.py` 経由で実際に打った。
>
> ## 実測結果
>
> - `th2-net/.github`: `expected_checkout_bytes: 212001` = 展開後 `checked_out_bytes(without .git): 212001`(一致、監査51と同じ)。
> - `th2-net/viewer`(`CAT8_MAX_CHECKOUT_BYTES=1000000`): `expected_checkout_bytes: 130782466 (limit 1000000)` → `rc=3`、`checkout would exceed the limit; nothing checked out`。ディレクトリには `.git` しか残らず、作業ツリーは 0 件で展開されなかった。
> - `GIT_NO_LAZY_FETCH=1` はスクリプト先頭で export され、`git cat-file --batch-check` の呼び出し(展開前)でも有効。実物の `.github` 実行で `.git` サイズは計算の前後で変化なし(後から取りに行っていない)。
> - シンボリックリンク: ローカル合成テストで、シンボリックリンクの blob サイズ(ターゲット文字列長)と `stat`/`du` が報告する実サイズは一致(20=20)。数え違いなし。
> - 空白入りファイル名(`file with spaces.txt`): `git cat-file --batch-check='%(objectsize)'`(`%(rest)` を含まない書式)では行全体が対象パスとして扱われ、正しく解決・正しい大きさで計算される(50000+1234=51234 が実物と一致)。数え違いなし。
>
> ## 見つけた問題
>
> **改行入りファイル名で計算値と実物が食い違う。** `.git/cat8_present` は NUL 区切りで書かれているが(この形式にした理由はまさに「名前に空白・改行があっても割れない」ため、スクリプトのコメントにもそう明記)、`expected_checkout_bytes` を計算する行は `tr '\0' '\n' < .git/cat8_present | sed 's/^/HEAD:/' | git cat-file --batch-check=...` としており、**NUL区切りをいったん改行区切りに戻している**。ファイル名に literal な改行バイトが含まれる場合(git のツリーオブジェクトでは NUL と `/` 以外の任意バイトが許される)、その1つの名前が複数行に分裂し、`git cat-file --batch-check` はどちらの行も解決できず `missing` を返す(実測: `HEAD:weird` と `HEAD:name.txt` の2行に分裂、いずれも `missing`)。この結果、その改行入りファイルの大きさは合計に一切算入されない。
>
> 実測(ローカル合成リポジトリ、777バイトの改行入りファイル名 + 1000バイトの通常ファイル):
> ```
> expected_checkout_bytes: 1000   ← 改行入りファイルの777バイトが欠落
> 実物 du -sb --exclude=.git: 1777
> ```
> 差分777バイトは、欠落した改行入りファイルの大きさと厳密に一致(実測で確認)。**つまりこの経路は、まさに検査対象の質問「計算値と展開後の実物が合うか」に対して、ファイル名に改行が含まれる場合は不一致になる。**しかも不一致の方向は**過小算出**(安全側=大きめに見積もるのではなく、危険側=小さく見積もる)ため、500 MB の上限判定を実質的に骨抜きにしうる(改行入りファイルの重みの分だけ実物が計算値を上回る)。
>
> th2-net 配下の実在リポジトリ(JS/TS/Python/Java中心)にファイル名改行が含まれる可能性は一般に低く、今回サンプルした3件では未確認。この回(10回目)を止めるべき実測上の根拠(既知の178件中に改行入りファイル名が実在する)は無い。
>
> ## 指摘
>
> 1. [直す] `scripts/cat8_repo_fetch.sh`(`tr '\0' '\n' < .git/cat8_present | sed 's/^/HEAD:/' | git cat-file --batch-check=...` の行)— ファイル名に改行が含まれる場合、`expected_checkout_bytes` がその分だけ過小算出され、`checked_out_bytes` と食い違うことを実機で確認した(上記777バイトの実測)。同じスクリプトが直前の行(`git checkout -q HEAD --pathspec-from-file=.git/cat8_present --pathspec-file-nul`)では NUL 区切りのまま渡してこの問題を正しく避けているのに、新設のサイズ計算だけが NUL→改行変換で退行している。過小算出は「500 MB 超は展開しない」という安全側の仕組みを結果的に弱める方向であり、CLAUDE.md §0.2 A-18(構造的に安全側と逆に振れることの禁止に相当する型 — ここでは安全策自体が漏れを持つ)に近い。178件中に改行入りファイル名を持つリポジトリが実在するかは未確認(P1 — 「無い」と断定していないので違反ではないが、実測されていない)。直すなら `git cat-file --batch-check` に `-z`(NUL区切り入出力)を使うか、NUL区切りのまま `--batch-check` に渡す形にすればよい(このリードへの示唆は監査役の役割を超えるため詳述しない)。
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(§2 の 1、新設文「止まったリポジトリは『取れなかった』に数え、名前と大きさを書く」)— 同じ段落の少し前にある既存規則「取れなかったリポジトリと読んでいないファイルがあれば…値は `未判別` のまま、**オーナーの PC から取る手順**…を書く」と、この新設の「取れなかった」が同一の扱い(オーナーのPCから取る手順を書く)を受けるのかが文面上わからない。500 MB 超で止まったケースは「取得元・経路」の問題ではなく「内容量」の問題なので、オーナーのPCから取り直しても解消しない性質の失敗であり、同じ remediation 文言(手順を書く)を適用させる意図なのか、それとも別扱い(単に大きさを記録するだけで済ませる)なのかが起動文から判断できない。
>
> 3. [聞く] 同じ行の配置 — 500 MB の単体展開上限の説明文が、300 MB の累積ダウンロード上限についての括弧書きの中に入れ子で挿入されており(「…和が300MBを超えたらそこで取得をやめる(…さらに道具は、展開する前に…500MBを超えるリポジトリは展開せずに…)」)、2つの異なる閾値・異なる管理対象(通信量の累積 vs 単体の展開後サイズ)が1つの括弧に同居している。誤読の実害は今回の実測では確認できなかったが、読み手(次回以降の下位モデル)がどちらの上限がどちらの数量に掛かるかを取り違えるリスクがあるかは判断が要る。
>
> ## 10 回目を起動してよい状態か
>
> 起動してよい(指摘1は既知の対象リポジトリで実害が確認された欠陥ではなく、改行入りファイル名という低確率の未確認前提に依存する過小算出であり、指摘2・3は「聞く」で実行を止める実測上の欠陥ではない)。

**リードの処置**:
1: 認める(リードは名前に改行のあるファイルで打っていなかった)。大きさの計算を、名前ではなく中身の番号(オブジェクト ID。空白も改行も含まない)の一覧から引く形に替えた。種類が blob の項目だけを数え、大きさが引けない項目があれば終了コード 1 で止まる。実測(2026-09-24、リード。scratchpad の合成リポジトリ: 名前に改行のある 777 バイト・1,000 バイト・名前に空白のある 3 バイト・シンボリックリンク): 計算 1,790 = 展開後 1,790 バイト / `th2-net/.github` 計算 212,001 = 展開後 212,001。
2: 答える。500 MB で止めたもの(終了コード 3)は「大きさで止めた」として名前と大きさを書き、値は `未判別`、オーナーの PC から取る手順は書かない(経路の問題ではないため。次の回の扱いはリードが決める)。それ以外の 0 でない終了コードで止まったものが「取れなかった」。起動文にそう書き分けた。
3: 認める。500 MB の上限を、300 MB の括弧の外の別の文に出し、「掛かる量が違う」と書いた。
直した版(起動文の指紋 `92fc008af262`)は監査 53 回目で確かめてから印を記録する。


## 監査 53 回目(対象 = 14a2c4a、起動文の指紋 `92fc008af262`)

監査役の出力(逐語。[直す] 1 件・[聞く] 3 件、[止める] 0 件):

> 監査 53 回目。対象コミット 14a2c4a。範囲: 監査 52 回目の 3 件(newline ファイル名の過小算出/exit3 と PC 取得手順の書き分け/300MB・500MB の括弧の入れ子)への処置だけ。
>
> ## 実測した内容
>
> scratchpad(`.../scratchpad/audit53/`、確認後 `rm -rf "${D:?}"` で削除済み)で `scripts/cat8_repo_fetch.sh` を、URL の行だけ `file://` に差し替えた写しで、以下の合成リポジトリに対して実行した(`git config uploadpack.allowfilter true` を立てて `--filter=blob:limit=1m` が実際に効く状態にして検証):
> 1. 空白入りファイル名・改行入りファイル名・シンボリックリンク・1MB超ファイル・**gitlink(mode 160000 のサブモジュールエントリ)** を同居させたリポジトリ → `expected_checkout_bytes: 57` = 展開後 `du -sb --exclude=.git .`: 57(完全一致)。gitlink は `meta.split()[1] == b"blob"` の判定で正しく `present_blobs`(大きさ計算対象)から除外され、`.git/cat8_present`(チェックアウト対象)には含まれるがサイズ寄与 0 で、`git checkout` 後も空ディレクトリのままサイズ不一致を起こさなかった。`checked_out_files` も一致。
> 2. 全ファイルが 1MB 超(present が空)のリポジトリ → `expected_checkout_bytes: 0`、`checked_out_files: 0 (expected 0)`、正しく空チェックアウトで完走(rc=0)。
> 3. 同一内容(同一 oid)を持つ 2 ファイル → 期待どおり 2 重に加算され一致。
> 4. `CAT8_MAX_CHECKOUT_BYTES=10` で強制的に超過させたテスト(上記1のリポジトリ、特殊ファイル名同居のまま)→ `expected_checkout_bytes: 57 (limit 10)`、`rc=3`、ディレクトリに `.git` 以外何も残らない(正しく止まる)。
> 5. 存在しない clone 元を指した場合 → `git clone` 失敗で `rc=128`(3 と衝突しない)。
> 6. グロブ特殊文字(`*` `?` `[` `]`)を含むファイル名 4 種 → 今回のテストでは一致(57=57 のケースとは別、48=48)。ただし git のパススペックは既定でリテラルでなくグロブ展開されるため、衝突するパターンが実在すれば理論上は不一致の余地がある(後述)。
>
> いずれも改行・空白・シンボリックリンク・gitlink サブモジュールについて、計算値と展開後の実物の不一致は**再現できなかった**。
>
> ## 指摘
>
> 1. [聞く] `scripts/cat8_repo_fetch.sh:60-64` — チェックアウト後の整合性検査は `n_out -eq n_present`(**件数**の一致)だけで、`expected_checkout_bytes` と `checked_out_bytes(without .git)` の**バイト総量そのものの機械的な突合せ**は無い(2 つの数値を並べて出力するだけで、道具自身は比較しない)。`git checkout --pathspec-from-file` は既定でグロブ展開する(`--literal-pathspecs` 無し)ため、ファイル名にグロブメタ文字(`*` `?` `[`)を含み、かつ他のツリーエントリともパターンが衝突するケースが理論上ありうる。今回のグロブ文字テスト(6 参照)では不一致を再現できず、th2-net の対象リポジトリ(JS/TS/Python/Java 中心)でこの型の衝突が実在するかは未確認(P1 — 「起きない」と断定していないが、機械的なバイト突合せは実装されていない)。500 MB 上限の安全性はこの想定に依存している。
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`「委任文 §6 の 6 は『ダウンロード』の量の規則なので、通信で取った量で数える」と、300 MB という具体的な数値の出所 — 実際に委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md:104` を確認したところ、§6 の 6 は「本体の一括ダウンロード(**数百 MB 以上**)はしない」としか書いておらず、「300」という具体的な数値そのものは書かれていない(§10、124 行も同様に数値なし)。「通信量で数える」という読みの出所は示されているが、「300」という境界値自体はリードの仮定であり、一次資料の逐語ではない(P2 — 数値の出所の分類。棄却方向・安全側の数値であれば A-12 に近い型にもなりうるが、この数値は許容の上限側に効くため逆方向の注意が要る)。「数百MB」の下限を 200 とすれば 300 はすでにその範域に入るという読みも成り立ち、この解釈がオーナー/リードで確認済みか。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` — 監査 52 回目の指摘 3(500MB の説明が 300MB の括弧の中に入れ子になっていた)への処置で、500MB の説明文は別文に分離されたことを確認した。ただし 300MB の文の括弧内には依然として「展開したあとの大きさ `checked_out_bytes` も1件ごとに残す」という一文が残っており、これは意味的には(展開後サイズを扱う)500MB の仕組みに近い記録項目である。300MB の説明の括弧の中に 500MB 側の記録項目が顔を出す形が残っており、読み手が「`checked_out_bytes` はどちらの上限の管理対象か」を誤読する余地がわずかに残る。
>
> 4. [聞く] `scripts/cat8_step.py:47-58` と `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:14,72-74` — `cat8_step.py` 自身が `--deadline` 指定時に**独自の理由で** `rc=3`(「期限切れ、手を打たなかった」)を返す実装になっていることを実装を読んで確認した。これは `scripts/cat8_repo_fetch.sh` の `rc=3`(「大きさで止めた」)とは全く別の意味だが、同じ数値である。今回の起動文はこの衝突を「`--deadline` は付けない」(§0 の 4)・「この回は予算で止めない」(§6)という**運用上の回避**でかわしているだけで、`rc=3`=「大きさで止めた」という起動文§2の1の説明自体は、この回避が守られて初めて成り立つ前提付きの記述である。次回以降、この道具の組み合わせ(`cat8_step.py -- sh cat8_repo_fetch.sh`)を `--deadline` 付きで再利用する回があれば、ログの `rc=3` だけでは「期限切れ」か「大きさで止めた」か区別できず(`note=` 欄の読み分けが必要)、この回はその作りへの言及・注記が無い。
>
> ## 10 回目を起動してよい状態か
>
> 起動してよい(指摘は 4 件すべて「聞く」/「直す」で、実行を止める実測上の欠陥は今回も再現できなかった。改行・空白・シンボリックリンク・gitlink サブモジュールを含む組み合わせで計算値と展開後の実物は一致し、上限超過時は正しく `rc=3` で空チェックアウトのまま止まった)。

**リードの処置**:
1: 認める。展開を `git --literal-pathspecs checkout` にした(名前の `*` `?` `[` を型として読まない)。展開後の実物の大きさが計算と合わなければ終了コード 1 で止まるようにした(大きさも道具が自分で突き合わせる)。実測(2026-09-24、リード): 合成リポジトリ(名前に改行・`*.txt`・`[ab].md` と `a.md`・シンボリックリンク)計算 791 = 実物 791 / `th2-net/.github` 212,001 = 212,001 / `th2-net/viewer` 130,782,466 = 130,782,466。
2: 答える。300 も 500 もリードが決めた数で、委任文は「数百 MB」とだけ書く。起動文にそう書いた。
3: 認める。`checked_out_bytes` の記述を 300 MB の括弧から出し、500 MB の文の側に移した。
4: 認める。大きさで止めたときの終了コードを 3 から 4 に替えた(3 は `cat8_step.py` の期限切れ)。上限を 10 バイトにして打つと終了コード 4。起動文の書き分けも 4 に揃えた。
直した版(起動文の指紋 `6ad91286ecde`)は監査 54 回目で確かめてから印を記録する。


## 監査 54 回目(対象 = 54e23c9、起動文の指紋 `6ad91286ecde`)

監査役の出力(逐語。[聞く] 1 件、[止める] 0 件):

> ## 監査 54 回目の結果
>
> 対象コミット 54e23c9。範囲: 監査 53 回目の 4 件(literal-pathspecs / バイト突合せ / exit 3→4 分離 / 300・500 の出所)への処置だけ。
>
> ### 実測した内容
>
> scratchpad(`.../scratchpad/audit54/`、確認後 `rm -rf "${D:?}"` で削除済み)で `scripts/cat8_repo_fetch.sh` を、URL の行だけ `file://` に差し替えた写しで、以下に対して `cat8_step.py` 経由で実行した。
>
> 1. **通常ケース**(空白・改行・シンボリックリンクを含む合成リポジトリ)→ `expected_checkout_bytes: 864` = 展開後 `864`(一致)。
> 2. **グロブ衝突ケース**(`star*.txt` を present、同じ `star*` に前方一致する `starFOOBAR.txt`(>1MB でフィルタ除外)を非present に配置): `--literal-pathspecs` を付けた現行版では `starFOOBAR.txt` は正しく除外され `834 = 834` で完走(rc=0)。**比較のため** `--literal-pathspecs` を外した版を同条件で打つと、`git checkout` のグロブ展開で `starFOOBAR.txt` がインデックスにだけ紛れ込み(実体は書かれない「幽霊エントリ」)、`checked_out_files: 7 (expected 6)` で件数不一致 → rc=1 で正しく止まった。**バイト側(`834=834`)は一致していた**ため、この衝突型は件数チェックだけで捕捉され、バイトチェック単体の追加価値をこのケースでは実証できなかった。
> 3. **大きさ超過ケース**(`CAT8_MAX_CHECKOUT_BYTES=100`)→ `expected_checkout_bytes: 834 (limit 100)` → `rc=4`、`.git` 以外何も展開されず。`cat8_step.py` 経由のログに `rc=4` がそのまま残ることを確認(委任文の「終了コードがそのまま生ログに残るか」の主張どおり)。
> 4. **実物**(`th2-net/.github`)→ `212001 = 212001`(監査 51〜53 と一致)。
> 5. **CRLF・`.gitattributes text=auto` を含む合成リポジトリ**を `core.autocrlf=true` の下でクローンしたところ、`expected_checkout_bytes: 31` に対し展開後 `checked_out_bytes(without .git): 32` と**バイト側だけが不一致**になり(件数は `2=2` で一致)、新設のバイトチェック(53 回目の指摘 1 への処置)が単独で正しく検出し `rc=1` で止まった。**この環境自体**(`git config --global/--system/--local --get core.autocrlf` すべて未設定)ではこの経路は発生しないことを確認済み。
>
> ### 指摘
>
> 1. [聞く] `scripts/cat8_repo_fetch.sh:48`(`git cat-file --batch-check` によるバイト計算)— 改行区切りの `.gitattributes`(`text=auto` 等)による行末正規化が、`core.autocrlf` の値によっては blob 上のサイズ(`objectsize`)と実際の展開後サイズを恒常的にずらすことを実機で確認した(上記 5)。**この回の実行環境ではこの経路は発生しない**(autocrlf 未設定を実測で確認)が、道具にもこの回の起動文にも「`core.autocrlf` は未設定であること」という前提は書かれていない。th2-net 配下の 178 件中に `.gitattributes` で行末正規化を指定しているリポジトリが実在するかは未確認(P1 相当 — 「起きない」と断定していないが、実行環境の git 設定という外的要因に依存した検査であることが記録されていない)。この経路が発火すると、実際には正しく取れたリポジトリが「取れなかった」(exit 1)として誤って除外されうる — KA-02 が問題にした「経路由来の失敗をデータそのものの失敗として扱う」型に近い。10 回目を止める実測上の根拠(この環境・対象 178 件でこの経路が実際に踏まれる)は無い。
>
> 以上より監査 53 回目の指摘 1・3・4(literal-pathspecs によるグロブ衝突回避・バイト突合せの追加・300MB 括弧からの分離・exit 3→4 の分離)は実物で成り立つことを確認した。指摘 2(300・500 の出所)は委任文 §2 の 1 の該当段落に「委任文は『数百 MB』とだけ書き、300 という数はリードが決めた」「500 もリードが決めた数」と明記され、`scripts/cat8_repo_fetch.sh` の実装(既定 524288000 = 500×1024×1024)とも数値が一致し、道具・委任文の間に食い違いは無い。終了コードの書き分け(4 = 大きさで止めた / それ以外の 0 でない = 取れなかった)も、起動文の記述(§2 の 1)と `scripts/cat8_repo_fetch.sh` の実装(exit 4 は上限超過のみ、exit 1 は件数・バイト不一致、exit 128 はクローン失敗)、`cat8_step.py` の `rc=3`(独自の期限切れ、この回は `--deadline` を付けないため到達しない)との衝突関係も、実装(コメント「3 is taken by cat8_step.py for a passed deadline」含む)・コミットメッセージ・起動文の三者で一致している。
>
> **10 回目を起動してよい状態か**: 起動してよい(指摘は 1 件のみで「聞く」。この環境での実測では発火しない未確認の環境依存前提であり、実行を止める実測上の欠陥ではない)。

**リードの処置**:
1: 認める(環境の設定に頼っていた)。展開の前に `.git/info/attributes`(リポジトリの `.gitattributes` より優先する)に `* -text -eol -filter -ident -working-tree-encoding` を書き、展開を `git -c core.autocrlf=false` で打つ。中身はバイトどおりに書かれ、行末の変換も filter も走らない。実測(2026-09-24、リード。`core.autocrlf=true` の HOME で、`* text=auto` と `*.bat eol=crlf` を持つ合成リポジトリ): 直した版は計算 37 = 実物 37・終了コード 0 / 直す前の版は実物 44 で「checked-out bytes differ」で止まる / `th2-net/.github` 212,001 = 212,001。起動文は変えていない(指紋 `6ad91286ecde` のまま)。
直した道具は監査 55 回目で確かめてから印を記録する。


## 監査 55 回目(対象 = 6820888、起動文の指紋 `6ad91286ecde`)

監査役の出力(逐語。[聞く] 1 件、[止める] 0 件):

> 監査 55 回目の結果。対象コミット 6820888(`scripts/cat8_repo_fetch.sh`)。範囲: 監査 54 回目の 1 件(行末変換で計算と実物がずれる)への処置だけ。
>
> ## 実測した内容
>
> scratchpad(`.../scratchpad/audit55/`、確認後 `rm -rf "${D:?}"` で削除済み)に、`HOME` をこの下に向けた孤立環境を作り(この環境自体の git 設定は変えていない)、`scripts/cat8_repo_fetch.sh` を URL の行だけ `file://` に差し替えた写しで、次の合成リポジトリ・環境の組み合わせに対して実行した。
>
> 1. **text=auto + eol=crlf**(監査54と同型)を、宛先側 `core.autocrlf=true`・`core.eol=crlf`(敵対的な利用者設定)の下で実行 → `expected_checkout_bytes: 164` = 展開後 `164`。CRLF・working-tree-encoding 用ファイルとも生の LF・生の UTF-8 バイトのまま展開され、変換は一切起きなかった。
> 2. **ident**(`*.id ident`、`$Id$` を含むファイル)→ 展開後も `$Id$` が展開されずリテラルのまま。
> 3. **filter**(`*.rot filter=sample`)を、宛先側のグローバル設定に実際に動く smudge フィルタ(`sed` で内容を書き換える)を仕込んだ状態で実行 → フィルタは走らず、コミット時の生の内容がそのまま出力された。
> 4. **working-tree-encoding**(`wte.txt working-tree-encoding=UTF-16`)→ 展開後もリポジトリに格納された生の UTF-8 バイト列のまま(UTF-16+BOM への再変換なし)。
> 5. **入れ子の `.gitattributes`**(ルートと `sub/` に別々の `eol=crlf` 設定)を敵対的宛先設定の下で実行 → 深い階層のファイルも含め全て LF のまま、`expected_checkout_bytes: 72` = 展開後 `72`。
> 6. **`filter=lfs required=true` で、宛先のグローバル設定の smudge/clean/process コマンドが存在しないバイナリを指す**(LFS 未導入環境の模写)場合 → `.git/info/attributes` の `-filter` により、この壊れたフィルタは**呼び出されすらせず**、`rc=0`・`expected_checkout_bytes: 50` = 展開後 `50` で正常完了した。**対照実験として同じリポジトリを `.git/info/attributes` を書かない旧経路(素の `git checkout`)で展開すると `fatal: ... not found` → `rc=128` でチェックアウト自体が失敗する**ことを確認した。つまりこの修正は「計算=実物」のずれを直すだけでなく、フィルタ未導入環境での致命的な失敗も防いでいる。
> 7. **グロブ特殊文字を含むファイル名(`star*.txt`)+ 1MB 超で除外される `big.bin` + シンボリックリンク + 上記 CRLF 属性**を組み合わせ、敵対的宛先設定の下で実行 → `expected_checkout_bytes: 52` = 展開後 `52`、`star*.txt` は LF のまま、`big.bin` は正しく除外(`skipped:`)、シンボリックリンクのターゲット文字列も壊れず。
>
> いずれの組み合わせでも `expected_checkout_bytes` と展開後の実物(`checked_out_bytes(without .git)`)は一致し、`text`/`eol`/`filter`/`ident`/`working-tree-encoding` のいずれも意図どおり発火しなかった。監査 54 回目が問題にした「行末の変換で計算と実物がずれる」という型は、この一連の組み合わせでは再現しなかった。
>
> ## 指摘
>
> 1. [聞く] `scripts/cat8_repo_fetch.sh:61`(`* -text -eol -filter -ident -working-tree-encoding` による filter の全面抑止)— 実測 6 のとおり、この道具は Git LFS のような `filter` 属性に依存するリポジトリに対して、**実ファイルの中身ではなくポインタファイル(小さいテキスト)をそのまま「取得成功」として展開する**(バイト数は計算と一致するため `rc=0` で正常終了と報告される)。この動作は「バイトどおりに書く」という設計意図(コメント・コミットメッセージに明記)からは正しい帰結だが、委任文の目的(§2「検索し」= 内容を読んで機能を探す)に照らすと、LFS 化された対象があれば**その部分だけ実質的に中身を読めていないのに、ログ上は成功としか見えない**。th2-net 配下の 178 件のどれかが LFS を使っているかは未確認(P1 相当 — 「起きない」と断定できる材料はない)。ポインタファイルらしき内容(`version https://git-lfs...` で始まる等)を検出して `skipped` 相当に計上するか、少なくとも起動文にこの限界を書くかは、リード/オーナーが決めることか。
> 2. 上記以外は、監査 54 回目が指摘した具体的な欠陥(行末変換による計算と実物のずれ)についても、その修正が触れた `-filter`・`-ident`・`-working-tree-encoding` の各属性についても、今回試した組み合わせ(入れ子の `.gitattributes`・敵対的な宛先 `core.autocrlf`/`core.eol`・グロブ特殊文字・シンボリックリンク・フィルタドライバ未導入)のいずれでも不一致・想定外の失敗は再現しなかった。
>
> ## 10 回目を起動してよい状態か
>
> 起動してよい(指摘は 1 件のみで「聞く」。今回試した広い組み合わせ〔行末・ident・filter・working-tree-encoding × 入れ子属性・敵対的宛先設定・特殊ファイル名・破損フィルタドライバ〕のいずれでも、監査 54 回目が問題にした計算と実物のずれ、およびフィルタの意図しない発火は再現しなかった。むしろ旧経路との対照で、この修正がフィルタ未導入環境での致命的な失敗〔rc=128〕も防ぐことを確認した)。

**リードの処置**:
1: 認める(成功に見えて中身を読めていない型なので、機械で数える)。展開のあとに、先頭が `version https://git-lfs.github.com/spec/` のファイル(シンボリックリンクは除く)を `lfs_pointers(not content)` の件数と `lfs_pointer:` の行で出すようにした。起動文 §2 の 1 に、`lfs_pointer:` のファイルは `skipped:` と同じに扱う(除外の種類でなければ「読んでいない」)と書いた。実測(2026-09-24、リード。合成リポジトリ: 置き換えの文字列のファイル・その文字列を文中に含むだけの文書・シンボリックリンク): `lfs_pointers(not content): 1`・`lfs_pointer: model.bin`(文書とシンボリックリンクは数えない)・計算 122 = 実物 122 / `th2-net/.github` 0 件・212,001 = 212,001。
あわせて、オーナーの答え L-508・L-509(述語の読みの 4 問は、4 問とも数える)を起動文に入れ、8-014 の E3a の値・段と E1b の段をこの回で決めるようにした(`fce7d41`)。
直した版(起動文の指紋 `c72dd4ba2a1e`)は監査 56 回目で確かめてから印を記録する。


## 監査 56 回目(対象 = f4e879b、起動文の指紋 `c72dd4ba2a1e`)

監査役の出力(逐語。[直す] 3 件・[聞く] 1 件、[止める] 0 件):

> 監査 56 回目の結果。
>
> ## A. オーナーの答えの取り込み(fce7d41)
>
> 1. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run10_prompt.md:15`(「オーナーに聞いている述語の読み」問い 2「8-014: LLM に『先読みの偏りを見つけよ』と指示する仕組みを、E3a・E6 の『機能』に数えるか」)— E6 をこの問い(LLM 指示)に束ねているが、`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` の監査 47 回目・指摘 2 と `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3186` を突き合わせると、E6 が `未判別` に戻された実際の根拠は `skfolio_validation.py` の `ModelValidator`(同梱の**別ライブラリ**、問い 3 の対象)であり、LLM のプロンプト指示(`subagents.py` の「lookahead bias」文言)ではない。E6 は問い 3 の対象であって問い 2 の対象ではないはずである。今回はオーナーが 4 問とも「数える」と答えたため結果(8-014 E6 = 印・段4)自体は変わらないが、この記録は E6 の「未判別」の由来を誤って問い 2 に結び付けている。この記録の誤りが、今後 E6 のような同型セルの由来を追うときに再び混同を生まないか(P2 — 記号・根拠の対応がずれている)。この問い自体は結果に影響しないため [直す]。
> 2. [聞く] `docs/OWNER_LOG.md:426`(L-509)— 「(b) の場合、4 問の『承認』は、4 問とも**数える**と読むことになります」という帰結は、L-508 への返答で提示した選択肢 (b) の説明文としてオーナーに見せた上で「b」の回答を得たのか、それとも「b」の回答を受け取った後にリードが書き足した読みなのか。`OWNER_LOG.md` の該当行はリード自身が書いた要約であり、選択肢 (b) の逐語がオーナーに提示された時点の文として別途確認できる記録(会話ログ以外の成果物)が本監査の対象範囲に見当たらなかった。事後の理由付けでないことの確認(P10 に近い型 — 目視・要約による判断と原典の再照合)。
>
> 台帳 `docs/DATA/tools_catalog_cat8.tsv` の実測(Python で全セル走査): 戻したセル(8-010 E3b=印/段1/状態 深掘り、8-014 段_E3b=4、8-014 E6=印/段4、8-004 E2=印/段3)は claim どおりで、9 回目の報告値・run9 監査の処置と食い違いは無かった。残存する `未判別` は 8-005(全 16 セル)・8-011 E6段・8-014 E1b段/E3a・8-016 E3a/E3b/E5 の 4 行のみで、これは起動文 §2 の 1・2・5・6 に 1 対 1 で割り当てられており(実測: 孤立セル 0 件)、監査 48 回目で見つかった「どのタスクにも現れない未判別」型の再発は無い。8-014 E1b 段・E3a を §2 の 6 に割り当てた書き方(`docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:22,55`)は L-509 の文言(`performance_metrics` の使用未確認 / 値・段とも報告に無い)と一致しており、十分と判断した。
>
> ## B. 起動文と `scripts/cat8_repo_fetch.sh` の LFS 検出
>
> scratchpad に合成リポジトリを 2 通り作り、`file://` で URL 行だけ差し替えた写しで実行して確認した(以下は実測、済んだら `rm -rf` で消去済み):
> - 単純ケース: 実物の LFS ポインタ(131 バイトの `version https://git-lfs.github.com/spec/v1...` テキスト)を含むリポジトリ → `lfs_pointer: big.bin` が 1 件検出された。
> - 判別ケース: 本物のポインタ・ポインタの見出し文を引用しただけの文書(`doc_quoting.md`、行頭に別テキストがある)・シンボリックリンク(`link.txt`)を同居させたリポジトリ → 本物のポインタだけが `lfs_pointer:` に載り、引用文書とシンボリックリンクは載らなかった(`checked_out_bytes` と `expected_checkout_bytes` も一致)。コミット `f4e879b` の要旨「ポインタは載る、引用文書とシンボリックリンクは載らない」は実物で成り立つ。
>
> 3. [直す] コミット `f4e879b` の要旨(「122 = 122 bytes・th2-net/.github 0 pointers」)は具体的な実測値を主張しているが、この検証に使った合成リポジトリ・スクリプト・出力は `docs/DATA/probes/` にも scratchpad にもコミットにも残っていない(`docs/AUDITOR/TRACE/2026-09-24_2da9385f.json` の move 74 は Bash 8 回という回数のみで中身が無い)。この区分の他の全ての実測(N/M・段の逐語など)は `docs/DATA/probes/20260923_tools_8_run*.log` に生ログとして残す運用を徹底してきたのに対し、この 1 件だけは CLAUDE.md O-3(「実行したコマンドと出力を同じ返答に出す。出せないなら『未確認』と書く」)の対象になる主張でありながら再照合できる形で残っていない(P10 寄り)。私自身の再実験では機構は成り立ったが、この特定の数値主張(122/0 件)そのものは未確認のまま採用されている。
> 4. [直す] `scripts/cat8_ledger.py:591-596` の `一覧 N 件 / 読んだ M 件` 検査は、報告本文に書かれた N と M が**互いに一致するか**だけを見ており、`scripts/cat8_repo_fetch.sh` が実際に出力する `files_in_tree` / `skipped:` / `lfs_pointer:`(audit 55 で追加)の件数と突き合わせる仕組みは無い(`scripts/check_scan_report.py` にも該当なし、`grep` で 0 件)。起動文 §2 の 1(`docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`)は「`lfs_pointer:` の行は `skipped:` と同じに扱う」と文章で指示しているだけで、機械検査には反映されていない。この区分は N/M の数え違いで 5 回・8 回目に差し戻された前歴があり、10 回目は th2-net/viewer 級(展開 25,985 件)の大きなリポジトリを扱う。この新しい会計規則が文章の指示だけで機械の網に掛かっていない状態のまま大きな候補に適用されることに問題は無いか(P8 — 規則は書くだけでは機能しない)。
>
> ## 結論
>
> [止める] に該当する指摘は無かった。上の [直す] 2 件・[聞く] 1 件(A)と [直す] 2 件(B)を記録すれば、10 回目は起動してよい状態と判断する。

**リードの処置**:
1: 認める。この節の冒頭の「オーナーに聞いている述語の読み」の問い 2 に、E6 を `未判別` にした理由は問い 3 だったこと、問い 2 に E6 を入れた理由は記録から確かめられない(未確認)ことを注として書いた。4 問とも「数える」なので台帳の値は変わらない。
2: 答える。選択肢 (b) の文「(b) の場合、4 問の「承認」は、4 問とも**数える**と読むことになります」と 4 問それぞれの当て直しの表は、オーナーの「b」の**前**のリードの返答(L-508 を記録した直後の返答)に書いて見せた。L-509 の 5 列目はその文の写し。会話の外の成果物には残っていない(会話の記録にだけある)。
3: 認める。リードの試しを `cat8_step.py` 経由で打ち直し、生ログ `docs/DATA/probes/20260924_cat8_repo_fetch_lead_tests.log` に残した(合成リポジトリ 8 件・`core.autocrlf=true` の下で計算 159 = 実物 159・`lfs_pointer: model.bin` 1 件 / `th2-net/.github` 113 件・212,001 = 212,001 / `th2-net/th2-common-py` 127 件・253,589 / `th2-net/viewer` 25,999 件・取らなかった 14 件・130,782,466 / 上限 1,000,000 で viewer は終了コード 4)。コミットの要旨の数(122)は打ち直す前の試しの数で、その試しは残していない(未確認のまま。以後は生ログに残す)。
4: 認める。`scripts/cat8_fetch_tally.py` を作った: 生ログの `cat8_repo_fetch.sh` の手を 1 件ずつ読み、終了コード・N・`skipped:`・`lfs_pointer:`・取った量・展開の大きさを表にし、和を出す。出力が `--keep` で切られている・件数の行と名前の行の数が合わない・終了コード 0 なのに件数の行が無い・同じリポジトリを 2 回打って結果が違う、のどれかがあれば 0 でない終了コードで止まる。上の生ログで打つと、viewer を 2 回(0 と 4)打ったことを止める側で拾った。起動文 §2 の 1 に、8-016 の N・除外・読んでいない件数はこの道具の出力から取ること、取得の手は `cat8_step.py --keep 200000` で打つこと(付けないと 3,000 文字で切られる)を書いた。リードは検収で同じ道具を打ち直す。
直した版(起動文の指紋 `6f026717612d`)は監査 57 回目で確かめてから印を記録する。


## 監査 57 回目(対象 = 15dcb95、起動文の指紋 `6f026717612d`)

監査役の出力(逐語。[直す] 1 件・[聞く] 1 件、[止める] 0 件):

> 監査 57 回目の結果。対象コミット 15dcb95。範囲: 監査 56 回目の 4 件への処置(生ログ・新道具 `cat8_fetch_tally.py`・起動文 §2 の 1 の追記 2 箇所)。
>
> ## 問い 1(`scripts/cat8_fetch_tally.py` の実測)
>
> 生ログ `docs/DATA/probes/20260924_cat8_repo_fetch_lead_tests.log` に対して実際に打つと、報告どおり `th2-net/viewer` の 2 回(rc=0 と rc=4)を「2 回打たれて結果が違う」として拾い、終了コード 1 で止まった。scratchpad(`.../scratchpad/audit57/`、確認後 `rm -rf "${D:?}"` で削除済み)で自作の生ログを使い次も確認: 出力が `[出力は N 文字。先頭 M 文字だけを残した]` で切られたケース→検出して止まる/終了コード 0 なのに `lfs_pointers(not content):` 行が欠けるケース→検出して止まる/`--prefix` の外のリポジトリ→正しく除外・エラーにならない/`rc=128`(取得失敗、件数行が一切無い)→「そのほか」に分類されエラーにならない(設計どおり)。
>
> **1. [直す]** `scripts/cat8_fetch_tally.py:66`(および docstring 9〜11 行の「a repo appears twice with different results」)— 「結果が違う」の判定は `(N, rc)` のタプル一致だけで、`downloaded_bytes`・`checked_out_bytes`・`skipped`/`lfs_pointer` の名前一覧は比較していない。実測: 同じリポジトリを `N`・`rc=0` は同一のまま `downloaded_bytes`(100→999999)・`checked_out_bytes`(50→88888)・`skipped:` の名前(`big.bin`→`different_file.bin`)だけ変えた 2 手を食わせたところ、エラー無し・終了コード 0 で後勝ちの値がそのまま合計に混ざった。道具の docstring・リードの処置4 の文言(「結果が違う」)は N・rc に限定されていないため、実装と主張がずれている。実行を止める段階の欠陥ではないが直す余地がある。
>
> ## 問い 2(生ログと処置 3 の数の一致)
>
> 処置 3(検収記録 291 行)の主張(合成リポジトリ 8 件・計算 159=実物 159・`lfs_pointer: model.bin` 1 件/`.github` 113 件・212,001=212,001/`th2-common-py` 127 件・253,589/`viewer` 25,999 件・skipped 14 件・130,782,466/上限 1,000,000 で rc=4)を生ログの該当行(2〜89 行)と 1 件ずつ突き合わせ、**全項目一致を確認**。指摘なし。
>
> ## 問い 3(§2 の 1 の追加文と道具の動きの整合)
>
> `git show 15dcb95` で追記が「`--keep 200000`」×2 箇所と N の出所の 1 文であることを確認。N = 終了コード 0 の N の和という定義は実装(70〜79 行)と一致。
>
> **2. [聞く]** `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` — `--keep 200000` の根拠はリードの 3 件実測(個々の `cat8_repo_fetch.sh` の 1 手あたりの出力が切られないこと)だけで、この追記が新たに指示する `cat8_fetch_tally.py` の**178 件全体の集計出力**(178 行の表+skipped/lfs 内訳+合計行)が 200,000 字に収まることは実測されていない(178 件規模の実行はこの監査の範囲外)。切られた場合、末尾の `== 終了コード 0 の N の和 = ...` 合計行自体が欠け、この出力を再度パースして truncation を検出する仕組みは無いため、無音で不完全な N が生ログに残るおそれがある。178 件規模でも十分かオーナー/リードで確認済みか。
>
> ## 結論
>
> 止める 0 件・直す 1 件・聞く 1 件。問い 2 の数値照合は完全一致、問い 1 のテスト(切断・欠落・重複・prefix 外)は「重複だが N・rc だけ一致し他の値は違う」ケースだけすり抜けた。
>
> **10 回目を起動してよい状態か**: 起動してよい(2 件とも「直す」「聞く」で実行を止める実測上の欠陥ではない。ただし 178 件規模の実行前に上記 2 点をリードが確認しておくことを推奨する)。

**リードの処置**:
1: 認める。同じリポジトリの 2 回目は、終了コード・N・`skipped:` の名前・`lfs_pointer:` の名前・取った量・展開の大きさの**全部**を比べ、どれかが違えば違う項目の名前を出して止まるようにした。実測(2026-09-24、リード。scratchpad の自作の生ログ: N と終了コードは同じで、取った量と `skipped:` の名前だけ違う 2 手): 「違う項目: skipped, dl」で終了コード 1。`docs/DATA/probes/20260924_cat8_repo_fetch_lead_tests.log` では「違う項目: rc, co」。
2: 認める(178 件の出力の長さは測っていない)。和の行を出力の**最初と最後**の 2 か所に出すようにし(切られても最初の和は残る)、誤りの行も標準出力に出して最後の和の直前に置いた。起動文 §2 の 1 で、この手は `--keep 2000000` で打ち、生ログのその手の最後の行が `---- 合計 N 件` でなければ切られているので打ち直す、と書いた。
直した版(起動文の指紋 `3dbd097edcaa`)は監査 58 回目で確かめてから印を記録する。


## 監査 58 回目(対象 = bd77fa9、起動文の指紋 `3dbd097edcaa`)

監査役の出力(逐語。[直す] 1 件・[聞く] 1 件、[止める] 0 件):

> 監査 58 回目の結果。対象コミット bd77fa9。範囲: 監査 57 回目の 2 件への処置だけ。
>
> ## 実測した内容
>
> scratchpad(`.../scratchpad/audit58/`、確認後 `rm -rf "${D:?}"` で削除済み)で以下を実測した。
>
> - `scripts/cat8_fetch_tally.py` を、実物の生ログ `docs/DATA/probes/20260924_cat8_repo_fetch_lead_tests.log` と自作の合成生ログ(2 種)に対して直接実行、および `cat8_step.py --keep <N>` 経由で実行。
> - 監査 57 回目の対象コミット `15dcb95` 時点の旧版(`git show 15dcb95:scripts/cat8_fetch_tally.py`)を取り出し、新版と挙動を比較。
> - `scripts/cat8_step.py` のソース(76〜82 行、`--keep` による切り詰めと `| ` エスケープの実装)を確認し、小さい `--keep` で実際に切り詰めを発生させて挙動を確認。
> - `scripts/cat8_ledger.py` の `cmd_check`(218〜266 行)・`cat8_fetch_tally.py` の docstring・`docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md` 中の `cat8_fetch_tally.py` への全言及箇所を突き合わせた。
>
> ### 問い 1 の結果(全項目比較・和の先頭末尾・誤りの標準出力)
>
> - 実物の生ログで実行 → `th2-net/viewer` の 2 回(rc=0/rc=4)を「2回打たれて結果が違う(違う項目: rc, co)」として検出、`ERR` は標準出力(stdout)に出た。
> - 自作の合成ログ(N・rc は同じで `skipped:` の名前だけ違う 2 回)→ 旧版(タプル `(N, rc)` のみ比較)は検出せず 0 件のまま通過。新版は `違う項目: skipped` で正しく検出。監査 57 回目・指摘 1 への処置が実物で成り立つことを確認した。
> - 和の行(`== リポジトリ N 件`・`== 終了コード 0 の N の和…`・`== 誤り N 件`)は出力の最初と最後の 2 か所に出た(`totals()` を先頭とループ後の 2 回呼ぶ実装どおり)。**重要な追加確認**: `rows`/`errs` は最初の `for` ループで全件処理を終えてから初めて `print` が始まるため、**1 回目の `totals()` 呼び出しの時点で既に最終値**(全件処理後の N 和・誤り件数)が出る。つまり出力の途中で切られても、冒頭の和は「部分集計」ではなく「全件の確定値」であり、この点は指示文にも道具の docstring にも明記されていないが、切り詰め耐性を実質的に強めている。
> - `ERR` 行が stderr でなく stdout に出ることを確認した。**旧版(15dcb95)との比較で分かったこと**: `cat8_step.py` は `subprocess.run(capture_output=True)` の `stdout` と `stderr` を**別バッファのまま単純連結**(`p.stdout + p.stderr`、時系列でインターリーブしない)して生ログに書く。旧版は `ERR` を `file=sys.stderr` に出していたため、**誤りが 1 件でもあると、生ログ上の実際の最後の行は常に `ERR …` になり、`---- 合計 N 件` が最後の行にならなかった**(自作ログで実測: 旧版を `cat8_step.py` 経由で打つと `---- 合計 1 件` の後に `ERR th2-net/foo: …` が続いて記録された)。今回の `ERR` を標準出力に移す修正は、まさにこの「誤りがある回だけ末尾判定が壊れる」という欠陥を解消しており、機能上必要な修正だった。この修正自体は「リードの処置」2 の一文(「誤りの行も標準出力に出して最後の和の直前に置いた」)に明記されており、独立検証の結果、この記述は実物と一致した。指摘なし。
>
> ### 問い 2 の結果(§2 の 1 の追加文と道具の動きの整合)
>
> - `git show bd77fa9 -- docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md` で差分は 1 行(50 行目)のみ、`--keep 200000` → `--keep 2000000` と、末尾の判定文の書き直しであることを確認。他の箇所に `cat8_fetch_tally.py` への言及は無く(grep で 1 箇所のみ)、重複・矛盾する記述は無かった。
> - 「N = 終了コード 0 の N の和」は実装(70〜79 行)と一致。「この道具が 0 件でない数の誤りを出したら、`なし` は書かない」の「誤り」= `ERR` 件数(exit code ではなく `== 誤り N 件`/`---- 合計 N 件` の数)という読みは道具の出力と整合する。
>
> ## 指摘
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(「生ログのその手の最後の行が `---- 合計 N 件` でなければ出力が切られている」)と `scripts/cat8_step.py:79` — `cat8_step.py` は出力中の `---` で始まる行に無条件で `| ` を前置してから生ログに書く(見出し行との誤認防止のための既存仕様、コメントに 2026-09-23 の経緯あり)。実測: `cat8_fetch_tally.py` の最終行 `---- 合計 N 件` も `---` で始まるため、生ログに実際に残るのは `| ---- 合計 N 件` であり、指示文が言う「最後の行が `---- 合計 N 件`」という**文字列そのもの**とは食い違う(先頭に `| ` が付く)。切り詰められた場合の末尾は `[出力は N 文字。先頭 M 文字だけを残した]`(この行は `---` で始まらないため `| ` は付かない)なので、視覚的な判別自体は可能だが、指示文の逐語と生ログの実物が一致しない点は、この区分が過去に逐語一致の検査で繰り返し躓いてきた型(監査 49 回目の全角コロン、6 回目の「引用が生ログに無い」等)に近い(P2 寄り — 記号・逐語の指す実体が資料間でずれている)。
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` と `scripts/cat8_ledger.py:218-266`(`cmd_check`)・`scripts/check_scan_report.py` — 「生ログの最後の行が `---- 合計 N 件` か」という切り詰め検出は、§7 の 4 つの完了ゲート(`check_scan_report.py`・`cat8_ledger.py check-elements`・`cat8_ledger.py check`・`git diff` の行数)のいずれにも組み込まれていない(3 つのスクリプトを `grep` して `fetch_tally`・切り詰め判定への参照が無いことを確認済み)。この確認は起動文の地の文の指示のみに依存し、実行者(10 回目の担当)が読んで手作業で最後の行を見るか、後でリードが検収時に同じ道具を打ち直して確かめる(「リードは検収で同じ道具を打ち直して数え合わせる」)ことに委ねられている。この区分は N/M の数え違いで 5 回目・8 回目に差し戻された前歴があり、CLAUDE.md §0.2 A-11(目視判定の禁止)・PRINCIPLES.md P8(規則は書くだけでは機能しない)の型に近い。機械ゲートに組み込む必要があるか、それとも「地の文の指示 + リードの検収時の打ち直し」の二重の人力確認で足りるとする判断は、リード/オーナーが決めることか。
>
> ## 結論
>
> 止める 0 件・直す 1 件・聞く 1 件。実測した範囲(全項目比較の検出・和の先頭末尾出力・誤りの標準出力化・§2 の1 追加文との整合)に、実行を止めるべき機能上の欠陥は見つからなかった。指摘 1 は表記と実物の字面のずれ(実害は限定的、視覚的には判別可能)、指摘 2 は機械ゲート化するかどうかの判断が要る点で、いずれも「聞く」「直す」の水準。
>
> **10 回目を起動してよい状態か**: 起動してよい(指摘 2 件はいずれも実行を止める実測上の欠陥ではない。ただし指摘 1(生ログの実際の文字列は `| ---- 合計 N 件`)は次回の指示文言を直すか、実行者向けの注記を足すことを推奨する)。

**リードの処置**:
1: 認める。集計の道具の最後の行を `== 集計の終わり(誤り N 件)` に替えた(`---` で始めない。`cat8_step.py` が `| ` を前に付けないため、生ログに書いたとおりの文字列で残る)。起動文の文もこれに揃えた。実測(2026-09-24、リード。scratchpad): `--keep 2000000` で打つと生ログの最後の行は `== 集計の終わり(誤り 1 件)`、`--keep 200` で打つと `[出力は 1775 文字。先頭 200 文字だけを残した]`。
2: 答える。切られていないかと N の数え合わせは、リードが検収で `python3 scripts/cat8_fetch_tally.py docs/DATA/probes/20260923_tools_8_run10.log --prefix th2-net/` を**生ログに通さず直接**打ち(出力は切られない)、その和と報告の 8-016 の N・除外・読んでいない件数を突き合わせる。調査班の生ログの最後の行の確かめは、その前の段の保険。検収の記録にこの突き合わせの出力を貼る。
直した版(起動文の指紋 `5a665546525c`)は監査 59 回目で確かめてから印を記録する。


## 監査 59 回目(対象 = 647e07d、起動文の指紋 `5a665546525c`)

監査役の出力(逐語。[聞く] 2 件・[直す] 1 件、[止める] 0 件):

> 監査 59 回目。対象コミット 647e07d(ブランチ claude/tools-survey-cat8)。
>
> ## 実施した実測
> - `scripts/cat8_fetch_tally.py` の最終行 `== 集計の終わり(誤り N 件)` を `cat8_step.py --keep 2000000` 経由で実行し、生ログの実物を確認。`| ` の前置なし・記述どおりの文字列がそのまま残ることを実測で確認(`scratchpad/audit59/out.log` で確認、確認後 `rm -rf` 済み)。切り詰め時(`--keep 50`)は `[出力は N 文字。先頭 M 文字だけを残した]` になり判別可能であることも確認。→ 監査 58 回目の指摘 1 への処置は実物で成り立つ。
> - `docs/DATA/tools_catalog_cat8.tsv` を Python で全セル走査し、`未判別` の残存セルが 8-005(全16セル)/8-011 段_E6/8-014 E3a・段_E1b・段_E3a/8-016 E3a・E3b・E5・対応する段、の 4 行のみであることを確認。これは起動文 §2 の 1・2・5・6 に 1 対 1 で割り当たっており孤立セルは無い(監査 56 回目の実測と一致)。
> - 起動文が §0 で参照する 5 文書の指紋(`ce0012c95154` 等)を `sha256sum` で実測し、全て一致(参照切れなし)。
> - `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md` 自体の指紋が `5a665546525c` と一致することを確認。
> - `scripts/cat8_repo_fetch.sh`・`scripts/cat8_fetch_tally.py`・`scripts/cat8_ledger.py` を読み、起動文が主張する出力キー(`files_in_tree(N)`・`skipped:`・`lfs_pointer:`・`downloaded_bytes`・`checked_out_bytes` 等)・終了コードの意味(0=成功/1=不整合/4=大きさで停止/128=clone失敗)・`一覧N件/読んだM件` 形式の機械検査(`cat8_ledger.py:591-596`)が起動文の記述と food で食い違わないことを確認。
> - L-508・L-509(`docs/OWNER_LOG.md` 425-426 行)を読み、起動文 §2 冒頭近くの「オーナーが決めた述語の読み」4 項目(別ライブラリの機能→数える/LLM指示文→数える/重複はじく→E2/1回だけの学習評価分け→E3b)が L-509 の 4 問の当て直しと 1 対 1 で対応していることを確認。§1「この回に回さないもの」の記述(8-010 E3b・8-014 E3b・E6 は L-508/509 で決めた)も台帳の実際の状態と整合。
>
> ## 指摘
>
> 1. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(§2 の 1、300 MB 累積上限)— 監査 51〜54 回目を通して「1 件あたり 500 MB」の展開後サイズ上限は `scripts/cat8_repo_fetch.sh` の `CAT8_MAX_CHECKOUT_BYTES`(既定 524288000)で機械的に強制されている(超過で exit 4、実測済み)一方、**「178 件を通した累積 300 MB」の上限には対応する機械の強制が一つも無い**ことを実測で確認した(`scripts/cat8_ledger.py`・`scripts/check_scan_report.py`・`scripts/cat8_fetch_tally.py`・`scripts/cat8_repo_fetch.sh` のいずれにも `300` や 300 MB 相当の定数・閾値チェックが無い、`grep` で 0 件)。起動文は「出力の `downloaded_bytes` の和を 1 件ごとに生ログに残し、和が 300 MB を超えたらそこで取得をやめる」と書くのみで、**和を計算し閾値と比較する責務は実行者(調査班)の自己申告に委ねられている**。`cat8_fetch_tally.py` は全ステップ終了後にしか `downloaded_bytes の和` を出力せず、途中経過での自動停止には使えない(そのような用途で回す指示も無い)。このプロジェクトは同種の自己申告(N/M の数え違い、5・8 回目の差し戻し)を繰り返し機械検査へ格上げしてきた経緯があり(P8「規則は書くだけでは機能しない」)、しかも今回の起動文 §0/§6 は L-507「案A」により「途中で止めない・予算で止めない」を明記している。500 MB 側と同じ水準の機械強制を 300 MB 側にも置くか、置かないままでよいかは、リード/オーナーが決めることか。
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:5` と `:50` — §0 は「途中で止めない。予算で止めない…次の候補へ進む(止まらない)」と回全体の終わり方を定める一方、§2 の 1 は候補 8-016 の内部で「300 MB を超えたらそこで取得をやめる」と書く。両者は「候補(8-016 全体)」と「候補内のリポジトリ反復」という異なる粒度の話だと読めば矛盾しないが、起動文はその粒度の違いを明示していない。300 MB で「そこで取得をやめる」ことが §0 の「止まらない」の例外として許容されている旨が明記されていないため、実行者が両者をどう調停するかは起動文の文面だけでは確定しない(指摘 1 と同じ論点の一側面)。
>
> 3. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run10_prompt.md` 冒頭(5〜17 行、監査 48 回目より前の「この回に回さないもの」表・「オーナーに聞いている述語の読み」4 問)は、L-508・L-509 の回答(2026-09-24)で全 4 問が「数える」と決着した後もそのまま残っており、現在の起動文本文(§1 の「8-010 E3b と 8-014 E3b・E6 は…決めた」)と比べると内容が古い(8-014 E3a が §1 では引き続き未決として個別に触れられているのに、冒頭表では E3a と E6 が同じ問い 2 に束ねられたまま)。監査 56 回目・指摘 1 で同型の記述の古さ自体は一度指摘・処置済みだが、その処置(注記の追加)は本文中の別の場所(問い 2 の直後)に足しただけで、この冒頭の表自体は未更新のまま残っている。実害は無い(現在の起動文は正しい状態を独立に記述している)が、この記録を初見で読む次回以降の担当が冒頭表だけを見て古い前提(8-014 E3a・E6 が同一の問いに紐づく)に引きずられる余地がわずかに残る。
>
> ## 10 回目を起動してよい状態か
>
> 起動してよい(指摘 3 件はいずれも「聞く」「直す」であり、監査 58 回目の 2 件への処置は実物で成り立つことを確認済み。指摘 1・2(300 MB 累積上限の機械強制の欠如とその粒度の不明記)はリード/オーナーの確認を推奨する)。

**リードの処置**:
1: 認める(500 MB は機械、300 MB は自己申告で、揃っていなかった)。取得の道具が、置き場の親の `.cat8_downloaded_total` に取った量の和を足していき、和が 300 MB(`CAT8_MAX_TOTAL_DOWNLOAD_BYTES`)以上なら取らずに終了コード 5 で止まるようにした。集計の道具は終了コード 5 の件数を別の行に出す。実測(2026-09-24、リード。scratchpad、上限を 200,000 バイトにして 3 件): `th2-net/.github` 和 0 → 111,764・終了コード 0 / `th2-net/th2-common-py` 111,764 → 235,184・0 / 3 件目は和 235,184 で「not cloned」・終了コード 5。起動文 §2 の 1 に、和は道具が数えること、このファイルは回の終わりまで消さないこと、終了コード 5 のあとの残りのリポジトリの書き方を書いた。
2: 認める。起動文に「止めるのは 8-016 の取得だけで、この回のほかの項目へは進む(§0 の「途中で止めない」は回全体のこと)」と書いた。
3: 認める。この記録の冒頭の「この回に回さないもの」の表の行と、4 問の見出しに、L-508・L-509 で決着したことと、10 回目で決めるもの(8-014 E3a の値・段、E1b の段)を書いた。
直した版(起動文の指紋 `255734e5cb1a`)は監査 60 回目で確かめてから印を記録する。


## 監査 60 回目(対象 = edb439c、起動文の指紋 `255734e5cb1a`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件・[聞く] 1 件):

> 監査 60 回目の結果。対象コミット edb439c(ブランチ claude/tools-survey-cat8)。範囲: 監査 59 回目の 3 件への処置だけ。
>
> ## 実測した内容
>
> scratchpad(`.../scratchpad/audit60/`、確認後 `rm -rf "${D:?}"` で削除済み)に合成リポジトリを作り、`scripts/cat8_repo_fetch.sh` を URL の行だけ `file://` に差し替えた写しで、以下を実行した(この環境の `/bin/sh` は `dash`(`readlink -f /bin/sh` = `/usr/bin/dash`)で実行環境と同一)。
>
> 1. 絶対パスの `$dest` で 2 件連続フェッチ → `.cat8_downloaded_total`(親ディレクトリ)が正しく積算(0→29404→58808)。
> 2. `CAT8_MAX_CHECKOUT_BYTES=1` で強制的に終了コード 4 を発生 → `downloaded_bytes` は total に正しく加算される(0→29404)。
> 3. 存在しないクローン元 → `rc=128`、`total_file` は変化なし(未着手扱いとして正しい)。
> 4. 完全に空のリポジトリ(0 ファイル、`--allow-empty` commit のみ)→ 道具内部の Python heredoc が `sys.exit("counts do not add up (tree 0, present 0, skipped 0)")` で `rc=1`。この分岐は総額計算(55行目)より前で発生するため `total_file` は更新されない。ただし調べた限り、`present`/`skipped` への振り分けは全エントリが必ずどちらか一方に入る実装のため、**この「件数が合わない」分岐は空リポジトリ(0ファイル)の場合以外には到達し得ない**(通常サイズの th2-net リポジトリでは発生しない)。
> 5. `.cat8_downloaded_total` を `""`(空)にして実行 → 18行目の `[ -ge ]` が `Illegal number:` エラーを出しつつ if 文としては素通り(300MB チェックを経ずに clone を実行)。ただし 55 行目の算術 `$((total_before + dl))` は空文字を 0 として扱い成功し、以後は自己修復した(29404 に正しく更新)。
> 6. `.cat8_downloaded_total` を `"not_a_number"` にして 2 回連続実行 → **両方とも** 18行目の `[ -ge ]` チェックを素通りして実際に `git clone` を実行(実測: `.git` が実ディスク上に 29,404 バイトずつ書き込まれ、ネットワーク・ディスクコストが実際に発生)、55行目の算術で同じ `Illegal number` エラーが再発して `rc=2` で異常終了。**この `rc=2` に至る経路では `total_file` への書き込みが一度も行われず**、`"not_a_number"` のまま残り続けた(2回目の呼び出しでも同じ症状が再現し、自己修復しない)。
> 7. `cat8_step.py` 経由で 6 のシナリオを再現し `scripts/cat8_fetch_tally.py` に通したところ、2件とも「そのほか」(rc not in 0/4/5)に正しく分類され、`downloaded_bytes の和` にはこの2件分(合計 58808)が正しく含まれていた(集計ツール自体は壊れていない)。
> 8. `cat8_step.py`(実 `cat8_repo_fetch.sh` という名前で正しく合わせたコピー)+ 合成 300MB(実際はテスト用に 50000バイト)上限で 3 件連続実行 → 2 件成功、3件目が正しく `rc=5` になり、`cat8_fetch_tally.py` の「終了コード5」カウント・`downloaded_bytes の和`(rc=5 行は `None`→0扱い)・`N の和`(rc=0 のみ集計)はいずれも実物で正しく機能した。
> 9. `$dest` を相対パス(例 `dest1`)にして実行 → 14行目 `total_file="$(dirname "$dest")/.cat8_downloaded_total"` は `cd "$dest"`(24行目)より前に確定するが、変数自体は相対パス文字列のまま残るため、55行目の書き込みは**意図した `$dest` の親ではなく `$dest` 自身の中**(`dest1/.cat8_downloaded_total`)に作られた。今回の起動文(§2 の 1)は常に絶対パスを渡す指定なので、この回の実行では踏まない経路。
>
> ## 指摘
>
> 1. **[止める]** `scripts/cat8_repo_fetch.sh:14-21,55`(`.cat8_downloaded_total` の読み取り・比較・書き込み)— 実測(上記6・7)で確認したとおり、このファイルの内容が数値として不正(例: `"not_a_number"`)になった場合、**300MB 上限のチェック自体が恒久的に無効化された状態のまま、実際の `git clone`(ネットワーク・ディスクコストを伴う)だけは以後もずっと実行され続ける**。原因は 18 行目の `[ "$total_before" -ge "$max_total" ]` がシェルの算術エラー(`Illegal number`)を出すだけで、`if` の条件としては「偽」(=上限未満)扱いになって素通りすること、かつ 55 行目の総額の再計算・再書き込みも同じ理由で失敗し、**壊れた値が一度も上書きされずに残り続ける**(自己修復しない)ことにある。監査 48 回目から続くこの一連のやり取りは「取った量の和を実物で機械的に強制する」ことを目的として来ており(監査 59 回目でまさに「自己申告ではなく道具に強制させる」形に直した)、この道具が自らの状態ファイルの破損に対して無検証であることは、その目的そのものを危険側(安全側ではなく、より多く取ってしまう方向)に破りうる。さらに、この経路で生じる終了コード `2` は、委任文(`docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`)が定義するどの終了コード(0/1/4/5/128)にも当たらず、委任文の分類規則(「4・5 以外の 0 でない終了コードは『取れなかった』に数える」)に従うと、経路・アクセスの問題向けの対処である「オーナーの PC から取る手順を書く」が割り当てられるが、実際の原因はローカルの状態ファイルの破損であり、オーナーの PC から取り直しても解消しない。総額ファイルがどう壊れうるか(書き込み中の中断、ディスク逼迫、外部からの編集など)自体は本監査では実演していないが、**壊れた場合にどう振る舞うかは実演・実測済み**であり、その振る舞いが安全側でないことが問題。(P3 — 「和を道具が機械的に強制する」という主張は境界条件まで実装して確かめるまで成り立たない。CLAUDE.md §0.2 A-18 の型に近い — 安全策自体に漏れがあり、失敗の方向が危険側)
>
> 2. **[直す]** `scripts/cat8_repo_fetch.sh:14`(`total_file="$(dirname "$dest")/.cat8_downloaded_total"`)— 実測(上記9)のとおり、`$dest` が相対パスのとき、`cd "$dest"`(24行目)の後も `$total_file` は相対パス文字列のまま残るため、意図した `$dest` の親ディレクトリではなく `$dest` 自身の中に総額ファイルが作られる。178件のループは各リポジトリの置き場を都度消すため、この経路を踏むと総額ファイルごと毎回消え、実質的に上限チェックが機能しなくなる。**今回の起動文(§2 の 1、50行目)は常に絶対パスを渡す指定になっており、実測でも絶対パスでは正しく機能することを確認した**ので、この回の実行を直ちに止める実測上の根拠ではないが、道具自体には「`$dest` は絶対パスであること」という前提のコメント・検査が無い(呼び出し側の規律だけに依存している)。
>
> 3. **[聞く]** `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(§0「途中で止めない」と §2 の 1 の 300MB/500MB 上限の関係)— 実測範囲では、§2 の 1 に足された「止めるのは 8-016 の取得だけで、この回のほかの項目へは進む(§0 の『途中で止めない』は回全体のこと)」という一文により、粒度(回全体 vs 8-016 内のリポジトリ反復)の食い違いは文面上解消されている(§0・§2 の 1・§6(予算=トークン/時間、追補§5 で別概念と確認)の間に直接の矛盾は見当たらなかった)。ただしこの一文は、N の定義・LFS・タリー道具の使い方など多数の論点を含む単一の長大な段落(§2 の 1)の中ほどに埋め込まれており、監査 53 回目の指摘 3(300MB/500MB の説明が入れ子で読みにくい)と同型の可読性の懸念が、この新しい一文についても残る(読み手がこの一文を見落とし、§0 の文言だけで「8-016 の取得を止めることも禁止」と誤読する余地)。実害は今回の実測では確認できていない(「聞く」)。
>
> ## 10 回目を起動してよい状態か
>
> 起動しない方がよい。指摘 1 は、監査 48〜59 回目を通じて機械的に強制する形へ直してきた「300MB 累積上限」の安全策そのものが、状態ファイルの破損という単一の障害点によって恒久的かつ無検出のまま無効化されうることを実測で確認したものであり、この回(178件のクローンを伴う長時間ループ)がまさにこの安全策に依存している。直すべき点: 総額ファイルを読む際に内容が非負整数であることを検証し、そうでなければ(空でない限り)安全側に倒す(取得を拒否する、または明示的にエラーで停止し `未判別` に倒すなど、対応はリード/オーナーが決めることを含め)形にすることを推奨する。指摘 2・3 は実行を止める実測上の欠陥ではない。

**リードの処置**:
1: 認める(壊れたときに危険側へ倒れていた)。和のファイルが有って中身が数字だけでない(空を含む)ときは、取らずに終了コード 6 で止まる。無いときだけ 0 から数える。書き込みは一時ファイルに書いてから置き換える。clone の直後に和を足すので、そのあとの手(件数の検査など)で止まっても取った分は数えられ、clone 自体が失敗したときも置き場に残った分を足してから終了コード 128 で止まる。実測(2026-09-24、リード。scratchpad の合成リポジトリ): 正常 0 → 29,228 / 空のリポジトリ(件数の検査で終了コード 1)でも 29,228 → 58,388 に足された / 無い取得元は和が変わらず 128 / 中身 `not_a_number` と空は「not a number; not cloned」で終了コード 6(置き場は作られない)。
2: 認める。置き場が絶対パスでなければ、何もせずに終了コード 6 で止まる。実測: 相対パスで終了コード 6。
3: 認める。起動文 §2 の 1 の直後に、取得の道具の終了コードの読み分け(0・4・5・6・それ以外)と「止めるのは 8-016 の取得だけ」を 1 行のまとめとして別に置いた。終了コード 6 は直さずに 8-016 の取得をやめて書く(リードが直す)。
直した版(起動文の指紋 `292183d28506`)は監査 61 回目で確かめてから印を記録する。


## 監査 61 回目(対象 = fe02b75、起動文の指紋 `292183d28506`)

監査役の出力(逐語。[直す] 1 件・[聞く] 1 件、[止める] 0 件):

> 監査 61 回目の結果。対象コミット fe02b75(ブランチ claude/tools-survey-cat8)。範囲: 監査 60 回目の 3 件への処置だけ。
>
> ## 実測した内容
>
> scratchpad(`.../scratchpad/audit61/`、確認後 `rm -rf "${D:?}"` で削除済み)に合成 git リポジトリ(空でないファイル・1MB超のファイル `big.bin`を含む)を作り、`scripts/cat8_repo_fetch.sh` を URL の行だけ `file://` に差し替えた写し(2 種類: 汎用名 `fetch.sh` とツール名に合わせた `cat8_repo_fetch.sh`)で以下を実行した。
>
> 1. 和のファイルが無い状態で 2 回連続フェッチ → `downloaded_total_before/after` が 0→29,437→58,874 と正しく積算。
> 2. `$dest` を相対パス(`venvs/parent/repoC`)にして実行 → クローン前に `exit 6`(`dest must be an absolute path`)、ディレクトリは作られない。
> 3. 和のファイルの中身を `not_a_number` にして実行 → クローン前に `exit 6`、対象ディレクトリは作られず、和のファイルの中身も変わらない。**2 回連続で打っても両方とも `exit 6` のまま(監査 60 回目が指摘した「壊れた値のまま `git clone` が実行され続ける」危険側の再現は無い)。**
> 4. 和のファイルを空にして実行 → 同様に `exit 6`、中身は空のまま。
> 5. 完全に空のリポジトリ(ファイル 0 件、`git rev-list` の件数検査で失敗)を対象に実行 → `exit 1` だが、**その手前で `add_total` が実行済みのため和は正しく加算されていた**(29,437→58,617)。リードの主張「件数の検査などで止まっても取った分は数えられる」を確認。
> 6. 存在しないクローン元を指定 → `exit 128`、和は加算 0(`58,617→58,617` のまま)、ディレクトリも作られない。
> 7. 追加の境界値テスト: 和のファイルが先頭ゼロ付き数字(`007`)→ 正しく数値として扱われ処理続行(危険側ではない)。和のファイルがディレクトリ → `cat` の失敗で `set -e` により即座に停止(`exit 1`、クローンは実行されない=安全側)。和のファイルが空白 1 文字のみ → `exit 6`(数字判定で正しく拒否)。
>
> 以上より、監査 60 回目の指摘 1・2 への処置(無ければ 0/数字だけでなければ exit 6/一時ファイルへ書いてから置き換え/clone 直後に加算/clone 失敗でも残存分を加算して 128/絶対パスでなければ exit 6)は、実物で成り立つことを確認した。試した範囲(先頭ゼロ・ディレクトリ・空白・2 回連続の破損値)でも、危険側(上限を超えて取ってしまう方向)に倒れる新たな経路は見つからなかった。
>
> ## 指摘
>
> 1. **[直す]** `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50-51` の新設「まとめ」行(終了コード 0/4/5/6/それ以外の読み分け)と `scripts/cat8_fetch_tally.py:75-77` の食い違い — `cat8_fetch_tally.py` の集計行は `終了コード 0 = N / 4 = N / 5 は下の行 / そのほか = N`(77 行目 `r["rc"] not in ("0", "4", "5")`)という 4 分類のままで、この commit で新設された終了コード 6 を認識していない(この commit は `scripts/cat8_repo_fetch.sh` と委任文だけを変更し、`cat8_fetch_tally.py` には触れていないことを `git show fe02b75` で確認済み)。実際に合成ログ(`cat8_step.py` 経由、rc=0 が 1 件・rc=6 が 1 件)を `cat8_fetch_tally.py --prefix th2-net/` に通したところ、クラッシュはしない(`N`/`dl`/`co` は `None` のまま出力され、誤りとしても検出されない)が、集計行では `終了コード 0 = 1 / 4 = 0 / 5 は下の行 / そのほか = 1` と出力され、**rc=6 の行が「それ以外(1・128 など)= 取れなかった」と同じ「そのほか」バケットに含まれる**ことを実測で確認した。一方、委任文の同じ段落(§2 の 1、50 行目)は「取れなかったリポジトリ(終了コード **4・5・6 以外** で止まったもの)」と、rc=6 を明示的に「取れなかった」から除外している(rc=6 は「直さずに 8-016 の取得をそこでやめ、そのことと残りの名前を書く。リードが直す」という別扱い)。個々のリポジトリ行(`repo\trc\t...`)を読めば rc=6 は raw の値として区別できるため実害は限定的だが、集計行の「そのほか」の数をそのまま「取れなかった件数」として使うと、委任文自身が定めた分類(4・5・6 を除く)と食い違う。この区分は N/M の数え違いで 5・8 回目に差し戻された前歴があり、機械の集計と文書の分類語がずれたまま次回以降に持ち越される型(P2 — 記号・分類の意味が資料間でずれている、P8 — 規則は書くだけでは機能しない)に近い。
>
> 2. **[聞く]** 同じ論点の帰結 — rc=6 は仕様上「8-016 の取得をそこでやめる」ため、実際のログに現れる rc=6 の行数は多くて 1 件程度になるはずだが、`cat8_fetch_tally.py` の集計行がこの前提(rc=6 は高々 1 件・別扱い)を検査で強制していない(rc=6 が複数回現れても「2 回打たれて結果が違う」以外のエラーにはならない)。指摘 1 の食い違いを、次回までに `cat8_fetch_tally.py` を直して解消するか、それとも「集計行はあくまで目安で、名前の列挙は別途手作業で行う」という現状の運用のままでよいかは、リード/オーナーが決めることか。
>
> ## 10 回目を起動してよい状態か
>
> 起動してよい。指摘 1 は「直す」(集計ツールの表示上の分類が委任文の新しい終了コード区分と食い違うが、生データ自体は壊れておらずクラッシュもしない)、指摘 2 は「聞く」(運用上の判断)であり、いずれも実行を止める実測上の欠陥ではない。監査 60 回目の指摘 1(和のファイルが破損したときに危険側へ倒れる)・指摘 2(相対パス)は、いずれも実物のテストで解消を確認した。

**リードの処置**:
1: 認める(道具の終了コード 6 を足したとき、集計の道具を直していなかった)。`scripts/cat8_fetch_tally.py` の集計の行を、0 / 4 / 5 / 6 / それ以外(= 取れなかった)に分けた。
2: 認める(機械で見る)。最初の終了コード 5・6 のあとに別のリポジトリが打たれていれば誤りとして止まるようにした。実測(2026-09-24、リード。scratchpad の自作の生ログ: 0・128・6・0 の順の 4 手): 「そのほか = 取れなかった = 1」「6 = 1」、4 手目に「終了コード 5・6(th2-net/c)で取得をやめたあとに打たれている」の誤り 1 件。起動文は変えていない(指紋 `292183d28506` のまま)。
直した道具は監査 62 回目で確かめてから印を記録する。
