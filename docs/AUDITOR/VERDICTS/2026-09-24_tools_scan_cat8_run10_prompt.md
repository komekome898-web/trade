# 監査記録: 道具サーベイ 区分 8 の 10 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md`(初版の指紋 `ad0962fb5bd4`)。9 回目の起動文(監査 45・46 回目を通した版)を元に、範囲を 8-016・8-005・辿る一覧・成熟度の枠組みの原典にし、9 回目の誤りの型への規則を足した。辿る一覧の指示は 2 回目の起動文 §2 の 4 の文をそのまま写した。

## この回に回さないもの

| 何 | 理由 |
|---|---|
| 8-010 E3b・8-014 E3a/E6 と E1b/E3b の段 | 述語の読みの問い(オーナーに聞いている。下の 4 問) |
| freqtrade の §4.0 の表の合わせ直し・8-015 の N の出所の書き直し | 資源化のとき |

## オーナーに聞いている述語の読み(2026-09-24)

1. 8-004: 重複した注文を「拒否する」機能を、E2 の「重複を検出する」に数えるか(8 回目にリードの読みで数えた)
2. 8-014: LLM に「先読みの偏りを見つけよ」と指示する仕組みを、E3a・E6 の「機能」に数えるか
3. 8-014: 候補が同梱・呼び出している別のライブラリ(`skfolio`・`scipy`)の機能を、候補の機能に数えるか
4. 8-010: 学習期間と評価期間を 1 回だけ分ける論文の作りを、E3b の「その時点で知り得ない情報が入らないようにする機能」に数えるか

## 監査 48 回目(対象 = bc8a144、起動文の指紋 `ad0962fb5bd4`)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件・[聞く] 2 件。確かめて不整合の無かった点: 9 回目の検収 §3・監査 47 回目の指摘 1・2・4・5 の処置、§2 への割り当て、2 桁の回の正規表現、読ませる文書の指紋):

> 1. [止める] `docs/DATA/tools_catalog_cat8.tsv` 12 行目(8-011)`段_E6` = `未判別` — Python で台帳の全行・全列(E1a〜E6 の値と段)を機械的に数え上げたところ、`未判別` が残るのは 8-005(全 16 セル)/ 8-010 E3b(値・段)/ 8-011 **段_E6 だけ**/ 8-014(E3a 値、E6 値・段、E1b・E3a・E3b の段)/ 8-016(E3a・E3b・E5 の値・段)の 5 行。このうち 8-005・8-016・辿る一覧・成熟度の枠組みは起動文 §2 の 1〜4 に、8-010 E3b と 8-014 の未判別は起動文 §1「この回に回さないもの」および検収記録の「この回に回さないもの」表・「オーナーに聞いている述語の読み」4 問に、それぞれ 1 回ずつ現れる。**8-011 段_E6 だけがどちらにも一度も現れない**(起動文 §1 の「ほかの行」という無名の受け皿でしか拾えない)。この未判別は監査 47 回目の指摘 1(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` 39・51 行、8-011 E6 段2 の「再確認した」根拠が別の手の生ログだった)を受けてリードが直したものだが、8-010/8-014 のように「述語の読みの問い(オーナー待ち)」ではなく、単に生ログを引き直せば決まる性質の未判別であるにもかかわらず、この回のどのタスクにも割り当てられていない(P3・P5 — 族の全水準の網羅性、数え上げの型)。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md` 44 行(監査 47 回目の指摘 3、E1b 知見#8 `performance_metrics` の使用未確認)の扱い — 同 56 行「リードの処置」は指摘 2 と 3 をまとめて「2・3: 認める…台帳で 8-014 … E1b・E3b の段を `未判別` にした」としか書いておらず、指摘 3 固有の要求(`performance_metrics` が実際にどこかで計算・使用されているかを、同クラスの `validation_history`(知見#5)と同じ厳密さで確かめる)は、起動文のどこにも次に引き継ぐ具体的な作業として残っていない。8-014 は「述語の読みの問い」としてまとめて次回以降に回されており(起動文 §1・§2 の「オーナーに聞いている述語の読み」3)、再開時に「機能の帰属の問い」には答える設計だが、「使用の有無を確かめる」という別種の指摘が同じ棚に紛れて消えている。
>
> 3. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50`(§2 の 1、th2-net 178 件を `git clone --depth 1` で全件取得)— 実測: `https://repos.ecosyste.ms/.../owners/th2-net/repositories` を 3 頁とも取得し 178 件のメタデータの `size`(GitHub 報告値、KB)を合計すると約 323 MB。さらにサンプル 10 件(最大級 1 件 + 無作為抽出 9 件)を実際に `git clone --depth 1` したところ、合計約 421 MB(内訳: `th2-documentation` 45 MB、`viewer` **324 MB・ファイル数 25,999**(サブディレクトリ `webpack-starter` だけで 285 MB)、残り 8 件は 1〜42 MB)。GitHub の `size` メタデータは実際のクローン容量を大きく下回ることもある(`viewer` は報告 34 MB に対し実クローン 324 MB)。10/178 件のサンプルだけで既に数百 MB に達しており、178 件全件では合計が委任文 §10 で禁止された「本体の一括ダウンロード(数百MB以上)」(起動文 §5「委任文 §10 のすべて」で継承)を超える可能性が高いが、起動文にはこの禁止規則との整合(サイズの上限・事前のメタデータ確認による大型リポジトリの除外・分割取得など)についての記述が一切無い。「取れなければ理由を書いて進む」規則(§2 冒頭)は届かない・登録が要る場合のためのもので、容量超過そのものを止める条件にはなっていない。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:50` と `:60` の置き場所の不一致 — §2 の 1 は 178 件のクローン先を「scratchpad の下」とだけ指定するのに対し、§3(書き換えの範囲)は「外部の道具を入れる場所は…`cat8/venvs/` の下だけ」と限定している。読むだけでビルド・実行はしない git clone が「外部の道具」に当たるか(当たるなら `cat8/venvs/` の下に置くべきで §2 の 1 の指定と矛盾、当たらないなら §3 の一文がなぜ「道具」に限定されているかが書かれていない)が起動文からは判断できない。
>
> 5. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run10_prompt.md:52`(§2 の 3、辿る一覧)— 文面は 2 回目の起動文 §2 の 4(`docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:36`)をほぼそのまま写しており、「一覧: <一覧の名前> — 開いた URL — 生ログの行」という**生ログの行を文中に埋め込む**形式のまま残っている。しかし同じ起動文 §2 の一般規則(冒頭近く)は「生ログの行は、文の中に書かず、決まった欄にだけ書く(…監査 34〜39 回目で、文の中から行の番号を読み当てる検査は何度直しても読み落としが見つかったので、読み当てるのをやめた)」と明記し、「要素と段」表・知見表には 7 列目・専用欄を機械検査(`logcol_errors`・`quotes_in_log`)で強制している。ところが `scripts/cat8_ledger.py` の `check-elements`(458・466〜467・638〜639 行、実測で確認)は、辿る一覧の節については `- \`名前\`` で始まる行数 `n_trace` を数えるだけで、埋め込まれた「生ログの行」の書式・実在は一切検査しない。辿る一覧だけが、この回で唯一「文中引用」を許したまま新しい引用規律の外に置かれている。
>
> 6. [聞く] 辿る一覧で見つかった名前をこの回の候補の一覧に載せない扱い(起動文 §2 の 3「候補の一覧には載せない…リードが台帳に `未着手` で足し、次の回に回す」)について、`cat8_ledger.py` に辿る一覧の名前を台帳へ機械的に取り込む経路が無く(実測: `add`/`sync` のコードに `辿る一覧`・`trace` への参照なし。`check-elements` は件数 `n_trace` を報告するだけ)、台帳への追加は完全にリードの手作業に依存する。これは監査 11 回目の指摘 4(候補の一覧からの黙った脱落)と同型のリスクだが、今回も機械的な突合せ(この回の `n_trace` の件数と、次回検収での台帳への `未着手` 追加件数の一致を確認する手段)が用意されていない。

**リードの処置**:
1: 認める。§2 の 5 に 8-011 E6 の段を足した(`hdt/stats.py` の各関数を生ログに印字して決める)。
2: 認める。§2 の 6 に 8-014 の `performance_metrics` の使用の確かめを足した(値・段は決めない。オーナーの答えを待つ)。
3: 認める。§2 の 1 を、取得の量に上限を置く形に改めた: 1 件ずつ `git clone --depth 1 --filter=blob:limit=1m` で取り、全ファイルの一覧を `git ls-tree` で残し、検索し、**消してから次へ**。取った量の和を 1 件ごとに生ログに残し、300 MB を超えたら取得をやめる(委任文 §10 の「数百 MB 以上の一括ダウンロード」を超えない)。1 MB を超えて取れなかったファイルは、画像・バイナリの拡張子なら除外、それ以外は「読んでいない」に数える(M < N なら `なし` は書けない)。
4: 答える。取得物の置き場を `cat8/venvs/th2/` の下に揃えた(§3 の「外部の道具を入れる場所」と同じにした)。
5: 認める。辿る一覧の「一覧:」の行は、生ログの行を `docs/DATA/probes/20260923_tools_8_run10.log:N` の形で行の最後に 1 つだけ書くようにし、`check-elements` が 10 回目以降、その形があるか・その行が生ログにあるかを見るようにした。2〜9 回目の結果は変わらない(8 回目は届いた報告のまま 5 件、処理済み)。
6: 約束する。10 回目の検収で、辿る一覧から出た名前を 1 件ずつ台帳に `未着手` で足すか、足さない理由を書き、**節の名前の数(`n_trace`)と、足した数 + 足さない数の和が合うこと**を検収の記録に書く。
直した版(起動文の指紋 `7f7891408a83`)は監査 49 回目で確かめてから印を記録する。
