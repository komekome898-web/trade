# 検収: 道具サーベイ 区分 8 の 30 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 30 回目の実行(2026-09-26)`(43409 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run30.log`(799,407 バイト)。届いたままのコミット 5e41dc8。起動文の印 `20260923_tools_survey_cat8_run30_prompt.md@c1e68a5c6cf0`。

## 0. 費用と時間

調査班 407,130 トークン・1,026,473 ms(17.1 分)・道具 113 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 30` 0 件 / `check "" run30.log` 0 件 / 過去の節から消えた行 0 / `/root/.m2` 無し、`/root/.cache/pip` 728M のまま。

## 2. 台帳

`import --round 30`: 変わった所 0。リードは値を変えていない。

## 3. 中身の監査(124 回目、owner-auditor)の逐語

> 監査 124 回目の指摘は以下のとおり(全6件、うち止める1件)。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43434`(知見2)・`:43437`(知見5)/生ログ `docs/DATA/probes/20260923_tools_8_run30.log:16`・`:832` — E2・E4のソース選定 `grep -i -E` は全1885ディレクトリ中それぞれ58件・6件を語で一致させたが、生ログには一致リストの実出力が無く(`tee ... | wc -l` の `wc -l` 側だけが印字される)、報告の知見2・5も「7ルートを選んだ」「1ルートを選んだ」とだけ書き、58件・6件という中間の一致数にも、それを7・1へ畳んだ理由にも一切触れていない。起動文§1「選ぶ手と出力を生ログに出す」、監査123回目・指摘5とその処置5(「findとgrepの手を生ログに出させる」)に照らすと、**手**はあるが**出力**が無い。scratchpad(成果物に含まれない一時ファイル `run30_E2_dirs.txt`・`run30_E4_dirs.txt`)を直接開いて確認した限り、今回は58件・6件全部が報告の7・1ルートの子孫で畳み方に誤りは無かったが、これは委任の成果物(報告+生ログ)だけでは確認できない検証であり、「探し尽くした」という否定的主張(未判別)の根拠が生ログの手だけでは裏付けられていない。
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43424,43435,43438` — 出典・知見3・6の「93ファイル・12,665行」「65ファイル・7,870行」を計算したコマンド(`find -type f` 相当)が生ログのどこにも無い。scratchpadで数え直すと数値自体は一致したが、生ログでの裏付けが無い。
>
> 3. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43857` と `:43947` — 43857行で「K12再検査はリードが検収で行う」と明記しながら、43947行では実際のコマンド・出力を貼らずに「K12が0件になり…」と確定的に断定している(数値自体は監査で実測し一致を確認)。O-3に照らし、誰がいつどのコマンドで再確認したかが生ログにも報告にも残っていない。
>
> 4. [直す] `docs/DATA/probes/20260923_tools_8_run30.log:837` — E4ソース側「打ち切りなしで全部読む」の手だけ、他3本(36・332・1273行)にある「keep=Xで切れないことを確認」の明記が無い(内容自体は431行全件が切れずに出ており問題なし。記載形式の不揃いのみ)。
>
> 5. [聞く] 「残り7のまま」(run29検収68行)から「残り7→6」への当て方 — `cat8_ledger.py recount` を打ち直すと現在の「残り」は10件(浅い6+未着手4)で「7」と一致しない。案B適用済みの内訳(どの行が既に外れているか)を示す記録が渡された資料に無い。
>
> 6. [聞く] リードの読みの案の「§4.0 一次資料12・実測25・推定1・未確認5、過半は一次資料か実測」という判定 — 8-032の§4.0全項目を通しで数え直せる資料が今回の対象範囲(30回目節・生ログ・29回目検収)に含まれておらず、案Bの条件(2)が現時点でも成り立つか検証できない。
>
> 止める指摘の有無: あり(項目1)。
>
> なお、生ログ21〜35行目の「keep=3000で3,000字に切れた手」については、報告の知見3(43435行)が「この後の手に上書きされ、根拠には使っていない」と明記し、実際に36行目以降でkeep=100000により431行全件を打ち切りなしで再読していることを確認した(問題なし)。ソースの選び方(E2/E4の語の一覧)・§4.0宣伝詐欺の兆候のx-research手順(WebSearch 5本、site:x.com)・起動文の範囲外への書き込み実行の有無も確認したが、いずれも起動文・スキル定義(`.claude/skills/x-research`)どおりで問題は見つからなかった。4つの検査(check-elements/check/diff/K12)は監査で打ち直し、報告の貼付値と一致した。

## 4. 処置(リード、監査 124 回目)

1. **止める → 直した**。調査班が scratchpad に書いた選んだディレクトリの一覧(`run30_E2_dirs.txt` 58 行・`run30_E4_dirs.txt` 6 行)の全部を、リードが報告の節の末尾の「リードの追記」に貼った(道の頭を省いたもの)。58・6 は報告の 7・1 ルートの子孫(監査役とリードがそれぞれ確かめた)。
2. **直した**。選んだディレクトリの直下のファイルの数を足した数(E2 93・E4 65)を、コマンドとともに「リードの追記」に書いた。行の数(12,665・7,870)は生ログに無いまま(数え直していない)。
3. **答える**。K12 は `check_scan_report.py` の出力の貼り付けの検査で、リードの検収の §1 の打ち直しでは K12 を見ていない。報告の「K12 が 0 件になり」の文は、調査班の貼り付けのあとの自己申告で、監査役が打ち直して一致を確かめた(この監査の結びのとおり)。
4. **受け取る**(形式の不揃いだけ。431 行全部が出ていることは監査役が確かめた)。
5. **答える(案 B で外した行の内訳)**。`cat8_ledger.py recount` は台帳の状態の語だけを数えるので、案 B は数えない。台帳で残りの状態の行は 10(`awk -F'\t' 'NR>1 && ($7=="浅い"||$7=="未着手"||$7=="判別に一次資料が要る")' docs/DATA/tools_catalog_cat8.tsv` → 8-026・8-029・8-030・8-031・8-032・8-033 が `浅い`、8-034・8-035・8-039・8-040 が `未着手`)。このうち案 B の当て方(設計票 §2)で残りに数えないとした行: 8-030 dbt(23 回目の検収 §4)・8-033 Prefect(25 回目の検収 §4)・8-031 Debezium(27 回目の検収 §4)。10 − 3 = 7。この回の 8-032 を足すと 6。**台帳の状態の語には案 B が出ないので、区分 8 の完了の判定の前に、台帳か索引に「案 B で残りに数えない」の印を付ける手を決める**(資源化の作業に入れる)。
6. **答える**。8-032 の §4.0 の表の数え: 28 回目の節(42047〜42859 行)の `Apache Kafka` の §4.0 の行の 5 列目 = 一次資料 12・実測 25・推定 1・未確認 5(43 行)。29 回目に当方データ投入・初回公開日が `実測` になり(初回公開日は、一般に公開された日は未確認、とリードの追記に書いた)、宣伝詐欺の兆候は 29 回目のリードの追記と 30 回目の報告で `未確認`。一次資料か実測は 39 行(= 12 + 25 + 2)。過半。

**8-032 の扱い**: `浅い`。`未判別` は E2・E4 で、2 つとも案 B の記録がある(文書は全部、ソースは語の一覧で機械的に選んだディレクトリの全部を、打ち切りなしで読んだ。当たりの行の数は 28 回目の見積もり)。§4.0 は 43 行中 39 行が一次資料か実測。設計票 §2 の案 B の当て方で、**残りに数えない。残り 7 → 6**(8-026 OpenClaw・8-029 MetaTrader・8-034 Pandas・8-035 Spark・8-039 NumPy・8-040 SciPy)。

## 5. これからの手

31 回目 = 8-034 Pandas(未着手、1 行だけ。24 回目の起動文(Python の道具)の形に、26・28 回目の学び = §6-1 の結論に使う配布物を全部並べる・実行のあとに §6-1 の手があれば実測の値を受け取らない・キャッシュを scratchpad の下に・最初と終わりの外のキャッシュの確かめ、30 回目の学び = 探す手を打ち切らない・ソースの選び方を語の一覧で機械的に決める、を入れる)。
