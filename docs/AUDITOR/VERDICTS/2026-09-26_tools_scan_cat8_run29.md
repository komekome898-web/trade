# 検収: 道具サーベイ 区分 8 の 29 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 29 回目の実行(2026-09-26)`(42860 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run29.log`(66,430 バイト)。届いたままのコミット 3ee31cf。起動文の印 `20260923_tools_survey_cat8_run29_prompt.md@cd0c7b5b9f33`。

## 0. 費用と時間

調査班 349,613 トークン・1,043,702 ms(17.4 分)・道具 104 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 29` 0 件 / `check "" run29.log` 0 件 / 過去の節から消えた行 0。`/root/.m2` 無し、`/root/.cache/pip` 728M のまま、Kafka のプロセスは残っていない。

## 2. 台帳

`import --round 29`: 8-032 の E6 `未判別` → `印`/3。**監査 122 回目の結果を見てから** `set 8-032 E1b=印 段_E1b=2`。`check` 0 件。

## 3. 中身の監査(122 回目、owner-auditor)の逐語

> ## 監査 122 回目 — 道具サーベイ区分8・29回目(Apache Kafka の残り)
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 29 回目の実行(2026-09-26)`(42860行目〜末尾43395行目)/ 生ログ `docs/DATA/probes/20260923_tools_8_run29.log`(コミット 3ee31cf、届いたまま)。起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md`(指紋 `cd0c7b5b9f33`、実ファイルの sha256 先頭12桁と一致・`VERDICTS/2026-09-26_tools_scan_cat8_run29_prompt.md` に記録済み)。台帳 `docs/DATA/tools_catalog_cat8.tsv` の 8-032 行も実測で照合。
>
> ### 決定的な検査で見つかったもの
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42887`(知見8)・`:43277`(§4.0表「宣伝詐欺の兆候」、印=実測) — 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md:22` は「宣伝詐欺の兆候(…**X などの投稿は本文を取って読む**。**検索の要約だけなら `未確認` と書き**、試した手段を書く)」と明記する。実測では `docs/DATA/probes/20260923_tools_8_run29.log:307-318` の3手はすべて `method=websearch`・`rc=NA` で、返っているのは「上位10件は…」という検索結果の要約のみ(個々の X 投稿・記事の本文を `curl`/取得して読んだ形跡なし)。起動文の条件に従えばこの項目は `未確認` と書くべきところを、`実測`・「見当たらない」(解消)と判定している(P1、起動文§1の2)。
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42882`〜`42884`(知見3〜5、E1b/E2/E4)・`:43187`〜`43191`(要素と段の表) 「developer-guide残り14頁を全部読了した」「探し尽くした」 — 実測の探索コマンド(`docs/DATA/probes/20260923_tools_8_run29.log:61`)は `| head -25` で各ファイルの当たりを打ち切っている。ファイルごとに `grep -c` で数え直すと、`config-streams.md` は全体45件中25件のみ表示(20件未検討)、`dsl-api.md` は全体163件中約25件のみ(約138件未検討)、`processor-api.md` は全体34件で、しかもこの手の出力全体が20000字で追加で打ち切られている(`run29.log:150`)ため、head -25 の枠にすら届かず一部のみ表示。第2波の検出・報告語彙走査(`run29.log:174`、`head -15`)も `config-streams.md` の実際の一致数15件と head の上限が一致しており際どい。「印を探し尽くした」「全部読了した」という完了宣言は、この打ち切りの事実と両立しない(P1・P3 の類推 — 「無い/見つからない」は試した範囲の全部を検討した結果でなければ言えない)。この打ち切りによって見落とされた候補の一つが、本監査依頼文中でリードが別途指摘した `dsl-api.md:1142/1183` の `count`(窓ごとの集約)の逐語であることは、本監査で `grep`/`sed` により実測し確認した(内容自体は正確だが、正規の探索コマンドでは拾えていなかった)。
>
> ### 判断が要る問い
>
> 3. [直す/聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:42885`(知見6、E6段3の根拠)・`:43295`(判断に迷った点1) — 段3の根拠として「28回目のE4(kafka-streams-application-reset)の扱いに合わせ」を挙げているが、28回目のE4の印・段判定自体は28回目の検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run28.md` §3項目3・§4処置3で `未判別` に差し戻され失効している。失効済みの先例に「合わせ」て段を確定させる当て方は、設計票 `docs/DATA/surveys/CAT8_DESIGN.md:86`「段は道具の側の記述だけで決める(当方が何を持っているかで決めない)」、および同じ検収 §3項目4・§4処置4(「他候補との整合という道具の記述ではない理由で段を確定させた」を誤りと判定)と同型の当て方に見える。この整合根拠を外しても段3が成り立つか。
>
> 4. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43295`(判断に迷った点1) — E6の段が3か4かを「判断に迷った」としつつ、同じ回の知見表・要素と段の表(`:42885`, `:43193`)では段3を確定値として報告している。起動文§2.2「問いに出した行は、値も段も `未判別` にする(決めてから問いに出さない)」に照らすと、値・段を決めずにおく理由の問いであるにもかかわらず `[それ以外の問い]` に分類し確定値を残している。28回目の検収(`VERDICTS/2026-09-26_tools_scan_cat8_run28.md` §3項目4・5)が同型の分類を問題視した直後の回で、同じパターンが再発していないか。
>
> 5. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43283`(§4.0未確認「コミット数」) — 理由の中に「この回は試していない(…再試行の対象にしていない)」という語がそのまま使われている。起動文§2.5(`docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md:64`)は「『この回では実施していない』…は理由にしない」と明記する。403到達不能という一次的な理由自体は妥当に見えるが、禁じられた言い回しを付記している点は直すべきではないか。
>
> 6. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43285`(§4.0未確認「規模の見積」) — 理由が「大規模実行は…上限を超えうるため試みていない」のみで、起動文§2.5が `未確認` に残してよいと認める2つの型(試した手段と出力を書いたもの/鍵・口座・登録・購入・外送信が要ると言う逐語を引いたもの)のいずれにも明確に当たらない。この理由は許容型か。
>
> 7. [聞く] 本監査依頼文中「リードの読みの案」(E1bを印・段2にする提案、`dsl-api.md:1142/1183` の逐語引用) — この引用を読んだコマンド・出力が `docs/DATA/probes/20260923_tools_8_run29.log` に無く、この監査依頼の本文にも実行したコマンド・出力そのものは貼られていない(引用内容そのものは本監査で `grep`/`sed` により実測し、行番号・逐語とも一致を確認した)。O-3(機構やデータの挙動を主張するときは実行したコマンドと出力を同じ返答に出す)に照らし、この追加の一次資料読みの手続きは満たされているか。
>
> 8. [聞く] 同提案が「22回目のdbt・23回目のGreat Expectationsの読み(§4の扱い)と一貫するか」を問うているが、それらの回の検収記録はこの監査には渡されておらず、本監査では独立に確認できていない。整合の確認は別途、該当検収記録と突き合わせて行う必要がある。
>
> 9. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:43279`・`43287`(§4.0「初回公開日」) — ASF incubator 入り(2011-07-04)を「KafkaがApache Software Foundationのプロジェクトとして最初に公開された日」と書いているが、これは「インキュベーション入り」の日であって「一般公開された日」そのものではない可能性がある(LinkedIn社内での先行公開や、ASF入り前の公開状態は未確認のまま)。この言い換え(インキュベーション入り=最初の公開)は妥当か。
>
> ### 確認できた点(問題なし、参考)
>
> - `check-elements --round 29`・`check "" run29.log`・`git diff -U0 HEAD | grep '^-[^-]' | wc -l` はいずれも本監査で打ち直し、報告の貼り付けと一致(0件/0件/0)。
> - 台帳 `docs/DATA/tools_catalog_cat8.tsv` の 8-032 行は、E6 のみ `未判別→印/3`、他は `未判別`(E1b・E2・E4)・`なし`(E3a・E3b)・`印`(E1a段3・E5段2)で、依頼文の説明と一致(リードは値を変えていない)。
> - 当方データ投入(`run29.log:270-306`)・§6-1 の順序(この回は §6-1 の手を打ち直しておらず、投入は 28 回目完了済みの §6-1 のあとの実行として扱われている)・Kafka プロセスの停止確認(`pgrep_rc=1`)は実測で確認済み、新規配布物・依存の取得も見当たらない。
> - developer-guide 18頁のうち今回読んだ14頁の一覧・数え直しは、28回目の既読3頁(+ライセンスのみの `_index.md`)と合わせて18頁に一致。
> - K13(33件)・K1/K2 の不整合はいずれも21〜28回目までの既存行で、この回(42860行目以降)からの新規追加は0件(実測で確認)。
> - 起動文の範囲外への書き込み・実行の痕跡は生ログに見当たらない(`docs/DATA/SCAN_2026-09-23_tools_cat8.md` と `run29.log` 以外への書き込みコマンドなし)。
>
> **止める指摘の有無**: あり(項目1・2の2件)。

## 4. 処置(リード、監査 122 回目)

1. **止める → 直した**。宣伝詐欺の兆候は `未確認`(試した手段 = WebSearch 3 本、要約しか読んでいない)。報告の節の「リードの追記」。
2. **止める → 受け取らない(E2・E4 は案 B の記録が足りない)**。探す手が `head -25` と出力の 20,000 字で打ち切られていたので、「探し尽くした」「全部読了した」は受け取らない。その打ち切りで見落とされていた `dsl-api.md` の `count` の逐語を、リードと監査役がそれぞれ `sed`/`grep` で確かめた(処置 7)。**E1b は `印`・段 2**(利用者が決めるのは窓とキーで、件数を数える計算の中身は Kafka Streams が持つ。窓ごとの件数は当方の特徴量と同じ種類の出力。E1b の読みは 23 回目の検収 §4 の処置 1 = dbt の MetricFlow、14 回目の Great Expectations の `ColumnMean` の行 `docs/DATA/SCAN_2026-09-23_tools_cat8.md:17493` と同じ。22 回目の dbt のモデルは、計算の中身を利用者が全部書くので数えなかった。`aggregate`・`reduce` は利用者が関数を渡すので数えない)。段は、呼べば出力を出すが判定しないので 2。**E2・E4 は、打ち切りのない読み直しの回を立てる**(30 回目)。
3. **答える・直した(E6 は段 3 のまま、根拠を差し替える)**。失効した 28 回目の E4 に合わせる根拠は使わない。道具の側の記述で決める: 検査の対象は、Kafka の「ストリームズグループ」の仕組みの中で動く利用者のトポロジ(Kafka Streams の枠組みの中のもの)で、§4.1 の段 3「その道具の枠組みの中のもの」に当たる。段 4 の「一般の Python の関数・CSV など広く使われる形式」を外から持ち込むことを言う逐語は無い。
4. **答える**。分類は `[値・段の問い]` が正しかった(調査班の誤り、28 回目の検収で問題にした型の再発)。値・段は処置 3 のとおり決めた。
5. **直した**。理由を「403・バッジ無し」だけにした(リードの追記)。
6. **答える**。委任文 §6-6「1 件の実行は数分まで」で、456 日分の規模の実行はこの環境の決まりの外。規則が止めているので、`未確認` に残してよい型として扱う(26 回目の検収で PostgreSQL の実行をリードが止めたのと同じ、決まりによる止め)。
7. **直した**。リードが打ったコマンドと出力を、報告の節の「リードの追記」に貼った(`sed -n '1142p;1183p' docs/streams/developer-guide/dsl-api.md`)。
8. **答える**。処置 2 のとおり、23 回目の検収 §4 の処置 1 と 14 回目の節の 17493 行を引いた。
9. **直した**。2011-07-04 は Apache のインキュベーションに入った日で、一般に公開された日は `未確認`(リードの追記)。

**8-032 の扱い**: `浅い`。`未判別` は E2・E4。案 B の「読んだ範囲」が打ち切りで足りない。**残りに数える。残り 7 のまま**。

## 5. これからの手

30 回目 = 8-032 Kafka の E2・E4 だけ(打ち切りのない読み直し。`head` で切らない、出力が長ければファイルに書いてから読む)。§4.0 の宣伝詐欺の兆候は、X の投稿の本文を取るか、`未確認` のまま試した手段を書く。
