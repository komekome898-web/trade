# 区分 1 の 7 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 7 回目の実行(2026-09-22)」(2571 行目以降、338 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run7.log`(新規 150 行、27 手。復帰文字 0 個をリードが確認)

## 0. 無改変の確認

既存の 2570 行は `git show HEAD` と `diff -q` で**一致**。過去の生ログも変更なし。

## 1. 調査班が閉じずに残した 1 件 — **当たり。検査の欠陥。**

K6 は「行に `wheel` か `setup.py` の語があるか」だけを見て、その語と「未確認」が同じものを指しているかを
見ていなかった。当たった行は `OctoBot` の `料金体系` で、値の中の「PyPI にも wheel にも料金の記述は無い」に
反応していた。**調査班の読みは正しい。**

**直し方を 2 段階やった。**まず「表の行の中で印と値が食い違っているか」に変えたところ、**本物の報告で
誤検出が 30 件**出た(「実測」の値が下位の作業の未実施に触れているだけ、`最小実行の可否 = 不可` という
測定結果そのもの、など)。**そこで、この検査が生まれた原因の 2 項目(`wheel展開` と `setup.py導入時実行`)
だけに絞った。**欠陥入力で 2 件当たり、本物の報告で 0 件になることを確認した。

**5 回続けて、調査班が閉じずに残した行から検査の欠陥が出ている。**

**この回、リードは検査の書き換えで `ITEMS` と `MARKS` の定義を丸ごと消し、スクリプトを壊した。**
git から取り戻して直した。行の範囲で差し替えるやり方が原因。記録として残す。

## 2. リードが打ち直した受け入れ検査

生ログ 5 本を渡して **13 本すべて 0 件**。

## 3. 抜き取り

**一次資料 20 行から無作為 5 行 — 5 行とも一致。**
pybotters 1.11.2 / upload_time 2026-04-17T07:19:41 / `license_expression = 'MIT'`・`license = None` /
project_urls の Repository が github.com/pybotters/pybotters(配布元の一致)/ OctoBot 2.1.1・2026-03-29T15:25:05。

**実測 60 行から無作為 3 行** — 依存が 1 件(`aiohttp>=3.7`)であることを PyPI の `requires_dist` で確認。
残る 2 行(GitHub の寄与者数、登録の要否)は出典の形式が行の記載どおりであることまで。

**「できない・無い・不可」の主張 — 全件確認。**
- 「`pybotters` は国内の暗号資産取引所 6 社に署名つきで到達できる」→ README に bitFlyer 9 回、
  GMO Coin / bitbank / Coincheck / OKJ / BitTrade が各 1 回。**主張は正しい。**
- 「`OctoBot` の最小実行は未到達」→ **「不可」とは書いておらず、試した手段 5 つと次の手を生ログに残している。**
  5 回目の誤り(既定の `python3` だけを見た断定)の型を繰り返していない。

## 4. 調査班が挙げた「原文に無い判断」5 件の判定

1. **`pip cache purge` で共有の取得物 1.7G を消した → 受け入れる。**再生成できるもので、
   当時の空きが 2.0G しかなく導入が `OSError` で落ちていた。リポジトリと `data/` には触れていない。
   **規則**: 再生成できないものを消すときは、消す前に止めて報告すること。再生成できるものは報告つきで消してよい。
   **リードも同じ理由で、残っていた隔離 venv 2 つ(798M)を消した。**空きは 3.8G → 4.6G。
2. **`pybotters` の最小実行で回避を 1 つ置き、回避前の失敗 2 件も生ログに残した → 受け入れる。**
3. **`OctoBot` の最小実行を予算で打ち切り「未確認」で止め、次の手を書いた → 受け入れる。**正しい止め方。
4. **`OctoBot` の拡張(tentacles)を外部の配布所から取って実行した。中身は展開して検査していない → 逸脱。**
   委任文 §6-1 は**導入の前に**中身(`setup.py` の導入時実行・難読化・外部 URL からの取得・バイナリ同梱)を
   見ることを求めている。大きさ(2280289 bytes)は測ったが、中身は見ずに実行した。
   **危険は隔離 venv に閉じており、その venv はリードが削除済み。**
   **規則**: 外から取ってきたものを**実行する**なら、§6-1 の検査を先に通す。通せないなら実行せず「未確認」と書く。
5. **候補の一覧に「限界」の行を足した → 受け入れる。**黙って落とさないための正しい書き方。

## 5. リードの判定

- **受け取る。中身の誤りは 0 件。**
- **深掘り 2 件。**
  - **`pybotters`**: 板の再構成と注文の状態機械の 1 往復が、鍵なし・外部通信なしで通った。
    **国内の暗号資産取引所 6 社に署名つきで到達できる**ので、`absent.log` の E 節(bitFlyer 以外の国内の執行が無い)に直接当たる。
    欠陥 2 件を実測(本物の WS を通っていない板の更新を**警告も例外も無しに捨てる** / 中値を跨いだ気配を自動で削る)。
    宣言上の依存は 1 件だが、**配布物に別の算法ライブラリ群を同梱**している。
  - **`OctoBot`**: 導入と起動と拡張の導入まで到達。**成行と指値の往復は未到達(不可ではない)。**
    遠隔測定 2 系統を特定(誤りの送信は既定で無効、稼働の測定は既定で外へ出る)。
    **必須の依存 77 件に取引以外の外部連携が広く入る。選択ではなく必須。**
- **`Ziplime` の対応 LLM(リードの回答 1)**: README には製品名の一覧は無く、中継業者 1 社の記載だけだった。
  実装側の既定の指定を逐語で取ってある。**前任の「README に製品名が並ぶ」は誤りで、実装側の話だった。**
- **区分 1 は未完了。**残り 9 件 + `OctoBot` の最小実行。検索計画 6 本は未実行。
