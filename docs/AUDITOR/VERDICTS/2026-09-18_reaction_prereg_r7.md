# 監査(owner-auditor)— 段 A 事前登録への **7 回目(走行前の再監査)20 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction.py`(以下 REACT)、`scripts/o3c_reaction_judge.py`(以下 JUDGE)、
試験 3 本(`tests/test_o3c_reaction.py` / `tests/test_o3c_reaction_judge.py` /
`tests/test_audit_gates_wired.py`)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `..._r2.md`(23 件 + ア〜エ)、3 回目は `..._r3.md`(23 件)、
4 回目は `..._r4.md`(22 件)、5 回目は `..._r5.md`(19 件)、6 回目は `..._r6.md`(18 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 4(1〜4)/ 直す 11(5〜15)/ 聞く 5(16〜20)= 20 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない。§3 の 6)。
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` は
1 行も触っていない。`regen_hook_manifest.sh` も走らせていない。**
**コミットもプッシュもしていない。**
**今回は `scripts/o3c_reaction.py` を変えた**(リードの委任文面「**今回は `scripts/o3c_reaction.py` を
変えてよい(決定 1・20 の分だけ)**」)。**変えたのは開封前の関門だけで、表を作る計算・列・乱数の
消費は 1 行も変えていない。****標本 4 本を走らせ直し、全列一致を確かめた**(§3 の 4)。

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 20 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r7.md` を新設(逐語を一字も変えずに貼り、20 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r6.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r7.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| REACT の `check_approval` に開封前の関門を足す | リードの決定 1・18・20 の逐語(§2 の 1 に全文) |
| JUDGE と試験を直す | リードの決定 2・3・6・7・11 の逐語(各件に引いた) |
| **標本 4 本を走らせ直し、全列一致を pandas で確かめて報告に貼る** | リードの委任文面「**変えたら標本 4 本を `--mode sample --seed 1 --mmr 0.004` で走らせ直し、table.csv / table_mixed.csv の全列一致を pandas で確かめて報告に貼る**」 |
| §14.6 に「2 度目・3 度目の走らせ直し」を足す | リードの委任文面「**§14.6 に「2 度目・3 度目の走らせ直し」の記録を足す = 決定 5**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| **関門・台帳・`.claude/` に触らない** | リードの委任文面「**`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/` は触らない(`regen_hook_manifest.sh` も走らせない)**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 走らせ直しの突き合わせ / 関門の通る側・止まる側 / 走査の出力 / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 7 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**20 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 9 つ目の表(19 行)に明記した**(PREREG:320〜353)。

**作業中に、原文に無い判断が必要になった点が 3 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書いた**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(7 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(6 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r6.md` + `scripts/o3c_reaction_judge.py` + `scripts/o3c_reaction.py` + テスト
>
> ## 監査(7 回目・走行前の再監査)— `owner-auditor` の返答
>
> 1. **[止める]** `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:2940` vs `scripts/o3c_reaction.py:179-222` — §14.1 は「**`--approval L-NNN` は `docs/OWNER_LOG.md` に行頭 `| L-NNN |` の行が実在しないと非 0 で止まる**」と書き、`check_approval` が見るのは**その行の実在だけ**である(**実測**: 関数に §14.4 の欄との突き合わせは無い)。一方 §14.6 の新しい機械は `params.approval` を§14.4 の欄と突き合わせるが、**それは 6 本を走らせた後の「読み」の段である**。**間違った(あるいは古い)L 番号のまま §14.1・§14.2 の 6 本を走らせても、456 日を 6 回開け終わるまで何も止まらない。**指摘 2 の処置が閉じたのは読みの側だけであるのに、§14.1 の注(:2919-2926)は「閉じた」と読める書き方をしている。**開封の前に承認番号を突き合わせる機械は、どこに書かれているか**(P-8、A-6)
>
> 2. **[止める]** PREREG:3575-3579 vs `scripts/o3c_reaction_judge.py:1472-1499, 1507-1534` — §14.6 は「**破れた走行に依存する判定は「不明(検査で止まった)」として結果に書き、1 周目はそこで終わる**」と決めた。しかし実装は `params` / #14 / 軸の作り方のどれか 1 つでも破れると「**表を 1 枚も書かずに終わる**」ので、**「破れた走行に依存する判定」は 576 行すべてになる**。感度 1 本の食い違いでも同じ(:3547「**`--sens` を外して判定の表だけ出すことはしない**」)。**456 日を 6 本開けたうえで 1 行も出さずに 1 周目が終わる、という帰結は走行の前に決まっているか。そのとき §10.3 の「なぜ」は何を材料に書くのか**(A-6、`research-protocol` §0.2)
>
> 3. **[止める]** `tests/test_o3c_reaction.py:868-881` vs PREREG:3476-3477 — 本走行が `params.approval` に**実際の L 番号**を書くことを測る試験が無い。今回足した `test_run_summary_records_the_approval_number_passed_to_the_run` は標本の走行を読み、`assert params["approval"] is None` と、**出力が無ければ `pytest.skip`** で終わる。**`--mode full --approval L-NNN` で L 番号が書かれる経路は 1 度も測られていない。**この経路が壊れていると、指摘 1 の帰結(全 576 行が読めない)に直結する。**この書き込みは何で確かめたか**(P-3、O-3)
>
> 4. **[止める]** PREREG:2942-2953 — 「**`scripts/o3c_reaction.py` は いまは `summary.json` に書いていない**(**実測** 2026-09-18: … `params` の鍵は … **18 個** …)」「**リードが開封の前に足す**」と書いてあるが、**実測**(本監査 2026-09-18): `backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json` の `params` は **19 鍵で `approval` が入っている**(コミット `8bbce48` で `scripts/o3c_reaction.py:1965` に追加済み)。**凍結した文書の中で「実測」の印を付けた記述が、同じコミットの現物と違う。オーナーはこの版を読んで開封の可否を決める。何をもって「実測」と書いたか**(P-2、O-3)
>
> 5. **[直す]** PREREG:3398,3400-3415 — §14.6 の表は「**`scripts/o3c_reaction.py` | 1 列だけ足した(下)。それ以外は変えない**」と書き、下の注も `matched_liq_id` 1 列の話しかしていない。**実測**: 変更は 2 件(`matched_liq_id` 列 + `params.approval`)で、**標本 4 本が 2 度目に走らせ直され `MD5SUMS` が 4 本とも変わっている**(`git show --stat 8bbce48`)。MD5SUMS は :3619「**結果を後から差し替えていないことを見るため**」の機械である。**2 度目の再走行と突き合わせの結果は、なぜ本書に記録されていないか**(P-3)
>
> 6. **[直す]** PREREG:2351 vs :2337 — 指摘 7 の処置は感度の表だけ見出しを `MDE(参考。族の α に入らない)` に替えたが、同じ表(:2349-2352)は **`observation_only.csv` に `MDE(α=0.05/576)` を残している**(**実測**: judge:1095-1101 の `OBS_HEADER` は `MDE_COL` をそのまま持つ)。`observation_only.csv` の 2,208 行も **576 にも α にも F1 の読みにも 1 行も入らない**(§10.2)。**族に入らない行に族の α の名前を付けた列を出してよいか、という指摘 7 の問いは、この表には当たらないのか**(P-2)
>
> 7. **[直す]** `scripts/o3c_reaction_judge.py:1591,1642-1643` vs PREREG:2457-2460 — §10.3 は欄を 2 つに分け、「**指定した反復回数に満たないセルの一覧(検定したセルだけ)**」と「**群が空 / n < 30 で検定していないセル数**」を別物とした。しかし同じ辞書の隣にある「**最小**」「**最大**」は `reps_used_all`(`judge_rows` 全部)から取るので、**群が空で本数 0 のセルを含む**。標準出力にも「最小」がそのまま出る(judge:1862)。**指摘 12 の「2 つを分ける」は、なぜ最小・最大には当たらないのか**(P-2、P-3)
>
> 8. **[直す]** PREREG:1821 — 「**この表はオーナーに見せる(§14.4 の「待つもの」の 7)**」。指摘 16 の処置で待つものは番号から名前(ア〜ソ)に替わっており、**番号の 7 はもう存在しない**(該当は **ス**)。**残った番号参照はこの 1 行だけか**(P-2、P-5)
>
> 9. **[直す]** PREREG:1958 — 「**97 日は連続した 1 つの塊なので、この欠測は期間の偏りを伴う**」。同じ §8.6 の走査の出力(:1887-1889)は **3 つの塊(1 日 + 89 日 + 7 日)**を出しており、:2637 は「**前版は「1 つの連続した塊」と書いていたが、2023-11-19 が離れている**」と自分で訂正している。**この 1 行は訂正から漏れていないか**(P-2、P-5)
>
> 10. **[直す]** PREREG:1906-1918 / :3062 / :3077-3086 — §8.6 の欄は「**取り直しの結果(リードが書き足す): (まだ無い。別の委任先が試行中。)**」、§14.4 の ケ も「**取り直しの結果(別の委任先が試行中)を待たずに**」のままである。**実測**: 取り直しは終わって `docs/DATA/probes/20260918_cm_metrics_missing_days.log` がコミット `7091c28`(本処置のコミットより前)に入っており、**欠測 99 日 × 2 = 198 件すべて 404 / S3 キー一覧に不在 / monthly ツリー無し / REST 451 / 第三者は鍵が要る / オーナー PC は未確認**まで書かれている。**オーナーに出す凍結文書が、既に在る取り直しの範囲と方法を持たないまま「試行中」と書いてよいか**(P-1、P-3)
>
> 11. **[直す]** PREREG:3480 vs judge:1483-1490 — §14.6 の `params` の表は `--sens NAME=DIR` の行に `mmr` / `seed` / `bin_pct` / `match_order` を要求するが **`approval` を要求していない**。実装も感度 4 本には `approval` を渡さない(`settings=True` のみ)。ところが §14.2 は「**`--mode full --approval L-NNN --seed 1 --mmr 0.004` は 6 本とも同じ**」と書き、**感度 4 本も同じ 456 日を開ける**。**判定 2 本だけ承認番号を突き合わせると決めたのは、どこか**(P-3)
>
> 12. **[直す]** PREREG:2296 vs judge:1095-1101 — §10.2 は観測のみの表を「**同じ形で出すが、`t` の列と `判定` の列を置かない**」と書くが、実装の `OBS_HEADER` は **`ブートストラップSE` / `CI下限` / `CI上限` / `バー近傍` / `有限な複製の本数` の 5 列も落としている**(落とすと書いてあるのは `有限な複製の本数` だけ = :2169)。**落とす列の一覧は文面と実装のどちらが正か**(P-3)
>
> 13. **[直す]** PREREG:3646-3649 — §14.6 は試験を「`tests/test_o3c_reaction_judge.py`(45 件 → **53 件**)と `tests/test_audit_gates_wired.py`(**12 件のまま**)」としか書かない。**実測**(本監査): 3 本で **116 件収集**、内訳は `test_o3c_reaction.py` **51 件**(承認の記録の試験を 1 件足した)/ judge 53 / wired 12。リポジトリ全体は **2,098 件**。**走行の道具側に足した試験が本書に無いのはなぜか**(P-5)
>
> 14. **[直す]** PREREG:3603-3606 — 「**`scripts/_research_audit_gate.py` の `WIRED` の一覧への追記は行えていない。このファイルは指紋の台帳に載っており、変更するとフックが全操作を拒否する**」。**この「全操作を拒否する」は何を試した結果か。**`CLAUDE.md` §3 / §5.0b は「この環境ではプロジェクト側のフックが 1 本も走っていなかった」実測と、その後の復旧を記録している。**拒否されることを実際に試したのか、それとも設定の読みか**(O-3、P-10)
>
> 15. **[直す]** PREREG:3062 ケ — 「**取れた分は D の被覆に足す(取り直した日の zip を読む経路はリードが決める)**」。取り直しの結果は 0 日(上の 10)なので、この分岐は空である。**空と分かっている分岐に「経路はリードが決める」という未決の手順を凍結文書に残すのは、走行の後に読む人に何を伝えるか**(P-3)
>
> 16. **[聞く]** PREREG:3070-3076 — 委任先は「**§13 の「報告する変更」の表の並び順のまま ア〜ク を振った**」ので、**ア〜オ = 報告 5 件 / カ〜ク = 答えが要る 3 件**になっている。一方リードが返答 062 で出す「答えが要る 3 件」は**判定語・F1 の条件・対照の作り方**(= 本書では **エ・オ・カ**)であり、**一致していない**。本書自身が「**リードの返答の分け方と食い違ったら、リードの返答が正で、本書のこの表を直す**」と書いている。**オーナーが同時に見る 2 つの一覧の食い違いは、報告を出す前に直すか**(P-5)
>
> 17. **[聞く]** PREREG:3129-3133 — 「**凍結した道具 = 開封のときの `scripts/o3c_reaction_judge.py` のコミットのハッシュ**」「**報告するのは §13 の表と待つものが変わったときだけ(道具の直しだけで両方が変わらない回は、追加の報告を出さずに開封へ進む)**」。**= オーナーが応答する時点の道具と、開封のときの道具は別版でありうる。**6 回の監査はいずれも道具を直しており(:3149「**5 回とも「読みの道具」が直っており、6 回目でも直っている**」)、7 回目も直る見込みである。**開封は一度きりである。オーナーは何の版の道具を承認したことになるか**(A-6、P-9)
>
> 18. **[聞く]** PREREG:3119 vs judge:591-616 — 読みの機械が保証するのは「**事前登録 §14.4 の欄の文字列と `params.approval` が同じ**」までで、走行側は「**その L 行が `OWNER_LOG.md` に在る**」までである。**その L 行が返答 062 への応答であること、L-199 より後であることを検査する機械は 1 つも無い。**欄はリード自身が書き写す。**指摘 2 の「報告への応答が来ていなくても通る」は、何をもって閉じたと言えるか**(P-8)
>
> 19. **[聞く]** PREREG:3760-3768 — 走査は「有効 20 / 差なし 15 / 陰性 33」を出し、その内訳の分類は「**目視**(リードではなく委任先が読んだ)」「**この分類に機械は掛かっていない**」と自分で書いている。**この文書はオーナーに出る納品物である。委任先の目視の分類を、誰がどこで確かめるか**(P-10)
>
> 20. **[聞く]** PREREG:2749-2759 / :3579 — §12 は「**判定区間を一度だけ開ける」は完全には守れていない**」(4 本)と書き、§14.6 は「**それでも再走行が要ると判断したら、新しい開封として §3.1 に数える。オーナーの決定が要る**」と決めた。**§3.1 に数える上限は書かれていない。**「一度だけ」を守る機械は無い(:3584-3586「**`check_approval` は承認行の実在しか見ず、開けた回数は見ていない**」)。**何本目までなら数えて進めてよいかは、オーナーが決めることではないか**(A-6、P-9)
>
> 判定: 止める
>
---

## 2. 20 件の処置

**行番号は処置後の PREREG / REACT / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**20 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] 開封の前に承認番号を突き合わせる機械が、どこにも書かれていない

**処置: 直した(機械を足した)。**リードの決定(逐語):
「**`scripts/o3c_reaction.py` の `check_approval` を `--mode full` について次の形にする:
(a) 事前登録 `REACTION_PREREG_2026-09-18.md` §14.4 の行「応答の L 番号: L-NNN」を読み、
欄が「(まだ無い)」なら「[止め]」、`--approval` の値がその欄と一致しなければ「[止め]」、
(b) その番号が 199 より大きくなければ「[止め]」(L-199 は「1 = a、9 = a」への応答であって
報告 062 への応答ではない)、(c) `docs/OWNER_LOG.md` に行頭 `| L-NNN |` の行が実在(従来どおり)、
(d) `--out-dir` が事前登録 §14.1・§14.2 の 6 つのパスのどれかでなければ「[止め]」、
(e) `--out-dir` が既に存在すれば「[止め]」(再走行・上書きを機械で止める = 「一度だけ開ける」の機械。
開けられる回数の上限 = 6 本、それ以上はこの機械を変えないと走らない)。
事前登録 §3.1・§14.1・§14.4・§14.6 に書く。テスト(一時ファイルで事前登録と OWNER_LOG を
差し替えて、通る側・止まる側の両方。迂回旗を作らない)。**」

- **答え(「どこに書かれているか」)**: **どこにも書かれていなかった。**
  **監査役の実測のとおり、前版の `check_approval` は (c) だけを見ていた。**
  **読みの側の突き合わせは 6 本を走らせた後の段なので、間違った L 番号のまま 456 日を
  6 回開け終わるまで何も止まらなかった。**
- **REACT に定数と関数を足した**: `PREREG`(REACT:176)/ `APPROVAL_FIELD_RE`・`APPROVAL_VALUE_RE` /
  `APPROVAL_MIN_L = 199`(REACT:188)/ `FULL_OUT_DIRS_REL`(6 つ。REACT:190〜199)/
  `read_approval_from_prereg()`(REACT:201〜225)。
- **`check_approval` に (a)〜(e) を足した**(REACT:249〜343。関門の本体は 296〜343)。**`main()` から `--out-dir` を渡す**
  (REACT:2297。`check_approval(a.mode, a.approval, days=days, out_dir=out_dir)`)。
- **迂回する旗は作っていない**(`--force` / `--skip-approval` / `--allow-reopen` / `--no-gate` は
  1 つも無い。**試験がそれを測る**)。**`prereg` と `allowed_out_dirs` は関数の既定引数で、
  `main()` はどちらも渡さない**(既存の `owner_log` と同じ形)。
- **PREREG に書いた**: §14.1 の関門の表(PREREG:3094〜3128)/ §3.1 の開ける回数の上限
  (PREREG:713〜729)/ §14.4 の回数(PREREG:3316)/ §14.6 の直した点の表(PREREG:3968〜3978)。
- **試験**(`tests/test_o3c_reaction.py`): `test_full_mode_gate_passes_when_all_five_checks_are_met`
  (**通る側**)/ `test_full_mode_gate_stops_on_each_of_the_five_checks`(**止まる側 7 通り**)/
  `test_full_mode_gate_has_no_bypass_flag_and_is_wired_into_main` /
  `test_the_approval_field_is_read_from_the_real_prereg`。
  **既存の `test_full_mode_requires_an_existing_owner_log_line` も (c) だけを見る形に直した。**
- **実測**(通る側・止まる側)は §3 の 5 に貼った。

### 2.[止める] 感度 1 本の食い違いで 576 行すべてが読めなくなる

**処置: 直した。**リードの決定(逐語):
「**判定 2 本の検査(params / #14 / 軸の作り方)が破れたら、表は書かないが `stopped.txt`
(破れた検査・走行・理由)を出力先に書く。感度の走行の検査が破れたら、その走行の表だけ書かず
`summary.json` に記録し、判定の表は書く(`--sens` は 4 本とも必ず渡す。1 本でも欠ければ「[止め]」)。
§14.6・§10.3 を直す(「なぜ」の材料 = `stopped.txt` と summary の記録)。テスト。**」

- **答え(「1 行も出さずに 1 周目が終わる帰結は走行の前に決まっていたか」)**: **決まっていた。**
  監査役の読みのとおりで、**前版は感度 1 本の食い違いでも判定の 576 行を 1 行も出さない形だった。**
- **JUDGE を分けた**: 判定の側(判定 2 本 + その標本)と感度の側で検査の結果を別に集め
  (JUDGE:1553〜1663)、**判定の側が破れたら `write_stopped()` で `stopped.txt` を書いて終了コード 1**
  (JUDGE:1323〜1361、1637〜1646)、**感度の側が破れたらその走行を落として `summary.json` に記録**
  (JUDGE:1649〜1663、1822〜1836)。
- **`--sens` は §14.2 の 4 本を必須にした**(`SENS_REQUIRED`。JUDGE:180、1516〜1530)。
  **1 本でも欠ける / 4 本に無い名前が混ざれば「[止め]」で表を 1 枚も書かない。**
  **理由はリードの決定の括弧書きのとおり**(感度で止めない形にすると「渡さなければ検査もされない」
  抜け道が空く)。
- **感度の走行が読めないとき(ディレクトリが無いなど)も「その走行の検査が破れた」として扱う**
  (JUDGE:1499〜1513)。**判定の 2 本とその標本は従来どおり読めなければ落ちる。**
- **PREREG に書いた**: §10.3 の「なぜ」の材料の表(PREREG:2549〜2571)/
  §14.6 の止め方の表(PREREG:3822〜3861)/ 出力の表に `stopped.txt` を足した(PREREG:3949)。
- **試験**: `test_all_four_sensitivity_runs_must_be_passed` /
  `test_a_broken_sensitivity_run_does_not_stop_the_judgment_tables` /
  `test_stopped_txt_records_which_check_broke`(**`stopped.txt` に判定語が 1 つも無いことも測る**)。
  **既存の試験 2 件(`..._run_dirs_must_match_their_names` の (4) と
  `..._frozen_run_settings_are_checked_on_the_sensitivity_runs_too`)は新しい止め方に合わせて直した。**
- **§4 の (1) に、この決定で必要になった判断を 1 件書いた**(試験の走らせ方)。

### 3.[止める] 本走行が `params.approval` に実際の L 番号を書くことを測っていない

**処置: 直した。**リードの決定(逐語):
「**`run_table` を合成データで呼ぶ既存の試験(あれば)に `approval="L-999"` を渡して
`params["approval"] == "L-999"` を測る試験を足す。無ければ `main` を
`--mode sample --days <標本 1 日> --approval` 無しで呼ぶ経路ではなく、`run_table` に直接渡す
最小の試験を書く(データは標本の 1 日を使ってよい。所要 10 秒程度なら可)。**」

- **答え(「この書き込みは何で確かめたか」)**: **前版は何も確かめていなかった。**
  監査役の読みのとおり、前版の試験は標本の出力を読んで `params["approval"] is None` を測り、
  出力が無ければ `pytest.skip` で終わっていた。
- **`run_table` に直接 `approval="L-999"` を渡す試験を足した**
  (`tests/test_o3c_reaction.py::test_run_table_records_the_real_approval_number`)。
  **標本 6 日のうち 1 日だけを渡す**(`react.SAMPLE_DAYS[0]` = 2023-06-25。**判定区間は開けない**)。
  **返り値と、書かれた `summary.json` の両方で `params["approval"] == "L-999"` を測る。**
- **所要**: **この 1 件で約 5 秒**(**実測**。ファイル全体は §3 の 1)。
- **PREREG §14.1 の注に試験の名前を書いた**(PREREG:3143〜3146)。

### 4.[止める] 凍結した文書の「実測」が同じコミットの現物と違う

**処置: 直した。**リードの決定(逐語):
「**§14.1 の「いまは書いていない / 18 鍵 / リードが開封の前に足す」を、現物(コミット 8bbce48 で
足した。params 19 鍵、`approval` あり)に合わせて書き直す。**」

- **答え(「何をもって「実測」と書いたか」)**: **6 回目の処置の途中の状態を測って書き、
  同じ処置の中で `scripts/o3c_reaction.py` に `approval` が足された後に書き直していなかった。**
  **監査役の実測のとおり、現物は 19 鍵で `approval` が入っている。**
- **§14.1 のこの段落を書き直した**(PREREG:3129〜3146)。
  **19 鍵を全部並べ、「標本の走行は承認なしなので `null`」と書いた**(**実測** 2026-09-18)。
- **「リードが開封の前に足す」は消した**(足し終わっているため)。

### 5.[直す] 2 度目の再走行と突き合わせが本書に記録されていない

**処置: 直した。**リードの決定(逐語):
「**§14.6 の変更の記録を 2 件(`matched_liq_id` 列 / `params.approval`)+ 今回(決定 1 の関門)に直し、
走らせ直しの突き合わせの出力を貼る(2 度目: 前の出力との比較は table.csv / table_mixed.csv 全列一致、
summary.json は `elapsed_sec` と `params.approval` の追加だけ、MD5SUMS は summary.json の分だけ変化。
3 度目は今回の実測)。**」

- **答え(「なぜ記録されていないか」)**: **6 回目の処置が §14.6 の記録を更新しなかったためである。**
  **`MD5SUMS` は「結果を後から差し替えていないことを見るため」の機械なので、
  走らせ直した回はここに記録が要る**(監査役の指摘のとおり)。
- **§14.6 に「走行の道具を変えた回」の表(3 行)を置き、3 度目の突き合わせの出力を貼った**
  (PREREG:3623〜3673)。**§8.5 の 152 セルの再照合も貼った。**
- **3 度目の実測は §3 の 4。**

### 6.[直す] `observation_only.csv` にも族の α の名前が付いた列がある

**処置: 直した。**リードの決定(逐語):
「**`observation_only.csv` の MDE 列の見出しも「MDE(参考。族の α に入らない)」に。§10.2 を直す。テスト。**」

- **答え(「指摘 7 の問いはこの表には当たらないのか」)**: **当たる。**
  **`observation_only.csv` の 2,208 行も 576 にも α にも F1 の読みにも 1 行も入らない。**
- **JUDGE**: `REF_MDE_COL`(JUDGE:1092)/ `OBS_HEADER` を差し替え(JUDGE:1103〜1112)/
  `to_ref_mde_rows()`(JUDGE:1115〜1124。**旧名 `to_sens_rows` / `SENS_HEADER` は同じものの別名として残した**)/
  観測のみの行にも当てる(JUDGE:1678〜1682)。**列の順と中身は変えていない。**
- **PREREG §10.2 の表を 3 行にした**(PREREG:2462〜2477)。
- **試験**: `test_the_observation_only_table_does_not_carry_the_family_alpha_in_its_mde_column`
  (**判定の 2 表は `MDE(α=0.05/576)` のままであることも同じ試験で測る**)。

### 7.[直す] 「最小」「最大」が検定していないセルを含む

**処置: 直した。**リードの決定(逐語):
「**「最小・最大」も検定したセルだけから取る。標準出力も同じ。§10.3 を直す。テスト。**」

- **答え(「指摘 12 の「2 つを分ける」はなぜ最小・最大に当たらないのか」)**: **当たる。**
  **6 回目の処置は一覧からだけ外し、隣の最小・最大を直していなかった。**
- **JUDGE**: `reps_used_all` を「判定が `不明(n < 30)` / `不明(n2 < 30)` でないセル」に絞った
  (JUDGE:1716〜1722)。**鍵の名前も「最小(検定したセルだけ)」「最大(検定したセルだけ)」に変え、
  「最小・最大の当て先」の注を足した**(JUDGE:1836〜1844)。**標準出力も同じ鍵を指す**(JUDGE:1985〜1986)。
- **PREREG §10.3 の表に行を足した**(PREREG:2604〜2616)。
- **試験**: `test_the_min_and_max_reps_come_from_tested_cells_only`
  (**検定していないセルが在る走行で、最小・最大が検定したセルの min / max と一致することを測る**)。

### 8.[直す] 「待つものの 7」という番号参照が残っていた

**処置: 直した。**リードの決定(逐語):
「**「待つものの 7」→「ス」。番号参照が他に残っていないか `grep -n '待つもの.*[0-9]'` で
確かめて報告に貼る。**」

- **答え(「残った番号参照はこの 1 行だけか」)**: **地の文ではこの 1 行だけである。**
  **走査の出力は §3 の 7 に貼った。**残る一致は**リードの決定の逐語の中**(§0.1 の表)と
  **件数(「8 件」「2 件」)**だけで、**待つものを番号で指している箇所は 0 である。**
- **PREREG:1891 を「**ス**」に直し、経緯を注に書いた**(PREREG:1891〜1899)。

### 9.[直す] 「97 日は連続した 1 つの塊」が訂正から漏れていた

**処置: 直した。**リードの決定(逐語):
「**「連続した 1 つの塊」の残りを「3 つの塊(1 日 + 89 日 + 7 日)」に直す(全箇所)。**」

- **答え(「この 1 行は訂正から漏れていないか」)**: **漏れていた。**
- **PREREG:2055〜2062 を直した**(3 つの塊を書き、うち 89 + 7 日が 2024 年 3〜6 月の連続した塊であると
  書いた)。**全箇所を走査した**: 残る「連続した塊」の記述は
  (a) 訂正の経緯(PREREG:2018〜2027、2791)、(b) 正しい記述(PREREG:2056、2774、3256)だけで、
  **「連続した 1 つの塊」と書いている地の文は 0 である**(**実測**: `grep -n '連続した 1 つの塊\|1 つの連続した塊'`。
  **範囲は本ファイル 1 本に切っている**)。

### 10・15.[直す] 「取り直しは試行中」のまま凍結文書に残っていた / 空の分岐が残っていた

**処置: 直した。**リードの決定(逐語):
「**§8.6 と §14.4 ケ の「試行中」を取り直しの結果で埋める: 取れた日数 0。公開アーカイブ
`data.binance.vision` の COIN-M daily metrics は欠測 99 日(判定区間内 97 + 外 2)の zip と
.CHECKSUM の 198 URL がすべて HTTP 404、S3 キー一覧(zip 1,791)にも不在、monthly/metrics の
ツリー自体が無い(5 本 404、一覧に無し)、REST dapi はこの環境から 451、第三者は Coinalyze 401 /
CoinGlass「API key missing」/ tardis は鍵なしの一覧が 200 でデータ取得は試していない(未確認)、
オーナー PC は未確認。2026-09-13 の取得ログでも同じ日が 404。欠けは COIN-M の metrics ツリー全体
(ETHUSD_PERP も同じ 98 日が無い、USD-M BTCUSDT は 0 日)、BTCUSD_PERP だけの欠けは 2023-11-19。
ログ `docs/DATA/probes/20260918_cm_metrics_missing_days.log`(コミット 7091c28)。
「取れた分は…経路はリードが決める」の空の分岐は消す。**」

- **答え(「既に在る取り直しの範囲と方法を持たないまま「試行中」と書いてよいか」)**: **よくない。**
  **取り直しはコミット `7091c28` で終わっており、本処置の前に在った。**
- **§8.6 に「取り直しの結果」の表(6 行)を置いた**(PREREG:1995〜2016)。
  **経路ごとに「何を試したか」と「結果」を書いた**(`CLAUDE.md` §5.2 の
  「「取れない・無い」の主張には範囲と方法を書く」の形)。
- **「取れた分」の行を「0 日だったので、この分岐は空である」に直し、
  「取り直した日の zip を読む経路はリードが決める」という未決の手順を消した**(指摘 15)。
- **§14.4 の ケ にも結果を書き、同じ空の分岐を消した**(PREREG:3256、3290〜3298)。
- **委任先はログを読んだだけで、取り直しそのものはしていない**(**別の委任先の成果物**)。

### 11.[直す] 感度 4 本に `params.approval` が要求されていない

**処置: 直した。**リードの決定(逐語):
「**感度 4 本にも `params.approval` の突き合わせを要求(同じ L 番号)。§14.6 の表と実装。テスト。**」

- **答え(「判定 2 本だけ突き合わせると決めたのはどこか」)**: **どこにも無い。**
  **6 回目の処置がそうしただけで、理由は書かれていなかった。****感度 4 本も同じ 456 日を開ける。**
- **JUDGE**: 感度の `check_run_params` に `approval=approval` を渡す(JUDGE:1595〜1600)。
- **PREREG §14.6 の `params` の表に `approval` を足し、節を置いた**(PREREG:3765、3773〜3784)。
- **試験**: `test_the_sensitivity_runs_also_match_the_approval_number`
  (**番号が違う感度はその表だけ書かれず、理由が `summary.json` に残ることを測る**)。

### 12.[直す] 観測のみの表で落とす列の一覧が文面と実装で違う

**処置: 直した(文面を実装に合わせた)。**リードの決定(逐語):
「**観測のみの表で落とす列を実装のとおり 6 列(`t` / `判定` / `ブートストラップSE` / `CI下限` /
`CI上限` / `バー近傍` / `有限な複製の本数` — 実装を読んで正確に)に §10.2 を合わせる。**」

- **答え(「落とす列の一覧は文面と実装のどちらが正か」)**: **実装が正である。**
- **実装を読んで数え直した**: 落ちている列は **`t` / `ブートストラップSE` / `有限な複製の本数` /
  `CI下限` / `CI上限` / `バー近傍` / `判定` の 7 列**である(**実測**: JUDGE:1103〜1112 の
  `OBS_DROPPED_COLS`)。**リードの決定は「6 列」と書いて 7 つ並べているので、
  本書は列を全部並べたうえで「= 落とす列は 7 列である」と書いた**(§4 の (2))。
- **PREREG §10.2 を直した**(PREREG:2399〜2412)。**実装は変えていない。**

### 13.[直す] 試験の数が本書に無い

**処置: 直した。**リードの決定(逐語):
「**§14.6 の試験の数を現物(3 本で 116 件: reaction 51 / judge 53 / wired 12。全体 2,098)に直し、
今回足した分も反映して最終値を書く。**」

- **答え(「走行の道具側に足した試験が本書に無いのはなぜか」)**: **6 回目の処置が
  「読みのスクリプトを直した点」の節にだけ数を書き、走行の道具側の試験を数えていなかったためである。**
- **§14.6 に 3 本の内訳とリポジトリ全体を書いた**(PREREG:3979〜3990)。
  **116 件 → 127 件**(reaction **51 → 56** / judge **53 → 59** / wired **12 のまま**)、
  **リポジトリ全体 2,098 → 2,109。**(**実測**。§3 の 2)。

### 14.[直す] 「フックが全操作を拒否する」は何を試した結果か

**処置: 直した(実測の記述に替えた)。**リードの決定(逐語):
「**「全操作を拒否する」を実測の記述に替える: 4 回目の処置で委任先が
`scripts/_research_audit_gate.py` を編集したところ PreToolUse のフック
(`.claude/hooks/_verify_manifest.sh` / `deny_protected_paths.sh`)が以後の Edit/Write を止めた
(処置書 `_r4.md` の 9 の経緯を引用)。試していないなら「未確認」と書く(処置書 r4 を読んで判断)。**」

- **答え(「拒否されることを実際に試したのか、それとも設定の読みか」)**: **試した結果である。**
  **処置書 `_r4.md` の 9 の逐語**: 「**1 行足した時点で `.claude/hooks/_verify_manifest.sh` が
  Edit も Bash も全部拒否した**(**実測** 2026-09-18: `[関門] 操作を拒否した。…食い違ったファイル:
  scripts/_research_audit_gate.py`)」。**戻し方も同じ処置書に書かれている。**
- **PREREG:3916〜3941 を書き直した**(**実測の記述と、戻した経緯と、待つもの サ への参照**)。
- **ただし「全操作」は r4 の実測より広い語だったので、r4 の逐語(Edit と Bash)に合わせた。**
  **`deny_protected_paths.sh` がこの拒否に関わったかは r4 の記録からは分からないので「未確認」と書いた**
  (**r4 が名前を挙げているのは `_verify_manifest.sh` だけである**)。

### 16.[聞く] オーナーが同時に見る 2 つの一覧が食い違っている

**処置: 直した。**リードの決定(逐語):
「**§14.4 の待つものの名前を**リードの返答と同じ対応付け**に直す: ア = 判定のバー
(t ≥ 2.0 → |t| ≥ 3.925)/ イ = 判定に使う走行 2 本 / ウ = ブートストラップの区間(正規近似)/
エ = 対照 (ii) の向きの要る量の符号 / オ = バーの数値(z 1 本、3.9247896)/
カ = 判定の分岐(判定語 7 語)/ キ = F1 の整合・反証の条件 /
ク = 対照 (ii) の群の分け方(24 群は相手の符号、14 群は受け継ぐ)/ ケ = 主軸 D の欠け /
コ = 関門なしのサニティ #13 / サ = 指紋の台帳の作り直し / シ = WIRED 未記載 /
ス = MDE の p の誤差 / セ = 凍結 6 項目 / ソ = 2 周目の材料。
答えが要るのは カ・キ・ク、報告は ア〜オ、事実は ケ〜ソ。§13 の表の並びもこの順に。**」

- **答え(「報告を出す前に直すか」)**: **直した。**
- **§14.4 の ア〜ク を上の対応付けに振り直した**(PREREG:3248〜3255)。
  **§13 の「報告する変更」の表の並びも同じ順にした**(PREREG:3023〜3030)。
  **行の中身は 1 つも変えていない**(**並べ替えと名前の振り直しだけ**)。
  **行頭に付いていた `(6)` `(7)` `(8)` という番号は、並びを指していたので取り除いた。**
- **「本書のこの表は委任先が決めた対応付けである」という注を、リードの対応付けに置き換えた**
  (PREREG:3236〜3247)。**答えが要るのは カ・キ・ク、報告は ア〜オ、事実は ケ〜ソ と明記した。**

### 17.[聞く] オーナーは何の版の道具を承認したことになるか

**処置: 直した(事前登録に書いた)。**リードの決定(逐語):
「**§14.4 に書く: オーナーの応答が承認するのは §13 の枠組み(カ・キ・ク と ア〜オ の形)であって
道具の版ではない。開封のときの道具の版はコミットのハッシュで §14.4 に書き、結果の報告に載せる。
応答の後に §13 の表か待つものが変わる直しが入ったら、開封の前に追加で報告する(既定どおり)。**」

- **答え**: **書かれていなかった。**本版で決めた。
- **§14.4 に表(3 行)を置いた**(PREREG:3328〜3346)。
  **承認の対象は枠組み / 版はコミットのハッシュで残す / 応答の後の直しは既定どおり追加報告。**
- **§14.4 の「打ち切りと凍結した道具」の表にも 1 行足した**(PREREG:3317)。

### 18.[聞く] その L 行が報告 062 への応答であることを検査する機械が 1 つも無い

**処置: 直した(機械を足した)。**リードの決定(逐語。指摘 1 と同じ決定の (b)):
「**(b) その番号が 199 より大きくなければ「[止め]」(L-199 は「1 = a、9 = a」への応答であって
報告 062 への応答ではない)。**」

- **答え(「指摘 2 の「報告への応答が来ていなくても通る」は何をもって閉じたと言えるか」)**:
  **6 回目の処置では閉じていなかった。**本版で (a)(b) を走行の側に足して閉じた。
- **機械が見るのは 2 つ**: **事前登録 §14.4 の欄と `--approval` が一致すること**(a)と、
  **その番号が L-199 より後であること**(b)。
- **射程(何を見ていないか。隠さずに書く)**: **「その L 行が報告 062 への応答である」ことそのものは、
  どの機械も読んでいない。**見ているのは**番号の大小**と**欄との一致**だけである。
  **欄はリード自身が書き写す**ので、**書き写しが正しいことは機械では担保していない**(**未確認**)。
  **L-200 以降に別の話題の記録が入り、それを欄に書き写した場合は通る。**
  **これは本版で閉じていない穴である**(§4 の (3))。

### 19.[聞く] 委任先の目視の分類を、誰がどこで確かめるか

**処置: 直した(確かめる人と場所を書いた)。**リードの決定(逐語):
「**走査の目視の分類はリードが確かめる(リードが grep の出力を読む。処置書には「リード確認待ち」と書き、
リードが ACTION_LOG に確認を書く)。**」

- **答え**: **リードが確かめる。****本処置の時点では「リード確認待ち」である。**
- **PREREG の走査の節に書いた**(PREREG:4122〜4127)。
  **リードが `grep -n '有効\|差なし\|陰性'` の出力を 1 行ずつ読み、確認を
  `docs/AUDITOR/ACTION_LOG.md` に書く。**
- **委任先は出力を §3 の 7 に貼った**(**44 行**)。
- **走査の 4 本も打ち直した**(本処置で本文が増えたため数が動いた。§3 の 7)。

### 20.[聞く] 「一度だけ」を守る機械が無く、§3.1 に数える上限も書かれていない

**処置: 直した(上限を決めて機械にした)。**リードの決定(逐語。指摘 1 と同じ決定の (e)):
「**(e) `--out-dir` が既に存在すれば「[止め]」(再走行・上書きを機械で止める = 「一度だけ開ける」の機械。
開けられる回数の上限 = 6 本、それ以上はこの機械を変えないと走らない)。**」

- **答え(「何本目までなら数えて進めてよいかはオーナーが決めることではないか」)**:
  **本書は「これから開けてよいのは 6 本」と決め、それを機械にした。**
  **その上限を超える判断はオーナーに出す**(§14.6 の「新しい開封として §3.1 に数える。
  オーナーの決定が要る」は変えていない)。
- **機械**: (d) 出力先が §14.1・§14.2 の 6 つのどれかでなければ止まる / (e) 既に在れば止まる。
  **7 本目を開けるには `FULL_OUT_DIRS_REL` を書き換えるしかなく、差分が残る。**
- **PREREG §3.1 に表(4 行)を置いた**(PREREG:713〜729)。
  **既に開けた 4 本はこの機械より前の出来事であることも書いた**(**隠さずに書く**)。

### (番外)PREREG:268 / :301 の「凍結の 6 項目は 1 つも動いていない」

**処置: 直した。**リードの決定(逐語):
「**PREREG :268 と :301 の「凍結の 6 項目は 1 つも動いていない」を「動いたのはバーの数値 0.0002 だけ
(§13 のオ)」に直す。**」(**返答の監査の指摘**)

- **同じ文書の §13 は「動いたのは「判定のバー」の数値 0.0002 だけである」と書き、
  §14.4 の **オ** はそれをオーナーに報告する項目である。****この 2 行だけが食い違っていた。**
- **PREREG:268〜275 と :309〜316 を直した**(**2 か所とも同じ文面**)。
  **本版で足した §0.1 の 9 つ目の表の末尾にも同じ形で書いた**(PREREG:351〜353)。

---

## 3. 報告(**実測**。コマンドと出力を同じ節に置く)

### 1. 試験(`-q` 無し)

```
PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
```

```
........................................................................ [ 56%]
.......................................................                  [100%]
127 passed in 53.34s
```

### 2. 数え上げ(`--collect-only`)

```
PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py --collect-only | tail -1
127 tests collected in 0.30s

PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py --collect-only -q | tail -4
tests/test_audit_gates_wired.py: 12
tests/test_o3c_reaction.py: 56
tests/test_o3c_reaction_judge.py: 59

PYTHONPATH=src python -m pytest --collect-only | tail -1
2109 tests collected in 1.66s
```

**内訳(**実測**)**: `test_o3c_reaction.py` **51 → 56 件**(開封前の関門 4 件 + `run_table` の承認 1 件)/
`test_o3c_reaction_judge.py` **53 → 59 件**(感度 4 本必須 / 感度が破れても判定は書く /
`stopped.txt` / 感度の承認 / 観測のみの表の MDE 列 / 最小・最大)/
`test_audit_gates_wired.py` **12 件のまま**(**中身は感度 4 本を渡す形に直した**)。
**リポジトリ全体 2,098 → 2,109 件。**

### 3. §8.5 の再現(152 セル)

```
sed -n '1582,1642p' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md > /tmp/…/mde_check.py
python3 /tmp/…/mde_check.py
```

```
照合したセル: 152 / 一致 152 / 食い違い 0
```

**標本 4 本を走らせ直した後の実測である**(下の 4)。

### 4. 標本 4 本の走らせ直しと突き合わせ(3 度目)

```
for spec in "gap60_w8 60 8" "gap60_w24 60 24" "gap30_w8 30 8" "gap180_w8 180 8"; do set -- $spec
  PYTHONPATH=src python scripts/o3c_reaction.py --mode sample --seed 1 --mmr 0.004 \
    --gap-sec $2 --window-hours $3 --out-dir backtest_data/o3c_reaction_20260918_sample/$1
done
```

**前の出力は走らせ直す前に退避してある**(`/tmp/…/scratchpad/before/`)。**`pandas` で全列を突き合わせた**:

```
gap60_w8/table.csv: 行×列 (662, 157) -> (662, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/summary.json: 食い違い 1 件 -> .elapsed_sec: 10.04 -> 10.8
gap60_w8/MD5SUMS: 変わったファイル ['summary.json']
gap60_w24/table.csv: 行×列 (665, 157) -> (665, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/summary.json: 食い違い 1 件 -> .elapsed_sec: 9.56 -> 10.24
gap60_w24/MD5SUMS: 変わったファイル ['summary.json']
gap30_w8/table.csv: 行×列 (784, 157) -> (784, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/summary.json: 食い違い 1 件 -> .elapsed_sec: 9.9 -> 11.18
gap30_w8/MD5SUMS: 変わったファイル ['summary.json']
gap180_w8/table.csv: 行×列 (503, 157) -> (503, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/table_mixed.csv: 行×列 (2, 157) -> (2, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/summary.json: 食い違い 1 件 -> .elapsed_sec: 9.4 -> 10.18
gap180_w8/MD5SUMS: 変わったファイル ['summary.json']
```

**= `table.csv` と `table_mixed.csv` は 4 本とも全列一致。**
**`summary.json` の食い違いは `elapsed_sec` の 1 件だけ。**
**`MD5SUMS` で変わったのは `summary.json` だけである**(**事実**)。

### 5. 開封前の関門の実測(通る側・止まる側。**一時ファイル**)

**一時ディレクトリに事前登録の写しと `OWNER_LOG.md` を置き、`check_approval("full", …)` を叩いた**
(**判定区間は開けていない。`--mode full` は 1 本も走らせていない**):

```
[通る] (通る側)5 つとも満たす
[止め] (a-1) 事前登録の欄が「(まだ無い)」
        [止め] 事前登録 §14.4 の「応答の L 番号」が読めないので、判定区間を開けない。
[止め] (a-2) 欄 L-200 に対して --approval L-201
        [止め] --approval L-201 が事前登録 §14.4 の欄(L-200)と違う。判定区間を開けない(prereg 監査(7 回目)の指摘 1)
[止め] (b) L-199 は報告 062 への応答ではない
        [止め] --approval L-199 は L-199 より後の番号でない。L-199 は「1 = a、9 = a」への応答であって、報告 062 への応答ではない(prereg 監査(7 回目)の指摘 18)
[止め] (c) OWNER_LOG に行が無い
        [止め] /tmp/…/OWNER_LOG.md に行頭 `| L-777 |` の行が無い。全件は走らせない(設計 §9 の機械)
[止め] (d) 出力先が §14.1・§14.2 の 6 つに無い
        [止め] --out-dir /tmp/…/どこか別の場所 は事前登録 §14.1・§14.2 の 6 つに無い。
[止め] (e) 出力先が既に在る(再走行・上書き)
        [止め] --out-dir /tmp/…/gap60_w8 は既に存在する。判定区間は一度だけ開ける(事前登録 §3.1。上書き・再走行はここで止まる)
[止め] (f) --out-dir が渡っていない
        [止め] --mode full の関門に --out-dir が渡っていない(事前登録 §14.1・§14.2 の 6 つのどれかでなければ走らせない)

既定の読み先(本物の事前登録): (None, "事前登録 §14.4 の「応答の L 番号」の欄が埋まっていない(欄の値: '(まだ無い。オーナーの応答を待っている。)')")
```

**= いまこの瞬間に `--mode full` を打っても、(a) で止まる**(**事実**。欄が埋まっていない)。

### 6. 判定区間を開けていないこと

```
ls backtest_data/o3c_reaction_20260918_full
ls: cannot access 'backtest_data/o3c_reaction_20260918_full': No such file or directory
```

### 7. 走査(指摘 8 と指摘 19 の材料)

**(a) 番号参照の走査(指摘 8)**: `grep -n '待つもの.*[0-9]'`(**範囲は本ファイル 1 本に切っている。
リポジトリ全体は数えていない**)の一致は **17 行**(**実測**: `grep -c` = 17)。
**内訳(目視。リード確認待ち)**: **13 行が「リードの決定の逐語」の中**
(:219 / :235 / :263 / :265 / :303 / :304 / :306 / :338 / :345 / :1893 / :3239 / :3300 / :3305。
**逐語なので変えない**)、**4 行は件数や直後の別の数字**(:3035「ア〜ク と同じ」/ :3044「この 8 件」/
:3074「§13 の…8 件」/ :3335「8 件の枠組み」)。
**待つものを番号で指している地の文は 0 行である**(直した :1891 が最後の 1 行だった)。

**(b) 判定語の行の走査(指摘 19。リードが 1 行ずつ読む材料)**:
`grep -n '有効\|差なし\|陰性' REACTION_PREREG_2026-09-18.md`(**範囲は本ファイル 1 本に切っている。
リポジトリ全体は数えていない**)。**一致 43 行**(**先頭 100 文字で切って貼る**):

```
54:> > **(4) §10.1 の判定を 4 分岐から 3 分岐にし、「差なし」「陰性」の語を消した**(prereg 監査(2 回目)の指摘 3・4)。
66:> **判定(予測できる / 使える / 有効)は 1 つも書かない。**
76:| 事前登録を書き、MDE を先に置く | **(該当語なし)** | `CLAUDE.md` §5.2「**MDE を書けない主張は陰性として扱わない**」+ `research-protoc…
102:| **§10.1 の判定の分岐の言葉を置く**(**本版は「不明(n < 30)」/「不明(n2 < 30)」/「不明(t 未算出)」/「差あり(+)」/「差あり(−)」/「不明(MDE 未…
135:| 3・4 | §10.1 の 4 分岐を 3 分岐にし、「差なし」「陰性」の語を消す | **(該当語なし)** | **リードの決定**「**(1) 群の束の数 < 30 →「不明(n <…
163:| ウ | §10.3 の「読み」の 4 語を §7.0 に揃える | **(該当語なし)** | 「**§10.3 の「読み」の 4 語は §7.0 の新しい 4 分岐(F1 と整合 / F…
216:| 2 | SE に使えた複製の本数を列に出す | **(該当語なし)** | **リードの決定**「**セルごとに SE の推定に使った有限な複製の本数を列 `有効反復数` に出す。§9.1…
294:| 6 | 「有効な本数」の残りを消す | **(該当語なし)** | **リードの決定**「**「有効な本数」の残りを「有限な複製の本数」に統一(全箇所。`grep -n '有効'` の結果…
397:**`CLAUDE.md` §5.2 の「△ や ✕ が残ったまま出た陰性は陰性ではなく不明」を、本書はそのまま引き継ぐ。**
398:**本走行の結果に △ / ✕ の行がかかるとき、その部分は「陰性」ではなく「不明」と書く。**
399:**△ 8 件 / ✕ 4 件が残ったまま走るので、段 A の結果はどの行についても「陰性」にはならない。**
446:**a が残ったまま出す結果は、トレンド側については「陰性」ではなく「不明」である。**
1045:**前進到達は「1 周目は測定不能」である**(**陰性ではない**。L-199「**9 = a**」)。
1320:**`CLAUDE.md` §5.2 の「偽陰性を防ぐ機構が無かった」への対策としては、
1733:**丸め**: 系統 1・2・3・6・7 は小数 5 桁、系統 5・8 は有効数字 5 桁(末尾の 0 も書く)。
1742:  (**陰性にしない**)。
2169:> > > 列名を `有効反復数` としていたが、**その語は §10.1 の末尾が禁じた「有効」を含み、
2174:> > > 「**「有効な本数」の残りを「有限な複製の本数」に統一(全箇所)**」。
2354:> **前版の 4 分岐は、\|t\| < 3.925 のセルを「差の絶対値 < MDE → 不明」/「≥ MDE → 差なし
2358:> **陰性の定義そのもの**である。一方 §1 は「**△ 8 件 / ✕ 4 件が残ったまま走るので、
2359:> 段 A の結果はどの行についても「陰性」にはならない**」と書いている。
2360:> **同じ文書が、陰性は出ないと書きながら陰性の判定語を表の列に置いていた。**
2361:> **本版は「差なし」「陰性」の語を §10.1 から消した。**
2364:> **欲しい効果を置かないまま、観測された差を陰性と不明の境界に使うことは、
2365:> `CLAUDE.md` §5.2 の「陰性と不明を分ける」と両立しない**(境界が事前に決まっておらず、
2371:> **「検出されず」は陰性ではない。**意味は「**この n とこの MDE では差を検出できなかった**」だけである。
2391:**判定語(予測できる / 使える / 有効)はどの行にも書かない。**
2402:**差を書いてよいが、「判定」「有効」「使える」と書かない。**
2657:機構のバグ」に、検出力不足(同 §5.2 の「陰性と不明を分ける」)を足したものである。**
2809:  **これは「陰性」ではなく「測定不能」である**(`CLAUDE.md` §5.2 の陰性 / 不明の区別より手前)。
3008:| #6 | 「欲しい効果」の大きさ(**prereg 監査(1 回目)の指摘 12 = 聞く。リードの決定: 黙認のまま進める。オーナーには返答 060 #6 と 061 §1 の 2 回…
3011:| **#9** | **前進到達(`fwd_node`)が 82.1% 作れないことへの対処(案 a / 案 b)** | **a。**1 周目は**前進到達なし**で進め、**「測定不能…
3028:| **§10.1 の判定の分岐(#2 に付く)** | **4 分岐。\|t\| < 3.925 を「差の絶対値 < MDE → 不明」/「≥ MDE → **差なし**(検出できる検出力…
3253:   | **カ** | **§10.1 の判定の分岐**: 4 分岐(「差なし」を含む)→ **7 分岐**(「差なし」「陰性」を使わない。§13 の 6 行目) | prereg 監査(…
4054:**判定語(予測できる / 使える / 有効)を、判定として書いた行はこの文書に 1 つも無い**
4068:for w in 予測できる 使える 有効 差なし 陰性; do
4090:--- 有効 ---
4095:--- 差なし ---
4100:--- 陰性 ---
4116:**上の 4 本が出すのは件数だけで、その行が「禁止の文」か「別の語(有効数字)」か
4118:**内訳**: 「有効」= 禁止の文 / 「有効数字 5 桁」という別の語 / 列名についてのリードの決定の
4119:引用と説明 / **この節のコマンドと出力**。「差なし」「陰性」= **その語を使わないと決めた経緯の記述**
4124:リードが ACTION_LOG に確認を書く)**」)**: **リードが `grep -n '有効\|差なし\|陰性'` の出力を
```

**この 43 行の分類は委任先の目視であり、機械は掛かっていない。**
**リードが 1 行ずつ読んで確かめ、確認を `docs/AUDITOR/ACTION_LOG.md` に書く**(指摘 19。
**本処置の時点では「リード確認待ち」である**)。

**(c) 判定語の 4 本の走査(`CLAUDE.md` §3 の型。範囲 = 本ファイル 1 本)**:
**本処置で本文が増えたので数が動いた。****打ち直して同じ数になるまで回し、PREREG の出力を貼り替えた**
(PREREG:4078〜4107):「有効」= 行 14 / 出現 21、「差なし」= 行 12 / 出現 16、
「陰性」= 行 27 / 出現 34、「予測できる」= 行 5 / 出現 5、「使える」= 行 6 / 出現 6。

### 8. 事実 / 推定 / 仮定 の印

| 主張 | 印 |
|---|---|
| 試験 127 件が通った / 3 本の内訳 / リポジトリ全体 2,109 件 | **事実**(上の 1・2 の出力) |
| §8.5 の 152 セルが食い違い 0 | **事実**(上の 3) |
| 標本 4 本の `table.csv` / `table_mixed.csv` が全列一致、`summary.json` の差は `elapsed_sec` だけ | **事実**(上の 4。`pandas` の突き合わせ) |
| 開封前の関門が (a)〜(e) の各条件で止まり、5 つとも満たすと通る | **事実**(上の 5。**一時ファイルでの実測**) |
| いまこの瞬間に `--mode full` を打つと (a) で止まる | **事実**(上の 5 の最終行。欄が埋まっていない) |
| `backtest_data/o3c_reaction_20260918_full/` が存在しない | **事実**(上の 6) |
| COIN-M metrics の欠測 99 日は公開アーカイブから取れない | **事実**(別の委任先の実測。ログ `docs/DATA/probes/20260918_cm_metrics_missing_days.log`)。**ただし第三者(tardis)のデータ取得とオーナー PC は未確認** |
| 開封前の関門が「一度だけ開ける」を守る | **推定**。**機械が止めるのは「同じ出力先への 2 度目」と「6 つ以外の出力先」だけである**(**出力先を消してから走らせ直す経路は止まらない**。§4 の (3)) |
| 「その L 行が報告 062 への応答である」ことの担保 | **仮定**(**欄への書き写しが正しいという仮定**。機械は番号の大小と欄との一致しか見ていない。§2 の 18 の射程) |
| 走査の 43 行の分類(禁止の文 / 別の語 / 経緯の記述) | **目視**(委任先)。**リード確認待ち** |

---

## 4. 委任の範囲外の判断(**黙って決めずに書く**。`CLAUDE.md` §0.1)

**(1) 試験で感度 4 本をどう渡すか(決定 2 に付随)。**
**決定 2 は「`--sens` は 4 本とも必ず渡す」と決めたが、試験が毎回 4 本の中身を作ると
1 本あたり 1,650 行 × 2,000 反復のブートストラップが 4 本分増え、試験の所要が跳ね上がる。**
**委任先の判断**: **試験は呼び出し側が中身を作った感度だけを本物にし、残りは「読めない感度」として
渡す**(`sens_required_args()`)。**読めない感度は「その表だけ書かない」経路に落ちるので、
判定の表は書かれる。****結果として、ほぼ全部の試験がこの新しい経路を毎回通る。**
**道具の側の要求は 1 つも緩めていない**(**迂回する旗は作っていない**)。
**所要は 3 本で 53 秒**(**実測**。前版は 3 本で約 52 秒)。

**(2) 「6 列」か「7 列」か(決定 12)。**
**リードの決定は「実装のとおり 6 列(`t` / `判定` / `ブートストラップSE` / `CI下限` / `CI上限` /
`バー近傍` / `有限な複製の本数` — 実装を読んで正確に)」と書いており、括弧の中に 7 つ並んでいる。**
**委任先は「実装を読んで正確に」に従い、実装を数えた**(**実測**: `OBS_DROPPED_COLS` は 7 列)。
**PREREG には 7 列を全部並べ、「= 落とす列は 7 列である」と書いた。**
**リードの決定の逐語は §0.1 の表にそのまま残してある**(6 と書かれたまま)。

**(3) 「一度だけ開ける」の機械には穴が残る(決定 20)。**
**(e) が止めるのは「出力先が既に在るとき」である。**
**出力先を消してから走らせ直せば、この機械は止めない**(**未確認ではなく、設計上そうである**)。
**`FULL_OUT_DIRS_REL` を書き換えないと 7 本目は開けられないが、6 本のうちどれかを
消して再走行する経路は残っている。****これを塞ぐかはリードが決めること**(委任の範囲外)。
**塞ぐなら「開けた出力先の台帳をリポジトリに持ち、消えていても止める」形になるが、
その台帳をどこに置くかは §5.0 の関門の範囲に触れるので、本処置では作っていない。**

---

## 5. 触っていないもの(**確認**)

- **`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/`**:
  **1 行も触っていない。`sh scripts/regen_hook_manifest.sh` も走らせていない。**
- **`backtest_data/o3c_reaction_20260918_full/`**: **作っていない**(§3 の 6)。
- **`--mode full`**: **1 本も走らせていない。**
- **コミット・プッシュ**: **していない。**
- **`docs/OWNER_LOG.md` / `docs/OWNER_STATUS.md`**: **触っていない**(オーナーの発言が無い回のため)。
  **`docs/OWNER_STATUS.md` は本処置の前から作業ツリーで変更されていた**(**実測**: 変更時刻
  2026-09-18 12:02 UTC で、本処置の最初の編集より前。**リード側の変更である**)。
- **`backtest_data/o3c_reaction_20260918_sample/`**: **走らせ直したので `summary.json` と
  `MD5SUMS` の 4 本ずつが変わっている**(`table.csv` / `table_mixed.csv` は変わっていない。§3 の 4)。

## 事前登録から移した処置の注記(16 ブロック。2026-09-19、L-228「移す先 = 巡ごとの VERDICTS と docs/DATA/probes/ でよい」。元の位置は節と、移す前の版(コミット fef6abb 時点)の行番号)

### 元: 0.1 対応表 / 旧 行 287-292

> **【本版で直した・prereg 監査(7 回目)の返答の監査の指摘。リードの決定の逐語:
> 「**PREREG :268 と :301 の「凍結の 6 項目は 1 つも動いていない」を「動いたのはバーの数値 0.0002 だけ
> (§13 のオ)」に直す**」】**
> **同じ文書の §13 は「凍結の 6 項目のうち、動いたのは「判定のバー」の数値 0.0002 だけである」と
> 書いており、§14.4 の **オ** はそれをオーナーに報告する項目である。**
> **この 2 行だけが「1 つも動いていない」と書いていた。**

### 元: 0.1 対応表 / 旧 行 328-333

> **【本版で直した・prereg 監査(7 回目)の返答の監査の指摘。リードの決定の逐語:
> 「**PREREG :268 と :301 の「凍結の 6 項目は 1 つも動いていない」を「動いたのはバーの数値 0.0002 だけ
> (§13 のオ)」に直す**」】**
> **同じ文書の §13 は「凍結の 6 項目のうち、動いたのは「判定のバー」の数値 0.0002 だけである」と
> 書いており、§14.4 の **オ** はそれをオーナーに報告する項目である。**
> **この 2 行だけが「1 つも動いていない」と書いていた。**

### 元: 3. 分割(探索 / 判定の境界) / 旧 行 888-893

> **【前版の穴(7 回目の処置書 `_r7.md` が自分で書いていた)】**
> 「**(e) が止めるのは「出力先が既に在るとき」である。出力先を消してから走らせ直せば、
> この機械は止めない(未確認ではなく、設計上そうである)**」
> 「**これを塞ぐかはリードが決めること**」。
> **この穴が、オーナーが開封の可否を決めるために読む本書(§3.1)に 1 行も書かれていなかった**
> (8 回目の指摘 2 の**実測**: `grep -n '出力先を消\|消してから'` の一致 0)。**本版で書いた。**

### 元: 8. 検出力(MDE)— 走らせる前に書く(§4.1) / 旧 行 2193-2199

  > **【本版で直した・prereg 監査(7 回目)の指摘 8。リードの決定の逐語:
  > 「**「待つものの 7」→「ス」。番号参照が他に残っていないか `grep -n '待つもの.*[0-9]'` で
  > 確かめて報告に貼る**」】**
  > **6 回目の指摘 16 の処置で待つものは番号から名前(ア〜ソ)に替わったのに、
  > この 1 行だけ「7」という番号のままだった**(該当は **ス** = MDE の `p` の誤差)。
  > **走査の結果は処置書 `..._r7.md` の報告に貼ってある**(残った番号は
  > **リードの決定の逐語の中**にしか無い = 逐語なので変えない)。

### 元: 8. 検出力(MDE)— 走らせる前に書く(§4.1) / 旧 行 2382-2385

  > **【本版で直した・prereg 監査(7 回目)の指摘 9。リードの決定の逐語:
  > 「**「連続した 1 つの塊」の残りを「3 つの塊(1 日 + 89 日 + 7 日)」に直す(全箇所)**」】**
  > **同じ §8.6 の走査の出力が 3 つの塊を出しており、下の訂正も「2023-11-19 が離れている」と
  > 書いているのに、この 1 行だけ「連続した 1 つの塊」のままだった**(指摘 9)。

### 元: 10. 結果の読みで必ず出す表(§4.3) / 旧 行 2727-2734

> **【本版で実装に合わせた・prereg 監査(7 回目)の指摘 12。リードの決定の逐語:
> 「**観測のみの表で落とす列を実装のとおり 6 列(`t` / `判定` / `ブートストラップSE` /
> `CI下限` / `CI上限` / `バー近傍` / `有限な複製の本数` — 実装を読んで正確に)に §10.2 を合わせる**」】**
> **前版は「`t` の列と `判定` の列を置かない」としか書いておらず、落とすと明記していたのは
> ほかに `有限な複製の本数` だけだった**(:2169)。**実測**(本版、`scripts/o3c_reaction_judge.py` の
> `OBS_DROPPED_COLS`): **落ちている列は `t` / `ブートストラップSE` / `有限な複製の本数` /
> `CI下限` / `CI上限` / `バー近傍` / `判定` の 7 列である。**
> **文面を実装に合わせた**(**実装は変えていない。落とす列の一覧が正しいのは実装の側だった**)。

### 元: 10. 結果の読みで必ず出す表(§4.3) / 旧 行 2792-2799

> **【本版で `observation_only.csv` も替えた・prereg 監査(7 回目)の指摘 6。リードの決定の逐語:
> 「**`observation_only.csv` の MDE 列の見出しも「MDE(参考。族の α に入らない)」に。§10.2 を直す。テスト**」】**
> **6 回目の処置は感度の表だけ見出しを替えたが、`observation_only.csv` の 2,208 行も
> 576 にも α にも F1 の読みにも 1 行も入らない**(本節)。**同じ理由がこの表にも当たる。**
> **実装**: `REF_MDE_COL` / `OBS_HEADER` / `to_ref_mde_rows()`(旧名 `SENS_HEADER` /
> `to_sens_rows` は同じものを指す別名として残した)。**列の順と中身は変えていない。**
> **試験**: `tests/test_o3c_reaction_judge.py::test_the_observation_only_table_does_not_carry_the_family_alpha_in_its_mde_column`
> (**判定の 2 表は `MDE(α=0.05/576)` のままであることも同じ試験で測る**)。

### 元: 10. 結果の読みで必ず出す表(§4.3) / 旧 行 2931-2938

> **【本版で最小・最大にも同じ区別を当てた・prereg 監査(7 回目)の指摘 7。リードの決定の逐語:
> 「**「最小・最大」も検定したセルだけから取る。標準出力も同じ。§10.3 を直す。テスト**」】**
> **6 回目の処置は一覧からだけ「検定していないセル」を外し、隣の「最小」「最大」は
> `judge_rows` 全部から取ったままだった**(指摘 7)。**群が空で本数 0 のセルが混ざるので、
> 「最小 0」が出る。**標準出力にもその「最小」がそのまま出ていた。**
> **本版は鍵の名前も「最小(検定したセルだけ)」「最大(検定したセルだけ)」に変えた**
> (**同じ量に 2 通りの読み方が残らないようにするため**)。
> **試験**: `tests/test_o3c_reaction_judge.py::test_the_min_and_max_reps_come_from_tested_cells_only`。

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 3506-3511

> **【なぜ足したか】前版の関門は (c) だけを見ていた**(**7 回目の指摘 1 の実測**: 関数に §14.4 の
> 欄との突き合わせが無い)。**読みの側の突き合わせ(§14.6 の決定 2'''')は 6 本を走らせた後の段なので、
> **間違った(あるいは古い)L 番号のまま §14.1・§14.2 の 6 本を走らせても、456 日を 6 回
> 開け終わるまで何も止まらなかった。**
> **「一度だけ開ける」を守る機械も無かった**(6 回目の指摘 3 の実測。**規約で閉じていただけ**)。
> **(d)(e) がその機械である。**

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 3520-3530

> **【本版で現物に合わせた・prereg 監査(7 回目)の指摘 4。リードの決定の逐語:
> 「**§14.1 の「いまは書いていない / 18 鍵 / リードが開封の前に足す」を、現物(コミット 8bbce48 で
> 足した。params 19 鍵、`approval` あり)に合わせて書き直す**」】**
> **前版はここに「`scripts/o3c_reaction.py` は いまは `summary.json` に書いていない」
> 「`params` の鍵は 18 個」「リードが開封の前に足す」と書き、**実測**の印を付けていた。**
> **同じコミットの現物は 19 鍵で `approval` が入っている**(指摘 4)。
> **凍結した文書の中で「実測」の印を付けた記述が現物と違っていた**(`CLAUDE.md` §0.2 の O-3 の型)。
> **走行に渡した番号が `params` に残る経路は試験でも測っている**:
> `tests/test_o3c_reaction.py::test_run_table_records_the_real_approval_number`
> (**`run_table` に `approval="L-999"` を直接渡し、`summary.json` に書かれることを測る**
> = 7 回目の指摘 3)。

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 3677-3694

   > **【名前の対応付けをリードの返答に合わせた・prereg 監査(7 回目)の指摘 16。リードの決定の逐語:
   > 「**§14.4 の待つものの名前を**リードの返答と同じ対応付け**に直す: ア = 判定のバー
   > (t ≥ 2.0 → |t| ≥ 3.925)/ イ = 判定に使う走行 2 本 / ウ = ブートストラップの区間(正規近似)/
   > エ = 対照 (ii) の向きの要る量の符号 / オ = バーの数値(z 1 本、3.9247896)/
   > カ = 判定の分岐(判定語 7 語)/ キ = F1 の整合・反証の条件 /
   > ク = 対照 (ii) の群の分け方(24 群は相手の符号、14 群は受け継ぐ)/ ケ = 主軸 D の欠け /
   > コ = 関門なしのサニティ #13 / サ = 指紋の台帳の作り直し / シ = WIRED 未記載 /
   > ス = MDE の p の誤差 / セ = 凍結 6 項目 / ソ = 2 周目の材料。
   > 答えが要るのは カ・キ・ク、報告は ア〜オ、事実は ケ〜ソ。§13 の表の並びもこの順に**」】**
   > **前版は §13 の表の並び順のまま ア〜ク を振っており(委任先が決めた対応付け)、
   > 「答えが要る 3 件」がリードの返答(判定語・F1 の条件・対照の作り方)と一致していなかった**
   > (指摘 16。**オーナーが同時に見る 2 つの一覧が食い違う形だった**)。
   > **本版はリードの対応付けが正で、§13 の表の並びもこの順に直した。**
   > **答えが要るのは カ・キ・ク**(判定の分岐 / F1 の整合・反証 / 対照 (ii) の群の分け方)、
   > **報告は ア〜オ と ケ**(**本版で ケ を報告の側へ移した** = prereg 監査(8 回目)の指摘 15)、
   > **事実として出すのは コ・サ・シ・ス・セ・タ・ソ・チ の 8 件**である
   > (**タ は 8 回目の指摘 18 で足した。****チ は本版で足した** = 9 回目の指摘 18)。
   > **上の分類の表と同じである。**

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 4367-4371

> **【なぜ書き直したか】前版のこの欄は「**1 列だけ足した(下)。それ以外は変えない**」で、
> 下の注も `matched_liq_id` 1 列の話しかしていなかった。**
> **実際の変更は 2 件で、標本 4 本は 2 度目にも走らせ直され `MD5SUMS` が 4 本とも変わっていた**
> (prereg 監査(7 回目)の指摘 5。**`MD5SUMS` は「結果を後から差し替えていないことを見るため」の
> 機械なので、走らせ直した回はここに記録が要る**)。**本版で 3 度目も含めて表にした。**

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 4547-4552

> **【なぜ変えたか】前版はこの 3 つのどれか 1 つでも破れると「表を 1 枚も書かずに終わる」形で、
> **感度 1 本の食い違いでも 576 行すべてが読めなくなった**(7 回目の指摘 2)。
> **456 日を 6 本開けたうえで 1 行も出さずに 1 周目が終わる、という帰結が走行の前に決まっていた。**
> **感度の行は 576 にも α にも F1 の読みにも 1 行も入らない**(§10.2)ので、**判定を止める理由が無い。**
> **判定の側については止め方を変えていない**(表は 1 枚も書かない)。
> **変えたのは「なぜ」の材料を残すことだけである**(`stopped.txt`。§10.3 の表)。

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 4641-4651

> **【本版で実測の記述に替えた・prereg 監査(7 回目)の指摘 14。リードの決定の逐語:
> 「**「全操作を拒否する」を実測の記述に替える: 4 回目の処置で委任先が
> `scripts/_research_audit_gate.py` を編集したところ PreToolUse のフック
> (`.claude/hooks/_verify_manifest.sh` / `deny_protected_paths.sh`)が以後の Edit/Write を止めた
> (処置書 `_r4.md` の 9 の経緯を引用)。試していないなら「未確認」と書く(処置書 r4 を読んで判断)**」】**
> **前版は「変更するとフックが全操作を拒否する」と書いていた。**
> **これは設定の読みではなく、4 回目の処置での実測である**(処置書 r4 の 9 を読んで確かめた)。
> **ただし止まったのは「`Edit` と `Bash`」であって、「全操作」という語は r4 の実測より広い。**
> **本版は r4 の逐語に合わせた。**
> **`.claude/hooks/deny_protected_paths.sh` がこの拒否に関わったかは、r4 の記録からは分からない**
> (**未確認**。r4 が名前を挙げているのは `_verify_manifest.sh` だけである)。

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 4707-4713

> **【なぜ分けたか】7 回目の処置は感度の `approval` の食い違いを「その走行の表だけ落とす」で
> 済ませていた。****しかし approval の食い違いは「オーナーの応答と違う番号で 456 日を
> 1 本開けた」ことを意味する**(8 回目の指摘 14)。
> **表の落とし先(`summary.json`)以外に止まる所が無かった。**
> **開封は一度きりなので、そこで止めてオーナーに報告する。**
> **試験**: `tests/test_o3c_reaction_judge.py::test_the_sensitivity_runs_also_match_the_approval_number`
> (**止まる側**)/ `::test_a_sensitivity_run_with_the_right_approval_still_passes`(**通る側**)。

### 元: 14. 走行の手順(リードが走らせる。委任先は走らせない) / 旧 行 4822-4827

> **【本版で数え直した・prereg 監査(7 回目)の指摘 13。リードの決定の逐語:
> 「**§14.6 の試験の数を現物(3 本で 116 件: reaction 51 / judge 53 / wired 12。全体 2,098)に直し、
> 今回足した分も反映して最終値を書く**」】**
> **前版は 6 回目の欄で「judge 45 → 53 件」「wired 12 件のまま」としか書かず、
> **走行の道具側の試験(`tests/test_o3c_reaction.py`、6 回目に 1 件足して 51 件)を書いていなかった**
> (指摘 13)。**本版は 3 本の内訳とリポジトリ全体を書く。**

