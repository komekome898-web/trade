# 検収: 道具サーベイ 区分 8 の 21 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 21 回目の実行(2026-09-26)`(37793 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run21.log`(289,616 バイト)。届いたままのコミットは台帳の取り込みの 1 つ前。起動文の印 `20260923_tools_survey_cat8_run21_prompt.md@87cc1becbcce`。

## 0. 費用と時間

調査班 625,413 トークン・3,394,027 ms(56.6 分)・道具 287 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 21` 0 件 / `check "" run21.log` 0 件 / `check_scan_report.py`(run1〜run21)40 件。21 回目の節の中は 38390 行の K13 の 1 件(「dbt の根拠が 7 行で同じ文言の複写」)。過去の節から消えた行は 0 件。

## 2. 台帳

`import --round 21`: 8-030 は `未着手` → `浅い`、E1a 印/4・E2 印/4・E3a なし・E3b なし・E5 印/3・E6 印/3、E1b・E4 は `未判別`(見積もり 7,878 行・9,472 行)。そのあと処置 2 で `set 8-030 段_E1a=3`。`check` 0 件。深掘り 27 / 残り 10(8-030 は `浅い` のまま)。

## 3. 中身の監査(102 回目、owner-auditor)の逐語

> 監査102回目。対象: docs/DATA/SCAN_2026-09-23_tools_cat8.md の `## 区分8 — 21 回目の実行(2026-09-26)`(37793行目〜末尾、8-030 dbt)と生ログ docs/DATA/probes/20260923_tools_8_run21.log。
>
> 以下、指摘のみを列挙する(最大25件、severityは[止める]/[直す]/[聞く])。
>
> 1. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:38407,38414,38415,38429,38430 — 8-030の隔離venvへの`pip install dbt-core==1.12.5 dbt-duckdb`(実測、time_s=26.83、38407)を実行したが、その前提となるべき委任文の「導入前の検査」(docs/DATA/delegations/20260922_tools_survey_prompt.md:99「`pip download --no-deps`で取った中身を開いてsetup.py/pyproject.tomlの導入時実行・難読化・外部URLからの取得・バイナリ同梱が無いか」、同:101「実行するのは…それも§6-1の検査のあと」)を行っていない。§4.0の該当4項目(wheel展開38414・setup.py導入時実行38415・難読化38429・外部URL取得38430)はいずれも「この回では実施していない」で埋められており、実行(install)が先、検査が後(未実施のまま)になっている。起動文docs/DATA/delegations/20260923_tools_survey_cat8_run21_prompt.md:59は最小実行の手順として委任文§5のみを指し、§6-1を名指ししていない――この抜けは起動文を書いたリード側の書き落としではないか。この単位を「浅い」のまま先に進めてよいか、それとも§6-1を先に済ませてから最小実行をやり直すべきか。(出典: 委任文§6-1・§6-3。研究規律的にはP8「規則は書くだけでは機能しない」に相当)
>
> 2. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:38119(要素と段のE1a行)・38453・38455(判断に迷った点と問い) — E1a=印/段4の根拠に`dbt-audit-helper`(README「Generates a row-by-row comparison…」、外部テーブルを持ち込める)を使っているが、`dbt-audit-helper`はdbt本体に同梱されず利用者が別途`dbt deps`で導入する外部パッケージであり、L-508「候補が同梱・呼び出している別のライブラリの機能は数える」に当たるかを調査班自身が問い#3(38455)で「この読み方が正しいかはリードの判断を仰ぐ」としている。にもかかわらずE1aの値・段はすでに「印/段4」で確定・報告されている。同型が問い#1(38453、N範囲=main+1.latestの両枝を含めるか)にも現れ、「値・段は決めたまま(両方の枝を検索対象に含めた状態)にする」と明記している。起動文§2.2(docs/DATA/delegations/20260923_tools_survey_cat8_run21_prompt.md:41)「問いに出した行は、値も段も`未判別`にする(決めてから問いに出さない)」に反していないか。E1aの段(4か3か)はこの読み方次第で変わりうる。
>
> 3. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:38222-38332(E3a当たりの判定)・38338-38380(E3b当たりの判定) — 111ファイル/149行・43ファイル/53行のうち大半が「述語に当たらない理由」欄に同一の定型文(例:「ソフトウェアの情報/メモリ漏洩(credential・token・memory・state等)の意味で、その時点で知り得ない情報の使用を検出・報告する機能ではない」)を複写している。起動文docs/DATA/delegations/20260923_tools_survey_cat8_run21_prompt.md:54「行の表・ファイルの表には、同じ文を繰り返さない…同じ文を何行・何ファイルにも当てた`なし`は受け取られない」に照らすと、この書き方のままE3a・E3bの`なし`を受け取ってよいか。check_scan_report.pyのK13はこの「当たりの判定」表(甲)を検査対象にしておらず(この回でK13が捕まえたのは§4.0の表1件のみ、38488)、機械の関門を素通りしている。
>
> 4. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:37828(知見#7、E1b)・37832(知見#11、E4) — 案B(docs/DATA/surveys/CAT8_DESIGN.md:37-38)は「未判別の要素には、印を探した範囲(読んだ文書・目次・ソースの範囲)と当たりの行の数が書いてある」ことを求めるが、E1b・E4の知見はいずれも見積もりコマンドの行数のみを示し、印を探すために実際に読んだ文書・目次・ソースの範囲を書いていない。生ログ(docs/DATA/probes/20260923_tools_8_run21.log:204-244付近)にも、見積もりコマンド以外にREADME・目次を読んで印を探索した記録が見当たらない。起動文§1(docs/DATA/delegations/20260923_tools_survey_cat8_run21_prompt.md:25)「その要素は`印`を探し尽くしてから、見積もりの手を打って`未判別`にする」を満たしているか。
>
> 5. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:37826(知見#5) — 起動文§1(docs/DATA/delegations/20260923_tools_survey_cat8_run21_prompt.md:22)は大きさ上限超過時に「除いたファイルの数」を書くよう求めるが、知見#5は「除いたファイル(`website/static`ほか)はNから抜けている」と書くのみで数を示していない。
>
> 6. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:38464,38488-38518 — この回で新規に出たK13の1件(38390、dbtの根拠7行の複写)を、6項目(wheel展開・setup.py導入時実行・難読化・外部URL取得・導入可否・install所要秒)が同じログ範囲(1050-1054)を指すための機械的誤検出として自己判定して閉じている。前例(NinjaTrader等)と同型に見え妥当そうだが、そのうち少なくとも4項目(wheel展開・setup.py導入時実行・難読化・外部URL取得)は指摘1の§6-1未実施という共通の実質的な未達成理由を持つ。誤検出かどうかを判定するのはリードである(委任文docs/DATA/delegations/20260922_tools_survey_prompt.md §12「判定するのはリードである」)。調査班自身がこの場で自己判定して閉じてよいか。
>
> 止める指摘: あり(1・2)。

## 4. 処置(リード、監査 102 回目)

- **リードの誤り 1(指摘 1)**: 21 回目の起動文 §2.5 で最小実行を「委任文 §5 の手順」とだけ書き、§6-1(導入前の検査)と §6-3(実行は §6-1 の検査のあと)を名指ししなかった。監査 101 回目の問い 2(安全の規則が足りているか)にも答えていなかった。
- **リードの誤り 2(指摘 2)**: 21 回目の起動文 §1 に「公式の文書が製品の一部として案内しているものを候補の機能に含める(L-508)」と書いた。L-508 の逐語は「候補が**同梱・呼び出している**別のライブラリの機能は数える」で、利用者が別に入れる公式のパッケージはそこに無い。**リードが導いた読みを、オーナーの決定の出典を付けて書いた**(§0.2 の B の型)。22 回目以降の起動文で消す。

1. **止める → 次の回で直す**。22 回目(dbt の残り)で、入れた 2 つの配布物(`dbt-core==1.12.5`・`dbt-duckdb`)に §6-1 の検査を全部行う(`pip download --no-deps` で取って中身を開く)。何か見つかれば、状態を `危険で導入停止` にし、`venvs/8-030/` の導入物を消し、実測に依る値(E2 の実測・最小実行・導入可否など)を文書だけの値に戻す。何も無ければ、順番の違反を記録したうえで実測の値を残す。それまで、21 回目の実測の値は「§6-1 の検査の前の導入による」と、この検収に書いておく(台帳の値は変えない。E2 の `印` は文書の逐語でも立つ)。導入した環境で常駐しているプロセスは無い(`dbt test` は終了している)。**以後の起動文では、最小実行の前に §6-1 の検査を全部済ませ、その手を生ログに出すことを名指しで書く。**
2. **止める → リードが L-508 の逐語で決めた**。`dbt-audit-helper` と `dbt-utils` は dbt に同梱されず、dbt が自分で呼び出すものでもない(利用者が `packages.yml` に書いて `dbt deps` で入れる)。L-508 の「同梱・呼び出している」に当たらないので、dbt の機能に数えない。E1a の `印` は、dbt 本体の単体テスト(`docs/build/unit-tests` の逐語「actual differs from expected」= モデルの出力を利用者が書いた期待値と照らして差を出す = 「実装と参照値」)で立つ。これは候補が自分自身を試験する仕組み(L-516)ではなく、利用者が自分のモデルに掛ける機能。対象はそのプロジェクトのモデルに限られるので **段 3**(台帳を `set 8-030 段_E1a=3`)。問い 1(main と 1.latest の両方の枝を N に含める)は、どちらも候補の公式のソースの枝で、1.latest は `pip install dbt-core` で実際に入る版なので、含めるのが正しい(値・段に影響しない)。`dbt-audit-helper`・`dbt-utils` は別の候補になりうる名前として、区分 8 の次の辿る一覧に回す。
3. **受け取る(リードが読んで決めた)**。ファイルの表の理由の欄は、全部の行でそのファイルの当たった行を逐語で引き(`L990「…leaks the synthetic id…」`など、行ごとに違う)、そのあとに語の意味の分類を付けている。分類の語尾は繰り返しだが、行を読んだことは引用で確かめられる。まったく同じ文の 16 ファイルは、`crates/dbt-docs-server/web/dist/assets/` の下の構文の色付けの定義(`angular-ts-*.js`・`dracula-*.js`・`typescript-*.js` など)で、リードが道を 16 本とも見て同じ種類のファイルであることを確かめた。E3a・E3b の `なし` を受け取る。
4. **次の回で直す**。22 回目で、E1b・E4 について `印` を探した範囲(読んだ文書・目次・ソースの範囲)を書かせる(案 B の記録。設計票 §2)。
5. **次の回で直す**。22 回目で、除いたファイルの数を書かせる。
6. **誤検出と判定する(リード)**。K13 は、6 項目の根拠の欄が同じ生ログの範囲(1050-1054)を指すことに当たったもので、根拠の文の複写ではない(NinjaTrader と同じ型)。ただし、そのうち 4 項目の中身の未達は指摘 1 の実の問題で、処置 1 で扱う。

## 5. これからの手

22 回目 = 8-030 dbt の残り(§6-1 の検査、E1b・E4 の案 B の記録、除いたファイルの数、§4.0 の `未確認` のうち起動文が認めない理由のもの = wheel展開・setup.py導入時実行・難読化・外部URL取得・宣伝詐欺の兆候・時刻の扱い・保守者名の一貫性・課金開始条件)。23 回目 = 8-033 Prefect。
