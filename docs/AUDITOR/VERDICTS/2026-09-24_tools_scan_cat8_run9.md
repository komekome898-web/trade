# 検収: 道具サーベイ 区分 8 の 9 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 9 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run9.log`(681 行・52 手)。届いたままのコミット 8369f21。
起動文 `20260923_tools_survey_cat8_run9_prompt.md` の指紋 `9bc2d15dbaf1`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **551,916 トークン・2,317,790 ms(38.6 分)・道具 233 回**。生ログの最初の手 07:54:29Z / 最後の手 08:27:16Z。§2 の項目すべてに手が付いた。描画の道具は使っていない。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 9 本)0 件 / `check-elements --round 9` 0 件(問いの印・問いの行の `未判別`・`なし` の要素の語・`/tmp` の直下・段 4/5 の引用・7 列目の形・引用と生ログ・`台帳の値のまま` と台帳の照合を含む)/ `check "" <run9.log>` 0 件 / 消えた行 0。

## 2. `[それ以外の問い]` の行(監査 46 回目の答え 2: 1 件ずつ読む)

| 問い | 読んだ結果 |
|---|---|
| 2(8-016 の §4.0 の語彙の表をどう扱うか) | 値・段を決めずにおく理由ではない(§4.0 の表は状態の条件)。印のとおり `[それ以外の問い]` でよい |

## 3. リードが台帳で直したもの

| 行 | 報告 | 直した値 | 理由 |
|---|---|---|---|
| 8-011 E5 の段 | 5 | **4** | 段 5 の (ア) の根拠が `seed` の引数。乱数の種は (ア) の「判定の基準(許容誤差・閾値・検出の条件)」に当たらない(監査 21 回目の処置 8 で決めた読み。台帳のほかの行にも同じく当てている)。段 4 の条件(汎用の DataFrame・任意の戦略の関数に掛かる)は報告の原文で支えられる |
| 8-016 E3a・E3b・E5 | `なし`(一覧 169 件 / 読んだ 169 件) | **`未判別`**、状態を `判別に一次資料が要る` に戻した | (1) 組織の総数 178 件のうち 9 件を「README が無い」で除外した。除外してよいのは画像・ロックファイル・バイナリだけ(起動文 §2)で、README の無いリポジトリにもファイルはある。(2) 読んだのは 169 件の **README だけ**で、各リポジトリの文書・ソースの全部ではない(`なし` と書くための条件 (a))。総数 178 件を `ecosyste.ms` で確定できたのは前進(7・8 回目は 100 件で頭打ち)。10 回目で、178 件の全ファイルの一覧を取れるか試す |

## 4. 台帳

`import --round 9`: 変わった所 20。§3 を `set` で直した。`check` 0 件。`recount`: 深掘り 12 / 浅い 1(8-014)/ 判別に一次資料が要る 1(8-016)/ 危険で導入停止 1 / 登録が要る 1。**残り 2 行**(8-014・8-016)。

## 5. 判定

**一部受け取り**(§3 の 4 セルを直した)。新しい `印` と段の決定が多い(8-010 E3b・8-014 E6/E1b/E3b/E4 の段・8-002 E2/E5・8-003 E5・8-004 E2・8-007 E6・8-011 E6・8-012 E5)ので、中身の回として監査役(47 回目)に通す。

## 監査 47 回目(中身の回。対象 = 8369f21 の 9 回目の節と生ログ)

監査役の出力(指摘の逐語。[止める] 2 件・[直す] 1 件・[聞く] 2 件。リードの検収の 2 つの直し(8-011 E5 段 4・8-016 の `未判別`)は「妥当」、問い 1 の扱いは設計票と整合、問い 2 はリードの裁量):

> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3280`(および同一の記述 `:3167`)— 8-011 `arXiv:2512.12924` の E6 段2 の根拠は「`hdt/stats.py` の `minimum_track_record_length`・`deflated_sharpe_ratio`・`combinatorially_symmetric_cv`・`pbo_from_train_test_pairs` をすべて再確認した」と書き、生ログの行として `docs/DATA/probes/20260923_tools_8_run9.log:492-519` を引く。しかし実際にその範囲(`run9.log:492-519`)を読むと、これは E5 の根拠として引かれている `hdt_validation.py` の `WalkForwardValidator.__init__`/`validate`(`train_window_days`・`market_data`・`generator`・`seed` 等)であり、`hdt/stats.py` にも `minimum_track_record_length` 等の関数名にも一切触れていない。`run9.log` 全体を `grep -n "stats.py\|minimum_track_record\|deflated_sharpe\|combinatorially_symmetric\|pbo_from_train"` で検索しても 0 件で、この回に「再確認した」ことを示す手が生ログのどこにも存在しない。この段2の判定そのものの当否ではなく、**「再確認した」という主張を支える生ログが引用箇所に存在しない**という citation の不一致である(P10 — 目視・記憶による要約と原典の再照合の欠如。近い型として KA-04 の出所不明にも当たる)。
>    - 付随して: `scripts/cat8_ledger.py` の `check-elements` の引用検査(`quotes_in_log`、380〜383行・544行・560行)は、6 列目に日本語の corner bracket「…」で引いた文だけを生ログと照合する仕組みで、この行のようにバッククォートの関数名(`` `hdt/stats.py`の`minimum_track_record_length` ``)で書かれた主張は照合対象に入らない(544行は段 4・5 のときだけ「…」引用を要求し、この行は段2なので対象外)。段2でも「再確認した」という**事実主張**をしている以上、この抜け道で検査が 0 件を返したことをもって根拠が確認されたと読むのは危険。
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3298`(E1b 段4)・`:3301`(E3b 段4)・`:3304`(E6 段4)— 8-014 `Fincept Terminal` の 3 つの段4判定は、いずれも `Analytics/python_skfolio_lib/skfolio_validation.py` の `ModelValidator`/`CrossValidationConfig` を根拠にしている。ところが `:3301` の本文自体が「`skfolio.model_selection` の `CombinatorialPurgedCV` を介して」と明記しているとおり、purge/embargo の実体は Fincept Terminal 自身のコードではなく、外部の別パッケージ `skfolio`(PyPI 上の独立ライブラリ)の機能である。E6 の統計検定(`_perform_statistical_test`)も `scipy.stats` ベースの薄いラッパーで、実体計算がどこまで Fincept 自身のものかは検討されていない。設計票 §3 は「述語は候補(道具)の機能に当てる」、起動文 §2 も同じ文言を繰り返しているが、この行では**同梱の別ライブラリの機能か候補自身の機能かを一度も問いに出していない**。同じ回・同じ候補(8-014)の E3a では類似の境界(LLM のプロンプト指示を「機能」と読んでよいか)を「値・段は未判別のまま、問いに出す」と扱っているのに、機能の帰属という同型の境界問題をE1b/E3b/E6では素通りして値を確定させている(出典: `docs/DATA/surveys/CAT8_DESIGN.md` §3)。
>
> 3. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3145`(知見#8)・`:3298`(E1b 段4)— E1b の根拠は `self.performance_metrics = ["sharpe_ratio", "annual_return", ...]` という**属性の初期化リスト**のみで、これらの指標を実際に計算する関数のコードは生ログに一度も示されていない(`run9.log` を `performance_metrics|sharpe_ratio|def.*performance` で検索しても該当箇所はリスト定義の1箇所だけ)。同じファイル・同じクラスの `validation_history` については「コンストラクタで初期化されるだけで一度も代入されない」ことを別の知見(#5)で確かめているのに、`performance_metrics` が実際にどこかで使われて計算されているかは確かめていない — 同じクラス内で同種の「宣言されているが使われているか未確認の属性」に対する検証の厳密さが一貫していない。
>
> 4. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3269`(要素と段の表、E3b)— 根拠は「look-back buffer(2018–2019)と評価期間(2020–2024)を論文全体の評価設計として時間的に分離する」という一次資料の逐語(生ログ `run9.log:32-36` で確認、内容は原文と一致)。監査で当該論文の前後文脈(`scratchpad/cat8/run9/2603_20319_wrapped.txt:190-191`)を読むと、この記述自体は確かに全15戦略の評価期間の設計に掛かっており、8回目に否認された「BM08だけの21日embargoギャップ」(ML カテゴリの4バリアントだけに掛かる、と原文 `2603_20319_wrapped.txt:165` で確認)とは別物である点は整合している。ただし、これは**1回きりの固定の学習・評価split**であり、評価期間(2020-2024)の**内部**で「その時点で知り得ない情報」の混入を反復的に防ぐ仕組み(purge/embargo/ウォークフォワードの繰り返し)ではない。設計票 §3 の E3b 述語「その時点で知り得ない情報が**入らないようにする**機能」に、固定split(学習後は評価期間中ずっと同じモデルを使う前提)が当たるかは読みが割れうる(段1なので影響は小さいが、`印` を付けたこと自体の当否に関わる)。
>
> 5. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3316`・`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9.md:25` — th2-net 組織の総数 178 件(ページ1=100・ページ2=78・ページ3=0)は `run9.log:180-197` 付近で実際に `curl` の出力として確認でき、生ログで支えられている。監査役が同じ経路(`https://repos.ecosyste.ms/api/v1/hosts/GitHub/owners/th2-net/repositories` および単一リポジトリのエンドポイント)を実際に叩いて試したところ、このAPIが返すのはリポジトリの**メタデータ**(readme の有無・ライセンス・stars 等)であり、リポジトリ内の**全ファイル(ソースツリー)一覧**は返さない。したがって、リードの検収が書いた「10 回目で、178 件の全ファイルの一覧を取れるか試す」は、この経路(ecosyste.ms)のままでは届かない可能性が高く、別の経路(GitHub の Git Tree API・`gh api --paginate owner/repo/git/trees` 等、オーナー PC の認証トークンが要る可能性)の準備が必要になりそうだと事前に書いておいた方がよい。

**リードの処置**:
1: 認める(引いた行がその主張の手でない。検査は「…」の引用しか照らさないので通った)。台帳で 8-011 E6 の段を `未判別` にした。10 回目の起動文で「根拠の欄で『確かめた』『再確認した』と書くときは、その手の行を 7 列目に書き、確かめた文を「…」で引く」と足す。
2・3: 認める。同梱の別のライブラリ(`skfolio`・`scipy`)の機能を候補の機能と数えてよいかは、設計票に書かれていない(述語の読みの問い)。台帳で 8-014 E6 を `未判別`、E1b・E3b の段を `未判別` にした。区分の締めでオーナーに見せる問いに入れる。
4: 認める(固定の分け方を「入らないようにする機能」と読んでよいかは読みの問い)。台帳で 8-010 E3b を `未判別` にし、状態を `浅い` にした。オーナーに見せる問いに入れる。
5: 答える。10 回目の 8-016 は、ほかの候補で使えている `git clone --depth 1`(公開のリポジトリの取得は、7〜9 回目に qf-lib・VectorBT・PineForge などで使えた)で 178 件を取り、全ファイルを検索する。取れなければ、オーナーの PC から取る手順を書く。

台帳: `check` 0 件。**残り 3 行**(8-010・8-014・8-016)。
