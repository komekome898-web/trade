# 検収: 道具サーベイ 区分 8 の 25 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 25 回目の実行(2026-09-26)`(40313 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run25.log`(38,774 バイト)。届いたままのコミット 2b4cb4c。起動文の印 `20260923_tools_survey_cat8_run25_prompt.md@8ebc364c6d81`。

## 0. 費用と時間

調査班 313,754 トークン・963,914 ms(16.1 分)・道具 109 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 25` 0 件 / `check "" run25.log` 0 件 / 過去の節から消えた行 0。`check_scan_report.py` 46 件(この回の節の中に新しいものは無い、と調査班。リードは件数だけ確かめた)。

## 2. 台帳

`import --round 25`: 変わった所 0。リードは値を変えていない。

## 3. 中身の監査(113 回目、owner-auditor)の逐語

> ## 監査結果(113回目・区分8・25回目)
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 25回目の実行(2026-09-26)`(40313〜40819行目)、生ログ `docs/DATA/probes/20260923_tools_8_run25.log`、起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run25_prompt.md`、24回目検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run24.md`、設計票 `docs/DATA/surveys/CAT8_DESIGN.md`。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md` 40313〜40819行目(全体、該当箇所なし。生ログ `20260923_tools_8_run25.log` にも該当なし) — 起動文§7(`.../run25_prompt.md` 96〜98行目)は「返す前に」手順2〜4として `cat8_ledger.py check-elements --round 25`・`cat8_ledger.py check "" ...run25.log`・`git diff -U0 ... | wc -l` の**出力を同じ節に貼る**ことを義務付けているが、25回目の節にはこの3つの出力が一切無い(`check-elements|check ""|git diff -U0` で節・生ログとも該当0件を機械的に確認)。貼られているのは§7手順1の `check_scan_report.py` の出力(40756〜40819行目)だけ。これは24回目検収の指摘1(全く同じ欠落)がリードによって「直した」と処置されたはずの型であり、25回目でそのまま再発している(直しの効果が確認できない)。
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md` 40735行目(§4.0で未確認のまま残した項目「時刻のミリ秒単位の具体的挙動」) vs 起動文§2.5(64行目)「『未確認』のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたものと、試すのに当方の鍵・口座・登録・購入が要るか外へ送信するもの…だけ。**『この回では実施していない』『読んでいない』は理由にしない。**」— 40735行目の理由は文字どおり「実際にミリ秒精度のタイムスタンプを渡して…確認する実行はこの回では行っていない(…この回は§1で指定された3項目のうち文書を読む方を優先し、新しい実行は追加しなかった)」であり、試して届かなかった記録ではなく単なる不実施の申告。24回目検収の指摘7が名指しした違反型が、それを明示的に禁じたこの回自身の起動文§2.5に反してもなお再発している。隔離venv `venvs/8-033/run/` は既にあり(起動文§1の3、23行目「動かしてよい」)、試みる手段自体は存在した。
>
> 3. [直す] 40647・40648・40652行目(`Prefect` E1a・E1b・E4 の「要素と段」表、根拠欄) — 設計票§2「案Bのリードの当て方」(1)は「未判別の要素の全部について…『印を探し尽くした範囲(読んだ文書・目次・ソースの範囲)』と『全件の当たりの行の数』が書いてある」ことを条件とするが、この3行の根拠欄は本体だけの見積もり行数・ファイル数のみで、印を探した際に何を読んだかの範囲(24回目の同じ行にあった「知見8」「知見11」への参照)が無く、ただ「(案Bの記録)」とだけ書かれている。24回目の行は具体的な知見番号を指していたのに、この回で更新した際にその参照が失われている(起動文§1の1は「当たりの行の数を、この数に直す」としか指示しておらず、読んだ範囲の参照を消してよいとは書いていない)。
>
> 4. [聞く] 40744〜40745行目(判断に迷った点1、E2のSLA除外の当て方) vs 起動文§2.2(44行目)「問いに出した行は、値も段も『未判別』にする(決めてから問いに出さない)…値・段を決めずにおく理由でない問いは『それ以外の問い』」— 調査班はSLAを列挙データの時刻ずれと区別してE2の`印`に数えない、と**決めたうえで**問いに出しており(「…と判定してE2の`印`にはしなかった」)、この切り分けの当否次第でE2の値(未判別のまま/印になる)が変わりうる。決めてから問いに出す型でありながら`[それ以外の問い]`と分類しているのは、§2.2の値・段の問いの定義に照らして正しいか。
>
> 5. [直す] 生ログ `docs/DATA/probes/20260923_tools_8_run25.log` 全体(該当箇所なし) — 起動文§2.6「生ログと報告は…90MB以内に保つ(`wc -c`を検索の手の前後に`cat8_step.py`で打つ)」の実施記録が一件も無い(`grep -n "wc -c"` 0件)。この回のログの実サイズでは実害が生じる規模ではないが、手順としては一度も打たれていない。
>
> (参考、指摘としては数えない)知見1(本体だけの写し・N=4,831)、知見2(E1a=1,664/499・E1b=1,490/479・E4=3,327/522)、知見3(E2=33,551/2,273)は生ログ2-34行目の実測値と完全一致。/ 知見4(E2の印を探し尽くした過程)は生ログ35-294行目の実際の検索・目次・SLA頁の読みと一致し、案Bの「読んだ範囲」記載も40649行目に具体的に書かれている(E1a・E1b・E4と対照的にE2はここが揃っている)。/ §4.0「登録の要否」「宣伝詐欺の兆候」の2件は`cat8_render.js`・`curl`・`x_fetch.py`による実際の取得(生ログ295-351行目)に基づき、要約のみでの判定ではない。/ コミット数の`未確認`は24回目の実測(プロキシ403)を引き継いだもので、新規の「この回では行っていない」型ではない。/ 起動文の範囲外のファイルへの書き込みは無い(コミット`2b4cb4c`は `SCAN_2026-09-23_tools_cat8.md`・`run25.log` の2つのみを変更、`git show --stat 2b4cb4c` で確認)。
>
> 止める指摘: あり(2件、#1・#2)。

## 4. 処置(リード、監査 113 回目)

1. **止める → 直した(記録)。仕組みを変える**。リードが 3 つの検査を打ち直し、出力を報告の節の末尾の「リードの追記」に貼った。**24 回目の処置 1 で「直した」と書いたのは、その回の節に貼ったことだけで、次の回で起きないようにはしていなかった**(A-17 の型)。調査班が貼り忘れても、リードは検収で必ず打ち直している(19 回目以降、毎回)。起動文の文をさらに強めても止まらないことは 24・25 回目で分かったので、**以後、§7 の 2〜4 の出力は検収でリードが打って貼る手順にする**(起動文には残すが、貼られていなくても検収で補う。貼られていたら、リードの打ち直しと一致するかを見る)。
2. **止める → 答える**。§4.0 の項目「時刻の扱い」は、実装を読んだ一次資料の値で埋まっている(40735 行の前の表の行)。「ミリ秒単位の具体的挙動」は、調査班が項目の外に足した細目で、「この回では行っていない」は起動文が認めない理由。項目の値には効かないので、細目の未確認として残し、§4.0 の項目の数には入れない(リードの追記)。
3. **直した**。E1a・E1b・E4 の読んだ範囲(24 回目の知見 #8・#11)と、この回の本体だけの数を、リードの追記で結んだ。
4. **答える**。SLA はワークフローの実行の遅れを検出する機能で、E2 の述語の列挙(時系列・約定・板・足・参照データ)の時刻のずれではない(24 回目の検収 §4 の処置 5 と同じ当て方)。E2 の値は、どちらに決めても 26 回目の時点では `未判別` のまま(`印` にするには列挙のデータを言う逐語が要る)なので、値は変わらない。分類は `[値・段の問い]` が正しかったが、値に効かないので直さない。
5. **直した(記録)**。リードが `wc -c` を打って大きさを追記に書いた。

**8-033 の扱い**: 状態は `浅い`。`未判別` は E1a・E1b・E2・E4 の 4 つで、4 つとも案 B の記録(読んだ範囲と、本体だけの当たりの行の数)がある。§4.0 の表の項目は 43 のうち 42 が一次資料か実測(`未確認` はコミット数だけ。24 回目の節の 5 列目の数え 一次資料 18・実測 21・未確認 4 のうち、未確認の 3 つがこの回に 実測・実測・一次資料 で埋まった)。設計票 §2 の案 B の当て方で、**残りに数えない。残り 9 → 8**(8-026 OpenClaw・8-029 MetaTrader・8-031 Debezium・8-032 Kafka・8-034 Pandas・8-035 Spark・8-039 NumPy・8-040 SciPy)。

## 5. これからの手

26 回目 = 8-031 Debezium(未着手、1 行だけ。24 回目の起動文の形)。そのあと 8-032 Kafka、8-034 Pandas、8-035 Spark、8-039 NumPy、8-040 SciPy、8-026 OpenClaw の残り、8-029 MetaTrader の E1a。
