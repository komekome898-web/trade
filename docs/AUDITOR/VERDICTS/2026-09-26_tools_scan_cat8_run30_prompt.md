# 監査: 道具サーベイ 区分 8 の 30 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md`(8-032 Apache Kafka の E2・E4 だけ。29 回目の起動文から作った)。

## 監査 123 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run30_prompt.md@e7ca9b4ee0ea`)の逐語

> 監査 123 回目の指摘です。
>
> 1. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md:21` — E2・E4の`印`探索の手順で、ソース側は「ファイルの名前の一覧から関わりうるディレクトリを選び、そこだけ全部読む」としているが、「関わりうる」の判定基準が起動文に一切定義されておらず、選定は調査班の裁量に委ねられている。29回目はhead/tail・20,000字での機械的な打ち切りが指摘された(29回目検収§3指摘2)が、この回はディレクトリ単位での範囲の絞り込みという別の形の狭め方に置き換わっている。E2・E4が「未判別」に落ちたとき、それが対象に本当に印が無いためか、選定した部分集合にたまたま無かっただけかを、起動文の文言だけからは区別できない。A-10(族・スコープを説明なく縮める)・P1(「無い」は全経路を試した結果でなければ言えない)の型に近くないか。「選び方を書く」という事後の説明要求だけで、探す範囲を狭めていないことの担保として足りるか。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md:21` — 項目見出しの「打ち切りなしで探し尽くす」という言明と、同じ項目内の「ソースの全部の当たり5万行を読む必要は無い」という明示的な許可が並存している。文書側(全頁を読む)とソース側(選んだ範囲だけを読む)とで「探し尽くす」の意味が異なることが、この一文だけからは読み取りにくい。射程の宣言(打ち切りなし)と実際の基準(選択的な読み)が食い違って見える(P9型)。冒頭でこの違いを明示すべきではないか。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md:21` — 「当たりの行の数は28回目の見積もり(E2 57,158行・E4 69,920行)を引いてよい」としているが、この数字は29回目の起動文(20260923_tools_survey_cat8_run29_prompt.md:21)が明記していたとおり「1つ目の一覧(371行、listed=7509)」由来である一方、この回の「N」(この回のファイル一覧の基準)は「689行、listed=7506」の一覧(3件少ない)を使うと明記されている(run30_prompt.md:25)。29回目はこの3件差と「引く前に、その行に数が出ていることを確かめる」という検証手順を明記していたが、この回はその注記と検証手順をどちらも落とし、数字だけを引き継いでいる。見積もりを取り直さないなら、差分注記と検証手順を残すべきではないか(出所の精度、P2寄り)。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md:77-83`(§4「この回の節に置くもの」) — ソースの選定基準・選んだディレクトリの一覧を記録する専用の欄が、必須の表(要素と段/知見/§4.0)のいずれにも無い。自由記述(知見の表など)任せだと、検収で選定の妥当性が見落とされる恐れは無いか。29回目もまさに「探し尽くした」という自己申告(SCAN文書 42882〜42884行)が実測(head -25の打ち切り)と食い違っていた前例がある。
>
> 5. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run30_prompt.md:21` — 「ソースは…関わりうるディレクトリを選び」の選定過程を、`cat8_step.py`経由で候補ディレクトリの一覧を機械的に出すコマンド(例: `ls`・`find`)で生ログに残すことが明文化されていない。選定がO-3(機構やデータの挙動を主張するときは実行したコマンドと出力を同じ返答に出す)の対象として痕跡に残る保証が、この文言だけでは読み取れない。
>
> **写し残し・消しすぎ(問い2)について**: 全文照合の結果、29回目固有の項目(当方データ投入・初回公開日・コミット数・規模の見積)への言及は run30 に見当たらず、§2.5(8-032の§4.0の表)の対象も「宣伝詐欺の兆候」1行に正しく絞られている(§4.0の表の行は1行以上=OK)。§7の生ログ本数(run1〜run30の30本)・`--round 30`・`check "" run30.log`はいずれも正しく更新されており、29回目からの写し残しは見当たらなかった。唯一の実質的な引き継ぎの粗さは指摘3(見積もり数値の出所と検証手順の欠落)。
>
> **実行可能性(問い3)について**: 参照するスクリプト(`cat8_step.py`・`cat8_search.py`・`cat8_mklist.py`・`cat8_ledger.py`・`cat8_render.js`・`check_scan_report.py`)・スキル(`x-research`)・委任文・追補・設計票・28/29回目の生ログとSCAN節・台帳の該当行はすべて実在し、指紋(`ce0012c95154`・`2c178ba75341`)も現物と一致した。台帳の8-032行(E1a印/3・E1b印/2・E3aなし・E3bなし・E5印/2・E6印/3・E2/E4未判別・状態浅い)は起動文冒頭の記載と完全に一致しており、このまま調査班が着手すること自体は妨げられない。指摘1・2・4・5は、実行不能ではなく「探索範囲の狭め方の説明責任」に関わる論点。
>
> **止める指摘の有無**: なし(すべて「直す」または「聞く」)。

## 処置(リード、監査 123 回目)

1. **直した**。ソースのディレクトリの選び方を、語の一覧で機械的に決めた(`find <src/kafka> -type d` の全部を生ログに出し、道の中に E2 = valid・check・dedup・idempot・duplicat・order・sequence・timestamp・schema・quality・corrupt・crc、E4 = replay・reset・reprocess・rewind・offset・restore・seek・retention・tiered を含むものを `grep -i -E` で選ぶ。調査班の裁量で足したり外したりしない)。ソースを選ぶこと自体は、案 B の記録の「印を探した範囲」をはっきり書く形で、結果は `印` か `未判別`(`なし` にしない)なので、陰性の主張の範囲を縮めるものではない。
2. **直した**。見出しを「文書は打ち切りなしで全部、ソースは選んだディレクトリの中を打ち切りなしで全部読む」にし、その区別を案 B の「読んだ範囲」に書かせる。
3. **直した**。1 つ目の一覧(listed=7509)に打った見積もりで N の一覧(listed=7506)と 3 件違うこと、引く前にその行に数が出ていることを確かめることを書き戻した(29 回目から消しすぎていた)。
4. **直した**。知見の表に「ソースの選び方と選んだディレクトリの一覧(数)」の行を 1 つ立てさせる。
5. **直した**。`find` と `grep` の手を生ログに出させる(処置 1)。

直したあとの起動文の印: `20260923_tools_survey_cat8_run30_prompt.md@c1e68a5c6cf0`。止める指摘は無いので、30 回目を起動する。
