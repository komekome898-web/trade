# バックテスト環境 6 回目の起動(wf_fe44f546-09e)項目 0: 記録とリードの処置(2026-09-24)

起動 09:57 JST(予約した知らせの回)。指紋 `362bf666dfac`。場面集と要件は 5 回目のもの(台本の `prebuilt`)。周の数え: 8 周済み、残り 2 周(L-426)。

## 周の途中の場面集の直し r4-1(第 3 周の批評家の場面集の側の指摘 9 件 → 場面係 → 監査役)

- 場面係の返り値: 場面 p4 の正解を要件の逐語どおりに / P0-5 に順を測る場面、P0-7 に費用、P0-2 に拒否の側の場面を追加 / 採点の値は runner が生の観測から作る / QuantCore の既定の上限を戻す(i0-r3-05)/ 検討表の道具「OK 誤り 0 件」/ 根本原因 `tests/bt/battery/item_0/ROOTCAUSE_r4-1.md` / 18 対象を走らせ直し、mutant の検査 OK。動かせなかった 2 件(54 finmarketpy・16 Luczinsritter)の理由は容量不足(`df -m /` → Available 333 MB、Use% 100%)。
- 監査役(場面):0#r4-1 = [止める] 0・[直す] 3・[聞く] 1(逐語は `docs/DISCUSSIONS/2026-09-23_backtest_env/item_0/battery/AUDIT.md`)。[止める] が無いので作業者の第 4 周へ進んだ(L-427: この監査は第 4 周と合わせて 1 周)。

### リードの処置(監査役の指摘)

1. [直す] br4-1-1(P0-2 の拒否の側の場面が無い。批評家が毎周見る運用に依存): 場面係は今回 P0-2 に拒否の側の場面を足したと返している(返り値の definitions の欄)。監査役の指摘は DEFINITIONS.md:27 の「場面にしていない観点・側面」の記述が前回と同じ、というもの。次の批評家が、場面が実際に足されたか・記述が古いだけかを確かめる(批評家は場面集の側の指摘を出せる = 次の周の前に場面係が直す)。
2. [直す] br4-1-2(検討表の冒頭の指紋 `362bf666dfac` が今の版 `a25b24aa10c9` と合わない): **原因はリード。**6 回目が走っている最中に、L-429(opus = claude-opus-5-5)の注記を委任文に足した(コミット 385172e)。走っている起動の委任先が引いている指紋(362bf666dfac)と、ファイルの指紋が食い違った。**以後、走っている起動の間は委任文を変えない**(変えるのは起動が止まってから。OWNER_STATUS に書く)。場面係の側の直しは要らない(指摘の版のほうが正しい)。次の批評家に、この食い違いはリードの手順の誤りで場面係の指摘に当たらないことを伝える。
3. [直す] br4-1-3(要件の資料係の版の確認が b6c5983 で止まっている): 6 回目は要件の固定を記録から使った(`prebuilt`)ので資料係が起こされていない。次の起動で資料係(要件の固定)を起こし、今の版との食い違いを確かめさせる(要件の中身 §1〜§4 は変えない)。
4. [聞く] br4-1-4(容量不足を理由にした「動かせない」に、時間超過と同じ実測を求めるか): **リードの判断(場面集の規則 4 はリードが書いた規則なので、リードが具体にしてオーナーへの報告に載せる)**: 求める。容量不足も「動かなかった(容量が足りなかった。試したこと: …)」と、`df -m /` の出力と、空きを作るために試したこと(キャッシュの削除など)を注記に書く。**あわせてリードが空きを作った**: 5 回目の途中で作られて今は参照されていない venv 7 本(r3・r4・r10・r11・r13・v6z・pst、参照の有無は `grep -rl "scratchpad/<名前>" tests/bt/battery/item_0` で確かめた)と pip・uv のキャッシュを消し、Available 330 MB → 9,213 MB(`df -m /`)。次に場面係が呼ばれたとき、54・16 の導入をやり直させる。委任文への反映は 6 回目が止まってから(上の 2)。

## 作業者の第 4 周(返り値。逐語は `round_4/REPORT.md` に書き出す)

- 試験: 項目 0 「341 passed」/ 項目 0 + 批評家 + 場面集 「1 failed, 394 passed」/ 全試験 「1 failed, 3281 passed, 4 skipped」。落ちる 1 件は批評家の試験 test_i0r2_battery_p5_grading.py::test_single_input_form_of_hand_over_order_agrees_with_keeping_one_input_in_order(KeyError: 'distinct_orders' = 場面係が正解の鍵を作り直したため。規則 8 で変えていない)。
- 構造の変化: エンジンに寿命の状態(例外のあとは EngineFailedError で断る。再入は EngineReentryError)/ 並びの規則を段ごとの表 _PATHS に / 履歴の読み出しを DeliveredEvents 型にし、未来の位置の名指しは FuturePositionError。
- 外部の道具: 入れていない。

### 作業者の問いへのリードの答え(次の周の起動文で渡す)

1. 指紋の食い違い(362bf666dfac ↔ a25b24aa10c9): リードの手順の誤り(上の r4-1 の処置 2)。起動の記録は 362bf666dfac のまま。差は L-429 の注記 1 行だけで、この周の規則には触れない。
2. adapter が核の規則を行番号で引いている: 場面集の側。名前(`ORDERING_RULE['source_merge']` など)で引く形に替える = 次に場面係が呼ばれたときに直させる(批評家が指摘に載せれば自動で回る。載せなければリードが起動文で渡す)。
3. 履歴の読み出しの線の引き方(未来の位置の名指しは例外、過去の側へのはみ出しと末尾から先の空は今までどおり): **リードの読みとして受ける**(要件の逐語「実行時エラーか型エラーで止まる(素通りしたら不合格)」と、場面 p4 の正解に揃っている。過去の側の切り詰めは先読みではない)。オーナーへの報告に載せる。
4. i0-r3-12(状態不明を確定させる読み取り専用の照会の口): 項目 0 の要件の 4 つの差し込み口の外なので、この項目では作らない。項目 2 の要件を固定するときに、核の口が要るかを資料係に検討させる(リードの読み。オーナーへの報告に載せる)。

## 6 回目の結果(2026-09-24 05:20 JST 頃に終了。第 4・5 周)

止まった理由(Workflow の返り値): 「周回の上限 10 に達した」。周の数え: 8(L-426)+ 第 4 周 + 第 5 周 = 10。記録: `round_4/`・`round_5/`(JUDGES.md・AUDIT.md・REPORT.md・CRITIC.md・materials/)、場面集の直しと監査は `battery/AUDIT.md`(r4-1・r5-1)。

| 周 | 盲検 対現状 | 盲検 対調査 | 批評家 [止める] | 作業者の試験 |
|---|---|---|---|---|
| 4(9 周目) | 3/3 新実装 | 0/3(P0-4 の 1 場面で調査結果の側が上) | 4(実装 2・場面集 2) | 項目 0 341 passed / 全試験 3281 passed |
| 5(10 周目) | 3/3 新実装 | 同等(表が左右同一 = L-422) | 4(実装 1・場面集 3) | 項目 0 + 批評家 + 場面集 492 passed(落ちる 1 件は批評家の古い試験)/ 全試験 3379 passed |

第 5 周の批評家の [止める](逐語は `round_5/CRITIC.md` と Workflow の返り値):
- i0-r5-01(実装、i0-r4-02 の続き): 路を渡る物が extra の外では今も値になっていない(size・price は numbers.Real の子のまま、str の子が通る)。送ったあとに送り手の状態が相手に届く。
- i0-r5-02(場面集): Basana の adapter が自分で書いた class で Basana に無い事象の型を運び、正解と一致にしている(p3-funding・p3-mixed-one-run・p1-merge-by-time の調査結果の側の最良の行はこの adapter の型から)。
- i0-r5-03(場面集): hftbacktest の p4 が開いた区間を試さずに正解と一致。
- i0-r5-04(場面集、i0-r4-03 の続き): 上位互換の「含む」を Basana の汎用の基の class で当てている行が 7 行。

L-431・L-432(部品は同等以上)で第 5 周を読み直すと、盲検は両方の組で通過(対現状 3/3、対調査 同等)。通らないのは批評家の [止める] 4 件だけ。ただし i0-r5-02 は「同等」の根拠(調査結果の側の 3 場面の一致)が adapter の作った型に乗っていることを指すので、場面集を直すと調査結果の側の行が下がる(新実装に不利にはならない)。

項目 0 の扱い(周の上限に届いた)はオーナーの決定。
