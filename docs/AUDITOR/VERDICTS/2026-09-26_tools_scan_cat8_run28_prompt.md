# 監査: 道具サーベイ 区分 8 の 28 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run28_prompt.md`(8-032 Apache Kafka、未着手。26 回目の起動文から作った)。

## 監査 119 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run28_prompt.md@faacfb9033d7`)の逐語

> 監査結果(8件、止める指摘なし)を以下に記載します。
>
> 1. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run28_prompt.md:15 — §0-5 の「学びの一覧」に 21・22・24・26 回目の失敗は列挙されているが、27 回目自身の失敗が抜けている。27 回目の検収(監査118回目、docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run27.md)の指摘2で「起動文が明記した『最初の手』(ls venvs/8-031 で check・filelist_full.bin・run・src の存在を確かめる)が生ログの最初に無かった」ことが指摘され、同じ検収の「5. これからの手」は「28回目の学び」として「最初の手の確かめ」を明示的に挙げているのに、run28 本文のどこにもこの要件(env・scratchpad がこのセッションに実在するかを最初に確かめる手)への言及が見当たらない(P8。学びが文書化された次の回に実行導線として現れていない)。
>
> 2. [直す] 同ファイル:15 — 同じ27回目検収の指摘1「出典欄の行番号引用が実物と食い違う(files=4520 の出典を誤った行に引いた)」という教訓も、この学びの一覧に含まれていない。21〜26回目の失敗だけが列挙され、直近(27回目)の失敗が学びから漏れている。
>
> 3. [直す] 同ファイル:62(§2.5) — 委任文§6-1の「依存の一覧(pom の依存)」に対応するKafkaの読み替えが「依存の一覧(配布物の libs/ の jar の一覧)」だけになっている。§1 で「Maven Central の成果物(クライアント・Streams など)」も候補の実体に含めうるとしているのに、そちらを候補の実体にした場合の依存確認(pom の <dependencies>)への言及が読み替え文に無い。Debezium 版(run26)は「依存の一覧(pom の依存)」で jar 単体を対象にしていたため一致していたが、Kafka 版は配布物の種類が増えたのに読み替えが狭いまま。
>
> 4. [聞く] 同ファイル:21,62(§1・§2.5) — 「200 MB を上限の目安にする(リードの仮定)」は16回目OpenClaw(Pythonリポジトリのgit clone、810 MB)の文脈からそのまま複製されている。本監査で実測: Apache公式配布の https://downloads.apache.org/kafka/4.3.1/kafka_2.13-4.3.1.tgz は130 MB(単体は収まる)。しかし§2.5は同じ回でこの圧縮配布物(check/)・Maven jar群(check/)・公式ソースリポジトリのgit clone(src/、§1のN)を別々に取る設計であり、200 MBがこれらの合計に掛かるのか、個別のダウンロードごとに掛かるのかが起動文に書かれていない。
>
> 5. [直す] 同ファイル:62(§2.5) — 「終わったら ls -ld /root/.m2 /root/.gradle を打って、この回が作っていないことを生ログに出す」の確認先に、同じ文で環境変数を指定している pip のキャッシュ(PIP_CACHE_DIR)への確認コマンドが無い。26回目の学び「キャッシュの置き場をscratchpadの下に」の確認漏れがmvn・gradleだけに限定されている。
>
> 6. [直す] 同ファイル:62(§2.5) — 「配布元(Apache の配布の場所・Maven Central の groupId・pom の <scm>)と GitHub の一致、.sha512 の照合」の一文が、性質の異なる2つの検査(配布元の同一性/ダウンロードの完全性検証)を読点だけでつないでおり、独立した検査項目として分離されていない。Debezium版(run26)にはハッシュ照合の項目自体が無く、Kafka版で新設された箇所のため見落とされやすい形になっている。
>
> 7. [聞く] 同ファイル:62(§2.5) — 最小実行の手順は、Debezium版(run26)では外部依存(PostgreSQL)の起動手順(initdb・pg_ctl start・psqlの接続確認)まで具体的に書かれていたのに対し、Kafka版ではKafka自体(主役)の起動手順(KRaftモードのkafka-storage.sh format・kafka-server-start.sh等)が「公式の文書が案内する、手元で1台で動く形(名前と逐語を引く)を使い、どれを使ったかを書く」とだけ抽象化されている。「種別は…調査班が決めて書く」設計自体は妥当だが、Debezium版との詳細さの落差が「消しすぎ」に当たらないか。
>
> 8. [直す] 同ファイル:62(§2.5) — Kafkaはソースをgradleでビルドする道具であるため、GRADLE_USER_HOMEの設定を新たに書き足しているのは妥当(本監査で/opt/gradle/bin/gradle(Gradle 8.14.3)の実在を確認)。しかしjava -version/mvn -v/docker infoの実測は本文にあるのに、同じ並びで追加されたgradleコマンド自体の実測(gradle -v等、この環境で動くか)が本文に書かれていない。§6-1は圧縮配布物を使う設計なので実害は薄いが、mvn・dockerと並べて書くならgradleも同様に実測すべきではないか。
>
> 環境実測の補足(タスク項目4への回答): 本監査環境でjava -version(openjdk 21.0.10)・mvn -v(Apache Maven 3.9.11)・docker info(failed to connect to the docker API at unix:///var/run/docker.sock ... no such file or directory)を実行し、run28本文の記載どおりであることを確認した。gradle -vも/opt/gradle/bin/gradle(Gradle 8.14.3)として動作することを確認した(本文には未記載)。Apache Kafka公式配布の最新版4.3.1のバイナリ配布物kafka_2.13-4.3.1.tgzは130 MBであることも実測した(指摘4の根拠)。
>
> 確認済みで問題なしと判断した点(参考): §0-4の見本を27回目ではなく26回目の節にすることは、run27検収の「5. これからの手」(「28回目=8-032 Apache Kafka…26回目の起動文の形」)と一致しており矛盾ではない。Debeziumという語・8-031というIDの残存は§0-4の注釈1箇所のみで、意図的な参照であり誤った写し残しではない(grepで確認)。§7の生ログ本数(1〜28本)・--round 28・生ログファイル名・節見出しの回数表記はすべて28で一貫している。26回目の学び4点(§6-1結論に使う配布物の名前を全部並べる/root以外の利用者でしか動かないなら止まる/キャッシュの置き場をscratchpadの下に/終わったら/root/.m2・/root/.gradleを確かめる)は、いずれもrun28本文に具体的な手順として反映されている。
>
> 止める指摘: なし。

## 処置(リード、監査 119 回目)

1. **直した**。§0 の 5 に 27 回目の失敗(最初の手を打たなかった)を足し、§2.5 に「**最初の手**で `ls -ld /root/.m2 /root/.gradle; du -sh /root/.cache/pip` を打ち、終わったら同じ手を打つ」を書いた(この回は新しい候補なので、前の回の置き場を確かめる手は要らない。最初の手は、外のキャッシュの前の状態を残す手にした)。
2. **直した**。§0 の 5 に「出典の欄に違う生ログの行を引いた」を足し、「行を引くときは、その行に引いた数・文が出ていることを確かめてから書く」と書いた。
3. **直した**。依存の一覧を「圧縮の配布物なら `libs/` の jar の一覧、Maven Central の成果物なら pom の `<dependencies>`」にした。
4. **答える・直した**。「1 つの取得ごとに 200 MB」と書き、監査 119 回目の実測(130 MB)を書き添えた。
5. **直した**。最初と終わりに `du -sh /root/.cache/pip` を打たせる(pip のキャッシュは、この回より前から 728 MB ある = 26 回目の検収に記録。増えたかを見る)。
6. **直した**。配布元の一致と、取った配布物の完全性(`sha512sum` で照合)を別の項目にした。
7. **答える**。起動の手順の名前は、公式の文書の逐語から調査班が引く(リードが先に書くと、確かめていない名前を書くことになる。26 回目の起動文の監査 115 回目の処置 4 と同じ)。PostgreSQL の手順を具体的に書いたのは、26 回目のそれが Debezium の外の道具だったため。
8. **直した**。リードが `/opt/gradle/bin/gradle -v` → Gradle 8.14.3 を打ち、実測に足した。

直したあとの起動文の印: `20260923_tools_survey_cat8_run28_prompt.md@9b8225871207`。止める指摘は無いので、28 回目を起動する。
