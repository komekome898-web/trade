# 監査: 道具サーベイ 区分 8 の 32 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md`(8-034 Pandas の残り。31 回目の起動文から作った)。

## 監査 127 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run32_prompt.md@c5b8adb4e226`)の逐語

> 監査 127 回目。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md`(指紋 `20260923_tools_survey_cat8_run32_prompt.md@c5b8adb4e226`)。
>
> 前提として確認した事実(指摘の根拠): 31 回目の起動文・31 回目の検収(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md`)・台帳 `docs/DATA/tools_catalog_cat8.tsv` の 8-034 行・31 回目の報告節(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:44025`)を全文照合した。31 回目起動文との構造 diff、引用行番号(44025・44692・1015 行の `listed=2234`・E4 950 行・E6 101,494 行など)、台帳値(E1a 印段4・E1b 印段2・E3b 印段4・E5 印段2・E2/E3a/E4/E6 未判別・状態浅い)はすべて一致した。オーナー逐語(L-506/L-507/L-508/L-509/L-516/L-519)は与えられた集合の範囲内でのみ使われており、それ以外をオーナーの決定として書いている箇所は見つからなかった(問い4は該当なし)。
>
> 以下、指摘のみを列挙する。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md:15` — 「述語の対象に届かない根拠で `印` を付けた(22 回目は E4、24 回目は E2、26・28・31 回目は E2 と E4。31 回目は E2)」という一文が自己矛盾している。前半は 31 回目が E2 と E4 の両方で「届かない根拠の印」を付けたと述べるが、31 回目の検収の「8-034 の扱い」節(`2026-09-26_tools_scan_cat8_run31.md` 末尾)では E4 は一度も `印` になっておらず、「案 B の記録(読んだ範囲・当たりの行の数)不足」で `未判別` のままだった(これは同じ検収文書内で 26・28・31 回目に共通する別種の失敗として §0 の同じ段落の後半で正しく述べられている)。直後の「31 回目は E2」で訂正しているように読めるが、「26・28・31 回目は E2 と E4」という先の記述を消していないため、調査班が「31 回目の E4 にも印の誤当てがあった」と誤読する余地が残る。31 回目起動文(`docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:15`)の同型の文「26・28 回目は E2 と E4」に「・31」を足しただけで、E4 の性質の違いを反映せずに残った写し残しに見える。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md:28` と `:69` — 「最初の手」が 2 箇所に別々に定義されている。§1 の N の段(28 行目)は「**最初の手**で `ls .../scratchpad/cat8/venvs/8-034` を打って生ログに出す」とし、§2.5(69 行目)は「**この回の最初の手**で `export TMPDIR=...` を打ち…同じ手で `ls -ld /root/.m2 /root/.gradle; du -sh /root/.cache/pip; ls /tmp | grep -c -E '^pip-|^pandas'` を打って生ログに出す」としている。どちらも文字どおり「この回の最初の手」と名指しており、実際にどちらを先に打つべきかが書かれていない。調査班が生ログの手の順序をどちらに合わせるべきか判断に迷う。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md:69` — 「`export TMPDIR=/tmp/.../venvs/8-034/tmp` を打ち(**ディレクトリを作る**)」という記述は誤り。`export` はシェル変数の設定だけで、ディレクトリを作成しない(`mkdir` が要る)。実際にこのディレクトリ(`.../venvs/8-034/tmp`)は現時点で存在しないことを確認した(`ls -la .../venvs/8-034/` に `tmp` は無い)。この節は直後で「`pip`・`python` を打つ手には毎回 `TMPDIR=<上の道>` … を前に付ける」と指示しており、そのディレクトリが実在しないまま `pip download`/`pip install` を打つと、pip・tempfile が `TMPDIR` 配下に一時ファイルを作ろうとして失敗しうる(この回の中心作業である §6-1 のやり直しが、最初の手の不備で止まりかねない)。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md:60`(§2.4 の E2 語の組。31 回目から不変)と、設計票 `docs/DATA/surveys/CAT8_DESIGN.md:54` — E2 の述語は「欠け・重複・順序の乱れ・時刻のずれ・外れ値・型や範囲の違反・**取引所間(情報源間)の食い違い**」の 7 種だが、`cat8_search.py` に渡す E2 の語の組(60 行目)は 6 群("欠損…" "重複…" "順序…" "外れ値…" "時刻のずれ…" "型・範囲…")で、「情報源間の食い違い」に対応する語(例: `discrepancy`・`cross-source`・`exchange` 等)が明示的に含まれていない。この回の task 2(E2)は「対象がこの列挙のデータであることを言う逐語」を探す指示だが、`印` が見つからず全件検索(`cat8_search.py`)にフォールバックした場合、この語の組では 7 番目の型に該当する記述を検索で拾えない可能性がある。この欠落は 31 回目からの引き継ぎで、今回新設されたものではないが、この回の中心課題である E2 の判定に直接関わるため、語の組を直すか、この回はそのまま使うのかをリードが決めているかを確認したい。
>
> 確認して問題が見当たらなかった点(指摘としては数えない):
> - 31 回目起動文からの写し残し・消しすぎは、上記 1・2・3 以外には見つからなかった(構造的な diff で全文照合)。
> - 31 回目の検収の処置 1〜5(TMPDIR、§6-1 文言の直し、隠れた依存の§6-1対象化、文書の頁の機械選定、docstring 段の確認)は、いずれも起動文の該当箇所に反映されており、引用行番号(1015 行 `listed=2234`、E4 950 行/E6 101,494 行、44025・44692 行、TSV の 8-034 行の値)はすべて現物と一致した。
> - オーナー逐語(L-506/L-507/L-508・L-509/L-516/L-519)以外を「オーナーの決定」として書いている箇所は無かった(§2.5 の「オーナーの決定ではない」という明示的な否定を含め、区別は一貫していた)。
> - `x-research` スキルは実在し、参照は委任文 §2 の記述と整合していた。
> - N の一覧(`list.txt`、NUL区切り 2234 件)は実際にディスク上に存在し、起動文の引用と一致した。

## 処置(リード、監査 127 回目)

1. **直した**。「22 回目は E4、24 回目は E2、26・28 回目は E2 と E4、31 回目は E2」にした。
2. **直した**。「この回の最初の手(生ログの 1 つ目の手)」を §2.5 の 1 か所だけにし、`ls .../venvs/8-034` もその手の中で打つ。§1 の N の段は §2.5 を指すだけにした。
3. **直した**。最初の手を `mkdir -p <道>` にし、「`export` だけでは作られない」と書いた。各手の前に付ける `TMPDIR=<上で作った道>` はそのまま。
4. **答える・直した**。E2 の語の組は、11 回目の起動文からリードが決めたもので(設計票・追補には無い。リードの実測: `git ls-files -z docs | xargs -0 grep -l '外れ値・outlier・anomal'` の最初は `20260923_tools_survey_cat8_run11_prompt.md`)、述語の 7 種のうち「情報源間の食い違い」の語が無かった。この回から 7 つ目の組 `食い違い・discrepan・inconsisten・cross-source` を足した(リードの決定。当たりを増やす向きだけに効く)。**11〜31 回目に全件の検索で付けた E2 の `なし` は、この 7 つ目の語で探していない。**区分の全体の検収で、E2 が `なし` の行を全部、この語で当て直す(当たりが出た行は `未判別` に戻す)。区分の完了の報告にこのことを書く。

直したあとの起動文の印: `20260923_tools_survey_cat8_run32_prompt.md@5d1ede7b3274`。止める指摘は無いので、32 回目を起動する。
