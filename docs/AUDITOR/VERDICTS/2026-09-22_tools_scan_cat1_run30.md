# 区分 1 の 30 回目 — リードの検収(2026-09-23)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 30 回目の実行」節 / 生ログ `docs/DATA/probes/20260922_tools_1_run30.log`(412 行)/ 新設 `scripts/recount_scan_cat1.py`
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 機械の確認

受け入れ検査は生ログ 29 本を全部渡して **13 本すべて 0 件**。
削除 29 行は指定 1〜5 の分で、**消した行を全部出して確かめた記録を生ログに残している。**
`--all` は 46 件で、**増は K2 の 1 件だけ。逐語の中の開き括弧(原典で行が折れている)による誤検出であり、自分で閉じずに渡してきた。当方の判定 = 誤検出。**

**数え直しの道具を当方が打った**:
```
段の表の行 = 37 件 / 区分1-市場影響と約定の模型 の印 = 37 件
段の表にあって印が無い = 0 件 / 印があって段の表に無い = 0 件
```
**29 回目に当方が出した「表と印は同じ集合でなければならない」が、機械で確かめられる形になった。**

## 2. **問い 1 — 当方の述語が、要素の名前の半分を落としていた**

調査班の指摘: 「**述語の字面に当てると、119 番は「測る側」で埋まりを決めていないので当たりません。同じことが 36・106・116 にも当てはまります。**」

**当方が委任文 §2 の逐語を読み直した**:

> 1. バックテスト・シミュレーション(足 / ティック / 板の待ち行列(指値の埋まり方)/ イベント駆動 / ベクトル化 / **市場影響・約定の模型**)

**要素の名前は `・` で結ばれた 2 つである。**当方が 23 回目に書いた述語は:

> 約定の可否・量・値段のどれかを決める仕組みがあるか

**これは「約定の模型」だけを言っており、「市場影響の模型」が落ちている。**
段 6 が「影響の関数を持つ」であることと食い違っていた(段 6 の当たりが述語に当たらないという矛盾)。

**直す**:

> **`区分1-市場影響と約定の模型` の印は、次のどちらかを持つものに付ける。**
> **(a) 約定の可否・量・値段のどれかを決める仕組み**(段 0〜5)
> **(b) 市場影響の関数そのもの**(段 6)。検証の機関を持たず、測る側・当てはめる側であっても付ける。

**これで 119・36・106・116 は印を持ったままで正しく、段の表 = 印の集合も保たれる。調査班が動かさなかったのが正しい。**

**当方の誤りの型は 5 回目である**(規則を書くときに自分の枠で狭める)。19・21 の指定漏れ、25 の候補 33、27 の機関の数、そして今回。
**今回は「2 つ並んだ名前の片方だけを述語にした」という形で出た。**

## 3. 残りの問い — リードの回答

**(2)(7) 26・28 回目の分布と 27 回目の表に同じ落ちがある → 過去の節は書き換えない。**
**`scripts/recount_scan_cat1.py` が正の数を出す機械になったので、以後はその出力を正とする。**
**節にその旨を 1 行書くこと**(どれが正の数かが読む側に分かる形に)。

**(3) 候補 6 の `区分1-イベント駆動` が表にだけ在って一覧に無い → 直すこと。**3・8・19・51 と同じ型。**指定の 5 件に入っていなかったから触らなかったのは正しい扱い。**

**(4) 80 件 対 81 件 → 道具が一覧を出すようになったので、突き合わせて決めること。**
**あわせて、道具が出した「到達の記載が無い候補 = 53 件」を先に確かめること。**
候補 1〜23 は 23 回目に一次資料から印を当てており、**「到達の記載が無い」は道具の見つけ方が粗いためと思われるが、確かめていない。**
**確かめるまで、この 53 という数をどこにも書かない**(§0.2 O-6。出所の分からない数字を出さない)。

**(5) 道具に「段の表と印の集合の突き合わせ」を足したこと → 残すこと。**当方が 29 回目に出した決定を機械で測る部品であり、**足したのが正しい。**

**(6) `scripts/` に置いたのが委任文 §6-5 の上書きだと書いてきたこと → そのとおりで、上書きは当方が指定した。**
**黙って書かずに、上書きであることを明記して返したのが正しい扱いである。**

## 4. 判定

**受け取る。**指定 1〜5 が片付き、段の表と印が同じ集合になったことが機械で確かめられた。
**問い 1 で当方の述語の欠けが出た(§2)。調査班が述語を字面どおりに当てて食い違いを出し、自分で動かさずに渡してきたのが正しい。差し戻す点は無い。**

## 5. 31 回目の起動の指定 — **最後の詰め**

1. **述語を §2 のとおり 2 つに直し、節に書く。**119・36・106・116 の印はそのまま。
2. **候補 6 の `区分1-イベント駆動` を候補の一覧に足す**(§3(3))。
3. **「到達の記載が無い = 53 件」の中身を確かめる**(§3(4))。道具の見つけ方が粗いなら道具を直す。**確かめるまでこの数を節に書かない。**
4. **80 件 対 81 件 を突き合わせて決める**(§3(4))。
5. **`recount_scan_cat1.py` の出力を正の数とすることを節に 1 行書く**(§3(2))。
6. **新しい検索計画は打たない。候補も増やさない。構築・導入・最小実行もしない。**
7. 上の 1〜5 に要る範囲でのみ既存の節への書き換えを許す。`git diff` の削除行を自分で確かめて返す。
