# 検収: 道具サーベイ 区分 8 の 6 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 6 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run6.log`(1,621 行・93 手)。届いたままのコミット babeb26。
起動文 `20260923_tools_survey_cat8_run6_prompt.md` の指紋 `a3a735b35643`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **478,968 トークン・2,330,074 ms(38.8 分)・道具 234 回**。生ログの最初の手 03:37:33Z / 最後の手 04:00:34Z。割り当ての 6 件すべてに手が付いた。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 6 本)0 件 / `check-elements --round 6` 0 件(候補の一覧 16 行・要素と段の表 128 行・知見の表 23 行)/ `check "" <run6.log>` 0 件 / 消えた行 0。

## 2. `なし` の数え直し(この回の新しい `なし` 11 件を全部)

| 候補 / 要素 | 報告の N / M | リードの確かめ | 処置 |
|---|---|---|---|
| 8-003 E3a・E3b・E6 | 99 / 98(`uv.lock` を除外) | 検索の手(`run6.log:12`・`321`・`480` の手)は `grep -r .` でリポジトリの全部(`.git/` と `uv.lock` を除く)。5 回目の `--include` の絞り込みは無い。検索の語に要素の名前(ルックアヘッド・品質・検証・再現)がある | 受け取る |
| 8-008 E1a・E2・E3a・E3b・E5・E6 | 107 / 105(本文が空の動画だけの記事 2 件を除外) | 8 分類の記事の URL を合わせた一覧の手(`run6.log:1067` の手、出力 114 行)。本文は `curl` で取った(`fxreplay_fetch_all.sh`、描画の道具は使っていない)。取った本文は `/tmp/fx_text/` に 105 本、空のファイル 0 本(リードが数えた)。4 回目の WebFetch の要約の文「no automated reconciliation」は原文に 0 件(`run6.log` 03:49:20Z の手)= 4 回目の根拠は原文に無かった | 受け取る。**限界**: 記事の本文は生ログに入っていない(scratchpad と `/tmp` にだけある。消えると確かめ直せない)。7 回目の起動文で、取った本文の置き場を scratchpad の下に揃え、件数と先頭の行を生ログに残させる |
| 8-013 E3a・E5 | 171 / 169(画像 2 件を除外) | 検索の手(`run6.log:1255`・`1258` の手)は `grep -r .`(`.git/` と `.png` を除く)。リポジトリの全部 | 受け取る |

## 3. 決めずに出した問いの行(起動文 §7 の 5「決めずに出した問いの対象の行は `未判別` のまま」)

- 問い 1(8-003 の E5 の段を 3 にした。`DATA_SHA256` を (ア) と読むか)・問い 3(8-016 の E2・E6 の段を 4 にした。(イ) を満たさないと判断)は、問いに出しながら段を決めていた(監査 30 回目の指摘 1 と同じ型)。**台帳で、8-003 の E5 の段、8-016 の E2・E6 の段を `未判別` にした。**
- 問い 2(8-006 は E1a〜E6 がすべて決まったが、§4.0 の表の過半が一次資料・実測でないので `浅い` のまま): 設計票 §2 の `深掘り` は「委任文 §4.0 の『深掘り』の条件を満たし、E1a〜E6 に `未判別` が 1 つも無い」の両方。**調査班の読みのとおり**。7 回目で §4.0 の表の `未確認` を埋める。

## 4. 台帳

`import --round 6`: 変わった所 20。§3 の段を戻した。`check` 0 件。`recount`: 深掘り 12 / 浅い 1(8-006)/ 判別に一次資料が要る 1(8-016)/ 危険で導入停止 1 / 登録が要る 1。**残り 2 行**。

## 5. 判定

**受け取る**(`なし` 11 件はすべて数え直して合っていた。§3 の段 3 つは台帳で `未判別` に戻した)。中身の回なので監査役(33 回目)に通す。

## 監査 33 回目(中身の回。対象 = babeb26 の 6 回目の節と生ログ)

監査役の出力(逐語。[止める] 1 件・[直す] 3 件・[聞く] 5 件):

> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2223-2226,2371,2376` — 8-016(Exactpro)の E2 の段再判定と E6 の新規`印`の根拠として引いた 3 つの逐語(`th2-estore` README「Event store (estore) is an important th2 component responsible for storing events into Cradle.」/ `th2-rpt-viewer` README「This is a web app that displays the stored test data...」/ `th2-check1` README「CheckRuleRequest - get message filter from request and check it with messages in the cache or await specified time...」)は、引用元として書かれた `docs/DATA/probes/20260923_tools_8_run6.log:1341` `:1356` `:1362` のいずれにも存在しない。その 3 行は `curl -sS ... -o /tmp/th2_check1_readme.md` のように `-o` で出力をファイルへ逃がした手のコマンド行そのものであり、README の本文は生ログの 1〜1621 行のどこにも印字されていない(`CheckRuleRequest`・`message filter`・`Cradle`・`report-data-provider` のいずれも生ログ全文検索で 0 件)。外部から同じ URL を取り直して確認した限り 3 つの逐語自体は実在し正確だったが、**それは生ログを読むだけでは確認できない**。この回はすでに 8-008 で「4 回目の WebFetch 由来の逐語が原文に存在しなかった」ことを裏取りして訂正しており(知見 17)、同じ「一次資料と称する引用が生ログで確認できない」型の欠陥を同じ回の別候補で見逃している。`check_scan_report.py` の K11(実測の根拠)は行番号がログの範囲内にあるかしか見ておらず、引用文の内容一致は検査していない(`scripts/check_scan_report.py:335`〜付近)ため、この型は自動検査を素通りする。8-016 の E2 段 4・E6`印`段 4 は、README の内容を `cat`/`sed` で生ログに印字し直してから確定すべきではないか(P10・O-3)。
>
> 2. [直す] `docs/DATA/probes/20260923_tools_8_run6.log:12,321` — 8-003 の E3a/E3b の「なし」の根拠になった手の `note=` は「E3a/E3b: look-ahead/lookahead/ルックアヘッド/防ぐ/検出 をuv.lock以外の98件で検索」と書いているが、実際に打たれた `grep -rniE` の正規表現は `look-?ahead|lookahead|ルックアヘッド|leak|future.?leak|forward.?fill.?bias|survivorship`(12 行目)と `peek|future.?data|purge|embargo|walk-forward|in-sample|out-of-sample|snoop`(321 行目)で、「防ぐ」「検出」という語は 2 つの手のどちらにも実際には入っていない。E3b の述語自体が「その時点で知り得ない情報が**入らないようにする**(防ぐ)機能」であるにもかかわらず、その和語がノートの主張どおりには検索されていない。監査項目2 の「検索の語に要素の名前が日本語と英語で入っているか」を生ログのノートの文言だけで確認すると誤り、実コマンドと突き合わせて初めて分かる食い違いである。リードの検収(`VERDICTS/2026-09-24_tools_scan_cat8_run6.md:18`)も「検索の語に要素の名前(ルックアヘッド・品質・検証・再現)がある」とだけ書いており、この食い違いを指摘していない。
>
> 3. [直す] `docs/DATA/probes/20260923_tools_8_run6.log:1266` — 8-013 の E5「なし」の根拠の手のノートは「E5補足: random/rng/**同じ結果**/consistent results を171件中169件で検索」と書いているが、実際の正規表現は `\brandom\b|\brng\b|same result|consistent result` で、「同じ結果」という和語はコマンドに存在しない(英語の "same result" のみ)。指摘 2 と同型の、ノートと実コマンドの不一致。
>
> 4. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:79,85` と検収 `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run6.md:24` — 設計票 §4.1 は段の値域を「`1`〜`5` / `未判別` / `-`」とし、「`-` はその要素が `なし` のときだけ」「要素が `未判別` のときの段は `未判別`」とだけ規定している。要素の**値**(印/なし)が確定したまま**段**だけを `未判別` にする組み合わせは設計票の値域規則に明記が無い。今回リードは検収で報告の値(8-003 の E5 = 印・段3、8-016 の E2・E6 = 印・段4)を台帳上で「印・段未判別」という設計票に無い組み合わせへ書き換えた。これは §3 の「述語を足したり変えたりしたら、全行に当て直す」の対象になる値域の拡張ではないか。台帳の状態が報告書と食い違ったまま資源化に進んでよいか。
>
> 5. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:84`(「E6 の段の一貫性…リードは毎回の検収で E6 に段を付けた行を全部読み、対象の原文と段の根拠を並べて、同じ種類の対象に違う段が付いていないかを見る」)— 8-016 の E6 が今回新たに `印`(段4)になったが、検収 `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run6.md` にはこの横断一貫性チェックを行った記述が無い(§0〜§5 のどこにも E6 の他行との比較が書かれていない)。他の E6`印` 行(この時点では 8-016 のみ)との比較のため書くまでもなかったのか、それとも単に検収の記録から漏れたのか。
>
> 6. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2405`(代替経路)— 8-016 の th2-net 組織のリポジトリ一覧(100 件で頭打ち)について試した経路は `ungh.cc`・GitHub HTML の `?page=2`・GitHub REST API・GitHub Search API・組織トップ頁・`web.archive.org` の 6 つで、いずれもこの環境のプロキシ制限(GitHub 系)か送出方針(`web.archive.org`)で塞がれている。一方、**この環境で GitHub API と別経路のはずの `WebSearch`**(例: `site:github.com/th2-net` や `th2-net repositories` の検索)や、パッケージレジストリ(Docker Hub・Maven Central・npm。th2 コンポーネントは JVM/TS 系で、これらに別名で登録されている可能性がある)からの間接的な名前の発見は、生ログに 1 件も見当たらない。報告自身が末尾の問い 4 で「他の探し方…をこの回のうちに試すべきだったか」と既に問うているが、`WebSearch` という具体的な未試行経路を名指ししていない。これは E3a・E3b・E4・E5 を `未判別` のまま残す判断(A-18 の逆側、過度な安全側ではなく単に手段の網羅性)の妥当性に関わる。
>
> 7. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2502-2508`(判断に迷った点と問い 1〜6)— 6 件のうち 1・3 はすでに検収でリードが「未判別に戻す」という形で処理済み(`VERDICTS/2026-09-24_tools_scan_cat8_run6.md:24`)、2 は「調査班の読みのとおり」として即決している(同 :25)。残る 4・5・6 は範囲・深さの選択(次回の検索経路を増やすか、ソース読解を深めるか、E4 を裏取りするか)で、`CLAUDE.md` §0.2 A-13(枠組みはオーナーに見せてから進める)に触れるような述語・族の変更ではなく、次回の割り当ての中身の選択に見える。報告のどの問いをリード単独で決めてよく、どれをオーナーに聞くかの基準が、検収にも報告にも書かれていない。
>
> 8. [直す] `scripts/check_scan_report.py`(K11 = `check_measured_evidence`)— 「実測」タグの行は生ログの行番号が**範囲内にあるか**しか検査しておらず、引用文の**内容がその行にあるか**は検査していない(指摘 1 の根本原因)。さらに「一次資料」タグの行はこの検査自体の対象外(`c[3] != "実測": continue`)で、指摘 1 の 3 行のうち知見表の 3 行(2223〜2226 行)は「一次資料」タグのため最初から K11 の対象にすら入っていない。この検査の穴は今回の欠陥を素通りさせた直接の原因であり、道具の直しとして扱うべきか、それとも当面は検収の目視に委ねるかの判断が要る。
>
> 9. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2219`(知見 16)/ `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run6.md:19` — 8-008 の記事本文は `/tmp/fx_text/` と scratchpad にのみ保存され生ログには入っていない、とリード自身が「限界」として明記し、7 回目での是正(置き場を scratchpad 下に揃え件数と先頭行を生ログへ残す)を予定している。この同型の欠落(一次資料の本文が生ログに現れない)が指摘 1 の th2 README 3 件にも当てはまるが、検収はそちらには触れていない。7 回目の是正指示に th2 README の再取得(`cat`/`sed` で生ログへ印字)も含めるべきではないか。

**リードの処置**:
1: 認める。台帳で 8-016 の E6 を `未判別` に戻した(E2 の値の `印` は 5 回目の根拠によるもので、段は検収で既に `未判別`)。7 回目で th2 の README を生ログに印字してから引かせる。
2・3: 認める(リードの検収はノートの文言だけを見て、実際のコマンドと突き合わせていなかった)。ただし、設計票 §3 の要素の名前(E3a・E3b は「ルックアヘッド」の検出・防止、E5 は「再現」)は、実際のコマンドに日本語(`ルックアヘッド`・`再現`)と英語(`lookahead`・`reproduc`)で入っていた(`run6.log:12`、`run6.log:1258`)。「`なし` と書くための条件」(b) は満たすので `なし` は戻さない。7 回目の起動文で「ノートの語は実際のコマンドの語と同じにする」と書き、検収でリードはコマンドの行を読む。
4: 答える。設計票 §4.1 の同じ箇所の最初の文「条件を決めるのに要る一次資料に届いていなければ `未判別`」は、段の列の値について書かれている。値が `印` で、段を決める一次資料に届いていない組み合わせ(`印` / 段 `未判別`)は、この文に当たる(台帳には 3 回目から 9 か所ある)。値の範囲を広げたものではない。報告と台帳が違う行は、台帳が正(検収の記録に理由がある)。
5: 検収の記録から漏れていた。E6 の `印` は 8-016 の 1 行だけで、1 で `未判別` に戻した。E6 の `印` が出たら、検収で E6 の全部の行を並べて書く。
6: 認める。7 回目で `WebSearch`(`site:github.com/th2-net`)と、パッケージの置き場(Docker Hub・Maven Central・npm・PyPI)から th2 の部品の名前を集めて、個別のリポジトリを直接開く。
7: 答える。リードが決めるのは、次の回の割り当ての中身(経路・深さ)と、設計票の逐語で決まること。オーナーに聞くのは、述語・段の定義を変える必要があるとき(設計票 §3)と、取り返しのつかないこと。
8: 認める。`cat8_ledger.py check-elements` に、7 回目以降、根拠の「…」の逐語(8 字以上)の先頭 30 字が、引いた生ログの手の出力(`実測`・`一次資料` の別を問わない)にあるかを見る検査を足した。scratchpad で、生ログに無い引用を止め、ある引用を通すことを確かめた。6 回目の節への結果は変わらない(0 件)。**限界**: 引用に生ログの行を添えていない根拠(URL だけ)は見ない。7 回目の起動文で、`一次資料` の引用にも生ログの行を添えさせる。
9: 認める。7 回目は、取った本文を scratchpad の下に置き、件数と、引いた文を含む行を生ログに印字させる(th2 の README を含む)。

台帳の残り: 2 行(8-006・8-016)。`check` 0 件。
