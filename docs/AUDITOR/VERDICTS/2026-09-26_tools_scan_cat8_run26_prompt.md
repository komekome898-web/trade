# 監査: 道具サーベイ 区分 8 の 26 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md`(8-031 Debezium、未着手。24 回目の起動文から作った)。

## 監査 114 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run26_prompt.md@25463ee555bc`、コミット a006ba8)の逐語

> ## 監査 114 回目 — 道具サーベイ区分 8・26 回目起動文(docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md)への問い
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:62` — 「この環境には `java`・`mvn`・`docker` がある」という断定に、実行したコマンドと出力が本文中に示されていない。監査中に実測すると `java`・`mvn` は動くが、`docker info` は `failed to connect to the docker API at unix:///var/run/docker.sock: ... no such file or directory`(デーモン未起動、CLI はあるがコンテナは動かせない)だった。Debezium は Kafka・データベースを要する CDC 道具で、「外のサーバーが要るなら…手元で動く形(`java`・`mvn`・`docker` がある)を使い」と書いてある以上、`docker` が実際に使えるかは §2.5 の実行可否そのものを左右する。調べずに「ある」と書いた疑いがある(PRINCIPLES P1「取れない・無いは全経路を試した結果でなければ言えない」の逆方向、CLAUDE.md §0.2 O-3「機構やデータの挙動を主張するときは実行したコマンドと出力を同じ返答に出す」)。
>
> 2. [聞く] 同 `:62` — 「外のサーバー(Kafka・データベースなど)が要るなら、公式の文書が案内する手元で動く形…を使い」としか書かれておらず、`docker` デーモンが動かない場合の代替(Debezium Embedded Engine など Kafka を介さない実行モード)が指定されていない。委任文§6「1 件の実行は数分まで」「常駐プロセスを残さない」との両立も含め、調査班がこの一文だけで実行可能な最小実行を組み立てられるか。
>
> 3. [聞く] 同 `:62`(§2.5 の読み替え文) — 委任文§6-1(`docs/DATA/delegations/20260922_tools_survey_prompt.md:99`)の項目5「`setup.py` `pyproject.toml` の導入時実行・難読化・外部 URL からの取得・バイナリ同梱が無いか」を、「導入時・起動時に外から何かを取ってくる設定・難読化・同梱のネイティブのバイナリが無いか」に読み替えている。「導入時実行」(取得に限らない任意コード実行そのものの危険)が「外から何かを取ってくる設定」に、「バイナリ同梱」が「同梱の**ネイティブの**バイナリ」に、それぞれ限定が加わっている。これは§6-1「1 つでも不審なら導入せず」の対象を狭めている疑いがあるが、狭めた理由が本文に書かれていない。
>
> 4. [聞く] 同 `:62` — この読み替え(「リードの読み替え」と明記)は、委任文§6 の安全側の検査基準の実質的な改変にあたる。事前に監査役に見せて承認を得たものか、今回この監査で初めて検査に出されたものか、本文からは判別できない。
>
> 5. [直す] `:15`(§0.5)・`:93-96`(§7) — run25 検収の処置1(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run25.md` §4-1)は「以後、§7 の 2〜4 の出力は検収でリードが打って貼る手順にする」という運用変更を明記しているが、この決定が run26 の §0.5・§7 いずれにも書き写されていない。調査班に示されるのは run24/25 と同一の「同じ節に貼る」義務文言のみで、3 回連続の同型欠落(21・24・25 回目に類似)を防ぐ手当てが起動文上は見えない。
>
> 6. [直す] `:21`(§1「N」節) — 「24 回目に、起動文が N から外すと書いた別の配布物を N に入れた」への手当てとして、N 節の文言は run24 と一字一句同一のまま再掲されているだけで、追加の機械的確認(N に含めたリポジトリ・配布物を生ログに列挙し検収で突き合わせる、等)は加わっていない。Debezium も connector ごとに別リポジトリ/別 Maven 成果物を持ちうる道具で、同種の誤りが再演しうる。
>
> 7. [直す] `:62`(§2.5) — 配布物の取得コマンドが具体的に指定されていない。run24 版は `pip download --no-deps` という具体コマンドを与えていたが、run26 版は「Maven Central の成果物の jar・pom、またはコンテナのイメージ」「取り」としか書かれておらず、`mvn dependency:get`・`curl`・`docker pull` のどれを使うかが調査班の裁量に委ねられている。再現性・検収時の照合可能性の観点で薄い。
>
> 8. [直す] `:78`(§4) — 「24 回目の節と同じ小節を置く」との指示だが、24 回目の節には検収時にリードが追記した「リードの追記」小節が含まれる(§0.4 の注記にも「末尾の『リードの追記』を含む」と明記)。除外する小節の列挙(`### 決まりの一覧`・`### 当たりの判定`)にこれが入っておらず、見本の対象から除くのかどうかが調査班に伝わらない。
>
> 9. [聞く] `:20`(§1) — 「10 回目の辿る一覧(dev.to の記事)の記事の中の名前として台帳に入った」という一文は run24 の Prefect 向け文言をそのまま Debezium に転用している。この記事が実際に Debezium も名指ししているかは本文だけでは確認できず、機械的な使い回しの跡が見える(PRINCIPLES P10「目視・記憶・要約による判断は原典を再照合するまで正しいとは言えない」)。
>
> 止める指摘: あり(1 件、#1)。
>
> (検査の方法)run26 起動文と run24 起動文(前回・Prefect 版)を `diff` で突き合わせ、候補名・回数・行番号・ログ名以外の実質差分(§1 候補実体、§2.5 §6-1 読み替え)を特定。/ 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md` §6-1 の原文(§5-1、99 行目)を直接読み、読み替え文と逐語で突き合わせ。/ `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の各回節の見出し行番号を `grep` で実測し、§0.4 が引用する 39661〜40312 行目が実際に 24 回目の節と一致することを確認(一致)。/ `docs/DATA/probes/` のログファイル一覧を確認し、run1〜run25 が揃っていることを確認(run26 は今回生成予定)。/ run24・run25 の検収(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run24.md`・`..._run25.md`)を全文読み、§0.5 の誤り要約が検収の指摘と対応しているか照合。/ `java -version`・`mvn -version`・`docker version`・`docker info` を実行し、起動文の環境主張を実測で確認(docker はデーモン未接続)。

## 処置(リード、監査 114 回目)

1. **止める → 直した。リードの誤り**: リードは `which java mvn docker` と `java -version` だけを打ち、docker のデーモンが動くかを確かめずに「docker がある」と書いた(O-3 の型)。打ち直した: `docker info` → 「failed to connect to the docker API at unix:///var/run/docker.sock ... no such file or directory」、`mvn -v` → Apache Maven 3.9.11、`ls /usr/lib/postgresql/16/bin` → `initdb`・`pg_ctl`・`postgres` など。起動文にこのコマンドと出力を書き、「コンテナは使えない。PostgreSQL 16 は scratchpad の下に立てられる」とした。
2. **直した**。Kafka を介さずに JVM の中で動かす形が公式の文書にあれば、名前と逐語を引いて使う。PostgreSQL は `run/` の下に `initdb` で作り、終わったら `pg_ctl stop` で止め、止まったことを生ログに出す。1 件の実行は数分まで(委任文 §6-6)。
3. **直した**。「導入時・起動時に任意のコードを実行する仕組み(ビルドの plugin・起動のスクリプトなど)・外から何かを取ってくる設定・難読化・同梱のバイナリ(ネイティブに限らない)」にした。狭めていた(リードの誤り)。
4. **答える**。読み替えはリードの決定で、この監査で初めて監査役に見せた。起動文にそう書いた。
5. **直した**。§7 に「2〜4 の出力は、貼られていなくても検収でリードが打ち直して貼る」と書いた。
6. **直した**。N に入れたリポジトリ・文書・配布物を全部、名前と URL で並べさせ、検収でリードが突き合わせる。
7. **直した**。`curl` で `repo1.maven.org` から `.jar`・`.pom` を 1 つずつ取る(依存を芋づるで取らない)と書いた。コンテナのイメージは使えない(処置 1)。
8. **直した**。「リードの追記」は調査班が置かない、と書いた。
9. **直した**。報告の 10 回目の節の 3668 行「`Debezium` — 同上 — 同上」と 3846 行を引いた(リードが `grep -n Debezium` で確かめた)。

直したあとの起動文の印: `20260923_tools_survey_cat8_run26_prompt.md@6467f06e44fa`。止める指摘(1)の直しを監査 115 回目に掛ける。

## 監査 115 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run26_prompt.md@6467f06e44fa`、コミット 08db8fb)の逐語

> ## 監査 115 回目 — `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md@6467f06e44fa` への問い
>
> **114 回目の処置1([止める])の事実確認**: 本監査で `java -version`・`mvn -v`・`docker info`・`ls /usr/lib/postgresql/16/bin` を実測した。結果は起動文62行目の記載(openjdk 21.0.10 / Apache Maven 3.9.11 / `docker info` が `failed to connect to the docker API at unix:///var/run/docker.sock ... no such file or directory` / PostgreSQL 16 の `initdb`・`pg_ctl`・`postgres` 等のバイナリが存在)と一致した。114 回目の[止める]指摘は事実どおりに解消されている。処置9(3668行・3846行の逐語引用)も `grep -n` で再確認し一致した。処置8(「リードの追記」を24回目の見本から除外)も、実際に39661〜40312行の範囲に40303行目「リードの追記」小節が含まれることを確認した上での妥当な対応。
>
> 1. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:62` — §6-1 の配布物取得は「`curl` で jar・pom を1つずつ取る。依存を芋づるで取る手は使わない」と限定されているが、直後の「最小実行」段で Debezium の種別の中核(通常 Kafka Connect API・JDBC ドライバ等の複数の推移的依存を要する)を動かすための依存の取得方法が指定されていない。「芋づるで取らない」がこの検査取得の手順に限定されるのか、実行段の依存取得にも及ぶのか本文からは判別できず、実行可能性そのものを左右する(委任文§6-6「1 件の実行は数分まで」との両立も含む)。114 回目の指摘2([聞く]、代替実行形の不指定)は形式上「答えた」ものの、実務上の依存取得の壁は残っている。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:62` — 「PostgreSQL 16 を scratchpad の下で手元に立てられる」という結論は `ls /usr/lib/postgresql/16/bin` でバイナリの存在を確かめただけで、`initdb` → 起動 → 接続まで実際に通したものではない。バイナリの実在とサーバーを立てられることは別の主張であり、CLAUDE.md §0.2 の規則 O-3(機構の挙動は実行したコマンドと出力を示す)の型に触れる一歩手前の断定になっている。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:62` — 「PostgreSQL は `.../run/` の下に `initdb` で作り、終わったら `pg_ctl stop` で止め」と書かれているが、`initdb`(データディレクトリの初期化)と `pg_ctl stop`(停止)の間にあるはずの起動手順(`pg_ctl start` 等)が明記されていない。調査班がここを自分で補うのか、それとも書き漏れかが分からない。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:62` — 処置2は「Kafka を介さずに JVM の中で動かす形が公式の文書にあれば、その名前と逐語を引いて使う」という一般則にとどまり、具体的な実行モード名(例: Debezium Embedded Engine)を挙げていない。114 回目の指摘2の核心(「調査班がこの一文だけで実行可能な最小実行を組み立てられるか」)への回答としてなお調査班の発見に懸かっており、答えの形にはなっているが不確実性は残る。
>
> 5. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:21` — §1「候補の実体」は「配布物は Maven Central の成果物(コネクタ・エンジン)やコンテナのイメージでありうる」のままで、§2.5 では処置1を受けてコンテナイメージの取得・検査手順が削除されている(「またはコンテナのイメージ」を消した)。コンテナを実行段で扱わない理由(docker デーモン不可)が§1側には書かれておらず、両節を離して読むと矛盾して見える。
>
> 6. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:22` — N 節冒頭に追加された「N に入れたリポジトリ・文書のサイト・配布物を全部、名前と URL で…並べる」の一文が、N の定義本体(直後の「候補の公式のソースのリポジトリ…」)より先に置かれ、まだ定義されていない N を先に参照する構成になっている。実害は小さいが読み違いを誘う。
>
> 止める指摘: なし(114 回目の[止める]指摘1は実測により解消を確認)。

## 処置(リード、監査 115 回目)

1. **直した**。「芋づるで取らない」は §6-1 の検査のための取り方に限る。最小実行で要る依存は §6-1 の結論のあとで `mvn` で解決してよく、解決した依存の一覧を生ログに出す(§6-1 の「依存の一覧」)と書いた。
2. **直した**。「立てられる」を「バイナリはある(サーバーを立てて接続できるかはリードは確かめていない。調査班が確かめる)」にした(O-3)。
3. **直した**。`initdb` → `pg_ctl start` → `psql` で接続を確かめる → `pg_ctl stop` と書いた。
4. **答える**。実行の形の名前は、公式の文書の逐語から調査班が引く(リードが名前を先に書くと、確かめていない名前を書くことになる)。
5. **直した**。§1 に「docker のデーモンが動かないので、コンテナのイメージは取らず、動かさない」と書いた。
6. **直した**。N の一覧を並べる文を、N の定義の文のあとに移した。

直したあとの起動文の印: `20260923_tools_survey_cat8_run26_prompt.md@378392fed1ce`。止める指摘は無いので、26 回目を起動する。
