# 監査: 道具サーベイ 区分 8 の 16 回目の起動文(2026-09-25)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md`。

## 監査 87 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run16_prompt.md@07bb4db48d5c`、コミット 62a0387)の逐語

> 対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md`(指紋 07bb4db48d5c、コミット 62a0387)。
>
> **依頼点1(15回目からの複製時の食い違い)の確認結果**: `diff` で 15回目起動文全文と突き合わせ、回の番号・生ログ名(`20260923_tools_8_run16.log`)・§1 の範囲(1候補・8-026のみ)・§0 の参照読み替え・§7 の4検査コマンド(`check_scan_report.py` の引数に `run15.log` と `run16.log` の両方が入っている、`--round 16`、`check "" run16.log`)・§3/§4 の回数表記(「1〜15回目の節は書き換えない」)を確認したが、致命的な食い違いは見つからなかった。以下、他の依頼点(2〜4)を中心に見つけた点。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:32` — 「E6 の組 495,145 行(約 34 MB)」の数値を、同じ手法(`git grep -I -i -E`、置き場 `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/8-026/openclaw`、まだ残存していたので実測可能だった)で独立に再現すると、行数 495,145 は一致したが、バイト数は `wc -c` で 58,323,084(約 58 MB)であり、記載の「約 34 MB」と約 1.7 倍食い違う。他の要素(E5=238,926行/実測約28.7MB、E4=212,658行/実測約24.4MB、E2=232,887行/実測約28.2MB、E1a・E1b=42,054行/実測約5.9MB、E3a・E3b=2,492行/実測約0.4MB)は行数がすべて記載と1件も違わず一致し、検索語の組の再現性そのものは高い。90 MB の上限判定は本文どおり `wc -c` のリアルタイム実測に頼る設計なので危険側には振れないが、記載の個別バイト見積もりは正確でない(合計は git grep 換算で約145.8MBとなり、本文の「200 MB前後」という総量見積もりとは大きくは矛盾しない)。(P2 に近い型 — 数値の出所)
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:33-35`(「まず要素ごとに『印』の根拠を探す」→「『なし』を書く要素だけ全件の検索を打つ」の順序)— この順序は文章の指示のみで、`cat8_ledger.py check-elements` はこの順序自体を検査しない(検査するのは結果の形式であって手順の順番ではない)。13回目・14回目の調査班はいずれも文章のみの手順(「ファイルごとに当たった行を読んで書く」「割り当てられた候補に手を付ける」)を守らなかった実績がある(§0の8で引用されている監査75・77回目)。もし調査班が先に大きい要素(E2・E4・E5・E6)の全件検索を打ってしまうと、90 MB の予算を使い切り、本来なら印で容易に解決できたはずの要素まで `未判別` に落ちる恐れがある。この順序遵守を検収でどう確認する計画か(生ログの手の並び以外に確認手段はあるか)は、リードの判断としてすでに検討済みか。(P8 — 規則は書くだけでは機能しない)
>
> 3. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:33` — 「印」の根拠を探す対象範囲(候補の文書・README・ソース全体か一部か)、探索をどこで打ち切って「なし」側の全件検索または「未判別」に進むかの基準が書かれていない。49,578 ファイルの大きい候補で、探索自体に上限が無いと、(a) 時間を消費しすぎる、または (b) 安易な一致で「印」と判定して切り上げる誘因が生まれるおそれがある。監査78回目以降繰り返し指摘された「決まりが狭い正規表現+虚偽の理由で真の陽性を隠す」型は `cat8_classify.py` 側の話だが、この「印」探索は人の目視判定であり、同種の誘因(量的プレッシャー下での甘い判定)が道具のチェック対象外の場所に残っていないか。
>
> 4. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:35` — 「報告...(いま約8MB)も90MB以内に保つ...書き足す前後に`wc -c`を生ログに残す」の一文は、直前の文(生ログ自体の`wc -c`を生ログに打つ)と並んでおり、この文の「生ログに残す」対象が(a) 報告書のサイズを測った出力を生ログに書き込むことか、(b) 生ログ自体のサイズを測ることかが一読で紛れる。具体的なコマンド例(`wc -c docs/DATA/SCAN_2026-09-23_tools_cat8.md` の出力をどう生ログに書き込むか)が無い。
>
> 5. [聞く] `docs/DATA/tools_catalog_cat8.tsv`(8-026行、`状態` 列)/ `docs/DATA/surveys/CAT8_DESIGN.md:26-27`(状態語の定義)— 台帳の 8-026 の状態は `判別に一次資料が要る`(定義: 「一次資料にまだ届いていない」)のままだが、14回目に実際にリポジトリを取得・展開しており(`docs/DATA/probes/20260923_tools_8_run14.log:354,383-386` に `files_in_tree(N)=49578`・`downloaded_bytes=157762552`・`checked_out_bytes=562285726` を確認)、一次資料に届いていないのではなく、E1a〜E6 の判定作業が終わらなかっただけに見える。この状態語は設計票§2の定義(`浅い` = 一次資料に届いたが深掘り条件を満たしていない)のほうが実態に近くないか。すでに検討済みか(前回までの検収で確認済みなら根拠を示してよい)。
>
> 6. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:5,25` — この回でも 90 MB の制約で 8-026 の一部要素が `未判別` のまま残った場合、次にどの回で拾うかが起動文に書かれていない。17回目は既に 8-029〜8-041 専用に割り当て済みであり(:25)、8-026 の残りとの同居を想定しているのか、別回(18回目)を新たに立てる想定かは、リードの判断としてすでに検討済みか。OpenClaw は 11回目以降すでに複数回(13→14→15→16)先送りされてきた候補であり、L-416「一気に終わらせようとせずに」の範囲内かどうかは、90 MB の制約下でも本当にこの回で完了する見込みが立っているかに依存する。
>
> 以上6件のほか、cat8_classify.py の「行の中の検索語が全部どれかの決まりに覆われるときだけ行を取る」という15回目検収の修正(c922b2f)を反映した文言(`run16_prompt.md:43`)は、15回目検収記録・監査86回目の確認済みの実装と一致していることを確認した。`CAT8_MAX_CHECKOUT_BYTES=734003200` の再利用根拠(展開562,285,726バイトが700MB上限内)、台帳8-026行のE1a〜E6が全部`未判別`であることも実測・確認済みで、致命的な不一致は見つからなかった。

## 処置(リード、監査 87 回目)

1. **直した**。リードの見積もりは `awk` の `length`(字の数)で数えていて、多バイトの字を含む行ではバイト数より小さく出ていた。起動文のバイト数を、監査役が `wc -c` で測った値に替えた(E6 約 58 MB・E5 約 29 MB・E2 約 28 MB・E4 約 24 MB・E1a/E1b 約 6 MB・E3a/E3b 約 0.4 MB、合わせて約 146 MB。道を付けるとさらに大きい)。
2. **答える(直した)**。起動文に、全件の検索より先に `印` を探す理由(先に大きい検索を打つと 90 MB を使い切る)を書いた。検収でリードが、生ログの手の並び(`印` を探す手が、その要素の全件の検索より前にあるか)を見ると書いた。機械の検査は掛けない。手の並びは生ログに全部残るので、検収で読める。
3. **答える(直した)**。起動文に、`印` の条件を緩めないこと(候補の機能に当てる。検証される戦略・例のコード・候補自身の試験は数えない)と、量が多いことは `印` を早く決める理由にならないことを書いた。どこを読んで `印` を探したかを、要素ごとに書かせる。探す時間の上限は置かない(L-506「中途半端で止まらないでください」)。
4. **直した**。報告と生ログの大きさは、1 つの手でまとめて測るとし、そのコマンドを書いた。
5. **答える(直さない)**。状態の語は、16 回目の検収で結果から付け直す。いまの `判別に一次資料が要る` と `浅い` のどちらでも、残りの数(残り = 未着手 + 判別に一次資料が要る + 浅い)は同じで、この回の作業も変わらない。
6. **答える**。この回で 8-026 に `未判別` が残ったら、17 回目(8-029〜8-041)のあとに 8-026 だけの回を立てる(18 回目)。残った要素と量を見て、その回の手を決める(生ログを要素ごとに分けるなど)。1 回に入れる量を増やさない(L-416「一気に終わらせようとせずに」)。回を待たずに続けて打つ(L-506)。

直したあとの起動文の印: `20260923_tools_survey_cat8_run16_prompt.md@6cb9224a95c8`。監査 88 回目に掛ける。

## 監査 88 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run16_prompt.md@6cb9224a95c8`、コミット 2657085)の逐語

> 1. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:33 — 監査87回目の指摘3に対する処置3は「印の条件を緩めない」「量が多いことは印を早く決める理由にならない」「どこを読んで印を探したかを書かせる」の3点を追記したが、**「印」の根拠を探す範囲の下限(README全体を読むのか、一部の抜粋で足りるのか、どこまで探せば「印なし」と結論してよいか)**は依然として書かれていない。文言は「候補の文書・README・ソースから」とあるだけで、`grep -n`・`sed -n` の対象をどこまで広げてよいかの上限も下限も無い。下限が無いまま「印なし」に急ぐと、監査87回目の指摘2が懸念した事態(印で済むはずの要素が安易に全件検索へ回り、90 MBを早期に使い切る)がそのまま残る。逆に上限が無いまま広く`grep -n`/`sed -n`を候補ソース全体に打てば、それ自体が事実上の全件検索となり、順序で節約しようとした予算をこの段階で使い切る恐れもある。この範囲の未定義はリードが検討済みで承知の上のものか。(P8)
>
> 2. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:35 — 監査87回目の指摘4(報告と生ログのどちらを測るかが紛れる/具体的なコマンド例が無い)を受けて、`cat8_step.py`で2ファイルをまとめて測るコマンドを追記した点は解消になっている。しかし、**このコマンドをいつ(どの契機・どの頻度で)打つべきか**が書かれていない。直前の生ログの規則は「検索の手を打つ前後に」という明確な契機を持つが、報告ファイル(`### 当たりの判定(行ごと)`の行が増えて肥大化する)については、追記された文に契機の指定が無い(元の版にあった「書き足す前後に`wc -c`を生ログに残す」という契機の文言がこの修正で削られ、コマンド例だけが残った)。契機を書かないまま残すと、報告の90 MB超過に気づくタイミングが調査班任せになり、§2の3の「報告も90MB以内に保つ」という規則が「書くだけで機能しない」形になる恐れがある。
>
> 3. 大きさを測るコマンド `python3 scripts/cat8_step.py --log docs/DATA/probes/20260923_tools_8_run16.log --method run --target size --note 大きさ -- wc -c docs/DATA/SCAN_2026-09-23_tools_cat8.md docs/DATA/probes/20260923_tools_8_run16.log` は実際に手元で動作確認した(ダミーファイルで実行し、`wc -c`の2ファイル分＋合計行が生ログに正しく記録されることを確認)。構文上の問題は無い。止める指摘なし。
>
> 4. バイト数の数え方の注記(32行目「バイト数は監査87回目が`wc -c`で測った値(道を除いた行の字だけ)」)は、監査87回目の逐語の実測値(E6=58,323,084バイト≈58MB、E5≈28.7MB→29MB、E2≈28.2MB→28MB、E4≈24.4MB→24MB、E1a・E1b≈5.9MB→6MB、E3a・E3b≈0.4MB)と行数の対応を1件ずつ突き合わせたが、要素と数値の取り違えは無く、合計約146 MBの根拠も個別値の合計(約145.4 MB)とおおむね整合していた。この点に止める・直す指摘なし。
>
> 5. `印` を先に探す手順の追記(33行目)は、監査87回目の指摘2への処置(順序の明記・検収での確認方法の明記)と文言・内容とも一致している。15回目・14回目の起動文で定義された「印は全件の検索を要さない」「引く文は生ログに印字する」との整合も確認し、用語の重複定義・意味のずれは見つからなかった。この点に止める指摘なし。
>
> 他に、監査87回目の処置5(状態語は変えず16回目の検収で付け直す)・処置6(残余は18回目で拾う計画)は、いずれも「起動文自体は変更しない」という処置内容と一致しており、起動文側に新たな食い違いは生じていない。
>
> 上記1・2以外に、起動文全体(§0〜§7)を通読した限り、調査班がこのまま読んで16回目を着手・完了できないという意味での止める指摘は見つからなかった。

## 処置(リード、監査 88 回目)

1. **答える(直した)**。`印` を探す段では `なし` を結論しないことにした。`なし` は全件の検索からだけ出すので、「どこまで探せば `印` なしと言えるか」という下限は要らない。上限は、出力の形で置いた。README と文書のディレクトリの目次をまず読む。ソース全体への `grep` は、ファイルの名前か件数だけを出す形(`grep -rlI`・`grep -rcI`)に限る。行そのものを出すのは、読むと決めたファイルの `sed -n` か `grep -n` だけにする。これで、`印` を探す段が事実上の全件の検索になることを防ぐ。
2. **直した**。大きさを測る契機を書いた。cat8_search.py・cat8_classify.py の手を打つたびにその前後で測る。報告の `### 当たりの判定` の表に行を書き足すたびにも、その前後で測る。

止める指摘は 87・88 回目とも無い。この 2 件の手当てで起動文を送る。送る版の印: `20260923_tools_survey_cat8_run16_prompt.md@22c786b0ad16`。
