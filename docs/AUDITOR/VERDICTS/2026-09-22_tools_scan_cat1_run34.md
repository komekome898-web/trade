# 区分 1 の 34 回目 — リードの検収(2026-09-23)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 34 回目の実行」節(+355 / −0)/ 生ログ `docs/DATA/probes/20260922_tools_1_run34.log`(2,912 行)

## 機械(手順書 `SURVEY.md` §3 の順)

- **Jev の検査(この回の節だけ)**: 印 7 件。288 行(委任文 §4.0)は**直した形(「下の」入り)で正しく、空振り**。5・29・287 行は起動の指定の引用で実物どおり。
  316 行は段 3 の定義の引用で**末尾の「こと」が落ちている**(問いの文の中なので記録に留める)。319 行の 20 回目の引用は実在する(2 件)。
- 削除行 0。届いたままを記録。受け入れ検査 13 本 0 件(生ログ 33 本)。
- 数え直し: **段の表 52 行 = 印 52 件**、段の表にあって印が無い 0 / 印があって段の表に無い 0 / 未分類のまま(機械)0。
- **板の待ち行列の印を持ち、市場影響と約定の模型の印を持たない候補: 13 → 2 件(100・109)**、2 件とも理由つき。

## **リードの誤りの訂正 — 候補 90 は比例配分ではなく FIFO だった**

リードは 20 回目の検収とオーナーへの報告で「**候補 90 は表示量に比例して配る。当方は FIFO で、配分の規則が違う**」と書いた。**README だけを根拠にしていた。**
リードが原典を取り直して確かめた(既定枝 `main`、`.py` 16 本・145,084 バイト):

```
README.md:134   … Queue position is simulated based on displayed size and pro-rata fill allocation.
実装 16 本の pro-rata / pro_rata / prorata の当たり = 0 件
実装           Tracks environment volume ahead and agent orders in FIFO.
               # Then fill agent orders FIFO
```

**候補 90 は当方の `maker_fill_ref.py` と同じ FIFO である。**20 回目の節は「README がそう**書いている**」と正しく書いており、**事実として言い換えたのはリード**。
これは「持っていると書いてあるのに効いていない」の**4 つ目の形 = 文書が名乗る機構が実装に無い**である(3 つの形 = 呼ばない / 既定で無効 / 当方の engine.py)。

## 調査班の問い 7 件 — リードの回答

1. **段 3 を列の模型に付けるか → 述語では付く。この回では動かさず、未処置として資源に書く。**約定の列を時刻つきで 1 件ずつ当てて埋まりを決める(95・90)なら「埋まりを決める到着が事象になっている」に当たる。98 番の前例ではなく述語で決める(例示で決めない = 手順書 P1)。
2. **37 番の量に比例する滑り(`bps = self.slippage_bps_per_100_shares * units`)を段 6 → よい。**注文量の関数なので市場影響の関数である(定率の滑りとは違う)。
3. **100・109 の `区分1-板の待ち行列` → 述語に照らすと外れる見込み。未処置として資源に書く。**述語は「自分の注文が列のどこに居るかを追う実装があるものにだけ付ける」。100 は照合の本体が空、109 は注文を入れる口が無い。
4. **90 の訂正の追記 → この回の知見 5 が訂正である。**20 回目の節は README の記述として正しいので書き換えない。**リードの誤りは上の節と、オーナーへの報告で訂正する。**
5. **65 の既定の機関は未確認のままでよい。**
6. **97 の生の板の型・90 の taker の型を段 4 → 段 4 の定義(「約定の出来高を読んで」)は提示量を含むかを言っていない。定義の穴として未処置に書く。**63 の前例に合わせたこと自体は、述語でなく例示で決めた形(手順書 P1)なので、次に定義を締めるときに当て直す。
7. **道の誤りの空振り 3 本を生ログに残して打ち直したのは正しい。**

## 判定

**受け取る。**13 件のうち 11 件に印と段が付き、段の表と印の集合が 52 = 52 で揃った。**リードが README から言い換えた誤りが 1 件出た(候補 90)。**
**区分 1 を閉じる。**未処置の 3 件(段 3・100/109 の印・段 4 の定義)は、完了の形(一次資料に到達)には響かない精度の直しとして、資源の索引に明記する。
