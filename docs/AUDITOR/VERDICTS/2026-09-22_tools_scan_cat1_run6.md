# 区分 1 の 6 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 6 回目の実行(2026-09-22)」(2270 行目以降、301 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run6.log`(新規 98 行、22 手。復帰文字 0 個をリードが確認)

## 0. 無改変の確認

既存の 2269 行は `git show HEAD` と `diff -q` で**一致**。`git diff --stat HEAD -- docs/DATA/probes/` は**変更なし**
(過去の生ログに手は入っていない)。5 回目の警告(生ログに `rc=` を書き足すのは検査に合わせて書く型)に対し、
今回は**生ログに 1 文字も足さず、報告側の引用の形を直して**K11 を 0 件にしている。正しい直し方である。

## 1. 調査班が閉じずに残した 1 件 — **当たり。検査の欠陥。**

**K12 が文書全体の最後の「---- 検査対象の合計 N 件」しか見ていなかった。**
古い節の貼り付けが新しい節の身代わりになるので、6 回目の貼り付けを書く前から K12 が 0 件で通っていた。
**最後の「受け入れ検査の出力」の節の中だけを見るように直した。**
直す過程で切り出しを 1 度誤り(見出し行自身を次の見出しとして拾った)、欠陥入力で直したことを確かめてから採用した。

**4 回続けて、調査班が閉じずに残した行から検査の欠陥が出ている。**§12 の規則はこの回も働いた。

## 2. リードが打ち直した受け入れ検査

生ログ 4 本を渡して **13 本すべて 0 件**。

## 3. 抜き取り

**一次資料 10 行から無作為 5 行 — 5 行とも一致。**
author = "Ziplime" / maintainer = None、最新版 1.19.16 の upload_time 2026-06-18T09:21:46、
vulnerabilities 0、最古 2024-12-06T15:21:25(0.1.11)、LICENSE は GPL v3。

**実測 30 行から無作為 3 行 —(今回から追加した手順)**
- 星 564 / fork 57 / watchers 9 → `ungh.cc` を打ち直して**一致**。
- 隠れた依存 `limexhub` `lime-trader-sdk` → PyPI の `requires_dist`(30 件)に**両方在る**。`yfinance` も在る。
- 対応取引所(`run --exchange-type` が模擬と lime-trader-sdk の 2 つ)→ ソースに `simulation_exchange` の取り込みを確認。
  **選択肢の全部は確かめていない**(導入せずに読んだ範囲)。ここだけ部分確認。

**「できない・取れない・無い」の主張 — 全部そのまま確かめた**(5 回目の誤りを受けて足した手順)。
- 「PyPI から `Ziplime` のリポジトリへ辿る道は無い」→ `project_urls` も `home_page` も **None**。
  名前から推測した `Ziplime/ziplime` は **404**。**主張は正しい。**
- 「README は Yahoo Finance を挙げるが、配布版の `ingest` にその選択肢が無い」→ README に `yahoo` が **4 回**。
  `__main__.py:109` の取り込みの選択肢は `click.Choice(['limex-hub', 'lime-trader-sdk'])` の **2 つだけ**。**食い違いは実在する。**
- 「取り込みは鍵が要る」→ `__main__.py` に `LIMEX_API_KEY` の文字列は 0 件だった。**鍵の読み出しは別の場所**と見られる。
  導入せずに読んだ範囲では**確かめきれていない**。誤りとは書かない(リードの確認が足りないだけ)。

## 4. 調査班が挙げた「原文に無い判断」3 件の判定

1. **`Ziplime` の README に並ぶ LLM の製品名を写さなかった → リードの判断: 写してよい。**
   委任文 §10 の「モデル名を書かない」は、**こちらの模型の素性をリポジトリに書かない**ための規則である
   (CLAUDE.md §6 と同じ趣旨)。**調べている道具が何に対応しているかは「できること」そのもの**で、
   §4 が一次資料の逐語で全部書けと求めている側に当たる。次の回で埋める。
2. **候補の一覧に「限界」の行を足し、LimexHub と Lime Trader SDK を候補に立てていないと明記した → 受け入れる。**
   この 2 つはデータと執行の道具なので、**区分 3(データ)と区分 2(執行)の候補**として引き継ぐ。
3. **最小実行で 3 つの回避を行い、回避しなかったときの誤りを全部生ログに残した → 受け入れる。**
   「動いた」だけを書くと欠陥が消える。7 回の失敗を残したのは正しい。

## 5. リードの判定

- **受け取る。中身の誤りは 0 件。**
- **深掘りは `Ziplime` 1 件。**区分 1 で**初めて「成行と指値の 1 往復」が通った**(`bt` と `fast-trade` は
  注文の種別の概念が無く、委任文 §5-4 が書けなかった)。
- **`Ziplime` は最新版に 3 つの欠陥**(`timedelta` を受ける注釈なのに polars が落ちる / `auto_close_date` が
  dataclass では None 可・DB では NOT NULL / 対照銘柄なしで `validate_benchmark` が None 参照)。
- **配布元の照合ができない道具である。**PyPI に所在が無く、GitHub の LICENSE を直接取って初めて GPL-3.0 と分かる。
- **区分 1 は未完了。**残り 10 件(Superalgos / OpenTrader / CryptoSignal / OctoBot / pybotters / zenbot / Bot18 /
  Mendl-Labs/BacktestingCore / Luczinsritter / mlflow)+ PySystemtrade は浅いまま。検索計画 6 本は未実行。

## 6. PySystemtrade(取り直し)

`--filter=blob:none --sparse` で **878 MB → 3.6 MB**。GPL v3 / 1.8.2 / 作者 Robert Carver /
**保守者 Andy Geach(作者と別人)** / 依存 15 件 / `.py` 328 本。依存に `pymongo` と `ib_async` があり、
そのまま使うには MongoDB と Interactive Brokers が前提。導入と最小実行は未確認。
**リードが 5 回目に許可した全部の clone は前提が外れていた。**部分取得に切り替えたのは正しい。
