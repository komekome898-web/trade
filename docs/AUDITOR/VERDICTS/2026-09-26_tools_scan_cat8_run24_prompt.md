# 監査: 道具サーベイ 区分 8 の 24 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run24_prompt.md`(8-033 Prefect、未着手。21 回目の起動文から作った)。

## 監査 110 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run24_prompt.md@a53bdc714b85`、コミット 6be7fe1)の逐語

> ## 監査結果(道具サーベイ区分8・24回目起動文)
>
> 対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run24_prompt.md`(指紋 a53bdc714b85、コミット 6be7fe1)。参照: 21〜24回目起動文、21〜23回目検収(`VERDICTS/2026-09-26_tools_scan_cat8_run21.md`〜`run23.md`)、21〜23回目起動文の監査(`..._run21_prompt.md`〜`..._run23_prompt.md`、監査101〜109回目)、`CAT8_DESIGN.md`、`20260922_tools_survey_prompt.md` §5・§6、`OWNER_LOG.md` L-508・L-509・L-516・L-519。
>
> 1. [直す] `run24_prompt.md:62` — §6-1 の項目として列挙されているのは「setup.py・pyproject.tomlの導入時実行 / 難読化 / 外部URLからの取得 / バイナリ同梱 / PyPIの配布元とGitHubの一致 / 保守者の数と名前の一貫性 / 既知の脆弱性」の7項目だが、委任文 `20260922_tools_survey_prompt.md:99`(§6-1)は「PyPIの配布元とGitHubの一致 / **初回公開日と最新版の日付** / **週のダウンロード数** / 保守者の数と名前の一貫性 / (pip downloadの中身の4項目) / 既知の脆弱性 / **依存の一覧**」を定義しており、「初回公開日と最新版の日付」「週のダウンロード数」「依存の一覧」の3項目が抜けている。委任文 §6-1 は「1つでも不審なら導入せず…止める」対象として全項目を並べているので、この3項目もリスク検出に効きうる(新設パッケージ・異常な依存数など)。この抜けは22回目起動文(`run22_prompt.md:21`)から同じ形で持ち込まれ、22回目の監査103・104回目、23回目の監査106〜109回目のいずれも捕まえておらず、24回目に3度目そのまま使われようとしている。「§6-1 の項目を全部見る」という文言と実際に列挙された項目数が一致しているか。(P1・P3)
>
> 2. [直す] `run24_prompt.md:15` — 「22 回目に E1b・E4 の言い換えで `印` を付けた」という要約は、E4 については「時刻順に」という原文に無い語の追加(=正確な言い換え)だが、E1b については22回目の調査班が dbt のモデル機能(利用者が全部書く SQL)を除外する読みがまだ確立していなかったために `印` を付けたもので、原文の言い換えというより「除外基準の未確立」が原因(23回目検収§4処置1「23回目の起動文の除外の文の書き方だけで…広すぎる文をリードが書いた」とも別の話)。異なる型の2つの誤りを同じ語「言い換え」でまとめると、この回の調査班が「何をしないべきか」を正確に引き継げない恐れがある。(O-7 の型)
>
> 3. [聞く] `run24_prompt.md:35`(E1b の読み) — 「利用者が定義を書き、計算の中身は道具が持つものは道具の計算に数える」という読みは、22・23回目検収で「14回目から変わっていない・新しい限定ではない」と確認済みだが、この区別(利用者が定義を書くか/計算の中身を道具が持つか)自体は設計票 `CAT8_DESIGN.md:53` の E1b 逐語には無い。設計票 §3 の自己規定(「述語を足したり変えたりしたら、全行に当て直す」)の対象になりうる読みを、当て直しをせずに3回連続(22・23・24回目)で既定として使い続けてよいか。全行の当て直しの要否は、この起動文だけでは判断できない外部の未決事項として残っていないか。
>
> 4. [聞く] `run24_prompt.md:22,62` — N(候補の公式リポジトリの `git clone`)は `.../scratchpad/cat8/venvs/8-033/` の下に置き、§2.5 の最小実行(pip でインストールする隔離環境)も同じ `.../scratchpad/cat8/venvs/8-033/` の下に置く、と同じ基底ディレクトリを指している。dbt の回では、この基底ディレクトリの共有(git 追跡のクローンと、pip インストール物である exec_venv が同居)が、107回目監査の止める指摘1(「由来の異なる3つの資材を同格に並べている」「バージョンがmain にも 1.latest にも対応しない」)の一因になった。run24 にはサブディレクトリを明示的に分ける指示(例: ソース探索用と実行用を別名で分ける)が書かれていない。同じ混同が Prefect でも起きないという保証はこの起動文から読み取れるか。
>
> (良好だった点 8 件は省かずに記す)§6-1(導入前の検査)が最小実行より先に置かれ、21回目の「実行が先・検査が後」の誤りは再発しない形になっている(`run24_prompt.md:62`)。/「何も当たらなかった」と書けるのは本当に何も見つからなかったときだけ、という22回目の指摘(結論が判断を隠す問題)への手当ても書かれている(同箇所)。/ 別の配布物(利用者が別に入れるプラグイン・パッケージ)は「同梱」の場合だけ数え、「呼び出している」「案内している」という21回目・23回目でリードが広げすぎた語は使われていない(`run24_prompt.md:21`)。/ E4 の「時刻順に」を言い換えで足さないことが明記されている(`run24_prompt.md:36`)。/ 案B(オーナー決定 L-519)の記録(読んだ範囲・当たりの行の数)と、その範囲の決め方・限界が明記されている(`run24_prompt.md:26`)。/ 500行の見積もりに「数えるのは候補の公式のリポジトリと文書だけ」と明記され、23回目の別配布物混入の誤りへの手当てがある(`run24_prompt.md:26`)。/「オーナーが承認した4つの問い」の見出しが、個別事例への承認であり他候補への当て方はリードの決定である旨を明示する形(108回目監査の処置4)のまま維持されている(`run24_prompt.md:37`)。/ 回の番号・生ログ名(`run24.log`)・`--round 24`・返す前の4検査(run1〜run24)はすべて一致しており、dbt/8-030 固有の文の残存は見当たらない(§2.1のE1b/E4の読みで過去回の実例として言及される箇所を除く)。
>
> 止める指摘: なし。

## 処置(リード、監査 110 回目)

1. **直した**。§6-1 の項目を委任文 §6-1 の逐語の並びのとおり 9 つ全部にした。**リードの誤り**: 22・23 回目の起動文でも 3 項目(初回公開日と最新版の日付・週のダウンロード数・依存の一覧)を書き落としていた。dbt(22 回目)の §6-1 の結論に、この 3 項目が入っていたかは、22 回目の生ログで確かめていない(未確認)。ただし 3 項目の値そのものは 21 回目の §4.0 の表にある(初回公開日 2016-03-23・週 DL 数 5,244,558・依存の一覧 61 件)。資源化の前の全体受け取り監査で、22 回目の §6-1 の結論を 9 項目で見直す。
2. **直した**。E4 は原文に無い「時刻順に」を足した言い換え、E1b は利用者が計算の中身を全部書く機能(モデル)を道具の計算に数えた、と分けて書いた。
3. **答える**。設計票 §3 の E1b の逐語は「…同じ種類の出力を、当方と別の実装で出す**計算を持つ**」。「計算を持つ」の主語は道具で、利用者が全部書く計算は道具が持つ計算ではない、というのは逐語の当て方で、述語を足したものではない(22 回目の検収 §4 の処置 2 で 27 行中 16 行を、23 回目の検収 §4 の処置 1 で 14 回目の Great Expectations の行と E1b の `なし` の 2 行を読んで確かめた)。読んでいない 11 行は全体受け取り監査で見る(22 回目の検収に書いたとおり)。
4. **直した**。ソースを読む `src/`・§6-1 で取る `check/`・動かす `run/` に分け、`run/` は pip で入れた物で git の枝ではないこと、版が違えば両方書くことを書いた。

直したあとの起動文の印: `20260923_tools_survey_cat8_run24_prompt.md@44054264f3c9`。止める指摘は無いので、24 回目を起動する。
