# 区分 1 の 25 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 25 回目の実行」節(309 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run25.log`(245 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲・受け入れ検査(機械)

`git diff --numstat` = **309 行追加 / 0 行削除**。削除の実測も 0 本。**過去の回の節は 1 行も触っていない。**
当方が全 24 本の生ログを渡して打った結果は 13 本すべて 0 件。**判定: 合。**

**直した K12 が現場で当たった。**調査班の報告の逐語: 「**この回の節を作る前に打ったとき、実際に K12 が 1 件で当たりました。**24 回目まではこの状態が 0 件で通っていたものです。」
**穴が塞がったことの実地の証拠である。**

## 2. **リードの誤りの訂正 — 24 回目の検収 §4 が間違っていた(調査班の問い 2)**

24 回目の検収に当方はこう書いた:

> 候補 4・33・2 は**段(機構)が高くても段(既定)は 1** である。

**このうち候補 33 は誤りである。**調査班が「締め直した定義を当てると 5 になる」と指摘してきたので、当方で原典を取り直した:

```
$ curl .../cpp/execution/execution_simulator.cpp        (7,277 バイト)
  8:    , slippage_model_(std::make_unique<ZeroSlippageModel>())   ← 滑りだけが既定で無効
 15:    // Set up matching engine callbacks
 16:    matching_engine_.set_fill_callback(
124:    // Submit to matching engine
```

**`slippage_model_` は差し替えられる `unique_ptr` で既定が `ZeroSlippageModel`。一方 `matching_engine_` はただの実体で、構築子で無条件に繋がれている。**
**つまり既定で無効になるのは滑りだけで、値段・時刻の優先による照合は既定でも働く。段(既定)は 5 が正しい。**

**誤りの原因**: 当方は「滑りが既定で無効」という一つの事実から「段(既定)は 1」へ飛び、**残りの約定の機構が既定で働くかを確かめなかった。**
そのうえ**規則を例示で書いた**(「候補 4・33・2 は…1 である」)。**規則は述語で書き、当てはまる候補は当てて出すものである。**
**この癖は 3 回目**(19 回目の集計の指定漏れ、21 回目の一本化の指定漏れ、今回)。

→ **リードの手順に固定する: 規則を書くとき、当てはまる先を数え上げて規則の一部にしない。**述語だけ書き、当てはめは測って出させる。
**調査班が当方の例示ではなく定義のほうに従い、食い違いを挙げて返したのは正しい。**自分で辻褄を合わせていたら誤りが残った。

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| **候補 21 `Qlib` の市場影響が既定で無効** | `qlib/backtest/exchange.py`(44,235 バイト)51 行 `impact_cost: float = 0.0,`、114 行 `:param impact_cost:     market impact cost rate (a.k.a. slippage). **A recommended value is 0.1.**`、890・892 行で `adj_cost_ratio = self.impact_cost * (trade_val / total_trade_val) ** 2` | **当たり** |
| **候補 33 の照合が既定で働く** | §2 のとおり | **当たり(当方の誤りが確定)** |

**候補 21 の発見は重い。**原典自身が「推奨は 0.1」と書いているのに**既定は 0.0** である。
**そのまま使うと、2 乗の影響の式は在るのに 0 倍で効かない。**広く使われている道具で、書いてあることと既定が違う。

## 4. **段(機構)と段(既定)が違うのは 26 件中 7 件 = 約 4 分の 1**

内訳は (a) 既定で段が下がる 5 件(2 `Backtrader`・4 `PyBroker`・21 `Qlib`・18 `zipline-reloaded`・33)と (b) 既定が決まらない 2 件(23・6)。

**これが 2 列にした値打ちである。**「滑りの模型を持っている」「市場影響の式を持っている」と書いてある道具の**約 4 分の 1 が、そのまま使うと書いてあるより楽観的に埋まる。**
**当方が道具を選ぶときに効くのはこの欄であり、1 列のままなら見えなかった。**

**候補 1 `Basana` だけが逆向き**(既定 `VolumeShareImpact` のほうが高く、差し替えると段 1 に落ちる)というのも、
**「既定を読まないと向きすら分からない」ことの実例である。**

## 5. 調査班が挙げた 4 件の判断 — リードの回答

**(1) 19 番 `Jesse` が段 3 か → 段 2 のままでよい。**
「どの逐語にも無いので段 2 にした」は正しい扱い(§0.2 O-2)。
**次の回に決めるなら読むのはここ**: 分割してできたものが**機関に事象として入る**のか、単に細かい足になるだけか。前者なら段 3。

**(2) 33 番の段(既定)→ §2 のとおり。当方が誤っていた。5 が正しい。**

**(3) 既定値を読むための GET は「深掘り」ではない。打ってよい。**
当方の指定の逐語は「**深掘り(構築・導入・最小実行)もしない**」で、**括弧の中が深掘りの定義である。**原典を読むことは入っていない。
**むしろ、これを禁じると段(既定)の欄は 1 行も埋まらず、2 列にした意味が消える。**
既に出典表に載っている原典から既定値の行だけを取り直した扱いで正しい。**26 回目以降も同じでよい。**

**(4) 23 番 `hftbacktest` と 6 番 `Ziplime` の段(既定)→ 埋めに行くこと。**(3) と同じ線引きで、原典をもう一段読むのは深掘りではない。
**埋まらなければ `未確認` のままでよいが、読まずに `未確認` にはしない。**

## 6. 安全側・所見

404 は 1 件(付け替えの確認のため意図して打ったもの)、TLS 切断 1 件は打ち直して 200、**404 の欄に入れていない。**
導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。

**字形の扱いを評価する。**調査班は「字形違いは見当たらなかった」と書きかけて、**実測したら当たった**と訂正している
(K12 の直しの逐語に EM DASH(U+2014)と EN DASH(U+2013)が入っており、半角 `-` に直すと別物になる)。
**書きかけたことを取り消して測り直した**のは、§0.2 O-2(調べずに「無い」と断定しない)がそのまま働いた形である。

## 7. 判定

**受け取る。**検査 13 本が 0 件、書き換えは追記のみ、抜き取りは 2 件とも原典に当たった。
**当方の誤りが 1 件確定し(§2)、直した K12 が現場で当たり(§1)、段を 2 列にしたことで「4 分の 1 が書いてあるより楽観的」が見えた(§4)。差し戻す点は無い。**

## 8. 26 回目の起動の指定

**整理は終わった。ここから残りを潰す。**

1. **最優先: 一次資料に未到達の 11 件(51・53・54・57・60・67・70・72・87・91・92)に当たる。**
   全部 `区分1-足` の「未着手」で、一覧の 1 行の説明しか無い。**一次資料に到達して、6 要素の印と段を決めること。**
   **これが尽きれば、オーナーの決めた完了の形(逐語「一次資料に到達した候補が尽きること」)に届く。**
   11 件を 1 回で終えられなければ、**終えた分と残りを正直に書くこと。**無理に詰めない。
2. **23 番 `hftbacktest` と 6 番 `Ziplime` の段(既定)を埋める**(§5(4))。読んで決まらなければ `未確認` でよいが、読まずに `未確認` にしない。
3. **19 番 `Jesse` の段 3 の判定**(§5(1))。分割の結果が機関に事象として入るかを読む。
4. **33 番の段(既定)を 5 に直す**(§2)。24 回目の検収の当方の誤りの訂正であることを節に書く。
   **24 回目の節は書き換えない**(過去の回は記録)。
5. **既定値を読むための原典の取得は許す**(§5(3))。**構築・導入・最小実行はしない。**
6. **新しい検索計画は打たない。候補も増やさない。**
7. 上の 1〜4 に要る範囲でのみ既存の節への書き換えを許す。**追記だけで果たせるならそのほうがよい。**
8. **印が無い 2 件(14 番 `Bot18`・22 番 `Lean CLI`)と 11 番 `OctoBot` の 5 要素はそのまま**(それぞれ公開されていない / 取得していない画像の中 / 模擬を回す必要があり範囲の外)。
   **完了の報告では、この 3 つを「尽きた」に数えず、理由つきで別立てにする。**
