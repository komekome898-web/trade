# 区分 1 の 19 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 19 回目の実行」節(175 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run19.log`(206 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 既存の節が書き換わっていないか(機械)

`git diff --numstat` = **177 行追加 / 2 行削除**。削除の 2 行は全部出した:

```
-4. `GFT Backtest Software`(49 番) — 状態: **この環境からは到達できない。**4 経路を全部試して塞がった。印は**どの要素にも付けられない**(6 要素とも未確認)。
-   第 2 経路(オーナー PC)でも、入口が `app.goatfundedtrader.com/login` の内側にあるので**登録が要る。**起動の指定の逐語「**登録はせず**」により当方は踏まない。
```

**18 回目の検収 §6(5) で許した 1 箇所ちょうどである。**塊は 2 つで、5730 行(候補 49)と 5823 行からの追加(19 回目の節)。**判定: 合。**

## 2. 受け入れ検査(機械)

生ログ 18 本を全部渡して 13 本すべて 0 件、`---- 合計 0 件`。**判定: 合。**

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| 候補 120 `wbt` が区分 1 の残り要素を**自分で射程の外に置いている** | `README_EN.md`(20,501 バイト)を取得。見出し「What wbt Is Not Trying To Solve」(46 行)の下に「Tick-level order book simulation.」(48 行)・「Exchange matching-engine microstructure.」(49 行)・「Broker-specific execution modeling.」(50 行) | **当たり** |
| 候補 120 がベクトル化では**ない** | 「The entire backtest loop runs in Rust (`rayon` thread pool, configurable `n_jobs`)」(32 行)。`vectoriz` の当たり **0 件** | **当たり** |
| 候補 120 と `czsc` の向き | 「czsc strategies naturally emit the weight tables that wbt consumes.」(287 行)。粒度「dt: bar end timestamp」(168 行)。許諾は `MIT License` | **当たり** |
| 候補 73 `vectorbt` の粒度と無料の版の限定 | README を取得(13,966 バイト)。「VectorBT takes a radically different approach to backtesting: instead of looping through bars…packs thousands of configurations into NumPy arrays」(49 行)・「PRO extends the library with parallelization, … **limit orders**, leverage, and over 100 other features」(56-57 行) | **当たり** |
| 候補 74 が行列演算だけで、滑りが**実装されていない** | `engine.py` を取得(118 行)。先頭「Vectorised backtest engine.」・`gross = (w_lag * returns).sum(axis=1)`(81 行)・時間の繰り返し 0 件。**「every realistic refinement (slippage models, borrow fees, T+1 settlement) goes into a sibling module rather than here」**(16-18 行) | **当たり** |
| 候補 44 の契約に**ティック・出来高で束ねる口が無い** | `backtester/contracts/runtime/bar-time-contract-v1.schema.json`(9,202 バイト)を取得。`"aggregation"`・`"const": "time"`・`minute`〜`month`・不変条件 `same_timestamp_lifecycle_order_is_data_derived_signal_order_fill` が**どれも原典に実在** | **当たり** |
| 候補 106 の綴りの 1 文字ずれ | `ungh.cc` で両方叩いた。`bernardopaulsen/almngren-chriss` = **404**、`bernardopaulsen/almgren-chriss` = **200**(説明・押し出し日が PyPI と一致) | **当たり** |
| 候補 106 の状態の書き換えが、前の警戒を黙って消していないか | 節を読んだ。**「導入は止めたままにする(メタデータの指す先が 404 のままなので、委任文 §6-1 の配布元の一致が満たされない)」が残っている。**15 回目の指摘も「当たっているが原因の粒度が粗かった」と明示 | **合。黙って消していない** |

**リード自身の誤りを 3 件出す**(調査班の誤りではない):
1. 候補 120 の逐語が当たらないので疑ったが、**当方が中国語の `README.md` を取っていた。**原典は `README_EN.md` で、同じ登録に両方在る(`README.md` の該当節は見出し「非目标」、本文「逐笔撮合与盘口微观结构仿真。」)。**逐語は原典どおり。**
2. 候補 106 を `github.com` の HTML で叩いて両方 403 を受け取り、綴りの差を確かめられないと一度読んだ。**403 はこの環境の代理が `github.com` の HTML を塞いでいるためで、`ungh.cc` なら 404 と 200 に分かれる。**
3. 18 回目に続き、**原典の在処を確かめずに当てずっぽうで URL を組み立てる癖**が 2 回出た(既定枝・言語別のファイル名)。**19 回目の起動の指定に入れた申し送り(字形違い)と同じ型なので、次回は「当方が検めるときも、まず追跡ファイルの一覧で在処を確かめる」を自分の手順にする。**

## 4. 調査班が挙げた 6 件の判断 — リードの回答

**(1) 候補 73 の番号が二重(1・2 回目の「3. vectorbt」と同じ登録)→ 73 番に一本化する。ただし古い節は書き換えない。**
1・2 回目の節は**その時点の測定の記録**なので、後から番号を書き換えると記録でなくなる。
20 回目の節に「**3 番と 73 番は同じ登録。以後 73 番を正とする**」と 1 行書いて、そこで繋ぐこと。

**これに関して、完了の前にやってもらう作業を 1 つ足す。**案 A'(オーナー逐語「**一次資料に到達した候補が尽きること**」)で完了を数える以上、
**同じ登録が別番号で 2 度数えられていると、完了の数そのものが嘘になる。**
→ **候補の一覧 120 件について、登録の同一性(`所有者/登録名` と配布物の名前)で重複を機械で洗い出し、一覧にして返すこと。**
1 件見つかった以上、他にも在ると考えるのが自然である(調べずに「無い」と書かない = §0.2 O-2)。

**(2) 73・74 に `区分1-足` を足したこと → よい。**起動の指定の「確定させる」には、一次資料が支える印を全部付けることが含まれる。範囲の拡大ではない。

**(3) 120 `wbt` に `区分1-ベクトル化` を付けなかったこと → 正しい。これは重要な線引きなので、規則として残す。**

> **並列化されたループはベクトル化ではない。**`rayon` の thread pool で足を 1 本ずつ回すのは、速いだけで繰り返しは在る。
> `区分1-ベクトル化` の印は、**損益・建玉の計算そのものが行列演算で、時間方向の繰り返しが無い**ものにだけ付ける。

**(4) 候補 106 の状態の語を変えたこと → よい。**理由(綴りのずれ)と、止めたままにする判断の両方が節に残っていることを当方で確かめた(§3 の最終行)。
**前の警戒を黙って消していない**ので §0.2 A-9 に当たらない。

**(5) 候補 49 の直しを候補の一覧だけに限り、知見 6 を残したこと → その判断が正しい。**
知見 6 は**「4 経路を打って何が返ったか」という測定の記録**で、事実そのものである。**消してはならない。**
変えるべきだったのは「到達できない」という**解釈**のほうで、それは候補の一覧の項にある。**調査班の切り分けに誤りは無い。**
ツールの表の「到達」の欄に同趣旨の語が残っている件は、**20 回目にその 1 マスだけ直してよい**(解釈の語を「登録の内側」に)。

**(6) 候補 44 の `区分1-ベクトル化` → 外すこと。`区分1-足` だけにする。**
理由: 前処理がベクトル化されているのは pandas を使えばほぼ全部がそうなるので、**それを印にすると印が何も選り分けなくなる。**
印の意味を途中で広げるのは §0.2 O-7(用語の意味を変えて使い回す)に当たる。(3) の規則を 44 にも同じく当てる。
**74 はそのまま**(約定と損益そのものが行列演算なので)。

## 5. 安全側の所見(調査班の報告を当方が確かめたもの)

候補 120 の `zengbin93/wbt` が根に作業者向けの手引き(8,068 バイト)を、候補 74 の `initial-d/ml-quant-trading` が
`AGENTS.md`(2,422 バイト)と作業道具の設定ファイル(130 バイト)を追跡していることを、生ログで確認した。
**調査班は委任文 §6-3 のとおり中身の指示に従わず、所在と大きさだけを記録している。対応は正しい。**
**当方もこの手引きの中身を読んで従うことはしない。**取り込んだ登録が当方の作業者に指示を出しうる経路として、
14 回目の候補 44・今回の 2 件で**通算 3 件**になった。**使う段でオーナーに出す。**
この回に導入・構築・最小実行・登録・鍵の発行・購入・発注は 1 件も行われていない(生ログで確認)。

## 6. 判定

**受け取る。**機械の検査 13 本が 0 件、既存の節の書き換えは許した 1 箇所ちょうど、抜き取りは 8 件とも原典に当たった。
**差し戻す点は無い。**(6) の印を 1 つ外すことだけ、次の回の作業に回す。

## 7. 20 回目の起動の指定

1. **候補の一覧 120 件の重複を機械で洗い出して一覧にする**(上の (1))。**完了の数を出す前にこれが要る。**
2. 既存の節への書き換えを**次の 3 箇所だけ**許す。他は 1 文字も触らない。
   (a) 候補 44 から `区分1-ベクトル化` を外す。(b) ツールの表の候補 49 の「到達」の欄を「登録の内側」の趣旨に直す。
   (c) 20 回目の節に「3 番と 73 番は同じ登録。以後 73 番を正とする」と 1 行書く(古い節は触らない)。
3. **残りの本体: `区分1-イベント駆動` の 9 件(41・52・58・61・62・65・68・69・63 のうち未確定のもの)。**
   次に `区分1-ティック` の 5 件と `区分1-板の待ち行列` の 4 件。**`区分1-足` の 26 件は最後。**
4. **新しい検索計画は打たない。**候補は増やさない。
5. **深掘り(構築・導入・最小実行)は要らない。**到達と一次資料の読みで印を決める。
6. 逐語を引くときは、**まず追跡ファイルの一覧で在処を確かめてから取る**(言語別・枝違いで別の中身を掴む。当方が 2 回やった)。
   字形違い(`‑` U+2011 など)を含む行はその旨を 1 行添える。
