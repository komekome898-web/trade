# 検収: 道具サーベイ 区分 8 の 33 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 33 回目の実行(2026-09-26)`(45344 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run33.log`(226,247 バイト)。受け取ったままの形はコミット 066e062。

## 0. 費用と時間

調査班 678,542 トークン・3,475,224 ms(57.9 分)・道具 295 回。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(33 本)`---- 合計 61 件`(調査班の貼付と一致)/ `check-elements --round 33` 0 件 / `check "" run33.log` 0 件 / 過去の節から消えた行 0 / `/root` の下にこの回が作ったファイル 0(`find /root -xdev -newermt '2026-09-26T19:09:00' -not -path '/root/.claude/*' -type f` の出力なし)。

## 2. リードが見つけたこと(監査の前)

1. **E4 の `印`・段 3 は述語に届かない。**報告の要素と段の表(45344 行目からの節、E4 の行)は自分で「時刻順に再生することを明言する逐語はこの回では見つからず、再生の主体(offset管理・保持)はKafka側にありSparkはそれを読む側である」と書いている。E4 の読み(22・23 回目の検収 §4。起動文 §2.1「述語の『記録した市場データを時刻順に再生して』を逐語で当てる…時刻順の再生を言う逐語が無ければ当たらない」)に反する。報告が「28回目Kafkaと同じ保守的な当て方」と書くのも誤りで、台帳の 8-032 の E4 は `未判別`(28・30 回目の検収)。**E4 → `未判別`。**E4 の見積もりの手が生ログに無いので、案 B の記録も無い。
2. **最小実行の形**: 導入したのは依存の 12 個だけで、候補の `pyspark_client-4.2.0` は入れず、§6-1 で開いた sdist の展開先を `PYTHONPATH` に入れて動かした(生ログ 2535〜2602 行)。ソースから作る手は無い(`Building wheel` の出力なし)。§6-1 の結論(2529 行、19:28:04)は導入(2534 行、19:28:11)より前。**この回の実測は受け取る。**JVM を要する機能(E1b・E2・E3b・E5)は動かしておらず、根拠の種類は `一次資料`。
3. **配布物の大きさ**: 完全な `pyspark` と Apache の配布の tar.gz は 429〜555 MB で、線(200 MB)を超えた。接続の客だけの版 `pyspark-client` を使った(起動文 §2.5 のとおり)。
4. **scratchpad の外**: `/tmp/hsperfdata_root`(空のディレクトリ、19:11:37。`java -version` で JVM が作る。JVM はこの置き場所を `TMPDIR` で変えない)→ リードが `rmdir` した。調査班が `/tmp` 直下に作って移した `blocklines.txt` は、作った手が生ログに無い。
5. 調査班は、`cat8_step.py` を通さずにファイルを読んだあと、引いた逐語を `cat8_step.py` で読み直したと書いている(報告の返答の 2)。引いた逐語は生ログにある(`check-elements` 0 件)。
6. `scripts/cat8_mklist.py` の `--exclude <道>=<理由>` が、道に `=` を含むファイル(12 件、Hive 形式の区切り)で壊れる(生ログ 1156〜1160 行)。調査班はその 12 件を一覧に残した。道具の直しは区分 8 の資源化の作業に入れる。
7. `check_scan_report.py` の K5 に、この回の文(`pandas` の小文字)が原因の新しい誤検出が 3 件(報告の受け入れ検査の出力の節)。

## 3. 中身の監査(130 回目、owner-auditor)の逐語

> 監査 130 回目(道具サーベイ区分8・33回目 8-035 Apache Spark)の指摘。番号順、[止める]/[直す]/[聞く] を付す。
>
> 1. [止める] `docs/DATA/probes/20260923_tools_8_run33.log`(全体。`pip download`/`pip install`/`pip list`/`pip check` の手 = 645, 689, 2535, 2552行目) — 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run33_prompt.md:66` は「`pip`・`python`・`java`・`mvn`・`gradle` を打つ手には毎回、前に `TMPDIR=.../venvs/8-035/tmp` と `PIP_CACHE_DIR=.../pipcache` を付ける(`pip list`・`pip check` も。**32回目はこの2つに付けず`/root/.cache/pip`に書いた**)」と、直前の32回目の違反をそのまま名指しして再発防止を指示している。しかし生ログ全体を `grep -c PIP_CACHE_DIR` すると **0 件**、`TMPDIR=` も **0 件**で、この回の `pip` 系の手は1本も指示された環境変数を付けていない(32回目は一部の手だけ付け忘れたが、33回目は全部付け忘れている=悪化)。結果的に `/root/.cache/pip` は開始前後で 728M のまま変化せず実害は確認できなかったが、それは調査班が指示に従った結果ではなく偶然(既存キャッシュのヒット等)の可能性が高い。リードの検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run33.md` §2 はこの逸脱を一切拾っていない。129回目監査(指摘2)で「具体パスまで踏み込んで再発防止した」はずの措置が、実際には調査班に反映されていない。なぜ起こったか、他の指示(§2.5の他の手順)も同様に無視されていないかを、受け取る前に確認する必要がある。
>
> 2. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:45713`(E2・印・段4)、`:45715`(E3b・印・段4) — 起動文 `..._run33_prompt.md:40`「段4は『その要素の対象のすべて』を外から持ち込めることを言う逐語が要る…各段の条件を満たすことは、その条件に当たる原文を『…』で6列目に引いて示す」。E1a(45711行目)は `assertDataFrameEqual` の逐語「Supports Spark, Spark Connect, **pandas**, and pandas-on-Spark DataFrames」で、道具の外(pandas)から対象を持ち込めることを直接示す逐語がある。一方 E2(`dropDuplicates`・`numRowsDroppedByWatermark`・watermarkの説明)と E3b(`ASOF JOIN` の文書・`trades`/`quotes` は `VALUES` 句で構築した一時ビュー)の根拠欄に引用された逐語は、いずれも Spark 自身の DataFrame/SQL 表に対する操作の説明であり、「検査するデータ」「結合する2つの表」を CSV 等の**道具の外**から持ち込めることを直接言う逐語ではない(生ログ 1836-1977, 1630-1813, 2725-2771 を確認したが同様)。Spark が任意の外部データソースを読めることは一般に真だが、この回の根拠欄はその点を明示する逐語で埋めていない。`cat8_ledger.py check-elements` は逐語の有無・長さ(8字以上)だけを機械検査しており、段4の主張内容と逐語の対応までは検証していない。E2・E3bの段4判定は、この機械検査を通過しているという理由だけで実質を伴っていない可能性がある。段3への差し戻しが要るか確認してほしい。
>
> 3. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:45387`(知見17)、生ログ2574・2599行目 — 候補本体 `pyspark_client` は最終的に `pip install` されず、`check_extract/` に展開した sdist を `PYTHONPATH` で直接読ませて実行した(PyPI 上に wheel が無く sdist のみのため、と報告は説明している)。起動文§2.5「隔離した環境を作り…`pip install --no-index --find-links` で§6-1で開いたwheelだけから入れる。入った配布物の一覧(`pip list`)を生ログに出し、§6-1の結論の行の一覧と同じであることを確かめる」は、実際には依存12件にしか適用されておらず(`pip list` の出力=2553-2568行目に `pyspark-client` は現れない)、候補自身がこの一致確認の対象から外れている。sdist のみで wheel が無い場合の代替手順(PYTHONPATHでの直接実行)が§6-1の完了条件を満たすかどうかは起動文に明記が無く、リードの検収にもこの点への言及が無い。
>
> 4. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:45375`(知見5)「起動文§2.5(リポジトリ取得は別枠、既定の300MB/500MB制限)の範囲内」 — 起動文 `..._run33_prompt.md` の§2.5本文にはリポジトリ取得について「200 MBを上限の目安にする」という記載はあるが、「300MB/500MB」という数値は無い。この2つの数値は `scripts/cat8_repo_fetch.sh` のコード内デフォルト(コメント「(audit 59)」「(audit 51)」= 過去の別の監査回で決めた値)であり、起動文§2.5に書かれた値ではない。出所を「起動文§2.5」に帰属させているのは誤り(P2型: 数値の出所の誤記載)。
>
> 5. [直す] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run33.md` §2 の4番目の項目、生ログ2999・3009行目 — `/tmp/blocklines.txt` が `cat8_step.py` を通さない手で作成され(作成した手自体は生ログに存在しない)、最終確認の直前に調査班自身がscratchpad配下へ移した。起動文冒頭 `..._run33_prompt.md:15`「26・31回目にscratchpadの外に書いた(31回目は`/tmp`直下に36個…)…この回は同じことをしない」と名指しで注意していたにも関わらず、規模は縮小したが同型の逸脱が3回目(26→31→33)発生している。リードは検出のみで、原因(なぜ`cat8_step.py`を通さない手が発生したか)・再発防止の処置がまだ書かれていない(§4は「監査の後に書く」のまま)。
>
> 6. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run33.md` 末尾「リードの読みの案」 — E4を印・段3から未判別へ戻す判断(項目1)は起動文§2.1のE4規則に照らして妥当に見えるが、この結果8-035は設計票 `CAT8_DESIGN.md` §2「案B」の例外(浅いの行を残りに数えない条件)を使えなくなる。その根拠として挙げている「E4の見積もりの手が生ログに無いので案Bの記録も無い」は正しいが、これは34回目の起動が「E4だけ」を対象に案Bのフル手順(500行見積もり→未判別なら読んだ範囲・当たりの行数の記録)を新たに実施することを意味する。E4はこの回で一次資料に届いた(印段3として一度扱われた)経緯があるため、「未判別」として扱う場合の一次資料の再利用可否(この回で読んだ`structured-streaming-kafka-integration.md`の逐語をそのまま案Bの「読んだ範囲」に転用できるか、それとも0から探し尽くす必要があるか)を34回目の起動文に明記していないと、次回また同じ論点で判断が割れる。
>
> 7. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:45716`(E4の根拠欄)「28回目Apache Kafkaと同じ保守的な当て方で段3とした」 — 起動文冒頭 `..._run33_prompt.md:15` は「22回目はE4」の述語の対象に届かない根拠で`印`を付けた過去の誤りを名指しで警告しているにも関わらず、33回目もE4に`印・段3`を付け(その後リードが検収で`未判別`に訂正)、根拠欄の文言自体が「時刻順に再生することを明言する逐語はこの回では見つからず」と書きながら`印`を付ける、という自己矛盾した記述になっている。E4のみ、印を付けるかどうかの判断で述語の逐語要件(起動文§2.1「時刻順の再生を言う逐語が無ければ当たらない」)を調査班が正しく適用できていない状態が起動文での警告後も再発しており、他の要素(E1a-E3b, E5)でも同様の「述語に届かない印」が紛れていないか、リードが機械的な再点検(述語の語をキーに根拠欄を全数点検する等)を行ったかを確認したい。
>
> 参考として問題なしと確認できた点: 配布物サイズ(429〜555MB)・sha512照合・OSV脆弱性15件0件・pyspark-connectのpyspark依存・§6-1結論行と導入の順序(19:28:04→19:28:11)は、いずれも生ログと報告が一致していた。リードの§2の項目1(E4)・4(blocklines.txt発見)・5(cat8_step経由の逐語読み直し)・6・7は生ログと整合していた。
>
> 他 0 件省略(見つけた指摘は上記7件のみ)。

## 4. 処置(リード、監査 130 回目)

1. **止める → 確かめた・直した**。
   - リードの実測: `grep -c 'PIP_CACHE_DIR' run33.log` → 0、`grep -c 'TMPDIR=' run33.log` → 0。指摘のとおり、`pip` の手の 1 本にも付いていない。リードは検収で数えていなかった(リードの見落とし)。
   - 実害の有無(リードの実測): `find /root/.cache/pip -newermt '2026-09-26 19:00' | wc -l` → 0、`du -sb /root/.cache/pip` → 740868444(開始前と同じ 728M)。`find /root -xdev -newermt '2026-09-26T19:09:00' -not -path '/root/.claude/*' -type f` → 出力なし。この回が `/root` の下に作ったファイルは無い。なぜ書かれなかったか(pip がキャッシュをどこに置いたか)はリードは確かめていない(未確認)。
   - §2.5 のほかの手順(リードが生ログで見た): 最初の手の開始の時刻・`mkdir`(7〜12 行)、配布物の大きさの一覧、wheel だけの取得(689 行)、§6-1 の結論の行(2529 行)が導入(2534 行)より前、`--no-index --find-links`(2535 行)、最後の `find /tmp`・`find /root`(生ログの末尾)は守られた。守られなかったのは環境変数を前に付けることだけ。
   - なぜ起きたか: 調査班に聞いていない(未確認)。規則を書き足すだけでは 32・33 回目の 2 回続けて守られなかったので、**道具で付ける形に直した**: `scripts/cat8_step.py` に `--sandbox <道>` を足した。付けた手とそれ以後の同じ生ログの手には、道具が `TMPDIR`・`PIP_CACHE_DIR`・`XDG_CACHE_HOME`・`GRADLE_USER_HOME`・`MAVEN_OPTS`(`-Dmaven.repo.local`)・`JAVA_TOOL_OPTIONS`(`-XX:-UsePerfData -Djava.io.tmpdir`。JVM が `/tmp/hsperfdata_root` を作らないように)を付け、見出しの行に `[sandbox <道>]` を書く。リードが通る側・止まる側を試した(`--sandbox` なしの手では `TMPDIR=unset`、付けた手と次の手では scratchpad の下の道が出た、`java -version` のあとに `/tmp/hsperfdata_root` が無い)。検収では、見出しの行に `[sandbox` が無い手を数える。
   - この回の値: 実害が無いことを確かめたので、実測の値は受け取る(ほかの §2.5 の手順は守られた)。
2. **答える・直した**。段 4 の「対象のすべてを外から持ち込める」を言う逐語は、E2・E3b の根拠の欄に無い(指摘のとおり)。**E2・E3b を段 3 に下げる**。設計票 §4.1 の段 4 の条件は「その要素の対象のすべてを…外から持ち込んでも…掛けられる」で、逐語が無ければ上の段は付けない(段は下から積み上げ)。Spark が外のデータを読めることは、この回の生ログに逐語が無いので使わない。次の回で逐語が見つかれば上げる。
3. **答える**。候補の `pyspark-client` は PyPI に sdist しか無く(報告の知見 17)、wheel だけで取る決まりのとおりにはできなかった。調査班は sdist を取って §6-1 で開き(ハッシュの照合・`setup.py` を読んだ)、**作らずに**展開先を `PYTHONPATH` に入れて動かした。ソースから作る手は無い(`Building wheel` の出力なし)。§6-1 の趣旨(使う配布物の全部を、使う前に開く)は満たすと読む(リードの読み)。ただし、どの `pyspark` を読み込んだかを印字する手(`python -c "import pyspark; print(pyspark.__file__)"`)が無いので、次の回からは入れる。起動文には「wheel が無い配布物は sdist を開いて §6-1 に掛け、作らずに展開先を `PYTHONPATH` で読む。読んだ道を印字する」と書く。
4. **直す**。報告の知見 5 の「起動文§2.5(…既定の300MB/500MB制限)」の数値の出所は `scripts/cat8_repo_fetch.sh` の既定値で、起動文 §2.5 ではない。報告の節の「リードの追記」に訂正を書く(調査班の節は書き換えない)。
5. **直した**。`/tmp/blocklines.txt` を作った手は生ログに無い。調査班が `cat8_step.py` を通さずに殻の手を打ったと読む(原因は調査班に聞いていない = 未確認)。scratchpad の外に書いたのは 26・31・32・33 回目。最後の手の `find /tmp -maxdepth 1 -newermt` は、調査班が移したあとに打たれたので、この回は捉えていない。直し: `--sandbox` で `TMPDIR` は道具が付けるが、`> /tmp/…` と書く手は止められない。次の起動文で、**`cat8_step.py` を通さない殻の手を打たない**(読むだけの `Read` の道具は別)ことを名指しし、最後の手の `find` の出力に何か出たら、そのファイルを移さず消さずに報告に書くことを残す。
6. **答える・直す**。34 回目の起動文に書く: この回に読んだ `structured-streaming-kafka-integration.md` などは案 B の「読んだ範囲」に入れてよい(同じ一覧の中の頁)。そのうえで、文書の全頁の道と題に E4 の語の組を機械的に当てて選んだ頁の本文を全部読み、ソースの全ディレクトリにも当て、見積もりの手を打つ。
7. **答える**。リードが Spark の `印` の行を述語の語で読み直した: E1a(対象の限定なし)・E1b(対象の限定なし)・E2(「event time」= 時刻を持つデータ。対象の限定を満たすと読む)・E3b(「trades」「quotes」の時点の結合、述語の例「時点の揃った結合」)・E5(乱数の種)。述語の対象に届かない `印` は E4 だけ。

### 8-035 の扱い(台帳に入れる値)

- E1a 印・段 4 / E1b 印・段 2 / **E2 印・段 3** / E3a 未判別(案 B の記録あり)/ **E3b 印・段 3** / **E4 未判別**(案 B の記録なし)/ E5 印・段 2 / E6 未判別(案 B の記録あり)
- 状態 `浅い`。**「残り」に数える**(残り 5 のまま)。34 回目 = 8-035 の E4 だけ。

### リードの読みの案(監査に掛ける)

- 台帳: E1a 印・段 4 / E1b 印・段 2 / E2 印・段 4 / E3a 未判別(案 B の記録あり)/ E3b 印・段 4 / **E4 未判別**(案 B の記録なし)/ E5 印・段 2 / E6 未判別(案 B の記録あり)。
- E2 の対象: `numRowsDroppedByWatermark` の「Number of input rows dropped because their event time was older than the watermark」と文書の「automatically track the current event time」は、出来事の時刻を持つ流れ(時系列)について、遅れて届いた行(時刻のずれ・順序の乱れ)を自動で数えて報告する機能と読む。31 回目の Pandas の `duplicated`(汎用の表)と違い、対象が時刻を持つデータであることを言う逐語がある。
- **8-035 は「残り」に数える**(E4 に案 B の記録が無い)。34 回目 = 8-035 の E4 だけ(時刻順の再生の逐語を探し、無ければ見積もりの手と案 B の記録)。
