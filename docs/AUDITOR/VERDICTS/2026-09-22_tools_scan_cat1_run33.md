# 区分 1 の 33 回目 — リードの検収(2026-09-23)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 33 回目の実行」節(+311 / −0)/ 生ログ `docs/DATA/probes/20260922_tools_1_run33.log`
この回の検収は、リードが L-403 で書いた手順書 `.claude/skills/research-squad/SURVEY.md` §3 の順で行った。

## 0. Jev の検査(スキル §6 の 0)— **3 回目以降で初めて当てた。本物を 1 件捕まえた**

- 報告書全体(1 万行超)に当てたら、`verify`・`triage` は 300 秒で打ち切り、`jev_report_intake.py` は通信のタイムアウトで終わった。**当てる範囲はその回の節だけにすべきだった**(手順書に書く)。
- 33 回目の節(310 行)だけに当てた結果: **印 4 件**(`citation_corner_quote`)。
  - **3 件は起動の指定(リードの起動文)を引いた行**で、実物は会話の記録にある(Jev から起動文が見えないための印)。
  - **1 件は本物**: 「委任文 §4.0 は「深掘りした道具は、文章より先に表に 1 行ずつ書く」」(反する確率 0.970)。委任文 39 行の実物は「**深掘りした道具は、文章より先に下の表に 1 行ずつ書く。**」で、**「下の」が抜けていた。**
- **同じ定型の文が 14・15・18〜33 回目の節に写されており、欠けは 16 か所。**リードの抜き取りも受け入れ検査も 16 回見逃していた。**16 か所すべてを実物どおりに直した**(出典の逐語の誤り = 全体監査の [止める] 1 と同じ扱い)。直した後に数え直して、欠け 0 / 直した形 18(追記の説明文の 2 本を含む)。

## 1〜4. 機械

- 削除行 0(+311 / −0)。届いたままを `7a359d9` で記録した。
- 受け入れ検査: 生ログ 32 本で 13 本すべて 0 件。**貼られた塊と一致。**リードの訂正と追記を入れたあとも 0 件。
- 数え直し: **最後の記載が「判別に一次資料が要る」のまま(機械)= 0 件**(この回の始めは 8 件)。6 要素のどれかに印 84 件、段の表 41 行と印 41 件が一致。

## 5. 抜き取り(在処を生ログで先に確かめてから原典を打った)

| 主張 | リードが打ったもの | 結果 |
|---|---|---|
| 94 `mote/backtest` は足の高安の範囲に入れば埋まり、埋まらない注文は残る | `backtest.py`(25,059 バイト)`elif o.level >= bar.lo and o.level <= bar.hi: #welp` / `fills.append(o)`。注文は `self.active` を回して残る | **当たり** |
| 111 `Vibe-Trading` の滑りの既定 0.0005 | `crypto.py` 59 行 `self.slippage_rate: float = config.get("slippage", 0.0005)` | **当たり** |
| 111 の中国株の機関は値幅の制限で約定を止める | `china_a.py`(7,814 バイト)98 行 `def _blocked_by_limit(`、106 行「Whether a price-limit band blocks a fill on this bar.」 | **当たり → 段 0 ではない(下の問い 2)** |
| 118 は区分 1 の道具として手に取れる物が無い | 生ログ: sitemap と記事 1 本、逐語「Capital at risk -- demo environment only.」 | **当たり。射程を書かせる** |

## 7. 調査班の問い 7 件 — リードの回答

1. **45 のウェブ版アプリの文言を一次資料として認める。**製品自身の文言だから。**ただし射程を書く**(導入型と料金の頁には届いていない / 段は文言から読んだもの)。
2. **111 は段 0 ではない。機関ごとに書く**(27 回目の候補 57 と同じ扱い)。中国株の機関は `_blocked_by_limit` で相場の値によって約定を拒むので段 1、暗号資産の機関は段 0。どちらが既定の市場かは未確認。
3. **111 の `SKILL.md` など 91 本は安全側の通算に数える。**手引きの形かではなく、取り込んだ当方の作業者に指示を出しうるかで数える(27 回目に 97 を数えたのと同じ)。
4. **118 を「区分 1 の道具ではない」とした判断は妥当。ただし射程を書く**(読んだのは sitemap と記事 1 本、検証の機関は公開されていない)。宣伝の兆候は安全側の通算に載せる。
5. **117 を 2 頁で仕分けたのは可。**他の区分へ回すのは退けることではなく、そこでもう一度見るため。**射程を書く。**
6. **段の表で在処を指したこと → 台帳の道具の側で合わせた。**在処を指すだけのマスは読み飛ばし、前の表の実物を引き継ぐ。
7. **生ログの作者のメールアドレスを伏せた扱いは正しい。**

1〜5 の射程と、安全側の通算(112・111・118 を足して **11 件**)は、リードが 33 回目の節の末尾に「リードの検収による訂正と追記」として書いた(調査班の本文は、逐語の欠けと 111 の段の 2 か所の訂正のほかは触っていない)。

## 8. **リードが見つけた食い違い — 34 回目で当て直す**

台帳を作り直したところ、**板の待ち行列の印を持つ 19 件のうち 13 件(37・65・90・95・96・97・99〜104・109)が、`区分1-市場影響と約定の模型` の印も段の表の行も持っていない。**
31 回目にリードが決めた述語「**(a) 約定の可否・量・値段のどれかを決める仕組み**」に照らせば、列の模型(95 は原典自身が `queue-aware fill model`)は埋まりを決めているので、この印と段を持つはずである。
**述語を決めたあと、板の待ち行列の側に当て直さなかった。**リードがオーナーに出した段の表では、段 5 の例として 95・104・90 を挙げていたが、その 3 件は台帳の段の表に行が無い。
→ **34 回目で 13 件を述語で当て直す。**

## 判定

**受け取る。**未分類のまま 8 件が 0 件になった。**3 回目以降飛ばしていた Jev の検査を当てたら、16 回写されていた逐語の欠けが出た。**
