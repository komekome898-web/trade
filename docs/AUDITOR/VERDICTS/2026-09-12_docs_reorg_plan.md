# 監査記録: 文書整理の案(`docs/DISCUSSIONS/2026-09-12_docs_reorg_plan.md`)— 2026-09-12

監査役: 本番 `owner-auditor`。問い 12 件(止める 2 / 直す 7 / 聞く 3)。要旨と応答(全文は監査役の出力、番号は同じ)。
**総括**: 初版は対象ファイルを読まずに棚卸し表だけで書いており、監査役が事実誤認を 4 件止めた(1・2・4・6)。

1. [止める] D4 の削除理由が `OPERATIONS.md` §6.5 を「運用の実態」として引くが、§6.5 は「ログを Claude に見せる手順」で無関係(P10)。
> **直した。** 読んでいなかった。実装は `fetch_all.bat` の 3 スクリプトで、運用文書に説明は無い。D4 の残す情報を「`DATA.md` 冒頭の守る仕組み節」に変更。

2. [止める] F4 が P10(BitMEX 保全)を完了扱いで畳む対象にしているが、状態板は「期限 2026-09-23、進行中」(CLAUDE.md §7、P1)。
> **直した。** 状態板と突き合わせていなかった。F4 は「状態板が完了と言う手順だけ」= P8 §1 のみに縮小。P9 は完了の記載なし、P10 は進行中で対象外。

3. [直す] D9「スキルの方が新しく L-043/L-050 を反映済み」は棚卸しが「未確認」と書いた推定の採用ではないか(P10)。
> **確認した。** `PHASE2_SPEC.md` を grep: L-043・L-050・意図マップの語なし。スキルにはある。D9 に「確認済み(grep)」と明記。§10・§11 は原文で移す。

4. [直す] D10 は `PHASE2_TEMPLATES.md` §5〜§7(エッジ推移・条件分析・調達票と盲点監査)がスキルにも DELEGATION にも無い固有内容であることを見ていない(P5)。
> **直した。** §5〜§7 は全文を `research-protocol` に移す(削らない)。

5. [直す] D5 の `DATA_PLAN_2026-08-23.md` に相場の断定が残っている(§5.1、KA-10)。他の削除候補も確認したか。
> **確認した。** 3 行の断定を D5 に明記(全捨てで消すべきだった行)。他 10 本を同じ語で grep し、該当なし。

6. [直す] D11 は `NOTE_CRYPTO_CFD` の事実 5 件のうち 1 件しか残さない。全文を読んだか(P5)。
> **直した。** 読んでいなかった。事実 5 件を全部 `OPERATIONS.md` §1 へ移す。「戦略への影響」節は §5.1 で無効なので移さない。

7. [直す] 「25 本 → 13 本」の起点が実数 24 本と合わない(P5)。
> **直した。** 数え直して 24 → 12。

8. [直す] M4 の参照箇所は 2 箇所ではなく 3 箇所(tests も)。
> **M4 を取り下げた。** `preflight_prereg.py` が事前登録のパスから雛形を導出する設計なので、動かさない。

9. [直す] F1(索引の全面書き直し)を削除より先に置くと、削除予定の文書が索引に残る。
> **直した。** F1・F7 を最後に。

10. [聞く] D7・D8 の「移す」は行の単位(事象 vs 資産)が違う。書式は決まっているか。
> **決めた。** `DATA.md` に「損失・欠損(事象単位、追記専用)」「保持期間(資産単位)」の 2 節を新設し、列はそのまま移す。資産行には混ぜない。

11. [聞く] F7 の規則は「書くだけ」で、行動の直前に働かせる仕組みが無い(L-104 の型、P8)。
> **直した。** 既存の `Write` フック(表示のみ)を拡張し、`docs/` 直下に新規ファイルを作る瞬間に規則を表示する。

12. [聞く] 「約 1,000 行減」は移設分を差し引いた数か。
> **計算し直した。** 削除 941 + 畳み込み 60 − 移設 140 ≈ 正味 850 行減。

**結果**: 止める 0 件残、直す 7/7、聞く 3 件は成果物内で決めた。
