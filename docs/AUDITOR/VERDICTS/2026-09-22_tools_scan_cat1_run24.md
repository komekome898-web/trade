# 区分 1 の 24 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 24 回目の実行」節(364 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run24.log`(524 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲(機械)

`git diff --numstat` = **364 行追加 / 0 行削除**。**過去の回の節は 1 行も書き換わっていない。判定: 合。**

## 2. 受け入れ検査 — **合。そのうえで、調査班が見つけた K12 の穴を当方が実測で再現し、検査を直した**

当方が全 23 本の生ログを渡して打った結果は 13 本すべて 0 件。貼られた塊とも一致している。**この回は合。**

**調査班が自分で閉じずに渡してきた指摘**: 「K12 は最後の『受け入れ検査の出力』の節だけを見るので、**貼るのを忘れた回は前の回の貼り付けで通ってしまう**」。

**当方で実測した(24 回目の貼り付けを見出しごと落として確かめた)**:

```
K12 検査の出力の貼付           0 件          ← 通ってしまう
---- 合計 0 件
```

**指摘は当たっている。**23 回目の貼り付けが身代わりになっていた。

**直した**(`scripts/check_scan_report.py`。指紋の台帳に載っていないことを `grep -c` で確認済み。§5.0 が保護対象としているもの(フック・設定ファイル・git 側の関門・監査役の定義)のどれにも当たらない):
**貼り付けが「いちばん新しい回の節の中」に無ければ当たる**ようにした。**壊した入力で当たり、正しい入力で通ることを 3 通りで測った**:

| 入力 | K12 | 期待 |
|---|---|---|
| 貼り付けを**見出しごと**落とした(= 貼るのを忘れた回) | **1 件** | 当たるべき → 合 |
| 貼り付けの**塊だけ**落とした(見出しは在る) | **1 件** | 当たるべき → 合 |
| 本物 | **0 件** | 通るべき → 合 |

**これで「貼り忘れ」と「途中の出力を貼る」の両方が止まる。**
**調査班が自分で閉じずに渡したのが正しい。**自分で閉じていたら、この穴は残ったままだった。

**なお、この検収を書くとき保護パスの関門が 2 回作動した。**検収の本文に保護対象の場所の名前を書いたためで、**迂回はせず書き方を変えた。**
関門が名前の一致だけで見ているので、**その場所について書くこと自体が止まる**。これは限界として記録しておく(振る舞いは正しい。止まるべきときに止まっている)。

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| **候補 3 の登録が付け替えられている** | `ungh.cc/repos/robcarver17/pysystemtrade` が `"repo":"pst-group/pysystemtrade"` を返す。**付け替えは本物** | **当たり** |
| **候補 6 の登録が別名である** | `Ziplime/ziplime` は取れず、`Limex-com/ziplime` が 200 | **当たり** |
| **候補 3 が段 2(未約定を勘定に残す)** | `systems/accounts/order_simulator/fills_and_orders.py`(3,133 バイト)に `from sysobjects.fills import Fill, ListOfFills, empty_fill`・`list_of_fills = ListOfFills(list_of_fills)  ## will remove unfilled`・`return empty_fill(fill_datetime)` が 3 箇所 | **当たり** |
| **候補 4 `PyBroker` の滑りが既定では無効** | `src/pybroker/strategy.py`(150,482 バイト)702 行 `slippage_model: Optional[SlippageModel] = None,`。呼び出しは 904・913・926・939 行に在る | **当たり。呼んではいるが、既定では何も起きない** |

**当てずっぽうの取得は当方も 0 件**(今回は先に `ungh.cc` で既定枝を読んだ)。23 回目まで 3 回連続で破っていた手順を、この回は守れた。

## 4. **新しい型の格上げ — 「呼んではいるが既定では無効」**

調査班がこの回に 3 件見つけた(4 番 既定 `None` / 33 番 既定 `ZeroSlippageModel` / 2 番 既定 滑り 0)。
**23 回目に格上げした「持っているが呼ばれない」の隣の型で、実害は同じである。**

**段の表に「既定」の欄を足す**(調査班の問い 4 への回答も兼ねる):

> **段は 2 つ書く。段(機構) = 差し替えれば届く高さ。段(既定) = そのまま使ったときの高さ。**
> 候補 4・33・2 は**段(機構)が高くても段(既定)は 1** である。

理由: **当方が道具を選ぶときに効くのは段(既定)のほうである。**「滑りの模型を持っている」と書いてあっても、
既定が `None` なら、そのまま使った結果は当方の `engine.py`(段 1)と同じ楽観になる。**この区別を落とすと、区分 1 の比較が実務で役に立たない。**

これで機構と実行経路の食い違いは 3 つの形に整理された。**いずれも「持っている」と書いてあるのに効いていない**:
1. **機構はあるが実行経路が呼ばない**(107 `sigc`、15 `BacktestingCore`、20 `VnPy`)
2. **呼ぶが既定では無効**(4 `PyBroker`、33、2 `Backtrader`)
3. **当方の `engine.py`** — 呼んでいるが模型そのものが段 1(自ら「楽観的」と明記)

## 5. 調査班が挙げた 4 件の判断 — リードの回答

**(1) 段 4「量を見る」の中身 → 調査班の振り分けが正しい。定義を締める。**

> **段 4 は「約定の出来高を読んで、埋まる量を決める」ものに限る。**
> **定率・定額の滑りは段を上げない**(価格に一定の掛け目を当てているだけで、相場の量を読んでいない)。
> **確率で部分約定させるのも段を上げない**(量ではなく乱数を読んでいる)。

よって 7 番 `Superalgos` は段 1、20 番 `VnPy` は段 2 でよい。**注記に「定率の滑りと確率の部分約定を持つが出来高を読まない」と書くこと。**

**(2) 13 番と 123 番を束ねるか → 束ねない。番号で数えた 80 件を正とする。**
完了の形は**候補**について言われている(オーナー逐語「一次資料に到達した**候補**が尽きること」)。候補は登録の単位である。
**ただし数え直しの表に、機関で数えた値(79 件 / 78 件)を併記して、どちらで数えたかを明記する**のは続けること。**調査班の扱いで正しい。**

**(3) 段を 1 つしか書けない形 → 複数書ける形に変える。**
33 番は照合の機関(段 5)と平方根の影響(段 6)の**両方**を持ち、38 番は段 5 だが段 6 は無い。
**段は「何ができるか」ではなく「何を見て埋めるか」の軸なので、複数の軸に当たるなら全部書くのが正しい。**
1 つに丸めると、**33 番と 38 番の違い(影響の関数を持つか)が消える。**これは区分 1 が見たい違いそのものである。
**注記に回したものを段の欄に戻すこと。**

**(4) 既定で無効になる機構 → §4 のとおり。段(機構)/ 段(既定)の 2 列にする。**

## 6. 安全側の所見

この回に導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。
404 は 1 件(`Ziplime/ziplime`)で、**付け替え先を探し当てている。**TLS 切断 2 件はすべて打ち直して 200、404 の欄に入れていない。
**登録の付け替えを 2 件見つけたのは収穫である**(`robcarver17` → `pst-group`、`Ziplime` → `Limex-com`)。
古い URL のまま引用していると、後から「取れない」と誤診する型なので、**候補の一覧の該当箇所に付け替え先を併記すること**(25 回目)。

## 7. 判定

**受け取る。**検査 13 本が 0 件、書き換えは追記のみ、抜き取りは 4 件とも原典に当たった。
**調査班が自分で閉じずに渡した K12 の穴は本物で、当方が再現して検査を直し、壊した入力で当たることまで測った(§2)。**
**「呼んではいるが既定では無効」の発見は、区分 1 の比較を実務で使える形にする(§4)。差し戻す点は無い。**

## 8. 25 回目の起動の指定

1. **段の表を 2 列にする**(段(機構)/ 段(既定))。**複数の段に当たるものは全部書く**(§5(3)(4))。
   注記に回した 33 番の段 5、38 番などを段の欄に戻す。
2. **段 4 の定義を §5(1) のとおり締め、当たる候補を当て直す。**7 番は段 1、20 番は段 2 のまま、注記を付ける。
3. **登録の付け替え 2 件を候補の一覧に併記する**(`robcarver17/pysystemtrade` → `pst-group/pysystemtrade`(既定枝 `develop`)、`Ziplime/ziplime` → `Limex-com/ziplime`(既定枝 `master`))。
4. **K12 を直したこと(§2)を節に書く。**以後、貼り付けは**いちばん新しい回の節の中**に無いと止まる。
5. **数え直しを 3 段で出す。**機関で数えた値も併記する(§5(2))。
6. 上の 1〜5 に要る範囲でのみ既存の節への書き換えを許す。**追記だけで果たせるならそのほうがよい。**
7. **新しい検索計画は打たない。深掘りもしない。候補も増やさない。**
8. **残っているのは: 一次資料に未到達 11 件(全部 `区分1-足`)/ 印が無い 2 件(14・22 番)/ 11 番 `OctoBot` の 5 要素(未確認のまま残す)。**
   **11 件は一覧の 1 行の説明しか無いものなので、26 回目以降にまとめて当たる。**25 回目は上の整理を終わらせること。
