# 監査記録: 道具サーベイ 区分 8 の設計票と追補

対象: `docs/DATA/surveys/CAT8_DESIGN.md`(設計票)/ `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(もとの委任文 `20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)の区分 8 の追補)。
監査は調査班の 1 回目を起動する前(SURVEY.md §1・§8 の 1、KA-101、関門 `delegation_audit_gate.sh`)。
**この記録は監査 3 回目の指摘 4(1・2 回目の指摘の書面の記録が無い)を受けて書いた。**1・2 回目の出力は、届いたその返答の中で次の直しより先に会話へ逐語で出していたが、この記録は 3 回目のあとにまとめて書いている(O-4 の型に当たる。事実として残す)。

## 1 回目(2026-09-23、対象 = 設計票 093917f)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件・[聞く] 3 件):

> 検査対象: `docs/DATA/surveys/CAT8_DESIGN.md`(コミット 093917f、ブランチ `claude/tools-survey-cat8`)。照合した材料: `CAT8_HANDOFF.md`、`.claude/skills/research-squad/SURVEY.md`、`docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 ce0012c95154)、`docs/DATA/tools_catalog.tsv`、`scripts/check_scan_report.py`(`scope()`)、`docs/PROJECT_GOAL.md`。数値・件数・正規表現の主張(1.77 MB・116 行・最大 124・`^## 区分\s*\d`・区分1から回された5件)は実物のコマンド(`wc -l`・`awk`・`ls -la`)で1件ずつ照合し、すべて一致した(不一致なし)。
>
> 1. [止める] `CAT8_DESIGN.md:64` — 「調査班に台帳への `add`・`set` を許す(委任文 §6-5 の書き込み先に、起動文で台帳を足す)」とあるが、委任文 §6-5(見出し「6. 安全の規則」の 5 番目「**リポジトリを変えない**: 書いてよいのは `docs/DATA/SCAN_2026-09-21_tools.md` と `docs/DATA/probes/20260921_tools_<区分>.log` だけ。scratchpad 以外にファイルを作らない。」)を実際に読むと、書き込み先を**この 2 つだけに限定する禁止条項**であり、台帳(`tools_catalog_cat8.tsv`)や `cat8_ledger.py` はどこにも含まれていない。同じ設計票の別行(L26「§5-6」)では section-item 番号を正確に引けているので、これは誤読ではなく、禁止条項を「書き込み先の列挙」であるかのように読み替えている。SURVEY.md の委任の型は「委任文は既存のものを使う…委任文を変えるなら監査役を通して指紋を `VERDICTS/` に記録してから(委任の関門が止める)」と明記している。監査済み・指紋つきの委任文が明示的に禁じる書き込み先を、委任文自体を改める手続きを経ずに各回の起動文で実質的に上書きしてよいか(CLAUDE.md §0.2 A-16 の型、SURVEY.md §2)。
>
> 2. [止める] `CAT8_DESIGN.md:66` — 報告書の在処を委任文 §9 の指定(`SCAN_2026-09-21_tools.md`)から新設の `docs/DATA/SCAN_2026-09-23_tools_cat8.md` へ移す決定も、同じ §6-5 の書き込み先の限定(2 ファイルだけ)の外にある。1.77 MB の衝突回避という運用上の理由は実測(`ls -la`: 1,772,705 バイト、確認済み)に基づき妥当そうだが、これが「委任文の変更」に当たるかどうかの判断そのものが本文に書かれていない。1 と合わせて、委任の関門(指紋つき VERDICTS 記録)を経ずに書き込み範囲を広げる決定が 2 件重なっている。
>
> 3. [直す] `CAT8_DESIGN.md:22-27` — 状態語 7 つのうち「危険で導入停止」「登録が要る」「区分8の要素なし」の 3 つには `=` で明示的な述語があるが、「未着手」「浅い」「深掘り」「判別に一次資料が要る」の 4 つには設計票自身の述語が無い。「浅い」「深掘り」は委任文 §4.0 に間接的な定義があるが、「判別に一次資料が要る」は委任文にもこの設計票にも定義が無く、区分 1 の実務(`scripts/recount_scan_cat1.py`・`TOOLS_CATALOG.md`)から述語を明示せずに持ち込まれている(grep で確認: 区分1側では「未着手」と区別のつく用法があるが、区分8向けの述語としては書かれていない)。SURVEY.md §1 の教訓(規則は述語で書く。§7 の P1・P6)に照らし、この状態語の述語をこの票に足さなくてよいか(P3 型: 台帳の語彙の一部が未定義のまま運用に入る)。
>
> 4. [聞く] `CAT8_DESIGN.md:16` — 「誰の語か」の対応表は「この票の §2〜§8」を一括で「リードの決定」としているが、§9(1回目の範囲、L91-95)がこの表に含まれていない。§9 には候補一覧を全部 `add` する手順や区分1から回された5件の扱いなど、実行に直結する決定が書かれている。§9 の記載は誰の語として扱われているか(L-162 型の帰属漏れ)。
>
> 5. [聞く] `CAT8_DESIGN.md:44,46` — E1「『突き合わせの相手になりうる別実装である』だけでは付けない」、E3「防ぐだけの機能(purged 交差検証・embargo など)は検出ではないので E3 はなし」という述語の狭め方は、過大な該当付与(偽陽性)を防ぐ一方で、区分8の該当候補数を減らす方向に働く。委任文冒頭のオーナー逐語(L-378〜380「ちゃんと調べさせる気のない委任文出すなよ」「調査に甘えや抜け…を無くせ」)はこの種の絞り込みに向けられたものではないと言えるか。この狭め方が構造的な安全側(A-18)や族の実質的な縮小(A-10、値は捨てず `他の区分へ`/E6 に回すとはいえ)になっていない根拠は、狭め方の理由(区分1の116行の大半が当たりうる=推定、と明記)以外に本文にあるか。
>
> 6. [聞く] `CAT8_DESIGN.md:57` — 「最初は段を置かない(区分8で要る軸はまだ分からない)」という判断は、委任文 §8「無いもの」B(別実装の再現ゲートの許容誤差)・C(ルックアヘッド検出の自動化の程度)から軸をある程度見積もれる可能性を検討した記述を伴わない。SURVEY.md §1「要ると分かっているなら最初に。当方に無い軸を落とさない」(区分1では遅延・取り消しの扱いが目盛りから落ちて後で13件の当て直しが発生した教訓)との整合は、「まだ分からない」の一文だけで足りるか。
>
> 7. [直す] `CAT8_DESIGN.md:64` — 台帳へ調査班が直接 `add`/`set` する設計は、区分1が採った方式(`build_tools_catalog.py` が報告書から台帳を作り直す。番号は台帳が振るが、報告本文からの再構築)と異なる。区分1の教訓(SURVEY.md §7 の P5「番号体系」: 1・2回目の独自番号・重複4・落ち4)が、調査班が台帳を直接書き換える方式でも再発しないと確かめた記述が無い。
>
> 数・事実の主張については、ファイルサイズ(1,772,705 バイト = 1.77 MB)・台帳の行数(116、`awk -F"\t" 'NR>1' docs/DATA/tools_catalog.tsv | wc -l`)・番号の最大値(124)・区分1から回された5件(62 qf-lib/70 PineForge/92 prediction-market-backtester/101 akurkar07/OrderBook/117 Exegy、いずれも「他の区分へ」列に「8」を含む)・`check_scan_report.py` の `scope()` 正規表現(`^## 区分\s*\d`、実際の見出し形式 `## 区分1 — N 回目の実行(YYYY-MM-DD)` と整合)を実物で確認し、不一致は見つからなかった。`cat8_ledger.py`・`tools_catalog_cat8.tsv`・`SCAN_2026-09-23_tools_cat8.md` はいずれも未作成で「新設」の記載と整合する。ブランチも `claude/tools-survey-cat8` で一致。

**リードの処置(コミット a83cd6a)**:
1・7: 調査班に台帳を書かせない。報告では新しい候補を `新` と書き、リードが検収で `cat8_ledger.py sync` で番号を振る。P5 の 3 つの型(独自の番号・重複・落ち)を何が塞ぐかを設計票 §5 に書いた。
2: 報告書の在処の変更は委任文の変更に当たると設計票 §5 に明記した。もとの委任文は変えず、追補(新設)に書き込み先を置き、監査に通して指紋を記録してから引く。
3: 状態の 7 語全部の定義を設計票 §2 に書いた。
4: 設計票 §1 の表を「§2〜§9 と追補」にした。
5: 狭めずに E1a(突き合わせる機能)/ E1b(突き合わせの相手)、E3a(検出する)/ E3b(防ぐ)に分け、どちらか一方でも区分 8 の要素に当たるとした。
6: 段は置かないが、分かっている軸(委任文 §8 の B・C、区分 1 の遅延の教訓など)を 1 回目から原文で記録させる表を設計票 §4 に置いた。

## 2 回目(2026-09-23、対象 = 設計票と追補 a83cd6a、追補の指紋 `3d29bbc7524f`)

監査役の出力(逐語。[直す] 2 件・[聞く] 3 件):

> 1. [直す] docs/DATA/surveys/CAT8_DESIGN.md:31 — `区分8の要素なし`の述語が「§3 の**6 要素**のどれにも当たらない」と書かれているが、§3 自体は監査1回目の指摘5を受けて E1/E3 を a/b に割り、台帳の列は**8 本**(E1a〜E6)と明記済み(44行目・57行目「E1a〜E6 がすべて `なし`」)。同じ判定基準を指す語が、文書内で「6 要素」と「8 列(E1a〜E6)」の2通りの表記のまま残っている。意味は両立しうる(6 概念×一部2列split=8列)としても、旧い言い回しが分割後に更新されずに残っているのではないか。§2 のこの行だけ §3 の書き換えに合わせて直っていないのではないか(P2)。
>
> 2. [聞く] docs/DATA/surveys/CAT8_DESIGN.md:59-60 — 監査1回目の指摘5(「相手になりうるだけでは付けない」「防ぐだけでは付けない」は族を狭める型)への対処として、E1b(相手になりうるだけ)・E3b(防ぐだけ)も単独で「区分8の要素に当たる」に変えた。その結果、設計文自身が「E1b は区分1の台帳の候補(バックテスト系)の**多くに当たりうる**(推定。数えていない)」と認めている。族を広げすぎて `区分8の要素なし` が事実上ほとんど成立しない基準になっていないか、この広さで区分8の分類が意味を持つと確認済みか(狭める方向の誤りを正した結果、広げる方向に振れすぎていないか)。
>
> 3. [直す] docs/DATA/surveys/CAT8_DESIGN.md:83 — `sync` の重複判定の規則が「名前と URL の正規形(**GitHub は**所有者/登録名の小文字)」とだけ書かれ、GitHub 以外の出典(PyPI・公式サイト・一覧の辿り・X 経由など、委任文§3 で認めている発見経路)の正規形の規則が書かれていない。区分1の P5 型(重複・落ち)をこの作りで塞ぐと述べているが、正規化ルールが及ばない出典では `sync` の重複検出・`check` の「落ち」検査が機能するか書かれていない。
>
> 4. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:11 と docs/DATA/surveys/CAT8_DESIGN.md:108 — 追補§1「区分8で書いてよいのは次の2つだけ(報告書・生ログ)」と、設計票§8「書くのは区分8の資料(報告・生ログ・**台帳・索引・検収の記録・cat8_ledger.py・起動文・SURVEY.mdへの追記**)と OWNER_LOG・OWNER_STATUS」は、前者が調査班向け・後者がリード(セッション全体)向けという文脈上の違いで両立していると読めるが、その適用範囲の違い(誰が書いてよい2つだけなのか)はどちらの文書にも明記されていない。追補は「調査班は起動の最初に…この追補…を読む」ものであり、追補単体を読んだ調査班に「書いてよいのは2つだけ」の主語がリード全体でなく自分(調査班)だけだと明確に伝わるか。
>
> 5. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:9 — 見出しは「委任文 §5-5・§6-5・§9 を**置き換える**」と書きつつ、実際に書き換えているのは各節の「書き込み先」部分だけで、§9 の必須小節6つなど他の規定は追補冒頭の一般条項「ここに書いていないことは、すべてもとの委任文のとおり」に依拠して存続する(このことは設計票§5(85行目)にしか明記されておらず、追補自体には「§9の他の規定は存続する」との明記がない)。「§9 を置き換える」という強い見出しと、実際は部分置換であることの整合は、追補単体を読む調査班にも明らかか。
>
> 上記 5 件のほか、以下は確認した限り整合していた(問題なし):
> - 1回目の監査指摘 1・2・3・4・5・6・7 はいずれも該当箇所に実際の修正が反映されている(§2 の7語すべてに述語、§3 の E1a/E1b・E3a/E3b 分割、§4 の記録軸表、§5 の書き込み先変更の明記と追補化、§1 の帰属表拡張、§5 の番号重複/落ち対策の説明)。
> - 追補の指紋 `3d29bbc7524f`(自己申告)は実ファイルの sha256 先頭12桁と一致。もとの委任文の指紋 `ce0012c95154` も実ファイルと一致。ただし `docs/AUDITOR/VERDICTS/` にはこの追補・設計票2回目の監査記録がまだ無い(今回の監査で記録される想定と見られる。実行前なので未着手そのものは問題ではない)。
> - 台帳の初期5件(62 qf-lib / 70 PineForge / 92 prediction-market-backtester / 101 akurkar07/OrderBook / 117 Exegy)と「最大124、116行」の数値は、実際の `docs/DATA/tools_catalog.tsv` を実測して照合済み、一致していた。
> - 追補が置き換える §5-5・§6-5・§7・§9・§12コマンドと、置き換えていない §6-1〜§6-4・§6-6・§10(危険検査・外部送信禁止・鍵登録禁止など安全規則)を突き合わせたが、安全規則を緩めている箇所は見当たらなかった。
> - 引き継ぎ書 `CAT8_HANDOFF.md` §1(オーナー逐語)と §5(競合)は、§1 がオーナー逐語のみに限定され、§5 の運用細目(ブランチ限定・L-500からの採番・OWNER_STATUS欄1つ)はいずれも「リードの決定」と自己申告されており(§5 冒頭は §1 の指示そのものではないと明記)、逐語をリードの決定にすり替えている箇所は見当たらなかった。

**リードの処置(コミット c3defdb)**:
1: 「8 列(E1a〜E6)がすべて `なし`」に直した。残る「6 要素」は設計票 44 行目(区分 8 の概念の数)と 83 行目(区分 1 の要素)で、意味が別。
2: 設計票 §3 に「広げすぎの側」の段落(8 列を束ねずに持つ / `区分8の要素なし` は回し先の記録用 / 広さは 1 回目の検収で列ごとの数を出して確かめ、確かめるまで「意味を持つ」と書かない)。
3: 名前・GitHub・PyPI・X・その他の URL の正規形と、どちらか一方で当たれば止まること、機械で拾えない重複を限界として書いた。
4: 追補 §1 の主語を「調査班が」とし、台帳などはリードが書くと添えた。
5: 見出しを「書き込み先の部分だけを置き換える」にし、§9 の他の規定と §5-5 の 1 手ずつ残す規則はそのままと明記した。

## 3 回目(2026-09-23、対象 = c3defdb、追補の指紋 `756c78acf64b`)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 3 件):

> 1. [止める] docs/AUDITOR/TRACE/2026-09-23_2da9385f.json rollup.P2_table_shown_before_first_write (num=0, den=2, value=0.0) — この commit c3defdb 自体に含まれる機械記録では、`first_write_at` より前に §0.1 の表(検出文字列「オーナーの原文の該当語」)が出た回数が 2 手中 0 である。監査 2 回目の指摘を受けたこの改訂(区分 8 の設計票・追補の書き換え)は、着手前に §0.1 の「やろうとすること / オーナーの原文の該当語」の 2 列表を出したか。出していないなら、それは承認済み常設スキル(research-squad)の固定手順としての例外に当たるのか、それとも§0.1 が要求する擦り合わせを経ずに着手したか(CLAUDE.md §0.1、KA-100 型)。
>
> 2. [聞く] 同ファイル rollup.P4_audit_pasted_immediately(num=0, den=2, value=0.0) — 監査呼び出し(`audit_calls`)の直後の手が ACTION_LOG への書き込みでない回が 2 手とも該当する。今回の改訂サイクルで、監査役の指摘をその場で逐語に出さずに別の手を先に行っていないか(§0.2 O-4「監査役の指摘を、その場で逐語に出さず後でまとめる」)。VERDICTS への記録など ACTION_LOG 以外の経路で即時に出したのであれば、その記録はどこにあるか。
>
> 3. [直す] docs/DATA/surveys/CAT8_DESIGN.md 38行目 — 「1 回の実行」の完了述語が「委任文 §11 の『1 回の実行の完了』のとおり」を引いているが、§11 の原文(`20260922_tools_survey_prompt.md`)は成果物が `SCAN_2026-09-21_tools.md` に揃うことを条件にしている。今回の追補(監査 2 回目の指摘 5 への対応)は §1 見出しを「§5-5・§6-5・§9 の書き込み先の部分だけを置き換える」に狭めており、§11 は書き換え対象の列挙(§5-5・§6-5・§9)に入っていない。区分 8 は新設の `SCAN_2026-09-23_tools_cat8.md` に書く設計なのに、完了述語の文言上は旧ファイル(区分1の報告)が揃うことを要求したままではないか。
>
> 4. [止める] docs/AUDITOR/VERDICTS/ に区分 8(CAT8_DESIGN / 追補)に対応する記録ファイルが 1 件も無い(ls で確認: 存在するのは `2026-09-21_tools_survey_prompt.md`・`2026-09-21/22_tools_scan_cat1_*` のみ)。CAT8_DESIGN.md と追補の本文は複数箇所で「監査 1 回目の指摘 1・2・5・6・7」「監査 2 回目の指摘 2」を根拠として引用している(例: 44・84・86行目、addendum 7行目)が、その指摘の書面記録がリポジトリに見当たらない。`.claude/hooks/delegation_audit_gate.sh` はこの追補の指紋が `VERDICTS/` の記録に無ければ Agent/SendMessage による区分 8 の委任を止める作りになっている。これらの監査 1・2 回目の指摘は `docs/AUDITOR/VERDICTS/` に書面として記録されているか、それとも会話上でのみ存在し、まとめてこの設計票に反映されただけか(§0.2 O-4、KA-101 の検査の型)。
>
> 5. [聞く] docs/DATA/surveys/CAT8_DESIGN.md 84行目(番号表、監査 2 回目の指摘 3 への対応) — 名前の正規形は「NFKC・小文字・空白と `-` `_` `.` を除く」という広い畳み込みで、「名前か URL のどちらか一方でも当たれば」重複と判定して `sync` を止める設計にした。監査 2 回目の指摘は正規形の**不足**(GitHub 以外に無い = 見落とし方向)を問うものだったが、今回の直しは名前側の畳み込みを広げ「一方でも当たれば」に強めたことで、逆方向(短い・一般的な名前を持つ別々の道具が名前だけ一致して誤って重複扱いされる = 過剰一致)の危険が新たに生じていないか。検討済みなら根拠を書く必要はないか。
>
> 6. [聞く] docs/DATA/surveys/CAT8_DESIGN.md 61行目(「広げすぎの側」、監査 2 回目の指摘 2 への応答) — 「広さが実際にどうだったかは 1 回目の検収で列ごとの数を出して確かめる(確かめるまで『意味を持つ』とは書かない)」とあるが、確かめた結果 E1b・E3b が想定より広すぎたと分かった場合にどう是正するか(述語の再定義か、`区分8の要素なし`・要素認定の基準の変更か)の基準がこの票に書かれていない。これは §3 の「述語を足したり変えたりしたら全行に当て直す」で自動的にカバーされる想定か、それとも別途この票への追記が要るか。
>
> 以上6件。1・2 は commit c3defdb 自体が含む機械記録(TRACE)から直接読める事実で、今回の改訂プロセスそのものに関する指摘。3・5・6 は今回の直し(監査2回目の指摘3・2・5への対応)が新たに書いた文言の内部整合性についての指摘。4 は今回に限らず、区分8のこれまでの監査サイクル全体について記録の所在を問う指摘。
>
> 監査 2 回目の指摘 1・4 に対応する直し(`区分8の要素なし`の8列表記、追補の主語明記)は本文中で一貫しており、旧い定義の残存(削除漏れ)は見当たらなかった(P2 の型としては解消)。diff の削除行はすべて同義の書き換えで、内容が消えた箇所は無かった。

**リードの処置**:
1: **指摘のとおり、§0.1 の対応表を書き込みの前に一度も出していなかった**(起動の指示が「最初の一手は対応表を出す」と言っていたのに、設計票を先に書いた)。3 回目の出力を出した返答で対応表を出した。空の行は 2 つ(テーマの 8 区分 = リードの枠組みで L-381 で承認済み / 設計票の中身 = SURVEY.md §1 の固定手順として L-179 の例外で作り、起動の前にオーナーに見せる)。
2: 1・2 回目の出力は、それぞれ届いた返答の中で、次の直しより先に会話へ逐語で出していた。記録(`VERDICTS/`)には書いていなかった → この文書。
3: 追補 §1 の見出しに §11 を足し、§11 の報告の在処の読み替えを書いた。設計票 §2 の「1 回の実行」の行にも読み替えを書いた。**あわせて追補に §7(完了の形)を新設した**(下の Jev の前段の印への処置。この処置の記述から漏れていたことを 4 回目の指摘 2 で問われ、追記した)。
4: この文書。
5: `sync` は止まるだけでまとめない。名前だけが当たった別の道具は、リードが両方の出典を開いて別物と確かめ、`sync --new` で足し、理由を検収の記録に書く。過剰一致の代償は 1 回の目視で、見落とし(区分 1 の P5)より軽いので当たる側に倒す、と設計票 §5 に書いた。
6: 広すぎても列を消したり E1b・E3b を外したりはしない(外すと 1 回目の指摘 5 の狭める型に戻る)。直すなら狭い集合を引く列を**足す**。足すのは述語の変更なので、全行に当て直し、監査役に通し、状態板に書いてオーナーに見せる、と設計票 §3 に書いた。

**Jev の前段**(delegated-study §1.5、`jev_delegate.py plan --prompt <追補>`): 印 1 件(`not_bounded`、is_bounded 0.59)→ 追補に §7(完了の形)を足して打ち直し、印 1 件のまま(is_bounded 0.63)。**閉じない。**追補は単独の仕事ではなく、1 回ずつの起動文と組で使う。完了の形は起動文の側に書き、毎回の起動文にも同じ前段を当てる。

**3 回目の処置 1 の裏付け(4 回目の指摘 3・4 を受けて追記)**: 会話に出した対応表の全文(逐語):

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 区分 8 の調査を、いまの Workflow と並行して始める | 「**区分8を今のワークフローと並行して始めておきたい**」(L-416) |
| 調査のあと、索引と機械で引ける台帳を作る(資源化) | 「**調査して資源化まで進めてほしい**」(L-416) |
| 区分 1 の手順書(`SURVEY.md`)の順に進める(設計票・台帳を先に作る・数え直しの道具・毎回の Jev・監査役は中身の回と締め) | 「**区分1の教訓を活かして**」(L-416) |
| 1 回ずつ起動し、毎回検収する。1 回で区分を終わらせない | 「**一気に終わらせようとせずに手順はきちんと守ること**」(L-416) |
| 別のブランチにだけ押し出す。区分 8 の資料だけを書く。`OWNER_LOG` は L-500 から振る | 「**今のワークフローとの競合に注意すること**」(L-416)/「**案1の別の会話で調査を進めたい**」(L-417) |
| 区分 8 のテーマ = 検証・再現・品質(4 語) | **(該当語なし)** 委任文 §2 の文(リードの枠組み)。L-381「**承認します。進めてください**」で承認済み |
| 設計票の中身(完了の形・状態の語・E1a〜E6 の述語・記録する軸・台帳の形・報告の在処・追補) | **(該当語なし)** 承認済みの手順書 `SURVEY.md` §1 の固定手順として、リードが作る(L-179 の例外)。**中身はリードの決定なので、起動の前にお見せして、そこで変えられるようにする** |

## 4 回目(2026-09-23、対象 = 92733b8、追補の指紋 `b44350ddedd1`)

監査役の出力(逐語。[止める] 1 件・[直す] 2 件・[聞く] 4 件):

> 監査 4 回目の結果。対象: (A) `docs/DATA/surveys/CAT8_DESIGN.md`、(B) `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`、(C) `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`(コミット 92733b8)。(C) の 1〜3 回目の引用の行番号は、各対象コミット(093917f / a83cd6a / c3defdb)の実物と 1 件ずつ照合し、すべて一致した(不一致なし)。以下は問いのみ。
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:50-53` と `docs/DATA/surveys/CAT8_DESIGN.md:38` — 「1 回の実行の完了」の述語が 2 箇所に別々に定義され、互いを参照していない。追補 §7 は「(委任文 §11 の…)に加えて」§4 の 8 列と状態記入・候補一覧の `8-NNN`/`新` の区別を要求するが、設計票 §2 の同じ行は「§11 のとおり + 台帳の検査(`cat8_ledger.py check`)が 0 件」を要求し、追補 §7 の条件に触れていない。逆に追補 §7 は台帳検査の条件に触れていない。追補冒頭(5 行目)は「完了の形…の本体は設計票にある」と述べているのに、追補自身がこの回(92733b8)で競合する完了述語を新設している。どちらが正で、両方を満たす必要があるのか、本文のどこにも明記されていない(P2, P9)。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md:88` — 3 回目指摘 3 への「処置」の記述(「追補 §1 の見出しに §11 を足し…設計票 §2 の行にも読み替えを書いた」)は、実際にこの回で追補に新設された §7(完了の形、8 行分)に一切触れていない。処置の記述が実際の変更点(diff)を網羅していないのではないか(§0.2 O-4 型。findings 1 と同型の遺漏)。
>
> 3. [聞く] `docs/AUDITOR/TRACE/2026-09-23_2da9385f.json` move 3 / VERDICTS…md:86(処置 1) — move 3 の `tools` に Write/Edit が無く(Read 2・Bash 3・Agent 1)、`first_write_at` は null。`scripts/trace_metrics.py` の `rollup()` は `wrote = [m for m in mv if m["first_write_at"] is not None]` で `P2_table_shown_before_first_write` の分母を作るため、この回は分母(92733b8 のロールアップも den=2 のまま)から機械的に除外されている。処置 1「3 回目の出力を出した返答で対応表を出した」という自己申告は、その回自身の TRACE 記録では確認も反証もできない(この指標が Bash 経由の書き込みを「書き込み」と見なさないため)。CLAUDE.md(2026-09-19)はフックの対象を Bash 書き込みにも広げたが、`trace_metrics.py` の P2 計算は Write/Edit ツールだけを対象にしたままではないか。この自己申告を裏付ける逐語(対応表の実物)はリポジトリのどこにあるか(O-3 型)。
>
> 4. [聞く] `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md:86` — 処置 1 で「対応表を会話に出した」結果が「空の行は 2 つ」という要約のみ記録され、対応表そのもの(全行・両列)が逐語でリポジトリに残っていない。この文書自身が冒頭(5 行目)で 1・2 回目の監査役出力を「会話でのみ出し、記録には書いていなかった」ことを O-4 型と自認しているが、今回の対応表についても同じ構造(会話にのみ存在・記録は要約止まり)になっていないか。この扱いで足りるか。
>
> 5. [聞く] `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md:93`(Jev の前段) — `not_bounded` の印を「閉じない」まま、完了の形は各回の起動文の側に書くとしている。CAT8_DESIGN.md / 追補が研究プロトコルの意味での事前登録(prereg)に当たるかが本文に明記されておらず、位置づけ次第でこの印を閉じずに進めてよいかの基準(閉じる条件・閉じないまま重ねてよい回数の上限)が変わるのではないか。
>
> 6. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:59-61` — 「E1b は区分 1 の台帳の候補(バックテスト系)の多くに当たりうる(推定。数えていない)」という広さの懸念は監査 2 回目(指摘 2)以降、3・4 回目(本件)まで未確認のまま設計が確定し、1 回目の起動に入ろうとしている。「広さが実際にどうだったかは 1 回目の検収で確かめる」とするが、確かめる主体・確かめられなかった場合の扱い(誰が / いつ / どの基準で「意味を持たない」と判定し直すか)がこの票のどこにも書かれていない。
>
> 7. [直す] `docs/DATA/surveys/CAT8_DESIGN.md:84` — 監査 3 回目指摘 5(「一方でも当たれば」重複と判定する強い畳み込みが過剰一致を招かないか)への処置は「過剰一致の代償は 1 回の目視で、見落としより軽いので当たる側に倒す」という判断のみで、代償の見積もり(短い一般名を持つ道具が候補群にどれだけの割合で出現しうるか)の根拠が書かれていない。指摘 5 は「検討済みなら根拠を書く必要はないか」と問うており、処置はこの「根拠」を示さないまま判断だけを記録している。
>
> 参照した実物: `docs/DATA/surveys/CAT8_DESIGN.md`(092733b8 時点)、`docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(同)、`docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`、`docs/AUDITOR/TRACE/2026-09-23_2da9385f.json`(`git diff c3defdb 92733b8` で確認)、`scripts/jev_delegate.py`(`is_bounded` 述語の定義箇所)、`scripts/trace_metrics.py`(`table_shown` / `first_write_at` / `audit_pasted_immediately` の実装)、`.claude/hooks/delegation_audit_gate.sh`、各対象コミット(093917f / a83cd6a / c3defdb)の該当ファイル。

**リードの処置**:
1: 設計票 §2 の「1 回の実行」の行を**唯一の定義**とし、(a) 委任文 §11(読み替えつき)/ (b) 候補の一覧の全行が `8-NNN` か `新` で、全行に 8 列と状態 / (c) リードが検収で打つ台帳の検査 0 件、の 3 つにした。追補 §7 は「設計票 §2 の (a)(b) を調査班の側から引いたもの。(c) はリードが打つ」と書き換えた。
2: 3 回目の処置 3 に、追補 §7 の新設を追記した。
3・4: 対応表の全文を上に逐語で記録した。`trace_metrics.py` が Bash 経由の書き込みを `first_write_at` に数えないことは、監査役の読みのとおりで、リードは確かめていない(未確認)。**この道具は監査役の定義の入力なので、区分 8 の作業では変えない**。オーナーへの報告に、監査役の指摘として書く。
5: 設計票と追補は研究プロトコルの意味の事前登録ではない(戦略の当否を測らない調査で、封印も判定区間も無い。委任文 §0「判定(使える / 使えない…)は書かない」)。印を閉じる条件: **毎回の起動文に `jev_delegate.py plan` を当て、起動文の側で `not_bounded` が出なくなるまで起動文を直す**。起動文で出たまま起動するときは、その理由を毎回の検収の記録に書く。追補そのものの印は、追補を書き換えるたびに打ち直して記録する。
6: 設計票 §3 に書いた: 確かめるのは**リードが毎回の検収で**、`recount` の列ごとの `印` の数と E1a・E3a だけで引いた集合の番号を検収の記録に書く。「意味を持つ」はどの回でも書かない。広すぎても列は消さず、狭い集合を引く列を足す(足したら全行に当て直し、監査、オーナーに見せる)。
7: 区分 1 の台帳 116 行で測った(コマンドと出力は下)。名前を全体の正規形で比べると衝突は 1 組(5 番と 22 番の `Lean CLI`)。登録名だけに切ると 5 組(うち 4 組は所有者の違う別の道具)。よって名前は全体で比べ、登録名だけに切らない、と設計票 §5 に書いた。区分 8 の候補で同じ割合になるかは測っていない(推定の根拠は区分 1 の 116 行だけ)。

```
$ python3 - (docs/DATA/tools_catalog.tsv の 2 列目を NFKC・小文字・空白と - _ . を除いて比べる。collide_lastseg は `/` の後ろだけで比べたもの)
names 116 distinct_norm 115
collide leancli [('5', 'Lean CLI'), ('22', 'Lean CLI')]
collide_lastseg leancli [('5', 'Lean CLI'), ('22', 'Lean CLI')]
collide_lastseg zenbot [('13', 'DeviaVir/zenbot'), ('123', 'carlos8f/zenbot')]
collide_lastseg awesomesystematictrading [('39', 'paperswithbacktest/awesome-systematic-trading'), ('110', 'wangzhe3224/awesome-systematic-trading')]
collide_lastseg limitorderbookmatchingengine [('96', 'sashankzade/limit-order-book-matching-engine'), ('104', 'jxm35/LimitOrderBook-MatchingEngine')]
collide_lastseg limitorderbooksimulator [('97', 'kahan15/Limit-Order-Book-Simulator'), ('102', '3yit/Limit-Order-Book-Simulator')]
```

**Jev の前段(4 回目の処置のあと、追補の指紋 `6a4c53ab3855`)**: `jev_delegate.py plan --prompt <追補> --summary` → 印 1 件(`not_bounded`、is_bounded 0.62)。処置 5 のとおり閉じない。

## 5 回目(2026-09-23、対象 = 7c0cc85、追補の指紋 `6a4c53ab3855`)

監査役の出力(逐語。[止める] 1 件・[聞く] 2 件):

> 監査 5 回目。対象: (A) `docs/DATA/surveys/CAT8_DESIGN.md`(コミット 7c0cc85)/ (B) `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `6a4c53ab3855`、実測で再確認して一致)/ (C) `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`。`git diff 92733b8 7c0cc85` の削除行は全て「旧文の上位互換への書き換え」で、内容が消えた箇所は見つからなかった(4 回目の指摘 1〜7 の直しはいずれも該当箇所に反映を確認)。処置 7 の名前衝突の測定は、実際に `docs/DATA/tools_catalog.tsv`(116 行、最大番号 124)に対して同じ正規化手順で打ち直し、(C) の出力(`names 116 distinct_norm 115`、`Lean CLI` の全体衝突 1 組、登録名だけに切った場合の衝突 5 組 = zenbot・awesome-systematic-trading・limit-order-book-matching-engine・limit-order-book-simulator・Lean CLI)と 1 件ずつ一致した。
>
> 以下、問いのみ。
>
> 1. [止める] `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md:148`(4 回目の Jev の前段の記載)、および `.claude/hooks/delegation_audit_gate.sh` — 委任の関門は、Agent/SendMessage のプロンプトに引用された `docs/DATA/delegations/<name>.md` について `os.path.basename(rel) + "@" + sha256[:12]` という**連結した 1 文字列**を `docs/AUDITOR/VERDICTS/*.md` 全文から探し、見つからなければ拒否する実装になっている(`grep -rln "20260923_tools_survey_cat8_addendum\.md@" docs/AUDITOR/VERDICTS/` で確認、および gate 本体のロジックをそのまま Python で再実行して再現)。実際に計算し直すと印は `20260923_tools_survey_cat8_addendum.md` + `@6a4c53ab3855`(リード注: 原文は連結した 1 語。この記録ではそれを印と誤読されないよう分けて写した)だが、この文字列は `VERDICTS/` のどのファイルにも存在しない。1〜4 回目のどの回の記録も「追補の指紋 `<hash>`」という言い回しのみで、ファイル名と `@` を連結した逐語の印は一度も書かれていない。対照的に、もとの委任文 `20260922_tools_survey_prompt.md` は多数の `VERDICTS/` ファイルにこの連結形式で書かれており、実際にこの形式で関門を通っている実績がある(`2026-09-21_tools_survey_prompt.md:146`、`2026-09-22_tools_survey_prompt_v12.md:198` ほか多数)。SURVEY.md §2「委任文を変えるなら監査役を通して指紋を `VERDICTS/` に記録してから(委任の関門が止める)」と、SURVEY.md §2 の起動文の型「委任文を最初に全文読め(指紋つき)」から、1 回目の起動文は追補のパスを引用するはずである。そのとき `delegation_audit_gate.sh` はこの追補を拒否しないか。監査の中身(4 回分)はすべて実施されているのに、記録の**形式**が関門の実装と噛み合っておらず、起動そのものが機械的に止まる可能性が高いという理解で正しいか。もし別の記録(私が見ていない場所)に正しい形式の印があるなら、その在処はどこか。
>
> 2. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:61` — 「広さ(E1b・E3b が区分 1 の候補の多くに当たりうる)は、確かめるまで『意味を持つ』と書かない」とし、確かめる主体・頻度(リードが毎回の検収で)は明記されたが、確かめた結果「広すぎて意味を持たない」と**判定し直す**基準(何回分のデータで・どの数値の閾値で)は書かれていない。対処(列を足す)は「広すぎたと分かっても」の後に書かれているが、「分かった」をどう判定するかの基準がこの票のどこにも無い。これは 4 回目の指摘 6 が問うた点の一部(確かめる主体は明記されたが、判定基準はまだ無い)ではないか。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`(処置 5、4 回目の指摘 5 への応答) — 「設計票と追補は研究プロトコルの意味の事前登録ではない」という自己分類の根拠(戦略の当否を測らない・封印や判定区間が無い・委任文 §0「判定は書かない」)は本文と実際に照合でき、一致していた。この自己分類自体について、今回(5 回目)以前に監査役の検査を経た記録は見当たらない(4 回目は「聞く」として問うただけで、リードの応答に対する監査の可否判断はまだ無い)。この自己分類は監査役として妥当と扱ってよいか、それとも追加の確認が要るか。
>
> 上記のほか、以下は実測で照合し、問題は見つからなかった: 4 回目の処置 1〜7 はいずれも (A)(B) の実物に反映されている(「1 回の実行」の定義が §2 の (a)(b)(c) に一本化され、追補 §7 が「(a)(b) を調査班の側から引いたもの、(c) はリードが打つ」と明記して重複定義を解消している)。処置 7 の測定は独立に打ち直して一致した。diff の削除行はすべて内容を保持したままの書き換えで、消えた文は無かった。

**リードの処置**:
1: 指摘のとおり。1〜4 回目の記録は指紋を `<hash>` の形でしか書いておらず、関門が探す形の印が無かった。**5 回目で中身の [止める] は残らなかったので、監査を通った版の印を下に関門の形で書く。**(上の逐語の中の印は、印として読まれないよう分けて写した。関門は印の文字列があればどこにあっても通すので、逐語のままだと、処置を書く前から印があることになる。)
2: 「広すぎる」を判定する数値の閾値は置かない(置けば根拠の無い値になる = §0.2 A-12)。**リードが単独で「広すぎる・意味を持たない」と判定し直すことはしない。**列を足す提案は、リードかオーナーが台帳に問いを立てて、今の 8 列では引けなかったとき(例: 「突き合わせの単位が約定 1 件のもの」を引こうとして引けなかった)に、その問いと引けなかった事実を添えて出す。毎回の検収では列ごとの数だけを書く。設計票 §3 に書いた。
3: 監査役は根拠を本文と照らして一致を確かめた。これ以上の確認は求めない。事前登録ではないので `research-protocol` の事前登録の手順は当てない。ただし、区分 8 の結果を研究(戦略の検証)に使う段になったら、その研究の側で事前登録を書く。

**監査を通った版(委任の関門の印)**: `20260923_tools_survey_cat8_addendum.md@6a4c53ab3855`(5 回目の監査の対象そのもの。この処置で追補は書き換えていない)

**5 回目のあとの変更(記録)**: 設計票 §3 の「広すぎの側」の段落に、処置 2 の文(閾値を置かない・リードが単独で判定し直さない・列を足す提案の条件)を足した。追補は変えていない。この設計票の変更は、1 回目の起動文を監査役に通すときに一緒に読ませる。

## 6 回目(2026-09-23、対象 = 2d0bbda、段の書き足し。追補の指紋 `4a1aa75968d9`)

経緯: オーナーの選択 L-502「**(1)案A (2)案A (3)案B**」を受けて、リードが段(自動化と汎用性の 5 段)を書いた。**この回から、監査役の出力は会話に全文を出さず、この記録にだけ逐語で残す**(オーナー逐語 L-196「**監査役の逐語は私が求めた時に出力するようにしてください**」。L-501 で履歴を読み直して、1〜5 回目に毎回全文を貼っていたのが L-196 に反すると分かった)。

監査役の出力(逐語。[止める] 1 件・[直す/聞く] 1 件・[聞く] 3 件):

> 監査 6 回目。対象: (A) `docs/DATA/surveys/CAT8_DESIGN.md`(§4・§5、コミット 2d0bbda)/ (B) `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(§4・§7、同コミット、現在の指紋を実測で `4a1aa75968d9` と確認)。`git diff 9cad82c 2d0bbda` の削除行はすべて「段の追加に伴う参照更新・上位互換の書き換え」で、内容が消えた箇所は見当たらなかった(例: E4/E6 行の `§4` → `§4.2` の参照更新、旧「段は最初は置かない」段落 → 新 §4.1・§4.2 への置き換え、いずれも同等以上の内容で置換)。
>
> 以下、問いのみ(最大 25 件、severity 高い順)。
>
> 1. [止める] docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md(現在のコミット 2d0bbda、`sha256sum` で実測した現在の指紋 `4a1aa75968d9`)と `.claude/hooks/delegation_audit_gate.sh` — この版の印 `20260923_tools_survey_cat8_addendum.md` + `@4a1aa75968d9`(リード注: 原文は連結した 1 語。印と誤読されないよう分けて写した)は `docs/AUDITOR/VERDICTS/` のどのファイルにも存在しない(`grep -rn "20260923_tools_survey_cat8_addendum.md@" docs/AUDITOR/VERDICTS/*.md` で確認、ヒットは旧版の印 `...@6a4c53ab3855`(5 回目監査時点)のみ)。今回のコミットは追補 §4・§7 に段関連の記述を書き足して中身を変えたが、その後の監査記録が新しい指紋で付いていない。`delegation_audit_gate.sh` は委任文の**今の**中身の sha256 と `VERDICTS/` の記録が一致しない限り Agent/SendMessage による委任を拒否する実装で(`.claude/state/owner_unlock_delegation` も未作成、解除されていないことを実測で確認)、5 回目の監査の指摘 1 がまさにこの型(印の形式不一致で起動が機械的に止まる)を検出した直後に、同型の状態(記録が最新版に追いついていない)が再び生じている。この状態のまま 1 回目の起動文を作ってよいか(P8、KA-101)。
>
> 2. [聞く] docs/DATA/surveys/CAT8_DESIGN.md:82 — 段の順序の根拠が「**上ほど、当方の研究の検証に人の手を介さず組み込める**」という、当方にとっての使いやすさを基準にした説明になっている。同じ §4 の冒頭(L65)は、段を1回目の前に決める案 B を選んだ代償として「まだ道具を1つも見ていないので**当方の位置を基準にした目盛りになりやすい**」ことを明記したうえでの選択だとしているが、実際に書かれた順序の根拠はその懸念どおり当方の統合のしやすさそのものになっていないか。外部の既存の枠組み(ソフトウェア検証・CI の自動化成熟度モデルなど)を調べたうえでこの5段の順を決めたという記述は本文に無い(P7、`.claude/skills/research-squad/SURVEY.md` §7 の P6「当方に無い軸を落とさない(当方の位置を基準に目盛りを組まない)」)。
>
> 3. [直す/聞く] docs/DATA/surveys/CAT8_DESIGN.md:5,16 と L65 — §1冒頭(L5)と§1の帰属表(L16)は「この票の §2〜§9…はすべてリードの決定」と一括で述べているが、§4(L65)は段を1回目の前に決めるという判断そのものを「オーナーの選択 L-502」と明記している。帰属表(L16)の行はこの L-502 の決定を項目として含んでおらず、「§2〜§9 は一括してリードの決定」という記述と §4 本文の「オーナーの選択」という記述が文書内で食い違っている。監査1回目の指摘4(§9 が帰属表から漏れていた)と同型の帰属漏れが、今回は §4 の新しい記述について再発していないか。
>
> 4. [聞く] docs/DATA/surveys/CAT8_DESIGN.md:70,78,85-96 — 段5の条件は「判定の基準を指定できる」と「結果を保存して次の実行と比べられる」という2つの性質を1段にまとめているが、どちらか一方だけを満たす機能(基準は指定できるが保存・比較はできない/その逆)の扱いが AND か OR か本文に無い。また「下から積み上げ」の規則(L70)により、自動判定をしない(段3を満たさない)が結果の保存・比較はできる機能は段2以下に留まり、その「保存・比較」という性質は §4.2 の記録軸(L88-96)のどこにも書く場所が無い(「比較」を明示する軸は E5 の行にしかなく、E1a〜E4・E6 には該当欄が無い)。L70 の「満たした部分は §4.2 の軸に原文で残す」という規則を実行する記入欄がここには無いのではないか。段と段の境目でどちらにも入らない機能がここで生まれていないか(P3)。
>
> 5. [聞く] docs/DATA/surveys/CAT8_DESIGN.md:76-77 — 段3「対象はその道具の枠組みの中(…)に限る」と段4「外から持ち込んだ**関数やデータ**(…)にも掛けられる」の述語は、関数とデータをひとまとめに「や」で結んでいる。外部データ(CSV など)は受け付けるが評価対象の関数・戦略コードは自道具の枠組みに限られる道具(またはその逆)が、段3・段4のどちらに入るかは本文からは一意に決まらないのではないか。
>
> 参照した実物: `docs/DATA/surveys/CAT8_DESIGN.md`(2d0bbda 時点)、`docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(同、`sha256sum` 実測)、`docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`(1〜5回目)、`.claude/hooks/delegation_audit_gate.sh`、`.claude/skills/research-squad/SURVEY.md` §1・§7、`git diff 9cad82c 2d0bbda`。

**リードの処置(コミット f8aa9eb)**:
1: 印は監査を通った版にだけ書く手順なので、この回の直しのあとの確認の監査(7 回目)を通してから書く。
2: 段の境目の説明を道具の側の性質だけで書き直し、順序の向きはプロジェクトの目標の文書(人が決めるものと自動化できるものを分ける / 手順と道具が資産)から取ったと明記した。当方の目標(向き)と当方の位置(基準点)を分けて書き、目標に合わせて順序を決めていることは事実として残した。外部の成熟度の枠組みは調べていない(未確認)と書き、1 回目の調査で出たら突き合わせるとした。
3: 設計票 §1 の表に L-502 の 3 件の選択を 1 行足し、「§2〜§9 はリードの決定」の行から除いた(段の中身はリードが書いたもの、と添えた)。
4: 段 5 は (ア)(イ) の両方(AND)とし、片方だけは段 4。§4.2 に全要素の行を足し、外から持ち込める対象・(ア)・(イ)・自動で判定するか、を段と別に記録する欄にした。
5: 要素ごとの「対象」を決め(E2 = 検査するデータ、E3 = 相手の計算のコードとデータ、など)、段 4 は対象のすべてを外から持ち込めること、一部だけなら段 3 とした。

## 7 回目(2026-09-23、対象 = 5aa98e1。6 回目の処置の確認。追補の指紋 `4a1aa75968d9`、この回は追補を書き換えていない)

監査役の出力(逐語。[直す] 3 件・[聞く] 3 件。[止める] 0 件):

> 監査 7 回目(6 回目の処置の確認)。対象: (A) `docs/DATA/surveys/CAT8_DESIGN.md`(コミット 5aa98e1 時点、実体は f8aa9eb の変更内容)/ (B) `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(指紋 `4a1aa75968d9`、この回は書き換えなし、確認済み)。
>
> 読んだ材料: `docs/AUDITOR/PRINCIPLES.md`、`docs/AUDITOR/KNOWN_ANSWERS.md`、`docs/DATA.md`、対象2文書、`docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_design.md`(1〜6回目)、`git diff 2d0bbda 5aa98e1`、`.claude/hooks/delegation_audit_gate.sh`、`docs/OWNER_STATUS.md`。
>
> 6回目の5件の処置(印は監査を通った版にだけ書く/段の境目の説明の書き直し/§1帰属表の追記/段5のAND化と記録欄新設/要素ごとの対象の新設)はいずれも実物のテキストに反映されていることを確認した(git diff で照合)。追補§4は設計票§4.1・§4.2を「逐語で当てる」と動的に参照する書き方のため、今回の§4.1/§4.2改訂に対して追補自体の書き換えは不要という理解も本文と整合していた。
>
> 以下、問いのみ(定義どおりの形式)。
>
> 1. [直す] `docs/DATA/surveys/CAT8_DESIGN.md:81` と `:98` — §4.1 に新設した「要素ごとの対象」(段 3・4 の「対象」の定義。E3a・E3b = 「検出する(防ぐ)相手の計算・特徴量・戦略のコードと、それに入れるデータ」)と、§4.2 の記録軸表の E3a・E3b 行に元々ある「対象(任意の関数か、その道具の枠組みの中だけか)」は、同じ「対象」という語で、範囲も内容も異なる 2 つの定義になっている(前者はコード+データの複合、後者は関数か枠内かの二択)。新設の「全要素(段と別)」行(:95、「外から持ち込める対象(上の『要素ごとの対象』のうちどれか)」)は :98 の古い軸とほぼ同じ問い(枠内か外かの判定)を、全要素向けに新しい定義でもう一度記録させる形になっており、同じコミット内で用語が意味を変えて併存している(P2)。この重複・衝突は今回の改訂(f8aa9eb)で :98 の行が見直されずに残った結果ではないか。
>
> 2. [直す] `docs/DATA/surveys/CAT8_DESIGN.md:56` と §4.2 の記録軸表(:92-100) — §3 の E6 の述語は「例として決めない。当たったものの原文を §4.2 に残す」と明記しているが、§4.2 の記録軸表には E1a・E1b(:96)/E2(:97)/E3a・E3b(:98)/E4(:99)/E5(:100)の行はあるが **E6 の行が無い**。§4.1 の「要素ごとの対象」(:81)には E6 の対象定義(「その機能の原文が名指す対象」)があるのに、§4.2 の記録先だけが欠けている。族(E1a〜E6 の 8 列のうち §4.2 対応が要る要素)の水準に埋まっていない項目が残っていないか(P3)。
>
> 3. [直す] `docs/DATA/surveys/CAT8_DESIGN.md:109` と `:115` — §5 の台帳の列に `段_E1a`〜`段_E6`(値は §4.1 のとおり)が追加されているが、§6 の `cat8_ledger.py` の各コマンドの記述(`set`(状態・E1a〜E6・回・行)/ `check`(台帳の語が §2・§3 の語彙の中にあるか…)/ `recount`(要素ごとの `印` の数と `未判別` の数…))はいずれも段列(`段_E1a` 等)にも §4.1 の語彙(`1`〜`5`/`未判別`/`-`)にも触れていない。台帳のスキーマには段列があるのに、それを書き込む・検査する・集計する道具側の記述には反映されていないのではないか。1 回目の前にリードが書く `cat8_ledger.py` の実装がこの記述どおりに作られた場合、段列は `set` も `check` も通らないままにならないか(P3、P8 型)。
>
> 4. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:81` — E3a・E3b をまとめて 1 つの「対象」(「検出する(防ぐ)相手の計算・特徴量・戦略のコードと、それに入れるデータ」)で定義しているが、§3(:53)での E3b の述語は「purged 交差検証・embargo・時点の揃った結合など」という**データパイプライン側の防止機構**を指しており、「相手の…コード」を検出/防止する対象として捉える E3a の枠組みと同じ「対象」定義がそのまま E3b にも当てはまるか、本文からは確認できない。段 3・4 の判定(枠内か外かの持ち込み)を E3b に適用する際、この共通定義で判断に迷いが生じないか。
>
> 5. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:82-86` — 段の順序の向き(上ほど良い側)をプロジェクト目標文書から取ったとし、「外部の既存の枠組み(検証や試験の自動化の成熟度の段など)を調べて決めたものではない(未確認)」と明記した上で、「1 回目の調査で、そうした枠組みが候補として出たら、この段と突き合わせる」と事後確認に留めている。段の中身(5 段の切り方そのもの)は 1 回目の起動前にオーナーへ見せる設計の核であり、外部の選択肢を先に調査してから確定する(P7)のではなく、見せたあとの調査に委ねてよいという判断は、この票のどこにも理由が書かれていない。
>
> 6. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:56,81` — E6 の「対象」は E1a〜E5 のような固定物ではなく「その機能の原文が名指す対象」という開いた定義であるため、段 3・4(枠内か外に持ち込めるか)の判定が回・候補ごとに一貫するかが本文の記述だけでは担保されていない。E6 は「例として決めない」設計だが、段の当て方の一貫性について検収でどう確かめるかの記述が無い。
>
> 参照した実物(コマンドで照合): `git diff 2d0bbda 5aa98e1 -- docs/DATA/surveys/CAT8_DESIGN.md`、`git diff 2d0bbda 5aa98e1 -- docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(差分なしを確認)、`sha256sum` による追補の現在の指紋(`4a1aa75968d9`、本文の自己申告と一致)、`grep -rn "20260923_tools_survey_cat8_addendum.md@" docs/AUDITOR/VERDICTS/*.md`(現行指紋の印はまだ無い。ただし 6 回目の処置 1「この確認監査を通してから書く」の設計どおりであり、それ自体は今回の指摘対象にしていない)、`.claude/hooks/delegation_audit_gate.sh` の実装、`docs/OWNER_STATUS.md` の区分 8 の欄(1 回目未起動を確認)、`ls docs/DATA/tools_catalog_cat8.tsv` `docs/DATA/SCAN_2026-09-23_tools_cat8.md` `scripts/cat8_ledger.py`(いずれも未作成、想定どおり)。

**リードの処置(設計票だけ。追補は変えていない)**:
1: §4.2 の E3 の行から「対象」を外し、外から持ち込めるかは「全要素(段と別)」の行だけに書くとした。E3 の行には方式だけを残し、E3b の方式(分割の仕方・空白期間・時点を揃えた結合)を足した。
2: §4.2 に E6 の行(機能の原文 / 名指す対象の原文)を足した。
3: §6 の `set`・`check`・`recount` に段の列を足した(`check` は §4.1 の語彙と、`印` の要素に段があり `なし` の要素の段が `-` かを見る)。
4: 「要素ごとの対象」を E3a(相手の計算のコードとデータ)と E3b(防ぐ仕組みを掛ける先のデータの流れと、それを使うコード)に分けた。
5: 先に調べずに見せる理由(枠組みを探すこと自体が区分 8 の調査の中身)と塞ぎ方(1 回目の検索計画に入れる / 1 回目の検収で突き合わせる / 合わなければ全行に当て直し・監査・オーナーに見せる)を書いた。
6: 毎回の検収で、E6 に段を付けた行を全部読み、同じ種類の対象に違う段が付いていないかを見る、と書いた。

**監査を通った版(委任の関門の印)**: `20260923_tools_survey_cat8_addendum.md@4a1aa75968d9`(6 回目と 7 回目の対象。7 回目で追補への指摘は 0 件。7 回目の処置では追補を書き換えていない)
**7 回目のあとの設計票の変更**は上の処置 1〜6 だけ。1 回目の起動文を監査役に通すとき、この変更も一緒に読ませる。

## 8 回目(2026-09-23、対象 = 1 回目の起動文(新設、8c0fc9c、指紋 `9c8f5d3c8ca1`)・7 回目のあとの設計票の変更・台帳と `cat8_ledger.py`)

オーナーの指示 L-503「**起動してください**」を受けて書いた起動文の監査。Jev の前段(`jev_delegate.py plan --prompt <起動文>`)は印 1 件 `not_bounded`(is_bounded 0.62 → 「この回の終わり方」の段落を足して 0.63)。

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 3 件):

> 1. [止める] docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md:35-36 — 「印」という語を、同じ表・同じ段落の中で(a)`値` 列に入る 3 値の 1 つ(要素が存在することを示す `印`/`なし`/`未判別`。設計票 `CAT8_DESIGN.md` §3 の語彙)と、(b)別の列名(委任文 §4.1 の 5 語 `一次資料`/`実測`/`推定`/`仮定`/`未確認`)の 2 つの異なる量に、名前を変えずに使い回していないか。35 行目「値は `印`/`なし`/`未判別`」の直後、36 行目で「印は委任文 §4.1 の 5 語」と、同じ字面の列見出し「印」に別の語彙を割り当てている。委任文 §4.1 自身の「一次資料」列を持つ既存の表(`道具|項目|値|印|根拠`)を継承した命名に見えるが、区分 8 の表は `値` 列にも `印` という語を持つため、既存表と違って衝突が生じている。CLAUDE.md §0.2 O-7「一度出した用語の意味を変えて使い回すことは…無限の論点ずらしの許容と同義」、P2 に当たらないか。(P-02, O-7)
>
> 2. [止める] docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md:74 と docs/DATA/surveys/CAT8_DESIGN.md:39、docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:53 — 追補 §7 と設計票 §2 の「1 回の実行」の完了条件(b)は「…印の要素には §4.1 の段が書かれている」ことを明示するが、起動文 §8「これが1回目の完了の形」の項目 2 は「候補の一覧の全行が `(8-NNN)` か `(新)` で書かれ、全行に 8 要素の表の 8 行と状態があること」とだけ書き、「印の要素には段が書かれていること」の句を含んでいない。§8 は起動文中で「これが1回目の完了の形」と明記された、調査班が返す前に照らす最終チェックリストであり、Jev の not_bounded(0.62→0.63)を埋めるために §0 末尾に足された「この回の終わり方」の実質的な運用箇所でもある。ここが追補・設計票より狭い場合、段の未記入のまま「完了」と申告されうるが、これは意図した省略か、書き漏れか。
>
> 3. [直す] 「要素の印と段」の表(`docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md:31-34` の 6 列表: 道具/要素/値/段/印/根拠)は、`scripts/check_scan_report.py` のどの検査にも読まれないことを実際に合成報告で確認した(K1・K2 以外は無反応。`read_table` は 5 列かつ 2 列目が `ITEMS` 語彙の行だけを拾い、`read_findings_table` は 4 列固定なので、この 6 列表はどちらにも一致しない)。`cat8_ledger.py` の `check`/`sync` もこの表を読まない(候補の一覧の `(8-NNN)`/`(新)` 行とログの見出ししか見ていない)。したがって、この表の `印`(委任文 §4.1 の 5 語)の語彙誤用・根拠の空欄・`値` と `段` の矛盾を機械的に検出する仕組みは今のところどこにも無い(K13 が見る「深掘りの中身が空でないか」も、対象は §4.0 の項目表であってこの 6 列表ではない)。区分 1 で K1〜K13 が積み上がった理由(自己申告の報告が繰り返し機械検査をすり抜けた実績)を踏まえると、区分 8 の中核データであるこの表だけ検査の空白域のままでよいか。(research-protocol の趣旨、P3 に近い)
>
> 4. [聞く] `scripts/cat8_ledger.py` の `check_rows`(202 行)は、要素の値が `未判別` の行について段が `-` でも `未判別` でもエラーにしない。一方、設計票 `CAT8_DESIGN.md:85` は「段の列の値は `1`〜`5` / `未判別` / `-`(その要素が `なし`)」と、`-` を明示的に「その要素が `なし`」の場合に限定して定義している。起動文 35 行目はさらに「段は…`なし`と`未判別`のとき`-`」と一本化して書いている。`-` が「確認済みの `なし`」と「未確認の `未判別`」の両方を表すことになり、あとから段の列だけを見ても両者を区別できなくなるが、これは意図的な仕様か(区分 8 の初期状態が実際に `未判別`/`-` のペアで初期化されている — `docs/DATA/tools_catalog_cat8.tsv` の 8-001〜8-005 行、`cat8_ledger.py` の `new_row` 90-97 行 — ため、既存の初期値とは整合している)。
>
> 5. [聞く] Jev の前段(`jev_delegate.py plan`)が `is_bounded` 1 件 `not_bounded`(0.62→「この回の終わり方」を足して 0.63)のまま、リードは閉じずに起動する予定と説明されている。起動文・設計票・追補のいずれにも `is_bounded` の閾値や「何点で閉じたとみなすか」の基準は見当たらなかった(`grep` で `is_bounded`/`not_bounded` はこの 3 文書に 0 件)。閉じずに記録して起動する運用そのものの当否は判定しないが、①この数値 0.63 が何の基準に照らして「起動してよい」側なのか、②「閉じずに記録する」がこの単位で初めてでないなら、過去の同型運用(`KNOWN_ANSWERS.md` KA-101 は「Jev の前段が未到達でも監査役は省かない」であって「省いてよい閾値」を定めたものではない)とどう整合するか、は本文に書かれているか。
>
> 6. [聞く] 起動文 §0(最初に全部読むもの)は設計票の読む範囲を「§2〜§5」に限定し、§6(`cat8_ledger.py` の仕様と受け入れ検査の被覆の測り方)・§8(監査を掛ける回・Jev を毎回当てる規則)を明示的な読み込み対象から外している。調査班自身が §6・§8 を実行するわけではない(台帳もscripts/cat8_ledger.pyも触らない)ため妥当に見えるが、§4.0 の「深掘り」の条件(委任文 §4.0、過半が一次資料/実測)を満たすかどうかの判定には §6 の「使う前に被覆を測る」計画が関わる。調査班が §6 を読まないことで、リードの検収時の期待(1 回目の被覆測定)との連携に支障が出ないか。
>
> 上記 6 件のうち、1・2 が最も具体的な根拠を持つ(実際にコードを読み、また合成入力で `check_scan_report.py` と `cat8_ledger.py` の `sync`/`check` を実行して検証済み)。候補の一覧の行の形式(`[深掘り]` とバッククォート、`(8-NNN)`/`(新)`、URL)自体は、`check_scan_report.py`(`marked_names`)と `cat8_ledger.py`(`NEW_RE`・`cmd_check` の `(8-\d{3})` 抽出)の両方で正しく読めることを合成報告で実測確認した(問題なし)。書き込み先(`docs/DATA/SCAN_2026-09-23_tools_cat8.md` と `docs/DATA/probes/20260923_tools_8_run1.log`)は追補 §1 の 2 つに収まっており、台帳・`cat8_ledger.py`・区分 1 の資料・`src/bot/bt/` 等への書き込みは明示的に禁止されている。禁止事項(委任文 §5・§6・§10)を緩めた記述は見当たらなかった。`cat8_ledger.py` の正規形・重複拒否・段の列の検査・残りの数え方は、設計票 §5・§6 の記述と一致することを、合成した `sync`/`check` の実行結果(台帳への追加・重複検出・突き合わせの出力)で確認した。起動文の指示は概ね述語で書かれており(「例として決めない」の自己言及もあり)、例示で決め打ちしている箇所は見当たらなかった。

**リードの処置(コミット 90ea2b1)**:
1: 起動文の表の列を「根拠の種類」に改め、値の列の `印` とは別のものと明記した。追補 §4 の同じ言い回し(「委任文 §4.1 の印」)も「根拠の種類(委任文 §4.1 の 5 語。要素の値の `印` とは別のもの)」に直した(追補を書き換えたので指紋が変わった: `46258eebcd93`)。
2: 起動文 §8 の 2 に「値が `印` の要素には段が書かれていること」を足した。
3: `cat8_ledger.py` に読むだけの `check-elements` を足した(候補の一覧の行の番号の形と状態 / 「要素と段」の表の 8 行・値・段・根拠の種類・根拠の空欄 / 表と一覧の名前の食い違い)。壊した合成の報告で 9 か所すべてに当たることを確かめた。調査班にも返す前に打たせ、出力を貼らせる(起動文 §8 の 2、追補 §2)。**被覆**: この検査が何行読んだかを最初の行に出す(「候補の一覧 N 行 / 要素と段の表 M 行」)ので、リードは検収で報告の行数と突き合わせる。
4: 意図した仕様ではなかった。要素が `未判別` の段は `未判別` にした(設計票 §4.1、起動文、`cat8_ledger.py` の初期値と検査、台帳の 5 行)。
5: `is_bounded` を閉じたとみなす閾値は、どの文書にも無い(リードは閾値を置かない。置けば根拠の無い値になる = A-12)。扱い: Jev の印は判定ではなく、完了の形が書けているかを人が見直す合図として使う。起動文には完了の形(§8)と終わり方(§0 の末尾)を書き、監査役がそれを読んでいる。印が残ったまま起動したことと数値は、この記録と検収に毎回書く。過去の同型の運用は、区分 1 と今回の追補(0.59 → 0.63)で、どちらも閉じずに進めた。
6: 調査班が読むのは §2〜§5 のままにする。被覆を測るのはリードの検収の仕事(設計票 §6)で、調査班の作業の中身は変わらないため。
