# 監査記録: 道具サーベイ 区分 8 の設計票と追補

対象: `docs/DATA/surveys/CAT8_DESIGN.md`(設計票)/ `docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md`(もとの委任文 `20260922_tools_survey_prompt.md`(指紋 `ce0012c95154`)の区分 8 の追補)。
監査は調査班の 1 回目を起動する前(SURVEY.md §1・§8 の 1、KA-101、関門 `delegation_audit_gate.sh`)。
**この記録は監査 3 回目の指摘 4(1・2 回目の指摘の書面の記録が無い)を受けて書いた。**1・2 回目の出力は、届いたその返答の中で次の直しより先に会話へ逐語で出していたが、この記録は 3 回目のあとにまとめて書いている(O-4 の型に当たる。事実として残す)。

## 1 回目(2026-09-23、対象 = 設計票 093917f)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件・[聞く] 3 件):

> 検査対象: `docs/DATA/surveys/CAT8_DESIGN.md`(コミット 093917f、ブランチ `claude/tools-survey-cat8`)。照合した材料: `CAT8_HANDOFF.md`、`.claude/skills/research-squad/SURVEY.md`、`docs/DATA/delegations/20260922_tools_survey_prompt.md`(指紋 ce0012c95154)、`docs/DATA/tools_catalog.tsv`、`scripts/check_scan_report.py`(`scope()`)、`docs/PROJECT_GOAL.md`。数値・件数・正規表現の主張(1.77 MB・116 行・最大 124・`^## 区分\s*\d`・区分1から回された5件)は実物のコマンド(`wc -l`・`awk`・`ls -la`)で1件ずつ照合し、すべて一致した(不一致なし)。
>
> 1. [止める] `CAT8_DESIGN.md:64` — 「調査班に台帳への `add`・`set` を許す(委任文 §6-5 の書き込み先に、起動文で台帳を足す)」とあるが、委任文 §6-5(見出し「6. 安全の規則」の 5 番目「**リポジトリを変えない**: 書いてよいのは `docs/DATA/SCAN_2026-09-21_tools.md` と `docs/DATA/probes/20260921_tools_<区分>.log` だけ。scratchpad 以外にファイルを作らない。」)を実際に読むと、書き込み先を**この 2 つだけに限定する禁止条項**であり、台帳(`tools_catalog_cat8.tsv`)や `cat8_ledger.py` はどこにも含まれていない。同じ設計票の別行(L26「§5-6」)では section-item 番号を正確に引けているので、これは誤読ではなく、禁止条項を「書き込み先の列挙」であるかのように読み替えている。SURVEY.md の委任の型は「委任文は既存のものを使う…委任文を変えるなら監査役を通して指紋を `VERDICTS/` に記録してから(委任の関門が止める)」と明記している。監査済み・指紋つきの委任文が明示的に禁じる書き込み先を、委任文自体を改める手続きを経ずに各回の起動文で実質的に上書きしてよいか(CLAUDE.md §0.2 A-16 の型、SURVEY.md §2)。
>
> 2. [止める] `CAT8_DESIGN.md:66` — 報告書の在処を委任文 §9 の指定(`SCAN_2026-09-21_tools.md`)から新設の `docs/DATA/SCAN_2026-09-23_tools_cat8.md` へ移す決定も、同じ §6-5 の書き込み先の限定(2 ファイルだけ)の外にある。1.77 MB の衝突回避という運用上の理由は実測(`ls -la`: 1,772,705 バイト、確認済み)に基づき妥当そうだが、これが「委任文の変更」に当たるかどうかの判断そのものが本文に書かれていない。1 と合わせて、委任の関門(指紋つき VERDICTS 記録)を経ずに書き込み範囲を広げる決定が 2 件重なっている。
>
> 3. [直す] `CAT8_DESIGN.md:22-27` — 状態語 7 つのうち「危険で導入停止」「登録が要る」「区分8の要素なし」の 3 つには `=` で明示的な述語があるが、「未着手」「浅い」「深掘り」「判別に一次資料が要る」の 4 つには設計票自身の述語が無い。「浅い」「深掘り」は委任文 §4.0 に間接的な定義があるが、「判別に一次資料が要る」は委任文にもこの設計票にも定義が無く、区分 1 の実務(`scripts/recount_scan_cat1.py`・`TOOLS_CATALOG.md`)から述語を明示せずに持ち込まれている(grep で確認: 区分1側では「未着手」と区別のつく用法があるが、区分8向けの述語としては書かれていない)。SURVEY.md §1 の教訓(規則は述語で書く。§7 の P1・P6)に照らし、この状態語の述語をこの票に足さなくてよいか(P3 型: 台帳の語彙の一部が未定義のまま運用に入る)。
>
> 4. [聞く] `CAT8_DESIGN.md:16` — 「誰の語か」の対応表は「この票の §2〜§8」を一括で「リードの決定」としているが、§9(1回目の範囲、L91-95)がこの表に含まれていない。§9 には候補一覧を全部 `add` する手順や区分1から回された5件の扱いなど、実行に直結する決定が書かれている。§9 の記載は誰の語として扱われているか(L-162 型の帰属漏れ)。
>
> 5. [聞く] `CAT8_DESIGN.md:44,46` — E1「『突き合わせの相手になりうる別実装である』だけでは付けない」、E3「防ぐだけの機能(purged 交差検証・embargo など)は検出ではないので E3 はなし」という述語の狭め方は、過大な該当付与(偽陽性)を防ぐ一方で、区分8の該当候補数を減らす方向に働く。委任文冒頭のオーナー逐語(L-378〜380「ちゃんと調べさせる気のない委任文出すなよ」「調査に甘えや抜け…を無くせ」)はこの種の絞り込みに向けられたものではないと言えるか。この狭め方が構造的な安全側(A-18)や族の実質的な縮小(A-10、値は捨てず `他の区分へ`/E6 に回すとはいえ)になっていない根拠は、狭め方の理由(区分1の116行の大半が当たりうる=推定、と明記)以外に本文にあるか。
>
> 6. [聞く] `CAT8_DESIGN.md:57` — 「最初は段を置かない(区分8で要る軸はまだ分からない)」という判断は、委任文 §8「無いもの」B(別実装の再現ゲートの許容誤差)・C(ルックアヘッド検出の自動化の程度)から軸をある程度見積もれる可能性を検討した記述を伴わない。SURVEY.md §1「要ると分かっているなら最初に。当方に無い軸を落とさない」(区分1では遅延・取り消しの扱いが目盛りから落ちて後で13件の当て直しが発生した教訓)との整合は、「まだ分からない」の一文だけで足りるか。
>
> 7. [直す] `CAT8_DESIGN.md:64` — 台帳へ調査班が直接 `add`/`set` する設計は、区分1が採った方式(`build_tools_catalog.py` が報告書から台帳を作り直す。番号は台帳が振るが、報告本文からの再構築)と異なる。区分1の教訓(SURVEY.md §7 の P5「番号体系」: 1・2回目の独自番号・重複4・落ち4)が、調査班が台帳を直接書き換える方式でも再発しないと確かめた記述が無い。
>
> 数・事実の主張については、ファイルサイズ(1,772,705 バイト = 1.77 MB)・台帳の行数(116、`awk -F"\t" 'NR>1' docs/DATA/tools_catalog.tsv | wc -l`)・番号の最大値(124)・区分1から回された5件(62 qf-lib/70 PineForge/92 prediction-market-backtester/101 akurkar07/OrderBook/117 Exegy、いずれも「他の区分へ」列に「8」を含む)・`check_scan_report.py` の `scope()` 正規表現(`^## 区分\s*\d`、実際の見出し形式 `## 区分1 — N 回目の実行(YYYY-MM-DD)` と整合)を実物で確認し、不一致は見つからなかった。`cat8_ledger.py`・`tools_catalog_cat8.tsv`・`SCAN_2026-09-23_tools_cat8.md` はいずれも未作成で「新設」の記載と整合する。ブランチも `claude/tools-survey-cat8` で一致。

**リードの処置(コミット a83cd6a)**:
1・7: 調査班に台帳を書かせない。報告では新しい候補を `新` と書き、リードが検収で `cat8_ledger.py sync` で番号を振る。P5 の 3 つの型(独自の番号・重複・落ち)を何が塞ぐかを設計票 §5 に書いた。
2: 報告書の在処の変更は委任文の変更に当たると設計票 §5 に明記した。もとの委任文は変えず、追補(新設)に書き込み先を置き、監査に通して指紋を記録してから引く。
3: 状態の 7 語全部の定義を設計票 §2 に書いた。
4: 設計票 §1 の表を「§2〜§9 と追補」にした。
5: 狭めずに E1a(突き合わせる機能)/ E1b(突き合わせの相手)、E3a(検出する)/ E3b(防ぐ)に分け、どちらか一方でも区分 8 の要素に当たるとした。
6: 段は置かないが、分かっている軸(委任文 §8 の B・C、区分 1 の遅延の教訓など)を 1 回目から原文で記録させる表を設計票 §4 に置いた。

## 2 回目(2026-09-23、対象 = 設計票と追補 a83cd6a、追補の指紋 `3d29bbc7524f`)

監査役の出力(逐語。[直す] 2 件・[聞く] 3 件):

> 1. [直す] docs/DATA/surveys/CAT8_DESIGN.md:31 — `区分8の要素なし`の述語が「§3 の**6 要素**のどれにも当たらない」と書かれているが、§3 自体は監査1回目の指摘5を受けて E1/E3 を a/b に割り、台帳の列は**8 本**(E1a〜E6)と明記済み(44行目・57行目「E1a〜E6 がすべて `なし`」)。同じ判定基準を指す語が、文書内で「6 要素」と「8 列(E1a〜E6)」の2通りの表記のまま残っている。意味は両立しうる(6 概念×一部2列split=8列)としても、旧い言い回しが分割後に更新されずに残っているのではないか。§2 のこの行だけ §3 の書き換えに合わせて直っていないのではないか(P2)。
>
> 2. [聞く] docs/DATA/surveys/CAT8_DESIGN.md:59-60 — 監査1回目の指摘5(「相手になりうるだけでは付けない」「防ぐだけでは付けない」は族を狭める型)への対処として、E1b(相手になりうるだけ)・E3b(防ぐだけ)も単独で「区分8の要素に当たる」に変えた。その結果、設計文自身が「E1b は区分1の台帳の候補(バックテスト系)の**多くに当たりうる**(推定。数えていない)」と認めている。族を広げすぎて `区分8の要素なし` が事実上ほとんど成立しない基準になっていないか、この広さで区分8の分類が意味を持つと確認済みか(狭める方向の誤りを正した結果、広げる方向に振れすぎていないか)。
>
> 3. [直す] docs/DATA/surveys/CAT8_DESIGN.md:83 — `sync` の重複判定の規則が「名前と URL の正規形(**GitHub は**所有者/登録名の小文字)」とだけ書かれ、GitHub 以外の出典(PyPI・公式サイト・一覧の辿り・X 経由など、委任文§3 で認めている発見経路)の正規形の規則が書かれていない。区分1の P5 型(重複・落ち)をこの作りで塞ぐと述べているが、正規化ルールが及ばない出典では `sync` の重複検出・`check` の「落ち」検査が機能するか書かれていない。
>
> 4. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:11 と docs/DATA/surveys/CAT8_DESIGN.md:108 — 追補§1「区分8で書いてよいのは次の2つだけ(報告書・生ログ)」と、設計票§8「書くのは区分8の資料(報告・生ログ・**台帳・索引・検収の記録・cat8_ledger.py・起動文・SURVEY.mdへの追記**)と OWNER_LOG・OWNER_STATUS」は、前者が調査班向け・後者がリード(セッション全体)向けという文脈上の違いで両立していると読めるが、その適用範囲の違い(誰が書いてよい2つだけなのか)はどちらの文書にも明記されていない。追補は「調査班は起動の最初に…この追補…を読む」ものであり、追補単体を読んだ調査班に「書いてよいのは2つだけ」の主語がリード全体でなく自分(調査班)だけだと明確に伝わるか。
>
> 5. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:9 — 見出しは「委任文 §5-5・§6-5・§9 を**置き換える**」と書きつつ、実際に書き換えているのは各節の「書き込み先」部分だけで、§9 の必須小節6つなど他の規定は追補冒頭の一般条項「ここに書いていないことは、すべてもとの委任文のとおり」に依拠して存続する(このことは設計票§5(85行目)にしか明記されておらず、追補自体には「§9の他の規定は存続する」との明記がない)。「§9 を置き換える」という強い見出しと、実際は部分置換であることの整合は、追補単体を読む調査班にも明らかか。
>
> 上記 5 件のほか、以下は確認した限り整合していた(問題なし):
> - 1回目の監査指摘 1・2・3・4・5・6・7 はいずれも該当箇所に実際の修正が反映されている(§2 の7語すべてに述語、§3 の E1a/E1b・E3a/E3b 分割、§4 の記録軸表、§5 の書き込み先変更の明記と追補化、§1 の帰属表拡張、§5 の番号重複/落ち対策の説明)。
> - 追補の指紋 `3d29bbc7524f`(自己申告)は実ファイルの sha256 先頭12桁と一致。もとの委任文の指紋 `ce0012c95154` も実ファイルと一致。ただし `docs/AUDITOR/VERDICTS/` にはこの追補・設計票2回目の監査記録がまだ無い(今回の監査で記録される想定と見られる。実行前なので未着手そのものは問題ではない)。
> - 台帳の初期5件(62 qf-lib / 70 PineForge / 92 prediction-market-backtester / 101 akurkar07/OrderBook / 117 Exegy)と「最大124、116行」の数値は、実際の `docs/DATA/tools_catalog.tsv` を実測して照合済み、一致していた。
> - 追補が置き換える §5-5・§6-5・§7・§9・§12コマンドと、置き換えていない §6-1〜§6-4・§6-6・§10(危険検査・外部送信禁止・鍵登録禁止など安全規則)を突き合わせたが、安全規則を緩めている箇所は見当たらなかった。
> - 引き継ぎ書 `CAT8_HANDOFF.md` §1(オーナー逐語)と §5(競合)は、§1 がオーナー逐語のみに限定され、§5 の運用細目(ブランチ限定・L-500からの採番・OWNER_STATUS欄1つ)はいずれも「リードの決定」と自己申告されており(§5 冒頭は §1 の指示そのものではないと明記)、逐語をリードの決定にすり替えている箇所は見当たらなかった。

**リードの処置(コミット c3defdb)**:
1: 「8 列(E1a〜E6)がすべて `なし`」に直した。残る「6 要素」は設計票 44 行目(区分 8 の概念の数)と 83 行目(区分 1 の要素)で、意味が別。
2: 設計票 §3 に「広げすぎの側」の段落(8 列を束ねずに持つ / `区分8の要素なし` は回し先の記録用 / 広さは 1 回目の検収で列ごとの数を出して確かめ、確かめるまで「意味を持つ」と書かない)。
3: 名前・GitHub・PyPI・X・その他の URL の正規形と、どちらか一方で当たれば止まること、機械で拾えない重複を限界として書いた。
4: 追補 §1 の主語を「調査班が」とし、台帳などはリードが書くと添えた。
5: 見出しを「書き込み先の部分だけを置き換える」にし、§9 の他の規定と §5-5 の 1 手ずつ残す規則はそのままと明記した。

## 3 回目(2026-09-23、対象 = c3defdb、追補の指紋 `756c78acf64b`)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 3 件):

> 1. [止める] docs/AUDITOR/TRACE/2026-09-23_2da9385f.json rollup.P2_table_shown_before_first_write (num=0, den=2, value=0.0) — この commit c3defdb 自体に含まれる機械記録では、`first_write_at` より前に §0.1 の表(検出文字列「オーナーの原文の該当語」)が出た回数が 2 手中 0 である。監査 2 回目の指摘を受けたこの改訂(区分 8 の設計票・追補の書き換え)は、着手前に §0.1 の「やろうとすること / オーナーの原文の該当語」の 2 列表を出したか。出していないなら、それは承認済み常設スキル(research-squad)の固定手順としての例外に当たるのか、それとも§0.1 が要求する擦り合わせを経ずに着手したか(CLAUDE.md §0.1、KA-100 型)。
>
> 2. [聞く] 同ファイル rollup.P4_audit_pasted_immediately(num=0, den=2, value=0.0) — 監査呼び出し(`audit_calls`)の直後の手が ACTION_LOG への書き込みでない回が 2 手とも該当する。今回の改訂サイクルで、監査役の指摘をその場で逐語に出さずに別の手を先に行っていないか(§0.2 O-4「監査役の指摘を、その場で逐語に出さず後でまとめる」)。VERDICTS への記録など ACTION_LOG 以外の経路で即時に出したのであれば、その記録はどこにあるか。
>
> 3. [直す] docs/DATA/surveys/CAT8_DESIGN.md 38行目 — 「1 回の実行」の完了述語が「委任文 §11 の『1 回の実行の完了』のとおり」を引いているが、§11 の原文(`20260922_tools_survey_prompt.md`)は成果物が `SCAN_2026-09-21_tools.md` に揃うことを条件にしている。今回の追補(監査 2 回目の指摘 5 への対応)は §1 見出しを「§5-5・§6-5・§9 の書き込み先の部分だけを置き換える」に狭めており、§11 は書き換え対象の列挙(§5-5・§6-5・§9)に入っていない。区分 8 は新設の `SCAN_2026-09-23_tools_cat8.md` に書く設計なのに、完了述語の文言上は旧ファイル(区分1の報告)が揃うことを要求したままではないか。
>
> 4. [止める] docs/AUDITOR/VERDICTS/ に区分 8(CAT8_DESIGN / 追補)に対応する記録ファイルが 1 件も無い(ls で確認: 存在するのは `2026-09-21_tools_survey_prompt.md`・`2026-09-21/22_tools_scan_cat1_*` のみ)。CAT8_DESIGN.md と追補の本文は複数箇所で「監査 1 回目の指摘 1・2・5・6・7」「監査 2 回目の指摘 2」を根拠として引用している(例: 44・84・86行目、addendum 7行目)が、その指摘の書面記録がリポジトリに見当たらない。`.claude/hooks/delegation_audit_gate.sh` はこの追補の指紋が `VERDICTS/` の記録に無ければ Agent/SendMessage による区分 8 の委任を止める作りになっている。これらの監査 1・2 回目の指摘は `docs/AUDITOR/VERDICTS/` に書面として記録されているか、それとも会話上でのみ存在し、まとめてこの設計票に反映されただけか(§0.2 O-4、KA-101 の検査の型)。
>
> 5. [聞く] docs/DATA/surveys/CAT8_DESIGN.md 84行目(番号表、監査 2 回目の指摘 3 への対応) — 名前の正規形は「NFKC・小文字・空白と `-` `_` `.` を除く」という広い畳み込みで、「名前か URL のどちらか一方でも当たれば」重複と判定して `sync` を止める設計にした。監査 2 回目の指摘は正規形の**不足**(GitHub 以外に無い = 見落とし方向)を問うものだったが、今回の直しは名前側の畳み込みを広げ「一方でも当たれば」に強めたことで、逆方向(短い・一般的な名前を持つ別々の道具が名前だけ一致して誤って重複扱いされる = 過剰一致)の危険が新たに生じていないか。検討済みなら根拠を書く必要はないか。
>
> 6. [聞く] docs/DATA/surveys/CAT8_DESIGN.md 61行目(「広げすぎの側」、監査 2 回目の指摘 2 への応答) — 「広さが実際にどうだったかは 1 回目の検収で列ごとの数を出して確かめる(確かめるまで『意味を持つ』とは書かない)」とあるが、確かめた結果 E1b・E3b が想定より広すぎたと分かった場合にどう是正するか(述語の再定義か、`区分8の要素なし`・要素認定の基準の変更か)の基準がこの票に書かれていない。これは §3 の「述語を足したり変えたりしたら全行に当て直す」で自動的にカバーされる想定か、それとも別途この票への追記が要るか。
>
> 以上6件。1・2 は commit c3defdb 自体が含む機械記録(TRACE)から直接読める事実で、今回の改訂プロセスそのものに関する指摘。3・5・6 は今回の直し(監査2回目の指摘3・2・5への対応)が新たに書いた文言の内部整合性についての指摘。4 は今回に限らず、区分8のこれまでの監査サイクル全体について記録の所在を問う指摘。
>
> 監査 2 回目の指摘 1・4 に対応する直し(`区分8の要素なし`の8列表記、追補の主語明記)は本文中で一貫しており、旧い定義の残存(削除漏れ)は見当たらなかった(P2 の型としては解消)。diff の削除行はすべて同義の書き換えで、内容が消えた箇所は無かった。

**リードの処置**:
1: **指摘のとおり、§0.1 の対応表を書き込みの前に一度も出していなかった**(起動の指示が「最初の一手は対応表を出す」と言っていたのに、設計票を先に書いた)。3 回目の出力を出した返答で対応表を出した。空の行は 2 つ(テーマの 8 区分 = リードの枠組みで L-381 で承認済み / 設計票の中身 = SURVEY.md §1 の固定手順として L-179 の例外で作り、起動の前にオーナーに見せる)。
2: 1・2 回目の出力は、それぞれ届いた返答の中で、次の直しより先に会話へ逐語で出していた。記録(`VERDICTS/`)には書いていなかった → この文書。
3: 追補 §1 の見出しに §11 を足し、§11 の報告の在処の読み替えを書いた。設計票 §2 の「1 回の実行」の行にも読み替えを書いた。
4: この文書。
5: `sync` は止まるだけでまとめない。名前だけが当たった別の道具は、リードが両方の出典を開いて別物と確かめ、`sync --new` で足し、理由を検収の記録に書く。過剰一致の代償は 1 回の目視で、見落とし(区分 1 の P5)より軽いので当たる側に倒す、と設計票 §5 に書いた。
6: 広すぎても列を消したり E1b・E3b を外したりはしない(外すと 1 回目の指摘 5 の狭める型に戻る)。直すなら狭い集合を引く列を**足す**。足すのは述語の変更なので、全行に当て直し、監査役に通し、状態板に書いてオーナーに見せる、と設計票 §3 に書いた。

**Jev の前段**(delegated-study §1.5、`jev_delegate.py plan --prompt <追補>`): 印 1 件(`not_bounded`、is_bounded 0.59)→ 追補に §7(完了の形)を足して打ち直し、印 1 件のまま(is_bounded 0.63)。**閉じない。**追補は単独の仕事ではなく、1 回ずつの起動文と組で使う。完了の形は起動文の側に書き、毎回の起動文にも同じ前段を当てる。
