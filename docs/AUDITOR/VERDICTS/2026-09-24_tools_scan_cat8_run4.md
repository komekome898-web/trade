# 検収: 道具サーベイ 区分 8 の 4 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 4 回目の実行(2026-09-24)`(この回の追記 589 行)/ 生ログ `docs/DATA/probes/20260923_tools_8_run4.log`(5594 行・184 手)。届いたままのコミット 3f0ac45。
起動文 `20260923_tools_survey_cat8_run4_prompt.md` の指紋 `636258a8e54c`、追補の指紋 `2c178ba75341`(L-507「**案A**」= 予算で止めない)。

## 0. 費用と時間

調査班 **621,458 トークン・3,177,992 ms(53.0 分)・道具 330 回**。1〜3 回目(23〜33 万トークン)の約 2 倍。生ログの最初の手 00:17:41Z / 最後の手 00:53:12Z(`cat8_ledger.py check` の参考の 1 行。35.5 分。残りの約 17 分は読む・報告を組み立てる・検査の時間)。**割り当てを途中で止めずに返した**(12 件すべてに手が付いた)。

## 1. 書き換えの範囲

報告に 589 行の追加・0 行の削除、新規の生ログ 1 本、TRACE だけ。`git diff -U0 HEAD~1 -- <報告> | grep -c '^-[^-]'` = 0。

## 2. 起動文の項目と報告の突き合わせ

| 起動文 §2 | 報告の状態 | リードの確かめ |
|---|---|---|
| 1 8-009 | 深掘り(§4.0 の表を新規、E2 を要素の名前で当て直して `なし`、合成データで最小実行) | — |
| 2 8-010 | 判別に一次資料が要る(本文は全文、コードは「upon acceptance」で未公開 = GitHub 404 を実測) | 試した手段が書かれている |
| 3 8-011 | 深掘り(本文 + `hdt/` のコード) | 抜き取り(§5) |
| 4 8-012 | 判別に一次資料が要る(E4 だけ未判別) | — |
| 5 8-013 | 判別に一次資料が要る(同定は確定 = `yasstake/rbot`。Rust の本体が未読で E2・E5 が未判別) | 3 回目の検収 §7 の 6 のとおり |
| 6 8-014 | 浅い(E1a〜E6 の未判別は解消。§4.0 の表の過半が `未確認` なので `深掘り` にしなかった) | 委任文 §4.0 の条件どおり |
| 7 8-015 | 深掘り | — |
| 8 8-016 | 判別に一次資料が要る(OSS の th2 の `th2-check2-recon` で E1a・E1b は印。他は未判別) | — |
| 9 8-003 | 深掘り。**E1a を なし → 印に訂正**(`validation.py`) | 抜き取り(§5)。訂正は原典どおり |
| 10 8-006 | 深掘り | — |
| 11 8-007 | 深掘り。**E1a を なし → 印に訂正**(Anti-Repainting の頁)。比較のページも開いた | — |
| 12 8-008 | 深掘り(ただし本文の取得に WebFetch = 問い 6) | 監査役の問い 2 に入れた |

## 3. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 4 本)0 件 / `check-elements --round 4` 0 件(候補の一覧 16 行・要素と段の表 128 行・知見の表 30 行・辿る一覧 0 行)/ `check "" <run4.log>` 0 件 / 消えた行 0。貼られた出力と一致。

## 4. 台帳

`cat8_ledger.py import <報告> --round 4`: 変わった所 71・台帳の検査 0 件。`recount`: 深掘り 9 / 浅い 1(8-014)/ 判別に一次資料が要る 4(8-010・8-012・8-013・8-016)/ 危険で導入停止 1 / 登録が要る 1 / 未着手 0。**残り 5 行**(3 回目の検収では 12 行)。

## 5. 抜き取り(原典で打ち直し)

- 8-003 E1a の訂正: `src/pm_bt/reporting/validation.py`(既定の枝 `master`、217 行)の 53 行 `def validate_run_directory(`・`tolerance: float = 1e-9`、`_assert_close(` が 110・116・122・128 行ほか。E2 の根拠 178 行 `is_sorted()`・183 行の価格帯の検査。**一致**。
- 8-011 E3b・E5: `hdt/validation.py` 49 行 `misaligned = [s for s, df in market_data.items() if len(df) != total_days]`、73 行 `fold_seed = None if seed is None else seed + fold`。**一致**。

## 6. Jev の検査(この回の節だけに当てた)

- `jev_survey.py verify`: 印 79 件、すべて `citation_corner_quote`(外部の資料からの鉤括弧の引用。道具は外の資料を開けないので、引用の真偽を確かめられない型。§5 で 2 件を原典で確かめて一致)。
- `jev_report_intake.py check --kind survey`: 印 18 件 = `unsourced_number` 17 件(候補の番号・星の数・頁数など。1・2 回目と同じ誤検出の型)、`prompt_vs_report` 1 件(`required_items_addressed` 0.67。起動文は 12 件すべてを `深掘り` までと書いたが、5 件が届かなかった。これは報告自身が書いている)。
- 区分の締めに `SURVEY.md` への追記案に入れる: Jev の検査は、外の資料の引用と候補の番号に印が偏り、この節の形では有効な印が少ない。

## 7. 調査班の問いへの答え(リードが決めるもの。監査役の答えで変わりうる)

1. Fincept の E1a の段: 未判別のまま。5 回目で、6 つのエンジンを切り替えた結果の突き合わせの記述を探す。
2. Fincept の E5(乱数を使わない解析解): E5 の述語は「同じ入力から同じ出力が出ることを**確かめるか固定する**機能」。作りが決定的なだけでは機能ではない。マニュアルに、再現を確かめる・固定すると書いた原文があるときだけ `印`。5 回目で当て直す。
3. Exactpro の範囲: 候補は「Exactpro の reconciliation testing」。公開の OSS(th2)を読み切り、商用の部分は委任文 §5-6(登録なしで取れるものを取り、オーナーが辿れる手順を書く)。
4. rusty-bot: Rust の本体(`rbot` の該当ファイル)を読む。5 回目。
5. VectorBT の E4: 8-009 と同じ答え(3 回目の検収 §7 の 1)。記録した足を時刻順に処理して戦略を再実行するなら当たる。5 回目で文書の原文で当てる。
6. WebFetch: 監査役の答えを待つ。リードの考えは「要約は一次資料ではない。この環境にあるブラウザー(Chromium)で頁を開き、本文を逐語で取り直す」。
7. E1a の誤判定が 3 件続いた: 1・2 回目は README だけで `なし` にしていた行がある(「`なし` と書くための条件」は 3 回目に入れた)。**1・2 回目に `なし` と書いた行は全部、条件 (a)(b)(文書とソースを全部読む・要素の名前で検索する)で当て直す**。5 回目の中心にする。
8. 「今の値で載せる」を優先したのは正しい(起動文の書き方の誤り = リード)。
9. 43.8MB は「数百 MB 以上」に当たらない。可。

## 8. 5 回目に回すもの

- 残り 5 行(8-010・8-012・8-013・8-014・8-016)。
- 1・2 回目に `なし` と書いた行の全部の当て直し(§7 の 7。8-001・8-002・8-004・8-005 を含む)。
- 辿る一覧(4 回続けて未着手)。成熟度の枠組みの原典。`akurkar07/OrderBook` の E6。
- 8-008 を一次資料で取り直す(§7 の 6。監査役の答え次第)。

## 9. 判定

**一部受け取り**(監査 21 回目で、`なし` の条件を満たさない行が 4 候補に見つかった。台帳で `未判別` に戻した。下の「監査 21 回目」)。ただし中身の回なので監査役(21 回目)に通し、その指摘で台帳を直すことがある。区分は完了していない(残り 5 行、辿る一覧が未着手、1・2 回目の `なし` の当て直し)。

## 監査 21 回目(中身の回。対象 = 3f0ac45 の 4 回目の節と生ログ)

監査役の出力(逐語。[止める] 6 件・[直す] 3 件・[聞く] 4 件):

> 1. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1053(知見23) / 要素と段 1102-1106 — `prediction-market-backtester` の E3a・E3b・E6「なし」の根拠は「tests/主要ファイルを全部読み」と書くが、生ログ(run4.log:3916-3954)ではファイル一覧取得の出力が755字で途中切断され(3943-3944行)、実際に検索した `tests/*.py` は7本のみ。リポジトリの `tests/` には `test_categories.py`・`test_cli.py`・`test_data_contracts.py`・`test_data_loaders.py`・`test_data_quality.py`・`test_execution.py`・`test_real_data_integration.py`・`test_reporting.py`・`test_scanner.py`・`test_smoke.py`・`test_strategies.py` など未検索のファイルが残る(`test_data_quality.py` はE2/E3a/E3bに直結しうる名前)。3回目起動文§2「なしと書くための条件」(a)「読んだ範囲=その候補の一次資料の全部の文書」を満たさないまま「確定」と書いている(P1・O-2)。
>
> 2. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1127 — `freqtrade` の E3b「なし(確定)」の根拠(run4.log:5370-5381)は、freqtradeの `docs/` 配下(全ファイル一覧は run4.log:4140 で取得済みだが読まれていない)のうち手で選んだ5本だけを検索した結果。根拠欄自身が「主要docs全体で検索」と書いており、"主要"(選抜)と"全体"(全部)が同じ文の中で矛盾している。条件(a)を満たしていない。
>
> 3. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1134 — `backtrex` の E3a「なし(確定)」の根拠が「Documentation全8頁のうちBacktesting系3頁+比較頁+ホームを読了」と、根拠欄自身が8頁中5頁しか読んでいないことを明記している。条件(a)を満たしていない。
>
> 4. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1065,1068,1069 — 1〜3の結果、8-003・8-006・8-007の状態は「深掘り(E1a〜E6に未判別なし)」と書かれているが、その根拠になった複数の「なし」自体が「なしと書くための条件」を満たしていない。設計票§2の「残り」の数え直し・区分完了の判定(§2の述語)に直接影響する。
>
> 5. [止める/聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1584(問い7) — 報告末尾の問い7自身が「READMEのみで『なし』と判定した過去の判定が繰り返し覆っている…同じ問題が起きていないか確認が要る」と、1〜4と同型の欠陥を報告班自身が察知していることを認めている。にもかかわらず該当行の値は既に「確定」「なし」と表に書かれ、状態も「深掘り」で締められている。問いを「決めずに」出す体裁を取りながら、実質の判定は先に確定させている(O-2、KA-10と同型)。
>
> 6. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1024,1059-1060,1141-1146,1583(問い6) — 8-008 `FX Replay` のE1a・E2・E3a・E3b・E5・E6の「なし確定」の根拠が、WebFetch(小型モデルが要約を返す道具。run4.log:5573-5591で「抽出結果(要約)」と明記)に基づいており、印はいずれも「一次資料」と書かれている。委任文§4.1(`docs/DATA/delegations/20260922_tools_survey_prompt.md:73`)は「検索結果の要約、第三者の記事、README の孫引きは一次資料ではない」と明記しており、WebFetchの出力はこの除外規定に当たる可能性が高い。報告自身が問い6でこれを疑問視しているが、表側の印は既に確定済みで、8-008全体の「深掘り」状態(line 1070)がこの根拠に依存している。
>
> 7. [直す] docs/DATA/probes/20260923_tools_8_run4.log:5573,5576,5579,5582 — WebFetchの手の見出しが `rc=NA time_s=NA`。追補§3の生ログ書式「`rc=<終了コード> time_s=<秒>`」は数値を想定した書式であり、字面上一致しない。`cat8_ledger.py check` は0件で通っているが、書式の規定そのものとは食い違う。
>
> 8. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1169,1177 — `arXiv:2512.12924` と `VectorBT` のE5を段5とした根拠が、いずれも乱数のseedパラメータの存在だけを設計票§4.1の(ア)「判定の基準(許容誤差・閾値・検出の条件)を使う人が指定できる」の充足として扱っている。seedは結果を固定する仕組みであり、(ア)が想定する許容誤差・閾値の指定とは性質が異なるように読めるが、報告はこの違いを問いとして出さず両道具とも段5で確定している。複数候補に共通して影響する解釈のため、個別確定でよいか。
>
> 9. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1582(問い5)、参考: 991(3回目節、nicferrariの同型問い) — VectorBTのE4(ベクトル化計算を「時刻順の再生」と呼べるか)が、nicferrari/backtesterでも同型の論点として既に問いになっている。同じ述語適用の揺れが複数候補で繰り返し「未判別のまま」保留されており、設計票§3「述語を足したり変えたりしたら全行に当て直す」の対象になりうる。リードの検収のみで個別に決めてよいか、監査役・オーナーに聞くべきか。
>
> 10. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1580(問い3) — 8-016 `Exactproのreconciliation testing` の範囲を、th2プラットフォームの一部(th2-check2-recon等)に絞るか、商用のShsha等も含めた候補全体として評価するかは、区分8の候補の切り方(族)に関わる。CLAUDE.md §0.2 A-10「族・スコープを説明なく縮める」の型に触れうるため、リード単独の判断でよいか。
>
> 11. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1585(問い8) — 4回目起動文§4の「候補の一覧と『要素と段』の表には8-001〜8-016の全行を今の値で載せる」という指示と、同じ文中の「(8-001〜8-008は台帳の値のまま写す)」という指示が、この回のまさに8-003・8-006・8-007・8-008を更新する§2の9〜12の指示と字面上矛盾している。リード自身が書いた起動文の不備であり、次回の起動文の文言を直すべき。
>
> 12. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1579(問い2) — Fincept Terminalの「Deterministic」なプライサー(乱数を使わない解析解)をE5の述語に当ててよいかは、E5述語(設計票§3)が例示する「乱数の種の固定」とは異なる性質の決定性であり、当てはめを広げると他候補への適用一貫性に影響しうる。監査役を通すべきではないか。
>
> 13. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:1069 と 1134 — 候補の一覧(1069)の8-007の状態説明は「E1aを訂正=なし→印、E3b・E4・E5を確定」とし、E3aの変更に触れていないが、要素と段の表(1134)ではE3aも「確定(未判別→なし)」と書かれている。変更点サマリと実表の変更箇所が食い違っており、検収・監査で変更点を見落とす経路になりうる(網羅性)。
>
> 補足: 上記1〜7は同一の構造的な穴(「なし」の確定条件を満たさないまま印・状態を確定させ、かつ問いとして自認しながら判定を先に固定する)に属します。8-003・8-006・8-007・8-008の4候補すべてに及んでおり、「残り」の再数え直しと区分8の完了判定の土台に関わるため、まとめて[止める]級として扱うべきものです。

**リードの処置**(報告は調査班の成果物なので書き換えない。直すのは台帳と次の起動文):
1〜6: 認める。リードの抜き取り(§5)は印の付いた行だけを見て、`なし` の行の読んだ範囲を確かめていなかった(検収の穴 = リード)。**台帳で戻した**: 8-003 の E3a・E3b・E6、8-006 の E3b、8-007 の E3a、8-008 の E1a・E2・E3a・E3b・E5・E6 を `未判別`(段も `未判別`)にし、4 行とも状態を `浅い` に。`cat8_ledger.py check` 0 件。残りは 5 → **9 行**(8-003・8-006・8-007・8-008・8-010・8-012・8-013・8-014・8-016)。5 回目で、条件 (a) の「全部」を機械で確かめる形にする(ファイル一覧を全件で取り、読んだファイルの数と一覧の数を並べて書かせる。一覧の出力を切らない)。WebFetch は一次資料に使わない(この環境の Chromium で頁を開いて本文を逐語で取る)。
7: 5 回目の起動文で、道具の呼び出し(WebFetch など)は使わないと書く(Chromium を `cat8_step.py` 経由で打てば rc と秒が入る)。
8: 設計票 §4.1 の (ア) は「判定の基準(許容誤差・閾値・検出の条件)」。乱数の種は結果を固定する仕組みで、(ア) には当たらない(設計票の逐語の読み = リード。述語は変えない)。**台帳で 8-011・8-012 の E5 の段を `未判別` に戻した**。5 回目で、E5 の段 5 の 4 行(8-002・8-003・8-011・8-012)を全部、(ア)(イ) の原文で当て直す。
9: 述語は変えない。3 回目の検収 §7 の 1 の読み(記録した足を時刻順に処理して戦略を再実行するなら当たる。速さ・粒度は問わない = 述語の括弧)を、E4 の全行に同じく当てる。5 回目で VectorBT は文書の原文で当てる。
10: 範囲は縮めない。候補は「Exactpro の reconciliation testing」の全体。公開の OSS(th2)を読み切り、商用の部分は委任文 §5-6。
11: 5 回目の起動文で直す(「候補の一覧と要素と段の表は、この回の値で 16 行全部」とだけ書く)。
12: §7 の 2 の答えのとおり(作りが決定的なだけでは E5 の機能ではない。確かめる・固定すると書いた原文があるときだけ印)。E5 の述語の逐語の読みで、述語は変えない。
