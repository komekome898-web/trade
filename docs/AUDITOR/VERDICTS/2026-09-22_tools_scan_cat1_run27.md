# 区分 1 の 27 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 27 回目の実行」節(500 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run27.log`
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲・受け入れ検査(機械)

`git diff --numstat` = **500 行追加 / 0 行削除**、塊は 1 つだけ。**過去の回の節は 1 行も触っていない。**
当方が全 26 本の生ログを渡して打った結果は 13 本すべて 0 件。**判定: 合。**

## 2. **リードの誤りの訂正 — 26 回目の検収が 2 か所で間違っていた**

### (a) 「高頻度・執行の経路は段 3・4・5」→ **執行だけである**

当方が 5 つの機関それぞれについて `MatchEngine` の当たりを数え直した:

```
CtaMocker    h=0 cpp=0
SelMocker    h=0 cpp=0
HftMocker    h=0 cpp=0
UftMocker    h=0 cpp=0
ExecMocker   h=2 cpp=0     ← ここだけ
```

**列の位置を追うのは執行の機関だけである。**高頻度は対手方の提示量で頭打ちにする段 3・4 で、注文の物に列の欄すら無い。

### (b) 機関は 4 つではなく **5 つ**

`CtaMocker` `SelMocker` `HftMocker` `UftMocker` `ExecMocker`。**当方は 26 回目の報告の「4 つ」をそのまま検収に書き写し、数えなかった。**

### (c) この 2 つは、当方がオーナーへ出した報告にも影響している

当方はオーナーに「**候補 57 `WonderTrader` が、自分の注文の列の位置を既定で追っています**」と書いた。**どの機関かを書かなかった。**
**正しくは「執行の機関(`ExecMocker`)が追っている」であり、足の経路では追っていない。**
当方の `engine.py` は足の検証なので、**比べる相手として並べるなら執行の機関のほうである**という但し書きが要る。**次の回のオーナー報告で訂正する。**

### (d) 逐語の写し誤りが 1 件

当方は 26 回目の検収で候補 91 の既定を `strength_bps: float = 0.0,`(末尾に読点つき)と引いたが、**原典に読点は無い。**
調査班が `grep -cF` で突き合わせて見つけた。**当方の引用が原典と違っていた。**

### (e) 原因 — **4 回目である**

(a) は「調査班の報告を数えずに書き写した」、そして **92 番の指定で当方はまた例示で書いた**(「注文の物を持つなら段 0、持たないなら該当なし」)。
**述語は当方が書いた「埋めるときに相場の値を 1 度も見ない」のほうなのに、結論を先に指定した。**
19・21・25(候補 33)・今回で **4 回**。**規則を書いたら、当てはめは測って出させる。結論を先に書かない。**

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| 57 番で `MatchEngine` を使うのは執行の機関だけ | §2(a) のとおり 5 機関を数えた | **当たり(当方の誤りが確定)** |
| 92 番が注文の物を持ち、値段は中値 + 半値幅 | `src/pm_bt/execution/simulator.py`(11,598 バイト)76 行 `class _PendingOrder:`、110 行 `self._pending: list[_PendingOrder] = []`、239 行 `half_spread = spread / 2.0`、241 行 `return _clamp_probability(mid_price + half_spread)` | **当たり。段 0 の定義に合わない** |

## 4. 調査班が挙げた 6 件の判断 — リードの回答

**(1) 候補 92 が段 0 の定義に合わない → 段 0 の定義のほうを直す。**
当方の定義(「相場の値を 1 度も見ない」)は**値段の作り方**を見ていたが、**この尺度が本当に測りたいのは「埋まるかどうかを相場に問うか」である。**
当方の `engine.py` の楽観と比べるのが目的なので、**「一度も埋まらないと言わない」ことが最も効く。**

> **段 0 = 相場に「埋まるか」を問わない(必ず埋まる)。値段の作り方は注記に書く。**
> 51 番 = 指定の値段と数量をそのまま成立させる。92 番 = 値段は中値 + 半値幅。**どちらも必ず埋まるので段 0。**

これで尺度が「相場の情報をどれだけ使って埋まりを決めるか」で単調になる。**新しい段は作らない。**

**(2) 6 番・18 番の足の時計を段 3 に当てるか → 当てない。値を変えなかった判断が正しい。**
**足の時計が足を配ることは段 3 ではない。**これを段 3 にすると、足を回す機関は全部当たって段 3 が空になる。
→ **段 3 の定義に 1 行足す**: 「**埋まりを決める到着が事象になっていること。足の時計が足を配るのは段 3 ではない。**」

**(3) 候補 97 の配布物に在る指示の文章 → 数えること。ただし種別を分ける。**
「作業者に指示を出しうる経路」という危険の筋は同じである。**手引きの形をしていないから数えない、では §6-3 の趣旨が漏れる。**
**通算を 2 段にする**: 作業者向けの手引きの同梱 **6 件**(44・74・120・41・58・19)/ 指示の文章の同梱 **1 件**(97)/ 外部送信の口 **1 件**(51)。**合計 8 件。**

**(4) 26 回目の検収との食い違い 2 件 → §2 のとおり、当方が誤っていた。調査班の測り直しが正しい。**

**(5) 段 3 を 4 件に新しく足したこと → よい。「読んでいない 14 件は決めない」も正しい。**
**否定に倒していない**のが §0.2 O-2 のとおり。読まずに「当たらない」と書いていたら差し戻していた。

**(6) 分布の表で 57 番を段 5 の行に入れたこと → よい。ただし丸めたことが見える形にすること。**
**分布の行に「機関ごとの表を見よ」の参照を付ける。**最も高い段で数えるのは構わないが、**丸めた事実が表の中から読めないといけない。**

## 5. 安全側

導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。
**逐語の突き合わせで 3 件の食い違いを自分で見つけて原典から直し、うち 1 件は当方の引用の誤りだった。**
24 回目が引いた候補 15 の語がどの生ログにも無かったので原典を取り直して自分で打ち、**0 件を再現**している。
**「前の回が書いたことを、証拠が無ければ自分で測り直す」が働いている。**

## 6. 判定

**受け取る。**検査 13 本が 0 件、書き換えは追記のみ、抜き取りは 2 件とも原典に当たった。
**当方の誤りが 4 件確定した(§2)。うち 1 件はオーナーへの報告にも出ており、次の回で訂正する。差し戻す点は無い。**

## 7. 28 回目の起動の指定 — **監査役にかける直前の最後の整え**

1. **段 0 の定義を §4(1) のとおり直し、51・92 を注記つきで段 0 に置く。**
2. **段 3 の定義に §4(2) の 1 行を足す**(足の時計は段 3 ではない)。
3. **安全側の通算を §4(3) の 2 段に直す**(手引き 6 / 指示の文章 1 / 外部送信 1 = 8 件)。
4. **分布の行に「機関ごとの表を見よ」の参照を付ける**(§4(6))。
5. **57 番の記述に「列を追うのは執行の機関だけ」を明記する**(§2(a)(c))。当方の `engine.py` は足の検証なので、
   **比べる相手として並べるなら執行の機関のほうである**という但し書きも書く。
6. **区分 1 の要約を 1 節にまとめる**(監査役に渡す入口になる)。含めるもの:
   - 完了の形と、それに対する現在地(未到達 0 件 / 別立て 4 件の理由)
   - 段の表(機構 / 既定、分布、当方の位置)
   - 「持っていると書いてあるのに効いていない」3 つの形と該当件数
   - 指値の埋まり方の模型の対比(95 / 57 / 104 の効く時点の違いと、当方がどこに居るか)
   - 安全側の通算 8 件
   - 登録の付け替え 3 件
7. **新しい検索計画は打たない。候補も増やさない。構築・導入・最小実行もしない。**
8. 上の 1〜6 に要る範囲でのみ既存の節への書き換えを許す。**追記だけで果たせるならそのほうがよい。**
