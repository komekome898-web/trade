# 区分 1 の 22 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 22 回目の実行」節(225 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run22.log`(727 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲(機械)

`git diff --numstat` = **235 行追加 / 4 行削除**。削除 4 行はすべて **21 回目の節の中**で、指定 2・6・7 の分ちょうど
(`Candle` の逐語 / `PublicTrade` の逐語 / 候補 123 の「粒度は未確認」/ 候補 68 の根拠の行)。
**1・2 回目の節にも 13〜20 回目の節にも削除は 1 行も掛かっていない。判定: 合。**

## 2. 受け入れ検査(機械)

生ログ 21 本を全部渡して 13 本すべて 0 件。本数も `ls … | wc -l` の出力つきで実物と一致。**判定: 合。**

## 3. 数え直しの検算(リードが報告書から自分で引き当てた)

**残り 11 件(51・53・54・57・60・67・70・72・87・91・92)を 1 件ずつ引き当てた。**
11 件とも最後の出現は **14 回目の一覧(4634〜4680 行)の「説明は『…』」の 1 行**で、39 番の一覧から取った説明しか無い。
**一次資料に到達していない = 残り、で正しい。11 件とも当たり。**

**「浅い」以上を到達済みとする定義も、オーナーの逐語に合っている**(逐語「**一次資料に到達した候補が尽きること**」であって、深掘りが尽きることではない)。
**数え方の是正そのものは合。**

## 4. **ただし、この数では完了を宣言できない。射程に穴がある(調査班の問い 3 が正しい)**

調査班の指摘を当方で引き当てた。**候補 1〜23 は「N 回目に深掘り済み。この回では何も足していない。」と書かれているだけで、6 要素の印が 1 つも無い**
(14 回目の一覧 4600〜4612 行。`hftbacktest`・`NautilusTrader`・`Jesse`・`VnPy`・`Qlib`・`Lean CLI`・`zipline-reloaded`・`mlflow` などがこの範囲)。
**6 要素の印は 14 回目に始まった仕組みで、それ以前に片付いた候補には遡って付いていない。**

オーナーの完了の形は**要素ごと**である(逐語「**6 要素…それぞれについて、一次資料に到達した候補が尽きること**」)。
**候補 1〜23 は一次資料に到達している。到達しているのに、どの要素に属するかが書かれていない。**
だから要素ごとに「尽きた」と言うことが、今の一覧では**できない。**

**→ 区分 1 の残りは「11 件」ではない。正しくは次の 2 つである:**

| 残りの種類 | 件数 | 中身 |
|---|---|---|
| **一次資料に未到達** | **11 件** | 51・53・54・57・60・67・70・72・87・91・92(全部 `区分1-足` の未着手) |
| **到達済みだが 6 要素の印が無い** | **23 件** | 候補 1〜23(1・2・3・8 回目に深掘り済み) |

**これを「11 件」とだけ報告していたら、深掘りまで済んだ 23 件を数から落としたまま完了を宣言することになっていた。**
**これは範囲を黙って縮めること(§0.2 A-10)であり、調査班が問いとして出したのは正しい。当方はここで止めずに、23 件を埋める作業を指定する。**

なお 23 件は**新しい調査ではない。**1・2・3・8 回目の節に一次資料の逐語が既にあるので、**それを読んで印を当てるだけ**である。
足りない要素があれば、その要素だけ原典を読み足す。

## 5. 調査班が挙げた 6 件の判断 — リードの回答

**(1) 候補 107 の `区分1-市場影響と約定の模型` を付けなかったこと → 正しい。規則どおりである。**
21 回目 §4(2) で決めた「**印は、検証の機関が実際に受け取る / 使うもので決める。持っているだけでは付けない**」の当て方として一貫している。
**ただしこの発見自体は所見として残すこと**: 「`ImpactModel { None, Linear, SquareRoot, AlmgrenChriss }` を持っているのに、
検証の実行経路は `period_return -= turnover * self.cost_bps / 10000.0;` しか差し引かない(`backtest.rs` から `CostModel` の呼び出し 0 件)」。
**これは当方の `engine.py:45-53` が自ら『楽観的』と書いている型と同じである** — 道具が持っている機構と、検証が実際に通す経路が違う。**使う段で効く。**

**(2) 候補 123 に `区分1-イベント駆動` を付けるか → 付けること。**
(1) と同じ規則を同じ向きに当てる。**実装が事象で駆動されているかで決める。原典が自分で "event driven" と名乗っているかは条件にしない。**
`eventBus.on('trade', queueTrade)` と `async.queue` は実装そのもので、根拠として十分である。
(自称を条件にすると、同じ実装でも言葉遣いで印が変わる = §0.2 O-7 の型になる。)

**(3) 総数 62 件の射程 → §4 のとおり。候補 1〜23 に印を当てる作業を 23 回目の本体にする。**

**(4) 20 回目の集計の落ち(候補 41 の `区分1-ティック`)を直すか → 直さない。20 回目の節はそのままにする。**
**過去の回の節はその時点の記録であり、後から書き換えると記録でなくなる。**22 回目の数え直しが正しい数を出し、
**食い違いの在処(6106 行と 6133 行)まで書いてある**ので、読む側は追える。**この扱いが正しい。**

**(5) 候補 61 の「無い」の射程 → 射程を主張の中に書くこと。`barter` 側も見ること。**
CLAUDE.md §5.2「**棄却には射程を書く。何を測ったかと、何を測っていないかを併記する**」に当たる。
今は射程が**問いの側にだけ**書かれていて、節の主張の側には無い。**主張の行に「射程 = `barter-execution/src/exchange/mock/` の 2 本。`barter` 側は未読」と書く。**
そのうえで 23 回目に `barter` 側に別の約定の模型が在るかを見て、無ければ射程を広げて書き直す。

**(6) 候補 107 に `区分4 へ` を足したこと → 印は残してよいが `推定` と明記すること。**
根拠が `walk_forward.rs` という**ファイル名だけ**で、中身は未読と自分で書いている。
**ファイル名だけを根拠にした仕分けは `推定` である**(§0.2 A-11「目視・ぱっと見で判定する」の隣)。中身を読んだら `実測` か `一次資料` に上げる。

## 6. 安全側の所見

作業者向けの手引きの同梱は zenbot・sigc とも 0 件で、通算 5 件(44・74・120・41・58)のまま。
**sigc の README と `install.sh` は導入手順を書いているが、読んだだけで従っていない**と明記されており、扱いは正しい。
`ungh.cc` への 1 本目が TLS 切断(`rc=35`)で落ちたあと待って打ち直して通している(`:31` 失敗 → `:33` 成功)。**000 や切断を 404 と同じに扱っていない。**
この回に導入・構築・最小実行・登録・鍵の発行・購入・発注は 1 件も無い。

## 7. 判定

**受け取る。**検査 13 本が 0 件、書き換えは指定の範囲ちょうど、残り 11 件は 1 件ずつ引き当てて全部当たった。
**数え方の是正は正しく、かつ、その是正が「射程に穴がある」という更に重い発見を連れてきた(§4)。差し戻す点は無い。**

## 8. 23 回目の起動の指定

**この回の本体は「候補 1〜23 に 6 要素の印を当てる」である。**これが済むまで区分 1 の完了は宣言しない。

1. **最優先: 候補 1〜23 に 6 要素の印を当てる。**
   **新しい調査ではない。**1・2・3・8 回目の節に既にある一次資料の逐語を読んで印を当てる。
   **根拠として、どの節のどの行の逐語で当てたかを必ず書く。**
   逐語が足りずに決まらない要素があれば、**その要素についてだけ**原典を読み足す(`ungh.cc` の files で在処を確かめてから取る)。
   それでも決まらなければ**「何を読んで決まらなかったか」を書く**(「道具に無い」とは書かない = §0.2 O-2)。
2. **候補 123 に `区分1-イベント駆動` を足す**(§5(2))。根拠は `eventBus.on('trade', queueTrade)` と `async.queue` の実装の逐語。
3. **候補 107 の所見を書く**(§5(1))。模型を持っているのに検証の経路が呼んでいないこと。**印は変えない。**
4. **候補 107 の `区分4 へ` に `推定` と明記する**(§5(6))。
5. **候補 61 の「市場影響と約定の模型は無い」の主張の行に射程を書く**(§5(5))。あわせて `barter` 側に別の約定の模型が在るかを見る。
6. **数え直しを §4 の 2 段で出し直す**(未到達 11 件 / 到達済みで印が無い 23 件 → この回で何件埋まったか)。
7. 上の 1〜6 に要る範囲でのみ既存の節への書き換えを許す。**それ以外は 1 文字も触らない。**`git diff` の削除行を自分で確かめて返す。
8. **新しい検索計画は打たない。深掘りもしない。候補も増やさない。**
9. 逐語は内側の型や記号まで原典どおり。生ログの本数は `ls … | wc -l` の出力を添える。
