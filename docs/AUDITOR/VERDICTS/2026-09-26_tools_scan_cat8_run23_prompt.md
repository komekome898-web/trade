# 監査: 道具サーベイ 区分 8 の 23 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run23_prompt.md`(8-030 dbt の E1b・E4 だけの回。22 回目の起動文から作った)。

## 監査 106 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run23_prompt.md@b5e1186c4b13`、コミット 7129d52)の逐語

> 監査 106 回目の結果(対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run23_prompt.md`、指紋 b5e1186c4b13、コミット 7129d52)を以下に示す。参照した他ファイル: `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md`、`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run22.md`、`docs/DATA/surveys/CAT8_DESIGN.md`、`docs/AUDITOR/PRINCIPLES.md`、`docs/AUDITOR/KNOWN_ANSWERS.md`。
>
> 1. [止める] `run23_prompt.md:25` — ソースの探索範囲を「dbt 本体の 2 つの枝(`main` と `1.latest`)」に限り、`dbt-duckdb`(アダプタ)を明示的に除いている。しかし L-508 の逐語(`run23_prompt.md:30` が引用)は「候補が**同梱・呼び出している**別のライブラリの機能は数える」であり、`dbt-duckdb` は `dbt-utils`・`dbt-audit-helper` のように利用者が任意に追加するものではなく、dbt を動かすために必須で dbt-core が実行時に呼び出すアダプタである(22 回目の §6-1 検査でも「入れた 2 つの配布物」として dbt-core と並べて扱われている)。E4(リプレイ・microbatch)や E1b(計算)に関わる機能がアダプタ側の実装・マクロにある可能性を排除できないまま「dbt 本体だけ」と線引きしており、除外の理由が本文に書かれていない。このまま進めると「探し尽くした」とする範囲がそもそも該当ライブラリを欠いたまま `未判別`(または `印`)に至る恐れがある(design票 §3「述語を足したり変えたりしたら、全行に当て直す」とも関わる射程の線引き)。
>
> 2. [直す] `run23_prompt.md:25` — 「`venvs/8-030/` の下の dbt 本体の 2 つの枝(`main` と `1.latest`)」という環境の事実が、22 回目の起動文にも 22 回目の検収にも見当たらない(grep で確認: 両ファイルに `1.latest` の語なし)。21 回目の生ログに根拠があるなら、他の箇所(26 行目の見積もり行の引用など)のように `docs/DATA/probes/20260923_tools_8_run21.log:NN` の形で行を引くべきだが、この主張だけ引用が無い。CLAUDE.md §3 の「機構やデータの挙動を主張するときは、実行したコマンドと出力を同じ返答に出す」(O-3 由来)に照らすと、調査班がこの前提のまま実行して枝が存在しなければ、この回の最初の手で詰まる。
>
> 3. [直す] `run23_prompt.md:24` / 依頼文の背景説明 — 「839 はリードが `find ... -name '*.md*' | wc -l` で数えた数」とあるが、`-name '*.md*'` は `-type f` の指定が無ければディレクトリや `.md` 以外の付随ファイルを含みうる。調査班が自分で数え直す(24 行目「件数を出す」)ため実害は限定的だが、範囲の基準として本文に掲げる数値である以上、算出コマンドと `-type f` の有無をこの回の生ログでも確認させる一文が無い。
>
> 4. [直す] `run23_prompt.md:55-63`(§2.4) — この回は「全件の検索(`cat8_search.py`)と見積もりの手は…打たない」(34 行目)と明記しているのに、§2.4 はファイルの表(甲)・当たりの判定・`なし` の根拠の書き方を、この回で使う手順であるかのように無条件に置いたままである。§2.5(§4.0 の表)には「この回は書かない」という明示の上書きがあるのに、今回同じく使わない §2.4 にはその注記が無い。写し残しで、調査班が「今回も §2.4 の手順を回さなければならないのか」と誤読する余地がある。
>
> 5. [聞く] `run23_prompt.md:28` — 「dbt 自身が中身を持つ計算(組み込みの関数・マクロ・集計)で、約定・損益・指標・特徴量・板の再構成と同じ種類の出力を出すものがあれば `印`」という一文は、22 回目の検収の処置 2 にも設計票 §3 の E1b 逐語にも無い、今回新たに書き下ろした肯定側の当て方に見える。処置 2 が答えた範囲は「利用者が書く SQL は数えない」という除外側の読みだけで、「dbt 本体の何が印になりうるか」という積極側の基準はここで初めて具体化されている。これは述語の新しい敷衍か、単なる言い換えか。新しい敷衍なら設計票 §3 の「述語を足したり変えたりしたら、全行に当て直す」との関係を確認したか。
>
> 6. [聞く] `run23_prompt.md:22-26` — 「探し尽くす」の範囲決定を「リードの決定」と明記しているのは適切だが、②(題を全部読んで関連しそうな頁だけ本文を読む)という絞り込み方式は、機能に触れていても表題・見出しに現れない頁を読み飛ばすリスクを内包する。結果はいずれも `印` か `未判別`(`なし` にはならない)なので偽陰性そのものではないが、`未判別` に添える「探した範囲」が実質を反映しているかは、この方式自体の限界として本文に一言も書かれていない。
>
> 7. [聞く] `run23_prompt.md:65-67`(§2.5)/ §4・§7 — 22 回目までは毎回 §4.0 の表を(部分的にでも)更新してきたが、この回は初めて「書かない(22 回目の値のまま)」という空の節にする。`scripts/check_scan_report.py` や `cat8_ledger.py check-elements` がこの形(ある回の節に §4.0 の実データが一切無い)を通すことは過去に実測されているか。22 回目の検収の「check-elements 0 件」「check 0 件」は §4.0 の表がある回の話であり、この「空節」パターンでの実測ではない。
>
> 8. [聞く] `run23_prompt.md:15` — 「22 回目の検収 §4 の処置 1・2(22 回目の E1b・E4 の `印` を受け取らなかった理由)」という要約は、処置1が E4 のみ、E1b の不受理理由は処置2 に含まれる、という構成を1文にまとめたもの。読み違いではないが、調査班が処置1単独を「E1bにも当たる」と早合点しないよう、この要約の書き方でよいか。
>
> 一方、確認して問題が無かった点: (a) §7 の受け入れ検査 4 項目(`run1〜run23` の 23 本、`--round 23`、`run23.log` 参照、「1〜22 回目の節は書き換えない」)は回の番号・生ログ名と矛盾なく更新されている。(b) 22 回目に集中していた §6-1(導入前検査)関連の手順文は今回の範囲に不要な部分が正しく落ちている(残存無し)。(c) E1b・E4 の除外側の当て方(利用者コードは数えない/独立クエリは時刻順でない)は 22 回目検収の処置 1・2 の論旨とおおむね一致し、明白な広げすぎ・狭めすぎの記述は無かった。
>
> 止める指摘: あり(1件)。

## 処置(リード、監査 106 回目)

1. **直した**。ソースの範囲に、dbt が実行時に呼び出すアダプタの層(21 回目に入れた環境の `site-packages/dbt/adapters/` の下の `base/` などの共通の層と `duckdb/`)を足した。dbt はアダプタ無しでは動かず dbt-core が呼び出すので、L-508「同梱・呼び出している」に当たる。利用者が `packages.yml` に書いて入れるパッケージ(dbt-utils など)とは違う、と本文に書いた。
2. **直した**。2 つの枝の在処を `venvs/8-030/dbt-core/`(`main`)と `venvs/8-030/dbt-1latest/`(`1.latest`)と書いた。リードが `git rev-parse --abbrev-ref HEAD` で確かめた(出力: `dbt-core` → `main`、`dbt-1latest` → `1.latest`)。調査班にも同じ手を打たせる。
3. **直した**。`find ... -type f \( -name '*.md' -o -name '*.mdx' \) | wc -l` で数え直した(493・346、変わらず)。そのコマンドを本文に書き、調査班にも打たせる。
4. **直した**。§2.4 の頭に「この回は使わない」と書いた。
5. **答える・直した**。述語の「計算を持つ」の言い換えで、新しい基準ではない。本文に「述語の言い換えで、新しい基準ではない」と書いた。
6. **直した**。範囲の限界(題に出ない機能の記述は読み落としうる。結果は `印` か `未判別` で `なし` にしない)を、知見の表の「読んだ範囲」の行に 1 文で書かせる。
7. **答える・直した**。`check_scan_report.py` は §4.0 の表を持たない節を表の検査から外す作り(`scripts/check_scan_report.py` 465〜477 行のコメントとコード)。`check-elements` での実測は無い(未確認)。止まったら、止まった出力を貼って返答に書かせる(表を写して通さない)。
8. **直した**。「処置 1 = E4、処置 2 = E1b と E4」と書いた。

直したあとの起動文の印: `20260923_tools_survey_cat8_run23_prompt.md@da6e6c5e5e8b`。止める指摘(1)の直しを監査 107 回目に掛ける。

## 監査 107 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run23_prompt.md@da6e6c5e5e8b`、コミット 3f51c42)の逐語

> 監査 107 回目(対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run23_prompt.md`、指紋 da6e6c5e5e8b、コミット 3f51c42)。参照: `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run22.md`、`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run23_prompt.md`(106 回目)、`docs/DATA/surveys/CAT8_DESIGN.md`、`scripts/check_scan_report.py`、`scripts/cat8_ledger.py`、`git diff 7129d52 3f51c42`、scratchpad 内の実物(`venvs/8-030/dbt-core`・`dbt-1latest`・`exec_venv`)。
>
> 1. [止める] `run23_prompt.md:25` — 「ソース: `dbt-core/`(枝 `main`)と `dbt-1latest/`(枝 `1.latest`)の2つ」と「dbt が実行時に呼び出すアダプタの層」を並べて 1 つの探索範囲として書いているが、実物を確認すると `dbt-core`(枝 `main`)は README 冒頭に **「dbt v1 development has moved to the 1.latest branch. main branch now contains all the source code of dbt v2.0 — a ground-up rewrite of dbt in Rust... no Python runtime or dependency management required」** と明記された、Python 版 dbt とは別物の Rust 書き直しである(実測: `dbt-core/Cargo.toml`・`crates/dbt-adapter-core`・`crates/dbt-adapter-engine` 等が存在、Python の `dbt/adapters/` は無い)。一方 `1.latest` 枝(Python v1)には `dbt/adapters/` の実装コード自体が無い(実測: `find . -iname '*adapter*'` は `scripts/migrate-adapters.py` と `docs/arch/5_Adapter.md` のみ)。実装は別リポジトリ `dbt-labs/dbt-adapters` に分離されており、今回追加した「アダプタの層」は `.git` の無い exec_venv の pip インストール物(`dbt_core==1.12.5`・`dbt_adapters==1.24.5`)で、`main`(v2.0)にも `1.latest`(`1.14.0a1` 実測)にもバージョンが対応しない第三の来歴である。「2 つの枝 + アダプタの層」を並列の探索範囲として書くと、`main` 枝自身が持つ Rust 版アダプタ相当(`dbt-adapter-core` 等のクレート)が読む範囲から抜け落ちたまま「探し尽くした」ことになる。これは L-508「同梱・呼び出している」の対象を正しく充てているか(P1・P3)。
>
> 2. [止める] `run23_prompt.md:70`(§2.5) — 「この回は §4.0 の表を書かない」を実際に `check_scan_report.py` に通した場合の効き方を実測した(scratchpad に最小再現を作成)。`scope()` は §4.0 形式(5 列・2 列目が語彙)の表を 1 行も持たない回の節を **K1・K2・K4〜K11・K13 の全部から丸ごと除外**する(`scripts/check_scan_report.py:453-477` のコメントは「K4・K5・K9・K13 だけ」と書くが、実際は `lines` を共有する 11 検査中 9 個に及ぶ)。実測: 同じ「知見」表に存在しない生ログ行番号(`fake_dummy.log:9999`、実ファイルは 1 行)を引いた行を作り、§4.0 表**あり**の回では K11 が `根拠の行が生ログに存在しない` で 1 件捕まえ、§4.0 表**なし**(今回と同じ形)の回では **0 件で通過**した。K11 はまさに 2026-09-22 に「知見表に一度も届いていなかった」ことを直した検査であり、今回の §4.0 省略はその穴を回 23 だけ再現する。処置 7 は「検査が止まったら出力を貼る」対策のみで、**検査が黙って通ってしまう**側のリスク(実測どおり)には触れていない。`cat8_ledger.py check-elements` は知見表の行番号の実在チェックを持たない(`quotes_in_log` は引用「…」がある行だけを見る)ので代替にもならない。この回の知見表(読んだ範囲・当たりの行数の実測根拠)は K11 の対象そのものなのに、機械検査が外れたまま出してよいか(P3・P8)。
>
> 3. [直す] `run23_prompt.md:25`・`:43` — 処置 1 の根拠は L-508/L-509 の承認 4 項目のうち「8-014 に同梱の skfolio・scipy の機能を Fincept の機能に数える」(配布物に**同梱**されたライブラリの例)。今回の「dbt が実行時に呼び出す**必須のランタイムプラグイン**」への敷衍は、43 行目の既存の要約文「候補が同梱・呼び出している別のライブラリの機能は候補の機能に数える」を根拠にしているが、この要約文自体が L-509 の 4 項目のどれとも一致しない(4 項目はいずれも「同梱」型か個別の手続きの数え方で、「実行時必須の外部プラグイン」型の例は無い)。この敷衍はオーナーに一度も見せていない新しい読みか、それとも既存の読みの範囲内か。
>
> 4. [聞く] `run23_prompt.md:29` — 処置 5 で追加した「(述語の言い換えで、新しい基準ではない)」は主張のみで、設計票 §3(`CAT8_DESIGN.md:53`)の E1b 逐語には「組み込みの関数・マクロ・集計」も「アダプタの層を含む」も無いことを確認した(実測)。設計票 §3 自身(`CAT8_DESIGN.md:65`)は「足すのは述語の変更なので、§3 の規則どおり**全行に当て直し**、監査役に通し、状態板の区分 8 の欄に書いてオーナーに見せる」と定めている。今回の一文が敷衍であるかどうかの判定を、当て直しをせずに「新しくない」と断定してよいか。
>
> 5. [直す] `scripts/check_scan_report.py:465-477` — コメントが実際のコードの影響範囲(K1・K2・K6・K7・K8・K9・K10・K11・K13 の 9 検査、K3・K12 のみ対象外)より狭く書かれている(「K4・K5・K9・K13」の 4 個のみと記載)。処置 7 がこのコメントだけを根拠に「未確認」で済ませているなら、実際の影響の大きさを過小に見積もっている。
>
> 6. [直す] `run23_prompt.md:25` — 追加した「アダプタの層」の在処(exec_venv、21 回目に pip で入れたスナップショット)には `.git` が無くバージョン(`dbt_core==1.12.5`/`dbt_adapters==1.24.5`)は `main`(v2.0、無版数の書き直し)にも `1.latest`(`1.14.0a1` 実測)にも対応しない。「2 つの枝(`git rev-parse` で確かめた)+ アダプタの層」という並記は、由来の異なる 3 つの資材(git 追跡された枝 2 つ・追跡されない pip インストール物 1 つ)を同格に並べているように読めるが、この非対称性を本文に書かなくてよいか。
>
> 止める指摘: あり(2件、項目 1・2)。

## 処置(リード、監査 107 回目)

- **リードの誤り(指摘 3 で分かった。10 回目から続いていた)**: 10〜22 回目の起動文(`docs/DATA/delegations/` の 13 本、ほかに検収 2 本と報告 1 か所)に「候補が同梱・呼び出している別のライブラリの機能は数える(L-508「1〜4全て承認します」・L-509「b」)」と書いてきた。オーナーが承認した問いの文は「8-014 に**同梱**の `skfolio`・`scipy` の機能を Fincept の機能に数える」(`OWNER_LOG` L-509 の対応列)で、「呼び出している」はリードが足した語。**リードが導いた読みを、オーナーの決定の出典を付けて書いた**(§0.2 の B の型。21 回目の検収 §4 のリードの誤り 2 と同じ型が、それより前から続いていた)。106 回目の処置 1(アダプタを「呼び出している」で数える)も、この誤った語に乗っていた。**これまでの判定で「呼び出している」だけを理由に数えた機能があるか**は、この回では調べていない(未確認)。資源化の前の全体受け取り監査で、10 回目以降の `印` のうち別の配布物の機能を根拠にしたものを洗う。
1. **直した**。範囲を「dbt の公式のリポジトリの 2 つの枝」だけにした。`main` は v2.0 の Rust の書き直しで、アダプタの層のクレート(`crates/dbt-adapter-*`)も同じリポジトリの中にあるので、dbt 本体として読む。v1 のアダプタ(`dbt-adapters`・`dbt-duckdb`)は別の配布物で別のリポジトリにあり、承認は「同梱」だけなので読まない。読まなかったことを案 B の「読んだ範囲」に書かせる(結果は `印` か `未判別` で、`なし` にはしない)。
2. **直した**。§4.0 の表の 43 行を 22 回目の値のまま、この回の節にも載せさせる(表が無い節は K11 などの検査から外れるため。実測は監査 107 回目)。値は変えない。
3. **直した**。§2.1 の「オーナーが決めた述語の読み」の 1 つ目を、承認された問いの文(同梱)に直した。23 回目の起動文から「呼び出している」の語は 0 件(`grep -c` → 0)。
4. **直した**。E1b の当て方の文を、述語の逐語だけにした(「組み込みの関数・マクロ・集計」「アダプタの層を含む」を消した)。
5. **答える**。`scripts/check_scan_report.py` は区分 8 の会話では書き換えない決まり(区分 1 の資源)。コメントと実際の影響範囲の食い違いは、監査 107 回目の逐語としてここに残し、区分 1 の会話への申し送りにする。
6. **直した**。v1 のアダプタは pip で入れた環境の中の別の配布物で、git の枝ではないと書いた(処置 1 のとおり読まない)。

直したあとの起動文の印: `20260923_tools_survey_cat8_run23_prompt.md@ef8ed55a1a76`。止める指摘の直しを監査 108 回目に掛ける。
