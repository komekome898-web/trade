# 監査(owner-auditor)— SIGNAL 探索段 2 の設計(2026-09-19)

対象: `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE2_DESIGN_2026-09-19.md`(コミット 3d0e9bd)。

## 監査役の返答(1 回目、逐語)

対象: `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE2_DESIGN_2026-09-19.md`(コミット 3d0e9bd)

## (2) 反証者の致命3件・直すべき10件のこの設計での扱い(番号対応)

| REFUTER_REVIEW の項目 | この設計での扱い | 該当箇所 |
|---|---|---|
| 致命1(起点の非対称) | 扱っている(E1・E3) | EXPLORE2 行4, 34-40 |
| 致命2(差のh形状が対照だけ) | 扱っている(E2) | EXPLORE2 行39 |
| 致命3(gapで1.6〜2.1倍) | 扱っている(E3、ただし固定は次単位へ先送り) | EXPLORE2 行40, 152 |
| 直すべき#1(清算21,198≠対照18,199、付かない2,999件) | **扱っていない** | — |
| 直すべき#2(SE naive→日クラスタで2倍前後) | 扱っている | EXPLORE2 行149 |
| 直すべき#3(対照(i)を表に併記) | 扱っている | EXPLORE2 行151 |
| 直すべき#4(bundle_width_ms Q1 = bundle_n_events_dedup Q1、10属性中実質9) | **扱っていない**(E4は3属性のみで重複問題自体を回避しているが、その理由は書かれていない) | EXPLORE2 行41 |
| 直すべき#5(反転割合のタイの非対称→r<0/r=0/r>0) | 扱っている | EXPLORE2 行150 |
| 直すべき#6(期間の穴、2024-02-15〜02-26 連続12日) | **扱っていない** | — |
| 直すべき#7(doi_pre_1h/implied_leverageのmetrics欠測による日の偏り) | **扱っていない**(E4で「換算レバレッジ」をそのまま使用) | EXPLORE2 行41 |
| 直すべき#8(清算行が正確に2.0005倍、出所未確認) | **扱っていない** | — |
| 直すべき#9(552行/180比較を次単位に引き継ぐ) | 扱っている | EXPLORE2 行153 |
| 直すべき#10(bf_bp_reactdirが既に部分的な答え、主表に併記) | 扱っている(S-6、印△) | EXPLORE2 行27 |

## 指摘

1. [直す] 行39, 48, 133, 141 — 致命1を分離する主機構(Δを清算・対照の両方に同じく当てて起点をそろえる)の検証手段として、`anchor_lag_ms` の分布をΔごとに出す出力計画が本文のどこにもない。§6 の出力一覧(E1〜E4、行141)に `anchor_lag_ms` の分布は含まれていない。清算直後は約定密度が高いため、Δ≥1秒でも清算側・対照側で「Δ秒以後の最初の約定」の実際のlag分布が揃うとは限らない(反証者の実測: 清算lag0ms 79.5% / 対照0.2%)。この検証が無いまま「Δで非対称が解消される」という前提でE1〜E4を読むことになる(P3・P6)。

2. [聞く] 行48, 119, 133 — `jev_design.py rank-covariates` は12変数すべてを「合わせなくてよい」と判定しているが、リードは1位の`anchor_lag_ms`と10位の`day_direction_bp`の2件だけを反証者の実測を根拠に特別扱いし、残り10件はJevの判定どおり受け入れている。この2件を選んだ基準(なぜ他の10件は実測との突き合わせをしないのか)が書かれていない。`docs/JEV.md` §10 は「Jevは選択肢の点数を返すだけで決めるのはcode/リード」という趣旨であり引用自体は整合するが、「Jevと食い違えば実測を採る」を適用する変数の選び方が恣意に見えないか(P2・P9)。

3. [直す] 行50-54 — 対照(i)「一様」の生成方法が「同じ日の一様な時刻(±5分以上)、符号は同じ日の束の側を当てる」としか書かれておらず、1つの束に対し1本の対照時刻を引く(対照(ii)と同じ1対1方式)のか、日単位で一様分布の平均を取ってから束の符号を当てる(反証者が事後にやった方法)のかが明記されていない。§7の実装指示(行154)は「1周目の道具の`build_cascades` / `PriceSeries.at_or_after`を使う」としか書いておらず、対照(i)の候補生成ロジック(1周目の`allowed_intervals` / `CONTROL_GAP_MS` / `MATCH_GRID_MS`に相当する部分)を再利用するか新規実装するかが不明(P3、委任の具体性)。

4. [聞く] 行41 — 探索段1の10属性から3属性(束の件数・換算レバレッジ・側)に絞った理由が書かれていない。他の7属性(VWAPからの距離・ノードからの距離・価格帯の薄さ・束の幅・束の想定元本・建玉の減り・時刻帯)をE4から外した根拠が本文にない(A-10「族・スコープを説明なく縮める」)。

5. [聞く] 行41 — 反証者「直すべき#7」(`doi_pre_1h` 欠測28.6%・`implied_leverage` 欠測38.0%はmetrics欠測97日への偏りを持つ)が指摘した2属性のうち、`implied_leverage`(換算レバレッジ)をE4の3属性の1つとしてそのまま使っている。この偏りへの対応(欠測日を明記する、別途注記するなど)が書かれていない。

6. [聞く] — REFUTER_REVIEW「直すべき#1」(清算21,198束と対照18,199行は同じ母集団でない。対照の付かない2,999件〈14.1%〉は1分平均−11.392bpで大きい束に偏る)がこの設計のどこにも扱われていない。E1〜E4のいずれも「対照が付いた/付かない」の分割を出力に含めていない。意図的に対象外としたなら、その旨が本文に書かれるべき。

7. [聞く] — REFUTER_REVIEW「直すべき#6」(456日は暦474日中18日欠け、2024-02-15〜02-26の連続12日を含む)がこの設計のどこにも扱われていない。

8. [聞く] — REFUTER_REVIEW「直すべき#8」(清算行の重複除去がちょうど2.0005倍、出所未確認)がこの設計のどこにも扱われていない。同じ`backtest_data/binance_cm_o3c_20260913`の清算データを再利用する以上、この未確認事項がそのまま持ち越される。

9. [直す] 行144-153 — §7「多重性」は「表の数(行)を数えて次の単位の事前登録に引き継ぐ」「探索段1の552行/180比較に足す」と書くのみで、この設計自体の行数を計算していない。設計のΔ(6本: 行146)・h(9本: 行147)・gap(3本: 行40)・属性(3×3分位: 行41)から機械的に積を取ると、E1=6行、E2=Δ3×h9=27行、E3=gap3×Δ6=18行、E4=3属性×3分位×Δ3=27行で、合計78行と概算できる(検証用の計算、リード自身の値ではない)。KA-09型(「n種」の数え上げ不一致)の再発を避けるため、設計段階で概数を明記すべきではないか(P5)。

10. [聞く] 行26 — S-5の備考「4時間超は無い(探索段1で1分以後は平らだった)」は、探索段1という同じ456日への2度目の読みの結果を根拠にhの上限を決めている。探索段1の「1分以後は平ら」という所見自体が致命2(対照側のドリフトで差の形状が作られていた)を含む段階の読みであり、その読みを根拠にスコープを縮めてよいか(A-10・A-18)。理由は書かれているため必ずしも違反ではないが、判断が要る。

11. [直す] 行154 — テスト計画(Δ=0が探索段1の1分の値と一致する、対照にも同じΔが当たる、`paper_logs/`を開かない)に、対照(i)・(ii)の1本の束に対する対応関係(1対1か日集約か)や`anchor_lag_ms`の分布出力を検証するテストが含まれていない(#1・#3の具体化不足の帰結)。

12. [聞く] 行11 — §0.1対応表の行「起点を…離す・秒単位のhの探索を、456日の公開アーカイブで行う」の逐語根拠がL-246「自前記録使ったら判定できなくなるからダメ」(自前記録の使用を禁じる否定形)であり、「公開アーカイブで行う」という積極的な行動を直接命じた逐語ではない。論理的帰結(自前記録が禁止された結果として公開アーカイブを使う)として妥当だが、CLAUDE.md §0.1が警戒する「引用はあるが行動と逐語の対応が間接推論」のパターンに近くないか確認を要する。

13. [聞く] 行13 — 同表の行「Δの候補値・hの刻み・gap・対照の形はリードの決定として§7に書く(探索段の固定手順)」の逐語根拠がL-245「始めてください」(着手の許可)であり、CLAUDE.md §0.1の例外(承認済み常設スキルの固定手順は聞かずに当ててよい)を援用している。しかし探索段1ではgapという同種の既定値だけで効果量が1.6〜2.1倍動いた(致命3)。数値に直結するハイパーパラメータの選定を「固定手順」の例外で処理してよいか、それとも個別に逐語確認すべき事項か。

## 確認事項ごとの結果

- (1) §0.1の右列は全行埋まっており(空行なし)、L-246の禁止(自前記録の探索利用)と矛盾する記述は本文中に見当たらない。行11・行13の2行は逐語が命令形でなく、指摘12・13として扱いの妥当性を問いとして残した。
- (2) 上表のとおり。致命3件はいずれも扱われているが、直すべき10件のうち#1・#4・#6・#7・#8の5件が扱われていない(明示的な「扱わない」宣言もない)。
- (3) jev_design 2表は(a)9の要件どおり存在する(観測量の順位表×3意図、対照の変数表×1)。判定に使う量が上位に無い場合の理由(E2でliq_side_h_shapeを使う等)は「リードの読み」に書かれている。§10の引用自体は整合するが(§10末尾「Jevは選択肢の点数を返すだけで、決めるのはcode/リード」)、実測との食い違いをどの変数に適用するかの基準は書かれていない(指摘2)。
- (4) 対照側の残存する非対称(密度差起因)を検証する`anchor_lag_ms`分布のΔ別出力が計画に無い(指摘1)。
- (5) 対照(i)の生成方法(1対1か日集約か)が明記されておらず、1周目の`control_uniform`定義との一致を保証する実装上の記述もない(指摘3)。
- (6) Δ6×h9×gap3×属性3×3分位から概算78行と計算できるが、設計はこの数を出していない(指摘9)。
- (7) 行26「4時間超は無い」がA-10/A-18/O-2との関連で判断を要する(指摘10)。
- (8) 対照(i)の生成方法・anchor_lag出力・テスト網羅性に具体性不足がある(指摘3・11)。

## 判定: 止める

理由: 致命1(起点の非対称)を分離する検証手段そのもの(`anchor_lag_ms`のΔ別分布出力)が計画されておらず、この設計の中心的な主張(「Δを離せば清算・対照の起点がそろう」)を確認する手立てが本文に無い(指摘1)。加えて反証者の「直すべき」10件のうち5件(#1・#4・#6・#7・#8)が沈黙のまま持ち越されており、「扱わない」という明示的な判断も書かれていない。research-protocol §11「撤回の手順」はこの単位が前単位の反証者所見を体系的に処理することを前提にしており、この欠落は測定実行前に埋めるべきと判断した。


## リードの応答(1 回目、1 件ずつ)
1 = 直した(E0 を新設: Δ × kind ごとの `anchor_lag_ms` の分布 = lag 0 の割合・中央値・p90・NaN。「Δ ≥ 1 秒でも完全にはそろわない」と明記し、ずれの大きさを E1 の読みに添える)。
2 = 答えて直した(基準 = 反証者が群の間の非対称と差への効きを数字で測った変数だけ。該当 2 件。残り 10 件は反証者 §5 が効きを「数十分の一 bp」と測っているので Jev の語のまま)。
3 = 直した(対照 (i) は 1 周目の表の `control_uniform` 行をそのまま使う。行は束に紐づかず side が空なので、符号は「同じ日の i 番目の一様行に表の順で i 番目の束の側」= 1 対 1、価格を見ない規則。反証者の日集約も併記して +10.7 の再現に使う。対照 (ii) も表の行そのまま)。
4 = 直した(E4 は探索段 1 と同じ 10 属性 30 群。縮めない)。
5 = 直した(E4 の群ごとに含む日数を列に出す。反証者 #7 を明記)。
6 = 直した(E1 の清算側を 全体 / 対照 (ii) 付き / 付かない の 3 行に分ける)。
7 = 直した(§6 に欠けた 18 日を列挙し、summary と tables の冒頭に出す。この段では埋めない理由を明記)。
8 = 直した(§6: 持ち越しを明記し、委任先に生の 3 日分で重複の正体とローダの読み方を確かめさせる。確かめられなければ未確認のまま)。
9 = 直した(§7 に数え上げ: E0 18 + E1 30 + E2 90 + E3 18 + E4 90 = 246 行。監査役の概算 78 とは E0・分割・全属性・h 10 本を足したぶん違う)。
10 = 答えて直した(上限を 8 時間に伸ばし、探索段 1 の「平ら」を根拠にしない)。
11 = 直した(テストに 対照 (i) の 1 対 1 の符号 と E0 の出力 を追加)。
12 = 答えて直した(§0.1 の行 2 に「直接の命令ではなく帰結。清算の生データはこの 2 つしか無い(N-012)」を明記)。
13 = 答えて直した(「始めてください」の援用をやめ、Δ・h・対照の形は**(該当語なし)= リードの決定**と書いて報告で示す。gap は 1 つに決めず 3 本とも走らせる = 致命 3 の測定そのもの。**この空の行はオーナーに見せる**)。

## 判定(1 回目)
判定: 止める(監査役)→ 13 件を処置して 2 回目にかける。

## 監査役の返答(2 回目、逐語の指摘一覧。前段の照合表は監査役の出力にあり、要点: 13 件とも処置の痕跡あり、#3・#9 に新たな不整合)

1. [止める] 行53, 行157 — 対照(i)の符号規則「同じ日の i 番目の一様行に、表の順で i 番目の束の側を当てる(1 対 1)」は、mixed な束(側が定義されず table から除外される)を含む 75/456 日(16.4%、実測)で、その日の control_uniform 行数(= 全束数)と側を持つ liq 行数(= 全束数 − mixed 数)が一致しないため、機械的に一意に実行できない。i の基準(liq 行の順か、mixed を含む全束の順か)が本文にない。テスト計画(行157「対照(i)の符号が同じ日の i 番目の束の側である」)もこの場合分けを検証しない(P-3)。
2. [直す] 行40, 142 vs 行26, 150, 156 — E2 の h の本数が本文内で「9 本」(行40・142)と「10 本」(行26・150、行156 の式は h10 を使用)に食い違っている。§7 の合計 246 行はこの矛盾を解消しないまま出されている(P-5, P-2)。
3. [聞く] 行41 vs 行156 — E3 の定義「gap 30/60/180秒で E1 を繰り返す」は文字どおりなら E1(Δ6×5行=30)の3倍(90)になるはずだが、§7 の行数式は「E3 = gap3×Δ6=18」で、E1 で追加した3行分割(全体/付き/付かない、対照(i)/(ii))を暗黙に落としている。縮小の理由が書かれていない(P-5)。
4. [聞く] 行134 vs 行38 — 「Δ を同じく当てることで anchor_lag_ms を構造でそろえる」(解決の断定)と、E0 の定義文「Δ≥1秒でも分布は完全にはそろわないはず」(未解決の予測)が同じ文書内で矛盾している。E0 は測定前の項目であり、測定前に「そろえる」と言い切ってよいか(O-2 に近い先取り断定、P-6 とも関連)。
5. [聞く] 行53 — 対照(i)の「符号は価格を見ない、時刻順だけで決まる」という設計は、反証者が事後にやった「日ごとの一様行の平均×束の符号」(併記対象)と別の統計的性質を持つ(1対1の個別ペアリング vs 日次集約)。両者が探索段1の +10.7 bp の再現確認に同時に使われる予定(行53)だが、mixed 束の扱いが不定なままでは 1 対 1 版の再現性そのものが指摘1の欠陥に依存する。

判定: 止める(監査役)

## リードの応答(2 回目)
1 = 直した(規則を一意に: 日ごとに liq 行を start_ms 昇順・一様行を time_ms 昇順に並べ i 番目どうしを組む。余った末尾の一様行(mixed の分、180 行)は落とし、日ごとの数を summary に書く。監査役の実測 21,378 / 21,198 / 75 日 を本文に入れた。テストも「余りが落ち、落ちた数が 180」に変えた)。
2 = 直した(E2 は 10 本に統一)。
3 = 直した(E3 は E1 と同じ 5 行の形で繰り返す = 90 行。合計 318 行、再掲を除き 288)。
4 = 直した(「構造でそろえる」を「起点の規則を同じにする。そろうかは E0 で測る」に)。
5 = 1 の処置で規則が一意になったので、1 対 1 版と日集約版の両方を併記し、+10.7 の再現は日集約版で確認する(1 周目の値は日集約でしか作れない)。

## 判定(2 回目)
判定: 止める → 5 件処置して 3 回目にかける。

## 監査役の返答(3 回目、逐語の指摘)

1. [直す] 行142 vs 行42・行156 — §6の出力一覧が「E4(3属性×3分位×Δ3本)」と書かれている。これは§2のE4定義(行42「探索段1と同じ10属性(3分位×8+側2+時刻帯4=30群)」)、および§7の行数式(行156「群30×Δ3=90」)と数が食い違う。「3属性×3分位」は、1回目監査の指摘4(属性を3つに絞った理由が書かれていない)で問題視され後に「10属性・縮めない」へ戻された**旧版の記述の取り残し**に見える。E2のh本数(9本→10本)と同型の食い違いが別の変数(属性数)で再発している(P-2, P-5)。
2. [聞く] 行139 — 「束: 1周目の道具のbuild_cascades(gap60秒を主、30/180を感度)。同じ束の集合(gap60_w8のcascade_id)を使い、起点だけを変える。」の文言は、明示的に名指ししているのが `gap60_w8` の cascade_id だけで、E3(gap30/60/180の束ね方感度、致命3の測定そのもの)がgap30/gap180用に何の束の集合(`gap30_w8`・`gap180_w8` のcascade_id …)を使うのかが本文に明記されていない。… 仮にE3の実装が誤ってgap60の束をgap30/180ラベルのまま流用すると、E3(この設計の中心目的である致命3の分離)が実質何も測らないことになる。

確認事項 (1): 5 件とも済(監査役が `per_day` を独立に再集計: 束 21,378 / mixed 180 / mixed の日 75 / 一様 21,378 / liq 21,198、全日で一様 = 束、同日内のタイ無し)。(2) 318 / 288 は本文定義から再計算して一致。(4) 断定なし。

判定: 通す(監査役。指摘 2 は実装前にリードが確認することを推奨)

## リードの応答(3 回目)
1 = 直した(§6 の出力一覧を「E4(10 属性 30 群 × Δ 3 本)」に)。2 = 直した(§6 に「gap 30 / 180 はそれぞれ gap30_w8 / gap180_w8 の表の束と対照。gap 60 の束を流用しない」。委任文にも同じ文がある)。

## 判定(3 回目)
判定: 通す(監査対象: SIGNAL_EXPLORE2/design)
