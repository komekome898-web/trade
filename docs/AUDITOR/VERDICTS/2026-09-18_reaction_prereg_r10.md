# 監査(owner-auditor)— 段 A 事前登録への **10 回目(走行前の再監査)18 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction.py`(以下 REACT)、`scripts/o3c_reaction_judge.py`(以下 JUDGE)、
試験 3 本(`tests/test_o3c_reaction.py` / `tests/test_o3c_reaction_judge.py` /
`tests/test_audit_gates_wired.py`)、`docs/DATA.md`。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `..._r2.md`(23 件 + ア〜エ)、3 回目は `..._r3.md`(23 件)、
4 回目は `..._r4.md`(22 件)、5 回目は `..._r5.md`(19 件)、6 回目は `..._r6.md`(18 件)、
7 回目は `..._r7.md`(20 件)、8 回目は `..._r8.md`(19 件)、9 回目は `..._r9.md`(20 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 4(1〜4)/ 直す 8(5〜12)/ 聞く 4(13〜16)/ 通す 2(17・18)= 18 件(省略なし)。
総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない = `ls` が `No such file or directory`)。
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` /
`CLAUDE.md` / `docs/AUDITOR/ACTION_LOG.md` は 1 行も触っていない。**
**`regen_hook_manifest.sh` も走らせていない。**
**コミットもプッシュもしていない。**
**今回も `scripts/o3c_reaction.py` を変えた**(リードの委任文面「**`scripts/o3c_reaction.py` は
決定 1〜4・13 の分だけ変えてよい**」)。**変えたのは版の担保の関数((`tool_commit` / `tool_dirty` /
`tool_version_block`)・開封前の関門 (c′)(i)・`summary.json` の 2 鍵の作り方だけで、
表を作る計算・列・乱数の消費は 1 行も変えていない。**
**標本 4 本を走らせ直し、全列一致を確かめた**(§3 の 4)。

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 18 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r10.md` を新設(逐語を一字も変えずに貼り、18 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r10.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| REACT を決定 1〜4・13 の分だけ変える | リードの委任文面「**`scripts/o3c_reaction.py` は決定 1〜4・13 の分だけ変えてよい**」 |
| JUDGE と試験を直す | リードの決定 1〜4・12・13 の逐語(各件に引いた) |
| 版の担保を道具の 2 ファイルに限る | リードの決定 1・2・3「**版の担保を**道具のファイルに限る**: (i) `tool_dirty` = `git diff --quiet HEAD -- scripts/o3c_reaction.py scripts/o3c_reaction_judge.py` が非 0 かどうか(この 2 ファイルだけ)。(ii) `tool_commit` = `git log -1 --format=%H -- scripts/o3c_reaction.py scripts/o3c_reaction_judge.py`(この 2 ファイルに最後に触れたコミット。HEAD ではない。事前登録の欄を書き足してもこの値は変わらない)**」 |
| 開封の手順(何をいつコミットするか)を §14 に書く | リードの決定 1・2・3「**開封の手順(何をいつコミットするか)を §14 に書く: 道具の 2 ファイルをコミット → その `git log -1 -- <2 ファイル>` を欄に書く → 欄の書き込みは道具のファイルではないので `tool_dirty` に効かない → 6 本を走らせる → 読み**」 |
| 決定 1〜3 の通る側を現物のリポジトリで実測する | リードの決定 1・2・3「**この設計は現物のリポジトリで通る側を実測する(道具の 2 ファイルが HEAD と一致した状態で `tool_dirty` = 偽になること、事前登録を編集しても偽のままであること、道具を 1 文字変えると真になること)**」+ 決定 16「**決定 1〜3 の通る側を現物のリポジトリで実測して処置書に貼る(差し替えなし)**」 |
| **標本 4 本を走らせ直し、全列一致を pandas で確かめて報告に貼る** | リードの委任文面「**変えたら標本 4 本を `--mode sample --seed 1 --mmr 0.004` で走らせ直し、table.csv / table_mixed.csv の全列一致を pandas で確かめ、§14.6 の表に行を足す**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| **関門・台帳・`.claude/`・`CLAUDE.md`・`ACTION_LOG` に触らない** | リードの委任文面「**`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/`・`CLAUDE.md`・`docs/AUDITOR/ACTION_LOG.md` は触らない**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 走らせ直しの突き合わせ / 決定 1〜3・13 の関門の通る側・止まる側 / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 6 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**18 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 12 個目の表(16 行)に明記した。**

**作業中に、原文に無い判断が必要になった点が 4 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書いた**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(10 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(9 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r9.md` + `scripts/o3c_reaction_judge.py` + `scripts/o3c_reaction.py` + テスト + `docs/DATA.md`
>
> ## 走行前の再監査(10 回目)— `owner-auditor` の返答
>
> 1. **[止める]** `scripts/o3c_reaction.py:599-609`(関門 (i))vs `scripts/o3c_reaction.py:2338-2348`・`:2475-2483` / `scripts/o3c_reaction_judge.py:703-722` — 決定 3・15 で入った 2 つの条件は**同時に満たせない**。(i) は「作業ツリーが `clean` でなければ [止め]」だが、`--mode full` の出力先 `backtest_data/o3c_reaction_20260918_full/` と台帳 `OPENED.txt` は **`.gitignore` に 1 行も掛かっていない**(事前登録 :852 が自分でそう書いている)。**実測**(本監査 2026-09-18): `backtest_data/` 配下に未追跡ファイルを置くと `git status --porcelain` は `?? backtest_data/__probe_tmp__/` を返す = `dirty`。したがって **1 本目が走った瞬間に作業ツリーは汚れ、2 本目は (i) で止まる**。さらに `run_table` は `df.to_csv(out_dir/"table.csv")`(:2339)の**後**に `"tool_dirty": _dirty_block()`(:2483)を作るので、**1 本目自身の `tool_dirty` も `dirty` になる**。読みの側は「(iii) 6 本とも汚れていない」でなければ 1 ファイルも書かない(judge:719-722、:1778-1790)。**456 日を開けたうえで表が 1 枚も出ない経路が、本版で新たに作られていないか**(P-3、A-6)
> 2. **[止める]** `docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json`(**実測**: 本監査時点の `git status --porcelain` に ` M` で出ている)vs `REACTION_PREREG_2026-09-18.md:3404` — TRACE は `CLAUDE.md` §5.0b ① の `trace_snapshot.sh` が**会話のたびに書き換える追跡下のファイル**である。リードが CLI から §14.1 を走らせる状況では作業ツリーは**構造的に汚れている**ので、(i) は 1 本目から止まる。**「開封はコミット済みの版からだけ」を満たす手順(何をいつコミットするか)が §14 のどこにも書かれていない**(**実測**: `grep -n 'コミットしてから\|コミットする\|走らせる順'` の一致 0)。**6 本を実際に走らせきれることを、走行の前に確かめたか**(P-3、P-11)
> 3. **[止める]** `REACTION_PREREG_2026-09-18.md:3719` / `:3749` vs `scripts/o3c_reaction_judge.py:708-709`・`:1775-1777` — 欄「**凍結した道具のコミット**: (まだ無い。開封の直前にここへ書き写す。)」は、読みの道具の `git rev-parse HEAD` と一致しなければ「[止め]」である。**欄は事前登録本文なので、書き写すと作業ツリーが汚れ、汚れを消すためにコミットすると HEAD はその書き写しの後の値に変わる。**欄に書ける値(直前のコミット)と、そのとき成立する HEAD は**定義上一致しない**。**「欄 = 開封時の HEAD」を満たせる手順は存在するか。存在しないなら、456 日を開けた後に読みの道具が 1 ファイルも書かない**(P-3、A-6)
> 4. **[止める]** `REACTION_PREREG_2026-09-18.md:3612`(待つもの タ)vs `scripts/o3c_reaction_judge.py:1778-1790` — オーナーが読む タ は「検査が破れたら 1 周目は表を 1 枚も出さずに終わる(**出るのは理由のファイル `stopped.txt` だけ**)」と書く。しかし本版で足した版の関門は `stopped.txt` を書く経路(:1902)より**前**にあり、破れると **1 ファイルも書かずに `return 1`** する。**「456 日を開けて、理由のファイルすら残らない」帰結が、待つもの 17 件のどこにも書かれていない**(P-5、A-6)
> 5. **[直す]** `REACTION_PREREG_2026-09-18.md:3336` / `:3575` — 「**ケ〜チ の 8 件**」「**ケ〜チ が残る 8 件**」。ケ・コ・サ・シ・ス・セ・ソ・タ・チ は **9 個**で、分類表(:3568-3573)の「報告 6(ケ を含む)+ 答えが要る 3 + 事実 8 = **合計 17**」と、ア〜ク の 8 件と足し合わせると 8 + 9 = 17 になる。**「8 件」と「17 件」は同時に成り立たない。返答 062 の側はどちらの数で書かれているか**(P-5)
> 6. **[直す]** `REACTION_PREREG_2026-09-18.md:3749` vs `:3791` — 同じ §14.4 の中で「**「凍結した道具」の定義**|**開封のときの HEAD 1 つである。走行の道具と読みの道具の両方を含む**」と「**「凍結した道具」の定義**|**開封(§14.1 を走らせるとき)の時点の `scripts/o3c_reaction_judge.py` である**」が併存している。**古い定義を消さずに新しい定義を足した形になっていないか**(P-2、O-7)
> 7. **[直す]** `REACTION_PREREG_2026-09-18.md:939` — 「**これで「5 つとも通ったときだけ 456 日が開く」ではなく、「7 つとも通ったときだけ、`judgment_days` の 456 日が開く」と書ける**」。9 回目の指摘 9 で直したのは `:3407`(「(a)〜(j) の 10 個とも」)だけで、**オーナーが開封の可否を読む §3.1 に「7 つとも」が残っている**。**関門の数の記述を走査したか**(P-2、P-5)
> 8. **[直す]** `REACTION_PREREG_2026-09-18.md:3305` / `:3330` / `:3793` / `:4692` — 監査の回数が 4 か所とも **8 回**のままである(`:3305` は「(6 回目)18 件 の処置で」、`:3793` と `:4692` は「25 件 / 23 件 + ア〜エ / 23 件 / 22 件 / 19 件 / 18 件 / 20 件 / 19 件」)。**9 回目(20 件、`_r9.md`)が入っていない。**この数は「リードが書く」とされ、オーナーへの報告に載る。**回数を現物に合わせる導線はどこにあるか**(P-5、P-8)
> 9. **[直す]** `REACTION_PREREG_2026-09-18.md:4710-4760`(判定語の走査の「**実測** 2026-09-18」)— 貼られている出力は `予測できる` 行数 5 / 合計 5、`陰性` 行数 27 / 合計 34 だが、**本監査で同じ語を数え直すと 予測できる 行数 6 / 合計 6、陰性 行数 29 / 合計 36** である(範囲 = 本ファイル 1 本に切った。切ったことを書く)。同じ節は「**上の出力は、貼った後にもう一度同じ 4 本を打って一致することを確かめてある**」と断定している。**凍結する文書の中の「実測」が、凍結の時点の現物と一致しているか**(P-2、O-3)
> 10. **[直す]** `/home/user/trade/CLAUDE.md:123` — 「**現在 2,126 件収集。2026-09-18 リードの実測(`--collect-only`、`ACTION_LOG` 062)**」。**実測**: `grep -n '^## ' docs/AUDITOR/ACTION_LOG.md | tail -1` の末尾は **`## 061`** で、**062 はまだ無い**。9 回目の指摘 8 は同じ型を事前登録について問い、処置は事前登録の側だけ番号を外した(`:4702` 付近)。**「まだ書かれていない記録を出所にしない」を、走査で全ファイルに当てたか**(P-5、O-3)
> 11. **[直す]** `REACTION_PREREG_2026-09-18.md:2954`(§11 サニティ #14)— 「**`bin_pct` の差が ±5.0 ポイント以内(`scripts/o3c_reaction.py` の `MATCH_TOL_PCT` の `MATCH_TOL_PCT`。境界を含む)**」と同じ語が 2 回続いている。指摘 12 の「行番号 → 関数名」の置き換えの残りである。**置き換えた後の行を 1 行ずつ読んだか**(P-5)
> 12. **[直す]** `REACTION_PREREG_2026-09-18.md:1511` vs `tests/test_o3c_reaction_judge.py`(`test_the_f1_reading_records_the_d1_cut`)— 事前登録の試験の行は「**0 以上 / 負 / 出ない、の 3 通り**」だが、現物の試験は `(3.5, 9.0)` / `(-2.0, 9.0)` / `NaN` / **群が無い**の **4 通り**を測っており、処置書 `_r9.md:149` も「4 通り」と書く。**同じ試験の分岐の数が 2 通りに書かれていないか**(P-2)
> 13. **[聞く]** `REACTION_PREREG_2026-09-18.md:3763`(応答が承認するもの)vs `scripts/o3c_reaction.py:540-566`((a)(b)(c))— 承認の定義は「**§13 の 8 件の枠組み(答えが要る カ・キ・ク と、報告する ア〜オ の形)を承認し、待つもの ケ〜チ を読んだこと**」だが、機械が見るのは **L 番号が欄と一致し、L-199 より後で、`OWNER_LOG.md` に行頭 `| L-NNN |` の行が実在すること**だけである。**カ・キ・ク に答えが無い L 行(例: 別件の記録)を欄に書き写しても (a)〜(c) は通る**と §14.4 の射程が自ら書いている。**「答えが要る 3 件に答えが無いまま開封する」ことを、誰がどこで止めるのか**(P-9、A-13)
> 14. **[聞く]** `REACTION_PREREG_2026-09-18.md:3604`(ケ)— 「**取り直しは終わっており、取れた日数は 0 である**」と書きつつ、同じ行の「取り直し不可の範囲」に「**tardis のデータ取得とオーナー PC は未確認**」がある。**未確認の経路が 2 つ残っている状態を「終わっている」と書いてよいか(範囲は付いたが、完了の語がその範囲を越えていないか)**(P-1、KA-03 の型)
> 15. **[聞く]** `REACTION_PREREG_2026-09-18.md:3693-3703`(9 回目の指摘 16 の処置)— 処置は「検出力について機械の関門は 1 つも掛かっていない」と注を足しただけで、**形(ケ = 報告。反対が無ければ進める)は変えていない**。§13 の #6 は「**欲しい効果を置かない → `research-protocol` §4.1 の関門は今回は当たらない**」と書く。**一度きりの開封について、検出力の関門が 1 つも無いまま進める判断を、注記の追加で満たしたと見てよいか(A-17 の「規則を増やすだけ」に当たらないか)**(P-9、A-17)
> 16. **[聞く]** `tests/test_o3c_reaction_judge.py:144-153`(`_stub_tool_version`)— 固定具の docstring は「**この試験環境の作業ツリーは必ず汚れている**(試験そのものが編集中のリポジトリで走る)ので、(i) 自分の作業ツリーと読みの道具の版だけを固定する」と書き、`git_status` を `clean` に差し替えている。**通る側は差し替えの中でしか測られていない。**`CLAUDE.md` §3 の「止まるはずの操作をして止まるかで見る」に対応する、**現物のリポジトリでの通る側の実測はどこにあるか**(P-3、P-11)
> 17. **[通す]** `docs/DATA.md:106` — 9 回目の指摘 11 の「別集合の 2 つの 97 日」は分離された(**欠測 99 日のうち 2024-03-04〜06-08 の 97 暦日(2024-06-01 を含む)**と、**判定区間内の欠け 2023-11-19 + 89 + 7 = 97 日**)。**再計算**: 3/4〜3/31 = 28、4 月 30、5 月 31、6/1〜6/8 = 8 → 97、および 28+30+31 = 89、6/2〜6/8 = 7、1+89+7 = 97 で §8.6 の 3 つの塊と一致する(P-5)
> 18. **[通す]** `REACTION_PREREG_2026-09-18.md:2832` / `:4357` / `:4486` — `stopped.txt` の中身は 3 か所とも「**破れた検査の名前・走行の名前・件数**」「理由も値も書かない」「引き金 4 つ(params / #14 / 軸の作り方 / **承認の番号**)」でそろっている。`:2825` と `:4350` に残る「破れた検査・走行・**理由**」はリードの決定の逐語の引用であり、逐語を変えていない(P-2)
>
> 判定: 止める

---

## 2. 18 件の処置

**行番号は処置後の PREREG / REACT / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**18 件のうち「変えない」で済ませたものは 3 件である**(15・17・18。**リードの決定がそう指示している**)。

### 1・2・3.[止める] 版の担保が同時に満たせない / 開封の手順が無い / 欄と HEAD が定義上一致しない

**処置: 直した(機械と文の両方)。3 件は同じ 1 つの直しで閉じる。**
リードの決定 1・2・3(逐語):
「**「作業ツリー全体が clean」は成立しない(出力先・台帳・TRACE・事前登録の欄の書き込みで必ず汚れる)ので、
版の担保を**道具のファイルに限る**: (i) `tool_dirty` =
`git diff --quiet HEAD -- scripts/o3c_reaction.py scripts/o3c_reaction_judge.py` が非 0 かどうか
(この 2 ファイルだけ)。(ii) `tool_commit` =
`git log -1 --format=%H -- scripts/o3c_reaction.py scripts/o3c_reaction_judge.py`
(この 2 ファイルに最後に触れたコミット。HEAD ではない。事前登録の欄を書き足してもこの値は変わらない)。
(iii) 事前登録 §14.4 の「凍結した道具のコミット」欄はこの値を書き写す。
読みのスクリプトは (a) 自分の `tool_dirty` が真なら「[止め]」、(b) 6 本の `tool_commit` がすべて同じで欄と一致、
(c) 6 本とも `tool_dirty` が偽、でなければ「[止め]」。走行スクリプトは `--mode full` で
`tool_dirty` が真なら「[止め]」。`tool_dirty` の判定は出力を書く前に行う。
**この設計は現物のリポジトリで通る側を実測する**(道具の 2 ファイルが HEAD と一致した状態で
`tool_dirty` = 偽になること、事前登録を編集しても偽のままであること、道具を 1 文字変えると真になること)。
§14.4 の「凍結した道具」の定義を 1 つに統一(古い定義を消す)。
開封の手順(何をいつコミットするか)を §14 に書く: 道具の 2 ファイルをコミット →
その `git log -1 -- <2 ファイル>` を欄に書く → 欄の書き込みは道具のファイルではないので
`tool_dirty` に効かない → 6 本を走らせる → 読み。テスト**」。

**REACT**: 定数 `TOOL_FILES_REL`(2 ファイル)を置き、`tool_commit()` を
`git log -1 --format=%H -- <2 ファイル>` に、`git_status()` を `tool_dirty()`
(`git diff --quiet HEAD -- <2 ファイル>` の終了コードが 0 でなければ真)に替えた。
`_dirty_block()` は同じ鍵 `状態` を保ったまま、`見た範囲` と
`git diff --quiet HEAD の終了コード` を書く(`変更ファイル` は消えた)。
**`tool_version_block()` を `run_table` / `run_anchor`(`_finish_anchor`)の入口で呼び、
出力を 1 バイトも書く前に測る**(指摘 1 の「`to_csv` の後で測っていた」)。
関門 (i) は `tool_state`(試験用の既定引数。旗ではない)か、無ければ実物の `tool_dirty()` を読む。

**JUDGE**: 同じ 2 つの関数に替え、`check_tool_versions` の (i)(iii) の文言を
「凍結した道具」に直した。`summary.json` の鍵も
`この道具の版(git log -1 --format=%H -- 道具 2 ファイル)` /
`この道具の凍結した道具の状態(git diff --quiet HEAD -- 道具 2 ファイル)` / `見た範囲` /
`git diff --quiet HEAD の終了コード` / `走行ごとの道具の状態` に直した。

**PREREG**: §3.1 の「残る穴」の (i) の段(:905 付近)、§14.1 の関門の表の (i) 行、
§14.4 の欄の直下に**開封の手順の表(5 行)**と**現物での実測**、担保の表の 5 行を書き替えた。
**「凍結した道具」の定義は §14.4 の担保の表 1 か所だけにした**(指摘 6 = 決定 6)。

**なぜこれで 3 件とも閉じるか**:
- **指摘 1**: 出力先・台帳が `.gitignore` に無くても、(i) は道具の 2 ファイルしか見ないので
  1 本目の出力で汚れない。**6 本を走らせきれる**(**設計上の読みである。
  `--mode full` は 1 本も走らせていないので、6 本を走らせて確かめてはいない**)。
- **指摘 2**: TRACE が会話のたびに書き換わっても同じ理由で (i) は止めない。
  **開封の手順を §14.4 に書いた**(何をいつコミットするか。**前版は `grep` の一致 0 だった**)。
- **指摘 3**: `tool_commit` が HEAD でなくなったので、**欄を書き足しても、それをコミットしても
  値が動かない。**「欄 = 開封のときの道具の版」を満たせる手順が存在する。

**射程(隠さずに書く)**: **道具以外のファイル(出力先・台帳・TRACE・事前登録・試験・
他のスクリプト)が未コミットでも、この機械は何も止めない。**本版で意図して狭めた範囲である。

**試験**: `tests/test_o3c_reaction.py::test_full_mode_gate_stops_when_the_frozen_tools_are_dirty`
(改名。通る側・止まる側)/ `::test_the_frozen_tools_are_the_two_script_files` /
`::test_the_tool_version_is_measured_before_any_output_is_written` /
`tests/test_o3c_reaction_judge.py::test_the_tool_version_gate_checks_the_tools_and_the_six_runs`
(改名)/ `::test_the_frozen_tools_are_the_two_script_files_on_the_real_repo`(**現物で測る**)。

**現物のリポジトリでの実測は §3 の 5 に貼った**(決定 16)。

### 4.[止める] 版の関門が破れると理由のファイルすら残らない

**処置: 直した(機械と文の両方)。**リードの決定 4(逐語):
「**版の関門が破れたときも `stopped.txt` を書く(引き金 5 つ = params / #14 / 軸の作り方 /
承認の番号 / 道具の版)。タ の文面と §14.6・§10.3 を「引き金 5 つ」に直す。テスト**」。

**JUDGE**: 定数 `CHK_TOOL_VERSION = "道具の版(決定 3・15 + 1・2・3)"` を置き、
版の関門が破れた経路で `write_stopped(...)` を呼んでから `return 1` する。
**`stopped.txt` の中身は決定 12 のまま「検査の名前・走行の名前・件数」だけで、
コミットのハッシュも値も 1 つも書かない**(試験で測った)。
**台帳の関門 `pass_audit_gate` は版の関門より前のままなので、
「台帳が閉じていれば 1 ファイルも書かない」は変わらない**(8 回目の指摘 11)。

**PREREG**: 待つもの **タ** の行、§10.3 の表、§14.6 の表と出力の一覧を
**「引き金 5 つ」**に直した(:3612 相当 / :2832 相当 / :4486 相当)。

**試験**: `tests/test_o3c_reaction_judge.py::test_the_tool_version_gate_checks_the_tools_and_the_six_runs`
(**(i)(ii)(iii) のそれぞれで `stopped.txt` が書かれ、表が 1 枚も書かれないことを測る。
値が 1 つも出ないことも測る**)/ `::test_the_frozen_tool_commit_is_recorded_and_matched`
(**出力先の中身が `stopped.txt` の 1 ファイルだけであることを測る**)/
`::test_the_audit_gate_comes_before_stopped_txt`(**順序は変えていない**)。

### 5.[直す] 「ケ〜チ の 8 件」と「合計 17 件」が同時に成り立たない

**処置: 直した。**リードの決定 5(逐語):
「**「ケ〜チ の 8 件」→「ケ〜チ の 9 件」(:3336・:3575 と grep で全箇所)。
合計 17 = ア〜ク 8 + ケ〜チ 9**」。

**PREREG**: §13 の末尾(:3385)と §14.4(:3633)の 2 か所を「9 件」にし、
「**合計 17 件 = 8 + 9**」と書き足した。
**`grep` の出力は §3 の 6 に貼った**(**残る「ケ〜ソ」「ケ〜タ」は、前版が何を書いていたかの
逐語の引用であって、現行の記述ではない**)。

### 6.[直す] 「凍結した道具」の定義が §14.4 の中に 2 つある

**処置: 直した(古い定義を消した)。**リードの決定 6(逐語):
「**「凍結した道具」の古い定義(:3791)を消す**」。

**PREREG**: §14.4 の打ち切りの表にあった
「**開封(§14.1 を走らせるとき)の時点の `scripts/o3c_reaction_judge.py` である**」を消し、
「**上の欄の直下の表 1 か所にだけ書く**」に替えた。
**定義は担保の表の 1 行(道具の 2 ファイル)だけになった**(`CLAUDE.md` §0.2 の O-7)。

### 7.[直す] §3 に「7 つとも」が残っている

**処置: 直した。**リードの決定 7(逐語):
「**「7 つとも通ったときだけ」→「(a)〜(j) の 10 個とも通ったときだけ」(:939 と grep で全箇所)**」。

**PREREG**: :939 相当の行を
「**(a)〜(j) の 10 個とも通ったときだけ、`judgment_days` の 456 日が開く**」に替えた。
**走査の出力は §3 の 6 に貼った**(**現行の記述に「7 つとも」は 1 つも残っていない。
残る 2 か所は「前版は「7 つとも」と書いていた」という経緯の記述である**)。
**(c′) は (c) の 2 段目なので、関門の数は 10 個のままである**(決定 13 の逐語「**§14.1 の (c) に足す**」)。

### 8.[直す] 監査の回数が 8 回のまま

**処置: 直した(4 か所 + 走査で拾った 2 か所)。**リードの決定 8(逐語):
「**監査の回数を 10 回に(:3305 / :3330 / :3793 / :4692 と grep)。10 回目 18 件を加える**」。

**PREREG**: §0 の凍結の宣言(:23)/ §13 の報告する変更の見出し(:3305 相当)/
§13 の末尾(:3379)/ §14.4 の打ち切りの表(:3926)/ §14.4 の「なぜ打ち切りの条件が要るか」/
文書の末尾(:4875)を **10 回**にし、件数の列挙を
**25 / 23 + ア〜エ / 23 / 22 / 19 / 18 / 20 / 19 / 20 / 18** にした。
**§13 の見出しの列挙にも 7〜10 回目を足した。**

### 9.[直す] 貼ってある判定語の走査が現物と食い違う

**処置: 直した(打ち直して貼り直し、固定点まで回した)。**リードの決定 9(逐語):
「**判定語の走査の 4 本を打ち直して貼り直す(範囲 = 本ファイル 1 本と明記)**」。

**PREREG**: §末尾の走査の出力を打ち直した。
**この節は自分自身を数えるので、貼るたびに数が動く。数が動かなくなるまで繰り返した**
(**機械**: 貼る → 打ち直す → 一致するまで繰り返す。**固定点に達した**)。
**出力は §3 の 3 に貼った。範囲は本ファイル 1 本に切っている(リポジトリ全体は数えていない)。**
**本文には数を書いていない**(**本文に数を書くと、その行自体が次の走査の対象になって固定点が動く**)。
**「残り 8 行」の記述には「これは 9 回目の時点の数である」という但し書きを足した。**

### 10.[直す] `CLAUDE.md` が「まだ書かれていない `ACTION_LOG` 062」を出所にしている

**処置: 変えない(`CLAUDE.md` は触らない)。**リードの決定 10(逐語):
「**`CLAUDE.md` は触らない。処置書に「`ACTION_LOG` 062 はリードが返答と同時に書く。
`CLAUDE.md` の参照はその時点で成立する」と書く**」。

**`ACTION_LOG` 062 はリードが返答と同時に書く。`CLAUDE.md` の参照はその時点で成立する。**

**委任先は `CLAUDE.md` と `docs/AUDITOR/ACTION_LOG.md` を 1 行も触っていない**
(リードの委任文面の禁止事項)。
**指摘 10 の実測(`grep -n '^## ' docs/AUDITOR/ACTION_LOG.md | tail -1` の末尾が `## 061`)は
本処置の時点でも変わっていない**(**実測**: §3 の 7)。

### 11.[直す] `MATCH_TOL_PCT` の重複語と、行番号の引用の残り

**処置: 直した(重複語 1 件 + 行番号の引用 1 件)。**リードの決定 11(逐語):
「**`MATCH_TOL_PCT` の重複語を直す。「行番号 → 関数名」の置換の残りを 1 行ずつ読んで直す**」。

**PREREG**: §11 の #14 の (c) を
「**(`scripts/o3c_reaction.py` の定数 `MATCH_TOL_PCT`。境界を含む)**」に直した。
**残りの行番号つきの引用を 1 件ずつ現物に当てた**(§3 の 8 に走査と照合の出力を貼った):
- `REACTION_RUN12_2026-09-18.md:64` / `:183` / `:186` / `:369` / `:395` — **現物と一致(直さない)**。
- `docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md:30` / `:73` / `:81` / `:142` / `:156` —
  **現物と一致(直さない)**。
- `scripts/o3c_reaction.py` の `:131`(`REACT_SIGN`)— **ずれていた(現物は 158 行目)。
  定数名に替えた。**
- `scripts/o3c_reaction.py:179`(:3993 付近)— **前版の文面の一部として意図的に残してある引用**
  (9 回目の指摘 12 の処置。**その旨が同じ段落に書いてある**)。**直さない。**

### 12.[直す] 同じ試験の分岐の数が 2 通りに書かれている

**処置: 直した。**リードの決定 12(逐語):「**試験の分岐を「4 通り」に統一**」。

**PREREG**: §7.0 の試験の行を
「**(4 通り: 0 以上 / 負 / `NaN` / 群が無い)**」に直した。
**試験の側にも、その 4 通りがこの試験の分岐であることを注に書いた**
(`tests/test_o3c_reaction_judge.py::test_the_f1_reading_records_the_d1_cut`)。

### 13.[聞く] 答えが要る 3 件に答えが無いまま開封することを誰が止めるのか

**処置: 直した(機械を足した)。**リードの決定 13(逐語):
「**承認の行の中身の機械: `check_approval` は `docs/OWNER_LOG.md` の該当 L 行
(行頭 `| L-NNN |` の行全体)に「カ」「キ」「ク」の 3 文字がすべて含まれていなければ「[止め]」
(答えが要る 3 件に答えが無い行は通さない)。§14.1 の (c) に足す((c′))。
射程: 文字が含まれることしか見ない。テスト**」。

**REACT**: `approval_line_text()`(行全体を返す)と
`approval_line_missing_names()`(無い名前を返す)を足し、`check_approval` の (c) の直後に
**(c′)** を置いた。**`approval_line_exists()` は `approval_line_text()` の薄い包みに変えた**
(非 full の経路の振る舞いは 1 つも変えていない)。

**PREREG**: §14.1 の関門の表に **(c′)** の行を足し、その直下の射程の段落に
**「文字が含まれることしか見ない。3 件に実際に答えているかは読んでいない。
穴が狭くなっただけで、閉じてはいない」**と書いた。
**関門の数は (a)〜(j) の 10 個のままである**((c′) は (c) の 2 段目。決定 7 の数と両立させた)。

**試験**: `tests/test_o3c_reaction.py::test_the_approval_line_must_answer_the_three_open_items`
(**通る側** 1 通り / **止まる側** 4 通り = 1 文字ずつ欠ける 3 通り + 別件の記録 /
**射程**: 3 文字が別の意味で並んでいるだけでも通ることを測る)。
**現物での止まる側の実測は §3 の 5 に貼った。**

### 14.[聞く] 完了の語が「未確認 2 経路」の範囲を越えていないか

**処置: 直した。**リードの決定 14(逐語):
「**ケ の「取り直しは終わっており」→「この環境から試せる経路は試し終えており
(tardis のデータ取得とオーナー PC は未確認)」**」。

**PREREG**: §14.4 の **ケ** の行と、その下の注の 2 か所を直した。
**`CLAUDE.md` §5.2「「取れない・無い」の主張には範囲と方法を書く」**の当て方として、
**完了の語そのものに範囲を付けた**(前版は範囲を別の括弧に書いていて、完了の語が越えていた)。

### 15.[聞く] 注を足しただけなのは A-17 に当たらないか

**処置: 変えない(形は変えない)。理由を §14.4 に書いた。**リードの決定 15(逐語):
「**ケ の形は変えない(オーナーが止められる報告)。処置書に「A-17 の指摘は受け止めるが、
検出力の関門を機械で置くには『欲しい効果』が要り、それを置かないのは §8.6 の決定(黙認)。
オーナーが見て止める形にしてある」と書く**」。

**A-17 の指摘は受け止める**(**注を足しただけであることは、そのとおりである**)。
**検出力の関門を機械で置くには「欲しい効果」の値が要り、それを置かないのは §8.6 の決定(黙認)である。**
**根拠のない「欲しい効果」を関門のために置くのは `CLAUDE.md` §0.2 の A-12(根拠のない設定値)に当たる。**
**よって機械は置かず、オーナーが見て止める形にしてある**(ケ は「報告」の側。反対があれば止まる)。
**この段落と同じ内容を PREREG §14.4 の ケ の注にも書いた。**

### 16.[聞く] 現物のリポジトリでの通る側の実測はどこにあるか

**処置: 実測した(差し替えなし)。**リードの決定 16(逐語):
「**決定 1〜3 の通る側を現物のリポジトリで実測して処置書に貼る(差し替えなし)**」。

**§3 の 5 に貼った。**やり方は次のとおりで、**固定具(`monkeypatch`)を 1 つも使っていない**:
1. 新しい版の 2 ファイルを scratchpad に退避する。
2. **現物の 2 ファイルを `git checkout` で HEAD に戻す**(= 道具が HEAD と一致した状態を作る)。
3. **新しい版を scratchpad の写しから読み込み、`repo = /home/user/trade` を見させて測る。**
4. 退避した新しい版を書き戻す。

**測ったのは 6 点**: (a) 作業ツリー全体は 13 ファイル未コミットなのに `tool_dirty` は偽 /
(b) `tool_commit` は道具に最後に触れたコミット / (c) 事前登録を 1 文字書き換えても偽のまま・
`tool_commit` も動かない / (d) 関門 (a)〜(j) が**現物の git を読んで**通る /
(e) (c′) の止まる側 / (f) 道具を 1 文字変えると真になり (i) が止める。

**試験の側にも現物で測る 2 件を足した**
(`tests/test_o3c_reaction.py::test_the_frozen_tools_are_the_two_script_files` /
`tests/test_o3c_reaction_judge.py::test_the_frozen_tools_are_the_two_script_files_on_the_real_repo`)。
**固定具の docstring も、何を固定していて何を固定していないかが分かる文に直した。**

### 17.[通す] `docs/DATA.md:106` の 2 つの 97 日

**処置: 変えない**(監査役の判定が「通す」で、リードの決定 17 も「**通す(変更なし)**」)。
**`docs/DATA.md` は本処置で 1 行も触っていない。**

### 18.[通す] `stopped.txt` の中身と引き金

**処置: 引き金だけ 4 つ → 5 つに増やした(決定 4)。中身の規約は変えない。**
リードの決定 18 は「**通す(変更なし)**」だが、**決定 4 が同じ 3 か所の「引き金」を
「5 つ」に直すよう指示している**ので、**中身の規約(検査の名前・走行の名前・件数だけ)は
1 文字も変えず、引き金の数だけ直した**。
**`:2825` と `:4350` に残る「破れた検査・走行・理由」はリードの決定の逐語の引用なので、
本処置でも 1 文字も変えていない**(指摘 18 がそう読んでいる)。

---

## 3. 走らせた結果

### 1. 試験(`-q` を足さない)

```
$ PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
........................................................................ [ 48%]
........................................................................ [ 97%]
....                                                                     [100%]
148 passed in 53.21s
```

### 2. `--collect-only`(**最終値**)

```
$ PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py --collect-only | tail -1
69 tests collected in 0.26s
$ PYTHONPATH=src python -m pytest tests/test_o3c_reaction_judge.py --collect-only | tail -1
67 tests collected in 0.09s
$ PYTHONPATH=src python -m pytest tests/test_audit_gates_wired.py --collect-only | tail -1
12 tests collected in 0.01s
$ PYTHONPATH=src python -m pytest --collect-only | tail -1
2130 tests collected in 1.43s
```

**3 本で 144 件 → 148 件**(reaction 66 → 69 / judge 66 → 67 / wired 12 のまま)。
**リポジトリ全体は 2,126 件 → 2,130 件。**
**`-q` を足すと `pyproject.toml` の `addopts = "-q"` と重なって `-qq` になり、
合計の行が出ない**ので足していない。

### 3. §8.5 の再現(152 セル)と判定語の走査

```
$ python3 <§8.5 の 2 本目の埋め込み>
照合したセル: 152 / 一致 152 / 食い違い 0
```

**判定語の走査(打ち直し。範囲 = `REACTION_PREREG_2026-09-18.md` 1 ファイルに切っている。
リポジトリ全体は数えていない)**:

```
--- 予測できる ---
ファイル数: 1
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:6
ファイルごとの出現数:       6 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 6
--- 使える ---
ファイル数: 1
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:6
ファイルごとの出現数:       6 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 6
--- 有効 ---
ファイル数: 1
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:14
ファイルごとの出現数:      21 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 21
--- 差なし ---
ファイル数: 1
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:12
ファイルごとの出現数:      16 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 16
--- 陰性 ---
ファイル数: 1
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:29
ファイルごとの出現数:      36 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 36
```

**貼った後にもう一度打って一致した(固定点に達した)。**
**指摘 9 が挙げた食い違い(予測できる 5 → 6、陰性 34 → 36)はこの打ち直しで解消した。**

### 4. 標本 4 本の走らせ直しの突き合わせ(6 度目)

`--mode sample --seed 1 --mmr 0.004` で 4 本を走らせ直し、前の出力(退避した写し)と
`pandas` で全列を突き合わせた:

```
gap60_w8/table.csv: 行×列 (662, 157) -> (662, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 9.94 -> 9.76 / .tool_commit: b5dd8e1c65ac7d7dd993f6fb23b723e36688bfda -> f154bb06f0cd5002267084e3c190eda3690295ba / .tool_dirty.git diff --quiet HEAD の終了コード: (無し) -> 1 / .tool_dirty.変更ファイル: ["backtest_data/o3c_reaction_20260918_sample/gap60_w8/MD5SUMS", "backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json", "docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json", "scripts/o3c_reaction.py", "scripts/o3c_reaction_judge.py", "tests/test_o3c_reaction.py", "tests/test_o3c_reaction_judge.py"] -> (無し) / .tool_dirty.見た範囲: (無し) -> ["scripts/o3c_reaction.py", "scripts/o3c_reaction_judge.py"]
gap60_w8/MD5SUMS: 変わったファイル ['summary.json']
gap60_w24/table.csv: 行×列 (665, 157) -> (665, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/summary.json: 食い違い 5 件 -> .elapsed_sec: 9.46 -> 9.61 / .tool_commit: b5dd8e1c65ac… -> f154bb06f0cd… / .tool_dirty の 3 鍵の入れ替え(変更ファイル -> 見た範囲 + 終了コード)
gap60_w24/MD5SUMS: 変わったファイル ['summary.json']
gap30_w8/table.csv: 行×列 (784, 157) -> (784, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 10.7 -> 10.29 / .tool_commit: b5dd8e1c65ac… -> f154bb06f0cd… / .tool_dirty の 3 鍵の入れ替え
gap30_w8/MD5SUMS: 変わったファイル ['summary.json']
gap180_w8/table.csv: 行×列 (503, 157) -> (503, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/table_mixed.csv: 行×列 (2, 157) -> (2, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 9.4 -> 9.24 / .tool_commit: b5dd8e1c65ac… -> f154bb06f0cd… / .tool_dirty の 3 鍵の入れ替え
gap180_w8/MD5SUMS: 変わったファイル ['summary.json']
```

**`table.csv` / `table_mixed.csv` は 4 本とも全列一致である**
(**表を作る計算・列・乱数の消費を 1 行も変えていない**)。
**前の 4 本の `.tool_dirty.変更ファイル` に
`backtest_data/o3c_reaction_20260918_sample/…` と `docs/AUDITOR/TRACE/…` が入っていること
そのものが、指摘 1・2 の実測である**(**作業ツリー全体を見ると、走行が自分の出力先を書いた
時点で「汚れ」になる**)。

### 5. 決定 1〜3・13 の関門の実測(**現物のリポジトリ。差し替えなし**)

**まず素の `git` のコマンドを 3 つの状態で打った**(決定 1・2・3 が名指ししているコマンドそのもの):

```
=== 状態 1: 道具の 2 ファイルが HEAD と一致(何も触っていない) ===
git status --porcelain(参考。作業ツリー全体):  M docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json  M docs/OWNER_STATUS.md
git diff --quiet HEAD -- <道具 2 ファイル> の終了コード: 0
git log -1 --format=%H -- <道具 2 ファイル>: f154bb06f0cd5002267084e3c190eda3690295ba
git rev-parse HEAD                        : f154bb06f0cd5002267084e3c190eda3690295ba

=== 状態 2: 事前登録(道具ではないファイル)だけを書き換えた ===
git status --porcelain の件数: 3
git diff --quiet HEAD -- <道具 2 ファイル> の終了コード: 0
git log -1 --format=%H -- <道具 2 ファイル>: f154bb06f0cd5002267084e3c190eda3690295ba

=== 状態 3: 道具のファイルを 1 文字だけ変えた ===
git diff --quiet HEAD -- <道具 2 ファイル> の終了コード: 1
git log -1 --format=%H -- <道具 2 ファイル>: f154bb06f0cd5002267084e3c190eda3690295ba

=== 後始末の確認(状態 1 に戻った) ===
終了コード: 0
```

**次に、新しい版の関数と関門そのものを現物のリポジトリに当てた**
(**道具の 2 ファイルを HEAD に戻し、新しい版は退避した写しから読み込んだ。
`monkeypatch` の類は 1 つも使っていない**):

```
=== 現物のリポジトリでの実測(道具の 2 ファイルは HEAD と一致した状態)===
git status --porcelain の件数(参考。作業ツリー全体): 13
走行の道具  tool_dirty(REPO) = (False, 0)  <- 通る側(偽)
走行の道具  tool_commit(REPO) = f154bb06f0cd5002267084e3c190eda3690295ba
読みの道具  tool_dirty(REPO) = (False, 0)  <- 通る側(偽)
読みの道具  tool_commit(REPO) = f154bb06f0cd5002267084e3c190eda3690295ba
事前登録を 1 文字書き換えた後 tool_dirty(REPO) = (False, 0)  <- 偽のまま
同じとき         tool_commit(REPO) = f154bb06f0cd5002267084e3c190eda3690295ba  <- 変わらない
関門 (a)〜(j)(tool_state を渡さず現物の git を読む): 通った  <- 通る側
(c′) の止まる側: [止め] …/OWNER_LOG_probe2.md の `| L-200 |` の行に カ / キ / ク が無い。
道具を 1 文字変えた後 tool_dirty(REPO) = (True, 1)  <- 真
(i) の止まる側: [止め] 凍結した道具のファイルに未コミットの変更がある(git diff --quiet HEAD -- scripts/o3c_reaction.py scripts/o3c_reaction_judge.py の終了コード 1)。
戻した後 tool_dirty(REPO) = (False, 0)
```

**1 行目と 2 行目が並んでいることが本版の要点である**:
**作業ツリー全体は 13 ファイルが未コミット(= 9 回目の版なら (i) が止める)なのに、
凍結した道具の 2 ファイルは `clean` である。**
**9 回目の版なら、この状態では (i) が 1 本目を止める**(指摘 1・2。
**設計上の読みである。9 回目の版で `--mode full` を走らせて確かめてはいない**)。

**副産物(隠さずに書く)**: **`git` が呼べない場所を指すと終了コードが 129 になり、
`tool_dirty` は「真」= 開封を止める側に倒れる**(**実測**: 写しの `REPO_ROOT` が
scratchpad を指していた最初の試行でそうなった)。**安全側ではなく、開封を止める側である。**

### 6. 「ケ〜チ の 8 件」「7 つとも」の走査(**範囲を切っていない。`git ls-files` の全ファイル**)

**打った時刻は本書を書く前である。**`git ls-files` は**追跡下のファイルだけ**を出すので、
**本書(`_r10.md`。この時点では未追跡)はこの数に入っていない**(**範囲を書く** = `CLAUDE.md` §3)。

```
$ git ls-files -z | xargs -0 grep -lZ 'ケ〜チ の 8 件' | tr -dc '\0' | wc -c
2
$ git ls-files -z | xargs -0 grep -lZ 'ケ〜チ が残る 8 件' | tr -dc '\0' | wc -c
2
$ git ls-files -z | xargs -0 grep -l 'ケ〜チ の 8 件'
docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md
docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
$ git ls-files -z | xargs -0 grep -l 'ケ〜チ が残る 8 件'
docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md
docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
$ grep -n 'ケ〜チ の 8 件\|ケ〜チ が残る 8 件' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
458:| 5 | 「ケ〜チ の 8 件」を 9 件に | **(該当語なし)** | **リードの決定**「**「ケ〜チ の 8 件」→「ケ〜チ の 9 件」…
3635:   (**合計 17 件 = 8 + 9**。**本版で「ケ〜チ が残る 8 件」から直した** = prereg 監査(10 回目)の指摘 5。
```

**PREREG に残る 2 件は、リードの決定の逐語(:458)と「前版は 8 件だった」という経緯(:3635)である。**
**現行の記述は「ケ〜チ が残る 9 件」「合計 17 件 = 8 + 9」の 1 通りである**(§14.4 の :3634)。
**`_r9.md` の 2 件は前回の処置の記録なので、変えない。**

```
$ git ls-files -z | xargs -0 grep -c '7 つとも' | grep -v ':0$'
docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md:1
docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md:2
docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:5
$ git ls-files -z | xargs -0 grep -o '7 つとも' | cut -d: -f1 | uniq -c
      1 docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md
      2 docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md
      6 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
$ git ls-files -z | xargs -0 grep -oh '7 つとも' | wc -l
9
$ grep -n '7 つとも' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
460:| 7 | 「7 つとも」を「(a)〜(j) の 10 個とも」に | … **リードの決定**「**「7 つとも通ったときだけ」→ …
983:> (**本版で「7 つとも通ったときだけ」から直した** = prereg 監査(10 回目)の指摘 7。
3460:**同じ文が §3 に「7 つとも」のまま残っていたのを本版で直した** = 同 指摘 7。
3461:**9 回目で「7 つとも通ったときだけ」から直した** = prereg 監査(9 回目)の指摘 9。
3462:**前版は表が (a)〜(h) の 8 行なのに「7 つとも」と書いていた**。
```

**PREREG の 5 行(6 出現)はすべて「前版がそう書いていた」という経緯か、リードの決定の逐語である。**
**関門の数を述べている現行の文は「(a)〜(j) の 10 個とも通ったときだけ」の 1 通りである**
(:982 の §3 と :3458 の §14.1)。
**処置書 `_r8.md` / `_r9.md` の 3 出現は前回までの記録なので、変えない。**
**「ファイルごとの出現数」を落としていない**(`CLAUDE.md` §3 の 4 本のうちの 1 本)。

### 7. `ACTION_LOG` の見出しの実測(指摘 10)

```
$ grep -n '^## ' docs/AUDITOR/ACTION_LOG.md | tail -1
8282:## 061 — L-198(途中経過には掛けない / 再開してください)の実施: #2〜#8 を黙認で事前登録に入れ、判定区間 456 日、#1・#9 を聞き直す(2026-09-18)
```

**末尾は `## 061` である**(**本処置の時点でも 062 は無い** = 指摘 10 の実測と同じ)。
**`ACTION_LOG` 062 はリードが返答と同時に書く。`CLAUDE.md` の参照はその時点で成立する**
(リードの決定 10)。**委任先は `CLAUDE.md` も `ACTION_LOG` も 1 行も触っていない。**

### 8. 行番号つきの引用の走査(**事前登録 1 ファイルに範囲を切っている。切ったことを書く**)

```
$ grep -on '`[A-Za-z_0-9/.]*:[0-9]\+' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
798:`:183
798:`:186
798:`:369
798:`:395
1456:`:131      <- 直した(定数名に替えた。現物は 158 行目)
3993:`scripts/o3c_reaction.py:179   <- 前版の文面の一部として意図して残す(9 回目の指摘 12)
3995:`:179                          <- 同上
4188:`:73
4188:`:81
4189:`:156
4194:`:156
4195:`:81
$ grep -on '[A-Za-z_0-9/]*\.py:[0-9]\+\(-[0-9]\+\)\?' docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
3993:scripts/o3c_reaction.py:179
```

**照合(1 件ずつ現物を開いて読んだ。`sed -n '<N>p' <ファイル>`)**:

| 引用 | 現物の行 | 判断 |
|---|---|---|
| `REACTION_RUN12_2026-09-18.md:64` | `\| backtest_data/o3c_reaction_20260918_anchor_trades_sample/ \| 手順 1(約定そのまま・標本 6 日)の出力…` | **一致** |
| 同 `:183` | `--out-dir backtest_data/o3c_reaction_20260918_anchor_trades_sample` | **一致** |
| 同 `:186` | `束 225(非タイ 110 / 両側とも非タイ 95) / 粒度 trades …` | **一致** |
| 同 `:369` | `` `..._anchor_trades_sample/summary.json`。n は両側とも非タイの 95 行): `` | **一致** |
| 同 `:395` | `--- backtest_data/o3c_reaction_20260918_anchor_trades_sample(約定そのまま・標本 6 日・生の列あり)` | **一致** |
| `BINANCE_CM_MMR_2026-09-17.md:30` | `>         "qtyCap": 50,` | **一致** |
| 同 `:73` | `- 確認できたこと(事実): developers.binance.com の COIN-M `leverageBracket` 系…` | **一致** |
| 同 `:81` | `**まとめ**: 「最小段階のMMRは0.4%(bracket 1、想定元本 0〜50 BTC…」` | **一致** |
| 同 `:142` | `\| BTCUSD_PERP の完全な段階表(全段階) \| 一次資料で確認(追記。公開 JSON …` | **一致** |
| 同 `:156` | `SPA が呼ぶ公開 JSON 経路を curl で試した(生ログ [14]〜[19])…` | **一致** |
| `scripts/o3c_reaction.py` の `:131`(`REACT_SIGN`) | **現物は 158 行目**(`REACT_SIGN = {"SELL": -1.0, "BUY": 1.0}`) | **ずれ。定数名に替えた** |
| `scripts/o3c_reaction.py:179`(:3993) | 前版の文面の引用 | **意図して残す**(同じ段落にその旨がある) |

### 9. 触っていないことの確認

```
$ ls backtest_data/o3c_reaction_20260918_full/
ls: cannot access 'backtest_data/o3c_reaction_20260918_full/': No such file or directory
$ git status --porcelain -- scripts/_research_audit_gate.py docs/AUDITOR/HOOK_MANIFEST.sha256 .claude CLAUDE.md docs/AUDITOR/ACTION_LOG.md docs/DATA.md
$ git status --porcelain
 M backtest_data/o3c_reaction_20260918_sample/gap180_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap180_w8/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap30_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap30_w8/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap60_w24/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap60_w24/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap60_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json
 M docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json
 M docs/OWNER_STATUS.md
 M docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
 M scripts/o3c_reaction.py
 M scripts/o3c_reaction_judge.py
 M tests/test_o3c_reaction.py
 M tests/test_o3c_reaction_judge.py
?? docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r10.md
```

**1 本目のコマンドは 1 行も出さない**(= 禁止された 6 つの場所は 1 行も触っていない)。
**2 本目が、触った 6 ファイル(+ 標本の出力 8 ファイルと、`--mode sample` の走行が
書き換えた TRACE・状態板)のすべてである。**
**`docs/OWNER_STATUS.md` と `docs/AUDITOR/TRACE/…` は本処置の開始時点で既に未コミットだった**
(**実測**: 処置の前に打った `git status --porcelain` に同じ 2 行が出ている = §3 の 5 の「状態 1」)。

---

## 4. 作業中に必要になった、原文に無い判断(4 件。黙って決めていない)

`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる。
黙って決めない**」。**委任先はオーナーに直接聞けないので、決めたことと理由をここに分けて書く。
リードが読んで差し戻せる形にしてある。**

| # | 決めたこと | なぜ | 逆の選択肢 |
|---|---|---|---|
| 1 | **(c′) を「11 個目の関門」ではなく「(c) の 2 段目」として数える** | **決定 7 は「(a)〜(j) の 10 個とも通ったときだけ」と書き、決定 13 は「§14.1 の (c) に足す((c′))」と書く。**「(c) に足す」を素直に読むと (c) の一部なので、数は 10 個のままになり、2 つの決定が両立する | **11 個と数える**(決定 7 の逐語と食い違う) |
| 2 | **`git` が呼べないとき `tool_dirty` を「真(= 止める)」に倒す** | 決定 1〜3 は「`git diff --quiet HEAD` が非 0 かどうか」と書いており、`git` が呼べない場合(例外)は逐語に無い。**9 回目の版も「不明なら止める」側だったので、それを引き継いだ** | **偽(= 通す)に倒す**(開封が静かに通る。取り返しがつかない) |
| 3 | **`summary.json` の `tool_dirty` の鍵 `変更ファイル` を消し、`見た範囲` と `git diff --quiet HEAD の終了コード` に替えた** | **新しい定義では「変更ファイルの一覧」が出ない**(`git diff --quiet` は一覧を出さない)。**鍵 `状態` は読みの側が読むので、名前も値の語(`clean` / `dirty`)も変えていない** | 一覧を別のコマンドで取って残す(決定 1〜3 の逐語に無いコマンドが増える) |
| 4 | **指摘 18(「通す」)の 3 か所の「引き金 4 つ」を「5 つ」に直した** | **決定 4 が同じ 3 か所を「引き金 5 つ」に直すよう指示している。**決定 18 の「通す(変更なし)」は「`stopped.txt` の**中身の規約**を変えない」と読んだ(中身は 1 文字も変えていない) | 引き金を 4 つのまま残す(決定 4 と食い違う) |

---

## 5. 触っていないもの(委任の禁止事項)

- **`--mode full` を 1 本も走らせていない。`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
- **`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` /
  `CLAUDE.md` / `docs/AUDITOR/ACTION_LOG.md` を 1 行も触っていない。**
- **`docs/DATA.md` も本処置では 1 行も触っていない**(指摘 17 が「通す」)。
- **`regen_hook_manifest.sh` を走らせていない。**
- **コミットもプッシュもしていない。**
- **判定(予測できる / 使える / 有効)を 1 つも書いていない。**
- **判定区間(456 日)の観測量を 1 つも新しく出していない。**
