# 監査記録: 調査班の委任文「トレードに使えるあらゆるツール」(`docs/DATA/delegations/20260921_tools_survey_prompt.md`)

オーナー承認 L-381(v4)。監査は送る前(KA-101、関門 `delegation_audit_gate.sh`)。

## 1 回目(2026-09-21、対象 = v4)

前段: jev_check 印 16 件(negative_claim 5、universal_claim 11)/ jev_delegate plan 印 1 件(not_bounded)。

監査役の出力(逐語。[止める] 2 件・[直す] 3 件・[聞く] 3 件):

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:65-66 — 「当方の道具立て(…)これ以外は何も無いと考えてよい」という否定・全称の主張(:42 negative_claim/universal_claim とも符合)に、列挙の方法(コード走査か記憶か)・範囲(リポジトリ全体か一部か)が書かれていない。この一覧は §4「当方に無いもの(全部)」(:42)の比較基準として調査全体で使われる。CLAUDE.md §2 の構成地図にある `risk/`(kill switch)・`order_management/`・`execution/`・`jpx/`(ON1 状態機械)・`resilience.py`・`reconciler.py`・`monitoring/gates.py` 等が U1〜U8 に一件も現れていない。この基準表が不完全なら、全区分の全候補で「当方に無いもの」列が系統的に誤る(P1)。
>
> 2. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:50 と :57 — §5-4「最小の実行は『読み込めた』で終わらせない」は pip/venv 前提の実行を要求するが、§6-4「鍵・登録・課金・署名をしない」は登録を全面禁止する。登録なしに機能を確認できない SaaS・ウェブ完結型のツール(オーナーの発端の指摘 L-377「Xのオススメ欄」は性質上こうしたツールを多く含むと想定される)について、「最小の実行」をどう満たすか、満たせない場合にどう記録するかが書かれていない。ここが空欄のままだと、オーナーが最も見たがっている層(登録の要る有料・SaaS 系)が構造的に浅い調査で終わり、L-378「ちゃんと調べさせる気のない委任文出すなよ」と同型の欠落を再生産しかねない(P9: §0 の禁止事項と §5 の実行要求が内部で衝突している)。
>
> 3. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:18 — 対応表の「8区分に分けて探す」の右列が「(該当語なし。区分はスキルの固定手順による区切り。L-179…)」となっているが、`.claude/skills/research-squad/SKILL.md` のモード2(scan)固定手順(§4「モード2: 市場・環境調査」)を読んだ範囲では「8 区分」という規定は見当たらない。この 8 分割はこの委任文のために新規に作られた族の切り方ではないか。CLAUDE.md §0.2 B の A-13「枠組み(…族の切り方…)はオーナーに見せてから進める」の対象にならないか(この列は「該当語なし」のまま §0.1 の擦り合わせを省いている)。
>
> 4. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:41 — 見出し「当方の用途との相性(**事実だけ**)」の直後、同じ項目内で「規模(…小さい実行の実測から**推定**と印を付けて外挿)」と明示的に「推定」の印を要求している。見出しの「事実だけ」と内容が矛盾している(P2 に近い用語一貫性の問題)。
>
> 5. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:37-44 — §4 の 6 項目のうち、事実/推定/仮定の印を明記して要求しているのは §5.5 の4軸(:43)だけで、料金の構造(:39)・到達と実行の記録(:40)・相性(:41)・当方に無いもの(:42)・危険(:44)には同種の印の要求が明記されていない。これらの欄にも数値・見積もり(規模の見積もり、活動の星・コミット数、公開からの日数等)が含まれる。出所の3分類(一次資料/実測/仮定)は §4 全体に一律で及ぶべきか、4軸だけでよいのか、本文に理由が書かれていない。
>
> 6. [聞く] `.claude/skills/delegated-study/SKILL.md` §1.5 は委任文を送る前に `jev_delegate.py plan` を当てることを定めているが、本委任文にその実行記録・印の言及が無い。research-squad 経由の委任(procure/scan/verify)はこの前段 Jev の対象に含まれるか。含まれないなら、その理由が委任文・`research-squad` `x-research` いずれにも書かれていない。
>
> 7. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:30 と :33 — 「前回の実行の『残りの候補名』があれば、まずそれから続ける」(:30)と「検索計画を先に書く…6本すべてを打つ」(:33)の関係が書かれていない。同じ区分の2回目以降の実行でも毎回新規に6クエリの検索を打ち直すのか、前回の候補の深掘りだけを続けてよいのかが不明。
>
> 8. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:54(§6-1)「1つでも不審なら導入せず『危険』列に理由を書いて止める」は、:6(§0)「判定(使える/使えない/不要/今の環境以下/採用)は書かない。判定はリードとオーナー」という射程とどこまで両立するか。安全側の停止(導入しない)によって候補がそれ以上深掘りされずに終わる判断を、調査班自身が単独で行ってよい範囲はどこまでか(P9)。

リードの処置(1 件ずつ、v5):

1. **直した。** §8 を `CLAUDE.md` §2 の構成地図と `git ls-files src scripts config deploy` の実物から package 単位で列挙し直した(risk / order_management / execution / jpx / resilience / reconciler / monitoring.gates を含む 13 群 + 無いもの 5 つ)。列挙の方法と範囲を書き、「これ以外は無い」は「無いと書く前に `git ls-files` で確かめる」に替えた。
2. **直した。** §5-6 を足した: 登録・鍵の要る道具は登録せずに取れるもの(機能一覧・API 文書・料金と規約の逐語・デモ・出力例・変更履歴・無料枠と試用の条件・利用者の報告・鍵なしの端点)を全部取り、「登録が要る(渡すもの)。実行はオーナーの判断待ち」とオーナーが辿れる手順を書く。文書だけの機能には「文書上の主張(未実行)」の印。浅い印のまま終わらせない。
3. **訂正した(監査役が正しい)。** 8 区分はスキルの固定手順ではなくリードの枠組み。対応表の右列をそう書き直し、v2〜v4 でオーナーに見せ L-381 で承認された事実を書いた。
4. **直した。**「事実だけ」→「印つき」。
5. **直した。** §4 の見出しに「すべての列のすべての数値・主張に 一次資料 / 実測 / 推定 / 仮定 の印。数値には出所の URL か実測のコマンド」。
6. **当てた。** `jev_delegate.py plan` を実行(印 1 件 = not_bounded)→ §11 を「1 回の実行の完了 / 区分の完了」に分けて有界にし、末尾に記録。research-squad 経由の委任も対象と明記。
7. **直した。** §2 に 2 回目以降の規則: 残りの候補を先に深掘り(打ち直さない)→ 空になったら幅・語を変えた新しい 6 本 → 新しい候補が 0 で区分の完了。
8. **答えた(§6-1 に書いた)。** 止めるのは「この環境への導入と実行」だけで判定ではない。止めた候補も他の列は文書から全部埋めて一覧に残す。別の場所で試すかはリードとオーナー。

## 2 回目(2026-09-21、対象 = v5)

監査役の出力(逐語。[止める] 1 件・[直す] 3 件・[聞く] 1 件):

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:78,82(§8) — §8 の同じ節内で、:78 は `validate_composite`(CLAUDE.md §2 の記述: 「`validate_composite.py`(composite の再現ゲート)」)を当方の道具立てとして列挙しているのに、2 行下の :82「無いもの(実物で確認)」は「別実装との突き合わせ(再現ゲート)」を当方に無いものとして挙げている。`scripts/validate_composite.py` の冒頭 docstring は「the reproduction gate the research protocol requires…: a new script's baseline must reproduce the existing result trade-for-trade」と明記しており、まさに「別実装との突き合わせ」そのもの。§8 は「『当方に無いもの』を書くときの参照」として全区分・全候補の比較基準に使われる(:42, :66)ため、この自己矛盾は全候補の「当方に無いもの」列を系統的に誤らせうる(P1: 否定的主張の裏取り不足)。
>
> 2. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:67,78(§8) — :67 は §8 の列挙方法を「`CLAUDE.md` §2 の構成地図と `git ls-files src scripts config deploy` の実物から package 単位で全部(2026-09-21、リード実測。記憶ではない)」と主張するが、実際に `git ls-files scripts` を実行すると `scripts/qa/`(13 ファイル、`agreement.py` = 2 回の監査結果の一致率算出、`maker_fill_ref*.py` = 「CODE-AS-CLAIM」を掲げる参照実装シミュレータで別実装突き合わせそのもの)と `scripts/phase2/`(13 ファイル)が :78 の scripts 列挙に一件も現れない。指摘 1 の矛盾(「無いもの」に挙げた「別実装との突き合わせ」)を裏付ける実物がまさにこの抜けたディレクトリに存在しており、「実測」の網羅性そのものに疑問が残る(P1・P3 に近い網羅性)。
>
> 3. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:37 と :43(§4) — :37 は「すべての列のすべての数値・主張に 一次資料 / 実測 / 推定 / 仮定 の印を付ける(4 軸だけでなく…も同じ)」と 4 分類の印を § 4 全体(4 軸を含む)に要求しているのに、同じ § 4 の 4 軸の項目(:43)には修正前の「4 軸それぞれに 事実 / 推定 / 仮定 の印」という 3 分類・旧語(「事実」)がそのまま残っている。1 回目監査の指摘 5 への処置(「§4 の見出しに…一次資料/実測/推定/仮定の印」を追加)が、既存の古い定義文(:43)を消さずに追記されたために起きた不一致(P2: 訂正は古い定義を消してから)。
>
> 4. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:78 — 「`scripts/` の research_* 56 / fetch_* 28 / o3c_* 22 / …」の `fetch_* 28` は実数と食い違う。実測: `git ls-files scripts | grep -c '^scripts/fetch_'` → 29。他の接頭辞(research_*=56, o3c_*=22, render_*=19, measure_*=16, jev_*=14, run_*=11, check_*=7, build_*=7, verify_*=3, record_*=5)はすべて実数と一致したが、fetch_* だけずれている。:67 が「リード実測。記憶ではない」と明記する § 8 の中の数え上げが実際には正確でない(P5: n 個の数え上げは列挙し直すと一致するか)。
>
> 5. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:66-82(§8) — :67・:82 は「実物で確認」「リード実測」と書くが、この主張を裏付けるコマンドと出力は委任文のどこにも示されていない(本文 §5-5 は調査班自身の将来のプローブには「コマンド・出力…を1手ずつ残す」ことを要求している一方、§8 の作成者自身の実測にはこの型が適用されていない)。指摘 1・2・4 で見つかった食い違い(validate_composite との矛盾、scripts/qa・scripts/phase2 の欠落、fetch_* の数え間違い)を踏まえると、`git ls-files` と `CLAUDE.md` §2 との突き合わせが実際にどこまで行われたか(全部か、一部か)が本文からは判定できない。

リードの処置(1 件ずつ、v6):

1. **直した(監査役が正しい)。**「無いもの」を「清算連鎖の単位(o3c)に対する別実装の再現ゲート(composite には `validate_composite.py` がある。o3c には無い)」に書き直し、`scripts/qa/maker_fill_ref*.py`(指値の約定の参照実装)を列挙に入れた。
2. **直した。** 初版は `git ls-files scripts/*.py`(最上位だけ)で数えていた。再帰で数え直し、`scripts/qa/` 13・`scripts/phase2/` 13・`scripts/jev/` 4 を列挙に入れた。
3. **直した。** 4 軸の行を「一次資料 / 実測 / 推定 / 仮定(§4 冒頭と同じ 4 分類)」に統一(古い 3 分類の文を消した)。
4. **直した。** fetch_* は `.py` だけで 28、拡張子を問わなければ 29(差 1 = `.py` 以外の 1 ファイル)。数え方を明記した(ログのコマンド 6)。
5. **書いた。** 列挙のコマンドと出力を `docs/DATA/probes/20260921_tools_inventory.log` に残し、§8 から参照した。

## 3 回目(2026-09-21、対象 = v6)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 1 件):

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:79(§8) — `docs/DATA/probes/20260921_tools_inventory.log:16-53`(コマンド2の出力)に `1 phase2`(サブディレクトリではないトップレベル接頭辞)という行があるが、本文 §8 のスクリプト列挙(:79)は `scripts/phase2/` 13 本しか書いておらず、別ファイル `scripts/phase2_seal.py` に一度も触れていない。実体を確認すると `scripts/phase2_seal.py` は研究の封印処理(CLAUDE.md §5.0 の関門1 `sealed.py: load_sealed` が読む `SEALED.json` を書き出すスクリプト、docstring 冒頭に明記)であり、機能として重要度が高い。この一覧は §4「当方に無いもの(全部)」(:42)・§0(:4「全部書く」)の比較基準として全区分で使われる。1回目監査の指摘1・2回目監査の指摘1・2(いずれも `scripts/qa/` `scripts/phase2/` などの取りこぼし)と同じ型の欠落が、直し直した同じ節で3回目にも見つかった。この基準表は本当に「全部」と言える状態か。(P1・P5)
>
> 2. [直す] docs/DATA/probes/20260921_tools_inventory.log:64-68(委任文 §8:67 が参照するログ) — コメント行は「コマンド 5: `git ls-files src/bot/*.py deploy config tests | wc -l`」と単一の `wc -l` パイプを記しているが、直後の出力はファイル一覧2段+「32」+「139」の計4つに分かれており、この1本のコマンドをそのまま実行した結果ではあり得ない(本監査で実際に打つと単一の数値 198 になる)。config=32・tests=139 という数値自体は本監査の実測と一致し正しいが、ログに記された「コマンド」の文字列は実際に打たれたコマンド(おそらく `git ls-files config | wc -l` と `git ls-files tests | wc -l` の2本)と一致していない。CLAUDE.md §3(L-173)の「数え直しは…そのコマンドを結果と一緒に出す」規則に照らし、この行の再現性は保たれているか。(P1、CLAUDE.md §3)
>
> 3. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:80(§8) — 「試験: `tests/` 139 ファイル(pytest、2,876 件)」の「2,876 件」は、`docs/DATA/probes/20260921_tools_inventory.log` のコマンド1〜6のどこにも対応する実行記録が無い。本監査で `PYTHONPATH=src python -m pytest --collect-only` を実行すると 2,880 件になり、一致しない。さらに CLAUDE.md §3 は同じ指標を「現在 2,130 件収集(2026-09-18 リードの実測)」と別の数値で記しており、本文はこの食い違いに触れていない。§8 冒頭(:67)は「2026-09-21、リード実測。記憶ではない」と §8 全体の数値の出所を保証しているが、この一件だけは出所のコマンドが示せず、実測結果とも食い違う。この数値は :67 の「記憶ではない」という宣言の対象に含まれていたか。(P1・P2、KA-04 と同型の「出所印なしの数値」)
>
> 4. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:66-84(§8全体) — §8 は1回目監査の指摘1、2回目監査の指摘1・2・4・5でそれぞれ取りこぼし・自己矛盾・数え間違いを指摘され作り直されており、3回目の本監査でも新たな欠落(指摘1)と未検証の数値(指摘3)が見つかった。§8 は8区分すべての「当方に無いもの」列(:42)の比較基準として調査全体で使われる。手書きの列挙を都度突き合わせ直す代わりに、`git ls-files` から §8 相当の一覧を機械的に再生成するスクリプトにする案は検討されたか、それとも毎回手作業での突き合わせを続ける前提か。(P8)

リードの処置(1 件ずつ、v7):

1〜4 まとめて: **手書きの列挙をやめた。**`scripts/tools_inventory.py`(新規)が `git ls-files` から package / 接頭辞 / サブディレクトリごとの件数と名前を機械的に出す(`scripts/phase2_seal.py` も `scripts/qa/` も出力に含まれることを確認)。§8 は「調査班が担当の最初にこれを打ち、出力の全文を SCAN の節の冒頭に貼る」に替え、中身の説明は構成地図の言葉だけ(件数は書かない)。ログ `20260921_tools_inventory.log` はスクリプトの出力そのものに置き換えた(指摘 2 の「コマンド 5」の食い違いは、実際に打った 2 本を 1 行に縮めて書いた誤りで、そのログは廃棄)。試験の件数は §8 から外した(2,876 は当日の全スイートの結果、2,880 は collect-only で数え方が違う。CLAUDE.md §3 の 2,130 は 09-18 の値。委任文には書かず、必要なら `--collect-only` を打つ、と書いた)。

## 4 回目(2026-09-21、対象 = v7)

監査役の出力(逐語。[止める] 1 件・[直す] 2 件):

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:68 — 「戦略(10 本、すべて未検証)」。同じ行の直前の括弧書き「件数はスクリプトの出力に従う」(:68 冒頭)に反し、実測(`git ls-files src/bot/strategy` および `docs/DATA/probes/20260921_tools_inventory.log:16`「src/bot/strategy: 9 / base.py breakout.py composite.py ema_cross.py inago.py range_fade.py rsi_reversion.py wick_reversal.py xborder_momentum.py」)では 9 本。「一覧は手書きしない」「手書きの列挙は監査で 3 回直して 3 回漏れた」(:67)として今回まさにこの節を機械化したにもかかわらず、地の文の数え上げは機械の出力と食い違ったまま残っている(P5: n 個の数え上げは列挙し直しても一致するか)。この節は §4「当方に無いもの(全部)」(:42)の比較基準として全 8 区分で使われる。
>
> 2. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:67 — 「調査班は担当の実行の最初にこれを打ち、出力の全文を…冒頭に貼る」とあるが、`scripts/tools_inventory.py` は既定(`--all-files` 無し)ではグループ内 14 件を超えると省略する(コード: `shown = ... if (a.all_files or len(names) <= 14) else ... + f" …(+{len(names) - 14})"`)。現に `docs/DATA/probes/20260921_tools_inventory.log:19`(research_*: 56 件中 14 件のみ列挙、`…(+42)`)・:20(fetch_*: 28 件中 14 件)・:23(measure_*: 16 件中 14 件)がこの状態で、「リードが打った同じ出力」(:67)として基準に使われるログ自体が「全文」ではない。`--all-files` を指定する指示が本文にどこにも無い(P1 に近い: 完全性を謳う一覧が実際には既定で切り詰められている)。
>
> 3. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:69 — 「無いもの(`git ls-files src scripts | grep -i` で確認、2026-09-21)」として 5 項目(板の待ち行列・o3c の再現ゲート・ルックアヘッドの自動検出・実験の追跡の道具・bitFlyer 以外の国内執行)を列挙しているが、各項目ごとに実際に打った検索語・コマンド・出力は本文にもログにも示されていない(CLAUDE.md §3 の「数え直しはそのコマンドを結果と一緒に出す」、§0.2 の O-3「機構やデータの挙動を主張するときは実行したコマンドと出力を同じ返答に出す」に対応する型)。試しにこちらで `grep -liE "lookahead"` を打つと 40 ファイルがヒットする(各研究スクリプトに埋め込まれた個別のルックアヘッド防止アサーションであり、汎用の自動検出ツールではないと読めるが、この区別自体が本文の記述からは追認できない)。この「無いもの」リストは §8 の中で唯一、今回機械化されなかった手書きのままの部分であり、1 回目〜3 回目監査で§8 に繰り返し指摘された「否定的主張の裏取り不足」と同型のパターンが、正の一覧(§8 前段)が機械化された後も負の一覧(:69)に残っている。

リードの処置(1 件ずつ、v8):

1. **直した。**「10 本」を消し「本数はスクリプトの出力どおり」に(数字を地の文に書かない)。
2. **直した。** スクリプトの既定を省略なしの全文にした(`--brief` で省略)。ログを作り直し、`…(+` が 0 件であることを確認。
3. **直した(裏取りで誤りが 1 つ見つかった)。** 5 項目それぞれの検索語・コマンド・一致ファイルを `docs/DATA/probes/20260921_tools_absent.log` に残し、§8 に結果を書いた。**板の待ち行列は `scripts/qa/maker_fill_ref.py` に参照実装として既にある**(初版の「無い」は誤り)。無いのは研究の模擬と `backtest/engine.py` に組み込まれていないこと。ルックアヘッドは個別の assert 4 本で汎用の検出器は無い。実験の追跡は `ITER.md` を参照する phase2 の 5 本だけで道具は無い。国内の執行は `src/` に無い(表示と記録だけ)。

## 5 回目(v8、2026-09-21)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:70 — 「板の待ち行列」の無いもの主張が、自ら引用する裏取りログと矛盾する。本文は「無いのは、それが `backtest/engine.py` と `scripts/o3c_*` で使われていないこと(`grep -liE 'queue|priority'` の一致は qa/ と dashboard・jev・record のみ)」と書くが、(a) `docs/DATA/probes/20260921_tools_absent.log:4-5` に記録された実際のコマンドは `grep -liE 'queue|priority|待ち行列|先行注文'`(4 語、本文が引用する 2 語ではない)で、一致は qa/・dashboard・jev・record だけでなく `research_*` 系 20 本超と `src/bot/backtest/engine.py`・`src/bot/order_management/manager.py`・`src/bot/research/xborder_p2.py` を含む計 32 件。(b) 本文が引用する通り `grep -liE 'queue|priority'`(2 語のみ)を実際に打ち直しても一致は 33 件で、その中に `src/bot/backtest/engine.py` 自身が含まれる(`grep -inE 'queue|priority' src/bot/backtest/engine.py` で 47・51 行がヒット、"queue position" への言及)。つまり本文が「engine.py で使われていない」ことの根拠として挙げた grep は、その engine.py 自身をヒットさせている——引用した検索と結論が食い違う。§8 は 1〜4 回目の監査で同型の欠陥(自己矛盾・取りこぼし・数え間違い)が繰り返し見つかった節であり、全 8 区分の「当方に無いもの」列の比較基準として使われる(:42, :67)。(P1)
>
> 2. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:72 — 「語の一致(`lookahead|未来`)は多いが、汎用の検出器は無い」の一致数の根拠が示されていない。本文が引用する語(`lookahead|未来`、2 語)をそのまま打つと一致は 10 件だが、裏取りログ(`docs/DATA/probes/20260921_tools_absent.log:12-13`)の実際のコマンドは `grep -liE 'lookahead|look-ahead|ルックアヘッド|未来'`(4 語)で一致は 40 件超。「多い」という形容がどちらの数字に基づくか本文からは分からず、本文引用の語をそのまま使うと「多い」というほどの件数にならない(項目 1 と同型: 本文が引用する grep パターンが、裏取りログの実際のコマンドと異なる)。(P1)
>
> 3. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:71 — 「『再現』の語の一致は katsuo 系の measure と judge_gates で、o3c には無い」とあるが、実際に裏取りログのコマンド(`grep -liE 'reproduc|再現ゲート|trade-for-trade'`)は「再現」という語そのものではなく「再現ゲート」等に絞った複合パターン。試しに素の「再現」で検索すると `scripts/o3c_reaction_judge.py` `scripts/o3c_signal_continue.py` `scripts/o3c_signal_continue_jev.py` が一致する(中身は「再現率」= recall の意味で、再現ゲートとは無関係な偽陽性と見られる)。主張の結論(o3c に再現ゲートは無い)自体は裏取りログの精密なパターンでは崩れないが、本文の「『再現』の語」という言い換えは実際に打った語を正確に表していない。項目 1・2 と合わせ、§8「無いもの」5 項目のうち少なくとも 2〜3 項目で、本文の地の文が引用する検索語と、裏取りログに記録された実際のコマンドとの間にずれがある。(P2 に近い: 同じ主張の根拠となる語が本文とログで一致しない)
>
> 4. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:47 — 「今日の実例: バイト範囲付きの `curl` は 403 だったが、正規のスクリプトの GET は通った」という具体的な実例が、根拠となるコマンド・出力の参照なしに書かれている。`docs/DATA/probes/20260921_tools_*.log`(inventory・absent の 2 本)を検索したが対応する記録は見つからなかった。この一文は調査班への振る舞いの手本として使われる(「今日の実例」に基づき「全部試すまで不可と書くな」と指示する文脈)。この実例の出典(どのツール・どの URL に対する試行か)はどこにあるか。(P1、CLAUDE.md §0.2 O-3 に近い型)

リードの処置(1 件ずつ、v9)。**同じ型を 3 回繰り返した(本文に検索語を写してログと食い違う)ので、直し方を変えた: 本文には検索語も件数も写さず、ログの節名とファイル・行番号だけで根拠を示す。**

1. **直した(監査役が正しい)。** 裏取りログを機械生成に作り直した(コマンド → `wc -l` の件数 → 一致ファイル名の全部。旧版は件数が抜けていた)。「engine.py で使われていない」の根拠を語の一致ではなく中身に替えた: `src/bot/backtest/engine.py` 47・51 行は自分で「実際の約定は待ち行列の位置に依存するが、この模型は通過した約定なら埋まったとみなす」と書いている(ログ A-2)。`scripts/o3c_*` は待ち行列の語が 1 本も無い(ログ A-3、rc=123)。本文に「ログ A の一致一覧(engine.py 自身を含む)は語の一致であって、組み込みの証拠ではない」と明記した。
2. **直した。** 検索語を本文から消し、根拠を「assert に埋め込んだ 4 本(ログ C-2 に名前)」だけにした。当初「多くは『未来』の説明文」と書き足したが、確かめると 48 本中 45 本が lookahead 系の語に一致したので、その文も消した(未確認の形容を足さない)。
3. **直した。** 「『再現』の語」を消し、「ログ B の一致一覧のうち o3c のファイルは 0 本(ログ B-3、`grep -c o3c` = 0)」に替えた。
4. **答えた(本文に出典を入れた)。** 出典は `docs/AUDITOR/VERDICTS/2026-09-21_signal_value_design.md` 1 回目の指摘 3(バイト範囲付き `curl` 1 回・HTTP 403 を根拠に「取り直し不可」と書いた)とリードの処置(正規の `scripts/fetch_tardis_samples.py` の GET で 16 標本日すべて取れた)、および `docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md` §4。tools の probe ログには無い(別の単位の記録)。
