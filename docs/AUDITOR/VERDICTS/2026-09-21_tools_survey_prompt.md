# 監査記録: 調査班の委任文「トレードに使えるあらゆるツール」(`docs/DATA/delegations/20260921_tools_survey_prompt.md`)

オーナー承認 L-381(v4)。監査は送る前(KA-101、関門 `delegation_audit_gate.sh`)。

## 1 回目(2026-09-21、対象 = v4)

前段: jev_check 印 16 件(negative_claim 5、universal_claim 11)/ jev_delegate plan 印 1 件(not_bounded)。

監査役の出力(逐語。[止める] 2 件・[直す] 3 件・[聞く] 3 件):

> 1. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:65-66 — 「当方の道具立て(…)これ以外は何も無いと考えてよい」という否定・全称の主張(:42 negative_claim/universal_claim とも符合)に、列挙の方法(コード走査か記憶か)・範囲(リポジトリ全体か一部か)が書かれていない。この一覧は §4「当方に無いもの(全部)」(:42)の比較基準として調査全体で使われる。CLAUDE.md §2 の構成地図にある `risk/`(kill switch)・`order_management/`・`execution/`・`jpx/`(ON1 状態機械)・`resilience.py`・`reconciler.py`・`monitoring/gates.py` 等が U1〜U8 に一件も現れていない。この基準表が不完全なら、全区分の全候補で「当方に無いもの」列が系統的に誤る(P1)。
>
> 2. [止める] docs/DATA/delegations/20260921_tools_survey_prompt.md:50 と :57 — §5-4「最小の実行は『読み込めた』で終わらせない」は pip/venv 前提の実行を要求するが、§6-4「鍵・登録・課金・署名をしない」は登録を全面禁止する。登録なしに機能を確認できない SaaS・ウェブ完結型のツール(オーナーの発端の指摘 L-377「Xのオススメ欄」は性質上こうしたツールを多く含むと想定される)について、「最小の実行」をどう満たすか、満たせない場合にどう記録するかが書かれていない。ここが空欄のままだと、オーナーが最も見たがっている層(登録の要る有料・SaaS 系)が構造的に浅い調査で終わり、L-378「ちゃんと調べさせる気のない委任文出すなよ」と同型の欠落を再生産しかねない(P9: §0 の禁止事項と §5 の実行要求が内部で衝突している)。
>
> 3. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:18 — 対応表の「8区分に分けて探す」の右列が「(該当語なし。区分はスキルの固定手順による区切り。L-179…)」となっているが、`.claude/skills/research-squad/SKILL.md` のモード2(scan)固定手順(§4「モード2: 市場・環境調査」)を読んだ範囲では「8 区分」という規定は見当たらない。この 8 分割はこの委任文のために新規に作られた族の切り方ではないか。CLAUDE.md §0.2 B の A-13「枠組み(…族の切り方…)はオーナーに見せてから進める」の対象にならないか(この列は「該当語なし」のまま §0.1 の擦り合わせを省いている)。
>
> 4. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:41 — 見出し「当方の用途との相性(**事実だけ**)」の直後、同じ項目内で「規模(…小さい実行の実測から**推定**と印を付けて外挿)」と明示的に「推定」の印を要求している。見出しの「事実だけ」と内容が矛盾している(P2 に近い用語一貫性の問題)。
>
> 5. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:37-44 — §4 の 6 項目のうち、事実/推定/仮定の印を明記して要求しているのは §5.5 の4軸(:43)だけで、料金の構造(:39)・到達と実行の記録(:40)・相性(:41)・当方に無いもの(:42)・危険(:44)には同種の印の要求が明記されていない。これらの欄にも数値・見積もり(規模の見積もり、活動の星・コミット数、公開からの日数等)が含まれる。出所の3分類(一次資料/実測/仮定)は §4 全体に一律で及ぶべきか、4軸だけでよいのか、本文に理由が書かれていない。
>
> 6. [聞く] `.claude/skills/delegated-study/SKILL.md` §1.5 は委任文を送る前に `jev_delegate.py plan` を当てることを定めているが、本委任文にその実行記録・印の言及が無い。research-squad 経由の委任(procure/scan/verify)はこの前段 Jev の対象に含まれるか。含まれないなら、その理由が委任文・`research-squad` `x-research` いずれにも書かれていない。
>
> 7. [直す] docs/DATA/delegations/20260921_tools_survey_prompt.md:30 と :33 — 「前回の実行の『残りの候補名』があれば、まずそれから続ける」(:30)と「検索計画を先に書く…6本すべてを打つ」(:33)の関係が書かれていない。同じ区分の2回目以降の実行でも毎回新規に6クエリの検索を打ち直すのか、前回の候補の深掘りだけを続けてよいのかが不明。
>
> 8. [聞く] docs/DATA/delegations/20260921_tools_survey_prompt.md:54(§6-1)「1つでも不審なら導入せず『危険』列に理由を書いて止める」は、:6(§0)「判定(使える/使えない/不要/今の環境以下/採用)は書かない。判定はリードとオーナー」という射程とどこまで両立するか。安全側の停止(導入しない)によって候補がそれ以上深掘りされずに終わる判断を、調査班自身が単独で行ってよい範囲はどこまでか(P9)。

リードの処置(1 件ずつ、v5):

1. **直した。** §8 を `CLAUDE.md` §2 の構成地図と `git ls-files src scripts config deploy` の実物から package 単位で列挙し直した(risk / order_management / execution / jpx / resilience / reconciler / monitoring.gates を含む 13 群 + 無いもの 5 つ)。列挙の方法と範囲を書き、「これ以外は無い」は「無いと書く前に `git ls-files` で確かめる」に替えた。
2. **直した。** §5-6 を足した: 登録・鍵の要る道具は登録せずに取れるもの(機能一覧・API 文書・料金と規約の逐語・デモ・出力例・変更履歴・無料枠と試用の条件・利用者の報告・鍵なしの端点)を全部取り、「登録が要る(渡すもの)。実行はオーナーの判断待ち」とオーナーが辿れる手順を書く。文書だけの機能には「文書上の主張(未実行)」の印。浅い印のまま終わらせない。
3. **訂正した(監査役が正しい)。** 8 区分はスキルの固定手順ではなくリードの枠組み。対応表の右列をそう書き直し、v2〜v4 でオーナーに見せ L-381 で承認された事実を書いた。
4. **直した。**「事実だけ」→「印つき」。
5. **直した。** §4 の見出しに「すべての列のすべての数値・主張に 一次資料 / 実測 / 推定 / 仮定 の印。数値には出所の URL か実測のコマンド」。
6. **当てた。** `jev_delegate.py plan` を実行(印 1 件 = not_bounded)→ §11 を「1 回の実行の完了 / 区分の完了」に分けて有界にし、末尾に記録。research-squad 経由の委任も対象と明記。
7. **直した。** §2 に 2 回目以降の規則: 残りの候補を先に深掘り(打ち直さない)→ 空になったら幅・語を変えた新しい 6 本 → 新しい候補が 0 で区分の完了。
8. **答えた(§6-1 に書いた)。** 止めるのは「この環境への導入と実行」だけで判定ではない。止めた候補も他の列は文書から全部埋めて一覧に残す。別の場所で試すかはリードとオーナー。
