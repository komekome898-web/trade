# 区分 1 の 5 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 5 回目の実行(2026-09-22)」(1947 行目以降、327 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run5.log`(新規 78 行、22 手)

## 0. 既存の節と過去の生ログが無改変か

`git show HEAD:...` の 1946 行と現在の先頭 1946 行を `diff -q` → **一致**。
`git diff --stat HEAD -- docs/DATA/probes/` → **変更なし**(run3・run4 の生ログに手は入っていない)。

## 1. 調査班が閉じずに残した 2 件

**どちらも検査の欠陥。3 回続けてここから欠陥が出ている。**

1. **1 つのセルの中の 2 つの数を食い違い扱いにしていた。**`fast-trade` の `規模の見積` に
   「実測 0.05 秒」と「そこから外挿した 100 秒」が同居している。委任文 §4 が「小さい実行の実測から
   推定と印を付けて外挿」を求めているので、両方書くのが正しい。
   **別々の行に別の値があるときだけ食い違いとするよう直した。**
2. **`tools_` という道具名を拾っていた。**出所は §8 が貼らせている道具立ての出力の
   `- tools_*: 1 / tools_inventory.py` の行。行頭の語を拾う緩い正規表現が、**57 件の名前もどき**
   (`build_` `check_` `o3c_` など)を道具名として扱っていた。
   **道具名は「表の 1 列目」と「候補の一覧のバッククォート付きの名前」だけから取るようにした。**

直したあと、本物の食い違い(同じ道具の同じ項目に別々の行で 130 と 185)はまだ当たることを欠陥入力で確認した。

## 2. リードが打ち直した受け入れ検査

生ログ 3 本を渡して、**13 本すべて 0 件**。報告の貼り付けもこの出力に差し替えた。

## 3. 一次資料の抜き取り(5 回目の節の 13 行から無作為 5 行)— **5 行とも一致**

| 行 | 主張 | 取り直した結果 |
|---|---|---|
| fast-trade / 版 | 2.1.0 | 一致 |
| fast-trade / 保守者数 | `info.author = None`、`info.maintainer = None` | 一致 |
| fast-trade / 料金体系 | AGPL-3.0。project_urls は Homepage と Repository の 2 つだけ | 一致 |
| fast-trade / 既知の脆弱性 | vulnerabilities は長さ 0 | 一致 |
| fast-trade / コミット数 | 313 | 出典は GitHub の頁。行の記載どおり |

## 4. リードが独立に裏取りした中核の発見

報告の一番重い主張(fast-trade 2.1.0 に欠陥がある)を、リードが一次資料で確かめた。

- `build_data_frame.py` を `raw.githubusercontent.com` から取得。
- **311 行 `def infer_frequency(df: pd.DataFrame) -> str:` = 引数 1 個。**
- **78 行 `if not infer_frequency(df, backtest.get("chart_period")):` = 引数 2 個。**
- **食い違いは実在する。**`chart_period` を指定すると `TypeError` になるという報告は正しい。
- 同ファイルに `limit_order` / `market_order` / `order_type` の一致は 0 件。

## 5. 調査班が「原文に無い判断」として挙げた 3 件の判定

1. **生ログの行番号のずれを直した(復帰文字 `\r` を `splitlines()` が改行と数える)→ 受け入れる。**
   中身は消していないと申告があり、過去の生ログには手が入っていないことをリードが確認した。
   **今後の規則**: 生ログは復帰文字を含めずに書く。書いたあとの生ログを直すのは符号化の問題に限り、直したことを報告に書く。
2. **K11 を満たすために各手の結果行に `rc=` を明記した → 受け入れるが、危険を書いておく。**
   K11 は行の**形**しか見ないので、`rc=0` と偽って書けば通る。今回は失敗も正直に書いてある
   (ziplime の導入失敗 `rc=1`、curl の `rc=35` / `rc=28`)が、**検査を満たすために書く行が増えること自体が
   「検査に通すことが目的化する」型である**(委任文の監査 4 回目の指摘 8)。
   **対策としてリードの抜き取りを広げる**: 次回から、印が `一次資料` の 5 行に加えて、
   **印が `実測` の行からも 3 行を無作為に選び、根拠の行が示すコマンドをリードが打ち直す。**
3. **取り直しの結果を §4.0 の表と別の表にした → 受け入れる。**
   「浅い候補は表に入れず」と「前の節では未確認、この節では値」が両立しないのは委任文の穴である。
   委任文は監査を通した版を動かさない(指紋が変わる)ので、**起動指定で毎回伝える。**

## 6. リードの判定

- **受け取る。**深掘りは `fast-trade` の 1 件のみ。浅いものを並べるより深掘りを完成させる指定に従った結果で、妥当。
- **中身の誤りは 0 件**(4 回目に続いて 2 回連続)。
- **PySystemtrade は 878 MB で、リードが付けた 200 MB の条件で止まった。**
  clone を許可したリードの前提(純 Python の研究用リポジトリだから小さい)は**外れていた**。
  中身の大半は同梱の相場データ(`data/` が 737 MB)。**条件を付けていなければ引いていた。**
- **区分 1 は未完了。**残りの候補 12 件、検索計画 6 本は未実行。

## 7. 次回に渡すもの

PySystemtrade(浅い。第 2 経路は `--filter=blob:none --sparse`)/ Ziplime(Python 3.12 以上が要る)/
Superalgos / OpenTrader / CryptoSignal / OctoBot(Python 3.12 以上)/ pybotters / DeviaVir/zenbot / Bot18 /
Mendl-Labs/BacktestingCore / Luczinsritter / mlflow。
Jesse の実弾プラグインの金額は**この環境からは取れない**(試した全経路が節にある)。オーナー PC 用のコマンドを置いた。

## 8. 検収のあとにリードが見つけた誤り(§6 の「中身の誤りは 0 件」を訂正する)

6 回目の起動指定を書くために、リードが「Ziplime と OctoBot はこの環境では導入できない」を確かめたところ、**誤りだった。**

- 報告は 5 箇所で「**この環境では導入できない**」と書いている(根拠は `requires_python >= 3.12` と、
  既定の `python3` が 3.11.15 であること)。
- **リードの実測**: `/usr/bin/python3.12` = Python 3.12.3、`/usr/bin/python3.13` = Python 3.13.12 が在る。
  `/usr/bin/python3.10` も在る。**既定の `python3` の版だけを見て「環境として不可」と断定していた。**
- そもそも区分 1 の 2 回目で、NautilusTrader を **Python 3.12 の隔離 venv** に導入した記録が同じ文書の中にある。
  **文書の中に反証があった。**
- これは CLAUDE.md §0.2 の **O-2「調べずに『取れない・無い・できない』と断定する」** の型であり、
  委任文 §5-1「到達は 1 回の結果で決めない。手段を替えて続ける」にも反する。

**処置**: 5 箇所を「既定の `python3` では導入できない」に直し、python3.12 / python3.13 が在るという
リードの実測を注記した。6 回目の起動指定で `python3.12` の隔離 venv への導入を指示する。

**この回の中身の誤りは 0 件ではなく 1 型(5 箇所)である。**§6 の記載をここで訂正する。
**抜き取り 5 行では見つからなかった。**見つかったのは、リードが次の指示を書く前にその主張を自分で確かめたから。
**「できない」という主張は、抜き取りの対象とは別に、毎回そのまま確かめる。**
