# 監査(owner-auditor)— 段 A 事前登録への **8 回目(走行前の再監査)19 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction.py`(以下 REACT)、`scripts/o3c_reaction_judge.py`(以下 JUDGE)、
試験 3 本(`tests/test_o3c_reaction.py` / `tests/test_o3c_reaction_judge.py` /
`tests/test_audit_gates_wired.py`)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `..._r2.md`(23 件 + ア〜エ)、3 回目は `..._r3.md`(23 件)、
4 回目は `..._r4.md`(22 件)、5 回目は `..._r5.md`(19 件)、6 回目は `..._r6.md`(18 件)、
7 回目は `..._r7.md`(20 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 3(1〜3)/ 直す 10(4〜13)/ 聞く 6(14〜19)= 19 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない)。
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` は
1 行も触っていない。`regen_hook_manifest.sh` も走らせていない。**
**コミットもプッシュもしていない。**
**今回は `scripts/o3c_reaction.py` を変えた**(リードの委任文面「**`scripts/o3c_reaction.py` は
決定 1・2・6・16 の分だけ変えてよい**」)。**変えたのは開封前の関門と `tool_commit` の記録だけで、
表を作る計算・列・乱数の消費は 1 行も変えていない。****標本 4 本を走らせ直し、全列一致を確かめた**
(§3 の 4)。

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 19 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r8.md` を新設(逐語を一字も変えずに貼り、19 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r7.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r8.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| REACT の `check_approval` を決定 1・2・6・16 の分だけ変える | リードの委任文面「**`scripts/o3c_reaction.py` は決定 1・2・6・16 の分だけ変えてよい**」 |
| JUDGE と試験を直す | リードの決定 10・11・12・14・16 の逐語(各件に引いた) |
| **標本 4 本を走らせ直し、全列一致を pandas で確かめて報告に貼る** | リードの委任文面「**変えたら標本 4 本を `--mode sample --seed 1 --mmr 0.004` で走らせ直し、table.csv / table_mixed.csv の全列一致を pandas で確かめ、§14.6 の「走行の道具を変えた回」の表に 4 行目として足す**」 |
| §14.4 を返答 062 と同じ分類・件数にする(報告 6 / 答えが要る 3 / 事実 7 = 16 件)、ケ の 97・100 日の書き分け、「15 軸」の 1 度の定義 | リードの追加の文面(返答の監査 4 巡目)「**事前登録 §14.4 を返答 062 と同じ分類・件数にしてください。(1) 報告(反対が無ければこの形で進める)= ア・イ・ウ・エ・オ・ケ の 6 件、答えが要る = カ・キ・ク の 3 件、事実 = コ・サ・シ・ス・セ・ソ・タ の 7 件、合計 16 件 …(2) §14.4 ケ の「被覆が 356/456 日なので…欠けるのは §8.6 の一覧の 97 日」を「zip が無い日 97(被覆 359)/ 当日+前日を要求すると欠け 100(被覆 356)」に書き分ける …(3) §4 の軸の表について「15 軸」と書く箇所は「W で展開した 15 軸(W を持つ 6 行を 2 つに数えた 12 + B1・B2・D)= 表の 11 行」と 1 度定義する。報告にこの 3 点の行番号も足してください。**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| **関門・台帳・`.claude/` に触らない** | リードの委任文面「**`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/` は触らない**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 走らせ直しの突き合わせ / 決定 1・2・6 の関門の通る側・止まる側 / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 6 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**19 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 10 個目の表(20 行)に明記した**(PREREG:368〜400)。

**作業中に、原文に無い判断が必要になった点が 4 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書いた**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(8 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(7 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r7.md` + `scripts/o3c_reaction_judge.py` + `scripts/o3c_reaction.py` + テスト
>
> ## 監査(8 回目・走行前の再監査)— `owner-auditor` の返答
>
> 1. **[止める]** `scripts/o3c_reaction.py:274-290` vs `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:719-722` — 新しい関門 (a)〜(e) は `if mode != "full": return`(REACT:289-290)の**後ろ**にしかない。`--mode sample --days <判定区間の日>` は従来どおり **(c) だけ**(「`docs/OWNER_LOG.md` に行頭 `| L-NNN |` の行が実在」)で通り、**L-199 でも通り、出力先の制限も「既に在れば止める」も掛からず、回数の上限も無い**。`--mode anchor` は関数の注のとおり「**`--mode anchor` は日では止めない**」。§3.1 は「**これから開けてよい本数 6 本**」「**7 本目を開けるにはこの機械(`FULL_OUT_DIRS_REL`)を書き換えるしかない。差分が残る**」と書くが、**既に開けた 4 本のうち 3・4 本目(§14.4.1 の埋め込み)はまさにこの非 full の経路**である。**456 日をこの 2 経路で開けることを止める機械はどこにあるか**(P-8、A-6)
>
> 2. **[止める]** `PREREG:713-729` vs `VERDICTS/2026-09-18_reaction_prereg_r7.md:659-666` — 処置書は自ら「**(e) が止めるのは「出力先が既に在るとき」である。出力先を消してから走らせ直せば、この機械は止めない(未確認ではなく、設計上そうである)**」「**これを塞ぐかはリードが決めること**」と書いた。**PREREG の §3.1 にはこの穴が 1 行も無い**(実測: `grep -n '出力先を消\|消してから'` の一致 0)。オーナーが読んで開封の可否を決めるのは PREREG である。**「一度だけ開ける」の機械の限界は、オーナーが読む文書のどこに書かれているか**(P-1、P-8)
>
> 3. **[止める]** `PREREG:3094-3128` vs `VERDICTS/..._r7.md:392-396` — 処置書は「**「その L 行が報告 062 への応答である」ことそのものは、どの機械も読んでいない。…欄はリード自身が書き写すので、書き写しが正しいことは機械では担保していない(未確認)。L-200 以降に別の話題の記録が入り、それを欄に書き写した場合は通る。これは本版で閉じていない穴である**」と書いた。**PREREG の §14.1 の (a)〜(e) の表にも §14.4 にもこの射程が無い**(実測: `grep -n '書き写'` の一致に該当行なし)。**開封の正当性についてこの穴を、オーナーはどこで読むのか**(P-8、A-6)
>
> 4. **[直す]** `PREREG:3413-3417` vs `scripts/o3c_reaction.py:249-341` — §14.4.1 は「**本版は `R.check_approval("full", a.approval)` を冒頭に置き、`docs/OWNER_LOG.md` に行頭 `| L-199 |` の行が無ければ `SystemExit` で止まる形にした**(**実測**: 関数の形は `scripts/o3c_reaction.py:179` の `check_approval(mode, approval, owner_log=OWNER_LOG, days=None)`)」と書く。**現物は 249 行の 7 引数**で、`--approval L-199` は (a)(b) で先に止まり、`--out-dir` を取らないこの埋め込みは (d) で必ず止まる。**凍結文書の「実測」の印が付いた記述が現物と違う、という 7 回目の指摘 4 と同じ型が、隣の節に残っていないか**(P-2、O-3)
>
> 5. **[直す]** `PREREG:3896-3898` — §14.6 は「**「一度だけ開ける」を守る機械は無いので、走行の前に規約で決める**」「(**実測**: `scripts/o3c_reaction.py:179` の `check_approval` に**回数・既存出力の検査は無い**)」と書くが、§3.1(:720)は同じ検査を「**(e) = `--out-dir` が既に在れば「[止め]」(再走行・上書きが止まる)**」と書く。**同じ文書が同じ関門について正反対を「実測」と書いている。どちらが現物か**(P-2、O-3)
>
> 6. **[直す]** `PREREG:3112` vs `scripts/o3c_reaction.py:2283-2297` — 「**5 つとも通ったときだけ 456 日が開く**」。しかし `main()` は `if a.days:` で任意の日集合を優先し、`check_approval` は `--mode full` について**日を 1 つも見ていない**。`--mode full --days <任意>` は (a)〜(e) を全部通り、**6 つの出力先の 1 つを 456 日以外の日で消費できる**(読みの側の `days` の数 456 の検査は開封の後である)。**「456 日が開く」と書ける根拠は何か**(P-3)
>
> 7. **[直す]** `PREREG:2789` vs `:2783` — §12 は「**この 97 日は 3 つの塊(1 日 + 89 日 + 7 日)で、うち 96 日が 2024 年 3〜6 月に連続しているので**」と書くが、直上の :2783 は「**2024-03-04〜05-31 の連続 89 日 + 2024-06-02〜06-08 の連続 7 日**」= **2024-06-01 を挟んだ別の 2 塊**である。7 回目の指摘 9 の決定は「**「連続した 1 つの塊」の残りを「3 つの塊」に直す(全箇所)**」だったが、走査語が `'連続した 1 つの塊\|1 つの連続した塊'` だけだったのでこの行に当たっていない。**「全箇所」は何を走査した結果か**(P-5、P-2)
>
> 8. **[直す]** `PREREG:3` vs `:8-16` — 文書の先頭の凍結の宣言は「**凍結: 走行の前に確定。以後、バー・族・α・h・軸を変えない**」で**5 項目**のままである。5 回目の指摘 4 の決定は「**凍結の対象は 6 項目(バー / 族 576 / α / h / 軸 48 群 / 判定に使う走行 2 本)に全箇所を統一**」であり、:8-16 と §14.4 の **セ** は 6 項目と書く。**オーナーが最初に読む 1 行が 5 項目なのは、統一から漏れていないか**(P-2、P-5)
>
> 9. **[直す]** `PREREG:17` / `:3042` — 「**6 回の走行前の監査で、次の 4 つは実際に動いた**」「**読みの道具は 6 回の走行前の監査で動いた**」。同じ文書の :3353 は「**本版の時点で 7 回**」、:4047 は「**走行前の再監査を 7 回受けた**」と書き、7 回目でも走行の道具・読みの道具の両方が動いている(§14.6 の表 :3968-3977)。**この 2 行は 7 回に更新しなくてよいか**(P-5)
>
> 10. **[直す]** `scripts/o3c_reaction_judge.py:1842` — `summary.json` の「承認の L 番号」欄は「**突き合わせ先: 判定 2 本の summary.json の params.approval**」のままである。7 回目の指摘 11 の処置で**感度 4 本にも同じ突き合わせを当てた**(JUDGE:1595-1600、PREREG:3773-3782)。**オーナーと 2 周目の読み手が読むのは summary である。ここだけ 2 本のまま残ってよいか**(P-2、P-3)
>
> 11. **[直す]** `scripts/o3c_reaction_judge.py:1639` vs `PREREG:3958-3960` — §14.6 は関門を「**表を書く前に `require_audit(…) `を呼ぶ。閉じていれば終了コード 1 で 1 ファイルも書かない**」と書くが、実装では `write_stopped`(:1639)が `pass_audit_gate`(:1991)**より前**にあり、判定側が破れた回は**台帳が閉じていても `stopped.txt` が 1 ファイル書かれる**。**「1 ファイルも書かない」は、この経路を含めて言えるか**(P-3)
>
> 12. **[直す]** `PREREG:3949` vs `scripts/o3c_reaction_judge.py:866-872` — 出力の表は `stopped.txt` を「**破れた検査の名前・走行の名前・理由。観測量は 1 つも書かない**」と書くが、`check_pairing` の理由文は「**bin_pct の差が ±5.0 を超える組が N 件(最大 4.7619)**」「**同じ相手を指す…(例: cascade_id)**」を含みうる。§14.4.1(:3404-3406)は汚染の線を「**§4.3 の観測量も §4 の分割軸の値も含まれない**」で引いており、`bin_pct` は §4 の分割軸 B の値である。**`stopped.txt` に書いてよいものの線は、どちらの基準か**(P-2、P-9)
>
> 13. **[直す]** `PREREG:4054` vs `:4122-4126` — 末尾は「**判定語(予測できる / 使える / 有効)を、判定として書いた行はこの文書に 1 つも無い**」と断定するが、その下は「**この分類に機械は掛かっていない**」「**本処置の時点では「リード確認待ち」である**」と書く。**確認が済む前のこの断定を、凍結してオーナーに出す文書に置いてよいか(「未確認」の印を付けるか)**(P-10)
>
> 14. **[聞く]** `scripts/o3c_reaction_judge.py:1592-1600` / `PREREG:3828-3835` — 感度 4 本の `params.approval` が事前登録の欄と違っても、その走行の表を落として**判定の 576 行は書く**。しかし approval の食い違いは「**オーナーの応答と違う番号で 456 日を 1 本開けた**」ことを意味する。表の落とし先(`summary.json`)以外に止まる所は無い。**承認外の番号で開いた走行が 1 本在ったとき、判定の表を書いてよいと決めたのはどこか**(A-6、P-9)
>
> 15. **[聞く]** `PREREG:3319-3324` vs `:3256` / `:3277-3278` — §14.4 の注は ケ(主軸 D の欠け)について「**「この検出力で 1 周目の唯一の機構仮説を判定してよいか」はリードが決めていない**」「**検出力についての関門が 1 つも掛かっていない**」「**よってオーナーに見せる対象に入れる**(A-13)」「**リードがここで判断を代行しない**」と書く。一方、7 回目の指摘 16 の振り直しで **ケ は「事実として出す ケ〜ソ」**に入り、**答えが要る 3 件(カ・キ・ク)から外れている**。**「リードが判断を代行しない」と書いた項目を、答えを求めない側に置いてよいか**(P-9、A-13)
>
> 16. **[聞く]** `PREREG:3351-3352` / `:3336-3337` — 「**凍結した道具 = 開封のときの `scripts/o3c_reaction_judge.py` のコミットのハッシュ**」「**(まだ無い。開封の直前にここへ書き写す。)**」。実測: 走行・読みのどちらのスクリプトも自分の版を出力に残さない(`grep -n 'コミットのハッシュ' scripts/` の一致 0)。**欄はリードの手書きで、走行の出力と突き合わせる機械は無い。「版はハッシュで残す」は何で担保されるか**(P-8)
>
> 17. **[聞く]** `PREREG:3337` — 「**道具の直しだけで §13 の表と待つものの両方が変わらない回は、追加の報告を出さずに開封へ進む**」。**「待つものが変わったか」を判定するのはリード単独である**(本監査の 20 件中、§13 の表と待つものを動かしたのは 16 と 10・15 だけ、と判断したのもリードである)。**開封は一度きりであるのに、報告を出さずに進める判断をリードが単独で行う形でよいか**(A-6、A-13)
>
> 18. **[聞く]** `PREREG:3830` / `:2549-2556` — 判定 2 本の検査が破れた回は「**表を 1 枚も書かない**」「**判定は 576 行とも出ないので、上の 5 列は「読み = 不明(検査で止まった)」の 1 行で埋める**」が残っている(感度の側だけ分けた)。**456 日を 6 本開けたうえで「なぜ」を `stopped.txt` の 1 行だけで終える帰結は、待つもの ア〜ソ のどれにも入っていない。開封の前にオーナーへ伝える項目ではないか**(`research-protocol` §0.2、A-6)
>
> 19. **[聞く]** `PREREG:3262`(ソ)/ `:449-450` — 「**2 周目は新しい期間(2024-10-15 以降)で測る**」と凍結文書が決めている一方、ソ は「**2024-10-15 以降で「清算 + 約定 + metrics」が揃った日が名前で見るかぎり 0 日**」「**公開アーカイブからの取り直しとオーナー PC は未確認**」と書く。**この期間について `DELEGATION.md` §3 の調達票(取得経路 3 つ以上・到達確認日)は、2 周目の計画を凍結する前に実行されたか**(P-7、KA-14 の型)
>
> 判定: 止める

---

## 2. 19 件の処置

**行番号は処置後の PREREG / REACT / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**19 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] 456 日を非 full の 2 経路で開けることを止める機械はどこにあるか

**処置: 直した(機械を足した)。**リードの決定(逐語):
「**判定区間の日は 6 本の `--mode full` 以外では開けない: `check_approval` は
`--mode sample --days` と `--mode anchor`(days を持つ・持たないに関わらず全日を読む)について、
日集合が判定区間の日(`judgment_days`)を 1 日でも含めば「[止め]」(承認があっても通さない)。
§3 の関門表・§3.1・§14.4.1(「本版は…止まる形にした」の記述を現物の 7 引数の形に直し、
この埋め込みは今の関門では (d) と本決定で止まる、と書く)。
テスト(通る側: 標本 6 日 / 止まる側: 判定区間の 1 日を含む `--days`、anchor)**」

**指摘のとおりだった**(**実測**): 前版の `check_approval` は `if mode != "full": return` の
**後ろ**にしか (a)〜(e) を持たず、`--mode sample --days <判定区間の日>` は (c) だけで通った。
`--mode anchor` は関数の注のとおり「日では止めない」だった。

| 何 | どこ |
|---|---|
| 判定区間の日の集合 | REACT `judgment_day_set(data_root)`(**在庫の全日 − 標本 6 日 − 12 日走行の 10 日**。`judgment_days` と同じ定義) |
| 関門 | REACT `check_approval` の冒頭(`if mode != "full":` の中)。**`--mode anchor` で `--days` が無い場合は在庫の全日を読む**ので、判定区間の日を含む |
| 事前登録 | PREREG:814〜832(§3.1 の表と射程)/ :3265 の関門表の **(h)** 行 |
| §14.4.1 | PREREG:3666〜3686(決定 4 と同時。下の 4) |
| 試験 | `tests/test_o3c_reaction.py::test_the_judgment_days_cannot_be_opened_outside_full_mode` / `::test_judgment_day_set_is_the_inventory_minus_the_opened_days` |

**実測(通る側・止まる側。実在の在庫で測った)**:

```
=== 決定 1: 判定区間の日は --mode full 以外で開けない(実在の在庫で測る) ===
[通る] --mode sample --days 標本 6 日
[通る] --mode anchor --days 標本 6 日
[通る] --mode sample --days 2024-02-15(12 日走行の 10 日)+ --approval
[止まる] --mode sample --days 2023-06-27(判定区間の日)+ --approval
          [止め] --mode sample では判定区間の日を開けない(判定区間の日が 1 日含まれている: 2023-06-27)。
[止まる] --mode anchor(--days なし = 全日)+ --approval
          [止め] --mode anchor では判定区間の日を開けない(判定区間の日が 456 日含まれている: 2023-06-27, 2023-06-28, 2023-06-29, 2023-06-30, 2023-07-01 …)。
```

**射程(隠さずに書く)**: 集合は `--data-root` の在庫から作るので、**在庫が読めない / 空なら
集合は空になり、この関門は何も止めない**(**そのときは走らせても読む日が 1 日も無い**)。
PREREG:831 に書いた。

### 2.[止める] 「一度だけ開ける」の機械の限界は、オーナーが読む文書のどこに書かれているか

**処置: 書いた(§3.1)+ 機械を足した。**リードの決定(逐語):
「**「出力先を消してからの再走行」の穴を §3.1 に書く。機械: 走行スクリプトは `--mode full` が
走るたびに `backtest_data/o3c_reaction_20260918_full/OPENED.txt` に「出力先・UTC 時刻・approval」を
追記し、`--out-dir` が OPENED.txt に既に載っていれば出力先の有無に関わらず「[止め]」。
OPENED.txt は git で追跡する(gitignore を確かめ、必要なら `!` の例外行を足す。`.gitignore` の変更は可)。
テスト。残る穴(OPENED.txt を消す / 機械を書き換える = 差分に残る)も §3.1 に書く**」

| 何 | どこ |
|---|---|
| 穴の記述(7 回目の処置書の逐語を引いた) | PREREG:785〜796 |
| 台帳と関門 (g) の表 | PREREG:798〜804 |
| 残る穴(台帳を消す / 機械を書き換える / 7 つ目の出力先) | PREREG:806〜812 |
| 実装 | REACT `FULL_OPENED_LEDGER` / `OPENED_HEADER` / `OPENED_LINE_RE` / `opened_out_dirs` / `record_opened` と `check_approval` の (g)、`main()` の `record_opened(out_dir, a.approval)` |
| 試験 | `::test_the_opened_ledger_stops_a_rerun_after_deleting_the_output` / `::test_the_opened_ledger_records_out_dir_time_and_approval` / `::test_the_opened_ledger_is_tracked_by_git` |

**`.gitignore` は 1 行も変えていない。****変える必要が無かった**(**実測** 2026-09-18:
`git check-ignore -q backtest_data/o3c_reaction_20260918_full/OPENED.txt` の終了コードは **1** =
除外されていない。`.gitignore` の `backtest_data/` の除外は `binance_*` の大容量ディレクトリ
だけに掛かっている)。

**実測(通る側・止まる側)**:

```
=== 決定 2: 出力先を消してからの再走行も止まる ===
[通る] 1 本目(台帳が空)
  台帳: …/gate/gap60_w8	2026-09-18T13:40:49Z	L-200
  出力先は存在するか: False
[止まる] 2 本目(出力先は作っていない = 消した状態)
          [止め] --out-dir …/gate/gap60_w8 は既に …/gate/OPENED.txt に載っている(この出力先で判定区間を一度開けている)。
[通る] 台帳に無い別の出力先
```

**`backtest_data/o3c_reaction_20260918_full/` は作っていない**(委任の禁止事項)。
**台帳は `--mode full` が実際に走ったときに初めて作られる**(試験は一時ディレクトリの台帳を渡す)。

### 3.[止める] 開封の正当性についてのこの穴を、オーナーはどこで読むのか

**処置: 書いた(2 か所)。**リードの決定(逐語):
「**「欄の書き写しが正しいことは機械で担保していない」の射程を §14.1 の (a)〜(e) の表の直下と
§14.4 に書く**」

| どこ | 中身 |
|---|---|
| **§14.1 の関門表の直下** | PREREG:3285〜3296。「(a) が見るのは値の一致だけ」「その L 行が報告 062 への応答であることはどの機械も読んでいない」「L-200 以降に別の話題の記録が入り、それを欄に書き写した場合は (a)〜(c) を通る」「本版でも閉じていない穴である」 |
| **§14.4 の「応答の L 番号」の欄の直下** | PREREG:3539〜3549。同じ射程を、**オーナーが応答を書く欄のすぐ横**に置いた |
| **§14.4 の「凍結した道具のコミット」の欄** | PREREG:3551〜3568。同じ形の穴が新しい欄にもあることを明記(下の 16) |

### 4.[直す] 凍結文書の「実測」の印が現物と違う(§14.4.1)

**処置: 現物に直した。**リードの決定(逐語):
「**§14.4.1 の `check_approval` の記述を現物に直す(決定 1 と同時に)**」

**PREREG:3666〜3686。**前版は「関数の形は `check_approval(mode, approval, owner_log=OWNER_LOG,
days=None)`」と「**実測**」の印つきで書いていた。**現物は 10 引数である**(本版で 7 → 10 になった)。
**書いた内容**:

- 現物の引数をそのまま貼った。
- **この埋め込みは `--out-dir` を取らないので (d) で必ず止まる。**
- **さらに決定 1 により、`R.judgment_days(root)` が作る日集合は判定区間の日そのものなので、
  `--mode full` 以外の呼び方でも止まる。**
- **`--approval L-199` は (a)(b) で先に止まる。**
- **「この埋め込みを走らせる」ことは、本版ではどの経路でもできない。**

### 5.[直す] 同じ文書が同じ関門について正反対を「実測」と書いている

**処置: 現物(§3.1 の側)に直した。**リードの決定(逐語):
「**§14.6 の「回数・既存出力の検査は無い」を現物((e) と OPENED.txt)に直す**」

**PREREG:4193〜4212。**「**「一度だけ開ける」を守る機械は無い**」「(**実測**: `check_approval` に
回数・既存出力の検査は無い)」を消し、**現物**に直した:
**既存出力の検査は (e)、回数の上限は (d) の 6 つ + (e) + (g) で機械になっている。**
**それでも規約は残す**(機械が塞いでいるのは「同じ出力先をもう一度開ける」ことだけで、
台帳を消す / 機械を書き換える経路は差分に残るだけである = §3.1 の「残る穴」)。

### 6.[直す] 「456 日が開く」と書ける根拠は何か

**処置: 直した(機械を足した)。**リードの決定(逐語):
「**`--mode full` では `--days` を受け付けない(渡されたら「[止め]」。日は `judgment_days` に固定)。
テスト。§14.1 に書く**」

**指摘のとおりだった**: 前版の `main()` は `if a.days:` を先に見ていたので、
`--mode full --days <任意>` が (a)〜(e) を全部通り、**6 つの出力先の 1 つを 456 日以外の日で
消費できた**(読みの側の `days` の数 456 の検査は開封の後である)。

| 何 | どこ |
|---|---|
| 関門 (f) | REACT `check_approval` の `days_given` |
| `main()` | `if a.mode == "full": days = judgment_days(root)` を `if a.days:` より先に置き、`days_given=bool(a.days)` を関門へ渡す |
| 事前登録 | PREREG:834〜849(§3.1)/ :3263 の関門表の **(f)** 行 / :3267「**7 つとも通ったときだけ、`judgment_days` の 456 日が開く**」(前版は「5 つとも通ったときだけ 456 日が開く」) |
| 試験 | `::test_full_mode_does_not_take_a_days_flag` |

**実測(通る側・止まる側)**:

```
=== 決定 6: --mode full は --days を受け付けない ===
[通る] --mode full(--days なし)
[止まる] --mode full --days 2023-06-27
          [止め] --mode full に --days は渡せない。判定区間の日は judgment_days(456 日)に固定である(prereg 監査(8 回目)の指摘 6)
```

### 7.[直す] 「全箇所」は何を走査した結果か(97 日の塊)

**処置: 直した(2 か所)+ 全該当行を走査した。**リードの決定(逐語):
「**「うち 96 日が 2024 年 3〜6 月に連続」→「2024-06-01(判定区間に無い日)を挟んだ 89 日 + 7 日」に。
`grep -n '連続'` で全箇所を見て報告に貼る**」

| どこ | 直した文 |
|---|---|
| **PREREG:2925〜2927**(§12) | 「この 97 日は 3 つの塊(1 日 + 89 日 + 7 日)で、**そのうち 2024 年 3〜6 月の分は 2024-06-01(判定区間に無い日)を挟んだ 89 日 + 7 日であるから**、D の欠測は期間の偏りを伴う」 |
| **PREREG:2189〜2192**(§8.6) | 「**2024 年 3〜6 月の分は 2024-06-01(判定区間に無い日)を挟んだ 89 日 + 7 日であるから**、この欠測は期間の偏りを伴う」 |
| **PREREG:3450**(§14.4 の ケ) | 同じ書き方に揃えた(決定 15 と同時) |
| 経緯の注 | PREREG:2930〜2937。**7 回目の走査語(`連続した 1 つの塊` / `1 つの連続した塊`)ではこの行に当たらなかった**ことを書いた |

**`grep -n '連続'`(範囲 = 本ファイル 1 本。切ったことを書く)の全該当行**:

```
 268: §0.1 の 8・9 の決定の引用「…「2024-03-04〜06-08 の連続 97 日」の記述は正しい集合に直す…」   ← 過去の決定の逐語。直さない
 352: §0.1 の 9 の決定の引用「…「連続した 1 つの塊」の残りを…」                                   ← 過去の決定の逐語。直さない
 385: §0.1 の本版の決定 7 の引用「97 日の塊の「96 日連続」を直す」                                 ← 本版の決定の逐語
2153 / 2156 / 2162: 「2024-03-04〜06-08 の連続 97 日」とは同じ集合ではない / 本書から消した       ← 消した書き方の説明(§8.6)
2193 / 2195 / 2197: 「連続した塊」の書き方は 8 回目の指摘 7 で直した / 過去の決定の引用            ← 本版の注(§8.6)
2780: 前版の併記文は「…2024-03-04〜06-08 の連続 97 日を欠いた 356〜359 日で出ている」だった        ← 消した書き方の説明(§10.3)
2910: 重なりの側で、2024 年 3〜6 月の連続した塊が丸ごと落ちる(実測、OI_DIST §2.5)                ← 別の集合(重なりの側)。判定区間の 97 日ではない
2919: 2023-11-19 の 1 日 + 2024-03-04〜05-31 の連続 89 日 + 2024-06-02〜06-08 の連続 7 日           ← 一覧。塊ごとに連続で正しい
2928 / 2931 / 2933 / 2935 / 2936 / 2937: 本版の訂正の注(§12)                                     ← 本版の注
```

**「96 日が連続している」と書いた行は、本版では 1 行も無い。**
**範囲は本ファイル 1 本に切っている**(`CLAUDE.md` §3。リポジトリ全体は数えていない)。

### 8.[直す] オーナーが最初に読む 1 行が 5 項目のまま

**処置: 6 項目にした。PREREG:3。**リードの決定(逐語):「**先頭行の凍結の宣言を 6 項目に**」

```
凍結: 走行の前に確定。以後、バー / 族 576 / α / h / 軸 48 群 / 判定に使う走行 2 本(= 6 項目)を変えない。
```

**経緯の注は PREREG:5〜10。**

### 9.[直す] 監査の回数を 8 回目までの現物に

**処置: 直した(5 か所)。**リードの決定(逐語):
「**「6 回の走行前の監査で」→ 8 回目までの現物の回数に(§14.6 の表と合わせる)**」

| どこ | 直した |
|---|---|
| PREREG:23〜31 | 「6 回の走行前の監査で、次の **4 つ**」→「**8 回**の走行前の監査で、次の **5 つ**」(**5 つ目 = 走行の道具**。§14.6 の表と合わせた) |
| PREREG:35 | 「この 4 つは」→「この 5 つは」 |
| PREREG:3192〜3196(§13) | 「対照の作り方・対照の符号・判定語・読みの道具**・走行の道具**は **8 回**の走行前の監査で動いた」 |
| PREREG:3605(§14.4 の表) | 「本版の時点で 7 回」→「**8 回**」(19 件を末尾に足した) |
| PREREG:3590 / :3624 | 「6 回の監査はいずれも」「5 回とも」→「**1〜6 回目**の監査はいずれも、7 回目・8 回目も」「**1〜8 回目のどの回でも**」 |
| PREREG:4424〜4428(末尾) | 「走行前の再監査を **8 回**受けた(… / 20 件 / **19 件**)」「8 回目の処置は `..._r8.md`」 |

### 10.[直す] summary の「突き合わせ先」がここだけ 2 本のまま

**処置: 直した。**リードの決定(逐語):
「**summary の「突き合わせ先」を「判定 2 本 + 感度 4 本の params.approval」に**」

**JUDGE の `summary["承認の L 番号(決定 2'''')"]["突き合わせ先"]` を
`"判定 2 本 + 感度 4 本の params.approval"` に変えた。**
**注の欄にも決定 14(感度の食い違いは判定側の破れと同じ扱い)を書いた。**
**事前登録は PREREG:4256 の `summary.json` の行に **(v)** として足した。**
**試験**: `::test_a_sensitivity_run_with_the_right_approval_still_passes` が
この文字列そのものを測る。

### 11.[直す] 台帳が閉じていても `stopped.txt` が 1 ファイル書かれる

**処置: 関門を前に移した。**リードの決定(逐語):
「**`pass_audit_gate` を `write_stopped` より前に(台帳が閉じていれば `stopped.txt` も書かない)。テスト**」

**JUDGE の `main()` で、3 つの検査を始める前に `pass_audit_gate(Path(a.root).resolve())` を呼ぶ。**
**表を書く直前の呼び出しは残した**(検査から表を書くまでの間に台帳が閉じられた回も止めるため。
**2 回とも外す旗は無い**)。**事前登録は PREREG:4311〜4324。**
**試験**: `::test_the_audit_gate_comes_before_stopped_txt`(**出力ディレクトリが 1 つも作られないことと、
実装で関門が `write_stopped` より前にあることの両方を測る**)。

### 12.[直す] `stopped.txt` に書いてよいものの線

**処置: 値を 1 つも書かない形にした。**リードの決定(逐語):
「**`stopped.txt` には値(bin_pct の差の最大値・cascade_id の例)を書かず、
検査の名前・走行の名前・件数だけ。§14.6 と実装。テスト**」

**JUDGE に `stopped_line(check, msg, prefix)` を足した。**
**理由の文言を濾すのではなく、名前と件数だけを拾って行を作り直す**(**拾わなかったものは
1 つも出ない**)。件数は「N 件」の形で書かれているものだけを写す。
**理由の全文は標準出力に出る**(出力ファイルには出ない)。

| どこ | 中身 |
|---|---|
| 事前登録 §14.6 の出力の表 | PREREG:4257(`stopped.txt` の行。「理由」を外した) |
| 事前登録 §14.6 の「書いてよいものの線」 | PREREG:4266〜4285(表 + 経緯) |
| 実装 | JUDGE `_COUNT_RE` / `stopped_line` / `write_stopped` |
| 試験 | `::test_stopped_txt_writes_no_values_only_names_and_counts`(**`bin_pct` も `cascade_id` の例も「最大」も出ないこと**)/ `::test_stopped_txt_records_which_check_broke`(**鍵の名前 `mmr` も値 `0.004` も出ないこと**) |

### 13.[直す] 確認が済む前の断定を凍結文書に置いてよいか

**処置: リードの確認の出所を書いた。**リードの決定(逐語):
「**末尾の断定に「リードが確認した(2026-09-18、ACTION_LOG 062: 残り 8 行を読み、
経緯の説明と走査の出力のみ)」と書く**」

| どこ | 直した |
|---|---|
| PREREG:4433〜4438(末尾の断定) | 「判定語を、判定として書いた行はこの文書に 1 つも無い(**下の走査と、その下の目視の分類による。リードが確認した(2026-09-18、`ACTION_LOG` 062: 残り 8 行を読み、経緯の説明と走査の出力のみ)**)」 |
| PREREG:4508〜4515(分類の節) | 「本処置の時点では「リード確認待ち」である」→「**リードが確認した(2026-09-18、`ACTION_LOG` 062)**」+ 経緯の注 |

**「この分類に機械は掛かっていない」はそのまま残した**(**隠さずに書く**)。

### 14.[聞く] 承認外の番号で開いた走行が 1 本在ったとき、判定の表を書いてよいと決めたのはどこか

**処置: 判定側の破れと同じ扱いにした。**リードの決定(逐語):
「**感度 4 本の `params.approval` の食い違いは判定側の破れと同じ扱い(表を 1 枚も書かず `stopped.txt`)。
理由: 承認外の番号で 456 日を開けた走行が 1 本でもあれば、1 周目はそこで止めてオーナーに報告する。
実装・テスト・§14.6**」

**JUDGE に `check_run_approval(run, approval, label)` を足し、感度の `check_run_params` からは
`approval` を外した**(承認の食い違いだけが `judge_bad` に入る = 表を 1 枚も書かずに終わる)。
**`params` が読めない感度には当てない**(「読めない感度」は決定 2''''' の側で扱う)。

| どこ | 中身 |
|---|---|
| 事前登録 | PREREG:4287〜4306(表 + 経緯) |
| 実装 | JUDGE `check_run_approval` と `CHK_APPROVAL = "承認の番号(決定 14)"` |
| 試験 | `::test_the_sensitivity_runs_also_match_the_approval_number`(**止まる側**。表が 1 枚も無いこと・`stopped.txt` に L 番号が出ないこと)/ `::test_a_sensitivity_run_with_the_right_approval_still_passes`(**通る側**) |

### 15.[聞く] 「リードが判断を代行しない」と書いた項目を、答えを求めない側に置いてよいか

**処置: ケ を「報告」の側に移した。**リードの決定(逐語):
「**ケ(主軸 D の欠け)は「事実」ではなく「報告(反対が無ければこの形で進める)」の側に置く:
「この欠けを抱えたまま走らせる = リードの判断(取り直し不可、代理は返答 060 の #5 と同じ論点なので
提案しない)。反対があれば止める」。§14.4 の ケ と、A-13 の注(「リードが判断を代行しない」)を
「リードの判断として明示し、オーナーが止められる形で出す」に書き換える**」

| どこ | 直した |
|---|---|
| PREREG:3419〜3437 | **分類と件数の表を足した**(報告 = ア・イ・ウ・エ・オ・**ケ** の 6 件 / 答えが要る = カ・キ・ク の 3 件 / 事実 = コ・サ・シ・ス・セ・ソ・**タ** の 7 件 / 合計 **16 件**)。前版は「ケ〜ソ が残る 7 件」「合計 15 件」 |
| PREREG:3450(ケ の行) | 見出しを「**ケ**(**報告**)」にし、末尾に「**この欠けを抱えたまま走らせるのはリードの判断である**(取り直し不可。代理は返答 060 の #5 と同じ論点なので提案しない)。**反対があれば止める**」を足した |
| PREREG:3482〜3490 | 「ケ はオーナーに選ばせる項目ではない。事実として報告する」→「**ケ は「報告(反対が無ければこの形で進める)」の側である。この欠けを抱えたまま走らせるのはリードの判断であり、反対があれば止める**」 |
| PREREG:3523〜3535(A-13 の注) | 「「この検出力で…」はリードが決めていない」「リードがここで判断を代行しない」→「**A-13 の当て方は「リードが判断を代行しない」ではなく「リードの判断として明示し、オーナーが止められる形で出す」である**」 |
| PREREG:3471〜3475 | 「報告は ア〜オ、事実として出すのは ケ〜ソ」→「**報告は ア〜オ と ケ、事実として出すのは コ・サ・シ・ス・セ・ソ・タ**」 |

### 16.[聞く] 「版はハッシュで残す」は何で担保されるか

**処置: 機械にした。**リードの決定(逐語):
「**走行スクリプトと読みのスクリプトは `git rev-parse HEAD`(取れなければ「不明」)を
`summary.json` に `tool_commit` として記録する。読みのスクリプトは事前登録 §14.4 の
「凍結した道具のコミット」欄が埋まっていれば自分の `tool_commit` と突き合わせ、
違えば「[止め]」(欄が「(まだ無い)」なら記録だけ)。§14.4 に書く。テスト**」

| 何 | どこ |
|---|---|
| 走行の道具 | REACT `tool_commit()`。`run_table` / `run_anchor` の `summary["tool_commit"]`(**`params` には入れない** = 読みの側の `params` の鍵の数を変えないため) |
| 読みの道具 | JUDGE `tool_commit()` / `read_frozen_commit_from_prereg()` / `commit_matches()`。`summary["tool_commit"]` と `summary["凍結した道具のコミット(決定 16)"]` |
| 突き合わせ | 欄が埋まっていて自分の版と違えば「[止め]」で 1 ファイルも書かない。**短縮形(7 桁以上)は前置一致で許す。**欄が「(まだ無い)」なら記録だけ |
| 事前登録の欄 | PREREG:3551「**凍結した道具のコミット**: **(まだ無い。開封の直前にここへ書き写す。)**」 |
| 事前登録の説明 | PREREG:3553〜3568(表。**担保していないこと**も書いた) |
| §14.4 の表の行 | PREREG:3603〜3604 |
| 試験 | `::test_the_frozen_tool_commit_is_recorded_and_matched`(**欄なし / 一致 / 短縮形 / 不一致**の 4 通り)/ `::test_the_run_records_its_own_commit` |

**担保していないこと(隠さずに書く)**: **欄に書き写すのはリードである。**
**機械が担保するのは「欄と、実際に走った道具の版が食い違ったら止まる」ことだけである**
(指摘 3 と同じ射程。PREREG:3568)。

### 17.[聞く] 報告を出さずに進める判断をリードが単独で行う形でよいか

**処置: 判断はリード単独のまま、検査できる形にした。**リードの決定(逐語):
「**「追加の報告を出さずに開封へ進む」の判断はリード単独だが、各回の監査の逐語と処置は
処置書に残り、開封前の最後の返答に「§13 の表と待つものが変わっていない」ことを
監査役(owner-model-auditor)が確認した記録を付ける、と §14.4 に書く**」

**PREREG:3578〜3588。**§14.4 の表に「**その判断を誰がするか**」の行を足した:
**(a) 各回の監査役の逐語と処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r<N>.md` に
1 件ずつ残る。(b) 開封前の最後の返答に、「§13 の表と待つものが変わっていない」ことを
`owner-model-auditor` が確認した記録を付ける。**
**「これは「変わっていないことの証明」ではない」と明記した**(**隠さずに書く**)。

### 18.[聞く] 「なぜ」を `stopped.txt` だけで終える帰結は、待つもののどれにも入っていない

**処置: 待つものに タ を足した。**リードの決定(逐語):
「**待つものに タ を足す: 「判定 2 本の検査が破れたら 1 周目は表を 1 枚も出さずに終わる
(理由のファイルだけ)」。事実の側**」

**PREREG:3457(タ の行)**:
「**判定 2 本(とその標本)の検査(`params` / サニティ #14 / 軸の作り方 / 承認の番号)が破れたら、
1 周目は表を 1 枚も出さずに終わる**(出るのは理由のファイル `stopped.txt` だけで、そこに書くのは
**検査の名前・走行の名前・件数**だけである = 決定 12)。
**456 日を 6 本開けたうえで判定の 576 行が 1 行も出ない帰結になりうる。**
`--mode full` の再走行はしない(§14.6)」
**分類は「事実」の側**(PREREG:3426 の表)。

### 19.[聞く] 2 周目の期間について、調達票は凍結の前に実行されたか

**処置: 期間を未確定に戻した。**リードの決定(逐語):
「**ソ を直す: 2 周目の期間は未確定(調達票 = 経路 3 つ以上・到達確認の後に決める)。
「2024-10-15 以降で測る」と決めた文を「新しい期間で測る(期間は調達票の後に決める)」に**」

| どこ | 直した |
|---|---|
| PREREG:501〜510(§1) | 「2 周目は新しい期間(2024-10-15 以降)で測る」→「**2 周目は新しい期間で測る。期間は未確定である**」+ 経緯の注(調達票の後に決める) |
| PREREG:1188〜1189(§7.1) | 同じ形に |
| PREREG:3064〜3069(§12) | 「2 周目は 2024-10-15 以降の新しい期間で測る」→「**2 周目は新しい期間で測る。その期間は未確定である**」+「**下の走査が示すのは「この環境の在庫には 2024-10-15 以降が 1 日も無い」ことだけで、どの期間なら取れるかは調べていない**(**未確認**)」 |
| PREREG:3456(ソ の行) | 「**よって 2 周目の期間は未確定である**(「2024-10-15 以降で測る」とは決めない。期間は `DELEGATION.md` §3 の調達票 = **取得経路 3 つ以上・到達確認**の後に決める)」を足した |

**調達票は実行していない**(**未確認**。本作業の範囲外である)。
**§0.1 の過去の決定の引用(PREREG:130)はそのまま残した**(**当時の決定の記録である**。
本版で何が変わったかは §1・§7.1・§12・§14.4 の 4 か所に書いた)。

### 追加(返答の監査 4 巡目の指摘。決定 15・18 と同じ節)

**リードの追加の文面(逐語)は §0.1 の対応表に全文を引いた。**行番号:

| # | 何 | どこ |
|---|---|---|
| **(1)** | §14.4 を返答 062 と同じ分類・件数に(報告 6 / 答えが要る 3 / 事実 7 = **16 件**) | **PREREG:3419〜3437**(分類と件数の表)/ :3450(ケ の見出しと末尾)/ :3457(タ)/ :3471〜3475(注)/ :3482〜3490 / :3523〜3535 |
| **(2)** | §14.4 ケ の「97(被覆 359)/ 100(被覆 356)」の書き分け | **PREREG:3450**(「**metrics の zip が無い日は 97 日(被覆 359 日)**、**`doi_pre_1h` が当日 + 前日の両方を要求するので実際に欠けるのは 100 日(被覆 356 日)**」+ 倍率を 1.1318 と 1.1270 の両方) |
| **(3)** | 「15 軸」を 1 度定義する | **PREREG:876〜885**(§4。「**W で展開した 15 軸**(W を持つ 6 行 = A1〜A5・E を 2 つに数えた 12 + B1・B2・D)= **表の 11 行**」。設計 §4 の「16 軸」との関係も 1 度だけ書いた)/ 参照側は :887(3 分位の切り方)と :1154(§4.3 の群の欄) |

---

## 3. 走らせた結果

### 1. 試験(`-q` 無し)

```
PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
........................................................................ [ 52%]
..................................................................       [100%]
138 passed in 53.11s
```

**リポジトリ全体**(`PYTHONPATH=src python -m pytest -q`)も走らせた
(**実測** 2026-09-18。終了コード **0** = 失敗 0 件):

```
..........................s............................................. [ 78%]
........................................................................ [ 81%]
...(略)...
................................                                         [100%]
[exited with code 0]
```

### 2. `--collect-only`

```
tests/test_o3c_reaction.py: 63
tests/test_o3c_reaction_judge.py: 63
tests/test_audit_gates_wired.py: 12
3 本まとめて: 138 tests collected
リポジトリ全体: 2120 tests collected
```

**前版は 3 本で 127 件(56 / 59 / 12)、リポジトリ全体 2,109 件。**
**足したのは 11 件**(REACT 側 7 件 / JUDGE 側 4 件)。**§14.6 に書いた**(PREREG:4341〜4344)。

### 3. §8.5 の再現(152 セル)

```
照合したセル: 152 / 一致 152 / 食い違い 0
```

**標本 4 本を走らせ直した後に打った**(**実測** 2026-09-18)。

### 4. 標本 4 本の走らせ直しの突き合わせ

**`--mode sample --seed 1 --mmr 0.004` で 4 本、`pandas` で全列を突き合わせた**(**実測** 2026-09-18)。

```
gap60_w8/table.csv: 行×列 (662, 157) -> (662, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/summary.json: 食い違い 2 件 -> .elapsed_sec: 10.8 -> 10.48 / .tool_commit: (無し) -> 759f0920e7cb56174cc97239a0989c7a474d2432
gap60_w8/MD5SUMS: 変わったファイル ['summary.json']
gap60_w24/table.csv: 行×列 (665, 157) -> (665, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/summary.json: 食い違い 2 件 -> .elapsed_sec: 10.24 -> 10.31 / .tool_commit: (無し) -> 759f0920e7cb56174cc97239a0989c7a474d2432
gap60_w24/MD5SUMS: 変わったファイル ['summary.json']
gap30_w8/table.csv: 行×列 (784, 157) -> (784, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/summary.json: 食い違い 2 件 -> .elapsed_sec: 11.18 -> 10.67 / .tool_commit: (無し) -> 759f0920e7cb56174cc97239a0989c7a474d2432
gap30_w8/MD5SUMS: 変わったファイル ['summary.json']
gap180_w8/table.csv: 行×列 (503, 157) -> (503, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/table_mixed.csv: 行×列 (2, 157) -> (2, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/summary.json: 食い違い 2 件 -> .elapsed_sec: 10.18 -> 9.95 / .tool_commit: (無し) -> 759f0920e7cb56174cc97239a0989c7a474d2432
gap180_w8/MD5SUMS: 変わったファイル ['summary.json']
```

**表を作る計算・列・乱数の消費は 1 行も変えていない**(**4 本とも `table.csv` / `table_mixed.csv` が
全列一致**)。**`summary.json` の食い違いは `elapsed_sec`(所要秒)と `tool_commit` の追加の 2 件だけ。**
**`MD5SUMS` で変わったのは `summary.json` だけ。**
**§14.6 の「走行の道具を変えた回」の表に 4 行目として足した**(PREREG:3904 と :3906〜3929)。

**git の差分でも同じことが見える**(**実測** 2026-09-18、`git diff --stat backtest_data/o3c_reaction_20260918_sample/`):

```
 gap180_w8/MD5SUMS      | 2 +-
 gap180_w8/summary.json | 3 ++-
 gap30_w8/MD5SUMS       | 2 +-
 gap30_w8/summary.json  | 3 ++-
 gap60_w24/MD5SUMS      | 2 +-
 gap60_w24/summary.json | 3 ++-
 gap60_w8/MD5SUMS       | 2 +-
 gap60_w8/summary.json  | 3 ++-
 8 files changed, 12 insertions(+), 8 deletions(-)
```

**`table.csv` と `table_mixed.csv` は 8 ファイルとも差分に 1 行も出ていない**
(= **バイト単位で同一**)。

**`tool_commit` の値 `759f0920e7cb…` は「走らせ直した時点の `git rev-parse HEAD`」であって、
本版の変更を含むコミットではない**(**本版はコミットしていない**)。

### 5. 判定語の走査(範囲 = 本ファイル 1 本。切ったことを書く)

**本版の追記で数は 1 つも動いていない**(**実測** 2026-09-18。4 本とも前版と同じ出力):

```
--- 予測できる ---   ファイル数 1 / 行数 5  / ファイルごとの出現数 5  / 出現数の合計 5
--- 使える ---       ファイル数 1 / 行数 6  / ファイルごとの出現数 6  / 出現数の合計 6
--- 有効 ---         ファイル数 1 / 行数 14 / ファイルごとの出現数 21 / 出現数の合計 21
--- 差なし ---       ファイル数 1 / 行数 12 / ファイルごとの出現数 16 / 出現数の合計 16
--- 陰性 ---         ファイル数 1 / 行数 27 / ファイルごとの出現数 34 / 出現数の合計 34
```

**リポジトリ全体は数えていない**(この走査は本書の中の判定語を見るためのものである)。

---

## 4. 作業中に必要になった、原文に無い判断(4 件。黙って決めていない)

| # | どこ | 決めたこと | 理由 |
|---|---|---|---|
| 1 | 決定 1 の「判定区間の日」の作り方 | **在庫(`--data-root`)から `judgment_day_set` を作る。**在庫が読めない / 空なら集合は空になり、この関門は何も止めない | リードの逐語は「日集合が判定区間の日(`judgment_days`)を 1 日でも含めば」で、`judgment_days` は在庫を引数に取る関数である。**在庫が無いときの挙動は逐語に無い。**「空集合 = 止めない」を選び、**射程として PREREG:831 と関数の注に書いた**(そのときは走らせても読む日が 1 日も無い) |
| 2 | 決定 2 の追記の時点 | **関門を通った直後(出力を書く前)に追記する** | 逐語は「`--mode full` が走るたびに追記」。**走行が途中で落ちた回を「開けていない」と扱うか**が逐語に無い。**落ちた回も「開けた」として残す**方を選んだ(落ちた回を消してやり直す経路も (g) で止まる)。`record_opened` の注に書いた |
| 3 | 決定 12 の「件数」の拾い方 | **理由の文言に「N 件」の形で書かれているものだけを写す。**無ければ件数を書かない | 逐語は「検査の名前・走行の名前・件数だけ」。**`check_run_params` の理由文には「N 件」の形が無い**(例: 「params.mmr が None」)。**そこで件数を無理に作らず、名前だけの行にした。**PREREG:4270 の表に「(理由の文言に「N 件」の形で書かれているものだけ)」と書いた |
| 4 | 決定 16 の一致の判定 | **前置一致で許す**(欄が短縮形 7 桁以上でも通る) | 逐語は「自分の `tool_commit` と突き合わせ、違えば「[止め]」」。**欄は手書きなので短縮形がありうる**が、逐語はそこに触れていない。**短縮形を「違う」として止めると、正しい版でも開封できなくなる。**`commit_matches` の注と PREREG:3566 に書いた |

---

## 5. 触っていないもの(委任の禁止事項)

- `scripts/_research_audit_gate.py` — **1 行も触っていない。**
- `docs/AUDITOR/HOOK_MANIFEST.sha256` — **1 行も触っていない。**`regen_hook_manifest.sh` も走らせていない。
- `.claude/` — **1 行も触っていない。**
- `.gitignore` — **1 行も触っていない**(決定 2 は「必要なら `!` の例外行を足す」だったが、
  **`git check-ignore` の実測で除外されていないことを確かめたので、足す必要が無かった**)。
- `--mode full` — **1 本も走らせていない。**
- `backtest_data/o3c_reaction_20260918_full/` — **作っていない**(**実測**: 存在しない)。
- コミット・プッシュ — **していない。**
