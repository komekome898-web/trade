# 調査班の委任文 改訂版(2026-09-22)の監査の記録

対象: `docs/DATA/delegations/20260922_tools_survey_prompt.md`
経緯: オーナー L-387「委任文がそもそも不適切 / 直し方がその場しのぎ」→ L-388「それで進めてみてください」。
承認済みの前の版 = `20260921_tools_survey_prompt.md`(指紋 840bf99ca5b0)。

## 1 回目(v11、指紋 d822acb9ef5f)— 監査役の出力(逐語)

> 15 回にわたる監査記録・スクリプト・旧版との diff を確認した。以下を報告する。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-21_tools.md` 系列で最も重かった欠陥型(round1 指摘1「本文『実測した(生ログ参照)』の生ログ本体に対応する成功実行が無く、背景起動しただけで出力を読んでいなかった」、リード自身が「今回いちばん重い指摘」「FILLEDの結果を一度も見ずに本文を書いた」と認めた型。round2 でも NautilusTrader に同型再発)を、改訂 3 箇所(§4.0 / §4.1 / §12)のどれも直接には防いでいない。§4.0 の根拠欄は「`実測` なら `<生ログのファイル名>:<行番号>`」(`docs/DATA/delegations/20260922_tools_survey_prompt.md:58`)を要求するが、引用した行番号の中身が実際にその主張を裏づけているかを検査する機械は `scripts/check_scan_report.py` のどこにも無い(K1〜K10 に該当検査なし)。バックグラウンド起動(`started pid=`)と出力未読を区別する規則も §4.0/§4.1 のどこにも追加されていない。この改訂で当該型は本当に塞がるか(P6・P10、CLAUDE.md §0.2 O-2/O-6)。
>
> 2. [止める] `docs/DATA/delegations/20260922_tools_survey_prompt.md:48-54` の 37 項目固定語彙に、同文書 `:93`(§6-1)「**PyPI の配布元と GitHub の一致** / 初回公開日と最新版の日付 / 週のダウンロード数 / 保守者の数と**名前の一貫性** / …`setup.py` `pyproject.toml` の導入時実行・**難読化**・**外部 URL からの取得**・バイナリ同梱が無いか / 既知の脆弱性 / **依存の一覧**」が要求する項目のうち、「配布元の一致」「難読化」「外部URLからの取得」「依存の一覧(名前)」に対応する項目が無い(あるのは「依存数」=件数のみ、「保守者数」=数のみで名前の一貫性は無い)。K8(`check_table_complete`)は**語彙に無い項目の欠落を検出できない**構造なので、この 4 項目は §12 を通っても永久に検査対象に入らない。安全側の検査(§6)がまさにこの型で漏れる余地を、通した検査自体が塞げていないのではないか(P3・P14 の隣接型)。
>
> 3. [直す] `docs/DATA/delegations/20260922_tools_survey_prompt.md:57`「**印**: `一次資料` / `実測` / `推定` / `仮定` / `未確認` のどれか 1 つ。定義は §4.1。」(新設)は、同文書 `:75`(§4 冒頭、今回無変更)「**すべての列のすべての数値・主張に 一次資料 / 実測 / 推定 / 仮定 の印を付ける**」および `:81`(§5.5、今回無変更)「**4 軸それぞれに 一次資料 / 実測 / 推定 / 仮定 の印(§4 冒頭と同じ 4 分類)。**」と矛盾する。旧本文は明示的に「4 分類」と書いているのに、新設の §4.1 はそこに `未確認` を加えて 5 分類にした。同じ語「印」を文書内で 2 通りに定義している(P2、CLAUDE.md §0.2 O-7「一度出した用語の意味を変えて使い回す」)。
>
> 4. [直す] `scripts/check_scan_report.py` の検査一覧(`docs/DATA/delegations/20260922_tools_survey_prompt.md:134-144`)は K1・K2・K3・K4・K6・K7・K8・K9・K10 で、**K5 が存在しない**。`scripts/check_scan_report.py` 本体(`main()` の `checks` リスト、208〜215 行)にも K5 に相当する関数呼び出しは無い。欠番の理由が委任文にもスクリプトのコメントにも書かれていない。P5(数え上げの検査)と同型で、「検査は 9 本」と数えたときにこの欠番自体が説明なしに存在する。
>
> 5. [聞く] `scripts/check_scan_report.py` の `check_conflicting_values`(K6、106-108 行)と `check_contradiction`(K7、127-128 行)は `tools = [...]` として区分1(バックテスト系)で見つかった固有名詞(hftbacktest・NautilusTrader・QSTrader・Jesse 等)を**ハードコード**している。区分2〜8(執行・データ・LLM・スクリーニング等)や区分1の3回目以降で新しく見つかる道具名はこのリストに無いため、同型の矛盾(未実施/実施の同居、同じ道具に別の値)が起きても K6・K7 は当たらない。§12 はこの検査を「受け入れ検査」として一般化して掲げているが、実体は区分1・実行2回目の特定候補に対してのみ機能する。次回以降(他区分・区分1の3回目)、このリストを誰がいつ更新するかが委任文に書かれていない(P8)。
>
> 6. [止める] round2 の 45 件のうち繰り返し最も多く検出された型(「直した箇所の隣の直し残し」— hftbacktest→NautilusTrader、zipline/Qlib/VnPy→Lean CLI→QSTrader、Jesse の危険欄、「9候補」ラベルと結果欄、「FILLED実証済み」の帰属表現、「185」の依存数、見出し「8件」と内訳の不一致など、`docs/AUDITOR/VERDICTS/2026-09-22_tools_scan_cat1_run2.md` で少なくとも 6〜9 回同型指摘)の多くは**非数値**(帰属の言い回し・見出しと内訳の一致・候補の件数)である。§4.0 の設計思想(`docs/DATA/delegations/20260922_tools_survey_prompt.md:41-42`「同じ事実が文章の4箇所に散らばっていたために起きた。表を1つの在処にすれば、機械が突き合わせられる」)は「道具・項目・値・印・根拠」という定量セルの重複だけを対象にしており、K4/K6/K10 も秒・個・パッケージという単位付き数値しか突き合わせない。見出しと本文の件数不一致(round1 指摘2・8、round6 指摘2)、非数値の帰属表現の重複(round1 指摘1、round2 指摘1、round6 指摘1、round7 指摘2)は、この改訂の後も同型のまま再発しうるのではないか。
>
> 7. [直す] 今回の依頼文(背景)に「改訂は 3 箇所だけ(前の版から 65 行追加・1 行削除、他は 1 文字も変えていない): §4.0…/§4.1…/§12…」とあるが、`diff docs/DATA/delegations/20260921_tools_survey_prompt.md docs/DATA/delegations/20260922_tools_survey_prompt.md` を実行すると、`83c121` として §11「完了の形」の1行が書き換わっている(旧: 「**1 回の実行の完了** = 担当区分について…」/ 新: 「**1 回の実行の完了** = **§12 の受け入れ検査が 0 件で通っていることに加えて**、担当区分について…**§4.0 の機械可読の表(深掘りした道具は 37 項目すべて)**…」)。これは §12 とは別の第 4 の変更箇所であり、「他は 1 文字も変えていない」という記述と食い違う(P5、CLAUDE.md §3「数え直しは…そのコマンドを結果と一緒に出す」に照らし、この diff 自体で機械的に確認できる)。
>
> 8. [聞く] `docs/DATA/delegations/20260922_tools_survey_prompt.md:146`「**検査を黙って無視しない。**誤検出だと判断したものは、その理由を報告に 1 件ずつ書く。」は、検査対象本人(調査班)が自分の検査結果を自分で「誤検出」と判定して免除できる構造になっている。`docs/AUDITOR/VERDICTS/2026-09-22_tools_scan_cat1_run2.md` の 15 回の往復は、名指しされた箇所だけ直し隣の同型を見落とす(リード自身「8 回繰り返した」と自認、299-301 行)という経緯があった。この自己判定による例外化が、同種の「その場しのぎ」を検査の外側で再生産する余地にならないか(CLAUDE.md §0.2 O-10「自分の作業を自分で採点して書く」に近い型)。
>
> 9. [聞く] `docs/DATA/delegations/20260922_tools_survey_prompt.md:148-149`「**この検査は「書いてあるか」しか見ない。**中身が正しいかは見ない。だから検査が 0 件でも、§4・§5・§6 の規則を守っていないものは差し戻しになる。検査は下限であって合格点ではない。」という注記があるにもかかわらず、`:121`(§11)は §12 の 0 件通過を「1 回の実行の完了」の並列条件の一つとして書くだけで、「§4・§5・§6 を守っているか」を**誰が・いつ**別途判定するかが書かれていない。「差し戻しになる」という受動態の主語(誰が差し戻すか)が不明であり、行動の直前にこの規則を読む導線が委任文の中に無いのではないか(P8)。
>
> 10. [直す] `docs/DATA/delegations/20260922_tools_survey_prompt.md:61`「**深掘りした道具は 37 項目すべての行を持つ。**1 行でも欠けたら未完了である。」と K8 のコメント(`scripts/check_scan_report.py:161`「監査 1 回目 = 危険検査の 3 項目が全候補で欠落」)を突き合わせると、K8 が実際に検査できるのは「表に載っている道具名について 37 項目が揃っているか」だけであり、「深掘りしたはずなのに表に一行も現れない道具」自体は検出できない(`read_table` は表の行を集めるだけで、候補一覧との突き合わせをしていない)。候補一覧(§3)にある「深掘り済み」の道具のうち §4.0 表に載っていないものを検出する仕組みが無い。
>
> 参照した成果物: `docs/DATA/delegations/20260922_tools_survey_prompt.md`、`docs/DATA/delegations/20260921_tools_survey_prompt.md`、`docs/AUDITOR/VERDICTS/2026-09-21_tools_survey_prompt.md`、`docs/AUDITOR/VERDICTS/2026-09-22_tools_scan_cat1_run2.md`、`scripts/check_scan_report.py`、`docs/AUDITOR/PRINCIPLES.md`、`docs/AUDITOR/KNOWN_ANSWERS.md`。

## リードの処置(v12、指紋 38a3ce244a08)

**10 件のうち 9 件は当たっていた。1 件だけ事実が違う。**1 件ずつ答える(§0.2 A-19)。

1. **止める → 一部は事実と違うが、主旨は当たっていたので直した。**
   「背景起動と出力未読を区別する規則も §4.0/§4.1 のどこにも追加されていない」は**事実と違う**。
   規則は 69 行目にある: 「背景で起動しただけ、出力を読んでいないものは **`未確認`** と書く(`実測` と書いてはならない)」。
   ただし**「機械が無い」という主旨は当たっている。**規則だけでは、1 回目・2 回目と同じく守られたかを誰も見ない。
   **K11 を新設した**: 印が `実測` の行について、根拠が実在する生ログの行を指しているか、その行が
   `started pid=` の背景起動ではなく `rc=` / `time_s=` を持つ実行の結果かを見る。
   欠陥入力(背景起動の行を根拠にした行 / 存在しない行番号 / 「手元の記憶」と書いた根拠)で 3 件とも当たることを確認した。
2. **止める → 直した。**指摘のとおり 5 項目(`配布元の一致` `難読化` `外部URL取得` `依存の一覧` `保守者名の一貫性`)が
   §6-1 の要求にあるのに語彙から抜けていた。語彙に足した。
   **あわせてリード自身の誤りが 1 件出た**: 委任文に「37 項目」と書いていたが、実際の語彙は 38 項目だった。
   数を文章に書いたことが原因なので、**委任文から項目の数を消した**(語彙そのものを唯一の在処にした)。
3. **直す → 直した。**§4 冒頭と §5.5 を 5 分類に揃えた。O-7 の型であるという指摘は妥当。
4. **直す → 直した。**K5 の欠番は、下書きで 1 本落としたまま番号を詰めなかったもの。K1〜K11 に詰め、欠番を無くした。
5. **聞く → 直した(聞き返さずに直せる指摘だった)。**道具名の直書きをやめ、表の 1 列目と候補の一覧から
   自動で取るようにした。区分ごとの書き換えが要らなくなったので、「誰がいつ更新するか」の問題自体が消えた。
6. **止める → 一部を直し、残りは「塞いでいない」と明記した。**
   非数値の型のうち**見出しの件数と内訳の不一致は K10 を新設して塞いだ**(欠陥入力で確認済み)。
   帰属の言い回しは、表の行については K11 が見るが、**文章の中の言い回しは見ていない。**
   委任文の §12 末尾に「この検査が塞いでいない型」として全部書いた。塞いだふりはしない。
7. **直す → 認める。**監査役への依頼文で「他は 1 文字も変えていない」と書いたのは誤り。
   §11「完了の形」の 1 行を書き換えている(第 4 の変更箇所)。**オーナーへの報告でもこの行の書き換えを名指ししていなかった**
   (「消したのは 1 行だけ」とは書いたが、それが §11 であることを書いていない)。オーナーに訂正して伝える。
8. **聞く → 直した。**調査班が自分で「誤検出」と判定して閉じられる構造だった。
   **直さずに残し、理由を書いてリードに渡す。判定するのはリード**に変えた。
9. **聞く → 直した。**「リードが差し戻す。判定するのはリードで、時点は報告を受け取った直後(監査役にかける前)」と主語と時点を書いた。
10. **直す → 直した。**候補の一覧で `[深掘り]` と印を付けた道具が §4.0 の表に 1 行も無い場合を K7 が拾うようにした。
    欠陥入力(表に無い `ghosttool`)で当たることを確認した。

**検査は 9 本 → 11 本。**新設した K10・K11 と、直した K7 は、いずれも**その欠陥を含む入力で当たることを確認してから**常設にした
(2026-09-22 に落ちない検査を 2 回作ったため、この確認を手順に入れた)。
