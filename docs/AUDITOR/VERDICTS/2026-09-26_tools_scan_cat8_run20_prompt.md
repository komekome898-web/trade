# 監査: 道具サーベイ 区分 8 の 20 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md`(19 回目の起動文から作った、それだけで読める形)。

## 監査 98 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run20_prompt.md@7f7cad43eaaf`、コミット d8cec0f)の逐語

> ## 監査 98 回目への回答
>
> 読んだもの: 対象の起動文全文(`docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md`、指紋 `7f7cad43eaaf`、コミット `d8cec0f` で実測一致確認済み)/ 19 回目の起動文全文と対応する監査記録 `2026-09-26_tools_scan_cat8_run19_prompt.md` / 19 回目の検収 `2026-09-26_tools_scan_cat8_run19.md`(§6 の中身の監査 97 回目を含む)/ `CAT8_DESIGN.md` §1〜§9 / `tools_catalog_cat8.tsv` の 8-038 行 / `SCAN_2026-09-23_tools_cat8.md` の 17・19 回目の節(実物の行番号を `grep -n` で実測)/ `scripts/cat8_ledger.py`(`cmd_check_elements`・`classified_searches`・`ELEM_TERMS_WIDE` を全文)/ 参照スクリプト 6 本の実在確認 / 参照ファイル 2 本(委任文・追補)の指紋を `sha256sum` で実測し起動文の記載と一致確認。
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:22`(§1 の 500 行規則)と `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:48`(§2.4「この回は、ファイルの表 (甲) だけを使う」)— この 2 文は「当たりが 500 行を超えたら、その要素の当たりの判定をせず(甲の表を書かず)`未判別` にしてよい」と定めるが、`scripts/cat8_ledger.py: cmd_check_elements`(行 934〜959、`classified_searches` 呼び出しは round 15 以降 (乙) の実行痕跡だけを拾う)は、その回の生ログにある **すべての** `cat8_search.py` の手について、(乙)`cat8_classify.py` で分類されていない限り、当たったファイルが全部 `### 当たりの判定` に理由つきで無いと無条件にエラーを出す(値が `印`/`なし`/`未判別` のどれであるかは見ない)。この回は §2.4 で (乙) の使用を禁じているため `classified_searches` は空集合になり、この無条件チェックが必ず効く。つまり、500 行を超える検索を実際に `cat8_search.py`(§2.4 が唯一許す検索手段)で走らせて件数を把握した時点で、その手の完了を示す `cat8_search: complete ...` の行が生ログに残り、それに続くヒットファイルを 1 件でも `### 当たりの判定` に載せなければ `check-elements --round 20`(§7.2、0 件必須)が落ちる。500 行超の要素で「判定をしない」という §1 の指示に文字どおり従うと、返す前の §7.2 の関門を通せない(逆に通すために全ファイルを判定すれば、500 行規則を導入した理由=「型の当てはめに戻る」を自分で再現する)。`cat8_ledger.py` 側に対応する例外が無く、起動文にもこの矛盾を解く手順(生ログの取り方・回避策)が書かれていない。コードの当該部分を `grep -n "未判別"` で全数確認したが、要素の値やヒット行数によるチェック除外は一切無い。
>
> 2. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:22` — 「500 行」という上限の根拠が起動文にも設計票にも書かれていない。12〜19 回目の実測(例: 19 回目の E1a 1,572 行)を踏まえた値だとは推測できるが、一次資料・実測・仮定のどの分類かが明記されておらず、なぜ 300 でも 1000 でもなく 500 かが分からない。
>
> 3. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:30`(E1a と E2 の境目の新設文)— 「データどうし(例: ティックと足、取引所どうし)の食い違いを見つける機能は E2…で、E1a ではない」は 19 回目検収 §3 の個別事例(実ティックと分足の自動突き合わせ)を一般化したものだが、設計票 `CAT8_DESIGN.md:49` の E1a 述語「2 つ以上の実装(**または実装と参照値**)の出力を突き合わせて、差を出す機能」が明示的に含む「実装と参照値」の場合(例: 自分の計算結果を取引所の生データという参照値と突き合わせて差を検出する機能)への言及が無い。この一文だけを読むと、本来 E1a の「実装と参照値」に当たりうる機能まで機械的に E2 に振り分けられないか。設計票の逐語(「実装と参照値」を含む)を狭めていないかの確認が要る。
>
> 4. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:90`(§7.5 の返答項目)— 「この回の `なし` の全部の…判定に (甲)(乙) のどちらを使ったか」という問いが残っているが、この回は §2.4 で (乙) の使用自体を禁じているため、答えは常に (甲) 固定であり、19 回目の起動文からの写し残しに見える(意味を持たない設問)。
>
> 5. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:14` — 「19 回目の節…この節は小節・表の形の見本だけにする。中身を写さない」という注意書き自体は 97 回目の指摘に対応しているが、その見本の節(`SCAN_2026-09-23_tools_cat8.md:36538` の AlgoNetwork 行、`:36542` の Oryon 行)は、97 回目 [止める] で問題視された「この回に取り直した」という事実でない長文がそのまま実物として今も残っている(処置は 検収記録側の説明のみで、報告本文は訂正されていない)。一般則(「6 列目は `台帳の値のまま(<回>回目の節)` だけにする」)で防げる設計にはなっているが、見本として提示する節に「まさに真似てはいけない実例」が 2 行そのまま残っている状態を、起動文で名指しして避けるよう書かなくてよいか。
>
> **該当なし** — 回の番号・生ログの名前(`20260923_tools_8_run20.log`)・§7 のコマンド(`--round 20`、`run1.log`〜`run20.log` の 20 本)・8-038 の出発点(17 回目の節、実測 33568 行目から一致)・台帳の値(E1a・E2・E3a のみ `未判別`、他は 17 回目に `印`。台帳の実物と一致)・§4.0 の「15 項目」の数え直し(17 回目の節の 8-038 行を実測し 15 件と一致確認)・検索語の 8 要素の組(`ELEM_TERMS_WIDE` と全語一致を実測確認、19 回目の E5/E4 欠落の再発無し)・参照ファイルの指紋(`ce0012c95154`・`2c178ba75341` とも実測一致)・8-029/MetaTrader 固有の消し残し(grep で 0 件)については、19 回目からの食い違いや消し残し・消しすぎは見つからなかった。
>
> **点 4(このまま実行できるか)への回答**: 指摘 1 により、「500 行超の要素を未判別のまま §7.2 の関門を 0 件で通す」という組み合わせが機械的に成立しない可能性が高く、これは調査班が実行中に必ず遭遇しうる詰まりである。**止める指摘は 1 件ある**ため、「止める指摘は無い」とは言えない。

## 処置(リード、監査 98 回目)

1. **直した(手順で。道具は変えない)**。`cat8_search.py` を打つ前に、同じ一覧・同じ語で当たりの行の数だけを見積もる手(`grep -I -c -i -E` の数を足す)を打たせる。見積もりが 500 行を超える要素には `cat8_search.py` を打たず、`未判別` にして見積もりの手の行と数を書く。件数だけの手は `cat8_search.py` の手ではないので、検査はその当たりの判定を求めない(`なし` の根拠にもならない)。500 行以下なら `cat8_search.py` を打ち、全部判定する。
2. **答える(書いた)**。500 は仮定: 12〜19 回目の観察で、数十〜数百行の要素では 1 行・1 ファイルごとに違う理由が書けていた(17 回目の AI Trading Lab 40 行など)が、560 行(17 回目の行の表)と 2,998 行(16 回目)では同じ文の当てはめになった。境目を実測したわけではない、と起動文に書いた。
3. **直した**。「計算の出力(損益・指標・約定の再計算など)を参照値(取引所の明細・別の計算の結果など)と照らして差を出す機能は、E1a の「実装と参照値」に当たる」を足した。E2 の側に振るのは、生のデータどうしの食い違いを見つける機能だけ。
4. **直した**。返答の項目から「(甲)(乙) のどちらを使ったか」を消した。
5. **直した**。起動文で、19 回目の節の AlgoNetwork・Oryon の 2 行(候補の一覧と「要素と段」の表)を、18 回目の文を写した誤りとして名指しし、見本にしないと書いた。

直したあとの起動文の印: `20260923_tools_survey_cat8_run20_prompt.md@f7ab0d66ecf8`。止める指摘(1)の直しは新しい手順なので、監査 99 回目に掛ける。
