# 監査記録: 道具サーベイ 区分 8 の 6 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run6_prompt.md`(初版の指紋 `8737790d5173`)。5 回目の起動文を元に、範囲(残り 6 行)・N の定義・§2 の項目を改めた。

## 7 回目以降に回すもの(ここに全部書く)

| 何 | 出所 |
|---|---|
| `深掘り` の行(8-001・8-002・8-009・8-010・8-011・8-012・8-014・8-015)と 8-004・8-005 の `なし` の当て直し(N の定義は 6 回目の起動文 §2) | 4 回目の検収 §7 の 7、監査 21 回目、5 回目の検収 §2 |
| 段が `未判別` の行の当て直し(8-002・8-011・8-012 の E5、8-014 の E1b・E3b・E4 ほか。6 回目の 6 行を除く) | 監査 21・22・30 回目 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜5 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| `akurkar07/OrderBook` の E6 | 同上 |

## 監査 31 回目(対象 = 7da2282、起動文の指紋 `8737790d5173`)

監査役の出力(逐語。[直す] 1 件・[聞く] 3 件。確かめて問題の無かった点の要点: 5 回目の検収と監査 30 回目の処置は漏れなく入っていた / 残り 6 行と、段が `未判別` の行を機械で数え上げた結果が §1・§2・7 回目以降の表と一致 / 8-006 の E3b・8-016 の (イ) は設計票の逐語どおり / §7 のコマンドは打てて、指紋は一致):

> 1. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run6_prompt.md:38 — 8-006(freqtrade)だけ、Nを事前に「`docs/` の全ファイルと README」に限定している。同じ§2の冒頭(32行)にある今回新設の一般規則「N は…一次資料の一覧を取った手の出力の件数そのもの…検索した範囲の件数ではない」、および§0で読ませる `20260922_tools_survey_prompt.md` の一次資料の定義(「公式の文書・**リポジトリ**・API の応答そのもの」= ソースコードも含む)と比べると、freqtradeのソースコード部分をNから外してよい理由が本文に書かれていない。5回目で差し戻された「検索した範囲をNにする」型(ヘルプの1分類だけ・`.rs`だけ等)と、範囲の狭め方として構造的に同じにならないか(P1類似・P13)。
>
> 2. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run6_prompt.md:16(§0の6が引く `20260923_tools_survey_cat8_run3_prompt.md` の「なしと書くための条件」(a): 「README に加えて `docs/`・`wiki`・API の文書など文書の全部を読む」)と 同ファイル:32(今回のN定義: 「リポジトリならファイル一覧の全件」)— 前者はリポジトリの一次資料を「文書」に絞り、後者は「ファイル一覧の全件」(ソースコード含む)としており、両方を「この回のすべての `なし` に当てる」と指示しているが、字面が食い違う場合にどちらを優先するかが本文に書かれていない。8-006の docs 限定(指摘1)は、この古い(a)の読みが N を狭める抜け道として残っている結果に見えないか(P2)。
>
> 3. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run6_prompt.md:17 — 「5 回目の `なし` 11 件のうち 10 件が…差し戻された」という数字は、`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5.md` §2 段階(打ち直し直後)のスナップショットのままで、同ファイル末尾の監査30回目 指摘5・リードの処置5(backtrex 8-007 の E3a も「台帳で `未判別` に戻した」)を反映していない。処置後の実態は 11 件のうち **11 件全部**が最終的に差し戻されている(§2 の 8-007 は当初「受け取る」だったが後で覆った)。読ませる文書の数字が最終状態と食い違っている(P5、数え上げの検査)。
>
> 4. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run6_prompt.md:40(8-008 FX Replay の指示)— 監査30回目 指摘10・リードの処置10(「6回目で旧根拠を含め全部を取り直す」)を受けているが、run6 の該当項目は「まず Support Center の全分類の全記事の一覧を curl で取り…」とだけ書かれており、旧根拠(WebFetch 由来の「No automated reconciliation mechanism exists between platforms」等)を今回取得した記事群と突き合わせて存在を裏取りする、という処置10の文言が委任文に明示的に落とし込まれているか(暗黙に「当て直す」に含まれるという読みでよいか)。

**リードの処置**(起動を止める指摘は無いが、どれも起動の前に直せる小さな直しなので直した):
1: 認める(5 回目と同じ型をリード自身が起動文に書いていた)。8-006 の N を「リポジトリのファイル一覧の全件(`docs/` だけにしない)」にした。
2: 認める。§0 の 6 に「(a) の『文書の全部』とこの起動文 §2 の N の定義が違うときは、広いほう(§2)を当てる」を足した。
3: 認める。「11 件とも差し戻された(10 件は N を検索の範囲で数えた、8-007 の 1 件はブログが N に入っていなかった)」に直した。
4: 認める。8-008 の項に「4 回目の WebFetch の要約にあった文が、取った記事の原文に実際にあるかを 1 つずつ確かめて書く」を足した。
直した版(指紋 `a3a735b35643`)は監査 32 回目で確かめてから印を記録する。
