# 監査(owner-auditor)— 段 A 事前登録への **5 回目(走行前の再監査)19 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction_judge.py`(以下 JUDGE)、試験 2 本
(`tests/test_o3c_reaction_judge.py` / `tests/test_audit_gates_wired.py`)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r2.md`(23 件 + ア〜エ)、
3 回目は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r3.md`(23 件)、
4 回目は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r4.md`(22 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 3(1〜3)/ 直す 13(4〜16)/ 聞く 3(17〜19)= 19 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない。§5 の実測)。
**`scripts/o3c_reaction.py` は 1 行も変えていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` は
1 行も触っていない。`regen_hook_manifest.sh` も走らせていない。**
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**コミットもプッシュもしていない。**

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 19 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r5.md` を新設(逐語を一字も変えずに貼り、19 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r4.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r5.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| JUDGE と試験を直す | リードの決定 1・2・3・12・15・16 の逐語(各件に引いた) |
| **`scripts/o3c_reaction.py` を変えない** | リードの委任文面「**`scripts/o3c_reaction.py`・`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/` は触らない(指紋の台帳に載っている。`regen_hook_manifest.sh` も走らせない)**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 標本 6 日での動作確認 / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 5 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**19 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 7 つ目の表(19 行)に明記した。**

**作業中に、原文に無い判断が必要になった点が 2 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書き、報告でも先に出す**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。
**作業を止めなかったのは、2 件とも残りの 17 件を進めるのに必要な分岐で、
どちらの選び方でもリードの決定の中身(測る内容)が変わらないためである。**
**選んだ側と、選ばなかった側を §4 に両方書く。**

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(5 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(4 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r4.md` + `scripts/o3c_reaction_judge.py` + テスト 2 本
>
> 1. [止める] /home/user/trade/scripts/o3c_reaction_judge.py:1159-1160,1228 vs /home/user/trade/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:1702-1703,1756 — §9.1 は「反復回数 **2,000 回**」「種 **1**」を凍結し「**走行の前に決めた 2,000 回を走行の後に変えない**」と書くが、読みのスクリプトは `--reps`(既定 2000)・`--seed`(既定 1)を**引数で受け取り、凍結値と違っても止まらない**。同じ節は「**`|t|` が 3.86〜3.99 に入る検定では、反復の引き直しで判定が変わりうる**」と書き、本版は「バー近傍」列でその行を名指しできるようにした。**開封後に `--reps` を変えて読みを引き直せる経路が閉じているか。閉じる機械は道具の側にしか置けず、道具は開封後に変えられない**(A-6、`research-protocol` §0「判定は設計ごとに一度だけ」)
>
> 2. [止める] judge:1201 vs judge:1179,1077-1084 — サニティ #14(`check_pairing`)が掛かるのは `runs` と `sens` だけで、**MDE の `p` / `s` を供給する標本の走行(`--sample-w8` / `--sample-w24` / `--sens` の `:SAMPLE_DIR`)には 1 度も掛からない**。標本側でも `pair_of_matched` と `mat_usable` が inherit 14 群・partner_sign 24 群の行の帰属を決めている(judge:1077-1104)。§11 の #14 は「**1 つでも破れたら「[止め]」を出して終了コード 1 で終わり、表を 1 枚も書かない**」と書く。**標本側の 1 対 1 が崩れたとき、MDE の母集団が黙って変わることは、どこで検出されるか**(P-3)
>
> 3. [止める] judge:1313-1322 vs PREREG:1971-1986 — `summary.json` の「MDE 列が空の理由」は、`:SAMPLE_DIR` を渡さなかった感度に対して機械が一律に「**対を成す標本の走行が在庫に無い**」と書く。指摘 8 が直したのは「**在庫には gap30_w8 と gap180_w8 が実在する**…**在庫の不在ではなく実装の都合である**」という取り違えそのものである。**渡さなかったことと在庫に無いことを同じ文で書く機械は、同じ型の断定を出力に残さないか。この文の範囲(どのディレクトリを、いつ、何で見たか)はどこに書かれるか**(P-1)
>
> 4. [直す] PREREG:31-33 vs :6-8,2450 — 冒頭の「**凍結する対象**」は **6 項目**を列挙する(バー / 族 / α / h / 軸 / **判定に使う走行 2 本**)一方、その 20 行上は「**凍結したのは下の 5 つ**(判定のバー / 族 576 / α / h / 軸 48 群)」と書き、§14.4 の待つもの 2 は「**「凍結」が指すのは 5 項目だけである**」をオーナーへの報告 1 行として並べている。**同じ節の中で凍結対象が 5 と 6 に分かれている。オーナーに出すのはどちらか**(P-5、P-2)
>
> 5. [直す] PREREG:2311 — §13 の「報告する変更」の 4 行目(§10.1 の判定の分岐)は、後の欄が「**3 分岐。「不明(n < 30)」/「差あり(+)(−)」/「検出されず(MDE = X)」**」のままである。本版の分岐は「不明(n2 < 30)」「不明(t 未算出)」「不明(MDE 未算出)」を含む(PREREG:1849-1856、judge:790-793 の `BRANCHES` は 7 語)。**この表はオーナーに報告する 8 件の本体である。報告される分岐の数と、事前登録の分岐の数は一致しているか**(P-2、P-5)
>
> 6. [直す] PREREG:2439 vs :2449 — §14.4 の 4 の本文は「**§13 の「報告する変更」の 7 件は、どれも返答 060 §3 で黙認されていた案からの変更である**」と書き、その直下の表の 1 行目は「**§13 の「報告する変更」の 8 件**」と書く。**報告する件数は 7 か 8 か**(P-5)
>
> 7. [直す] PREREG:93,1834 vs judge:790-793 — §0.1 は「**本版は「不明(n < 30)」/「不明(n2 < 30)」/「不明(t 未算出)」/「差あり(+)」/「差あり(−)」/「不明(MDE 未算出)」/「検出されず」の 6 分岐**」と書くが、列挙は **7 語**である。§10.1 の列は「**判定 | 下の分岐(6 つ)**」、実装の `BRANCHES` は 7 要素、PREREG:1879 には「**これで分岐は 4 つになったが**」が残っている。**この数え上げは列挙し直しても同じ数になるか**(P-5)
>
> 8. [直す] PREREG:1610-1612 — §8.6 は欠ける 97 日を「**2023-11-19 の 1 日 + 2024-03-04〜05-31 の連続 89 日 + 2024-06-02〜06-08 の連続 7 日**」と列挙した上で、「**§12 が書く「2024-03-04〜06-08 の連続 97 日」と同じ塊である**(**実測**: 一致)」と書く。前者は 2023-11-19 を含み 2024-06-01 を含まない(§2.1 の在庫に無い 6 日)ので、**2 つの 97 は同じ集合ではない**。**「一致」は何を何と突き合わせた実測か**(P-2、P-5)
>
> 9. [直す] PREREG:2031 — §10.3 が「**結果の報告に必ず併記する**」と走行前に固定した文は「**この判定は 2024-03-04〜06-08 の連続 97 日を欠いた 356〜359 日で出ている**」である。§8.6 の表では **356 日は「当日 + 前日」を要求したときの日数(欠けるのは 100 日)**、**359 日が 97 日を欠いた日数**である(456 − 97 = 359 / 456 − 100 = 100 差)。**「97 日を欠いた 356 日」は両立するか。併記文は走行後に直せない(A-6)**(P-2)
>
> 10. [直す] PREREG:243 と :245 — §0.2 の参照ラベルの表に「**prereg 監査(3 回目)の指摘 N**」の行が **2 行**あり(片方は「本書(2,090 行)への走行前の再監査」、もう片方は「本書(2,090 行)+ `scripts/o3c_reaction_judge.py` への走行前の再監査」)、5 つの監査に 6 行が対応している。この節自身が「**同じラベルが 2 つの集合を指していた = `CLAUDE.md` §0.2 の O-7**」を直すために作られた節である。**このラベルは 1 度だけ定義されているか**(P-2)
>
> 11. [直す] PREREG:460-470 vs 実測 — §3 は「**手順 1 と 12 日走行が作った出力は 5 つある**(…**前版は 4 つと書いていた。数え直した**)」とし、5 つ目を足した理由を「**前版は…判定区間側だけで数え、標本 6 日側の出力を数えていなかった**」と書く。作業ツリーの `backtest_data/o3c_reaction_20260918*` は **6 ディレクトリ**で、6 つ目 `..._sample/`(判定の MDE の `p`・`s` の出所、§8.6)が表に無い。**「標本 6 日側も数える」という数え直しの規則を当てると 5 のままか。5 つ目だけを足した線引きは中身を見た結果か**(P-5)
>
> 12. [直す] judge:283,1302-1303 vs PREREG:742-748,2345-2346 — 読みのスクリプトは `--run-w8` / `--run-w24` に渡されたディレクトリを無条件に `gap60_w8` / `gap60_w24` と名付け、**`summary.json` の `window_hours` と日数を記録するだけで、その走行が W = 8h / 24h か、判定区間 456 日のものかを検査しない**。§4 の内訳表(「**W に依らない 12 群は W = 8h の走行から取る**」)と 576 検定の同一性はこの対応に依存している。**渡すディレクトリの取り違えは、どの機械が止めるか**(P-3)
>
> 13. [直す] judge:1210-1219 vs PREREG:2781-2789 — `check_axis_kinds` は**感度の走行にも掛かり**、1 本でも固定と食い違えば判定の表も 1 枚も書かずに終了コード 1 になる。§14.6 が事前登録したのは**サニティ #14 が感度で破れたときの対処**だけ(「**`--sens` を外して判定表だけ出すことはしない**」)で、軸の作り方の食い違いについては書かれていない。**感度の走行の軸の作り方が固定と違って出たとき、何をすると事前登録に書いてあるか**(A-6)
>
> 14. [直す] PREREG:2875-2884 — 末尾の走査は `grep -c`(**一致した行数**)を 5 語ぶん出し、「**判定として使った行は 0 である**」と結論する。内訳(禁止の文 / 「有効数字」/ 列名の引用 / この節)は出力からは出ず、人が読んで分けたものである。`CLAUDE.md` §3 が要求する 4 本(ファイルごとの出現数・出現数の合計)は打たれていない(範囲を 1 ファイルに切ったことは明記されている)。**「判定として使った行 0」は機械の出力から出るか、目視の分類か**(P-10、P-5)
>
> 15. [直す] /home/user/trade/tests/test_o3c_reaction_judge.py:251 — 分岐を 6 つに増やした後も試験名は `test_decide_has_exactly_three_branches_in_order` のままで、docstring は「**決定 2 + 17' + 1'' + 20''**」と書く。`test_three_branches_come_out_on_synthetic_data`(:328)も同様。**この試験が「いくつの分岐を全部当てているか」を、名前から取り違えないか**(P-2)
>
> 16. [直す] PREREG:1828 vs judge:1000-1003,868-874 — §10.1 は列「**有限な複製の本数**」について「**少ないセルはこの本数で `1/√(2·n)` を読む**」「**閾値は置かない**(A-12)」と決めたが、`summary.json` に出るのは最小・最大・指定に満たないセルの数だけで、**その本数が小さいセルを結果の読みでどう扱うか(併記するのか、どの列と一緒に読むのか)は §10.3 の併記の規約に入っていない**(バー近傍だけが §10.3 に入っている)。**本数が 2 本のセルと 2,000 本のセルを、読む人はどこで見分けるか**(P-3)
>
> 17. [聞く] /home/user/trade/docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r4.md:260-277 vs PREREG:2444-2452 — 処置 9 は「**フックが復旧路として開けている `sh scripts/regen_hook_manifest.sh` を 1 回走らせて解錠し**」「**`git checkout -- scripts/_research_audit_gate.py docs/AUDITOR/HOOK_MANIFEST.sha256` で両方を元の版に戻した**」と書く。§14.4 の「待つもの(= オーナーに報告する対象)」4 件にこの事実と `WIRED` 未追記は入っていない。**指紋の台帳を一度作り直したこと(A-16)と、読みのスクリプトの関門が `WIRED` に載っていないこと(§5.0 の「関門が呼ばれていることを毎回の pytest で測る」が片肺であること)を、開封の可否を判断するオーナーに出す対象に入れるか**(P-8)
>
> 18. [聞く] PREREG:9-14,2326 — 凍結の宣言は「**4 回の走行前の監査で、次の 4 つは実際に動いた**: 対照の作り方 / 対照 (ii) の向きの要る量の符号 / 判定語 / 読みの道具」と書き、4 回とも読みの道具が直っている(§14.6 の「直した点」は 3 回目 9 件・4 回目 12 件)。本監査は 5 回目で、道具はまだ直りうる。**走行前の監査を何回で打ち切り、どの時点の道具を「凍結した道具」と呼ぶかは誰が決めるか。オーナーへの報告にこの回数を入れるか**(A-6、P-9)
>
> 19. [聞く] PREREG:1574-1577,1829 — §8.6 は「**標本 6 日の行数は 224 なので、3 群に切ると 1 群 70〜80 行前後になり、そこから取る `p` / `s` の推定誤差は大きい**」と自分で書いた上で、その `p` / `s` から作る MDE を §10.1 の全 576 行に併記し、「検出されず」の読みの材料にする。§14.4 の待つもの 4 は D の被覆(MDE 約 1.13 倍)だけをオーナーに出す。**「検出できる最小」として報告する MDE そのものの誤差の大きさを、オーナーに見せる対象に入れるか**(P-16)
>
> 判定: 止める

---

## 2. 19 件の処置

**行番号は処置後の PREREG / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**19 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] `--reps` / `--seed` で開封後に引き直せる経路

**処置: 直した(引数ごと消した)。**リードの決定(逐語):
「**`--reps` と `--seed` の引数を消し、定数 `REPS = 2000` / `SEED = 1` に固定
(開封後に引き直せる経路を閉じる)。summary に定数を記録。テストを直す。**」

- **答え**: **閉じていなかった。**前版は `--reps`(既定 2000)/ `--seed`(既定 1)を
  argparse の引数で受け、凍結値と違う値でも止まらなかった。
- **JUDGE に定数 `REPS = 2000` / `SEED = 1` を置き、`add_argument("--reps")` /
  `add_argument("--seed")` を消した**(JUDGE:139〜151、1236〜1239)。
  **ブートストラップの引き方は `np.random.default_rng(SEED)` と `size=(REPS, nd)`**(JUDGE:1339〜1344)。
- **`summary.json` に `"seed と reps の出所": "定数(引数では変えられない。決定 1'''。§9.1 で凍結した値)"`
  を出す**(JUDGE:1393)。
- **PREREG §9.1 の表に「この 2 つをどう固定したか」の行を足した**(PREREG:1892)。
  **§14.6 の入力の注も直した**(PREREG:3036〜3037。「既定なので省いてよい」→「引数は無い」)。
- **試験**: `::test_reps_and_seed_are_constants_and_not_flags`
  (**`--reps` / `--seed` を渡すと argparse が終了コード 2 で拒否することを実測する**)。
  **既存の試験は、定数そのものを差し替える `frozen()` に替えた**(迂回の旗ではない)。
- **実測**(2026-09-18): `--reps 20` を渡すと
  `unrecognized arguments: --reps 20` で終了コード 2。

### 2.[止める] 標本の走行にサニティ #14 が掛かっていない

**処置: 直した(標本にも掛けた)。**リードの決定(逐語):
「**サニティ #14(対応の検査・一意性)を標本の走行(`--sample-w8` / `--sample-w24` /
`:SAMPLE_DIR`)にも掛ける。破れれば「[止め]」。§11 に書く。テスト。**」

- **答え(前版)**: **どこでも検出されなかった。**標本の 1 対 1 が崩れると、
  inherit 14 群・partner_sign 24 群の**標本側の帰属**が変わり、MDE の `p`・`s` の母集団が黙って変わる。
- **`main` の #14 の輪に `samples` と `sens_samples` を足した**(JUDGE:1307〜1318)。
  **1 つでも破れたら「[止め]」で終了コード 1、表は 1 枚も書かない。**
- **標本の走行の `Run` には名前を分けて付けた**(`gap60_w8(標本)` など。JUDGE:1259〜1260、1277)。
  **どちらが破れたかが文面で分かるようにするため。**
- **`summary.json` に「標本の走行(サニティ #14 と params の検査を掛けた)」を出す**(JUDGE:1436〜1445)。
- **PREREG §11 の #14 の行と、その下の注を書き直した**(PREREG:2305、2315〜2330)。
- **試験**: `::test_sanity_14_is_applied_to_the_sample_runs_too`
  (**標本の側だけを壊し、判定の走行が無傷でも止まることを測る**)。
- **実測**(2026-09-18、標本 6 日): 標本の合わせた対照 2 件に同じ相手を指させると
  `[止め] サニティ #14(1 対 1 の対応)が通らない…- gap60_w8(標本): 同じ相手を指す合わせた対照が
  1 件ぶん重複` / 終了コード 1 / **出力ディレクトリは作られなかった。**

### 3.[止める] 「MDE 列が空の理由」に在庫の話を機械が書く

**処置: 直した(文面を替え、渡した一覧を出す)。**リードの決定(逐語):
「**summary の「MDE 列が空の理由」を「`:SAMPLE_DIR` を渡していない
(在庫の有無はこの道具では見ていない)」に変え、渡したディレクトリの一覧も記録する。**」

- **答え**: **残していた。**前版は機械が一律に「**対を成す標本の走行が在庫に無い**」と書いていたが、
  **読みのスクリプトは `backtest_data/` を 1 度も見ていない**(見ているのは引数だけ)。
  **4 回目の指摘 8 が直したのと同じ型の断定が、出力の側に残っていた**(`CLAUDE.md` §0.2 の O-2)。
- **文面を「`--sens` に `:SAMPLE_DIR` を渡していない(在庫の有無はこの道具では見ていない)」にした**
  (JUDGE:1456〜1463)。
- **`summary.json` に「渡した標本ディレクトリの一覧」(判定の走行 + 感度の走行 + 注)を足した**
  (JUDGE:1465〜1471)。**MDE の `p`・`s` の出所のすべてがそこに並ぶ。**
- **在庫についての主張は PREREG の側に、範囲と方法つきで置いた**(PREREG:2172〜2189)。
  **範囲** = **この環境の `backtest_data/o3c_reaction_20260918_sample/` だけ**
  (**実測** 2026-09-18 の `ls` = `gap180_w8` / `gap30_w8` / `gap60_w24` / `gap60_w8` の 4 本)。
  **公開アーカイブからの取り直しとオーナー PC は見ていない**(**未確認**)。
- **試験**: `::test_the_empty_mde_reason_does_not_talk_about_the_inventory`
  (**「在庫に無い」が理由の文に出ないこと、渡した一覧が出ることを測る**)。

### 4.[直す] 凍結対象が 5 と 6 に分かれている

**処置: 直した(6 項目に統一した)。**リードの決定(逐語):
「**凍結の対象は 6 項目(バー / 族 576 / α / h / 軸 48 群 / 判定に使う走行 2 本)に全箇所を統一。
§14.4 の報告の 1 行も「6 項目」に。**」

- **答え(オーナーに出すのはどちらか)**: **6 項目である**(冒頭の「凍結する対象」が列挙している側)。
- **直した箇所**: PREREG:6〜17(凍結の宣言)/ :26(バーの数値の注)/ :33(報告の 1 行)/
  :2582(§13 の (8))/ :2593〜2595(あわせて報告すること)/ :2720(§14.4 の待つもの 2)/
  :3228〜3231(末尾)。
- **§0.1 の 3 回目の表の 23 行目は逐語の引用なので変えていない**(PREREG:211。
  **過去のリードの決定の逐語である**)。**本版で 6 に統一したことは §0.1 の 5 回目の表と
  凍結の宣言に書いた。**

### 5・7.[直す] 判定の分岐の数え上げ

**処置: 直した(7 語に統一した)。**リードの決定(逐語):
「**判定の分岐は 7 語(不明(n < 30)/ 不明(n2 < 30)/ 不明(t 未算出)/ 不明(MDE 未算出)/
差あり(+)/ 差あり(−)/ 検出されず)に全箇所を統一(§0.1・§10.1・§13 の表・
「4 つになった」の残りも)。**」

- **答え**: **列挙し直すと 7 である**(実装の `BRANCHES` も 7 要素)。
- **直した箇所**: PREREG:111(§0.1 の「6 分岐」→「7 分岐」)/ :2027(§10.1 の列「下の分岐(6 つ)」→
  「(7 つ)」+ リードの決定の逐語)/ :2072〜2080(「これで分岐は 4 つになったが」→
  いつの時点の数かを明記し、現在は 7 つと書いた)/ :2578(§13 の表の後の欄を 3 分岐 → 7 分岐)。
- **§13 の表を直した理由をその欄に書いた**: **この表はオーナーに報告する 8 件の本体なので、
  報告される分岐の数と事前登録の分岐の数が違ってはならない。**
- **試験**: `::test_decide_has_exactly_seven_branches_in_order` が
  **`len(judge.BRANCHES) == 7` を測る**(件 15 の改名と同じ試験)。

### 6.[直す] 報告する件数は 7 か 8 か

**処置: 直した(8 件に統一した)。**リードの決定(逐語): 「**報告する変更は 8 件に統一。**」

- **答え**: **8 件である**(4 回目の指摘 4 で 8 件目「バーの数値」を足した)。
- **§14.4 の 4 の本文を「7 件」→「8 件」に直し、前版が食い違っていたことを書いた**(PREREG:2708〜2710)。
- **§13 の表の上にも「この表は 8 件である」と書いた**(PREREG:2571)。

### 8・9.[直す] 欠ける日の集合が 2 つある / 併記文の 356 と 359

**処置: 直した(集合を 1 つにし、併記文から日数を外した)。**リードの決定(逐語):
「**欠ける日の集合を 1 つに: 判定区間 456 日のうち metrics の zip が無い日を
ファイルの有無だけで数え直し、一覧(日付)を 1 か所(§8.6)に置いて §12・§10.3 はそれを参照する。
「2024-03-04〜06-08 の連続 97 日」の記述は正しい集合に直す(2024-06-01 は判定区間に無い日である点を明記)。
§10.3 の併記文は「D が作れた束の数と日数は走行後に `summary.json` から書き写す。
欠ける日の一覧は §8.6」の形にし、356/359 の取り違えを消す。**」

- **答え(8 の「一致」は何を突き合わせた実測か)**: **何とも突き合わせていない**
  (`CLAUDE.md` §0.2 の O-3 の型)。**2 つの 97 は同じ集合ではない。**
- **数え直した**(**実測** 2026-09-18、**ファイルの有無だけ。観測量は 1 つも読んでいない**):
  判定区間 456 日 / metrics の zip が無い日 **97 日** /
  塊は **2023-11-19(1 日)+ 2024-03-04〜05-31(89 日)+ 2024-06-02〜06-08(7 日)** /
  **`"2024-06-01" in jd` は `False`**(清算の zip が無いので判定区間に入らない)。
- **一覧と走査のコマンド・出力を §8.6 に 1 か所だけ置いた**(PREREG:1737〜1806)。
- **§12 と §10.3 はそれを参照する形にした**(PREREG:2442〜2445、2460〜2463、2468、2196〜2199)。
- **「2024-03-04〜06-08 の連続 97 日」という書き方を本文から消した**
  (**残っているのは §0.1 の 3 回目の表の逐語の引用 1 件と、§8.6 の「前版はこう書いていた」の説明だけ**)。
- **9 の答え**: **両立しない。**456 − 97 = **359**、456 − 100 = **356**(当日 + 前日を要求したとき)。
  **併記文から日数を外し、「D が作れた束の数と日数は走行後に `summary.json` から書き写す」に替えた**
  (PREREG:2203)。**走行の前に直した**(A-6。走行後には直せない)。

### 10.[直す] 参照ラベルの表に 3 回目が 2 行ある

**処置: 直した(重複を消し、5 回目の行を足した)。**リードの決定(逐語):
「**§0.2 の参照ラベル表を 1 監査 1 行(5 行)に。**」

- **答え**: **1 度だけ定義されていなかった**(3 回目が 2 行)。**重複を消した。**
- **消したのは「本書(2,090 行)への走行前の再監査」の側で、監査役の逐語(`_r3.md` の冒頭)に
  合う「本書 + `scripts/o3c_reaction_judge.py`」の側を残した**(PREREG:284〜287)。
- **【原文に無い判断・§4 の 1 に書いた】**リードの決定は「(5 行)」と書いているが、
  **本監査(5 回目)のラベルを足すと 6 行になる。**「1 監査 1 行」の側を採って 6 行にし、
  **その判断と理由を §0.2 の注に書いた**(PREREG:289〜297)。

### 11.[直す] §3 の出力の一覧は 5 か 6 か

**処置: 直した(6 つに数え直した)。**リードの決定(逐語):
「**§3 の出力の一覧に `backtest_data/o3c_reaction_20260918_sample/`(標本 6 日の走行 4 本。
MDE の p・s の出所。標本なので開封ではない)を 6 つ目として足す。
`ls backtest_data | grep o3c_reaction_20260918` の出力を貼る。**」

- **答え**: **5 のままにはならない。**「標本 6 日側も数える」という規則を当てると **6** である。
  **前版はその規則を当てきらず、`..._anchor_trades_sample/` の 1 本だけを足していた。**
- **`ls` の出力を貼り、「まず全部挙げてから封か否かを書く」形にした**(PREREG:517〜533)。
  **実測**(2026-09-18): `o3c_reaction_20260918_anchor` / `..._anchor_trades` /
  `..._anchor_trades_sample` / `..._anchor_v1_rawcols` / `..._sample` / `..._scale12_judgmentdays`
  の **6 ディレクトリ**。
- **表に 6 行目を足した**(PREREG:544)。**`..._sample/` は MDE の `p`・`s` の出所であり、
  §4 の群の大きさ・§11 の #14 の実測・§10.2 の感度の標本もこれである。標本 6 日なので開封ではない。**

### 12.[直す] 渡すディレクトリの取り違えを止める機械が無い

**処置: 直した(`params` を名前と突き合わせる)。**リードの決定(逐語):
「**読みのスクリプトは `--run-w8` / `--run-w24` / `--sens` の各ディレクトリの `summary.json` の
`params`(`mode`、`window_hours`、`gap_sec`、日数)を検査し、名前と合わなければ「[止め]」
(判定の 2 本は mode full・gap 60・W 8/24・日数 456。標本は mode sample・日数 6)。
テスト(標本で試すときは `--allow-sample-as-run` のような迂回旗を**作らず**、
試験用に summary.json を差し替えた一時ディレクトリで検査する)。**」

- **答え(前版)**: **どの機械も止めなかった。**渡されたものを無条件に `gap60_w8` / `gap60_w24` と
  名付けていた。
- **JUDGE に定数 `JUDGMENT_N_DAYS = 456` / `SAMPLE_N_DAYS = 6` / `GAP_SEC_JUDGE = 60` /
  `WINDOW_HOURS_FIXED` と、`check_run_params()` / `_sens_name_params()` を足した**
  (JUDGE:144〜151、505〜548)。**`main` が判定 2 本・標本 2 本・感度・感度の標本の全部に当てる**
  (JUDGE:1279〜1305)。**食い違えば「[止め]」で終了コード 1、表は 1 枚も書かない。**
- **感度は名前 `gap<秒>_w<時間>` から gap と W を読む。読めない名前は止める**
  (**リードの決定の逐語に無い分岐。§4 の 2 に書いた**)。
- **PREREG §14.6 に要る `params` の表と理由を置いた**(PREREG:3086〜3116)。
- **試験**: `::test_the_run_dirs_must_match_their_names`
  (**W の取り違え / 標本を判定の走行として渡す / 判定の走行を標本として渡す /
  gap が名前と違う感度 / 名前の形が違う感度 の 5 通りで止まり、名前どおりなら通ることを測る**)。
  **迂回の旗は作っていない**(試験は `summary.json` を差し替えた一時ディレクトリを渡す)。
- **実測**(2026-09-18、標本 6 日): 標本を `--run-w8` に渡すと
  `params.mode が 'sample'(要る値は 'full')` と `params.days の日数が 6(要る値は 456)`、
  W = 24h の走行を `--run-w8` に渡すと `params.window_hours が 24.0(要る値は 8.0)`。
  **どちらも終了コード 1 で、出力ディレクトリは作られなかった。**

### 13.[直す] 感度の軸の作り方が固定と食い違ったときの対処

**処置: 直した(事前登録に書いた)。**リードの決定(逐語):
「**§14.6 に「感度の走行で軸の作り方が固定と食い違ったときも判定の表を出さずに止まる。
対処はその走行を調べて直す。`--sens` を外さない」を足す。**」

- **答え(前版)**: **書かれていなかった**(サニティ #14 の側だけ書いてあった)。
- **PREREG §14.6 に節を置いた**(PREREG:3130〜3144)。**理由も書いた**:
  感度で対照の軸の作り方が変わって出るなら、同じ道具で作った判定の 2 本も同じ変わり方を
  している可能性があり、確かめずに判定だけ出すのは「検査を通すために中核を外す」形になる
  (`CLAUDE.md` §0.2 の A-9)。**止まり方が #14 と同じなので、対処も同じにした。**

### 14.[直す] 末尾の走査が 1 本しかない / 「判定として使った行 0」の出所

**処置: 直した(4 本にし、目視であることを明記した)。**リードの決定(逐語):
「**末尾の走査を `CLAUDE.md` §3 の 4 本(ファイル数 / 行数 / ファイルごとの出現数 /
出現数の合計。範囲 = 本書 1 ファイルと明記)に替え、「判定として使った行 0」は目視の分類で
あることを明記する(機械の検査は読みのスクリプトの出力走査であって本文ではない)。**」

- **答え**: **目視の分類である。**4 本が出すのは件数だけで、
  「禁止の文か / 別の語(有効数字)か / 判定として使った行か」は人が 1 行ずつ読んで分けた。
- **PREREG の末尾を書き直した**(PREREG:3236〜3298)。**4 本のコマンドと出力を貼り、
  範囲を本ファイル 1 本に切ったことを明記した。**
- **行数と出現数の合計が違うことが、4 本にして初めて出た**(**実測**: 「有効」は
  **行数 12 / 出現数 17**、「陰性」は **行数 25 / 出現数 32**。**1 行に 2 回以上出る行がある**)。
- **この節が自分自身を数えるので、貼った後にもう一度 4 本を打って一致することを確かめた**
  (**実測** 2026-09-18。**動かなくなるまで打った**)。
- **機械が掛かっているのは読みのスクリプトの出力(CSV / JSON)の側だけであることを明記した**
  (決定 11''。**本書の本文にはその機械は掛かっていない**)。

### 15.[直す] 試験の名前が分岐の数と合っていない

**処置: 直した(改名し、docstring も直した)。**リードの決定(逐語):
「**テスト名を分岐の数に合わせて改名(`seven_branches` など)、docstring も直す。**」

- **`test_decide_has_exactly_three_branches_in_order` →
  `test_decide_has_exactly_seven_branches_in_order`**(`tests/test_o3c_reaction_judge.py:306`)。
  **docstring に 7 語を列挙し、`len(judge.BRANCHES) == 7` を測る行を足した。**
- **`test_three_branches_come_out_on_synthetic_data` →
  `test_seven_branches_come_out_on_synthetic_data`**(同 :392)。
  **docstring に「この試験が当てているのは 5 つで、残る 2 つは別の試験が当てている」と書いた**
  (**取り違えを名前だけで防がない**)。
- **試験ファイルの冒頭の「測るもの 1」も 7 分岐に直した**(同 :6〜7)。
- **実測**(2026-09-18): `grep -rn "three_branches" --include=*.py --include=*.md .` の一致は、
  **処置後は過去の処置書(`_r3.md` / `_r4.md` / 本書の逐語)と、
  改名した試験の docstring の中の「前版の名前は」の説明だけ**である
  (**試験の関数名としては 1 件も残っていない**)。

### 16.[直す] 本数が 2 本のセルと 2,000 本のセルをどこで見分けるか

**処置: 直した(§10.3 に併記の規約を置き、summary に一覧を出した)。**リードの決定(逐語):
「**§10.3 の併記の規約に「有限な複製の本数が 2,000 未満のセルは、本数と 1/√(2·n) を併記して読む」を足す。
summary にそのセルの一覧(観測量・群・h・本数)を出す。**」

- **答え(前版)**: **行単位では見分けられなかった**(summary に最小・最大・件数しか無かった)。
- **`summary.json` に「指定した反復回数に満たないセルの一覧」を足した**(JUDGE:1379〜1390、1420)。
  **1 行に 観測量・群・h・有限な複製の本数・`1/√(2·n)`。****閾値は置いていない**(A-12)。
- **PREREG §10.3 に併記の規約を置いた**(PREREG:2237〜2254)。
  **§10.1 の列の説明にも参照を足した**(PREREG:2021)。
- **バー近傍の印と合わせて読むことも書いた**(**本数が少なく、かつ `|t|` がバーの近傍にある行が、
  反復の引き直しで判定が変わる幅が最も大きい**)。
- **試験**: `::test_low_reps_cells_are_listed_in_the_summary`
  (**一覧の件数が「満たないセルの数」と一致し、本数が表の列と一致し、`1/√(2·n)` が式どおりであることを測る**)。
- **実測**(2026-09-18、標本 6 日・反復 2,000): **満たないセルは 24 件、最小は 1,998 本。**

### 17.[聞く] 台帳の作り直しと `WIRED` 未記載をオーナーに出すか

**処置: 出す(待つものに 2 件足した)。**リードの決定(逐語):
「**§14.4 の「待つもの(オーナーに報告する対象)」に 2 件足す: (a) 4 回目の処置で委任先が
指紋の台帳を `regen_hook_manifest.sh` で 1 回作り直し、直後に `git checkout` で
`_research_audit_gate.py` と台帳を元の版に戻し、食い違い 0 を実測したこと(A-16 に触れる操作。
リードが報告する)、(b) 読みのスクリプトが `WIRED` 一覧に未記載であること(テスト 2 件で守られている)。**」

- **§14.4 の「待つもの」に 5 と 6 として足した**(PREREG:2724〜2725)。
  **理由と逐語もその下に置いた**(PREREG:2727〜2739)。
- **本作業では `scripts/_research_audit_gate.py` も `docs/AUDITOR/HOOK_MANIFEST.sha256` も
  `.claude/` も 1 行も触っていない**(**リードの委任文面の指示**)。
  **`regen_hook_manifest.sh` も走らせていない。**
- **実測**(2026-09-18、本作業の終わり): `git status --porcelain` に
  `scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` は 1 件も出ない。

### 18.[聞く] 監査を何回で打ち切り、どの道具を凍結と呼ぶか

**処置: 決めた(§14.4 に書いた)。**リードの決定(逐語):
「**§14.4 に書く: 走行前の監査は「止める」が 0 件になった回で打ち切る。
凍結した道具 = 開封のときのコミットのハッシュを §14.4 に書き写す。
オーナーへの報告に監査の回数(5 回)を入れる(リードが書く)。**」

- **答え(誰が決めるか)**: **リードが決めた**(上の逐語)。**本処置はそれを事前登録に書いた。**
- **§14.4 に 4 行の表を置いた**(PREREG:2756〜2768): 打ち切りの条件 /
  「凍結した道具」の定義 / **開封のときのコミットのハッシュの欄(まだ空)** /
  オーナーへの報告に入れるもの(**回数 5 回**)。
- **なぜ条件が要るかも書いた**: **5 回とも読みの道具が直っている。
  「監査のたびに道具が変わる」ままでは、どの版を凍結したのかが決まらない**(A-6)。
- **回数の上限は置いていない**(A-12)。**「止める」が 0 件になった回で切る。**

### 19.[聞く] MDE そのものの誤差をオーナーに見せるか

**処置: 見せる(§8.6 に表を貼り、待つものに足した)。**リードの決定(逐語):
「**§14.4 の「待つもの」に足す: MDE の p・s は標本 6 日(1 群 70〜80 行)から取るので
推定誤差が大きい。その大きさの目安として、§8.5 の `mde()` で p の標準誤差 √(p(1−p)/n_sample) を
主軸 D の 3 群 × 到達 2 系統 × h = 60 分について出し、§8.6 に表で貼る
(標本の値だけで計算。判定区間は開けない)。**」

- **§8.6 にコマンドと 6 行の表を貼った**(PREREG:1660〜1700)。**標本 6 日だけを読んだ。
  判定区間は 1 日も開けていない。**
- **実測**(2026-09-18): `p` は 0.2267〜0.6111、**その標準誤差は 0.0483〜0.0663**。
  n は実群 74〜75 / 対照 54〜88。
- **読み方も走行の前に書いた**: **MDE は `√(p1(1−p1)/n1 + p2(1−p2)/n2)` に比例するので、
  `p` のこの誤差はそのまま MDE の誤差になる。判定そのもの(`|t| ≥ z`)はこの `p` を使わないので、
  効くのは「検出されず」の行に併記する MDE の大きさだけである。**
- **§14.4 の「待つもの」に 7 として足した**(PREREG:2726)。

---

## 3. 事前登録に入れたリードの決定の逐語(19 件)

**PREREG §0.1 の 7 つ目の表(PREREG:238〜261)に、19 件の逐語をそのまま置いた。**
**右の列は 19 行とも「(該当語なし)」である**(= オーナーの原文に該当語が 1 つも無い
= **リードの判断であることの印**。`CLAUDE.md` §0.1)。
**逐語と処置が食い違う件(10 の「5 行」と 12 の「名前の形」)は、その行と §4 に理由つきで書いた。**

---

## 4. 原文に無い判断が必要になった 2 件(黙って決めていない)

| # | 分岐 | 選んだ側 | 選ばなかった側 | なぜ止めずに進めたか |
|---|---|---|---|---|
| 1 | **§0.2 の表は「1 監査 1 行」だが、5 回目のラベルを足すと 6 行になる**(件 10。リードの決定は「(5 行)」) | **「1 監査 1 行」を採り、5 回目の行を足して 6 行にする** | 5 行のままにし、5 回目のラベルを表に置かない | **本書は 5 回目の指摘を 19 件とも参照するので、ラベルの定義が無いと参照できない**(O-7 の型に戻る)。**「(5 行)」は重複を消した時点の数として読んだ。****どちらでも「重複を消す」という中身は同じ** |
| 2 | **`--sens` の名前が `gap<秒>_w<時間>` の形でないときどうするか**(件 12。リードの決定は「名前と合わなければ止める」まで) | **止める**(名前から gap と W を読めない以上、「名前と合うか」を測れないため) | 形が違う名前は検査を飛ばして通す | **飛ばすと、名前を変えるだけで検査を外せる = 迂回の旗を作るのと同じになる**(リードの決定は「迂回旗を作らない」と明記している)。**判定の 2 本と標本の検査は、どちらを選んでも同じ** |

---

## 5. 本書で新しく出していないもの / リードに上げること(射程)

- **判定区間(456 日)の観測量は 1 つも出していない。**
  本書に出た判定区間の数は、**日数(456 / 359 / 356 / 97 / 100)と欠ける日の日付**だけで、
  **どれもファイルの有無から出たものである**(§8.6 の走査。**観測量を 1 つも読んでいない**)。
- **判定(予測できる / 使える / 有効)は本書に 1 つも書いていない。**
- **`--mode full` を 1 本も走らせていない**(**実測** 2026-09-18:
  `backtest_data/o3c_reaction_20260918_full/` は存在しない)。
  **`backtest_data/o3c_reaction_20260918_judge/` も作っていない。**
- **`scripts/o3c_reaction.py` は 1 行も変えていない。**
  **`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` も
  1 行も触っていない。`regen_hook_manifest.sh` も走らせていない。**
- **標本 6 日での読みのスクリプトの動作確認は、試験用の台帳(スクラッチ領域)と
  スクラッチ領域の出力先で行った。**判定区間の出力は 1 つも触っていない。
  **この確認で出た F1 の読みは標本 6 日(探索段)のものであり、結果ではない。本書に書かない。**
- **【リードに上げる 1】件 10 の「5 行」。**§0.2 は 6 行になった(§4 の 1)。
  **5 行に戻すなら、5 回目のラベルの置き場所をリードが決めること。**
- **【リードに上げる 2】件 12 の「名前の形が違う感度」。**止める側にした(§4 の 2)。
  **感度の名前を `gap<秒>_w<時間>` 以外にする予定があるなら、リードが決めること。**
- **【リードに上げる 4】件 12 の射程。****標本の側で見るのは `mode` と日数の 2 つだけである**
  (リードの決定の逐語が「**標本は mode sample・日数 6**」と書いているとおりに実装した)。
  **「W = 24h の標本を `--sample-w8` に渡す」取り違えは、この機械では止まらない**
  (**どちらも `mode: sample` / 6 日だからである**)。**足すかはリードが決めること**
  (PREREG §14.6 にも同じ射程を書いた)。
- **【リードに上げる 3】件 17・18・19 の報告。**
  **「待つもの」の 5・6・7 はリードが返答でオーナーに出すものである**(本書と PREREG には書いた)。
  **監査の回数(5 回)も、リードが報告に書く。**
- **オーナーの応答(L 番号)はまだ無い。判定区間は開けていない。**
- **`CLAUDE.md` §3 の収集件数が 2,084 件のままである**(**実測**: `CLAUDE.md:123`)。
  **本作業で 2,089 件になった**(**実測** 2026-09-18:
  `PYTHONPATH=src python -m pytest --collect-only` の末尾 = `2089 tests collected`。
  内訳 = `tests/test_o3c_reaction_judge.py` が 40 → **45 件**。**足したのは 5 件**:
  `::test_reps_and_seed_are_constants_and_not_flags` /
  `::test_sanity_14_is_applied_to_the_sample_runs_too` /
  `::test_the_run_dirs_must_match_their_names` /
  `::test_low_reps_cells_are_listed_in_the_summary` /
  `::test_the_empty_mde_reason_does_not_talk_about_the_inventory`。
  **`tests/test_audit_gates_wired.py` は 12 件のままである**)。
  **`CLAUDE.md` は委任の範囲外なので触っていない。リードが直すこと。**
