# 区分 1 の 13 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 13 回目の実行(2026-09-22)」(4212 行目以降、303 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run13.log`(新規 785 行。復帰文字 0 個)

## 0. 無改変・検査

既存の 4211 行は **無改変**。生ログ 11 本を渡して打ち直し、**13 本すべて 0 件**。閉じ残し 0 件(6 回連続)。
**報告の「生ログ 747 行」は、リードが数えると 785 行。**最後の追記の前に数えた値と見られる。誤りではないが、
**生ログの行数は書かないほうがよい**(数えた時点で変わる)。次の起動で伝える。

## 1. リードが一次資料で裏取りした 2 件 — **一致**

- **`microsoft/MarS` の逐語**: 「We strongly recommend using docker to run MarS. **Direct installation without
  Docker is not supported due to specific system dependencies and CUDA requirements.**」**一致。**
  試して駄目だったのではなく、**要件として書いてある**という書き分けも正しい。
- **`Ros522/backtestlob` に板も待ち行列も無い**: `src/backtest.cpp`(247 行)に `queue` 0 件 / `depth` 0 件。
  `step_by_tick` は 2 件。**一致。**LICENSE は master も main も **404** で、ライセンス未確定も一致。

## 2. 調査班が渡してきた判断への回答

1. **`QuantCore` を深掘りと呼んでよいか → 呼んでよい。ただし「§5-4 の指値側は未達」と併記する。**
   §4.0 の全項目が埋まり、導入・実行・危険検査を通しているので深掘りである。
   一方、**自分の指値を置く口が公開名に無く、板に並ぶのは合成された相手方の注文**なので、
   §5-4 の「成行と**指値**の 1 往復」は通っていない。**書き分けてあるので、そのままでよい。**
   **この道具の一番重い所見は別にある**: **同じ入力で 4 回打って結果が毎回ちがい、乱数の種を渡す口が公開名に無い。**
   `97232.81` / `97156.51` / `97344.42` / `97124.64`。**検証の道具として再現性が無いことを意味する。**
   知見の表でこれを一番上に置くこと(次の起動で伝える)。
2. **分類器に導入を止められたあと回避しなかったこと → 正しい。回避してはならない。**
   止めたのは取得の経路ではなく**許可の仕組み**である。回り込むのは、どんな理由でも認めない。
   **リードの判断: `backtestlob` は導入しない。**LICENSE が無く再配布・商用の条件が読めないうえ、
   **知りたかったこと(板と待ち行列を持つか)は原文を読んで既に答えが出ている**(持たない)。
   状態 = 一次資料まで到達し、導入はしない(確定)。
3. **一覧から抜いた 39 件を全部候補に足したこと → §3-3 のとおりで、落とさなかったのは正しい。**
   **ただし、これで区分 1 の完了条件が事実上到達できなくなった。**枠組みの問題なのでオーナーに諮る(§4)。

## 3. この回で確定した状態

| 候補 | 状態 |
|---|---|
| `Ros522/backtestlob` | 一次資料まで到達、導入しない(確定)。**名前に Limit Order Book とあるが板も待ち行列も無い。**`step_by_tick` が約定の向きを見る点だけ当方より 1 段細かい |
| `flashalpha-fill-simulator` | 一次資料まで到達(浅い)。**自分の仕様書で「待ち行列の模型を持たない」「size-aware fill model は無い」と明記。**対象はオプションのスプレッド |
| `backtesting_execution_simulator` | 一次資料まで到達(浅い)。**価格水準ごとの FIFO の待ち行列を本当に持つ。**星 0・保守者 1 名の若い物なので導入せず |
| `QuantCore` | 導入して実行まで通した。**再現性が無い(同じ入力で結果が毎回ちがう)。**§5-4 の指値側は未達 |
| `MarS` | この環境では動かせない(**要件として Docker と CUDA が要ると公式が明記**)。板は本物 |
| `ThePredictiveDev/...` | README まで到達(浅い)。**板の厳密な価格・時間優先、板の控えと決定的な再生、FIX 4.2、マーケットメイク。当方に無いものが最も多い候補** |
| `ForexTester` | この環境からは到達できない(確定)。**5 経路すべて 403 か 404、archive.org に控え 0 件** |
| `AlgoTest` | 登録なしで取れる範囲を取った(浅い)。無料枠の逐語あり。**料金ページは JS の殻で値が無く、文書の側に逐語があった** |
| ほか(35・36・39・42) | 登録情報・README まで到達(浅い) |

## 4. 区分 1 の完了条件が到達できなくなった — **オーナーに諮る**

`paperswithbacktest/awesome-systematic-trading`(星 14,389)は**道具の一覧そのもの**で、
調査班は §3-3(黙って落とさない)に従い、そこから **39 件**を候補に足した。**この判断自体は正しい。**

結果、**残りの候補は 7 + 39 = 46 件**になり、しかも**その一覧の残りの節(データ源・機械学習・時系列)からは
まだ名前を抜いていない。**一覧を全部たどれば候補は数百件になる。

**委任文 §2 の完了条件(残りが空 + 新しい検索計画が新しい候補を 0 件)は、このままでは到達できない。**
**スコープを黙って縮めるのは §0.2 A-10 で禁じられている**ので、リードが独断で決めずオーナーに諮る。
