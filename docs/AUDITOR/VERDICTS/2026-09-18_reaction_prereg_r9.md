# 監査(owner-auditor)— 段 A 事前登録への **9 回目(走行前の再監査)20 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction.py`(以下 REACT)、`scripts/o3c_reaction_judge.py`(以下 JUDGE)、
試験 3 本(`tests/test_o3c_reaction.py` / `tests/test_o3c_reaction_judge.py` /
`tests/test_audit_gates_wired.py`)、`docs/DATA.md`。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `..._r2.md`(23 件 + ア〜エ)、3 回目は `..._r3.md`(23 件)、
4 回目は `..._r4.md`(22 件)、5 回目は `..._r5.md`(19 件)、6 回目は `..._r6.md`(18 件)、
7 回目は `..._r7.md`(20 件)、8 回目は `..._r8.md`(19 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 3(1〜3)/ 直す 11(4〜14)/ 聞く 6(15〜20)= 20 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない = `ls` が `No such file or directory`)。
**`backtest_data/o3c_reaction_20260918_full/` を作っていない。**
**`scripts/_research_audit_gate.py` / `docs/AUDITOR/HOOK_MANIFEST.sha256` / `.claude/` /
`CLAUDE.md` / `docs/AUDITOR/ACTION_LOG.md` は 1 行も触っていない。**
**`regen_hook_manifest.sh` も走らせていない。**
**コミットもプッシュもしていない。**
**今回も `scripts/o3c_reaction.py` を変えた**(リードの委任文面「**`scripts/o3c_reaction.py` は
決定 1・3・15・20 の分だけ変えてよい**」)。**変えたのは開封前の関門((i)(j))・台帳の状態の列・
`summary.json` の `tool_dirty` だけで、表を作る計算・列・乱数の消費は 1 行も変えていない。**
**標本 4 本を走らせ直し、全列一致を確かめた**(§3 の 4)。

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 20 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`_r9.md` を新設(逐語を一字も変えずに貼り、20 件の処置)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md` の形に倣い**」 |
| 監査役の逐語を全文読む | リードの委任文面「**監査役の逐語は `/tmp/…/audit_prereg_r9.md`(全文読む)**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**リードの決定(各件。「該当語なし」= リードの判断。逐語で事前登録に入れ明記)**」 |
| REACT を決定 1・3・15・20 の分だけ変える | リードの委任文面「**`scripts/o3c_reaction.py` は決定 1・3・15・20 の分だけ変えてよい**」 |
| JUDGE と試験を直す | リードの決定 2・3・15・14 の逐語(各件に引いた) |
| `docs/DATA.md` の 106 行目を直す | リードの決定 11「**`docs/DATA.md` の 106 行目の「連続 97 日」の書き方を…に直す(`docs/DATA.md` は触ってよい)**」 |
| **標本 4 本を走らせ直し、全列一致を pandas で確かめて報告に貼る** | リードの委任文面「**変えたら標本 4 本を `--mode sample --seed 1 --mmr 0.004` で走らせ直し、table.csv / table_mixed.csv の全列一致を pandas で確かめ、§14.6 の「走行の道具を変えた回」の表に行を足す**」 |
| §14.4 を 17 件(報告 6 / 答えが要る 3 / 事実 8)にし、範囲の語をそろえ、チ を足す | リードの追加の文面(返答の監査 7 巡目)「**事前登録 §14.4 を返答 062 と一字一句同じ分類・件数・範囲にしてください。(1) 待つものは 17 件 = 報告 6(ア・イ・ウ・エ・オ・ケ)/ 答えが要る 3(カ・キ・ク)/ 事実 8(コ・サ・シ・ス・セ・タ・ソ・チ)。「合計 16」「ケ〜タ が残る 8 件」「ケ〜ソ」の全箇所を grep で拾って直す(処置書に grep の出力を貼る)。(2) 「応答が承認するもの」の定義を「§13 の 8 件の枠組み(カ・キ・ク と ア〜オ の形)を承認し、待つもの ケ〜チ を読んだこと。道具の版ではない」に統一(:3575 付近)。(3) 決定 18 の チ は §14.4 の一覧に名前「チ」で入れる(意図マップの ✕ 4 件 = I-6 / I-7 / I-8 / I-16 の逐語)。(4) 報告の末尾に、3 本と全体の `--collect-only | tail -1` の最終値を必ず貼る(リードが返答に書く数はそれを使う)**」 |
| **判定区間を開けない** | リードの委任文面「**`--mode full` を走らせない。`backtest_data/o3c_reaction_20260918_full/` を作らない。**」 |
| **関門・台帳・`.claude/`・`CLAUDE.md`・`ACTION_LOG` に触らない** | リードの委任文面「**`scripts/_research_audit_gate.py`・`docs/AUDITOR/HOOK_MANIFEST.sha256`・`.claude/`・`CLAUDE.md`・`docs/AUDITOR/ACTION_LOG.md` は触らない**」 |
| 報告の中身(§0.1 の 2 列表 / 処置の 1 行要約と行番号 / 試験の末尾と `--collect-only` / §8.5 の再現 / 走らせ直しの突き合わせ / 決定 1・3・20 の関門の通る側・止まる側 / 事実・推定・仮定の印) | リードの委任文面「**## 報告(日本語)**」の 6 行 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**20 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 11 個目の表(20 行)に明記した**(PREREG:406〜442)。

**作業中に、原文に無い判断が必要になった点が 5 件ある。**
**黙って決めずに、決めたことと理由を §4 に分けて書いた**
(`CLAUDE.md` §0.1「**作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる**」)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(9 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(8 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r8.md` + `scripts/o3c_reaction_judge.py` + `scripts/o3c_reaction.py` + テスト
>
> ## 監査(owner-auditor)— 段 A 事前登録・走行前の再監査(9 回目)
>
> 1. **[止める]** `/home/user/trade/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:798-812` vs `/home/user/trade/scripts/o3c_reaction.py:242-256` — 台帳の節は「**`--mode full` が走るたびに 1 行追記する(出力を書く前に)**」とだけ書き、`record_opened` の注は「**走行が途中で落ちても「開けた」ことは残る(= 落ちた回を消してやり直す経路も (g) で止まる)**」と書く。**この帰結(走行が 1 分で異常終了しても 6 つの出力先の 1 つが永久に死に、再走行は §14.6 の「新しい開封として §3.1 に数える。オーナーの決定が要る」に落ちる)は PREREG に 1 行も無い**(実測: `grep -n '落ちた\|途中で落\|中断'` の一致 0)。処置書 `2026-09-18_reaction_prereg_r8.md:584` は「**落ちた回も「開けた」として残す方を選んだ**」を**原文に無い判断**として自分で挙げている。**開封の可否を決めるオーナーがこの帰結をどこで読むか**(P-8、A-6。8 回目の指摘 2 と同型)
>
> 2. **[止める]** `REACTION_PREREG_2026-09-18.md:1385` vs `:1394` — F1 の機構は「**観測量: `doi_pre_1h` が負の群で、戻り到達が対照との差で大きい**」(符号の条件)だが、実際に判定する 12 セルは「**主軸 D の D1(`doi_pre_1h` が最も負の 3 分位)**」(順位の条件)である。**D1 の切り値が負であることを確かめた記述が本書に 1 行も無く**(実測: `grep -n '最も負\|負の群'` の一致は上の 2 行だけ)、**判定区間の切り値が 0 以上に出たときに F1 の読みをどうするかが決まっていない**。開封後にここを読み替えると A-6 に当たる。**「負の群」と「最も負の 3 分位」が同じものだと書ける根拠は、走行の前にどこにあるか**(P-3、A-6)
>
> 3. **[止める]** `REACTION_PREREG_2026-09-18.md:3564-3566` vs `:3928-3930` — §14.4 の「何で担保するか」の表は、担保していないこととして「**欄に書き写すのはリードである**」だけを挙げる。しかし `tool_commit` は `git rev-parse HEAD` であって作業ツリーを見ない(`scripts/o3c_reaction.py:259-275` / `scripts/o3c_reaction_judge.py:631-646` に `git status --porcelain` の類は無い = 実測)。同じ文書の :3928 は「**`tool_commit` の値 `759f0920e7cb…` は「走らせ直した時点の `git rev-parse HEAD`」であって、本版の変更を含むコミットではない**」と、**記録された版が実際に走った道具と違った実例を自分で書いている**(本監査の時点でも PREREG と `_r8.md` は未コミットのまま = 実測 `git status --porcelain` 4 ファイル)。**「欄と、実際に走った道具の版が食い違ったら止まる」と書ける根拠は何か。未コミットの変更がある状態で開封したとき、何がそれを検出するか**(P-8、O-3、A-6)
>
> 4. **[直す]** `REACTION_PREREG_2026-09-18.md:629-633` — §3 の関門表は「**`--mode sample --days …` | 無審査 | `--approval L-NNN` が必須**」「**`--mode anchor` | 日では止めない**」のままである。同じ文書の §3.1:783 は「**判定区間の日を開ける経路は 6 本の `--mode full` だけである**」、§14.1:3265 の (h) は「**`--approval` があっても通さない**」と書く。**8 回目の指摘 5(同じ文書が同じ関門について正反対を書く)と同型が §3 に残っていないか。表 (a)〜(h) と一致するのはどちらか**(P-2、O-3)
>
> 5. **[直す]** `REACTION_PREREG_2026-09-18.md:601` / `:2043` / `:2074` — 3 か所の埋め込みが `R.check_approval("full", "L-199")  # §3 の関門を通す` を先頭に置き、その下に「**出力(実測、2026-09-18)**」を貼っている。**現物の関門ではこの呼び出しは必ず止まる**(実測: `PYTHONPATH=src:scripts python3 -c "R.check_approval('full','L-199')"` → `[止め] 事前登録 §14.4 の「応答の L 番号」が読めないので、判定区間を開けない`。さらに (b)「L-199 より後でない」と (d)「`--out-dir` が無い」でも止まる)。**8 回目の指摘 4 は §14.4.1 だけを現物に直した。§3・§8.6 の同じ型はなぜ残っているか。「実測」の印が付いた出力を、現行の関門で再現できない手順のまま凍結してよいか**(P-2、O-3)
>
> 6. **[直す]** `REACTION_PREREG_2026-09-18.md:2694` / `:4128` vs `:4257` — :4257 は「**書くのは「破れた検査の名前・走行の名前・件数」だけである(本版で「理由」を外した = 8 回目の指摘 12。値は 1 つも書かない)**」だが、:2694 と :4128 は「**`stopped.txt`(破れた検査の名前・走行の名前・理由)**」のままである。加えて :2694 / :4128 は引き金の検査を「params / サニティ #14 / 軸の作り方」の 3 つとし、**決定 14 で足した「承認の番号」が入っていない**(:4257 は 4 つ)。**同じファイルの中身と引き金が 3 通りあるのは、走査を掛けた結果か**(P-2、P-5)
>
> 7. **[直す]** `REACTION_PREREG_2026-09-18.md:3198` / `:3575` — 待つものは本版で 16 件(ケ を報告へ、タ を追加)になったのに、:3198 は「**§14.4 の 4 に並ぶ全部(ケ〜ソ)をオーナーに報告し**」、:3575 は「**§14.4 の待つもの ケ〜ソ を読んだこと**」のままである(:3427 の表は「**合計 16 件**」、:3429 は「**ケ〜タ が残る 8 件**」)。**オーナーの応答が承認する範囲に タ は入るのか、入らないのか**(P-5、P-2)
>
> 8. **[直す]** `REACTION_PREREG_2026-09-18.md:4435` / `:4509-4511` — 凍結文書は「**リードが確認した(2026-09-18、`ACTION_LOG` 062: 残り 8 行を読み、経緯の説明と走査の出力のみ)**」を、末尾の断定「**判定語を、判定として書いた行はこの文書に 1 つも無い**」の出所にしている。**実測**: `/home/user/trade/docs/AUDITOR/ACTION_LOG.md` の見出しは `## 061` までで、**062 は存在しない**(`grep -n '^## '` の末尾 = 061)。**まだ書かれていない記録を確認の出所として凍結文書に置いてよいか(8 回目の指摘 13 は「確認が済む前の断定」を問うていた)**(P-2、O-3)
>
> 9. **[直す]** `/home/user/trade/tests/test_o3c_reaction.py:932` / `:944` vs `REACTION_PREREG_2026-09-18.md:3267` — 事前登録は「**7 つとも通ったときだけ、`judgment_days` の 456 日が開く**」と書き、関門は (a)〜(h) の 8 行になったが、試験名は `test_full_mode_gate_passes_when_all_five_checks_are_met` / `test_full_mode_gate_stops_on_each_of_the_five_checks` のままで、:3277-3278 はその名前をそのまま引用している。**5 回目の指摘 15「テスト名を分岐の数に合わせて改名」と同型が、関門の側に残っていないか**(P-5)
>
> 10. **[直す]** `REACTION_PREREG_2026-09-18.md:3450`(ケ)vs `:2140-2150`(§8.6 の表)— ケ は「**この欠けを抱えたまま走らせるのはリードの判断である(取り直し不可)**」と断定する。§8.6 の同じ根拠の表は「**第三者 … tardis は鍵なしの一覧が 200。データの取得は試していない(未確認)**」「**オーナー PC | 未確認(この環境からは見られない)**」と書く。**オーナーが読む ケ の「取り直し不可」に、この 2 行の範囲が付いていないのはなぜか**(P-1、KA-03 の型)
>
> 11. **[直す]** `/home/user/trade/docs/DATA.md:106` vs `REACTION_PREREG_2026-09-18.md:2153-2162` — 台帳は「**欠測99日(`2023-09-25`・`2023-11-19` の単発 2 日 + `2024-03-04`〜`2024-06-08` の連続 97 日)。うち段 A の判定区間内は 97 日**」と、**別集合の 2 つの「97 日」を同じ行に並べている**(前者は 2024-06-01 を含み 2023-11-19 を含まない / 後者は逆)。事前登録は「**「2024-03-04〜06-08 の連続 97 日」という書き方は本書から消した**」と自分からは消している。**消した書き方が、事前登録が参照する台帳に残っていることは走査したか**(P-2、P-5)
>
> 12. **[直す]** `REACTION_PREREG_2026-09-18.md:2816` / `:624` / `:3238` — 行番号つきの引用が現物とずれている。**実測**: `MATCH_TOL_PCT` は `scripts/o3c_reaction.py:152`(本書は `:140` と書く)/ 読めない日を `missing` に積むのは `:1170-1183`(本書は `:901-904`、現物の 901-904 は `window_hours: float,` の引数列)/ `--window-hours` の引数は `:2401`(本書は `:2110`、現物の 2110 は相関の docstring)。**7 回目の指摘 4・8 回目の指摘 4 と同じ型である。行番号つきの「実測」の引用を全部走査したか、走査したならその出力はどこにあるか**(P-2、O-3)
>
> 13. **[直す]** `/home/user/trade/CLAUDE.md` §3 の試験の行 vs `REACTION_PREREG_2026-09-18.md:4344` — 事前登録は「**リポジトリ全体は 2,109 件 → 2,120 件**」と書き(**実測**: 本監査で `--collect-only` = `2120 tests collected`)、`CLAUDE.md` §3 は「**現在 2,041 件収集**」のままである。**この単位で 3 回足した試験を、収集数の台帳へ反映する導線はどこにあるか**(P-5、P-8)
>
> 14. **[直す]** `/home/user/trade/scripts/o3c_reaction_judge.py:1752` vs `:1710` — 感度の承認の食い違いは `CHK_APPROVAL = "承認の番号(決定 14)"` として `judge_bad` に入るが、**判定 2 本の承認の食い違いは `check_run_params` の中なので `stopped.txt` には `params の検査(決定 12''')` として出る**。`stopped.txt` は「検査の名前・走行の名前・件数」だけで理由を書かない(決定 12)。**「承認の番号が破れた」ことを `stopped.txt` だけで見分けられるか**(P-2)
>
> 15. **[聞く]** `REACTION_PREREG_2026-09-18.md:3603` / `:3564` — 「**「凍結した道具」の定義 = 開封(§14.1 を走らせるとき)の時点の `scripts/o3c_reaction_judge.py` である**」。走行の道具 `scripts/o3c_reaction.py` は `summary.json` に `tool_commit` を**記録するだけ**で、6 本の走行どうしでも、§14.4 の欄とも突き合わせない(実測: `grep -n 'tool_commit' scripts/o3c_reaction_judge.py` の突き合わせ先は自分の版だけ)。§14.6:3899-3904 は走行の道具を **4 回変えた**と書く。**6 本が同じ版の走行の道具で走ったことを何が担保するか。「凍結した道具」に走行の道具を入れないと決めたのは誰か**(A-6、P-8)
>
> 16. **[聞く]** `REACTION_PREREG_2026-09-18.md:3424`(分類の表)/ `:3450`(ケ)— ケ は「**報告(反対が無ければこの形で進める)**」に置かれ、その中身は「**F1 の 12 セルの MDE は §8.3 の見込みの約 1.13 倍**」「**この欠けを抱えたまま走らせるのはリードの判断である**」である。一方 §14.4:3524-3526 は「**§8.6 が「欲しい効果は置かない」ため `research-protocol` §4.1 の関門(欲しい効果 < MDE なら実行しない)が当たらず、検出力についての関門が 1 つも掛かっていない**」と書く。**検出力について関門が 1 つも無い状態で、オーナーの沈黙(反対が無い)によって 456 日が一度きり開く形でよいか**(P-9、A-13)
>
> 17. **[聞く]** `REACTION_PREREG_2026-09-18.md:3456`(ソ)/ `:3063` — ソ は「**事実**」の側に置かれている。中身は「**1 周目で 472 日を使い切る**」「**この環境の在庫には 2024-10-15 以降で「清算 + 約定 + metrics」が揃った日が名前で見るかぎり 0 日**」「**2 周目の期間は未確定である**」である。**「手持ちのデータを 1 周目で全部使い切り、2 周目の材料は未調達のまま」という選択は、答えを求めない「事実」でよいか(調達票 = 経路 3 つ以上・到達確認は、この判断の前に実行されたか)**(P-7、KA-14 の型)
>
> 18. **[聞く]** `REACTION_PREREG_2026-09-18.md:451` / `:3680`(§10.3 の候補 2)— §1 は「**△ 8 件 / ✕ 4 件が残ったまま走るので、段 A の結果はどの行についても「陰性」にはならない**」と書き、✕ 4 件がどの意図かは設計 §8 にしかない。待つもの 16 件に ✕ 4 件の中身は 1 件も入っていない。**一度きりの開封の前に、「✕ 4 件が F1 の判定のどこに掛かるか」をオーナーが見る必要はないか(`research-protocol` §0.5 は「△ や ✕ が残ったまま出た陰性は不明」と書くが、走ってよいかは書いていない)**(P-3、A-9)
>
> 19. **[聞く]** `REACTION_PREREG_2026-09-18.md:3578` — 「**開封前の最後の返答に、「§13 の表と待つものが変わっていない」ことを監査役(`owner-model-auditor`)が確認した記録を付ける**」。`CLAUDE.md` §5.0b ① は `owner-model-auditor` の入力を「**オーナーの逐語 + TRACE だけに限る**」と定めており、事前登録の §13 の表の差分はその入力に含まれない。**この確認を当てる先はこの監査役でよいか(入力に無いものを確認させる形になっていないか)**(P-8、A-15)
>
> 20. **[聞く]** `REACTION_PREREG_2026-09-18.md:831` — 関門 (h) の射程は「**集合は `--data-root` の在庫から作る。在庫が読めない / 空なら集合は空になり、この関門は何も止めない**」と書かれている。一方 (d)(e)(g) は `--data-root` に依らない。**「在庫を差し替えると (h) だけが効かなくなる」という非対称は、オーナーが読む §3.1 の「残る穴」(:806-812)の 3 件に入っていない。穴の一覧はこれで全部か**(P-1、P-8)
>
> 判定: 止める

---

## 2. 20 件の処置

**行番号は処置後の PREREG / REACT / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**20 件のうち「変えない」で済ませたものは 1 件だけである**(13。**リードの決定がそう指示している**)。

### 1.[止める] 走行が途中で落ちた回の帰結を、オーナーはどこで読むか

**処置: 直した(機械と文の両方)。**リードの決定(逐語):
「**走行が途中で落ちた回も「開けた」として残す(台帳に「started」で記録し、完了時に「done」を追記。
「done」が無い出力先の再走行も (g) で止まる)。その帰結(6 つの出力先の 1 つが死に、
再走行は新しい開封としてオーナーの決定が要る)を §3.1 と §14.6 に明記する**」

| 何 | どこ |
|---|---|
| **台帳の 1 行に状態の列(`started` / `done`)** | **REACT `OPENED_HEADER` / `OPENED_LINE_RE` / `OPENED_STARTED` / `OPENED_DONE`**(`scripts/o3c_reaction.py` の台帳の節)。**古い 3 列の行も読める**(状態は `None`) |
| **`record_opened(..., status="started")` / `record_done(...)` / `opened_status()`** | 同ファイルの同じ節 |
| **`main()` が最後まで終わった回だけ `done` を足す** | REACT `main()` の末尾(`s = run_table(...)` の**後**) |
| **(g) は `started` / `done` を区別しない** | `opened_out_dirs()` の注 |
| **帰結を §3.1 に明記** | **PREREG:862〜865**(台帳の表に「状態の 2 語」の行)/ **PREREG:867〜882**(「走行が途中で落ちた回の帰結」の表 4 行: 台帳に `started` が残る / (g) で 6 つの 1 つが死ぬ / 7 つ目は新しい開封でオーナーの決定が要る / どこで見えるか) |
| **帰結を §14.6 に明記** | **PREREG:4126〜4133** |
| **試験** | `tests/test_o3c_reaction.py::test_the_ledger_marks_started_before_the_run_and_done_after_it`(落ちた回 = `started` だけ → (g) で止まる / 終わった回 = `started` + `done`)/ `::test_the_opened_ledger_records_out_dir_time_and_approval`(4 列目を測る) |

**実測は §3 の 5 にある**(通る側・止まる側の両方)。

### 2.[止める] 「負の群」と「最も負の 3 分位」が同じものだと書ける根拠

**処置: 直した(記述を D1 に統一し、切り値を機械が残す形にした)。**リードの決定(逐語):
「**F1 の記述を「`doi_pre_1h` が最も負の 3 分位(D1)」に統一し、「負の群」の語は消す。
読みのスクリプトは D1 の上側の切り値を `summary.json` と `f1_reading.txt` に記録し、
切り値が 0 以上なら「D1 は負の群と一致しない(切り値 X)」を射程として必ず書く
(判定は D1 のまま。走行後に読み替えない)。§7.0・§10.3 に書く。テスト**」

| 何 | どこ |
|---|---|
| **F1 の機構の文から「負の群」を消し、「最も負の 3 分位(主軸 D の D1)」に** | **PREREG:1480**(§7 の機構の文)+ **:1482〜1490**(なぜ直したかの注) |
| **切り値を必ず残す規約(5 行の表)** | **PREREG:1502〜1511** |
| **実装** | JUDGE `f1_cut_note()`(`F1_GROUP` の直下)。`f1_reading.txt` に「D1 の上側の切り値(doi_pre_1h)」と「射程:」の 2 行、`summary.json` に「F1 の D1 の上側の切り値(決定 2)」の 5 鍵 |
| **切り値が 0 以上なら** | 「**D1 は負の群と一致しない(切り値 X)**」を両方に書く。**判定は D1 のまま** |
| **試験** | `tests/test_o3c_reaction_judge.py::test_the_f1_reading_records_the_d1_cut`(0 以上 / 負 / NaN / 群が無い、の 4 通り) |

**標本 6 日での実測(参考。判定区間ではない)**: **D1 の上側の切り値 = −12085.666666666662**
(§8.5 の再現コマンドと同じ `cut[0]`。**PREREG:1510 に書いた**)。
**判定区間で同じ符号になるとは限らない**(**未確認**。だから機械で残す)。

### 3・15.[止める / 聞く] 版の担保は何か / 「凍結した道具」に走行の道具を入れないと決めたのは誰か

**処置: 直した(版の担保を 1 本にまとめた)。**リードの決定(逐語):
「**版の担保を 1 本にする: 走行・読みの両スクリプトは `git rev-parse HEAD` に加えて
`git status --porcelain` の結果(汚れているか / 変更ファイルの一覧)を `summary.json` に記録する。
読みのスクリプトは (i) 自分の作業ツリーが汚れていれば「[止め]」、(ii) 判定 2 本 + 感度 4 本の
`tool_commit` がすべて同じで、かつ §14.4 の「凍結した道具のコミット」欄と一致し、
(iii) 6 本とも汚れていない、でなければ「[止め]」。走行スクリプトは `--mode full` のとき
作業ツリーが汚れていれば「[止め]」(開封はコミット済みの版からだけ)。
「凍結した道具」= 開封時の HEAD 1 つ(走行と読みの両方を含む)。§14.4 に書く。テスト**」

| 何 | どこ |
|---|---|
| **`git status --porcelain` を読む関数** | REACT `git_status()` / `_dirty_block()`、JUDGE `git_status()` / `run_dirty_state()` |
| **`summary.json` に `tool_dirty`** | REACT の `run_table` と `run_anchor` の両方の summary(`"tool_dirty": _dirty_block()`) |
| **走行の側の関門 (i)** | REACT `check_approval` の (i)。**`--mode full` は作業ツリーが汚れていれば「[止め]」**(`不明` でも止める) |
| **読みの側の 3 つを 1 つの関門に** | JUDGE `check_tool_versions()` + `main()`。**台帳の関門 `pass_audit_gate` の後ろに置いた**(台帳が閉じている回は版の検査より先に止まる = `tests/test_audit_gates_wired.py` が測っている経路) |
| **欄が「(まだ無い)」でも「[止め]」に変えた** | JUDGE `main()`。**前版は記録だけして進んだので、欄を空にすれば版の検査が 1 つも掛からなかった** |
| **「凍結した道具」の定義を広げた**(走行の道具も含む) | **PREREG:3730〜3754**(§14.4 の「何で担保するか」の表を 6 行に書き直した) |
| **§14.1 の関門の表に (i) を足した** | **PREREG:3404** |
| **試験** | `tests/test_o3c_reaction.py::test_full_mode_gate_stops_when_the_worktree_is_dirty` / `::test_the_run_records_its_own_commit`(`tool_dirty` の 3 鍵)/ `tests/test_o3c_reaction_judge.py::test_the_tool_version_gate_checks_the_worktree_and_the_six_runs` / `::test_the_frozen_tool_commit_is_recorded_and_matched` |

**射程(隠さずに書く。PREREG:3754 に書いた)**: **版の検査に入るのは「読めた走行」だけである。**
**読めない感度の走行はこの検査より前に落ち、その表は 1 枚も書かれない**(決定 2''''')。
**よって版の検査の対象から外れる。これは本版で閉じていない。**

### 4.[直す] §3 の関門表と (a)〜(h) のどちらが現物か

**処置: 直した。**リードの決定(逐語):
「**§3 の関門表を (a)〜(h) と一致する記述に直す**」

**PREREG:681〜694**。**表を 3 列から 4 列にし、「判定区間の日を 1 日でも含む」の列を足した。**
**`--mode sample --days` と `--mode anchor` はその列で「(h) で「[止め]」。`--approval` があっても
通さない」になり、§3.1・§14.1 と一致する。**
**「`--mode full` は (a)〜(j) を全部通ったときだけ走る」に直した。**

### 5.[直す] §3・§8.6 の埋め込みが現行の関門で再現できない

**処置: 直した(注記を足し、「実測」の印はそのまま)。**リードの決定(逐語):
「**§3・§8.6 の埋め込みスクリプト 3 か所を「当時の関門で実行した記録(現行の関門 (a)(b)(d)(h) では
止まる。再現するには当時のコミット XXX の版が要る)」と注記し、「実測」の印はそのまま(当時の実測)**」

**3 か所とも直した**: **PREREG:641**(埋め込みの行)+ **:650〜659**(注)/ **:2159 + :2172〜2181** /
**:2201 + :2221〜2230**。
**現行の関門で止まることを実測した**(**実測** 2026-09-18):

```
PYTHONPATH=src:scripts python3 -c "
import o3c_reaction as R
try: R.check_approval('full','L-199')
except SystemExit as e: print('[止まった]'); print(str(e))
"
[止まった]
[止め] 事前登録 §14.4 の「応答の L 番号」が読めないので、判定区間を開けない。
       事前登録 §14.4 の「応答の L 番号」の欄が埋まっていない(欄の値: '(まだ無い。オーナーの応答を待っている。)')
       オーナーの応答(L 番号)を §14.4 の欄に書き写してから走らせる。
```

**(a) で止まる**(**欄が「(まだ無い)」**)。**欄を埋めても (b)「L-199 は L-199 より後でない」と
(d)「`--out-dir` が渡っていない」で止まる**(**関数の分岐を読んだ = 推定ではなく実装の読み**)。
**「実測」の印はそのままにした**(**当時の実測だからである**)。

### 6.[直す] `stopped.txt` の中身と引き金が 3 通りある

**処置: 直した。**リードの決定(逐語):
「**`stopped.txt` の記述を全箇所「検査の名前・走行の名前・件数。引き金 4 つ
(params / #14 / 軸の作り方 / 承認の番号)」に統一**」

**PREREG:2832**(§10.3 の表)と **:4357**(§14.6 の出力の表)を、**:4489**(既に直っていた行)と
同じ文言にそろえた。**3 か所とも「検査の名前・走行の名前・件数」「理由も値も書かない」「引き金 4 つ」**である。

### 7.[直す] 「ケ〜ソ」の残り / オーナーの応答が承認する範囲に タ は入るか

**処置: 直した(ただし件数はリードの追加の文面により 17 件・ケ〜チ)。**
リードの決定(逐語、9 回目の指摘 7):
「**「ケ〜ソ」の残りを「ケ〜タ の 8 件」「合計 16 件」に統一(:3198・:3575 ほか grep で全箇所)**」
**その後のリードの追加の文面(返答の監査 7 巡目)**: 「**待つものは 17 件 = 報告 6 / 答えが要る 3 /
事実 8(コ・サ・シ・ス・セ・タ・ソ・チ)。「合計 16」「ケ〜タ が残る 8 件」「ケ〜ソ」の全箇所を
grep で拾って直す**」。**後の文面が正である**(チ を足したため)。

**直した行**: **PREREG:3336**(§13 の「判定区間を開けるのは…ケ〜チ の 8 件」)/
**:3575**(「ケ〜チ が残る 8 件」)/ **:3568〜3573**(分類と件数の表: 事実 8 件 / 合計 17 件)/
**:3629**(注の「事実として出すのは コ・サ・シ・ス・セ・タ・ソ・チ の 8 件」)/
**:3763**(応答が承認するもの)。

**grep の出力は §3 の 6 にある**(範囲を切っていない全ファイルの走査)。

### 8.[直す] まだ書かれていない `ACTION_LOG` 062 を出所にしている

**処置: 直した(番号を書かない形にした)。**リードの決定(逐語):
「**「ACTION_LOG 062」を出所にしている箇所を「リードが確認した(2026-09-18。記録は
`ACTION_LOG` に書く)」に直す(番号を書かない)**」

**PREREG:4702〜4705** と **:4777〜4790**(注を含む)。
**残った 1 か所(:4783)は「前版はここと上の 2 か所で「`ACTION_LOG` 062」を…」という
経緯の記述である**(**前版の誤りを消さないために残す**)。
**§0.1 の :391 は 8 回目のリードの決定の逐語なので触っていない。**

**実測**(監査役の指摘のとおり): `grep -n '^## ' docs/AUDITOR/ACTION_LOG.md | tail -2` の出力は
`## 060` と `## 061` で、**062 は無い**。

### 9.[直す] テスト名が分岐の数と合っていない

**処置: 直した(改名)。**リードの決定(逐語):
「**テスト名 `five_checks` → `eight_checks` に改名し、事前登録の引用も直す**」

`tests/test_o3c_reaction.py::test_full_mode_gate_passes_when_all_eight_checks_are_met` /
`::test_full_mode_gate_stops_on_each_of_the_eight_checks`。
**止まる側の試験は (a)(a-2)(b)(c)(d)(d')(e)(f)(g)(h) を 1 つずつ崩す形にした。**
**事前登録の引用は PREREG:3418〜3424。****「7 つとも通ったときだけ」は
「(a)〜(j) の 10 個とも通ったときだけ」に直した**(**PREREG:3407**)。

> **【原文に無い判断。§4 の 1 に書いた】**
> **リードの決定は「`eight_checks` に改名」である。**
> **一方、決定 3・15 と 20 で関門が 2 つ増えて (a)〜(j) の 10 個になった。**
> **試験名を `ten_checks` にするとリードの逐語から外れ、`eight_checks` のままだと
> 関門の数と合わない。****逐語のとおり `eight_checks` にし、
> 「この 8 は表の (a)〜(h) の 8 行を指す」と試験の docstring と PREREG:3421 に明記して、
> (i)(j) は専用の試験 2 本で測る形にした**(決定 3・20 がそれぞれ「テスト」と書いている)。

### 10.[直す] ケ の「取り直し不可」に範囲が付いていない

**処置: 直した。**リードの決定(逐語):
「**ケ の「取り直し不可」に範囲を付ける: 「公開アーカイブ・monthly・REST・Coinalyze・CoinGlass は
実測で不可 / tardis のデータ取得とオーナー PC は未確認」**」

**PREREG:3604**(ケ の行)。**`CLAUDE.md` §5.2「「取れない・無い」の主張には範囲と方法を書く」**を
引いた。**§8.6 の表(tardis は鍵なしの一覧が 200 でデータ取得は未試行 / オーナー PC は未確認)と
一致する。**

### 11.[直す] `docs/DATA.md` が別集合の 2 つの「97 日」を同じ行に並べている

**処置: 直した。**リードの決定(逐語):
「**`docs/DATA.md` の 106 行目の「連続 97 日」の書き方を「2024-03-04〜06-08 の 97 暦日
(うち 2024-06-01 は清算の zip も無い日)。判定区間内の欠けは 2023-11-19 + その 96 日 = 97 日」に
直す(`docs/DATA.md` は触ってよい)**」

**`docs/DATA.md`:106**(COIN-M metrics の行)。
**2 つの集合を分けて書いた**: **欠測 99 日のうちの 97 暦日(2024-03-04〜06-08。2024-06-01 を含む)**と、
**判定区間内の欠け 97 日(2023-11-19 + 2024-03-04〜05-31 の 89 日 + 2024-06-02〜06-08 の 7 日)**。
**89 + 7 = 96、1 + 96 = 97 で、事前登録 §8.6 の 3 つの塊と一致する。**

### 12.[直す] 行番号つきの引用が現物とずれている

**処置: 直した(全部走査し、関数名で指せるものは関数名に替えた)。**リードの決定(逐語):
「**行番号つきの引用(`scripts/o3c_reaction.py:NNN` など)をすべて走査し、現物と一致させる
(関数名で指せるものは関数名に替える)。走査のコマンドと出力を処置書に貼る**」

**走査のコマンドと出力は §3 の 7 にある。**

| 前版 | 本版 |
|---|---|
| `scripts/o3c_reaction.py:2110`(2 か所) | `scripts/o3c_reaction.py` の `main()` の `--window-hours` |
| `scripts/o3c_reaction.py:901-904` | `scripts/o3c_reaction.py` の `_anchor_chunks_bar60` と `agg_day` が `missing` に積む |
| `scripts/o3c_oi_distance.py:542` / `:87` | `scripts/o3c_oi_distance.py` の `_apply_liqdir_and_leverage` と `LIQDIR_SOURCE` |
| `scripts/o3c_oi_distance.py:127` | `scripts/o3c_oi_distance.py` の `LIQ_SIGN` |
| `scripts/o3c_reaction.py:2124`(粒度) | `scripts/o3c_reaction.py` の `main()` の `--granularity` |
| `scripts/o3c_reaction.py:42` / `:131` | `scripts/o3c_reaction.py` の冒頭の注と定数 `REACT_SIGN` |
| `scripts/o3c_reaction.py:140` | `scripts/o3c_reaction.py` の `MATCH_TOL_PCT` |
| `BINANCE_CM_MMR_2026-09-17.md:30` / `:142` | `docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md:30` / `:142`(**別のディレクトリなので道を足した**) |
| `scripts/o3c_reaction.py:179`(§14.4.1) | **そのまま残し、「この `:179` は前版の文面の一部であり、現物を指す引用ではない」と注記**(**直すと前版の誤りが消える**)。**PREREG:3858〜3861** |

**残った 4 件の相対名(`REACTION_RUN12_2026-09-18.md:64` ほか / `DESIGN_2026-09-17.md:19` /
`OI_DISTANCE_2026-09-17.md:293` / `:518`)は同じディレクトリのファイルで、
現物を 1 行ずつ確かめて一致した**(§3 の 7)。
**PREREG 自身への 5 件(`:5` `:6` `:14` `:12` `:27`)は行番号ではなく `grep -cH` の出力
(ファイル名:一致行数)である**(走査の道具が「引用」として拾っただけで、誤りではない)。

### 13.[直す] `CLAUDE.md` §3 の収集数が古い

**処置: 変えない(リードの決定がそう指示している)。**リードの決定(逐語):
「**`CLAUDE.md` は触らない(既に 2,120 に更新済み = コミット 0bd6e43。監査の指摘は古い読み)。
処置書にその旨を書く**」

**委任の文面でも `CLAUDE.md` は禁止されている**(「**`CLAUDE.md` …は触らない**」)。
**本作業で `CLAUDE.md` は 1 行も編集していない**(**実測**: `git status --porcelain` に出ていない)。
**本版で試験が 6 件増えて全体 2,126 件になったが、`CLAUDE.md` §3 の更新はリードが行う。**

### 14.[直す] 「承認の番号が破れた」ことを `stopped.txt` だけで見分けられるか

**処置: 直した。**リードの決定(逐語):
「**判定 2 本の承認の食い違いも `stopped.txt` に「承認の番号」の検査名で出す
(params の検査と分ける)。テスト**」

**JUDGE**: 判定 2 本の `check_run_params(...)` から `approval=` を外し、
**`CHK_APPROVAL = "承認の番号(決定 14)"` の側で判定 2 本にも `check_run_approval` を当てる。**
**感度 4 本と同じ名前になる。**
**PREREG:4540〜4554**(§14.6 に「承認の番号は `params` の検査と分けて出す」の節)。
**試験**: `tests/test_o3c_reaction_judge.py::test_the_judgment_runs_approval_is_named_separately_in_stopped_txt`
(**`stopped.txt` に「承認の番号(決定 14)」と 2 本の走行名が出て、「params の検査」は出ず、
L 番号の値は 1 つも出ない**)。

### 16.[聞く] 検出力の関門が 1 つも無い状態で、沈黙によって 456 日が開く形でよいか

**処置: ケ は報告の側のまま。注に書いた。**リードの決定(逐語):
「**ケ は報告の側のまま(オーナーが反対できる形)。§14.4 の注に「検出力の関門は無いが、
オーナーが止められる形で出す」と書く**」

**PREREG:3693〜3703**。**書いたこと**: **検出力について機械の関門は 1 つも掛かっていない
(§8.6 が「欲しい効果」を置かないため `research-protocol` §4.1 が当たらない)**、
**そのうえで ケ は「報告(反対が無ければこの形で進める)」の側に置く**、
**これは「関門がある」ことではなく「オーナーが読んで止めなければ開く」形である**。

### 17.[聞く] 2 周目の材料が未調達のままという選択を「事実」でよいか

**処置: ソ は事実のまま。次の手順を書いた。**リードの決定(逐語):
「**ソ は事実のまま。§14.4 に「2 周目の計画を凍結する前に調達票(経路 3 つ以上・到達確認)を
実行する」を次の手順として書く**」

**PREREG:3610**(ソ の行の末尾)。
**実測**(範囲を切っていない): `git ls-files -z | xargs -0 grep -lZ '調達票'` = **36 ファイル**、
そのうち O-3c の調達票は `docs/DATA/surveys/O3C_PROCUREMENT_*.md` の **6 本**で、
**いずれも 2026-09-12〜13 のもので、対象期間は 1 周目の 2023-06-25〜2024-10-14 である。**
**2024-10-15 以降についての調達票はまだ無い。**
**1 周目の開封はこの調達票を待たない**(1 周目の材料は在庫の 472 日で足りる)と書いた。

### 18.[聞く] ✕ 4 件が F1 の判定のどこに掛かるかをオーナーが見る必要はないか

**処置: 待つものに チ として足した(事実の側)。**リードの決定(逐語):
「**設計 §8 の意図マップの ✕ 4 件を名前で §14.4 の待つものに足す(**チ**: 「意図マップの ✕ 4 件
= …(名前)。F1 の判定のどの読みに掛かるか」)。事実の側。合計 17 件**」
**リードの追加の文面(7 巡目)**: 「**決定 18 の チ は §14.4 の一覧に名前「チ」で入れる
(意図マップの ✕ 4 件 = I-6 / I-7 / I-8 / I-16 の逐語)**」

**PREREG:3611**(チ の行)。**✕ 4 件を設計 §8 から逐語で引いた**:
**I-6**「**bitmex(300GB近い大量データをPCで取得済)を最初に使って検証**」/
**I-7**「**現在も稼働中の海外取引所とbitFlyerの清算データ**」/
**I-8**「**価格毎の約定履歴の積み上げからどの価格帯で清算が起きるかの予測できるか**」/
**I-16**「**必要データ収集の手段と管理の徹底方法**」。
**F1 の判定のどの読みに掛かるか**も同じ行に書いた(I-8 は段 A の射程の外 / I-6・I-7 は 12 セルの
外部妥当性 / I-16 は ソ の 2 周目の材料)。
**§1 からも チ を参照させた**(**PREREG:490〜491**)。
**分類と件数は 事実 8 件 / 合計 17 件**(**PREREG:3568〜3573**)。

### 19.[聞く] 開封前の最後の確認を当てる先はこの監査役でよいか

**処置: 直した(`owner-auditor` に)。**リードの決定(逐語):
「**開封前の最後の確認は `owner-auditor`(成果物の監査)で行う、に直す**」

**PREREG:3766**。**理由も同じ行に書いた**: **`CLAUDE.md` §5.0b ① は `owner-model-auditor` の
入力を「オーナーの逐語 + TRACE だけ」に限っており、事前登録 §13 の表の差分はその入力に含まれない。**
**§0.1 の :395 と :3770 は 8 回目のリードの決定の逐語なので触っていない。**

### 20.[聞く] 「在庫を差し替えると (h) だけが効かなくなる」は穴の一覧に入っていない

**処置: 直した(穴に足し、full の側を塞ぐ関門 (j) を作った)。**リードの決定(逐語):
「**(h) が `--data-root` に依る非対称を §3.1 の「残る穴」に足し、`--mode full` では
`--data-root` の変更を受け付けない(既定以外なら「[止め]」)。テスト**」

| 何 | どこ |
|---|---|
| **穴の一覧に 2 件足した**((h) の非対称 / 「`git status` が読めない」ときの扱い) | **PREREG:892〜907** |
| **関門 (j)** | REACT `check_approval` の (j)。**`--mode full` は `--data-root` が既定でなければ「[止め]」** |
| **§14.1 の表に (j) の行** | **PREREG:3405** |
| **塞いでいないこと(隠さずに書く)** | **非 full の側**: 別の `--data-root` を渡せば (h) の集合が空になる。**ただしその走行はその在庫の中の日しか読めない** |
| **試験** | `tests/test_o3c_reaction.py::test_full_mode_gate_rejects_a_non_default_data_root`(通る側 2 通り・止まる側) |

**実測は §3 の 5 にある。**

---

## 3. 走らせた結果

### 1. 試験(`-q` 無し)

```
PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py
........................................................................ [ 50%]
........................................................................ [100%]
144 passed in 52.97s
```

**リポジトリ全体**(`PYTHONPATH=src python -m pytest`)も走らせた(**実測** 2026-09-18):

```
........................................................................ [ 98%]
......................................                                   [100%]
2125 passed, 1 skipped in 269.74s (0:04:29)
```

**2,126 件 = 2,125 通過 + 1 スキップ**(**前版は 2,120 件**。本版で 6 件足した)。

> **【`-q` を足すと合計の行が出ない(8 回目の処置で実測済み。本作業でも同じ形で打った)】**
> `pyproject.toml` に **`addopts = "-q"`** があるので、手で `-q` を足すと **`-qq` になり、
> pytest は末尾の「N passed」の行を出さない。****合計件数を確かめるときは `-q` を足さない。**

### 2. `--collect-only`(**最終値**)

```
PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py --collect-only | tail -1
144 tests collected in 0.29s

PYTHONPATH=src python -m pytest --collect-only | tail -1
2126 tests collected in 1.50s
```

**内訳(1 本ずつ `--collect-only | tail -1`)**:

```
tests/test_o3c_reaction.py:        66 tests collected in 0.24s
tests/test_o3c_reaction_judge.py:  66 tests collected in 0.08s
tests/test_audit_gates_wired.py:   12 tests collected in 0.01s
```

**前版は 3 本で 138 件(63 / 63 / 12)、リポジトリ全体 2,120 件。**
**足したのは 6 件**(REACT 側 3 件 / JUDGE 側 3 件)。**§14.6 に書いた**(PREREG:4584〜4590)。

### 3. §8.5 の再現(152 セル)

```
照合したセル: 152 / 一致 152 / 食い違い 0
D1 の上側の切り値: -12085.666666666662
```

**標本 4 本を走らせ直した後に打った**(**実測** 2026-09-18)。
**「D1 の上側の切り値」の行は本作業で足した**(決定 2 の射程を実測するため。
**§8.5 の表の照合そのものは前版と同じコマンド・同じ出力である**)。

### 4. 標本 4 本の走らせ直しの突き合わせ

**`--mode sample --seed 1 --mmr 0.004` で 4 本、`pandas` で全列を突き合わせた**(**実測** 2026-09-18)。

```
gap60_w8/table.csv: 行×列 (662, 157) -> (662, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 10.48 -> 9.94 / .tool_commit: 759f0920e7cb56174cc97239a0989c7a474d2432 -> b5dd8e1c65ac7d7dd993f6fb23b723e36688bfda / .tool_dirty.変更ファイル: (無し) -> [...] / .tool_dirty.汚れているか: (無し) -> True / .tool_dirty.状態: (無し) -> dirty
gap60_w8/MD5SUMS: 変わったファイル ['summary.json']
gap60_w24/table.csv: 行×列 (665, 157) -> (665, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap60_w24/summary.json: 食い違い 5 件 -> .elapsed_sec: 10.31 -> 9.46 / .tool_commit: 759f0920e7cb… -> b5dd8e1c65ac… / .tool_dirty の 3 鍵の追加
gap60_w24/MD5SUMS: 変わったファイル ['summary.json']
gap30_w8/table.csv: 行×列 (784, 157) -> (784, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/table_mixed.csv: 行×列 (1, 157) -> (1, 157) / 列名の並び一致 True / 全列一致 True
gap30_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 10.67 -> 10.7 / .tool_commit: 759f0920e7cb… -> b5dd8e1c65ac… / .tool_dirty の 3 鍵の追加
gap30_w8/MD5SUMS: 変わったファイル ['summary.json']
gap180_w8/table.csv: 行×列 (503, 157) -> (503, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/table_mixed.csv: 行×列 (2, 157) -> (2, 157) / 列名の並び一致 True / 全列一致 True
gap180_w8/summary.json: 食い違い 5 件 -> .elapsed_sec: 9.95 -> 9.4 / .tool_commit: 759f0920e7cb… -> b5dd8e1c65ac… / .tool_dirty の 3 鍵の追加
gap180_w8/MD5SUMS: 変わったファイル ['summary.json']
```

**表を作る計算・列・乱数の消費は 1 行も変えていない**(**4 本とも `table.csv` /
`table_mixed.csv` が全列一致**)。
**`summary.json` の食い違いは `elapsed_sec`・`tool_commit` の値の変化・`tool_dirty` の 3 鍵の追加**
(**`tool_commit` が変わったのは、8 回目の処置の後にコミット `b5dd8e1` が作られて
`git rev-parse HEAD` が進んだためである**。**本作業はコミットしていない**)。
**`MD5SUMS` で変わったのは `summary.json` だけ。**
**§14.6 の「走行の道具を変えた回」の表に 5 行目として足した**(PREREG:4096 と :4098〜4133)。

**git の差分でも同じことが見える**(**実測** 2026-09-18、
`git diff --stat backtest_data/o3c_reaction_20260918_sample/`):

```
 gap180_w8/MD5SUMS      |  2 +-
 gap180_w8/summary.json | 23 ++++++++++++++++++++--
 gap30_w8/MD5SUMS       |  2 +-
 gap30_w8/summary.json  | 21 ++++++++++++++++++--
 gap60_w24/MD5SUMS      |  2 +-
 gap60_w24/summary.json | 19 ++++++++++++++++--
 gap60_w8/MD5SUMS       |  2 +-
 gap60_w8/summary.json  | 17 ++++++++++++++--
 8 files changed, 76 insertions(+), 12 deletions(-)
```

**`table.csv` と `table_mixed.csv` は 8 ファイルとも差分に 1 行も出ていない**
(= **バイト単位で同一**)。

**この走らせ直しの `tool_dirty` は `dirty` である**(処置の途中なので作業ツリーに未コミットの
変更がある)。**これは標本 6 日の走行であって開封ではない。**
**`--mode full` は関門 (i) が汚れた作業ツリーを止める**(下の 5)。

### 5. 決定 1・3・20 の関門の実測(通る側・止まる側)

**判定区間は 1 日も開けていない**(`check_approval` を一時ディレクトリの引数で呼ぶだけである)。
**出力(**実測** 2026-09-18)**:

```
### 決定 3・15 — 関門 (i) 作業ツリー
[通った] (i) 通る側: worktree=clean
[止まった] (i) 止まる側: worktree=dirty
    [止め] 作業ツリーがコミット済みでない(git status --porcelain: dirty、scripts/o3c_reaction.py)。
[止まった] (i) 止まる側: worktree=不明
    [止め] 作業ツリーがコミット済みでない(git status --porcelain: 不明)。
  既定(実物の git status): dirty / 変更ファイル数 15

### 決定 20 — 関門 (j) --data-root
[通った] (j) 通る側: data_root 既定
[通った] (j) 通る側: data_root を渡さない
[止まった] (j) 止まる側: data_root が別
    [止め] --mode full では --data-root を既定から変えられない(渡された値: /tmp/tmpv19xc926/別の在庫 / 既定: /home/user/trade/backtest_data/binance_cm_o3c_20260913)。

### 決定 1 — 台帳の started / done と (g)
  台帳の状態: {'gap60_w8': ['started']}
[止まった] (g) 止まる側: started だけの出力先(= 途中で落ちた回)
    [止め] --out-dir /tmp/tmpv19xc926/gap60_w8 は既に /tmp/tmpv19xc926/OPENED.txt に載っている(この出力先で判定区間を一度開けている)。
  台帳の状態: {'gap60_w8': ['started'], 'gap60_w24': ['done', 'started']}
[止まった] (g) 止まる側: done まで書いた出力先
    [止め] --out-dir /tmp/tmpv19xc926/gap60_w24 は既に /tmp/tmpv19xc926/OPENED.txt に載っている(この出力先で判定区間を一度開けている)。
[通った] (g) 通る側: 台帳に無い出力先
  台帳の中身:
    # `--mode full` が判定区間 456 日を開けた出力先の台帳(事前登録 §3.1。prereg 監査(8 回目)の指摘 2)。
    # 1 行 = <出力先の絶対パス>\t<UTC 時刻>\t<--approval の L 番号>\t<started|done>。
    # `started` = 関門を通って開けた(出力を書く前)。`done` = 走行が最後まで終わった。
    # **走行が途中で落ちれば `done` の行は出ない。**その出力先は `started` のまま残り、
    # 再走行は出力先の有無に関わらず関門 (g) で止まる(prereg 監査(9 回目)の指摘 1。リードの決定 1)。
    <tmp>/gap60_w8	2026-09-18T14:38:13Z	L-200	started
    <tmp>/gap60_w24	2026-09-18T14:38:13Z	L-200	started
    <tmp>/gap60_w24	2026-09-18T14:38:13Z	L-200	done
```

**同じ 3 つを毎回の pytest でも測る**:
`tests/test_o3c_reaction.py::test_full_mode_gate_stops_when_the_worktree_is_dirty` /
`::test_full_mode_gate_rejects_a_non_default_data_root` /
`::test_the_ledger_marks_started_before_the_run_and_done_after_it`。

### 6. 「ケ〜ソ」「ケ〜タ」「合計 16 件」の走査(**範囲を切っていない。全ファイル**)

**`CLAUDE.md` §3 の 4 本を打った**(**実測** 2026-09-18。**処置後**):

```
--- ケ〜ソ ---
ファイル数: 4
行数:
  docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r6.md:2
  docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r7.md:2
  docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md:3
  docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:5
ファイルごとの出現数:
  2 docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r6.md
  2 docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r7.md
  3 docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md
  6 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
出現数の合計: 13

--- ケ〜タ ---
行数: docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:3

--- 合計 16 件 ---
行数:
  docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r8.md:1
  docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:3
```

打ったコマンド(**範囲を切っていない**):

```
git ls-files -z | xargs -0 grep -lZ 'ケ〜ソ' | tr -dc '\0' | wc -c
git ls-files -z | xargs -0 grep -c 'ケ〜ソ' | grep -v ':0$'
git ls-files -z | xargs -0 grep -o 'ケ〜ソ' | cut -d: -f1 | uniq -c
git ls-files -z | xargs -0 grep -oh 'ケ〜ソ' | wc -l
```

**事前登録に残った 7 行の内訳(1 行ずつ読んだ)**:

| 行 | 何 | 直すか |
|---|---|---|
| **:358 / :398 / :423 / :436 / :3622** | **リードの決定の逐語**(§0.1 の対応表と §14.4 の注の中の引用) | **直さない**(**逐語を変えると `CLAUDE.md` §0.2 の A-1 に当たる**) |
| **:3581 / :3589** | **「前版は「合計 16 件」「ケ〜タ が残る 8 件」で…」という経緯の記述** | **直さない**(**直すと前版の誤りが消える**) |

**処置書 r6 / r7 / r8 の 7 行は過去の処置書であり、本作業では触っていない。**

### 7. 行番号つきの引用の走査(**事前登録 1 ファイル全体。範囲は切っていない**)

**走査の道具**(本作業で書いた。`/tmp/…/scratchpad/scan_refs.py`):
`([A-Za-z0-9_./-]+\.(?:py|md|yaml|yml|txt|sh|json)):(\d+)(?:[-–](\d+))?` を全行に当て、
拾った引用の**現物の行を印字して突き合わせる**。

**処置後の出力(**実測** 2026-09-18)**:

```
拾った引用: 12 件(範囲を切っていない = 事前登録 1 ファイル全体)
  [ファイルなし] :764 REACTION_RUN12_2026-09-18.md:64
  [ファイルなし] :1300 DESIGN_2026-09-17.md:19
  [ファイルなし] :3042 OI_DISTANCE_2026-09-17.md:293
  :3858 scripts/o3c_reaction.py:179
      現物 179: # §14.4 の欄と突き合わせるが、それは 6 本を走らせた後の段である。**
  :4053 docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md:30
      現物 30: >         "qtyCap": 50,
  :4059 docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md:142
      現物 142: | BTCUSD_PERP の完全な段階表(全段階) | 一次資料で確認(追記。公開 JSON [15][19]、…
  [ファイルなし] :4063 OI_DISTANCE_2026-09-17.md:518
  :4733 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:5
  :4738 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:6
  :4743 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:14
  :4748 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:12
  :4753 docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:27
```

**処置前は 21 件で、うち 8 件が現物とずれていた。**
**残った 12 件の内訳**:

| 何 | 件数 | 判定 |
|---|---|---|
| **同じディレクトリの相対名**(走査の道具が道を補わないので「ファイルなし」と出る) | **4**(`REACTION_RUN12:64` ほか / `DESIGN_2026-09-17:19` / `OI_DISTANCE_2026-09-17:293` / `:518`) | **1 行ずつ現物を読んで一致を確かめた**(下) |
| **`docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md:30` / `:142`** | **2** | **道を足した。現物と一致**(`"qtyCap": 50,` / BTCUSD_PERP の段階表) |
| **`scripts/o3c_reaction.py:179`** | **1** | **前版の文面の一部**(現物を指す引用ではない)。**注記を足して残した** |
| **事前登録自身への `:5` `:6` `:14` `:12` `:27`** | **5** | **行番号ではなく `grep -cH` の出力(ファイル名:一致行数)である。**誤りではない |

**同じディレクトリの 4 件を 1 行ずつ確かめた(**実測** 2026-09-18)**:

```
REACTION_RUN12_2026-09-18.md:64  -> | `backtest_data/o3c_reaction_20260918_anchor_trades_sample/` | 手順 1(約定そのまま…
REACTION_RUN12_2026-09-18.md:183 ->     --out-dir backtest_data/o3c_reaction_20260918_anchor_trades_sample
REACTION_RUN12_2026-09-18.md:186 -> 束 225(非タイ 110 / 両側とも非タイ 95) / 粒度 trades / 生の bp_h 列 あり
REACTION_RUN12_2026-09-18.md:369 -> `..._anchor_trades_sample/summary.json`。n は両側とも非タイの 95 行):
REACTION_RUN12_2026-09-18.md:395 -> --- backtest_data/o3c_reaction_20260918_anchor_trades_sample(約定そのまま・標本 6 日…
DESIGN_2026-09-17.md:19          -> - **積み上げ(プロファイル)**: 清算時刻 t の直前 **W = 24 時間**の aggTrades を…
OI_DISTANCE_2026-09-17.md:293    -> **97 日**(重なりは 375 日)。本走行が「読もうとして無かった日」は**前の日も含めて 100 日…
OI_DISTANCE_2026-09-17.md:518    -> - 維持証拠金率 MMR = **0.004(0.4%)**。出所 = Binance COIN-M API リファレンス…
```

**8 件とも、事前登録がその行について書いていることと一致する。**

---

## 4. 作業中に必要になった、原文に無い判断(5 件。黙って決めていない)

| # | どこ | 決めたこと | 理由 |
|---|---|---|---|
| 1 | 決定 9 のテスト名と、決定 3・20 で増えた関門の数 | **試験名は逐語どおり `eight_checks` にし、「この 8 は表の (a)〜(h) の 8 行を指す」と docstring と PREREG:3421 に明記。****(i)(j) は専用の試験 2 本で測る** | リードの逐語は「`five_checks` → `eight_checks`」。**同じ回の決定 3・15 と 20 で関門が (i)(j) の 2 つ増えて 10 個になった。****`ten_checks` にすると逐語から外れ、`eight_checks` のままだと関門の数と合わない。**逐語を優先し、「8」が何を指すかを書いて食い違いを消した |
| 2 | 決定 3 の (ii)(iii) を「読めない感度の走行」にどう当てるか | **版の検査に入るのは「読めた走行」だけである**(読めない感度は検査より前に落ちて、その表は 1 枚も書かれない) | 逐語は「判定 2 本 + 感度 4 本の `tool_commit` がすべて同じで」。**読めない感度には `summary.json` が無く、突き合わせる値が存在しない。**「読めない感度は表を 1 枚も書かない(決定 2''''')」が既にあるので、そちらに合わせた。**射程として PREREG:3754 と `check_tool_versions` の注に書いた** |
| 3 | 決定 3 の関門を `pass_audit_gate` の前に置くか後ろに置くか | **後ろに置いた**(台帳が閉じている回は、版の検査より先に止まる) | 逐語は順序に触れていない。**前に置くと `tests/test_audit_gates_wired.py::test_o3c_reaction_judge_gate_actually_stops_the_write`(= 台帳の関門が呼ばれていることを止まる操作で測る試験)が、版の関門で止まるようになり、何を測っているか分からなくなる。**どちらも 1 ファイルも書かないので、止まる強さは変わらない |
| 4 | 決定 2 の「切り値が出ない」とき(群が空 / NaN) | **「D1 の上側の切り値が出ない(群が空、または NaN)ので、D1 が負の群と一致するかを確かめられない」を射程として書く** | 逐語は「切り値が 0 以上なら…」の分岐しか書いていない。**切り値が出ない分岐が逐語に無い。**黙って何も書かない形にすると射程が消えるので、1 行書く方を選んだ |
| 5 | 決定 3 で `git status` が読めないとき | **「不明」として扱い、関門 (i) は「[止め]」にする** | 逐語は「汚れていれば「[止め]」」。**読めないときが逐語に無い。**「読めない = 汚れていないことを確かめられない」なので、**開封を止める側**を選んだ(**A-18 の「安全側 = 棄却側」ではなく、開封を止める側である**) |

---

## 5. 触っていないもの(委任の禁止事項)

- `scripts/_research_audit_gate.py` — **1 行も触っていない。**
- `docs/AUDITOR/HOOK_MANIFEST.sha256` — **1 行も触っていない。**`regen_hook_manifest.sh` も走らせていない。
- `.claude/` — **1 行も触っていない。**
- `CLAUDE.md` — **1 行も触っていない**(決定 13)。
- `docs/AUDITOR/ACTION_LOG.md` — **1 行も触っていない。**
- `--mode full` — **1 本も走らせていない。**
- `backtest_data/o3c_reaction_20260918_full/` — **作っていない**(**実測**: `ls` が
  `No such file or directory`)。
- コミット・押し出し — **委任先は 1 度も実行していない。**

**変えたファイル(**実測**: `git status --porcelain`)**:

```
 M backtest_data/o3c_reaction_20260918_sample/gap180_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap180_w8/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap30_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap30_w8/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap60_w24/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap60_w24/summary.json
 M backtest_data/o3c_reaction_20260918_sample/gap60_w8/MD5SUMS
 M backtest_data/o3c_reaction_20260918_sample/gap60_w8/summary.json
 M docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json
 M docs/DATA.md
 M docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md
 M scripts/o3c_reaction.py
 M scripts/o3c_reaction_judge.py
 M tests/test_o3c_reaction.py
 M tests/test_o3c_reaction_judge.py
```

(**本書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r9.md` は新規なので上の一覧には出ない。**
`docs/AUDITOR/TRACE/2026-09-18_fa7bf0d4.json` は会話の記録で、委任先が書いたものではない。)
