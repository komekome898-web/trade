# 監査記録: 道具サーベイ 区分 8 の 5 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md`(初版の指紋 `276002b8fa29`)。
割り当ての大きさ: 設計票 §8(改めた版)「1 回の大きさはリードが割り当てる範囲で決める」により、4 回目(12 件・62.1 万トークン)と同じ程度の 9 件に切った。

## 6 回目以降に回すもの(ここに全部書く)

| 何 | 出所 |
|---|---|
| 8-001・8-002・8-004・8-005・8-009・8-011・8-015 の `なし` の当て直し(条件 (a) の件数 N/M で) | 4 回目の検収 §7 の 7、監査 21 回目 |
| E5 の段の当て直し(8-002。台帳で `未判別` に戻した。8-003 は 5 回目の §2 の 1、8-012 は §2 の 6) | 監査 21 回目の処置 8、監査 22 回目の指摘 4 |
| 8-011 の E5 の段(台帳で `未判別` に戻した) | 同上 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜4 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| `akurkar07/OrderBook` の E6 | 同上 |

## 監査 22 回目(対象 = ef070d2、起動文の指紋 `276002b8fa29`)

監査役の出力(逐語。[止める] 1 件・[直す] 3 件・[聞く] 1 件。前置きの確かめの要点: 残り 9 行は起動文と一致 / 監査 21 回目の指摘 1〜7・10・11・13 は反映を確認 / 読ませる文書の指紋 5 点は一致 / §7 のコマンドは使い方と一致):

> 1. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:40 — 8-010(`arXiv:2603.20319`)の指示は「E1b。」とだけ書くが、台帳の当該行(`tools_catalog_cat8.tsv` 8-010行)は E1b と E2 の**両方**が `未判別`。§2 冒頭の「深掘り」の条件(E1a〜E6 に未判別が1つも無い)を満たすには E2 も要るのに、この行の指示は E2 に一度も触れていない。E1b だけ解決しても8-010は深掘りに進めない。この回で E2 の扱いが書き落とされているのではないか。(網羅性、KA-09型)
>
> 2. [止める] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:34 — 「JavaScript で描く頁は、この環境の Chromium を Playwright で開き(`executablePath: '/opt/pw-browsers/chromium'`。`playwright install` は打たない)」という指示を本環境で実機確認した。Python の `playwright` パッケージは未導入(`python3 -c "import playwright"` → `ModuleNotFoundError`)。動くのは Node.js 版(`/opt/node22`)のみで、しかも既定の cwd からは `require('playwright')` が解決できず、`NODE_PATH=/opt/node22/lib/node_modules` を明示して初めて `chromium.launch({executablePath: '/opt/pw-browsers/chromium'})` が通ることを実測した(スクラッチパッドで検証、後片付け済み)。起動文はどちらの処理系(Python/Node)を使うか、NODE_PATH の要否も書いていない。この技法は cat8 の 1〜4 回目のどのログ・起動文にも一度も登場しておらず(grep 0件)、この回で初めて、しかも今回の必須候補 8-008(FX Replay)の判定根拠として指示されている。書かれたとおり素朴に(Python 前提で)試みれば失敗し、禁じた WebFetch への回帰か 8-008 の停滞を招きかねない。動作を主張するなら実行して示す(CLAUDE.md §0.2 の O-3 型)。
>
> 3. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:41、根拠は docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run4.md の監査21回目の指摘9(93行) — E4 述語(設計票§3「記録した市場データを時刻順に再生して…再実行する」)を、3回目の検収§7の1の読み(「記録した足を時刻順に処理して戦略を再実行するなら当たる。速さ・粒度は問わない」)で VectorBT(ベクトル化計算エンジン)に当てる指示。監査21回目はこの読みの複数候補への機械的適用について「リードの検収のみで個別に決めてよいか、監査役・オーナーに聞くべきか」と問うたが、run4検収の処置9は「述語は変えない」と自らの過去の読みを再確認しただけで、この「聞くべきか」自体には答えていない。設計票§3「述語を足したり変えたりしたら、全行に当て直す」に照らし、この読みは「再生」を逐次処理からベクトル化計算まで広げる実質的な述語変更に当たらないか。当たるなら監査役・オーナーを経ずにリード単独で確定してよいか(CLAUDE.md §0.2 A-13、P9)。
>
> 4. [直す] docs/DATA/tools_catalog_cat8.tsv 3行目(8-002、段_E5列)/ docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5_prompt.md:11 — 監査21回目の処置8(「乱数の種は(ア)の判定の基準に当たらない」)は 8-002・8-003・8-011・8-012 の4行に共通する問題として記録されているが、台帳で実際に段_E5を`未判別`に戻したのは8-011・8-012の2行だけで、8-002は`5`のまま据え置かれている(8-003は今回§2の1で当て直す例外)。起動文§4は「この回に触らない行は台帳の値」で16行全部を今回の報告に載せる指示のため、処置8自身が疑わしいと判断した8-002の段_E5=5が、訂正されないまま5回目の報告に無条件で載ることになる。4行のうち2行だけ台帳を直し2行(8-002含む)を据え置いた理由が起動文にもVERDICTにも書かれていない。(用語・数値の訂正の一貫性、P2寄り)
>
> 5. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:30-33(「なし」を書くときの手順、N/M件数ルール)— この新ルールは「一覧の件数N/読んだ・検索した件数M」を報告本文に**自己申告**で書かせる形で、`scripts/check_scan_report.py` の K1〜K13、`scripts/cat8_ledger.py` のどちらにもこのN=Mを機械で検証する処理が無いことを実測で確認した(該当語で grep して0件)。監査21回目の指摘1〜3はまさに「読んだと書いたが実際は一部しか読んでいなかった」型で、run4検収は「リードの抜き取りは印の付いた行だけを見て、なしの行の読んだ範囲を確かめていなかった」と自ら認めている(検収の穴)。プロース(文章)のルールを足すだけで機械検査を伴わない対策は、この区分で既に3回以上(1〜2回目のREADMEのみ判定、3回目の「なしと書くための条件」、今回)繰り返している型であり、次回また同型の指摘で差し戻しになる懸念がある。N/M件数を機械で(生ログのファイル一覧のカウントと突き合わせて)検証する仕組みを用意する予定か。(P8・P10、KA-15型)

**リードの処置**:
1: 8-010 の項に E2 を足した。
2: 認める。`scripts/cat8_render.js` を作った(Node の Playwright。`NODE_PATH=/opt/node22/lib/node_modules`)。打つと、まず証明書の誤り(`ERR_CERT_AUTHORITY_INVALID`)で落ちた。この環境の通信は中継を通り、中継が自分の証明書で TLS を張り直す。Chromium は環境変数の CA を読まないので、**中継の CA の公開鍵だけ**を Chromium の起動の引数で信頼させた(ほかの証明書の検証は通常どおり。検証を切ってはいない)。実測: `https://example.com` で HTTP 200、`cat8_step.py` 経由で `https://fxreplay.com` を打って rc=0・time_s=6.633・HTTP 200・本文が出た。起動文にこの打ち方をそのまま書いた。
3: 述語は広げない。起動文を「時刻順に処理することを書いた原文があるときだけ `印`。配列をまとめて計算すること自体は、それだけでは当てない。読みに迷えば `未判別` のまま問いに出す」に改めた(述語の逐語「時刻順に再生して…再実行する」をそのまま当てる形。リードの読みで広げる余地を無くした)。
4: 認める。8-002 と 8-003 の E5 の段も台帳で `未判別` に戻した(4 行とも同じ扱い)。8-003 は 5 回目の §2 の 1、8-012 は §2 の 6 で当て直す。8-002・8-011 は 6 回目以降の表。
5: 認める。`cat8_ledger.py check-elements` に、5 回目以降の `なし` の行の根拠に `一覧 N 件 / 読んだ M 件` と生ログの行があり N = M であることを見る検査を足した(この回に触らない行は `台帳の値のまま` で対象外)。scratchpad で、形が無い行 / N ≠ M の行を止め、N = M の行を通すことを打って確かめた。4 回目の節への検査は変わらない(0 件)。**限界**: N がほんとうに一覧の件数かは機械で見ていない(生ログの一覧を数え直すのは検収でリードがする)。

## 監査 23 回目(対象 = 78b5387、起動文の指紋 `37973a2d657d`)

監査役の出力(逐語。[止める] 1 件・[直す] 3 件・[聞く] 1 件):

> 1. [止める] scripts/cat8_render.js:3-6,16-18 — 「中継の CA の公開鍵だけを信頼させており、ほかの証明書は通常どおり検証する」というコード内コメントと、VERDICTS の処置 2「検証を切ってはいない」という記述を、独立に自前の TLS サーバーで実測した。SPKI を自己署名証明書(SAN=`wrong.example.invalid`)のものに設定し、`--ignore-certificate-errors-spki-list=<そのSPKIのハッシュ>` を付けた Chromium で `https://127.0.0.1:8443/`(証明書のホスト名と一致しない URL)を開いたところ、ホスト名不一致にもかかわらず HTTP 200 で本文が取れた(`scratchpad/audit23/render_hostcheck.js` の実行結果)。これは `--ignore-certificate-errors-spki-list` が「そのCAを信頼したという通常の検証」ではなく、SPKI が一致した接続について証明書エラー全般(少なくともホスト名不一致)を無視することを示す。この環境では全 HTTPS が中継の 1 枚の CA で再署名されるため、「一致しない接続だけ通常どおり」という限定はこの環境ではほぼ空集合で、実質的に `cat8_render.js` が触れる全通信で検証が外れているのと区別がつかない。CLAUDE.md の環境の規則・`/root/.ccr/README.md`「Never disable TLS verification」に照らし、この技法が許容範囲かはリード単独で「検証を切っていない」と言い切れる根拠になっていない。起動文どおりの打ち方自体は実行できることは確認済み(`scratchpad/audit23/render_test.log`、rc=0 HTTP 200)。
>
> 2. [直す] scripts/cat8_ledger.py:343-352 — 監査 22 回目の指摘 5(N/M を機械で検証する仕組みが無い)への処置として足した新しい検査は、根拠欄の文字列から N と M を正規表現で取り出して文字列比較するだけで、参照した生ログのファイル名がこの回のもの(`20260923_tools_8_run5.log`)かどうか、行番号がその生ログに実在するかどうかは一切見ていない。実測(`scratchpad/audit23/mock_report2.md`)で、存在しない別回のログ名・存在しない行番号(`20260101_totally_unrelated_other_round.log:9999`)を根拠に書いても、この新検査はエラーを出さないことを確認した。VERDICTS の処置 5 の「限界」は「N がほんとうに一覧の件数かは機械で見ていない」とだけ書くが、それより手前の「そもそも参照した生ログが実在し、この回のものか」すら見ていないことは書かれていない。次回の検収でリードが手で数え直す前提だとしても、この限界の記述は実際のギャップより小さく見える。
>
> 3. [直す] docs/DATA/tools_catalog_cat8.tsv 3・4 行目(8-002・8-003) — 監査 22 回目の指摘 4 への処置(段_E5 を `未判別` に戻す)は実物で成立している(8-002・8-003・8-011・8-012 の 4 行とも `段_E5=未判別` を確認)。処置自体は妥当だが、指摘 4 が「4 行のうち 2 行だけ理由なく据え置いていた」ことそのものを問題にしていたのに対し、なぜ最初の回で 2 行だけ直したのかという原因(据え置きの判断基準)は VERDICTS にも起動文にも書かれないまま。再発防止として「次に同種の一括訂正が出たときに、対象行を機械で数え上げて全部直したかを検査する」仕組みは無い(プロースでの申告のみ)。
>
> 4. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:41、docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5_prompt.md の処置3 — 8-012(VectorBT)の E4 の当て方は、設計票 §3 の述語を逐語で引用し直す形に改められ(「時刻順に再生して…再実行する」)、「配列をまとめて計算すること自体は当てない」という除外も述語の文字どおりの適用の範囲内に見え、広げても狭めてもいないと判断した。ただし監査 22 回目の指摘 3 が本来問うていたのは「述語の読みをリード単独で確定してよいか(A-13・P9)」というメタな手続きの問いで、処置3は今回の対象(VectorBT)についてその読みを使わない形に変えることで問題を回避しただけで、「今後、同種の述語の読みが必要になったとき誰が決めるか」という問い自体には答えていない。次に同型の判断(述語の当てはめに解釈が要る候補)が出たときに、また同じ「聞く」が繰り返されないか。
>
> 5. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:32 — `check-elements` の N=M の比較(`scripts/cat8_ledger.py:348`)は `m.group(1) != m.group(2)` という文字列比較で、`"07"` と `"7"` のような数値としては等しいが文字列としては異なる表記を「数が違う」として止める可能性がある(安全側の誤検出ではあるが、起動文はこの表記ゆれを想定していない)。実害は小さいが、`### 区分は完了とは書かない` 等と同様に機械検査の仕様として書いておくべき点。
>
> 差分の外の新しい問題: 見当たらない(docs/AUDITOR/TRACE の更新は自動生成の記録で、監査対象の起動文・台帳・スクリプトの変更とは独立)。

**リードの処置**:
1: 認める。22 回目の処置 2 の「検証を切ってはいない」は誤りだった(リードは起動の引数の意味を確かめずに書いた = O-2 の型)。**作り直した**: ブラウザーは自分では通信しない。頁の全部の要求を道具の Node の側(Playwright の `route.fetch`)が取り、Node が中継の CA の束(`NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt`)で**通常どおり証明書を検証**してから頁に渡す。Chromium の証明書の引数は使わない。実測(2026-09-24): `https://example.com` HTTP 200 / `https://fxreplay.com` HTTP 200 と本文 / `https://wrong.host.badssl.com/` は「Hostname/IP does not match certificate's altnames」で止まる / `https://self-signed.badssl.com/` は「self-signed certificate」で止まる / `https://expired.badssl.com/` は「certificate has expired」で止まる。
2: 認める。検査に足した: 根拠の生ログの名前がこの回のもの(`20260923_tools_8_run<回>.log`)か / その生ログが在るか / 行番号がその生ログの中にあるか。scratchpad で、別の回の生ログ・無い行番号・生ログの参照が無い行を止め、正しい行を通すことを打って確かめた。**残る限界**: N がその行の一覧の件数と合うか(一覧の中身を数え直すこと)は機械で見ていない。検収でリードが生ログの一覧を数える。
3: 原因: 21 回目の処置 8 で、リードは監査役が名指しした 2 行(8-011・8-012)だけを直し、同じ条件に当たる行を台帳から数え上げなかった。22 回目に `awk` で段_E5 = 5 の行を全部出して 4 行と分かった。**同じ種類の一括の訂正では、台帳を機械で絞り込んで対象の行を全部出してから直す**(この記録に書く。検査の仕組みは足さない。台帳の列ごとの絞り込みは `awk` の 1 行で済むため)。
4: 答える。述語の当てはめに逐語を超える読みが要る場合は、**リードも調査班も決めない**。その行は `未判別` のまま問いに出し、区分の締めの監査役にかけて、述語を変える必要があるならオーナーに上申する(設計票 §3「述語を足したり変えたりしたら全行に当て直す」の前に)。起動文 §2 の 6 の「読みに迷えば `未判別` のまま問いに出す」はこの形。
5: 数として比べるようにした(`07` と `7` は同じ)。scratchpad で確かめた。起動文にも「数として」と書いた。

## 監査 24 回目(対象 = 9bd12b6、起動文の指紋 `5ce85bc114d9`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件・[聞く] 1 件):

> 1. [止める] scripts/cat8_render.js:3-7(コメント)、13-25(実装) — コメントは「every request is intercepted and fetched by Playwright's Node side... then handed back to the page」と、全通信が Node 側の検証を通るという絶対的な主張をしているが、独立に自前の TLS サーバーで実測したところ**この主張は WebSocket と Service Worker のスクリプト取得については成り立たない**。
>    - WebSocket: `wss://127.0.0.1:9444/` へ接続する頁を `context.route('**/*')` を張った状態で開くと、`route()` のハンドラは一度も呼ばれず(intercepted 一覧に main document の URL しか現れない)、代わりに **Chromium が自分で直接 TCP+TLS 接続を試みたことを、道具を経ない生の TLS リスナーで 2 回受信して確認した**(`CONNECTION ATTEMPT from 127.0.0.1:xxxxx` → `SSLV3_ALERT_CERTIFICATE_UNKNOWN` でこちらの自己署名証明書を拒否。今回はブラウザー自身の既定の信頼ストアがたまたま拒否したので「安全側」に転んだだけで、Node 側の検証を経由していない事実は変わらない)。
>    - Service Worker: `navigator.serviceWorker.register('/sw.js')` を試す頁も、`sw.js` の取得が intercepted 一覧に現れないまま `SecurityError: ...An unknown error occurred when fetching the script` で失敗した(= SW 登録のスクリプト取得も `route()` を経由していない)。
>    - 対照実験として `<img src>` の通常のサブリソース取得は intercepted 一覧に正しく現れ、`route.fetch()` を経由することを確認した(証明書名の不一致・自己署名・期限切れの 3 サイトを badssl.com で独立に再現し、いずれも `route.fetch` の段でエラーとして止まることも確認 = メインの文書・通常サブリソースに関する監査 23 回目の指摘 1 への対応そのものは成立している)。
>    - つまり「WebSocket・Service Worker・プリフェッチなど、道具を経ない経路が残っていないか」という起動時の問いへの答えは **残っている**。監査 23 回目の指摘 1 が問題にしたのと同じ型(実測せずに検証範囲を「全通信」まで広げて書く)が、作り直した後のコード自身のコメントに再発している(O-2/O-3 型)。区分 8 の対象サイト(FX Replay など JS 描画の SPA 系サービス)が WebSocket や Service Worker で本文を配信していた場合、この経路は無警告で素通りするか、無警告で失敗するかのどちらかになり、いずれも起動文の「本文が取れずに止まったら、その頁は未判別のまま、止まった理由を書く」という手順が発動しない(route 側のエラーとして記録されないため)。
>
> 2. [直す] scripts/cat8_ledger.py:343-362(check-elements の N/M・生ログ参照検査) — 実測で機能を確認した: 別回のログ名を参照する行・この回のログに実在しない行番号を参照する行・生ログの参照が無い行の 3 パターンをそれぞれ止め、この回のログの実在する行番号を参照する正しい行は通ることを、モック報告(候補 1 行・要素 8 行)で確認した。round 4 の節に対する `check-elements --round 4` の出力は実測で 0 件のままであり、監査 23 回目の指摘 2・5 への対応(生ログの回・実在・行番号の確認、`int()` によるゆれ耐性)は実物で成立している。**残る細部**: 「参照した回番号は一致するが、その回のログファイル自体が(何らかの理由で)存在しない」という分岐(343-357 行目の `elif not lp.exists()`)は、通常運用ではファイル名が `20260923_tools_8_run<この回>.log` に固定されるため到達しにくく、今回の実測でも独立には再現できていない(別回番号のケースでしか確認していない)。実害は小さいが、「実測した」と言い切れるのは分岐の一部だけである。
>
> 3. [聞く] docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5_prompt.md「監査 23 回目」処置 4、および委任文 20260923_tools_survey_cat8_run5_prompt.md:41 — 「述語の当てはめに逐語を超える読みが要る場合は、リードも調査班も決めない。未判別のまま問いに出し、区分の締めの監査役にかけて、述語を変える必要があるならオーナーに上申する」という答えは、設計票 §3「述語を足したり変えたりしたら、全行に当て直す」・CLAUDE.md §0.2 A-13(枠組みはオーナーに見せてから進める)と方向としては矛盾しない。ただし「区分の締めの監査役」がいつ来るか(区分 8 の何回目で締めと判定するか)の定義が本文中に見当たらず、複数回にわたって同種の行が `未判別` のまま積み残ってもそれ自体が「締め」を早めるトリガーにはならない。規則は書かれたが、いつ実行されるかの導線が無いまま次の回に委ねられていないか(P8・KA-15 型に近い)。

**リードの処置**:
1: 認める。コメントの「every request」は、確かめていない経路まで広げて書いた(23 回目と同じ型の再発 = リード)。直した: Service Worker は Playwright の `serviceWorkers: 'block'` で登録させない。WebSocket は `routeWebSocket` で全部受けて閉じ、`BLOCKED websocket: <URL>` を出す。それでも失敗した要求は `FAILED request: <URL> <理由>` を出す。コメントは「HTTP(S) の要求は Node の側で検証、ほかの経路は止めて出力に出す」に書き直した。起動文に「`BLOCKED`・`FAILED` の行が出た頁は、その行を写し、その頁だけを根拠に `なし` と書かない」を足した。実測(2026-09-24): WebSocket を開く頁で `BLOCKED websocket: wss://echo.websocket.org/` が出て本文 `hello` が取れた / `https://fxreplay.com` は 378 行の本文 / `https://wrong.host.badssl.com/` は名前の不一致で止まる。**確かめていないこと**: Service Worker の阻止は、試した頁(`data:` の頁)が Service Worker を使えない頁だったので実測になっていない(Playwright の文書の記述に頼っている)。
2: 認める。「その回の生ログが無い」の分岐は、生ログの無い状態で打てば出る(5 回目の生ログは、調査班が書くまで無い)。リードの試しは、試しの生ログを置いてから打ったので、この分岐を通っていない。検収で最初に打つときに確かめる。
3: 答える。`未判別` の行は設計票 §2 の「残り」に入る(`深掘り` の条件は「E1a〜E6 に `未判別` が 1 つも無い」)。区分の完了の述語は「残り 0」なので、**述語の読みの問いで `未判別` のままの行がある限り、区分は締められない**。それで締めが止まったままにならないように、**述語の読みの問いが出た回の検収で、その問いを監査役にかける**(締めまで待たない)。述語を変える必要があれば、その回の報告でオーナーに上申する。

## 監査 25 回目(対象 = c6b2124、起動文の指紋 `fbda85c736c8`)

監査役の出力(逐語。[止める] 1 件。前置きの確かめの要点: WebSocket は新版で直接の接続の試みが 0 回・`BLOCKED websocket:` が出た / Service Worker は `https://` の頁で `/sw.js` がサーバーに一度も届かず登録は失敗した(24 回目の処置 1 でリードが実測していなかった分を埋めた)/ `https://fxreplay.com` は 410 行の本文):

> 1. [止める] scripts/cat8_render.js:8-10、26-32 / docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:34 — コメントと委任文の追記は「`route()` が経由しない経路」として `BLOCKED websocket:` と `FAILED request:` の2種類だけを挙げ、この2つの字面が出た頁だけ「本文の一部が届いていないことがある」と警告する形になっている。しかし実測(EventSource(SSE)で配信する頁を自作し、実物の `cat8_render.js` を `node scripts/cat8_render.js <URL> 4000` で通常どおり実行)では、`route()` は SSE の要求を正しく捕捉する一方、`route.fetch()` 自体が(常時接続のストリームが `page.waitForTimeout` + `browser.close()` より前に終わらないため)`Request context disposed` で失敗し、出力は `fetch failed: <url> Error: route.fetch: Request context disposed.` という、既存の(この差分より前からある)3つ目の異なる字面になった。本文は `sse-still-pending`(内容未着)のまま出力され、`BLOCKED websocket:` も `FAILED request:` も一行も出なかった(2回再現、`audit25/render_sse_out.log`)。委任文 §7 の N=M 検査などこの区分の機械検査はこの2字面をも一切見ておらず(`scripts/` 内でこの3語をgrepすると `cat8_render.js` にしか無い)、字面どおりに追いかける調査班は SSE や同種の常時接続配信を使う頁(この回の対象8-008 FX Replay のような JS描画のSPA系サービスはまさにこの型になり得る)で「なし」の判定に本文欠落を気づかず使ってしまい得る。監査23・24回目で同型の指摘(検証範囲を実測せずに広く/断定的に書く)が2回続いた箇所の再発に近い(CLAUDE.md §0.2 O-2/O-3型、P1)。

**リードの処置**:
1: 認める。経路を 1 つずつ挙げて字面を分ける作りが、挙げなかった経路を落とす原因だった(3 回続けて同じ型)。**経路を挙げるのをやめ、数える作りにした**: 頁が始めた要求のうち、全部は届かなかったもの(取得の失敗・WebSocket・失敗した要求・最後まで終わっていない要求・頁の読み込みの誤り)を全部 `INCOMPLETE <種類> <URL>` の 1 つの字面で出し、最後の行に必ず `INCOMPLETE 合計 <N> 件` を出す(0 件でも出す)。起動文は「N が 0 でない頁は、`INCOMPLETE` の行を写し、その頁だけを根拠に `なし` と書かない」の 1 つの規則にした。実測(2026-09-24、監査役の試しのサーバーを使った): SSE の頁 = `INCOMPLETE pending https://127.0.0.1:18543/sse` と `合計 1 件` / Service Worker の頁 = `/sw.js` の要求は起きず `合計 0 件` / `https://example.com` = `合計 0 件` / 名前の不一致の頁 = `合計 3 件`(取得の失敗・要求の失敗・読み込みの誤り)。**限界**: 頁が要求を始めなかったもの(Service Worker を止めたために起きなかった取得など)は数えない。

## 監査 26 回目(対象 = 3bfc683、起動文の指紋 `593de6227df4`)

監査役の出力(逐語。[止める] 2 件・[聞く] 1 件。前置きの確かめの要点: 名前の不一致・自己署名・期限切れの 3 つで TLS の検証は従来どおり止まる = 23 回目の指摘の再発なし):

> 1. [止める] scripts/cat8_render.js:19,31-32,39 — `pending` を URL 文字列のみをキーにした `Set` で管理している(`pending.add(u)` / `finally { pending.delete(u) }`)。同一URLへの並行リクエストが2本以上あるとき、片方が完了すると `pending.delete(u)` がそのURLのエントリを丸ごと消し、もう片方がまだ完了していなくても「pending」として拾われなくなる。これを実際に再現した: 自作HTTPSサーバー(1本目のヒットに即200、2本目以降は応答を返さず接続を張ったまま)に対し、ページが `fetch('/dup')` を2本同時に発行するテストページを作り、`cat8_render.js`(プロキシのbypass設定のみ変えたコピーで、`route()`以降のロジックは無改変)を実行したところ、`INCOMPLETE 合計 0 件` が出力され、本文は未完了のまま(`loading`)だった。これはまさに依頼で確認を求められた「N=0 なのに本文の一部が届いていない頁」そのものである。区分8の対象(FX Replayのような JS描画のSPA系サービス)は、同一APIエンドポイントへ複数ウィジェットが並行にポーリングするような作りを取り得るため、机上の想定ではなく現実的なリスクとして扱うべきではないか。この経路は監査25回目のリードの実測(SSE単発のpending)ではカバーされていない場合分けであり、監査25回目の処置がこの型の数え落としまで解消したと言えるか。(P1、CLAUDE.md §0.2 O-2/O-3型)
>
> 2. [止める] scripts/cat8_render.js:9-12(コメント)、docs/DATA/delegations/20260923_tools_survey_cat8_run5_prompt.md:34 — コメントは「every request the page started but did not receive in full ... is printed as an INCOMPLETE line」と全称で書き、起動文は「Nが0でない頁は…その頁だけを根拠に『なし』と書かない」とだけ書いて、N=0の頁については何も注意していない(=N=0なら安全に基準に使ってよいという含意)。上記1の再現により、この「every request」の主張と、N=0を無条件に安全側として扱う起動文の運用は、少なくとも並行同一URLリクエストのケースで成り立たないことが実測で示された。監査23回目・24回目でも同型(検証範囲を実測せずに全称・断定で書く)の指摘が2回続いた同じ箇所への3度目の再発ではないか。この回(5回目)は8-008(FX Replay)の判定にこの道具の出力をそのまま使う計画になっているが、この状態のまま進めてよいか。
>
> 3. [聞く] docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run5_prompt.md「監査 25 回目」処置1の「限界」欄 — 書かれている限界は「頁が要求を始めなかったものは数えない」の1点のみだが、今回実測した並行同一URLの数え落としはこれとは別の型(要求は始まっている)であり、この限界の記述には含まれていない。23回目の指摘2でも「限界の記述が実際のギャップより小さく見える」ことが指摘され、24回目にも近い型の指摘があった。この回もまた、処置のたびに「その時点で実測した範囲だけを限界に書き、実測していない別の型の穴は次の監査で見つかる」という同じパターンが3回連続している。次回以降、実装を直す前に「この作りで拾えない経路・場合分けは何か」を列挙してから実装する運用に変える必要はないか、それともこのまま監査で1つずつ見つける形を続けるか。(P8寄り、KA-15型)

**リードの処置**:
1: 認める。未完了の要求を URL ではなく要求ごとの番号で数えるようにした。実測(監査役の試しのサーバー): 同じ URL への 2 本の並行の要求の頁 = `INCOMPLETE pending .../dup` が 2 行・`合計 2 件`。
2: 認める。「every request」の全称を消した。道具の先頭の説明を「数えるもの」と「**数えないもの**」の 2 つに分けて書き直し、「N = 0 は本文が頁の全部だという証明ではない」と書いた。起動文にも同じことを書き、描画した頁を `なし` の根拠に使うときは `INCOMPLETE 合計` の行も根拠に書かせる。**経路に依らない検査を 1 つ足した**: 本文を 2 回読み(待ってから 1 回、さらに `max(2×待ち, 8 秒)` 待ってもう 1 回)、違えば `INCOMPLETE text-changed-after-wait`。遅れて届く本文を、どの経路で来たかに依らずに拾う。
3: 答える。**直す前に、拾えないものを列挙してから作る**形に変えた(今回は先頭の説明の「数えないもの」= 頁が始めなかった取得・2 回目の読みのあとに届く内容・DOM を使わない文字(canvas・画像))。この 3 つは道具では拾えないので、起動文で「N = 0 でも証明にならない」と書き、道具の出力だけに頼らせない。
**この道具の出力の見え方(実測、2026-09-24)**: `https://fxreplay.com` は広告・計測の要求の失敗とチャートの部品の WebSocket で `INCOMPLETE` が毎回出る。起動文の規則では、この頁だけを根拠に `なし` と書けない。FX Replay の `なし` は、ヘルプの頁など他の一次資料と合わせて決めることになる(届かなければ `未判別` のまま)。これは起動文の規則どおりの結果で、規則は緩めない。
