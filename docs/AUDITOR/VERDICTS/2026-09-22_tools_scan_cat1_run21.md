# 区分 1 の 21 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 21 回目の実行」節(261 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run21.log`(382 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲(機械)

`git diff --numstat` = **269 行追加 / 7 行削除**。削除 7 行を全部出した:

```
-55. `backtesting.py` — 未着手。出典は 39 番の一覧。          ← 13 回目の一覧
-58. `nautilus_trader` — 未着手。出典は 39 番の一覧。          ← 13 回目の一覧
-75. `Freqtrade` — 未着手。出典は 39 番の一覧。                ← 13 回目の一覧
-55. `backtesting.py` — 説明は「a Python framework …」。…未着手。   ← 14 回目の一覧
-58. `nautilus_trader` — 説明は「A high-performance …」。…未着手。  ← 14 回目の一覧
-75. `Freqtrade` — 説明は「a free and open source …」。…未着手。    ← 14 回目の一覧
-`区分1-ベクトル化` は 44・73・74 の 3 件(数は変わらないが…)。      ← 19 回目の集計
```

差分の位置は 4347・4350・4367・4638・4641・4658・5929 と末尾への追記だけ。**1・2 回目の節(8〜944 行)に 1 つも掛かっていない。判定: 合。**

## 2. 受け入れ検査(機械)

生ログ 20 本を全部渡して 13 本すべて 0 件。**本数も `ls … | wc -l` の出力つきで、実物と合っている。判定: 合。**

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| 候補 61 に**自分の注文の列を追う実装が無い** | `barter-execution/src/exchange/mock/account.rs` を取得(4,676 バイト、調査班の記録と同値)。`queue` の当たり **0 件** | **当たり** |
| 候補 61 が**足とティックの両方を受け取る** | `barter-data/src/event.rs` 124 行 `pub enum DataKind { Trade(PublicTrade), OrderBookL1(OrderBookL1), OrderBook(OrderBookEvent), Candle(Candle), Liquidation(Liquidation) }` | **当たり**(ただし下の「直し 1 件」) |
| 121 `qstrader` の到達の記録が 1・2 回目に在る | 報告書 611 行に「`equity curve rows=261, first={'Equity': 1000000.0}, last={'Equity': 698485.43}`」「指値注文・板・待ち行列という概念がソースのどこにも存在しない」 | **当たり** |
| 122 `pyalgotrade` の廃止と後継 | 報告書 629 行に「This repository was archived by the owner on Nov 13, 2023」「You may be interested in taking a look at **Basana**」 | **当たり。原文の綴り誤り `mantained` をそのまま残し、その旨も書いてある。扱いが正しい** |

**直し 1 件(差し戻しではない)**: 候補 61 の `DataKind` の逐語で、5 つのうち 3 つの内側の型が落ちている
(報告: `OrderBookL1`・`OrderBook`・`Liquidation` / 原典: `OrderBookL1(OrderBookL1)`・`OrderBook(OrderBookEvent)`・`Liquidation(Liquidation)`)。
**意味は変わらないが逐語ではない。**22 回目で原典どおりに直すこと。

## 4. 調査班が挙げた 6 件の判断 — リードの回答

**(1) 14 回目の節の 3 行も直したこと → 許しの範囲内。黙ってやらずに出したのも正しい。**
当方の指定が「一本化する」であって行を数え上げていなかった。**同じ 3 候補が 2 つの一覧に「未着手」と書かれていた以上、片方だけ直せば一本化は済まない。**
**これはこちらの指定の漏れである**(19 回目の集計の件と同じ型で 2 回続いた)。
→ **リードの手順を直す: 訂正を指定するときは、例示(「この行」)ではなく条件(「55・58・75 を未着手と書いている行すべて」)で書く。**

**(2) 候補 68 の `区分1-足` → 残す。ただし根拠は文書ではなく実装に置くこと。規則として残す。**

> **印は、検証の機関が実際に受け取る事象の型で決める。公式の文書が供給として挙げているだけでは付けない。**

候補 68 は実装に `BarEvent`(OHLCV、`interval = 86400`)が在るので、実装の側で足を受け取っている。**印は立つ。**
節の根拠の欄を、文書の逐語ではなく `BarEvent` の実装の逐語が先に来る形にすること。

**(3) 候補 61 の `区分1-市場影響と約定の模型` → 22 回目で読むこと。**
`mock/account.rs` は模擬の口座で、**約定をどう作るかがそこに在る。**列は無いと分かったので、次は「何で埋めるか」(価格だけか、量も見るか)を読む。
**未確認のまま完了に数えない。**

**(4)「6 要素ごとの残り」の数え方が 2 通りに読める → これが一番重い。数え方を決める。**

指摘は正しい。18 回目の見出しは「**状態がまだ確定していない候補の数**」だが、中身はその印を持つ候補を全部数えていた
(18 回目に印が決まった 40・46・48・56・80・85・86 が、決まった直後に「残り」に足されている)。
**つまり今まで報告されてきた「残り」は、残りの数ではなかった。**オーナーが決めた完了の形(逐語「**一次資料に到達した候補が尽きること**」)で
数える以上、この食い違いは**完了の判定そのものを壊す。**→ **次のとおり定義する。**

> **その要素の印を持つ候補について、2 つの数を併記する。**
> - **総数** = その印を持つ候補の数
> - **残り** = そのうち**一次資料に到達していない**ものの数(状態が「未着手」または「判別に一次資料が要る」のまま)
>
> **「浅い」以上は到達済みであり、残りではない。**(深掘りの有無は完了の条件ではない。オーナーの逐語は「一次資料に到達した候補が尽きること」であって、深掘りが尽きることではない。)

**22 回目で 6 要素すべてをこの定義で数え直し、総数 / 残り の 2 段で書くこと。**
**現時点で区分 1 がどこまで来ているかは、この数え直しをするまで誰も知らない**(当方も知らない。だから今は書かない = §0.2 O-2)。

**(5) 候補 123・124 に印を付けなかったこと → 124 は正しい。123 は決めること。**
- **124 `ccxt/ccxt`** は検証の機関ではなく取引所の窓口である。**区分 1 の印が付かないのが正しい。**
  ただし「印が無い」ままにせず、**`区分2 へ` と明記する**(印が空だと「未確定」と見分けが付かない)。
- **123 `carlos8f/zenbot`** は粒度が未確認のままなので、**22 回目に一次資料で決めること。**2022-02-15 にアーカイブ済みでも、原典は読める。

**(6) 候補 107 に登録の鍵 `gh:skelf-research/sigc` が出たこと → 22 回目にその登録を一次資料として読むこと。**
「場」だと思っていたものが「登録された道具」なら、**区分 1 の 6 要素で印が付きうる。**仕分けを動かすかは、中身を読んでから決める。

## 5. 11 件の洗い直しについて

**11 件ともホスト名の鍵が取れ、重複 0 件。**ホスト名すら確定しないものが 0 件だったのは、当方の想定より良い結果である。
副産物の候補 118 の正体(表題の逐語「HyperTrader | 866 AI Models & Strategies for Institutional-Grade Crypto Trading」= **記事ではなく商品の場、暗号資産を名指し**)も、
15 回目の積み残しを片付けている。**11 件を完了の数に入れるかは、(4) の数え直しの結果と一緒にリードが決める。今は決めない。**

## 6. 安全側の所見

この回に取った原典(`barter-rs` / `quanttrader`)に**作業者向けの手引きの同梱は 0 件**。通算は 5 件(44・74・120・41・58)のまま。
**原典は毎回 `ungh.cc` の files で在処を確かめてから取っており、当てずっぽうの取得 0 件・404 も 0 件。**
20 回目の検収 §4 でリードが 2 回破った手順を、調査班の側は守っている。
この回に導入・構築・最小実行・登録・鍵の発行・購入・発注は 1 件も無い(生ログで確認)。

## 7. 判定

**受け取る。**検査 13 本が 0 件、書き換えは一本化に必要な範囲ちょうど、抜き取りは 4 件とも原典に当たった。
**一覧の修復は済んだ。**(4) の数え直しと、逐語の直し 1 件を次の回に回す。**差し戻す点は無い。**

## 8. 22 回目の起動の指定

**この回で「区分 1 が今どこまで来ているか」を初めて正しく出す。**

1. **最優先: 6 要素すべてを §4(4) の定義で数え直し、総数 / 残り の 2 段で書く。**
   残り = その印を持つ候補のうち、状態が「未着手」または「判別に一次資料が要る」のもの。**「浅い」以上は到達済みで、残りではない。**
   **数え方を変えたことと、変えた理由(見出しと中身が食い違っていた)を節に書くこと。**古い回の節は書き換えない。
2. **候補 123 `carlos8f/zenbot` の粒度を一次資料で決める**(§4(5))。
3. **候補 124 に `区分2 へ` を明記する**(§4(5))。印が空のまま放置しない。
4. **候補 107 の登録 `skelf-research/sigc` を一次資料として読み、仕分けを決める**(§4(6))。
5. **候補 61 の `mock/account.rs` を読んで、約定をどう作るかを決める**(§4(3))。
6. **候補 61 の `DataKind` の逐語を原典どおりに直す**(§3 の直し 1 件)。
7. **候補 68 の `区分1-足` の根拠を、実装(`BarEvent`)の逐語が先に来る形に直す**(§4(2))。
8. 上の 1〜7 に要る範囲でのみ既存の節への書き換えを許す。**それ以外は 1 文字も触らない。**`git diff` の削除行を自分で確かめて返す。
9. **新しい検索計画は打たない。深掘りもしない。候補も増やさない。**
10. 逐語は**内側の型や記号まで原典どおり**に引く(§3)。生ログの本数は `ls … | wc -l` の出力を添える。
