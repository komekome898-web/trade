# 監査記録: 「値段の続き」の方策 + 全部逆張りの最適化の結果(`SIGNAL_VALUE_RESULT_2026-09-21.md`)

## 1 回目(2026-09-21、対象コミット 212e219)

前段(jev_check): 印 6 件(negative_claim 1、universal_claim 4、verification_claim 1)。

監査役の出力(逐語。[直す] 2 件・[聞く] 3 件・止める 0):

> 1. [直す] SIGNAL_VALUE_RESULT_2026-09-21.md:75 — 「多重性: 表の行 2,995(前段まで)+ 段 1 801 + 費用 34 + 段 2 696 …= 4,526」の「段1 801」は現物と矛盾する。`backtest_data/o3c_signal_value_20260921/stage1_firsthalf/` の各 csv を実際に `wc -l` で数え直すと合計 833(手元で実測済み)。VALUE_STAGE1_REPORT_2026-09-21.md 自身の (10-b) 節(1096行目)が「801/8,328 → 833/8,626」と明記して更新後の値を 833 としており、VALUE_STAGE2_REPORT_2026-09-21.md:54 の多重性引き継ぎも「段1 833」を採用している。RESULT だけが更新前の 801 を使っており、合計 4,526 は実測(2,995+833+34+696=4,558)と 32 ずれる。この文書内の別の数値(段1 初版の「74%」= 再点検後の値)は正しく最終版を追っているのに、この行数だけ更新が反映されていない(P-05)。
>
> 2. [直す] SIGNAL_VALUE_RESULT_2026-09-21.md:73(射程・多重性の限界一覧) — VALUE_STAGE2_REPORT_2026-09-21.md の「(5) 未決・限界」節(685〜688行目)にある N-5(合計保有時間は目標時刻ベース・同時建玉は約定時刻ベースで 2 つの時計が揃っていない)と N-6(`出口の理由` 列の意味が①の行では固定文字列で方策により意味が違う)が、RESULT の「測っていない」リストに引き継がれていない。RESULT の §1 の結論 4(「②opt は保有 1 時間あたり +50.4 bp」)はまさに露出(合計保有時間)の数値を使っており、その定義の時計が揃っていないという既知の限界が上位文書に落ちている。CLAUDE.md §5.2 の「限界(全部書く)」の趣旨(下位の記録にある限界を上位の記録で省略しない)に照らして、この 2 点が RESULT で書かれているか確認を要する。
>
> 3. [聞く] SIGNAL_VALUE_RESULT_2026-09-21.md:4 — 「**リードの読み**: 数は `cascades.csv.gz`(連鎖ごとの行)から自分で組み直し、委任先の表と一致した(2026-09-21)。」という機構主張に、実行したコマンドと出力が本文に示されていない。CLAUDE.md §0.2 B の O-3(「機構やデータの挙動を主張するときは、実行したコマンドと出力を同じ返答に出す。出せないなら『未確認』と書く」)の観点で、この一致の確認はどう検証されたか(P-01隣接)。
>
> 4. [聞く] SIGNAL_VALUE_RESULT_2026-09-21.md:82 — 「3. ②optを自前記録(2026-08-20〜)で判定する事前登録…どれもリードの案で、原文に該当語は無い。」という否定的主張(原文に該当語が無いこと)について、どの範囲(このセッションの L-342 のみか、OWNER_LOG 全体か)をどう確認したかが書かれていない。同じ文書の他の箇所(§0.1 対応表)では空欄の扱いが着手前の擦り合わせ(L-344等)を経ているのに対し、この §6「次」の 3 項目は事後に付け加えられた提案で、同種の確認プロセスを経た記録が無い(P-01 の型に近い)。
>
> 5. [聞く] CLAUDE.md §5.0 の「2 測定後・報告前」に定める `scripts/judge_gates.py`(`--unit` 必須)→ `scripts/_research_audit_gate.py` の関門が、この単位(SIGNAL_VALUE、探索段)に対して実行されたか、RESULT・段1・段2 のいずれの報告にも記載が無い。この単位が §5.0 の対象(事前登録の監査)に該当するかどうかも含め、確認を要する(P-08 隣接、規則が行動の直前に実際に呼ばれたかの検証)。

リードの処置(1 件ずつ):

1. **直した。** 段 1 の csv の行を数え直して 833(監査役と一致)、合計 4,558 に直し、初版の 801 が再点検前の値だったことを書いた。
2. **直した。** N-5(露出の 2 つの時計)と N-6(`出口の理由` の列)を §5 の限界に引き継いだ。
3. **書いた。** 検算のコマンドと出力を `docs/DATA/probes/20260921_o3c_signal_value_verify.log` に置き、冒頭に例の数値を添えた。
4. **書いた。** 確認の範囲(`OWNER_LOG` 全体 L-001〜L-352 を grep、件数)を §6 に書き、§0.1 の擦り合わせを経ていない = 着手前に見せる、と明記。
5. **書いた。** 探索段(事前登録なし・判定なし)なので §5.0 の 2 の対象外として走らせていない。前段の探索段の記録 2 本も同じ(`grep -c judge_gates` = 0)。探索段にも掛けるかはオーナーが決めることとして §5 に書いた。
