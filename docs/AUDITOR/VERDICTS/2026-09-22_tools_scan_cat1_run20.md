# 区分 1 の 20 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 20 回目の実行」節(230 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run20.log`(610 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 既存の節が書き換わっていないか(機械)

`git diff --numstat` = **232 行追加 / 2 行削除**。削除の 2 行を全部出す:

```
-| `GFT Backtest Software` | 未確認(公開の文書に記事が 1 件も無い) | 未確認 | 未確認 | **不可**(4 経路が全部塞がり、入口は登録の内側) |
-1. `lo2cin4/lo2cin4bt`(44 番) — 状態: **浅い(足の契約の原典から粒度を確定)。**印は `区分1-足` と `区分1-ベクトル化`、および `区分4 へ`。
```

**19 回目の検収 §7(2) で許した (a)(b) のちょうど 2 箇所である。**(c) は追加行なので削除を伴わない。**判定: 合。**

## 2. 受け入れ検査(機械)

生ログを全部渡して 13 本すべて 0 件、`---- 合計 0 件`。**判定: 合。**

**ただし本数の数え違いが 1 件**: 報告の文に「生ログ 20 本」とあるが、実物は **19 本**(`run2` 〜 `run20`。`run1` の生ログは存在せず、1 回目の証拠は節の中にある)。
検査は実物を渡して通っているので結果に影響は無いが、**数は書いたら合わせる**(CLAUDE.md §3)。次回は `ls … | wc -l` の出力を添えること。

## 3. 最優先だった重複の洗い出し — **これが本体である**

リードが原典(報告書そのもの)で全部引き当て直した。**調査班の主張は 4 件とも当たっている。**

| 調査班の主張 | リードが確かめたもの | 結果 |
|---|---|---|
| 現行 1〜120 の中での重複 **0 件** | 突き合わせの手順と鍵の出所 3 つが生ログに在り、鍵が取れた 101 件について照合されている | **合** |
| 旧 2 番 = 58 番 / 旧 4 番 = 75 番 / 旧 5 番 = 55 番 | 1・2 回目の節に「2. NautilusTrader」「4. freqtrade」「5. Backtesting.py」が**独自の番号で**在る(180 行・476 行の 2 つの「候補の一覧」が別の番号体系) | **合** |
| **その 3 件が現行では「未着手」のまま** | `4347:55. backtesting.py — 未着手`・`4350:58. nautilus_trader — 未着手`・`4367:75. Freqtrade — 未着手`(いずれも「出典は 39 番の一覧」) | **合** |
| 落ちた 4 件が現行の番号つき一覧に無い | 4320〜4700 行を `qstrader\|pyalgotrade\|carlos8f\|ccxt/ccxt` で数えて **0 件** | **合** |

**この発見の重さを記録する。**到達の記録が現行の番号から切れていた 3 件のうち、**候補 75 `Freqtrade` は 2 回目に導入して実行までしている**:

> 「隔離venvにfreqtrade導入(ccxt 4.5.82同梱)、`freqtrade list-exchanges -a`を実測: bitFlyer行「**missing: fetchOrder, fetchOHLCV**」(必須欠落)。`freqtrade list-exchanges`(非-a、使える79取引所)に**bitFlyerは含まれずbitbankは含まれる**」

**当方の市場そのものについての実測であり、区分 1 で最も直接に効く事実の一つが「未着手」の札の下に埋まっていた。**
候補 55 も 2 回目に「**指値の1往復に成功**(最小実行)」まで行っている。
落ちた 4 件の側にも `gbeced/pyalgotrade` の「**指値の1往復に成功**」「**2023-11-13 にアーカイブ済み、後継として Basana を公式に指名**」が在る。

**オーナーが決めた完了の形(逐語「一次資料に到達した候補が尽きること」)で数えるなら、この状態のまま完了を宣言していたら数は嘘になっていた。**
**洗い出しを最優先に置いた判断は正しかった。調査班の仕事も正確である。**

## 4. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| 候補 65 `aat` が**価格帯ごとの到着順の列**を持つ | `aat/core/order_book/price_level/price_level.py`(9,601 バイト)を取得。`def add` の下にコメント `# append order to deque`、`self._orders.append(order)` | **当たり** |
| 候補 65 の README の逐語 | 「`aat` also provides a complete limit-order book implementation, including flags like `fill-or-kill` and `all-or-nothing`, which is used to power the synthetic testing exchange.」(50 行) | **当たり** |
| 候補 90 が**比例配分**で埋める | `Oddpool/PredictionMarketBench` の README 134 行「Orders can rest in the book and receive fills when historical trades match your price. **Queue position is simulated based on displayed size and pro-rata fill allocation.**」 | **当たり** |
| **当方の参照実装が FIFO であること**(対比の当方側) | `scripts/qa/maker_fill_ref.py` を読んだ。「resting order joins the **back of the displayed queue** at insertion」「partially per **FIFO**」「Queue-ahead at insertion = the raw displayed size … minus own size」 | **当たり。対比は本物である** |

**候補 90 の発見は重い。**当方は先行注文を **FIFO** で食わせているが、候補 90 は**表示されている量に比例して配る**。
**同じ「待ち行列を模擬する」でも配分の規則が違い、当方の埋まり方の見積もりはこの選択に依存している。**
候補 104 の `queuePosition`・候補 95 の約定の列・候補 63 の自分の注文の遅延の列と合わせて、**指値の埋まり方の模型が 5 通り**になった。

**リード自身の誤りを 2 件出す**(調査班の誤りではない):
1. 候補 90 を検めるとき、生ログから**候補 41 の URL を取ってきて候補 90 に使い**、「逐語が原典に無い」と一度読んだ。**取り違えである。**
2. 候補 65 の経路を当てずっぽうで組み立てて 0 バイトを受け取った。**正しい経路は `order_book/price_level/price_level.py`(入れ子)**で、
   調査班は 404 を受けてから一覧で引き直している。**19 回目の検収で自分に課した「まず在処を確かめる」を、当の回で 2 回とも守らなかった。**
   → **当方の手順に固定する: 原典を引くときは、まず生ログの `label=` か `ungh.cc` の files で在処を確かめてから取る。**

## 5. 調査班が挙げた 7 件の判断 — リードの回答

**(1) 19 回目の節の「6 要素ごとの残り」に `区分1-ベクトル化` が 44・73・74 の 3 件と残っている → 直すこと。**
同じ 1 つの訂正 (a) の一部である。許しの箇所が足りなかったのはこちらの指定の漏れ。**21 回目で直してよい。**

**(2) 印を足したこと(52・58・62・69・65・90・41)→ よい。**19 回目 §4(2) と同じ。一次資料が支える印を全部付けるのが「確定させる」である。
リードが抜き取った 65・90 は原典に当たった。

**(3) 候補 58・41 に `区分1-板の待ち行列` を付けなかったこと → 正しい。規則として残す。**

> **板を入力として受け取ることと、待ち行列の位置を模擬することは別である。**`区分1-板の待ち行列` の印は、
> **自分の注文が列のどこに居るかを追う実装**があるものにだけ付ける。入力の口があるだけでは付けない。

18 回目の候補 63 の切り分け(遅延の列 ≠ 価格帯の列)と同じ線であり、**この線があるから 5 通りの違いが見えている。**

**(4) 候補 61・68 の粒度を `未確認` のままにしたこと → 正しい。**
README に語が無いことは実装に口が無いことではない(§0.2 O-2)。**「道具に無い」と書かなかったのが正しい。**
ただし**案 A' の完了には届いていない**ので、**21 回目で実装まで降りて決めること。**未確認のまま完了に数えない。

**(5) 落ちた 4 件を立て直すか → 立て直すこと。**
「候補を増やすな」は**新しい検索で新しい道具を足すな**という意味で、**一度調べたものを一覧に戻すのは修復であって拡大ではない。**
委任文 §3-3 の「候補の一覧から黙って落とさない」に正面から当たる。**121〜124 番として立て、1・2 回目の到達の記録を根拠として引く**(古い節は書き換えない)。

**(6) 重複した 3 件(55・58・75)を一本化するか → 一本化すること。**
現行の番号の側に**到達済みの状態と、1・2 回目の節への参照**を書く。**1・2 回目の古い節は 1 文字も触らない。**
これをしないと、案 A' の完了の数が嘘になる(§3)。

**(7) 鍵が取れなかった 11 件(40・45・46・48・49・50・107・108・109・117・118)→ 別の鍵で洗い直すこと。**
この 11 件は GitHub の登録でも PyPI の配布物でもない(場・有償の商品)。**鍵は公式の場の URL のホスト名にする。**
**「鍵が無いから重複なし」とは数えない**という調査班の扱いが正しい。**候補ごとに、どの鍵で照合したかを書く。**
完了の数に入れるかは、**洗い直した結果を見てからリードが決める**(今は決めない)。

## 6. 安全側の所見

作業者向けの手引きを同梱する登録が、この回に **2 件**(候補 41 の `AGENTS.md`、候補 58 `nautilus_trader` の `AGENTS.md` と `CLAUDE.md`)。
**通算 5 件**(44・74・120・41・58)。調査班は中身を読まず所在だけ記録しており、委任文 §6-3 のとおり。**リードも中身を読んで従わない。**
**候補 58 は区分 1 で印が 3 つ付いた有力なものなので、使う段のオーナーへの所見に必ず載せる。**
この回に導入・構築・最小実行・登録・鍵の発行・購入・発注は 1 件も無い(生ログで確認)。

## 7. 判定

**受け取る。**機械の検査 13 本が 0 件、書き換えは許した 2 箇所ちょうど、抜き取りは 4 件とも原典に当たった。
**最優先に置いた重複の洗い出しが、完了の数を壊す欠陥を 8 件(重複 4 + 落ち 4)見つけた。差し戻す点は無い。**

## 8. 21 回目の起動の指定

**この回は「一覧の修復」の回とする。**印を増やすより先に、数えられる状態にする。

1. **落ちた 4 件を 121〜124 番として立てる**(§5(5))。1・2 回目の到達の記録を根拠に引く。**古い節は書き換えない。**
2. **55・58・75 の状態を一本化する**(§5(6))。現行の番号に到達済みの状態と 1・2 回目の節への参照を書く。
3. **鍵が取れなかった 11 件を、公式の場の URL のホスト名を鍵にして洗い直す**(§5(7))。候補ごとに使った鍵を書く。
4. **候補 61・68 の粒度を実装まで降りて決める**(§5(4))。
5. **19 回目の節の「6 要素ごとの残り」から `区分1-ベクトル化` の 44 を外す**(§5(1))。
6. 上の 1〜5 に要る範囲でのみ既存の節への書き換えを許す。**それ以外は 1 文字も触らない。**`git diff` の削除行を自分で確かめて返す。
7. **新しい検索計画は打たない。深掘りもしない。**
8. 原典を引くときは**まず在処を確かめてから取る**(リードが 2 回破った)。生ログの本数は `ls … | wc -l` の出力を添える。
