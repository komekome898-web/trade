# 区分 1 の 12 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 12 回目の実行(2026-09-22)」(4016 行目以降)
生ログ: `docs/DATA/probes/20260922_tools_1_run12.log`(新規 135 行、10 手。復帰文字 0 個)

## 0. 無改変・検査・後始末

既存の 4015 行は **無改変**。生ログ 10 本を渡して打ち直し、**13 本すべて 0 件**。
**閉じずに残した行は 0 件**(5 回連続)。**残っている処理も 0 件**(リードが `ps` で確認)。

## 1. リードが一次資料で裏取りした中核 — **一致**

`OctoBot` の指値が「板の待ち行列ではなく価格の到達の事象」で埋まるという主張を、ソースで確かめた。

- `octobot_trading/exchange_data/prices/price_events_manager.py` が **HTTP 200** で実在。
- `new_event(price, timestamp, trigger_above, allow_instant_fill=True)` と
  `_is_triggered_by_last_recent_prices(price, timestamp, trigger_above)` が在る。**引き金は価格の到達。**
- **同ファイルに `order_book` / `queue` / `depth` / `volume` / `bid` / `ask` の一致が全部 0 件。**

**当方に無い視点が 1 つ増えた。**当方の `src/bot/backtest/engine.py` は「通過した約定なら埋まったとみなす」形で、
**約定を事象として持つ層が無い。**`PySystemtrade`(次の足の価格と指値の大小)とも違う 3 つ目の模型である。

## 2. `OctoBot` の状態 — **最小実行まで通した(確定)**

成行買い → 資産が `{'BTC':'10','USDT':'100000'}` から `{'BTC':'11','USDT':'99900'}` へ →
指値売りは出した直後 `open` → 価格の事象の注入で `filled`(110)→ `{'BTC':'10','USDT':'100010'}`。
**1 往復が閉じて現金だけ増えている。**`ELAPSED_SEC= 2.01`、`PROC_RC=0`。

**到達までに手段を 5 回替えている。**1 回目のエラーで止めていれば「不可」と書いていた型で、
委任文 §5-1 が要求している続け方が実際に効いた例である。

## 3. 調査班が渡してきた判断への回答

1. **価格の事象を 1 点注入して指値を埋めたのは §5-4 を満たすか → 満たす。**
   §5-4 の中核は「成行と**指値**の 1 往復を通し損益を出す」ことで、時計をどう進めるかは道具の設計に従う。
   `OctoBot` の模型が価格の到達で埋める以上、**それが「この道具で指値が埋まる」ということ**である。
   ティック列を流すには取引所ごとの拡張の型が要り、危険と時間が増えるだけで分かることは増えない。
   **報告に「ティック列を流したのではなく価格の到達を 1 点与えた」と書き分けてあるので、読み違えようがない。**
2. **前の回が残した処理 2 本を kill した → 正しい。**§6-6 は「常駐プロセスを残さない」で、
   前の回の残りを片付けるのは同じ規則の範囲。触ったのは scratchpad の隔離 venv の処理だけ。
3. **新しい候補 16 件を「浅い」として表に入れず一覧に書いた → 正しい。**§4.0 末尾の規則どおり。

## 4. リードの判定を 1 件追加(調査班が渡してきた 47 番の「限界」)

X の投稿に出た `BacktestingMax` / `GFT Backtest Software` / `AlgoTest` の 3 件を候補に立てるかの問い。
**→ 立てる。候補 48〜50 とする。**
委任文 §5-6 は「登録の要る道具(SaaS・ウェブ完結型)も浅く終わらせない。登録なしで取れるものを全部取る」と
明記しており、§3-3 は「候補の一覧から黙って落とさない」。**宣伝の投稿から出たことは、候補にしない理由にならない。**
登録はせず、公式の機能一覧・料金と規約の逐語・無料枠の条件・公開の利用者の報告まで取る。

## 5. 区分 1 の完了判定 — **未完了**

委任文 §2 の 2 条件のうち、
- **前半(残りの候補が空)= 一度満たしたが、新しい検索計画で 16 件出たので再び満たさない。**
- **後半(新しい検索計画が新しい候補を 1 件も出さない)= 満たしていない。**

**残りは 19 件**(31〜46 の 16 件 + リードが足した 48〜50 の 3 件)。
**完了までの道筋**: 19 件の状態を確定させる → もう一度新しい検索計画 6 本 → 新しい候補が 0 件なら完了。
**1 回の実行で 1〜4 件が上限なので、あと 5〜7 回はかかる見込み。**

## 6. 予算

時間・トークンとも超過。注文の層で手段を 5 回替えたところが重い。
**新しい候補 19 件は大半が「一次資料に到達して浅い印を付ける」で済むので、1 回あたりの件数は増やせる見込み。**
