# 検収: 道具サーベイ 区分 8 の 24 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 24 回目の実行(2026-09-26)`(39661 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run24.log`(488,236 バイト)。届いたままのコミット 5e8f6c2。起動文の印 `20260923_tools_survey_cat8_run24_prompt.md@44054264f3c9`。

## 0. 費用と時間

調査班 514,004 トークン・2,346,589 ms(39.1 分)・道具 233 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 24` 0 件 / `check "" run24.log` 0 件 / 過去の節から消えた行 0。`check_scan_report.py` 46 件(この回の節の中は 40088 行の K1・K2)。

## 2. 台帳

`import --round 24`(変わった所 6): 8-033 は `未着手` → `浅い`、E2 印/4・E3a なし・E3b なし・E5 印/4・E6 印/4、E1a・E1b・E4 は `未判別`。**監査 111 回目の結果を見てから**、`set 8-033 E2=未判別 段_E2=未判別 段_E6=3`。`check` 0 件。

## 3. 中身の監査(111 回目、owner-auditor)の逐語

> # 監査結果(111回目・区分8・24回目)
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(39661〜40301行目、`## 区分8 — 24回目の実行`節)、生ログ `docs/DATA/probes/20260923_tools_8_run24.log`、起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run24_prompt.md`、設計票 `docs/DATA/surveys/CAT8_DESIGN.md`、委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md`。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md` 39661〜40301行目(全体、該当箇所なし) — 起動文§7(`.../20260923_tools_survey_cat8_run24_prompt.md` 91〜97行目)は「返す前に」の手順2〜4として `cat8_ledger.py check-elements --round 24`・`cat8_ledger.py check "" ...run24.log`・`git diff -U0 ... | wc -l` の**出力を同じ節に貼る**ことを明記しているが、24回目の節にはこの3つの出力がどこにも無い(`check-elements|cat8_ledger|git diff -U0` で該当0件を機械的に確認)。貼られているのは§7手順1の `check_scan_report.py` の出力(40234〜40300行目)だけ。手順2〜4が実際に打たれ0件だったのか自体が節から読み取れない。
>
> 2. [止める] 生ログ 3400-3451行目(導入=`pip install`、12:13:40)/3472-3496行目(最小実行、12:14:48〜12:15:03)/3500-3535行目(「wheel展開確認」、12:15:32) vs 起動文§2.5「**最小実行の前に、委任文§6-1の導入前の検査を全部やる**」 — §6-1項目5(バイナリ同梱・難読化の検査)のうち wheel の展開・確認(`.so`/`.dll`/`.exe`/`.pyd`の有無、`setup.py`の有無を見る手)は、実測時刻上、導入(pip install)より後、しかも最小実行が完了した後に打たれている。報告 39703行目(知見15)は「§6-1の検査(危険の所見なし)」として sdist の`hatch_build.py`検査と wheel 展開検査を並べて「以上、導入を止める所見は無い」と一つの結論に書いているが、後者が実行**後**に行われたという順序のずれは開示されていない。
>
> 3. [直す] 報告 40086〜40148行目(`Prefect` E3a の当たりの判定・甲の表、59ファイル) — 起動文§2.4「行の表・ファイルの表には、**同じ文を繰り返さない**…同じ文を何行・何ファイルにも当てた『なし』は受け取られない」に対し、59ファイル中58行が結び文「ソフトウェアの情報/メモリ/接続/認証情報の漏洩(leak)の意味で、計算・特徴量・戦略・模擬がその時点で知り得ない情報を使っていることを検出・報告する機能ではない」を一字一句同一のまま使っている(引用する当該行自体はファイルごとに異なる)。500行以下だったため全件判定に進んだ結果だが、結論文が単一なのは規則の文言に反していないか。
>
> 4. [直す] 報告 39692行目(知見4)・40226行目(判断に迷った点2)、起動文21行目 — 起動文は「数えるのは候補の公式のリポジトリの中のもの(と、候補の配布物に同梱されたもの)だけ。**別の配布物(利用者が別に入れる統合・プラグイン・パッケージなど)は、候補の機能に数えず、Nにも入れず**」と明記。調査班は `src/integrations/*`(prefect-aws等19件、別PyPI配布物であることを自ら40226行目で認めている)をNに含め、その結果(N=5,407・5,097ファイル)を根拠にE1a/E1b/E4の500行判定・E3a/E3bの全件検索を実施した後で、含めてよいかを「判断に迷った点」として未決のまま提出している。委任文の手順(迷ったら決めずに問いに出す)に対し、決定してから疑問を出す順になっていないか。
>
> 5. [聞く] 報告 39694行目(知見6、E2 印・段4) — E2述語(設計票54行目)の対象は「時系列・約定・板・足・参照データ」だが、根拠に引く `pass-inputs.mdx` はflow/taskへの**任意のPython型・pydantic BaseModel**の汎用型検証を述べており、対象が「時系列・約定・板・足・参照データ」であることを一次資料自体は限定していない。16回目にOpenClawの汎用キャッシュをE2から除外した当て方(区分8-026、起動文38行目に言及)と比べて同型と言えるか、あるいは対象が合成データで実際に trade を模した点で異なるのか。リードの案(未判別に戻す)を採るにせよ、狭める側への修正である点でA-18の観点からの理由づけが要る。
>
> 6. [聞く] 報告 39701行目(知見13、E6 印・段4) — 段4の条件(設計票81行目)は「その要素の対象のすべてを、**その道具の外から持ち込んでも(一般のPythonの関数・CSVなど広く使われる形式)**掛けられる」ことを言う8字以上の逐語が要る(§2.1、39行目相当)。知見13が引く逐語は `block: type[Block]` というコード片で、これはPrefect自身が定義する抽象基底クラス`Block`のサブクラス型であり、「一般のPythonの関数・CSV」に当たることを直接述べた一次資料の文ではない。「外部から持ち込む対象」という言い方はリードの言い換えで、原文逐語ではないのではないか。
>
> 7. [直す] 報告 40213〜40215行目(§4.0で未確認のまま残した項目のうち「登録の要否」「宣伝詐欺の兆候」「時刻の扱いの具体的挙動」) — 委任文§4.1(72〜79行目)の未確認の定義は「根拠の欄に**試した手段の一覧**を書く」だが、この3行はそれぞれ「`cat8_render.js`によるJS描画はこの回では実施していない」「専用に探す検索はこの回では実施していない」「実測するテストはこの回では行っていない」と、試して届かなかった記録ではなく単に行わなかった申告になっている。とくに`cat8_render.js`は起動文§2.6がまさにこの状況(JSで描くSPA)のために用意した手段であり、使わなかった理由が「この回では」以外に書かれていない。P1「取れない・無いは全経路を試した結果でなければ言えない」と同型が「未確認」の側で起きていないか。
>
> 8. [聞く] 報告 40236行目・40242行目・40252〜40253行目(受け入れ検査の出力、K1・K2の40088行目の説明) — 「表の塊全体では偶奇が合わないために生じる誤検出」という説明は、19612・30448行目等と「同型」と述べるのみで、この回固有の内訳(太字5件・丸括弧84対79・大括弧12対10がそれぞれどの逐語引用に由来するか)を行レベルで示していない。誤検出を自分で閉じずにリードへ渡す規則(委任文§12)自体は守られているが、リードが検収で個々に確かめる材料としては薄いのではないか。
>
> 止める指摘: あり(2件、#1・#2)。

## 4. 処置(リード、監査 111 回目)

1. **止める → 直した**。リードが 3 つの検査を打ち直し(0 件・0 件・0 行)、コマンドと出力を報告の節の末尾の「リードの追記」に貼った。
2. **止める → 記録した(結論は変わらない)**。生ログの時刻で確かめた: sdist と `hatch_build.py` の検査(12:11:44〜12:12:36)は導入(12:13:40)の前、wheel の展開(12:15:32)は導入と最小実行のあと。wheel に止める所見は無かった。**21 回目と同じ型の違反(§6-1 の一部を導入のあとに行った)が、起動文で名指ししても起きた**。起動文の文だけでは止まらないので、25 回目以降の起動文では「§6-1 の結論の行を生ログに出してから `pip install` を打つ。検収でリードが生ログの時刻を見て、結論の行が `pip install` の行より前にあることを確かめる」と書き、検収でリードが必ず時刻を見る。
3. **受け取る**。21 回目の検収 §4 の処置 3 と同じ: 理由の欄は全部の行でそのファイルの当たった行を逐語で引き(行ごとに違う)、そのあとに語の意味の分類を付けている。行を読んだことは引用で確かめられる。
4. **直した(記録)**。`src/integrations/*` は起動文が N から外すと書いた別の配布物で、N に入っていた(調査班の違反)。E3a・E3b の `なし` は広い一覧で全部判定したので変わらない。E1a・E1b・E4 の案 B の当たりの行の数は、本体だけで数え直していない(未確認)。次の Prefect の回で数え直す。
5. **答える(E2 は `未判別`)**。E2 の述語の逐語は「**時系列・約定・板・足・参照データについて**…型や範囲の違反…を検出するか報告する機能」。一次資料(pass-inputs.mdx)が言うのはワークフローの引数の型の検査で、その対象を列挙のデータに限っていない、というのは監査役の指摘のとおりで、だから「列挙のデータへの検査」を言う逐語が無い。16 回目の OpenClaw(チャットのメッセージの重複)と同じく、対象が述語の列挙に当たることを言う逐語が無いものは `印` にしない。**A-18 について**: `なし` にはしていない(全件の検索を打っていない。不明のまま)。狭める方向の直しだが、棄却ではなく「根拠が述語に届いていない」ことの記録で、Prefect に E2 の機能が無いとは書かない。合成の値に price という名前を付けた最小実行は、述語の対象を示す一次資料ではない。
6. **答える(E6 は段 3)**。監査役の指摘のとおり、`block: type[Block]` は Prefect の枠組みの型で、段 4 の「一般の Python の関数・CSV など」を言う逐語ではない。段 3。
7. **次の回で直す**。「この回では実施していない」型の `未確認` 3 件(登録の要否・宣伝詐欺の兆候・時刻の扱い)を、次の Prefect の回で試す(`cat8_render.js` を含む)。
8. **誤検出と判定する(リード、行の内訳は見ていない)**。40088 行の K1・K2 は、長い表の塊の中の一次資料の逐語(コードの括弧・引用符)で偶奇が合わない型で、19612・30448 行と同じ。行ごとの内訳はリードも確かめていない(未確認)。

**8-033 の扱い**: `浅い`。`未判別` は E1a・E1b・E2・E4。案 B の記録は E1a・E1b・E4 にあるが数が本体だけでない(処置 4)、E2 には無い(印を探し尽くしたわけではない)。§4.0 の `未確認` の 3 件が残る。よって、残りに数える。**残り 9 のまま**。

## 5. これからの手

25 回目 = 8-033 Prefect の残り(本体だけの当たりの数・E2 の印の探し尽くしと案 B の記録・§4.0 の 3 件)。そのあと大きいリポジトリ(8-031 Debezium・8-032 Kafka・8-034 Pandas・8-035 Spark・8-039 NumPy・8-040 SciPy)、8-026 OpenClaw の残り、8-029 MetaTrader の E1a。
