# 区分 1 の 23 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 23 回目の実行」節(346 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run23.log`
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲(機械)

`git diff --numstat` = **346 行追加 / 0 行削除**。**過去の回の節は 1 行も書き換わっていない。判定: 合。**

## 2. 受け入れ検査 — **合。ただし 22 回目の貼り付けに欠陥を見つけた**

当方が全 22 本の生ログを渡して打った結果は 13 本すべて 0 件。**この回は合。**

**しかし 22 回目の節(6714 行)に貼られた出力は `K12 検査の出力の貼付 1 件` / `---- 合計 1 件` で、その 4 行下の文
「**この回は 13 本すべてが 0 件で通った**」と食い違っている。**貼られた理由も出力の中に書いてある:

```
K12 検査の出力の貼付           1 件
    docs/DATA/SCAN_2026-09-21_tools.md:0  貼られた出力に「---- 検査対象の合計 N 件」の行が無い(全文をそのまま貼ること)
```

**正体は K12 の自己参照である。**貼り付ける前に打つと「貼り付けが無い」で 1 件当たり、その出力を貼ると当たりが消える。
**つまり貼られているのは途中の実行の出力で、最終の状態の出力ではない。**文のほうは最終の状態について正しい(当方の打ち直しで 0 件)。

**これは欠陥である。**貼り付けは主張の証拠として置かれているのに、**証拠が主張と逆のことを言っている。**
読む側が確かめに行くと `合計 1 件` に当たる。

**当方も 22 回目の検収でこれを見落とした。**検査を自分で打って 0 を見て、文の 0 を読み、**貼られた塊と突き合わせなかった。**
→ **リードの手順に足す: 検収では、貼られた塊の数字と、当方が打った結果を 1 行ずつ突き合わせる。**

→ **調査班への規則**: 貼るのは**最終の実行の出力**とする。K12 の自己参照でそれが作れない回は、
**「貼ったのは貼り付け前の実行で、K12 の 1 件は自己参照。貼り付け後に打ち直した結果は 0 件」と節に書く。**黙って並べない。

**22 回目の節は書き換えない**(過去の回の節は記録。20 回目の集計の落ちと同じ扱い)。**24 回目の節に訂正を書く。**

## 3. 中身の抜き取り(リードが原典を自分で打ち直した)

| 検めた主張 | リードが打ったもの | 結果 |
|---|---|---|
| **15 番 `BacktestingCore` が滑りと市場影響を持つのに検証が呼んでいない** | `backtest/src/synthetic_book.rs`(11,997 バイト)に `//! The simulator uses these to model realistic slippage and market impact.`(5 行)・`pub fn from_candle(`(80 行)・`pub fn calculate_slippage`(157 行)・`pub fn calculate_market_impact`(205 行)。一方 `backtest/src/simulation.rs`(**97,658 バイト**)の `synthetic_book\|SyntheticOrderBook` の当たりは **0 件** | **当たり。97KB の検証の本体が、12KB の滑りと影響の板に一度も触っていない** |
| **16 番が自称に反してイベント駆動ではない** | `backtest_engine.py`(6,680 バイト)の `queue\|deque\|Event(\|dispatch` の当たり **0 件**。駆動は 62 行 `exec_price = self.data['Open'].iloc[ind_nbr + 1]` = 次の足の始値 | **当たり。登録名が `event_driven_backtesting_engine` でも実装は素の繰り返し** |

**16 番の判断は、22 回目 §5(2) で決めた「自称を条件にしない」が両方向に効いた最初の例である。**
候補 123 には自称が無くても実装で付け、候補 16 には自称があっても実装が無いので付けない。**規則の当て方として正しい。**

**リード自身の誤りを 1 件出す**: 15 番を検めるとき、また既定枝を確かめずに `main` を叩いて 404(14 バイト)を受け取った。
**既定枝は `master`。**21・22 回目の検収で自分に課した手順を、**3 回連続で破っている。**調査班の側は同じ回に当てずっぽうの取得 0 件である。

## 4. **問い 1 の決定 — `区分1-市場影響と約定の模型` の下限を決める**

これが区分 1 で一番重い定義の問題である。調査班の整理は正しい:
呼ぶと足の検証のほぼ全部が当たって切れ味が落ち、呼ばないと 3 番・8 番・19 番がどの要素にも入らない。

**決定: 印は付ける。そのうえで「どの段か」を必ず書く。**

理由: **「跨いだら埋まる」を模型と呼ばないなら、当方の `engine.py` にも約定の模型が無いことになる。**
当方の engine.py:45-53 は自ら「通り抜けた約定でいつでも埋まる / **maker-TP の結果は上限であって約束ではない**」と書いており、
**この比較こそが区分 1 の目的である。**下限を切ると、比較の対象から当方自身が外れる。
一方、印だけでは切れ味が無いという指摘も正しいので、**切れ味は印ではなく段で出す。**

**段(この 23 回で出そろったもの。当方の位置も書く)**:

| 段 | 何で埋めるか | 当たった候補 |
|---|---|---|
| 0 | 何も見ない(要求の値をそのまま約定にする) | 61 `barter-rs` |
| 1 | 価格が跨いだら埋まる(量も滑りも見ない) | 8 `OpenTrader`、**当方の `engine.py`(通り抜けた約定)** |
| 2 | 次の足の値と指値を比べ、**未約定を勘定に残す** | 3 `PySystemtrade` |
| 3 | 価格の**到着を事象**として見る | `OctoBot` |
| 4 | **量を見る**(部分約定・滑り) | 123 `zenbot`(`Math.min(remaining_size, trade.size)` + 既定 `0.045`) |
| 5 | **列の位置を追う** | 104(`queuePosition`)、95(約定の列)、90(**比例配分**)、当方の `maker_fill_ref.py`(**FIFO**) |
| 6 | **影響の関数を持つ** | 106 `almgren-chriss`、116 `acOptTxns`、119 `LOB-Bench`(測る側) |

**24 回目で、印を持つ 23 件すべてに段を書くこと。**3・8・19 番には印を付け、段 2・段 1・(19 は読んでから)とする。

## 5. **横断する型の格上げ — 2 件出たので常設の検査にする**

**「機構を持っているのに、検証の実行経路が呼んでいない」**が 2 件確定した(107 `sigc`、15 `BacktestingCore`)。
**当方の `engine.py` の「楽観的」もこの型である。**1 件の偶然ではないという調査班の判断は正しい。

→ **以後、区分 1 で印を付けるたびに次を確かめ、結果を節に書くこと(新しい検査項目)**:
> **その機構を、検証の実行経路が実際に呼んでいるか。**呼び出しの当たりが 0 件なら、**印は付けず、所見として書く。**

これは 22 回目 §5(1) で決めた「持っているだけでは付けない」の**測り方**である。規則はあったが、測り方が無かった。

## 6. 残りの問い — リードの回答

**(2) 決まらなかった 8 件 → 7 件は原典を読んで埋める。11 番 `OctoBot` の 5 要素は `未確認` のまま残す。**
模擬を回すのは深掘りであり、この段では範囲の外。**「何を読んで決まらなかったか」を書いてあるので、未確認で正しい。**
**完了の報告では、この 5 要素を「尽きた」に数えない。**

**(3) 22 番 `Lean CLI` と 52 番 `QuantConnect` → 番号は両方残し、互いへの参照を書く。数えるのは機関として 1 つ。**
番号は登録・入口の単位、数えるのは機関の単位。**どちらの数え方をしたかを数え直しの表に明記すること。**

**(4)「6 要素は付けない」を埋まったに数えること → 正しい。**
完了の形が要素ごとである以上、**「どの要素にも属さない」と理由つきで言えることは決着である。**「未決」と混ぜてはならない。

**(5) 13 番 `DeviaVir/zenbot` と 123 番 `carlos8f/zenbot` → 番号は両方残し、互いへの参照を書く。**
20 回目の洗い出しのとおり**登録としては別物**(分岐元と分岐先)なので、一覧から消さない。
ただし**印の根拠が同じ実装の系統から来ている**ので、**「独立した 2 件の観測として読まないこと」を節に書く。**

**(6) 過去の節を触らずに指定 5 を満たしたか → 満たしている。**
22 回目が定めた「状態はその候補が**最後に書かれた箇条**から読む」に沿っており、射程が新しい節の主張の側に書かれている。
**追記だけで済むならそのほうがよい**(記録を壊さない)。

## 7. 安全側の所見

**19 番 `Jesse` の登録に `AGENTS.md`・`.claude/skills/README.md`・`.claude/skills/jesse-strategy-tests/SKILL.md`・`.agents/skills` が同梱。通算 6 件目。**
一覧に在ることを数えただけで本文は取っておらず、従っていない。**扱いは正しい。**
**`Jesse` は区分 1 の有力な候補なので、使う段のオーナーへの所見に必ず載せる。**
404 は 3 件で全部「一覧の取得」、TLS 切断 5 件は全部打ち直して 200、**404 の欄に入れていない。**
この回に導入・構築・最小実行・登録・鍵の発行・購入・発注は 1 件も無い。

## 8. 判定

**受け取る。**23 件のうち 21 件が決着し、抜き取りは 2 件とも原典に当たった。
**自称に反して印を外した 3 件(16・2・20 番)は、規則が両方向に効いていることの実証である。**
**22 回目の貼り付けの欠陥は当方の見落としでもあり、手順に組み込んだ(§2)。差し戻す点は無い。**

## 9. 24 回目の起動の指定

1. **22 回目の貼り付けの食い違いの訂正を 24 回目の節に書く**(§2)。22 回目の節は書き換えない。
   **以後、貼るのは最終の実行の出力。**作れない回はその旨を書く。
2. **`区分1-市場影響と約定の模型` の印を持つ 23 件すべてに「段」を書く**(§4 の表)。
   **3 番 `PySystemtrade`(段 2)・8 番 `OpenTrader`(段 1)に印を付ける。19 番 `Jesse` は読んでから決める。**
3. **決まらなかった 7 件(4・5・6・15・18・19・21 番)を原典を読んで埋める。**
   **11 番 `OctoBot` の 5 要素は `未確認` のまま残す**(模擬を回すのは深掘り = 範囲の外)。
4. **印を付けるたびに §5 の新しい検査を当てる**(その機構を検証の実行経路が呼んでいるか)。**結果を節に書く。**
   既に印がある候補についても、**24 回目で当てられる範囲で当てる。**
5. **22 番と 52 番、13 番と 123 番に互いへの参照を書く**(§6(3)(5))。数え直しの表に、番号で数えたか機関で数えたかを明記する。
6. **数え直しを 3 段で出す**(未到達 / 印が無い / 個別の要素が未決)。
7. 上の 1〜6 に要る範囲でのみ既存の節への書き換えを許す。**それ以外は 1 文字も触らない。**
8. **新しい検索計画は打たない。深掘りもしない。候補も増やさない。**
9. 既定枝は `ungh.cc` で確かめてから取る(**リードが 3 回連続で破った。調査班は 0 件**)。生ログの本数は `ls … | wc -l` の出力を添える。
