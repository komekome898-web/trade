# 監査記録: 文書整理の実行結果 — 2026-09-12

監査役: 本番 `owner-auditor`。問い 14 件(止める 1 / 直す 11 / 聞く 2)。要旨と応答。

1. [止める] 遅延プローブを `start_all.bat` の常駐リストに加え P13 を改訂したが、承認された計画(L-131)に無い変更。PC で常時動くものの構成変更を提示なしに紛れ込ませていないか(CLAUDE.md §4、DELEGATION §8)。
> **直した + 上申。** そのとおり承認外だった。3 つの bat の変更を戻し、P13 を元の手動手順に戻した(share_logs のログ共有だけ残す)。「夜間再起動で自動起動させる」案はオーナーへ yes/no で上申(状態板の次の一手)。
2. [直す] P10 は状態板が「完了(L-132)」なのに F4 の畳み込みが適用されていない。
> **直した。** P10 を 3 行の完了スタブに畳んだ(手順は git 履歴)。
3〜10. [直す] 削除・移動した文書への参照が `fetch_all.bat`・`intake_ledger.py`・`constants_inventory.py`(出力先が `docs/CONSTANTS_TODO.md` のまま = 削除物を再生成)・`.gitattributes`・`.gitignore`・`config/constants.yaml`・`schema/*.json` に残っている。
> **直した。** すべて新しい場所(`docs/DATA.md` §0/§8、`docs/OPERATIONS.md` §1、`docs/DATA/surveys/`)へ付け替え。`constants_inventory.py` の出力先は `results/CONSTANTS_INVENTORY.md`(INDEX (ii) 生成物は docs/ に書かない)に変更、テストも更新。
11. [直す] research-protocol §13 の実装行から原文の「(2026-09-06 追加予定)」が落ちている(D10 は逐語のはず)。
> **直した。** 原文どおり復元。
12. [直す] OPERATIONS §1 の事実 2 で、原文の後半(収集データと検証結果の帰属)が削られている。
> **直した。** データの帰属は原文どおり復元。「検証結果」への言及だけは §5.1(全捨て)により除く旨を括弧で明記。
13. [聞く] DELEGATION §4 の見出しに「週次要約」が残るのは意図的か。
> **直した。** 見出しを「判断が要る項目の受け皿(旧 週次要約。文書は削除済み、Routine も 2026-09-12 停止)」に。Routine `trig_01R9Mud…` は無効化した。
14. [聞く] 旧 DATA_LOSS_REGISTER の「分類の出所」「載せない判断」を git 履歴にしか残さない判断でよいか。
> **直した。** 両節を原文のまま `docs/DATA.md` §8 の末尾へ移した。

**結果**: 止める 0 件残(1 件は直したうえで上申)、直す 11/11、聞く 2 件とも成果物で対応。
