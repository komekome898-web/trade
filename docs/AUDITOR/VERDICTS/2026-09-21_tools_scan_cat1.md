# 監査の記録 — 調査班の報告 `docs/DATA/SCAN_2026-09-21_tools.md`(区分 1、1 回目の実行)

委任文 `docs/DATA/delegations/20260921_tools_survey_prompt.md`(監査済み v10、指紋 840bf99ca5b0)。オーナーの要求 L-377〜L-381。

## 1 回目(v1 = 調査班が届けたまま、コミット 9886344)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/SCAN_2026-09-21_tools.md:175 — hftbacktestの「スター 4.7k、フォーク 912」「コミット数=1,038件」「PyPI月間ダウンロード 14,033件」は出典を「一次資料(WebFetch(github.com/nkaz001/hftbacktest))」「pypistats.org/api/packages/hftbacktest/recent」としているが、生ログ`docs/DATA/probes/20260921_tools_1.log`にはこのURLへのWebFetch・curlは1件も記録されていない。この数値は実際に取得したものか、記憶で埋めたものか(CLAUDE.md §0.2 O-3、P1「取れない/無いは全経路を試した結果」の裏返しで「取れた」も実行記録が要る)。
>
> 2. [止める] docs/DATA/SCAN_2026-09-21_tools.md:176 — hftbacktestの「対応取引所: 国内取引所の記載なし…**無し(README に記載が無いことによる推定)**」は、1つのREADME上の非記載だけを根拠に「無し」と結論している。ソースコード・issue・依存関係等の別経路を試したか(P1)。
>
> 3. [止める] docs/DATA/SCAN_2026-09-21_tools.md:210-211 — NautilusTraderの「スター29.2k、フォーク3.9k、コミット21,335件」「PyPI月間ダウンロード308,832件」および「bitFlyer・GMOコイン・bitbankの記載は…無し、と推定」も同様に、対応するWebFetch/pypistatsアクセスが生ログに存在しない(CLAUDE.md §0.2 O-3、P1)。
>
> 4. [止める] docs/DATA/SCAN_2026-09-21_tools.md:232 — vectorbtの「スター9.1k、フォーク1.2k、1,081コミット(WebFetch実測)」も同じく生ログに対応取得記録が無い。「実測」の表示は妥当か。
>
> 5. [止める] docs/DATA/SCAN_2026-09-21_tools.md:230-241 — vectorbt.proの料金(月額$25・年払い実質$20/月・生涯$500)とライセンス文言は`vectorbt.pro/become-a-member`のWebFetchを出典とするが、生ログに当該URLへのアクセスは無い。ライセンス文言には「未検算」の留保が付いているのに、価格の数値には同じ留保が付いていない。両方とも「未確認」で統一しないのはなぜか(P2、CLAUDE.md §0.2 O-3)。委任文§4は「無料/有料の1語で済ませない」を要求しており(L-378)、根拠不明のまま具体的な金額を確定的に書くと、かえってL-378が懸念した種類の誤情報を生む危険がある。
>
> 6. [止める] docs/DATA/SCAN_2026-09-21_tools.md:262-263 — freqtradeのスター54.6k・フォーク11.3kの活動指標、および「bitFlyer/GMOコイン/bitbankは公式`exchanges`ページに個別記載が無い」の一次資料側(WebFetch実測と表示)も、同URLへのアクセスが生ログに無い。非対応の「理由」部分だけを「未検算」と自認しているが、記載の有無そのものの取得記録が欠けている点は同型の欠陥ではないか。
>
> 7. [止める] docs/DATA/SCAN_2026-09-21_tools.md:223 — NautilusTraderの「危険」欄「自動発注機能: 有り」という安全性の判断は、README記載(WebFetch由来、生ログに記録なし)にのみ依拠している。安全判定の根拠として、ログに残らない引用をどこまで確定事実として書いてよいか。
>
> 8. [直す] docs/DATA/SCAN_2026-09-21_tools.md:82-85, 164 — 検索計画6本を「すべて実行(未実行なし)」、候補一覧を「未クリックの候補は無し」と完了扱いしているが、WebSearchの実行自体が生ログに1件も記録されていない。この完了の主張を裏づける実行記録はどこにあるか(委任文§5-5「1手ずつ残す」、P1)。
>
> 9. [直す] docs/DATA/SCAN_2026-09-21_tools.md:299 — X検索(`site:x.com`)は「2回のみ」と自己申告し委任文§3-2の下限(3回以上)未達を認めているが、実施したと書かれている2回分のWebSearchすら生ログに記録が無い。実施した分の実測記録はどこにあるか。
>
> 10. [止める] docs/DATA/SCAN_2026-09-21_tools.md:182-185(hftbacktestの「危険」節) — hftbacktestは実際に`pip install`まで実行した(生ログ12-14行)。しかし委任文§6-1が導入前検査として要求する「初回公開日」「保守者の数と名前の一貫性」「週のダウンロード数」のうち、初回公開日と保守者の一貫性の確認が本文に無い(最終更新日と月間ダウンロード数のみ記載)。実際に導入した候補について、§6-1の検査項目は全部書かれているか。
>
> 11. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:131, 242 — 「バックテスト専用としての利用に限られる可能性が高い」「無料版だけでは不十分な可能性がある」という記述は、委任文§0「判定(使える/使えない/不要/今の環境以下/採用)は書かない。判定はリードとオーナー。」に抵触しないか。ヘッジ表現(「可能性が高い」)は判定と観測記述の境界としてリードの基準に合っているか。
>
> 12. [止める] docs/DATA/SCAN_2026-09-21_tools.md:324-326 — 「トークン: 本セッション内の正確な消費量は自己計測できない(未確認)」と書いた直後に「5万トークンに近いと推定」と具体的な推定値を出している。ハーネス実測(188,843トークン)との乖離は約3.8倍。「自己計測できない」との整合性、およびこの推定の根拠は何か。同節の実時間見積り(「約20分相当」)も、実測650秒(≈10.8分)との乖離が同様に未説明(CLAUDE.md §0.2 O-2)。
>
> 13. [直す] docs/DATA/SCAN_2026-09-21_tools.md:159 — Bot18の「$49.99の8桁アンロックコード制」は出典「WebSearch(存在確認のみ)」とされているが、存在確認のみのはずが具体的な価格・制度の数値まで書かれている。一次資料URLと取得日は書かれていない。
>
> 14. [聞く] docs/DATA/SCAN_2026-09-21_tools.md 全体 / docs/DATA/probes/20260921_tools_1.log — 報告本文のWebFetch/WebSearchへの言及(委任文が要求する「経路(a)(b)(c)」のうち(a)と(c)の大半)が生ログに1件も対応しないまま「1回目の実行」の完了物として扱われている。委任文§11「完了の形」は「ツールの表(全列)…生ログ…が揃っていること」を要求しており、生ログとツール表の記載内容が食い違う場合、この区分は完了と呼べる状態か。
>
> 15. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:176, 211 — 「対応取引所: 無し」という結論が、深掘り済み4件のうち2件(hftbacktest・NautilusTrader)では追加確認の予定(次回裏取り項目)に載っていない一方、freqtradeでは次回優先項目に載っている(315-317行)。同じ型の断定に対して扱いが揃っていない理由は何か。
>
> 検査した対象: `docs/DATA/SCAN_2026-09-21_tools.md`(区分1、326行)、`docs/DATA/probes/20260921_tools_1.log`(60行)、`docs/DATA/delegations/20260921_tools_survey_prompt.md`(監査済みv10)。参照した監査資料: `docs/AUDITOR/PRINCIPLES.md`、`docs/AUDITOR/KNOWN_ANSWERS.md`。`docs/DATA.md`はテーマ上参照不要と判断し読んでいない。
>
> 指示の(2)委任文§4の列の網羅性と(3)委任文§6の安全遵守については、深掘り済み4件の表・危険節は形式上すべて埋まっているか「未確認」と明記されており、登録・鍵・発注・外部送信・リポジトリ外書き込みについても明確な違反は本文からは確認できなかった(ただし上記10番の通り§6-1の一部項目の欠落は指摘した)。この2点について、上記以外の追加の指摘は見つからなかった。

## リードの処置(v2)

**起点(問い 1「実際に取得したか、記憶で埋めたか」)の答え**: 取得していた。ハーネスが保存した調査班の道具呼び出しの記録から、WebFetch 8 手・WebSearch 16 手(計 24 手)を機械抽出し、報告の数値・語(4.7k / 912 / 1,038 / 14,033 / 29.2k / 3.9k / 21,335 / 308,832 / 9.1k / 1.2k / 1,081 / 54.6k / 11.3k / $25 a month / $240 / $500 one time / non-commercial / 49.99 / Bot18 / ZalgoNet など)がそれぞれの取得結果に含まれることを 1 件ずつ照合した(全部「有」)。**照合したのは数値と固有名詞の一致で、文章の主張(「〜のみ」「〜と読める」)がその生ログ行にあるかは照合していない(監査 2 回目の指摘 3 で訂正)。数値・固有名詞の範囲では記憶で埋めたものは 0 件。**抽出の結果と照合は生ログ W 節に追記(W-1〜W-24 + WebFetch の要約の先頭 400 文字 + 照合表)。

1. **直した。** 数値の出典を生ログ W-9・W-10 に付け替え、「一次資料」を「WebFetch の要約」に改めた(要約は原文そのものではない)。pypistats の日次・週次も足した。
2. **直した(監査役が正しい)。** 「無し(推定)」を「README に記載なし。ソース・issue・依存は見ていない(未確認)」に改め、次回の裏取り項目 7 に載せた。
3. **直した。** 1・2 と同じ(生ログ W-11・W-12、未確認の印、項目 7)。
4. **直した。** 「WebFetch実測」を「WebFetch の要約(生ログ W-13)」に。
5. **直した(留保を揃えるのではなく、価格は原文で確かめた)。** リードが `curl` で原文を取り直し、4 つの文言が原文にあることを確認(生ログ L-1)。ライセンス文言は原文未取得のまま「未検算」と明記。
6. **直した。** 活動指標は W-14。exchanges ページはリードが原文を取り直し、国内 3 社の語 0 件・Binance 38 件などを確認(生ログ L-2)。
7. **直した。** 「有り」を「有り、と README の記載から読める(WebFetch の要約 W-11。ソース未確認 = 未検算)」に。次回の裏取り項目 8。
8. **直した。** 検索計画 6 本の記録は W-1〜W-6。本文に参照を付けた。
9. **直した。** X の 2 回は W-16・W-17。
10. **直した(監査役が正しい)。** 初回公開 2022-11-02(v1.0)・保守者の名義の一貫性(nkaz001、1 名義)・週のダウンロード 1,855 を足した(生ログ L-3・W-10)。
11. **答えた: 抵触する。直した。** 2 箇所とも判定の言い回しを観測の記述に置き換えた(「ライブ実行の対応先は README に 2 つ」「無料版に指値があるかは未確認」)。
12. **直した。** 自己申告の推定を消し、ハーネスの計測(188,843 トークン・650 秒・65 回)を書き、5 万の 3.8 倍だったこと、自己申告が根拠のない推定だったことを明記。次回からトークンは調査班に推定させず、リードが計測値を書く。
13. **直した。** 出典を「WebSearch の結果の要約(検索語・W-24)」とし、価格と制度の文言に未検算の印を付けた。
14. **答えた: 完了と呼べない。** 見出しに「未完了」と、W 節でリードが補った旨を明記した。区分 1 は残り候補 13 件のまま次回に持ち越す。
15. **答えた: 理由は無い(調査班の書き漏れ)。直した。** hftbacktest・NautilusTrader も次回の裏取り項目 7 に載せ、扱いを揃えた。

## 2 回目(v2、コミット ad664ce)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/SCAN_2026-09-21_tools.md:131, 176 — hftbacktestの「対応取引所」欄が2箇所とも出典を「生ログ W-9」としているが、`docs/DATA/probes/20260921_tools_1.log:91`(W-9、「結果356文字」= 全文表示)にはスター数・フォーク数・ライセンス・コミット数の話しか無く、取引所(Binance Futures/Bybit)についての記載が一切無い。この2箇所の「(生ログW-9)」という出典表示は事実と食い違っていないか。実際の出典は`pypi.org/pypi/hftbacktest/json`のdescription(curl、生ログ4行目・175行の「できること全部」欄)ではないか。1回目の指摘1・2はここを直したはずだが、直した文自体が新しい誤った出典表示を持ち込んでいる(P1裏返し・CLAUDE.md §0.2 O-3)。
>
> 2. [止める] docs/DATA/SCAN_2026-09-21_tools.md:223 — NautilusTraderの「危険」節「自動発注機能: 有り」の根拠として引用する「Live: Identical strategy implementations between research and live deployment」は出典を「WebFetch の要約 = 生ログ W-11」としているが、`docs/DATA/probes/20260921_tools_1.log:93`(W-11、「結果624文字」のうち表示されている約400文字)はリポジトリ概要(スター・フォーク・ライセンス・対応取引所・データプロバイダー)のみで、この引用文言は表示部分に無い(残り約224文字は未表示のため断定はできない)。安全性判定(自動発注機能の有無)の根拠として、この引用が実際にW-11に含まれることを確認したか。含まれないなら出典は別(PyPI JSON description)ではないか。
>
> 3. [直す] docs/AUDITOR/VERDICTS/2026-09-21_tools_scan_cat1.md:43 — 「WebFetch 8手・WebSearch 16手(計24手)を機械抽出し…1件ずつ照合した(全部「有」)。記憶で埋めた数値は0件」という「起点の答え」は、上記1・2で見つかった出典の食い違い(対応取引所・自動発注機能という文章の主張)を照合対象に含めていない。照合表(`docs/DATA/probes/20260921_tools_1.log:101-112`)は数値・固有名詞のトークン一致のみを検査しており、本文の文章の主張(「〜のみ」「〜と読める」)がその生ログ行に実在するかまでは検査していない。「全部有」「記憶で埋めた数値は0件」という結論はこの照合の範囲を超えて言い過ぎではないか(P1裏返し)。
>
> 4. [聞く] docs/DATA/probes/20260921_tools_1.log:111-112 — 照合表はW-23(Jesse)・W-24(Bot18)について「JesseGPT=有, MIT=有」「49.99=有, Bot18=有, ZalgoNet=有」としているが、W-23・W-24はWebSearchであり、ログの「### Wの WebFetchの結果」節(88-99行)にはWebFetch分(W-7〜W-15,19,21)の本文しか掲載されておらず、WebSearchであるW-23・W-24の検索結果本文はログのどこにも印字されていない。この2行の「有」判定は、この生ログを読む第三者(監査役を含む)が独立に検証できる形になっているか。
>
> 5. [直す] docs/DATA/SCAN_2026-09-21_tools.md:327 — 1回目監査の指摘12は「トークンの推定(5万)」と「実時間の見積り(約20分相当)」の両方について、実測(188,843トークン・650秒)との乖離が未説明だと指摘した。v2の処置はトークンの乖離(3.8倍)は説明したが、時間の乖離(自己申告「約20分相当」対実測650秒≈10.8分、約1.85倍)は「時間は目安の内側」の一文で済ませており、なぜ自己申告が実測と乖離したのかという指摘12後半の問いには答えていない。
>
> 6. [直す] docs/DATA/SCAN_2026-09-21_tools.md:208 と docs/DATA/probes/20260921_tools_1.log:93 — 本文はNautilusTraderのライセンスを「LGPL-3.0-or-later(一次資料 PyPI classifiers)」と記載する一方、同じ節が引用するGitHub README由来のW-11(ログ93行)は「GNU Lesser General Public License v3.0 (LGPL-3.0-only)」としており、"or-later"と"only"は法的に別のライセンス条件である。2つの一次資料が食い違っているのに、この食い違い自体が本文のどこにも記録・言及されていない。
>
> 7. [聞く] docs/DATA/SCAN_2026-09-21_tools.md 全体(例: 230, 235, 240, 242, 246, 263, 290行の「未検算」)— 委任文§0・§4(`docs/DATA/delegations/20260921_tools_survey_prompt.md`)が指定する分類は「一次資料/実測/推定/仮定」+「未確認(試したこと: …)」の5種のみで、「未検算」という語は委任文のどこにも定義されていない。文書全体で10箇所以上使われている独自語だが、「未確認」とどう違うのか(単に取得できなかったのか、取得はしたが原文と突き合わせていないのか)の定義が本文に無い。これは委任文が指定した語彙からの逸脱に当たらないか(P2)。
>
> 8. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:179 と docs/DATA/probes/20260921_tools_1.log:13 — hftbacktestの「隠れた依存: なし(numpy/numba/polars/matplotlib/holoviels は全て無料OSS、実測でインストール確認済み)」という記載のうち、生ログのpip installコマンド(`pip install hftbacktest numpy numba polars`)には matplotlib・holoviews が含まれていない。これらは「依存46パッケージ」(183行)の中に транзит的に含まれていた可能性はあるが、その全リストはログのどこにも印字されていない。「実測でインストール確認済み」と断定できる根拠(46件の依存の実際の一覧)はどこにあるか。
>
> 他 0 件省略(該当なし、上記が全て)。

## リードの処置(v3)

1. **直した(監査役が正しい。リードが v2 で出典番号を中身を見ずに付けた)。** 確かめると、W-9(GitHub の要約)に Binance・Bybit は無く、「currently for Binance Futures and Bybit」は PyPI JSON の description(curl、生ログ 4 行目)にあった(生ログ S 節)。2 箇所とも出典を PyPI JSON に付け替え、W-9 に取引所の記載が無いことも書いた。
2. **直した(同上)。** 「Identical strategy implementations…」は PyPI JSON の description にあり(生ログ 5 行目・S 節)、W-11 は「バックテストと本番運用で同じコードが動作します」と要約している。出典を付け替えた。
3. **直した。** 「起点の答え」を「照合したのは数値と固有名詞の一致で、文章の主張は照合していない。数値・固有名詞の範囲では記憶で埋めたものは 0 件」に改めた。1・2 がその範囲外で見つかった実例。
4. **答えた: なっていなかった。直した。** W-23・W-24 の検索結果の先頭 900 文字と、語(49.99・ZalgoNet・JesseGPT・MIT)の前後を生ログに追記した。
5. **直した。** 時間の自己申告「約 20 分相当」も根拠がない(時計を持たず道具呼び出しの回数から推定)こと、実測 650 秒との乖離 1.85 倍で方向がトークンと逆(多く見積もった)ことを書き、次回から時間もリードが計測値を書くとした。
6. **直した(監査役が正しい)。** PyPI JSON の `license` = LGPL-3.0-or-later と classifier = LGPLv3+(生ログ N 節)、README の要約 = LGPL-3.0-only の食い違いを本文のライセンス欄に明記し、LICENSE の原文は未取得(未確認)として次回の裏取り項目 10 に載せた。
7. **答えた: 逸脱に当たる。定義を足した。** 報告の見出し直後に「未検算 = 取得はしたが要約止まりで原文と突き合わせていない(委任文の印では推定)/ 未確認 = 取得していない / 実測 = この環境で打ったコマンドの出力」の定義を書いた。語は残す(取得したかどうかの区別に要る)。
8. **答えた: 生ログに無かった。直した。** scratchpad の venv で `pip freeze` を打ち、43 件の全一覧を生ログ D 節に載せた(matplotlib 3.11.2・holoviews 1.23.2 は依存として入っている)。本文の「46」は根拠不明だったので 43 に訂正した。

## 3 回目(v3、コミット 5385619)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/SCAN_2026-09-21_tools.md:133 — 知見表の hftbacktest 行(対応取引所)で、印列は「一次資料(README Key Features 最終項)」のままだが、同じ行の説明文自体が「PyPI JSON の description(curl、生ログ4行目・S節)。GitHub の要約 W-9 には取引所の記載なし」と明記しており、印列と説明文が同一行内で矛盾している。docs/AUDITOR/VERDICTS/2026-09-21_tools_scan_cat1.md:83(処置1)は「2箇所とも出典をPyPI JSONに付け替えた」と報告しているが、この印列は付け替わっていない。1・2回目に指摘・処置したはずの出典の誤表示(P1裏返し、CLAUDE.md §0.2 O-3)が、処置済みと自己申告された箇所に残っている。
>
> 2. [止める] docs/DATA/SCAN_2026-09-21_tools.md:225 — NautilusTrader「危険」節の冒頭「自動発注機能: 有り、とREADMEの記載から読める」が、直後の括弧内で「この引用文は PyPI JSON の description にあり…GitHub の要約 W-11 は別の文で要約している」と自己否定している。安全性判定の根拠を示す一文の主語(README)と、実際の出典(PyPI JSON)が矛盾したまま残っている。VERDICTS...:84(処置2)は「出典を付け替えた」とするが、冒頭句は書き換えられていない。#1 と同型の未完了修正。
>
> 3. [直す] docs/DATA/SCAN_2026-09-21_tools.md:178 — 処置1で新たに挿入された「PyPI JSON の description = README と同文」という等価性の主張は、生ログのどこにも裏づけ(diff・突き合わせの記録)が無い。curl(生JSON)と WebFetch(要約)を同一視する根拠が示されないまま、出典の信頼性を底上げする方向の新規記述になっている(P1裏返し)。
>
> 4. [直す] docs/DATA/SCAN_2026-09-21_tools.md:136 — freqtrade の知見表の同一行内で、本文は「検索結果の要約(未検算の二次情報)」、含意欄は「本節では『未検算』として扱う」と書く一方、印列だけが「推定(検索結果の要約。一次資料の逐語ページには未到達)」となっている。同一の事実に対し同一行で2つの異なる分類語が使われている(P2)。
>
> 5. [直す] docs/DATA/SCAN_2026-09-21_tools.md:139 — Jesse の知見表でも同型: 本文は「…禁止と検索結果に出た(未検算)」、印列は「一次資料(PyPI license classifier)+推定(pricing ページの検索結果要約、逐語未確認)」。同一セル内の料金主張に対し「未検算」と「推定」が並記され、どちらが正式な分類か本文から判定できない。
>
> 6. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:10 — 2回目監査の指摘7を受けて追加した「未検算」の定義は「委任文の印では『推定』に当たる」と明記し、実質的に「未検算 = 推定」としている。しかし本文は上記4・5のように、同じ根拠(WebFetch/WebSearchの要約止まり)を持つ主張の一部を「推定」、一部を「未検算」と使い分けており、両者の使い分けの基準が本文のどこにも説明されていない。委任文§4が指定する語彙(一次資料/実測/推定/仮定/未確認)からの逸脱(監査2回目指摘7)への処置として、定義を足しただけで使用箇所の整理はされていない状態だが、これで足りるか。
>
> 7. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:150-161, 283-292 — 「浅い」候補(Backtesting.py・zipline-reloaded・QSTrader・Lean CLI・PyAlgoTrade・Qlib・VnPy・Jesse等)のライセンス・版・最終更新日は「一次資料(PyPI classifiers)」と印付けられているが、対応する生ログ(20260921_tools_1.log:18-26 のコマンド、33-41 の出力)は委任文§5-5の仕様どおり先頭200文字のみを記録しており、その200文字の中に license classifier・version・upload_time の値は含まれていない(例: 33行目 backtesting の出力は "Intended Audience :: Science/Rese" で切れ、AGPL-3.0 の文字列は無い)。深掘り済み4件ではS節・N節・D節でリードが再照合しているのに対し、この8件は同様の裏取りが無いまま「一次資料」の印で確定的な数値として表に載っている。委任文の仕様(head200)を満たしていれば足りるという整理でよいか、それともこの8件も次回の裏取り対象に加えるべきか。
>
> 他 0 件省略(該当なし、上記が全て)。
>
> 補足(監査の射程外だが参考): この文書は事前登録(prereg)を名乗っていないため、jev_design の判定表の要求(P-xx項目9)は適用対象外と判断し、検査していない。round2の8件の処置のうち5・8(価格の一次資料取り直し、pip freeze全件記載)は本文と生ログの対応が取れており、追加の指摘は無かった。round1の「判定語」禁止(委任文§0)の再侵犯も無かった(grep で確認)。

## リードの処置(v4)。1・2 は直した箇所の隣の語を直し残した(2 回目と同じ型)ので、今回は「未検算」「README」の全出現を grep で洗ってから直した。

1. **直した(監査役が正しい)。** 印列を「一次資料(PyPI JSON の description の Key Features 最終項、curl = 生ログ 4 行目・S 節)」に。132 行の印列(PyPI description・GitHub README)も同型だったので直した。
2. **直した(同上)。** 冒頭句を「PyPI JSON の description(README 相当の本文)の記載から読める」に。「ソースは見ていない = 未検算」は取得していないので「未確認」に改めた。
3. **直した。** 「= README と同文」を消し、「README 相当の本文。GitHub 上の README 原文との一致は未確認」に。NautilusTrader・freqtrade の「できること全部」の出典も「GitHub README」から「PyPI JSON の description(curl、生ログ 5・7 行目)。GitHub の WebFetch は要約のみ」に改めた(逐語は description から取ったもので、README 原文は取得していない)。
4. **直した。** 印列を「推定 = 未検算(W-22)」に統一。
5. **直した。** 本文・印列とも「推定 = 未検算(W-23)」に。
6. **答えた: 足りていなかった。** 定義を「印は委任文の 5 種だけ。未検算は推定の下位区分で、必ず『推定 = 未検算(取得の番号)』の形で書く」に改め、全出現(grep で 10 箇所)をその形に揃えた。
7. **答えた: 足りない。次回に回さず、リードが 9 件全部を打ち直した(生ログ P 節)。** 結果、**QSTrader(MIT)と Lean(Apache)のライセンス欄が「classifiers 無し」と書かれていたのは誤り**で、表を訂正した。他 7 件は一致。Jesse は取得(15:44Z)後の 16:01Z に 3.2.1 が出ていたので注記した。freqtrade の供給網の一致も PyPI JSON の project_urls で再確認(生ログ Q)。

## 4 回目(v4、コミット d35f113)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/SCAN_2026-09-21_tools.md:134 — 知見表の NautilusTrader 行の印列「一次資料(PyPI description・GitHub)」は、同じ行の本文「対応先は Binance/Bybit/OKX/Coinbase/Kraken/IB 等で、bitFlyer は一次資料の対応表に見当たらない」に対して不正確。この取引所一覧(Binance/BitMEX/Bybit/Coinbase/Kraken/OKX/Derive/dYdX/Hyperliquid/Lighter/Interactive Brokers/AX Exchange/Betfair/Polymarket)は本文中の同ツール個別表(213行)では出所を「README のアダプタ一覧の WebFetch の要約、生ログ W-11」と明記しているのに対し、208行に逐語引用された PyPI JSON の description(Key Features)には個別取引所名は一切含まれていない。つまり「一次資料の対応表に見当たらない」という文は、実際には一次資料ではなく WebFetch 要約(W-11)にしか存在しない対応表を指している。VERDICTS v4 処置1のノート(「132 行の印列(PyPI description・GitHub)も同型だったので直した」)は hftbacktest の132行だけを直し、同型のはずのこの134行(NautilusTrader)には触れていない。「直した箇所の隣の直し残し」がラウンド3までと同型で今回も再発している(P1裏返し、CLAUDE.md §0.2 O-3)。
>
> 2. [止める] docs/DATA/SCAN_2026-09-21_tools.md:135 — 知見表の vectorbt 行「無料版に無い機能(並列化・ポートフォリオ最適化・パターン認識・指値注文サポート等)が PRO 限定」は、印列で「一次資料(vectorbt.pro/become-a-member 引用)+ X投稿(使用報告)」とされているが、本文中の同ツール節(242行)ではこの機能一覧の出所を「推定 = 未検算(検索結果の要約 W-18)」と明記しており、become-a-member ページ(W-19、リード取り直し L-1)には並列化・パターン認識等の個別機能名は含まれていない。加えて同じ行の「100万戦略を1秒」という活動主張には、URL・生ログ行・W番号のいずれの出典表示も無い(文書全体を検索しても他に一致箇所なし)。1と同型の、知見表と本文の出典不一致。
>
> 3. [止める] `未検算` の全出現(`grep -n 未検算`)— 冒頭(10行)で新設した定義「本文では必ず『推定 = 未検算(取得したものの番号)』の形で書く」に対し、実際の出現は不統一。317行「現状は WebFetch の要約止まり(推定 = 未検算)」は番号が無い。237行「ライセンス文言(…W-13)は原文未取得のまま = 推定 = 未検算」も、「未検算」の直後に番号が付いていない(W-13 は文中の別語「ライセンス文言」に係る)。136・139・161・232・242・244・292行はいずれも「(推定 = 未検算、W-XX)」という、外側の括弧がフレーズ全体を包みコンマで番号を続ける形で、定義文が指定する「推定 = 未検算(W-XX)」(括弧が「未検算」に直接係る形)とは異なる。VERDICTS v4 の処置6は「全出現(grep で 10 箇所)をその形に揃えた」と報告しているが、実際に grep すると統一されていない箇所が複数残っており、この完了の主張自体が本文と食い違う(O-2/O-3 型の断定)。
>
> 4. [直す] docs/DATA/SCAN_2026-09-21_tools.md:178, 225 — round3 指摘3への処置(「PyPI JSON の description = README と同文」→「README 相当の本文」への言い換え)は、178行では直後に「GitHub 上の README 原文との一致は未確認」という留保が付くが、225行の同じフレーズ「PyPI JSON の description(README 相当の本文)」にはこの留保が付いていない(節末尾の「ソースは見ていない = 未確認」はソースコード未検証の話であり、README一致の話ではない)。同じ言い換えフレーズへの留保の付け方が箇所によって揃っていない。また「相当」という語自体、一致が未確認のままの等価性主張である点は round3 指摘3の懸念が完全には解消されていない可能性がある(P1裏返し)。
>
> 5. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:210 — NautilusTrader のライセンス食い違い(PyPI = LGPL-3.0-or-later / GitHub README 要約 = LGPL-3.0-only)は次回の裏取り項目10に回されているが、ライセンス条件の違い(or-later と only)は法務確認に直結しうる情報であり、他の「浅い」候補のライセンス確認(round3 指摘7、P節で今回リードが優先して打ち直した)と同程度の優先度で今回中に一次資料(LICENSE原文)を取るべきではなかったか、次回送りの基準は何か。
>
> 検査した対象: `docs/DATA/SCAN_2026-09-21_tools.md`(326行、v4)、`docs/DATA/probes/20260921_tools_1.log`(153行、P・Q節含む)、`docs/AUDITOR/VERDICTS/2026-09-21_tools_scan_cat1.md`(1〜4回目の記録全文)。round3 の7件のうち5(推定/未検算の統一)・6(定義追加)・7(P節打ち直し)の処置自体は生ログとの対応が取れていることを確認した(finding 3 は「統一した」という完了の主張が実際のgrep結果と食い違う点を指摘するもので、処置6で足した定義文そのものの妥当性は問題視していない)。他に指摘なし。

## リードの処置(v5)。4 回続けて「直した箇所の隣」を直し残した。今回は手で 1 箇所ずつ直す方法をやめ、(a) 知見表の印列は本文の個別表の出典と突き合わせて書き直し、(b) 「未検算」は定義のほうを実際の書き方に合わせたうえで、定義行以外の全出現が「推定 = 未検算、W-n」の形であることを python の検査(行ごとの出現数 = 正規形の出現数)で通してから保存した。(c) 「README 相当」は全出現を消した(grep で 0 件を確認)。

1. **直した(監査役が正しい)。** NautilusTrader の知見行: 本文を「その一覧に無い(一覧は GitHub の要約にしか無く、PyPI JSON の description に個別の取引所名は無い)」に、印列を「一次資料(同一コードパス = PyPI JSON の description)+ 推定 = 未検算、W-11(対応先の一覧)」に。
2. **直した。** vectorbt の知見行: PRO 限定機能の一覧は「推定 = 未検算、W-18」、料金は「一次資料(become-a-member の原文、L-1)」、X は W-17 → x_fetch と分けた。「100万戦略を1秒」は PyPI の description にも W-13 にも無く出所不明なので削除(生ログ V)。
3. **直した(監査役が正しい。「揃えた」は誤りだった)。** 定義を実際の多数形「推定 = 未検算、W-n」に改め、外れていた 5 箇所(136 印列・136 含意・139 印列・158・237・242・317)を直し、機械検査で定義行以外の全出現(11 箇所)が正規形であることを確認してから保存した。
4. **直した。** 「README 相当」を 4 箇所とも消し、「PyPI JSON の description。GitHub の README 原文は未取得」に統一した(等価性の主張をしない)。
5. **答えた: 基準は無かった。今取った。** `raw.githubusercontent.com/nautechsystems/nautilus_trader/develop/LICENSE`(200、7,651 バイト、生ログ N2)は LGPL v3 の定型本文そのもので、「以降」の宣言は定型文には無い。「v3 以降」は PyPI のメタデータ、「v3 のみ」は README の要約にだけある。食い違いは LICENSE では決着せず、README 原文とソースのヘッダで確かめる(次回項目 10 をそう書き換えた)。

## 5 回目(v5、コミット cb2f550)— 監査役の出力(逐語)

> 1. [止める] docs/DATA/SCAN_2026-09-21_tools.md:136 と :265 — 知見表の freqtrade 行の印列は「推定 = 未検算、W-22(検索結果の要約。**一次資料の逐語ページには未到達**)」だが、同じ知見の一部である「公式 exchanges ページに bitFlyer/bitbank/GMOコインの個別記載はなく」という主張は、本文の個別表(265行)で「リードが原文を `curl` で取り直し、bitflyer/bitbank/gmo の語が 0 件…であることを確認 = 生ログ L-2」と、まさに一次資料(原文ページの curl、`docs/AUDITOR/PRINCIPLES.md` の定義=定義行10「一次資料 = 原文ページの curl から取った値」に合致)に到達済みと明記されている。印列の「一次資料の逐語ページには未到達」は本文と矛盾する。3回目・4回目監査はこの同型の欠陥をNautilusTrader行(134)・vectorbt行(135)で見つけてリードが「+」で複数出典を正しく分けて直したが(v5処置1・2)、隣接する freqtrade 行(136)には同じ処置が及んでいない。「直した箇所の隣の直し残し」がラウンド4回連続の型のまま今回も再発している(P1裏返し、CLAUDE.md §0.2 O-3)。
>
> 2. [直す] docs/DATA/SCAN_2026-09-21_tools.md:317 — 冒頭の定義(10行目)は「未検算」の書式を「推定 = 未検算、W-n」(n は単一の取得番号)と定めているが、317行は「推定 = 未検算、W-13・W-18」と2つの番号を「・」で連結している。VERDICTS v4→v5処置3は「機械検査で定義行以外の全出現(11箇所)が正規形であることを確認してから保存した」と報告しているが、実際に確認すると、その検査は「行ごとの『未検算』の出現数」と「行ごとの『推定 = 未検算、W-\d+』の出現数」を数えて一致を見るだけの緩い検査で(実際に python で再現して確認済み)、317行のような複数番号の連結は正規表現の先頭一致で「1件」とカウントされ通過してしまう。「全出現が正規形」という完了の主張は、この検査の精度が保証する範囲を超えている。
>
> 3. [直す] docs/DATA/SCAN_2026-09-21_tools.md:58-60 と 委任文 `docs/DATA/delegations/20260921_tools_survey_prompt.md` §8 — 委任文は「`python3 scripts/tools_inventory.py` の出力を**省略なしで**」「出力の全文を `SCAN_2026-09-21_tools.md` の担当節の冒頭に貼る」と明記し、その理由として「手書きの列挙は監査で3回直して3回漏れた」という履歴を挙げている。しかし本文58-60行は「本節は行数の都合で scripts/ の内訳のみ集計形で圧縮した」と、要求された全文貼り付けをせず集計形に置き換えたことを自認している。委任文が全文を要求する根拠(手書き・要約は過去に繰り返し省略を生んだ)と、今回の「行数の都合」という圧縮理由が正面から衝突していないか。
>
> 4. [聞く] docs/DATA/SCAN_2026-09-21_tools.md:132 — hftbacktest 知見表1行目の印列「一次資料(PyPI JSON の description、curl = 生ログ 4 行目。**GitHub は要約 W-9**)」は、queue position・feed/order latency というこの行の主張に対して、W-9(生ログ`20260921_tools_1.log:91`)の実際の中身がスター数・フォーク数・ライセンス・コミット数のみで queue position 等への言及が無いことと照らすと、「GitHub は要約 W-9」という並記が何を裏づけているのか本文から読み取れない。単なる補足(GitHub側はこの主張の裏づけではないという注記)なら、134・135行のように「+」で明確に区別した書き方に揃えるべきではないか。
>
> 他 0 件省略(該当なし、上記が全て)。
>
> 参考(指摘外・確認したが問題なし): 4回目の指摘1・2(NautilusTrader行134、vectorbt行135の知見表印列と本文出典の不一致)は本文と照合した結果、正しく修正されている。「README 相当」は grep で0件を確認、消去は完了している。NautilusTrader のライセンス食い違い(LGPL-3.0-or-later 対 -only)は LICENSE原文取得(生ログN2)で決着しなかったことが本文にも次回項目10にも正直に記載されており、新たな矛盾は見当たらない。`docs/DATA.md` はこのツール調査と直接の対応が無く(grep 0件)、照合対象の記載なし。判定語(使える/使えない/不要/今の環境以下/採用)の混入も grep で確認したが再発なし。

## リードの処置(v6)

1. **直した(監査役が正しい。5 回連続の同型)。** freqtrade の知見行の印列を「一次資料(exchanges ページの原文 = 生ログ L-2、国内 3 社の語 0 件)+ 推定 = 未検算、W-22(bitFlyer が動作しない理由)」に分けた。
2. **直した(監査役が正しい。検査が緩かった)。** 317 行を「ライセンス文言は推定 = 未検算、W-13。機能差は推定 = 未検算、W-18」に分け、検査を「正規形を取り除いた残りに『未検算』が残らない、かつ正規形の直後に『・W』『、W』が続かない」に強めて通した。
3. **直した(委任文 §8 違反を見落としていた)。** 調査班の圧縮版(集計形)を、リードが打ち直した `python3 scripts/tools_inventory.py` の全文 83 行に置き換えた(調査班の HEAD f203ce2 の圧縮版は git 履歴 cb2f550 まで)。
4. **答えた: 裏づけではない補足だった。消した。** hftbacktest 行の印列を「一次資料(PyPI JSON の description、curl = 生ログ 4 行目)」だけにした。
