# 検収: 道具サーベイ 区分 8 の 20 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 20 回目の実行(2026-09-26)` / 生ログ `docs/DATA/probes/20260923_tools_8_run20.log`(68,953 バイト)。届いたままのコミット f654ba6。起動文の印 `20260923_tools_survey_cat8_run20_prompt.md@b11a105664ae`。

## 0. 費用と時間

調査班 554,743 トークン・2,832,160 ms(47.2 分)・道具 234 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 20` 0 件 / `check "" run20.log` 0 件 / `check_scan_report.py`(run1〜run20)39 件。39 件の行番号の最大は 36881 で、20 回目の節(37195 行目から)には 1 件も無い。`git diff -U0 HEAD`(届く前の HEAD との比較)で過去の節から消えた行は 0 件。

## 2. リードが見た点

- E1a・E2 の逐語を生ログ(137〜165 行・190〜195 行)で確かめた。調査班の引用と一致。
- E3a の検索の手(生ログ 66〜83 行)は、500 行の見積もりの手(合計 8)のあとに `cat8_search.py` を打っている。語は起動文 §2.4 の E3a の組を全部含む(監査 100 回目も確かめた)。
- 500 行を超えて `未判別` にした要素は無い。

## 3. 台帳

`cat8_ledger.py import --round 20`: 8-038 の E1a `印`/3・E2 `印`/3・E3a `なし`/-、状態 `浅い` → `深掘り`。ほかの 40 行は「最後の記載の回」と行番号だけが変わった。`check` 0 件。**深掘り 27 / 残り 10**(OpenClaw・MetaTrader・大きいリポジトリ 8 つ)。コミット 7529bc8。

## 4. 中身の監査(100 回目、owner-auditor)の逐語

> 監査 100 回目の結果を返す。対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 20 回目の実行(2026-09-26)`(37195〜37778行目)、`docs/DATA/probes/20260923_tools_8_run20.log`。
>
> 指摘(番号・severity・file:line・問い):
>
> 1. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:37221`(知見#3)・`37580`(要素と段表)・`37690`(判断に迷った点1) — NinjaTrader の E1a「印」・段3 の根拠(`syncing_account_positions.htm`「Account Position(参照値)とStrategy Position(実装の計算結果)を自動で比較し、不一致なら成行注文で埋める」)は、実運用中に本物の注文を自動発注してポジションを是正する機能である。設計票 `docs/DATA/surveys/CAT8_DESIGN.md:49` の述語自体は文脈を明示的に限定していないが、18回目検収の最終処置(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:73-77`)は「述語の括弧の例はどれも『同じものを出すはずの2つ』を照らし合わせるもので、E1a は『別実装の再現ゲート』(=正しさの検証)の在処を引く列」という趣旨に基づく読みで AlgoNetwork を確定させた。区分8自体の見出しも「検証・再現・品質」である。今回の対象は比較する2つの性質(実装と参照値)では E1a に近いが、比較の目的が「正しさの検証」ではなく「ライブ執行中の自動リスク是正」であり、当プロジェクト自身が `OrderStateUnknown` を自動リトライしない(CLAUDE.md §1)という正反対の思想を持つ。リードが「述語に文脈の限定が無いので当たる」と読む方針は、18回目が確立した趣旨に基づく読みから後退していないか、区分8の趣旨を捉えているかを問う。段の付け方自体(段4の逐語が無いため段3)には機械的な不備は無い。
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:37234`(候補の一覧#16)・`37655`(要素の表)・`37680-37681`(§4.0未確認項目) — 3箇所とも「『動かせない』とは書かない」と明記しつつ、直前に「この環境では起動できない」という同義の断定を書いている。起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:56`(§2.5)の指示と自己矛盾。範囲・未試行手段(Wine)は併記されているため実害は限定的。
>
> 3. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:37656`・`37692` — 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run20_prompt.md:20`(§1)は範囲を「E1a・E2・E3a と§4.0の15項目」に限定するが、「最小実行の中身」は17回目時点で既に一次資料(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:34156`)であり15項目に含まれないのに、今回の登録調査で得た情報で値を上書きしている。報告自身が問い3として提起済みで未解決。
>
> 4. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:37221`・`37222` — 設計票 `docs/DATA/surveys/CAT8_DESIGN.md:97-98`(§4.2)は印が付いた要素ごとに「突き合わせの単位・許容誤差を指定できるか」(E1a)、「検出する異常の種類・直すか報告だけか」(E2)を1回目から記録するよう義務付けている。過去の全候補(qf-lib・Backtrader・Lean・FinanceToolkit・Qlib等)はこれを個別行で記録しているが、今回初めて印が確定した NinjaTrader の E1a・E2 にはこの記録が一切無い。`cat8_ledger.py check-elements`/`check` は形式のみを見るためこの欠落を検出しない。
>
> 5. (参考、該当なし) 40道具の「台帳の値のまま(19回目の節)」行に19回目本文の複写は見当たらなかった。E3aの検索語(`docs/DATA/probes/20260923_tools_8_run20.log:67`)は起動文§2.4の10語すべてを含み、甲の表の判定も生ログと一致。4つの受け入れ検査を独立再実行し、報告の貼付出力と一致することを確認した(`cat8_ledger.py check-elements/check` 0件、`check_scan_report.py` 39件=既知の誤検出、`git diff` 0行)。
>
> 止める指摘: なし。

## 5. 処置(リード、監査 100 回目)

1. **`印` のままにする(リードが述語と例で決めた。オーナーには聞かない。18 回目の検収 §7 と同じ決め方)**。18 回目に置いた読みは「例はどれも、**同じものを出すはずの 2 つ**を照らし合わせる」で、戦略どうしの成績の比較(違う結果を出して当然の 2 つ)をそれで外した。NinjaTrader の比較の 2 つは、戦略が計算した建玉と口座の実際の建玉で、**一致するはずのもの**(食い違いは誤りを意味する)。これは例の「実装と参照値」「別の道具の結果の取り込みと比較」の形に当たり、18 回目の読みから後退しない。述語は目的(検証か執行か)で限定していない。**当方が取り込むかどうか**(自動で成行を出して合わせる振る舞いは、当方の「曖昧な失敗は自動で再送しない」とは逆)は分類ではなく導入の判断の問題なので、ここでは値を変えず、この段落を資源化のときの索引の注記に写す。段 3 はそのまま(突き合わせの対象はその道具の戦略と接続先の口座だけ)。比較は起動時の 1 回だけという限定を §4.2 の軸に足した(処置 4)。
2. **直した**。3 か所の「この環境では起動できない」を「この環境(Linux)で起動する手は打っていない」に書き換えた。
3. **受け取る(範囲の外の書き込みとして記録する)**。17 回目の値は「登録が要る(渡すもの: 未確認。…)」で、欄の中に未確認が残っていた。この回の登録の要否の調査(15 項目の 1 つ)で、登録フォームの逐語(生ログ 223〜311 行)から同じ欄の未確認が埋まった。新しい問いを立てたのではなく、範囲内の調査で得た一次資料の転記なので、戻さない。
4. **直した(リードが足した)**。報告節の末尾に「リードの追記(検収、監査 100 回目の指摘 4)」を足した。E1a = 単位は建玉(多銘柄の戦略は銘柄ごと)/ 許容誤差を指定する記述はこの頁に無い(他の頁は未確認)/ 比較は起動時の 1 回だけ。E2 = 種類は外れ値 / 直す(捨てる)/ (ア) 基準(%)は利用者が指定できる。全部、調査班が取った本文の逐語から。

直したあとも `check-elements --round 20` 0 件 / `check` 0 件 / `check_scan_report.py` 39 件(変わらず)。

## 6. これからの手

残り 10: 8-026 OpenClaw の残り / 8-029 MetaTrader の E1a(当たり 1,572 行、その要素だけの回)/ 8-030〜035・039・040 の大きいリポジトリ 8 つ。1 回を小さく、起動文はそれだけで読める形で作る。

## 7. リードの実測(21 回目の前、2026-09-26): 大きいリポジトリで `なし` に届くか

8-026 OpenClaw のリポジトリ(16 回目に `venvs/8-026/openclaw` に取ったもの。810 MB)で、起動文 §2.4 の要素ごとの語の組を全ファイルに当てた当たりの行の数(`grep -rIci -E '<語の組>' . | awk -F: '{s+=$NF} END{print s}'`、この環境で打った):

| 要素 | リポジトリ全体 | `docs/` だけ |
|---|---|---|
| E1a | 42,161 | 2,545 |
| E1b | 35,611 | 未測 |
| E2 | 233,208 | 未測 |
| E3a | 2,037 | 133 |
| E3b | 469 | 未測 |
| E4 | 213,009 | 7,038 |
| E6 | 496,179 | 未測 |

1 回 500 行まで・当たりは 1 行ずつ判定、という今の手順では、E3b を除いて `なし` に届かない(E1a だけで 85 回分)。`docs/` に絞っても E1a・E4 は 500 を超える。残り 10 のうち 8-026 と大きいリポジトリ 8 つ(8-030〜035・039・040)に同じことが起きる見込み(8 つはまだ測っていない = 推定)。区分 8 の調査の完了の述語(設計票 §2「残り = 0 行」)に届かない分岐なので、オーナーに聞く(CLAUDE.md §0.1「作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる」)。`印` を先に探す回は、どの答えでも要るので進める。
