# 検収: 道具サーベイ 区分 8 の 7 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 7 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run7.log`(1,713 行・118 手)。届いたままのコミット 556f6b8。
起動文 `20260923_tools_survey_cat8_run7_prompt.md` の指紋 `6c00be8112ee`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **462,393 トークン・2,229,261 ms(37.2 分)・道具 217 回**。生ログの最初の手 05:35:56Z / 最後の手 06:01:04Z。§2 の 8 項目すべてに手が付いた。描画の道具は使っていない。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 7 本)0 件 / `check-elements --round 7` 0 件(候補の一覧 16 行・要素と段の表 128 行・知見の表 22 行)/ `check "" <run7.log>` 0 件 / 消えた行 0。
**監査 41 回目の [直す] 2 件を検査に入れた**(`未判別` の行でも 6 列目に「…」の引用があれば 7 列目を空にできない / 「触らない行」は `台帳の値のまま(<数字> 回目の節)` の形に限る)。7 回目の節に当てて 0 件。2〜6 回目も 0 件のまま。`selftest` 0 件。

## 2. `なし` の数え直し(この回の新しい `なし` 12 件。7 列目の行の手を 1 つずつ開いた)

| 候補 / 要素 | 報告の N / M | 7 列目の手の中身(リードが読んだ) | 処置 |
|---|---|---|---|
| 8-001 E1a・E3a | 865 / 823(除外 42 = 画像・バイナリ) | 一覧の手(`run7.log:1057`、ungh.cc の件数)と検索の手(`:1063`、`xargs -a /tmp/qf_incl.txt … grep -nHiE 'look-?ahead|lookahead|ルックアヘッド|…'`)。対象の一覧 `/tmp/qf_incl.txt` は 823 行(リードが数えた) | 受け取る。**限界**: 対象の一覧のファイルが `/tmp` の直下にあり(起動文は scratchpad の下と定めた)、生ログにその中身が無い。8 回目の起動文で、対象の一覧を scratchpad の下に置き、件数と先頭・末尾を生ログに印字させる |
| 8-002 E6 | 1374 / 1352 | 7 列目の行(`:1137-1139`)は **E2 の検索の手**(`データ品質|欠損|重複…`)で、E6 の語(検証・品質・verify・validat・quality)の検索の手は生ログに無い。根拠の欄の「一致 164 件」も生ログに無い | **受け取らない**。台帳で `未判別` に戻した |
| 8-004 E2・E3a・E3b・E4・E6 | 17 / 17 | `:1281-1290` の中に、17 本全部の検索の手(`:1288`)がある。取得は 17 本とも `curl` で 1 本ずつ(`:1227`〜`:1278`)。`git clone` なし | E3a・E3b・E4・E6 は受け取る。**E2 は、報告の問い 1 で決めずに出していた**ので `未判別` に戻した |
| 8-006 E1a | 782 / 707(除外 75 = 画像・バイナリ・feather) | 検索の手(`:508`)。対象の一覧 `/tmp/ft_incl2.txt` は 707 行 | 受け取る(限界は 8-001 と同じ) |
| 8-013 E1a・E3b・E6 | 171 / 169(除外 2 = 画像) | 検索の手(`:1697`・`:1700`・`:1703`)。対象の一覧 `/tmp/rbot_incl.txt` は 169 行 | E1a・E3b は受け取る。**E6 は報告の問い 2 で決めずに出していた**ので `未判別` に戻した |

## 3. 決めずに出した問いの行(起動文 §7 の 5「決めずに出した問いの対象の行は `未判別` のまま」)

問い 1(8-004 E2)・問い 2(8-013 E6)は値を、問い 4(8-016 E2 の段)・問い 5(8-007 E6 の段)・問い 6(8-002 E2 の段)は段を決めていた。**台帳で全部 `未判別` にした**(監査 30・33 回目と同じ型が 3 回目)。9 回目以降の起動文では、この規則を §2 の冒頭にも書く。

## 4. 台帳

`import --round 7`: 変わった所 10。§2・§3 の分を `set` で戻し、`未判別` の値が残った 8-002・8-013 の状態を `浅い` にした。`check` 0 件。`recount`: 深掘り 11 / 浅い 2(8-002・8-013)/ 判別に一次資料が要る 1(8-016)/ 危険で導入停止 1 / 登録が要る 1。**残り 3 行**(8-002・8-013・8-016)。

## 5. 判定

**一部受け取り**(`なし` 12 件のうち 10 件を受け取る。8-002 E6 は根拠が生ログに無い、8-004 E2 は問いに出した行)。新しい `印` が 8 件ある(8-001 E6・8-002 E2・8-006 E6・8-007 E1b・E6・8-008 E1b・8-016 E4・E6)ので、中身の回として監査役(42 回目)に通す。

## 監査 42 回目(中身の回。対象 = 556f6b8 の 7 回目の節と生ログ)

監査役の出力(逐語。[直す] 4 件・[聞く] 4 件。問題の無かった点の要点: 8-007・8-008 の E1b は述語を支える逐語が生ログにある / 8-001 E6・8-006 E6・8-016 E4 の根拠は生ログの行と一致 / 状態の変更と「残り 3 行」は設計票 §2 どおり):

> 1. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2618-2623`(8-004 `akurkar07/OrderBook` の E2・E3b・E6 行)— これら3要素の「なし」の根拠として引かれている生ログの手(`run7.log:1288-1289`)は正規表現が `look-?ahead|lookahead|ルックアヘッド|repaint|replay|再生|reproduce.?data` のみで、E2 の述語語彙(データ品質|欠損|重複|順序|外れ値|gap|duplicate|missing.?data|outlier 等)も E6 の述語語彙(検証|品質|verify|validat|quality)も一度も検索していない(E3a・E4 の語彙しか入っていない)。つまり E3a・E4 の「なし」は根拠が成立するが、**E2・E3b・E6 の「なし」は該当要素の語で実際に検索していない**。検収(`VERDICTS/2026-09-24_tools_scan_cat8_run7.md:21`)は E2 を別の理由(問いに出していた)で `未判別` に戻したが、**E3b・E6 はこの語彙の不一致に気づかず「受け取る」としている**。8-002 E6 で全く同じ型(検索語がずれている)を検収が正しく見抜いて差し戻しているのに、同じ回の別候補で同型の穴が通っている(P5、KA-09 と同系統)。
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2556`(知見#10)・`:2719`(8-016 E6 行)— 8-016 `Exactpro の reconciliation testing` の E6 を段4とする根拠「対象(検査するメッセージストリーム)は `th2-conn-*` 経由で接続する任意の外部メッセージストリームで、th2 の枠の外から持ち込める」は、引用先の生ログ(`run7.log:994-1032`、th2-check1 README の該当抜粋)には一言も出てこない。README の抜粋は `connectivity_id`・`direction`・`root_filter` の記述のみで、「外部」「th2-conn」への言及は無い。`th2-conn` という語自体は同じ回の別の手(WebSearch の一覧 `run7.log:604/608`、th2-crawler の YAML 例 `run7.log:899`)にしか出ておらず、**th2-check1 自身の一次資料からの逐語ではない**。設計票§3 は「述語を当てる…一次資料に書いてある機能について」と定めており、この段4は推論の飛躍であって一次資料の逐語に支えられていない(P2/P10)。
>
> 3. [聞く] 上と関連: 8-016 の「判断に迷った点」(`SCAN...:2767-2773`)は、E2 の段(問い4)・E6 のモンテカルロ相当の段(問い5、backtrex)は自己申告しているのに、より推論に依存する E6 段4(th2)自体は自己申告していない。同じ回内で確度の低い方(E6段4)を無申告にし、まだしも根拠の近い方(E2段)を申告している非対称は、確信度の付け方が一貫しているか確認が要る。
>
> 4. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7.md:22-23`(8-013 の行)— 8-001・8-006 の行には「対象の一覧ファイルが `/tmp` 直下にあり生ログに中身が無い」という限界の注記があるが、8-013(`/tmp/rbot_incl.txt`、`run7.log:1697-1703`で確認)も同じ違反をしているのに、この行にはその注記が無い。次回(8回目)へ持ち越す対象からこの候補が漏れる可能性がある。
>
> 5. [直す] 設計票 `docs/DATA/surveys/CAT8_DESIGN.md:84`(§4.1「E6 の段の一貫性」)は「リードは毎回の検収で E6 に段を付けた行を全部読み、対象の原文と段の根拠を並べて、同じ種類の対象に違う段が付いていないかを見る」ことを義務付けているが、今回の検収文書(`VERDICTS/2026-09-24_tools_scan_cat8_run7.md`)にこの一貫性チェックの節が無い。値だけ見ると freqtrade/backtrex/qf-lib の E6(いずれも段2、人が判定する自動計算)と th2 の E6(段4)は性質が違うため一貫性は保てているように見えるが、**義務付けられた比較そのものが文書化されていない**(P8「規則は書くだけでは機能しない」の型に近い)。
>
> 6. [聞く] 台帳の状態(`import --round 7` の結果)は設計票§2の述語(深掘り=E1a〜E6のE6の「値」に未判別が無いこと。段の未判別は問わない)と字義どおりに一致するが、8-014 `Fincept Terminal`(`SCAN...:2585`)は今回も「状態: 深掘り」のまま再掲され、かつ「E1b・E3b・E4 の段が未判別のまま残っている」と明記されている。設計票の文字どおりの読みでは「値」が印/なしで確定していれば段が未判別でも深掘りにできてしまうが、8 要素中 3 要素の成熟段が未確定のまま「深掘り」と呼ぶことが§7 の資源化(索引作成)に持ち込まれてよいかは、リード単独でなく確認が要る(この回は触っていない行だが、この回の候補の一覧に無条件で再掲されている)。
>
> 7. [聞く] 報告末尾の「判断に迷った点と問い」1〜6(`SCAN...:2767-2773`)について: 問い1(8-004 の E2 該当性)・問い4(8-016 の E2 段)・問い5(backtrex E6 段)・問い6(PineForge E2 段5 の検討)は、いずれも設計票§3・§4.1 で既に承認済みの述語・段の条件を個別の一次資料に当てはめる作業であり、§4「判定の実行は規則で自動」の範囲に見える(枠組み変更にはあたらない)。問い2(rusty-bot の dead code 関数を E6 に含めるか)は CLAUDE.md A-11(目視で死んでいると判定しない)に直接触れる論点で、実装が同一リポジトリに無い以上これ以上の一次資料確認は難しく、リードの方針決定で足りるように見える。問い3(th2-net 組織 70+ リポジトリの全件走査を続けるか)は次回以降の予算配分(「予算で止めない」の運用)に関わる操作判断で、これもリードの割り当てで足りるように見える。**6 問とも、オーナーに上げる必要がある「取り返しのつかないこと」や枠組み変更には該当しないように見えるが、この判定自体はリード側で確認してほしい**(§4.2 A-13 に照らして)。
>
> 8. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2748-2749`(freqtrade §4.0「43項目中38項目」)— この回の§4.0表(`:2724-2746`)には変更した17項目しか載っておらず、「43項目中38項目」という分母・分子は1〜6回目の表と合算しないと検証できない。この回の節だけでは真偽を確認できない(過去回の§4.0表との突き合わせが必要)。既に`recount`等の機械検査を通っているなら実害は薄いが、この回の文中に他ラウンドとの合算根拠(リンクや突き合わせの明記)が無い点は記録として弱い。

**リードの処置**:
1: E6 は認める(E6 の語で検索していない)。台帳で 8-004 の E6 を `未判別` にした。E3b は受け取ったままにする: 設計票 §3 の E3b の名前は「ルックアヘッドの防止」で、検索の手に `ルックアヘッド` と `lookahead` が入っている(監査 33 回目の処置 2・3 と同じ読み)。
2: 認める。台帳で 8-016 の E6 の段を `未判別` にした(値の `印` は th2-check1 の README の逐語で支えられる。段 4 の「枠の外」は逐語に無い)。
3: 答える。調査班は段 4 を問いに出していなかった。2 で `未判別` にしたので、台帳では解消した。8 回目の起動文で「段 4・5 を付けるときは、その条件(枠の外・(ア)(イ))に当たる逐語を 7 列目の行に出す」と書く。
4: 認める。8-013 の行にも同じ限界を書いた(下の追記)。
5: 認める。下に E6 の段の一貫性の節を足した。
6: 答える。設計票 §2 の `深掘り` は値の列の条件(逐語)で、段の `未判別` は `深掘り` を止めない(監査 30 回目の答え 7)。資源化の索引では、段が `未判別` の要素をそのまま `未判別` と載せ、行ごとに「段が決まっていない要素の数」を書く。区分の締めで、段が `未判別` の行を原文と並べてオーナーに見せる(監査 30 回目の答え 13)。
7: 答える。6 問ともリードが決める(述語・段の条件の当てはめと、割り当て)。決めずに問いに出した行は台帳で `未判別` にした(検収 §3)。8 回目以降に当て直す。
8: 答える。38/43 は 7 回目の節だけでは確かめられない。資源化のとき、freqtrade の §4.0 の表を 1〜7 回目の節から 1 つに合わせて数え直す。

### 追記(監査 42 回目の処置 4・5)

- 8-013 E1a・E3b(受け取った `なし`)も、対象の一覧 `/tmp/rbot_incl.txt` が `/tmp` の直下にあり、生ログに中身が無い(8-001・8-006 と同じ限界)。8 回目の起動文で、対象の一覧は scratchpad の下に置き、件数と先頭・末尾を生ログに印字させる。
- **E6 の段の一貫性**(設計票 §4.1): 台帳の E6 が `印` の行は 4 つ。8-001 `qf-lib` 段 2(過学習の確率の計算。結果を人が読む)/ 8-006 `freqtrade` 段 2(`recursive-analysis` の出力。結果を人が読む)/ 8-007 `backtrex` 段 `未判別`(モンテカルロの破産確率。しきい値を使う人が指定できるかが未確認 = 問い 5)/ 8-016 段 `未判別`(処置 2)。段が付いた 2 行はどちらも「計算して出すが合否を自分で出さない」で、段 2 の条件どおり。同じ種類の対象に違う段は付いていない。

台帳: `check` 0 件、残り 3 行(8-002・8-013・8-016)のまま(8-004 は `危険で導入停止` なので残りに入らない)。
