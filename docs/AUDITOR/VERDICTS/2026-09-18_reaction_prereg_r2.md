# 監査(owner-auditor)— 段 A 事前登録への **2 回目(走行前の再監査)23 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 11(1〜11)/ 直す 6(12〜17)/ 聞く 6(18〜23)= 23 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。
**別に、読みのスクリプトの実装から出た食い違い 4 件(ア〜エ)も処置した**(§3)。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は存在しない)。
**`scripts/o3c_reaction.py` の本体は 1 行も変えていない**(変えたのは PREREG §14.4.1 の**埋め込み**スクリプトだけ)。
**`scripts/o3c_reaction_judge.py` と `tests/test_o3c_reaction_judge.py` には触っていない**(別の委任先が作成。参照のみ)。
**コミットもプッシュもしていない。**

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 23 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r2.md` を新設: 冒頭に監査役の逐語を一字も変えずに貼り、その下に 23 件それぞれの処置(何を・どの行で・なぜ / 変えないなら理由)を書く**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**`REACTION_PREREG_2026-09-18.md` を直す。リードの決定(下)を逐語で本文に入れ、「該当語なし = リードの判断」と明記する**」 |
| 凍結の宣言に改訂を書き足す | リードの委任文面「**凍結の宣言は「2026-09-18 走行前の再監査(2 回目)で改訂。走行前」と書き足す**」 |
| §13「報告する変更」に、黙認された案から変わるものを追加する | リードの委任文面「**§13「報告する変更」の表に、下の決定のうち黙認された案(返答 060 §3)から変わるものを追加する**」 |
| 23 件の処置の中身(各件) | **リードの決定 1〜23 の逐語**(本書の各件に引いた。PREREG §0.1 の 3 つ目の表にも同じ逐語を置いた) |
| **判定区間を開けない** | リードの決定 20「**開けない。判定区間の走行は、この 2 件(+ 上の決定で新たに変わる 3・4・18)をオーナーに報告し、応答を得てから**」 |
| 読みのスクリプトの実装から出た食い違い 4 件(ア〜エ)を同じ改訂に反映する | リードの追加の委任文面「**読みのスクリプト(`scripts/o3c_reaction_judge.py`、完成済み)の実装から出た事前登録との食い違いを、いま処理中の改訂に追加で反映してください(リードの決定つき)**」(ア〜エの逐語は §3) |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |
| `scripts/o3c_reaction.py` の本体を変えない(§14.4.1 の埋め込みだけ変える) | リードの委任文面「**`scripts/o3c_reaction.py` の本体は変えない(§14.4.1 のサニティ用の埋め込みスクリプトだけは下の 8 で変える)**」 |
| `o3c_reaction_judge.py` / `test_o3c_reaction_judge.py` に触らない | リードの委任文面「**その 2 ファイルには触らない(参照はしてよい)**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**23 件 + ア〜エ の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 3 つ目の表(22 行)と 4 つ目の表(4 行)に明記した**(PREREG:94〜135)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(2 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(確定版、1,502 行、25 件処理後)
>
> 以下、`docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(1,502 行)と `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、`scripts/o3c_reaction.py` を突き合わせた結果の問いだけを返す。ファイルには何も書いていない。
>
> 1. [止める] /home/user/trade/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:1243-1255, 996-1017 — §14 は走行 6 本のコマンドしか固定していないが、576 行の表(群分け・三分位の切り出し・実群−対照の差・ブートストラップ t・CI・群ごとの MDE 再計算)を作る道具は §14 のどこにも書かれていない。`scripts/o3c_reaction.py` に `bootstrap` の実装は 1 つも無い(実測: `grep -rn bootstrap scripts/o3c_reaction.py` 一致 0。同語は他の 50 スクリプトにある)。**判定の計算コードを開封後に書く形になっていないか。書くなら、どのファイルで、走行の前に書くのか**(P-3、A-6)
>
> 2. [止める] 同 :964-972 — §9.1 は「反復 **2,000 回**」「**CI の水準は α = 0.05/576 に合わせる**(= 99.99132%)」「区間 = パーセンタイル法」を同時に固定している。パーセンタイル法で両側 99.99132% を取るには下側 4.34e−5 分位が要るが、2,000 反復の最小順序統計量は 5.0e−4 にしか届かない。**この設定で「ブートストラップ CI が 0 を除外」は何を意味するか(全 2,000 本が同符号、と読むのか)。反復数はその読みで足りるか**(P-3)
>
> 3. [止める] 同 :88 vs :1026 — §1 は「**△ 8 件 / ✕ 4 件が残ったまま走るので、段 A の結果はどの行についても「陰性」にはならない**」と書く。一方 §10.1 の 4 分岐は「差の絶対値 ≥ MDE → **差なし**(検出できる検出力で不在を測った)」で、この括弧の中は `CLAUDE.md` §5.2 の陰性の定義そのものである。**同じ文書が、陰性は出ないと書きながら陰性の判定語を表の列に置いていないか**(P-9、P-2)
>
> 4. [止める] 同 :1025-1026 vs :903-908 — §8.6 は「**「欲しい効果」の大きさは置かない**」とし、§10.1 は「**差の絶対値 &lt; MDE → 不明 / ≥ MDE → 差なし**」と、**観測された差**を陰性・不明の境界にしている。観測差が MDE 以上で `|t| &lt; 3.925` のセルを「不在を測った」と書く形になるが、**欲しい効果を置かないまま、陰性と不明の境界を観測差に置くことは `CLAUDE.md` §5.2 の「陰性と不明を分ける」と両立するか**(P-16)
>
> 5. [止める] 同 :927 vs :1125, :1131 — §8.6 は「**主軸 D(`doi_pre_1h`)は減らない = 224/224、欠測 0**(実測)」と書くが、根拠は標本 6 日である。§12 は metrics が清算と重なるのは **375 日**で、うち「**2024-03-04〜06-08 の連続 97 日が丸ごと落ちる**」と書く。`doi_pre_1h` は metrics から作るので、判定区間 456 日での D の被覆は 100% にならないはずである。**456 日のうち D が作れる日数(= zip の突き合わせ)は観測量を出さずに今数えられるのに、数えずに走るのはなぜか**(前回の指摘 14 と同型)(P-1、P-7)
>
> 6. [止める] 同 :915 vs :262 — §8.6 は MDE の `p`・`s` を「**標本 6 日の走行で固定**」とし、§4 は三分位を「**判定区間の実群で … 決め**」「**標本 6 日の切り値は使わない**」とする。すると 44 群の `p`・`s` を標本 6 日から取るとき、**標本側の行をどの切り値で群に分けるか**(標本で切り直すのか、判定区間の切り値を標本に当てるのか)が本書のどこにも無い。**走行後に選べる余地が残っていないか**(A-6、P-3)
>
> 7. [止める] 同 :260-265 — 三分位の規約は「**判定区間の実群で W ごとに分位点(33.33% / 66.67%)を決め、対照にも同じ切り方を当てる**」までで、(a) 同点(境界値)が上下どちらの群に入るか、(b) 軸の値が欠測の行がどの群に入るか(落とすのか第 4 群にするのか)が書かれていない。標本 6 日の実測では B1 `bundle_n_events_dedup` は 224 行中 **128 行(57%)が値 1**、E `implied_leverage` は **34 行が空**、A4 系は **21 行が空**である(実測: 標本 `gap60_w8/table.csv`)。**同点と欠測の規約が無いまま「48 群」を凍結してよいか。B1 の 3 群が 1/3 ずつにならないことは走行前に分かっているが、その形で 576 を数えているか**(P-3、A-6)
>
> 8. [止める] 同 :1358-1372 — §14.4.1 の数え上げは `o3c_reaction` を import して `R.judgment_days(root)` を直接呼ぶ。`check_approval` は `main()` の中(`scripts/o3c_reaction.py:2160`)にしかないので、**この経路は §3 の関門表が要求する `--approval L-NNN` を 1 つも通らない**。**判定区間の日を関門の外から開ける経路を、事前登録が自分で作っていないか**(`CLAUDE.md` §5.0 の関門 1)
>
> 9. [止める] 同 :276-280, :74-76 — 冒頭で「凍結」と宣言した確定版の中に、「**この仮置きはリードが確定させる**」(W に依らない 12 群の出どころ)、「**委任先の仮置き**」(ブートストラップ CI の水準)、「**リードが確定させる**」(#13 を 2 回数える)の 3 件が未確定のまま残っている。**走行の前にこの 3 件は確定したか。確定したなら、その逐語は本書のどこにあるか**(A-6)
>
> 10. [止める] 同 :488 vs :1024 — §7.0 の「F1 と整合」の条件は「**差が正 かつ \|t\| ≥ 3.925 かつ 差 ≥ MDE**」で、§4.3・§10.1 のバーにある「**ブートストラップ CI が 0 を除外**」が入っていない。逆に §10.1 の「差あり」には MDE の条件が無い。**同じ 12 セルに 2 つの別々の合格条件が置かれているが、どちらが F1 の判定か**(P-2)
>
> 11. [止める] 同 :1085, :1338 vs :1440 — §11 の #13 と §14.4 は「**リードが走行の直前に 1 回だけ実行する**」「**下の数え方で 1 回だけ数える**」と書き、§14.4.1 は「**#13 は W = 8h と W = 24h の 2 回数える**」と書く。**判定区間を開ける前に走らせる回数は 1 回か 2 回か**(P-2、P-5)
>
> 12. [直す] 同 :415, :439, :458, :929 — 「2026-09-18 の監査の指摘 12 / 21 / 22 / 11」という参照が、本書の中で 2 つの別々の監査(`VERDICTS/2026-09-18_reaction_run12.md` 24 件 と `VERDICTS/2026-09-18_reaction_prereg.md` 25 件)の番号を、同じ呼び名で混ぜて指している(例: :439 の「指摘 22」は run12 側の重み付けの件、:1343 の「指摘 22」は prereg 側のサニティ #13)。**同じラベルが 2 つの集合を指していないか**(P-2、O-7)
>
> 13. [直す] 同 :358, :1217 vs :891-892, :653 — 指摘 18 の処置は「**運用は `alpha=0.05/576` を §8.5 の `mde()` に直接渡す形を正とする**」だが、§4.3 の α の行は「**MDE は §8.3 の α = 0.05 の表に ×1.701326**」、§13 の #2 は「**×1.70133**」と、倍率の経路と桁がまだ 2 通り残っている。**§5:416「同じ量が 2 か所で別の桁に見えないようにする」と両立するか**(P-2)
>
> 14. [直す] 同 :954, :1015, :1023 vs :1005 — §9 の項 1 と §10.1 の分岐は「**群の束の数が 30 未満**」で不明に落とすと書くが、同じ表の n の列は「**実群 / 対照の件数(欠測を引いた後)**」である。`doi_post_1m` のように欠測で n が 1/6 になる列がある(:761)。**30 を当てるのは束の数か、欠測を引いた後の n か**(P-2)
>
> 15. [直す] 同 :148 vs :171, :576 — §2 は約定の「**欠落 3 日(2023-09-25 / 2024-06-11 / 06-12)**」を記録するが、§3 の 456 日と n1 = 21,198 の引き算にこの 3 日は出てこない。実装は読めない日を黙って飛ばす(`scripts/o3c_reaction.py:901-904` の `missing` に積むだけ)。**この 3 日の束は 456 日と 21,198 に入っているか、観測量はどうなるか**(P-5、P-3)
>
> 16. [直す] 同 :261 vs :785 — 事前登録の本文は分位点を「**33.33% / 66.67%**」と書き、§8.5 の実行コードは `np.nanpercentile(..., [100/3, 200/3])` = 33.333…% / 66.666…% を使う。**どちらが判定の切り値か(境界の行が動く)**(P-2)
>
> 17. [直す] 同 :1155 — §12 の読みの括弧に「**逆向きは SELL 91.5% / BUY 81.3%**」という数があるが、直前の表(:1145-1148)にも本書の再現コマンドにも、この 2 つの数の出所が無い(表は `fwd_node` の側だけ)。**この 2 つの割合は何を数えたもので、出所の 3 分類のどれか**(P-2)
>
> 18. [聞く] 同 :488-490 — F1 の支持は「**12 セルのうち 1 つでもある**」、反証は「**12 セルすべてで差が有意に負**」である。支持 1/12・反証 12/12 という非対称で、反証はほぼ到達不能な条件になっている。**この非対称は意図した形か。結果の読みが実質「整合 / 混在」の 2 択にならないか**(P-16)
>
> 19. [聞く] 同 :271-274 — 48 群の内訳表は、B1・B2・C・D・全体の 12 群を **W = 8h の走行からだけ**取ると決めている。すると W = 24h の走行にも同じ 12 群の値が出るが、それは 576 検定にも §10.1 の表にも現れない。**W = 24h 側の 12 群の値は捨てるのか、観測として出すのか。出すなら、判定に使わないことをどこで担保するか**(P-3、P-13)
>
> 20. [聞く] 同 :1229-1234, :15 — 「報告する変更」の 2 件(バー t ≥ 2.0 → \|t\| ≥ 3.925、判定に使う走行 1 本 → 2 本)は「**次の返答でオーナーに報告する**」と書かれており、どちらも黙認されていた案からの変更である。**オーナーがこの 2 件を見る前に判定区間を開けてよいか、報告への応答を待つか**(`CLAUDE.md` §8 の A-13)
>
> 21. [聞く] 同 :1085, :934-935 — サニティ #13(判定区間で `fwd_node` が定義できる行数)は、L-199「9 = a」で前進到達を 1 周目の対象から外した後も残っている。**この数がどう出たら何が変わるのか。何も変わらないなら、開封前に判定区間へ当てる必要があるか**(P-3、A-6)
>
> 22. [聞く] 同 :147 — §2 は「**暦日で 478 日あるので 6 日が在庫に無い**(どの日かは本書では数えていない)」と書く。範囲は在庫に限定されているが、**その 6 日を公開アーカイブから取り直せるかは試したか。試していないなら「未確認」と書かれているか**(P-1)
>
> 23. [聞く] 同 :198 vs :1003, :1006 — §3 は「**全体分布の水準を根拠にした主張は書かない**」(L-199「1 = a」の条件)と書く一方、§10.1 の 576 行の表には「全体」群の行と「実群の到達率」の列がある。**「全体」群の実群到達率は、a が禁じた「全体分布の水準」に当たらないか。当たらないなら、その線引きはどこに書いてあるか**(P-9)
>
> 判定: 止める

---

## 2. 23 件の処置

**行番号は処置後の PREREG のものである**(処置前の行番号は §1 の逐語に残っている)。
**23 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] 判定の計算コードを開封後に書く形になっていないか

**処置: 直した。**リードの決定(逐語):
「**判定の計算は `scripts/o3c_reaction_judge.py`(開封前に書く。並行して作成中)。§14 に「読みのコマンド」節を足し、
入力(`--run-w8 --run-w24 --sample-w8 --out-dir --seed 1 --reps 2000`)と出力(`judgment_576.csv` /
`f1_12cells.csv` / `observation_only.csv` / `summary.json` / `MD5SUMS`)を書く。
「走行の前に書き、開封後は変えない」と明記。**」

- **§14.6「読みのコマンド」を新設**(PREREG:2028〜2086)。ファイル名・書く時期・テスト・入力のコマンド・出力の表を固定し、
  「**開封前。走行の前に書き、開封後は変えない**」と明記した(PREREG:2036〜2041)。
- **監査役の実測(`grep -rn bootstrap scripts/o3c_reaction.py` 一致 0)は §14.6 の冒頭に引いた**(PREREG:2033)。
- **実装が完成したので、実際の引数・出力・関門に合わせ直した**(下のエ。PREREG:2043〜2086)。
- **`scripts/o3c_reaction_judge.py` 自体には触っていない**(別の委任先が作成。参照だけした)。

### 2.[止める] 2,000 反復のパーセンタイル法で 99.99132% の CI は取れない

**処置: 直した。**リードの決定(逐語):
「**パーセンタイル法をやめ、正規近似にする: CI = 差 ± z × ブートストラップ SE、z = z_{1−α/2}、
α = 0.05/576 → z = 3.925(= バーと同じ数。ここから |t| ≥ 3.925 と「CI が 0 を除外」は同値になる)。
2,000 回は SE の推定に使う(SE の推定には十分。パーセンタイルの裾には足りない = 監査の指摘どおり)。
§9.1 の表を書き換える。**」

- **§9.1 の表の「区間」の行を書き換えた**(PREREG:1311): パーセンタイル法 → **正規近似 `CI = 差 ± z × SE`、z = 3.925**。
- **「反復回数」の行に、2,000 回の使い道が SE の推定だけであることを書いた**(PREREG:1308)。
- **訂正の注を足した**(PREREG:1309〜1325): **監査役の算術をそのまま引いた**(1/2000 = 5.0e−4 > 必要な 4.34e−5)。
  **「\|t\| ≥ 3.925」と「CI が 0 を除外」が同値になる理由を式で書いた。**
- **射程を書いた**: 正規近似は差の標本分布が正規に近いことを**仮定**する(リードの仮定)。
  **n < 30 の群は §10.1 の分岐 (1) で落ちるので判定に使わないが、n が 30〜数百の群での近似の誤差は未確認。**
- **§4.3 の「統計量のバー」の行にも、2 つが同値であることを書き足した**(PREREG:577)。
- **§13 の「報告する変更」に 1 行足した**(PREREG:1693)。**黙認された案からの変更だから。**

### 3・4.[止める] 「陰性は出ない」と書きながら陰性の判定語を列に置いている / 観測差を陰性・不明の境界にしている

**処置: 直した(2 件を 1 つの処置で片付けた = §10.1 の分岐の書き替え)。**リードの決定(逐語):
「**§10.1 の 4 分岐を次に替える: (1) 群の束の数 < 30 → 「不明(n < 30)」/ (2) |t| ≥ 3.925 → 「差あり(符号)」/
(3) それ以外 → 「検出されず」(MDE = X bp を併記)。「差なし」「陰性」の語は使わない
(§1 の △8 / ✕4 により段 A の結果は陰性にならない = §1 と整合)。
観測差と MDE の大小比較は列として残す(「MDE との比較」列)が、分岐には使わない。
「欲しい効果」は置かない(§8.6 のまま)。**」

- **§10.1 の判定を 4 分岐 → 3 分岐に書き替えた**(PREREG:1377〜1383)。
- **「MDE との比較」列は残し、「観測として出すだけで、判定の分岐には使わない」と明記した**(PREREG:1370)。
- **理由を 2 つに分けて書いた**(PREREG:1385〜1406):
  **理由 1(指摘 3)** 前版の「差なし(検出できる検出力で不在を測った)」は `CLAUDE.md` §5.2 の陰性の定義そのもので、
  §1 の「段 A の結果はどの行についても「陰性」にはならない」と矛盾していた。
  **理由 2(指摘 4)** 欲しい効果を置かないまま観測差を境界にすると、**出てきた差で境界が動く。**
- **「検出されず」は陰性ではないことを書いた**(PREREG:1403〜1404)。
- **「欲しい効果」は本版でも置かない**(§8.6 のまま)。
  **指摘 4 への答えは「境界を外す」であって「欲しい効果を置く」ではない**、と明記した(PREREG:1400)。
- **§10.3 の「読み」の列**(PREREG:1440)**と §14.5 の決定 1**(PREREG:1866)**も新しい語に合わせた。**
- **§13 の「報告する変更」に 1 行足した**(PREREG:1694)。

### 5.[止める] 456 日での D の被覆を数えずに走るのはなぜか

**処置: 直した(数えた)。**リードの決定(逐語):
「**判定区間 456 日のうち metrics(`doi_pre_1h` の元)が在庫にある日数を、ファイルの有無だけで数える
(観測量を読まない。`judgment_days` の日付リストと metrics の在庫パスの突き合わせ。
`R.judgment_days` は下の 8 の関門を通した形で呼ぶ)。その数と、それによる D 群の欠測見込み(日数ベース)を
§8.6 と §12 に書く。「224/224、欠測 0」は「標本 6 日では」と限定する。**」

**数えた(実測 2026-09-18。関門 `R.check_approval("full", "L-199")` を通した形で `judgment_days` を呼んだ)**:

```
判定区間の日数: 456
metrics の zip がある判定区間の日: 359 / 456
当日 + 前日の両方がある日: 356 / 456
当日の zip が無い日: 97
```

**欠ける 97 日の並び**: 2023-11-19 の 1 日 + 2024-03-04〜05-31 の連続 89 日 + 2024-06-02〜06-08 の連続 7 日。
**§12 が書く「2024-03-04〜06-08 の連続 97 日」と同じ塊である**(2024-06-01 は清算の zip が無いので判定区間に入らない)。

- **§8.6 にコマンドと出力を置いた**(PREREG:1209〜1251)。**D 群の欠測見込みは日数ベースで 97/456 = 21.3%。**
- **§12 に「(3) 判定区間 456 日のうち metrics の zip がある日 = 359 日、無い日 = 97 日」を足した**(PREREG:1545〜1557)。
- **「224/224、欠測 0」を「標本 6 日では」に限定した**(PREREG:1257、754)。
  **標本 6 日は 6 日とも metrics が在庫にある**(**実測**: 6 日とも `exists()` が True)。
- **束の数ベースの見込みは「未確認」と書いた**(走行の出力が要るため)。
  **日数が一様に束を持つと仮定した推定(n1 21,198 → 約 16,690、MDE 約 1.13 倍)には「一様の仮定は測っていない」を併記した。**
- **97 日は 1 つの連続した塊なので、D の欠測は期間の偏りを伴う。その向きは測っていないと書いた。**
- **§8.3 の D 群の MDE が metrics の欠測を引いていないことも書いた**(PREREG:1264〜1269)。

### 6.[止める] 44 群の `p`・`s` を標本から取るとき、標本側をどの切り値で群に分けるか

**処置: 直した。**リードの決定(逐語):
「**44 群の p・s は「判定区間の切り値を標本 6 日の行に当てて」群に分けた標本の行から取る。
切り値は軸の値の分位点(観測量ではない)で、読みのスクリプトが走行後・観測量を読む前に計算する。§8.6 に書く。**」

- **§8.6 に 6 行の手順表を置いた**(PREREG:1187〜1207)。1 走行 → 2 判定区間の実群で切り値 →
  3 その切り値を標本 6 日に当てる → 4 標本から `p` / `s` → 5 判定区間から `n` → 6 MDE。
- **各段で「判定区間の観測量を読むか」の欄を付けた。**切り値は**軸の値**の分位点であって観測量ではない、と明記。
- **「走行後に選べる余地」を閉じた**: 切り値の決め方も当てる先も上の 6 行で決まり、人が途中で選ぶ場所が無い。
- **射程**: 標本 6 日は 224 行なので 3 群に切ると 1 群 70〜80 行前後になり、`p` / `s` の推定誤差は大きい。
  **ただし `p` / `s` の誤差は MDE に効くだけで、判定(\|t\| ≥ 3.925)には効かない**(§10.1 の分岐 (2) は MDE を使わない)。
- **§10.1 の MDE 列からもこの手順を参照した**(PREREG:1369)。

### 7.[止める] 同点と欠測の規約が無いまま 48 群を凍結してよいか

**処置: 直した。**リードの決定(逐語):
「**同点 = `<=` で下側の群に入れる(§8.5 の `g` と同じ)。欠測 = どの群にも入れない(落とす)。
切り値 2 つが同じ値なら中の群は空になり n < 30 → 不明。B1 は値 1 が 57% なので D1 側に寄ることを走行前に書く。
576 の数え方は変えない(空の群も行として出し「不明(n < 30)」)。**」

- **§4 に「同点と欠測の規約」の表を新設**(PREREG:431〜438)。4 行(同点 / 欠測 / 切り値が同値 / 576 の数え方)。
- **走行前に分かっていることを実測で書いた**(PREREG:493〜501):
  **B1 は 224 行中 128 行(57.1%)が値 1。標本での切り値 `[1.0, 2.0]`、群の大きさ 128 / 43 / 53。**
  **`<=` の規約で同点は下側 = B1 の第 1 群に寄る。**
  参考: `bundle_total_qty_accum` 75/74/75、`doi_pre_1h` 75/75/74、`implied_leverage` は欠測 34 を落として 64/63/63。
- **576 の数え方は変えない**(空の群も 576 行の表に出し、判定欄に「不明(n < 30)」)。**A-6。**
- **監査役が挙げた「E は 34 行が空、A4 系は 21 行が空」も、欠測の規約(群に入れない)で扱いが決まった。**

### 8.[止める] §14.4.1 が関門の外から判定区間を開けている

**処置: 直した(関門を足した)+ 実行済みであることを隠さずに書いた。**リードの決定(逐語):
「**§14.4.1 の埋め込みスクリプトの冒頭に `R.check_approval("full", None, "L-199", …)`
(実際の関数の引数は `scripts/o3c_reaction.py:179` 付近を読んで合わせる)を足し、
L-199 の行が無ければ止まる形にする。記録: リードは 2026-09-18 07:13〜07:25 UTC にこの関門を通さない形
(現行の埋め込みスクリプト)で #13 を W8h・W24h の 2 回実行した。…
これを §14.4.1 に「実行済み。関門を通していなかった」と隠さず書く。**」

- **関門を足した**(PREREG:1860〜1866)。`--approval` 引数を足し、`--judgment` の枝の先頭で
  **`R.check_approval("full", a.approval)`** を呼ぶ。コマンド行も `--approval L-199` を含む形に直した(PREREG:1848)。
- **関数の形を実測で合わせた**: `scripts/o3c_reaction.py:179` の
  `check_approval(mode, approval, owner_log=OWNER_LOG, days=None)`。
  **委任文面の例示は第 3 引数に `"L-199"` を置いていたが、実物の第 3 引数は `owner_log` である。**
  **リードの委任文面「実際の関数の引数は … 読んで合わせる」に従って `("full", a.approval)` にした。**
- **関門が効くことを実測した**(委任先、2026-09-18): `--approval` 無しで `--judgment` を通すと
  `[止め] --mode full には --approval L-NNN が要る(設計 §9: 標本 6 日の表をオーナーに見せた回の記録の番号)`
  を出して**終了コード 1** で止まる。**重い読み込みの前に止まる**(`check_approval` が `judgment_days` より先)。
- **実行の記録を §14.4.1 に置いた**(PREREG:1937〜1972)。**「関門を通さずに開けた」と明記した。**
  **6 行の出力をそのまま貼った**(W 8h / W 24h、real 5m32s / 5m12s)。
- **照合**: **非 mixed 束 21,198 は §3 の n1 = 21,198 と一致**(実測)。
- **読みを書いた**: 判定区間で `fwd_node` が定義できる割合は W 8h 27.7% / W 24h 40.5% で標本 6 日より高いが、
  **9 = a は変えない。**理由は、a を決めたのが割合の低さだけでなく**欠測が清算の向きに依存する**ことだから
  (判定区間でも SELL 30.2% 対 BUY 24.4%(W 8h)、SELL 43.6% 対 BUY 36.4%(W 24h)。**実測**: 6 行から計算)。
  **L-199「9 = a」はオーナーの逐語であり、リードが走行前に数を見て覆さない**(A-6・A-9)。
- **`scripts/o3c_reaction.py` の本体は 1 行も変えていない。**

### 9.[止める] 凍結した確定版に「仮置き」が 3 件残っている

**処置: 直した(3 件とも確定した)。**リードの決定(逐語):
「**3 件ともリードが確定: (a) W に依らない 12 群は W = 8h の走行から取る、(b) CI の水準は上の 2 のとおり、
(c) #13 は 2 回数える(実施済み)。「仮置き」の語を消し「リードの決定(該当語なし)」に替える。**」

- **§0.1 の 3 行を書き替えた**(PREREG:86〜88)。「委任先の仮置き」→「**リードの決定(該当語なし)**」。
- **§4 の注を書き替えた**(PREREG:496〜514)。「【該当語なしの仮置き】」→「【**リードの決定(該当語なし)**】」。
  **「前版はここを『委任先の仮置き。この仮置きはリードが確定させる』と書いていた。確定した」**と残した。
- **(b) は指摘 2 の処置で正規近似に替わったので、§0.1 の行にもそれを書いた**(PREREG:87)。
- **(c) は §14.4.1 の実行の記録で「実施済み」になった**(PREREG:88、1483)。
- **本文に残る「仮置き」の語は、消したことを書いている箇所だけである**(実測: PREREG:108、516)。

### 10.[止める] 同じ 12 セルに 2 つの別々の合格条件がある

**処置: 直した。**リードの決定(逐語):
「**F1 の条件を §10.1 の「差あり」に一本化: 「F1 と整合」= 12 セルのうち差あり(正)が 1 つ以上 かつ
差あり(負)が 0。「差 ≥ MDE」の条件は消す(差ありなら検出されている)。**」

- **§7.0 の表を書き替えた**(PREREG:728〜736)。**12 セルそれぞれの判定は §10.1 の 3 分岐で決まり、
  §7.0 はその結果を数えるだけで新しいバーを置かない**、と冒頭に書いた(PREREG:728〜729)。
- **「差 ≥ MDE」を消した**(PREREG:738〜746)。**差ありなら既に検出されているので MDE を重ねて課さない。**
- **「ブートストラップ CI が 0 を除外」が入っていなかった件は、指摘 2 の処置で同値になったので解けた**
  (§9.1 が正規近似なので「\|t\| ≥ 3.925」と「CI が 0 を除外」は同じ条件)。**その旨を §7.0 に書いた**(PREREG:744)。

### 11.[止める] #13 は 1 回か 2 回か

**処置: 直した(2 回に統一)。**リードの決定(逐語):
「**「1 回だけ」→「W = 8h と W = 24h の 2 回(各 1 回)」に統一。**」

- **§11 の #13 の状態欄**(PREREG:1483): 「未実施 … 1 回だけ実行する」→
  「**実施した(2026-09-18 07:13〜07:25 UTC、リード。W = 8h と W = 24h の 2 回(各 1 回))**」。
- **§14.4 の 1**(PREREG:1805〜1806): 「1 回だけ数える」→「**W = 8h と W = 24h の 2 回(各 1 回)数える**」
  + 「**2026-09-18 に 2 回とも実施した**」。
- **§14.4.1 の末尾**(PREREG:1934): 「2 回数える」→「**2 回(各 1 回)数える**」。
- **実測**: 本文に残る「1 回だけ」は、この決定の逐語を引いている §0.1 の 1 行だけである(PREREG:110)。

### 12.[直す] 同じラベルが 2 つの監査の集合を指している

**処置: 直した。**リードの決定(逐語):
「**参照のラベルを「run12 監査の指摘 N」「prereg 監査(1 回目)の指摘 N」「prereg 監査(2 回目)の指摘 N」に分ける(全箇所)。**」

- **§0.2「監査の参照ラベル」を新設**(PREREG:137〜146)。3 つのラベルと、指す監査・件数・逐語の置き場所の表。
- **全 53 箇所を 1 件ずつ分類して付け替えた**(run12 側 9 箇所 / prereg 1 回目側 41 箇所 / prereg 2 回目側は新規)。
  分類は 2 つの VERDICTS の逐語と本文の話題を突き合わせて決めた。
  **監査役が挙げた例(:439 の「指摘 22」= run12 の重み付け、:1343 の「指摘 22」= prereg のサニティ #13)は
  その分類どおりになっている**(処置後の PREREG:667 が run12 側、PREREG:1823 が prereg 1 回目側)。
- **検算(実測)**: `grep -n '2026-09-18 の監査の指摘' REACTION_PREREG_2026-09-18.md` の一致は **3 行**で、
  **3 行とも §0.2(PREREG:139・142・143)の中で古い書き方を引用している行である。**
  **参照として使っている箇所は 0 である。**
  ラベルの付いていない「指摘 N」も、§0.2 で**古い書き方を引用している 1 行だけ**である(PREREG:143 付近)。

### 13.[直す] 倍率の経路と桁が 2 通り残っている

**処置: 直した。**リードの決定(逐語):
「**倍率の記述をすべて「`alpha=0.05/576` を `mde()` に直接渡す」に揃え、
×1.70133 / ×1.701326 は「参考(概算)」と明記するか消す。**」

- **§4.2**(PREREG:564): 「×1.70133 を掛ける」→「**`alpha=0.05/576` を §8.5 の `mde()` に直接渡して出す**」。
- **§4.3 の α の行**(PREREG:576): 「×1.701326」→ 同じ形に。「**倍率は掛けない**」を明記。
- **§8.2 の「倍率の使い方」**(PREREG:916〜920): 倍率を「**参考(概算)**」と明記し、
  「**判定に使う MDE は 1 つも倍率から作らない**」を足した。
- **§8.5 の末尾**(PREREG:1153〜1156): 「`alpha=` を変える」が正の手順であることを強め、
  倍率の行を「**参考(概算)。この倍率の経路は判定に使わない**」にした。
- **§8.5 の「採る α」**(PREREG:1159)と **§13 の #2**(PREREG:1674)も同じ形に揃えた。
- **§8.2 の倍率の表そのもの(×1.827059 / ×2.042858 / ×1.701326)は残した。**
  **消さない理由**: この 3 つは §8.2 の再現コマンドの出力そのもので、**出所のある実測値だからである。**
  **「判定の経路ではない」と書くことで、同じ量が 2 通りに見える問題は消える。**

### 14.[直す] 30 を当てるのは束の数か、欠測を引いた後の n か

**処置: 直した。**リードの決定(逐語):
「**30 は「その検定に使う欠測を引いた後の実群の n」に当てる(観測量ごとに違ってよい)。**」

- **§9 の項 1**(PREREG:1296): 「群ごとの束の数が 30 未満」→
  「**30 を当てるのは「その検定に使う、欠測を引いた後の実群の n1」である**」。
- **§10.1 の「検出力」列**(PREREG:1372)と **3 分岐の 1 行目**(PREREG:1381)も同じ形にした。
- **§14.5 の決定 1**(PREREG:1866)も同じ形にした。
- **監査役が挙げた例(`doi_post_1m` は欠測で n が 1/6 になる)がそのまま効く形になった**:
  **同じ群でも観測量ごとに n1 が違ってよい。**判定の対象である戻り到達 2 系統についてはその列の n1 を使う。

### 15.[直す] 約定の欠落 3 日は 456 日と 21,198 に入っているか

**処置: 直した(確かめた)。**リードの決定(逐語):
「**欠落 3 日(2023-09-25 / 2024-06-11 / 06-12)が判定区間 456 日に入っているか `judgment_days`(関門つき)で
確かめ、入っていれば「約定が読めない日の束は観測量が全部欠測になり、n から落ちる(束の数 21,198 には含まれる)」と
§3 に書く。入っていなければその旨。**」

**確かめた(実測 2026-09-18、関門つき)**:

```
判定区間の日数: 456
約定欠落 2023-09-25: 判定区間に入る = False / 清算 zip 在庫 = False
約定欠落 2024-06-11: 判定区間に入る = False / 清算 zip 在庫 = False
約定欠落 2024-06-12: 判定区間に入る = False / 清算 zip 在庫 = False
```

- **3 日とも判定区間に入っていない。**しかも**清算の zip が在庫に無い日**である(§2.1 の 6 日のうちの 3 日)。
- **§3 に「入っていない」と書いた**(PREREG:296〜323)。コマンドと出力を同じ場所に置いた。
- **「456 と 21,198 の引き算にこの 3 日が出てこない」のは、引く前から入っていないためである**と書いた。
- **射程を足した**: これは**清算の zip が無い**ことの測定である。
  **在庫にある日の約定が 1 日ぶん丸ごと読めるか(`scripts/o3c_reaction.py:901-904` の `missing` の経路が
  判定区間で何日発火するか)は未測定**と書いた。**監査役が指した実装の行はそのまま引いた。**

### 16.[直す] 分位点は 33.33% / 66.67% か `100/3, 200/3` か

**処置: 直した。**リードの決定(逐語):
「**切り値は `[100/3, 200/3]`(コードのとおり)。本文の 33.33% / 66.67% を「100/3 % / 200/3 %」に直す。**」

- **§4 の本文**(PREREG:423): 「33.33% / 66.67%」→「**`100/3 %` / `200/3 %`**」。
- **訂正の注を足した**(PREREG:425〜429): **33.33 と 33.3333… は別の分位点で、境界の行が動く。**
  **判定に使う切り方は変えていない**(§8.5 のコードが最初から `100/3, 200/3` だった = 実測)。
- **実測**: 本文に残る「33.33 / 66.67」は、この訂正の注で古い書き方を引用している 2 行だけである。
  (§6 の「31.25 対 66.67」は `bin_pct` の別の量で、分位点ではない。)

### 17.[直す] 91.5% / 81.3% の出所

**処置: 直した(出所が見つかった)。**リードの決定(逐語):
「**91.5% / 81.3% の出所を探す(RUN12 §4.5b 付近か `table.csv` の再計算)。
見つかれば出所を付ける。見つからなければその数を消し「出所不明のため削除」と書く。**」

**見つかった(実測 2026-09-18、標本 6 日の `table.csv` を数え直した)**:

```
gap60_w8  SELL n=117 順(fwd)=18(15.385%) 逆=111(94.872%)
gap60_w8  BUY  n=107 順(fwd)=22(20.561%) 逆=95(88.785%)
gap60_w24 SELL n=117 順(fwd)=22(18.803%) 逆=107(91.453%)
gap60_w24 BUY  n=107 順(fwd)=40(37.383%) 逆=87(81.308%)
```

- **91.5% = 107/117(W = 24h・SELL の逆向き)、81.3% = 87/107(W = 24h・BUY の逆向き)。**
- **出所の 3 分類では「実測」**(標本 6 日。**判定区間ではない = 開封ではない**)。
- **§12 にコマンドと出力を置いた**(PREREG:1581〜1605)。
- **前版の表が `fwd_node`(順方向)の側だけを載せていたので出所が追えなかった**、と書いた。
- **W = 8h 側の対応する数 94.9% / 88.8% が §4.3 と §12 の既存の記述と一致することも実測で確かめた。**

### 18.[聞く] F1 の支持 1/12・反証 12/12 という非対称は意図した形か

**処置: 直した(対称にした)。**リードの決定(逐語):
「**対称にする: 整合 = 正の差あり ≥1 かつ 負の差あり 0 / 反証 = 負の差あり ≥1 かつ 正の差あり 0 /
混在 = 両方あり / 不明 = 差ありが 0(不明(n<30)と検出されずの内訳を併記)。§7.0 を書き換える。**」

- **§7.0 の表を 4 行に書き替えた**(PREREG:728〜736)。整合 / 反証 / 混在 / 不明。
- **反証が「12 セルすべて」から「1 つ以上 かつ 逆向きが 0」になった。**
  **これは反証を通りやすくする向きの変更である**(`CLAUDE.md` §0.2 の A-18「構造的に安全側に振れる」の逆向き)。
  **その向きを §7.0 に明記した**(PREREG:735)。
- **「不明」の行に「内訳(n < 30 が何個 / 検出されずが何個)を併記する」を入れた。**
- **監査役の問い「結果の読みが実質「整合 / 混在」の 2 択にならないか」への答え**:
  **前版はそうなっていた。本版は 4 つとも到達できる。**
- **§13 の「報告する変更」に 1 行足した**(PREREG:1695)。

### 19.[聞く] W = 24h 側の 12 群の値は捨てるのか、観測として出すのか

**処置: 直した(観測として出す)。**リードの決定(逐語):
「**W = 24h 側の 12 群は §10.2 の観測のみの表に出す(t と判定の列なし)。576 には入れない。**」

- **§10.2 に「この表に入れるもの」の表を新設**(PREREG:1429〜1437)。
  **W = 24h の走行から出る W に依らない 12 群 × 2 系統 × h 6 = 144 行**を観測のみで出す。
- **判定に使わないことの担保**: **§10.2 の表の形の規約(`t` の列と `判定` の列を置かない)がそれである**、と書いた。
- **576 には入れない**(入れると同じ 12 群を 2 本から二重に数えることになる)。**α にも数えない。**
- **§4 の内訳表の注にも同じことを書いた**(PREREG:519〜525)。
- **射程**: **W = 8h 側と W = 24h 側で同じ 12 群の差が食い違ったときの読み方は本書では決めていない。**
  **1 周目は判定を W = 8h 側に固定し、食い違いは §10.3 の「2 周目に測るもの」に送る**(**リードの判断**)と書いた。

### 20.[聞く] オーナーが 2 件を見る前に判定区間を開けてよいか

**処置: 開けない。応答を待つ。**リードの決定(逐語):
「**開けない。判定区間の走行は、この 2 件(+ 上の決定で新たに変わる 3・4・18)をオーナーに報告し、
応答を得てから。§14.4 に「オーナーの応答(L 番号)を得てから走らせる」を足す。**」

- **§14.4 に「4」を足した**(PREREG:1809〜1818)。
  「**オーナーの応答(L 番号)を得てから走らせる。**」
  「**応答の L 番号: (まだ無い。オーナーの応答を待っている。)**」
- **§14.4 に「5」を足した**(PREREG:1819〜1823): **判定区間の走行は 1 本も走っていない**
  (**実測**: `backtest_data/o3c_reaction_20260918_full/` は存在しない)。
- **冒頭の凍結の宣言にも書いた**(PREREG:4〜5、26)。
- **§13 の「報告する変更」の表を 2 行 → 5 行にした**(PREREG:1690〜1694)。
  **上の 2 行が prereg 監査(1 回目)、下の 3 行が prereg 監査(2 回目)で決めたもの。**
  **「判定区間を開けるのは、この表の 5 件をオーナーに報告し、応答(L 番号)を得てから」と書いた**(PREREG:1701)。
- **根拠**: `CLAUDE.md` §8 の A-13(枠組みはオーナーに見せてから進める)。
  **開けるのは一度きりなので、報告が後になると取り返しがつかない。**

### 21.[聞く] #13 の数がどう出たら何が変わるのか

**処置: 直した(位置づけを書いた)。**リードの決定(逐語):
「**#13 は 2 周目(前進到達を測る周)の設計の入力(どれだけ行が作れるか)として残す。
1 周目の判定には何も変えない、と明記。**」

- **§11 に「#13 は何のためか」の表を新設**(PREREG:1514〜1523)。4 つの問いに 1 行ずつ答えた。
  - この数がどう出たら 1 周目の何が変わるか → **何も変わらない**(576・48 群・α・バー・h・軸のどれにも入っていない)。
  - 開封前に判定区間へ当てる必要があったか → **「必要」ではない。**リードが 2 周目の設計のために当てた(**リードの判断**)。
  - 判定区間を汚したか → 出したのは件数(整数 6 つ)だけ。**ただし「この 6 つの数からは読めない」という主張であって
    「どんな件数も安全」ではない。**
  - 関門を通したか → **通していない**(指摘 8。本版で関門を足した)。
- **§11 の #13 の行からこの表を参照した**(PREREG:1483)。

### 22.[聞く] 在庫に無い 6 日を取り直せるかは試したか

**処置: 直した(列挙した + 未確認と書いた)。**リードの決定(逐語):
「**「未確認(この環境から公開アーカイブへの取り直しは試していない。オーナー PC は未確認)」と書く。
どの 6 日かは在庫のファイル名から数えて列挙する(ファイル名の有無だけ。中身は読まない)。**」

**数えた(実測 2026-09-18。ファイル名の有無だけ。中身は 1 行も読んでいない)**:

```
清算 zip: 472 本 / 暦日: 478 日 / 在庫に無い日: 6
在庫に無い日の一覧: ['2023-09-09', '2023-09-23', '2023-09-25', '2024-06-01', '2024-06-11', '2024-06-12']
```

- **§2.1「在庫に無い 6 日」を新設**(PREREG:234〜272)。コマンドと出力を置いた。
- **「取り直せるか: 未確認。この環境から公開アーカイブへの取り直しは試していない。オーナー PC は未確認」と書いた**
  (`CLAUDE.md` §5.2「**「取れない・無い」の主張には範囲と方法を書く**」。**「取れない」とは書かない**)。
- **照合(実測)**: **6 日のうち 4 日(2023-09-09 / 2023-09-23 / 2024-06-11 / 2024-06-12)は、
  §12 の「metrics はあるが清算の zip が無い日 = 4 日」と同じ日である。**
- **§2 の表の「どの日かは本書では数えていない」を「§2.1 に列挙した」に直した**(PREREG:231)。
- **この 6 日は 472 日にも 456 日にも 21,198 束にも入っていない**と書いた(在庫が無いので束が作られない)。

### 23.[聞く] 「全体」群の実群到達率は a が禁じた「全体分布の水準」に当たらないか

**処置: 直した(線引きを書いた)。**リードの決定(逐語):
「**「全体」群の行は「実群 − 対照」の差を判定するもので、L-199 1 = a が禁じたのは
「全体分布の水準(実群の到達率そのもの)を根拠にした主張」。表の「実群」列は差の入力として出すが、
その水準を根拠にした文は結果の読みに書かない、と §3 と §10.1 に線引きを書く。**」

- **§3 に「「全体」群の行との線引き」の表を新設**(PREREG:346〜365)。3 行:
  **出す(判定に使う)= 実群 − 対照 (ii) の差と `t`・CI・MDE**(**a が既知とするのは群分けも対照も無い分布の数で、
  対照との差は既知の側に入っていない** = 手順 1 は対照を 1 つも作っていない)。
  **出す(差の入力として)= 実群の到達率の列**(差を出すなら実群の値は必ず出る。隠すと差が検算できない)。
  **書かない = その水準そのものを根拠にした文**(これが a の禁じたもの)。
- **§10.1 にも同じ線引きを書いた**(PREREG:1419〜1421)。**結果の読みで当てる場所がそこだから。**
- **射程を書いた**(PREREG:362〜365): **手順 1 で既に見えているのは `bp_h`(価格変化)の中央値・平均だけで、
  判定の観測量である戻り到達率 2 系統の全体の水準はまだ 1 つも見ていない**(実測: §3.1)。
  **それでも上の線引きを掛けるのは、a の条件を広い側に読んだためである**(**リードの判断**。
  **出す側ではなく書かない側を広くしているので A-18 の逆向きである**と自覚して書いた)。

---

## 3. 読みのスクリプトの実装から出た食い違い 4 件(ア〜エ)

**監査役の 23 件とは別。**`scripts/o3c_reaction_judge.py` が完成したことで見つかったもので、
**リードが決定を付けて追加で委任した。**
**PREREG §0.1 の 4 つ目の表(PREREG:124〜135)に逐語を置いた。**
**4 件ともオーナーの原文の該当語は無い = リードの判断である。**
**走行の前の変更で、バー・族(576)・α・h・軸(48 群)は 1 つも動いていない。**

### ア. 対照行に軸の値が無い軸がある

**リードの決定(逐語)**:
「**対照行に軸の値が無い軸では、合わせた対照 (ii) は 1 対 1 の相手の実群の束の群を受け継ぐ
(§6.1 の 4「実群と 1 対 1 で対応する」が根拠)。一様対照 (i) は受け継ぐ先が無いのでその 40 群には入れず、
対照 (i)・差 (i) は空。主指標は差 (ii) なので 576 検定は変わらない。§4 と §10.1 に書く。
受け継いだかどうかは読みのスクリプトが走行ごとに数えて `summary.json` に残す。**」

**委任先が数え直した(実測 2026-09-18、標本 6 日の `table.csv`。対照行で値が非空の件数)**:

```
gap60_w8  A1 bin_pct (uniform 225, matched 213) / D doi_pre_1h (225, 211)
          A2 A3 A4 A5 E B1 B2 C … いずれも (0, 0)
gap60_w24 A1 bin_pct (uniform 225, matched 216) / D doi_pre_1h (225, 215)
          A2 A3 A4 A5 E B1 B2 C … いずれも (0, 0)
```

- **§4 に「対照行に軸の値が無い軸の扱い」を新設**(PREREG:440〜491)。実測・決定の表・群の数。
- **§10.1 の 差 (i) の列**(PREREG:1368)**と 対照 (i) の列**(PREREG:1352)**に「38 群では空」と書いた。**
- **§4.3 の対照 (i) の行にも重み付けの対象を書き直した**(PREREG:578)。
- **数の食い違いを隠さずに書いた(PREREG:486〜491)**: **委任の文面は「その 40 群」だが、
  §4 の 48 群の内訳から数え直すと 38 群である**(A2〜A5・E の 5 軸 × (3+3) = 30 + B1 3 + B2 3 + C 2 = 38。
  残る 10 群 = A1 の 6 + D の 3 + 全体 1 で、30 + 8 + 10 = 48 と合う)。
  **40 との差 2 の出所は分からない。38 を採ったのは足し算が合うからである。リードが確かめること。**

### イ. §6.1 の重み付けの対象に当たる列が無い

**リードの決定(逐語)**:
「**§6.1 の重み付けの対象を `*_reactdir` の 3 系統(価格変化・最大順行・最大逆行。
対照行は REACT_SIGN = +1 の生の値)に改め、到達の列には当てないと書く。読みのスクリプトはその形で実装済み。**」

- **§6.1 の見出しと 2 つの本文を書き替えた**(PREREG:665、679、690)。
- **訂正の注を足した**(PREREG:671〜682): **1 周目の到達の列はどれも絶対価格への到達で、
  実群と対照で同じ定義である。向きで値が変わるのは 1 周目で外した `fwd_node` だけ。**
- **実測で裏を取った**: `scripts/o3c_reaction.py:42`「**対照行(side が無い)は `REACT_SIGN` を +1 として
  生の値を入れ、絶対値の列を併記する**」+ `:131` の `REACT_SIGN = {"SELL": -1.0, "BUY": 1.0}`。
- **効き方を書いた**: **`*_reactdir` の 3 系統は §4.3 で「観測のみ」なので、§6.1 は判定(576 検定)に効かない。**
  **判定の主指標 差 (ii) の対照 (ii) には重み付けが要らない**(§6.1 の 4)。

### ウ. §10.3 の「読み」の 4 語が §7.0 と揃っていない

**リードの決定(逐語)**:
「**§10.3 の「読み」の 4 語は §7.0 の新しい 4 分岐(F1 と整合 / F1 の反証 / 混在 / 不明)に揃える
(「差なし」を消す)。**」

- **§10.3 の「読み」の行を書き替えた**(PREREG:1440)。
  **「F1 と整合 / 差あり(向きが逆)・混在 / 差なし / 不明」→「F1 と整合 / F1 の反証 / 混在 / 不明」。**
  **セル単位の語(差あり(+) / 差あり(−) / 不明(n < 30) / 検出されず)は §10.1 を指すと書き分けた。**

### エ. §14.6 が実装の実際の引数・出力・関門と違う

**リードの決定(逐語)**:
「**§14 の読みのコマンドの実際の引数: `python3 scripts/o3c_reaction_judge.py --run-w8 <dir> --run-w24 <dir>
--sample-w8 backtest_data/o3c_reaction_20260918_sample/gap60_w8
--sample-w24 backtest_data/o3c_reaction_20260918_sample/gap60_w24 --out-dir <dir>`
(seed 1・reps 2,000 は既定)。出力: judgment_576.csv / observation_only.csv(… 実際の行数は summary から)/
f1_12cells.csv / f1_reading.txt / why_frame.csv / summary.json / MD5SUMS。
関門: 表を書く前に `_research_audit_gate.require_audit("o3c_reaction_20260918", "結果")` を呼び、
閉じていれば終了コード 1 で何も書かない。列に `対照(i)SE` と `ブートストラップSE` を足している
(§10.1 の列は落としていない)。**」

- **§14.6 の入力を実際の引数に直した**(PREREG:2043〜2053)。`--sample-w24` を足し、既定の 2 つは省いてよいと書いた。
- **出力の表を 5 行 → 7 行にした**(PREREG:2055〜2065)。`f1_reading.txt` と `why_frame.csv` を足し、
  `observation_only.csv` の行数は `summary.json` から読むと書いた。
  `summary.json` に**対照 (ii) が実群の群を受け継いだ件数**(ア)が入ることも書いた。
- **関門を書いた**(PREREG:2070〜2072): `require_audit("o3c_reaction_20260918", "結果")`。
  **実測**: `scripts/o3c_reaction_judge.py:736-739`。
- **足した列を書いた**(PREREG:2074〜2075): `対照(i)SE` / `ブートストラップSE` / `CI下限` / `CI上限`。
  **実測**: 同 `:527-535` の `JUDGE_HEADER`。**§10.1 の列は 1 つも落としていない。**
- **走行の前の変更であることを明記した**(PREREG:2077〜2081)。**A-6 に抵触しない。**

---

## 4. 処置の後に実測で確かめたこと

| # | 確かめたこと | コマンド | 出力 |
|---|---|---|---|
| 1 | **§8.3 の MDE 152 セルが変わっていない** | §8.5 の 2 つ目の再現コマンド(処置の前と後で 2 回) | **前**: `照合したセル: 152 / 一致 152 / 食い違い 0` / **後**: 同じ |
| 2 | **判定区間の走行は 1 本も走っていない** | `test -e backtest_data/o3c_reaction_20260918_full` | **存在しない** |
| 3 | **§14.4.1 の新しい関門が効く** | `--approval` 無しで `--judgment` を通す | `[止め] --mode full には --approval L-NNN が要る…` / 終了コード 1 |
| 4 | **古いラベルが参照として残っていない** | `grep -n '2026-09-18 の監査の指摘' PREREG` | **3 行。3 行とも §0.2 で古い書き方を引用している行**(参照として使っている箇所は 0) |
| 5 | **禁じた語が判定語として残っていない** | `grep -n '差なし\|陰性\|仮置き\|1 回だけ' PREREG` | 残るのは**否定形**(「陰性にならない」「陰性ではなく不明」「陰性にしない」「陰性ではない」)、**消したことの記述**、**`CLAUDE.md` §5.2 の定義の引用**だけ |

---

## 5. 本書で新しく出していないもの(射程)

- **判定区間(456 日)の観測量は 1 つも出していない。**
  本書に出た判定区間の数は、**日数(456 / 359 / 97)と束の件数(21,378 / 21,198 / 5,876 / 8,590 と side 別)だけ**である。
  **どれも件数であって、§4.3 の観測量でも §4 の分割軸の値でもない。**
- **`scripts/o3c_reaction.py` の本体は 1 行も変えていない。**
- **`scripts/o3c_reaction_judge.py` と `tests/test_o3c_reaction_judge.py` には触っていない**(参照のみ)。
- **判定(予測できる / 使える / 有効)は本書に 1 つも書いていない。**
- **オーナーの応答(L 番号)はまだ無い。判定区間は開けていない。**
- **ア の群の数(38 か 40 か)はリードが確かめること。**委任の文面は 40、委任先の数え直しは 38。

## 事前登録から移した処置の注記(8 ブロック。2026-09-19、L-228「移す先 = 巡ごとの VERDICTS と docs/DATA/probes/ でよい」。元の位置は節と、移す前の版(コミット fef6abb 時点)の行番号)

### 元: (冒頭) / 旧 行 68-74

> > **【2026-09-18 走行前の再監査(2 回目)を受けた変更(prereg 監査(2 回目)の指摘 2・3・4・18。リードの決定)】**
> > **(3) ブートストラップ CI をパーセンタイル法から正規近似に替えた**(§9.1。prereg 監査(2 回目)の指摘 2)。
> > **(4) §10.1 の判定を 4 分岐から 3 分岐にし、「差なし」「陰性」の語を消した**(prereg 監査(2 回目)の指摘 3・4)。
> > **(5) §7.0 の F1 の整合 / 反証を対称にした**(prereg 監査(2 回目)の指摘 18)。
> > **バー(\|t\| ≥ 3.925)・族(576)・α(0.05/576)・h・軸(48 群)は 1 つも動かしていない。**
> > **この 3 件も黙認された案(返答 060 §3)から変わるので、§13 の「報告する変更」に足した。**
> > **判定区間を開けるのは、この報告へのオーナーの応答を得てからである**(prereg 監査(2 回目)の指摘 20。§14.4)。

### 元: 4. 構成ファミリー(列挙する。後から追加しない) / 旧 行 1034-1038

> **【本版で直した・prereg 監査(2 回目)の指摘 16】前版は本文を「33.33% / 66.67%」と書き、
> §8.5 の実行コードは `np.nanpercentile(..., [100/3, 200/3])` を使っていた。
> **切り値は `[100/3, 200/3]`(コードのとおり)である。**本文の表記を `100/3 %` / `200/3 %` に直した。
> **33.33 / 66.67 と書くと境界の行が動く**(33.3333…% と 33.33% は別の分位点)。
> **判定に使う切り方は変えていない**(コード側が最初から `100/3, 200/3` だった。**実測**: §8.5)。

### 元: 4. 構成ファミリー(列挙する。後から追加しない) / 旧 行 1211-1223

> **【リードの決定(該当語なし)。prereg 監査(2 回目)の指摘 9 で確定した】**
> 「W に依らない 12 群をどちらの走行から取るか」は、**オーナーの逐語にも L-199 にも該当語が無い。**
> **リードが決めた: W = 8h の走行から取る。**
> **理由**: 同じ群を 2 本から二重に数えると 48 群・576 検定が壊れる(観測量 `reach_back_*` は
> 目標価格が W のプロファイルから来るので、W が違えば値も違う。**実測**: 標本 6 日で
> `p_tgt_node_up` が W 8h で 133 / W 24h で 147 行と違う)。
> **前版はここを「委任先の仮置き。この仮置きはリードが確定させる」と書いていた。確定した。**
>
> **W = 24h 側の同じ 12 群をどうするか(prereg 監査(2 回目)の指摘 19。リードの決定)**:
> **捨てない。§10.2 の「観測のみ」の表に出す。**
> **`t` の列と `判定` の列は置かない**(§10.2 の形。判定に使わないことはそれで担保する)。
> **576 には入れない**(入れると同じ群を二重に数えることになる)。
> **この 12 群 × 2 系統 × h 6 = 144 行は観測のみで、α に数えない**(§4.3 の「観測のみ」と同じ扱い)。

### 元: 7. 1 周目の機構仮説(F1 だけ) / 旧 行 1589-1602

> **【本版で直した 2 点(prereg 監査(2 回目)の指摘 10・18。リードの決定)】**
> **(1) 条件を §10.1 の「差あり」に一本化した(prereg 監査(2 回目)の指摘 10)。**前版の「F1 と整合」は
> 「差が正 かつ \|t\| ≥ 3.925 かつ **差 ≥ MDE**」で、§4.3・§10.1 のバーにある
> **「ブートストラップ CI が 0 を除外」が入っておらず**、逆に §10.1 の「差あり」には
> **MDE の条件が無かった** = **同じ 12 セルに 2 つの別々の合格条件が置かれていた。**
> **本版は「差あり」= §10.1 の分岐 (2) だけを使う。**「差 ≥ MDE」の条件は消した
> (**差ありなら既に検出されているので、MDE は重ねて課す条件ではない**)。
> **なお §9.1 を正規近似に替えたので、「\|t\| ≥ 3.925」と「CI が 0 を除外」は同値である**(§9.1)。
> **(2) 整合と反証を対称にした(prereg 監査(2 回目)の指摘 18)。**前版は支持が「12 セルのうち 1 つでもある」、
> 反証が「**12 セルすべてで**差が有意に負」で、**反証はほぼ到達不能な条件だった。**
> **本版は両方「1 つ以上 かつ 逆向きが 0」にした。**
> **これは反証を通りやすくする向きの変更である**(`CLAUDE.md` §0.2 の A-18「構造的に安全側に振れる」の逆向き)。
> **走行の前の変更であり、バー(\|t\| ≥ 3.925)・α・族・h・軸は動かしていない。**
> **黙認されていた案(返答 060 §3)から変わるので、§13 の「報告する変更」に足した。**

### 元: 8. 検出力(MDE)— 走らせる前に書く(§4.1) / 旧 行 2390-2394

  > **【本版で限定した・prereg 監査(2 回目)の指摘 5】前版は §8.6 に「**主軸 D(`doi_pre_1h`)は減らない = 224/224、
  > 欠測 0(実測)**」と書いていた。**根拠は標本 6 日だけである。**
  > **標本 6 日(2023-06-25/26、2024-02-19/20、2024-10-13/14)は 6 日とも metrics が在庫にあるので
  > 224/224 になる**(**実測**: 上の走査で 6 日とも `exists()`)。
  > **判定区間 456 日では 97 日ぶんが落ちる。**「224/224、欠測 0」は**標本 6 日での話である。**

### 元: 8. 検出力(MDE)— 走らせる前に書く(§4.1) / 旧 行 2402-2410

  > **【訂正・2026-09-18】前版は「建玉側の軸(A4・A5・**D**・E)は埋まる行がさらに減る
  > (標本 6 日で 203/224)」と書いていた。誤りだった**(run12 監査の指摘 11)。
  > **D は ΔOI の差分で、建玉プロファイルの被覆に依らない。**
  > **F1 の主軸 D の n は、標本 6 日で 224、判定区間の見積もりで 21,198(3 分位で 7,098 / 7,098 / 7,003)。**
  > **【本版で足した・prereg 監査(2 回目)の指摘 5】この 21,198 は metrics の欠測を引いていない。**
  > **判定区間 456 日のうち 97 日は metrics が在庫に無い**(§8.6 の走査)。
  > **§8.3 の D 群の MDE は、metrics が在庫にある前提の n で出した値である。**
  > **判定のときに使う n は走行後の実件数(欠測を引いた後)なので、§10.1 の MDE 列は
  > §8.3 より大きく出る**(**推定**: 一様の仮定で約 1.1318 倍 = 当日 + 前日の 356 日で見た値。§8.6)。

### 元: 10. 結果の読みで必ず出す表(§4.3) / 旧 行 2677-2698

> **【本版で直した・prereg 監査(2 回目)の指摘 3・4。リードの決定】**
> **前版の 4 分岐は、\|t\| < 3.925 のセルを「差の絶対値 < MDE → 不明」/「≥ MDE → 差なし
> (検出できる検出力で不在を測った)」に分けていた。2 つの理由で消した。**
>
> **(理由 1・prereg 監査(2 回目)の指摘 3)** 括弧の中の「検出できる検出力で不在を測った」は `CLAUDE.md` §5.2 の
> **陰性の定義そのもの**である。一方 §1 は「**△ 8 件 / ✕ 4 件が残ったまま走るので、
> 段 A の結果はどの行についても「陰性」にはならない**」と書いている。
> **同じ文書が、陰性は出ないと書きながら陰性の判定語を表の列に置いていた。**
> **本版は「差なし」「陰性」の語を §10.1 から消した。**
>
> **(理由 2・prereg 監査(2 回目)の指摘 4)** §8.6 は「**「欲しい効果」の大きさは置かない**」としている。
> **欲しい効果を置かないまま、観測された差を陰性と不明の境界に使うことは、
> `CLAUDE.md` §5.2 の「陰性と不明を分ける」と両立しない**(境界が事前に決まっておらず、
> **出てきた差で境界が動く**)。**本版は観測差を分岐に使わない。**
> **「MDE との比較」列は残す**(検出力の大きさを読む材料として。**分岐には使わない**)。
> **「欲しい効果」は本版でも置かない**(§8.6 のまま。**prereg 監査(2 回目)の指摘 4 への答えは「境界を外す」であって
> 「欲しい効果を置く」ではない**)。
>
> **「検出されず」は陰性ではない。**意味は「**この n とこの MDE では差を検出できなかった**」だけである。
> **「差が無い」「効かない」と読み替えて書かない。**
> **この変更で、バー(\|t\| ≥ 3.925)・α・族(576)・h・軸は 1 つも動いていない。**
> **走行の前の変更である。**§13 の「報告する変更」に足した。

### 元: 12. 射程(何を測っていないか。「検出されず」「測定不能」を書くときに必ず併記する) / 旧 行 3132-3134

  > **【本版で足した・prereg 監査(2 回目)の指摘 5】前版は §8.6 に「主軸 D は減らない = 224/224、欠測 0(実測)」と
  > 書いていたが、**根拠は標本 6 日だけだった。**標本 6 日は 6 日とも metrics が在庫にある(**実測**)。
  > **判定区間 456 日では 97 日が落ちる。**§8.6 の記述を「標本 6 日では」に限定した。

