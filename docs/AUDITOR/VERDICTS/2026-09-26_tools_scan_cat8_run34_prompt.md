# 監査: 道具サーベイ 区分 8 の 34 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run34_prompt.md`(8-035 Apache Spark の E4 だけ。33 回目の起動文から作った)。

## 監査 131 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run34_prompt.md@a7597baac81e`)の逐語

> 対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run34_prompt.md`(指紋 `20260923_tools_survey_cat8_run34_prompt.md@a7597baac81e`)。以下、番号順。
>
> 1. [聞く] `20260923_tools_survey_cat8_run34_prompt.md:20` と `:60` — §1(line 20)は「**33 回目に読んだ頁(`docs/streaming/structured-streaming-kafka-integration.md` など)は案 B の『読んだ範囲』に入れてよい**(33 回目の検収 §4 の処置 6)」と書く一方、§2.4(line 60、run33 から無変更)は「生ログはこの回のものだけを根拠にする(ほかの回の検索の結果を引かない)」と書いている。「要素と段」表の 7 列目(生ログの行)は `docs/DATA/probes/20260923_tools_8_run34.log:10` の形式だけを許す規則(§4)なので、33 回目に読んだページを「読んだ範囲」に算入してよいとしても、それを E4 行の根拠欄・7 列目にどう書けばよいか(run33 のログ行を引用してよいのか、それとも run34 で読み直して初めて 7 列目に書けるのか)がこの起動文に書かれていない。33 回目の検収 §4 処置 6 は「この回に読んだ〜」という表現で、34 回目自身が読み直すことを前提にしているようにも読め、run34 の起動文の書き方(「33 回目に読んだ頁は…入れてよい」)とニュアンスが微妙にずれている。調査班が run33 のログを「根拠」として直接引用してしまうと §2.4 に反し、`check "" run34.log` や `check-elements` の機械検査でどう扱われるか不明。
>
> 2. [直す] `20260923_tools_survey_cat8_run34_prompt.md:82` — 「この回の節に置くもの」が「**31 回目の節と同じ小節を置く**」と書かれているが、§0 の 4(`:14`)は「**小節・表の形はこの節に合わせる**」として明示的に **33 回目の節** を今回のひな型に指定している。line 82 は run33 の起動文(`..._run33_prompt.md:86`)の文言をそのまま写しており、run34 では「33 回目の節と同じ小節を置く」に更新すべきところが取り残されている(実質は 33 回目も 31 回目と同じ小節構成のはずだが、同一文書内でひな型の参照先が 31 と 33 に割れており、読み手が混乱しうる)。
>
> 3. [直す] `20260923_tools_survey_cat8_run34_prompt.md:20` — 「E4 の `印` を探し尽くし、見つからなければ `未判別` のまま案 B の記録を書く」という E4 専用の書き方は、§1 の一般規則(`:23`、「500 行以下なら `cat8_search.py` を打ち、当たりを全部判定する」)が定める「見積もりが 500 行以下なら全件検索して `なし`(または新たな `印`)もありうる」という分岐を明示的に書いていない。line 20 の末尾で `:23` の見積もり手順に言及してはいるが、見積もりが 500 行を超えなかった場合に `未判別` 以外の結論(`なし`)へ進む可能性があることは、E4 専用の記述だけを読むと読み落としうる(E4 の語の組がリポジトリ全体では大きくなりがちなので実害は小さいと見られるが、起動文の記述としては分岐が非対称)。
>
> 4. [直す] `20260923_tools_survey_cat8_run34_prompt.md:66` — 見出し `### 2.5 §4.0 の表`(`:62`)の配下に、§4.0 の表自体は「書かない」（`:64`）と明記した直後、`--sandbox` を使った「最初の手」の詳細な手順が置かれている。§4.0 の表と無関係な内容が旧セクション名の下に残っており(run33 の §2.5 が §4.0 の表作成の実務全般を扱っていた名残)、節見出しと中身が対応していない。実行上の支障は無いが、次回以降さらに使い回すと迷いやすい。
>
> 5. 確認した事項(問題なし、参考まで): §0-4 の行番号「45344 行目」は `## 区分8 — 33 回目の実行` の実際の見出し行と一致。§1 の「33 回目の生ログ 1220 行の `listed=24454`」は `docs/DATA/probes/20260923_tools_8_run33.log:1220` の実際の出力(`cat8_mklist: complete ... listed=24454 ... candidate=8-035`)と一致。§1 の台帳の値(E1a 印段4/E1b 印段2/E2 印段3/E3a 未判別/E3b 印段3/E4 未判別/E5 印段2/E6 未判別、状態「浅い」)は `docs/DATA/tools_catalog_cat8.tsv` の 8-035 行(37 列目までの実データ)と完全一致。これは 33 回目の検収(監査 130 回目)の処置 2・処置 6(E2・E3b を段 3 に下げ、E4 を未判別に戻す)を正しく反映した値であり、`docs/DATA/SCAN_2026-09-23_tools_cat8.md` の run33 節本文(知見 12・14、候補の一覧 45427 行目、要素と段の表 45713・45715・45716 行目)には未訂正の旧値(E2・E3b 段 4、E4 「印・段 3」)がそのまま残っているが、これは「調査班の節は書き換えない」規則どおりで、末尾の「リードの追記(検収、監査 130 回目)」(45956〜45964 行目)が正しい値を上書きしている。台帳の「最後の記載の回=33・最後の記載の行=45427」も実際の候補一覧の行と一致。`scripts/cat8_step.py` の `--sandbox` 実装(`TMPDIR`・`PIP_CACHE_DIR`・`XDG_CACHE_HOME`・`GRADLE_USER_HOME`・`MAVEN_OPTS`・`JAVA_TOOL_OPTIONS` を付与し、見出し行に `[sandbox <道>]` を書き、`--sandbox` 省略時は生ログ末尾の直近のタグを引き継ぐ)は起動文の説明と一致。scratchpad を実地に確認したところ `venvs/8-035/list.txt` は NUL 区切りで実測 24,454 エントリ(`tr -cd '\0' | wc -c` = 24454)であり、起動文が「取り直さない」とする N の値と一致する。33 回目が残した `/tmp/blocklines.txt` は `run33_tmp_cleanup/` に退避済みで `/tmp` 直下には残っていない。オーナー逐語(L-506・L-507・L-508・L-509・L-516・L-519)の引用は既存回から無変更で、新規に追加された `--sandbox` の記述は「33 回目の検収 §4 の処置 1・6」(リードの処置)に正しく帰属しており、オーナー逐語として誤って書かれた箇所は見つからなかった。E4 の当て方(「時刻順の再生」の逐語を要求し、汎用の到着順再生では当たらないとする記述、`:20`〜`:21`)も 22・23・28・33 回目の検収の基準をそのまま踏襲しており、安全側・甘い側いずれかに偏った記述は見当たらなかった。
>
> 他 0 件省略(見つけた指摘は上記 4 件のみ)。

## 処置(リード、監査 131 回目)

1. **答える・直した**。この回に読み直す前提である。33 回目に読んだ頁も、この回に `cat8_step.py` で読み直して生ログに出したうえで案 B の「読んだ範囲」に入れ、根拠と 7 列目に引くのはこの回の生ログの行だけ、と書いた。
2. **直した**。「33 回目の節と同じ小節を置く」にした。
3. **直した**。「見積もりが 500 行以下なら `cat8_search.py` を打って当たりを全部判定し、`なし` もありうる(500 行を超えれば `未判別` のまま案 B の記録)」と書いた。
4. **直した**。見出しを「§4.0 の表と、最初の手・最後の手」にした。

直したあとの起動文の印: `20260923_tools_survey_cat8_run34_prompt.md@991a2eee1799`。止める指摘は無いので、34 回目を起動する。
