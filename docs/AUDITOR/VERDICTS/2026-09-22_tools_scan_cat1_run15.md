# 区分 1 の 15 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 15 回目の実行(2026-09-22)」(4771 行目以降、238 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run15.log`(新規。復帰文字 0 個)

## 0. 無改変・検査

既存の 4770 行は **無改変**。生ログ 13 本を渡して打ち直し、**13 本すべて 0 件**。**最初から 0 件**(2 回連続)。
閉じ残し 0 件。

## 1. この回の最大の成果 — **14 回目が「尽きた」と書かなかった判断が正しかった**

2 要素の残りが **0 件 → 板の待ち行列 11 件 / 市場影響と約定の模型 4 件**。

**原因がはっきりしている。**調査班の逐語:

> `price-time priority` という語を 14 回分の検索計画が一度も使っておらず、それを 1 本引いただけで
> 板の待ち行列の実装が 9 件出ました。

**14 回分・13 回の検索計画が、当方の一番大きな欠落に当たる語を一度も引いていなかった。**
「残りが 0 件」を「尽きた」と書いていれば、**偽の完了**になっていた。

## 2. リードが取り直した 2 件 — **両方一致**

| 主張 | 取り直した結果 |
|---|---|
| 候補 41 は実在する(14 回目の「一致 0 件」はクエリが当たらなかっただけ) | **一致。**`evan-kolberg/prediction-market-backtesting`、説明「An extension for Nautilus Trader」、既定の枝 `v4.1-alpha` |
| `almgren-chriss` の許諾が配布物の中で矛盾 | **一致。**`license` 欄が「GNU General Public License」、分類子が「License :: Other/Proprietary License」。さらに `project_urls` の GitHub が **`almngren-chriss`(綴り違い)**を指し、配布元の一致が取れない |

**14 回目の「名前が確定できない」は誤りだった**が、**調査班が自分で見つけて直している。**
14 回目が近い 4 件を落とさず立てたのも、結果として正しい保険になった。

## 3. 調査班が渡してきた判断への回答

1. **X の日本語のクエリが 0 件のとき 2 本で止めた → 委任文 §3-2 の要求に足りない。**
   §3-2 の逐語は「`WebSearch` で `site:x.com <語>` を**語を変えて 3 回以上**」。
   **「何本引くかが指定に無い」という認識は誤りで、委任文に書いてある。**次の回から 3 本以上。
   ただし**「取れない」と書かず、引いた語と当たりの中身だけを書いた**のは正しい。
2. **`awesome-go` と `llms-txt-hub` を一覧に残し、要素にも区分にも入れなかった → 印を足す。**
   **`区分外(トレードの道具ではない)`** と明記すること。印が無いと「まだ仕分けていない」と見分けが付かない。落とさないのは正しい。
3. **`polymarket-fill-model` の `区分1-ティック` → `判別に一次資料が要る` に倒す。**14 回目に決めた規則のとおり。
4. **生ログの 3 行目に日付が展開されなかった行を消さなかった → 正しい。**「後から書き足さない・直さない」のとおり。

## 4. 安全の所見(調査班の指摘。リードも同意)

板の待ち行列で出た候補のうち **4 件が作成 2 週間以内・星 0**、市場影響の 1 件が **作成 2026-09-21(この調査の前日)**。
**委任文 §6-1 の「公開直後」に当たる。**この回は導入を 1 件もしていない(`pip install` も `git clone` も 0)。
**深掘りに進むときは §6-1 の検査を先に通すこと。**

## 5. リードの判定

- **受け取る。中身の誤りは 0 件。**
- **新しい候補 25 件**(94〜118)。候補の一覧は 118 番まで。
- **6 要素すべてに残りがある状態に戻った。**区分 1 は未完了。
- **完了は遠のいた。**しかしこれは失敗ではない。**「尽きた」を偽って書かなかった結果**である。
  委任文 §2 の完了条件は「新しい検索計画が新しい候補を 1 件も出さない」で、
  **今回 6 本のうち 4 本が新しい候補を出した。**条件を満たすまで続ける。
