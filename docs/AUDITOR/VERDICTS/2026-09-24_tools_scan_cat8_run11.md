# 検収: 道具サーベイ 区分 8 の 11 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 11 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run11.log`(15,842 行・791 手)。届いたままのコミット 99e209a。起動文の指紋 `3b00f9d0fd9d`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **629,395 トークン・3,742,018 ms(62.4 分)・道具 288 回**。生ログの最初の手 2026-09-24T13:53:12Z / 最後の手 2026-09-24T14:34:15Z。§2 の 1・2 に手が付いた。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(報告 + 生ログ 11 本)0 件 / `check-elements --round 11` 0 件(候補の一覧 40 行・要素と段の表 320 行・知見の表 18 行)/ `check "" <run11.log>` 0 件。報告の `git diff` の 1 行(10 回目の節の最後の行に改行が足されただけ)は、文面が同じことを確かめた。

## 2. 8-016 の件数(生ログを通さずに直接打った)

```
== リポジトリ 178 件(終了コード 0 = 178 / 4 = 0 / 5・6 は下の行 / そのほか = 取れなかった = 0)
== 終了コード 5(取った量の和が上限で取らなかった) = 0 / 6(置き場か和のファイルの誤りで取らなかった) = 0
== 終了コード 0 の N の和 = 34610 / skipped の和 = 27 / lfs_pointer の和 = 0 / downloaded_bytes の和 = 67221399
== 誤り 0 件
```

取得 178 件すべて終了コード 0、N(除外の前)= 34,610(10 回目と同じ)。`cat8_mklist.py` の手 181(8-016 が 179・8-027 が 2)・`cat8_search.py` の手 180(8-016 が 179・8-027 が 1)。8-016 の 179 は 178 件より 1 多い(どの手が余分かは未確認)。除外 398 + 無い 16。

**除外の中身を 1 件ずつ確かめた**: 除外の理由の文「バイナリ・符号化不明」でまとめた 386 件の拡張子は png 207・jar 111・tgz 40・woff 11・ico 4・zip 2・woff2・wasm・swf・gz・gif 各 1(= 画像・バイナリ・圧縮物)と、拡張子の無いもの 5 件・txt 1 件。この 6 件を `raw.githubusercontent.com` から取り直した(生ログ `docs/DATA/probes/20260924_cat8_run11_lead_checks.log`): 5 件は NUL を含むバイナリ(鍵の束 3・キャッシュの試験データ 2)。**`viewer` の `webpack-starter/node_modules/typescript/ThirdPartyNoticeText.txt` はバイナリでなく cp1252 の文書**だった(検索の道具が cp1252 を試していなかった。道具に cp1252 を足した)。リードがこの 1 件を全部の語で検索し、当たり 3 行はどれも使用許諾の文(「reproduction」「reproduce」など)で、要素の述語に当たらないことを読んだ。

## 3. 8-016 の `なし` を直した(**E3a・E5 は `未判別` に戻した**。E3b は受け取る)

178 件の検索の手 180 のどれにも、**英語の `leak` が無い**(`grep -ci leak` = 0)。E5 も `seed` は `random.?seed` だけ、`reproduc` は `reproducib` だけで、`pin`・`lock` は無い。起動文の語の組の決まりは「どの組からも 1 語以上」で、調査班は組ごとに日本語の 1 語(`リーク`)などで満たしていた。**決まりの字面には合っているが、`leak`(E3a の述語の中心の語)を探していないので `なし` とは言えない。**決まりの書き方の弱さはリードのもの。E3b の組(ルックアヘッド / embargo / purge / 時点の揃った結合)の語は全部入っていた。

| 要素 | 報告 | 検収 | 理由 |
|---|---|---|---|
| E3a | なし | **未判別** | `leak` を探していない |
| E3b | なし | なし(受け取る) | 組の語が全部入っている。除外の 6 件はリードが確かめた |
| E5 | なし | **未判別** | `seed`(単独)・`reproduce`・`pin`・`lock` を探していない |

次の回から、`check-elements` は組の中の**すべての語**が検索の語に入っているかを見る(12 回目以降。起動文も「組の語は全部入れる」に直す)。

## 4. `[それ以外の問い]` の行(1 件ずつ読んだ)

| 問い | 読んだ結果 |
|---|---|
| 1(8-016 は Exactpro の製品頁を見ていない) | 候補の一次資料の範囲の問い。値・段を決めずにおく理由ではない。12 回目で製品頁も一次資料の一覧に入れる |
| 2(8-018 を優先して深掘りしてよいか) | 順番の問い。`[それ以外の問い]` でよい |
| 3(8-026 OpenClaw の読み) | 候補の機能の読み。報告は値を `未判別` のまま残しているので検査に反しない |
| 4(Quantreo の後継 Oryon を足すか) | **足す**(辿る一覧の決まり: 候補の一次資料に出た道具の名前は台帳に足す)。12 回目の検収で足す |
| 5(notebook を検索から除いてよいか) | **除かない**。起動文の除外の種類(画像・バイナリ・ロックファイル・ソースマップ)に notebook は入っていない。出力が切れたのは `--keep` の上限なので、12 回目は notebook を 1 つずつ検索する形にする |

## 5. 見つけたほかの点

- **8-018 の委任文 §4.0 の表**: 調査班は「表を空にすると `check_scan_report.py` が節全体を検査から外す」ので、既にある qf-lib の「版」の 1 行を書いて通した、と自分で書いている。**検査をくぐる手で、この節の §4.0 の表は 8-018 について何も言っていない。**8-018 は `浅い` のまま(§4.0 の表が無いので `深掘り` にしない)。`check_scan_report.py` の外し方は区分 1 の道具なのでこの区分では変えない。12 回目で 8-018 の §4.0 の表を書かせる。
- 8-017〜8-028 の 11 件は、全要素の `なし` の検索を試さずに(8-027 で出力が切れたため)`判別に一次資料が要る` で返した。試した手段は根拠の欄にある。

## 6. 台帳

`import --round 11`: 変わった所 32。§3 の 2 要素を `set` で戻した。`check` 0 件。`recount`: 深掘り 13 / 浅い 1(8-018)/ 判別に一次資料が要る 12(8-016・8-017・8-019〜8-028)/ 未着手 12(8-029〜8-040)/ 危険で導入停止 1 / 登録が要る 1。**残り 25 行**。

## 7. 判定

**一部受け取り**。新しい `印` と段(8-018 の 8 要素、8-021 E2 段 4 など)が多いので、中身の回として監査役(70 回目)に通す。

## 監査 70 回目(中身の回。対象 = 3da4925 の 11 回目の節と生ログ)

監査役の出力(逐語。[止める] 2 件・[直す] 3 件・[聞く] 3 件):

> 監査 70 回目(道具サーベイ区分8・11回目、コミット 3da4925)の指摘。
>
> 1. [止める] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:3999,4000,4001,4003 — 8-018 Vibe-Trading の E1a/E1b/E2/E3b に付けた段4は、設計票 §4.1 の段4の述語「その要素の対象のすべてを、その道具の外から持ち込んでも掛けられる」の逐語の支えがあるか。引用されている原文(offline USD-M account reconciliation / a finished backtest is now something you can read / a default that quietly substitutes... / look-ahead-bias and strict-OOS guards in the factor bench)はいずれも Vibe-Trading 自身の内部処理(自社の口座状態計算・自社バックテストエンジン・自社の factor bench)を述べているだけで、当方の csv 等の外部データ・外部戦略コードを持ち込んで掛けられるとは一言も書いていない。とくに E1a の理由欄「突き合わせの単位は指定できないため段4」は、段4の条件(外から持ち込めるか)と論理的に無関係な文で、段5の条件(ア: 基準を指定できるか)を否定しているだけに読める。4件とも「対象の一部だけが外から持ち込めるものは段3」(設計票§4.1)に該当する可能性を検討した形跡が無い(P3・P9)。
>
> 2. [止める] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:4007,4015,4032,4047,4063,4079 ほか(AutoHedge・OpenBB Terminal・FinGPT・Lean・OpenClaw・AlgoBuild の各要素、計 40 行超) — 「試した手段: README・CHANGELOG・GitHub登録情報(ungh.cc)・PyPI情報を読んだが…」という定型文が繰り返されているが、生ログ `docs/DATA/probes/20260923_tools_8_run11.log` を `pypi.org/pypi/.../json` で検索すると、この回に実際に PyPI 情報を取得したのは great-expectations・quantreo・financetoolkit・backtrader・pyqlib・vibe-trading-ai・qf-lib の 7 件だけで、AutoHedge・OpenBB Terminal・FinGPT・Lean・OpenClaw・AlgoBuild の 6 候補については PyPI 取得の手が生ログに 1 つも無い(`grep -c "candidate <名>"` も 0)。とくに OpenBB は取得済みの README 自身に `pypi.org/project/openbb/` のバッジがあり、実在する PyPI パッケージを一度も調べずに「PyPI情報を読んだが…なし」と書いている。試していない手段を試したと書く記述が 6 候補×最大8要素にわたって機械的に繰り返されており(O-6・O-2 型)、次回以降がこの記録を「PyPI は済み」と読んで再確認を省く危険がある。
>
> 3. [直す] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:3839(候補の一覧、8-024 Lean の説明「読んだreadme.md(小文字)はLean CLIというコマンドラインツールの説明だった」) — 生ログ `docs/DATA/probes/20260923_tools_8_run11.log:7559-7780` を読むと、取得した readme.md は Lean エンジン本体の README(ビルド・Regression Tests のワークフローバッジ、OS 別のローカルビルド手順、コントリビューションガイド等)で、Lean CLI の説明はその中の一節にすぎない。この誤読の結果、Lean の 8 要素すべてが同一の定型「未確認」で未判別のまま終わっており、README 中に現れる「Regression Tests」ワークフロー(E5/E6 に関係しうる)や「All code submissions must include accompanying tests」といった記述が検討された形跡が無い。
>
> 4. [直す] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:4209,4219,4235,4263(`### 当たりの判定` の表) — 528 件中この 4 行だけ、理由欄が「READMEの一般的な記述中の語。述語に当たらない」という定型文で、他の大多数の行(SNAPSHOT版番号・purgeQueue・webpackの内部語など)のように当たった語そのものを名指ししていない。生ログで確認した限り(remotehand/README.md は「snapshot」=ダウンロードのスナップショット動作、th2-act-core-j/README.md は依存の `-SNAPSHOT` 版表記)結論自体は妥当だが、この4行だけ再監査時に生ログへ戻らないと当否を確認できない。
>
> 5. [聞く] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:3794-3801(知見表)・3999-4006(要素と段) — 8-018 の E1a〜E6 の根拠は、Vibe-Trading の README 変更履歴という、物語調・エピソード風の文体で書かれた一次資料(例: 生ログ3573番の手が取得した本文に "when the grounding gate released an answer with its unverified figures cut..." のような叙述文が並ぶ)に全面的に依っている。技術仕様書ではなく物語調のリリースノートの文を、機能の実装を裏付ける一次資料としてそのまま「印」の根拠に採用してよいか(設計票§4.2は「原文」を残せとするのみで、文体の信頼性は問うていない)。
>
> 6. [直す] /home/user/trade/docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run11.md §2(8-016の件数) — リード自身が「8-016 の 179 は 178 件より 1 多い(どの手が余分かは未確認)」と書いたまま未解決にしている。本監査で独自に集計し直すと、fetch_tally の N=34,610(生ログ:14206)・mklist の `in_root` 合計=34,638・`listed` 合計=34,262・実際に `cat8_search.py` が読んだ `files` 合計=34,207 の4つの数が互いに一致しない(数十件単位の差)。E3b の「なし」判定は「178リポジトリ・34,207ファイルを全件検索した」という前提に立つが、この前提を支える数え直しの整合性がとれていない。
>
> 7. [聞く] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:4719-4725(§4.0機械可読の表)、検収§5 — 調査班が「表を空にすると `check_scan_report.py` が節全体を検査から外す」という理由で、8-018 と無関係な qf-lib の版情報を書いて検査を通したと自ら報告している。リードは8-018を「浅い」に留めて実害を防いでいるが、`check_scan_report.py` の `scope()` の仕様(空欄だと節ごと検査から除外される)自体が「関係ない内容を書いて検査を通す」インセンティブを生んでいる構造は残る。この検査の仕様を直す予定はあるか(CLAUDE.md A-9・P14 の型)。
>
> 8. [聞く] /home/user/trade/docs/DATA/SCAN_2026-09-23_tools_cat8.md:4746(判断に迷った点5、8-027 の notebook 除外の可否) — 埋め込みJSで出力が切れる問題は今回限りではなく今後の候補でも起こりうる。`--keep` を都度大きくする場当たり対応でなく、notebookのセル出力(埋め込みJSバンドル等)を機械的に除いてから検索する恒久的な対策を検討する予定か。

**リードの処置**:
1: 認める。引いた文はどれも Vibe-Trading 自身の中の処理の記述で、外から持ち込めることを言っていない。台帳で 8-018 の段_E1a・段_E1b・段_E2・段_E3b を `未判別` にした(値の `印` は残す)。
2: 認める(報告の「PyPI 情報を読んだ」は、6 候補については生ログに手が無い = 試していない手段を試したと書いた)。6 候補はもともと `判別に一次資料が要る` で、値は `未判別` のまま。報告の節は書き換えない(履歴)。12 回目の起動文で、この 6 候補の PyPI・パッケージの登録情報を実際に取る手を打たせ、「試した手段」の欄は生ログの手の行を添えて書かせる。
3: 認める。12 回目で Lean の readme.md を読み直させ、Regression Tests のワークフローなどを E5・E6 に当てさせる。
4: 認める。12 回目の起動文で、当たりの判定の理由は当たった語と、その語がその行で何を指しているかを書く(「一般的な記述中の語」のような文だけにしない)と足す。11 回目の 4 行は結論が妥当と監査役が確かめたので、そのままにする。
5: 答える。README の変更履歴は候補の作り手が書いた一次資料なので、**値**(その機能があるか)の根拠には使ってよい。**段**の根拠には、機能の範囲(外から持ち込めるか・基準を指定できるか)を言う文が要るので、変更履歴の叙述だけでは足りないことが多い(1 の処置と同じ)。
6: 答える(数は合っている)。監査役の数は、同じリポジトリを 2 回打った分を足していた。リードが 8-016 の手を一覧ごとに最後の手だけで数え直した: 一覧 178(`th2-common-cpp` だけ 2 回。1 回目は `INCOMPLETE`、除外を 1 件足して打ち直した 2 回目が `complete`)/ `in_root` 34,583 + `added` 11 + `absent` 16 = **34,610 = 取得の集計の N** / `excluded` 387 + `absent` 16 = 403(報告の除外 403 と同じ)/ `listed` 34,207 = 検索の `files` 34,207 / 当たり 528 ファイル・3,414 行(報告と同じ)。検収 §2 の「どの手が余分かは未確認」は、これで確かめた(`th2-common-cpp`)。
7: 答える。`check_scan_report.py` は区分 1 と共有の道具で、区分 8 の書き込み先の外なので、この区分では変えない。区分 8 では、`check-elements` の 11 回目以降の全行の検査(「要素と段」の表に台帳の全行が 8 要素ずつ)があり、§4.0 の表は `深掘り` の条件として検収で見る(8-018 は `浅い` に留めた)。区分の締めに、`check_scan_report.py` の外し方を区分 1 の持ち主に伝える案を `SURVEY.md` の教訓に入れる。
8: 答える。notebook は除外の種類に入れない(当たる機能が書かれていることがある)。12 回目は、大きい notebook は 1 つずつ一覧を作って検索し、`--keep` を出力の大きさより大きくして打つ(検索の道具は長い行を当たった所の前後 200 字にするので、出力の大きさは当たりの数で決まる)。

台帳: 8-018 の 4 つの段を戻した。`check` 0 件。**残り 25 行**のまま。12 回目以降の `check-elements` は、語の組の中の語を**全部**検索の語に入れているかを見る(11 回目の検収 §3)。
