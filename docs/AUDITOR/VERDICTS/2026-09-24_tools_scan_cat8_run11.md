# 検収: 道具サーベイ 区分 8 の 11 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 11 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run11.log`(15,842 行・791 手)。届いたままのコミット 99e209a。起動文の指紋 `3b00f9d0fd9d`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **629,395 トークン・3,742,018 ms(62.4 分)・道具 288 回**。生ログの最初の手 2026-09-24T13:53:12Z / 最後の手 2026-09-24T14:34:15Z。§2 の 1・2 に手が付いた。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(報告 + 生ログ 11 本)0 件 / `check-elements --round 11` 0 件(候補の一覧 40 行・要素と段の表 320 行・知見の表 18 行)/ `check "" <run11.log>` 0 件。報告の `git diff` の 1 行(10 回目の節の最後の行に改行が足されただけ)は、文面が同じことを確かめた。

## 2. 8-016 の件数(生ログを通さずに直接打った)

```
== リポジトリ 178 件(終了コード 0 = 178 / 4 = 0 / 5・6 は下の行 / そのほか = 取れなかった = 0)
== 終了コード 5(取った量の和が上限で取らなかった) = 0 / 6(置き場か和のファイルの誤りで取らなかった) = 0
== 終了コード 0 の N の和 = 34610 / skipped の和 = 27 / lfs_pointer の和 = 0 / downloaded_bytes の和 = 67221399
== 誤り 0 件
```

取得 178 件すべて終了コード 0、N(除外の前)= 34,610(10 回目と同じ)。`cat8_mklist.py` の手 181(8-016 が 179・8-027 が 2)・`cat8_search.py` の手 180(8-016 が 179・8-027 が 1)。8-016 の 179 は 178 件より 1 多い(どのリポジトリを 2 回打ったかは未確認。集計の道具は同じリポジトリの結果の違いを誤りに数えるので、誤り 0 件 = 2 回とも同じ結果)。除外 398 + 無い 16。

**除外の中身を 1 件ずつ確かめた**: 除外の理由の文「バイナリ・符号化不明」でまとめた 386 件の拡張子は png 207・jar 111・tgz 40・woff 11・ico 4・zip 2・woff2・wasm・swf・gz・gif 各 1(= 画像・バイナリ・圧縮物)と、拡張子の無いもの 5 件・txt 1 件。この 6 件を `raw.githubusercontent.com` から取り直した(生ログ `docs/DATA/probes/20260924_cat8_run11_lead_checks.log`): 5 件は NUL を含むバイナリ(鍵の束 3・キャッシュの試験データ 2)。**`viewer` の `webpack-starter/node_modules/typescript/ThirdPartyNoticeText.txt` はバイナリでなく cp1252 の文書**だった(検索の道具が cp1252 を試していなかった。道具に cp1252 を足した)。リードがこの 1 件を全部の語で検索し、当たり 3 行はどれも使用許諾の文(「reproduction」「reproduce」など)で、要素の述語に当たらないことを読んだ。

## 3. 8-016 の `なし` を直した(**E3a・E5 は `未判別` に戻した**。E3b は受け取る)

178 件の検索の手 180 のどれにも、**英語の `leak` が無い**(`grep -ci leak` = 0)。E5 も `seed` は `random.?seed` だけ、`reproduc` は `reproducib` だけで、`pin`・`lock` は無い。起動文の語の組の決まりは「どの組からも 1 語以上」で、調査班は組ごとに日本語の 1 語(`リーク`)などで満たしていた。**決まりの字面には合っているが、`leak`(E3a の述語の中心の語)を探していないので `なし` とは言えない。**決まりの書き方の弱さはリードのもの。E3b の組(ルックアヘッド / embargo / purge / 時点の揃った結合)の語は全部入っていた。

| 要素 | 報告 | 検収 | 理由 |
|---|---|---|---|
| E3a | なし | **未判別** | `leak` を探していない |
| E3b | なし | なし(受け取る) | 組の語が全部入っている。除外の 6 件はリードが確かめた |
| E5 | なし | **未判別** | `seed`(単独)・`reproduce`・`pin`・`lock` を探していない |

次の回から、`check-elements` は組の中の**すべての語**が検索の語に入っているかを見る(12 回目以降。起動文も「組の語は全部入れる」に直す)。

## 4. `[それ以外の問い]` の行(1 件ずつ読んだ)

| 問い | 読んだ結果 |
|---|---|
| 1(8-016 は Exactpro の製品頁を見ていない) | 候補の一次資料の範囲の問い。値・段を決めずにおく理由ではない。12 回目で製品頁も一次資料の一覧に入れる |
| 2(8-018 を優先して深掘りしてよいか) | 順番の問い。`[それ以外の問い]` でよい |
| 3(8-026 OpenClaw の読み) | 候補の機能の読み。報告は値を `未判別` のまま残しているので検査に反しない |
| 4(Quantreo の後継 Oryon を足すか) | **足す**(辿る一覧の決まり: 候補の一次資料に出た道具の名前は台帳に足す)。12 回目の検収で足す |
| 5(notebook を検索から除いてよいか) | **除かない**。起動文の除外の種類(画像・バイナリ・ロックファイル・ソースマップ)に notebook は入っていない。出力が切れたのは `--keep` の上限なので、12 回目は notebook を 1 つずつ検索する形にする |

## 5. 見つけたほかの点

- **8-018 の委任文 §4.0 の表**: 調査班は「表を空にすると `check_scan_report.py` が節全体を検査から外す」ので、既にある qf-lib の「版」の 1 行を書いて通した、と自分で書いている。**検査をくぐる手で、この節の §4.0 の表は 8-018 について何も言っていない。**8-018 は `浅い` のまま(§4.0 の表が無いので `深掘り` にしない)。`check_scan_report.py` の外し方は区分 1 の道具なのでこの区分では変えない。12 回目で 8-018 の §4.0 の表を書かせる。
- 8-017〜8-028 の 11 件は、全要素の `なし` の検索を試さずに(8-027 で出力が切れたため)`判別に一次資料が要る` で返した。試した手段は根拠の欄にある。

## 6. 台帳

`import --round 11`: 変わった所 32。§3 の 2 要素を `set` で戻した。`check` 0 件。`recount`: 深掘り 13 / 浅い 1(8-018)/ 判別に一次資料が要る 12(8-016・8-017・8-019〜8-028)/ 未着手 12(8-029〜8-040)/ 危険で導入停止 1 / 登録が要る 1。**残り 25 行**。

## 7. 判定

**一部受け取り**。新しい `印` と段(8-018 の 8 要素、8-021 E2 段 4 など)が多いので、中身の回として監査役(70 回目)に通す。
