# 監査: 道具サーベイ 区分 8 の 29 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md`(8-032 Apache Kafka の残り。27 回目の起動文から作った)。

## 監査 121 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run29_prompt.md@d7d0c6fe3e29`)の逐語

> ## 監査 121 回目 — 道具サーベイ区分 8・29 回目の起動文
>
> 1. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md:22` — §1 の 2「当方データ投入」は、28 回目に導入した同じ環境(`venvs/8-032/check/kafka_2.13-4.3.1/`・`run/`)を再利用してブローカーを新たに起動し、tardis 形式の CSV を投入・再生する新しい**実行**を指示している。しかし 28 回目の検収 §4 処置 1 は「実行のあとに §6-1 の手があった回の実測の値は『§6-1 を済ませる前の導入による値』として扱う」「次の新しい候補の回からは、実行のあとに §6-1 の手が 1 つでもあれば実測の値をリードが受け取らない、と起動文に書き結果に効く形にする」と定めた。29 回目は「新しい候補の回」ではなく同一候補(8-032)の続きだが、この処置が定めた新しいルールが継続回の新規実行(§1 の 2 のデータ投入)にも及ぶのか、及ばないのかを起動文はどこにも書いていない(「§6-1」の語自体が起動文に 1 箇所も現れない)。28 回目の当方データ投入の実測(生ログ 1766-1774 行)自体も §6-1 完了(1907-1935 行)より前に行われており、28 回目の検収はこの点を「リードの追記」で明記的に触れていない。29 回目で新たに得る実測値に、同じ扱い(§6-1 完了前 or 完了後)を適用するのか、その理由を明記しなくてよいのか(委任文 §6-1、28 回目の検収 §4 処置 1)。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md:21` — 「E1b・E6 は、28 回目に `docs/streams/developer-guide/` の下の個別の頁を読み切っていない(28 回目の報告の返答の 1)」の出典表示。実際に「読み切っていない」と認めているのは 28 回目の報告の `### 判断に迷った点と問い` の 3 番目(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:706`)と 28 回目の検収 §4「8-032 の扱い」("未読の個別頁が残る(探し尽くしていない)")であり、報告本文の候補の一覧(28回目の項目32、`SCAN...md` の該当行、"E1b・E6 は印を探し尽くしたが見つからず")はむしろ「探し尽くした」と書いている。出典を「返答の 1」とするのは、この 2 つの食い違う記述のどちらを根拠にしているか不明瞭で、28 回目自身の報告内の矛盾(「探し尽くした」vs「読み切っていない」)を素通りしている(P2)。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run29_prompt.md:14` — 「報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 28 回目の実行(2026-09-26)` の節(42046 行目から末尾まで…)」。実測では見出し行は 42047 行目で、42046 行目は見出し直前の空行(`grep -n "^## 区分8 — 28 回目" docs/DATA/SCAN_2026-09-23_tools_cat8.md` → `42047:...`)。実害は小さい(空行を含めるだけ)が、この起動文が §7 で「回の番号・生ログ・件数」の突き合わせを厳密に要求する流儀と比べると、行番号の指示は 1 行ずれている(P2)。
>
> その他の決定的検査(Debezium 固有の記述の残留、回の番号・生ログ・`--round`・8-001〜8-041 の 41 行の整合、E2/E4 の当て方の逐語、E1a/E5 を据え置く根拠、§1 で引用された 28 回目の見積もり行(E1b 5,285・E2 57,158・E4 69,920・E6 211,597、`run28.log:375-395`)、listed=7509 と 7506 の 3 件差の記載)はすべて生ログ・台帳・検収の記載と突き合わせて一致を確認した。台帳の値(E1a 印/段3・E3a なし・E3b なし・E5 印/段2・E1b/E2/E4/E6 未判別・状態「浅い」)も `tools_catalog_cat8.tsv` の 8-032 行と一致する。§4.0 の未確認 7 項目のうちこの回が拾った 3 項目(宣伝詐欺の兆候・当方データ投入・初回公開日)は、起動文が禁じる理由(「この回では実施していない」「未投入」)に該当するものであり、残る 4 項目(コミット数・週DL数・規模の見積・4軸4_向上)はいずれも既に許容される理由(API非公開・既存回の先例)が付いているため、この回で拾わなくても矛盾はない。
>
> **止める指摘の有無**: なし。

## 処置(リード、監査 121 回目)

1. **答える・直した**。`kafka_2.13-4.3.1` の §6-1 は、28 回目に全部の項目の手が済み、止める所見は無い。28 回目は一部の手が実行のあとだったので、28 回目の実測の値(当方データ投入の実測を含む)は「§6-1 を済ませる前の導入による値」と記録した(28 回目の報告の節の末尾の「リードの追記」の「この回の最小実行の実測の値」は、同じ回の当方データ投入の手も含む)。**この回の実行は §6-1 を全部済ませたあとなので、この回の実測の値はその扱いにならない**。新しい配布物・依存を 1 つでも取ってきたら、その回の実測の値はリードが受け取らない。起動文にそう書いた。
2. **直した**。出典を「28 回目の報告の問い 3 と 28 回目の検収 §4 の 8-032 の扱い」にし、候補の一覧の「探し尽くした」と食い違うこと、この回は「読み切っていない」を正とすることを書いた。
3. **直した**。42047 行目の見出しから、にした。

直したあとの起動文の印: `20260923_tools_survey_cat8_run29_prompt.md@cd0c7b5b9f33`。止める指摘は無いので、29 回目を起動する。
