# 検収: 道具サーベイ 区分 8 の 26 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 26 回目の実行(2026-09-26)`(40831 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run26.log`(251,977 バイト)。起動文の印 `20260923_tools_survey_cat8_run26_prompt.md@378392fed1ce`。

## 0. 費用と時間

調査班 550,073 トークン・2,367,873 ms(39.5 分)・道具 254 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 26` **6 件**(scratchpad の外を使う手。処置 4・5)/ `check "" run26.log` 0 件 / 過去の節から消えた行 0。

## 2. 台帳

`import --round 26`(変わった所 5): 8-031 は `未着手` → `浅い`、E2 印/3・E3a なし・E3b なし・E4 印/3。**監査 116 回目の結果を見てから** `set 8-031 E2=未判別 段_E2=未判別 E4=未判別 段_E4=未判別`。`check` 0 件。

## 3. 中身の監査(116 回目、owner-auditor)の逐語

> ## 監査 116 回目 報告
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 26 回目の実行(2026-09-26)`(40831〜41499行)、生ログ `docs/DATA/probes/20260923_tools_8_run26.log`、起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md`、設計票 `docs/DATA/surveys/CAT8_DESIGN.md`、委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md` §5・§6。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:41166,41394` — Debezium の E4 を「印・段3」のまま確定し、問い#2(41394行)は「それ以外の問い」に分類しているが、その問い自体は「E4述語の『記録した市場データ』にDebeziumの『データベースの変更ログ』が当てはまるか」という値そのものの当否を尋ねている。同一文書内の同型の論点(`OpenClaw` の `jsonl-replay.ts`、市場データでない会話記録の再生)は16回目(33491行)で明確に「[値・段の問い]」に分類され、「この理解が付くまで値・段は未判別のまま残す」と明記され、以後25回目(41126行)まで10回連続で`未判別`のまま据え置かれている。同じ設計票§3のE4述語をめぐる同型の論点が、Debeziumでは「それ以外の問い」として値を確定させる扱いになっているのは、§2.2(「値・段を決めずにおく理由でない問いは`[それ以外の問い]`」)への違反、または過去の扱いとの不整合ではないか(P2)
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:41163,41393` — Debezium の E2 を「印・段3」のまま確定しているが、24回目検収で確立された規則(この節にも引用されているPrefectのE2判定=「E2述語の対象(時系列・約定・板・足・参照データ)に当たることを言う逐語は見つからなかった」)に照らすと、根拠の`NumberOfErroneousEvents`は「change events(データベースの変更イベント)」を対象とする逐語であり、述語の列挙(時系列・約定・板・足・参照データ)への明示的な言及ではない。報告自身が問い#1(41393行)でこの差を「リードの判断を仰ぐ」としながら「それ以外の問い」に分類し値を確定させているのは、§2.2違反ではないか(P2)
>
> 3. [止める] `docs/DATA/probes/20260923_tools_8_run26.log:914-1050,1249-1360,1344` — §6-1の結論(1030行、target=`6-1_結論_debezium-embedded`)は`debezium-embedded`のjar/pomのみを取得・展開(905-989行)して出されているが、最小実行(phaseA_run、1249行、13:53:47)で実際にPostgreSQLへ接続・操作したのは`debezium-connector-postgres`であり、そのjarの中身を開く手(1344行、13:56:26、note「§6-1補強」)は最小実行の完了(pg_ctl_stop、1311行、13:55:02)より後に打たれている。報告の知見6(40861行)はこれを「§6-1の結論(危険の所見なし)」として両方のjarをまとめて記述しており、両方が実行前に検査済みだったかのように読める。§2.5「最小実行の前に…§6-1の導入前の検査を全部やる」「§6-1の結論の行を生ログに出してから、導入・実行の手を打つ」という順序の要件を、実際に実行された配布物(connector-postgres)について満たしているか(委任文§6「安全の規則」の趣旨)
>
> 4. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:41479-41489`(起動文§7-2、`docs/DATA/delegations/20260923_tools_survey_cat8_run26_prompt.md:95`)— `cat8_ledger.py check-elements --round 26`が6件のまま返され、起動文§7-2「を0件にする」を満たしていない。24・25回目(40307行・40824行)はいずれも0件で通っており、非0のまま返されたのはこの回が初めて。理由の注記のみで完了条件自体は達成されていない
>
> 5. [聞く] `docs/DATA/probes/20260923_tools_8_run26.log:1111,1124,1142,1148,1153` — 上記4の6件はいずれもscratchpadの祖先ディレクトリ(`/tmp/claude-0`等、ハーネスのディレクトリ)への`chmod 711`。委任文§5-5・起動文§3「書いてよいのは…の2つだけ。scratchpad以外にファイルを作らない」が、scratchpadの外側(祖先ディレクトリ)の権限変更(新規ファイル作成ではない)を許容範囲に含むかどうかは明記が無い。この操作を今後も許容してよいかはリード・オーナーの判断が要るのではないか
>
> 6. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:41362,41381` — 「宣伝詐欺の兆候」行の印(根拠の種類)欄は`推定`だが、同じ理由(「専用検索は未実施」)が「§4.0で未確認のまま残した項目」一覧(41381行)にも再掲されている。他の未確認5項目(41339,41341,41363,41366,41370行)は印欄がすべて`未確認`で一貫しているのに対し、この1行だけ表記が食い違っている
>
> 補足(問題なしと判断した点): E3a・E3bの「なし」判定(甲の表、41252〜41324行)は当たり全行に個別の引用と理由が付き、同じ文言の複写(K13型)は見られなかった。台帳から引き継いだ「他の行の写し」(qf-lib・OpenClaw・Prefect等をサンプル抽出)は25回目の値と一致しており、改変や取りこぼしは見当たらなかった。
>
> 止める指摘の有無: あり(1・2・3)。

## 4. 処置(リード、監査 116 回目)

1. **止める → E4 は `未判別`**。E4 の述語は「**記録した市場データ**を時刻順に再生して、**戦略・執行・計算を再実行する**」。Debezium が発生順に流すのはデータベースの変更の記録で、市場データであることも、戦略・計算を再実行することも言う逐語が無い。16 回目の OpenClaw(会話の記録の再生)と同じ当て方。`なし` ではない。
2. **止める → E2 は `未判別`**。`NumberOfErroneousEvents` の対象はデータベースの変更のイベントで、述語の列挙(時系列・約定・板・足・参照データ)を言う逐語が無い。24 回目の Prefect と同じ当て方。`なし` ではない。
3. **止める → 記録した(結論は変わらない)**。`debezium-connector-postgres` の jar の中身は実行のあとに開いた(1344 行)。出力に止める所見は無い。**§6-1 の一部が実行のあとになったのは 21・24・26 回目の 3 回目**。起動文の「実行の前に全部」の文では止まらない。27 回目以降の起動文では、**§6-1 の結論の行に、導入・実行で使う配布物(依存を解決して入る主な成果物を含む)の名前を全部並べ、その全部を開いた手の行を引く**ことにし、検収でリードが時刻と名前を突き合わせる。
4. **直した(記録)・起動文の誤り(リード)**: `check-elements` 6 件は、起動文 §3「scratchpad 以外にファイルを作らない」への違反を検査が捕まえたもの。1 件(56 行)は一時ファイル。5 件はハーネスのディレクトリの権限の変更。
5. **答える・直した**。scratchpad の祖先のディレクトリの権限を変えることは、起動文の範囲に無い(ハーネスのディレクトリで、ほかの作業に効きうる)。リードが検収で 700 に戻した。さらに、**`mvn package` が `/root/.m2` に 76 MB のキャッシュを作っていた**(リードが見つけた。起動文に `-Dmaven.repo.local=<scratchpad の下>` を書かなかったリードの誤り)。作られた時刻を `stat` で確かめてから消した。以後の起動文では、(a) scratchpad の外の権限を変えない、(b) 別の利用者でしか動かないもの(PostgreSQL など)が要るときは、止まって報告する、(c) `mvn` には `-Dmaven.repo.local=<scratchpad の下>`、`pip` など他の道具もキャッシュを scratchpad の下に向ける、と書く。
6. **直した**。「宣伝詐欺の兆候」の根拠の種類を `未確認` とした(リードの追記)。

**8-031 の扱い**: `浅い`。`未判別` は E1a・E1b・E2・E4・E5・E6 の 6 つ。E2・E4 は案 B の記録(読んだ範囲・当たりの行の数)が無い。§4.0 の `未確認` に起動文が認めない理由(専用の検索をしていない・当方のデータを入れていない)が残る。**残りに数える。残り 8 のまま**。

## 5. これからの手

27 回目 = 8-031 Debezium の残り(E2・E4 の `印` の探し尽くしと案 B の記録、§4.0 の残り。新しく何も入れない・動かさない)。
