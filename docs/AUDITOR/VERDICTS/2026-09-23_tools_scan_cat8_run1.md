# 検収: 道具サーベイ 区分 8 の 1 回目(2026-09-23、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 1 回目の実行(2026-09-23)`(478 行)/ 生ログ `docs/DATA/probes/20260923_tools_8_run1.log`(212 行、1 本)。届いたままのコミット 95edb1b。
起動文 `20260923_tools_survey_cat8_run1_prompt.md@0ac68ca101f9`、追補 `20260923_tools_survey_cat8_addendum.md@46258eebcd93`。
手順は `.claude/skills/research-squad/SURVEY.md` §3 の順。

## 0. 費用と時間(完了通知の実測)

調査班 **324,630 トークン・1,376,871 ms(22.9 分)・道具 113 回**。予算の目安 25 万トークンを **約 3 割超えた**(時間は 25 分の内)。報告は予算の超過を書いていない。

## 1. 書き換えの範囲(`git status` / `git diff --numstat`)

新規は報告と生ログの 2 本だけ。ほかの変更は `docs/AUDITOR/TRACE/2026-09-23_2da9385f.json`(フックが書く記録)だけ。**許した範囲に収まっていた。**

## 2. 受け入れ検査と被覆

- `python3 scripts/check_scan_report.py <報告> <生ログ>` をリードが打ち直して **0 件**。貼られた出力の合計行(0 件)と一致。
- `python3 scripts/cat8_ledger.py check-elements <報告> --round 1` を打ち直して **0 件**(候補の一覧 8 行 / 要素と段の表 64 行 / 道具 8)。貼られた出力と一致。
- **被覆(検査が実際に読んだ行)**: `check_scan_report.py` の関数を直接呼んで数えた(`scope` / `read_table` / `read_findings_table` / `marked_names`):
  - §4.0 の表: 報告 86 行 → 読んだ 86 行
  - **知見の表: 報告 28 行 → 読んだ 0 行**。知見の表の列が `道具 | 要素 | 記録する軸 | 原文` の 4 列で、**根拠の種類(委任文 §4.1 の 5 語)の列が無い**。検査は 3 列目の見出しが「印」の 4 列の表しか読まないので、この表は検査の外。**原因はリード: 起動文 §2 の 6 に知見の表の形を書かなかった。**
  - **`[深掘り]` の印: 状態が「深掘り」の 2 件のうち 1 件(PineForge)しか読めない**。候補の一覧の 1 行目 `qf-lib` は状態「深掘り」なのに行頭に `[深掘り]` が無い(同じ節の注記は「1 と 2 に付けた」と書いている = 注記と行が食い違う)。そのため K7・K13 は `qf-lib` の表を深掘りの表として検査していない。
- 生ログの見出しの形(`cat8_ledger.py check "" <生ログ>`): 0 件。

## 3. 数え直し(台帳から)

`sync` で `新` の 3 行に番号を振った(8-006 `freqtrade` / 8-007 `backtrex` / 8-008 `FX Replay`)。`freqtrade` の発見の経路は URL から「GitHub・PyPI・公式」と推されたが、生ログでは検索計画 4 の結果なので「検索」に直した。報告の状態・8 列・段を台帳に写した。`cat8_ledger.py check <報告>` 0 件。

## 4. 黙って落とされた候補(委任文 §3 の 3「候補の一覧から黙って落とさない」)

生ログの検索の結果に名前が出ているのに、候補の一覧に 1 行も無いもの(生ログの行を添える)。**報告の「残りの候補名: 無し」は成り立たない。**リードが台帳に `未着手` で足した(8-009〜8-016):

| 番号 | 名前 | 出た場所 |
|---|---|---|
| 8-009 | `nicferrari/backtester` | 生ログ 97 行(検索計画 2)。登録は ungh.cc で確かめた |
| 8-010 | `arXiv:2603.20319` | 生ログ 97 行(5 つのエンジンで 15 戦略、参照実装を含む比較の論文) |
| 8-011 | `arXiv:2512.12924` | 生ログ 103 行(ウォークフォワード検証の枠組みの論文) |
| 8-012 | `VectorBT` | 生ログ 118・122 行(X の投稿)。登録は ungh.cc で確かめた(`polakowo/vectorbt`) |
| 8-013 | `rusty-bot` | 同上(URL は生ログに無いので、発見の出典は投稿) |
| 8-014 | `Fincept Terminal` | 生ログ 112 行(X の投稿のリポジトリ列挙)。登録は ungh.cc で確かめた |
| 8-015 | `TradingView のリプレイ機能` | 生ログ 100 行(検索計画 3) |
| 8-016 | `Exactpro の reconciliation testing` | 生ログ 97 行(検索計画 2) |

候補ではなく**辿る一覧**として次の回に回すもの: dev.to の reconciliation tools の記事(生ログ 97 行)/ X の `aiwithjainam` の投稿のリポジトリ列挙(生ログ 112 行)/ zenn の仮説検証フレームワークの記事(生ログ 106 行)/ quantreo・algorier・fortraders の look-ahead bias の解説記事(生ログ 103 行)。(行番号は監査 10 回目の指摘 1 で直した。初版はコマンドの行を指していて、どれも 1 行少なかった)検索計画 7(E2 の補助)の「一般的な pandas/numpy/scipy 等のライブラリ紹介(専用ツールでない)」は、**専用かどうかで外すのは当否判定**(委任文 §10)なので、名前を出させる。

## 5. 抜き取り(原典で打ち直し)

**一次資料 5 行**(§4.0 の表から無作為): PineForge の星 187・フォーク 43(ungh.cc、一致)/ PineForge の README 217 行「4,190 raw trade CSVs, counts and full grades」(原文は太字の記号つきで一致)/ PineForge の README 291 行の比較用の依存(PyneCore・PineTS・vectorbt、一致)/ `qf-lib` の版 4.0.7・Apache License 2.0・requires_python >=3.8.0(PyPI JSON、一致)/ PineForge の LICENSE = Apache 2.0(一致)。**5 行とも合った。**

**実測 3 行**: `qf-lib` の既知の脆弱性 = OSV の応答 `{}`(打ち直して一致)/ `qf-lib` の当方データ投入 = `CSVDataProvider`(導入済みのソースに `class CSVDataProvider` があり一致)/ **`qf-lib` の再現性 = 「無し(専用の乱数シード固定・再現性保証機能なし。テストユーティリティに1件のみ)」→ 誤り**。導入済みの同じソースを `grep -rn -i seed` で打ち直すと、利用者向けの引数 `seed`(`backtesting/fast_alpha_model_tester/scenarios_generator.py` 97-98 行「seed used to make the scenarios deterministic」、`backtesting/alpha_model/random_trades_alpha_model.py` 51-52 行 同文)と、`backtesting/strategies/alpha_model_strategy.py` 150-152 行(「which would be deterministic across multiple backtests」+ `random.seed(self.timer.now().timestamp())`)がある。**E5(再現)は `なし` ではなく `印` の候補**で、`qf-lib` を「深掘り」とした前提(未判別が無い)も崩れる。

## 6. 「無い」の主張(全件の確かめ)

報告の「要素と段」の表で値が `なし` の行は **21 行**(`awk '/^### 要素と段/,/^### 知見/' <報告> | grep -c '| なし |'`。初版はファイル全体に `grep -c` を打って 22 と書いた。残る 1 行は §4.0 の表の「PineForge | 課金開始条件 | なし」で、要素の判定ではない = 監査 10 回目の指摘 2)。そのうちリードが打ち直した 1 行(上の `qf-lib` E5)が誤りだった。**残りの 20 行はリードが打ち直していない**(監査役が原典で照合した行は §14)。2 回目で、要素と段の表の `なし` の 21 行すべてについて、読んだ範囲(ファイルと行、または URL と節)と打ったコマンドを生ログに残して確かめ直させる。

## 7. 生ログの形

- 別々の 6 本の検索の見出しの時刻が**同じ秒**(14:12:09)、別々の 8 手が**同じ秒**(14:22:22)。1 手ずつ打った時刻ではなく、あとからまとめて書いた形である。
- 実測の手のコマンドが打ち直せる形になっていない(例: 193 行 `$ grep -rli "reconcil\|cross.check" / "duplicate\|…" / "random_state\|seed\|reproduc" 等`)。出力も要約(「0件(E1a なし)」など)で、委任文 §5-5 の「出力(先頭 200 文字)」ではない。
- **このため、実測の行の根拠(生ログの節)は、主張を打ち直せる記録になっていない。**受け入れ検査 K11 は形しか見ないので通る(検査の限界)。
- 処置: **`scripts/cat8_step.py` を置いた**。コマンドを打たせて、時刻・所要時間・終了コード・コマンド・出力の先頭 3000 文字を道具が生ログに書く。シェルの外の道具(WebSearch など)は `--manual` で、返ってきた一覧をそのまま流させ、時間と終了コードは `NA` と書く(作った数を書かない)。2 回目からはこれを通させる。

## 8. Jev の検査(その回の節だけ。`awk '/^## 区分8 — 1 回目/,0'` で切り出した 471 行)

- `jev_report_intake.py check --kind survey --prompt <起動文>`: 印 5 件 = 委任文の必須項目「代替経路」が無い(`alt_route` missing)/ 判定を言う文 1 件(報告 467 行、`[深掘り]` の読み方の確認の文。確率 0.65)/ 委任文と報告の照合 1 件(0.72)/ **出所の無い数 2 件**(報告 273 行の「312 戦略」「413 件」。README 34 行に原文があるので、出所を書かせる)。
- `jev_survey.py triage`: 印 9 件 = 出典の表に取得日が無い行 6 / 知見の表の行に URL が無い行 3。
- `jev_survey.py verify`: 引用 55 件すべてに印(`citation_corner_quote`)。確率の低いもの(0.09 など)まで立っていて、1 件ずつは追っていない。高いものの例: 報告 116 行の Testing Maturity Model の引用(0.78)= 調査班自身が「ページ本文は未読、検索結果の要約のみ」と書いている。**検索の要約を「」で囲んで引いたもので、一次資料の逐語ではない。**

## 9. 段(設計票 §4.1)と外部の枠組みの突き合わせ

調査班が見つけたのは一般のソフトウェアの Testing Maturity Model(Level 1 Initial〜Level 5 Optimizing)の**検索の要約だけ**で、ページ本文は読んでいない(未確認)。要約の限りでは、組織の試験の工程の成熟度の段であって、道具の機能の段(当方の段)とは軸が違う(推定)。**本文を読むまで段は変えない。**2 回目で一次資料を読ませ、リードが 2 回目の検収で突き合わせる。

## 10. 区分 8 に数える範囲の広さ(設計票 §3。毎回、列ごとの数を書く)

`cat8_ledger.py recount`(1 回目の 8 行について): E1a 印 2(8-002・8-004)/ **E1b 印 5**(8-001・8-002・8-003・8-004・8-006)/ E2 印 3 / **E3a 印 1**(8-006)/ E3b 印 1 / E4 印 4 / E5 印 3 / E6 印 0。E1a か E3a に印がある行 = 8-002・8-004・8-006 の 3 行。行が少なく、広さについては何とも言えない。

## 11. 調査班の問いへの答え(規則は述語で、当てはめは測らせる)

1. `qf-lib` の `drop_consecutive_duplicates()` の E2: E2 の述語は「**検出するか報告する**」。重複を取り除くだけで、どこが重複だったかを返さない・知らせないなら当たらない。**原典を確かめた**(監査 10 回目の指摘 3。監査役が wheel を展開して読み、リードも導入済みのソースで打ち直した: `qf_lib/common/utils/miscellaneous/consecutive_duplicates.py` の関数は 27 行で始まり、返すのは 69 行の `return no_duplicates_series` だけで、取り除いた位置も件数も返さず、ログにも出さない)。**この関数は E2 の述語に当たらない。**`qf-lib` の E2 の値を決めるには、ほかの関数(欠け・外れ値など)を探す必要があるので、2 回目でソースを検索させる(打ったコマンドを残す)。
2. `[深掘り]` の印: 状態が「深掘り」の行には必ず付ける(付けない行は「深掘り」にしない)。1 行目の `qf-lib` は食い違っている。
3. `freqtrade` の `lookahead-analysis`(同じエンジンの 2 回の実行を比べる): E1a の述語は「2 つ以上の実装(または実装と参照値)」なので、同じ実装の 2 回は E1a ではない。E3a の方式「切り詰めた計算との比較」に当たるかを原文で確かめる。**分類はこのままでよい。**
4. PineForge の E1a と E4 に同じ根拠: 同じ原文を 2 つの要素に使ってよいが、**要素ごとに、その要素の述語に当たる箇所を原文で示す**。E4 の述語は「記録した市場データを時刻順に再生して再実行する」なので、TradingView との突き合わせの原文だけでは E4 の根拠にならない。再生の原文を示せなければ E4 は `未判別`。
5. `akurkar07/OrderBook` を「危険で導入停止」: 委任文 §6-1 の「公開直後・保守者不明」は、止める理由として書いてある。**止めたのはこの環境への導入と実行だけで、候補の当否ではない。**他の列は文書から埋めること。このままでよい。
6. 4 軸の「推定」: 委任文 §4.1 の「推定 = 一次資料か実測から外挿した。何から外挿したかを根拠の欄に書く」を満たしていればよい。外挿元の無い行は直す。

## 12. リード自身の誤り

- 起動文に**知見の表の形**を書かなかった(§2 の被覆 0/28 の原因)。2 回目の起動文で、委任文の知見の表の形(`# | 知見 | 印 | 根拠`、印は §4.1 の 5 語)に揃えさせ、道具・要素・軸は「知見」の列に書かせる。
- 起動文 §2 の 3 の「候補の一覧を全部書いてから」を、**検索の結果の一覧と候補の一覧を突き合わせる手順**として書かなかった。2 回目は、検索の結果(`cat8_step.py --manual` で生ログに残した題名と URL の一覧)に出た名前を 1 つずつ候補の一覧に載せるか、載せない理由(道具でない・重複など、判定ではない理由)を書かせる。

## 13. 判定(この回の受け取り)

**一部を受け取り、差し戻す**(委任文 §12「§4・§5・§6 を守っていないものはリードが差し戻す」)。受け取るのは、8 件について届いた一次資料の記載(抜き取りの一次資料 5 行は全部合った)。差し戻すのは、(1) 落とした候補 8 件と辿る一覧、(2) 要素と段の表の `なし` の 21 行の確かめ直し、(3) `qf-lib` の E5・E2 と深掘りの状態、(4) 知見の表の形と根拠の種類、(5) 生ログの 1 手ずつの記録、(6) 候補の一覧の注記「`[深掘り]` は 1 と 2 に付けた」と 1 行目の食い違いを直す(監査 10 回目の指摘 6)、(7) `prediction-market-backtester` の README の `make setup` の節にある「optionally verifies `DATA_SHA256`」(監査役が原典 151 行で見つけた、取得したデータの整合の検証)を E2・E6 の述語に当てる(監査 10 回目の指摘 4)。**区分 8 の残り = 12 行**(`cat8_ledger.py recount`: 8-003・8-006〜8-016)。区分は完了していない。

## 14. 監査 10 回目(2026-09-23、対象 = この検収の初版 b940e05 と 1 回目の報告)

監査役の出力(逐語。[直す] 3 件・[聞く] 3 件):

> 検査対象: 委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md` / 追補 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md` / 設計票 `docs/DATA/surveys/CAT8_DESIGN.md` / 報告 `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(区分8・1回目) / 生ログ `docs/DATA/probes/20260923_tools_8_run1.log` / 台帳 `docs/DATA/tools_catalog_cat8.tsv` / 新道具 `scripts/cat8_step.py` / リードの検収 `docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run1.md`。
>
> 原典再照合の方法: qf-lib(PyPI wheel を実際に `pip download`・展開し `consecutive_duplicates.py`・`data_provider.py` 等を直接読んだ)、PineForge(README を `raw.githubusercontent.com` から取り直し行番号を突き合わせた)、akurkar07/OrderBook(README を取り直しチェックリストを確認)、freqtrade(`docs/lookahead-analysis.md` を取り直し引用行49・58を確認)、backtrex・FX Replay(実ページを取得しテキスト抽出)、prediction-market-backtester(README を取り直し)。リードが抜き取った5行(PineForgeの星・フォーク/README217行/README291行、qf-libの版・ライセンス、PineForgeのLICENSE)以外の一次資料引用を中心に照合した。全体として、照合した逐語引用は原典と一致しており、明確な「引用の捏造・改変」は見つからなかった。一方、以下の手続き上の問題を見つけた。
>
> 1. [直す] docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run1.md:35,36,37,38,40,41,42,44 — 「落とされた候補」の生ログ行番号引用が、実際に候補名が書かれている行より系統的に1行少ない(WebSearchのコマンド行`$ WebSearch query=...`を指しており、結果が書かれた行ではない)。実測: 8-009/8-010(`nicferrari/backtester`・`arXiv:2603.20319`)は「生ログ96行」引用だが実際の記載は`log:97`。8-011(`arXiv:2512.12924`)は「102行」引用だが実際は`log:103`。8-012(VectorBT)は「117・121行」引用だが実際は`log:118`・`log:122`。8-014(Fincept Terminal)は「111行」引用だが実際は`log:112`。8-015(TradingViewのリプレイ機能)は「99行」引用だが実際は`log:100`。8-016(Exactpro)・辿る一覧のdev.to記事は「96行」引用だが実際は`log:97`。辿る一覧のzenn記事は「105行」引用だが実際は`log:106`。quantreo等の記事は「102行」引用だが実際は`log:103`。全件確認したがすべて同じ向きに1行ずれている。一方、同じ検収が更新した台帳`docs/DATA/tools_catalog_cat8.tsv`側の範囲引用(例: 8-015行の`:98-100`、8-016行の`:95-97`)は結果行を正しく含んでおり範囲指定のため実害が無い。VERDICT本文の単一行引用だけがずれている(P10。この行番号のまま2回目の担当者が原典を開くとコマンド行しか見えず、候補名の記載を見落としうる)。
>
> 2. [直す] docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run1.md:54,93 — 「報告の『要素と段』の表で値が`なし`の行は22行」の実測方法は`grep -c '| なし |' docs/DATA/SCAN_2026-09-23_tools_cat8.md`(ファイル全体に対する実行)。実際に「要素と段」表(SCAN report:160-228)だけに絞って数え直すと21行(うち1行=qf-lib E5がリードの確認で誤りと判明済み)。残る1件の「| なし |」はSCAN report:405の§4.0機械可読表「PineForge | 課金開始条件 | なし | 一次資料 | …」という別表・別種の主張で、E1a〜E6の要素判定「なし」ではない。「22行すべてを2回目で確かめ直させる」(:54,93)という差し戻し指示は、対象の定義(要素と段の表限定か、ファイル全体の文字列一致か)が曖昧なまま数値が独り歩きしており、2回目の担当者が22行分の対応先を誤って探す可能性がある(P2、数値の出所)。
>
> 3. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:166(qf-lib E2)/ VERDICT:79(問い1) — 原典`qf_lib/common/utils/miscellaneous/consecutive_duplicates.py`を実際に開いて確認すると、`drop_consecutive_duplicates()`は変換後のSeriesのみを返し、削除した重複の位置・件数を戻り値にもログにも一切出さない(docstring・実装とも確認済み)。設計票`CAT8_DESIGN.md:51`のE2述語は「検出するか報告する」だが、この関数は内部的に重複位置を計算するのみで外部に報告しない。VERDICTは「返り値やログに取り除いた行・件数を出すかを、ソースで確かめて決める」と2回目送りにしている(:79)が、原典は既に開けば読める状態にあり、この検収の時点で「なし」寄りの判断材料が揃っていたのではないか。2回目に送る前に、この原典の中身(戻り値・ログ出力の有無)を検収に書いておくべきではないか。
>
> 4. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:295(prediction-market-backtester) — README(`make setup`節、原典151行)に「optionally verifies `DATA_SHA256`」というダウンロードデータの整合性検証の記述があるが、報告のE2(データ品質)・E6(その他検証)いずれの知見表にも計上されていない。E2述語(欠け・重複・順序の乱れ・外れ値等)には直接当たらないとしても、E6(「E1a〜E5のどれにも当たらない検証機能」)の候補として拾えるのではないか。リードの検収はこの記述に触れておらず、2回目の確認対象に含めるべきかが検収に書かれていない。
>
> 5. [直す] scripts/cat8_step.py — `--note`引数は改行を含んでいてもサニタイズ(one_word化やエスケープ)されずそのまま生ログの見出し行に連結される。実際に改行入りのnoteを渡して検証したところ、見出し行の直後に孤立した継続行(コマンド行でも出力行でもない行)が生ログに残り、`python3 scripts/cat8_ledger.py check "" <生ログ>`はこれを検出せず「---- 合計 0 件」で通過した(本監査で実行・再現)。この道具はVERDICT §7が指摘した「1手ずつ、打ち直せる形」を保証する目的で今回新設されたばかりであり、その前提(1見出し=1行、見出し直後は必ず`$ コマンド`)を壊す入力を受け入れ検査がすり抜ける。2回目の運用前に`--note`の改行を拒否または除去する直しが要るのではないか。
>
> 6. [聞く] docs/AUDITOR/VERDICTS/2026-09-23_tools_scan_cat8_run1.md:93 — 差し戻す5項目の列挙に、§2(:22)で指摘した「候補の一覧の注記『[深掘り]は1(qf-lib)と2(PineForge)に付けた』と実際の表記(qf-lib行に印が無い)の食い違い」を直させる指示が明示的に入っていない。(3)「qf-libのE5と深掘りの状態」に含まれる想定と読めなくもないが、注記の文言自体を訂正させる指示が独立の項目として無く、2回目の担当者が見落としうる。
>
> その他の確認結果(問題なし、参考まで): §1(書き換え範囲)は`git show 95edb1b --stat`で確認した実際の差分(報告・生ログ・TRACEの3ファイルのみ)と一致。§2の被覆数値(§4.0表86行/知見表28行)は実測と一致。§13「区分8の残り=12行」は`python3 scripts/cat8_ledger.py recount`の実行結果(8-003・8-006〜8-016の12行)と一致。§0の予算超過率(約3割)は報告記載の「25万トークン・25分」という基準(追補§5と一致)から正しく算出されている。freqtradeのlookahead-analysis.md引用(行49・58)、PineForge READMEの引用(行37・217・263・267・291・328・411)、akurkar07/OrderBookのCI/CDチェックリスト未チェックの記述は、いずれも原典と一致した。

(リード注: 監査役の出力の 1 行目の「追補 `…cat8_run1_prompt.md`」は起動文のこと。追補は `…cat8_addendum.md`。)

**リードの処置**:
1: §4 の行番号を全部直した(`grep -n -i <名前> <生ログ>` で打ち直し: 97 / 97 / 103 / 118・122 / 112 / 100 / 97、辿る一覧 97 / 112 / 106 / 103)。
2: §6・§13 を「要素と段の表の `なし` = 21 行」に直し、数えたコマンドを書いた。
3: §11 の 1 に原典の中身(69 行の `return` だけ、位置・件数・ログ無し)を書き、この関数は E2 の述語に当たらないとした。`qf-lib` の E2 の値は、ほかの関数を探してから決める(2 回目)。
4: §13 の差し戻しに (7) として足した。
5: `scripts/cat8_step.py` の見出しの `note` とコマンドの行を 1 行にまとめるようにした(改行と連続する空白を 1 つの空白に)。改行を含む note で打ち直して、見出しが 1 行になることを確かめた。`cat8_ledger.py check` にも「見出しの直後が `$ ` の行でない」を当たるようにし、壊した生ログで当たることと、1 回目の生ログ・`cat8_step.py` の出力で 0 件のことを確かめた。
6: §13 の差し戻しに (6) として足した。

**監査役が原典で照合して一致した行**(§6 の「打ち直していない 20 行」とは別に、引用の逐語の照合): freqtrade の `lookahead-analysis.md` 49・58 行 / PineForge の README 37・217・263・267・291・328・411 行 / akurkar07/OrderBook の CI/CD の点検表。
