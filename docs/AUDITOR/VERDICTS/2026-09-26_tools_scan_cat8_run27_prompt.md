# 監査: 道具サーベイ 区分 8 の 27 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md`(8-031 Debezium の残り。25 回目の起動文から作った)。

## 監査 117 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run27_prompt.md@4aa1d811add3`)の逐語

> ## 監査 117 回目 報告
>
> 対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md`(指紋 `4aa1d811add3`)。参照: `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run26.md`、`docs/DATA/surveys/CAT8_DESIGN.md`、`docs/DATA/delegations/20260922_tools_survey_prompt.md`。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md:23` と `:64` — §1 の 3 は「当方データ投入」の行について「文書の逐語で投入の経路…を書き、『動かすのはリードが止めた(26 回目の検収 §4 の処置 5)』と書く」とだけ指示し、この行に付ける `印`(一次資料/実測/推定/仮定/未確認のどれか、委任文 §4.1)を明記していない。一方 §2.5(64 行目)は「`未確認` のまま残してよいのは、試した手段と出力を生ログの行を添えて書いたものと、試すのに当方の鍵・口座・登録・購入が要るか外へ送信するもの(そう言う逐語を引く)だけ」「『この回では実施していない』…は理由にしない」と、この回に限って `未確認` の許容理由を 2 つに絞っている。「別の利用者で PostgreSQL を立てる必要がある」はこの 2 つのどちらにも明示的には当たらず(鍵・口座・登録・購入・外部送信のどれでもない、リードの運用判断)、調査班がこの行を `未確認` で埋めた場合、理由の書き方が §2.5 自身が禁じる「この回では実施していない」型(24 回目検収の指摘 7 と同型)に接近する。`印` に何を書くか(一次資料 として扱うのか、`未確認` とするなら §2.5 の 2 分類のどちらに当たると説明させるのか)を起動文自身が決めていない(P2 に近い、記号・欄の定義の不整合)。
>
> 2. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md:21,26` — E2・E4 の「印を探し尽くす」課題は「26 回目と同じ一覧(`venvs/8-031/filelist_full.bin`。26 回目の知見の N)」を前提に、そこへ `grep -I -c -i -E` の見積もり手を打つ(27 行目)ことになっている。同様に `N` の一覧(`venvs/8-031/src/`)も「取り直さない」(26 行目)とされている。だが起動文はこの一覧・展開済みソースが**この回(27 回目)の実行環境に実在すること**を検証・保証する記述を持たない。区分 8 の各回が調査班にとって新規のセッション(新規の scratchpad)であれば、26 回目に作った `venvs/8-031/` 配下のファイルは 27 回目には存在せず、「取り直さない」の指示自体が実行不能になり、調査班は指示に反して取り直すか、詰まって停止するかの二択を迫られる。この回の主課題(E2・E4 の探索)が丸ごと成立するかどうかに関わる前提であり、着手前にこの一覧・展開ソースが今回のセッションから読めることを確かめたか(research-protocol の「準備測定も測定」原則に近い、実装(環境)と指示の突き合わせ)。
>
> 3. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md:70` — 取得本文の置き場所として指定されている scratchpad の絶対パス `/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/` の UUID (`2da9385f-4fe4-506a-be3d-78153c549662`) は、本監査役の今回のセッションに割り当てられた scratchpad の UUID と一致している。区分 8 の調査班セッションと本監査役セッションが同一の上位セッションに属し scratchpad の名前空間を共有する設計であれば問題ないが、そうでなければ、この起動文に書かれたパスは調査班の実行環境には存在しないパスであり、§2.6 の取得本文の保存先指示が丸ごと機能しない。委任前にこのパスが調査班の実行環境で有効かどうかを確かめたか。
>
> 4. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md:26` — 26 回目検収 §4 処置 5(c)は「`mvn` には `-Dmaven.repo.local=<scratchpad の下>`、`pip` など他の道具もキャッシュを scratchpad の下に向ける、と書く」としているが、起動文はこの回について `mvn` の具体的なフラグだけを書き、`pip` 側の具体的な向け方(例: `PIP_CACHE_DIR` や `--cache-dir`)を挙げていない(「`pip` などを打つなら…向ける」とあるだけで手段が無い)。もっとも同じ行で「新しく何も入れない・動かさない」とも書かれており、この回に `pip` を打つ場面自体が無い可能性が高く、実害は小さい。
>
> 5. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run27_prompt.md:26` — 「(ディレクトリの権限を変えない…)」の注意書きは 26 回目の事故(scratchpad 祖先ディレクトリの `chmod 711`)の再発防止として書かれているが、実際に権限変更が発生したのは `mvn package` 実行時(26 行目自身が言及)であり、この回は導入・実行を一切行わない設計になっている。この注意がこの回において実質的に発火しうる操作(Java/Maven を伴わない `curl`・`grep` のみ)であるにもかかわらず書かれているのは無害な冗長だが、逆に言えば「動かさない」の徹底が守られていれば不要な条項であり、今後この条項を「新規導入がある回にだけ出す」よう整理する余地があるか(直接の欠陥ではなく、規則の導線の掛け方の確認)。
>
> **止める指摘の有無: あり(#2)。**
>
> 補足(確認して問題なしと判断した点、参考): 26 回目検収の処置のうち E2・E4 の当て方(処置 1・2)、scratchpad の外に書かない指示(処置 5a)は §1(21・24・26 行目)に正しく反映されている。Prefect 固有の文の写し残し・消しすぎは見当たらなかった(24 行目の「24 回目の Prefect」は当て方の先例引用であり、削除対象の残骸ではない)。§7 の生ログ本数(run1〜run27 の 27 本)・`--round 27` は回数と整合している。

## 処置(リード、監査 117 回目)

1. **直した**。「当方データ投入」は、文書の逐語で経路を書いて根拠の種類を `一次資料` にし、値の欄に「動かしての確かめはリードが止めた(別の利用者で PostgreSQL を立てる必要があるため)」と書き添える。逐語が無ければ `未確認` とし、読んだ頁の一覧を試した手段として書く。
2. **止める → 答える・直した**。調査班(下位モデル)は、リードと同じコンテナで、同じ scratchpad の道を使って動く(21〜26 回目の調査班は全部この scratchpad の `venvs/` に書き、リードが検収でそれを読んだ)。リードの実測: `ls .../scratchpad/cat8/venvs/8-031` → `check`・`filelist_full.bin`・`run`・`src`、`ls .../src` → `debezium`、`tr -dc '\0' < .../filelist_full.bin | wc -c` → 4520(26 回目の N と一致)。起動文に実測を書き、最初の手で同じ `ls` を打ち直して、無ければ作業せずに返すようにした。
3. **答える**。処置 2 のとおり、同じ scratchpad の名前空間を使う(道は有効)。
4. **直した**。`pip` は `PIP_CACHE_DIR=<scratchpad の下>` と書いた。
5. **答える**。この回は動かさないので、条項は念のための重ね。消さない(26 回目は、起動文に書かなかった場所で外に書いた)。

直したあとの起動文の印: `20260923_tools_survey_cat8_run27_prompt.md@b06ea566dbc3`。止める指摘(2)は実測で前提が満たされることを確かめ、最初の手で調査班にも確かめさせる形にした。これで起動する。
