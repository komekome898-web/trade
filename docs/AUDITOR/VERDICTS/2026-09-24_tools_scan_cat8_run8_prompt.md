# 監査記録: 道具サーベイ 区分 8 の 8 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run8_prompt.md`(初版の指紋 `dad21665a0e9`)。7 回目の起動文(監査 34〜41 回目を通した版)を元に、範囲・「問いの行は未判別」・7 列目の行の中身・対象の一覧の置き場・段 4/5 の逐語の規則を足した。

**リードが決めた読み(7 回目の問い 1 への答え)**: 8-004 の README「Duplicate IDs are rejected」「Orders require a valid buy/sell side and non-zero quantity」は、板への入力の重複と型や範囲の違反の検出で、E2 の述語の「板」「重複」「型や範囲の違反」に当たる。述語の逐語の当てはめで、述語は変えていない(監査 42 回目の答え 7: 当てはめはリードが決める)。

## 9 回目以降に回すもの(ここに全部書く)

| 何 | 出所 |
|---|---|
| 段が `未判別` の行(8-003 E5・8-011 E5・8-012 E5・8-014 E1b/E3b/E4・8-007 E6。8 回目の §2 で当てる行を除く) | 監査 21・22・30・33・42 回目 |
| 8-005 `Exegy`(`登録が要る`) | 1 回目 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜7 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| freqtrade の §4.0 の表を 1〜7 回目から 1 つに合わせて数え直す(資源化のとき) | 監査 42 回目の答え 8 |

## 監査 43 回目(対象 = 9187dfe、起動文の指紋 `dad21665a0e9`)

監査役の出力の要点(確かめて欠けの無かった点: 7 回目の検収と監査 42 回目の処置はすべて入っている / 台帳を全セル数え上げて、残り・値の `未判別`・段の `未判別`・数え直していない `なし` が起動文 §1・§2 と 9 回目以降の表に過不足なく 1 回ずつある / §7 のコマンドは打てる / 指紋は一致)。指摘は [聞く] 1 件・[直す] 4 件(逐語):

> 1. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run8_prompt.md:46 — 8-004 の E2 の読み(README「Duplicate IDs are rejected」等を E2 述語「重複」「型や範囲の違反」に当てる)は、設計票の述語が要求する「検出するか報告する」に対し原文は「reject(入力段階の拒否)」であり、拒否と検出/報告が同義かは自明ではない。監査 42 回目答え 7 は「誰が決めるか(権限)」だけを裁き、読みの中身(述語を広げていないか)は検討していない。確定させると以後「不正入力を拒否するだけの道具」が軒並み E2 に当たる先例になり得る。CAT8_DESIGN.md §3 の「述語を足したり変えたりしたら全行に当て直す」の対象にならないか、この読みで確定してよいか。
>
> 2〜5. [直す] 4 件とも、7 回目の誤りを塞ぐために足した新ルール(問いに出した行は値も段も未判別/7列目はその要素の語の検索/対象一覧はscratchpad下+印字/段4・5の逐語)が、`scripts/cat8_ledger.py` の `check-elements`・`check_scan_report.py` では**機械的に検査されていない**ことを実測で確認した(`cmd_check_elements` の実装を読み、対応する検査ロジックが無いことを確認)。具体的には:
>    - line 31(問いに出した行→未判別): 「問い」の語を検査するコードが無い。3 回目の同じ誤りが 7 回目まで 3 回起きた規則で、対策が文書のみ(P8「規則は書くだけでは機能しない」)。
>    - line 32(7列目=その要素の語の検索): check-elements は 7 列目の**書式**と引用文の在否しか見ず、参照先コマンドの正規表現がその要素の語彙を含むかは見ない(監査42指摘1と同型が再発しても機械では止まらない、P10)。
>    - line 33(対象一覧をscratchpad下に置く): 一覧のN/M数字の一致とログ行の存在は見るが、一覧ファイルの置き場所(/tmp直下かscratchpad下か)は検査していない(P10)。
>    - line 34(段4・5の逐語): `quotes_in_log` は根拠欄に「…」の引用が**書かれている場合にだけ**照合し、段4・5に必ず引用が要ることを強制する検査は無い(P10)。
>    いずれも8回目で同型の誤りが再発しても機械では検出できず、引き続きリード・監査役の目視(検収)に依存する。

**リードの処置**(起動を止める指摘は無い。起動文は変えずに起動し、機械の検査は 8 回目の検収で足して届いた報告に当てる):
1: 答える。述語は「…を**検出するか報告する**機能を持つ」。拒否は、重複や範囲の違反を見つけたうえで入力を受け付けないことで、見つけること(検出)を含む。述語の逐語の当てはめで、述語は変えていない、とリードは読む。ただし、この読みは「入力を拒否するだけの道具」にも E2 を付ける先例になるので、**区分の締めでオーナーに見せる一覧(段が `未判別` の行と並べて、述語の当てはめの判断を並べたもの)に、この 1 件を入れる**。オーナーが違うと言えば、E2 の該当の行を全部当て直す。
2〜5: 認める。8 回目の検収で、次を `check-elements` に足して届いた報告に当てる: (a) 報告の「判断に迷った点と問い」の節に名前と要素が出る行は、値か段が `未判別` でなければ止める / (b) `なし` の 7 列目の手の `$` の行に、その要素の語(設計票 §3 の名前の日本語か英語)が無ければ止める / (c) 生ログの手の `$` の行に、`/tmp/` の直下のファイル(scratchpad の外)を読むものがあれば止める / (d) 段 4・5 の行は、6 列目に「…」の引用が無ければ止める。足すまでの間は、検収でリードがこの 4 つを読む。

**監査を通った版(委任の関門の印)**:
- 8 回目の起動文: 20260923_tools_survey_cat8_run8_prompt.md@dad21665a0e9
- 区分 8 の追補: 20260923_tools_survey_cat8_addendum.md@2c178ba75341(記録済み。変えていない)
