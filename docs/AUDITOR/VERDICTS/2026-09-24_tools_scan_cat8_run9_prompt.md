# 監査記録: 道具サーベイ 区分 8 の 9 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md`(初版の指紋 `b226e48e551a`)。8 回目の起動文(監査 43 回目を通した版)を元に、範囲(台帳の `未判別` の値・段を 8-005 を除いて全部)と、8 回目の誤りの型への規則を足した。

**リードが決めた読み(区分の締めにオーナーに見せる一覧に入れる)**: 8-014 の LLM のエージェントの判断は、検出・検証をするように作られた機能(コードの中のワークフロー)があれば値は当たり、確率的かどうかは段で決める(述語の「機能を持つ」の逐語の当てはめ)。

## 10 回目以降に回すもの

| 何 | 出所 |
|---|---|
| 8-005 `Exegy`(`登録が要る`) | 1 回目 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜8 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| freqtrade の §4.0 の表を 1〜7 回目から 1 つに合わせて数え直す(資源化のとき) | 監査 42 回目の答え 8 |
| 8-015 の N の出所の書き直し(資源化のとき) | 監査 44 回目の処置 2 |

## 監査 45 回目(対象 = b4a7b45、起動文の指紋 `b226e48e551a`)

監査役の出力(逐語。[止める] 1 件・[聞く] 2 件・[直す] 2 件):

> 1. [止める] docs/DATA/tools_catalog_cat8.tsv 15 行目(8-014)と docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md:22・48 — 台帳を機械で数え上げると、8-014 は段_E3a・段_E6 も `未判別`(値 E3a・E6 と同じ)だが、起動文§1「段が `未判別` の要素は…8-014 E1b・E3b・E4」の列挙にこの 2 セルが入っておらず、§2 項目 2「E3a・E6 の値と、E1b・E3b・E4 の段を決める」の文言も段_E3a・段_E6 を名指していない。設計票の「深掘り」完了条件(CAT8_DESIGN.md:29「E1a〜E6 に `未判別` が 1 つも無い」)は値の列だけを見て段は見ないため、この 2 セルは深掘り判定にも `check-elements` 検査にも引っかからずに残り得る。検収記録の「10 回目以降に回すもの」表にもこの 2 セルは載っていない。§1 冒頭「台帳で `未判別` の値・段…を全部決める」という宣言と、実際の列挙・指示は一致しているか。(P5、関連 P3)
>
> 2. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md:45 — 「当たる原文が無ければ、その段より下の、原文のある段にする」という段の決め方は、設計票 `docs/DATA/surveys/CAT8_DESIGN.md` §4.1「段 1 から順に条件を満たすかを見て、条件を満たす一番上の段を付ける」「上の段の条件の一部だけを満たしても、その段は付けない」の逐語の当てはめか、それとも各段の累積条件(例: 段 4 =「段 3 に加えて…」)を確認せず、差分文言の原文の有無だけで段を決める別の手順に置き換えているか。監査 44 回目の指摘 1(8-004 E2 の段 4 を推論で当てていた)と同型の誤りが、形を変えて残っていないか。(P3)
>
> 3. [聞く] docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md:48、docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run9_prompt.md:5 — 「LLM のエージェントの判断は…機能があれば値は当たり、確率的かどうかは段で決める」というリードの読みは、設計票§3・§4.1 に無い新しい当てはめ(原文は「機能を持つ」とだけで、LLM 出力の確率性と段 2/3 の境界の対応は書かれていない)。CLAUDE.md §0.1「作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる。黙って決めない」に対し、この読みは「区分の締めにオーナーに見せる」形で、それより先にこの回の複数セルの値決定に使われる。この回の実行前にオーナーへ確認すべき判断ではないか。(CLAUDE.md §0.1)
>
> 4. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md:31、比較先 docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run8.md:69(処置 7) — 処置 7 は「値・段の問いでないものは検査の対象外と書く」ことを 9 回目の起動文に足すと約束したが、起動文には問いの種類を 1 語で書かせる指示(「値か段についての問い」か「それ以外」か)しかなく、「それ以外」に分類された問いが検査(a)(問いに出した行は未判別)の対象外になるという明文の宣言が本文に見当たらない(`grep -n "対象外\|それ以外" docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md` で確認、該当箇所は N/M の行のみ)。8 回目の 8-010「問い 2」のような事例は、今回もリードが検収でその都度手動判断するしかないままではないか。(P8)
>
> 5. [直す] docs/DATA/delegations/20260923_tools_survey_cat8_run9_prompt.md:31-33 — 新規追加の 4 規則(問いの種類を 1 語で書く/取り下げた判断を「判断に迷った点と問い」に書く/述語を候補〈道具〉の機能に限定し戦略の作りを数えない/N を一覧の和で作るときの書き方)は、`scripts/check_scan_report.py`・`scripts/cat8_ledger.py check-elements` のどちらにも対応する機械検査が無い(該当語 `戦略`・`それ以外`・`一覧ごと`・`の和` でスクリプトを検索しヒットなしを実測で確認)。既存の「問いに出した行は未判別」検査(check-elements 内 `in_q` 判定)と「一覧 N 件/読んだ M 件」の N=M 検査は機械化されているのに対し、この 4 つは検収時の目視頼みのままである。8 回目までの差し戻しが「規則を書いても次の回で別の形で同じ型が再発する」という繰り返しだったことを踏まえ、この 4 規則にも機械的な導線が要るかどうかは検討したか。(P8)

**リードの処置**:
1: 認める。§1 に、値が `未判別` の要素(段も `未判別`)と段だけが `未判別` の要素を分けて全部書き、「値を決めた要素は段も同じ回に決める」と足した。
2: 認める(リードの言い換えが設計票の積み上げの規則を落としていた)。起動文の段の決め方を、設計票 §4.1 の逐語(「段は下から積み上げで決める…条件を満たす一番上の段…上の段の条件の一部だけを満たしても、その段は付けない」)の引用に置き換えた。
3: 認める(原文に無い読みで値を決めさせようとしていた = CLAUDE.md §0.1)。リードの読みを起動文から外した。E3a・E6 は述語の逐語で決め、LLM の判断を「機能」と読んでよいか迷えば、値も段も `未判別` のまま原文を引いて問いに出す(リードは決めない)。この問いは区分の締めでオーナーに見せる(8-004 の E2 の読みと並べて)。
4: 認める。起動文に「問いの行には `[値・段の問い]` か `[それ以外の問い]` を必ず書く。`[それ以外の問い]` は検査の対象外」と書いた。
5: 一部を機械にした: 9 回目以降、「判断に迷った点と問い」の番号の付いた行に 2 つの印のどちらも無ければ止める / `[それ以外の問い]` の行は「問いに出した行は `未判別`」の検査から外す(scratchpad で、印の無い問いが止まり、`[それ以外の問い]` が外れることを確かめた)。ほかの 3 つ(取り下げた判断を書く・候補の機能に当てる・N の和の書き方)は文の意味の判断なので機械にせず、検収でリードが読む。
あわせて、`台帳の値のまま` の行を台帳と照らす検査は、**最新の回の節にだけ**当てるようにした(台帳は今の値なので、前の回の節を照らすと、その後に変えた行が全部止まる)。2〜7 回目は 0 件、8 回目は届いた報告のまま 5 件(8 回目の検収 §1 の 5 件で、台帳で処理済み)。
直した版(起動文の指紋 `9bc2d15dbaf1`)は監査 46 回目で確かめてから印を記録する。
