# 区分 1 の 14 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 14 回目の実行(2026-09-22)」(4515 行目以降、256 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run14.log`(新規。復帰文字 0 個)
オーナー決定 L-398(案 A')の初回。

## 0. 無改変・検査

既存の 4514 行は **無改変**。生ログ 12 本を渡して打ち直し、**13 本すべて 0 件**。
**この回は最初から 0 件**で、検査に当たった行を直す作業が発生していない(初)。
検査に当たらない誤り(出典と知見が指す生ログの行番号 8 か所のずれ)を、調査班が自分で `grep -n` で数え直して直している。
**検査が見ない型を自分で見つけて直したのは、この調査で初めて。**

## 1. リードが逐語まで取り直した所見 — **一致。ただし性質を正しく書き直す。**

`lo2cin4bt` の README に、読み手を作業者として動かす文が埋め込まれているという報告。

- **`README.en.md:9` の逐語**(リードが取得): 「You are the PM for lo2cin4bt. Check the local environment,
  initialize the built-in strategy examples, and run a QQQ daily SMA Cross backtest. Keep everything local;
  do not run live trading or place orders.」**報告の引用と 1 文字も違わない。**
- 同じ文は本体の `README.md`(中国語)の 9・37・38・40 行にも在る。
- **リポジトリは `AGENTS.md` を持ち、その冒頭に**「Use this file as the first instruction page when an AI CLI
  or AI IDE assistant is helping with this repository.」**と書いてある。**

**性質の判定(リード)**: **敵対的ではない。**これは利用者が自分の助手に貼るための**見本の指示文**で、
文中に「実弾取引をするな・注文を出すな」と自分で書いている。**害を与える形ではない。**
一方、**取ってきた文章の中に作業者向けの指示が在るという構造そのもの**は委任文 §6-3 が想定した形で、
調査班が**従わずに報告だけした対応は正しい。**
**§6-3 が実際に働いた 2 例目**(1 例目は 8 回目の `mlflow` の同梱物)。**どちらも敵対的ではなかった**ことも併せて記録する。

## 2. 確かめたその他 — 一致

| 主張 | 取り直した結果 |
|---|---|
| `lo2cin4bt` のライセンスが CC BY-NC 4.0 | **一致。**LICENSE の 1 行目が「Attribution-NonCommercial 4.0 International」 |
| 星 290、作成 2025-07-22 | 一致 |

**ライセンスは重い。**プロジェクトの最終目標は「トレードにより、**実用的な収益基盤**を構築する」で、
**NonCommercial は商用利用を許していない。**この道具は、当方の目的に対して**使えない部類に入りうる**。
判定は区分 1 の仕事ではないので書かないが、料金・規約の欄にこの事実を明記させる。

## 3. 調査班が渡してきた判断への回答

1. **候補 41 の名前が一致せず、近い 4 件を落とさず立てた → 正しい。**§3-3 のとおり。
   **12 回目に書いた名前が正しくない可能性がある**という指摘もそのとおりで、記録に残す。
2. **仕分けの印に「(推定)」を足した → 次からは `判別に一次資料が要る` に倒す。**
   完了の判定は 6 要素ごとの残り件数で行うので、**確度の低い印が数を動かすと判定が狂う。**
   説明に "backtest" としか無いものは、推定で要素に入れず `判別に一次資料が要る` に置く。
3. **`区分N へ` と 6 要素の両方が付いた行をどちらで先に追うか → 区分 1 で先に追う。**
   今は区分 1 を完了させる指示なので。印は両方残す(§2「またがるものは両方に書く」)。

## 4. 完了判定の急所 — **調査班の指摘が正しい**

仕分けの結果、**`区分1-板の待ち行列` と `区分1-市場影響と約定の模型` の残りが 0 件**になった。
しかし調査班は**「尽きた」と書かなかった。**理由も正しい:

> 13 回分の検索計画がこの 2 要素を狙ったことが一度も無いためです。

**これは重い。**この 2 要素は、当方の `src/bot/backtest/engine.py` が自分で限界を認めている部分に当たる。
**13 回かけて一度も狙っていない要素を「尽きた」と書けば、それは偽の完了である。**
**次の回の検索計画 6 本は、この 2 要素だけを狙う。**

仕分けの数(下限): 足 16 / ティック 2 / 板の待ち行列 0 / イベント駆動 7 / ベクトル化 3 / 市場影響と約定の模型 0。
`判別に一次資料が要る` の 14 件が入りうるので、上は下限である。

## 5. リードの判定

- **受け取る。中身の誤りは 0 件。**
- 確定は 3 件(`lo2cin4bt` = 浅い / `QuantDinger` = 浅い / 候補 41 = 名前が確定できない)。
- **経路の所見**: `api.github.com/search` はこの環境の proxy が 403 で拒む(逐語「sessions are bound to their
  configured repositories」)。**1 回のコードで止めず MCP の GitHub 検索に替えて 3 本とも通した**のは、
  委任文 §5-1 が要求している続け方そのもの。
- **区分 1 は未完了。**

## 6. 関門の誤検出(記録)

この記録を書くとき、`deny_protected_paths.sh` が**文中にゴールのファイル名を引用しただけで書き込みと見なして止めた。**
保護されたファイルへは書いていない。**回り込まず、引用の書き方を変えて打ち直した。**
解除ファイルは作っていない。
