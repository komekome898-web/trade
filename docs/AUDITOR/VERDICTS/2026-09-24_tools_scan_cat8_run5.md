# 検収: 道具サーベイ 区分 8 の 5 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 5 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run5.log`(11,668 行・113 手)。届いたままのコミット 97435c2。
起動文 `20260923_tools_survey_cat8_run5_prompt.md` の指紋 `b422d3b52021`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **583,019 トークン・2,523,675 ms(42.1 分)・道具 265 回**。生ログの最初の手 02:33:18Z / 最後の手 03:02:47Z(`cat8_ledger.py check` の参考の 1 行)。割り当ての 9 件すべてに手が付いた。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 5 本)0 件 / `check-elements --round 5` 0 件(候補の一覧 16 行・要素と段の表 128 行・知見の表 25 行)/ `check "" <run5.log>` 0 件 / 消えた行 0。
検収で足した検査(監査 29 回目への答え 1): `印` の根拠に引いた描画の手に `text-changed-after-wait` があれば止める。5 回目の節に当てて 0 件(この回は描画した頁を `印` の根拠に使っていない = 報告 §6 のとおり)。scratchpad の試しの生ログで止まることを確かめた。

## 2. `なし` の抜き取り(この回に新しく書いた `なし` 11 件を全部、生ログと数え合わせた)

| 候補 / 要素 | 報告の N / M | リードの数え直し | 処置 |
|---|---|---|---|
| 8-003 E3a・E3b・E6 | 96 / 96 | ファイル一覧 99 件(`run5.log:10` の ungh.cc の出力)。検索の手(`run5.log:15`)は `--include='*.py' '*.md' '*.yaml'` で、当たるのは **88 件**。外れた 11 件のうち、報告が除外と書いたのは 3 件(csv 2・uv.lock 1)で、残りの 8 件(`.env.example`・`.github/workflows/ci.yml`・`.gitignore`・`LICENSE`・`Makefile`・`pyproject.toml`・`scripts/setup_data.sh`・`vendor/.../LICENSE`)は検索もされず除外の理由も書かれていない | **台帳で `未判別` に戻した**、状態 `浅い` |
| 8-007 E3a | 9 頁 | `curl` で 9 頁(`run5.log:193`〜`281`)。描画の道具は使っていない | 受け取る |
| 8-008 E1a・E2・E3a・E3b・E5・E6 | 40 / 40 | 記事の本文は描画の道具で 44 回読み、根拠には `curl` の一覧の行(`run5.log:1426`)を引いていた(起動文 §2「描画した頁は `なし` の根拠に使わない」に反する。機械の検査の限界として監査 27 回目の記録に書いた型そのもの)。かつ、Support Center の 8 分類のうち「Product Guide & Features」の 1 分類だけで、N をその 1 分類に狭めていた | **台帳で `未判別` に戻した**、状態 `浅い` |
| 8-013 E5 | 75 / 75 | リポジトリのファイル 171 件のうち `.rs` の 75 件だけを検索(`run5.log` の 02:49:26Z の手)。文書(README など)を検索していない | **台帳で `未判別` に戻した** |

**型**: 調査班は N を「検索した範囲」で数えていた(N と M が自動的に一致する)。起動文の N は「候補の一次資料の一覧の件数」。6 回目の起動文で、N はファイル一覧の手の出力の件数そのもの(除外はファイル名と理由を全部書く)と書き、リードは検収で全部の `なし` を数え直す。

## 3. 台帳

`cat8_ledger.py import <報告> --round 5`: 変わった所 29。そのあと §2 の 3 候補を `set` で戻した。`check` 0 件。`recount`: 深掘り 10 / 浅い 2(8-003・8-008)/ 判別に一次資料が要る 2(8-013・8-016)/ 危険で導入停止 1 / 登録が要る 1。**残り 4 行**(8-003・8-008・8-013・8-016)。

## 4. 抜き取り(`印` の原典)

- 8-006 E3b(なし → 印): freqtrade `docs/strategy-customization.md` 245 行「To account for this, the strategy can be assigned the `startup_candle_count` attribute.」。**一致**。

## 5. 判定

**一部受け取り**(§2 の 3 候補の `なし` 10 件は受け取らない)。中身の回なので監査役(30 回目)に通している。
