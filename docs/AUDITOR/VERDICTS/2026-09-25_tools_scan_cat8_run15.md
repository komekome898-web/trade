# 検収: 道具サーベイ 区分 8 の 15 回目(2026-09-25、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 15 回目の実行(2026-09-25)`(19091 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run15.log`(5,033 行・48 手)。届いたままのコミット 8c087dd。起動文の印 `20260923_tools_survey_cat8_run15_prompt.md@1d21e6c15a85`。

## 0. 費用と時間

調査班 544,664 トークン・2,120,218 ms(35.3 分)・道具 225 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 15` 0 件 / `check "" <run15.log>` 0 件。`check_scan_report.py` は調査班の報告で 21 件。内訳は次のとおり。
- K13 18 件: 前の回からあるもので、閉じずに残す。
- K1 1 件・K2 2 件: `### 当たりの判定(行ごと)` のソースの 1 行の逐語引用と、`### 決まりの一覧` の正規表現そのものによる。表の約 40 行が、ソースの文法的に閉じていない所(`def foo(` のような関数の書き出しなど)をそのまま引いており、1 行ごとに括弧や太字の対が 1 つずつずれる。`check_scan_report.py` は空行の無い塊(この 290 行の表の全体)を 1 つの段落として数えるので、そのずれの合計が出る(監査 86 回目がこう訂正した)。引用を書き換えると逐語でなくなる。誤検出として閉じず、理由をここに書く。`check_scan_report.py` は区分 1 と共有なので触らない。

## 2. リードが見た点と直し

- **(乙) の決まりの抜け(リードが検収で見つけた)**: 15 回目の `cat8_classify.py` は、行の中の検索語の当たりのうち **1 つでも**決まりに重なれば、その行ごと取っていた。行に `records`(一般名詞)と別の検索語(`capture` など)があると、`records` の決まりが行ごと取り、もう一方の語は誰にも読まれない。数え方: 取られた 1,503 行のうち 76 行で、取った決まりが行の検索語の一部にしか重なっていなかった(語の内訳 record 67・capture 7・replay 2・rerun 1)。
- **直した(`scripts/cat8_classify.py`)**: 行を取るのは、行の中の検索語の当たりが**全部**、どれかの決まりに重なるときだけにした。1 つでも重ならない当たりがあれば `unmatched` に出す。16 回目から効く。
- **15 回目の当たりへの当て直し**: 直した道具で、生ログの写し(scratchpad)の検索の手 900 行目に同じ決まり 22 本を掛けた。結果は `matched=1475 unmatched=318`(元は 290)。新しく `unmatched` に出た 28 行を、リードが全部読んだ。
  - `record` 23 行: データの 1 行(`to_records`・「failed records」・「trip record data」)、ログの記録(`logging.LogRecord`・`caplog.records`)、テストの登録簿の項目(`registry.py`・`test_data_source_wiring.py`・README の「declared record」)。
  - `replay` 2 行: `docs/docusaurus/yarn.lock:3045・3053`。Sentry のブラウザ録画(`@sentry-internal/replay`)の依存。
  - `rerun` 1 行: `tests/integration/test_script_runner.py:376`。`flaky_test(rerun_filter=…)` で、不安定な試験を再試行する仕組み。
  - `capture` 2 行: `integrity.md:50` の「records that capture transaction details」(記録するという一般の語)と、`test_data_source_registry.py:1429` の「the capture records」(テストが値を写すこと)。
  - `ExpectQueryResultsToMatchComparison.json:3` の当たりは `record` の一般名詞。「compares」は E4(再生)の述語ではない。
  - **28 行とも、記録した市場データを再生する機能を指さない。8-017 E4 の `なし` は変わらない。**報告の行の表は届いたままにし、28 行の判定はこの記録に置く。
- 決まり 22 本(`### 決まりの一覧`)を読んだ。いちばん広いのは `r_record_compound_id`(`[A-Za-z_]record|record[A-Za-z_]`、316 行)と `r_records`(`records\b`、488 行)。どちらも「record」をデータの行として使う Great Expectations のコードに当てたもので、上の当て直しで、ほかの検索語と同じ行にあるものは `unmatched` に出た。決まりごとの見本 10 行と、ファイルごとの数は、生ログの 3593 行目からの手にある。
- **§4.0**: 2 候補の 13 項目を、隔離環境での導入・`pip check`・最小実行と、料金の頁の描画で埋めた。`保守者数` は `未確認` のまま。試した手段は 2 つある。1 つは `api.github.com` で、この会話のアクセスの範囲の外として 403 が返った(DNS や TLS の失敗ではない)。もう 1 つは `ungh.cc` の貢献者の一覧で、コミット数の順しか出ず、保守者を示す項目が無い。
- 問い(報告の `### 判断に迷った点と問い`)への答え(リードの判断):
  1. 描画した料金の頁を §4.0 の根拠にしたこと: 受け取る。E1a〜E6 の描画の条件 (i)(ii)(iii) を満たしている。
  2. `コミット数` を貢献者の上位 30 名の合計(下限)として「推定」としたこと: 受け取る。「推定・下限」の印のままにする。
  3. OpenBB の最小実行で `openbb-technical` を足したこと: 受け取る。OpenBB 自身の拡張で、隔離環境の中だけに入れた。
  4. `保守者数` の定義を読み替えるか: 読み替えない(定義を変えずに `未確認` とし、試した手段を残す)。
- **8-017 E3a・E3b・8-020 E3a の (甲) の表**を読んだ(18・2・29 ファイル)。理由は、ファイルごとに当たった語とその行での意味を書いている(例: `look-ahead` は構文解析の先読み、`as-of` は版の日付の説明)。述語(道具の機能としての先読みの検出・防止)に当たる行は見当たらなかった。

## 3. 台帳

`import --round 15`: 変わった所 6(8-017 E3a・E3b・E4 と 8-020 E3a が `なし`、2 候補が `深掘り`)。`check` 0 件。`recount`: 深掘り 24 / 残り 14(8-026、8-029〜8-041)。

## 4. 判定

**受け取る**(中身の監査 86 回目を通す)。

## 5. 中身の監査(86 回目、owner-auditor)の逐語

> 監査 86 回目・道具サーベイ区分8・15回目の実行(2026-09-25)の検査結果。
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 15 回目の実行(2026-09-25)`(19091行目〜)/生ログ `docs/DATA/probes/20260923_tools_8_run15.log`/検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run15.md`/`scripts/cat8_classify.py`(コミット c922b2f)。
>
> 実測は既存の scratchpad の venv(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/cat8/venvs/`、生ログ収集時のまま残存)を読むだけで行い、`scripts/cat8_classify.py` を委任文どおりのコマンドで実際に打って照合した。一時ファイルはすべて scratchpad 内に置いて削除済み。`git status --porcelain` は `docs/AUDITOR/TRACE/2026-09-25_2da9385f.json` の自動更新のみで、他に変更・未追跡物は無い。
>
> ## 指摘
>
> 1. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:19144,19147`(候補の一覧の説明文)と `:19514-19515,19528-19529`(§4.0 の表) — 候補の一覧の説明文は両候補とも「無料枠の上限・課金開始条件は引き続き未確認」と書いているが、同じ節の §4.0 の表では、この2項目とも `一次資料`(`greatexpectations.io/pricing`・`openbb.co/pricing` を `cat8_render.js` で描画して得た具体的な金額・プラン名)で埋まっている。生ログでも実際に埋まっていることを確認した(GE: `docs/DATA/probes/20260923_tools_8_run15.log:4525` 付近に「Developer」「Team」「Enterprise」、OpenBB: 同 `4603` 付近に「$2,400」「$1,200/year」「$500/year/seat」「20 queries/day」の逐語を確認)。同じ節・同じ回の中で、要約文と詳細表が矛盾している。14回目に「未確認13項目」だったうち、実際に埋めたのは10項目(実測)+2項目(無料枠の上限・課金開始条件、一次資料)+1項目(規模の見積、推定)の計13項目全部のはずで、要約文の「引き続き未確認」は古い状態の書き忘れと見られる(P2 に近い型: 同じ主張が文書内の2箇所で食い違う)。
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:19905-19907`(`### 決まりの一覧` 冒頭)と検収記録 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run15.md:17-19` — 検収でリードが見つけた不備(1つでも決まりが重なれば行ごと取ってしまうバグ)を受けて `scripts/cat8_classify.py` を修正した(コミット c922b2f)が、修正後の道具を同じ生ログ・同じ決まりに掛け直すと `matched=1475 unmatched=318`(実測で再現・確認済み)になる。しかし報告書本体(`docs/DATA/SCAN_2026-09-23_tools_cat8.md`)の `### 決まりの一覧` と `### 当たりの判定(行ごと)`(19608-19904行目、290行の表)は、修正前の道具の出力(`matched=1503 unmatched=290`)のまま据え置かれている。つまり現在リポジトリに入っている `cat8_classify.py` を同じ入力で再実行しても、報告書が自ら引用する数値を再現できない。検収記録には新たな28行の判定を書いたが、報告書本体側は直していない(「報告の行の表は届いたままにし、28行の判定はこの記録に置く」と明記)。結論(E4は「なし」)自体は、私が独立に同じ28行を読み直しても揺るがない(後述)が、報告書の数値の出所を将来たどると、現在の道具では再現できない値が「一次資料」として残ることになる。
>
> 3. [聞く] 上記2に関連して、検収記録だけに新事実を残し報告書本体を直さないという処理方針(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run15.md:19,25`)は、今後この報告書を単独で読む人(次回以降の調査班・監査役)に、修正前の道具の出力が現行の道具でも再現できると誤解させないか。追記コストは小さい(表の冒頭に「修正後の道具では matched=1475/unmatched=318 になる。新たに unmatched となった28行の判定は検収記録参照」と1行足すだけ)。直すかどうかはリードの判断だが、確認済みか。
>
> ## 確認できた点(問題なし)
>
> - 8-017 Great Expectations の E3a(39件/18ファイル)・E3b(2件/2ファイル)・8-020 OpenBB の E3a(66件/29ファイル)の「なし」の(甲)表は、生ログの実際の grep 出力と1件ずつ突き合わせ、件数・ファイル数・理由の要旨とも一致することを確認した(`docs/DATA/probes/20260923_tools_8_run15.log:832-891,3249-3315`)。理由文も、実際にその行を読んだ上での記述になっている(例: `STATEMENT_SCHEMA_README.md` の「13.5 Point-in-Time Considerations」節を実際に開いて読み、`pit_mode` という緩和策を案内する設計文書であって自動検出機能ではないという記述が正確であることを確認した)。
> - `scripts/cat8_classify.py` の「行の中の検索語の当たりが全部どれかの決まりに重なるときだけ行を取る」への修正(diff: c922b2f)は、記述どおりに実装されている。修正前は1語でも重なれば行ごと取っていた(`break` で最初に一致した決まりへ丸ごと計上)バグが、修正後は各語ごとに独立して被覆判定するよう直っている。合成データでの再実験は行わなかったが、実データ(GE E4 の900行目の手)への適用結果が検収記録の数値(matched=1475/unmatched=318)と完全に一致することを確認した。
> - 検収記録が新たに読んだ28行(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run15.md:20-24`)を実際に生ログから抽出し、記載どおりの内訳(record 23・replay 2・rerun 1・capture 2)になることと、個々の行の文意(`logging.LogRecord`、GE自身のテスト登録DSLの「record」、Sentryブラウザセッションリプレイの依存、`flaky_test(rerun_filter=...)`、`captured before the move` のテスト定数コメント等)が、記載された理由と食い違わないことを確認した。市場データ再生機能を指す行は1件も無く、E4「なし」の結論は妥当と判断する。
> - §4.0 の表の実測値(GE: install 21.76秒・35パッケージ・`pip check` 正常・最小実行で`unexpected_count: 1`検出・テレメトリ関連パッケージ0件。OpenBB: install 29.49秒・98パッケージ・`pip check` 正常・`ichimoku()`の`lookahead`パラメータの実動作確認・`pandas_ta_openbb`のバグと回避策)は、いずれも生ログの該当行と一致する。ungh.cc の貢献者上位30名の contributions 合計(GE: 10738、OpenBB: 5004)も実際に手計算して一致を確認した。`保守者数` を `未確認` のまま残した理由(`api.github.com` が403、`ungh.cc` に保守者を明示する項目が無い)も生ログどおりで、根拠が薄い「時間が掛かる」等の理由ではない。
> - 検収記録の K1(太字奇数)・K2×2(括弧不一致)の「誤検出として閉じる」判断は、`check_scan_report.py` の `paragraphs()` が空行の無い塊(この場合は290行の巨大な表全体)を1つの段落として扱う仕様に起因することを実測で確認した。ただし検収記録の説明「複数行にまたがる太字や括弧を1行だけ切り出しているため」は不正確で、実際には表内の約40行(`def foo(` のように関数シグネチャの冒頭だけを引用した行など)がそれぞれ独立に丸括弧の不均衡(±1)を持ち、それらの合計が表全体で一致しない形になっている(太字の奇数も同様に複数箇所由来)。私が全290行を機械的に再点検した限り、これらの不均衡はすべて「原典コードの引用が文法的に閉じていない箇所を字面どおり引用した」ことに由来し、表そのものの体裁崩れ(セル抜け等)は見つからなかったので、誤検出という結論自体は妥当と判断する。
>
> ## 対象外とした点
>
> - 起動文(`run15_prompt.md`)の監査は78回目から85回目まで、`cat8_classify.py`・`cat8_ledger.py` の機械的な抜け道について非常に広く検査済みで、いずれも「この回(GE・OpenBBの規模)では顕在化しない latent な欠陥」として処理されている。私自身の実測でもこれらの記述と矛盾する点は見つからなかったため、重複した指摘はしていない。次回(16回目、OpenClaw、当たり最大33,906件)でこれらの latent な欠陥が顕在化しないかは、今回の対象外(起動文であり報告書の中身ではない)として明記しておく。

## 6. 処置(リード、監査 86 回目)

1・2・3. **直した(報告の本文は届いたままにし、節の末尾に注記を足した)**。15 回目の節の末尾に `### 検収の注記(リード、2026-09-25。本文は届いたまま)` を置き、次の 2 つを書いた。1 つは、候補の一覧 17・20 の「引き続き未確認」が古い書き方で、§4.0 の表では埋まっていること。もう 1 つは、決まりの一覧と行の表の数が直す前の道具の出力であること、直した道具では matched=1475 / unmatched=318 になること、新しく出た 28 行の判定がこの記録の §2 にあること。
- K1・K2 の説明は、監査 86 回目の訂正のとおりに §1 を直した。

判定は変えない(**受け取る**)。
