# 検収: 道具サーベイ 区分 8 の 15 回目(2026-09-25、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 15 回目の実行(2026-09-25)`(19091 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run15.log`(5,033 行・48 手)。届いたままのコミット 8c087dd。起動文の印 `20260923_tools_survey_cat8_run15_prompt.md@1d21e6c15a85`。

## 0. 費用と時間

調査班 544,664 トークン・2,120,218 ms(35.3 分)・道具 225 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 15` 0 件 / `check "" <run15.log>` 0 件。`check_scan_report.py` は調査班の報告で 21 件。内訳は次のとおり。
- K13 18 件: 前の回からあるもので、閉じずに残す。
- K1 1 件・K2 2 件: `### 当たりの判定(行ごと)` のソースの 1 行の逐語引用と、`### 決まりの一覧` の正規表現そのものによる。複数行にまたがる太字や括弧を 1 行だけ切り出しているためで、引用を書き換えると逐語でなくなる。誤検出として閉じず、理由をここに書く。`check_scan_report.py` は区分 1 と共有なので触らない。

## 2. リードが見た点と直し

- **(乙) の決まりの抜け(リードが検収で見つけた)**: 15 回目の `cat8_classify.py` は、行の中の検索語の当たりのうち **1 つでも**決まりに重なれば、その行ごと取っていた。行に `records`(一般名詞)と別の検索語(`capture` など)があると、`records` の決まりが行ごと取り、もう一方の語は誰にも読まれない。数え方: 取られた 1,503 行のうち 76 行で、取った決まりが行の検索語の一部にしか重なっていなかった(語の内訳 record 67・capture 7・replay 2・rerun 1)。
- **直した(`scripts/cat8_classify.py`)**: 行を取るのは、行の中の検索語の当たりが**全部**、どれかの決まりに重なるときだけにした。1 つでも重ならない当たりがあれば `unmatched` に出す。16 回目から効く。
- **15 回目の当たりへの当て直し**: 直した道具で、生ログの写し(scratchpad)の検索の手 900 行目に同じ決まり 22 本を掛けた。結果は `matched=1475 unmatched=318`(元は 290)。新しく `unmatched` に出た 28 行を、リードが全部読んだ。
  - `record` 23 行: データの 1 行(`to_records`・「failed records」・「trip record data」)、ログの記録(`logging.LogRecord`・`caplog.records`)、テストの登録簿の項目(`registry.py`・`test_data_source_wiring.py`・README の「declared record」)。
  - `replay` 2 行: `docs/docusaurus/yarn.lock:3045・3053`。Sentry のブラウザ録画(`@sentry-internal/replay`)の依存。
  - `rerun` 1 行: `tests/integration/test_script_runner.py:376`。`flaky_test(rerun_filter=…)` で、不安定な試験を再試行する仕組み。
  - `capture` 2 行: `integrity.md:50` の「records that capture transaction details」(記録するという一般の語)と、`test_data_source_registry.py:1429` の「the capture records」(テストが値を写すこと)。
  - `ExpectQueryResultsToMatchComparison.json:3` の当たりは `record` の一般名詞。「compares」は E4(再生)の述語ではない。
  - **28 行とも、記録した市場データを再生する機能を指さない。8-017 E4 の `なし` は変わらない。**報告の行の表は届いたままにし、28 行の判定はこの記録に置く。
- 決まり 22 本(`### 決まりの一覧`)を読んだ。いちばん広いのは `r_record_compound_id`(`[A-Za-z_]record|record[A-Za-z_]`、316 行)と `r_records`(`records\b`、488 行)。どちらも「record」をデータの行として使う Great Expectations のコードに当てたもので、上の当て直しで、ほかの検索語と同じ行にあるものは `unmatched` に出た。決まりごとの見本 10 行と、ファイルごとの数は、生ログの 3593 行目からの手にある。
- **§4.0**: 2 候補の 13 項目を、隔離環境での導入・`pip check`・最小実行と、料金の頁の描画で埋めた。`保守者数` は `未確認` のまま。試した手段は 2 つある。1 つは `api.github.com` で、この会話のアクセスの範囲の外として 403 が返った(DNS や TLS の失敗ではない)。もう 1 つは `ungh.cc` の貢献者の一覧で、コミット数の順しか出ず、保守者を示す項目が無い。
- 問い(報告の `### 判断に迷った点と問い`)への答え(リードの判断):
  1. 描画した料金の頁を §4.0 の根拠にしたこと: 受け取る。E1a〜E6 の描画の条件 (i)(ii)(iii) を満たしている。
  2. `コミット数` を貢献者の上位 30 名の合計(下限)として「推定」としたこと: 受け取る。「推定・下限」の印のままにする。
  3. OpenBB の最小実行で `openbb-technical` を足したこと: 受け取る。OpenBB 自身の拡張で、隔離環境の中だけに入れた。
  4. `保守者数` の定義を読み替えるか: 読み替えない(定義を変えずに `未確認` とし、試した手段を残す)。
- **8-017 E3a・E3b・8-020 E3a の (甲) の表**を読んだ(18・2・29 ファイル)。理由は、ファイルごとに当たった語とその行での意味を書いている(例: `look-ahead` は構文解析の先読み、`as-of` は版の日付の説明)。述語(道具の機能としての先読みの検出・防止)に当たる行は見当たらなかった。

## 3. 台帳

`import --round 15`: 変わった所 6(8-017 E3a・E3b・E4 と 8-020 E3a が `なし`、2 候補が `深掘り`)。`check` 0 件。`recount`: 深掘り 24 / 残り 14(8-026、8-029〜8-041)。

## 4. 判定

**受け取る**(中身の監査 86 回目を通す)。
