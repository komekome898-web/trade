# 監査(owner-auditor)— `REPORT_2026-09-16_policy4.md` と `AUDIT_LEDGER_2026-09-14.md`(2026-09-16)

対象: `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md`(主)/ `AUDIT_LEDGER_2026-09-14.md` / `REPORT_2026-09-14.md` §0 の更新ブロック / 生ログ `evidence_2026-09-16/15_第4の方針.log`。
監査役の返答は逐語のまま。各件の直下にリードの応答(直した / 上申 / 採用しない)。

## 監査役の返答(逐語)

> 検査した結果を以下に示す。対象は `REPORT_2026-09-16_policy4.md`、参照した関連資料は `AUDIT_LEDGER_2026-09-14.md`、`REPORT_2026-09-14.md` §0 冒頭の更新ブロック、生ログ `evidence_2026-09-16/15_第4の方針.log`、および実装 `src/bot/research/liq_response.py`・`scripts/verify_liq_instrument.py`。
>
> 1. [止める] docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md:37,55-63 — 3候補すべての「不合格」判定は `許容 ±3.0bp` という一本の値に依存している。`scripts/verify_liq_instrument.py:80-84` を見ると、この値は「健全な合成データは通し、条件1の変異試験(効果半減=誤差10bp)は確実に落とす」ために設計された、**別の検査(素の合成データの効果量検査)用の閾値**である。この報告書は同じ定数を「群で対称な向き付け方針が仕込み値を再現できるか」という別の問いの合否バーとして転用しているが、その転用が妥当かの検討・出所の分類(一次資料/実測/仮定)は本文にも§0.1の対応表にも書かれていない。この閾値が変われば§3の合否表(特に症状の境界に近い行、例: symfine行4の偏差3.954bp)が変わりうる。この判定バーの出所と、条件1用に設計された値を第4の方針の合否に転用した根拠は書かれているか(P-2、P-16)。

**リードの応答: 直した(報告書 §8 の 1)。**出所 = `TOLERANCE_BP`(リードが 09-14 に置いた仮定)、転用の根拠 = 既存 3 方針と同じバーで比べるため、感度 = 行 3 の到達範囲の上端 +16.190 と候補の最悪ずれ 9.85bp 以上により、許容を ±9.8 まで広げても不合格のまま。

> 2. [直す] docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md:112 — §6 は「`PYTHONPATH=src python -m pytest -q tests/ -k "liq"` → 89 passed, 1814 deselected」と書くが、同じ返答が検収の根拠として指す生ログ `evidence_2026-09-16/15_第4の方針.log:163-166` にはドット表示(進捗率)しかなく、pytest が実際に出す集計行(`89 passed, 1814 deselected in ...`)自体は含まれていない。「89」はドット数と一致するため passed 側は裏取りできるが、「1814 deselected」はログの本体出力からは確認できず、ログ末尾の注記(`※`、172行目)にのみ現れる。この数はどのコマンド出力から取ったものか(O-3、P-10)。

**リードの応答: 直した。**`-q` 無しで打ち直し、集計行 `89 passed, 1814 deselected in 6.38s` を生ログ末尾に追記(コマンドも同じ行に)。

> 3. [聞く] docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md:18-20 — §0.1 の対応表で「必要条件(a)〜(d)・合否±3.0bp・8行の場面・候補は最大3つ・判定は候補ごと一度だけ」という枠組み全体が「(該当語なし)—リードの依頼文の事前登録」に分類されている。CLAUDE.md §0.2 B の A-13「枠組み(問いの立て方・族の切り方・検定単位)はオーナーに見せてから進める」に照らすと、この枠組みが着手前にオーナーへ提示・確認された記録は本報告書内に見当たらない。この枠組みは L-179 の「承認済みの常設スキルの固定手順」に該当するので聞かずに当ててよい範囲なのか、それとも A-13 が対象とする「族の切り方・検定単位」そのもの(候補数の上限3・判定バー±3.0bpなど)としてオーナーに見せる必要があったか(P-9、CLAUDE.md §0.2 B/A-13)。

**リードの応答: 報告書 §8 の 3 に事実を書いた。**場面・許容・判定の対象は L-180 への返答で見せた(逐語を引用)。(a)〜(d)・候補上限 3・判定 1 回は見せていない。L-179 の固定手順には当たらない。見せずに進めた事実と、結果への影響(行 3 は狭まっていない)を明記。**オーナーへは返答本文でも伝える。**

> 4. [聞く] docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md:33,100 — 必要条件(d)は「`bp_*` を読まない」だが、同じ行で「`widen` と `ohlc` は `end_ms` より後の価格を見るので、反応窓と時間的に重なる」と自ら認めている。この2候補は形式上(d)を満たすとされたまま§2・§3の数値判定(不合格)に進んでいるが、この時間的重なりが(d)の実質的な違反にあたるかどうかの基準は示されていない。もし重なりを実質違反とみなすなら、この2候補は数値判定の前に不合格(条件不成立)として扱うべきだったのではないか。射程(§5.2)で「実データでこの重なりが無害かは測っていない」と書く一方、合成データでの判定そのものへの影響(重なりが合成データの数値をどちら向きに動かしたか)も測っていない。この単位の必要条件(a)〜(d)と、実際に候補を判定に進めた基準は一致しているか(P-9)。

**リードの応答: 報告書 §8 の 4 に答えを書いた。**形式上は満たし、実質は境界。事前登録の (d) は窓の重なりまで禁じていなかった。2 候補とも不合格なので結論は変わらない。合格していたら採用できなかった。次回は (d) に明記する。合成での影響の向きは未測定(射程のまま)。

> 5. [聞く] docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-16_policy4.md:18 — 「候補は最大3つ」という上限自体の根拠が本文に無い(§5.4は「符号の付け方を尽くしてはいない」と射程として認めるのみ)。この上限はリードが単独で置いたスコープ縮小であり、CLAUDE.md §0.2 B の A-10(族・スコープを説明なく縮める)に照らして、なぜ3つで打ち切ったのか(計算量か、網羅性の検討結果か)を明示していない。この上限は道具・時間の制約か、それとも「これ以上は仮説として意味が薄い」という判断か(P-13)。

**リードの応答: 報告書 §8 の 5 に答えを書いた。**予算(上位モデル 90% / 自主上限 70%、1 回の委任に収める)と、網羅性の代替(到達範囲の測定)。「意味が薄い」という判断ではない。

## 状態

未対応の「止める」0 件 / 上申 0 件。届けてよい。
