# 検収: 道具サーベイ 区分 8 の 23 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 23 回目の実行(2026-09-26)`(39130 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run23.log`(222,783 バイト)。届いたままのコミット 00c3cf4。起動文の印 `20260923_tools_survey_cat8_run23_prompt.md@9b52c42f5ab4`。

## 0. 費用と時間

調査班 400,314 トークン・1,392,737 ms(23.2 分)・道具 156 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 23` 0 件 / `check "" run23.log` 0 件 / `check_scan_report.py`(run1〜run23)の当たりのうち 23 回目の節の中は 0 件。

## 2. 台帳

`import --round 23`: 変わった所 0。そのあとリードが `set 8-030 E1b=印 段_E1b=2`(コミット fef1223。**監査 109 回目より前に当てた**。処置 1)。`check` 0 件。

## 3. 中身の監査(109 回目、owner-auditor)の逐語

> 監査 109 回目の指摘を以下に示す。
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 23 回目の実行(2026-09-26)`(39130 行目〜末尾)と生ログ `docs/DATA/probes/20260923_tools_8_run23.log`。参照: 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run23_prompt.md`(指紋 `9b52c42f5ab4`、実測 sha256 と一致確認済み)、22 回目検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run22.md`、設計票 `docs/DATA/surveys/CAT8_DESIGN.md`、`docs/DATA/tools_catalog_cat8.tsv`、`git log`/`git show`(コミット `00c3cf4`「cat8 round 23: dbt E1b/E4 exhaustive mark search (as delivered)」・`fef1223`「cat8 ledger: round 23 import; dbt E1b set to mark stage 2 ... audit pending」)。
>
> 1. [止める] `docs/DATA/tools_catalog_cat8.tsv:31`(コミット `fef1223`)対 `docs/DATA/SCAN_2026-09-23_tools_cat8.md:39449,39453,39561-39564` — 監査対象の報告(コミット `00c3cf4`「as delivered」= 調査班の最終返答そのもの)は `dbt`/E1b=`未判別`・段=`未判別` を明記し、知見#4で「除外規定の当たり方が未確定」、問い#1で「決めずに問いとして出す」と明示的に未解決のまま提出している。ところが同日、報告とは別のコミットで台帳の E1b が `印`/段 `2` に書き換えられている(`git show fef1223 -- docs/DATA/tools_catalog_cat8.tsv`)。この書き換えは、調査班が自ら答えを保留して監査に委ねた問い#1を、この監査(本回)より前にリードが単独で確定させたものであり、①設計票 §3「述語を足したり変えたりしたら、全行に当て直す」の要否判定(14 回目から適用してきた既存の読みの範囲を狭める/広げるかの判定)を経ていない、②CLAUDE.md §0.2 A-13「枠組みはオーナーに見せてから進める」に照らし、除外規定の解釈という枠組みレベルの変更をオーナーに見せる前に台帳へ確定させている。22 回目検収(105 回目監査の指摘2・処置2)では逆方向(印→未判別で維持)の判定について 27 行中 16 行を読み直す当て直し確認を行っているのに、今回の逆方向(未判別→印)の判定にはそれに相当する当て直し確認が artifact 内に無い。(出典: `CAT8_DESIGN.md` §3、CLAUDE.md §0.2 A-13、KA-06 の型)
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:39161-39162`(知見#9・#10「合計7,878件」「合計9,472件」)対 `docs/DATA/delegations/20260923_tools_survey_cat8_run23_prompt.md:31`(「数えるもの: dbt の公式のリポジトリの中のもの…だけ。別の配布物(…`dbt-audit-helper`・`dbt-utils` のパッケージ)は数えない」) — 知見#9・#10 が引用する合計値は `docs/DATA/probes/20260923_tools_8_run21.log:211-218,235-242` の内訳どおり dbt-utils(149/46 件)・audit-helper(351/23 件)を含んで合算している。これはこの回自身の起動文 §1 が「数えない」と明記した配布物であり、この回の族の定義と引用した「当たりの行の数」の対象範囲が一致していない(main+1.latest+docs のみなら E1b=7,378・E4=9,403)。500 行の上限を超える結論自体は変わらないが、案 B の記録として書く数値がこの回のスコープ外のものを含んでいる。
>
> 3. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:39196`(候補の一覧「dbt」の行)/ `CAT8_DESIGN.md:38`(案 B のリードの当て方「(2) 未判別以外の点では深掘りの条件(委任文§4.0の条件)を満たすものは、残りに数えない」) — この回の報告は §4.0 の表を「版」の 1 行だけに絞っており(§2.5 の指示どおり)、dbt の §4.0 表(9 項目程度)が「過半が一次資料か実測」を満たすことをこの回の artifact 内で再確認・再記載していない。「8-030 を『残りに数えない』」という判定は、この artifact だけからは確認できない。
>
> 4. [聞く] 依頼文にある「13 回目ごろに受け取った Great Expectations の E1b(`ColumnMean` が 3 つのエンジンで平均を計算する。利用者が選ぶのは列)と同じ形」という比較は、この回の報告・生ログのどこにも再照合した記述が無い(知見#3・#4 は MetricFlow 側の一次資料だけを引用し、GE 側の一次資料には触れていない)。この比較はリードの記憶によるものか、13 回目の報告・生ログを再度開いて確認したものか(P10)。
>
> 5. [聞く] 台帳で E1b=なし の行は `TradingView のリプレイ機能`・`Quantreo library` の 2 件のみ残っている。「利用者が指標の定義(パラメータ)を指定し、計算の中身は道具が作る」という今回の新しい読みを E1b に適用するなら、この 2 件の `なし` 判定も同じ観点での当て直しが要るか(指摘1と表裏一体)。
>
> 範囲外の書き込みについては、候補の一覧(39167-39207)・要素と段の表(39212-39543)・§4.0 表(39549-39553)のいずれも dbt/8-030 以外は「台帳の値のまま(22回目の節)」で統一されており、起動文 §1・§3 の書き換え範囲を超えた痕跡は見当たらなかった。`check_scan_report.py`・`cat8_ledger.py check-elements`・`check ""`・`git diff` の 4 検査もこの節内では 0 件で、K13 の 4 件は 22 回目検収で受け取り済みの型と同型という説明も機械的に妥当に見える。知見#5・#6 が引用する Time Machine / record-replay の逐語(`docs/DATA/probes/20260923_tools_8_run23.log:1203-1567`)は生ログの範囲内で確認でき、E4 を L-516 で除いた判断・microbatch 再検討(知見#7、run23.log:1336-1421)の一次資料も生ログと整合していた。
>
> 止める指摘: あり(1)。

## 4. 処置(リード、監査 109 回目)

1. **止める → 答える(値は `印`・段 2 のまま)**。
   - **順番の誤り(リード)**: 調査班が問いに出した値を、監査の前に台帳へ当てた。これまでの回は「取り込み → 監査 → 処置」だったが、この回は処置を先にした。以後、調査班の値を変えるときは、監査の結果を見てから台帳に当てる。
   - **読みは新しくない(確かめた)**: 14 回目の節の Great Expectations の E1b の行(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:17493`、根拠 `docs/DATA/probes/20260923_tools_8_run14.log:9356-9450`)は、「`ColumnMean` が『Pandas Mean Implementation』『SqlAlchemy Mean Implementation』『Spark Mean Implementation』の 3 エンジンで独立に column.mean() を計算する」で `印`。利用者が選ぶのは列で、計算の中身は道具が持つ。MetricFlow も、利用者が書くのは指標の定義(どの列を・どの窓で)で、計算の中身(窓関数の SQL)はコンパイラが作る(生ログ 1057〜1135 行)。同じ形なので、E1b の読みは 14 回目から変わっていない。22 回目に数えなかった「モデル」は、計算の中身を利用者が SQL で全部書くもので、この形と違う。**変わったのは 23 回目の起動文の除外の文の書き方だけで**、「指標の定義として書く SQL や設定」まで除外に読める広すぎる文をリードが書いた(リードの誤り。調査班はその文どおりに迷って問いに出した)。
   - **当て直しの確かめ(指摘 5)**: E1b が `なし` の 2 行を読んだ。`TradingView のリプレイ機能`(`:3306`)は、公式ヘルプの Bar Replay の 8 頁に、含み損益の表示の 1 項目しか無い、という `なし` で、この読みで変わらない。`Quantreo library`(`:5125`、12 回目)は、理由の欄が「損益・ポジション等の計算は行わない」だけで、述語の列挙にある**特徴量**に触れていない(Quantreo は特徴量のライブラリ)。**この読みの変更とは別の、12 回目の判定の弱さ**で、12 回目のファイルの表(96 ファイル・1,703 行)を読み直していないので、値は変えない(目視で判定しない。A-11)。資源化の前の全体受け取り監査で、Quantreo の E1b を当て直す項目に入れる。
   - **A-13(枠組みをオーナーに見せる)**: 今回の報告の返答に、この読み(利用者が定義を書き、計算の中身は道具が作るものは、道具の計算に数える)をそのまま出す。
2. **直した**。案 B の当たりの行の数を、dbt の公式のリポジトリと文書だけで数え直した(E1b 7,378 行・E4 9,403 行)。報告の節の末尾の「リードの追記」。
3. **答える**。§4.0 の表は 22 回目の節の 43 行(38986〜39028 行あたり)。22 回目の調査班の数えで 43 行中 39 行が一次資料か実測。22 回目の検収で「宣伝詐欺の兆候」を `推定` に下げたので 38 行。過半を満たす。この回の報告には再掲していない(表を写さない形にした)。
4. **答える**。記憶ではなく、処置 1 のとおり 14 回目の節の行と生ログの行を開いて確かめた(13 回目ではなく 14 回目だった。依頼文の「13 回目ごろ」はリードの記憶違い)。
5. **答える**。処置 1 の「当て直しの確かめ」のとおり。

**8-030 の扱い**: 台帳の状態は `浅い`。`未判別` は E4 だけで、案 B の記録(知見 #10 とリードの追記の数)があり、§4.0 の表は過半が一次資料か実測(処置 3)。設計票 §2 の案 B の当て方で、**残りに数えない**。残り 10 → 9(8-026 OpenClaw・8-029 MetaTrader・8-031〜035・039・040 のうち、8-033 Prefect を含む大きいリポジトリ 7 つと OpenClaw・MetaTrader)。

## 5. これからの手

24 回目 = 8-033 Prefect(未着手、1 行だけ。21 回目・22 回目の学び = §6-1 を最初に、別の配布物は数えない、案 B の記録)。
