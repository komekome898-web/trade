# 監査(owner-auditor)— 段 A 事前登録の確定版(2026-09-18)への 25 件の処置

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)。
設計は `REACTION_DESIGN_2026-09-18.md`(オーナー承認 L-197「**全てOKです。**」)、
確定の出所は L-199「**1 = a、9 = a**」。

件数: **止める 7(1〜7)/ 直す 11(8〜10, 13, 16〜19, 21, 22, 24)/ 聞く 7(11, 12, 14, 15, 20, 23, 25)
= 25 件(省略なし)。**
**監査役の指摘は逐語のまま。**その直下に処置(何をどう直したか、直さないなら理由)。
**止める 7 件と聞く 7 件はリードの決定に従った**(決定文も逐語で引く)。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**走らせていない**(`--mode full` は 1 本も実行していない)。

## 0.1 対応表(本作業)

| やろうとすること | オーナーの原文の該当語(逐語) | 中央の出所 / 誰の判断か |
|---|---|---|
| 25 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | **(該当語なし)** | リードの委任文面(本作業の指示) |
| 止める 7 件・聞く 7 件はリードの決定に従う | **(該当語なし)** | リードの委任文面「**リードの決定(止める 7 件 + 聞く 7 件。これに従う)**」 |
| 判定バーを \|t\| ≥ 3.925 にし、オーナーに次の返答で報告する | **(該当語なし)** | リードの決定 2(逐語は下の 2 番) |
| **W に依らない 12 群をどちらの走行から取るか** | **(該当語なし)** | **委任先の仮置き**(下の「決定に無い判断」1) |
| **ブートストラップ CI の水準** | **(該当語なし)** | **委任先の仮置き**(同 2) |
| **サニティ #13 を W 8h / 24h の 2 回数える** | **(該当語なし)** | **委任先の仮置き**(同 3) |

---

## 止める(7 件)

> **1. [止める] PREREG:219-224, 300, 972, 976 — §4 の 48 群は A1〜A5・E の 6 軸に「8h / 24h」の両方を数えて 3+3 としている(「| A1 | `bin_pct` 3 分位 | 8h / 24h | 3 + 3 |」)。一方 `scripts/o3c_reaction.py:2110` は `--window-hours` を 1 本の走行に 1 値しか取らず、出力の `summary` も `"window_hours": window_hours` 1 値である。§14.1 は「**この 1 本の出力に §4.3 の 576 検定を当てる**」と書くが、W = 24h の 18 群(18 × 2 系統 × h 6 = 216 検定)は主表(W = 8h)から出ない。その 18 群を出す gap60_w24 の走行は §14.2 で「感度(**観測のみ**。α に数えない)」と書かれている。**判定する 576 検定は、主表 1 本から実際に作れるか。作れないなら 576 と 48 群のどちらが正か**(P-3)**

**処置: 直した(リードの決定 1)。**逐語:
「**W は 48 群の分割軸なので、判定に使う走行は gap 60 秒 × W 8h と gap 60 秒 × W 24h の 2 本
(合わせて 48 群・576 検定)。感度 = gap 30 / 180 × W 8h / 24h の 4 本(観測のみ)。
§4.3・§14.1・§14.2 をこの形に直す(「主表 1 本」の記述を全部直す)。**」

- **§14.1** を「判定に使う 2 本」に書き替え、コマンドを 2 本並べた(`--window-hours 8` と `24`)。
- **§14.2** を「感度 4 本」にし、表の位置づけ欄で 6 本のうち 2 本を「判定」に直した。
- **§4.3** に「判定に使う走行」の行を足し、観測のみの表の「感度 5 本」を「感度 4 本」に直した。
- **§4.2** の「感度(gap 3 × 粒度 2)」の段を「判定 2 本 / 感度 4 本」に直した。
- **§7.2** に、6 本のうちどの 2 本が判定かを書いた注を足した。
- **§4(軸表の下)** に **48 群の内訳表**(W に依る 36 群は各走行から 18 ずつ、W に依らない 12 群は
  W = 8h の走行から)を新設した。
- **冒頭の凍結の宣言**に「判定に使う走行(2 本)」を足し、変更の理由を書いた。
- **§0.1・§13 の #2 / §13 の「報告する変更」**にも「主表 1 本 → 2 本」を書いた。
- **576 と 48 群は動かしていない**(監査の問い「どちらが正か」への答え = **両方正で、走行の本数が足りなかった**)。

> **2. [止める] PREREG:301-302, 814 — §4.3 は「α = **0.05 ÷ 576 = 8.681e−5**」、§9 項 3 は「**t ≥ 2.0**(ブートストラップ CI が 0 を除外)」を判定バーとする。t ≥ 2.0 の両側 p は 0.0455(実測: `NormalDist`)で、α = 8.681e−5 に対応する z は 3.9248(§8.2 の表と同じ値)。**α = 0.05/576 は MDE の倍率にしか効いておらず、採否の判定は無補正の t ≥ 2.0 で行う形になっている。576 セルに t ≥ 2.0 を当てることを、族を 576 に切った理由とどう整合させるか**(P-3、`research-protocol` §4-3)**

**処置: 直した(リードの決定 2)。**逐語:
「**判定バーを α = 0.05/576 と整合させる: 差あり ⇔ |t| ≥ 3.925(両側 α = 0.05/576 に対応する z。
§8.2 の 3.9248)。「t ≥ 2.0」は規約の単一検定の既定で、576 検定には当てない、と明記。
返答 060 #7 で黙認された「t ≥ 2.0」からの変更なので、§13 #7 に「変更: 監査の指摘 2 により、
族 576 と整合する 3.925 に。オーナーに次の返答で報告」と書く。**」

- **§4.3 の「統計量のバー」**を `|t| ≥ 3.925` に変え、「規約の既定 t ≥ 2.0 は単一検定のバー」と明記。
- **§9 の項 3** を同じ形に直し、§9 の見出しも「置くバーは \|t\| ≥ 3.925 だけ」に変えた。
- **§8.2 の再現コマンド**に 2 行足して、`t = 2.0 の両側 p = 0.04550026389635842` と
  `α = 0.05/576 の両側 z = 3.924789651405654` を**同じ返答の出力として**出した(**実測**、2026-09-18)。
- **§10.1 の分岐**を `|t| ≥ 3.925` に直した。
- **§13 の #7** に「**変更: \|t\| ≥ 3.925**」と、指摘 2 による変更であることを書いた。
- **§13 に「報告する変更」の表**を新設し、バーと走行 2 本の 2 件を**次の返答でオーナーに報告する**と書いた。
- **開封後の変更ではない**(走行は 1 本も走っていない)。

> **3. [止める] PREREG:829 — §10 の 3 番目に「**進行条件・反証条件・採用条件のセルごとの「満たす / 満たさない」と根拠の数値**」とあるが、進行条件・反証条件・採用条件という語は本書の他のどこにも定義が無い(全文検索: `grep -n "進行条件\|反証条件\|採用条件" PREREG` の一致はこの 1 行のみ)。§7 の F1 は「反証の形: その群で戻り到達の差が出ない」とだけ書く。**576 セルのうちどのセル(群・h・系統)が満たされたら F1 を支持/反証と書くのかが、走行前に固定されているか**(`research-protocol` §0.3、A-6)**

**処置: 直した(リードの決定 3)。§7.0 を新設した。**逐語:
「**F1 の支持 / 反証の形をセル単位で固定: 主軸 D の D1(`doi_pre_1h` が最も負の 3 分位)で、
戻り到達率の 実群 − 対照(ii) の差が 正(清算の後の方が戻りやすい)かつ |t| ≥ 3.925 かつ
差 ≥ MDE のセルが、h 6 本 × 系統 2 の 12 セルのうち 1 つでもあれば「F1 と整合」、
12 セルすべてで |差| < MDE なら「不明」、12 セルすべてで差が有意に負(|t| ≥ 3.925 で負)なら
「F1 の反証」、それ以外は「差あり(向きが F1 と逆)」または混在として観測を書く。
他の 47 群は副次(表に出すが F1 の判定には使わない)。「進行条件・反証条件・採用条件」の語は
§10 から消し、この規約に置き換える。**」

- **§7.0**(新設)に上の 4 行の表をそのまま置いた。
- **§7 の F1 のブロック**の「反証の形」を §7.0 への参照に変えた。
- **§10 の 3 番目**から 3 語を消し、「§7.0 の規約を 12 セルに当てた表」に置き換えた(訂正の注つき)。
- **§7.0** に「D は W に依らない軸なので gap 60 秒 × W 8h の走行から取る」と書いた(§4 の内訳表と整合)。

> **4. [止める] PREREG:849, 786 — §10.1 の MDE 列は「**§8.3 に無い群はその群の n で計算し直す**(式は §8.1、手順は §8.5)」、§8.6 は「**残り 44 群の MDE は出していない**」。§10.1 の 3 分岐は「差の絶対値 < MDE → 不明 / ≥ MDE → 差なし」なので、MDE は判定の境界そのものである。**44/48 群の境界が開封後に計算されること、およびその p / s を標本 6 日と判定区間のどちらから取るかが本書に書かれていないが、開封後に決める形で凍結と両立するか**(P-3、A-6)**

**処置: 直した(リードの決定 4)。**逐語:
「**MDE の入力: p・s は標本 6 日で固定(§8.2)、群ごとの n だけを走行後の群の件数から取る
(n は結果ではなく件数)。この形で「開封後に決める」余地を閉じる、と §8.6・§10.1 に明記。**」

- **§8.6** に入力の 3 行の表(p・s = 標本 6 日で固定 / z = α と β で固定 / n = 走行後の件数)を足し、
  「**n は観測量ではなく件数である**」と書いた。
- W = 8h の群は `..._sample/gap60_w8/table.csv`、W = 24h の群は `..._sample/gap60_w24/table.csv` から
  p・s を取る、と走行別に指定した(判定に使う走行が 2 本になったため)。
- **§10.1 の MDE 列**に「p・s は標本 6 日で固定、n だけ走行後の群の件数」と書き、
  **`alpha=0.05/576` を §8.5 の `mde()` に直接渡す**(倍率で読み替えない)と手順を 1 本に決めた。

> **5. [止める] PREREG:425-426, 219-228 — 3 分位の切り方が書かれているのは主軸 D だけである(「**3 分位の切り方: 実群で分位点を決め、対照にも同じ切り方を当てる**(委任先の判断)」)。A1〜A5・E・B1・B2 の 14 軸 × 3 分位(= 判定対象 48 群のうち 45 群の大半)について、分位点を実群で切るか対照込みで切るか、W ごとに切り直すか、判定区間で切るか標本 6 日の切り値を使うかが未記載である。**残り 14 軸の切り方は走行前に固定されているか**(P-3、A-6)**

**処置: 直した(リードの決定 5)。**逐語:
「**残り 14 軸の 3 分位の切り方: 判定区間の実群で W ごとに分位点を決め、対照にも同じ切り方を当てる
(D と同じ)。§4 の軸表に 1 行で書く。B1(束の大きさ)など分位でない軸は設計 §4 の定義のまま。**」

- **§4 の軸表の直下**に「3 分位の切り方(全 15 軸に当てる)」を置いた:
  **判定区間の実群で W ごとに分位点(33.33% / 66.67%)を決め、対照にも同じ切り方を当てる。
  標本 6 日の切り値は使わない。**
- **B1・B2** は 3 分位だが W に依らないので **W = 8h の走行の実群で 1 回だけ切る**と書いた。
  **C(side)は分位でない**ので設計 §4 の定義のまま。
- **§7 の D の行**も「判定区間の実群で」に直し、14 軸と同じ切り方であることを明記した。

> **6. [止める] PREREG:885, 1041-1044 — §11 のサニティ #9「`after_shift` の先読み防止の保証」は「**未通過**(REPORT §0.0 の C 行。埋めていない)」。原典 `docs/PHASE2/INSTRUMENT_VERIFY/REPORT_2026-09-14.md:46` も「測っていない」と書く。§14.4 は「**§11 のサニティ 13 件**」を走行前に通すとしながら、手当てしているのは #13 だけで #9 に触れていない。**起点の先読み防止が未測定のまま、一度きりの開封をしてよいか**(P-3)**

**処置: 通過にした(リードの実測 + 委任先の再実行)。**リードの決定の逐語:
「**先読み防止の実測(リードが実行済み): 手順 1 の出力 2 本(`backtest_data/o3c_reaction_20260918_anchor/table.csv`
と `..._anchor_trades/table.csv`)で `anchor_lag_ms`(= anchor_ts − end_ms)の最小値は 0.0、
負は 0 件 / 22,036 束(両方)。= 起点が束の終わりより前に来ることは無い。
これを §11 の #9 に「通過(2026-09-18、リード実測。コマンドと出力は VERDICTS)」と書く。**」

**再実行して同じ数が出ることを確かめた(2026-09-18、委任先)。再現コマンド**:

```
python3 - <<'PY'
import csv
for p in ['backtest_data/o3c_reaction_20260918_anchor/table.csv',
          'backtest_data/o3c_reaction_20260918_anchor_trades/table.csv']:
    v = [float(r['anchor_ts_ms']) - float(r['end_ms']) for r in csv.DictReader(open(p, newline=''))]
    print(p)
    print('  行数:', len(v), '/ anchor_lag_ms 最小:', min(v), '/ 負の件数:', sum(1 for x in v if x < 0))
PY
```

出力(**実測**、2026-09-18):

```
backtest_data/o3c_reaction_20260918_anchor/table.csv
  行数: 22036 / anchor_lag_ms 最小: 0.0 / 負の件数: 0
backtest_data/o3c_reaction_20260918_anchor_trades/table.csv
  行数: 22036 / anchor_lag_ms 最小: 0.0 / 負の件数: 0
```

- **§11 の #9** を「**通過**(2026-09-18、リード実測)」に直し、コマンドと出力を §11 の下に置いた。
- **射程も書いた**: これは**起点の位置**の測定で、起点より後の値を観測量が読んでいないことは
  サニティ #5 と #7 が別に見ている。**その 3 件の外は測っていない。**

> **7. [止める] PREREG:814 — 判定バーは「**日次 or イベントクラスタ** t ≥ 2.0(ブートストラップ CI が 0 を除外)」と規約の文言を引き写したままで、**どちらのクラスタ単位を採るか・ブートストラップの反復回数・リサンプルの種**が本書のどこにも固定されていない。**走行後に単位を選べる余地が残っていないか**(A-6、`research-protocol` §4-3)**

**処置: 直した(リードの決定 7)。§9.1 を新設した。**逐語:
「**ブートストラップ: クラスタ = 日(UTC 日)、反復 2,000 回、種 = 1、統計量 = 実群 − 対照の差 /
そのブートストラップ標準誤差 = t。§9 に固定。**」

- **§9.1**(新設)に 6 行の表を置いた: クラスタ = **日(UTC 日)**/ リサンプル = **日単位で置換あり**/
  反復 **2,000 回**/ 種 **1** / 統計量 `t` = 差 ÷ ブートストラップ標準誤差 / 区間 = パーセンタイル法。
- **§4.3** にも「ブートストラップ」の行を足して、判定の対象の表から読めるようにした。
- **CI の水準**は決定に無かったので **α = 0.05/576 に合わせた**(委任先の仮置き。下の「決定に無い判断」2)。

---

## 直す(11 件)

> **8. [直す] PREREG:772 vs 508-511 — §8.5 の末尾に「α の他の 3 通りは `alpha=` を変えるか、§8.2 の倍率(**×1.82698 / ×2.04283** / ×1.70133)を掛ける」とあるが、§8.2 の訂正は「**下書きは倍率を ×1.82698 / ×2.04283 と書いていた…正しくは ×1.827059 / ×2.042858 である**」と書いている。**訂正した旧値が同じ文書内に残っている**(P-2)**

**処置: 直した。**§8.5 の末尾を **×1.827059 / ×2.042858 / ×1.701326** に直し、
「**前版はここに旧値が 1 行だけ残っていた**」と訂正の注を付けた。
**この 2 通りは使わない α なので、判定に使う値は変わらない。**

> **9. [直す] PREREG:1055 vs 969 — §14.5 の止める点 3 は「**§14.1 の走行コマンドに `--mmr` が無い**」と書くが、§14.1 のコマンド行は `--out-dir … --mmr 0.004` を含んでいる。**凍結の宣言(冒頭 3 行)の後に §14.1 を書き換えた結果、本文が自分自身と食い違っている。どちらが確定版の文面か**(P-2)**

**処置: 直した。確定版の文面は §14.1 のコマンド(= `--mmr 0.004` あり)である。**
§14.5 の「止める点 3」の見出しに「**下の決定 3 で解決済み。§14.1 のコマンドは `--mmr 0.004` を含む**」
と書き、表の下に「**前版は §14.1 を書き替えたのにこの文面が残っていて、本文が自分自身と
食い違っていた**」と明記した。**止める点の表は経緯として残す**(消すと何が起きたか分からなくなるため)。

> **10. [直す] PREREG:1063 vs 833-859 — §14.5 の決定 1 は「群ごとの束の数が 30 未満の検定は『不明』(**検出力の欄**に「n < 30」と書く)…**§10.2 の 3 分岐**(差あり / 不明 / 差なし)の『不明』に流す」と書くが、3 分岐があるのは §10.1 で、§10.2 は「**`t` の列と `判定` の列を置かない**」表である。また §10.1 の「列は次の順で、**1 つも落とさない**」の列一覧に「検出力の欄」は無く、3 分岐にも n < 30 の分岐が無い。**凍結した §10.1 の表に決定 1 が反映されているか**(P-2、P-5)**

**処置: 直した(3 か所)。**

1. **§10.1 の列**に「**検出力**」を足した(群の束の数が 30 未満なら `n < 30`)。
2. **§10.1 の分岐**を 3 → **4 分岐**にし、一番上に「**群の束の数 < 30 → 不明**」を置いた。
3. **§14.5 の決定 1** の参照を「§10.2 の 3 分岐」→「**§10.1 の 4 分岐**」に直し、訂正の注を付けた。

**§9 の項 1**(前版は「オーナー / リードが決める」と空だった)も「**当てる**」に埋めた。

> **13. [直す] PREREG:968-970, 986-987 — §14 の走行コマンドに `--seed` が無い。`scripts/o3c_reaction.py:2112` の既定は `--seed 1` で、対照 (i) の無作為時刻と対照 (ii) の同点時の選択に効く(`f"{seed}|{day}"`)。**主指標(実群 − 対照 (ii))の再現に効く種が事前登録に書かれていないが、走らせ直して別の種を採る余地は閉じているか**(A-6)**

**処置: 直した。**§14 の**6 本すべてのコマンドに `--seed 1` を明記**した(§14.1 の 2 本は本文に、
§14.2 の 4 本は「`--mode full --approval L-199 --seed 1 --mmr 0.004` は 6 本とも同じ」として)。
§14.1 に理由も書いた: **種は対照 (i) の時刻と対照 (ii) の同点時の選択に効き、
主指標は実群 − 対照 (ii) なので、種は主指標に効く。既定が 1 でも書く。
走行の後に種を変えて取り直さない。**

> **16. [直す] PREREG:491 — §8.2 の群ごとの n の行に「対照 D1 **54** / D2 **69** / D3 **88**(**実測**)」とあるが、和は 211 で、同じ節の対照の母数 213(および n2 の元の 213/224)と 2 行合わない。実群側は「計 224、欠測 0」と母数が明示されている。**対照側の 2 行がどこへ行ったか(`doi_pre_1h` 欠測か)が書かれていないが、D 群の n2 の見積もりはどの母数で割ったものか**(P-5)**

**処置: 直した。答えは「`doi_pre_1h` の欠測 2 行」で、母数は 213(NA 込み)である。**
**実測**(2026-09-18、委任先): `table.csv` の `kind == 'control_matched'` 213 行のうち
`doi_pre_1h` が空の行が **2**(D1 54 + D2 69 + D3 88 + NA 2 = 213)。
§8.2 の該当行を「**+ `doi_pre_1h` が欠測で群に入らない 2(計 213)**」に直し、
節の下に「**n2 の見積もりは母数 213(NA を含む)で割っている**(§8.5 の `mde()` の `len(ctl)` = 213)」
と明記した。

> **17. [直す] PREREG:557-562 vs 489 — §8.3 の D 群の n1 は 7,098 + 7,098 + 7,003 = **21,199** で、§8.2 の n1 = **21,198** と 1 合わない(丸めの積み上がり)。**§3 が「3 つの数が一致する」と強調した 456 / 456 / 21,198 の系列に、この 1 の差は入れなくてよいか**(P-5)**

**処置: 直した(数は動かさず、由来を書いた)。**§8.2 に次を足した:
**理由は群ごとに `round()` してから足しているため**(**実測**: 21,198 × 75/224 = 7,097.8…、
× 74/224 = 7,002.4…。3 つを独立に丸めると和が 1 増える)。
**§3 の 456 / 456 / 21,198 の系列にこの 1 は入らない。21,198 が母数で、7,098 / 7,098 / 7,003 は
表示用の丸めである。判定のときは丸めた値ではなく走行後の群の実件数を使う**(§8.6)。

> **18. [直す] PREREG:542 vs 508-511, 354 — §8.2 の例「h = 60 分 1.30690 → **2.22347**。h = 240 分 2.32052 → **3.94797**」は、丸めた倍率 1.70133 を掛けた値である(厳密な倍率 1.701326397 では 2.22346 / 3.94796。実測: `NormalDist`)。§8.2 の訂正は「**丸めた `z` の和…から割ったための誤差**」を直したものであり、§10.1:849 は「§8.3 の α = 0.05 の値 × **1.70133**」と丸めた倍率を運用の手順に固定している。**§5:354 の「同じ量が 2 か所で別の桁に見えないようにする」と、この 2 経路(丸めた倍率 / `alpha=` 直接)は両立するか**(P-2)**

**処置: 直した。経路を 1 本に決めた。**

- **運用の正は `alpha=0.05/576` を §8.5 の `mde()` に直接渡す形**とし、**倍率は読み替えの目安**に落とした
  (§8.2 と §10.1 の両方をこの文言に直した)。
- **厳密な倍率 1.701326397** を §8.2 の再現コマンドの出力(9 桁)として出した(**実測**)。
- **例の 2 値を訂正**: 1.30690 → **2.22346**、2.32052 → **3.94796**(0.34028 は変わらない)。
- **判定に使う α もバーも動かしていない。**

> **19. [直す] PREREG:264, 887, 943 — 「粒度」が 2 つの量に使われている。§4.2 の表の見出しと §13 #2 は「感度(gap 3 × **粒度 2**)」= W(8h / 24h)の意味で使い(§7.2 は同じものを「gap 3 通り × W 2 通り」と書く)、§11 の #11 は「手順 1 を手順 2 と同じ**粒度**(約定そのまま)でも走らせる」= `--granularity {bar60,trades}`(`scripts/o3c_reaction.py:2124`)の意味で使っている。**一度出した用語が同じ文書内で 2 つの量を指していないか**(P-2、O-7)**

**処置: 直した(O-7)。**「粒度」は **`--granularity {bar60, trades}` の意味だけ**に使うことにし、

- **§4.2 の表の見出し**を「**走行 6 本(gap 3 通り × W 2 通り)**」に直し、訂正の注を付けた。
- **§13 の #2** の「感度(gap 3 × 粒度 2 = 6)」を「**走行 6 本(gap 3 通り × W 2 通り)**」に直した。
- **§5 の記号表**に「**粒度**」の行を足し、「**`W` の意味に使わない**」と書いた。
- **§14.5 の決定 2** の「感度(gap 3 × 粒度 2)」も「感度 4 本(gap 30 / 180 秒 × W 8h / 24h)」に直した。
- **この訂正で数(6 本・47,520)は変わらない。**

> **21. [直す] PREREG:806 — 「**§3.1 により**第 2 項のネット期待値バーは課さない」の §3.1 は `research-protocol` §3.1 を指すと読めるが、本書の §3.1 は「判定区間の日を既に開けたこと」であり別内容である。参照先の文書名が書かれていない。**どちらの §3.1 か**(P-2)**

**処置: 直した。`.claude/skills/research-protocol` の §3.1 である。**
§9 の該当文を「**`.claude/skills/research-protocol` の §3.1(「上流の単位ではコストを引かない /
判定バー(ネット期待値バー)は下流の単位にだけ課す」)により**」と書き替え、
**実測の見出し**(同ファイル §3.1「**その単位の位置に合うコストだけを扱う**(2026-09-09、
オーナー指摘、L-048)」)を訂正の注に引いた。§9 の表の項 2 の参照も同じに直した。

> **22. [直す] PREREG:889, 1041-1042 — サニティ #13「判定区間で `fwd_node` が定義できる行数を**開封前に**数える」は「**未実施**」で、§14.4 は「主表を走らせる前に数える(件数を数えるだけなので判定区間を汚さない)」とだけ書く。**どのコマンドで、どの出力先に、どの列を書かずに数えるか**(§3 の関門表は `--mode anchor` だけが出力の側を絞ると書いている)が固定されていないが、この 1 回が判定区間の開封に当たらないことは何で担保されるか(P-3)**

**処置: 直した。§14.4.1 を新設し、コマンド・出力先・出す数・出さない列を固定した。**
リードの決定の逐語:
「**22 = サニティ #13 の数え方を固定(`--mode full` の前に、`judgment_days` の日の束と `fwd_node` の
定義可否だけを数える小さなコマンドを書く。観測量の列は出さない。今は走らせない = 開封は 1 回、
リードが走行の直前に実行する)。**」

- **出す数**: 日数 / 束の数(mixed)/ 非 mixed 束 / `fwd_node` が定義できる数 / SELL・BUY 別 = **整数 6 つ**。
- **出さないもの**: 観測量の列(`bp_*` / `reach_*` / `mfe_*` / `mae_*` / `doi_*` / 距離)の
  **値・平均・中央値・分位・分布を 1 つも出さない。表も CSV も書かない**(標準出力 3 行だけ)。
- **出力先**: **標準出力のみ**(`--out-dir` を取らないのでファイルを 1 つも書かない)。
- **担保の射程も書いた**: 「この 6 つの数からは判定の向きが読めない」という主張であって、
  「どんな件数も安全」という主張ではない、と明記した。
- **今は走らせていない**(`--judgment` を付けた実行はしていない)。
- **再現の確認は標本 6 日で行った**(開封ではない): 同じコマンドが **W = 8h で 40 / 224**、
  **W = 24h で 62 / 224** を返し、RUN12 §4.5b の 40/224 と `table.csv` の `p_tgt_fwd_node` の
  非空件数に一致した(**実測**)。

> **24. [直す] PREREG:122 vs 898 — §2 は metrics を「zip **379 日**ぶん(**実測**)」、§12 は「**建玉は 472 日のうち 375 日ぶんしかなく**」と書く。`docs/DATA.md:106` は 379/478 日、`OI_DISTANCE_2026-09-17.md:293` は「重なりは 375 日」と別の量として記録している。**本書の中で 379 と 375 が別の量であることが読めるか**(P-5)**

**処置: 直した。§12 の先頭に「2 つある。混ぜない」の段を置いた。**
**(1) 在庫の zip = 379 日**(§2)/ **(2) 清算の 472 日と重なる日 = 375 日**(§12)。
**差の 4 日を実測で特定して書いた**(**実測** 2026-09-18、委任先。zip 名の日付で差集合):

```
清算 zip 日数: 472 / metrics zip 日数: 379 / 重なり: 375 / metrics だけ: 4
metrics だけ: 2023-09-09 / 2023-09-23 / 2024-06-11 / 2024-06-12
```

**本書で「379」は在庫の日数、「375」は重なりの日数である**と明記した。

---

## 聞く(7 件)

> **11. [聞く] PREREG:1064 — 決定 2「台地条件を**当てない**。段 A の 1 周目はパラメータの格子探索(グリッド勝者)が無い」。`research-protocol` §4-4 は確かに「グリッド勝者は台地条件」と書くが、同スキル §5 の掘り出し(fishing)の項は「**候補数を明記し、台地条件とペア比較 CI で潰す**」と書き、台地条件を勝者選択以外の掘り出し対策にも位置づけている。**判定 576 セルから差の出たセルを読むとき、隣の分位で劣化していないかを見る機構は何で代替されるか**(P-7、`research-protocol` §4-4 / §5)**

**処置: リードの決定に従って代替を置いた。**逐語:
「**(聞く 11)台地条件の代替: 差ありのセルについて、隣の分位(同じ軸の隣の群)の値を併記する
(観測のみ、バー無し)。§10.1 に列を足す。**」

- **§10.1 に「隣の分位」列**を足した(3 分位の軸のときだけ。**同じ軸の隣の群の 差 (ii) と `|t|`**)。
- **観測のみでバーにはしない**と明記した(§9 の項 4 も同じ文言に直した)。
- **§9 の項 4(前版は「未定」)** を「**バーとしては当てない。代わりに隣の分位の値を併記する**」に埋めた。

> **12. [聞く] PREREG:779-784, 947 — 「**「欲しい効果」の大きさは置かない**(根拠のある値が無い)…`research-protocol` §4.1 の「**欲しい効果 < MDE なら、その単位は実行しない**」の関門は**今回は当たらない**(当てる値が無い)」。これは規約の関門を、当てる値を置かないことで通過させる形である。出所は返答 060 §3 #6 の案で「反対なし(**黙認**)」であり、オーナーの逐語ではない。**関門を外す判断として、この黙認で足りるか**(P-9、A-6)**

**処置: 黙認のまま進める(リードの決定)。**逐語:
「**(聞く 12)欲しい効果の関門: 黙認のまま(返答 060 #6 と 061 §1 で 2 回オーナーに見せた)。
VERDICTS にそう書く。**」

- **文書は変えていない**(§8.6 と §13 の #6 の中身はそのまま)。
- **§13 の #6 の欄の見出し**に「**2026-09-18 の監査の指摘 12 = 聞く。リードの決定: 黙認のまま進める。
  オーナーには返答 060 #6 と 061 §1 の 2 回見せている**」と、経緯だけを足した。
- **監査役の指摘のとおり、これはオーナーの逐語ではない。**その事実は §13 に元から書いてあり、消していない。

> **14. [聞く] PREREG:321-329, 900-907 — 「前進到達は 1 周目は**測定不能**」の根拠は標本 6 日・gap 60 秒・**W = 8h** の 224 行だけである(RUN12 §4.5b)。対処は「**2 周目で W かビン幅を変えて作れる行を増やしてから測る**」。ところが RUN12 §4.2 に「W = 24h(gap 60 秒)は liq 224 / 一様 225 / 合わせた対照 216、所要 8.81 秒」と、**W = 24h の標本走行は既に手元にある**。**W を変えると `fwd_node` が作れる行が増えるかは、その手持ちの標本で今数えられるのではないか。数えずに「2 周目送り」にしていないか**(P-1、P-7)**

**処置: 数えた(リードの決定)。指摘のとおり、数えられた。**逐語:
「**(聞く 14)W = 24h の標本走行(`backtest_data/o3c_reaction_20260918_sample/gap60_w24/table.csv`)で
`fwd_node` が定義できる行数を今数える(SELL / BUY 別も)。増えるなら §12 に書く。標本なので開封ではない。**」

**数え上げ(実測、2026-09-18、委任先。標本 6 日 = 開封ではない)**:

| 標本 6 日・gap 60 秒 | 非 mixed 束 | `fwd_node` が定義できる | SELL | BUY |
|---|---|---|---|---|
| **W = 8h** | 224 | **40(17.9%)** | 117 中 **18(15.4%)** | 107 中 **22(20.6%)** |
| **W = 24h** | 224 | **62(27.7%)** | 117 中 **22(18.8%)** | 107 中 **40(37.4%)** |

コマンドと出力は本書の末尾「数え上げの再現」。
**§12 に上の表を書き、読みも書いた**: **W を 3 倍にすると定義できる行は 17.9% → 27.7% に増えるが、
向きへの依存は消えない**(BUY 37.4% 対 SELL 18.8%。逆向きは SELL 91.5% / BUY 81.3%)。
**よって「W = 24h なら前進到達を測れる」とは書かない。**
**2 周目で何をどこまで変えれば偏りが消えるかは測っていない(未確認)。**

> **15. [聞く] PREREG:963-984 — §14 は主表 1 本 + 感度 5 本の**計 6 本すべてを `--mode full`(= 判定区間 456 日)**で走らせる。§3:144 は「**判定区間** = 残り 456 日。**一度だけ開ける**」と書く。**6 本で 456 日を 6 通りの設定で開いた後、§4.3:329・§12:905 が約束する「2 周目で前進到達を測る」段に、未開封のデータは残るか**(P-9、A-6)**

**処置: 残らない。それを §12 に書いた(リードの決定)。**逐語:
「**(聞く 15)6 本すべてが判定区間を開く。2 周目のための未開封データは、この 472 日の中には残らない。
2 周目は新しい期間(2024-10-15 以降。`paper_logs/liquidations/` に 2026-09 の記録がある)で測る、と §12 に書く。**」

- **§12** に「**1 周目で 472 日を使い切る。2 周目は新しい期間で測る**」の段を新設した。
- **手元の材料(実測 2026-09-18)**: `paper_logs/liquidations/` に **45 ファイル**
  (`binance_cm_YYYYMMDD.jsonl.gz`、2026-09-09 以降)。
- **2024-10-15〜2026-09-08 の在庫はこの環境で数えていない**(**未確認**。取り直せるかも試していない)と併記した。
- **2 周目の期間とデータの調達は別の事前登録で決める**と書いた。

> **20. [聞く] PREREG:1065 — 決定 3 の根拠「0.004 = Binance COIN-M の維持証拠金率の第 1 段(**0〜5 BTC**、`docs/DATA/surveys/BINANCE_CM_MMR_2026-09-17.md`。**清算の 98.6% が第 1 段**)」。挙げた出所の当該文書は段階 1 を「`qtyCap` **50**(0〜50 BTC)」と書き(:73, :81)、0〜5 BTC と 98.6% は別文書 `OI_DISTANCE_2026-09-17.md:518` / `ACTION_LOG` 056 の実測である。さらに原典は「表の日付は **2026-05** で、2023〜2024 年当時の表かは不明」と限界を明記している。**出所の付け替えと、この「当時の表かは不明」の射程を事前登録に引き継がなくてよいか**(P-2、P-16)**

**処置: 付け替えた(リードの決定)。**逐語:
「**(聞く 20)`--mmr 0.004` の出所を正しく付け直す(0〜5 BTC と 98.6% は `OI_DISTANCE_2026-09-17.md` §7.1 /
ACTION_LOG 056。調査文書は段階 1 を 0〜50 BTC と書く = 食い違いがあるならそれも書く)。
「表の日付は 2026-05 で当時の表かは不明」の射程を引き継ぐ。**」

- **§14.5 に出所の 3 行の表**を新設し、3 つの主張を別々の出所に付けた
  (0.004 = `BINANCE_CM_MMR_2026-09-17.md` / 0〜5 BTC = `OI_DISTANCE_2026-09-17.md` §7.1 /
  98.6% = 同 + `ACTION_LOG` 056)。
- **食い違いも書いた(実測)**: **API リファレンスの例は `qtyCap 50`(0〜50 BTC)**
  (`BINANCE_CM_MMR_2026-09-17.md:30` / `:73` / `:81`)、**公開 JSON の段階表(2026-05-11)は上限 5 BTC**
  (同 `:156`)。**前版は 0〜5 BTC と書きながら出所を API リファレンス側に付けていた。**
- **射程を引き継いだ**: **表の日付は 2026-05 で 2023〜2024 年当時の表かは不明**(同 `:142` / `:156`)、
  **API リファレンス側も「ドキュメント上の例」で実勢との一致は未照合**(同 `:81`)。
- **向きも書いた**: `implied_leverage` は**過大側にしか外れない**(`OI_DISTANCE_2026-09-17.md:518`)。
  軸 E は順序だけを使い、段階別に出し直した差は分位で 0〜1(`ACTION_LOG` 056)。

> **23. [聞く] PREREG:910-916, 122 — 「(d′) の 3 列のうち 2 列は在庫の 379 日・109,098 行すべてで空で、**公開アーカイブから取り直しても空である**」。取り直しの範囲は §2 に「公開アーカイブからの取り直し **1 日**(md5 一致)」と書かれている。**379 日のうち 1 日の取り直しから「取り直しても空」と書く形になっていないか**(範囲は併記されているが方法は 1 経路 1 日。`docs/DATA.md:128` は経路 3 つを当てたと書くので、そちらの 3 経路を本書に引くのが正しくないか)(P-1)**

**処置: 3 経路を引いた(リードの決定)。**逐語:
「**(聞く 23)L/S 比 2 列の「取り直しても空」の範囲: 取り直しは 1 日、在庫は 379 日。
`docs/DATA.md:128` の経路 3 つを引く。**」

- **§12 に範囲と方法の表**を置いた: 在庫の走査 = **379 日・109,098 行の全部** / 取り直し = **1 日**(md5 一致)/
  **経路 ① USD-M アーカイブ BTCUSDT は 8 列とも埋まる(別商品)/ ② COIN-M アーカイブ再取得は空 /
  ③ COIN-M REST `dapi` は HTTP 451(地域制限)/ オーナー PC は未確認。**
- **書ける範囲に言い直した**: 「**在庫の全部で空、当てた 3 経路では ②③ が空か到達不能で ① は別商品、
  オーナー PC は未確認**」まで。**「どこにも無い」とは書かない**(`CLAUDE.md` §5.2)。
- **§2 の該当セル**からも「取り直し 1 日」だけを根拠にする書き方を外し、§12 の表を参照させた。

> **25. [聞く] PREREG:822-831 — §10「結果の読みで必ず出す表」の 4 項目は、表・n・条件の充足・誤りの向きの数え上げで、**「なぜそうなるのか」を出す工程が無い**(§10.1 は 3 分岐で終わる)。`CLAUDE.md` §5.2 は「**「なぜ」を出さずに棄却だけ返す報告は不合格**」、`research-protocol` §0.2 も同じ。**結果の読みの形を凍結する本書に、原因を候補に分けて測る欄を置かなくてよいか**(P-6、`research-protocol` §0.2)**

**処置: 欄を置いた(リードの決定)。§10.3 を新設した。**逐語:
「**(聞く 25)§10 に「なぜ」の欄を足す: 差あり / 差なし / 不明 のそれぞれで、候補の原因
(機構にエッジが無い / 実装が意図を反映していない / バグ / 検出力不足)と、2 周目に測るものを書く形
(欄の形だけ固定。中身は結果の後)。**」

- **§10.3**(新設)に 6 列の表を置いた: 読み / 候補 1 機構にエッジが無い / 候補 2 実装が意図を
  反映していない(**意図マップの △ 8 件・✕ 4 件を番号で挙げる**)/ 候補 3 バグ(**サニティの番号**)/
  候補 4 検出力不足(**n・MDE・差**)/ **2 周目に測るもの**。
- **§10 の必ず出す表**に 5 番目として「なぜ」の欄を足した。
- **欄の形だけ固定し、中身は結果の後に書く**と明記した(走行後に欄を減らさない = A-6)。

---

## 決定に無い判断(**該当語なし**。仮置きであることを明記した)

**リードの決定にもオーナーの逐語にも該当語が無く、作業の中で必要になった判断が 3 件ある。**
**3 件とも PREREG の本文に「仮置き」と書いてある。リードが確定させる。**

| # | 判断 | 置いた値 | 置き場所 |
|---|---|---|---|
| 1 | **W に依らない 12 群(B1・B2・C・D・全体)をどちらの走行から取るか** | **gap 60 秒 × W 8h の走行から取る** | §4 の内訳表の注 |
| 2 | **ブートストラップ CI の水準** | **α = 0.05/576 に合わせる(99.99132%)** | §9.1 |
| 3 | **サニティ #13 を何回数えるか** | **W = 8h と W = 24h の 2 回** | §14.4.1 |

**1 の理由**: 同じ群を 2 本から二重に数えると 48 群・576 検定が壊れる。観測量 `reach_back_*` の
目標価格は W のプロファイルから来るので、W が違えば値も違う(**実測**: 標本 6 日で `p_tgt_node_up` が
W 8h で 133 行 / W 24h で 147 行)。
**2 の理由**: 決定 7 は「クラスタ = 日、反復 2,000、種 1」までで CI の水準に触れていない。
バー(\|t\| ≥ 3.925)と同じ α に合わせないと、CI とバーが別の水準になる。
**3 の理由**: 判定に使う走行が 2 本になったので、#13 も 2 本ぶん要る。

---

## 変えた節の一覧(PREREG)

| 節 | 何を変えたか | 指摘 |
|---|---|---|
| 冒頭の凍結の宣言 | バーを \|t\| ≥ 3.925 に / 判定に使う走行 2 本を凍結対象に追加 / 変更 2 件の注 | 1, 2 |
| §0.1 対応表 | 2 回目の処置の 12 行を追加(仮置き 3 件を含む)/ 既存 2 行を現況に直した | 全体 |
| §2 | metrics の L/S 比 2 列の根拠を §12 の 3 経路の表へ | 23 |
| §4(軸表) | 3 分位の切り方(全 15 軸)/ 48 群の内訳表(どの走行から取るか) | 5, 1 |
| §4.2 | 「粒度 2」→「W 2 通り」/ 判定 2 本・感度 4 本 | 19, 1 |
| §4.3 | 判定に使う走行の行 / バー \|t\| ≥ 3.925 / ブートストラップの行 / 感度 4 本 | 1, 2, 7 |
| §5 | 記号表に「粒度」の行 | 19 |
| §7 | F1 の反証の形を §7.0 へ / D の切り方を「判定区間の実群で」 | 3, 5 |
| **§7.0(新設)** | F1 の支持 / 反証の 4 分類(12 セル) | 3 |
| §7.2 | 6 本のうち 2 本が判定である注 | 1 |
| §8.2 | 対照 213 の内訳(NA 2)/ 群の n1 の和が 1 多い理由 / 倍率の経路を 1 本に / 再現コマンドに 2 行追加 | 16, 17, 18, 2 |
| §8.5 | 末尾の旧倍率 ×1.82698 / ×2.04283 を訂正 | 8 |
| §8.6 | MDE の入力表(p・s 固定 / n だけ走行後) | 4 |
| §9 | 見出しと項 3 のバー / 項 1・項 4 を埋めた / §3.1 の参照先を明記 | 2, 10, 11, 21 |
| **§9.1(新設)** | クラスタ・反復・種・統計量・CI | 7 |
| §10 | 3 語を §7.0 の規約に置換 / 「なぜ」の項を追加 | 3, 25 |
| §10.1 | 走行の列 / MDE の手順 / 検出力の列 / 隣の分位の列 / 4 分岐 | 1, 4, 10, 11, 2 |
| **§10.3(新設)** | 「なぜ」の欄(4 候補 + 2 周目に測るもの) | 25 |
| §11 | #9 を通過に(コマンドと出力)/ #13 の数え方を §14.4.1 へ | 6, 22 |
| §12 | 379 と 375 の区別(差の 4 日)/ W = 24h の数え上げ / 2 周目は新しい期間 / L/S の 3 経路 | 24, 14, 15, 23 |
| §13 | #2 の文言 / #6 の経緯 / #7 を変更に / 「報告する変更」の表を新設 | 19, 12, 2, 1 |
| §14 / §14.1 / §14.2 | 判定 2 本 + 感度 4 本 / `--seed 1` を 6 本すべてに / 出力先の表 | 1, 13 |
| **§14.4.1(新設)** | サニティ #13 の数え方(コマンド・出力先・出す数・出さない列) | 22 |
| §14.5 | 止める点 3 を解決済みに / 決定 1 の参照を §10.1 に / 決定 2 を代替つきに / `--mmr` の出所表 | 9, 10, 11, 20 |

---

## 数え上げの再現(本書で使った実測)

**(1) 先読み防止(指摘 6)** — コマンドと出力は上の 6 番。

**(2) `fwd_node` の定義可否(指摘 14)** — 標本 6 日の 2 つの `table.csv` を数えた:

```
python3 - <<'PY'
import csv
from collections import Counter
for p in ['backtest_data/o3c_reaction_20260918_sample/gap60_w8/table.csv',
          'backtest_data/o3c_reaction_20260918_sample/gap60_w24/table.csv']:
    liq = [r for r in csv.DictReader(open(p, newline='')) if r['kind'] == 'liq']
    d = sum(1 for r in liq if r['p_tgt_fwd_node'])
    print(p, '/ liq', len(liq), '/ fwd_node 定義できる', d)
    for s in ('SELL', 'BUY'):
        sub = [r for r in liq if r['side'] == s]
        print(f'   {s}: {len(sub)} 中 {sum(1 for r in sub if r["p_tgt_fwd_node"])}')
PY
```

出力(**実測**、2026-09-18):

```
backtest_data/o3c_reaction_20260918_sample/gap60_w8/table.csv / liq 224 / fwd_node 定義できる 40
   SELL: 117 中 18
   BUY: 107 中 22
backtest_data/o3c_reaction_20260918_sample/gap60_w24/table.csv / liq 224 / fwd_node 定義できる 62
   SELL: 117 中 22
   BUY: 107 中 40
```

**§14.4.1 に置いたサニティ #13 のコマンドを標本 6 日に当てると同じ数になる**(**実測**、2026-09-18):

```
（--window-hours 8）  日数 6 / 束 225(mixed 1) / 非 mixed 束 224 / fwd_node 定義できる 40
                       SELL 117 中 18 / BUY 107 中 22
（--window-hours 24） 日数 6 / 束 225(mixed 1) / 非 mixed 束 224 / fwd_node 定義できる 62
                       SELL 117 中 22 / BUY 107 中 40
```

**(3) metrics の日数(指摘 24)** — zip 名の日付で集合演算:

```
清算 zip 日数: 472 / metrics zip 日数: 379 / 重なり: 375 / metrics だけ: 4
metrics だけ: ['2023-09-09', '2023-09-23', '2024-06-11', '2024-06-12']
```

**(4) 対照の D 群の内訳(指摘 16)**:

```
liq: {'D1': 75, 'D2': 75, 'D3': 74} 計 224
ctl: {'D1': 54, 'D2': 69, 'D3': 88, 'NA': 2} 計 213
ctl doi_pre_1h 欠測: 2
群ごとに丸めた n1 の和: 7098 + 7098 + 7003 = 21199(N1 = 21198)
```

**(5) バーの根拠(指摘 2)と倍率(指摘 8・18)** — §8.2 の再現コマンドの出力:

```
0.05         z=1.9600 z合計=2.801585 倍率=1.000000000
0.05/2640    z=4.2770 z合計=5.118662 倍率=1.827059263
0.05/47520   z=4.8816 z合計=5.723239 倍率=2.042857500
0.05/576     z=3.9248 z合計=4.766411 倍率=1.701326397
検定数 48*2*6 = 576
t=2.0 の両側 p = 0.04550026389635842
α=0.05/576 の両側 z = 3.924789651405654
```

**(6) §8.5 の照合器の再実行(処置の後)** — §8.3 の表が本作業の編集で壊れていないことの確認:

```
照合したセル: 152 / 一致 152 / 食い違い 0
```

---

**本書に判定語(予測できる / 使える / 有効)は 1 つも無い。**
**判定区間(456 日)の観測量は 1 つも新しく出していない。**
**`--mode full` は 1 本も走らせていない。**
