# 検収: 道具サーベイ 区分 8 の 16 回目(2026-09-25、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 16 回目の実行(2026-09-25)`(19997 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run16.log`(131,045 行・27,633,084 バイト)。届いたままのコミット d2511a7。起動文の印 `20260923_tools_survey_cat8_run16_prompt.md@22c786b0ad16`。報告のファイルは 16,344,669 バイト(どちらも 90 MB 以内)。

## 0. 費用と時間

調査班 642,055 トークン・3,434,596 ms(57.2 分)・道具 252 回。

## 1. 受け入れ検査(調査班の報告)

`check-elements --round 16` 1 件: 生ログ 130089 行目の手 `npm_registry` が、取った本文を `/tmp` の直下(scratchpad の外)に書いた。起動文(14 回目の起動文 §2「取った本文は scratchpad の下に置き、`/tmp` の直下に置かない」)に反する実際の違反で、誤検出ではない。同じ取得は 131035 行目の `npm_registry_v2` で scratchpad の下に打ち直してある。生ログは書き換えないので、この 1 件は 16 回目の既知の 1 件として残す(8 回目の既知の 5 件と同じ扱い)。`check` 0 件。`check_scan_report.py` 25 件(前の回からのものと、この回の §4.0 の根拠の複写・コードの断片の引用による括弧の食い違い。調査班が報告に理由を書いた)。

## 2. リードが見た点と直し

- **行の表の理由が型の当てはめ(4 回目の同じ型)**: `### 当たりの判定(行ごと)` は 2,998 行あるが、理由の文は 11 種類しかない。1,232 行に同じ「reconcile系の関数・変数・定数の定義行(DEF_KWの決まりにより機械的にunmatched)。…」、731 行に「compare系…」、341 行に「diff系…」、121 行に「変数・関数・型などの定義名の一部に検索語を含む行…」、102 行に「JS/TSの `for (const X of Y)` 構文の一部…」。`DEF_KW` の行は、決まりに取らせずに 1 行ずつ読ませるために `unmatched` に出している行で、起動文は「理由はその行を読んで書く」と書いた。
- **ファイルの表も型の当てはめ**: `### 当たりの判定` は 9,958 行で、理由の文は 3 種類。8,283 行は、誤字(「突き合わ」)で打った古い検索の手(17144 行目)の当たりのファイル全部に「打ち直した 73509 行目の手と同じ内容」と当てた文、1,464 行は、読めないファイルが残って `INCOMPLETE` で終わった古い手(1183 行目)に同じ型で当てた文、211 行は補助の検索の手の文。
- **E3a・E3b の `なし` が、読まれていない文に支えられていた**: E3 の検索の手(6030 行目)には cat8_classify.py を 5 回打ち、そのうち 15074 行目の回は決まりが広すぎて止まり(`broad ... r_purge_context`)、終わりの行が無い。監査 83 回目の決まりにより、この検索の手は (乙) として数えず、ファイルの表を求める。その要求は、古い手 1183 行目の型の文の行が満たしていた(ファイルの表は道で引くので、同じファイルの行があれば通る)。
- **受け取らないもの**: 8-026 の E1b・E3a・E3b の `なし`。台帳で `未判別` に戻した。
- **E4 の問い(問い 1)への答え**: 設計票 §3 の E4 の述語は「**記録した市場データを時刻順に再生して、戦略・執行・計算を再実行する**」。`jsonl-replay.ts` が再生するのは会話の記録で、市場データではないので、この機能は E4 の `印` の根拠にならない(設計票の逐語で決まるのでオーナーに聞かない)。E4 に `なし` と書くには全件の検索が要り、この回は打っていないので `未判別` のまま。
- **受け取るもの(中身の監査 89 回目で見る)**: E1a 印・段 3、E2 印・段 3、E5 印・段 4、E6 印・段 3。とくに E5 の段 4(対象 = 固定・比較する実験のすべてを外から持ち込めるか)と、E6 の根拠が別のリポジトリ(届かない)を指す文書の説明だけであること(問い 3)を、監査で見る。
- **手の並び(リードが生ログの見出しの行で確かめた)**: 文書を読んで `印` を探す手(46・71・151・280・468・612 行目。qa の README・qa-e2e・mantis・attestation・transcripts・dedupe-runtime)は、最初の全件の検索(1183 行目)より前にある。E6 の文書の手(17033 行目)は E3 の全件の検索(6030 行目)のあと、E1b の全件の検索(17144 行目)の前。E1a の根拠の一部(runtime-parity、129939・130031 行目)と E4 の候補の手(129870・129931 行目)は、全件の検索のあとに打たれている。E1a の根拠の主な手(mantis、151 行目)は全件の検索の前にある。
- **状態**: 8-026 は一次資料に届いており、判定が終わっていない。設計票 §2 の語で `浅い` にした(監査 87 回目の指摘 5)。

## 3. 台帳

`import --round 16`: 変わった所 7。そのあと `set` で E1b・E3a・E3b を `未判別`、状態を `浅い` にした。`check` 0 件。`recount`: 深掘り 24 / 残り 14(8-026 と 8-029〜8-041)。

## 4. 判定

**一部受け取り**。8-026 の残り(E1b・E3a・E3b・E4)は 18 回目に回す。18 回目の前に、`DEF_KW` の行が多い候補で 1 行ずつの判定を現実にする手を考える(たとえば、定義された名前ごとにまとめて出し、名前ごとに定義を読んで判定させる)。

## 5. 中身の監査(89 回目、owner-auditor)の逐語

> 対象を読了。生ログの逐語照合・行の並びの実測・設計票の述語照合をもとに、指摘を返す。
>
> ## E1a(印・段3)関連
>
> 1. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:20288` — E1a=印の根拠 2 本のうち Mantis/RuntimeParity(`docs/DATA/probes/20260923_tools_8_run16.log:151, 129939`)は、OpenClaw 自身の PR の before/after を比較する QA/CI 機構(「Live transport scenarios compare a known-bad baseline with a candidate ref」「These are the canonical QA comparison cells」)であり、OpenClaw 開発チームが自分自身のランタイム変更を試験するための道具に見える。委任文 §2 の 1(`docs/DATA/delegations/20260923_tools_survey_cat8_run16_prompt.md:33`)は「述語は候補(道具)の機能に当て…候補自身の試験は数えない」と明記している。ここでの「突き合わせ」が、ユーザー(当方)が持ち込む 2 実装を比較できる汎用機能なのか、それとも候補自身の CI が候補自身を試験しているだけ(=除外対象)なのかを、この除外規定に照らして詰めた記述が報告・検収のどちらにも無い。qa/README.md の「private」の語義自体が知見#6(`SCAN_2026-09-23_tools_cat8.md:33496`)で未確定のまま残っている点も同根。
>
> 2. [聞く] 同じ行(`SCAN_2026-09-23_tools_cat8.md:20288`)— E1a の 2 つ目の根拠(`docs/plugins/sdk-channel-outbound.md`、生ログ`130031`見出し・実際の引用は`130071-130073`)は、曖昧な送信をプロバイダ側の権威ある状態で確定させる仕組み(=単一系の reconcile。当プロジェクトの `OrderStateUnknown` の reconcile に近い概念)で、E1a 述語「2 つ以上の実装(または実装と参照値)の出力を突き合わせて、差を出す機能」に当たるかは疑わしい。段 3 の理由文(同じ行末尾)は Mantis/RuntimeParity の記述だけを根拠に挙げており、この 2 つ目の根拠は段の判定に使われていない — 印の判定だけに効いている構造。この根拠を抜いても E1a=印 が成立するか、無ければ危ういかの整理が無い。
>
> 3. [直す] 引用行の付け方: 知見表(`SCAN_2026-09-23_tools_cat8.md:20017`)・要素と段表(同`20288`)の E1a 根拠が指す `...run16.log:130031` は、実際には cat コマンドの見出し行(`$ sed -n 380,430p`)で、引用文そのものは `130071-130073` にある。生ログの慣例(`--- ` 見出し→出力)としては許容範囲だが、離れた行(見出しから 40 行下)を指すのに見出し行番号だけで済ませているため、機械検査では検出できない(K4等は数値の有無しか見ていない)。今回は実際に確認して一致したが、次回以降、根拠を分割コマンドで複数回引く形式(この回のように 1 コマンドで長い出力を返す場合)では、実際の引用箇所の行番号も併記するよう促す余地がある。
>
> ## E2(印・段3)
>
> 4. [止める/直す] `SCAN_2026-09-23_tools_cat8.md:20290` — E2=印 の根拠(`src/plugin-sdk/dedupe-runtime.ts`、生ログ`612`)は Discord/Telegram 等チャットメッセージの重複配信防止キャッシュであり、当てているのは「メッセージ」の重複検出。設計票 §3 の E2 述語(`docs/DATA/surveys/CAT8_DESIGN.md:51`)は対象を明示的に「**時系列・約定・板・足・参照データ**について…欠け・重複…を検出するか報告する機能」と列挙しており、金融データの種類を限定している。チャットメッセージの重複排除はこの列挙のどれにも当たらない。E1a・E1b・E3a・E3b・E4・E5 は対象を「出力」「情報」「実験」のように抽象的に書いているのに対し、E2 だけは対象データの種類を具体的に列挙しているため、この逸脱は見過ごしにくい。§4.0 の表(`SCAN_2026-09-23_tools_cat8.md:20429` 付近)自体が「対応取引所: 該当なし…トレーディング道具ではない」と明記しており(候補がそもそも金融データを一切扱わない道具である事実と、E2=印 の根拠が金融データ以外のメッセージ重複検出である事実が、同じ節の中で整合していない)。述語の対象語を無視して「重複検出」という機能名だけで印を付けていないか、確認が要る。
>
> ## E5(印・段4)
>
> 5. [聞く] `SCAN_2026-09-23_tools_cat8.md:20294` — 段 4 の条件(設計票 §4.1、`CAT8_DESIGN.md:78`)は「その要素の**対象のすべて**(E5=固定・比較する実験(**コード・データ・設定**))を、道具の外から持ち込んでも掛けられる」こと。今回の根拠は `policy.jsonc`(設定)とその evidence(実行時状態)のハッシュ固定・比較のみで、「コード」「データ」の再現性・比較にこの機構が掛かるという記述は無い(知見#17、`SCAN_2026-09-23_tools_cat8.md:20032`も「対象=policy.jsonc・実行時の状態evidence」で「設定」のみ)。設計票の同じ段の但し書き「対象の一部だけが外から持ち込めるものは段 3」に照らすと、示されているのが 3 種のうち 1 種(設定)だけなら段 3 ではないか。検収(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run16.md:20`)もこの点を「監査で見る」と名指ししており、報告・検収のどちらも「対象のすべて」を満たす根拠を明示していない。
>
> ## E6(印・段3)
>
> 6. [聞く] `SCAN_2026-09-23_tools_cat8.md:20295` — E6=印 の唯一の根拠(`docs/security/formal-verification.md`、生ログ`17033`)は、TLA+/TLC 検証モデルの実体が別リポジトリ `vignesh07/openclaw-formal-models` にあり「currently unreachable」と明記されている文書。当該候補(openclaw/openclaw)自身のリポジトリにはモデルの実装コードは含まれておらず、「こういう検証を(別の場所で)行っている」という説明文だけが一次資料。報告自身がこれを問い 3(`SCAN_2026-09-23_tools_cat8.md:33493`)として明示し、検収も監査へ送っている(`VERDICTS/2026-09-25_tools_scan_cat8_run16.md:20`)。設計票の根拠の種類(一次資料/実測/仮定)のどれにも完全には当てはまらない状態 — 「機能の存在を主張する記述はあるが、機能の実体には一切到達できていない」。この状態で E6=印(段3)を維持してよいか、それとも「実体未到達」を段・印いずれかに反映する扱いを設計票に足すべきか、リードが決める前に一度オーナー相談の要否を検討する価値がある(A-15 =監査役定義の変更ではなく設計票 §3 の運用の話なのでリード権限内だが、他候補にも波及する解釈なので次に同型が出たときの扱いを決めておいた方が良い)。
>
> ## E1b・E3a・E3b の「なし」却下(検収の判断)
>
> 7. [聞く] `VERDICTS/2026-09-25_tools_scan_cat8_run16.md:15-18` — 却下理由は「2,998 行の理由文が 11 種類に集約される(型の当てはめ)」という**文面の多様性**を基準にしている。私が抜き取った行(`SCAN_2026-09-23_tools_cat8.md:30446` 以下 20 行程度)では、各行は異なるファイル・異なる字句を示しており、理由の類型化自体は一見妥当に見えた(DEF_KW 強制行の多くが実際に `purge`/`leak`/`diff`/`compare` 等を含む識別子定義で、述語外の使用に見える)。却下(未判別へ差し戻し)は安全側 = 追加検証を要求する方向であり A-18(構造的に安全側=棄却側に振れる)の型そのものではないが、判定基準が「個別の誤判定の有無」ではなく「文面の多様性」である点は、次回も同じ疑問(本当に 1 行ずつ読んだ結果として同じ結論に達したのか、パターンで一括処理したのか)を残す。抜き取りで実際の誤判定(述語に当たるのに「なし」にされた行)が 1 件でも見つかったかどうかを検収記録に残すべきではないか。
>
> 8. [直す] `SCAN_2026-09-23_tools_cat8.md:20067` — 候補の一覧の 8-026 行末尾が「状態: 判別に一次資料が要る — 状態: 判別に一次資料が要る」と同文言を 2 回連続で書いている(書式の乱れ)。実害は無い(検収が `浅い` に訂正済み、`VERDICTS/2026-09-25_tools_scan_cat8_run16.md:22,26`)が、次回以降の同型の重複を防ぐ手当てを検討してよい。
>
> ## 18 回目に向けた案(検収 §4)への意見
>
> 9. [聞く] `VERDICTS/2026-09-25_tools_scan_cat8_run16.md:30` の「定義された名前ごとにまとめて出し、名前ごとに定義を読んで判定させる」案は、同じ識別子名(`purge`・`diff`・`compare` 等)が別々のファイル・モジュールで異なる文脈・意味を持ちうることを考慮していない。名前だけを束ねの単位にすると、今回リードが指摘した「型の当てはめ」問題を、単位を「行」から「名前」に変えて再演する恐れがある。束ねる前提として、少なくとも同一ファイル内・同一モジュール内での出現に限定する、あるいは束ねた後も一定割合を抜き取って個別確認する、といった歯止めを併記した方がよいのではないか。
>
> ## 手の並び・件数・既知の1件(生ログとの整合)
>
> 10. 確認できた範囲では、検収の事実の文は生ログと一致していた: 印を探す手の行番号(46・71・151・280・468・612)がいずれも該当箇所(qa/README・qa-e2e・mantis・attestation・transcripts・dedupe-runtime)を指し、最初の全件検索(見出し `--- ...E3a_E3b...` 、`run16.log:1185` 付近)より前にあること/ E3a_E3b の全件検索見出し(`run16.log:6030`、hits=2895)と E6 の文書読み(`run16.log:17033`)・E1b の全件検索(`run16.log:17144` 誤字版、`73509` 打ち直し版、いずれも hits=35,610)の順序/ 既知の 1 件(`npm_registry` が `/tmp/openclaw_npm.json` に書いた件、`run16.log:130088-130091`)。いずれも本文の記述と食い違いは見つからなかった。指摘なし。
>
> ## E4 判断(項目3)について
>
> 11. 検収の判断(`VERDICTS/2026-09-25_tools_scan_cat8_run16.md:19`)— 設計票 §3 の E4 述語「記録した**市場データ**を時刻順に再生して…」の逐語に照らし、`jsonl-replay.ts` が再生するのは会話記録であって市場データではないため E4 の根拠にならない、という判定は、述語の逐語を機械的に当てはめた結果として整合している。指摘なし(妥当と判断)。
>
> 以上。書き換え・ファイル作成は行っていません(scratchpad も未使用)。

## 6. 処置(リード、監査 89 回目)

- **リードの誤り(監査 89 回目の指摘 1 で分かった)**: 16 回目の起動文 §2 の 1 に「候補で検証される 1 つの戦略・例のコード・候補自身の試験は数えない(14 回目の起動文 §2 の冒頭)」と書いた。14 回目の起動文(`20260923_tools_survey_cat8_run14_prompt.md:32`)にあるのは「道具で検証される 1 つの戦略の作り・例のコードの作りは、道具の機能として数えない」だけで、「候補自身の試験」はリードが足した語である。設計票にも無い。既存の規則であるかのように出典を付けていた(§0.2 の B の型: リードが導いた規則をほかの規則として書く)。17 回目以降の起動文では、この語を使わない。
- 4. **直した(E2)**: 設計票 §3 の E2 の述語は「**時系列・約定・板・足・参照データについて**…を検出するか報告する機能」。チャットのメッセージの重複を防ぐキャッシュは、この列挙に当たらない。逐語で決まるので、`印` を取り消して `未判別` にした(`なし` には全件の検索が要る)。
- 5. **直した(E5)**: 根拠が示す対象は「設定」(policy.jsonc と実行時の状態)だけで、設計票 §4.1「対象の一部だけが外から持ち込めるものは段 3」に当たる。段 4 → **段 3**。`印` はそのまま(同じ状態から同じハッシュを出し、食い違いを報告する機能は、E5 の述語「同じ入力から同じ出力が出ることを確かめる」に当たる。利用者が自分の設定に掛ける CLI の機能)。
- 1・6. **直した(E1a・E6 を `未判別` に)**: E1a の主な根拠(Mantis・RuntimeParity)は、OpenClaw の開発チームが OpenClaw 自身の版やランタイムを比べる仕組み。E6 の根拠は、OpenClaw 自身の安全性の主張を別のリポジトリ(届かない)の形式モデルで確かめているという説明。どちらも「候補が自分自身を試験する仕組みを、候補の機能として数えるか」という境目に当たる。この境目は設計票にも起動文にも書かれておらず(上のリードの誤りの語を除けば)、リードが決めると規則を足すことになる。そこで `未判別` にし、**区分 8 の完了の報告で、オーナーに示す問いに回す**(問いの形は、そのときに 1 つにまとめて書く)。
- 2. **直した**: E1a の 2 つ目の根拠(`sdk-channel-outbound.md` の reconcileUnknownSend)は、曖昧な送信を 1 つの系の権威ある状態で確定させる仕組みで、「2 つ以上の実装の出力の突き合わせ」に当たらない。E1a の `印` の根拠から外す。
- 3. **答える**: 17 回目以降の起動文で、長い出力の中の離れた行を引くときは、見出しの行に加えて引用の行も書くよう求める。
- 7. **答える(事実を書く)**: リードは、E1b・E3 の行の表から、述語に当たるのに `なし` にされた行を**見つけていない**(抜き取りもしていない)。受け取らなかった理由は、見つかった誤りではなく、手順の違反である: 起動文は「理由はその行を読んで書く」と求め、`DEF_KW` の行はまさに 1 行ずつ読ませるために出している。そこに 11 種類の型の文が当ててあり、1 行ずつ読んだかどうかを報告から確かめられない。値は `なし` でも `印` でもなく `未判別` に戻しただけで、候補を退けてはいない。
- 8. **答える**: 報告の本文は届いたまま。状態は台帳で `浅い` にしてある。
- 9. **受け取る**: 18 回目の手は、「定義された名前 × ファイル」でまとめ、まとめたものごとに定義の行を読んで判定させる形にし、まとめたものから一定の割合をリードが抜き取って確かめる。道具と起動文は 18 回目の前に作り、監査に通す。

台帳(`set`): 8-026 の E1a・E2・E6 を `未判別`、段_E5 を 3。`check` 0 件。8-026 は E5 だけが `印`(段 3)、ほかは `未判別`、状態 `浅い`。**判定は一部受け取り**(E5 の `印` と段 3)。
