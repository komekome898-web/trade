# 監査(owner-auditor)— `OI_DISTANCE_SPLIT_2026-09-18.md`(側別の按分を価格まで分けた版)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/OI_DISTANCE_SPLIT_2026-09-18.md`(委任先の実装。前の文書 `OI_DISTANCE_2026-09-17.md` の続き)。承認: `docs/OWNER_LOG.md` L-196「**6.OK 7.OK 進めてください。**」。監査役の返答は逐語のまま。各件の直下にリードの応答。

## 監査役の返答(逐語)

> 1. [直す] `docs/PHASE2/O3C/PRICE_LEVEL/OI_DISTANCE_SPLIT_2026-09-18.md:334` — 「37,845 行のうち 23,042 行(60.9%)が完全一致」(事実)を `backtest_data/o3c_oi_distance_20260917/w8/table.csv` と `backtest_data/o3c_oi_distance_split_20260918/w8/table.csv` の `oi_side_total_delta` 列で実際に突き合わせたところ、完全一致は 21,033 行(55.6%)であり、この特定の数字は再現しなかった(1e-15 の相対誤差・0 の相対最大値・side 4 列以外の完全一致という他の主張は再現した)。この数字はどこから来たか、再計算し直したか(P-10, P-02)。

**リードの応答: 直した。**リードが再計算した(2026-09-18): `kind,time_ms,side` で 2 つの `table.csv` を突き合わせ、NaN を除いた 37,845 行のうち `oi_side_total_delta` が完全一致 23,042 行(60.9%)、相対差の最大 2.62e−15。本文の数は再現した。監査役の 21,033(55.6%)はリードでは再現していない(突き合わせの鍵が違う可能性。未確認)。§3.3 に「リードが再計算して再現(ACTION_LOG 057)。監査役の再計算 21,033 は再現せず、原因未確認」を書いた。

> 2. [直す] `OI_DISTANCE_SPLIT_2026-09-18.md:10`(「重心 = 平均建値」)— 前の文書 `OI_DISTANCE_2026-09-17.md:44`「`L = 1/(d/1e4 + MMR)`、d = 重心からの距離。**「建値 = 重心」と仮定する**」、および同 `:519`「d = プロファイルの重心(…)。本書で『平均建値』と略しているのは全部この重心で、**清算された建玉の実際の建値ではない = 仮定**」、`:522` の見出し「位置(重心 = 平均建値の**近似**)」では、この等式は明示的に「仮定」「近似」と限定されていた。本書冒頭ではこの限定が落ち、「用語は前の文書と同じ」として無条件の等式で再掲されている。本書全体で「重心」「側別 重心」「建玉 重心」の値を事実の欄で扱う記述に、この等式が仮定であることの再掲は一度も出てこない。この等式の限定を落としたのは意図的な単純化か、書き落としか(P-02, P-16)。

**リードの応答: 書き落とし。直した。**冒頭 :10 を「重心(前の文書で「平均建値」と略したもの。実際の建値ではなく近似 = 仮定。OI_DISTANCE §7)」に改め、§2 の表の見出しの直前に同じ 1 文を置いた。

> 3. [聞く] `OI_DISTANCE_SPLIT_2026-09-18.md:25-32` — §0.1 の 8 行が「(該当語なし)」で、CLAUDE.md §0.1 の規則「空行があればそこだけ聞く」「作業中に、原文に無い判断が必要になったら、そこで止めて追加で擦り合わせる。黙って決めない」に対し、この単位はいずれも止めずに委任先/リードが単独で決定し、事後に §4(:340-375)へ列挙する形になっている。この単位は L-179 の例外(承認済み常設スキルの固定手順)に当たるという判断は書かれていない。この 8 行の決定は、実行前にオーナーへ確認すべき対象だったか(research-protocol §0.2 / CLAUDE.md §0.1)。

**リードの応答:** 8 行は、オーナーが承認した行「按分を価格まで側別に分ける(買い taker だけの VWAP / 売り taker だけの VWAP)版も測る」の実装上の細目で、委任の枠内としてリードが置いた(着手前に聞かなかった)。L-179 の例外ではない。§0.1 の冒頭に「この 8 行は承認された行の実装上の細目。リードの判断で着手前に聞かなかった。変えたいところがあればオーナーが言う」と明記した。

> 4. [直す] `OI_DISTANCE_SPLIT_2026-09-18.md:340`「印は 仮定」対 `:348,356,358,361,366,368,373,375` — §4 の判断 9 件のうち、明示的に事実/推定/仮定の印が付くのは 1 件目だけで、2〜9 件目(価格範囲の選び方・按分不変・列を増やさない・一意化・差の向き・鍵の差し替え・出力先)には印が付いていない。特に 2 件目(価格範囲の選び方)は §5(:389-393)で「本書のデータからは分けられない」と自ら書く未検証の判断であり、1 件目と同種の「仮定」に見えるが印が無い。印の付け方の基準は何か(CLAUDE.md §8「報告の各主張に事実/推定/仮定の印を付ける」)。

**リードの応答: 直した。**§4 の 9 件全部に印を付けた(2 件目 = 仮定、3 件目「按分不変」= 事実(§3.3 の実測)、4 件目 = 判断(印なしでよいものは「判断」と書く)、5 件目 一意化 = 事実、6 件目 差の向き = 事実(表)、7・8・9 = 判断)。基準を 1 行書いた: 「数を含む主張は事実 / 推定 / 仮定、数を含まない設計の選択は「判断」」。

> 5. [聞く] `OI_DISTANCE_SPLIT_2026-09-18.md:380-402`(§5)— 「OI_DISTANCE §5 の射程はすべてそのまま続いている」として括弧内に要約列挙しているが、前の文書 `OI_DISTANCE_2026-09-17.md` §5 にあった「側別の按分が当たっているかを測っていない…taker の向きの出来高比で割るのは仮定」という項目(本書がまさに拡張している対象そのもの)が、この要約列挙に再掲されていない。「そのまま続いている」という主張と、実際に再掲された項目のリストは一致しているか。

**リードの応答: 直した。**§5 の列挙に「側別の按分(taker の向きの出来高比で割る)が当たっているかは本書でも測っていない(仮定のまま。本書が変えたのは価格の置き方だけ)」を足した。

> 6. [聞く] `OI_DISTANCE_SPLIT_2026-09-18.md:154,155,157` — 用語「側別 VWAP」(買い側/売り側どちらかを指す総称として使用)が、冒頭(:9-12)で宣言した「本書で新しく使う語は 2 つだけ(買い側 VWAP・売り側 VWAP)」に数えられていない。「側別 VWAP」は既存語の組み合わせの言い換えであり第3の新語ではない、という理解でよいか。

**リードの応答: その理解でよい。**冒頭に「「側別 VWAP」は買い側 VWAP と売り側 VWAP の総称(新語ではない)」と 1 行足した。

## 状態

直す 3・聞く 3 = 6 件。**全部処置(直した 6)。**
