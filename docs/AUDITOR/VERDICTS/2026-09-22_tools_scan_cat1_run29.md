# 区分 1 の 29 回目 — リードの検収(2026-09-23)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 29 回目の実行」節(393 行追加 / 7 行削除)/ 生ログ `docs/DATA/probes/20260922_tools_1_run29.log`(558 行)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲・受け入れ検査(機械)

削除 7 行は、4571 行の知見 1 本と、別立ての表 6 本(欄を足すための作り直し)。**指定 1・2 の分ちょうど。**
当方が全 28 本の生ログを渡して打った結果は 13 本すべて 0 件。**判定: 合。**

`--all` は 45 件で当方の検収と同数。**内訳が 2 つ動いており、どちらも説明が付く**:
- **K11 が 1 → 0**(本物の 1 件を直したため)。
- **K2 が 14 → 15**(この回で 1 件増。逐語 2 本が閉じない丸括弧を持つため。**折れている行を 1 行に繋がない**という申し送りに従って閉じずに残したもので、**誤検出。当方の判定として記録する**)。

## 2. 中身の抜き取り — **候補 98 は 4 つ目の立場だった。当方が原典で確かめた**

`src/orderbook.py`(21,118 バイト)を取得:

```
 36: Cancelled orders in the queues. cancel() flags the order and leaves it where
 67: # Live (non-cancelled) order count per price level. cancel() is lazy —
```

**取り消した注文に印を付けて、列にそのまま置いておく。**列は繰り上がらないが、その注文は埋まらない。
**当方から見ると「先に居る誰かが取り消しても、そこを通り過ぎるまで順番は良くならない」**という立場で、**4 つとも別である**:

| 候補 | 先行注文の取り消しをどう扱うか | 効く時点 |
|---|---|---|
| 95 | **何もしない**(原典自身が `pessimistic` と明記) | — |
| 57 `WonderTrader` | **差した瞬間に 1 回だけ**、率で先行量を割り引く(既定 `0`) | 注文を出すとき |
| 104 | **取り消しが起きた時点で**順位が繰り上がる | 取り消しのとき |
| **98**(新) | **印を付けて列に残す(怠惰)。**繰り上がらないが埋まりもしない | 通り過ぎるとき |

**当方の `maker_fill_ref.py` は 95 の側である。**
**28 回目の要約は 3 つしか並べていなかった。**「遅延」と「取り消しの扱い」を欄として足させた直しが、**足したその回に 4 つ目を連れてきた。**
**当方が目盛りから落としていた軸に、実際に中身があったということである。**

**遅延の側も 6 件見つかった**(23・33・63・91・92・98)。うち 3 件(33・98・15)はこの回が初めて取った逐語。
**33 番は注文の送信と取り消しで別々の遅延を持つ**(`order_submit_latency_ns` と `order_cancel_latency_ns`)。**当方にはどちらも無い。**

**語の一致を軸の証拠にしなかったのも正しい**(「取り消し」に当たった 11 件は自分の注文を取り消す道具で、先行注文の軸ではない。60 の `max_delay_count` などは同じ語の別の軸)。

## 3. 調査班が挙げた 6 件 — リードの回答

**(1) 14 回目の「6 要素ごとの残り」が要素ごとに違う数え方 → 過去の節は書き換えない。**
22 回目の数え直しが正しい数を出しており、**食い違いの在処もこの回の節に書かれている**ので読む側は追える。**20 回目の集計の落ちと同じ扱い。**

**(2) 候補 14 の「公開されていない」が要約の外に 2 箇所 → 直すこと。**
**射程が落ちるのは測定の記録ではなく欠陥である**(§5.2「棄却には射程を書く」)。出典の取り違え 7 箇所を直したのと同じ理由で、**過去の節でも直す。**

**(3) 段の表 37 行に対し分布が 36 件(91 番が落ちている)→ 直すこと。**単純な数え落ち。

**(4) 段の表の行の集合が何であるべきか → 印を持つ候補と一致させる。5 件を 1 件ずつ当て直すこと。**

段は `区分1-市場影響と約定の模型` の下位の尺度であり、**印の述語(23・24 回目で決めた)は「約定の可否・量・値段のどれかを決める仕組みがあるか」**である。
**段が付くということは、その候補が埋まりを決めているということなので、述語と一致する。**よって:

> **段の表に載るのは、`区分1-市場影響と約定の模型` の印を持つ候補。両者は同じ集合でなければならない。**

3・8・19・51・119 の 5 件は、**述語に照らして 1 件ずつ決める**: 埋まりを決めているなら**印を足す**、決めていないなら**段の表から外して「該当なし」と書く。**
**51 番の「この印は付けない」は、述語と食い違っている**(段 0 = 指定の値段と数量をそのまま成立させる = 埋まりを決めている)。**節の側を直す。**

**(5) 数え直しの規則が 1 か所に無い → 固定すること。**
22 回目の `recount.py` が残っていないので同じ規則で数え直せない、という指摘は当たっている。
→ **数え直しの手順を `scripts/` に残し、節はその出力を貼る。**口伝えの規則にしない(§0.2 O-6)。

**(6)「軸が立たない」と書いたのは調査班が置いた規則 → 規則としては正しい。ただし書き分けること。**
**読んで、その機構が働く余地が無いと分かったもの = 「該当なし」**(段 0 と「尺度の外」を分けたのと同じ理屈)。
**読んでいないもの = 「未確認」。**この 2 つを同じ語で書かない。

## 4. 安全側

`code=000` が 4 本、全部打ち直し。**67 番 `lumibot` は `ungh.cc` が 3 度とも 000 で、別の道に替えて 1 本だけ取れた**と書いてある。
**「追跡ファイルの一覧が取れていないので走査の網が 1 本しか掛かっていない」と射程を自分で書いている。**
**当てずっぽうの取得が 2 本あったことと、その応答の符号が記録に残っていない(未確認)ことも自分で書いている。**
**隠していない。**導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。

## 5. 判定

**受け取る。**指摘 1〜5 が片付き、抜き取りは原典に当たった。
**「軸が落ちていた」という当方の誤りを直させたら、その回に 4 つ目の立場と遅延の 6 件が出た。目盛りを直した値打ちが実際に出ている。差し戻す点は無い。**

## 6. 30 回目の起動の指定 — **帳尻を合わせる回**

1. **候補 14 の「公開されていない」を、要約の外の 2 箇所(7957・8450 行)も射程つきに直す**(§3(2))。
2. **27 回目の段(既定)の分布に 91 番を入れて数え直す**(§3(3))。
3. **段の表の 5 件(3・8・19・51・119)を述語で当て直す**(§3(4))。印を足すか、表から外して「該当なし」と書くか。**51 番の節の「この印は付けない」も直す。**
4. **数え直しの手順を `scripts/` に残し、節にはその出力を貼る**(§3(5))。以後の回もそれを使う。
5. **「該当なし」と「未確認」を書き分ける**(§3(6))。既に書いたものも見直す。
6. **新しい検索計画は打たない。候補も増やさない。構築・導入・最小実行もしない。**原典を読むのは許す。
7. 上の 1〜5 に要る範囲でのみ既存の節への書き換えを許す。`git diff` の削除行を自分で確かめて返す。
