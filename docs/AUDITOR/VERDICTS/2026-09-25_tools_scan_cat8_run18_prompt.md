# 監査: 道具サーベイ 区分 8 の 18 回目の起動文(2026-09-25)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md`(この回から、ほかの回の起動文を参照しない、それだけで読める形)。

## 監査 93 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run18_prompt.md@a37bf14cc78b`、コミット 632b666)の逐語

> 監査 93 回目。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md`(指紋 a37bf14cc78b、コミット 632b666)。
>
> 読んだもの: run18 本文全文 / run17・run16・run15・run14 の起動文(比較のため)/ `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md`(監査 92 回目の逐語込み)/ `docs/DATA/surveys/CAT8_DESIGN.md` §1〜§9 / `docs/DATA/tools_catalog_cat8.tsv` の 8-037・8-041 行 / `scripts/cat8_ledger.py`(`cmd_check_elements` の中身)/ `docs/OWNER_LOG.md` L-102・L-416・L-506・L-508・L-509・L-511・L-512・L-516 / `docs/DISCUSSIONS/2026-09-13_worst_day.md` 発言 13。PRINCIPLES.md・KNOWN_ANSWERS.md は事前に確認済み。
>
> 以下、指摘ごとに severity・ファイル行・原則番号を付す。書き換えはしていない。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:75-81`(§7) — `scripts/cat8_ledger.py check-elements --round 18` は round≥9 で「判断に迷った点と問い」の番号付き行すべてに `[値・段の問い]` か `[それ以外の問い]` のタグが無いと機械で止める(`cmd_check_elements` 該当ブロック)。この規約自体が run18 本文のどこにも書かれていない。14〜17 回目はいずれも明記していた。check-elements が求める形を本文が説明していない(P3・P8)。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:36-46`(§2.3) — 「問いに出した行は値も段も `未判別` にする(決めてから問いに出さない)」という規則も本文から落ちている。check-elements は round≥8 でこれを機械検査する。調査班がこの規則を知らないまま値を決めてから問いに出すと、§7 の 2 の検査が 0 件にならず往復が生じる(P3)。
>
> 3. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:20,24-46` — WebFetch を一次資料に使わない禁止と `scripts/cat8_render.js` による描画取得の使い方・条件(印の根拠にするための (i)(ii)(iii))が丸ごと落ちている(14 回目 §2 冒頭にあった条項)。AlgoNetwork の一次資料は `algorier.com` のウェブサイト(§1、20 行目)で、過去の回(MetaTrader・AI Trading Lab など)では同種のサイトで JS 描画頁が繰り返し問題になっていた。WebFetch の使用は `cat8_ledger.py` の機械検査では検出できない(ツール呼び出しをログから機械判定する仕組みが無い)。一次資料の真正性に関わる規則が、機械の安全網が無いまま本文から消えている(P1)。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:26-31`(§2.1) — オーナーが決めた述語の読み(`docs/OWNER_LOG.md:425-426` L-508・L-509「同梱・呼び出す別ライブラリの機能は候補の機能に数える」「重複をはじく処理は E2 に数える」「1 回だけの固定の学習・評価の分け方は E3b に数える」)が本文から落ちている。今回の範囲は AlgoNetwork の E2(データ品質)・E3b(ルックアヘッドを防ぐ機能)を含み、これらの述語の読みが直接関係しうる。`CAT8_DESIGN.md` §3 にもこの解釈は載っていない(オーナー逐語のみに存在)。落としたのは意図的か、単に「起動文だけで読める」形にする過程での見落としか。
>
> 5. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:44` — 「同じ理由を2行以上に書きたくなったら、それは決まりにする」は機械検査を伴わない純粋な文章の指示である(`cat8_ledger.py` に理由文の重複を検出する処理は無い)。監査 92 回目の指摘 5・6(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md:50-53`)が問題にした AlgoNetwork E2 の 40 件超は「**ほぼ**そのまま」であって文字どおり同一文ではなかった。この規則の字面(「同じ文を繰り返さない」)を厳密に読むと、語尾や語順を変えた言い換えは規則の外側に出せる。12・13・14・16・17 回目と 5 回続いた型の文の当てはめに対し、機械の裏付けの無いテキスト規則 1 つだけで抜け道が塞がるか。`docs/OWNER_LOG.md:105`(L-102「機械が検査する仕組みは既に多すぎる」)と `docs/DISCUSSIONS/2026-09-13_worst_day.md:31`(発言 13「測定器を直すは対処療法」)に照らして「機械を足さない」という設計判断自体は妥当としても、それが「テキスト規則だけで足りる」ことの根拠にはならない。
>
> 6. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:21` — Oryon の E6 は 17 回目で既に全件検索済み(2,460 行・104 ファイル、検収 `run17.md:21` および監査 92 回目指摘 4 `run17.md:48`)で、問題は「理由が語の意味を言っていない」ことだった(検索のやり直しが必要とは指摘されていない)。run18 §2.3 は「自己試験の根拠しか無い要素は、全件の検索で…確かめる」とあるが、既存の検索結果の理由を書き直すのか、検索から打ち直すのかを明示していない。
>
> 7. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:13-14` — 「要素と段」表の 7 列の意味・「生ログの行」列の書式・「台帳の値のまま(<何>回目の節)」の厳密な書式など、check-elements が機械検査する詳細な形式要件が、本文には無く「17 回目の節を見本にする」の一文だけに依存している。例示のみに頼る圧縮は、下位モデルが例の中の暗黙の書式規則を読み落とすリスクがある(P8)。
>
> 8. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:30`(§2.1)/ 比較対象 `docs/DATA/delegations/20260923_tools_survey_cat8_run14_prompt.md:48`(「各段の条件を満たすことは、その条件に当たる原文を「…」で6列目に引き」) — 段 4・5 の判定に原文の逐語引用が必要という指示が run18 から落ちている。`CAT8_DESIGN.md` §4.1 にもこの具体的な書式指示は無く、14 回目以降の起動文にしかなかった手順知識。今回 Oryon の E1b の段(現在 `未判別`、台帳 8-041 行)を決める可能性があり、check-elements は段 4/5 のとき 6 列目に 8 字以上の「…」引用が無いと止める。
>
> 9. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:20` — AlgoNetwork の N「algorier.com の全 68 頁」には同社の別サービス AlgoBuild(台帳の別候補 8-028)や一般教育ブログ記事が含まれると検収(`run17.md:23`、監査 92 回目指摘 7)が既に指摘している。広い N の採用自体は監査 92 回目の処置 7 で確定したが、AlgoBuild 自身のページの記述を AlgoNetwork の機能として誤って根拠に使わないための個別の注意が本文に明記されていない。
>
> 10. 範囲について(タスクの問い 3): `docs/OWNER_LOG.md:415`(L-416「一気に終わらせようとせずに」)・`docs/OWNER_LOG.md:423`(L-506「中途半端で止まらないでください」)と照らすと、8-037・8-041 の残り要素だけに絞った範囲は、検収(`run17.md:36`)が示した「1 回に読ませる量を減らす」方針の直接の実装であり、矛盾は見当たらない。該当なし。
>
> 11. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:21` — Oryon のリポジトリ取得(`scripts/cat8_repo_fetch.sh`)について、14 回目起動文にあった終了コードの読み分け(0/4/5/6/その他)の説明が run18 に無い。Oryon は小さいリポジトリと見られ実害は薄いと推測されるが未確認。
>
> 以上 11 件(項目 10 は「該当なし」の確認結果)。承認・却下は判断していない。

## 処置(リード、監査 93 回目)

短くしたときに、検査が求める形と、これまでに要ると分かった規則を落とした。リードの見落としである。どれも 1〜2 行で足した(起動文は 81 行 → 92 行)。

1・2. **直した**。§2.2「問いの書き方」を足した: 問いに出した行は値も段も `未判別`。番号付きの問いの行には全部 `[値・段の問い]` か `[それ以外の問い]`。
3. **直した**。§2.6 に、WebFetch を一次資料に使わないこと(もとから書いてあった)に加えて、`cat8_render.js` の打ち方、描画した頁は `印` の根拠にだけ使い `なし` には使わないこと、`印` にする条件 (i)(ii)(iii) を足した。
4. **直した(見落とし)**。§2.1 に、オーナーが決めた述語の読み(L-508・L-509 の 4 つ)と、乱数の種は段 5 の (ア) に当たらないことを足した。
5. **答える(直した)**。「語尾や語順を変えただけの同じ内容も、同じ理由とみなす(リードが検収で読む)」を足した。機械の検査は足さない(L-102・発言 13)。守られたかは、リードが検収で理由の表を読んで確かめる(17 回目の検収で数えたのと同じ手)。
6. **直した**。Oryon の E6 は、この回に検索から打ち直す(生ログはこの回のものだけを根拠にする)と書いた。
7. **直した**。§4 に、検査が形を見る所(7 列の表の見出しと 7 列目の書き方、触らない行の 6 列目、知見の表の根拠の欄)を書いた。
8. **直した**。§2.1 に、段の条件の原文を「…」で 6 列目に引くこと(段 4・5 は 8 字以上)を足した。
9. **直した**。§2.6 に、`algorier.com` の中の別の製品 AlgoBuild(8-028)の頁の機能の記述を AlgoNetwork の `印` の根拠にしないことを書いた。
10. 該当なし。
11. **直した**。§2.6 に、取得の道具の終了コードの読み分けと、和のファイルの扱いを書いた。

直したあとの起動文の印: `20260923_tools_survey_cat8_run18_prompt.md@ad849c867a6e`。監査 94 回目に掛ける。

## 監査 94 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run18_prompt.md@ad849c867a6e`、コミット c00fb5c)の逐語

> 監査 94 回目の結果
>
> 対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md`(指紋 `ad849c867a6e`、コミット c00fb5c)。
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:41-51`(§2.4、(乙)決まりで分ける) — `scripts/cat8_ledger.py: cmd_check_elements`(621-634行)は、報告に `### 決まりの一覧` の見出しがあれば**ラウンドを問わず無条件で**その表を検査し、1 列目が `docs/DATA/probes/<生ログ>:<検索の手の行>` の形か・列が 6 つか・同じ手/id の重複が無いか等を機械で見る。この書式(1 列目の形・6 列の意味・正規表現中の `|` を `\|` にする等)は 17 回目の起動文 §2 の 2(`run17_prompt.md:48`)にしか明文化されておらず、run18 は「使い方は 17 回目の生ログの手と、道具の説明 `sed -n 1,60p scripts/cat8_classify.py` を見本にする」としか書いていない(run18:48)。AlgoNetwork は 17 回目で E2・E3a・E3b とも 800〜900 行超の当たりがあり(乙)を使っている(`SCAN_...cat8.md:383-385`)ため、18 回目でも(乙)が必要になる可能性が高い。書式を誤ると `check-elements --round 18` が止まる(監査 93 回目の処置 7 が「検査が形を見る所…を書いた」と述べた範囲(§4、run18:73-76)には `決まりの一覧` の書式が含まれておらず、処置の文と実際にカバーした範囲が食い違っている)。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:62`(§2.6、描画した頁を `印` の根拠にする条件 (ii)) — 14 回目の起動文 §2(`run14_prompt.md:46`)の原文は「URL の持ち主がその候補自身(候補の公式のドメイン)の `--- frame <URL>` の下にある(**広告・計測・チャットなど第三者の埋め込みの frame の文は使わない**)」だが、run18 は「候補自身のドメインの `--- frame <URL>` の下にある」とだけ書き、括弧内の除外例(広告・計測・チャット)を落としている。一次資料の真正性に関わる注意書きなので、単なる圧縮か意図的な削除かが分からない。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:62`(同上) — 14 回目原文の結びの一文「満たさなければ `未判別`」(`run14_prompt.md:46` 末尾)が run18 に無い。(i)(ii)(iii) を満たさない場合にどう扱うか(未判別に戻すこと)が明示されていない。
>
> 4. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:92`(§7 の 5、返答に書く項目) — 14 回目(`run14_prompt.md:81`)・17 回目(`run17_prompt.md:83`)の起動文はいずれも「描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧(候補・要素・読めなかった頁)」を返答の必須項目に挙げていたが、run18 の §7・5 の一覧からはこの項目が落ちている。AlgoNetwork(algorier.com、68 頁)は JS 描画の対象になりうるサイトであり、この項目が無いと描画起因の未判別がリードの検収で見えにくくなる。
>
> 5. [聞く] 監査 93 回目の処置 7(検収文書 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18_prompt.md:46`)は「§4 に、検査が形を見る所(7 列の表の見出しと 7 列目の書き方、触らない行の 6 列目、知見の表の根拠の欄)を書いた」と書いているが、実際に足された文(run18:73-76)はこの 3 点(要素と段の表・触らない行・知見の表)に限られ、同じく `cmd_check_elements` が機械で見る `決まりの一覧` の表(項目 1 参照)は含まれていない。処置の文が「検査が形を見る所」を書いたと述べる範囲と、実際に足した範囲が一致していない。
>
> 上記 5 件のうち、項目 1 が唯一「check-elements --round 18 を止めうる」実務上のブロッカーで、2〜4 は本文の欠落・処置文との不一致(P8 型)。項目 3(L-508・L-509 の読み)・cat8_render.js の (i)(ii)(iii) 全体の趣旨、および監査 93 回目の処置 1・2・4・5・6・8・9・11 に対応する追記(§2.1 の L-508/L-509 の 4 規則・乱数の種の扱い、§2.2 の問いのタグ付け、§2.1 の 段 4/5 の逐語引用要件、§2.6 の AlgoBuild 混入防止・取得コードの読み分け)は、14〜17 回目の起動文・設計票の逐語と照らして字面の食い違いは見当たらなかった(項目 2・3 の (ii) の簡略化を除く)。

## 処置(リード、監査 94 回目)

1・5. **直した**。§2.4 の (乙) の項に、2 つの表の形(`### 決まりの一覧` の 6 列・1 列目は検索の手の見出しの行・`|` の書き方、`### 当たりの判定(行ごと)` の 3 列)と、分類の手の打ち直しの扱い(最後の 1 回を数える・切れた手のある検索の手は数えない・一度 `unmatched` に出た行は残す)を書いた。15 回目の起動文 §2 の 2(監査 78〜85 回目を通った版)の要点を写した。監査 93 回目の処置 7 の「検査が形を見る所を書いた」は、この表を含めていなかった(処置の文が実際の範囲より広かった)。
2・3. **直した**。(ii) の括弧(第三者の埋め込みの frame の文は使わない)と、「満たさなければ `未判別`」を、14 回目の起動文 §2 の字のとおりに戻した。
4. **直した**。§7 の 5 に「描画でしか読めない一次資料しか無いために `未判別` のまま残った要素の一覧」を戻した。

足した文は、どれも監査を通った版(14 回目・15 回目の起動文)の文を写したもので、新しい規則は無い。止める指摘(1)はこの写しで塞いだので、監査を重ねずに送る。送る版の印: `20260923_tools_survey_cat8_run18_prompt.md@4bc5c0d40b3f`。
