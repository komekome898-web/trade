# 検収: 道具サーベイ 区分 8 の 12 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 12 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run12.log`(139,674 行・1,558 手・約 45 MB)。届いたままのコミット 6d5c318。起動文の指紋 `d9f9f2206ad7`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **477,635 トークン・14,837,336 ms(247 分)・道具 750 回**。生ログの最初の手 15:20:45Z / 最後の手 17:31:54Z(そのあとの約 1 時間半は返す前の検査の打ち直し)。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(報告 + 生ログ 12 本)**1 件**(K13: 8-018 の §4.0 の表の 11 行が同じ生ログの行を根拠に引いている。調査班は「1 つの一次資料から 11 項目を読んだ」と書いて渡した。中身の監査で見る)/ `check-elements --round 12` 0 件(候補の一覧 41 行・要素と段の表 328 行・知見の表 19 行)/ `check "" <run12.log>` 0 件。
`cat8_fetch_tally.py`: 178 件すべて終了コード 0、N 34,610(10・11 回目と同じ)。誤り 6 件は、6 件のリポジトリを 2 回取って `downloaded_bytes` だけが違ったもの(取り直すと git の圧縮の大きさが少し変わる。N・取らなかったファイルは同じ)。

## 2. リードが確かめたこと

- **語の組の語が全部入っているか**: `check-elements --round 12` が組の中の語を 1 つずつ見て 0 件(11 回目に足りなかった `leak`・`seed` なども入った)。起動文の字のとおりか別の表記も足したか(監査 72 回目の答え 1)は、8-016 の検索の語が起動文の字に加えて単語の境界 `\b` を付けた形だった(調査班の報告)。中身の監査で見る。
- **除外**: 8-016 以外の候補の除外(8-017・8-020〜8-025・8-027)は、拡張子を数えると画像(png・jpg・webp)・表計算(xlsx)・直列化の物(pickle・pkl・npy)・カバレッジの DB(`.coverage`)だけで、文書は無かった(8-021・8-022・8-025・8-027 は全件、8-017・8-020・8-024 は件数だけ見た)。
- **N・M の二重数え(検査の作りの欠陥)**: AutoHedge E1a「一覧 102 件 / 読んだ 102 件(51 件 × 2)」のように、同じ一覧を 2 回検索した行は N・M が 2 倍になっている(台帳の検査が、根拠に引いた検索の件数を足していたため、調査班が数を合わせた)。**本当の N・M は一覧 1 本の件数**(AutoHedge 51、Qlib 584、Backtrader 378、FinanceToolkit 1,677、Quantreo 130)。13 回目以降、`check-elements` は同じ一覧を 1 回だけ数える(コミットはこの検収と同じ)。検索の中身は、2 回の検索の語を合わせて組の語を全部含む。

## 3. 台帳

`import --round 12`: 変わった所 67。`check` 0 件。新しい `なし` 28(8-016 の E3a・E5、8-019 の 7、8-021 の 1、8-022 の 3、8-023 の 4、8-025 の 5、8-027 の 6)。

## 4. 判定

**受け取りは中身の監査のあとに決める**。`なし` 28・段 4・5(8-017 E2 段 5 など)が多いので、監査役(73 回目)に通す。
