# 検収: 道具サーベイ 区分 8 の 12 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 12 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run12.log`(139,674 行・1,558 手・約 45 MB)。届いたままのコミット 6d5c318。起動文の指紋 `d9f9f2206ad7`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **477,635 トークン・14,837,336 ms(247 分)・道具 750 回**。生ログの最初の手 15:20:45Z / 最後の手 17:31:54Z(そのあとの約 1 時間半は返す前の検査の打ち直し)。

## 1. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(報告 + 生ログ 12 本)**1 件**(K13: 8-018 の §4.0 の表の 11 行が同じ生ログの行を根拠に引いている。調査班は「1 つの一次資料から 11 項目を読んだ」と書いて渡した。中身の監査で見る)/ `check-elements --round 12` 0 件(候補の一覧 41 行・要素と段の表 328 行・知見の表 19 行)/ `check "" <run12.log>` 0 件。
`cat8_fetch_tally.py`: 178 件すべて終了コード 0、N 34,610(10・11 回目と同じ)。誤り 6 件は、6 件のリポジトリを 2 回取って `downloaded_bytes` だけが違ったもの(取り直すと git の圧縮の大きさが少し変わる。N・取らなかったファイルは同じ)。

## 2. リードが確かめたこと

- **語の組の語が全部入っているか**: `check-elements --round 12` が組の中の語を 1 つずつ見て 0 件(11 回目に足りなかった `leak`・`seed` なども入った)。起動文の字のとおりか別の表記も足したか(監査 72 回目の答え 1)は、8-016 の検索の語が起動文の字に加えて単語の境界 `\b` を付けた形だった(調査班の報告)。中身の監査で見る。
- **除外**: 8-016 以外の候補の除外(8-017・8-020〜8-025・8-027)は、拡張子を数えると画像(png・jpg・webp)・表計算(xlsx)・直列化の物(pickle・pkl・npy)・カバレッジの DB(`.coverage`)だけで、文書は無かった(8-021・8-022・8-025・8-027 は全件、8-017・8-020・8-024 は件数だけ見た)。
- **N・M の二重数え(検査の作りの欠陥)**: AutoHedge E1a「一覧 102 件 / 読んだ 102 件(51 件 × 2)」のように、同じ一覧を 2 回検索した行は N・M が 2 倍になっている(台帳の検査が、根拠に引いた検索の件数を足していたため、調査班が数を合わせた)。**本当の N・M は一覧 1 本の件数**(AutoHedge 51、Qlib 584、Backtrader 378、FinanceToolkit 1,677、Quantreo 130)。13 回目以降、`check-elements` は同じ一覧を 1 回だけ数える(コミットはこの検収と同じ)。検索の中身は、2 回の検索の語を合わせて組の語を全部含む。

## 3. 台帳

`import --round 12`: 変わった所 67。`check` 0 件。新しい `なし` 28(8-016 の E3a・E5、8-019 の 7、8-021 の 1、8-022 の 3、8-023 の 4、8-025 の 5、8-027 の 6)。

## 4. 判定

**受け取りは中身の監査のあとに決める**。`なし` 28・段 4・5(8-017 E2 段 5 など)が多いので、監査役(73 回目)に通す。

## 監査 73 回目(中身の回。対象 = f062b76 の 12 回目の節と生ログ)

監査役の出力(逐語。[止める] 1 件・[直す] 4 件・[聞く] 2 件):

> 監査 73 回目(道具サーベイ区分 8・12 回目の中身)の指摘。対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 12 回目の実行(2026-09-24)` 節(4796〜14570 行)、生ログ `docs/DATA/probes/20260923_tools_8_run12.log`。
>
> 1. [止める] docs/DATA/SCAN_2026-09-23_tools_cat8.md:5108, 5114, 8001 — `FinanceToolkit`(8-025)の E1a・E5 の `なし` は、この回自身が作った `### 当たりの判定` 表(8001 行)に載っている `tests/performance/test_performance_model.py` の当たりと矛盾していないか。この行は `# Cross-check against an independent Numpy OLS fit of the same regression.` というコメントの下で、`np.random.seed(42)` → FinanceToolkit 自身の `get_treynor_mazuy_model` の出力 → 独立実装の `np.linalg.lstsq` の出力 → `assert result["Alpha"] == pytest.approx(coefficients[0], abs=1e-6)` という形で、**FinanceToolkit 自身のテストスイート**が自分の計算を独立した NumPy 実装と突き合わせている(GitHub `JerBouma/FinanceToolkit` の同ファイルで実物を確認済み)。これは E1a の述語「2つ以上の実装の出力を突き合わせて差を出す機能(…参照実装との一致試験)」にほぼ逐語で当たり、`np.random.seed(42)` と `recorder.capture(...)` は E5 の述語(乱数の種・実験の再実行と結果の差分)にも当たる。ところが E1a 行の根拠は「当たりはコード中のcompareの一般的使用・`ReconciledDepreciation`等…のみ」と書き、E5 行の根拠は「seed/deterministic/snapshotの当たりはexamplesのnotebook中の…利用者が手元で書くサンプルコード」と書いている ── しかし当該ファイルは `examples/` ではなく **候補自身の `tests/` 配下**であり、「利用者のサンプルコード」という記述は事実と違う。同じ回の中で、`FinGPT`(`tests/test_transaction_data.py`)・`Backtrader`(`tests/test_analyzer-sqn.py`)・`Lean`(`Tests/RegressionTests.cs`)の**候補自身のテストスイート内の assert**は同種の証拠として E5/E6 の `印` の根拠に採用されている。同じ種類の証拠が FinanceToolkit だけ見落とされている(または見落とされた理由が書かれていない)ように見える。本文中のどこにも `test_performance_model.py` への言及は無い(全文検索で 0 件)。この `なし` が成立するなら、なぜこのファイルが対象外なのかを本文に書く必要がある。成立しないなら、8-025 の状態(`浅い`)と検収の受け取り判断に影響する。(P3、KA-05/KA-06 型)
>
> 2. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:8271 — `FinGPT`(8-022)の E2 の `印` 根拠(`finogrid/agents/audit_governance/agent.py` の LLM プロンプト「Detect anomalous patterns in payout flows」、知見 #10・要素行で使用)と、同じファイルの `### 当たりの判定` 表の行(8271 行)の理由欄が食い違う。表の理由は「「anomal」の使用箇所(例: `- Detect anomalous patterns in payout flows`)」とだけ書き、この当たりが E2 の `印` の根拠として採用済みであることを示す注記が無い。起動文 §2「`印`の根拠になった当たりなら、理由の欄にその旨を書く」に反する。機械検査は素通りしているが、内容の対応関係を崩す(読み手が「未評価のまま残った当たり」と「印の根拠」を区別できない)。
>
> 3. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:7188(および同種 137 件、抽出コマンド: `awk 'NR>=5303&&NR<=14508' … | grep -E '「[^」]+」の使用箇所' | grep -v 一般的な使用`) — `th2/viewer` の `node_modules/node-gyp/.../gyp-tests.el`(`"List of golden samples to check"` `"Recreate the golden samples"` 、生ログ 36496・36502・36509 行で実在確認済み)は、E5 の述語語「golden」に文字どおり golden-file テストの意味で当たっているが、理由欄は「「golden」の使用箇所(例: …)」とだけ書かれ、L-509(同梱・呼び出す別ライブラリの機能を候補の機能に数えるか)に照らした判断(数える/数えないとその理由)が書かれていない。同種の「〜の使用箇所」とだけ書いて判断を欠く行は run12 の `### 当たりの判定` に少なくとも 137 件ある。起動文 §2 の「理由には…その語がその行で何を指しているかを書く(一般的な記述中の語のような文だけにしない = 監査70回目の指摘4)」の要求を機械検査は捉えられていない。8-016 の E5 `なし` を覆すとまでは判断しないが(node-gyp は th2 自身が呼ぶ機能というより frontend ビルド依存の奥深くにある third-party の devtool で、L-509 の「呼び出している別のライブラリ」に当たるかは疑わしい)、理由を書かずに素通りしている点は直すべき。
>
> 4. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:5253・5257-5296 — 「8-018 Vibe-Trading についてこの回に語彙の**全41項目**を書く」「**過半の確認**: **41項目**中27項目が…」と2箇所に書いているが、実際にこの節に書かれた `Vibe-Trading` の行は **43 行**(`awk 'NR==5251,NR==5302'…grep -c '^| \`Vibe-Trading\` |'` → 43)で、委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md` §4.0 の語彙を数え直しても 43 項目(版〜保守者名の一貫性)。過半という結論自体(27/43 でも 62.8%)は揺るがないが、書かれた分母(41)は表の行数・委任文の語彙数のどちらとも一致しない。P5(n 種の数え上げは列挙し直しても一致するか)。
>
> 5. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:5082 — `Qlib`(8-021)E5 の段4 の根拠は、README の qrun の一般的な説明と、`examples/benchmarks/Localformer/workflow_config_localformer_Alpha158.yaml` の `seed: 0`(生ログ 48177 行で確認: これは **Qlib 自身が同梱する example ファイル**)を挙げ、「利用者が用意する外部のYAML設定ファイルで乱数の種を指定できる」と書いている。しかし引用元は Qlib 自身のリポジトリ内の example であり、「外部ユーザーが用意した設定ファイルに掛かる」ことを直接示す逐語ではない(qrun が任意の外部 YAML を受け付けるという別の一次資料からの外挿)。設計票 §4.1「段4・5を付けるときは…条件に当たる逐語を…引く。逐語が無ければ段は未判別」に照らし、この段4はどの逐語で支えられているか。同様の間接推論が `Lean` E1b・E4(README の一般的な説明文のみ)にも見られる(5036-5139 行の該当行)。
>
> 6. [聞く] docs/DATA/SCAN_2026-09-23_tools_cat8.md:4798 の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run12_prompt.md:28`「各候補を設計票§2の`深掘り`の条件まで進める」に対し、実際に §4.0 表を書いて `深掘り` に到達したのは 8-018(Vibe-Trading)1 件のみ。8-019(AutoHedge)・8-021(Qlib)・8-022(FinGPT)・8-023(Backtrader)・8-025(FinanceToolkit)・8-027(Quantreo)は E1a〜E6 の値をすべて確定させた(`未判別` 0)にもかかわらず §4.0 表を書かず `浅い` のまま止まっている。起動文の項目 (b)(c)(d) は個々にはこれらの候補で「§4.0 を書く」と明記しておらず(明記は 8-018 のみ)、指示の冒頭の一文と実際の作業範囲が食い違って見える。判断に迷った点と問いの一覧(14563 行以下)にもこの食い違いへの言及は無い。指示未達か意図的な範囲の絞り込みかをリードが確認する必要がある。
>
> 7. [直す] docs/DATA/SCAN_2026-09-23_tools_cat8.md:5108, 5082 ほか(検収 §2 で自認済み) — `AutoHedge`・`Qlib`・`Backtrader`・`FinanceToolkit`・`Quantreo` の E1a(・一部 E1b/E3a)の `一覧N件/読んだM件` は、設計票 §2 の定義(N = 一次資料の一覧を取った手の出力の件数そのもの)どおりの数ではなく、同じ一覧を2回検索した結果を単純合算した**2倍の数値**(例: FinanceToolkit 1677件の一覧を2回検索して3354件と記載)になっている。検収 §2 は「台帳の検査(`check-elements`)が二重に数える作りの欠陥で、調査班が数を合わせた」「本当のN・Mは一覧1本の件数」「13回目以降は直す」と自認しているが、**この回として納品された SCAN 文書自体には、設計票の定義と一致しない N が今も記載されたまま**である。13回目での修正を待つとしても、この回の記載が定義どおりでないことは報告書に明記すべきではないか(現状は根拠欄の説明文だけで、`一覧N件/読んだM件` という規定の書式自体は虚偽の体裁のまま残っている)。
>
> 上記のうち 1 が最も重い(8-025 の中核の判定を揺るがしうる)。2〜7 は記録・理由付け・指示整合性の直しで、8-016 の E3a・E5 のやり直し自体(語の組を全語含めた再検索、178 リポジトリ全件検索)や、PyPI 実測の再取得(8-019・8-020・8-022・8-024・8-026・8-028、生ログ 13778-13813 行で実在確認済み)、Lean readme.md の読み直しは、指示どおり実行されていることを確認した。

**リードの処置**:
1: 認める。8-025 FinanceToolkit の `tests/performance/test_performance_model.py` は、候補自身の試験が自分の計算を独立の NumPy の実装と突き合わせ、乱数の種を固定している。E1a・E5 の `なし` の根拠(「examples の利用者のサンプルコード」)は事実と違う。
3 の型の広さを、リードが数えた: この回の `### 当たりの判定` の表 9,193 行のうち、理由が「「<語>」の使用箇所(例: …)」だけで、その語が何を指すかの判断が無い行は **224 行**。候補ごとには th2(8-016)133 / Lean 21 / FinanceToolkit 16 / Qlib 14 / Great Expectations 11 / FinGPT 11 / OpenBB 10 / Backtrader 8 / AutoHedge・Quantreo・Exactpro の頁 0。**この 224 行の当たりに依っている `なし` は、根拠が読まれていないので、値・段とも `未判別` に戻した**: 8-016 の E3a・E5(状態は `判別に一次資料が要る`)/ 8-021 E1a / 8-022 E3a・E3b・E4 / 8-023 E1a・E2・E3a・E3b / 8-025 E1a・E3a・E3b・E4・E5。**AutoHedge(8-019)と Quantreo(8-027)の `なし` は、理由の欠けた行が無いので残した**(中身の抜き取りは 13 回目の検収でも続ける)。13 回目で、この 224 行だけを読み直させる(全部の検索のやり直しはさせない)。
2: 認める。8-022 E2 の `印` の根拠になった当たりの行に、その旨を書かせる(13 回目)。
3: 認める(1 に書いた)。
4: 認める。委任文 §4.0 の語彙は 43 項目。報告の「41 項目」は誤り(過半の結論 27/43 は変わらない)。
5: 認める。Qlib E5 の段 4 は、Qlib 自身の例の設定ファイルの `seed: 0` からの外挿で、外から持ち込めることを言う逐語ではない。Lean E1b・E4 の段 4 も README の一般の説明だけ。台帳で 8-021 段_E5、8-024 段_E1b・段_E4 を `未判別` にした。
6: 答える。起動文 §2 の冒頭は「各候補を `深掘り` の条件まで進める」だが、項目 (a)〜(d) で §4.0 の表を書けと名指ししたのは 8-018 だけだった(リードの書き方の漏れ)。13 回目で、値がそろった候補(8-019・8-021〜8-023・8-025・8-027)の §4.0 の表を書かせる。
7: 認める。報告の節は書き換えない(履歴)。この記録の §2 に本当の N・M を書いた(AutoHedge 51、Qlib 584、Backtrader 378、FinanceToolkit 1,677、Quantreo 130)。13 回目以降は検査が同じ一覧を 1 回だけ数える。

台帳: `check` 0 件。**判定: 一部受け取り**(8-018 の `深掘り`、PyPI の実測、Lean の読み直し、AutoHedge・Quantreo の `なし` は受け取る)。
