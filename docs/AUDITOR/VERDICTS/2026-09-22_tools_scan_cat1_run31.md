# 区分 1 の 31 回目 — リードの検収(2026-09-23)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 31 回目の実行」節(+226 / −0)/ 生ログ `docs/DATA/probes/20260922_tools_1_run31.log`(395 行)/ `scripts/recount_scan_cat1.py`(+84 / −17)
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 機械の確認

受け入れ検査は生ログ 30 本を全部渡して **13 本すべて 0 件**。**既存の節は 1 行も触っていない**(+226 / −0)。

## 2. **「到達の記載が無い = 53 件」は道具の粗さだった。欠けは 0 件**

当方が 30 回目に「**確かめるまでこの数をどこにも書かない**」と止めたもの。調査班が 1 件ずつ確かめた結果:

> 53 件の内訳は 浅い 24 / 状態の語なし 19 / `[深掘り]` 7 / 到達の記録 2 / 打ち消し 1 で、**「未着手」も「一次資料に未到達」も 0 件**。
> 原因は、道具が到達を 4 つの語句の字面だけで見ていて、**報告が実際に使っている定義**(22 回目の逐語「**「浅い」以上は到達済みであり、残りではない。**」)**の語彙が入っていなかった**こと。

**道具を定義の語彙に合わせ直して、到達の記載が無い候補は 0 件になった。**
**止めた判断が当たった。**あのまま「53 件が未到達」と書いていたら、**完了の宣言を自分で覆す嘘の数**を出していた。

**あわせて同じ型の粗さを 3 件見つけて直している**(22 番 = 他の候補の印を自分のものとして数えていた / 68 番 = 中黒の箇条を拾えていなかった / 46 番 = 「可能性があるが決めない」を肯定で数えていた)。
**落ちと余分が打ち消し合って総数では見えていなかった**という指摘は、数え方の検査として正しい着眼である。

**結果、6 要素のうち 5 つと番号で数えた値が、節と機械でぴたりと一致した。**

## 3. **リードの数え違い — 30 回目の検収の「46 件」は誤り**

調査班が「`--all` の合計が 30 回目の節の 46 件と合わない」と、**自分で閉じずに渡してきた。**
**当方が押し出す前と後で測り直した**:

```
$ git show HEAD~1:docs/DATA/SCAN_2026-09-21_tools.md > before.md
$ python3 scripts/check_scan_report.py before.md <生ログ 30 本> --all   → K2 17 件 / 合計 48 件
$ python3 scripts/check_scan_report.py docs/DATA/SCAN_2026-09-21_tools.md <同> --all → K2 17 件 / 合計 48 件
```

- **当方の「46 件」は誤りで、実数は 48 件**(K1 14 / K2 17 / K5 1 / K9 15 / K12 1、**K11 は 0**)。
- **調査班の「当方の節による増は 0 件」は正しい。**前後で同じ値になることを当方が確かめた。
- 調査班の基準値「47 件」とも 1 件ずれるが、**当方が測った前後の値は一致しているので、増が 0 であることに影響しない。**

**当方が数え違えた数を検収に書き、それを調査班が突いた。**§0.2 O-6(出所の分からない数字を出さない)は当方にも掛かる。

## 4. 残りの問い — リードの回答

**(1) 候補 18 `zipline-reloaded` の `区分1-イベント駆動` を一覧に足すか → 足すこと。**
候補 6 とまったく同じ型(24 回目の表に「付ける」、根拠 `for dt, action in self.clock:` があるのに一覧に無い)。
**指定が名指ししなかったから触らなかったのは正しい扱い。****これで 6 要素すべてで差が 0 になる。**

**(2) 26・28 回目の分布と 27 回目の表に注記を足すか → 足さない。**
**`recount_scan_cat1.py` が正の数を出す機械になり、そのことが 31 回目の節に書かれた。**過去の節は記録のまま残す。

**(3) 25・30 回目に残る古い述語の逐語に注記を足すか → 足さない。**
**古い述語は、その時点でその節が判断に使った規則であり、記録である**(出典の取り違えのような「事実として誤っているもの」とは違う)。
新しい述語が 31 回目の節に日付つきで書かれていれば足りる。

**(4) 道具に足した当て方(「同じ文で自分以外の『<数> 番』が前に出ていたら数えない」)→ 残すこと。**
**これが 22 番の余分を消した部品であり、残さなければ数が合わない。**

**(5) `--all` の合計 → §3 のとおり。当方の誤り。**

## 5. 判定

**受け取る。**検査 13 本が 0 件、既存の節は不変、**当方が止めた「53 件」が道具の粗さだと確かめられ、数え方の粗さが 3 件見つかって直った。**
**当方の数え違いを自分で閉じずに渡してきたのも正しい。差し戻す点は無い。**

## 6. 32 回目の起動の指定 — **これで区分 1 を閉じる**

1. **候補 18 `zipline-reloaded` の `区分1-イベント駆動` を候補の一覧に足す**(§4(1))。
2. **`recount_scan_cat1.py` を打ち直し、6 要素すべてで節と機械の差が 0 になったことを出力で示す。**
3. **`--all` の実数を 48 件として節に書く**(当方の 46 件は誤り。§3)。内訳も書く。
4. 上の 1〜3 に要る範囲でのみ既存の節への書き換えを許す。**それ以外は 1 文字も触らない。**
5. **新しい検索計画は打たない。候補も増やさない。構築・導入・最小実行もしない。**
