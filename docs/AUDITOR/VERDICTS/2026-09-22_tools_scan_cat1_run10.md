# 区分 1 の 10 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 10 回目の実行(2026-09-22)」(3463 行目以降、227 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run10.log`(新規 640 行、20 手。復帰文字 0 個)

## 0. 無改変・検査

既存の 3462 行は **無改変**。過去の生ログも変更なし。生ログ 8 本を渡して打ち直し、**13 本すべて 0 件**。
**調査班が閉じずに残した行は 0 件**(3 回連続)。K11 に当たった 20 件は**参照の付け替えで直し、生ログには書き足していない。**

## 1. リードが一次資料で裏取りした中核の主張 — **全部一致**

`fills_and_orders.py` と `hourly_limit_orders.py` を取り直して読んだ。

- **埋まる規則**: 買いは `limit_price > market_price` のとき約定(71 行)、売りは `limit_price < market_price` のとき約定(80 行)、
  それ以外は `empty_fill`。**調査班が 4 通り直接叩いた結果と完全に一致する。**
- **左右の非対称も実在する**: 買いの枝が `price_requires_slippage_adjustment=False`(76 行)、
  売りの枝が `True`(85 行)。
- **板・待ち行列・出来高を見ていない**: `orderbook` / `order_book` / `queue` / `depth` / `volume` / `bid` / `ask` の
  一致がこのファイルで**全部 0 件**。「次の足の価格と指値の大小だけ」という主張は正しい。

**これは当方の `src/bot/backtest/engine.py` が自分で書いている限界(「通過した約定なら埋まったとみなす」)と
同じ層の模型**であり、**「出したが埋まらない」を持つ点だけが当方に無い。**

## 2. 抜き取り — **中身の誤りが 1 件**

| 行 | 取り直した結果 |
|---|---|
| 星 3522 | 一致 |
| 版 1.8.2 | 一致(`pyproject.toml` の `version`) |
| 保守者数 30 / 先頭の名前 | 一致 |
| ライセンス GPLv3 | 一致 |
| PyPI に wheel も sdist も無い | **一致**(`pysystemtrade` と `PySystemtrade` の両方で 404) |
| **言語と動作環境「pyproject の requires-python は >=3.10」** | **誤り。`requires-python` は存在しない**(リードが打ち直して一致 0 件)。`setup.py` にも `python_requires` は無い。在るのは整形器の設定 `target-version = ['py310']` で、対応する版の宣言ではない |

**訂正済み。**実際に動いた事実(python3.11 の venv で RUN_RC=0)は変わらない。変わるのは根拠。
**型は 9 回目と同じ「根拠に、資料に無いものを書く」。**2 回続けてこの型が出ている。

## 3. 調査班からの問い(自分で閉じずに渡してきた)

**「[16] に `rc=` の行が無く K11 を満たせないので、同じ試行を [19] として打ち直し、打ち直した値を書いた」
→ 受け入れる。正しい扱い。**
生ログへの後からの書き足しではなく、**実際に打った新しい手の追加**である。生ログは打ったことをそのまま記録している。
**ただし危険は書いておく**: K11 は行の**形**しか見ないので、「検査を満たすために手を増やす」向きの圧力が働く。
今回は本当に打ち直しているので問題にならない。**リードの抜き取り(実測の行 3 件の打ち直し)がこの穴の受け皿である。**

## 4. 「原文に無い判断」4 件の判定

1. **同梱の例ではなく合成データで中核の関数を直接呼んだ → 受け入れる。**
   同梱の例は MongoDB と時間足を要求し、配布物に時間足が無い。§5-4 の「合成のティック列で成行と指値の 1 往復」に
   忠実なのは調査班が選んだ側である。
2. **`git sparse-checkout add` を「data を除く全部」まで広げた → 受け入れる。測ってから広げている。**
   測った値: `multiple_prices_csv` 443.7 MiB / `adjusted_prices_csv` 236.1 MiB / data を除く全部 **30.3 MiB**。
   **9 回目に 880 MB になった原因がここで確定した。**
3. **§8 の `tools_inventory.py` の全文貼り付けを 2 回目以降やっていない → リードの判断: 区分ごとに 1 回でよい。**
   §8 の狙いは「当方の道具立てを手書きさせない」ことで、同じ区分の中で毎回貼り直す意味は無い。
   **区分の最初の回に貼り、以後の回はその節を参照する。**委任文は監査済みの版を動かさないので、起動指定で毎回伝える。
4. **`OctoBot` の隔離 venv が片付けで消えており、入れ直しから始まる → 受け入れる。**次の起動で時間を見込む。

## 5. リードの判定

- **受け取る。中身の誤りは 1 件**(根拠の誤り。結論は変わらない)。
- **確定した状態 1 件**: **`PySystemtrade` の指値の注文模擬が通った。**
  成行は 2 件出して 2 件約定、**指値は 5 件出して 2 件約定・3 件未約定。**
  **「出したが埋まらない」を模型として持つのは、区分 1 でこれが初めて。**
  入力は pandas の Series 2 本だけで、同梱データも DB も要らない。**当方の csv.gz を 2 本に直せばそのまま掛かる。**
  ただし**板の情報を渡す口は無い。**
- **区分 1 は未完了。**残るのは Superalgos / OpenTrader / CryptoSignal / Bot18 / `OctoBot` の最小実行。
  検索計画 6 本は未実行。
- **`scratchpad/pst` の作業木(149M + .git 91M)は残す。**区分 2 で `sysexecution` と `sysbrokers/IB` にそのまま使える。
