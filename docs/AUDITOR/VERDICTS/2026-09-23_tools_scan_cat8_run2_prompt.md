# 監査記録: 道具サーベイ 区分 8 の 2 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md`。オーナーの指示 L-504「**進めてください**」。
Jev の前段(`jev_delegate.py plan --prompt <起動文>`): 初版 印 0 件(is_bounded 0.78)/ 処置後の版 印 0 件。

## 監査 11 回目(対象 = 初版 c65d5de、指紋 `4179e7e6face`)

監査役の出力(逐語。[止める] 3 件・[直す] 3 件・[聞く] 2 件):

> 監査対象を通読し、`docs/DATA/surveys/CAT8_DESIGN.md`・`docs/DATA/delegations/20260922_tools_survey_prompt.md`・追補・1 回目の起動文・1 回目の検収・`scripts/cat8_ledger.py`・`scripts/check_scan_report.py` の該当部分を読み、`check_scan_report.py` の `scope()` の挙動を合成の報告で実際に打って確かめました。
>
> 1. [止める] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:22-29`(§2 の 1「1 回目の訂正」)— 合成の報告で実測: `scripts/check_scan_report.py` の `scope()`(`/home/user/trade/scripts/check_scan_report.py:458-485`)は、`## 区分\d` の節ごとに、その節の中に §4.0 の機械可読表(`道具|項目|値|印|根拠`)の行が **1 行も無ければ節ごと空行に置き換え**、K1・K2・K4〜K11・K13 の対象から**丸ごと外す**。今回の起動文の「1 回目の訂正」(§2 の 1)は §4.0 の表を書けとは指示しておらず、`### 要素と段`・知見の表・候補の一覧だけを更新する形になっている。§2 の 3(残りの候補の深掘り = §4.0 の表を作る唯一の作業)は §6 の予算で「予算の 8 割に近づいたら新しい候補に手を付けずに §7 へ進む」と後回しにされており、1 回目が予算を 3 割超えた実績を踏まえると、2 回目が §2 の 1・2 だけで予算を使い切り §2 の 3 に届かない可能性は低くない。その場合、2 回目の節全体(訂正した知見・候補の一覧を含む)が `check_scan_report.py` の主要な検査から機械的に除外され、K12 の「合計 0 件」が「実際に読んで 0 件だった」ではなく「読まれずに 0 件になった」ことを意味してしまう。この起動文には、この落とし穴への言及も、§4.0 の表を最低 1 行でも作る指示も無い。(P11)
>
> 2. [直す] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:24`(qf-lib E5 の訂正指示)— 1 回目の検収 §5・§13 差し戻し(3)は §4.0 の機械可読表の項目「再現性」の値(`無し(専用の乱数シード固定・再現性保証機能なし…)`)が誤りだったと認定しているが、この起動文は `要素と段` 表の E5 を直せとしか指示しておらず、**§4.0 の表そのものの「再現性」行を訂正する指示が無い**。1 回目の節を書き換えない(同 §4 行 40-45)ため、§4.0 の表に残る旧い「無し」の記載は、この回で訂正された E5=印 の主張と文書内で矛盾したまま残る。この食い違いは `check_conflicting_values`(K5)が数値(秒/個/パッケージ)しか見ないため検出されない。(P2)
>
> 3. [直す] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:22-29` — 1 回目の検収 §11(問い 6: 「4 軸の『推定』…外挙元の無い行は直す」)、および §8(Jev 印 = 判定を言う文 1 件・委任文と報告の照合 1 件・知見の表の行に URL が無い行 3)は、起動文の見出し(行 22「検収§13の(2)(3)(6)(7)と§8・§11」)では対象に含まれているように読めるが、§2 の 1 の各箇条書き(23〜28 行)にはこれらへの具体的な作業指示が無い。「§8・§11」を丸ごと引いているのに、その中の一部の項目だけが箇条書きに落ちており、残りは何を直すか書かれていない。(1 回目の検収§11・§8)
>
> 4. [止める] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:32`(§2 の 4「辿る一覧」)と `:14,51-53`(§6 予算)— §2 の 4 は「深掘りしなくてよい。状態は `未着手`」と書くが、1 回目の起動文 §2-5(この回もそのまま当てると §0 の 4 が指示している箇所)は「浅い・未着手の候補も 8 行を書く(分からない要素は `未判別`、根拠に試した手段)」を定めている。`scripts/cat8_ledger.py` の `check-elements`(`/home/user/trade/scripts/cat8_ledger.py:307-311`)は候補の一覧に載った**全名前**について `要素と段` の 8 行を要求し、状態が `未着手` でも免除しない。辿る一覧(dev.to・X・zenn・quantreo 等の記事)から出てくる道具名の数は未知で、これを候補の一覧へ `(新)` として載せた瞬間、その全員に 8 行(未判別+試した手段)を書く義務が生まれる。§6 の予算(51-53 行)は §2 の 1・2・3 の優先順位しか述べておらず、§2 の 4 がこの義務を通じて生む追加の作業量を予算計画に織り込んでいない。「深掘りしなくてよい」という射程の文言と、実際の受け入れ検査(§7 の 2)の判定基準が食い違っている。(P9)
>
> 5. [止める] 同上 — §0(14 行)の「新しい検索計画は打たない」という射程の宣言と、`scripts/check_scan_report.py` の `check_sections`(K3、`/home/user/trade/scripts/check_scan_report.py:71-89`)が `## 区分\d` の節ごとに無条件で要求する必須小節 6 つ(`検索計画`・`出典`・`知見`・`候補の一覧`・`ツール1件ごとの表`・`予算`)が矛盾しうる。2 回目の節に「### 検索計画」の見出しを(たとえ「この回は打たない」という文だけでも)置く指示がこの起動文に無く、予算が §2 の 1・2 で尽きた場合、「### ツール1件ごとの表」「### 出典」も実質空のまま §7 へ進む可能性がある。K3 は見出しの有無だけを見るので、置き忘れると §7 の受け入れ検査 1 で当たる。(P9)
>
> 6. [聞く] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:51-53`(§6 予算)— 「予算の 8 割に近づいたら…§7へ進む」は 1 回目の追補(`docs/DATA/delegations/20260923_tools_survey_cat8_addendum.md:45`「超えそうなら中断」)と同種の言葉による目安で、機械的な閾値検査(トークン数を都度確認する道具)は無い。1 回目は同種の指示(「超えそうなら」)のもとで実測 32.5 万トークン・約 3 割超過という実績があるにもかかわらず、2 回目もほぼ同じ強さの言葉による自己申告に留まっている。より強い導線(例: 一定間隔で消費を書き出させる、または §2 の 3・4 に入る前に明示的な残高確認を義務付ける)を置かなくてよいか。(P8)
>
> 7. [聞く] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:40-45`(§4 書き換えの範囲)— 「1 回目の節は書き換えない」は文章の指示としては明記されているが、これを機械的に確かめる手段(例: 返す前に round1 節のハッシュ値の一致を確認する)が §7(55-62 行)の返す前の手順に含まれていない。指示の遵守は調査班の自己申告に委ねられており、CLAUDE.md §0.2 O-3 型の「機構の挙動の主張には実行したコマンドと出力」に相当する検証手段が無い。検収の段階でリードが round1 節の不変を確かめる予定はあるか。(P8)
>
> 8. [直す] `/home/user/trade/docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:24`(qf-lib の E2)— 「`drop_consecutive_duplicates()` 以外の関数(欠け・外れ値・時刻などの検査)をソースで検索してから決める」とあるが、1 回目の検収 §11 問い 1 で監査役はすでに「原典を開けば読める状態にあり、なし寄りの判断材料が揃っていたのではないか」と指摘済み(§14 の 3)。今回の指示は「他の関数を探せ」とするのみで、探索の範囲(qf-lib の全ソースか、特定のモジュールか)や、見つからなかった場合に `未判別` のままにするか `なし` と確定するかの基準が書かれていない。
>
> 以上です。他に見つけた軽微な点はありません。

**リードの処置(コミット 03ed419、指紋 `366133e422fa`)**:
1: §3 に「`### 4.0 機械可読の表` を空にしない」と `scope()` の挙動を書き、§2 の 1 に「1 回目の §4.0 の表で値が変わる項目(少なくとも `qf-lib` の再現性)をこの回の §4.0 の表に新しい値で書く」を足した。これで訂正だけで終わった回でも、表に少なくとも 1 行ある。
2: 上の 1 と同じ箇条で塞いだ。
3: §2 の 1 に、4 軸の「推定」の外挿元 / 判定を言う文を問いの形に直して問いの一覧へ移す、の 2 つを足した。知見の表の行に URL が無い件は、§3 の知見の表の形(根拠の列が必須)で塞ぐ。
4: 辿る一覧の名前は候補の一覧に載せず、`### 辿る一覧から出た名前` に名前・URL・出た一覧だけを書かせる。リードが台帳に `未着手` で足す。
5: §3 に、この回の節に必ず置く小節の一覧(K3 の 6 つを含む)を足し、検索計画の節には「この回は打たない」と書かせる。
6: トークン数は調査班から見えないので、時間で区切る。開始と各段・各候補の前に `cat8_step.py --method budget … -- date -u` を打たせ、開始から 20 分を過ぎたら次に入らずに返させる。
7: §7 に、`git diff -U0 <報告> | grep '^-[^-]' | wc -l` で消えた行が 0 行であることを確かめて貼らせる手順を足した。リードも検収の §1 で確かめる。
8: 探す範囲を「導入済みの `qf_lib` パッケージ全体」とし、E2 の述語の 7 種それぞれに自分で語を選んで検索し(語を生ログに残す)、当たる関数が無いと読めたら `なし`(範囲と語を根拠に)、読み切れなければ `未判別`、とした。

## 監査 12 回目(11 回目の処置の確認。対象 = 03ed419、指紋 `366133e422fa`)

監査役の出力(逐語。[直す] 4 件・[聞く] 2 件。[止める] 0 件):

> 以下は起動文の実物(コミット 03ed419、指紋 `366133e422fa`)を実際に読み、`check_scan_report.py`・`cat8_ledger.py`・`cat8_step.py` を合成データ・実コマンドで打って確かめた結果です。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:66`(§7 の 4「git diff で消えた行 0」)— 実測: `git diff -U0 <path>` は引数なしだと working tree と index の差分であり、HEAD との差分ではない。合成リポジトリで、round1 節相当の行を書き換えたうえで `git add` すると `git diff -U0 f.txt` は空(0 行)を返すが `git diff -U0 HEAD -- f.txt` は変更を検出した(実測)。起動文の §5(54 行)は「commit も push もしない」としか禁じておらず `git add` は禁じていない。調査班または経由するツールがこのファイルを一度でも `git add` すると、この検査は「1 回目の節は無傷」という偽陰性を返しうる。round1 の改変を検出することが目的のこの機械の意義が、想定外のステージングで崩れないか。(P11)
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:22`(§2 の 1 見出し「検収 §13 の (2)(3)(6)(7) と §8・§11」)と、検収文書 `2026-09-23_tools_scan_cat8_run2_prompt.md:33`(処置 3)— 監査 11 回目の指摘 3 は「§8・§11 を丸ごと引いているのに一部の項目しか箇条書きに落ちていない」だった。1回目の検収 §8 の Jev 印 5 件のうち、今回の処置で箇条化されたのは「出所の無い数」「判定を言う文」「4 軸の推定」の 3 件のみで、「必須項目『代替経路』が無い(`alt_route` missing)」と「委任文と報告の照合 1 件」の 2 件は、処置後の起動文にも処置欄自身の説明にも一度も現れない。§8 を見出しで丸ごと引く形(22 行)は変わっておらず、その中の一部項目が今回も落ちたままではないか。(P9)
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:40`(知見の表の形「根拠は URL と取得日か生ログの行」)— 検収文書の処置 3 は「知見の表の行に URL が無い件は、§3 の知見の表の形(根拠の列が必須)で塞ぐ」とするが、実測: この文言は元の委任文 `docs/DATA/delegations/20260922_tools_survey_prompt.md:60` と 1 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run1_prompt.md:36` に一言一句近い形で既に存在し、それにもかかわらず 1 回目の報告は知見表に URL の無い行を 3 件出した(1 回目の検収 §8)。同じ文言をそのまま「塞いだ」根拠に挙げているが、その文言は一度この欠陥を防げなかった実績がある。今回、この 3 件を名指しで直させる指示も、根拠の形式を機械的に確かめる新しい手立ても追加されていない。(CLAUDE.md §0.2 A-17「対策を記録するだけで終わらせる」)
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:35`(§2 の 4「辿る一覧」)と `:43`(§3「4.0 の表を空にしない」の直前の必須小節一覧)— 監査 11 回目の指摘 4 への対応で、辿る一覧から出た名前は `候補の一覧` から外し `### 辿る一覧から出た名前` という別小節に自由形式(`- \`名前\` — URL — 出た一覧`)で書かせる形にした。実測: `cat8_ledger.py check-elements`・`sync`(`NEW_RE` は `` `名前`(新) `` の形のみ拾う)・`check_scan_report.py` の `marked_names`/`read_table` のいずれも、この小節の形式には反応しない(意図どおり衝突しない代わりに、網羅性を確かめる機械が一つも無い)。5 つの一覧(dev.to・X・zenn・quantreo 等・検索計画 7)を実際に全部辿ったか、出た名前を漏れなく書いたかは、次回以降リードの目視確認だけに委ねられることになるが、この形で足りるか。(P8、A-11 目視判定)
>
> 5. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:58`(§6 予算、監査 11 回目の指摘 6 への対応)— `python3 scripts/cat8_step.py … --method budget …` は実測どおり打てる(`--method` は語彙制限の無い自由文字列)。ただし「開始から 20 分を過ぎていたら次に入らない」の判定は、調査班自身がログのタイムスタンプを目算で比較する自己申告のままで、機械的な打ち切りではない。1 回目は同種の言葉の目安(「超えそうなら中断」)のもとで 3 割超過した実績があるが、今回もその実績を踏まえた強制力の追加(例: 次の段に入る前に `cat8_step.py` の出力から経過分を計算させ、超過なら非ゼロ終了させる、等)は無い。指摘 6 が求めた「より強い導線」への答えとして、時間ベースの自己申告で十分と判断してよいか。(P8)
>
> 6. [直す・軽微] `docs/DATA/delegations/20260923_tools_survey_cat8_run2_prompt.md:35` — 辿る一覧の書式指示 `` `- \`<名前>\` — <URL…> — <出た一覧>` `` は、単一バッククォートのインラインコードスパンの中でバックスラッシュエスケープしたバックティックを使っているが、CommonMark のコードスパンはバックスラッシュエスケープを解釈しないため、レンダリング結果が意図通りにならない可能性がある。スクリプトの正規表現(`sync`/`check-elements`)には影響しないことは確認済みだが、表記自体の妥当性。
>
> 以下、機械的に再現・確認できたため指摘に至らなかった点(参考、指摘ではない): 監査 11 回目の指摘 1(`scope()` が §4.0 の表を持たない節を丸ごと検査から外す)は、2 節構成の合成報告で実測し、処置後は round2 節が正しく検査対象に残ることを確認。指摘 4 の書式(`- \`名前\` — …`)は `cat8_ledger.py check-elements`・`sync` の正規表現と衝突しないことを実測で確認。`git diff -U0 … | grep '^-[^-]' | wc -l` 自体の行カウント方式(`---` ヘッダ行を正しく除外)は健全であることを確認(上記 1 の「HEAD 指定漏れ」とは別の論点)。`jev_delegate.py plan --prompt` を現行コミットに対して再実行し、印 0 件を再現(検収冒頭の記載と一致)。指紋 `366133e422fa` は `sha256sum` の実測と一致。

**リードの処置(起動文の指紋 `e908af5ff4ed`。Jev の前段は印 0 件)**:
1: §7 の 4 を `git diff -U0 HEAD -- <報告>` にした。
2: 「代替経路が無い」は §2 の 1 の「この環境から不可」の箇条がそれであると明記し、該当が無ければ「なし」と書かせる。「委任文と報告の照合」は、返答で §2 の 1〜4 の進み具合を書かせる §7 の 5 がそれに当たる。
3: 根拠の形を**機械で見る**ようにした: `cat8_ledger.py check-elements` に、知見の表の行の形(`| # | 知見 | 印 | 根拠 |`)・印が 5 語・根拠に URL か生ログの参照があること、の検査を足した(合成の報告で当たることを確かめた。1 回目の報告に打つと 28 件当たる = 1 回目の知見の表が形に合っていないことを機械でも確かめた)。1 回目の URL の無かった 3 行は名指しで直させる。
4: 各一覧の最初に「一覧: <名前> — 開いた URL — 生ログの行」の 1 行を書かせ、`check-elements` の 1 行目に「辿る一覧から出た名前 N 行」を出すようにした。網羅(一覧に載っていた名前を全部書いたか)は機械では確かめられないので、リードが検収で一覧を開いて数えて突き合わせる(限界として残る)。
5: **自己申告にしない**: `cat8_step.py` に `--deadline` を足した。期限を過ぎていると手を打たずに生ログに「期限切れ」の 1 手を書き、終了コード 3 で止まる(期限の内と外の両方で打って確かめた)。起動文 §6 で、全部の手に `--deadline` を付けさせる。
6: 書式の指示を、コードの記号の中に記号を入れない文の説明に書き直した。
