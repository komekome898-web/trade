# 区分 1 の 9 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 9 回目の実行(2026-09-22)」(3118 行目以降)
生ログ: `docs/DATA/probes/20260922_tools_1_run9.log`(新規 391 行、31 手。復帰文字 0 個)

## 0. 無改変・検査

既存の 3117 行は **無改変**。過去の生ログも変更なし。リードが生ログ 7 本を渡して打ち直し、**13 本すべて 0 件**。
**調査班が閉じずに残した行は 0 件**(2 回連続)。当たった行(K11 84 件 / K13 1 件 / K2 2 件)は
**すべて中身を直して消しており、「誤検出」として閉じた行は 0 件**。K11 の直し方も、生ログに書き足すのではなく
参照の付け替えで、7 回目に決めた正しいやり方を踏襲している。

## 1. 抜き取りで見つかった中身の誤り — **1 件**

**「構築に要る git 依存 2 件がどこからも見えない」は誤り。2 件のうち 1 件は公開されている。**

- `Nwagbara-Group-LLC/databaseschema` は `raw.githubusercontent.com` の `main` と `master` の両方で
  `Cargo.toml` が **HTTP 200**。`license = "FSL-1.1-ALv2"` と説明文まで読める。
  `ungh.cc` も**打ち直すと 200**(調査班の 1 回目は接続の失敗 = code 000 で、404 ではなかった)。
- **見えないのは `LoggingEngine` だけ。**リードが綴りの揺れ 5 通り × 2 枝を打ち直し、**全部 404**。
- **「構築できない」という結論は変わらない**(`LoggingEngine` が無ければ構築は通らない)。
  変わるのは**根拠**で、「2 件が見えない」ではなく「1 件が見えない」。**本文 3 箇所を訂正した。**
- 原因は「接続の失敗(000)を 404 と同じに扱った」こと。**規則**: 応答が 000 のときは 1 回打ち直す。
  404 と 000 を同じ欄に書かない。

## 2. 抜き取りのその他 — **一致**

| 行 | 取り直した結果 |
|---|---|
| Luczinsritter / 星 0・fork 0・watchers 0 | 一致 |
| Luczinsritter / ライセンスが確定できない | **一致**。LICENSE は main も master も 404。README のバッジは MIT、本文は「Personal project — free to use and modify for educational or demonstration purposes.」 |
| PySystemtrade / 保守者 30 名、先頭 robcarver17・bug-or-feature・tgibson11 | 一致 |
| PySystemtrade / 依存の一覧(pandas==2.1.3 ほか) | 一致 |
| PySystemtrade / 注文の型に `quantity` と `limit_price` | **一致**。`simple_orders.py:14-15` に `quantity: int` と `limit_price: float = None` |

## 3. 調査班が挙げた「原文に無い判断」の判定

1. **`PySystemtrade` の取得が 880 MB になり、§6-6 に触れた → 半分はリードの責任。**
   リードの起動指定が「部分取得は 3.6 MB で済みます」と書きながら、**どの旗を使うかを書かなかった。**
   6 回目に効いたのは `--filter=blob:none --sparse` で、`--sparse` が無いと作業木の取り出しで同梱データが落ちてくる。
   **規則**: 取得の大きさを**測ってから**進む。clone は `--no-checkout` か `--sparse` を必ず併用する。
   リードの起動指定は**旗をそのまま書く**。空きは片付け後に 12G に戻っている。
2. **最小実行に道具の同梱標本データを使った(§5-4 の「合成データだけ」の読み替え)→ 受け入れる。**
   §5-4 の狙いは**当方のデータ・鍵・記録を試行に使わせない**ことで、道具が自分で配っている標本はそれに当たらない。
   **ただし、それで示せるのは「道具が自分のデータで動く」ことだけである。**
   `当方データ投入` の欄が「csv の入れ物は在るが列の形は先物向け」と書き分けてあることを確認した。**書き分けは正しい。**
3. 予算の 20 分に対し 22 分。**超過は 2 分。**記録して次へ。

## 4. リードの判定

- **受け取る。中身の誤りは 1 件**(上の §1)。3 回続いた 0 件が途切れた。
- **確定した状態が 4 件**(この回の狙い):
  - **`PySystemtrade` = 導入して最小実行まで通した。**注文の型に `limit_price` を最初から持ち、指値専用の
    注文模擬 `hourly_limit_orders.py` を同梱(**未実行**)。同梱の先物データ 252 銘柄。発注の実体も在る(IB 経由)。
  - **`Luczinsritter` = 導入して最小実行まで通した。**ただし**指値の概念が無く**、`print_wealth` が
    空売りの建玉を足し算する**欠陥を実測**。**ライセンスが確定できない**(バッジと本文が食い違う)。
  - **`Mendl-Labs/BacktestingCore` = 構築できない**(依存 1 件が非公開)。ライセンスは Functional Source License 1.1 で、
    GitHub の説明文の "Open-source" と食い違う。
  - **`DeviaVir/zenbot` = 危険なので止めた**(8 回目で確定)。
- **区分 1 は未完了。**残るのは Superalgos / OpenTrader / CryptoSignal / Bot18 / `OctoBot` の最小実行 /
  `PySystemtrade` の `hourly_limit_orders.py`。検索計画 6 本は未実行。
