# 監査記録: 「値段の続き」の方策 + 全部逆張りの最適化の設計(`SIGNAL_VALUE_DESIGN_2026-09-21.md`)

## 1 回目(2026-09-21、対象コミット e4010bd = 設計の初版)

前段(jev_check): 印 7 件(negative_claim 3 件、symbol_definition 2 件、universal_claim 1 件、verification_claim 1 件)。

監査役の出力(逐語。[止める] 3 件・[直す] 1 件・[聞く] 2 件):

> 1. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:108-110(見出し「10. 設計の段の判定」)— rank-covariates の表(ii)が本文に存在しない。理由として書かれているのは「rank-covariates は使わない(対照を作らない = §6)」の1行のみ。§6 が指しているのは「対照(清算の無い時刻)という比較群を作らない」という決定であり、rank-covariates が扱う「判定の量が構造的に依存する変数(対照で合わせるべき変数)」の欠如を正当化するものではない — Q3(189組のグリッド探索)や Q4(6本の方策の並置比較)には連鎖の大きさ・位置・日といった構造変数が現に存在する。research-protocol §1 は「(i)(ii) の出力…を見出しで事前登録に貼る。無ければ監査役が止める」と明記しており、rank-covariates の欠落に対する「対照群を作らないから」という理由は、この監査に与えられた検査項目(9)が要求する「対照が『合わせる』の変数を合わせていない理由」への答えになっていない。この問いに答えないまま次段(段1委任)に進んでよいか。
>
> 2. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:32、:70 — 「この環境からは取れない」「この環境には無い」という否定的主張が、docs/DATA.md:69(2026-09-13 時点、同じパス `data/tardis/bitflyer_FX_BTC_JPY_trades/` に対する記録)の「取得済」と矛盾する。DATA.md:44 は「取得済」を「この環境のディスク上に実体があることを ls で確認した」と明確に定義しており、「PC に未共有の可能性」とは別のカテゴリである。ところが設計文書は DATA.md の「取得済」を「PC にある」と読み替えて(:70「`docs/DATA.md` §2 に『取得済』、**PC にある**」)、この環境での不在の根拠にしている。この読み替え(「取得済」の意味の付け替え)と、環境がいつ・なぜこのデータを失ったか(セッション間でgitignore済みの `data/` が失われる仕様か、単なる見落としか)についての調査が本文に無い。この矛盾を確認しないまま、PC 側の追加手順(§4「PC で集計の道具を1回走らせる」)を要求してよいか。
>
> 3. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:70 — 「取り直しも不可」の根拠は `curl` によるバイト範囲リクエスト1回・HTTP 403のみ。このデータを実際に取得した正規の手段 `scripts/fetch_tardis_samples.py`(同じ `datasets.tardis.dev/v1/bitflyer/trades/...` エンドポイントを叩く既存スクリプト。リポジトリに現存)自体を再実行して確かめた記録が無い。1つの経路の1回の失敗(この環境の代理経由の curl)を「取り直しも不可」に一般化していないか。DATA.md が「取得済」と記録した 2026-09-13 と今回(2026-09-21)の間で、単に一時的なネットワーク/代理の問題である可能性を排除したか。
>
> 4. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:70 — 「Tardis の利用規約 9.2 により生データは共有しない」という主張に一次資料(URL・取得日)が付いていない。`docs/DATA.md` を grep しても「利用規約」「9.2」の言及はこの節に見当たらない(DATA.md の別の 9.2 節は無関係な「ベニューそのものが消える」の節番号)。この主張の出所は一次資料・実測・仮定のどれか(research-protocol §1「各数値の出所を3分類で明記」)。この主張は「PC でしか生データを扱わない」という運用手順全体の根拠になっている。
>
> 5. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:85 — 「これは探索段の格子であり改良ループ(§0.3)ではない。189組は多重性に全部算入する」という分類。§5 の189組は、前段(SIGNAL_POLICY_RESULT)の4つの異なる「なぜ」由来の知見(位置・入る条件・出口・損切り、いずれも前段の分解結果から個別に導かれた仮説)を同時に1つの格子に組み込んだ設計であり、research-protocol §0.3「改良は『なぜ』から導かれていなければならない。1回の改良は1つの機構仮説に対応させる」という改良ループの定義とどう区別されるのか、その区別の理由が本文に書かれていない。「探索段」と呼ぶことで、改良ループに課される「1周1機構仮説」「収束しなければ止める」等の縛りを外れているように見えるが、これは意図した整理か。
>
> 6. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:108-206 と config/jev_design_examples/signal8_observables.yaml — jev_design の候補観測量11本は、この文書の Q0〜Q6 で計算予定の量とほぼ1対1で対応している(例: `per_cascade_policy_pnl_net_of_cost` の定義文はそのまま Q2 の説明文と同一)。候補観測量の作成が本設計の Q0〜Q6 の確定より前に独立に行われたか、それとも計画済みの量に事後で観測量名を当てはめたものか。後者であれば、上位にランクされる代理量は「計画と一致すること」を確認しているに過ぎず、jev_design が本来担うはずの「意図との整合を独立に検査する」機能を果たしていないのではないか。

リードの処置(1 件ずつ):

1. **直した。** `rank-covariates` を Q4 の量(後半 2,000 本の方策ごとの費用込み損益)× 変数 10 本(`config/jev_design_examples/signal8_covariates.yaml`)で走らせ、表 (ii) を §10 に貼った。「合わせる」= 費用 c・遅れ d の 2 本 → 全方策に同じ値を使うことで揃える。「合わせなくてよい」8 本のうち位置・大きさ・側・日は層別して出す。§10「リードの読み(ii)」に書いた。
2. **直した(監査役が正しい)。** この実行環境は会話ごとに clone から作られ、gitignore 域の `data/` は残らない(この会話の容器の `ls data` → `jev` のみ)。09-13 の「取得済」は別の容器での確認で、記録どおり。初版の「PC にある」は DATA.md の「取得済」の意味の付け替えだった。`docs/DATA.md` 69 行目に追記し、設計 §4 を書き直した。PC 側の手順は**不要になった**(3 を参照)。
3. **直した(監査役が正しい。O-2 の断定)。** 正規の `scripts/fetch_tardis_samples.py` を再実行 → 2023-07〜2024-10 の 16 標本日すべて取得できた(`16/16 days ok`、9.7 MB)。初版の 403 はバイト範囲要求(`-r 0-1023`)への拒否で、1 回の別経路の失敗を「取り直しも不可」に一般化した誤り。§4 に実測のコマンドと出力、誤りの経緯を書いた。
4. **直した。** 一次資料 `https://docs.tardis.dev/legal/terms-of-service`(2026-09-21 取得)の 9.2 を逐語で §4 に引用した(生データの再配布禁止、10 分以上の集計の派生データは可)。初版の出所はスクリプトの docstring で、一次資料ではなかったと明記。
5. **答えた(§5 に追記)。** 意図した整理。§0.3 は事前登録した判定のあとの改良に掛かる縛りで、この単位は判定前の探索段。ただし趣旨は守る: 4 ノブ = 4 仮説を格子で同時に測るのは出口 × 損切りのように打ち消し合う組があるためで、代わりに Q3 で 1 ノブずつの寄与を出す(どの仮説が効いたかを 1 本ずつ言える)。格子は 1 回だけ、規則を満たす組が無ければ「無い」で止め、水準を足して再走行しない。189 組は多重性に全部算入。
6. **答えた(§10 に事実を書いた)。** 事後だった(Q0〜Q6 を書いてから候補を英語にした)。計画外の候補 7 本を足して再実行し、その表に置き換えた。計画外はどの意図でも「無関係」で上位は変わらなかったが、これは「候補を先に独立に作った場合と同じ検査ではない」と明記した。次の単位から候補を Q より先に書く。
