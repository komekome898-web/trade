# 監査記録: 「値段の続き」の方策 + 全部逆張りの最適化の設計(`SIGNAL_VALUE_DESIGN_2026-09-21.md`)

## 1 回目(2026-09-21、対象コミット e4010bd = 設計の初版)

前段(jev_check): 印 7 件(negative_claim 3 件、symbol_definition 2 件、universal_claim 1 件、verification_claim 1 件)。

監査役の出力(逐語。[止める] 3 件・[直す] 1 件・[聞く] 2 件):

> 1. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:108-110(見出し「10. 設計の段の判定」)— rank-covariates の表(ii)が本文に存在しない。理由として書かれているのは「rank-covariates は使わない(対照を作らない = §6)」の1行のみ。§6 が指しているのは「対照(清算の無い時刻)という比較群を作らない」という決定であり、rank-covariates が扱う「判定の量が構造的に依存する変数(対照で合わせるべき変数)」の欠如を正当化するものではない — Q3(189組のグリッド探索)や Q4(6本の方策の並置比較)には連鎖の大きさ・位置・日といった構造変数が現に存在する。research-protocol §1 は「(i)(ii) の出力…を見出しで事前登録に貼る。無ければ監査役が止める」と明記しており、rank-covariates の欠落に対する「対照群を作らないから」という理由は、この監査に与えられた検査項目(9)が要求する「対照が『合わせる』の変数を合わせていない理由」への答えになっていない。この問いに答えないまま次段(段1委任)に進んでよいか。
>
> 2. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:32、:70 — 「この環境からは取れない」「この環境には無い」という否定的主張が、docs/DATA.md:69(2026-09-13 時点、同じパス `data/tardis/bitflyer_FX_BTC_JPY_trades/` に対する記録)の「取得済」と矛盾する。DATA.md:44 は「取得済」を「この環境のディスク上に実体があることを ls で確認した」と明確に定義しており、「PC に未共有の可能性」とは別のカテゴリである。ところが設計文書は DATA.md の「取得済」を「PC にある」と読み替えて(:70「`docs/DATA.md` §2 に『取得済』、**PC にある**」)、この環境での不在の根拠にしている。この読み替え(「取得済」の意味の付け替え)と、環境がいつ・なぜこのデータを失ったか(セッション間でgitignore済みの `data/` が失われる仕様か、単なる見落としか)についての調査が本文に無い。この矛盾を確認しないまま、PC 側の追加手順(§4「PC で集計の道具を1回走らせる」)を要求してよいか。
>
> 3. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:70 — 「取り直しも不可」の根拠は `curl` によるバイト範囲リクエスト1回・HTTP 403のみ。このデータを実際に取得した正規の手段 `scripts/fetch_tardis_samples.py`(同じ `datasets.tardis.dev/v1/bitflyer/trades/...` エンドポイントを叩く既存スクリプト。リポジトリに現存)自体を再実行して確かめた記録が無い。1つの経路の1回の失敗(この環境の代理経由の curl)を「取り直しも不可」に一般化していないか。DATA.md が「取得済」と記録した 2026-09-13 と今回(2026-09-21)の間で、単に一時的なネットワーク/代理の問題である可能性を排除したか。
>
> 4. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:70 — 「Tardis の利用規約 9.2 により生データは共有しない」という主張に一次資料(URL・取得日)が付いていない。`docs/DATA.md` を grep しても「利用規約」「9.2」の言及はこの節に見当たらない(DATA.md の別の 9.2 節は無関係な「ベニューそのものが消える」の節番号)。この主張の出所は一次資料・実測・仮定のどれか(research-protocol §1「各数値の出所を3分類で明記」)。この主張は「PC でしか生データを扱わない」という運用手順全体の根拠になっている。
>
> 5. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:85 — 「これは探索段の格子であり改良ループ(§0.3)ではない。189組は多重性に全部算入する」という分類。§5 の189組は、前段(SIGNAL_POLICY_RESULT)の4つの異なる「なぜ」由来の知見(位置・入る条件・出口・損切り、いずれも前段の分解結果から個別に導かれた仮説)を同時に1つの格子に組み込んだ設計であり、research-protocol §0.3「改良は『なぜ』から導かれていなければならない。1回の改良は1つの機構仮説に対応させる」という改良ループの定義とどう区別されるのか、その区別の理由が本文に書かれていない。「探索段」と呼ぶことで、改良ループに課される「1周1機構仮説」「収束しなければ止める」等の縛りを外れているように見えるが、これは意図した整理か。
>
> 6. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:108-206 と config/jev_design_examples/signal8_observables.yaml — jev_design の候補観測量11本は、この文書の Q0〜Q6 で計算予定の量とほぼ1対1で対応している(例: `per_cascade_policy_pnl_net_of_cost` の定義文はそのまま Q2 の説明文と同一)。候補観測量の作成が本設計の Q0〜Q6 の確定より前に独立に行われたか、それとも計画済みの量に事後で観測量名を当てはめたものか。後者であれば、上位にランクされる代理量は「計画と一致すること」を確認しているに過ぎず、jev_design が本来担うはずの「意図との整合を独立に検査する」機能を果たしていないのではないか。

リードの処置(1 件ずつ):

1. **直した。** `rank-covariates` を Q4 の量(後半 2,000 本の方策ごとの費用込み損益)× 変数 10 本(`config/jev_design_examples/signal8_covariates.yaml`)で走らせ、表 (ii) を §10 に貼った。「合わせる」= 費用 c・遅れ d の 2 本 → 全方策に同じ値を使うことで揃える。「合わせなくてよい」8 本のうち位置・大きさ・側・日は層別して出す。§10「リードの読み(ii)」に書いた。
2. **直した(監査役が正しい)。** この実行環境は会話ごとに clone から作られ、gitignore 域の `data/` は残らない(この会話の容器の `ls data` → `jev` のみ)。09-13 の「取得済」は別の容器での確認で、記録どおり。初版の「PC にある」は DATA.md の「取得済」の意味の付け替えだった。`docs/DATA.md` 69 行目に追記し、設計 §4 を書き直した。PC 側の手順は**不要になった**(3 を参照)。
3. **直した(監査役が正しい。O-2 の断定)。** 正規の `scripts/fetch_tardis_samples.py` を再実行 → 2023-07〜2024-10 の 16 標本日すべて取得できた(`16/16 days ok`、9.7 MB)。初版の 403 はバイト範囲要求(`-r 0-1023`)への拒否で、1 回の別経路の失敗を「取り直しも不可」に一般化した誤り。§4 に実測のコマンドと出力、誤りの経緯を書いた。
4. **直した。** 一次資料 `https://docs.tardis.dev/legal/terms-of-service`(2026-09-21 取得)の 9.2 を逐語で §4 に引用した(生データの再配布禁止、10 分以上の集計の派生データは可)。初版の出所はスクリプトの docstring で、一次資料ではなかったと明記。
5. **答えた(§5 に追記)。** 意図した整理。§0.3 は事前登録した判定のあとの改良に掛かる縛りで、この単位は判定前の探索段。ただし趣旨は守る: 4 ノブ = 4 仮説を格子で同時に測るのは出口 × 損切りのように打ち消し合う組があるためで、代わりに Q3 で 1 ノブずつの寄与を出す(どの仮説が効いたかを 1 本ずつ言える)。格子は 1 回だけ、規則を満たす組が無ければ「無い」で止め、水準を足して再走行しない。189 組は多重性に全部算入。
6. **答えた(§10 に事実を書いた)。** 事後だった(Q0〜Q6 を書いてから候補を英語にした)。計画外の候補 7 本を足して再実行し、その表に置き換えた。計画外はどの意図でも「無関係」で上位は変わらなかったが、これは「候補を先に独立に作った場合と同じ検査ではない」と明記した。次の単位から候補を Q より先に書く。

## 2 回目(2026-09-21、対象コミット 9f773ac)

前段(jev_check): 印 9 件(negative_claim 2、symbol_definition 4、universal_claim 1、verification_claim 2)。

監査役の出力(逐語。[止める] 1 件・[直す] 5 件・[聞く] 5 件):

> 1. [止める] SIGNAL_VALUE_DESIGN_2026-09-21.md:112-114 — 「候補の作り方」で、2 回目もリード自身が「1 回目の候補 11 本は Q0〜Q6 を書いたあとに量を英語にして並べたもので独立ではなかった」「2 回目は計画外 7 本を足して再実行したが、これは『計画の量より意図に近い量が候補の中に無かった』ことしか言わず、候補を先に独立に作った場合と同じ検査ではない」「次の単位からは候補を Q より先に書く」と明記している。1 回目監査の指摘 6(候補観測量が Q0〜Q6 と事実上 1 対 1 で、独立検査になっていない)が、2 回目でも本質的には解消されておらず、是正を「次の単位」に先送りしたまま本設計を進めてよいとしている。L-242 が要求する jev の設計段判定は「候補観測量を目的の逐語に独立に対して順位付けする」ことが前提のはずだが、この前提が今回も満たされていないと文書自身が認めた状態で、この設計を「判定は済んだ」として次段(委任)に進めてよいか(research-protocol、L-242)。
>
> 2. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:55 — Q1 の「併記: 前段の 5,000 件(後半、既に読んだ出力の再集計)での較正表」は、前段が**清算ラベル**で較正した後半 5,000 件に、この単位で新設した**値段のラベル**(`value_continuation_60`)を当てて集計し直すもの。冒頭(:3)の「11 度目の読み」は「①と②を同じ後半 2,000 本に一度だけ通す」ことだけを数えており、この 5,000 件・新ラベルでの集計は数え方に入っていないように見える。既に読んだ「出力」の再集計ではなく、後半データに新しい列(値段のラベル)を初めて当てる「読み」ではないか。(P1 に近い多重性/読み回数の会計の問い)
>
> 3. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:85 — 「この単位は判定の前の探索段(§0.0)にあり、§0.3(改良ループ)の縛りは掛からない」という分類は、1 回目監査の指摘 5 に対しリード自身が答えたもの。§0.1 の対応表(:18-34)で右列が埋まっているのは①・②を進める指示だけで、「探索段として§0.3 を外れてよいか」という枠組みの分類自体にオーナー逐語は無い。CLAUDE.md §0.2 A-13(「枠組みはオーナーに見せてから進める」)に照らし、この分類はこの設計を見せる際にオーナーへ明示的に確認すべき項目ではないか。
>
> 4. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:45, 211-232(V-5、印△) — jev の rank-observables(V-5)で 2 位の `pnl_on_bitflyer_prices`(代理)はこの単位では測定しない(§8 限界(1))。理由は「経路差は本単位では測らない」という範囲宣言のみ。V-5 の原文逐語「実際の運用で使うシグナルとしての価値」に対し、上位代理量を測らないまま△のまま進めることの十分性(範囲宣言で足りるか、bitFlyer 価格での再計算を先に済ませるべきか)は文書内で判断されていない。
>
> 5. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:46, 234-255(V-6、印△) — jev の rank-observables(V-6)で 2 位の `jev_judgment_on_value_label`(代理)は「Jev は呼ばない…①の答えが出てから決める」として先送りされている。V-6 の原文逐語「清算を検知→jev でこの清算は続くか判断」はこの単位の主眼(①=logistic による代理実装)そのものの意図であり、Jev を呼ぶ判断を次の単位へ先送りし続けることは、意図マップの△が解消されないまま複数単位にわたって繰り越される構造にならないか。
>
> 6. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:48 — 「△ の扱い(§5.2)」という節番号参照。この文書に §5.2 は無く(§5 は「② の最適化」)、`research-protocol` にも §5.2 という番号の節は無い(該当箇所は無番号の「## 5.」)。この参照は何を指しているか。
>
> 7. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:64, 70, 79, 93 — 「材料」という語が、①logistic の入力特徴の組(:64)、②実効スプレッド算出用の tardis 生データ(:70)、③ノブの入場条件に使う個別特徴(:79「材料12」「材料14」「材料15」)、④特徴量ファイルの中身(:93)という、少なくとも 4 つの異なる対象に使われているが、冒頭の「用語」表(:5-16)にこの語の定義は無い。読者は各節の文脈だけで指示対象を判別する必要がある。
>
> 8. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:64 vs :79 — §3 の材料リスト(1 件目 10 本 = 14・8・C3・C4・11・A6・R1・9・15・N2、連鎖の中 11 本 = F5・1・F3・14・A6・C4・15・11・C3・8・2)はこの文書内で定義されず「前段 R2.1 と同じ組」とのみ書かれている。一方 §5 は「材料12」「材料14」「材料15」を具体式で定義している(:79)。この文書だけを読んで、§3 の「14」と §5 の「材料14」が同じ量であることを確認できるか。
>
> 9. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:119-256(§10 各表の「順位」列) — 順位 1〜18 の数字(特に「14」「11」「15」「12」「8」「9」)が、§3・§5 で「材料14」等として使っている番号と偶然一致する。文書内でこの 2 つの数字体系(jev 順位番号と材料番号)を明示的に区別する記述が無い。
>
> 10. [直す] SIGNAL_VALUE_DESIGN_2026-09-21.md:106 — §9 の多重性見積もりで Q2 の内訳「84 + 42 + 10」の「84」が、本文の Q2 の記述(7 方策 × 遅れ 3 × 費用なし/費用あり(2通り))から直接は導けない(7×3×3=63、7×3×2=42 等はあり得るが 84 にはならない)。この「84」の内訳(何 × 何)を明記できるか。
>
> 11. [聞く] SIGNAL_VALUE_DESIGN_2026-09-21.md:83 — 選ぶ規則 (a)(b) を満たす組が無い場合「②opt は全部逆張り(素)のまま後半へ」となる。この場合 Q4 の 6 本の並びで②opt と全部逆張り(素)が同一の数値になり、実質 5 本の比較に縮退する。この縮退が起きた場合の報告の書き方(「②opt は選べなかった」と明記するか)は本文に決まっていない。

リードの処置(1 件ずつ):

1. **直した(今の単位で)。** 設計を見ていない下位モデル(sonnet)に逐語 V-1〜V-6 と背景 1 段落だけを渡し、候補 15 本を独立に作らせた(`config/jev_design_examples/signal8_observables_independent.yaml`、逐語のまま)。それで rank-observables を走らせ直し(3 回目、問い 90 件)、§10 をその表に置き換えた。独立の候補の上位が設計のどの問いに当たるかを §10 に 1 本ずつ書き、当たっていなかった 3 本を設計に足した(Q4 の対差の分布・側別、Q1 の到達時間の分布)。
2. **答えた(冒頭と §7 を直した)。** 規則(「生データから新しい量を出したか」)の上では既存の列の再集計だが、監査役の言うとおり後半に新しい較正表を当てる行為なので、**11 度目の読みに含め、段 2 で行う**(段 1 では触れない)。Q5b も同じ扱い。
3. **答えた(§0.1 に行を足した)。** 探索段としての分類はリードの決定で逐語が無い → §0.1 の空の行に「探索段として扱い、格子を 1 回で測る」を足し、オーナーに見せる項目にした(空の行 4 → 6)。
4. **直した(Q5b を足した)。** bitFlyer の価格は 456 日のうち tardis 標本日 16 日にだけある。その日に掛かる連鎖(後半 2,000 本のうち 81 本、前半 285 本。リード実測)で、bitFlyer の約定で損益を計算し直して Binance の損益と対で比べる下見を Q5b として足した。456 日全体には広げない(価格が無い)。V-5 は △ のまま、理由を §1 に書いた。
5. **答えた(§1 V-6 を直した)。** 先送りの理由はリードではなくオーナー逐語 L-321「codeでの判断が勝てるならjevを使う意味はありません」。繰り越しの条件を書いた: ① の code が後半で全部逆張り(素)に届かなければ、次の単位で Jev を値段のラベルに当てるかをオーナーに問い、△ をそこで ○ か ✕ にする。
6. **直した。** `CLAUDE.md` §5.2「陰性と不明を分ける」と明記。
7. **直した。** 用語表に「材料」を定義し(ts 以前に計算できる量、番号は前段 §3.1、記号は `SIGNAL_MATERIALS_DESIGN`)、費用の入力データとファイルには「材料」を使わないよう言い換えた。
8. **直した。** §3 に番号 1・2・8・9・11・12・14・15 の定義(`rows_continue.csv.gz` の列名)を書き、§5 と同じ体系と明記。
9. **直した。** §3 と §10 に「順位」列は材料の番号と無関係と書いた。
10. **直した。** 84 = 方策 7 × 型 2 × 遅れ 3 × 含める / 含めない 2。費用あり表は遅れ 1 秒だけと決めて内訳を書き直し、合計を約 564 → 約 669 に更新(Q1・Q4・Q5b の追加分を含む)。
11. **直した。** 規則を満たす組が無いときは Q3 に「規則を満たす組は無かった」と書き、②opt の行は作らず(素のまま複製しない)、Q4 は 5 本の比較としてその旨を見出しに書く。規則を緩めて選び直さない。

## 3 回目(2026-09-21、対象コミット 762059b)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件・[聞く] 2 件):

> 1. [止める] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:105 — §8 限界(2)「値段のラベルの割合が半期でずれる(前半 0.548 / 後半 0.469、リード実測)」の「後半 0.469」は、同一系列の `REFUTER_REVIEW6_2026-09-20.md`(:131「後半の基準(0.6246)」、:146「プリント 0.6246」、:300「割合は前半 0.5476 / 後半 0.6246」、:338、:378)が繰り返し記録している「後半の値段の続きの基準率 0.6246」と大きく食い違う(前半側は 0.548 ≈ 0.5476 でほぼ一致するのに後半側だけ 0.469 対 0.6246 で方向まで逆)。同じ量(値段の続きラベルの後半基準率)を指しているように見えるが、母集団の違い(件数の切り方など)が本文に書かれていないため、どちらが正しいか、あるいは別の量なのかを確認できるか。この数値は Q1 の帯較正の妥当性の限界説明そのものに使われている(P2、P1)。
>
> 2. [止める] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:113-119、config/jev_design_examples/signal8_observables_independent.yaml:1-17 — 「候補観測量は設計を見ていない下位モデルにオーナー逐語 V-1〜V-6 と『背景 1 段落』だけを渡して独立に作らせた」と主張しているが、その「背景 1 段落」の逐語はこの設計にも yaml のコメントにも引用されていない。一方で生成された候補の文面には「2,000 sampled test-split chains」「optimized on the train split」「entry offset, exit/hold rule」「jev's stop/continue/unsure call」など、V-1〜V-6 の逐語だけからは出てこない(§5 で確定済みの 4 ノブ・train/test 分割・後半 2,000 本という)この設計固有の実装詳細と一致する語彙が複数現れている。「背景 1 段落」に §5 相当の設計の中身が含まれていたのではないか、含まれていたなら独立生成の意味を成さないのではないか。1 回目監査の指摘 6・2 回目監査の指摘 1 が問題にした「候補が計画と一致することを確認しているだけ」という懸念が、根拠となる入力テキストが非公開のまま再演していないか確認できるか(監査指摘 9・research-protocol、O-3 に近い検証可能性の欠如)。
>
> 3. [直す] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:89, 119, 128-129 — §5「総収支を物差しにする理由: … 1 本あたりの平均だけで選ぶと機会が消える組が勝つ」と明記して②の最適化基準を「合計」に固定しているが、§10「リードの読み(i)」で V-2 の直接候補として挙げている `all_reverse_optimized_pnl_per_chain`(定義は "Mean net PnL … per chain")は連鎖あたりの平均であり、設計が退けた物差しと同じ形をしている。この不一致(独立候補の上位が「平均」を測る量である一方、設計は「合計」を選んでいる)への言及が §10 に無い。判定に使う量が候補の上位の形と食い違う理由を書く必要があるか(監査指摘 9)。
>
> 4. [直す] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:121-122、244-257 — rank-covariates の「合わせなくてよい」8 本のうち `pre_volatility_60s`・`cascade_notional`・`time_of_day` の 3 本は、直後の「リードの読み(ii)」でも本文のどこでも触れられていない(言及されているのは 位置・大きさ・側・日・半期基準率の 5 本のみ)。この 3 本が Q0〜Q6 のどこにも現れない理由(層別する必要が無いと判断したのか、単に書き漏れか)は書かれているか。
>
> 5. [聞く] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:9 — 用語表の「値段の続き」の説明に付いている「(後半の一致 79.16%、反証者 6 示唆 6)」という数値に、一次資料・実測・仮定のいずれかの分類タグが付いていない(同じ文書の §8(2)の隣接する数値には「リード実測」という印がある)。この 79.16% はどの分類か明記できるか。
>
> 6. [聞く] docs/PHASE2/O3C/SIGNAL/SIGNAL_VALUE_DESIGN_2026-09-21.md:113-115 — 「1 回目(候補 11 本)と 2 回目(+ 計画外 7 本)は独立ではなかった」と自認したうえで「3 回目は独立」としているが、2 回目までの経緯(Q0〜Q6 の確定後に候補語彙を当てはめた)を知っているのは他ならぬリード自身であり、「背景 1 段落」の作文者もリードである。作文者と評価者が同一人物である場合の独立性の限界(生成物自体は独立でも、依頼文自体が非独立な設計者の手で書かれている)について、本文で自覚的に書かれているか。

リードの処置(1 件ずつ):

1. **直した(監査役が正しい。リードの数え間違い)。** 再計算: `rows_continue.csv.gz` を `kind` で分けると、後半のプリント 30,162 件の値段のラベルの割合は **0.6246**(前半 21,838 件は 0.5476)。初版の 0.469 は同じファイルの Q7 の対照候補 22,545 行(割合 0.262)を後半のプリントに混ぜた平均。用語表と §8 (2) を直し、誤りの経緯を書いた。方向が逆になったので限界の文も書き直した(後半の方が 8 ポイント高い = 「わからない」の帯が後半で「続く」側に寄り得る)。用語表の「後半 52,707 プリント」も同じ誤りで、30,162 に直した。
2. **直した。** 渡した依頼文の全文を `signal8_observables_independent.yaml` の冒頭に逐語で載せ、§10 に「背景に含まれていた設計の中身」(連鎖の定義・3 択と行動の形・前後半と 2,000 本・①②の一文)と「含めなかったもの」(4 ノブ・格子・選ぶ規則・費用・帯・Q0〜Q6)を分けて書いた。候補の語のどれが背景から来たかも書いた。
3. **直した。** §10 に、独立の候補の上位は「連鎖 1 本あたりの平均」で設計の物差しは「合計」であること、平均は報告し合計で選ぶ理由(§5)、これが §0.1 でオーナーに見せる「物差しの具体」の行に含まれることを書いた。
4. **直した。** `pre_volatility_60s`・`cascade_notional` は材料 11・12・15 として ① の入力と ② の条件に入っている(層別はしない)、`time_of_day`(材料 6)はこの単位では使わない(行数を増やさないリードの決定)と §10 に書いた。
5. **直した。** 79.16% に「後半のプリント 30,162 件、反証者 6 示唆 6 の実測」の印を付けた。
6. **書いた(§10)。** 独立なのは候補の文をリードが書いていないことだけで、依頼文(前後半・2,000 本・3 択の枠)はリードが書いた = 依頼文まで独立ではない。次からは背景をオーナーの逐語だけにする。
