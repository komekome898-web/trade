# 監査(owner-auditor)— 段 A **2 周目の事前登録**(起草版 c4d96f5)への **走行前 1 回目・12 件の処置**(2026-09-19)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_R2_PREREG_2026-09-19.md`。併せて 1 周目の報告と監査、1 周目の事前登録、設計 §3・§8、research-protocol §0・§1・§4・§8・§11、`bugcheck2.log`、CLAUDE.md §0.2。
前段(Jev、`scripts/jev_check.py audit`): 印 1 件(symbol_definition 1 件)。1 箇所を行番号で渡した。

## 監査役の返答(逐語)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_R2_PREREG_2026-09-19.md`(コミット c4d96f5、走行前監査1回目)。

以下は表計算・実データ照合(`backtest_data/o3c_reaction_20260918_{full,sample,judge}/...`)を含めて確認した結果の指摘。

1. [止める] `REACTION_R2_PREREG_2026-09-19.md:9-12`(§0) — F1′ は「1 周目の判定区間の観測(観測のみの表)を見てから立てた仮説」であり、それを**同じ判定区間**でそのまま検定にかける。`.claude/skills/research-protocol/SKILL.md` §8 の禁止事項 1「**自己汚染: 判定区間から仮説を生成 → 同じデータで検証。新鮮データが貯まるまで待ち、係属仮説として事前登録だけしておく**」に文字どおり該当する型ではないか。本書は「多重性 582 に算入」「独立な確認とは呼ばない」と明記して対処したつもりだが、多重性の補正(Bonferroni 系)は検定の**偽陽性率**を制御するものであり、「観測して合致する形の仮説を選んでから、同じデータでその仮説を確かめる」ことで生じる**推定量そのものの選択バイアス**(t 値・Δ_h の期待値が実際より仮説に有利な方向へ寄る)を補正しない。この区別が本文のどこにも書かれていない。§8 の禁止事項が求める代替策(フレッシュデータを待って係属仮説として登録するだけに留める)を取らなかった理由が書かれているか(A-6 / A-7、`research-protocol` §8-1)。

2. [止める] `REACTION_R2_PREREG_2026-09-19.md:70-71`(§7) — MDE の入力 s(標準偏差)を「標本 6 日の 対照 438 行」から取ると書くが、実データで確認すると **438 = control_uniform(225 行)+ control_matched(213 行)を D1 の切り値で絞らずに合算した数**である(`backtest_data/o3c_reaction_20260918_sample/gap60_w8/table.csv` を実測。`kind` の内訳 `{liq:224, control_uniform:225, control_matched:213}`、225+213=438)。一方、§3 が定義する「対照 C」は「`control_matched` で `doi_pre_1h` ≤ 切り値、相手が mixed の束でないもの」であり、同じ切り値(-14472.33333333332)で絞ると `control_matched` は **50 行**しか残らない(実測)。D1 側は同じ切り値で正しく絞った **67 行**(§7 の記載どおり、実測でも一致)。**つまり MDE の分散は「D1 で絞った 67 行」と「絞っていない・種類も違う 438 行」という異なる母集団から作られており、§3 が定義した対照 C(n2=4,101 の元になる集合)とは別物である。**この母集団の食い違いは MDE をどちらの方向にどれだけ歪めるか書かれていない。`research-protocol` §4.1 は「**この n・この分散で検出できる最小効果**」を要求しており、実際の検定対象と異なる分布から取った分散でその要求を満たしていると言えるか(A-12、P16)。

3. [直す] `REACTION_R2_PREREG_2026-09-19.md:70-71`(§7) — 仮に母集団の食い違いを措くとしても、MDE の計算式 `MDE_h = (z+0.8416)×sqrt(s_D1²/n1 + s_C²/n2)` は単純な 2 標本差の分散式であり、実際に判定に使う統計量 Δ_h(§3: 10 分位の帯ごとに D1 と C を比較し、束の重みで加重和を取る「距離をそろえた差」、SE_h は帯内での層別ブートストラップ)の分散構造(帯内の相関・重み付け)を反映していない。この近似が保守的(MDE を大きく見積もる)側か楽観的側かの注記が無い。

4. [直す] `REACTION_R2_PREREG_2026-09-19.md:9-12`(§0)・`68-71`(§7) — §0 の「改良点」の実証根拠として引用する `docs/DATA/probes/20260919_o3c_reaction_bugcheck2.log` の「距離をそろえた差 +0.003 / +0.015」は `reach_back_vwap_240m`(到達率、1 周目の判定指標)を標準化した結果であり、**2 周目の判定指標である `bp_reactdir`(§3 の `r_h`)を標準化した実測ではない**。§8 のサニティ #4 も同じ `reach_back_vwap_240m` で「標準化の道具」の検算をしているだけで、`bp_reactdir` 自体が同種の距離の交絡を受けるかどうかは 1 周目でも 2 周目の事前登録でも一度も測っていない。「改良点は 2 つだけ」という前提(距離標準化で 1 周目の欠陥が直る)が、実際に判定へ使う指標については未検証のまま置かれていないか(P3、P6)。

5. [聞く] `REACTION_R2_PREREG_2026-09-19.md:51-53`(§5) — 対照 C の `r_h` は「相手の束の side の符号」を借りて作る(§3)。`research-protocol` §4.2 は「対照群は**仮定を要求しないので、仮定を間違えようがない**」ことを対照群を使う根拠として挙げているが、符号を相手の清算から借りる操作自体が「同じ日の流れ(市場のトレンド・自己相関)が対照にも実群と同じ方向で乗る」という仮定を含んでいる。§5 は「それを差で引く」とだけ書き、差し引きで本当に相殺できる根拠(借用した符号のバイアスの向き・大きさ)を示していない。これは §4.2 が想定する「仮定を要求しない対照群」ではなく、"対照群の体裁を取った、仮定つきの比較" ではないか。

6. [直す] `REACTION_R2_PREREG_2026-09-19.md:89-91`(§9) — 「反証者の欄(§11)」という参照先が、本書自身には §11 が存在しない(本書は §0〜§10 のみ)ため、`research-protocol` SKILL.md §11(反証者レビュー = **リードの説明文を渡さない独立サブエージェントに PREREG・コード・数表だけを渡し、致命が1つでもあれば保留**とする必須プロセス)を指しているのか判別できない。さらに §9 はこの「欄」に「『同じ日の流れ』を対照が拾い切れていない可能性を…見る」という**結論めいた中身を先に固定**しており、SKILL §11 が要求する独立レビューのプロセスと、結果表の 1 列としての「反証者の欄」が同じ語で別の物を指している疑いがある(P2、O-7)。

7. [聞く] 文書全体(§0.1 対応表の不在) — `CLAUDE.md` §0.1 が要求する「やろうとすること / オーナーの原文の該当語」の対応表がこの事前登録には無い。前段の設計文書 `REACTION_DESIGN_2026-09-18.md` §0.1 は同種の対応表を持っていた。本書の冒頭が根拠にする「L-232『はやくやれ』」は着手の速度を促す発言であり、F1′ という新しい機構仮説・6 セルの判定・Δ_h の標準化式という「枠組み」(A-13 がオーナーに見せてから進めるべきとする対象)への個別の承認語には読めない。この 2 周目の枠組みはオーナーに見せた上での承認か、それとも 1 周目の設計承認(L-196 等)を援用しているだけか。

8. [直す] `REACTION_R2_PREREG_2026-09-19.md:58`(§6) — 「不明(n2 < 30)(いずれかの帯で n < 30 なら、その帯を落とさず「不明」にする)」の括弧内注記が、直前の「不明(n < 30)」(D1 側 n1 の分岐)にも掛かるのか、直後の「不明(n2 < 30)」(対照側 n2 の分岐)だけに掛かるのか、文の並びから判別できない。§8 のサニティ #5「各帯の n ≥ 30(D1・C とも)」は両側を要求しているように読めるが、分岐の定義文としては片方の項目にしか注記が付いていない。

9. [直す] `REACTION_R2_PREREG_2026-09-19.md:59`(§6) — 「不明(6 セル全部が検出されず or 不明)」という定義は、1 周目 §7.0 が要求していた「不明の内訳(5 つの不明化けの語それぞれが何個か)の併記」(prereg 監査で複数回かけて追加された規約)を引き継いでいない。この詳細度の縮小に説明が無い(P13 に近い型: 縮小そのものは是正の代わりにならない、という以前に、縮小を明示していない)。

10. [直す] `REACTION_R2_PREREG_2026-09-19.md:70`(§7) — 「欲しい効果: 1 分で 2 bp、240 分で 5 bp」は「1 周目の観測 −7.6 / −15.0 より小さい値を置く」ことで正当化されているが、この −7.6 / −15.0 は**距離標準化前**の生の観測差(`REACTION_RESULT_2026-09-19.md` §7 の表)であり、2 周目で実際に判定する Δ_h は**距離標準化後**の量である。指摘 4 のとおり、標準化で効果が大きく縮む可能性(到達率の例では −0.065 → +0.003 まで縮んだ)がある以上、未標準化の値を基準に「欲しい効果」の上限を置く比較は、実際に測る量とスケールが異なる可能性があり、その旨の注記が無い(A-12 への自己防御は書かれているが、比較対象のスケール不一致には触れていない)。

11. [直す] `REACTION_R2_PREREG_2026-09-19.md:73-83`(§8) — 「読みの道具が自動で検査し、1 つでも破れたら表を書かない」と冒頭に書くが、サニティ #1〜#6 は期待値だけが書かれ、どの関数・テストが検査を実行するか、破れたときにどう止まるか(1 周目の `stopped.txt` のような機構)が明記されていない。#7(`_research_audit_gate.require_audit`)だけが実装への参照を持つ。1 周目のサニティ表(§11、テスト名・関数名を個別に明記)と比べて検証可能性が下がっている。

12. [直す] `REACTION_R2_PREREG_2026-09-19.md:59`(§6) — 1 周目 §7.0 は判定語を「**F1 と整合 / F1 の反証**」と F1 を明示して書いていたが、本書は「**整合 / 反証**」と F1′ を省略している。文脈上 F1′ を指すと読めるが、用語をそのまま引き継がず短縮しており、他の箇所(§10.3 相当のなぜの欄)でこの短縮形が別の仮説の判定語と混同されないかは要確認(軽微、P2 に近い型)。

**実測で確認し、問題が無かった項目(参考)**: D1 の切り値 −14472.33333333332(`backtest_data/o3c_reaction_20260918_judge/summary.json` の「F1 の D1 の上側の切り値(決定 2)」と一致)/ n1=5,048・n2=4,101(`f1_12cells.csv` と一致)/ 主表 `table.csv` の行数 60,903・MD5 `80be954122d877a0809db703e50460e8`(実測一致)/ D1 67 行(標本 6 日データに凍結した切り値を当てて実測、一致)/ セルの分岐 7 語の語彙(1 周目と同一集合)。

## リードの応答(1 件ずつ)

### 指摘 1
**上申(止める)。** 指摘のとおり research-protocol §8-1 の型で、多重性の補正は推定量の偏りを直さない。この区別を §0.2 に逐語で書いた。代替策(フレッシュデータを待つ)を取らなかった理由 = フレッシュな清算データが公開経路に無い(実測 2026-09-19、`docs/NEGATIVE_FACTS.md` N-012: data.binance.vision の COIN-M `liquidationSnapshot` は 2024-10-13 まで、以後 404、USD-M 無し。他の経路は未確認)。**2 択(同じ区間で判定する / 係属仮説として置いてフレッシュデータを待つ)を §0.2 に書き、オーナーに上申する。リードの推薦は後者。**

### 指摘 2
**直した(止める → 実測で置き換えた)。** s_C を §3 の対照 C の定義(切り値・mixed 除外・相手の符号)で絞った 50 行から取り直した(1 分 5.2 / 5 分 12.6 / 15 分 18.4 / 30 分 24.9 / 60 分 41.3 / 240 分 73.2 bp)。前版の 438 行は種類も切り値も違う集合で誤り、と本文に書いた。向き: 前版の 438 行の SD は小さめ(1 分 4.9)だったので MDE を楽観側に見ていた。

### 指摘 3
**直した**: 式の近似の向きを書いた(D1 の項は同じ、C の項は帯の n の違いで実際の分散が大きい = MDE を楽観側に見る)。`--power` で標本の帯の n から重み付きの分散も出して両方を貼る。

### 指摘 4
**直した**: `bp_reactdir` が距離の交絡を受けるかは未測定、と §0 に明記。標準化は交絡の有無に依らず出発点をそろえる対照の作り方、と位置づけ、交絡の有無は観測 (d)(標準化する前の差との比較)で結果と一緒に出す。同じ区間の `bp_reactdir` を今ここで標準化して見ることは、判定そのものの先取りになるのでしなかった。

### 指摘 5
**答えて直した**: そのとおり仮定を 1 つ含む。§5 に仮定を 1 行で明記し、標本 6 日(判定区間の外)で対照 C の `r_h` の平均 ± SE を実測して併記した(長い h ほど正に寄る傾向、SE の中)。判定区間では観測 (e′) で同じ量を出し、0 から z × SE 以上離れた h は読みに併記する(判定は変えない)。

### 指摘 6
**直した**: 「反証者の欄」を「反証者レビュー(research-protocol §11 の手順)」に改め、独立の下位モデルにこの文書・道具・数表だけを渡す手順として §9・§10 に置いた。結論めいた中身は消し、結果表の列とは語を分けた。

### 指摘 7
**直した(§0.1 の対応表を置いた)**: 右が埋まる行 = 2 周目の着手(L-232)、判定の量と対照(L-231 の目的の逐語と、リードの返答「この価格変化を主指標にし、対照を距離でも合わせて判定します」への L-232)。右が空の行 = 1 機構仮説・6 セル・α の付け方(research-protocol のリードの規則)と、**同じ区間で判定すること**(→ 指摘 1 の上申)。

### 指摘 8
**直した**: 不明(n < 30)= 帯で D1 の n < 30、不明(n2 < 30)= 帯で C の n < 30、どちらも帯を落とさずセルを不明に、と分けて書いた。

### 指摘 9
**直した**: 不明の内訳を 4 語それぞれの個数で併記する、と 1 周目と同じ規約を書いた。

### 指摘 10
**直した**: −7.6 / −15.0 は標準化前の値でそろえた後は縮みうる、と注記し、欲しい効果はその半分より小さい値、と根拠を書き直した。

### 指摘 11
**直した(実装後に埋める欄を作った)**: 読みの道具が `--judge` の中で自動で検査し、破れたら表を書かず `stopped.txt` を書く、と機構を書き、関数名 / テスト名の列を足した(実装が届いたら記入し、再監査)。

### 指摘 12
**直した**: 「F1′ と整合 / F1′ の反証 / 混在 / 不明」に改めた。

## 判定
判定: 止める(指摘 1 の上申中 = §0.2 の 2 択はオーナーの判断。他 11 件は直した。実装の関数名(指摘 11)は道具が届いたら記入して再監査)
