# 監査: 道具サーベイ 区分 8 の 19 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md`(18 回目の起動文から作った、それだけで読める形)。

## 監査 96 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run19_prompt.md@adbd7066cd2d`、コミット 2a57fad)の逐語

> ## 監査結果(96 回目、起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md@adbd7066cd2d`、コミット 2a57fad)
>
> 読んだもの: 対象の起動文全文 / 18 回目の起動文全文 / 18 回目の検収と監査 93・94・95 回目の逐語 / 17 回目の検収と監査 90・91・92 回目の逐語 / `CAT8_DESIGN.md` 全文 / `tools_catalog_cat8.tsv` 8-029 行 / `scripts/cat8_ledger.py: cmd_check_elements` / `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の 17・18 回目の節(実物の行番号・§4.0 表・知見表)/ `scripts/cat8_search.py`・`cat8_mklist.py`・`cat8_classify.py`・`cat8_step.py`・`cat8_render.js`・`check_scan_report.py` の実在確認 / 対象起動文と 18 回目起動文の全行 diff。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:14` — 「報告…の節(35575 行目から)」とあるが、対象コミット `2a57fad` の `docs/DATA/SCAN_2026-09-23_tools_cat8.md` で `## 区分8 — 18 回目の実行(2026-09-25)` の実際の見出し行は 35577(`git show 2a57fad:docs/DATA/SCAN_2026-09-23_tools_cat8.md | grep -n` で実測)。2 行ずれている。同じ文中の「17 回目の節(33568 行目から)」は実測(33568)と一致しており、18 回目側だけがずれている。実害は小さい(直後に空行 2 行を挟んで見出しに着く)が、写すときの数え違いである。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:60`(§2.5) — 「17 回目に `未確認` だった 11 項目は全部この回に試す」とあるが、17 回目の節の `### 4.0 機械可読の表` で 8-029 行の 4 列目(値の確度)が `未確認` のものを実際に数え直すと **12 項目**(版・最終更新日・ライセンス・初回公開日・既知の脆弱性・料金体系・課金開始条件・最小実行の可否・外部送信・当方データ投入・規模の見積・4軸4_向上。実測コマンド: `awk '/^### 4\.0/,/^### [^4]/' <17回目の節> | grep "MetaTrader の Strategy Tester" | 5列目を数える`)。列挙し直すと数が合わない(P5)。指示自体は「全部試す」なので実務上の見落としは起きにくいが、数え上げ語が実物と食い違っている。
>
> 3. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:21`(§1「N」) — N に新しく「MQL5 の言語の文書(リファレンス)の全頁」を足した。17 回目の報告(8-029 の E1a 知見)は「N(MetaTrader 端末の文書・ヘルプ・ブログの全部の頁)は膨大(MQL5リファレンス・記事等を含む)」と自ら述べており、その意味では今回の追加は 17 回目の解釈の延長ではある。ただし監査 91 回目の処置が確定させた文言は「その機能を含む製品の文書・ヘルプ・ブログの全部の頁(広いほう)」という抽象的な表現に留まり、「MQL5 言語リファレンス全体」を具体的に含めると決めたわけではない。17 回目検収の根本原因の見立ては「量を減らす方向で組み直す」であり、18 回目以降はそのために候補・要素を絞ってきた(この回も 8-029 単独に絞っている)。その直後の回で、Strategy Tester という 1 機能の N に、文字列・配列・トレード関数などを幅広く含む汎用プログラミング言語のリファレンス全体を追加することは、検索語(compar・diff・valid・test 等)が高頻度で無関係にヒットしやすく、12〜18 回目で繰り返した「型の文の当てはめ」を 8-029 で再発させるリスクがある。**この量の判断が「量を減らす」是正方針と整合しているかを、送る前に確かめたか。**
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:21` — 「MQL5 の言語の文書(リファレンス)の全頁」の境界(URL のパスなど)が示されていない。mql5.com にはリファレンス以外にフォーラム・Market・CodeBase・記事など膨大な頁がある(現に AlgoNetwork の回では同一ドメイン内の別サービスの混入が 2 度問題になった)。「一覧はサイトの地図・目次から全件で取り」だけでは、調査班が mql5.com 全体のサイトマップを対象にしてしまう恐れがないか。AlgoNetwork/AlgoBuild のときのような「別の製品の頁を根拠にしない」注意書きに相当するものが無い。
>
> 5. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:53`(§2.4) — 語ごとの決まりを避ける一文は、18 回目で実際に起きた形(`.valid|valid.`・`compar\w+` 等、語に 1 字足しただけ・語そのものを取るだけの正規表現)を名指しで禁じている。ただし「語 + ありふれた周辺語」(例: `compar\w+.{0,20}(strategy|strategies)`)のように、文脈らしきものを加えただけでなお汎用の当てはめになりうる形までは明示的に防いでいない。安全網は 18 回目と同じく「リードが検収で決まりの一覧と当たりを読んで確かめる」ことに依存しており(18 回目検収 §4・処置 5 と同型)、道具を足さない選択(L-102)自体は妥当としても、この一文だけで新しい抜け道が塞がると言えるかは判断が要る。
>
> 6. 該当なし — E1a の述語の括弧の例の読み(`:29`)は、設計票 §3 の逐語(「差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較」)をそのまま引用し、18 回目検収 §7 の結論(「戦略どうしの成績の比較はどの例にも当たらない」)と趣旨が一致している。書き換え・言い換えによる意味のずれは見つからなかった。
>
> 7. 該当なし — §1 の範囲定義(8-029 のみ、`未判別` = E1a・E3a・E5、他の 40 行は台帳のまま、8-038 は 20 回目に回す)は台帳 `tools_catalog_cat8.tsv` の 8-029 行の値と一致している。18 回目候補固有の文(AlgoNetwork の N の中の別製品・`cat8_repo_fetch.sh` の終了コード)は今回の範囲に無関係なため適切に削除されており、消し残しは見当たらなかった(「17 回目の AlgoNetwork の E6」への言及は E6 述語の一般的な説明のための例示として残しており、8-029 の判定に直接使う文ではない)。
>
> 8. 該当なし — §7 の 4 検査のコマンド(回番号 19・生ログ 1〜19 本・`--round 19`)、参照する道具(`cat8_ledger.py`・`cat8_search.py`・`cat8_mklist.py`・`cat8_classify.py`・`cat8_step.py`・`cat8_render.js`・`check_scan_report.py`)、参照する生ログ(`run17.log`・`run18.log`)はいずれも実在し、`cmd_check_elements` の実装(7 列の表・`台帳の値のまま` の検査・決まりの一覧の検査)とも整合している。
>
> **止める指摘は無い。** 3・4 の N の広さの論点は、起動文自身が持つ安全弁(「全部を取れない・大きさの上限を超えるなら `未判別` にし、試した手段と量を書く」)により、たとえ範囲が過大でも回自体は完結できる作りになっている。ただし 3・4・5 は、過去 2 回にわたり同じ候補(8-029)の N の広さそのものが監査(90・91 回目)の主題になった経緯があるため、リード単独の判断で押し出す前に検討済みかを確認したい。

## 処置(リード、監査 96 回目)

1. **直した**。35575 → 35577。
2. **直した**。11 → 12 項目。
3. **答える(入れたままにする)**。Strategy Tester の機能の一部(TesterStatistics・OnTester など)は MQL5 の文書に書かれているので、MQL5 の文書は候補の製品の文書に含まれる。外すと N を狭めて `なし` に寄せる(A-10・A-18 の型)。量の心配には、起動文の 2 つの手で当たる: (a) `印` を先に探し、全件の検索は `なし` を書く要素だけに打つ(§2.3)。(b) 全部を取れない・大きさの上限を超えるなら `なし` を書かずに `未判別`(§1)。「量を減らす」は 1 回に入れる候補と要素を減らすことで、N を狭めることではない。
4. **直した**。MQL5 の文書は `https://www.mql5.com/en/docs` の下の頁だけに限り、`mql5.com` のほかの頁(フォーラム・Market・CodeBase・記事・求人など)は、利用者や第三者の投稿・売り物の一覧で製品の文書・ヘルプ・ブログではないので N に入れない、と書いた(入れなかったものの名前を書かせる)。
5. **答える**。文脈らしきものを足しただけの決まりまでは、起動文の一文では防げない。道具は足さない(L-102)。安全網は検収でのリードの読みで、18 回目にしたとおり、決まりの一覧を読み、候補の機能が書かれうる頁(ここでは Strategy Tester・テストの頁と、MQL5 の文書の Tester の関数の頁)の当たりをリードが読んで確かめる。

止める指摘は無い。この手当てで送る。送る版の印: `20260923_tools_survey_cat8_run19_prompt.md@4bbe7648d90b`。
