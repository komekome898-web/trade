# 区分 1 の 26 回目 — リードの検収(2026-09-22)

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「区分1 — 26 回目の実行」節(313 行追加)/ 生ログ `docs/DATA/probes/20260922_tools_1_run26.log`
委任文: `docs/DATA/delegations/20260922_tools_survey_prompt.md@ce0012c95154`

## 1. 書き換えの範囲・受け入れ検査(機械)

`git diff --numstat` = **313 行追加 / 0 行削除**。削除の実測も 0 本。**過去の回の節は 1 行も触っていない。**
当方が全 25 本の生ログを渡して打った結果は 13 本すべて 0 件。**判定: 合。**

## 2. **一次資料に未到達 = 0 件。オーナーが決めた完了の形の最上段に届いた**

| 残りの種類 | 25 回目 | **26 回目** |
|---|---|---|
| **一次資料に未到達** | 11 | **0** |
| 到達済みだが 6 要素の印が無い | 2 | 2(14 番 `Bot18`・22 番 `Lean CLI`) |
| 印もあるが個別の要素が未決 | 1 | 1(11 番 `OctoBot` の 5 要素) |

オーナー逐語(L-398): 「**6 要素それぞれについて、一次資料に到達した候補が尽きること**」。
**未到達が 0 件になったので、残る 3 つは「到達できない理由が確定しているもの」だけである**(公開されていない / 取得していない画像の中 / 模擬を回す必要があり範囲の外)。
**これらは「尽きた」に数えず、理由つきで別立てにする**という扱いで正しい。候補 49(登録の内側)と合わせて**別立ては 4 件。**

## 3. 中身の抜き取り — **最も重い 2 件を当方が原典で確かめた。報告より強い内容だった**

### 候補 57 `WonderTrader` — **自分の注文の列の位置を、既定で追っている**

当方が `src/WtBtCore/MatchEngine.cpp`(9,732 バイト、既定枝 `master`)を取って確かめた:

```
249:    ordInfo._queue = lastTick->bidqty(0);      ← 差した瞬間の表示量を先行量にする
 91:    double& quepos = ordInfo._queue;
 94:    if (volume <= quepos)
 96:        quepos -= volume;                      ← 約定が来るたびに先行量を削る
```

**当方の `scripts/qa/maker_fill_ref.py` と同じ立て方である**(逐語「Queue-ahead at insertion = the raw displayed size」)。
**当方が研究用に別建てで書いた参照実装が、この道具では検証の機関そのものに入っている。**

**さらに、報告に無かったものを当方が見つけた**(`MatchEngine.h` と `.cpp` の 254・288 行):

```
MatchEngine.h:57   MatchEngine() : _tick_cache(NULL),_cancelrate(0), _sink(NULL)
MatchEngine.cpp:254  ordInfo._queue -= (uint32_t)round(ordInfo._queue*_cancelrate);
```

**先行注文の「取り消しによる目減り」を率で模擬する旋盤を持ち、その既定が 0 である。**
これは区分 1 で見つけた模型の中で**いちばん際どい位置**にある:
- 候補 95 は「先行の取り消しは見えない」と明言していた。
- 候補 104 は取り消しで列を進める実装を持っていた(95 と逆の立場)。
- **候補 57 はその 2 つの間を旋盤にしてあり、既定では 95 の側に倒れている。**

**そして既定が 0 であることは、24 回目に格上げした「呼ぶが既定では無効」の型そのものである。**通算 7 件目。

### 候補 91 `homerun` — 列を追い、影響は既定で無効

逐語 `order.queue_ahead_shares = compute_queue_ahead_shares(`、`depth that disappeared between snapshots is volume that traded ahead of us in the FIFO queue.`、
影響側は `strength_bps: float = 0.0,` / `strength_bps = 0 disables impact entirely`。**1 つの道具の中で向きが分かれる。**
**`区分1-足` を外した判断も正しい**(機関は `async for snapshot in book_source.iter_snapshots():` で板の写真を回しており、原典に `candle`・`ohlc` が 0 件)。
**一覧の仕分けは推定だったので、実装で覆したのが正しい扱いである。**

### 候補 51 の登録の付け替え

`ungh.cc/repos/QUANTAXIS/QUANTAXIS` が `"repo":"yutiansut/QUANTAXIS"` を返すことを当方でも確認。
**`code=200` のまま中身が別の登録という、25 回目の 3 番と同じ「気づけない型」。**通算 3 件目。

## 4. 調査班が挙げた 6 件の判断 — リードの回答

**(1) 70 番に段 3、19 番に段 2 と逆の答えを出したこと → 正しい。その分け方を段 3 の定義にする。**
調査班が使った述語(「**事象の座標を持って予定表に載るか**」)が、まさに段 3 が見たいものである。**原文に無いのは当方が書いていなかったからで、書くべきだったのは当方。**

> **段 3 = 分けたもの・届いたものが、時刻の座標を持って機関の予定表に載り、戦略を動かす事象になる。**
> 足を細かくするだけ・他の足の見え方を直すだけは段 3 ではない。

19 番の `with_execution=False,` / `with_generation=False,` と、70 番の `return GeometricHit{*t_cross, level, true};` は、この述語で綺麗に分かれる。

**(2) 段 0 を 4 件に当てたこと → 2 つに分ける。**

> **段 0 = 注文の物は持つが、埋めるときに相場の値を 1 度も見ない。**
> **注文の物そのものを持たないものは「該当なし(注文の物を持たない)」と書き、段の尺度に載せない。**

51 番は注文を持ち、指定の値段と数量をそのまま成立させるので**段 0 で正しい。**
54 番・87 番は注文の物が無いので**「該当なし」に直すこと。**0 と「尺度の外」を同じ欄に書くと、後で平均や数え上げをしたときに混ざる。

**(3) `区分1-市場影響と約定の模型` を「約定の可否・量・値段のどれかを決める仕組みがあるか」で付けたこと → その述語を正とする。**
23 回目に当方が決めた「印は付ける、切れ味は段で出す」と同じ向きで、一貫している。**これも原文に無いのは当方が書いていなかったためで、書き足す。**

**(4) 段(機構)と段(既定)が違うのが 8 件か 9 件か → (2) を当ててから数え直すこと。**
92 番が注文の物を持つなら段 0 は本物なので 9 件、持たないなら「該当なし」なので尺度の外(8 件)。**数の前に (2) の当てはめが要る。**

**(5) 57 番の機関が 4 つあり「そのまま使ったとき」が 1 つに決まらないこと → 機関ごとに段を書くこと。**
「複数の段に当たるなら全部書く」(24 回目 §5(3))と同じ理屈である。**1 つに丸めると、足の経路では上がらないという事実が消える。**
**表に「どの機関か」の欄を足す。**高頻度・執行の経路は段 3・4・5、足の経路(`CtaMocker`)は上がらない、と並べて書く。

**(6) 51 番の外部送信を危険の欄に書き、候補から外さなかったこと → 正しい。**
委任文 §6-1 の逐語「止めるのは『この環境への導入と実行』だけ」のとおり。**導入していないので止めるものが無い。**
**ただし通算を数えること**: 作業者向けの手引きの同梱 6 件に加えて、**外部送信の口を持つものが 1 件。**使う段でまとめて出す。

## 5. 安全側

HTTP 404 は 0 件、TLS 切断 2 件はどちらも打ち直して 200、**404 の欄に入れていない。**
**当方も同じ回に TLS 切断を 1 度受け、待って打ち直して通した**(この扱いが正しい形であることを当方でも再現した)。
**逐語を `grep -cF` で 1 件ずつ生ログと突き合わせ、足りなかった 10 件を原典から取り直して生ログに足した**のは、
「貼った証拠が主張を支えているか」を自分で測ったということで、**22 回目の貼り付けの欠陥と同じ型を自分で塞いでいる。**
原典で 2 行に折れていた逐語を 1 行に繋いで引きかけ、**原典どおり 2 行に直した**のも同じ規律である。
導入・構築・最小実行・登録・鍵の発行・購入・発注・新しい検索計画・候補の追加は 1 件も無い。

## 6. 判定

**受け取る。**検査 13 本が 0 件、書き換えは追記のみ、抜き取りは 3 件とも原典に当たり、**うち 1 件は報告より強い内容だった(§3 の `_cancelrate`)。**
**一次資料に未到達が 0 件になり、オーナーの決めた完了の形に届いた。差し戻す点は無い。**

## 7. 27 回目の起動の指定 — **仕上げの回**

**新しい到達はもう要らない。表を整えて、全体の受け取り監査にかけられる形にする。**

1. **(2) を当てはめ直す。**54 番・87 番(と 92 番を確かめて該当すれば)を「該当なし(注文の物を持たない)」に直し、**段(機構)と段(既定)が違う件数を数え直す。**
2. **段 3 の定義を §4(1) のとおり節に書き、当たる候補を当て直す。**
3. **`区分1-市場影響と約定の模型` の述語を §4(3) のとおり節に書く。**
4. **57 番の表に「どの機関か」の欄を足し、機関ごとに段を書く**(§4(5))。
5. **57 番の `_cancelrate` を節に足す**(§3。既定 `0`、`MatchEngine.h:57` と `.cpp:254`・`288`)。
   **候補 95・104 との三者の位置関係も書くこと** — 区分 1 でいちばん際どい発見である。
6. **登録の付け替えを 3 件まとめて一覧に併記する**(`robcarver17`→`pst-group` / `Ziplime`→`Limex-com` / `QUANTAXIS/QUANTAXIS`→`yutiansut/QUANTAXIS`)。
   **`code=200` のまま中身が別の登録という型であることを 1 行書く。**
7. **安全側の通算を 1 か所にまとめる**: 作業者向けの手引きの同梱 6 件(44・74・120・41・58・19)+ 外部送信の口 1 件(51)。
8. **数え直しを 3 段で出し、別立ての 4 件(14・22・11 の 5 要素・49)を理由つきで並べる。**
9. **新しい検索計画は打たない。候補も増やさない。構築・導入・最小実行もしない。**原典を読むのは許す。
10. 上の 1〜8 に要る範囲でのみ既存の節への書き換えを許す。**追記だけで果たせるならそのほうがよい。**
