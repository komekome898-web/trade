# 監査記録: 道具サーベイ 区分 8 の 12 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run12_prompt.md`(初版の指紋 `663b2133da12`)。11 回目の起動文(監査 64〜69 回目を通した版、指紋 `3b00f9d0fd9d`)を元に、範囲を 8-016 の E3a・E5 のやり直しと、8-017〜8-028 の手直し・8-018 の §4.0 にした。11 回目の検収と監査 70 回目の処置を入れた: 語の組の語を全部入れる(`check-elements` の 12 回目以降の検査、コミット 02c8fcb)/ 当たりの理由の書き方 / PyPI を実際に取る / Lean の読み直し / notebook を除かない / 8-018 の §4.0 を当の候補で書く。台帳に 8-041 `Oryon`(8-027 の後継の案内)を足した。

## この回に回さないもの

| 何 | 理由 |
|---|---|
| 8-029〜8-041(13 件) | 13 回目。L-416「一気に終わらせようとせずに」 |
| 8-016 の E3b | 11 回目で受け取った |
| freqtrade の §4.0 の表の合わせ直し・8-015 の N の出所の書き直し | 資源化のとき |

## 監査 71 回目(対象 = f7d8a5b、起動文の指紋 `663b2133da12`)

監査役の出力(逐語。[直す] 1 件・[聞く] 2 件、[止める] 0 件):

> 監査 71 回目。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run12_prompt.md`(指紋 663b2133da12、コミット f7d8a5b)。11 回目起動文(指紋 3b00f9d0fd9d)との `diff -u`、`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run11.md`(監査 70 回目の逐語込み)、`scripts/cat8_ledger.py`(`check-elements` の実装、コミット 02c8fcb)、`docs/DATA/tools_catalog_cat8.tsv`(41 行 + 8-041 `Oryon`)を突き合わせて検査した。検証は主に `scripts/cat8_ledger.py` の `ELEM_TERMS_WIDE` と round≥12 の "lack" 算出ロジック(694〜700 行)を Python で直接呼び、実際に run11 の生ログで使われた検索パターン文字列を与えて再現する形で行った(フルの `check-elements` パイプラインを合成報告で通す実験はしていない。作業ツリー・scratchpad に新規ファイルは残していない)。
>
> 1. [聞く] `scripts/cat8_ledger.py:693-700`(round≥12 の `lack` 算出)/ 起動文 line 52(語の組の定義) — 「組の語を全部」の検査は、`ELEM_TERMS_WIDE` の各組を `|` で割った個々の語(例: `look.?ahead` → `["ルックアヘッド","look.?ahead"]`)を、そのまま**正規表現として**調査班の検索コマンド文字列に `re.search` で当てている。`look.?ahead`・`point.?in.?time`・`as.?of`・`out.?of.?order` の `.?` は「区切り文字 0〜1 文字」しか許容しない。run11 の実際の 8-016 検索パターン(`lookahead|look-ahead|look ahead|...|point-in-time|point in time|as-of|...`)で再現実験したところ、ハイフン・スペース・アンダースコア・区切りなしの単一区切りはすべて一致した(実測、上記の Python 再現で確認)。しかし区切りが 2 文字以上になる書き方(例: `point - in - time`、二重スペース、`out_of_order` 以外の複合区切り)や、`look[-_ ]?ahead` のようなブラケット表現(意味的には同じか、より広い正規表現)は、`.?` の 1 文字制約を満たさず一致しない。正しく述語の概念を検索していても、書き方の違いだけで `なし` が `未判別` に差し戻される経路が理論上残っている。今回の実測では run11 の実際の書き方はすべて許容範囲内だったので、この回の 8-016 再検索・8-017〜8-028 の新規検索が実際にこの経路で誤って止まるかは未確認(次の検収で `check-elements` の出力にこの型の指摘が出ないか見るのが実地の確認になる)。この検査機構をこのまま使うか、区切り文字を正規化してから比較する形に直すかは、リードが決めることか、それとも一旦このまま様子を見るのか。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run12_prompt.md:52` — 「検索の手の決まり」の同じ行に「リポジトリごとに打ってよい(8-016 は 1 件ずつ消すので、1 件ごとに、**E3a・E3b・E5** の全部の語を 1 つの正規表現に入れて打つ)」という 11 回目そのままの文が残っている。しかし同じ文書の §1 は「**この回に回さないもの**: … / 8-016 の E3b(11 回目で受け取った)」と明記し、§2 の 1 の見出しも「E3a・E5 のやり直し」だけで E3b を含めていない。この 1 文だけが古い範囲(E3a・E3b・E5)のまま残り、本文の他の箇所(§0.7・§1・§2 の 1 冒頭)と矛盾している(P2 型: 同じ範囲の定義が文書内で 2 箇所以上に別の形で存在)。正直に指示に従う調査班が、この 1 文を読んで E3b まで検索し直してしまう(無駄な作業)か、逆に E3b の行を「触った行」として書いてしまい `台帳の値のまま(11 回目の節)` の免除を外れて round≥12 の全語検査に晒される経路が残る。
>
> 3. [聞く] `docs/DATA/tools_catalog_cat8.tsv:42`(8-041 `Oryon`)/ `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run11.md` 末尾「4: 足す…**12 回目の検収で足す**」 — 監査 70 回目を通った run11 検収の処置記録は「Oryon は 12 回目の**検収**(round12 実行後のリードの受け入れ作業)で台帳に足す」と明記していたが、実際にはコミット f7d8a5b(12 回目の**起動文の起案**、すなわち 12 回目の**実行前**)で台帳に足された。結果自体(41 行・§1 の除外リストに 8-041 を含める)は一貫しているが、記録された処置のタイミングと実際の行動のタイミングが 1 段ずれている。実害は今のところ見当たらないが、「検収で足す」と書いた処置がなぜ前倒しで実行されたかの説明が、起動文にも run12_prompt 用の監査記録(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run12_prompt.md`)にも無い。
>
> その他の確認事項(指摘なし、参考までに記載):
> - 監査 70 回目の 8 件(指摘 1〜8)は、run12 起動文の §0.7 の要約(6 項目)・§2 の 1(8-016 の Exactpro 製品頁追加・E3a/E5 限定の再検索)・§2 の 2(a)〜(d)(8-018 の §4.0 を当の候補で書く/変更履歴だけで段 4 にしない、8-019・8-020・8-022・8-024・8-026・8-028 の PyPI 実取得、Lean README 読み直し、notebook を除かない)・「検索の手の決まり (b)」への「理由には当たった語と何を指しているかを書く」追記、のすべてに 1 対 1 で対応する処置が見つかった(検収 §5 の「答える」扱いの 2 件(仕様変更なし・区分 1 の道具のため)は妥当な理由が記されている)。
> - 台帳の行数(41)・8-001〜8-041 の範囲記載・§4 の「41 行全部を載せる」・§7 の生ログ列挙(run1〜run12)は相互に整合していた。
> - 8-017〜8-028 の現在の台帳の値はすべて `未判別` か `印` で、`なし` の行が一つも無いことを確認した(round≥12 の厳格化が既存の `なし` 判定を遡って壊す経路は今回は存在しない)。
> - 「8-016 の N・除外・読んでいない件数は文で数えず `cat8_fetch_tally.py` の出力から取る」「`なし` の N/M は `cat8_mklist.py` の `listed=` の和」等、正直な調査班が誤って `なし` を書く/試していない手段を試したと書く主要経路は、`cat8_search.py`・`cat8_mklist.py` の専用道具縛りと `check-elements` の機械照合(候補番号の一致・`files=`/`listed=` の一致・`当たり F/H` の一致・引用の生ログ照合)でほぼ閉じられている。8-019/8-020/8-022/8-024/8-026/8-028 の PyPI 未実測の穴(監査 70 回目の指摘 2)には「試した手段の欄には手ごとに生ログの行を書く」という具体的な対策が入っている。
>
> 12 回目を起動してよい状態か: 起動してよいが、指摘 2(E3a・E3b・E5 の古い文言)は起動前に直すべき軽微な食い違いであり、指摘 1・3 はリードが答えるか、少なくとも意識した上で進める必要がある。

**リードの処置**:
1: 答える(検査はこのまま)。起動文が語の組の語を字で並べている(`look-ahead`・`point-in-time`・`as-of` など)ので、調査班が並べた字のとおりに入れれば検査に合う。起動文に「ここに書いた字のとおりに(区切りは 1 文字で)」と足した。区切りの書き方の違いで止まったら、検収でその行を読み、書き方だけの違いなら検査の語を直す。
2: 認める。「E3a・E3b・E5 の全部の語」を「E3a・E5 の全部の語」に直し、E3b の行は `台帳の値のまま(11 回目の節)` と書く、と足した。
3: 答える。11 回目の検収の記録に「12 回目の検収で足す」と書いたが、12 回目の起動文で台帳の行数(41 行)と「この回に回さないもの」に入れるため、起動文を書く前に足した。記録の予定と実際の順が違う。足した根拠(生ログ `docs/DATA/probes/20260923_tools_8_run11.log:4154` の「Active development has moved to Oryon」)は台帳の「発見の出典」の列にある。
直した版(起動文の指紋 `d9f9f2206ad7`)は監査 72 回目で確かめてから印を記録する。
