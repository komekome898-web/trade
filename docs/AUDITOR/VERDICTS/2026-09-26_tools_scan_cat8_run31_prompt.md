# 監査: 道具サーベイ 区分 8 の 31 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md`(8-034 Pandas の 1 行。24 回目の起動文から作った)。

## 監査 125 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run31_prompt.md@346d3a836755`)の逐語

> 監査 125 回目。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md`(指紋 `346d3a836755`)。
>
> 以下、指摘のみを列挙する(最大25件、severityは[止める]/[直す]/[聞く])。
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:5` — 「期限は無い。途中で止めない。」の直後に「予算で止めない(オーナーの決定 L-507「案A」)」とだけ出典が付き、「期限は無い。途中で止めない」自体には出典が無い。この文がオーナーの逐語(L-506「期限を指示した覚えがありません。中途半端で止まらないでください。」)に基づくものであれば、その出典を明示すべきではないか。24回目起動文でも同型で出典を欠いたまま踏襲されている(P1、§0.1)。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:15` — 「22・24・26・28 回目に、述語の対象(E2 の列挙のデータ、E4 の記録した市場データ)に届かない根拠で `印` を付けた」との一文は、4回すべてでE2・E4双方の誤りがあったように読めるが、検収を照合すると 22回目はE4のみ(dbtの「時刻順に」、`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run22.md` 指摘1・処置1)、24回目はE2のみ(Prefect、`..._run24.md` 指摘5・処置5)の誤りで、E2とE4の両方が問題になったのは26・28回目だけ(`..._run26.md` 指摘1・2、`..._run28.md` 指摘2・3)。調査班がどの回にどちらの要素の誤りがあったか誤読しないか(P2)。
>
> 3. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:15` — 「29・30 回目に、検索の要約だけで宣伝詐欺の兆候を `実測` にしかけた」との記述。`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run29.md` 指摘1・処置1でこの誤りが実際に起き `未確認` に直っているが、`..._run30.md` にはこの型の新規の指摘が無く、30回目の処置6は29回目で既に直った `未確認` の値をそのまま引き継いだだけ(`docs/DATA/SCAN_2026-09-23_tools_cat8.md` 43409行台〜、43478行の台帳と整合)。30回目に同じ誤りが独立に再発したかのように読める書き方は、検収記録と合っているか(P2)。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:63` — 「Pandas の wheel はコンパイルした拡張(`.so` など)を含みうる。…(「バイナリがある」だけで止めも通しもしない)」は、委任文 §6-1(`docs/DATA/delegations/20260922_tools_survey_prompt.md:99`。「バイナリ同梱が無いか」を検査項目に挙げるのみで、単独での停止条件とは明記していない)から導いたリードの読みに見えるが、「リードの判断」等の帰属が付いていない。オーナーの決定として書いたものと区別できるか(P2、§0.1)。
>
> 5. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:63` — 「**最初の手**で `ls -ld /root/.m2 /root/.gradle; du -sh /root/.cache/pip` を打って生ログに出し…、**終わったら**同じ手を打って」の一文が、§6-1・導入・最小実行の記述より前に置かれている。「終わったら」が §2.5 全体(最小実行の完了)を指すのか、直前の手の直後を指すのか、文の位置だけでは曖昧で、調査班が2回目の確認を導入・実行前に打ってしまう読み違いの余地がある(P8、この回に実行できるかに関わる)。
>
> 6. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:21` — 24回目(Prefect)にあった商用版(Prefect Cloud)の切り分けの文言が、Pandas版では削除されている。Pandasには同種の商用版懸念が無いという判断は妥当に見えるが、その判断根拠(なぜPandasには不要か)が本文のどこにも一言も書かれていない(暗黙の判断。P2寄り)。
>
> 7. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:15` — 引いた「42047 行目の見出しから 42859 行目まで」自体は現物と一致するが(実測 `grep -n` で確認)、参照元の検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run28.md:4` 自身は「42046 行目から」と1行ずれた値を書いている。この回の起動文が正しい値を書いているのは良いが、検収側の値とのずれが放置されたままなので、資源化の際に混乱しないよう記録しておくべきではないか(P2)。
>
> ---
>
> 補足(照合の過程で確認し、問題が見当たらなかった点。指摘としては数えない):
> - 24回目(Prefect)固有の記述(商用版クラウドの扱い、Prefect特有の外部サーバー・テレメトリの節)はPandas版から削除されており、写し残しは見当たらなかった。
> - 台帳の値(`docs/DATA/tools_catalog_cat8.tsv` の8-034行、41行・8要素すべて`未判別`・状態`未着手`)は起動文の記述と一致。
> - 報告10回目の節の引用行(3671行「Pandas — 同上 — 同上」、3849行「34. Pandas (8-034) …」)は現物と一致。
> - §6-1の並び(委任文§6-1の逐語)は`docs/DATA/delegations/20260922_tools_survey_prompt.md:99`と一致し、順序(§6-1の結論の行→導入→実行)の要求は26・28回目の検収の学びを正しく反映していた。
> - §3(書き換えの範囲、1〜30回目)・§7(31本の生ログ・`--round 31`)のカウントは31回目として正しく更新されていた。

## 処置(リード、監査 125 回目)

1. **直した**。「途中で止めない」に L-506 の逐語を出典として付けた。
2. **直した**。回ごとに要素を分けた(22 回目は E4、24 回目は E2、26・28 回目は E2 と E4)。
3. **直した**。「29 回目に」だけにした(30 回目は 29 回目の値を引き継いだだけで、再発ではない)。
4. **答える・直した**。リードの読みである。委任文 §6-1 は「バイナリ同梱が無いか」を検査の項目に挙げるだけで、それだけで止めるとは書いていない。起動文に「リードの読みで、オーナーの決定ではない」と書いた。
5. **直した**。「この回の最初の手(§6-1 より前)」と「この回の最後の手(最小実行と検索が全部終わり、§7 の検査の前)」と書いた。
6. **答える・直した**。Pandas に商用版があるとの記述をリードが持っていないので外したが、無いと確かめてはいない。起動文に、商用版・有料の版の有無を調査班が調べて書くこと、あれば登録・購入せず候補の機能に数えないことを書いた。
7. **記録する**(起動文は変えない)。28 回目の検収の見出しの「42046 行目から」は 1 行ずれていて、正しくは 42047 行目(29 回目の監査 121 回目の指摘 3 でも同じずれを直した)。資源化のときに、この節を見て正しい値を使う。

直したあとの起動文の印: `20260923_tools_survey_cat8_run31_prompt.md@450257b52aff`。止める指摘は無いので、31 回目を起動する。
