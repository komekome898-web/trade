# 監査記録: P4-N 夜間の自動 restart_all(手順 + `nightly_restart.bat` + `restart_all.bat` 差分)— 2026-09-12

監査役: 本番 `owner-auditor`。問い 7 件(止める 3 / 直す 2 / 聞く 2)。全文と応答。

1. [止める] docs/OWNER_PROCEDURES.md:63 — 「以後の確認はリードが翌朝の share_logs で `logs\nightly_restart.log` を見る(オーナーの作業なし)」とあるが、`deploy/share_logs.bat` のコピー対象は個別に列挙された既知ファイル(`logs\bot.jsonl`・`logs\status.json` 等)のみで、`logs\*.log` のワイルドカードも `nightly_restart.log` を名指しした行も無い。この経路で本当にこのログがリードに届くか。届かないなら、無人化の唯一の検証手段が「オーナーが手元PCで直接ログを開く」という手作業になっていないか。(P11)

> **直した(実害)。** 届かなかった。`share_logs.bat` に `logs\nightly_restart.log` のコピー行を追加。KA-30 の型(届くと思い込んでオーナーに聞く)を納品前に止めた。

2. [止める] docs/OWNER_PROCEDURES.md:49 — 「bitFlyer の日次メンテナンス 04:00〜04:10 の中」を、無人でstop/startしても安全な時間帯である根拠として使っている。この時間帯という数値自体は `docs/NEGATIVE_FACTS.md` N-006 に「bitFlyer公式の毎日メンテナンス窓…の一次文書は本環境から直接確認不能→二次情報(WebSearch要約)止まり」と記録されている(賞味期限あり)。P4-N はこの数値を一次資料・実測・仮定のどれとして採用しているか明記していない。窓がずれる/短い場合、この節の安全性の前提そのものが崩れないか。(P2)

> **直した。** 「仮定(二次情報、N-006)」と明記。窓がずれても失うのは毎晩 1 分の記録で、安全性(誤発注・二重起動)の前提ではないことを書いた。

3. [止める] deploy/nightly_restart.bat(全体)/ docs/OPERATIONS.md:516-517 — 既存の `bitflyer-start-all`(ログオン時+1時間ごと・無期限)・`bitflyer-fetch`(15分ごと・無期限)の2タスクが04:02前後で発火した場合との競合が扱われていない。`deploy/restart_all.bat:13-24` の見出しは「半端に更新された木への再起動」「コンポーネントの二重起動」が安全でないと明記し、コピー機構と[4/5]検証はnightly_restart.bat自身の逐次実行内の重複しか防いでいない。独立にスケジュールされたこの2タスクがgit pull中(半端な木)やstop_all〜start_all間に割り込む可能性は、P4-N・nightly_restart.batのどこにも記述が無い。この競合は検討済みか。

> **直した(記述で。防止はしていない)。** 検討していなかった。`start_all.bat` は動いているものを飛ばす(行 41「already running - skipped」)ので、停止〜起動の間に割り込んでも二重起動にならない。`fetch_all` が pull の数秒に当たれば 1 回失敗しうるが、全スクリプトが差分・冪等で次回に埋まる。bat のヘッダと手順に**推定・未実測**として明記。

4. [直す] docs/OWNER_PROCEDURES.md:56 — 「(清算記録の修正 P14 と同じ回でよい)」とあるが、本ファイルには `## P14` の節が存在しない(P1〜P13、および P4-N のみ)。オーナーがこの指示に従って「P14」を探しても見つからない。

> **直した。** 「次に渡す手順 P14。この文書にまだ無い」と明記(P14 は委任中の納品に含まれる)。

5. [直す] deploy/nightly_restart.bat — `logs\nightly_restart.log` は毎回無条件に追記するだけで、ローテーション・サイズ上限が無い。P4-N は恒久運用を前提にしているため、このログは無期限に肥大化する。

> **直した。** 5 MB を超えたら `.1` に退避(1 世代)。

6. [聞く] docs/OWNER_PROCEDURES.md:53,60 — 「P6と同じく『ログオン中のみ実行』の登録」と書いているが、示されている `schtasks /Create` コマンド自体には /RU 等ログオン条件を指定する引数が無い。この挙動がP6同様に実機で「ログオン中のみ」になることを確認した上での記述か、既定値への期待か。

> **既定値への期待(推定)と明記した。** 根拠は P6 の `trade_share_logs` が同じ書き方で毎朝動いていること。オーナーへの問いにはしない(登録後の初回ログで分かる)。

7. [聞く] docs/OWNER_PROCEDURES.md:65-66 — 「注意(未確認)」は pause 抑止のTask Scheduler下での挙動に限定して書かれている。nightly_restart.bat の自己書き換え回避(コピー実行)機構自体や、無人実行中のgit資格情報の扱い(GUIプロンプトが出ればハングし得る)は、この「未確認」の範囲に含まれているか、それとも別途確認済みか。

> **直した。** 未確認の範囲を (a)〜(d) に広げて列挙。資格情報のプロンプトは `GIT_TERMINAL_PROMPT=0` / `GCM_INTERACTIVE=never` で「待たずに失敗」に変えた(これも未検証に含める)。

**結果**: 止める 0 件残、直す 2/2、聞く 2 件はリードの判断と根拠を成果物に書いた(上申なし)。
