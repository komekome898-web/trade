# 検収: 道具サーベイ 区分 8 の 32 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 32 回目の実行(2026-09-26)`(44701 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run32.log`(130,738 バイト)。受け取ったままの形はコミット e315131。

## 0. 費用と時間

調査班 613,382 トークン・1,974,247 ms(32.9 分)・道具 219 回。

## 1. 受け入れ検査(リードが打ち直した)

- `check_scan_report.py`(32 本): `---- 合計 61 件`(調査班の貼付と一致)
- `check-elements --round 32`: 3 件(生ログ 24・34・456 行)。24・34 行は、起動文 §2.5 が指定した `.../venvs/8-034/tmp` の道の中の `tmp/` に検査の正規表現が当たったもので、scratchpad の外ではない(リードが道を読んで確かめた。誤検出)。456 行は `curl -o /tmp/x_$pkg.json` で `/tmp` の直下に書いた実際の違反(同じ手で消している)。
- `check "" run32.log`: 0 件 / 過去の節から消えた行: 0
- `/root/.m2` 無し、`/root/.cache/pip` 728M。

## 2. リードが見つけたこと(監査の前)

1. **§6-1 の順序は守られた。**配布物 4 つ(pandas 3.0.6・numpy 2.4.6・python-dateutil 2.9.0.post0・six 1.17.0)を `--only-binary :all: --no-deps` で wheel だけ取り(生ログ 48〜72 行)、結論の行(498 行、18:16:46)のあとに `--no-index --find-links` で導入(504 行、18:16:56)。結論の前の手は、`pip install --dry-run`(14〜47 行。キャッシュの wheel の中の METADATA を読むだけで、ソースから作っていない。出力は「Using cached ….whl」だけ)と、依存を確かめる隔離環境の作成(34 行)。**この回の実測は受け取る。**
2. **scratchpad の外に書いたもの**(リードが `find / -xdev -newermt` で 18:12〜18:48 を探した):
   - `/tmp/x_$pkg.json`(456 行の手。同じ手で消えている)
   - `/tmp/nul`(419 バイト、18:35:44 に作られた。中身はこの回の節の書き出しの断片。**生ログに手が無い**。報告の下書きを書くときに出たものと読む)→ リードが `scratchpad/cat8/run32_tmp/nul` へ移した。
   - `/root/.cache/pip/selfcheck/18d4fdbb…`(188 バイト、18:17:08 に新しく作られた)と、`/root/.cache/pip/http-v2/a/1/9/5/3/a19537d3…`(既存の項目の頭の部分が 18:17:08 に書き直された)。18:17:08 の `pip list`(513 行)・`pip check`(524 行)の手に `PIP_CACHE_DIR` が付いていなかったため。→ リードが selfcheck の 1 個を消した。http-v2 の項目は元に戻せないので残す(pip の索引のキャッシュの頭の書き直しで、中身の `.body` は 09-23 のまま)。
   - `/tmp/x_exclargs.txt` は 09-24 に作られたもので、この回のものではない(触らない)。
3. **E2**: `asfreq` の docstring「Convert time series to specified frequency.」「The values corresponding to any timesteps in the new index which were not present in the original index will be null (``NaN``)」(生ログ 624〜653 行)。対象が時系列であることを言う逐語と、欠けた時点を `NaN` として出力に示す逐語が、同じ機能にある。調査班は `未判別` のまま問いにした(問い 1)。
4. **E3a の `なし`**: 44 ファイルに、当たった行ごとに違う理由が書かれた(31 回目に名指した `governance.md:181`・`test_select.py:105`・`test_read_errors.py:58`・`test_indexing.py:1301` を含む)。同じ文は whatsnew の漏れの修正の記録 5 ファイルだけで、どれも当たった行を引いた上で意味が同じもの。
5. **E4・E6 の案 B の記録**: 文書の全 217 頁の道と題に語の組を機械的に当て、E4 は 0 頁、E6 は 2 頁を選んで本文を読んだ。ソースはリポジトリの全部のディレクトリ(生ログ 919〜1215 行、`src/pandas` の根に `find -type d`)に語を当てた。(監査 128 回目の指摘 2 で直した。下書きでは「`pandas/` の下だけ」と書いていた = リードの誤り)
6. §4.0 の宣伝詐欺の兆候: 検索 3 回と、投稿の本文 2 件を取って読んだ。値は「見当たらない」、印は `未確認`。

## 3. 中身の監査(128 回目、owner-auditor)の逐語

> 監査 128 回目。対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md`「## 区分8 — 32 回目の実行(2026-09-26)」(44701〜45317行目)、生ログ `docs/DATA/probes/20260923_tools_8_run32.log`、リードの検収下書き `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run32.md`。
>
> 確認できたこと(参考、指摘ではない): §6-1の順序(wheel取得→展開→検査→結論行[498-500行]→導入[501行以降])は生ログで確認どおり実測された。TMPDIRのmkdir先行・`export`では作らない旨の修正(監査127回目の処置2・3)も反映されていた。E3aの「なし」の44ファイル/66行は表の内訳を合計しても66に一致し、asfreq・assert_frame_equal・merge_asof・sampleの逐語(改行をまたいだ引用を含む)はscratchpad内のpandasソース原本と突き合わせて実在を確認した。案Bの条件(2)「§4.0の過半が一次資料か実測」を31・32回目を合算した8-034の§4.0全43項目(委任文の語彙43件と1:1で一致)で数え直したところ、一次資料8+実測31=39/43(約91%)で過半を満たす。週DL数・.so個数(numpy20+pandas45=65)・build/resample秒数・RSSなど主要数値は生ログと一致した。
>
> 以下、指摘のみ列挙する。
>
> 1. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の32回目節(44701行目以降) — 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run32_prompt.md` §7「返す前に」の項目2〜4が報告本文に**貼られていない**。具体的には (a) `cat8_ledger.py check-elements --round 32` の最後の出力の全文(§7の1文目「最後の出力の全文を貼る」)、(b) `cat8_ledger.py check "" run32.log` の最後の出力の全文、(c) `git diff -U0 HEAD -- SCAN...md | grep '^-[^-]' | wc -l` の値、のいずれも32回目節には無い(check_scan_report.pyの出力ブロックだけがある)。31回目節(44025行目以降)はこの3つを全部貼っていた(例: 44025節内の658行目・665行目)ので、32回目は退行している。リードの検収§1「受け入れ検査」は自分で打ち直した値を書いているが、報告自体のこの欠落は§2の指摘リストに挙がっていない。起動文自身が定める「1回の実行の完了」の形式要件を報告が満たしていない。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run32.md` 26行目(§2の5)と42行目(§4「リードの読みの案」) — 「ソースは `find pandas -type d`(`pandas/` の下だけ。`doc/`・`web/`・`scripts/` などの最上位の他のディレクトリは入っていない)」という記述が、生ログと矛盾する。実際のコマンド(`docs/DATA/probes/20260923_tools_8_run32.log:920`)は `.../venvs/8-034/src/pandas`(git clone のルート、"pandas"という名前のリポジトリ全体)を対象にしており、その直下には `.github`・`ci`・`doc`・`pandas`(パッケージ本体)・`scripts`・`web` が並列に存在する(`sed -n '919,1215p' run32.log` で確認)。E6のディレクトリ選定(1212-1214行目)では実際に `web/tests`・`.github/actions/run-tests`・`scripts/tests`・`scripts/tests/data` が語に当たっており、報告自身の知見9(45057行目)も `web/tests` を検討対象として挙げている。つまり検索は`pandas/`の下だけでなくリポジトリ全体を対象にしていたのに、検収はそれを「`pandas/`の下だけ」と書いている。結論(印が新たに見つからない)自体は探索が想定より広かった側に振れるため覆らないと見られるが、案Bの「探し尽くした範囲」の記述根拠が事実と異なったまま検収・§2に記録される。P10(目視・記憶による判断は原典再照合するまで正しいと言えない)に該当しうるので、生ログを再照合して直すべきである。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run32.md` 40行目「E2 → 印・段2」の読み — 31回目はduplicated/merge(indicator=True)を「汎用の表についてで、述語の対象(時系列・約定・板・足・参照データ)に届かない」として`なし`扱いを拒み(31回目検収§2の4)、32回目はasfreqのdocstring(「Convert time series to specified frequency」+「timesteps...not present...will be null」)を根拠に印へ動かす提案をしている。asfreqの「時系列」は対象を限定する記述で31回目の却下理由(対象不一致)は解消されるが、「will be null」はNaN埋めという再インデックスの一般的な副作用であり、duplicated()のような明示的な真偽値の検出関数と同列の「検出するか報告する」機能と言えるかは、設計票§3の述語上なお解釈の余地がある(調査班自身が起動文§1のtask 2の指示どおり「判断に迷った点1」として保留していた)。この当て方を他の道具(14回目Great Expectations・23回目dbt等)のE2判定基準と揃えて確認したうえで採否を決めているか、確認してほしい。なお、E2を印に解決しない場合、E2は「印を探し尽くしても見つからない」という案Bの適用場面(設計票§2「案B」)に本来当てはまらないため(何かは見つかっている)、8-034を「残り」から除く判定の前提(案Bの条件(1)が未判別要素の全部に成立していること)が崩れる点も併せて注意されたい。
>
> 4. [聞く] 生ログ末尾の最終確認(`docs/DATA/probes/20260923_tools_8_run32.log:1885`付近、`ls /tmp | grep -c -E '^pip-|^pandas'`)は、実際に発生した scratchpad 外の書き込み(`/tmp/nul`、419バイト、18:35:44。検収§2の2でリードが `find / -xdev -newermt` を使って事後的に発見)を検知できるパターンになっていない(`nul`はこの正規表現に一致しない)。`/tmp/nul`の発生手は生ログにもこの回の自己点検にも記録が無く、起動文§2.5が求める「常駐のプロセスを残さない」等の確認と同様に、scratchpad外書き込みの自己点検も生ログに残る形で行われるべきだが、今回はそうなっていない。次回以降、この最後の確認手のパターンを広げる(または`find /tmp -maxdepth 1 -newermt <この回の開始時刻>`のような形にする)必要がないか、確認してほしい。

## 4. 処置(リード、監査 128 回目)

1. **直した**。報告の節の末尾の「リードの追記」に、リードが打ち直した 3 つの検査の出力の全文を貼った(`check-elements --round 32` 3 件の全文・`check "" run32.log` 0 件・消えた行 0)。調査班が貼らなかったことは記録する(24〜26 回目と同じ型)。
2. **直した**。リードの誤り。生ログ 920 行の手は `src/pandas`(取ったリポジトリの根)に `find -type d` を打っており、`.github`・`ci`・`doc`・`pandas`・`scripts`・`web` を含むリポジトリの全部のディレクトリが母集合だった。§2 の 5 と「リードの読みの案」の「`pandas/` の下だけ」を消し、「リポジトリの全部のディレクトリ(生ログ 919〜1215 行)」に直した。
3. **答える**。14 回目の Great Expectations(`expect_column_values_to_not_be_null` などは合否を自分で出すので段 3 以上)と比べると、`asfreq` は合否を出さない。段の定義の段 2「呼ぶと結果(差分・異常の一覧・再生の出力など)を出すが、合否や検出を自分で判定しない(人が読んで決める)」に当たるかが問い。docstring は、元の時系列に無かった時点(欠けた時点)を名指しで扱い、それを `NaN` で出力に示すと書いている(「The values corresponding to any timesteps in the new index which were not present in the original index will be null」)。欠けを埋める・直すのではなく(`method` を与えない既定の動き)、欠けた位置を出力の中に示すので、「報告する」の段 2 に当たると読む。**`印`・段 2 のまま台帳に入れる。**ただし、これは汎用の道具の E2 の当て方の問い(28 回目の検収 §4 の処置 3 で、区分の完了の報告でオーナーに見せると決めたもの)に入れて、オーナーに見せる。E2 を `未判別` にしたら 8-034 は案 B の場面に当たらなくなる、という指摘はそのとおりで、その場合は「残り」に数える。
4. **直す**。次の起動文から、最初の手で開始の時刻を印字し、最後の手を `find /tmp -maxdepth 1 -newermt '<開始の時刻>'` と `find /root/.cache/pip -newermt '<開始の時刻>' -type f` にする(出力ごと生ログに出す)。この回の `/tmp/nul` と pip のキャッシュの 2 個は、リードが事後に見つけて片付けた(§2 の 2)。

### 8-034 の扱い(台帳に入れる値)

- E1a 印・段 4 / E1b 印・段 2 / E3b 印・段 4 / E5 印・段 2(根拠の種類はこの回の実測)
- **E2 印・段 2**(`asfreq`。汎用の道具の E2 の当て方として、区分の完了の報告でオーナーに見せる)
- E3a なし(一覧 2,234 件 / 読んだ 2,234 件、当たり 44 ファイル / 66 行)
- E4・E6 未判別(案 B の記録あり。文書は全 217 頁の道と題、ソースはリポジトリの全部のディレクトリに語を当てて選んだ)
- 状態 `浅い`、**「残り」に数えない**(案 B の条件: (1) 未判別の要素の全部に読んだ範囲と当たりの行の数がある (2) §4.0 は 43 項目のうち一次資料 8・実測 31 = 39、監査 128 回目の数え直し)。**残り 5**(OpenClaw・MetaTrader・Spark・NumPy・SciPy)。

### リードの読みの案(監査に掛ける)

- E1a 印・段 4 / E1b 印・段 2 / E3b 印・段 4 / E5 印・段 2 は、この回の実測で根拠の種類を `実測` に戻す(§6-1 の後の実行)。
- **E2 → 印・段 2**(リードの読み)。対象(時系列)と欠け(新しい索引にあって元に無い時点を `NaN` で示す)が同じ機能の逐語にある。欠けの判定は利用者が出力を読んで行う(自動で合否を出さない)ので段 2。**安全側に振って `未判別` に置く理由を、リードは持っていない**(A-18)。
- E3a → `なし`(一覧 2,234 件 / 読んだ 2,234 件、当たり 44 ファイル / 66 行)を受け取る。
- E4・E6 → `未判別`、案 B の記録あり。ソースはリポジトリの全部のディレクトリ、文書は 217 頁の全部に語を当てている。**8-034 は「残り」に数えない**(案 B の条件: 未判別の要素の全部に読んだ範囲と当たりの行の数があり、§4.0 の過半が一次資料か実測)。
- 違反(2 の 2)は記録する。次の回の起動文に、`pip` の手の全部に `PIP_CACHE_DIR` を付けること、報告の下書きも scratchpad の下に書くことを入れる。
