# 区分 1 の 17 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 17 回目の実行(2026-09-22)」(5353 行目以降)
生ログ: `docs/DATA/probes/20260922_tools_1_run17.log`(新規。復帰文字 0 個)

## 0. 無改変・検査

既存の 5352 行は **無改変**。生ログ 15 本を渡して打ち直し、**13 本すべて 0 件**(4 回連続)。閉じ残し 0 件。

## 1. この回の成果 — **同じ問いに対する 2 つの立場が、2 つの実装として揃った**

**候補 104 は先行注文量を外から読める。**リードが原典で確認した(`lib/OrderBook/src/entries/OrderBookEntry.cpp`
の `Limit::GetOrderRecords()`):

> `uint32_t queuePosition = 0;` … `orderRecords.push_back(OrderStruct{ …, queuePosition, }); queuePosition++;`

調査班の実測: 先行 2 本(50 枚・30 枚)の後ろに 10 枚を置くと `queuePosition=2`、先行 A を消すと **`queuePosition=1` へ繰り上がる。**

**これは候補 95 と正反対の立場である。**95 は README で自分から
「Cancellations ahead of us are **invisible**, so we never move up the queue」と書き、繰り上がりを捨てている。
**「前の注文が取り消されたとき自分は前に出られるか」という同じ問いに、繰り上がる実装(104)と繰り上がらない実装(95)が
両方手元にある。**当方の `engine.py` はそもそも待ち行列を持たない(「grants the fill on any strict through-trade」)。

**リードが原典で見つけた欠陥(調査班の報告と一致)**: `GetOrderRecords()` の
`entryPtr = entryPtr->next;` が `if` の中に在る。**残量 0 の注文が列に残ると、進める文が実行されず止まらなくなる。**

## 2. 確定した状態(C++ 4 件)

| # | 状態 | 構築の時間 | 先行注文量 |
|---|---|---|---|
| 101 `akurkar07/OrderBook` | 構築して動かした(`ctest` 5 本が通った) | 8.73 秒 | **内部に持つが外から読めない**(公開 API が 4 つだけ) |
| 102 `3yit/...` | 構築して動かした(C++ と Python の両方) | C++ 24.98 秒 / 束縛 14.05 秒 | **持たない**(代わりに `Imbalance`) |
| 103 `IsaacCheng9/...` | **この環境からは到達できない**(3 経路とも塞がった) | — | 未確認 |
| 104 `jxm35/...` | `lib/OrderBook` の範囲で構築して動かした | 2.12 + 3.78 秒 | **持つ。外から読める。繰り上がりも実装** |

**隠し玉・表示サイズは 4 件ともどれも持たない。**16 回目の 3 件と合わせ、**7 件連続で持たない。**
当方に無いのは**先行注文量**であって隠し玉ではない、という 16 回目の結論が補強された。

**103 の「到達できない」は 3 経路を全部試している**: docker の socket が無い / `dockerd` は在るが `sudo -n` が通らない /
docker を通さず import すると `pydantic` が無く、さらに `requires-python` が `>=3.14`(この環境は 3.11.15)。
**第 2 経路のコマンドも書いてある。**正しい書き方。

**116 `blotter::acOptTxns`**: この環境に R は無い(`command -v R Rscript` が exit=1)。
**第 4 経路 `raw.githubusercontent.com` で原典に到達。**模型は Almgren-Chriss の 2 分割(恒久 `gamma * v_k` /
一時 `epsilon * sgn(n_k) + eta * v_k`)。**候補 105 と同じ 2 分割だが、105 は平方根則・べき乗則も持つのに 116 は線形のみ。**

## 3. 許諾 — **重い**

**許諾があるのは 101(MIT)だけ。102・103・104 は `LICENSE` も `COPYING` も無い。**
リードが打ち直して確認: 102 と 104 は `LICENSE` が **404**、101 は **200**。
**16 回目の 96 番の所見(上流の 104 に許諾が無く、複製の側が足している)と一致する。**
**最も欲しい機能(先行注文量を外から読める)を持つ 104 に、許諾が無い。**

## 4. 調査班が渡してきた判断への回答(5 件)

1. **104 の構築で、配布物に無いヘッダ 2 本を代用し、`<cstdint>` の取り込み忘れを補った → 受け入れる。**
   **ただし表の書き方を変える。**「構築して動かした」ではなく
   **「配布物そのままでは構築できない(欠けているヘッダ 2 本を代用、取り込み忘れを補った)。補ったうえで動いた」**
   と書くこと。**これは当方の作業の瑕疵ではなく、その道具についての所見である。**
2. **102 の Python 束縛に `-DCMAKE_POSITION_INDEPENDENT_CODE=ON` を足した → 同じ。**
   **「配布物のままでは束縛の構築が落ちる」**と書くこと。
3. **102 の保守者名の食い違い(所有者 `3yit` / 唯一の貢献者 `Rimcheb`)を止める理由にしなかった → 妥当。**
   **規則として書く**: **名前の不一致だけでは止めない。**止めるのは**取り込むものが変わりうる型**
   (導入時実行・外部からの取得・同梱バイナリ・難読化)である。名前の不一致は**表に残し、使う段でオーナーに出す。**
   今回は高い信号の 4 項目が全部 0 件だったので、進めて所見を残したのは正しい。
4. **候補 109 の要旨に出た `LOB-Bench` に番号を付けなかった → 次の回で候補に立てる。**
   「新しい検索計画を打たない」は検索計画の話で、**読んだ一次資料から出た名前は候補である**(§3-3)。リードの指示が曖昧だった。
5. `sortedcontainers` を scratchpad の venv に入れた → 問題なし。

**外部からの取得の所見**: 104 の tests と benchmarks は googletest の zip と benchmark の git を取りに行く。
**調査班はその 2 つの下位ディレクトリを構築せず、経路を 1 度も通していない。**正しい避け方。

## 5. 6 要素の残り

足 16 / ティック 2 / **板の待ち行列 4**(8 → 4)/ イベント駆動 8 / ベクトル化 3 / 市場影響と約定の模型 2。
**区分 1 は未完了。**
