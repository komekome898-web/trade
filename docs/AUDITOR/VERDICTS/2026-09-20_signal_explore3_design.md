# 監査記録: 探索段 3 の設計(`SIGNAL_EXPLORE3_DESIGN_2026-09-20.md`)

## 1 回目(2026-09-20、対象コミット 04493ca)

監査役の出力(逐語。監査役は役割定義により「通す / 止める」の判定語を付けていない。[止める] 2 件があるので止められたものとして処置した):

> 1. [聞く] file:24(§0.1) vs file:153-165(§7) — L-247でオーナーに見せた「空の行」の要約(`docs/OWNER_LOG.md` L-247)は「基準の取り方(束の最初の清算の直前の約定)・h=60秒/5分/15分・後続の清算は条件付けせず全体と『180秒以上』を併記」の3点のみである。しかし§7には、基準の具体式(`at_or_before(start_ms−1, 300_000)`、5分以内に無ければNaN)、戻り率f(h)の3区分の定義そのもの、対照(a)の掃き合わせ方式、SE・gap・多重性・テスト計画など、L-247の要約には現れない多数の決定が並んでいる。この段の中心的な測定方法(f・3区分・対照(a))がL-247で承認された「空の行」の中身に含まれていたか、それとも承認後にリードが単独で具体化したものか(§0.1、P-9)。
>
> 2. [直す] file:6-17(用語表) vs file:143,86,99,111 — `r_end` が本文(§5「リードの読み」・jev_design観測量表)で使われているが、用語表には定義されていない(P2)。
>
> 3. [直す] file:150(§6) — 「31群(10属性30群+sweepの3分位=33群)」の「31群」は30+3=33と食い違う。直後の「×h3=99行」は33群を使っており、合計165行の内訳を検算しようとすると誤ったラベルに当たる(P5・P2)。
>
> 4. [止める] file:158(§7、対照(a)の掃き合わせ) — 実行手順が機械的に一意でない。(i) 「清算側の束の幅の中央値」がgap30/60/180のどれに対応する中央値か(gapごとに別値か単一値の流用か)不明。(ii) `scripts/o3c_reaction.py` を見ると対照(ii)(`control_matched`)は既に薄さ(bin_pct)で1束につき1本の時刻が確定している(1対1)。それにもかかわらず§7は「対照(ii)の時刻から前に…符号付き変化を取り」「同じ10分位帯・同じ向きの候補を…1対1で取る」と書いており、候補が複数無ければ分位帯で選別する余地が無い(候補プールがどこから来るか本文に無い)。探索段2の設計監査1回目・指摘3、2回目・指摘1は同型の「対照の生成方法が機械的に一意でない」を理由に[止める]と判定した前例がある(P3)。
>
> 5. [聞く] file:65(§4(b)) vs file:150(§6) — §4(b)は「基準からの量ではないので、F2の3区分は清算側だけ」と明記するが、§6の出力一覧はF2を「h3×{清算 全体、清算180秒以上、対照(a)、対照(b)-i、対照(b)-ii}=15行」と、対照(b)-i/(b)-iiも同じ15行構造の中に含めている。対照(b)の行に3区分が入らないなら、その行は何を格納する構造なのか本文からは読めない(P2・P3)。
>
> 6. [聞く] file:143(§5「リードの読み」) — 「対照(a)が**構造で**同じ大きさの値動きを要求する(合わせるのではなく、比較の定義に入れる)」という表現は、探索段2設計監査2回目・指摘4で「Δを同じく当てることでanchor_lag_msを**構造で**そろえる」という測定前の断定が指摘され「そろうかはE0で測る」に直された前例と同型の言い回しに見える。この段には対照(a)が実際にどう振る舞うかを測る出力(E0に相当するもの)が計画されていない(前例参照、P6)。
>
> 7. [止める] file:全体(REFUTER_REVIEW2_2026-09-19.md の「直すべき」10件との突合せ) — 以下が本設計のどこにも扱われておらず、「扱わない」の明示も無い: #3(両側混在束の除外率を表に出す)、#4(短いhの3区分を群間で比べない — この段のF2という中心的比較に直結する)、#7(清算行の2倍重複は3日分しか確認していない=未確認のまま持ち越し)、#10(`doi_pre_1h`/`implied_leverage`の欠測日の偏り、F5で同じ10属性をそのまま再利用)。探索段2の設計監査1回目は同型の未処理(5件が沈黙のまま)を理由に[止める]と判定した前例がある(確認事項(3)への直接の回答、P3)。
>
> 8. [聞く] file:147-148(§6) — 終端は`at_or_after(end_ms)`、h後の価格は`at_or_before(終端+h)`という非対称な取り方をしているが、対照(a)・対照(b)にも同じ非対称性がどう効くかを検証する出力(探索段2のE0=anchor lag分布に相当するもの)が計画に無い。致命1(起点の非対称)の再発確認手段が無いのではないか(前例参照、P3)。
>
> 9. [聞く] file:11,155(§7 基準の定義) — 基準は`at_or_before(start_ms−1, 300_000)`で取るが、直前の別の束(先行するカスケード)が300秒以内に終わっていた場合、基準の約定が「この束の前の平常水準」ではなく「直前の束の一部」を拾う可能性がある。F1の自己点検(「基準の約定が束の中の約定か」)は同じ束への混入だけを見ており、別の(直前の)束からの混入は言及がない(確認事項(5)への直接の回答、P3)。
>
> 10. [直す] file:15(用語表「sweep」) vs file:131,158(「sweep_bp」) — 同一概念の呼称が本文中で「sweep」と「sweep_bp」の間で揺れている(P2、軽微)。
>
> 以上10件。他は明確な機械的不整合として指摘するには根拠が弱いと判断し省略しました(P3のjev_design 2表の存在自体、候補ファイルの件数(8件・11件)、§6の合計165行の総和、f(h)の3区分の境界の数学的整合性、致命1〜3の扱いについては本文中に明示的な対応があり問題なしと判断しました)。

### リードの応答(1 件ずつ)

1. 事実: L-247 の直前にオーナーへ見せた文は「h 後の価格が基準を清算と逆の側に越えていれば行き過ぎの修正、基準と束の終わりの間にあれば減衰」まで(会話記録から逐語で確認)。3 つ目の区分(f < 0)・f を比で置くこと・基準 5 分の NaN・対照 (a) は承認後のリードの具体化。→ §0.1 の表に空の行として足し、オーナーに見せる(対照 (a) に異論があれば外す。清算側の行は (a) に依らない)。SE・gap・多重性・テストは `research-protocol` の固定手順(L-179 の例外)としてその旨を表に書いた。
2. 用語表に `r_end(h)` を足した。
3. 「31 群」→ 33 群に直し、行数を数え直した(合計 240 行。§6)。
4. 対照 (a) を §4 の手順 1〜5 に書き直した: 候補 = 10 秒刻み 8,640 点、束ごとの幅 W_b(中央値を使わない)、同じ 10 分位帯の中で |c_t − S_b| 最小、同値は早い t、置換なし、束と重なる区間は除く。対照 (ii) の時刻は使わない。
5. §4(b) と §6 を合わせた: 3 区分は清算と対照 (a)。F2 の列を明記し、(b) の行は r_end と約定なしの割合だけ(3 区分の列は「—」)。
6. 「構造で」を取り下げ、F0(|c_t − S_b| の分位)で測ると書いた。
7. §8 に反証者 2 の 10 件の表(扱う / 扱わない と理由)。#3 → F1 に除外率、#4 → h ≥ 60 秒だけ + 約定なしの割合を添える、#7 → 472 日の多重度を `summary.json` に、#10 → F5 に含む日数と欠測の割合の列。
8. F0 に 4 種 × (基準の遅れ・終端の遅れ・h 後の遅れ)の分位を置いた(探索段 2 の E0 に当たる)。
9. `prev_bundle_gap_s` を行に残し、F1 に「基準の約定が直前の束の `end_ms` 以下の束の数」と「前の束から 300 秒未満の割合」、F2 に「前の束から 180 秒以上」の部分集合を足した。
10. 用語表で「sweep(列名 `sweep_bp`)」と同一の量であることを書いた。

## 2 回目(2026-09-20、対象コミット b81cfd7)

監査役の出力(逐語。[止める] なし):

> 1. [直す] file:79 vs file:165 — §4 の F0 自己点検の定義は「h 後の価格が終端と同じ(約定なし)割合」を 4 種それぞれの出力に含めると書いているが、§6 の F0 行数式「4 種×(基準の遅れ+終端の遅れ+h後の遅れ3)=20行×gap3本=60行、対照(a)の合わせ具合1行×gap3本=3行。計63行」にはこの量が行として数えられていない。63 行(ひいては合計 240 行)はこの量を欠いたまま計算されていないか(P5・P2)。
>
> 2. [直す] file:175 vs file:166 — §7「直前の束からの混入」(監査1回目・指摘9の処置)は「F1 に『基準の約定の時刻が直前の束の `end_ms` 以下である束の数』と『前の束から 300 秒未満の束の割合』を出す」と書くが、§6 の F1 行数式「全体・側2・束の件数3=6行+両側混在で除外した束の割合1行=7行×gap3本=21行」にはこの2項目が算入されていない。21 行(ひいては合計 240 行)はこの追加分を数え落としていないか(P5)。
>
> 3. [聞く] file:168-169 vs file:184 — F0・F1・F2 は gap 3 本すべて出すのに対し F3・F5 は「(gap 60)」限定で、§7 は「gap: 60 主、30/180 併記(固定しない)」と書く。F3・F5 だけ gap 30/180 を出さない理由が本文のどこにも書かれていない(A-10「族・スコープを説明なく縮める」、P13)。
>
> 4. [聞く] file:71-79(§4 対照 (a)) — 「S_b の 10 分位帯(同じ gap の S_b > 0 の束で切る)」の区切り方(分位点の計算法・タイの扱い)と、候補 c_t(sgn(s) を掛けるため負になりうる)が S_b > 0 だけから切った分位帯の下限を下回る場合の扱いが書かれていない。1 回目監査の指摘4「実行手順が機械的に一意でない」と同型の未規定がこの角のケースに残っていないか(P3)。
>
> 5. [聞く] file:181(§7) vs `/home/user/trade/docs/DATA.md:134` — `docs/DATA.md`(2026-09-17、`ACTION_LOG.md` #053)はすでに 472 日全部の清算行多重度(全列一致で一意化、106,822 行→53,398、多重度2が53,385群・4が13群)を測っている。ところが `REFUTER_REVIEW2_2026-09-19.md`(致命ではないが直すべき#7)は「3 日分しか確認していない(未確認)」とし、この設計の §7 も「委任先は472日すべてで…数え、summary.jsonに残す」とゼロから再委任している。この設計が求める多重度測定(`build_cascades` の `bundle_n_events_dedup` 由来)は `DATA.md` の既存測定と同じ問いか、違うなら違いが本文のどこにも書かれていないのはなぜか(P1 関連: 既存資料を確認せずに「未確認」を引き継いでいる)。
>
> 6. [聞く] file:33(§0.1) vs file:55,78-79(§2 F2・§4(b)) — §0.1 は対照 (a) を「(該当語なし)—リードの具体化…オーナーの異論があれば対照 (a) は報告から外す」と明記するが、§4(b) は「F2 の 3 区分は清算側と対照 (a) だけ」と定義し、この段の中心的な問い(F2、行き過ぎか減衰か)の比較対象を対照 (a) に一本化している。オーナーが対照 (a) に異論を示した場合、F2 はどの対照も持たずに清算側だけの 3 区分になるという理解でよいか、それとも設計そのものが組み直しを要するか。
>
> 7. [聞く] file:200(§8 #8) — `REFUTER_REVIEW2_2026-09-19.md` の #8(対照 (i) で落とす 180/82/447 行の時刻の偏り)への処置は「扱わない: h≤15分なので落とす行は少ない見込みだが、落とした行数と時刻の分位はsummary.jsonに残す」と書くが、この段の対照 (b)-i は「日集約版だけ」(§8 #1 の処置)であり、探索段 2 の 1 対 1 版のような行の切り捨ては日集約版には存在しないはずである。ここでいう「落とす行」は対照 (a) のマッチ失敗束(§4 手順4)を指しているのか、それとも今回使わないはずの 1 対 1 対照の切り捨てを指しているのか、本文からは判別できない(P2・P3)。

### リードの応答(1 件ずつ)

1. 約定なしの割合は h 後の遅れの行の列であり行は増えない、と §4 に書いた。
2. F1 の 2 項目は各行の列であり行は増えない、と §7 に書いた。
3. F3 は gap 3 本に広げた(9 行、合計 246 行)。F5 は gap 60 だけに残し、理由(30 群の切り値が探索段 1 の gap 60 の表で切ったもので、gap ごとに切り直すと探索段 1・2 と並ばない)と「gap 30 / 180 の属性は本単位では測らない」を §6 に書いた。
4. 分位帯の切り方(`numpy.quantile` 線形補間、q_0 = 0、q_10 = +∞、[q_{k−1}, q_k)、S_b も同じ規則、c_t ≤ 0 は候補にならない、空の帯は「対照 (a) なし」)を §4 手順 3 に書いた。
5. 事実: `docs/DATA.md` §2 に 2026-09-17 の実測がある(リードの確認不足。反証者 2 の「未確認」を検めずに引き継いだ)。§7 を「既に測ってある」に直し、出典を引き、委任先には同じ数え方で再計算して一致を報告させる形にした。
6. §0.1 の行に答えを書いた: 対照 (a) を外しても F2 は清算側の 3 区分だけで答えられ、(a) は「同じ大きさの値動きが清算なしで起きたときにどれだけ戻るか」の参照。組み直しは要らない。
7. §8 #8 を直した: (b)-i は日集約版で落とす行は無い。落ちるのは対照 (a) の取れなかった束だけで F0 に出す。「落とした行数を `summary.json` に」は取り下げ。

## 3 回目(2026-09-20、対象コミット 977ce70)

監査役の出力(逐語。[止める] なし):

> 1. [直す] file:74(§4 対照 (a) 手順 3)、同内容が delegation prompt にも継承 — 「帯 k=[q_{k−1},q_k)(下限を含み上限を含まない)」と定義しておきながら、直後で「c_t≤0(負または0)は帯 1 の下限 q_0=0 を**下回る**のでどの帯にも入らず、候補にならない」と書く。帯 1=[0,q_1) は下限を含むので c_t=0 は本来この帯に入るはずであり、「下回る」という説明は数学的に誤り。境界値(c_t=0)の扱いが規則の中で自己矛盾している(P2・P3)。
>
> 2. [聞く] file:73-75(§4 対照 (a) 手順 3・4) — 「切り値が同値で帯が空になる場合はその帯の束は『対照 (a) なし』」という規定はあるが、帯自体(カットオフ)は正常に存在するのに、その日・その gap の生存候補(重複除外・置換なし後)がたまたま 1 つもその帯に落ちない場合の扱いが書かれていない。1 回目監査・指摘 4 が求めた「機械的に一意」がこの角のケースで再び曖昧なままではないか(P3)。
>
> 3. [聞く] file:58(§2 F5)・65(§3)・144-154(§5 共変量表) — F5 は「探索段 1 と同じ 10 属性 30 群」(側・時刻帯・`doi_pre_1h`・`dist_node_bp` を含む 8 連続属性)を使うと明記するが、§3・§5 の jev_design 共変量リスト(11 件)は「探索段 1 の属性 8」と称しながら実際は `dist_node_bp` と `doi_pre_1h` を欠いた別組成(側・時刻帯 + 6 連続属性)。`doi_pre_1h` は反証者 2 の #10 で欠測日の偏りを名指しされた属性であり、この除外が意図的か見落としかの理由が本文のどこにも書かれていない(P2・P5、A-10 関連)。
>
> 4. [聞く] file:83(§5 コマンド)・134(判定の量)・157(リードの読み) — `rank-covariates` は判定の量に `r_pre` を渡しているが、「リードの読み」は同じ §5 で「主な量は `retrace_fraction`・`overshoot_share`(1・2 位『直接』)、`r_pre` は代理(2〜3 位)」と明記する。対照で合わせるべき変数の判定が、F2 が実際に報告する主要量(戻り率 f・3 区分)ではなく、その代理量に対して行われている。この選び方の理由が書かれていない(item(a)9・P3)。
>
> 5. [聞く] file:169(§6 F5) — 「gap 30/180 を F5 で測らない理由」は「10 属性 30 群の切り値は探索段 1 が gap 60 の表で切ったもの」という話のみで、この段で新設した sweep の 3 分位(33 群のうち 3 群)が同じ理由で gap 60 限定になる説明になっていない。また sweep 3 分位の切り方(分位手法・タイの扱い・S_b>0 の束だけを母集団にするか全束か)は、対照 (a) の分位帯(§4)ほど詳細に規定されていない(P3)。
>
> 6. [直す] `docs/DATA/delegations/20260920_o3c_signal_explore3_prompt.md:15` — 「SE は日でクラスタ…**naive** も併記」と書くが、設計文書には「naive」という語も定義も一度も現れない(設計は「日で等重みの点推定を併記」としか書いていない、§7 line183)。委任文が設計に無い語・量を独自に追加している(P2、委任文と設計の食い違い)。
>
> 7. [聞く] `SIGNAL_EXPLORE3_DESIGN_2026-09-20.md:167`(§6 F2 列挙) vs `20260920_o3c_signal_explore3_prompt.md:17`(f2_retrace.csv 列一覧) — 設計の F2 列挙(r_end 平均±SE、r_pre 平均±SE …)には naive SE・日等重みの平均が無いが、委任文の f2_retrace.csv 仕様にはこれらが追加されている。委任文は「設計から逸脱しないこと」を求められているが、設計の F2 列挙自体がこの追加を反映しておらず、委任文の詳細が設計より先行してしまっている(P2)。
>
> 8. [聞く] file:24-33(§0.1) vs `20260920_o3c_signal_explore3_prompt.md` 全体 — §0.1 は対照 (a)・戻り率 f の 3 区分・基準 5 分の NaN 規則を「(該当語なし)—リードの具体化…オーナーの異論があれば対照 (a) は報告から外す」と明記し、まだオーナー確認が取れていないことを自ら記録している。にもかかわらず委任文は既に対照 (a) の実装・実行(F0・F2・F3 の相当部分)を委任先に指示している。CLAUDE.md §0.1「右が空の行があれば、そこだけ聞く」を、着手(委任)より前に満たしたか。

### リードの応答(1 件ずつ)

1. 帯 1 = (0, q_1)(0 を含まない)、帯 k ≥ 2 = [q_{k−1}, q_k) に直し、「下回る」を消した。委任文も同じ。
2. 帯が正常でも生存候補が帯に落ちなければ「対照 (a) なし」と明記した。委任文も同じ。
3. 事実: `dist_node_bp` と `doi_pre_1h` の欠落はリードの見落とし(意図ではない)。共変量ファイルを 13 件に直して `rank-covariates` を再実行し、§3 に経緯を書いた。
4. `retrace_fraction`(主)と `r_pre`(代理)の 2 回走らせ、両方の表を §5 に貼った(13 件とも両方で「合わせなくてよい」)。前版が代理だけだったことを §5 に書いた。
5. sweep の 3 分位の切り方(gap 60 の S_b > 0 を母集団、探索段 2 の `tertile_cuts` / `tertile_mask` の規則、S_b ≤ 0 は欠測)と、gap 60 だけにする理由(F5 の表に足す列。sweep の gap 別の分布は F1)を §6 に書いた。
6. naive SE(母標準偏差 / √n、探索段 2 の `mean_se_cluster` が返す)を §7 に定義した。
7. §6 の F2 の列に naive SE と日で等重みの平均を入れ、委任文と揃えた。
8. 事実: 空の行は 2026-09-20 に委任の前にオーナーへ見せた(返答待ち)が、返答を待たずに委任した。これはリードの決定で、理由(清算側の行は依らない、探索は判定でない、対照 (a) は外せる)と「『止まる』は満たしていない」ことを §0.1 の行に書いた。オーナーに再度示す。

## 4 回目(2026-09-20、対象コミット 57c2130)

監査役の出力(逐語。[止める] なし、[直す] なし):

> 1. [聞く] `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md:33` — §0.1 は対照 (a)・3 区分・NaN 規則の行を「(該当語なし)…この行は 2026-09-20 に委任の前にオーナーへ見せた(返答待ち)。**リードの決定として、返答を待たずに委任した**」と明記する(監査 3 回目・指摘 8 の処置)。実際に `docs/DATA/delegations/20260920_o3c_signal_explore3_prompt.md` は対照 (a) の実装・実行(F0・F2・F3 系)を既に委任先へ指示している。CLAUDE.md §0.1 は「空行があれば、そこだけ聞く」であって「聞いた上で返答を待たずに進めてよい」とは書いていない。自己申告どおり「『止まる』は満たしていない」状態のまま着手したことは、この行だけの判断か、それとも §0.1 が想定する「合意」概念そのものに対する例外運用として今後も使うのか(P-xx: research-protocol §0.1 の趣旨、A-13 関連)。
>
> 2. [聞く] `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md:144,171,184` — `rank-covariates` の 2 回の実行結果はいずれも 13 変数**すべて**「合わせなくてよい」で、`sweep_bp` は `retrace_fraction` で 1 位・`r_pre` で 2 位に来ている。にもかかわらず対照 (a) の中心手順(§4 手順 3)は `sweep_bp`(= S_b)を軸に候補を分位帯マッチングする設計になっている。184 行目の「リードの読み」は「同じ大きさの値動きを比較の定義に入れる(構造でそろえるのではなく F0 で測る)」と述べて整合性を図っているが、Jev が「合わせなくてよい」と判定した変数を対照生成の主軸に採用する理由が、この一文だけで十分か(P3、jev_design の判定と実際の対照設計の整合性)。
>
> 3. [聞く] `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md:194` vs `:210`、`docs/DATA/delegations/20260920_o3c_signal_explore3_prompt.md:17` — F2 の列一覧は「r_end 平均 ± 日クラスタ SE ± naive SE(6 種すべて)」に対し「r_pre 平均 ± 日クラスタ SE ± naive SE **と日で等重みの平均**(清算と (a))」と、日で等重みの点推定が r_pre にだけ付き r_end には付かない。§7(210 行目)の追記は「日で等重みの点推定を併記(探索段 2 の反証者 #6)」と量を限定せず一般的に書かれており、r_end を除外する理由が本文のどこにも無い。委任文 17 行目も同じ非対称のまま引き継いでいる。r_end を除いたのは意図的か、書き漏れか(P2・P4)。
>
> 4. [聞く] `docs/PHASE2/O3C/SIGNAL/SIGNAL_EXPLORE3_DESIGN_2026-09-20.md:188` — 「入力: …(約定 475 日・清算 472 日)。対象日は探索段 1・2 と同じ 456 日(**欠けた 18 日はそのまま**)」とあるが、472 − 456 = 16 であり 18 と一致しない(475 − 456 = 19 でも一致しない)。この「456 日」「18 日」という数字は監査 1〜3 回目のいずれでも扱われておらず、出典(探索段 1・2 のどちらの母数を基準にした欠測数か)も本文に書かれていない。この差の根拠は何か(P2、数値の出所)。
>
> 他 0 件省略(上記 4 件が本ラウンドで見つかった全て。監査 3 回目の指摘 1〜7 — 帯境界の自己矛盾、空帯/生存候補ゼロの扱い、共変量 13 件化、jev_design の 2 系列(retrace_fraction/r_pre)実行、sweep 3 分位の切り方明記、naive SE の設計への追記、F2 列一覧と委任文の整合 — は本文中でそれぞれ機械的に整合する形に直っており、指摘 8(対照 (a) 未承認のまま委任)は上記 1 として形を変えて残っている)。

### リードの応答(1 件ずつ)

1. この行だけの判断で、§0.1 の例外運用にはしない。次からは右が空の行に依る作業は返答が来るまで委任しない。是非はオーナーが決める(報告の冒頭に再掲)。§0.1 の行に書いた。
2. 対照 (a) は Jev の表から導いたものではない。`rank-covariates` の答えは「公平な比較のためにそろえる必要があるか」で、(a) は別の問い(清算なしで同じ大きさの値動きが起きたときの戻り)を置くリードの設計判断。§5 に書いた。
3. 書き漏れ。r_end にも日で等重みの平均を付けた(設計 §6 と委任文)。委任先に伝える。
4. 事実: 18 = 暦の日数 474 − 456(探索段 2 の `summary.json`)。清算 472 日との差(16)ではない。出所を §6 に書いた。
