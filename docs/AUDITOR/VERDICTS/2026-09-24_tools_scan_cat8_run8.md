# 検収: 道具サーベイ 区分 8 の 8 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 8 回目の実行(2026-09-24)` / 生ログ `docs/DATA/probes/20260923_tools_8_run8.log`(3,008 行・111 手)。届いたままのコミット 3555806。
起動文 `20260923_tools_survey_cat8_run8_prompt.md` の指紋 `dad21665a0e9`、追補の指紋 `2c178ba75341`。

## 0. 費用と時間

調査班 **558,271 トークン・2,782,473 ms(46.4 分)・道具 252 回**。生ログの最初の手 06:31:19Z / 最後の手 07:02:18Z。§2 の 10 項目すべてに手が付いた。描画の道具は使っていない。

## 1. 受け入れ検査

`check_scan_report.py`(生ログ 8 本)0 件 / `check-elements --round 8` 0 件(届いた時点の検査で)/ `check "" <run8.log>` 0 件 / 消えた行 0。

**監査 43 回目の処置 (a)〜(d) を検査に足して、届いた報告に当てた**(8 回目以降に効く): (a) 「判断に迷った点と問い」に候補の名前と要素が出る行は値・段が `未判別` / (b) `なし` の 7 列目の手の `$` の行に要素の名前の語(日本語か英語)がある / (c) 生ログの手が `/tmp` の直下(scratchpad の外)を使わない / (d) 段 4・5 の行は 6 列目に「…」の引用がある。**8 回目の節に当てて 5 件止まった**:

| 行 | 止まった理由 | 処置 |
|---|---|---|
| 8-002 `PineForge` E2(段 4) | 段 4 の条件に当たる逐語「…」が 6 列目に無い | 台帳で段を `未判別` にした(値の `印` はそのまま) |
| 8-010 `arXiv:2603.20319` E3b・E4(段 1) | 問い 2 に候補と要素が出ている | **止めない**(リードが読んだ): 問い 2 は「コードの公開を今後追うか」で、値・段についての問いではない。段 1 は「文書だけ」の条件どおり。検査の (a) は語の一致で拾うので、この形は拾いすぎる(限界として書く) |
| 8-014 `Fincept Terminal` E3a(`なし`) | 問い 1 に候補と要素が出ている(LLM に「lookahead bias を識別せよ」と指示するプロンプトが E3a に当たるか) | 台帳で `未判別` にし、状態を `浅い` にした(問いに出した行は値も段も `未判別` = 起動文 §2。4 回目の同じ誤り) |
| 8-015 `TradingView のリプレイ機能` E1b(`なし`) | 7 列目の手の検索の語に、E1b の名前(「突き合わせ」など)が無い(指標・損益の語だけ) | 台帳で `未判別` にし、状態を `浅い` にした(「`なし` と書くための条件」(b) を満たさない) |

**調査班の返答の注記**: 「当初案にあった『段の値を決めつつ同時に妥当性を疑問視する』形の問い 2 件は、値・段をすでに確定させたため質問リストから外し」とある。問いに出す前に値を決めて、問いを消した形で、起動文の規則(問いに出した行は `未判別`)を避けている。どの 2 件かが報告に無いので、監査役(44 回目)に、知見の表に移された 2 件を探して述語に当たるかを見てもらう。

## 2. `なし` の抜き取り

- 8-015 の E1b は §1 のとおり。
- 8-013 E6(`なし`): `validate_db_by_date` は `cargo check -p bitflyer -v` でコンパイルの対象に一度も入らないことを実測した(生ログ)。CLAUDE.md §0.2 A-11(目で見て死んだコードと決めない)に対し、目視ではなく実測で決めている。述語は「機能を持つ」で、どこからも呼ばれずコンパイルもされない関数は、その道具の機能として持っていない、と読む。受け取る。
- ほかの `なし`(8-002 E6 1348 件・8-004 E6 18 件・8-009 6 要素 41 件・8-010 E3a/E6 58 節・8-011 E1a/E3a・8-012 E1a/E3a/E6 206 件・8-014 E3a・8-015 の 5 要素 8 頁)は、検査 (b)(c) を通った(7 列目の手に要素の語がある / `/tmp` の直下を使っていない)。N の数え方(除外の理由)は監査役に見てもらう。

## 3. 台帳

`import --round 8`: 変わった所 13。§1 の 3 行を `set` で直した。`check` 0 件。`recount`: 深掘り 11 / 浅い 2(8-014・8-015)/ 判別に一次資料が要る 1(8-016)/ 危険で導入停止 1 / 登録が要る 1。**残り 3 行**(8-014・8-015・8-016)。8-002・8-013 は `深掘り` に進んだ。

## 4. 判定

**一部受け取り**(§1 の 3 行を台帳で直した)。新しい `印` が 5 件ある(8-004 E2 段 4・8-010 E3b/E4 段 1・8-011 E6 段 5・8-014 E6 段 2)ので、中身の回として監査役(44 回目)に通す。

## 監査 44 回目(中身の回。対象 = 3555806 の 8 回目の節と生ログ)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 6 件・所見 2 件):

> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2928` — `akurkar07/OrderBook` E2 の段4(「対象(Order構造体)は呼び出し側がAPI経由で任意の外部データから構築できるため段4(自動判定+枠の外の対象)」)は、根拠に引いた生ログ(`run8.log:677-683`, `2972-2982`)のどの `$` の出力にも対応する逐語が無い。起動文自身が「段4・5 を付けるときは、その条件…に当たる逐語を、7列目の行の手の出力に出す。逐語が無ければ段は `未判別`」(起動文§2)と定めているのに、この行はリードの推論(「呼び出し側が…できるため」)だけで段4にしている。C++の関数はどの関数も呼び出し側が任意の引数を構築できるため、この基準では事実上すべてが段4になり得る — 「その道具の枠組みの中」(段3)と「枠の外」(段4)を区別する逐語上の根拠が本当にあるか。(CAT8_DESIGN.md §4.1、起動文§2)
>
> 2. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2896` — `TradingView のリプレイ機能`(8-015)の変更点欄「N=8頁と確定。folderのsolutions辞書から機械的に切り出して確定…実際のフォルダ内訳で確定」という記述が、生ログの実測と食い違う。`run8.log:1758`(波括弧対応で正確に切り出した「厳密抽出v2」)の出力は「件数: 7」であり、フォルダ自身の `solutions` 辞書からの機械的抽出では7件しか出ていない。8件目(`43000747376` 「How to select replay interval for the Bar Replay in mobile apps」)は、フォルダ限定の抽出ではなく site-wide のタイトル検索(`replay` を含む17件)との union から HTTP 200 だったために加わったもの(`run8.log:2245-2269`)。「フォルダのsolutions辞書から確定」という報告の文言は、この8件目の出所を隠す。N の確定方法として起動文が求める「一覧を取った手の出力の件数そのもの」(起動文§2)に、この8件目は一次資料(フォルダ辞書)からは出ていない。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run8.md:23` — 「調査班の返答の注記」として引用された「段の値を決めつつ同時に妥当性を疑問視する」形の問い2件を「値・段をすでに確定させたため質問リストから外し」たという説明は、`docs/DATA/SCAN_2026-09-23_tools_cat8.md` の committed な本文(知見表・「判断に迷った点と問い」節)のどこにもこの言い回しでは現れず、どの2件かをテキストから機械的に特定できなかった(唯一近い候補は `SCAN_2026-09-23_tools_cat8.md:2874` の知見#16「外から持ち込める対象…未検証、コードの型注釈から…段4の条件は満たし(ア)(イ)も満たすため段5」で、これは値(段5)を決めながら支える根拠(外部入力を受け付けること)自体を「未検証」と書いている)。この構造(先に決めてから疑問を質問リストに載せずに知見側だけに残す)は、検収の検査(a)「問いに出した行は未判別」が捕まえられない抜け道になっている — 問いに出さなければ検査は発火しないため。この注記の出所(調査班の返答のどの部分か)と、残る1件が何かを、リードは特定できているか。(research-protocol §0.2 O-2 に近い型: 断定の根拠が本文から再照合できない)
>
> 4. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3013` と `:3009`(=8-014 `Fincept Terminal` の要素と段の表、E3a行)— 同じ候補・同じ `hedgeFundAgents` の LLM プロンプト指示について、E3a では「LLMの確率的な振る舞いで決定的な検出機能とは言い切れない」として `なし`(未確定のまま問いに出す)としたのに対し、E6(`SignalValidationWorkflow`)では同種のLLMプロンプト駆動の判断を「自動で判定するがLLM/人が最終判断」として段2の `印` にしている。同じ「LLMの確率的判断」という性質を、E3aでは機能として認めず、E6では機能として認めている。この扱いの違いは述語(E3a/E6)の違いから必然的に出るものか、それとも同じ疑わしさを一方だけ問いに出さずに通した結果か。(CAT8_DESIGN.md §3 の述語の一貫適用、P2 に近い型)
>
> 5. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2989`(`arXiv:2512.12924` E6 段5)— `hdt/stats.py` の `minimum_track_record_length` 等(`run8.log:1090-1106`)は数値(float)を返す統計量の計算関数であり、コード上に合否を自動で出す分岐(`pass`/`fail` を返す・閾値と比較して真偽を返す)は生ログに現れていない。CAT8_DESIGN.md §4.1 の段3の条件「合否・検出…を自動で行う」を満たしていることが、段2(「結果を出すが合否を自分で判定しない、人が読んで決める」)ではなく段3以上である根拠として、生ログのどの逐語で支えられているか。(ア)(イ)(利用者が信頼水準を指定できる/結果をcsvに保存し比較できる)自体は逐語で裏付けられているが、これは段4→5の条件であって、段3の前提(自動判定であること)を飛ばしていないか確認したい。
>
> 6. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2874`(知見#16)— 上記5と関連。「未検証、コードの型注釈から」という文言をそのまま段5決定の根拠行に残しており、CAT8_DESIGN.md §4.2 が求める「記録する軸」の書式(道具の側の記述だけを書く)と、リード自身が疑いを表明している文が同居している。3の指摘とあわせて、この1件は「決めながら同時に疑う」型そのものに見えるため、`未判別` にすべきではなかったか、少なくとも「判断に迷った点と問い」に載せて処理すべきではなかったか。
>
> 7. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run8.md:19` — 検収が8-010の問い2(「次回以降、このリポジトリの再確認…を追跡すべきか」)を「段の値そのものは段1で決めており、これは次回以降の追跡要否だけを問うている」として検査(a)の対象から外し「止めない」とした判断自体は、生ログ(`run8.log:774-896`)の逐語(21日ギャップ・Algorithm 1・コード未公開404)がE3b/E4段1をよく支えているため妥当に見える。ただし、この種の「値と別の質問だから検査(a)の対象外」という手動オーバーライドが今後繰り返し使われると、3の指摘と同型の抜け道(問いの言い回しを変えれば検査(a)を避けられる)になり得ないか。次回以降、この種のオーバーライドを許す基準を明文化する必要はないか。
>
> 8. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2891`(候補一覧#10、`arXiv:2603.20319` の変更点)— E3bの根拠として採用された「21日のembargoギャップ」は、論文内の15の戦略群のうち1つ(BM08、ML系)だけが持つ設計であり、論文全体(=区分8の候補としての「道具」)の汎用機能ではなく、個別の戦略の特徴量生成手順である。CAT8_DESIGN.md §3 のE3bの述語「その時点で知り得ない情報が入らないようにする機能を持つ」は、"道具"の機能を指しているはずだが、ここでは"道具で検証される個々の戦略"の設計選択を、道具側の機能として数えている。この区別(道具の機能 vs 道具で扱う一戦略の設計)は意識して線引きされているか。
>
> 9. [聞く] 全体 — 点3の N=298件中92件(VectorBT)・59件中18件(8-011実装リポジトリ)の除外は、いずれも画像・図表バイナリの拡張子(png/svg/gif/ico/jpg/pdfの図と同名対)に限られており(`run8.log:1187-1248`、`run8.log:1161`)、設計票・起動文が定める除外基準(画像・ロックファイル・バイナリのみ、除外は名前と理由を全部書く)に沿っていた。この点については具体的な逸脱は見つからなかった。
>
> 10. 点6(棄却側・安全側への振れ、8-016のE3a/E3b/E5の`未判別`)について — `SCAN_2026-09-23_tools_cat8.md:2897` の変更点欄と生ログ(`run8.log:513-635`)を見る限り、th2-net組織の総リポジトリ数を確定できない(ungh.cc・api.github.comの複数経路すべてで403/ページング不可)状態で `なし` と書かず `未判別` のまま残しており、これはむしろ安全側(A-18)を避ける挙動に見える。オーナーPCでの認証つき `gh api --paginate` 等、試していない経路があることも「代替経路」節(`SCAN_2026-09-23_tools_cat8.md:3052`)に明記されている。この点は具体的な違反は見つからなかった。

**リードの処置**:
1: 認める(検査 (d) は「…」の引用があるかを見るだけで、その引用が段 4 の条件に当たるかは見ていない = 検査の限界)。台帳で 8-004 E2 の段を `未判別` にした(値の `印` はそのまま)。
2: 直す(記録を正す)。N = 8 の出所は 2 つの一覧の和で、どちらの一覧の手も生ログにある: (1) フォルダの `solutions` の厳密な切り出し(`run8.log:1758` の手、7 件)と (2) サイト全体のタイトル検索(`replay` を含む 17 件)のうち HTTP 200 でこのフォルダの頁だった 1 件(`run8.log:2245` の手)。8 頁は全部読まれている。和の N で `なし` の読み残しは無いので `なし` は戻さないが、報告の「フォルダの solutions 辞書から確定」は事実と違う。資源化の索引では「N = 8(フォルダの一覧 7 件 + サイト検索で見つけた同じフォルダの 1 件)」と書く。9 回目以降の起動文で「N を 2 つ以上の一覧の和で作るときは、一覧ごとの件数と和を書く」と足す。
3: 答える。注記は調査班の**返答**(リードへの報告の文。報告のファイルではない)の §5 の末尾にある。どの 2 件かは返答にも書かれていない。1 件は監査役が挙げた知見 #16(8-011 E6 段 5、「未検証」と書いたまま段 5)とリードも読む。もう 1 件は特定できない。**処置**: 知見 #16 の行は、6 のとおり段を `未判別` にした。9 回目以降の起動文で「問いにしかけて取り下げた判断は、取り下げた理由と行を報告の『判断に迷った点と問い』の節に書く」と足す。
4: 認める(一方だけ問いに出していた)。同じ LLM のエージェントの判断なので、E6 も E3a と同じ扱いにし、台帳で 8-014 E6 を `未判別` にした。
5・6: 認める。段 3 の「自動で判定する」に当たる逐語(合否を返す分岐)が生ログに無い。台帳で 8-011 E6 の段を `未判別` にした。
7: 答える。検査 (a) を止めない扱いにできるのは、問いの文が**値・段についての問いでない**ときだけで、リードが検収の記録に理由を書く(8 回目の 8-010 の 1 件)。9 回目の起動文で、問いに出すときは「値か段についての問いか」を 1 語で書かせ、値・段の問いでないものは検査の対象外と書く。
8: 認める。E3b の述語は候補(道具)の機能で、論文の 15 の戦略のうち 1 つの作りは道具の機能ではない。台帳で 8-010 E3b を `未判別` にし、状態を `浅い` にした。9 回目で、論文の枠組み(全部の戦略に掛かる仕組み)に embargo があるかを当て直す。
9・10: 所見(問題なし)。

台帳: `check` 0 件。**残り 4 行**(8-010・8-014・8-015・8-016)。
