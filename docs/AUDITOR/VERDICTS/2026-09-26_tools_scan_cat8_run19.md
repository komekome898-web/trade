# 検収: 道具サーベイ 区分 8 の 19 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 19 回目の実行(2026-09-26)` / 生ログ `docs/DATA/probes/20260923_tools_8_run19.log`(2,847,371 バイト)。届いたままのコミット 30b15ec。起動文の印 `20260923_tools_survey_cat8_run19_prompt.md@4bbe7648d90b`。報告は 17,368,820 バイト。

## 0. 費用と時間

調査班 258,508 トークン・6,266,729 ms(104.4 分)・道具 477 回。

## 1. 受け入れ検査(調査班の報告)

`check-elements --round 19` 0 件 / `check` 0 件 / `check_scan_report.py` 39 件(前の回からのものと、この回の K2 1 件 = 決まりの正規表現の括弧)。`git diff -U0 HEAD`(届く前の HEAD との比較)で過去の節から消えた行は 0 件(リードが打ち直した)。調査班は途中で一括置換が過去の回の行 36 件を巻き込み、自分で戻したと報告している。

## 2. リードの誤り

- **19 回目の起動文の検索語の組に、E5(と E4)の組を書き落とした**。18 回目の起動文から写したとき、18 回目の対象に E5 が無かったので、その組も落ちていた。調査班は設計票 §3 の述語から語を組み、台帳の検査が求める語(snapshot・golden・pin・lock・版の固定)を足した(報告の問い 1)。**受け取る**(語は検査が求める組を満たしている)。次の起動文からは、要素の語の組を 8 要素とも書く。

## 3. リードが見た点と直し

- **決まりが語ごとの決まりのまま**: `(?i)compar[a-z]`(986 行)・`(?i)diff[a-z]`・`(?i)[a-z]lock`・`(?i)[a-z]pin`・`(?i)fixed\s+\S` など、検索した語に 1 字足しただけの形が多い。起動文 §2.4 が禁じた形。**起動文の一文では、この型は止まらなかった**(18 回目に続いて 2 回目)。
- **E1a の `印` は受け取らない**: 根拠「The tester compares the tick data to the minute bar parameters ... If a mismatch is detected, all ticks corresponding to this minute bar are discarded」は、データどうし(実ティックと分足)の食い違いの検出で、設計票 §3 の E2 の述語「**取引所間(情報源間)の食い違い**…を検出するか報告する」に当たる(8-029 の E2 はすでに `印`)。E1a は「2 つ以上の**実装の出力**を突き合わせて差を出す機能」(括弧の例: 差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較)で、データの突き合わせは例に当たらない。E1a の全件の検索(720 行目の手、1,572 行)は `compar[a-z]` 1 本で 986 行を取っていて支えにならない。**E1a は `未判別`**。
- **E5 の `なし` は誤り(`なし` 側に振れていた)**: リードが、E5 の機能が書かれうる頁(端末のヘルプの `algotrading/` の頁と、MQL5 の文書の `runtime/testing`・`event_handlers/ontester*`)の当たり 87 行を読んだ。`www.metatrader5.com/en/terminal/help/algotrading/testing.txt:103`「To save parameters for future use in the current platform, click "Save Version". These saved presets will be available then in the "Load Version" sub-menu. They can be applied at any time」は、テストの設定を保存して同じ設定で再実行できるようにする機能、`testing.txt:54`「Fixed Delay」はランダムな遅延の代わりに遅延を固定する設定で、どちらも E5 の述語「同じ入力から同じ出力が出ることを確かめるか**固定する**機能(乱数の種・**環境や依存の固定・実験の再実行**と結果の差分・データの版の固定)」に当たる。決まり `r_fixed_setting`・`r_product_version` がこれらの行を一般の語として取っていた。結果の差を自動で判定するとは書いていないので **`印`・段 2**。
- **E3a の `なし`**: 当たり 4 行を調査班がファイルごとに判定(ワンタイムパスワードの漏洩・メモリリークのコメント・Leaky ReLU)。**受け取る。**
- **§4.0**: 12 項目のうち 5 項目を確かめ、1 項目は「サーベイの外」、7 項目は `未確認`(版・最終更新日は頁に記載なし、ライセンスは N に頁が無い、初回公開日は Wayback への到達が 2 回失敗、最小実行・外部送信・当方データ投入はデスクトップの実行ファイルをこの環境で起動できない)。理由は「時間」ではない。
- **Oryon(8-041)の行**: この回の報告は Oryon の行を「台帳の値のまま」とせず 18 回目の報告の行(E1b 段 4)を写していたので、取り込みで台帳の 段_E1b が 3 → 4 に戻った。18 回目の検収 §2 で段 3 に直した値なので、台帳を 3 に戻した。
- **状態**: 8-029 は E1a が `未判別` なので `浅い` のまま。

## 4. 台帳

`import --round 19`: 変わった所 5。`set` で 8-029 の E1a を `未判別`、E5 を `印`・段 2、状態を `浅い`、8-041 の 段_E1b を 3。`check` 0 件。深掘り 26 / 残り 11。

## 5. 判定と、これからの手

**一部受け取り**(E3a の `なし`、§4.0 の確かめた 5 項目)。中身の監査 97 回目を通す。

語ごとの決まりは、起動文の一文では止まらなかった(18・19 回目)。道具は足さない(L-102)。代わりに、次の回から調査班に (乙) を使わせず、(甲) だけにする(ファイルごとに、当たった行を読んで語と意味を書く)。当たりの多い要素は、`印` を先に探すことと、1 回に入れる要素を減らすことで量を抑える。リードは検収で、候補の機能が書かれうる頁・ファイルの当たりを読む(18・19 回目にしたとおり)。

## 6. 中身の監査(97 回目、owner-auditor)の逐語

> # 監査 97 回目 報告
>
> ## 対象の理解
>
> 読んだもの: `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(19 回目の節、36462〜37368 行あたり)、`docs/DATA/probes/20260923_tools_8_run19.log`(該当箇所を grep で抽出)、`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md`、`docs/DATA/tools_catalog_cat8.tsv`(8-029・8-041 行)、`docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md`、`docs/DATA/surveys/CAT8_DESIGN.md`、`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md`。台帳の値は Python で列を突き合わせて確認済み(tab 区切りの手動 `sed|tr` は空欄で列がずれるため不使用)。
>
> ## 指摘
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:35656` と `:36538`(AlgoNetwork、8-037)、`:35660` と `:36542`(Oryon、8-041)— 19 回目の「候補の一覧」の AlgoNetwork・Oryon の説明文は、18 回目の節の同じ説明文と**一字一句同一**(比較済み)。にもかかわらず文面は「algorier.com全68頁を**この回に**取り直し検索」「GitHubリポジトリを**この回に**取り直し取得」「§4.0の表の未確認項目を**この回に**確認」と書かれている。19 回目の起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:20,22` は「8-029 だけ」「この回に手を付けないもの: ほかの 40 行(台帳の値のまま書く)」と明記し、同 `:78-79` は触らない行を「6 列目を『台帳の値のまま(<何>回目の節)』だけにし…7 列目は空でよい」と定めている。実際、他の 38 行の候補一覧はこの短い形になっており(例: 36502 行以降の 1〜28・30〜36・38〜40)、AlgoNetwork・Oryon だけが例外的に長文のまま残っている。さらに対応する「要素と段」表の根拠欄(36538 行下)はすべて `docs/DATA/probes/20260923_tools_8_run18.log` を引用しており、19 回目の生ログを根拠にした記載は一つも無い(=実際に何も再検証していない)。つまり報告は「この回に確認した」という主張をスコープ外の 2 候補について行っているが、根拠は前回のログのままであり、その虚偽的な体裁は `check_scan_report.py` の K13(複写検出)にも捕まっておらず(K13 の新規検出は 0 件、既存分のみ)、リードの検収 §2〜§3(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:14-25`)も 8-041 の `段_E1b` の値のずれ(3→4)という**症状**だけを直し、なぜスコープ外の候補が「この回に」と書かれて再登場したのか(単なる複写のミスか、実際にスコープ外作業をしたのか)という**原因**には触れていない。(起動文 §1・§4 の逐語違反。P-xx なし、出典は上記起動文该当行)
>
> 2. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:21`(E5 `なし`→`印`・段2 の書き換え)— 根拠は `testing.txt:103`「Save Version」「Load Version」(テスト設定のプリセットの保存・再適用)と `testing.txt:54-55`「Fixed Delay」(実際の ping に合わせて遅延を固定するリアリズム設定)。設計票 `docs/DATA/surveys/CAT8_DESIGN.md:55` の E5 述語は「**同じ入力から同じ出力が出ることを確かめるか固定する**機能」であり、段 2(`:76`)は「呼ぶと**結果(差分・異常の一覧・再生の出力など)を出す**が、合否や検出を自分で判定しない」。しかし「Save Version/Load Version」は設定プリセットの保存・再適用にすぎず、差分や結果を出す機能の記述が原文に無い。「Fixed Delay」も原文は「real ones に近い条件で試験するため」という**リアリズム**の説明であり、「同じ出力を確かめる/固定する」という再現性の文脈で書かれていない(近傍に「Random Delay」との対比を裏づける逐語も引かれていない)。この 2 例は、調査班が広すぎる決まり `r_fixed_setting`(`(?i)fixed\s+\S`、`docs/DATA/probes/20260923_tools_8_run19.log:5503`)・`r_product_version` に一般語として吸収されていたものをリードが個別に読み直して拾い上げたものだが、拾い上げた語自体が E5 の述語に**字面で当たるが機能としては当たらない**可能性が残る。18 回目の教訓(過度に広い決まりで見落とす)への対処が、今度は「拾いすぎ」に振れていないか。
>
> 3. [聞く] 上と同じ箇所 — リードが読み直したのは「端末のヘルプの `algotrading/` の頁と、MQL5 の文書の `runtime/testing`・`event_handlers/ontester*`」の当たり 87 行のみ。しかし E5 の全件検索は matched=1279(9 決まり)・unmatched=72 で構成されており、`r_fixed_setting`(200 件)・`r_product_version`(778 件)など決まりに吸収された残りの当たりは、この 87 行の対象頁以外にも多数のファイルに散っている(`docs/DATA/probes/20260923_tools_8_run19.log:5503-5100` 台の file 一覧に多数のファイル名がある)。リードの読み直しが「候補の機能が書かれうる頁」に限定した抜き取りであり、決まりが吸収した残りの当たりを機械的に信頼したままなら、E5 の`印`判定はまだ十分に検証されていない可能性がある(=同じ理由で見落としている別の当たりが無いことは確認していない)。
>
> 4. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:20`(E1a `印`→`未判別`)/ `docs/DATA/SCAN_2026-09-23_tools_cat8.md:36530` — E1a を E2 と区別した読みそのものは 18 回目の先例(戦略比較を E1a から除外した読み)と整合的に見える。しかし全件の検索(`compar[a-z]` 系、986 行)は分類済みでなく、`未判別`のまま次回へ持ち越されている。今回は「予算で止めない」(`docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:5`)回であり、`(甲)`でこの 986 行を読み切って `なし`/`印` を確定させることは同じ回内で可能だったのではないか。`未判別` を選んだことが、8-029 を`浅い`のまま次(20 回目以降)に送る安全側の選択になっていないか。
>
> 5. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:23` と 報告自身の問い `docs/DATA/SCAN_2026-09-23_tools_cat8.md:37105`(問い3)— 報告は「最小実行の可否・外部送信・当方データ投入」を`未確認`として残す理由(デスクトップの実行ファイルをこの環境で起動できない)が、起動文§2.5(`docs/DATA/delegations/20260923_tools_survey_cat8_run19_prompt.md:60`)の許す 2 類型(試した手段と出力を示したもの/鍵・口座・登録・購入が要ると分かる逐語を引いたもの)のどちらにも文字どおり当たらないと自ら明記し、18 回目の検収の同種の指摘(処置3)に照らして確認が要ると問いに残している。しかし検収 §3 は「理由は『時間』ではない」とだけ書いて一括で受け取り、この開いた問いに答えていない。加えて、生ログには MetaTrader 5 の公式 Linux 導入手順(Wine を使う旨、`install_linux.txt`、`docs/DATA/probes/20260923_tools_8_run19.log` の r_product_version バケツに読み込み済み)が存在するが、これを試みた記録は無く「試していない」とだけ書かれている(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:37077,37080,37083`)。「取れない」ではなく「試していない」と正直に書いている点は評価できるが、既知の代替経路(Wine)を試さずに`未確認`で止めてよいかは、P1 の型(全経路を試した結果でなければ否定的な状態を確定できない)に照らして検討が要る。(P1)
>
> 6. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:35`(次回から (乙) を禁止し (甲) だけにする方針)— 経緯(18・19 回目と 2 回連続で、広すぎる決まりが本来 `印` になるべき行を一般語として吸収し、しかもその見落としをリードの検収でしか捕まえられなかった)を踏まえると方向としては理解できるが、(甲)は当たった行を**全部**個別に読んで書く方式であり、当たりが多い要素(この回の E5 で 1750 行、うち 1279 行が決まりで吸収)では調査班・リードの双方の読む量を増やす方向に働く。CLAUDE.md §4 の I-007(「監査役の指摘のうち機械的な直しは委任し、リードは判断だけ書く」「300 行超のファイルを丸ごと読まない」)や、この監査の依頼が明示した「読む量の問題」に照らし、(乙)を完全に廃止する代わりに「決まりの正規表現に前後の文脈を要求し、リードが決まりごとに一定数の当たりを抜き取り確認する」といった中間的な対処を検討したか、が書かれていない。また、(甲)自体も過去(12・13・14・16・17 回目)に「同じ文言を何行にも複写する」という別の型で差し戻された経緯があり、(乙)から(甲)への一律切替がその型を再発させない保証も書かれていない。(出典: L-102、CLAUDE.md §4 I-007)
>
> 7. [直す] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run19.md:24`(Oryon の 段_E1b 修正)— 台帳のずれ(3→4→3)の**症状**は直っているが、原因(なぜスコープ外の 8-041 の値が19回目の取り込みで変わったか)の記載が「この回の報告は Oryon の行を『台帳の値のまま』とせず 18 回目の報告の行を写していた」という一文で済まされており、指摘1で挙げた候補一覧の複写問題(単なる書式の乱れか、それとも別の意図があるものか)への言及が無い。指摘1と合わせて、原因の切り分けを検収に明記すべきではないか。(研究の規律 §5.2「なぜを書かない報告は差し戻す」に近い型)
>
> ## 確認して問題が無かった点(参考)
>
> - E3a の `なし`(当たり 4 行: OTP の漏洩・メモリリークのコメント・Leaky ReLU ×2)は生ログ(`docs/DATA/probes/20260923_tools_8_run19.log:2905-2916`)と完全に一致していた。
> - §4.0 で「確認できた」とした 5 項目(既知の脆弱性・料金体系・課金開始条件・規模の見積)の逐語は、生ログの当該行(4993-4997, 5782, 5805, 5798-5801)と一致していた。
> - §4.0 の「版・最終更新日・ライセンス・初回公開日」の `未確認` は、試した手段(Wayback CDX API 2 回失敗・URL 推測 404・sitemap に該当 URL 無し)が明記されており、起動文§2.5 の類型 (a) を満たしていた。
> - 台帳 8-029・8-041 の列の値そのもの(E1a=未判別、E5=印/段2、8-041 段_E1b=3 など)は、検収の記載どおりに反映されていた。

## 7. 処置(リード、監査 97 回目)

1・7. **答える(原因を切り分けた)**。19 回目の報告の AlgoNetwork・Oryon の行は、18 回目の報告の行を一字一句写したもので、根拠の生ログも 18 回目のもの(`run18.log`)だけ。この回にそれらの候補の作業をした手は生ログに無い(19 回目の生ログは 8-029 の手だけ)。**作業をしたのではなく、写しの誤り**で、「この回に」という文は 18 回目の文がそのまま残ったもの(事実ではない)。原因はリードの起動文にある: 19 回目の起動文 §0 の 4 が「この回の節の小節・表の形は、この節(18 回目の節)を見本にする」と書き、調査班が見本の行を、形だけでなく中身ごと写した。台帳への影響は Oryon の 段_E1b(3 → 4)だけで、§4 で 3 に戻してある。AlgoNetwork の値は 18 回目と同じなので、台帳は変わっていない。次の起動文に「見本は形だけ。この回に触らない行は、6 列目を `台帳の値のまま(<何>回目の節)` だけにする。ほかの回の行を写さない」と書く。
2. **直した(一部を外した)**。「Fixed Delay」の原文は、実際の通信の遅れに合わせて試験するための設定で、再現のための文ではない。E5 の根拠から外す。「Save Version」「Load Version」(テストの設定を保存し、いつでも同じ設定を当て直せる)は、E5 の述語「同じ入力から同じ出力が出ることを…**固定する**機能(…環境や依存の固定・**実験の再実行**…)」の「実験の再実行」のための設定の固定に当たるので、`印` のままにする。段 2 は「呼ぶと結果を出すが、合否や検出を自分で判定しない」で、設定を当て直して再実行すると結果が出るが、同じ出力かを自動で確かめるとは書いていないので段 2 とする。
3. **答える**。リードが読んだのは、候補(Strategy Tester)の機能が書かれうる頁(端末のヘルプの `algotrading/` と、MQL5 の文書のテスターの頁)の当たり。ほかの頁(言語の一般の関数・製品の版の案内・インストール)の当たりは、Strategy Tester の機能の記述ではないと読んだ。E5 が `印` になったので、E5 について `なし` を支える必要は無い(見落としがあっても、`印` の値は変わらない。段は変わりうるので、段 3 以上の根拠がほかの頁に無いかは確かめていない、と書く)。
4. **答える**。E1a の当たりは 1,572 行で、1 回で 1 行ずつ読める量(数百行)を超える。リードも調査班も、この回の中で読み切れる量ではなかった。E1a だけの回を立てる(次の NinjaTrader の回のあと)。
5. **答える**。MetaTrader 5 は、公式の手順では Linux で Wine を使って動かす(`install_linux.txt`)。この環境には Wine が無く、入れて Windows のデスクトップのアプリを動かすことは試していない。「動かせない」とは書かず、「Wine で動かす道は試していない」として `未確認` のまま残す(P1: 否定の結論にしない)。
6. **答える(方針を足す)**。(甲) だけにしても、1 回に読む量が多ければ同じ文の当てはめに戻る(12〜17 回目)。そこで量の上限を置く: 1 つの要素の全件の検索の当たりが数百行を超えるときは、その要素だけで 1 回にする(この回の E1a の扱い)。(乙) を残して抜き取りで確かめる案は、18・19 回目に語ごとの決まりが 2 回続き、そのたびにリードの読みで `印` の見落としが見つかったので採らない。

台帳(変更なし。E5 の根拠の文だけを直す)。
