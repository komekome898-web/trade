# 検収: 道具サーベイ 区分 8 の 2 回目(2026-09-23、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 2 回目の実行(2026-09-23)`(この回の追記 269 行、報告全体 747 行)/ 生ログ `docs/DATA/probes/20260923_tools_8_run2.log`(587 行、手の見出し 26 本)。届いたままのコミット 54f823c。
起動文 `20260923_tools_survey_cat8_run2_prompt.md@fbd510552276`。手順は `SURVEY.md` §3 の順。

## 0. 費用と時間(完了通知の実測)

調査班 **290,665 トークン・771,305 ms(12.9 分)・道具 75 回**。トークンは目安 25 万を**約 16% 超えた**(1 回目は約 3 割)。時間は期限の内。

## 1. 書き換えの範囲

`git diff --numstat`: 報告に 269 行の追加・0 行の削除、新規の生ログ 1 本、ほかは TRACE(フックの記録)だけ。`git diff -U0 HEAD -- <報告> | grep -c '^-[^-]'` = **0**(1 回目の節から消えた行なし)。**許した範囲に収まっていた。**

## 2. 起動文でやれと書いた項目と、報告の突き合わせ(監査 14 回目の処置 2 で、検収に入れると決めた表)

| 起動文 | 報告 | リードの確かめ |
|---|---|---|
| §2 の 1 1 回目の訂正 | 済み(`### 1 回目の訂正`、`なし` 21 行: なしのまま 20 / 印 1) | §5 の抜き取りで、なしのまま 20 行のうち PineForge の 2 行が誤り |
| §2 の 2 成熟度の枠組みの一次資料 | 途中(段ごとの定義に届かず) | リードが打っても本文を取れなかった(§6) |
| §2 の 3 残りの候補 8-009〜8-016 の深掘り | **未着手**(「時間の制約」) | **生ログの最後の手は 15:38:00、期限は 15:51:53**。期限の 14 分前に止めている。**「時間の制約」は生ログと合わない**(トークンの消費を気にした可能性はあるが、報告はそう書いていない) |
| §2 の 4 辿る一覧 | **未着手**(同上) | 同上 |
| §3 生ログを `cat8_step.py` で・期限つき | 26 手すべて期限つき | `check "" <生ログ> --require-deadline`: 当たった 2 件は出力の中の `---` の行(誤検出。調査班は閉じずに渡してきた)。道具を直した(§7) |
| §3 知見の表の形 | 8 行、形どおり | `check-elements` 0 件(知見の表 8 行を読んだ) |
| §3 必ず置く小節 | 全部あった | 見出しを `grep -n '^###'` で確かめた |
| §7 の 1〜4 の検査 | 貼ってあった | リードが打ち直して一致(`check_scan_report.py` 0 件 / `check-elements` 0 件 / 生ログの形 2 件 / 消えた行 0) |

## 3. 受け入れ検査と被覆

- `check_scan_report.py <報告> <生ログ 1> <生ログ 2>`: **0 件**(打ち直して一致)。
- `check-elements --round 2`: **0 件**。読んだもの = 候補の一覧 16 行 / 要素と段の表 128 行(道具 16)/ 知見の表 8 行 / 辿る一覧から出た名前 0 行。
- この回の節には §4.0 の表があるので、`scope()` はこの節を検査の対象にしている(監査 11 回目の指摘 1 の穴は塞がっていた)。

## 4. 台帳(`cat8_ledger.py`)

この回の値を台帳に写した(変わったのは 3 か所: 8-001 の E2 = 印・段 3 / 8-001 の E5 = 印・段 3 / 8-002 の E4 の段 5 → 3)。そのうえで §5 の結果により、**8-002 の E3a・E3b を `なし` → `未判別` に、状態を `深掘り` → `浅い` に**リードが直した(設計票 §2「深掘り = 未判別が 1 つも無い」)。`check <報告>` 0 件。**残り = 13 行**(8-002・8-003・8-006〜8-016)。

## 5. 抜き取り(原典で打ち直し)

- `qf-lib` の E2(`DataCleaner`): 導入済みのソースの `qf_lib/common/utils/data_cleaner.py` 22-40・94-114 行を読んだ。欠けの割合を測って閾値を超えた列を落とし、落とした理由を `incorrect_columns`・`columns_with_holes`・`start_late_columns` に残す。**E2 の述語(欠けを検出するか報告する)に当たる。合っていた。**
- `qf-lib` の E5(`verify_preloaded_data`): `backtesting/trading_session/backtest_trading_session.py` 95-130 行。読み込んだデータの束のハッシュを期待値と比べ、違えば `ValueError`。対象はデータだけ(コード・設定は含まない)なので、段は設計票 §4.1 の「対象の一部だけなら段 3」で 3。**合っていた。**
- `prediction-market-backtester` の `DATA_SHA256`: README 145・151 行。**合っていた。**調査班は E2・E6 ではなく E5(データの版の固定)に根拠として足した。設計票の E5 の述語の「データの版の固定」に当たるので、**この読みでよい**(起動文が E2・E6 に当てよと書いたのは、当てて確かめよという意味で、当たらなければ当たる要素に書けばよい)。
- **PineForge の E3a・E3b = `なし`(README を `future.leak|peek|survivorship|forward.fill|snoop` で検索して 0 件)→ 誤り。**リポジトリの `docs/pages/mtf.md` 143-153 行に「When the run begins, the source host validates each registered lower-TF site … `lookahead` and `gaps` must be off (TV does not expose them on this builtin and PF refuses to fake them). Violations raise at run-time with a precise diagnostic」がある(リードが `raw.githubusercontent.com` から取得。ファイル一覧は `ungh.cc/repos/pineforge-4pass/pineforge-engine/files/main` で 1,329 本)。ルックアヘッドの設定を実行の始めに検出して止める機能なので、E3a か E3b の述語に当たりうる(どちらかは 3 回目で当てる)。**調べた範囲が README だけで、検索の語に要素の名前そのもの(`lookahead`)が入っていなかった。**

**「なし」の主張の誤りは 2 回続けて出た**(1 回目 `qf-lib` の E5、2 回目 PineForge の E3a・E3b)。どちらも**読んだ範囲が狭い**(1 回目は grep の結果の読み違い、2 回目は README だけ)。3 回目の起動文で、`なし` と書くための条件を述語で決める: (a) 読んだ範囲 = その道具の一次資料の**全部の文書**(README に加えて docs・wiki・API の文書。ファイル一覧を打って、読んだ文書の名前を生ログに残す)、(b) 検索の語に要素の名前そのもの(日本語と英語。`lookahead`・`look-ahead` など)を必ず入れる、(c) 満たせなければ `未判別`。

## 6. 段と外部の枠組み(設計票 §4.1)

調査班が読めたのは abstracta.us のページの静的な部分だけで、段ごとの定義には届いていない。段の名前(Level 1 Initial〜5 Optimization)は取れた。リードが同じページを取ると本文が取れなかった(`curl -A 'Mozilla/5.0'` で `Level [1-5]` の一致 0 件)。**名前の限りでは組織の試験の工程の成熟度の段で、道具の機能の段とは軸が違う(推定)。段は変えない。**別の一次資料(Testing Maturity Model を定めた原典)を 3 回目に探させる。

## 7. 道具の直し

- `scripts/cat8_step.py`: 出力の中の `--- ` と `$ ` で始まる行に `| ` を付けて残すようにした(2 回目の生ログの 584・586 行が見出しの形の検査に当たった件)。試しの出力で、見出しの検査が 0 件になることを確かめた。

## 8. Jev の検査(この回の節だけ、264 行)

- 受領の検査: 印 19 件 = 委任文と報告の照合 1 件(0.87。§2 の 3・4 が未着手なのと合う)/ 出所の無い数 18 件(ほとんどが候補の番号 8-001 などに当たったもので、数の出所の指摘ではない)。
- 項目の検査: 印 3 件(出典の行 2・知見の行 1)。
- 引用の検査: 印 18 件(1 件ずつは追っていない)。

## 9. 区分 8 に数える範囲の広さ(毎回、列ごとの数)

`recount`(16 行): E1a 印 2 / E1b 印 5 / E2 印 3 / E3a 印 1 / E3b 印 1 / E4 印 4 / E5 印 4 / E6 印 0。E1a か E3a に印 = 8-002・8-004・8-006。8-009〜8-016 が未着手なので、広さはまだ何とも言えない。

## 10. 調査班の問いへの答え

1. `DATA_SHA256` を E5 に当てた: よい(§5)。
2. `qf-lib` の E5 の段: 対象(実験 = コード・データ・設定)のうちデータだけなので段 3(設計票 §4.1「対象の一部だけなら段 3」)。(ア)(イ)は段と別に §4.2 に記録する。
3. PineForge の E4 の段: 段は要素ごとに、その要素の原文で決める。回帰ゲートは E1a の原文で、E4(再生)の原文ではない。E4 は段 3 のまま。
4. `akurkar07/OrderBook` の重複した注文番号など: E2 の述語の対象は「時系列・約定・板・足・参照データ」なので、注文の検証は E2 ではない。**E6(その他の検証・品質)の述語「戦略・計算・データ・実装の正しさを確かめる機能」には当たりうる**ので、3 回目で E6 に当てる。
5. backtrex の比較のページ: 開いていないなら、開いていないと書き、3 回目で開く。
6. Testing Maturity Model: §6 のとおり、原典を探させる。この要約の逐語で段を決めない。

## 11. 判定(この回の受け取り)

**一部を受け取り、差し戻す。**受け取るのは §2 の 1 の訂正のうち、原典で確かめられた `qf-lib` の E2・E5、`DATA_SHA256`、PineForge の E4 の段、出典の取得日と数の出所。差し戻すのは: (1) PineForge の E3a・E3b(§5)/ (2) `なし` のまま据え置いた残りの 18 行を、§5 の (a)(b)(c) で当て直す / (3) 8-009〜8-016 の深掘り(未着手のまま)/ (4) 辿る一覧(未着手のまま)/ (5) 成熟度の枠組みの原典 / (6) `akurkar07/OrderBook` の E6 / (7) 「時間の制約」と書いたが期限の 14 分前に止めていた件は、次の回では**止めた理由を生ログの時刻と一緒に書かせる**。

**監査役にかけなかった理由**: `SURVEY.md` §3「監査役は中身の回と区分の締めだけ」。この回は 1 回目の訂正が主で、新しい中身(新しい候補の深掘り)が無い。代わりにリードが抜き取り(原典 5 件)と起動文との突き合わせの表(§2)を行った。
