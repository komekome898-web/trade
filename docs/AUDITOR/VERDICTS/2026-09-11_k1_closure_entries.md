# 監査記録: K1 終了の登録(N-011 / O-3 到達点 / HANDOFF 状態 / P11 手順)— 2026-09-11

対象: `docs/NEGATIVE_FACTS.md` N-011、`docs/STRATEGY_IDEAS.md` O-3/O-6 の到達点、`docs/PHASE2/K1/HANDOFF.md` 冒頭と §4、`docs/OWNER_PROCEDURES.md` P11。
監査役: 本番 `owner-auditor`(コミット `2cfb5bd` の定義)。問い 7 件(止める 1 / 直す 4 / 聞く 2)。以下、監査役の出力の全文と各問いの直下にリードの応答。

---

1. [止める] docs/NEGATIVE_FACTS.md:15 — N-011は「同一取引所 5分−1.82/15分+0.15、Binance→bitFlyer 5分+2.20、Bybit→bitFlyer 5分+0.70/15分+1.75」という5つの点推定を挙げた直後に「4 通りすべて点推定が床の下」と結論している。出典の `docs/PHASE2/EXEC/RESULT.md` §1.3・§1.5-1 の「4 通り」表は Bybit の2値(+0.70/+1.75)だけを2種の床と比べた4行であり、Binance→bitFlyer の +2.20 は含まれていない。かつ Binance→bitFlyer 5分 +2.20 は「往復の床(無条件)2.16 bp」を実際には上回っている。この「4 通り」が指す対象は何か、Binance の値を含めるなら「4 通りすべて床の下」という一文は成立するか(P5・P10)。

> **直した。** 指摘のとおり「4 通り」は Bybit の 2 値 × 床 2 種であり、Binance→bitFlyer 5 分 +2.20 は無条件の床 2.16 を上回る。N-011 を書き直し、「④-1 の 4 通りは Bybit の 2 値 × 床 2 種」と範囲を明記、Binance 5 分 +2.20 が無条件の床をわずかに上回りシグナル直後の床 2.50 は下回ること、半フレッシュで in-sample の分だけ甘いこと(第 17 部 §17.4-3)を併記した。6 値すべての区間が床を跨ぐ点も明記。

2. [直す] docs/OWNER_PROCEDURES.md:292-304 — 「自動で始まるか: はい…追加の手順・登録は不要」と述べた直後に、同じバックフィルを「15分ごとの定期実行に紛れ込ませず、手動で1回、様子を見ながら流すことを勧める」としている。`restart_all.bat` を実行すれば15分後には `fetch_all.bat` が自動で同じ `extract_tape.py --board-top 10` のバックフィルを開始しうるが、`extract_tape.py`・`fetch_all.bat` に多重実行を防ぐロック機構はあるか。無い場合、オーナーが「様子を見ながら」手動実行する前に自動実行が先行し、2プロセスが同じ `data\ws` を同時に走査する事態を防げているか(P11)。

> **直した(実害のある指摘)。** ロックは無かった。`scripts/extract_tape.py` に `RunLock`(ON1 と同じファイルロック、12 時間で失効)を入れ、二重起動は「already running - skipped」で終わるようにした(テスト 2 件追加)。手順 P11 は「手動コマンドは原則不要、自動実行に任せる」に書き直し、手動実行が要る場合はリードの指示を待つ形にした。**オーナー PC にはロックが次の P4 まで無い**ことも手順に明記。タスクスケジューラの多重起動方針(既定「新しいインスタンスを開始しない」)は**推定**であり、この環境からは確認できない。

3. [直す] docs/OWNER_PROCEDURES.md:312-320 — 手順1〜4は `git pull`/`restart_all.bat` の実行を含まず、`cd` の直後に `.venv\Scripts\python.exe scripts\extract_tape.py --board-top 10` を実行する。同じP11節内の他箇所(P12・P13手順)は明示的に `deploy\restart_all.bat` を先に実行するよう書いているのに対し、ここではその手順が抜けている。`--board-top 10` フラグを含むコードが手順実行時点でオーナーPCに既にpull済みであることは、この手順のどこで保証されているか。

> **直した。** 手動コマンドの手順自体を外し、バックフィルは「P4(2026-09-11 実施済み、L-118)の後の最初の fetch_all が自動で行う」と前提を明記した。

4. [直す] docs/DATA.md:45 と対応する `docs/OWNER_PROCEDURES.md` P11 の「バックフィル」節 — P11節は「オーナーPCの `data\ws\*.jsonl.gz`(板WS生記録)は共有されたことがなく、これまで手元に残っている分がそのまま残っている」という、規模・期間未確定の未共有データ源の存在を明記している。しかし `docs/DATA.md` にはこの「オーナーPC上の未共有の生WS記録」に対応する行(状態=「PCに未共有の可能性」)が見当たらず、45行目の`data/ws`行はこの環境側の2026-08-20単日分のみを指す。この既知のデータ源は台帳への登録対象外なのか、登録漏れか(P1)。

> **直した(登録漏れ)。** `docs/DATA.md` §1 に「WS 生ログ(オーナー PC、未共有)」の行を追加(状態 = PC に未共有の可能性、保持日数は未確認、P11 で抽出後を共有)。L-099 の型そのもの。

5. [聞く] docs/PHASE2/K1/HANDOFF.md:9-13 と §5(未処理表) — §5は「事前登録 `PREREG.md` は L-056 以降の決定(帰無・MDE・バーを作らない)が反映されていない…現状 `RESULT.md` が実態で、`PREREG.md` は古い」ことを終了時点でも未処理のまま残していると明記する。この状態のまま K1 を「条件付きの理解として終了」と決定したことは、`research-protocol` §0.5 の意図マップ要件(△・✕が残る限り陰性ではなく不明)と整合するか。

> **上申(オーナーへ)。リードの推奨: 整合する、このまま閉じる。** 理由: (i) 終了は「陰性の判定」ではなく「条件付きの理解」であり、N-011 も「床より小さいとまでは言えない(不明の側を含む)」と書いている。(ii) 意図マップ(`docs/legacy/KATSUO_INTENT_MAP.md`)は測定前に作ってあり、§0.5 の要件は満たしている。古いのは `PREREG.md`(帰無・MDE・バーを作らないという L-056 の決定が未反映)で、意図マップではない。(iii) `PREREG.md` を今直すと、決定後に事前登録を書き換える形になり、それ自体が規律違反。HANDOFF §5 に「古い」と明記して残すのが正直。

6. [聞く] docs/NEGATIVE_FACTS.md:15 / docs/PHASE2/EXEC/RESULT.md §1.6 — EXEC/RESULT.md §1.6は「P11のバックフィル(板10段、全期間)…が共有されたら、同じ事前登録で…広い窓で再実行し、本節を差し替える」と、より広い窓での床の再測定を既に予告・準備中(承認 L-098)としている。この再測定が完了する前に、狭い窓(16日/7日)の床を根拠にK1を閉じる決定(L-118)を確定させる必要はあったか、それとも再測定を待ってから確定すべき判断だったか。

> **上申(オーナーへ)。リードの推奨: 決定は維持し、再測定で床が 1 bp 台に下がったら再開条件 (iii) で戻す。** 理由: 0.01 BTC の往復は既に板 5 段の内側で測れており、10 段・広い窓で下がる余地は小さい(上がる方向の情報 = 大きいサイズ・大相場が増える)。オーナーは §1.6 を承知のうえで (a) を選んだ(L-117 の提示に §1.6 を含めていた)。再開条件を O-3 の到達点に明記済み。

7. [直す] docs/NEGATIVE_FACTS.md:15 — `docs/PHASE2/K1/HANDOFF.md` §6(オーナー明示要求)は「取引あたりの平均と並べて、年ごとの総損益…と取引数を必ず出す」と定めている。N-011は経費前の平均bpのみを列記し、対応する取引数・総損益をエントリ自体には記載していない(出典ファイルへのポインタのみ)。この否定的事実台帳の登録行自体にも、平均と並べて総量・件数を記載する必要はないか(P4)。

> **直した。** N-011 に 6 値それぞれの取引数と年別総損益の合計(第 16・17 部は 2023 除く 4 年、第 18 部は 5 年)を併記した。

---

**結果**: 止める 0 件残、直す 4/4 対応、聞く 2 件を上申(本ファイル §5・§6、`OWNER_STATUS.md` に件数)。監査役の 7 件のうち 1・2・4・7 は納品前に実害を止めた(1 は誤った一般化、2 は二重書き込みの穴、4 は台帳漏れ、7 はオーナー明示要求の欠落)。
