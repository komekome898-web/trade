# 監査(owner-auditor)— 段 A 結果の読みの報告 **2 回目**(L-231 の追記後)**7 件の処置**(2026-09-19)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_RESULT_2026-09-19.md`(コミット 7b6a42a)。改めた箇所 = §4 候補 2・候補 3・「2 周目に測るもの」、新設 §7。併せて `docs/DATA/probes/20260919_o3c_reaction_bugcheck.{py,log}`、`observation_only.csv`、`f1_12cells.csv`、事前登録・設計・`scripts/o3c_reaction.py`。
前段(Jev): 前回の印を再利用(改めた箇所は数値の照合が主なので監査役の実測に任せた)。

## 監査役の返答(逐語)

1. [止める] `REACTION_RESULT_2026-09-19.md:77` — 候補 2(主因)の結論文「**同じ距離なら清算の方が同じか高い**」が、同じセル内に引用した 5 つの距離帯の数値そのものと矛盾する。5 帯のうち近い 3 帯(0〜10bp: 0.944/0.943、10〜25bp: 0.838/0.751、25〜50bp: 0.661/0.640)は清算 ≥ 対照だが、遠い 2 帯(50〜100bp: 0.451/**0.462**、100bp 超: 0.201/**0.218**)は清算 < 対照であり、しかもこの 2 帯は n(清算 1548 / 1777、対照 1283 / 857)が最大で D1(n=5,048)の 65.9% を占める。`docs/DATA/probes/20260919_o3c_reaction_bugcheck.log` の帯別数値をそのまま加重平均すると清算 0.471 / 対照 0.536(=元の差 −0.065 とほぼ一致)で、距離を揃えても差はほとんど消えない。「主因 = 距離」というオーナー指摘(L-231)への回答の中核が、報告自身が示した数値によって支持されていないのではないか。これは §7 の「上がりすぎ・下がりすぎは捉えられている」という読み(112-113 行)にも影響する分岐点であり、判定そのもの(§1)は動かさない前提でも、候補 2 を「主因」と呼べる根拠になっているか(P6: なぜの欠落/断定の飛躍、P10: 記載された数値と結論の再照合)。

2. [直す] `REACTION_RESULT_2026-09-19.md:99-108` — §7 の表の h=1 行だけ、「対照(ii)の平均」列の主表示値(+0.3)が、同じ行の「差(ii)」列(−7.6)と算術的に整合しない(−7.7 − (+0.3) = −8.0 ≠ −7.6。−7.7 − (−0.1) = −7.6 で一致)。h=5/15/30/60/240 の各行は主表示値が `observation_only.csv` の対照(ii)値(判定の道具の 1 対 1 符号付け)とそのまま一致するが、h=1 行だけ主表示値が `bugcheck.log` の「生の表の符号でそのまま平均」した値(+0.3)になっており、括弧内の「判定の道具の符号付けでは −0.1」が本来の主値になるべきものと入れ替わっている(脚注 108 行「対照 (ii) の平均は判定の道具が 1 対 1 の相手の側の符号を当てた値」という説明とも矛盾)。事実の印が付いた表の中の内部矛盾であり、値そのものは他所に書いてあるので実害は小さいが、「事実」表記の信頼性に関わる。

3. [聞く] `REACTION_RESULT_2026-09-19.md:77-78` と `docs/DATA/probes/20260919_o3c_reaction_bugcheck.py:35-42` — 候補 2 の距離帯別到達率の対照(ii)群は `bugcheck.py` が `table.csv` から `kind=='control_matched' & doi_pre_1h<=cut` で独自に再構成したもので n=4,126(帯別の n の和 333+547+1106+1283+857)。一方、公式の読みの道具(F1 判定・`f1_12cells.csv`)が使う D1 の対照(ii)は n2=4,101(§1 冒頭、13 行「n1 = 5,048 / n2 = 4,101」)。両者は 25 件差があり、報告のどこにもこの差の理由(NaN 除外・mixed 相手除外など)が書かれていない。候補 2・候補 3(「バグは無い」)の根拠として使っている距離帯の表が、判定に使われた D1 対照群そのものと同一の集合であることを確認したか。

4. [直す] `REACTION_RESULT_2026-09-19.md:110` — 「建玉の変化(事実、D1): `doi_post` は 1 分 −3,088 枚 → 240 分 −29,934 枚(対照 −11,110 枚)」という 1 文の中で、1 分値は n1=949/n2=783(D1 の 5,048 のうち 18.8%)、240 分値は n1=5,035/n2=4,097(同 99.7%)と母数が全く異なる(`observation_only.csv` 909 行目・1149 行目)。母数が 5 倍以上違う 2 点を「1 分 → 240 分」の連続した推移のように書いており、母数の違いが本文のどこにも書かれていない(P4 に近い型: 母数が変わる比較で数値だけを並べている)。

5. [聞く] `REACTION_RESULT_2026-09-19.md:77` — 候補 2 の説明「対照 (ii) は建玉の減り(軸 D)で合わせただけで距離は合わせていない」は、読者に「D 軸(`doi_pre_1h`)で 1 対 1 マッチングした」という印象を与えるが、事前登録(`REACTION_PREREG_2026-09-18.md:1125`)によれば対照 (ii)(`control_matched`)の実際のマッチング変数は `bin_pct`(薄さ)であり、D 軸への割り当ては対照行自身の実現値をそのまま使う「own」方式(マッチングではなく事後の分類)である。「合わせた」という語がマッチング変数(bin_pct)を指すのか、事後分類(own の D 軸)を指すのか、読者が誤読しない書き方になっているか。

6. [聞く] `REACTION_RESULT_2026-09-19.md:91-117`(§7 全体) — A-6 の断り書き(91 行「判定は §1 のまま動かさない」)は付いているが、§7 の結論文(113 行「上がりすぎ・下がりすぎは捉えられている」、115 行「燃料切れは起きていない」)は MDE・多重性補正なしの記述式描写でありながら断定的な語尾で書かれている。§1 の公式判定(「F1 の反証」= 戻り到達では差あり(−)1・差あり(+)0)と、§7 の非公式な読み(反転 74.6%・55.0%)が異なる指標で異なる方向の印象を与える場合、オーナーに見せた際にどちらが「答え」として伝わるかについて、書き方の意図を確認したい。

7. [聞く] `REACTION_RESULT_2026-09-19.md:78` — 候補 3「無い(検査した範囲で)」の根拠(6 不変条件 + テスト 148 件)は `table.csv` 自体の構造的整合性(単調性・目標価格式・符号・mfe/mae の境界・W8/W24 の束集合一致)を検査したものであり、指摘 3 で挙げた「読みの道具(`o3c_reaction_judge.py`)が対照(ii)群 n2=4,101 をどう構成するか」というロジック自体は今回の 6 不変条件の対象に含まれていないように読める。「検査した範囲」にこの境界(生の `table.csv` の整合性 と 読みの道具の集計ロジック)が明記されているか。

該当なし(参照値が原典と一致することを確認できたもの、参考まで): §1 の 15,143/353/103 の書き写し、§4 候補 1 の 12 セル数値、§4 候補 4 の MDE 比較、§7 表の bp_reactdir 実群値・分位・mfe/mae 240 分の最大値、reactdir 符号の定義(設計 §3(a) 正=継続/負=反転、コード `REACT_SIGN`)、テスト収集数(69+67 を実測で確認)、「2 周目に測るもの」が 1 項目に収まっている点。

## リードの応答(1 件ずつ)

### 指摘 1
**文は直した。「主因」は保つ(根拠を足した)。** 「同じ距離なら清算の方が同じか高い」は 5 帯中 2 帯で誤りで、「同じ帯では差は ±2 ポイント以内」に改めた。監査役の「帯別数値をそのまま加重平均すると元の差とほぼ一致」は、各群を**自分の**帯の重みで加重しているので、元の到達率(0.471 / 0.536)を再現するだけで、距離の効きを分ける検査にならない。距離をそろえる検査は、**清算の距離分布で対照の帯別到達率を加重する**こと(標準化)で、結果は 0.468(5 帯)/ 0.456(10 分位)= 距離をそろえた差 +0.003 / +0.015(そろえる前 −0.065)。記録 `docs/DATA/probes/20260919_o3c_reaction_bugcheck2.log`。差の −0.065 は距離の違いでほぼ全部説明される、が「主因」の根拠。

### 指摘 2
**直した**: h = 1 の対照 (ii) の主値を −0.1(判定の道具の符号付け)にし、+0.3 を括弧に移した。

### 指摘 3
**確認した(直した)**: 判定の道具と同じ集合を再構成した — 切り値で 4,126 → 相手が mixed の束を除くと 4,101 = n2。240 分の到達率は 0.471276 / 0.536698 で `f1_12cells.csv` と一致。帯別の表もこの集合で出し直した(`..._bugcheck2.log`、候補 2 の数値を差し替え)。

### 指摘 4
**直した**: 1 分(n1 = 949 / n2 = 783)と 240 分(n1 = 5,035 / n2 = 4,097)の母数を併記し、「→」の推移の書き方をやめた。

### 指摘 5
**直した**: 対照 (ii) は薄さ(`bin_pct`)で 1 対 1 に合わせ、軸 D は対照行自身の値で事後に分類(own)、距離はどちらにも入っていない、と書いた。

### 指摘 6
**直した**: §7 の冒頭に「どれが答えか」を置いた — 判定は §1(F1 の反証)で動かさない。§7 は判定ではなく観測(補正も MDE も無い)。結論の語尾を「観測の上では〜と読める」に改め、「差あり」とは言わないと明記した。

### 指摘 7
**直した**: 候補 3 に検査の範囲を書いた — 不変条件 6 つは生の `table.csv` の整合性。読みの道具の集計は D1 の対照 (ii) の再構成(n2 = 4,101 と到達率の一致)で確かめた。それ以外(他の 47 群・他の観測量・ブートストラップ)はテスト 67 件だけが根拠で再計算していない。

## 判定
判定: 通す(7 件とも処置。判定 §1 と凍結 6 項目は触れていない)
