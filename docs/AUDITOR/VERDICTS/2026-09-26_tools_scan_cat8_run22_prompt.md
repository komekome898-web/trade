# 監査: 道具サーベイ 区分 8 の 22 回目の起動文(2026-09-26)

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md`(8-030 dbt の残りを直す回。21 回目の起動文から作った)。

## 監査 103 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run22_prompt.md@69ac88d11348`、コミット a4e549d)の逐語

> 監査 103 回目。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md`(指紋 `69ac88d11348`、コミット a4e549d)。
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md:21` — §6-1 で「何か当たれば」の書き換え対象リストが「導入可否・install所要秒・依存数・pip check・最小実行・実行所要秒・当方データ投入」と「E2 の根拠」に限られているが、21 回目の §4.0 表(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:38386-38433`)には、同じ導入済み venv の実行結果に依っている項目がほかにも残っている(例: 「依存の一覧」= `実測、pip list --format=freeze の61件全部`〔38427行〕/「外部送信」=`実測: pip listでsnowplow-trackerを同梱することを確認し…最小実行した`〔38423行〕/「登録の要否」=`実測、アカウント作成なしでpip install・dbt seed・dbt testが成功`〔38422行〕/「同梱バイナリ」=一部実測混在〔38416行〕)。設計票の `危険で導入停止` の定義(`docs/DATA/surveys/CAT8_DESIGN.md:30`「§4 の他の列を文書から埋めたもの」)は他の列も含めて文書だけに戻すことを求めているのに、この起動文の書き換えリストはそれらを名指ししていない。このまま §6-1 で何か当たった場合、`危険で導入停止` の行に「実測(危険と判定された導入で得た結果)」の根拠が複数残る形になる。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md:25` — 「起動文が認めない理由(『この回では実施していない』『読んでいない』など)で残った項目を全部試す」というリストに「コミット数」を含めているが、21 回目の当該項目の理由(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:38434`「試した手段: shields.ioに総コミット数のバッジが無く、github.com・api.github.comは…403のため到達できなかった」)は §2.5 が認める「試した手段と出力を…添えて書いたもの」型であり、他の 6 項目(この回では実施していない/読んでいない型)とは理由の種類が異なる。リストの見出しが主張する共通性(「起動文が認めない理由」)と実際の理由が食い違っており、下位モデルが「なぜこれも同じ扱いで再挑戦するのか」を誤読しうる。
>
> 3. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:37-38` — オーナー逐語(L-519「案B」)は「印を探し尽くしても見つからない要素は `未判別` のまま残す。完了の条件を『残り0件』から『未判別の要素には、印を探した範囲と当たりの行の数が書いてある』に変える」とだけ述べているが、設計票の「リードの当て方」はこれに条件 (2)「`未判別` 以外の点では `深掘り` の条件(委任文 §4.0 の条件)を満たすもの」を追加している。この (2) はオーナーの逐語に無い。誰の語かを分ける表記(「リードの当て方」)はあるが、追加条件が完了をより難しくする方向(残りを増やす方向)であるにせよ、オーナーの選んだ文自体には無い制約であることは事実として残る。この追加は妥当か、オーナーに確認済みか。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md:20-23` — 「次の5つだけ(順番どおり)」として、item2(E1b・E4 の `印` 探索)が item3(候補の機能に数えるのは dbt 本体だけ、`dbt-audit-helper`・`dbt-utils` は数えない)より先に置かれている。21 回目の止める指摘 2(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run21.md` 指摘2)は E1a で `dbt-audit-helper` の機能を誤って dbt の印として数えた事案そのものであり、item3 の制約は E1b・E4 の探索にも当然及ぶはずである。「順番どおり」という明示の指示に沿ってitem2をitem3を読む前に実行した場合、同型の誤りが E1b・E4 で再発するリスクが構造的に残る(§0 の「最初に読むもの」で全文を読んでから着手すると解消される問題ではあるが、順番の指定と矛盾する)。item3 を item2 より先に置くべきではないか。
>
> 5. 数え直し・写し残しの確認: 「43 項目」(`run22_prompt.md:62`)は 21 回目の §4.0 表の実行数(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:38386-38433`、43 行)と一致(`sed -n '38386,38433p' ... | grep -c '^| `dbt`'` → 43)。生ログ名(`run22.log`)・§7 のコマンド本数(22 本、`--round 22`)・候補一覧の41行の指示は run21 から正しく更新されている。「未着手」候補向けの文(候補の実体の特定など)の残りは見当たらない。E1a の段の書き方(`台帳の値のまま(21回目の節)`、値 `印`・段 `3`)は 21 回目の検収の処置2と整合している。
>
> 止める指摘: あり(1)。

## 処置(リード、監査 103 回目)

1. **直した**。「何か当たれば」の書き換えの対象を、「§4.0 の表で、21 回目に導入した環境で得た結果に依る行を全部」にした。例として 13 項目(導入可否・install所要秒・依存数・pip check・依存の一覧・最小実行の可否・最小実行の中身・実行所要秒・当方データ投入・隠れた依存・登録の要否・外部送信・同梱バイナリ)を挙げ、「この並びで足りるとせず、43 行を 1 行ずつ見て決める」と書いた。文書に無い行は「§6-1 で止めた(導入物は消した)」と書く。設計票 §2 の `危険で導入停止` の定義を引いた。
2. **直した**。「コミット数」を試す項目の並びから外した(21 回目の理由は、試した手段と出力を添えた、認められる型)。
3. **答える(オーナーには聞かない)**。案 B の選んだ文が変えたのは「残り」の数え方のうち `未判別` の扱いだけで、`浅い` と `深掘り` のほかの条件(委任文 §4.0 の条件)には触れていない。(2) を書かずに当てると、§4.0 の表が深掘りの条件を満たさない行まで「残りでない」に数えることになり、選んだ文より広く当てることになる。(2) は新しい制約ではなく、設計票 §2 の既存の定義をそのまま残すことを書いたもの。
4. **直した**。「dbt 本体だけを数える」を 2 に、「E1b・E4 の印を探し尽くす」を 3 にし、3 に「2 のとおり、dbt 本体の機能だけを数える」と足した。

直したあとの起動文の印: `20260923_tools_survey_cat8_run22_prompt.md@f9291365b421`。止める指摘(1)の直しを監査 104 回目に掛ける。

## 監査 104 回目(owner-auditor、対象 `20260923_tools_survey_cat8_run22_prompt.md@f9291365b421`、コミット 6d4e077)の逐語

> 監査 104 回目の結果は上記の通り。要旨:
>
> 1. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md:21` — 処置1で拡張した書き換え対象の括弧内の言い換え「根拠の種類が `実測` の行」は、主定義「21 回目に導入した環境で得た結果に依る行」より字面上広い。21 回目の §4.0 表(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:38386-38433`)には、根拠種類=実測だが導入環境と無関係な外部サイト実測行が複数ある(「最終更新日」`:38391`・「保守者数」`:38397`・「週DL数」`:38398`・「既知の脆弱性」`:38400`・「到達経路」`:38406`、いずれも shields.io/pypistats.org/OSV.dev/PyPI・GitHub 到達確認)。これらは実際には 13 項目の例示に含まれておらず(照合の結果、13 項目は導入環境依存の行と過不足なく一致していた)、除外自体は妥当だが、括弧内の言い換え文だけでは「実測なら全部対象」と機械的に誤読される余地が残る。「43 行を 1 行ずつ見て決める」という補足で緩和されてはいるが、判定基準(導入環境依存かどうか)そのものが言い換え文に明記されていない。P2 寄りの曖昧さ。
>
> 2. [聞く] `docs/DATA/surveys/CAT8_DESIGN.md:38` — 処置3(案 B の当て方の条件 (2) はオーナーに聞かない)。理由(「(2) は新しい制約ではなく既存の `深掘り` 定義の維持」)は論理的には一貫しているが、これはオーナーが選んだ文(L-519 案B)に無い制約をリードが「完了しにくくなる方向」に明文化した解釈であり、CLAUDE.md §0.2 A-13(枠組みはオーナーに見せてから進める)に照らして、リード単独の解釈確定で足りるかは判断が割れうる。オーナーに一度見せて確認すべきではないか。
>
> その他: 処置1・2・4 は概ね指摘を解いていることを確認した(13 項目は SCAN 表の実測/実行結果混在行と過不足なく一致、番号入れ替え後も §2.5 の「§1 の 1・5」等の参照はズレていない、「コミット数」除外の理由も §2.5 の除外条件に合致)。
>
> 止める指摘: なし。

## 処置(リード、監査 104 回目)

1. **直した**。決め方を「21 回目に `venvs/8-030/` に入れた環境で打った手の結果に依るか」だけにし、外のサイトを見た実測(shields.io・pypistats.org・OSV.dev・PyPI の頁・到達の確認)は対象にしない、と書いた。
2. **オーナーに見せる(問いにはしない)**。A-13 は「見せてから進める」。当て方の文を、この回の報告の返答にそのまま出す(yes を求める問いにはしない。O-9)。オーナーが違うと言えば直す。

直したあとの起動文の印: `20260923_tools_survey_cat8_run22_prompt.md@f1c0df536ed6`。止める指摘は無いので、22 回目を起動する。
