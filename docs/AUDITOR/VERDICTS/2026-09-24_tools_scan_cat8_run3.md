# 検収: 道具サーベイ 区分 8 の 3 回目(2026-09-24、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 3 回目の実行(2026-09-24)`(この回の追記 249 行)/ 生ログ `docs/DATA/probes/20260923_tools_8_run3.log`(669 行)。届いたままのコミット ab6d020。
起動文 `20260923_tools_survey_cat8_run3_prompt.md@1ff8dd5b2d7e`。

## 0. 費用と時間

調査班 **231,765 トークン・744,458 ms(12.4 分)・道具 88 回**。**初めて予算の目安(25 万トークン)の内に収まった。**

## 1. 書き換えの範囲

報告に 249 行の追加・0 行の削除、新規の生ログ 1 本、TRACE だけ。`git diff -U0 HEAD -- <報告> | grep -c '^-[^-]'` = 0。

## 2. 起動文の項目と報告の突き合わせ

| 起動文 | 報告 | リードの確かめ |
|---|---|---|
| §2 の 1 残りの候補 8-009〜8-016 | 途中。8-009 は 8 列確定(§4.0 の表なしで「浅い」)/ 8-010・8-011・8-012・8-014 は「判別に一次資料が要る」/ 8-013・8-015・8-016 は未着手 | 状態は台帳に写した(§4) |
| §2 の 2 PineForge の E3a・E3b | 済み(両方 `印`・段 3) | 原典で確かめて合っていた(§5) |
| §2 の 3 辿る一覧 | 未着手 | 3 回続けて未着手 |
| 冒頭の「終わり方」: 期限の前に自分から止めない / 止めたら時刻と理由 | 最後の手 23:27:22、期限 23:41:48。理由「残り時間を報告書の組み立てと 4 つの検査の通過に充てるため」 | `check "" <生ログ> --require-deadline` の参考の 1 行と一致。**理由は書かれた**が、期限の 14 分前に止めたのは 2 回目と同じ。期限が「調べる時間」と「報告を組み立てて検査を通す時間」を分けていないのが原因(起動文の作りの問題 = リード)。4 回目は期限を 2 つにする(§8) |

## 3. 受け入れ検査(リードが打ち直した)

`check_scan_report.py`(生ログ 3 本)0 件 / `check-elements --round 3` 0 件(候補の一覧 16 行・要素と段の表 128 行・知見の表 10 行・辿る一覧 0 行)/ `check "" <生ログ> --require-deadline` 0 件 / 消えた行 0。貼られた出力と一致。

## 4. 台帳

`cat8_ledger.py import <報告> --round 3` を新設して写した。**初めに手で写したとき、報告の状態の後ろに空白なしで続いていた括弧の注記(「深掘り(前回から変更なし…)」)まで状態の列に入れてしまい、`recount` の状態ごとの数がすべて 0 になった**(リードの誤り)。状態の語だけに切り直し、`import` は設計票 §2 の 7 語だけを拾う作りにした(打ち直して変わった所 0・台帳の検査 0 件)。
`recount`: 深掘り 2(8-001・8-002)/ 浅い 5 / 判別に一次資料が要る 4 / 未着手 3 / 危険で導入停止 1 / 登録が要る 1。**残り 12 行**。

## 5. 抜き取り(原典で打ち直し)

- PineForge の E3a: `docs/pine_v6_audit_master.md` 157 行「`request.security()` [F14]: 3 hard restrictions (support_checker.py:683-760): … (2) `lookahead_on` rejected outright」— 一致。
- PineForge の E3b: `docs/coverage.md` 844-850 行「*Out of scope by design*. Mechanically possible — just remove the guard — but `lookahead_on` combined with synthesized intrabar bars exposes information from a not-yet-complete sub-bar. That is a backtest-validity footgun.」— 一致。同じ節に「Higher-TF aggregation already honours `lookahead_on` for partial updates (which is the legitimate use case)」もあり、調査班の問い 4 の非対称は原典どおり。
- `nicferrari/backtester` の `なし` 6 行: 読んだ範囲 = README の全文・`src/` 16 本・`examples/` 8 本(ファイル一覧 43 本、`docs/` なし)。E3a の検索の語に `lookahead`・`look.ahead` がある。**「`なし` と書くための条件」の (a)(b) を満たしている。**ただし E2 の検索の語に要素の名前そのもの(「データ品質」/ `data quality`)が入っているかは、報告の根拠の欄からは読めない(`missing／duplicate／outlier／gap` 等とある)。4 回目の当て直しの対象に入れる。
- **リードの手癖**: この確かめで、`ungh.cc` のファイル一覧を既定の枝 `main` と決め打ちして打ち、失敗した(既定の枝は `master`。調査班は正しく `master` で打っていた)。`SURVEY.md` §5「既定枝を先に読む」を、リードが守っていない。

## 6. Jev の検査

この回は当てていない。**理由**: 2 回目までに、この節の形(候補の番号・表)に対して `unsourced_number` が候補の番号に当たる誤検出が大半で(2 回目の検収 §8: 18 件中ほとんど)、残りの有効な印(委任文と報告の照合)は §2 の突き合わせの表で代わりに見ている。**当てないのは SURVEY.md §3 の 0 に反するので、4 回目の検収では当てる**(誤検出の型は記録して、区分の締めに `SURVEY.md` への追記案にする)。

## 7. 調査班の問いへの答え

1. `nicferrari/backtester` の E4(ベクトル化したバーの処理は再生か): E4 の述語は「記録した市場データを時刻順に再生して、戦略・執行・計算を再実行する」。記録した足を時刻順に処理して戦略を再実行するなら当たる。速さ・粒度は問わない(述語の括弧)。**このままでよい**(段 3)。
2. `arXiv:2603.20319` の欠陥の検出: 要素ごとに、その要素の述語に当たる原文で決める。突き合わせの結果として欠陥を見つけた記述は E1a、突き合わせと別に実装の正しさを確かめる機能の記述があれば E6 にも付く。論文の本文を読んでから決める(4 回目)。
3. PineForge の E3a・E3b を両方 `印`: 別の原文(静的な検査で拒否 = 検出 / 設計でガードを外さない = 防止)がそれぞれの述語に当たるので、両方でよい(二重計上ではない。列が違う機能を見ている)。
4. 上位の時間足では `lookahead_on` を許している非対称: E3b の段と §4.2 の「対象」に書く(防ぐのは下位の時間足の合成の経路だけ)。段は変えない。
5. VectorBT の E2・E5 の段 3 が当方の csv.gz でも成り立つか: 段は道具の側の記述だけで決める(設計票 §4.1)。当方のデータで成り立つかは §4.2 の「入力(原文)」と委任文 §4 の「当方データ投入」の列に書く。
6. `rusty-bot` の一次資料: 投稿からリポジトリに辿れないなら、名前で検索して(GitHub・crates.io など)見つかった候補を、同じものか確かめてから書く。確かめられなければ「判別に一次資料が要る」のまま、試した手段を書く。

## 8. 4 回目への申し送り

- 期限を 2 つに分ける: 調べる期限(開始から 15 分)と、返す期限(開始から 22 分)。`cat8_step.py` の `--deadline` には調べる期限を付け、§7 の検査はそのあとに打つ(3 回とも、検査の時間を残すために調べるのを早く止めていた)。
- 3 回目の起動文の冒頭の表に回した 4 件(残りの `なし` の当て直し・成熟度の枠組みの原典・`akurkar07/OrderBook` の E6・`backtrex` の比較のページ)と、この検収の §5・§7 の項目。
- 残りの候補(番号順に 8-010 から)と辿る一覧(3 回続けて未着手)。

## 9. 判定

**受け取る**(差し戻す誤りは見つからなかった)。この回に届いた PineForge の E3a・E3b、8-009 の 8 列、8-010〜8-012・8-014 の一部の印は台帳に写した。区分は完了していない(残り 12 行、辿る一覧が未着手)。

**監査役にかけなかった理由**: SURVEY.md §3(中身の回と区分の締めだけ)。この回は新しい候補の中身を含むが、抜き取りで誤りが見つからず、次の回で同じ候補の続きを調べる。中身がまとまる回(残りの候補の深掘りが揃う回)で監査役にかける。
