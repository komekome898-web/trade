# 区分 1 の 11 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 11 回目の実行(2026-09-22)」(3690 行目以降、326 行)
生ログ: `docs/DATA/probes/20260922_tools_1_run11.log`(新規 162 行、13 手。復帰文字 0 個)

## 0. 無改変・検査

既存の 3689 行は **無改変**。生ログ 9 本を渡して打ち直し、**13 本すべて 0 件**。
**閉じずに残した行は 0 件**(4 回連続)。当たった 48 件は中身を直して消しており、生ログへの書き足しはしていない。

## 1. 止めた判断の根拠 — **リードが 4 件とも一次資料で確かめた。全部一致。**

| 主張 | リードの取り直し |
|---|---|
| `Superalgos` の導入時実行は 3 件で、重いのは `presetup` | **一致。**`package.json` の scripts に `presetup = "git checkout develop && git pull upstream develop"` / `postsetup = "npm start"` / `prepare = "husky install"`。**リードの起動指定が `prepare` だけを挙げていたのは不十分で、調査班の訂正が正しい** |
| `OpenTrader` の `postinstall` が homedir に書き、外部コマンドを走らせる | **一致。**npm の配布物を取って `scripts/postinstall.mjs` を読んだ: `homedir()` + `".opentrader"`、`execSync` で `prisma generate` / `prisma migrate deploy` / `node seed.mjs`、`writeFileSync` で合言葉、`mkdirSync` で戦略の置き場 |
| `CryptoSignal` の固定版が入らないのは配布物の性質 | **一致。**numpy 1.14.0 の配布物 23 件の内訳は cp27=7 / cp34=5 / cp35=5 / cp36=5 / source=1。**3.10 以降の wheel は 1 件も無い。**この環境にあるのは 3.10〜3.13 の 4 本 |
| `Bot18` は scripts が空で導入時実行の危険は無い | **一致。**`scripts = None`、`license = "https://bot18.net/licensing"`、`unpackedSize = 82488973`、`fileCount = 12263` |

抜き取り(一次資料 64 行から 5 行 / 実測 40 行から 3 行)も一致。星 5659、OpenTrader の配布物 50 ファイル・8.8 MB。

## 2. 調査班が渡してきた判断への回答

1. **`OpenTrader` を `npm install --ignore-scripts` で入れ直すか → 入れない。止めたままで確定。**
   理由: 道具の実体(`bin/opentrader.mjs`)が **homedir に置き場を作る設計**なので、`--ignore-scripts` で導入だけ
   通しても、最小実行の段で §6-5(scratchpad 以外に書かない)に触れる。prisma の生成が飛ぶので動く見込みも薄い。
   **状態 = 危険検査で止めた(確定)。**第 2 経路(オーナー PC)のコマンドは節にある。
2. **`Bot18` を取得するか → 取得しない。状態を確定させる。**
   理由 3 つ: (a) **ライセンスの条件が一次資料から読めない**(SPDX ではなく自社サイトの URL)ので、
   §4 が求める再配布・商用の欄を埋められない。(b) 最後の公開が 2018 年で配布が止まっている。
   (c) 82 MB を取っても (a) が解けない。
   **状態 = 配布が止まっており、ライセンスの条件が一次資料から読めない(確定)。**
   §6-1 の意味での「危険」ではないので、そうは書かない。**取得は「見送り」であって「不可」ではない。**
3. **`Superalgos` の止めた理由が起動指定と違う → 調査班が正しい。**リードの指定が不十分だった。

## 3. リードの判定

- **受け取る。中身の誤りは 0 件。**
- **この回で 4 件の状態が確定した。**`OpenTrader` = 危険で止めた / `Superalgos` = 危険で止めた /
  `CryptoSignal` = この環境からは到達できない / `Bot18` = 配布が止まりライセンスが読めない(リードの判定で確定)。
- **残りは `OctoBot` の注文の層 1 件だけ。**データの層までは通っている
  (合成の 1 分足 400 本を `.data` に書き `WRITE_ROWS=400`、読み戻して `AVAILABLE_TABLES=['ohlcv']`)。
  §5-4 の中核(成行と指値の 1 往復)には届いていない。**不可ではなく未到達。**
- **区分 1 は未完了。**残りが空になっていないので、新しい検索計画 6 本にはまだ進んでいない(委任文 §2 の順序どおり)。
- 予算は時間もトークンも超過。表の生成が重い。**次の回は `OctoBot` の 1 件に絞る。**

## 4. 調査班が報告してきたコミットについて

作業中に `7ccf6f6` というコミットが作られていた件は、**リードが打ったもの**(未追跡の生ログと痕跡ファイルを
版付けするため)。調査班は `git commit` を打っていない。**報告してきたのは正しい対応**で、
身に覚えのない版付けを黙って見過ごさなかった。
