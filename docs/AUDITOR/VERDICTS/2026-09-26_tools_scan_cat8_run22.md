# 検収: 道具サーベイ 区分 8 の 22 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 22 回目の実行(2026-09-26)`(38545 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run22.log`(46,115 バイト)。起動文の印 `20260923_tools_survey_cat8_run22_prompt.md@f1c0df536ed6`。

## 0. 費用と時間

調査班 440,884 トークン・1,871,864 ms(31.2 分)・道具 173 回。

## 1. 受け入れ検査(リードが打ち直した)

`check-elements --round 22` 0 件 / `check "" run22.log` 0 件 / `check_scan_report.py`(run1〜run22)43 件。22 回目の節(38545 行目から)の中には 0 件。新しい K13 は 21 回目の節の 38390 行(21 回目の検収 §4 の 6 と同じ型の誤検出)。

## 2. 台帳

`import --round 22` で 8-030 の E1b 印/2・E4 印/3・状態 `深掘り` が入ったあと、リードが `set 8-030 E1b=未判別 段_E1b=未判別 E4=未判別 段_E4=未判別 状態=浅い` で戻した。`check` 0 件。深掘り 27 / 残り 10。

## 3. 中身の監査(105 回目、owner-auditor)の逐語

> ## 監査 105 回目の指摘
>
> 対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 22 回目の実行(2026-09-26)`(38545行目〜末尾)と生ログ `docs/DATA/probes/20260923_tools_8_run22.log`。参照した他ファイル: 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md`、21回目検収 `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run21.md`、設計票 `docs/DATA/surveys/CAT8_DESIGN.md`、`docs/AUDITOR/PRINCIPLES.md`、`docs/AUDITOR/KNOWN_ANSWERS.md`。
>
> 1. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38870`(E4 行)/ `docs/DATA/probes/20260923_tools_8_run22.log:190-196,204-208` — E4 行の説明文は「`event_time`列を持つ記録データを時間範囲指定して**時刻順に**バッチ処理・再実行する」と書くが、引いた原文は「dbt will process the batches between the start and end as **independent queries**」(196行目)であり、「時刻順に」の語は原文に無い。さらに同じログが引く `concurrent_batches` の説明(208行目)は「Setting to `true` runs batches concurrently (in parallel)」と、同時実行(順不同)を明記する。E4 の述語「記録した市場データを**時刻順に再生**して…再実行する」に当てるための言い換えが、引用した一次資料そのものより強い主張になっていないか。(出典: `docs/DATA/surveys/CAT8_DESIGN.md` §3、述語を逐語で当てる規則)
>
> 2. [止める] `docs/DATA/surveys/CAT8_DESIGN.md:62`「述語を足したり変えたりしたら、全行に当て直す」 — リード(本監査の依頼文)は今回、E1b(「利用者が書く計算は候補の計算に数えない」)と E4(「バッチは時刻順に流す再生ではなく独立の問い合わせ」)について、22回目の報告が確定した印を未判別へ差し戻す新しい読みを示した。これらは設計票 §3 の元の述語文には無い追加の限定であり、もし述語の実質的な変更(狭め)に当たるなら、設計票 §3 の自己規定により E1b=印・E4=印 の他の全行(E1b: backtrex・VectorBT・freqtrade・rusty-bot・nicferrari/backtester・AutoHedge・FX Replay・AlgoNetwork・prediction-market-backtester・arXiv 2件・Oryon等。E4: 同様に印の行)にも当て直しが要る。当て直しの計画を立てないまま、この1候補だけに新しい読みを適用してよいか。
>
> 3. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38576`(知見#4「合計2,264ファイル」)/ `docs/DATA/probes/20260923_tools_8_run22.log:227`(「total=3902 included_by_pattern=1621 excluded=2281」) — 知見#4は「正規表現で…除いた数と、全体からNを引いた数の両方を確認した」と書くが、生ログ自身の正規表現による除外数は2,281件(3902-1621)で、報告の結論2,264件(3902-1638)と一致しない(差17件)。「両方を確認した」としながら実際には一致しない数値のどちらか一方だけを採用しており、差の理由が書かれていない。
>
> 4. [止める] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38573`(知見#1)/ `docs/DATA/delegations/20260923_tools_survey_cat8_run22_prompt.md:21`(§6-1の項目「…PyPIの配布元とGitHubの一致 / 保守者の数と名前の一貫性 / 既知の脆弱性」) — 起動文が定義する§6-1の項目は7つだが、知見#1は(a)〜(f)の6項目しか列挙せず、「保守者の数と名前の一貫性」は別の知見#7(生ログ779行目、時刻07:19:00)で後から扱われている。知見#1の「何も当たらなかったため実測値を残す」という結論に使われた手はいずれも07:09:24〜07:11:14の範囲(生ログ2-130行目)で、保守者名の確認(07:19:00)より前である。この回の目的自体が21回目の「実行が先、検査が後」という順番違反を正すことだったのに、§6-1の7項目を全部見終える前に安全側の結論を書いていないか。
>
> 5. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38573`(知見#1)/ `39028`(問い#3) — 知見#1の(e)は「dbt-duckdbはPyPI Home-page=github.com/duckdb/dbt-duckdb(dbt-labsでなくduckdb組織)」という実際の相違を記載しているのに、結論部は「**何も当たらなかったため**、21回目の実測値…は変更せず残す」と書く。起動文§1-1の枠組みは「何か当たれば→危険で導入停止」「何も当たらなかれば→実測値を残す」の二択だが、実際には「見つかったが自己整合的で詐称でないと判断した」という中間の判断が行われている。「何も当たらなかった」という言葉が、行った判断そのものを隠していないか。
>
> 6. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:39026-39027`(問い#1・#2) — 起動文§2.2は「値・段を決めずにおく理由でない問いは[それ以外の問い]」とするが、問い#1は「E1bの述語(比較機能を含まない、単なる計算の保有)にそもそもどう対応するのか、設計票§4.1に明記が無い」、問い#2は「SQL自体は広く使われる一般的な言語であり…段4とされた候補との整合性は確認していない」と、いずれも値・段そのものの妥当性への疑義に読める。それでも[それ以外の問い]に分類し、印/段は確定済みのまま報告している。この分類は正しいか。
>
> 7. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38574`(知見#2) — E1bの根拠は「dbtのモデル機能(利用者がSQL/Pythonで書く計算)」を「当方と別の実装で出す計算」に当てている。この計算は利用者が書くもので、起動文§2.1「候補で検証される1つの戦略の作り・例のコードの作りは数えない」と同型の構造(利用者が候補の枠組みの中で書くもの)に見える。この除外規定をE1bの述語(§3)にも及ぼすかどうかは設計票のどこにも明記が無い。今回リードが示した「利用者が書く計算は数えない」という読みは、設計票の逐語に基づくものか、それとも今回新たに立てた基準か。
>
> 8. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38987-38993`(§4.0「導入可否」「install所要秒」「依存数」「pip check」「最小実行の可否・中身」「実行所要秒」) — これら7項目の根拠は全て21回目の生ログのみを指し、この回の§6-1検査(危険性なし)を踏まえた注記は「導入可否」の行にしか無い。§6-1の結果を反映すべき対象(§1-1「実測に依る値…を文書だけの値に戻す」の判定基準に照らして検討すべき行)には、この7項目のうち「依存数」「pip check」も含まれるはずだが、注記が揃っていない。
>
> 9. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38616`(候補の一覧30番「状態: 深掘り」)/ `39029`(問い#4「この判定…はリードの検収での確認を経ていない。誤っていれば`浅い`に戻すことになる」) — 調査班自身が状態確定を要確認として問いに出しているのに、候補の一覧の本文はすでに「深掘り」と確定して書かれており、暫定値であることが本体に示されていない。
>
> 10. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:38580`(知見#8「宣伝詐欺の兆候」根拠=実測)/ `docs/DATA/probes/20260923_tools_8_run22.log:785-790`(method=websearch, rc=NA) — 起動文§2.6は「WebFetchは一次資料に使わない(要約を返すため)、まずcurlで生のHTMLを取る」とするが、宣伝詐欺の兆候・使用報告の判定はWebSearchの検索結果の要約のみに基づき、個々の投稿をcurl等で裏取りした形跡が無い。この根拠を「実測」に分類してよいか(WebFetchの要約を一次資料としない規則と同じ理由が当てはまらないか)。
>
> 11. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:39028`(問い#3「他候補で同種の『本体と別組織が保守する公式扱いの依存』が出た場合の扱い方…を聞きたい」) — この一般論は今回の8-030自身の判定(依存数61件・dbt-duckdbの保守者数など、dbt-duckdb経由の実測を含む行)にすでに影響している。一般論の結論を待たずに8-030を「深掘り」に確定してよいか(9番の指摘と合わせて検討)。
>
> ---
>
> 止める指摘: あり(1・2・4)。
>
> ## 補足(依頼された4点への直接の対応)
>
> 1. リードの2つの読み(E1b・E4)は、狭めているというより設計票の元の述語文には無い新しい限定基準の導入に見える(指摘2・7)。逆に調査班の読み(E4の「時刻順に」)は、原文の「independent queries」「concurrent_batches」より強い主張への言い換えで、広げすぎの側に見える(指摘1)。
> 2. §6-1の後からの検査は、起動文が定義する7項目のうち「保守者の数と名前の一貫性」の扱いが時系列的に結論(知見#1)より後になっており、全項目を見てから結論したとは言い切れない(指摘4)。dbt-duckdbが別組織保守である点は認識・記載されているが、結論の書き方(「何も当たらなかった」)がその判断を隠している(指摘5)。
> 3. 除いたファイル数は§4.0の9項目解消(過半39/43が一次資料/実測)とは整合するが、除外数そのもの(2,264 vs 生ログの2,281)は不一致(指摘3)。
> 4. E4の「時刻順に」という言い換えが原文に無い(指摘1)。それ以外に、この回自身の生ログ・報告内で同一文言の複製・誤引用は新規には見当たらなかった(check_scan_report.pyのK13新規4件はdbtの§4.0表が21回目と同一根拠文字列を共有する機械的な誤検出で、内容の複写ではないと報告は説明しており、その説明自体は妥当に見える)。

## 4. 処置(リード、監査 105 回目)

1. **受け取る(E4 は `未判別`)**。原文は「independent queries」「concurrently」で、「時刻順に」は調査班の言い換え。E4 の述語の「時刻順に再生して」の逐語に当たる文が無い。
2. **止める → 答える(新しい限定ではない。当て直しは要らない)**。
   - E4: 述語の文そのものが「記録した市場データを**時刻順に再生して**」と書く。バッチが独立・同時に走る機能はこの逐語に当たらない、というのは逐語どおりの当て方で、述語を狭めたものではない。
   - E1b: 述語は「…同じ種類の出力を、当方と別の実装で出す**計算を持つ**」。起動文 §2.1「述語は候補(道具)の機能に当てる。道具で検証される 1 つの戦略の作り・例のコードの作りは数えない」は 14 回目から全部の要素に当ててきた既存の読み(21 回目の起動文でも同じ文)。利用者がモデルとして書く SQL は、道具で動かす利用者のコードで、道具の計算ではない。
   - **当て直しが要らないことの確かめ(リードの実測、範囲を書く)**: 台帳で E1b が `印` の 27 行のうち、報告の中の最後の `印` の行を機械で引けた 16 行の根拠を読んだ(コマンド: 台帳の E1b 列が `印` の名前ごとに、報告の `| \`<名前>\` | E1b | 印 |` の行のうち `台帳の値のまま` でない最後の行の 6 列目を引く)。どれも道具自身の計算(backtrex・FX Replay・MetaTrader・NinjaTrader の指標の計算、Backtrader の Analyzer、Lean の PortfolioStatistics、Vibe-Trading の `run_backtest`、Great Expectations の 3 つのエンジンの平均の計算、FinanceToolkit・OpenBB の関数など)で、利用者が書く計算に依るものは無かった。**残りの 11 行(qf-lib・PineForge・prediction-market-backtester・akurkar07/OrderBook・freqtrade・nicferrari/backtester・arXiv 2 件・VectorBT・rusty-bot・Exactpro)は、表の列の形が違って機械で引けず、読んでいない(未確認)**。資源化の前の全体受け取り監査で見る。E4 の `印` の行の当て直しは、述語の逐語どおりに当てただけなので要らない。
3. **直した(報告の節の末尾に「リードの追記」)**。2,281 と 2,264 の両方を書き、差の 17 件の内訳は未確認と書いた。
4. **止める → 直した**。779 行の保守者の名前の確認を含めて 7 項目全部を見たあとでも、導入を止める所見は無いことを、リードの追記に書いた(結論は変わらない)。
5. **直した**。「何も当たらなかった」ではなく、`dbt-duckdb` が別の組織の保守であることが見つかり、配布元の表示と GitHub の組織が一致するので詐称ではないと判断した、と書き直した(リードの追記)。
6. **答える**。問い 1・2 は E1b・E4 の値・段への問いだった。リードが処置 1・2 で `未判別` に戻したので、結果として `[値・段の問い]` の扱いになった。
7. **答える**。処置 2 の E1b のとおり、既存の起動文 §2.1 の読み(14 回目から)による。
8. **直した**。7 行が §6-1 の前の導入の結果であることを、リードの追記にまとめて書いた。
9. **直した**。状態は `浅い`。リードの追記に「候補の一覧の『深掘り』は受け取らない」と書いた。
10. **直した**。宣伝詐欺の兆候の根拠の種類を `推定` とした(リードの追記)。§4.0 の過半の条件は、この 1 行を除いても満たす。
11. **答える**。状態を `浅い` に戻したので、問いは残らない。別の組織が保守する依存への §6-1 の当て方は、この回と同じく「配布元の表示と GitHub の組織の一致」を見る(委任文 §6-1 の「PyPI の配布元と GitHub の一致」の逐語どおり)。

## 5. これからの手

8-030 dbt の残りは E1b・E4 の 2 つ。どちらも、dbt 本体の文書の目次の全部(`website/docs/docs/` と `website/docs/reference/`)とソースの該当しうる所を読んで `印` を探し、無ければ案 B の記録(読んだ範囲と当たりの行の数)を書いて `未判別` のまま残す。23 回目 = これだけの小さい回。24 回目 = 8-033 Prefect。


**訂正(同じ回、リード)**: 処置 2 の件数を、最初は「33 行のうち 23 行」「残り 10 行」と書いた。台帳を数え直すと E1b が `印` の行は 27(`awk -F'\t' 'NR>1 && $9=="印"' docs/DATA/tools_catalog_cat8.tsv | wc -l` → 27)、根拠を引けたのは 16 行、引けなかったのは 11 行だった。数えずに書いた(§0.2 O-3 の型)。
