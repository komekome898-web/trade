# 監査(owner-auditor)— 段 A 事前登録への **3 回目(走行前の再監査)23 件の処置**(2026-09-18)

対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(以下 PREREG)と
`scripts/o3c_reaction_judge.py`(以下 JUDGE)。
1 回目の処置は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg.md`(25 件)、
2 回目は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r2.md`(23 件 + ア〜エ)、
走行 1・2 の報告への監査は `docs/AUDITOR/VERDICTS/2026-09-18_reaction_run12.md`(24 件)。

件数: **止める 7(1〜7)/ 直す 12(8〜19)/ 聞く 4(20〜23)= 23 件(省略なし)。総合「止める」。**
**監査役の指摘は逐語のまま、一字も変えずに §1 に貼る。**その下に 1 件ずつ処置を書く。

**判定(予測できる / 使える / 有効)は本書に 1 つも書かない。**
**判定区間(456 日)の観測量は本書で 1 つも新しく出していない。**
**`--mode full` は 1 本も実行していない**(**実測**: `backtest_data/o3c_reaction_20260918_full/` は
存在しない。§4 の表)。
**`scripts/o3c_reaction.py` は 1 点だけ変えた**(指摘 6 の処置 = `matched_liq_id` 列の追加)。
**コミットもプッシュもしていない。**

## 0.1 対応表(本作業)

| やろうとすること | オーナーまたはリードの逐語 |
|---|---|
| 23 件を全部処置し、本書に 1 件ずつ「指摘の逐語 → 処置」を書く | リードの委任文面「**`docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r3.md` 新設: 冒頭に監査役の逐語を一字も変えずに貼り、23 件の処置(何を・どの行で・なぜ)**」 |
| 前回の処置書と同じ形にする | リードの委任文面「**前回の処置書 `docs/AUDITOR/VERDICTS/2026-09-18_reaction_prereg_r2.md` と `_prereg.md` の形に倣う**」 |
| PREREG を直し、リードの決定を逐語で本文に入れる | リードの委任文面「**事前登録の改訂(下の決定を逐語で入れ「該当語なし = リードの判断」と明記)**」 |
| `scripts/o3c_reaction.py` を 1 点だけ変え、標本 4 本を走らせ直す | リードの委任文面「**`scripts/o3c_reaction.py` の最小の変更 1 点(決定 6)と、それに伴う標本 4 本の再走行**」 |
| JUDGE と試験を直す | リードの委任文面「**`scripts/o3c_reaction_judge.py` と `tests/test_o3c_reaction_judge.py` の改訂(決定 1・5・6・8・11・12・17・19・20)**」 |
| 23 件の処置の中身(各件) | **リードの決定 1〜23 の逐語**(本書の各件に引いた。PREREG §0.1 の 5 つ目の表にも同じ逐語を置いた) |
| **判定区間を開けない** | リードの委任文面「**判定区間の出力 `backtest_data/o3c_reaction_20260918_full/` は存在しない。作らない・開けない。`--mode full` を走らせない**」 |
| オーナーに見える文はすべて日本語 | リードの委任文面「**オーナーに見える文はすべて日本語。**」+ `CLAUDE.md` §0.2 の O-1 |
| コミット・プッシュをしない | リードの委任文面「**コミット・プッシュはしない。**」 |

**右が空の行は 1 つも無い**(`CLAUDE.md` §0.1。**引用が無いこと自体が検出器である**)。
**23 件の処置の中身はすべて「リードの決定」で、オーナーの原文の該当語は 1 件も無い。**
**そのことを PREREG §0.1 の 5 つ目の表(23 行)に明記した**(PREREG:152〜185)。

---

## 1. 監査役の返答(逐語。一字も変えていない)

> ## 2026-09-18 走行前の再監査(3 回目)— `owner-auditor` の返答(逐語)
>
> 監査対象: `docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md`(2,090 行、2 回目の処置後)+ `VERDICTS/2026-09-18_reaction_prereg_r2.md` + `scripts/o3c_reaction_judge.py`
>
> `docs/AUDITOR/PRINCIPLES.md`・`KNOWN_ANSWERS.md`・成果物 2 本・`scripts/o3c_reaction_judge.py`・標本 6 日の `table.csv`(実測)を突き合わせた結果の問いだけを返す。ファイルには何も書いていない。
>
> 1. [止める] /home/user/trade/docs/PHASE2/O3C/PRICE_LEVEL/REACTION_PREREG_2026-09-18.md:442,454-456 — §4 は「**「対照にも同じ切り方を当てる」は、対照行にその軸の値があることを前提にしていた。無い軸がある**」「**対照行に値があるのは `bin_pct`(A1)と `doi_pre_1h`(D)だけである**」と断定するが、数えたのは `*_liqdir` の付いた列だけである。**実測**(標本 6 日 `gap60_w8/table.csv`、`kind == 'control_matched'` 213 行): `dist_node_bp` **213**、`dist_vwap_bp` **213**、`oi_dist_node_bp` **192**、`oi_dist_vwap_bp` **192** が非空で、空なのは符号を当てた `*_liqdir` 版と `implied_leverage` だけである。**A2〜A5 の 24 群では、対照自身の軸の大きさは在り、符号だけが無い。**符号は 1 対 1 の相手から取れる(読みのスクリプトは `*_reactdir` に対して既にその手を使っている = `scripts/o3c_reaction_judge.py:238-247`)。**この否定的主張は何の列を何通り数えた結果か。試していない経路(符号なし列 × 相手の side)を挙げられるか**(P-1、P-7、P-14)
>
> 2. [止める] 同 :460-474 vs :1530-1657 — ア の決定「**1 対 1 の相手の実群の束の群を受け継ぐ**」により、38 群の対照 (ii) は**対照自身の軸の値で分けたものではなく、実群と同じ群ラベルを貼っただけのもの**になる。すなわちその 38 群で「対照 = 清算が無いときの同じ条件」は「同日・同 `bin_pct` の時刻を、清算側の条件でラベル分けしたもの」に変わる。**§12(射程)にこの含意が 1 行も無い。**§10.1:1368 の括弧(「**対照 (ii) は 1 対 1 の相手の実群の群を受け継ぐので 差 (ii) は 576 行すべてで出る**」)は出ることだけを書き、意味が変わることを書いていない。**主指標 差 (ii) の 38/48 群で対照の条件付けが実群由来になることは、射程に書かれているか**(P-9、P-3)
>
> 3. [止める] 同 :367-387、:1640-1644 vs :1937-1959 — §3.1 は判定区間を開けた事実を **2 件**(手順 1 の 472 日、12 日走行の 10 日)として列挙し、§12 も「**`--mode sample --days` の関門が塞がる前に、判定区間の日を含む走行が 2 本起きた**」と書く。一方 §14.4.1 には「**リードは 2026-09-18 07:13〜07:25 UTC に…2 回実行した…= 判定区間の 456 日を、§3 の関門を 1 つも通さずに開けた**」とある。**456 日を 2 回開けた 3 件目(と 4 件目)が、開封の一覧と射程のどちらにも数えられていない。**(P-5、P-9、A-6)
>
> 4. [止める] 同 :1832-1835 vs :1964-1969 — §14.4.1 は「**出るのは件数(整数 6 つ)で…判定の向きが読めない**」としながら、その直後に判定区間の率を計算して読んでいる(「**W = 8h で 27.7%(5,876/21,198)、W = 24h で 40.5%**」「**SELL 30.2%(3,651/12,097)対 BUY 24.4%**」)。`fwd_node` が作れる / 作れないは §7.1:789 が「**作れない = 目標価格が無い**」として置いた 4 分類の 1 つである。**判定区間の「作れない率」とその side 別の差を走行前に公表することは、§3 の「判定するのは群差・対照差だけ」「一度だけ開ける」と両立するか。件数と率の線引きはどこに書いてあるか**(P-9、A-6)
>
> 5. [止める] 同 :671-683、:696 vs /home/user/trade/scripts/o3c_reaction_judge.py:238-247 — §6.1 の本版は「**対照行は `REACT_SIGN` を +1 として生の値が入っている**」「**対照 (ii)…は実群と 1 対 1 で対応するので、重み付けは要らない**」と書き、処置書 §3 イ は「**読みのスクリプトはその形で実装済み**」と書く。実装は「要らない」ではなく**別の規則**を当てている: `values(KIND_MAT, *_reactdir)` は 1 対 1 の相手の `side` で下向き / 上向きを選び直す(`mfe` と `mae` の入れ替えを含む = 同 :138-149)。**対照 (ii) の向きの要る量に符号を当てる規則は、事前登録のどこに書いてあるか。「実装済み」という §3 イ の記述は、この符号付けを含んでいるか**(P-2、P-3)
>
> 6. [止める] /home/user/trade/scripts/o3c_reaction_judge.py:195-213 vs 同 PREREG:462、:1469-1483 — 「1 対 1 の対応」は表の列ではなく `cascade_id` の連番の算術(日ごとの最小番号 + 対照の連番)で**復元**している。事前登録にこの復元方法もその前提(「**束の行は cascade_id が日ごとに連番である**」)も書かれておらず、§11 のサニティ 13 件にも無い。**実測**(標本 6 日): 213 対のうち 212 対は相手の `bin_pct` 差が最大 4.76 ポイント(マッチングの ±5 と整合)、**1 対は相手が見つからず `None`**(相手が mixed 束のため)。この復元が 456 日・mixed 180 でも成り立つかは測られておらず、**崩れても静かに行が落ちるだけで止まらない**(`membership` が NaN を返す)。**38 群の主指標がこの復元に依存することは事前登録のどこに書かれ、判定区間でどう検査するか**(P-3、P-10)
>
> 7. [止める] 同 PREREG:1689-1702 vs :440-474、:124-135 — §13 の「報告する変更」5 件は、バー・走行 2 本・CI・§10.1 の分岐・§7.0 の F1 条件だけで、**ア(対照 (ii) が実群の群を受け継ぐ = 38/48 群の対照の作り方)が入っていない。**ア は §0.1 の 4 つ目の表で「**オーナーの原文の該当語は 1 件も無い = リードの判断**」と自認されており、対照の定義は枠組みに当たる。**「開けるのは一度きりなので、報告が後になると取り返しがつかない」(:1814)が掛かる変更に、ア は入らないと判断した根拠は何か**(`CLAUDE.md` §8 の A-13、P-9)
>
> 8. [直す] 同 :1367 vs /home/user/trade/scripts/o3c_reaction_judge.py:204-213 — 実群側は mixed 180 束を落として n1 = 21,198 とする一方、**mixed 束に対して作られた合わせた対照は `table.csv` に残り、非受け継ぎの 10 群(全体・A1 の 6・D の 3)では自分の `bin_pct` / `doi_pre_1h` で群に入る**(実測: 標本 6 日で 1 件がこの経路に入る)。**この非対称(実群からは落ち、対照には残る束)は 456 日で何件になり、§6.1 の 4「1 対 1 で対応する」とどう両立するか**(P-5、P-3)
>
> 9. [直す] 同 :1346 — §10 の項 3 が「**4 つの読み(F1 と整合 / 不明 / F1 の反証 / 差あり(向きが逆)・混在)**」と旧語のままである。§7.0:731-736 の 4 語(F1 と整合 / F1 の反証 / 混在 / 不明)とも、ウ の決定「**§10.3 の「読み」の 4 語は §7.0 の新しい 4 分岐…に揃える**」とも違う。**ウ の処置が §10.3 だけに当たって §10 の項 3 に残っていないか**(P-2、O-7)
>
> 10. [直す] 同 :1804-1806 — 同じ項に「**#13 は未実施なので、§14.1 を走らせる直前に…2 回(各 1 回)数える**」と「**→ 2026-09-18 に 2 回とも実施した**」が同居している。§11:1483 は「**実施した**」。**#13 の状態は未実施か実施済みか、走行の直前にもう一度数えるのか**(P-2、P-5)
>
> 11. [直す] 同 :2065、:2067 vs /home/user/trade/scripts/o3c_reaction_judge.py:812-866 — §14.6 は「**行数は `summary.json` に出す**」「`summary.json` … **対照 (ii) が実群の群を受け継いだ件数**」と書くが、実装の `summary` に観測表の行数のキーは無く(標準出力 :879 だけ)、受け継ぎは**群ごとの真偽値**「対照は1対1の束から受け継いだか」として出る。**ア の決定「受け継いだかどうかは…走行ごとに数えて `summary.json` に残す」は、この形で満たされているか**(P-3)
>
> 12. [直す] 同 :587、:1311 vs /home/user/trade/scripts/o3c_reaction_judge.py:68、:78-80 — 事前登録は「**\|t\| ≥ 3.925**(**両側 α = 0.05/576 に対応する z = 3.9248。§8.2 の表と同じ値**)」と、丸めた 3.925 と 3.9247896 を同じ数として書く。実装はバーと CI に **3.925**(`BAR_T`)、MDE に **3.9247896…**(`Z_ALPHA`)と 2 つの値を使い分けている。**同じ α の z が 1 本の判定の中で 2 通りあることは、§5:644「同じ量が 2 か所で別の桁に見えないようにする」と両立するか**(P-2)
>
> 13. [直す] 同 :839 vs :661 — n2 の元を「**21,198 × (213/224 = 0.9509)**」とするが、§6 は対照の除外率を「**liq 0% / 合わせた対照 5.33%(12 / 225)**」= 213/225 と書く。母数が 224(非 mixed の実群)と 225(対照を作った束)で食い違う。**対照作成率の母数はどちらか**(P-2、P-5)
>
> 14. [直す] 同 :1234-1236、:1248 — 走査の出力は「**metrics の zip がある判定区間の日: 359 / 456**」と「**当日 + 前日の両方がある日: 356 / 456**」の 2 つを出しているのに、欠測見込み(97 日、×1.13 倍、21,198 → 約 16,690)はすべて 359 だけで計算している。`doi_pre_1h` は 1 時間前を読むので前日が要る日があり得る。**D の n を決めるのは 359 か 356 か、その理由はどこに書いてあるか**(P-2、P-3)
>
> 15. [直す] 同 :1308、:1319-1320 — 「**2,000 回。使い道は…SE の推定だけ**」「**SE の推定には 2,000 回で足り**」と書くが、この「足りる」には出所の 3 分類(一次資料 / 実測 / 仮定)が付いていない。バーが \|t\| ≥ 3.925 なので SE の推定誤差は判定の境界に直接効く。**この「足りる」は誰の仮定か、それとも実測か**(P-2、P-16)
>
> 16. [直す] 同 :362-363 vs :394、:290-295、:1493-1494 — §3 は「**手順 1 と 12 日走行の出力(`..._anchor_v1_rawcols/` と `..._scale12_judgmentdays/`)は封のまま**」と 2 つだけ挙げるが、§3.1 の案 b は手順 1 の出力を 3 つ(`..._anchor_v1_rawcols/` と `..._anchor/` と `..._anchor_trades/`)と書き、本書は `..._anchor/table.csv` と `..._anchor_trades/table.csv` を §3 の数え上げと #9 の実測に使っている。**封の対象はどの 4 つのうちどれで、封の中で数えてよいのは何か**(P-5、P-9)
>
> 17. [直す] 同 :1383 vs /home/user/trade/scripts/o3c_reaction_judge.py:514 — §10.1 は「**検出されず**(**MDE = X を必ず併記する**)」とするが、実装は MDE が計算できないとき `検出されず(MDE = 未算出)` を書く。**MDE 無しの「検出されず」が出る条件と、その行の読み方は事前登録のどこにあるか**(P-3、A-6)
>
> 18. [直す] 同 :469-471 — ア の群の数が「**委任の文面は「その 40 群」…委任先が §4 の表から数え直すと 38 群である…40 との差 2 の出所は分からない…リードが確かめること**」のまま凍結に入っている(実測: 読みのスクリプトは標本 6 日で 38 を返す)。**未解決の数え上げを残したまま「凍結」と書けるか**(P-5)
>
> 19. [直す] 同 :1435 vs :2047-2053 — §10.2 は「**感度 4 本(gap 30 / 180 秒 × W 8h / 24h)の全列**」を観測のみの表に入れると書くが、§14.6 の読みのコマンドは `--run-w8` / `--run-w24` の 2 本しか受け取らない(実装 `scripts/o3c_reaction_judge.py:755-756`)。**感度 4 本の表を作る道具はどれで、走行の前に書くのか**(prereg 監査(2 回目)の指摘 1 と同型。P-3、A-6)
>
> 20. [聞く] /home/user/trade/scripts/o3c_reaction_judge.py:334-349 vs 同 PREREG:1429-1440 — W = 24h 側の「W に依らない 12 群」は、実装が **W = 24h の実群で切り直した切り値**を使う。§10.2 は行数(144)と「`t` と `判定` の列を置かない」ことしか書いていない。**観測のみの 12 群の切り値は W = 8h の切り値を当てるのか、W = 24h で切り直すのか。食い違ったときの読み(:1439-1440「本書では決めていない」)はこの選択に依らないか**(P-3)
>
> 21. [聞く] 同 :725-736 — F1 の 12 セルは**同じ D1 群の同じ束集合**を 2 系統 × h 6 で見たもので、互いに強く相関する。読みは「**差あり(正)が 1 つ以上 かつ 差あり(負)が 0**」で決まる。**セルごとに α = 0.05/576 を当てたうえで「1 つ以上」で整合を宣言する形は、意図した検出力の置き方か(相関するセルの最大値を採る形になっていないか)**(P-16)
>
> 22. [聞く] 同 :1543-1553 vs :725-726 — F1 の 12 セルはすべて主軸 D の D1 だが、D は `doi_pre_1h` から作るので**判定区間 456 日のうち 97 日(2024-03-04〜06-08 の連続した塊)が丸ごと落ちる**。§12 は「**この 97 日は 1 つの連続した塊なので、D の欠測は期間の偏りを伴う。その向きは測っていない**」と書く。**1 周目の唯一の機構仮説の判定が特定の 3 か月を欠いたまま出ることは、§10.3 の候補 4(検出力不足)ではなく射程として結果に併記されるか。走行前に決めておく必要はないか**(P-16、P-6)
>
> 23. [聞く] 同 :3-6、:20-26 — 冒頭は「**凍結: 走行の前に確定。以後、バー・族・α・h・軸を変えない**」としつつ、同じ見出しの下で 3 度目の改訂(25 件 → 23 件 → ア〜エ)を受けている。凍結対象の 5 項目は確かに動いていないが、対照の作り方(ア)・符号(イ)・判定語(ウ)・読みの道具(エ)は動いた。**「凍結」が指すのは 5 項目だけであることを、オーナーに報告する 5 件の中で明示するか**(A-6、P-9)
>
> 判定: 止める

---

## 2. 23 件の処置

**行番号は処置後の PREREG / JUDGE のものである**(処置前の行番号は §1 の逐語に残っている)。
**23 件のうち「変えない」で済ませたものは 1 件も無い。**

### 1.[止める] 「対照行に値があるのは A1 と D だけ」は何を数えた結果か

**処置: 直した(数え直して 3 通りに分けた)。**リードの決定(逐語):
「**A2〜A5 の 24 群は、対照行自身の符号なしの大きさ(`dist_node_bp` / `dist_vwap_bp` /
`oi_dist_node_bp` / `oi_dist_vwap_bp`)に 1 対 1 の相手の実群の `side` の符号(`LIQ_SIGN` と
同じ規則)を当てて対照自身の軸の値を作り、判定区間の切り値をその値に当てる(= 受け継がない)。
受け継ぐのは E(`implied_leverage`、6 群)・B1(3)・B2(3)・C(2)の **14 群だけ**
(束そのものの属性で、対照に対応物が無い)。§4 の「対照行に値があるのは A1 と D だけ」の断定は、
数えた列(`*_liqdir` 付き)と数えなかった列(符号なし)を書いて訂正する(O-2 の型)。
読みのスクリプトの `liq_only_columns()` / `build_groups` をこの形に直す。**」

- **PREREG §4 の節を書き替えた**(PREREG:524〜629)。見出しを
  「対照行に軸の値が無い軸の扱い」→「**対照 (ii) の軸の値の作り方(3 通りに分ける)**」に変えた。
- **訂正を O-2 の型で書いた**(PREREG:529〜542): **数えた 10 列**(`bin_pct` / `doi_pre_1h` /
  `*_liqdir` 4 本 / `implied_leverage` / `bundle_*` 2 本 / `side`)と
  **数えなかった 4 列**(符号なしの大きさ)を列挙し、**試していなかった経路**
  (符号なし列 × 1 対 1 の相手の `side`)を書いた。
- **14 列を全部数えたコマンドと出力を同じ場所に置いた**(PREREG:544〜597)。
  **監査役の実測(213 / 213 / 192 / 192)と一致した**(**実測**: `gap60_w8` の `control_matched`)。
  W = 24h 側も同じ形で出した(216 / 216 / 177 / 177)。
- **3 分岐の表を置いた**(PREREG:603〜607): **own 10 群 / partner_sign 24 群 / inherit 14 群。**
  **10 + 24 + 14 = 48。**
- **JUDGE を直した**: `liq_only_columns()` を廃し、**`control_axis_kinds()`** に替えた
  (JUDGE:365〜402)。走行ごとに測って 3 通りに分ける。
  **`Group.pair_inherit` は `control_axis == AX_INHERIT` の別名にした**(JUDGE:341〜363)。
  **`Run.matched_partner_sign_axis()` を足した**(JUDGE:302〜338)。
  **`build_groups` / `build_flat_groups_w24` / `membership` をこの形に直した**(JUDGE:408〜525)。
- **試験を 2 件足した**: `test_only_fourteen_groups_inherit_and_twentyfour_take_the_partner_sign`
  (14 / 24 / 10 の内訳を名前で照合)と
  `test_the_partner_sign_axis_is_the_controls_own_value`(相手の束の値そのものではないこと)。
- **実測**(標本 6 日で読みのスクリプトを走らせた `summary.json`):
  **inherit 14(E 6 / B1 3 / B2 3 / C 2)/ partner_sign 24(A2〜A5 × (3+3))/ own 10。**

### 2.[止める] 受け継ぐ群で対照の条件付けが実群由来になることが射程に無い

**処置: 直した。**リードの決定(逐語):
「**§12 に射程を書く: 受け継ぐ 14 群では対照 (ii) は「対照自身の属性で分けたもの」ではなく
「同日・同 `bin_pct` 帯の清算の無い時刻を、相手の清算の属性でラベル分けしたもの」。
差 (ii) の意味は「その属性の清算があった時刻」対「同条件の清算が無い時刻」で、
対照側の属性の条件付けではない。**」

- **§12 に項を新設した**(PREREG:2013〜2025)。**14 群の 差 (ii) の意味を書き、例(`C_SELL`)を付けた。**
- **「残る 34 群(own 10 + partner_sign 24)では対照は自分の軸の値で分かれている」と併記した。**
- **「この違いは結果の読みに必ず併記する」と書いた**(14 群と 34 群で 差 (ii) の意味が違うため)。
- **射程**: **14 群で「対照側でも同じ属性で条件付けた場合」の差は測っていない**(**未確認**。
  **対照行にその属性が無いので、本走行の出力からは作れない**)。
- **指摘 1 の処置により、この射程が掛かる群は 38 → 14 に減った**(24 群は対照自身の値で分かれる)。

### 3.[止める] 456 日を開けた 3・4 件目が開封の一覧にも射程にも無い

**処置: 直した(4 件に数え直した)。**リードの決定(逐語):
「**§3.1 の開封の一覧に、2026-09-18 07:13〜07:25 UTC のサニティ #13 の 2 回(W8h / W24h、
関門なし、出たのは整数 6 つ)を 3 件目・4 件目として足す。§12 の「2 本起きた」も
「4 本(うち 2 本は件数のみ)」に直す。**」

- **§3.1 の一覧を 2 件 → 4 件にした**(PREREG:440〜458)。
  **3・4 件目に「§3 の関門を 1 つも通していない」「出たのは整数 6 つ」「ファイルは 1 つも書いていない」を書いた。**
- **§3.1 の射程にも 1 行足した**(PREREG:460〜464)。
- **§12 の「2 本起きた」を「4 本」に直した**(PREREG:2027〜2041)。**4 本それぞれの扱いを 1 行ずつ書いた。**

### 4.[止める] 「件数だけ」と書きながら判定区間の率を読んでいる

**処置: 直した(率と side の比較を消した)。**リードの決定(逐語):
「**§14.4.1 から率(27.7% / 40.5% / SELL 30.2% / BUY 24.4%)と side の比較の文を消し、
整数 6 つだけ残す。「この 6 つから導ける率・side の差は結果の読みに使わない。
作れない率は §7.1 の 4 分類の 1 つなので、判定区間の値として読まない」と書く(射程にも 1 行)。**」

- **§14.4.1 の「読み」の段落を削除し、消したことを書いた**(PREREG:2365〜2382)。
  **6 つの整数(日数 456 / 束 21,378 / mixed 180 / 非 mixed 21,198 / 定義できる数 / side 別)は残した。**
- **消した理由を書いた**: **`fwd_node` が作れる / 作れないは §7.1 の 4 分類の 1 つで、
  その率は判定区間の観測量である。**
- **「L-199「9 = a」を変えないことは、率を見なくても決まっている」と書いた**(A-6・A-9)。
- **射程を 1 行足した**: **6 つの整数を出したこと自体は取り消せない**(§3.1 の 3・4 本目)。
  **消したのは、そこから率を作って読んだ部分である。**
- **§12 にも 1 行足した**(PREREG:2037)。
- **実測**: 本文に残る 27.7% は**標本 6 日**の数(W = 24h で 62/224)だけである
  (PREREG:782 / 1945 / 1951。**判定区間の率は 1 つも残っていない**)。

### 5.[止める] 対照 (ii) の向きの要る量に符号を当てる規則がどこにも無い

**処置: 直した(規則を §6.1 に書いた)。**リードの決定(逐語):
「**§6.1 に対照 (ii) の `*_reactdir` の符号の規則を書く: 対照行の生の値(REACT_SIGN = +1)に、
1 対 1 の相手の実群の側の符号を当てる。下向き(SELL 側)では `mfe` と `mae` が入れ替わる
(`mfe = max(s·up, s·dn)` の定義から)。「重み付けは要らない」ではなく「相手の符号を当てる」と書く。
読みのスクリプトの実装(`values(KIND_MAT, *_reactdir)`)と一致させる。**」

- **§6.1 の 4 を書き替えた**(PREREG:900〜934)。**3 列 × 2 行の表で符号の当て方を固定した。**
  **下向きで `mfe` と `mae` が入れ替わることを、実装の定義(`max` / `min` と符号反転)から説明した。**
- **前版の「重み付けは要らない」が誤りだったことを訂正の注で書いた**(PREREG:903〜908)。
  **処置書 2 回目の §3 イ の「実装済み」がこの符号付けを含んでいなかったことも書いた。**
- **JUDGE の該当箇所に同じ規則を docstring で書いた**(JUDGE:276〜285)。**実装は変えていない**
  (もともとこの規則だった。事前登録の側が書いていなかった)。

### 6.[止める] 1 対 1 の対応を連番の算術で復元している

**処置: 直した(列で持つ形にした + サニティ #14 を足した)。**リードの決定(逐語):
「**`scripts/o3c_reaction.py` の合わせた対照の行に `matched_liq_id` 列(相手の実群の `cascade_id`。
相手が mixed 束なら `table_mixed.csv` 側の id)を足す**(最小の変更。他の列・値・順序・
乱数の消費を変えない)。標本 4 本を `--mode sample` で走らせ直し、**新列を除く全列が前の出力と
一致すること**を `pandas` で突き合わせて報告に出す。MD5SUMS は再生成(`./` 無し)。
読みのスクリプトは `matched_liq_id` で対応を取り、連番の算術による復元を消す。
対応の検査(相手が存在 / 同日 / `bin_pct` の差がマッチングの許容内)を通らなければ
「[止め]」で終了コード 1。§11 にサニティ #14 としてこの検査を足す。**」

- **`scripts/o3c_reaction.py` を 2 か所だけ直した**:
  **(a)** `MATCHED_PAIR_COLUMNS = ["matched_liq_id"]` を**列の並びの末尾**に足した
  (`scripts/o3c_reaction.py:726〜752`)。**末尾なので既にある列の位置は 1 つも動かない。**
  **(b)** 行を組むところで `cascades_of_day[i].cascade_id` を入れた(同 :1580〜1590)。
  **`i` は `cascades_of_day` の添字である**(`cascade_list` の詰め方。**実測**: 同 :1449〜1470)ので、
  **相手が mixed 束でも正しい id が入る。**
- **標本 4 本を走らせ直した**(`--mode sample --mmr 0.004 --seed 1`)。**突き合わせは §4 の表。**
  **8 ファイル(table.csv / table_mixed.csv × 4 本)とも、新列を除く列名の並び・行数・値が完全一致。**
  **`summary.json` の食い違いは `elapsed_sec` の 1 鍵だけ**(**実測**)。**MD5SUMS は再生成した。**
- **JUDGE を直した**: `_cascade_index()` と連番の算術を**消し**、`matched_liq_id` で引く形にした
  (JUDGE:234〜246。`Run.__init__`)。**引き当ての辞書は主表の束 + `table_mixed.csv` の両方から作る。**
- **サニティ #14 を足した**: `check_pairing()`(JUDGE:527〜566)。**(a) 相手が実在 /
  (b) 同じ日 / (c) `bin_pct` の差が ±5.0 以内**。**1 つでも破れたら `[止め]` を出して
  終了コード 1、表を 1 枚も書かない**(`main` の先頭で全走行に当てる。JUDGE:971〜981)。
  **許容は `scripts/o3c_reaction.py:140` の `MATCH_TOL_PCT = 5.0` を読んで書いた**(JUDGE:104〜106)。
  **境界を含む**(**実測**: gap60_w24 に差がちょうど 5.0000 の組がある。
  マッチングが `bp ± tol` を閉区間で取るため)。
- **PREREG に書いた**: §11 に **#14** の行(PREREG:1850)、§14.6 に列の追加の経緯(PREREG:2452〜2467)。
- **試験を 3 件足した**: `test_matched_liq_id_is_the_last_column_and_only_matched_rows_carry_it`
  (`tests/test_o3c_reaction.py`。49 → 50 件)、
  `test_the_pairing_comes_from_the_matched_liq_id_column` と
  `test_a_broken_pairing_stops_before_any_table_is_written`(`tests/test_o3c_reaction_judge.py`)。
- **実測**: 標本 4 本で**相手が引けない対照は 0 件**になった(前版の算術では 1 件が `None` だった)。

### 7.[止める] §13「報告する変更」に対照の作り方が入っていない

**処置: 直した(5 件 → 7 件)。**リードの決定(逐語):
「**§13「報告する変更」に 2 行足す: (6) 対照の作り方(14 群は相手の属性を受け継ぐ /
24 群は相手の符号を当てた対照自身の値)、(7) 対照 (ii) の向きの要る量の符号 = 相手の側。
5 件 → 7 件。**」

- **§13 の表を 5 行 → 7 行にした**(PREREG:2089〜2090)。
- **どの監査で決まったかを書き分けた**(PREREG:2092〜2095): 上 2 行 = 1 回目、
  次 3 行 = 2 回目、下 2 行 = 3 回目。
- **§14.4 の 4 と「待つもの」も 7 件に直した**(PREREG:2210・2214)。
- **指摘 23 の処置(凍結は 5 項目だけ)も、同じ回にオーナーへ報告することを書いた**(PREREG:2089〜2099)。

### 8.[直す] mixed 束の対照だけが対照側に残る非対称

**処置: 直した(全群から落とす)。**リードの決定(逐語):
「**相手が mixed 束の合わせた対照は、全群から落とす(1 対 1 の対称性)。
落とした件数を `summary.json` に出す。§6.1 の 4 に書く。**」

- **JUDGE で落とす形にした**: `Run.mat_usable`(JUDGE:247〜258)を `membership` の全経路に掛けた
  (JUDGE:478〜525)。**「全体」群にも掛かる。**
- **件数を `summary.json` に出した**: 走行ごとの「相手がmixed束で落とした合わせた対照」
  (JUDGE:1067〜1074)。
- **§6.1 の 4 に書いた**(PREREG:923〜934)。**理由(実群の側が mixed を主表から外している)を書いた。**
- **実測**(標本 6 日): **gap60_w8 1 件 / gap60_w24 1 件 / gap30_w8 1 件 / gap180_w8 2 件。**
  **その 1 件が D の第 1 群に入っていたので、D1 の対照は 54 → 53 になる**
  (`table.csv` の行としては 54 / 69 / 88 + NA 2 = 213 のまま)。
- **456 日で何件になるかは走行の出力が要る**(**未確認**。PREREG:934 に書いた)。
- **試験 `test_the_prereg_group_counts_are_reproduced_on_the_sample_run` の期待値を
  54 → 53 に直し、落とした 1 件を足すと 213 に戻ることを同じ試験で測る形にした。**
- **試験を 1 件足した**: `test_matched_controls_of_mixed_bundles_are_dropped_from_every_group`。

### 9.[直す] §10 の項 3 が旧語のまま

**処置: 直した。**リードの決定(逐語):
「**§10 の項 3 の 4 語を §7.0 の 4 語に直す。**」

- **§10 の項 3 を書き替えた**(PREREG:1646〜1653。項 3 の本文と注)。
  **「F1 と整合 / 不明 / F1 の反証 / 差あり(向きが逆)・混在」→
  「F1 と整合 / F1 の反証 / 混在 / 不明」。**
- **2 回目の ウ の処置が §10.3 にしか当たっていなかったことを書いた**(O-7)。
- **セル単位の語(§10.1 の分岐)とこの 4 語を書き分けた。**

### 10.[直す] #13 は未実施か実施済みか

**処置: 直した(1 つに統一)。**リードの決定(逐語):
「**#13 は「実施済み(2026-09-18、2 回)。走行の直前には数え直さない」に統一。**」

- **§14.4 の 1 を書き替えた**(PREREG:2203〜2207)。「未実施なので直前に数える」を消した。
- **§11 の #13 の状態欄も同じ文にした**(PREREG:1849)。
- **前版が同じ項に 2 つの状態を同居させていたことを書いた。**

### 11.[直す] §14.6 の `summary.json` の記述が実装と違う

**処置: 直した(実装に足して、文面を合わせた)。**リードの決定(逐語):
「**`summary.json` に観測のみの表の行数、受け継いだ群の一覧と行数、
mixed 相手で落とした対照の件数を出す。§14.6 の文面を実装に合わせる。**」

- **JUDGE の `summary` に足した**(JUDGE:1061〜1090):
  **(a) 観測のみの表の行数**(`observation_only.csv` と感度の各ファイル)/
  **(b) 「対照(ii)の軸の作り方」の 3 分類と「受け継いだ群」の一覧 + `n_control_matched`** /
  **(c) 相手が mixed 束で落とした対照の件数**(走行ごと)/
  **(d) サニティ #14 の `bin_pct` の差の最大**(走行ごと)。
- **§14.6 の出力の表を書き替えた**(PREREG:2489〜2506)。**前版がどう違っていたかも書いた。**
- **試験で測る**: `test_summary_records_the_frozen_settings`(行数)と
  `test_only_fourteen_groups_inherit_...`(受け継いだ群)と
  `test_matched_controls_of_mixed_bundles_are_dropped_from_every_group`(落とした件数)。
- **実測**(標本 6 日): 観測のみの表 2,208 行 / 受け継いだ群 14 / 落とした対照 1 件(gap60_w8)。

### 12.[直す] 同じ α の z が 2 通りある

**処置: 直した(1 本にした)。**リードの決定(逐語):
「**z は 1 本: `Z = norm.ppf(1 − α/2)`(3.9247896…)をバー・CI・MDE のすべてに使う。
本文は「3.925(丸め表示。計算は同じ z)」と書く。`BAR_T` と `Z_ALPHA` を 1 つにする。
テストの期待値も合わせる。**」

- **JUDGE で 1 本にした**(JUDGE:95〜102): **`Z_ALPHA = norm.ppf(1 − α/2)`、`BAR_T = Z_ALPHA`。**
  **丸め表示は `BAR_T_SHOWN = "3.925"` という別の名前で持ち、数としては使わない。**
- **§5 に「`z` と「3.925」の約束」の表を新設した**(PREREG:797〜806)。**ここ 1 か所で決める形にした。**
- **§4.3 のバーの行**(PREREG:741)**と §9.1 の区間の行**(PREREG:1592)**を直し、
  §9.1 に訂正の注を置いた**(PREREG:1594〜1613)。
- **バーが 3.925 → 3.9247896… に 0.0002 だけ緩む向きに動くことを書いた**
  (**事実**: 3.925 − 3.9247896514 = 0.0002103486)。**走行の前の変更である。**
- **試験を 1 件足した**: `test_there_is_only_one_z_for_the_bar_the_ci_and_the_mde`。
  **既存 2 件の期待値(`ci_normal` と `summary`)を `judge.BAR_T` に直した。**

### 13.[直す] 対照作成率の母数が 224 と 225 で食い違う

**処置: 直した(実測で 225 に決めた)。名前を分けた 1 件はリードに上げる。**リードの決定(逐語):
「**対照の作成率の母数を `summary.json` の `control_matched_made` / 候補数から 1 つに決めて統一
(224 か 225 か、実測で)。**」

**測った(実測 2026-09-18、標本 6 日の `summary.json`)**:

```
束の合計(mixed 込み) = 225 / mixed = 1 / 非 mixed = 224
control_matched_made = 213 / 候補なし = 12 / 和 = 225
作成率 = made / 束(mixed 込み) = 0.946667
```

- **`control_matched_made` 213 + 「候補なし」12 = 225 = mixed を含む束の数である。**
  **よって「作成率」「除外率」の母数は 225 に決めた**(213/225 = 94.67% / 12/225 = 5.33%)。
- **§6 にコマンドと出力を置いた**(PREREG:828〜853)。
- **§8.2 / §8.5 に残る `213/224` は名前を分けた**(PREREG:855〜866):
  **これは作成率ではなく「非 mixed 束 224 に対する合わせた対照の行数 213 の比」である**
  (n1 が非 mixed 束の数なので行数どうしで揃えたもの)。**O-7 に当たらない形にした。**
- **§8.5 の係数は動かしていない**: 動かすと **§8.3 の 152 セルが全部動く**(A-6)。
  **決定 8' の後に対応する数は 212/224 = 0.946429 で、213/224 = 0.950893 との差は 0.47%、
  MDE には √ で効くので 0.24% 程度である**(**事実**: `sqrt(0.950893/0.946429) = 1.002356`)。
  **判定に使う n2 は走行後の実件数なので、この係数は事前の見積もりにしか効かない**(§8.6)。
- **【リードに上げる】**「**母数を 1 つに決めて統一**」を §8.5 の係数まで及ぼすと
  §8.3 の 152 セルが動く。**本作業では動かさなかった。動かすかはリードが決めること**
  (PREREG:865〜866 にも同じことを書いた)。

### 14.[直す] D の欠測見込みを 359 だけで計算している

**処置: 直した(356 日を主にし、359 も併記)。**リードの決定(逐語):
「**D の被覆の見込みは 356 日(当日 + 前日)で計算し直し、359 も併記する。**」

- **§8.6 に 2 行の表を置いた**(PREREG:1503〜1531)。
  **356 日(主)**: n1 の見込み **約 16,549**、MDE 倍率 **約 1.1318 倍**。
  **359 日(参考)**: 約 16,689、約 1.1270 倍。
- **なぜ 356 かを書いた**: **`doi_pre_1h` は起点の 1 時間前から積むので、
  日の頭に近い束では前日の metrics が要る**(**実測**: 実装の `window_sum(..., start_ms − 3,600,000, start_ms)` と
  metrics を `prev_days + [day, 翌日]` で読む形)。
- **射程を書いた**: **356 と 359 の差の 3 日は「当日はあるが前日が無い日」で、
  その 3 日の束のうち実際に前日が要る束の数は測っていない**(**未確認**)。
  **よって 356 は「全部落ちる」側の見込みで、真の n はこの 2 つの間にある**(**推定**)。
- **§12 と §8.6 の他の箇所の 16,690 / 1.13 倍も直した**(PREREG:1552・1841)。

### 15.[直す] 「2,000 回で足りる」に出所が付いていない

**処置: 直した(仮定と明記し、式を添えた)。**リードの決定(逐語):
「**「2,000 回で足りる」を仮定と明記し、式を添える: ブートストラップ SE 自身の相対誤差
≈ 1/√(2·reps) = 1.58%(2,000 回)。バー 3.925 の近傍で |t| に ±0.06 程度(事実 = 式から)。**」

- **§9.1 の訂正の注に足した**(PREREG:1615〜1630)。
- **出所**: **仮定(リード)。一次資料でも実測でもない**と書いた。
- **式**: `1/√(2 × 2,000) = 1.5811%`(**事実**: 標準偏差の推定量の漸近分散 σ²/(2n) から)。
  **`t` の相対誤差も同じ 1.58% 程度で、バーの近傍 |t| ≈ 3.925 ではゆらぎが
  3.9248 × 0.0158 = 約 ±0.062**(**事実**: 掛け算)。
- **「|t| が 3.86〜3.99 に入る検定では、反復の引き直しで判定が変わりうる」と書いた。**
  **種は 1 に固定してあるので、同じ入力なら同じ答えが出る。**
  **走行の後に反復数を変えない**(A-6)。
- **射程**: 上の式は正規を仮定した漸近の話で、**この標本でのブートストラップ SE の
  ばらつきは測っていない**(**未確認**)。

### 16.[直す] 封の対象は 4 つのうちどれか

**処置: 直した(4 つ列挙し、何を読んだかを書いた)。**リードの決定(逐語):
「**封の対象を 4 つのディレクトリ名で列挙し、それぞれ「何を読んだか」
(`anchor/table.csv` と `anchor_trades/table.csv` は §3 の束の数え上げと #9 の実測に読んだ /
`anchor_v1_rawcols` と `scale12_judgmentdays` は読んでいない)を書く。**」

- **§3 に 4 行の表を新設した**(PREREG:413〜428)。**封 2 つ / 封でない 2 つ。**
- **読んだ列を書いた**: **`day` / `direction` / `anchor_ts_ms` / `end_ms` の 4 列だけ**
  (**実測**: §3 の数え上げのコマンドと §11 の #9 のコマンド)。
  **§4.3 の観測量も §4 の分割軸の値も 1 つも読んでいない。**
- **3・4 は `emit_raw_bp` が False の版で、生の観測量の列を持たないことも書いた**(§3 の関門表)。
- **射程**: **3 と 4 のファイルに何の列が入っているかを全部数えたわけではない**(**未確認**)。

### 17.[直す] MDE 無しの「検出されず」

**処置: 直した(「不明(MDE 未算出)」に分けた)。**リードの決定(逐語):
「**MDE が計算できないセルは「不明(MDE 未算出)」にする(「検出されず」にしない)。
実装とテストを直す。**」

- **JUDGE の `decide()` に分岐を 1 つ足した**(JUDGE:677〜702)。
  **`UNKNOWN_MDE = "不明(MDE 未算出)"`。**
- **理由を書いた**: **「検出されず」は「この n とこの MDE では検出できなかった」という意味なので、
  MDE が無い行にその語を当てると、検出力が分からないことを不在の側に読ませてしまう**
  (`CLAUDE.md` §0.2 の A-18)。
- **§10.1 の分岐の表を 3 → 4 にした**(PREREG:1691〜1709)。**バー・α・族・h・軸は動いていない**
  (分けたのは「検出されず」の中身であって、バーの位置ではない)。
- **§7.0 の「不明」の内訳にも 1 つ増やした**(PREREG:974)。**`f1_reading()` も直した**(JUDGE:859〜877)。
- **§10.3 の「セル単位の語」と §10 の項 3 も揃えた。**
- **試験の期待値を直した**(`test_decide_has_exactly_three_branches_in_order` の
  `MDE = 未算出` の行)。**`summary.json` の「分岐」も 5 つになった。**

### 18.[直す] 「40 群」が未解決のまま凍結に入っている

**処置: 直した(出所を書いて未解決を消した)。**リードの決定(逐語):
「**「40 群」の出所 = 読みのスクリプトを書いた委任先の報告の語をリードがそのまま渡したもの
(列の数え方の違い)。§4 の数は 38、決定 1 の後は「受け継ぐ 14 群 + 相手の符号を当てる 24 群」。
未解決の記述を消す。**」

- **§4 の注を書き替えた**(PREREG:615〜625)。**「リードが確かめること」を消した。**
- **出所**: **読みのスクリプトを書いた委任先の報告の語を、リードがそのまま委任の文面に
  引き写したものである**(列の数え方の違い: `implied_leverage_side` や `oi_side_dist_*_liqdir` を
  別の軸として数えるかで数が変わる)。**オーナーの逐語ではない。**
- **本版の数**: **受け継ぐ 14 + 相手の符号を当てる 24 = 38。**

### 19.[直す] 感度 4 本の表を作る道具が無い

**処置: 直した(`--sens` を足した)。**リードの決定(逐語):
「**読みのスクリプトに `--sens NAME=DIR`(繰り返し可)を足し、感度 4 本それぞれの
観測のみの表 `observation_only_<NAME>.csv`(t と判定の列なし)を出す。§14.6 に書く。テストを足す。**」

- **JUDGE に `--sens NAME=DIR` を足した**(JUDGE:961〜964 と 971〜980)。**繰り返し可。**
- **`build_groups_single()` を足した**(JUDGE:453〜475): **全体 1 + 3 分位 9 軸 × 3 + side 2 = 30 群**を
  **その走行の実群だけで切る。**
- **系統は判定 12 + 観測 43 = 55。30 × 55 = 1,650 行**(**実測**: 標本 6 日で 1,650 行)。
- **MDE の列は空**である(**対を成す標本の走行が無い**: 標本 6 日は 4 本しかなく、
  感度の gap30_w24 と gap180_w24 には相手が無い。**実測**: `..._sample/` の中身)。
  **`build_rows` が `samples.get()` で受け、標本が無ければ MDE を出さない形にした**(JUDGE:806〜815)。
- **§10.2 と §14.6 に書いた**(PREREG:1781〜1795、2471〜2483)。**走行の前に書いた。**
- **試験を 1 件足した**: `test_sens_writes_one_observation_only_table_per_run`
  (行数・列・走行名・MDE が空・判定の表が 576 のまま・`MD5SUMS` に入ること)。

### 20.[聞く] W = 24h 側の 12 群の切り値はどちらか

**処置: 直した(W = 24h で切り直すと書いた)。**リードの決定(逐語):
「**W = 24h 側の 12 群(観測のみ)は W = 24h の実群で切り直した切り値を使う、と §10.2 に書く。**」

- **§10.2 に書いた**(PREREG:1767〜1780)。**実装も同じ形である**(**実測**: `build_flat_groups_w24` が
  `tertile_cuts(run24.values(...))` を呼ぶ)。
- **理由**: W が違えば実群の軸の分布そのものが違うので、W = 8h の切り値を当てると
  「その走行の 3 分位」でなくなり、**行数が 1/3 ずつにならない**(**リードの判断**)。
- **射程を書いた**: **同じ名前の群でも W = 8h 側と W = 24h 側は「同じ切り値で切った同じ群」ではない。
  2 つの差を直接引き算しない**(**リードの判断**)。
- **監査役の問い「食い違ったときの読みはこの選択に依らないか」への答え**:
  **依らない。どちらの切り方でも「食い違いは 2 周目に送る」で同じだからである**(**リードの判断**。
  PREREG:1779〜1780 に書いた)。
- **試験を 1 件足した**: `test_the_w24_flat_groups_recut_their_own_cuts`。

### 21.[聞く] F1 の 12 セルの α の置き方は意図した形か

**処置: 直した(意図した形であることを根拠つきで書いた)。**リードの決定(逐語):
「**意図した形。セルごとの α = 0.05/576 は族 576 全体の Bonferroni なので「12 のうち 1 つ以上」は
族の水準 0.05 で制御されている。相関は保守側に働く。§7.0 に書く。**」

- **§7.0 に項を新設した**(PREREG:991〜1009)。
- **理由 1**: **12 セルのうち 1 つ以上が偽陽性になる確率は 12 × 0.05/576 = 0.00104 以下**
  (**事実**: Bonferroni の和の上界。相関に依らない)。**族 576 全体でも 0.05 以下。**
- **理由 2**: **相関は保守側に働く**(**事実**: 正に相関するほど実際の第 1 種の誤りは上界から離れる)。
- **射程**: **これは偽陽性の話である。**検出力については、相関する 12 セルに Bonferroni を当てるのは
  **保守的すぎる向き**であり、**その保守性の大きさは測っていない**(**未確認**)。
  **偽陰性への対策は、セルごとの MDE を §10.1 に必ず併記することで置いている。**

### 22.[聞く] 97 日の欠測は射程として結果に併記されるか

**処置: 直した(走行前に決めて書いた)。**リードの決定(逐語):
「**97 日(2024-03-04〜06-08)の欠測は、結果の報告に**射程として必ず併記する**
(§10.3 の候補 4「検出力不足」には入れない)。§10.3 と §12 に書く。**」

- **§10.3 に 3 行の表を新設した**(PREREG:1809〜1815)。
  **候補 4 に入れない理由**: 候補 4 は「n と MDE と差の大きさ」の話で、
  **欠測が期間に偏っていることは検出力の大小とは別の問題である。**
- **併記する文を走行前に固定した**: 「**この判定は 2024-03-04〜06-08 の連続 97 日を欠いた
  356〜359 日で出ている。欠測は期間に偏っており、その向きは測っていない**」。
  **読みが 4 つのどれになっても書く。結果を見てから書き足す形にしない**(A-6)。
- **§12 にも書いた**(PREREG:1923〜1928)。
- **射程**: **欠けた 97 日が他の日と同じ性質かは測っていない**(**未確認**)。
  **2 周目でこの期間の metrics を取り直せるかも試していない**(**未確認**。**「取れない」とは書かない**)。

### 23.[聞く] 「凍結」が指すのは 5 項目だけであることを報告するか

**処置: 直した(§0 の凍結の宣言と §13 の両方に書いた)。**リードの決定(逐語):
「**「凍結」が指すのは 5 項目(バー・族・α・h・軸)だけであり、対照の作り方・符号・判定語・
読みの道具は 3 回の監査で動いた、と §0 の凍結の宣言に書く。オーナーへの報告にも書く(リードが書く)。**」

- **§0 の凍結の宣言に書いた**(PREREG:6〜18)。**動いた 4 つを名前で挙げた**:
  **対照の作り方 / 対照 (ii) の向きの要る量の符号 / 判定語 / 読みの道具。**
  **この 4 つは凍結の 5 項目に入っていないので A-6 に当たらないことも書いた。**
- **§13 の「報告する変更」にも 1 行足した**(PREREG:2089〜2099)。
  **「リードが書く」と明記した**(オーナーへの報告はリードの仕事である)。

---

## 3. 処置の後に実測で確かめたこと

| # | 確かめたこと | コマンド | 出力 |
|---|---|---|---|
| 1 | **標本 4 本の再走行で、新しい列を除く全列が前と一致する** | `pandas` で 8 ファイルを突き合わせ(§4) | **8 ファイルとも 列名の並び・行数・値が完全一致。**`summary.json` の差は `elapsed_sec` だけ |
| 2 | **§8.3 の MDE 152 セルが変わっていない** | §8.5 の 2 つ目の再現コマンド(再走行の後) | `照合したセル: 152 / 一致 152 / 食い違い 0` |
| 3 | **判定区間の走行は 1 本も走っていない** | `test -e backtest_data/o3c_reaction_20260918_full` | **存在しない** |
| 4 | **サニティ #14 が標本 4 本で通る** | 読みのスクリプトを標本 6 日に当てた | 4 本とも (a)(b)(c) を通過。`bin_pct` の差の最大 4.7619 / 5.0000 / 4.7619 / 4.9020 |
| 5 | **試験が通る** | `PYTHONPATH=src python -m pytest tests/test_o3c_reaction.py tests/test_o3c_reaction_judge.py tests/test_audit_gates_wired.py` | `90 passed in 42.00s` |
| 6 | **収集件数** | `PYTHONPATH=src python -m pytest --collect-only` | `2072 tests collected`(本作業で 9 件増: `test_o3c_reaction.py` 49 → 50、`test_o3c_reaction_judge.py` 22 → 30) |

---

## 4. 決定 6 の突き合わせ(標本 4 本。**逐語の出力**)

**走らせたコマンド**(RUN12 §4.1 と同じ引数 + `--seed 1`。**標本 6 日なので `--approval` は不要**):

```
PYTHONPATH=src python3 scripts/o3c_reaction.py --mode sample --gap-sec 60  --window-hours 8  --mmr 0.004 --seed 1 --out-dir backtest_data/o3c_reaction_20260918_sample/gap60_w8
PYTHONPATH=src python3 scripts/o3c_reaction.py --mode sample --gap-sec 60  --window-hours 24 --mmr 0.004 --seed 1 --out-dir backtest_data/o3c_reaction_20260918_sample/gap60_w24
PYTHONPATH=src python3 scripts/o3c_reaction.py --mode sample --gap-sec 30  --window-hours 8  --mmr 0.004 --seed 1 --out-dir backtest_data/o3c_reaction_20260918_sample/gap30_w8
PYTHONPATH=src python3 scripts/o3c_reaction.py --mode sample --gap-sec 180 --window-hours 8  --mmr 0.004 --seed 1 --out-dir backtest_data/o3c_reaction_20260918_sample/gap180_w8
```

**行数は前と同じである**(**実測**: 末尾の 1 行。RUN12 §4.1 と一致):
`gap60_w8` 662 行(liq 224 / uniform 225 / matched 213)/ `gap60_w24` 665 行(216)/
`gap30_w8` 784 行(247)/ `gap180_w8` 503 行(165)。

**突き合わせ(実測、2026-09-18)**:

```
gap60_w8/table.csv: 前 662 行 x 156 列 / 後 662 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap60_w8/table_mixed.csv: 前 1 行 x 156 列 / 後 1 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap60_w24/table.csv: 前 665 行 x 156 列 / 後 665 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap60_w24/table_mixed.csv: 前 1 行 x 156 列 / 後 1 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap30_w8/table.csv: 前 784 行 x 156 列 / 後 784 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap30_w8/table_mixed.csv: 前 1 行 x 156 列 / 後 1 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap180_w8/table.csv: 前 503 行 x 156 列 / 後 503 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
gap180_w8/table_mixed.csv: 前 2 行 x 156 列 / 後 2 行 x 157 列 / 足した列 ['matched_liq_id'] / 落ちた列 [] / 新列を除く列名の並びが一致 True / 行数が一致 True / 値が完全一致 True
```

**`summary.json` の食い違い(実測)**: 4 本とも **`elapsed_sec` の 1 鍵だけ**。
**`MD5SUMS` は 4 本とも再生成した**(`./` を付けない相対パス。**実測**: `gap60_w8` は
`642f0a319cb1bee586df54295e012116  table.csv` ほか 3 行)。

**新しい列の中身(実測)**:

```
gap60_w8 合わせた対照 213 / 相手が見つからない 0 / 同日 213 / bin_pct 差の最大 4.7619 / 相手が mixed 束 1 / 他の kind で空でない行 0
gap60_w24 合わせた対照 216 / 相手が見つからない 0 / 同日 216 / bin_pct 差の最大 5.0000 / 相手が mixed 束 1 / 他の kind で空でない行 0
gap30_w8 合わせた対照 247 / 相手が見つからない 0 / 同日 247 / bin_pct 差の最大 4.7619 / 相手が mixed 束 1 / 他の kind で空でない行 0
gap180_w8 合わせた対照 165 / 相手が見つからない 0 / 同日 165 / bin_pct 差の最大 4.9020 / 相手が mixed 束 2 / 他の kind で空でない行 0
```

**前版の連番の算術では `gap60_w8` の 213 対のうち 1 対が `None` だった**(監査役の実測)。
**列で取ると 0 件になる**(**実測**: 上の「相手が見つからない 0」)。

---

## 5. 標本 6 日での動作確認(**判定区間ではない。開封ではない**)

**試験用の台帳(`--root` で差し替え)を作り、標本 6 日の 4 本を入力にして読みのスクリプトを走らせた。**
**判定区間の出力は 1 つも開いていない**(**実測**: §3 の表の 3)。

```
判定の表: 576 行 / 観測のみの表: 2208 行 / 群 48
感度(観測のみ)の表 gap30_w8: 1650 行
感度(観測のみ)の表 gap180_w8: 1650 行
F1 の 12 セルの読み: 不明(差あり 0。内訳: 不明(n < 30) 0 / 不明(MDE 未算出) 0 / 検出されず 12)
出力先: (作業用の一時ディレクトリ)
```

**この「F1 の 12 セルの読み」の行は標本 6 日(探索段)のものである。**
**判定ではない。結果として引用しない。**
**道具が動くことの確認としてだけ出している。**

**`summary.json` から(実測)**:

```
観測のみの表の行数: {"observation_only.csv": 2208, "observation_only_gap30_w8.csv": 1650, "observation_only_gap180_w8.csv": 1650}
受け継いだ群の数: 14 / 相手の符号を当てた群の数: 24
受け継いだ群: ['E_W8h_Q1','E_W8h_Q2','E_W8h_Q3','E_W24h_Q1','E_W24h_Q2','E_W24h_Q3','B1_Q1','B1_Q2','B1_Q3','B2_Q1','B2_Q2','B2_Q3','C_SELL','C_BUY']
own(対照行自身の値): ['A1_W24h_Q1','A1_W24h_Q2','A1_W24h_Q3','A1_W8h_Q1','A1_W8h_Q2','A1_W8h_Q3','D_Q1','D_Q2','D_Q3','全体']
走行 gap60_w8: 日数 6 / 行数 {'liq': 224, 'control_uniform': 225, 'control_matched': 213} / 落とした対照 1 / #14 の bin_pct 差の最大 4.7619
走行 gap60_w24: 日数 6 / 行数 {'liq': 224, 'control_uniform': 225, 'control_matched': 216} / 落とした対照 1 / #14 の bin_pct 差の最大 5.0
走行 gap30_w8: 日数 6 / 行数 {'liq': 268, 'control_uniform': 269, 'control_matched': 247} / 落とした対照 1 / #14 の bin_pct 差の最大 4.7619
走行 gap180_w8: 日数 6 / 行数 {'liq': 168, 'control_uniform': 170, 'control_matched': 165} / 落とした対照 2 / #14 の bin_pct 差の最大 4.901999999999999
バー: {"|t|": 3.924789651405654, "|t|の丸め表示": "3.925", ...}
分岐: ['不明(n < 30)', '差あり(+)', '差あり(−)', '不明(MDE 未算出)', '検出されず(MDE = X)']
```

**観測のみの表 2,208 行 = 48 群 × 観測 43 系統(2,064)+ W24h 側 12 群 × 判定 12 系統(144)。**
**感度 1 本 1,650 行 = 30 群 × 55 系統。**

---

## 6. 本書で新しく出していないもの / リードに上げること(射程)

- **判定区間(456 日)の観測量は 1 つも出していない。**
  本書に出た判定区間の数は、**日数(456 / 359 / 356 / 97 / 100)と束の件数(21,198 / 21,378 / 180)だけ**である。
  **判定区間の率は 1 つも出していない**(指摘 4 の処置で、前版にあったものも消した)。
- **判定(予測できる / 使える / 有効)は本書に 1 つも書いていない。**
- **オーナーの応答(L 番号)はまだ無い。判定区間は開けていない。**
- **`scripts/o3c_reaction.py` を 1 点だけ変えた**(指摘 6。列の追加。**標本 4 本で全列一致を実測**)。
- **【リードに上げる 1】指摘 13 の「母数の統一」を §8.5 の係数(213/224)まで及ぼすと、
  §8.3 の 152 セルが全部動く。**本作業では動かさなかった(A-6)。**動かすかはリードが決めること。**
- **【リードに上げる 2】`CLAUDE.md` §3 の収集件数が 2,063 件のままである。**
  **本作業で 2,072 件になった**(**実測**: `--collect-only`)。
  **`CLAUDE.md` は委任の範囲外なので触っていない。リードが直すこと。**
- **【リードに上げる 3】指摘 12 でバーが 3.925 → 3.9247896… に 0.0002 緩む向きに動いた。**
  **走行の前の変更だが、§13 の「報告する変更」には足していない**(**同じ α の z の書き方の違いであって、
  バーそのものの変更ではないと判断した = リードの判断**)。**報告に足すかはリードが決めること。**
