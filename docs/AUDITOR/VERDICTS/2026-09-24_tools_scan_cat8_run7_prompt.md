# 監査記録: 道具サーベイ 区分 8 の 7 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md`(初版の指紋 `6712ec9368c2`)。6 回目の起動文(監査 31・32 回目を通した版)を元に、範囲・引用の印字・ノートの語の規則を改めた。監査 32 回目の [直す](見出しの回番号)もこの版で直した(「4〜6 回目の差し戻しの対策」)。

## 8 回目以降に回すもの(ここに全部書く)

| 何 | 出所 |
|---|---|
| 8-009・8-010・8-011・8-012・8-014・8-015 の `なし` の当て直し(台帳の `なし` の数: 6・4・3・3・2・6) | 4 回目の検収 §7 の 7、5 回目の検収 §2 |
| 段が `未判別` の行の当て直し(8-002 E5・8-003 E5・8-011 E5・8-012 E5・8-014 E1b/E3b/E4、8-005 の全部。8-016 は 7 回目) | 監査 21・22・30・33 回目 |
| 8-005 `Exegy`(`登録が要る`) | 1 回目 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜6 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| ~~`akurkar07/OrderBook` の E6~~ → 7 回目の §2 の 5 で当て直す(監査 34 回目の指摘 1) | 同上 |

## 監査 34 回目(対象 = 8347007、起動文の指紋 `6712ec9368c2`)

監査役の出力(逐語。[止める] 1 件・[直す] 2 件・[聞く] 1 件):

> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md:24` と同 `:43` — §1「この回に回さないもの」は「`akurkar07/OrderBook` の E6」を**この回で手を付けない**ものとして明記しているが、§2 の 5 は「8-004 `akurkar07/OrderBook`: `なし` の E2・E3a・E3b・E4・**E6** を当て直す」と、同じ E6 を**この回でやること**として明記している。§1 のこの一文は 6 回目の起動文(同じ位置に同じ文言)からの持ち越しで、6 回目時点では 8-004 が今回対象外だったための除外だが、7 回目で 8-004 の E6 を対象に含めたのに §1 の除外リストを更新していない。台帳を数え直すと(`docs/DATA/tools_catalog_cat8.tsv`)8-004 の `なし` セルは E2・E3a・E3b・E4・E6 の 5 つ全部で、§2 の 5 の記述はこの 5 つと一致する(§1 の記述だけが古いまま)。調査班がどちらを正として動くかで、E6 を当て直さない(§2 の指示を無視)か、§1 の明記を破るかのどちらかになる。
>
> 2. [直す] `scripts/cat8_ledger.py:277-304`(`quotes_in_log`)/ 起動文 `:35`(引用印字の規則) — scratchpad で合成の報告・生ログを作り実行して確かめた。根拠(知見表の根拠列・要素と段の根拠列)に「20260923_tools_8_run<N>.log:1356,1362」のように**1 つのセルで複数の行番号をカンマ区切り(ファイル名を 2 回目以降省略)で書く**形式 — これは実際に `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2223`(`log:15,23`)・`:2226`(`log:1067(N=107),1186-1223`)・`:2371`(`log:1356,1362`)など、この文書ですでに広く使われている書き方 — を与えると、`quotes_in_log` の正規表現 `(20260923_tools_8_run(\d+)\.log):(\d+)` は最初の「ファイル名:行番号」しか拾わず、2 つ目以降の行番号(「,1362」の部分)を取りこぼす。結果、セル内に引用が複数あると、抽出できなかった行番号に対応する引用が**実際には生ログに存在するのに「無い」と誤検出**される(実測: `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2371` と同じ形の根拠文言・同じ 2 つの引用文を合成データで再現し、正しく生ログに両方置いた状態で `check-elements --round <N>` を打つと、2 つ目の行番号に対応する引用側が「生ログの手の出力に無い」と誤って出た)。7 回目は th2-estore・th2-rpt-viewer・th2-check1 の README を「本文を生ログに印字し直してから引く」よう指示しており(起動文 `:17,40`)、この再引用がまさに複数 README・複数行番号を 1 セルにまとめる形になりやすい。起動文はこの形式上の制約(1 引用につき 1 回「ファイル名:行番号」をフルで書く、など)を指示しておらず、§7 の 2 の検査が正当な引用を誤って止める可能性がある(P10)。
>
> 3. [直す] 起動文 `:36`(「ノートの語は実際のコマンドの語と同じにする」) — `scripts/cat8_ledger.py`・`scripts/check_scan_report.py`・`scripts/cat8_step.py` を確認したが、ノートの文言と実際に打ったコマンドの正規表現・語を突き合わせる機械的検査は無い(grep で該当箇所なし)。この型は 6 回目に 2 回発生し(監査 33 回目の指摘 2・3)、しかもリード自身の検収がノートの文言だけを見て見逃していた(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run6.md:41` 「リードの検収…この食い違いを指摘していない」)。起動文はこの規則を文章で追加しただけで(`:36`)、§7 の完了条件にも検収の手順にもこれを機械的に見る手立てが無いままなので、CLAUDE.md P8 の型(規則は書くだけでは機能しない)に当てはまらないか。次回の検収でノートと実コマンドを 1 手ずつ突き合わせる手順を明記しておく必要は無いか。
>
> 4. [聞く] 起動文 `:43` と `docs/DATA/SCAN_2026-09-23_tools_cat8.md:305` — 1 回目の「危険で導入停止」の理由は「ビルド・実行(導入)は行っていない。README・LICENSEからの文書情報のみで§4の他の列を埋めた」であり、以降の回もこの候補には「未着手」で来た。7 回目は初めて「ファイルを読んで検索するだけ(`git clone` は scratchpad の下で、実行しない)」として、README・LICENSE 限定だった調査範囲をリポジトリ全ファイルの取得・検索まで広げる。実行しない点は 1 回目の理由と矛盾しないが、1 回目が調査範囲そのものを README・LICENSE に絞っていた(公開直後・保守者不明という危険信号を理由に)のに対し、7 回目はリポジトリ全体を clone してファイル一覧を検索する — この範囲拡大が「危険で導入停止」の判断(取得すら避けるか、実行さえしなければ取得・検索は安全と見るか)とどう整合するかは、起動文にも 1 回目の記載にも説明が無い。この拡大はリードが確認済みの判断か。

**リードの処置**:
1: 認める(6 回目の文の持ち越し)。§1 の「回さないもの」から `akurkar07/OrderBook` の E6 を消した。8 回目以降の表の同じ行も「7 回目の §2 の 5 で当て直す」に直した。
2: 認める。`quotes_in_log` が、1 つの参照の中の複数の行(`<生ログ>:1356,1362`)と範囲(`<生ログ>:1186-1223`、上限 500 行)を全部拾うようにした。scratchpad で、`:3,6` の 2 つの引用がどちらも通り、生ログに無い引用は止まることを確かめた。6 回目の節への結果は 0 件のまま。
3: 認める。機械では見ない(ノートは自由な文なので語の対応を機械で決めにくい)。**検収の手順に入れる**: リードは、`なし` の根拠に引いた手の全部について、ノートの語と `$` の行の正規表現を並べて読み、要素の名前が日本語と英語でコマンドに入っているかを書く(7 回目の検収 §2 の表に列を足す)。
4: 1 回目の危険信号(公開直後・保守者不明)は、その道具を**動かす**ことの危険で、読むことの危険ではない、とリードは判断する。ただし `git clone` は取得の仕組みを通すので、起動文を「`git clone` もしない。ファイル一覧を `ungh.cc` で取り、生の本文を `curl` で 1 本ずつ取って読むだけ。実行権を付けず、開くのは `grep`・`sed` だけ」に改めた。
直した版は監査 35 回目で確かめてから印を記録する。

## 監査 35 回目(対象 = 09f1fb4、起動文の指紋 `149ad2b0b4a7`)

監査役の出力(逐語。[止める] 2 件・[聞く] 1 件。34 回目の指摘 1・3 の処置は妥当、差分の外の新しい問題は無し):

> 1. [止める] `scripts/cat8_ledger.py:283`(`quotes_in_log` の正規表現 `[\d,\-]+\d`)— 監査 34 回目の指摘 2 が実例として引いた `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2219`(実測。検収の記録では `:2226` と書かれていたが実際の行は 2219)の実際の文言 `log:1067(N=107),1186-1223(検索と個別確認)` をそのまま regex に通すと、`(N=107)` の丸括弧で文字クラスが切れ、`1067` しか拾えず `1186-1223` は完全に落ちる(scratchpad で実測: `re.findall` の結果は `nums='1067'` のみ)。監査 34 回目が「直った」根拠として自分で引用した実例そのものが、実装した正規表現では直っていない。リードの処置欄が確認したのは単純形 `:3,6` のみで、注記付きの実例では再照合していない(P10: 目視・簡易ケースでの確認を「直った」の根拠にしている)。この形式(`N(N=…),lo-hi`)は 6 回目の報告に複数箇所で既に使われており、7 回目の「一覧 N 件 / 読んだ M 件」の書式とも自然に組み合わさるため、7 回目の `check-elements --round 7`(完了条件 §7 の 2)がこの形式の正当な引用を「生ログに無い」と誤って止める(過検出)可能性がある。
>
> 2. [止める] `scripts/cat8_ledger.py:390`(`印` の描画劣化検査)と `:416`(`なし` の一覧存在検査)— どちらも監査 34 回目で直していない旧正規表現 `(20260923_tools_8_run(\d+)\.log):(\d+)` を使い続けている。scratchpad で合成ログ・合成報告を作って実測: 単独参照 `log:10`(描画で `text-changed-after-wait` を含むブロック)を根拠にすると `印` の検査は正しく止まる。ところが同じ参照を先行する別行番号と 1 セルにまとめた `log:4,10` にすると、2 件目の `10` が一切拾われず検査が **無言で通る**(0 件)。`なし` の検査(:416〜437、描画ページを `なし` の根拠にしない規則)でも同様に、単独 `log:10` は正しく「描画したページを `なし` の根拠にしない」で止まるが、`log:4,10` にまとめると同じく無言で通る。7 回目の §2 の 5(8-004: ファイルを 1 本ずつ curl して読む)・2(8-016: 複数 README を生ログに印字し直して引く)は、まさに複数の生ログ行を 1 セルにまとめる引用を誘発する指示であり、この 2 箇所の未修正がそのまま §7 の 2(「0 件」を完了条件とする検査)の見落としに直結する。`tests/` に `cat8_ledger` の単体テストは無い(grep 0 件)ため、この種の回帰を機械的に検出する手段が今は無い。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:32`(指摘 4 への処置)— 監査 34 回目の指摘 4 は severity `[聞く]`(「この拡大はリードが確認済みの判断か」)だったが、処置は「リードは判断する」と自己完結で決め、オーナーへの確認は記載が無い。`危険で導入停止`(1 回目、公開直後・保守者不明)と判定した候補の調査範囲を README/LICENSE 限定からリポジトリ全ファイルの取得・grep 検索に広げる判断であり、実行はしない設計(`git clone` 禁止・`ungh.cc` + `raw.githubusercontent.com` の `curl`・実行権を付けない)自体は整合しているが、「動かすことの危険であって読むことの危険ではない」という線引きの妥当性そのものは、監査役が聞くべき判断として出した問いであり、リード単独の解釈で確定してよいものかは本文に理由が書かれていない。

**リードの処置**:
1・2: 認める(34 回目の直しは 1 か所の、単純な形だけで確かめていた)。生ログの参照を読む関数 `log_refs` を 1 つにまとめ、**3 つの検査(引用・`印` の描画・`なし` の一覧と描画)の全部で同じ関数を使う**ようにした。読む形: `:10` / `:4,10` / `:1186-1223`(範囲は 500 行まで)/ `:1067(N=107),1186-1223(注記)`(括弧の注記を飛ばす)/ 同じ根拠の中の複数の参照。確かめたこと: 報告の実物 2219 行の文言から 39 行(1067 と 1186〜1223)を拾う / 監査役の合成の報告 5 本(`audit35/`)を打ち直し、`:4,10` の形でも `印` の描画(条件 (iii))と `なし` の描画の禁止が止まる / 6 回目の節は 0 件のまま。
3: 答える。1 回目の `危険で導入停止` の理由は、その道具を**動かす**ことの危険(公開直後・保守者不明のコードを実行する)。7 回目にするのは、ファイルの文字を `curl` で取り、実行権を付けずに `grep`・`sed` で読むことだけで、コードを実行する経路が無い。作業はこの一時的な容器の中で、取り返しのつかないこと(CLAUDE.md §0.2 A-14: 実弾・口座・資本・新市場・リスク上限)に当たらない。**リードの判断で進める**。取ったファイルに実行できる形のもの(スクリプト・バイナリ)があっても開くのは文字としてだけ、と起動文に書いてある。
直した版は監査 36 回目で確かめてから印を記録する(起動文は 35 回目から変えていない。変えたのは `scripts/cat8_ledger.py` だけ)。

## 監査 36 回目(対象 = fcdda51、起動文の指紋 `149ad2b0b4a7`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件・[聞く] 1 件。確かめて問題の無かった点の要点: 生ログの参照を読む正規表現は `log_refs` の 1 か所に統合済みで、3 つの検査が全部これを呼ぶ / 2〜6 回目の節は 0 件 / 実物 2219 行の注記付きの形から 39 行を拾う):

> 1. [止める] `scripts/cat8_ledger.py:278-296`(`log_refs`)— 全角読点「、」を参照の区切りとして扱っておらず、監査 34・35 回目が「直した」としたのと同型の取りこぼしが、この文書に実在する別の区切り文字で再発している。実測(scratchpad、`/tmp/.../audit36/`):
>    - `docs/DATA/SCAN_2026-09-23_tools_cat8.md:1717`(`backtrex` / `E3a` / 値 `なし` — `要素と段` 表の内側で `check-elements --round 5` の対象)の根拠列は文言 `...20260923_tools_8_run5.log:188(一覧を取った手)、192,203,214,225,236,247,258,269,280(9頁の取得)` を含む。この cell を実物からそのまま `log_refs` に通すと `[('...run5.log','5','188')]` の 1 件しか返らず、`192〜280` の 9 件が無言で落ちる(全角読点の直後で正規表現が数字にマッチせず、`mm.end() < len(part)` の分岐でループ全体が break するため)。同型の欠落は `:1991`・`:2079`・`:2081` にもあるが、これらは `要素と段` 表の外(`### 4.0 機械可読の表`)なので `check-elements` の対象外。
>    - 上記の欠落が実害を持つことを合成データで再現: `docs/DATA/probes/` に一時ファイル `20260923_tools_8_run99.log`(通常の手 = 10 行目、`cat8_render.js` を使った手 = 20 行目)を作り、`なし` の根拠に `...log:10(一覧)、20(描画)` と書いた合成報告を `check-elements --round 99` に通したところ、**`合計 0 件`** で通過した(本来は `:444-449` の「描画した頁を `なし` の根拠にしない」規則で止まるべき行)。確認後、一時ファイルは削除済み(`git status --porcelain docs/DATA/probes/` = 出力なし)。
>    - 監査 35 回目の「処置 1・2」は「3 つの検査の全部で同じ関数を使う」ことと、報告実物 2219 行(半角カンマ形式)・監査役の合成の報告 5 本のみで確認しており、**この文書に既に存在する全角読点区切り(1624・1717・1991・2079・2081 行)では再照合していない**。監査 35 回目自身が指摘 1 で名指しした型(P10: 目視・簡易ケースでの確認を「直った」の根拠にすること)が、この差分でも同じ形で再発している。`tests/` に `cat8_ledger` の単体テストが無いことも監査 35 回目の指摘 2 のまま未着手。
>    - 7 回目の起動文は 8-004(README・LICENSE 限定から複数ファイルを curl で読む)・8-016(複数 README を生ログに印字し直して引く)を指示しており、複数参照を 1 セルにまとめる引用を誘発する。この文書では読点区切りが既に自然発生している(round 5 の知見・要素と段表の複数箇所)ため、7 回目も同じ区切りで書かれる可能性が高く、その場合 `quotes_in_log`(知見表、round≥7)・`印`/`なし` の描画検査のいずれも一部の参照を検査せずに通す。
>
> 2. [直す] `scripts/cat8_ledger.py:278-296` — 1 と同根拠。`log_refs` の docstring(`:279-280`)は対応する形として `:10` / `:1356,1362` / `:1186-1223` / `:1067(N=107),1186-1223(注記)` の 4 形しか挙げておらず、全角読点区切りは仕様として明記も試験もされていない。次回直すときは、この文書で実際に使われている区切り文字を機械的に洗い出してから正規表現を書くか(今回のように事後に見つかる形を繰り返さない)、最低限 `tests/` に `log_refs` の単体テストを 1 本置いて回帰を機械で検出できるようにする必要があるのではないか。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:47`(監査 35 回目・指摘 3 への処置)— 監査 35 回目は severity `[聞く]`(「動かすことの危険であって読むことの危険ではない、という線引きの妥当性はリード単独の解釈で確定してよいものか」)として出したが、処置は「答える…**リードの判断で進める**」とリード単独の理由付け(CLAUDE.md §0.2 A-14 の範囲に当たらない)で自己完結させ、オーナーへの確認は書かれていない。A-14 の範囲に直接は当たらないという理屈自体は書かれているが、監査役が名指しで「聞く」と出した判断をリードが単独で答えて閉じてよいかは、本文だけでは判断できない。

**リードの処置**:
1・2: 認める(3 回続けて、見つかった書き方だけを足して直していた)。**直し方を変えた**: (a) 報告の実物で使われている参照の書き方を機械で洗い出した(`grep -o 'run[0-9]\.log:[0-9][^|]\{0,40\}'` の形ごとの数。区切りは `,`・`、`・注記の括弧・範囲の `-`)。`log_refs` は `,`・`、`・`，`・`;`・`；` を区切りとして読む。(b) **読み落としを黙って通さない形にした**: 7 回目以降は、ファイル名を付けずに行の番号を並べた参照(`<生ログ>:10、20`・`:4,10`・`:1067(注記),1186`)そのものを `check-elements` が止め、「行(か範囲)ごとにファイル名を書く」ことを求める。読める形が狭いので、読めない書き方は止まる。起動文にもこの書き方を書いた。(c) `cat8_ledger.py selftest` を足した(`tests/` は区分 8 の書き込み先に無いので、道具の中に置いた)。形 8 つ(`:10`・`:4,10`・範囲・注記付き・読点区切り・参照 2 つ・前に件数・後ろに句点)で、拾う行と列挙の検出の両方を見る。8 つとも通った。実物 1717 行から 10 行(188 と 192〜280 の 9 行)を拾う。2〜6 回目の節は 0 件のまま。
3: 答える。リードの判断で閉じる。理由: CLAUDE.md §0.1 で、作業中に原文に無い判断が要るときは止めて擦り合わせる。ただし、この判断は割り当ての中の安全の線引きで、取り返しのつかないこと(§0.2 A-14)に当たらず、取り消しもきく(読んだものを使わなければよい)。そのうえで、**オーナーへの報告にこの判断を書いて示す**(異論があれば 8-004 の当て直しを外す)。
直した版(起動文の指紋 `6dc34a0d1d62`)は監査 37 回目で確かめてから印を記録する。

## 監査 37 回目(対象 = a1d007a、起動文の指紋 `6dc34a0d1d62`)

監査役の出力(逐語。[止める] 2 件・[直す] 2 件。1〜6 回目の節への影響なし、`selftest` は書かれたとおりに動く):

> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md:35` と `scripts/cat8_ledger.py:278-296`(`log_refs`)— 起動文がこの回に「よい」と明記する例文そのもの `20260923_tools_8_run7.log:10、20260923_tools_8_run7.log:20`(ファイル名を省略せず繰り返す、監査 36 回目の指摘への直しが求める書き方)を、実際に `log_refs` に通すと壊れる。実測(scratchpad): `log_refs("20260923_tools_8_run7.log:10、20260923_tools_8_run7.log:20")` -> `['10', '20260923', '20']`(2 番目のファイル名の日付部分 "20260923" を行番号として誤読)/ `ref_list_errors(同文字列)` -> `['20260923_tools_8_run7.log:10、2']`(列挙の形として誤って止める)。原因: `rest` を区切り文字で split したあと、直前の part が最後まで数字で終わっていると break せず次の part に進むが、次の part が「ファイル名の文字列そのもの」になっているケースを想定しておらず、`re.match(r"^(\d+)...")` がファイル名冒頭の日付 8 桁を行番号として拾ってしまう。結果、起動文が「よい」と教える書き方を字面通り書くと、(a) `check-elements --round 7`(§7 の 2、7 回目の完了条件)がこれを「列挙の形」として誤って止める、(b) 仮に止まらなくても phantom な行番号(20260923)が `なし` の根拠検査に渡り「行番号が生ログに無い」という誤ったブロックを生みうる。実際の文書は過去 6 回とも常に `docs/DATA/probes/` の path prefix を付けて書いており(grep で確認、prefix 無しの例は 0 件)、prefix を付ければこの誤読は起きないが、**起動文の例文自体が prefix を付けていない**ため、この文をそのまま真似て書くと壊れる。
>
> 2. [止める] `scripts/cat8_ledger.py:278-305`(`log_refs` / `ref_list_errors`)— 区切り文字が `,`・`、`・`，`・`;`・`；` の 5 種に限られており、この文書で最も頻出する列挙記号「・」(全文で 403 回出現、要素と段の表の随所で通常の区切りとして使われている)や、スペース・全角スペース・「と」「および」・改行では、`log_refs` が最初の 1 件しか拾わず残りを無言で落とし(3 回連続で見つかった型と同じ break の構造)、`ref_list_errors` もこれらの形を検出しない(検出の正規表現も同じ 5 文字クラスしか見ていない)。実測(scratchpad、合成報告 `report_space.md` + 合成ログ。使用後は `docs/DATA/probes/` から削除済み): `なし` の根拠を `一覧 2 件 / 読んだ 2 件、docs/DATA/probes/20260923_tools_8_run99.log:2 6`(スペース区切り)にして `check-elements --round 99` を通すと、**本来は「描画した頁を `なし` の根拠にしない」規則(:481-486)で止まるべき 2 行目(cat8_render.js の手)が検査されず、合計 0 件で通過**した。同じ内容をカンマ区切り(`:2,6`)に変えると正しく 2 件(列挙の形・描画頁)が検出される対照実験も確認済み。監査 36 回目が見つけたのと全く同型の欠陥(「見つかった区切り文字だけを追加して直す」)が、区切り文字の集合を広げても再発している。
>
> 3. [直す] `scripts/cat8_ledger.py:308-329`(`cmd_selftest`)— 8 つの試しケースは、処置欄が書く通り「報告の実物で使われている参照の書き方」だけから作られており(`grep -o 'run[0-9]\.log:[0-9][^|]\{0,40\}'` による既存インスタンスの洗い出し)、この文書の一般的な列挙記号(・)や、起動文自身が「よい」と書く複数ファイル名反復の形を 1 件も含んでいない。監査 35 回目が同じ差分(1・2 の直し)に対して出した指摘(「目視・簡易ケースでの確認を『直った』の根拠にしている」)と同じ型が、この回の `selftest` にも当てはまる。`selftest` が全 OK でも「読み落としを黙って通さない」という今回の目標(処置(b))が実際には達成されていないことは 1・2 の実測が示す通り。
>
> 4. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md` 全文 / `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:66`(監査 36 回目・指摘 3 への処置)— 処置は「オーナーへの報告にこの判断(動かす危険であって読む危険ではない、という線引き)を書いて示す」と約束したが、起動文のどこにも(§7 の 5「返答に書く項目」の一覧にも、§2 の 5(8-004 の指示本文)にも)この判断を報告に含めるよう求める記述が無い。約束した行動が、次に実際に動く文書(この起動文)の導線に反映されていない(CLAUDE.md P8 型 — 規則は書くだけでは機能しない)。

**リードの処置**:
1: 認める。`log_refs` は、次のファイル名(`docs/DATA/probes/` の有無を問わない)の前で読むのを止め、次の参照は別に読む。起動文の例文に `docs/DATA/probes/` を付けた。
2・3: 認める(区切りの文字を挙げる作りが原因で、挙げていない文字で漏れる)。**`ref_list_errors` を、区切りの文字を挙げない作りに変えた**: 参照(`<生ログ>:N` か `:N-M`、直後の括弧の注記は飛ばす)の直後から、文字(かな・漢字・英字)に当たる前に数字が出たら止める(記号・空白・全角空白・「・」・「/」を全部含む)。「と」「および」「又は」「and」「or」のあとの数字も止める。表の欄の境(`|`)と改行の先は見ない。次の参照(ファイル名で始まるもの)は止めない。`selftest` に、起動文の例文の 2 つの形・空白・「・」・「と」・「/」・注記つきの空白・欄の境・文字で始まる文の形を足して 18 通り、全部通った。報告の実物に当てると、列挙の形の参照を 60 か所拾う(`N,N` 50・`N、N` 6 ほか。1〜6 回目は検査の対象外なので止まらない)。2〜6 回目の節の `check-elements` は 0 件のまま。**限界**: 参照の直後に文字で始まる語を挟んで番号を続ける書き方(`:2 行目と 6 行目`)は、「と」の前に文字があるので止まらない。これは起動文の「行ごとにファイル名を書く」の規則と、検収のリードの読みで見る。
4: 認める。約束は「オーナーへの報告に書く」で、調査班の返答の項目ではない。オーナーへの返答(2026-09-24 の会話)で示し、状態板の区分 8 の欄にも書いた(「8-004 はファイルを文字として読むだけにする。異論があれば外す」)。
直した版(起動文の指紋 `f4d52bd80ce1`)は監査 38 回目で確かめてから印を記録する。

## 監査 38 回目(対象 = aea16ce、起動文の指紋 `f4d52bd80ce1`)

監査役の出力(逐語。[止める] 1 件・[直す] 3 件・[聞く] 1 件。確かめて問題の無かった点の要点: 37 回目の直し自体は名指しの形すべてで動く / 起動文の「よい」例文と「悪い」例は書かれたとおりに通る・止まる / `selftest` 18 通り OK / 2〜6 回目は 0 件):

> 1. [止める] `scripts/cat8_ledger.py:308-322`(`ref_list_errors`)/ `:278-296`(`log_refs`)/ 実例 `docs/DATA/SCAN_2026-09-23_tools_cat8.md:1996` — 単一の注記の中に丸括弧が入れ子になる書き方(例「`(Table: Ours (internal) – Python vectorised retained)`」)があると、`^\s*[(（][^)）]*[)）]` は最初に出た閉じ括弧までしか取り除かず、残った文字列の先頭が英単語(`Python`)になる。その結果、後ろに続く列挙の番号を `ref_list_errors` が**無言で通す**(実測: 合成入力 `"...retained)、555"` → `ref_list_errors` = `[]`)。`log_refs` も最初の番号(`4369`)しか拾わない。**この注記の書き方(入れ子の丸括弧)は文書の実物(1996 行目、5 回目)に既に 1 件存在しており**、round 7 が README 引用や表の引用で同種の注記を書く可能性は排除できない。記録された「限界」(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:83`「文字で始まる語を挟んで番号を続ける書き方は止まらない」)はこの経路(入れ子括弧が単一注記のストリップを壊す)を名指ししていない(「見つかった形だけを直す」の再発パターン)。
>
> 2. [直す] `scripts/cat8_ledger.py:288`(`log_refs`)— 区切りを挟まずに丸括弧注記が 2 つ連続する書き方(例 `:2(note1)(note2)6`)を与えると、`log_refs` は 2 つの番号を分離せず、"2" と "6" を連結して**架空の行番号 "26"** を作る(実測: `['26']`)。`ref_list_errors` もこの形を検出しない(0 件)。この形の実例は文書中に 0 件(grep で確認)だが、読み落としではなく捏造した行番号を生む点で 1 とは別の失敗の型。
>
> 3. [直す] `scripts/cat8_ledger.py:309`(`re.split(r"[|\n]", ...)` による表の列境の判定)— 注記の中にエスケープ済みのパイプ文字(`\|`)が入ると、参照直後の走査がそこで打ち切られ、続く番号が無言で見落とされる(実測: `"...log:2(A\|B)、6"` → `ref_list_errors` = 0 件、`log_refs` は "2" のみ)。この文書には既にコマンド・コード引用中のエスケープ済みパイプがある(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:525`)ため、8-016(th2 README 引用)や検索コマンドの引用で同型の注記が付く可能性は排除できない。
>
> 4. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:83`(監査 37 回目への処置の「限界」の一文)— 記録された限界は「参照の直後に文字で始まる語を挟んで番号を続ける書き方」の 1 種類のみを名指ししている。1〜3 で示した経路(入れ子の丸括弧・連続する丸括弧注記・注記中のエスケープ済みパイプ)はいずれもこの一文とは別の機構で、うち 1 は文書に実例のある書き方から到達できる。35・36・37 回目と同型(「見つかった形だけを追加して直す」の反復)がここでも起きているとすれば、起動前に `ref_list_errors` を「文字種の壁」ではなく丸括弧の対応を数える(入れ子対応)形に直すべきか、それとも今回もこの 3 経路を「検収のリードの読みで見る」対象に含めてよいか。
>
> 5. [直す] `scripts/cat8_ledger.py:308-322` — 句点(「。」)の直後に西暦など無関係な数字が続く地の文(例「…log:2。2026年…」)も「列挙の形」として誤って止める(実測: 過検出 1 件)。読み落としの逆(過検出)で、正当な書き方を書き直させる負荷になるだけなので severity は低い。

**リードの処置**:
1〜4: 認める。34〜38 回目の 5 回、「書き方を読み当てる」作りを広げるたびに、読み当てられない書き方が見つかった。**作りを逆にした**(7 回目以降): 書き方を読み当てるのをやめ、**根拠の欄で数字を置ける場所を決め、それ以外の場所にある数字を全部止める**(`unaccounted_numbers`)。置ける場所は、(1) 参照 `<生ログ>:N`・`<生ログ>:N-M`(1 つずつファイル名を付ける)、(2) 「…」と `…` の中、(3) URL、(4) 日付 `2026-09-24`、(5) `N 件`、(6) 英字に付いた数字(`E1a`・`run7`)。これ以外の数字はどんな書き方(列挙・入れ子の括弧・続く括弧・`\|` を含む注記・空白・記号)でも止まる。7 回目以降の 3 つの検査(引用・`印` の描画・`なし`)は、参照を上の (1) の形だけで読む(`strict_refs`。列挙は読まない。読まれなかった番号は必ず「決まった場所に無い数字」で止まるので、黙って通る経路が無い)。`selftest` は 19 通り(38 回目の 3 つの形を含む)で、止まるか通るかを確かめて全部通った。2〜6 回目の節は 0 件のまま(1〜6 回目は元の読み方)。6 回目の節の文をこの規則に当てると 19 か所が止まる(どれも番号を並べた参照)ので、調査班が書き方を合わせれば通る量。起動文に数字の置き場の規則を書いた。
5: 過検出は残る(「。2026年」のような地の文の数字も止まる)。止まったら「…」に入れるか `2026-09-24` の形で書けば通る。黙って通る向きの誤りではないので、このままにする。
**限界**: 決まった場所の中((2)「…」・`…` の中)に行の番号を書いて逃げる書き方は止まらない(たとえば「…」の中に `:20` と書く)。これは起動文の規則を破る書き方で、検収でリードが「…」の中身を読む。
直した版(起動文の指紋 `932731abf043`)は監査 39 回目で確かめてから印を記録する。

## 監査 39 回目(対象 = b90f3ba、起動文の指紋 `932731abf043`)

監査役の出力(逐語。[止める] 4 件・[直す] 4 件・[聞く] 1 件):

> 1. [止める] `scripts/cat8_ledger.py:319-329`(`unaccounted_numbers`)/ 実例 `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2371`・`:2293`・`:2271` — 「段N」(要素の判定段)というこの調査そのものの中核語彙は、`段`が英数字のいずれでもないため常に「決まった場所に無い数字」として止まる。実測(`docs/DATA/SCAN_2026-09-23_tools_cat8.md` 全体で `段[0-9]` が 107 回、`要素と段` 表の根拠列の中だけで 47 行に出現。scratchpad の合成 7 回目報告で `段3を満たす根拠は…` を含む根拠セルを `check-elements --round 7` に通すと実際に「決まった場所に無い数字: 3」で止まった)。7 回目の起動文 §2 の 2(8-016 Exactpro/th2、「段5の(イ)を…で読んでよいかは述語の読みの問い」)と 6 回目の同候補・8-006 freqtrade の根拠が現にこの「段N」表現で書かれていることから、この回の必須成果物そのものがこの検査で止まる可能性が高いのではないか。起動文 §2(`:35`)の数字の置き場の規則にこの語彙の扱いが無いが、この状態で起動してよいか。
>
> 2. [止める] `scripts/cat8_ledger.py:302,319-329` — 「英字に付いた数字(E1a・run7 など)」という許容範囲は、実装では「直前が `[A-Za-z0-9_.\-]` のいずれか、または直後が `[A-Za-z0-9_]`」という無制限の隣接規則になっており、構造化された記号(E1a・run7)に限定されていない。実測(scratchpad、`check-elements --round 7` 実行): 根拠セル `段3を満たす根拠は L1362 にある。\`1200\` も参考。https://example.com/page,1400 も参照` を通すと、止まったのは `段3` の `3` だけで、`L1362`・`` `1200` ``・URL に付いた `1400` はいずれも「決まった場所に無い数字」として検出されず素通りした。記録(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:102`)の「読まれなかった番号は必ず『決まった場所に無い数字』で止まるので、黙って通る経路が無い」という主張と、この実測結果は食い違わないか。
>
> 3. [止める] `scripts/cat8_ledger.py:324`(`` `[^`]*` `` の無条件除去)— バッククォートで囲んだ内容は中身を問わず丸ごと数字検査から除外される。実測は 2 と同じ合成報告(`` `1200` `` が無検出で通過)。設計意図(コード片・コマンドの引用)を超えて、根拠に裸の数字を `` ` ` `` で囲むだけで検査を回避できる状態だが、これは意図した許容範囲か。
>
> 4. [直す] `scripts/cat8_ledger.py:325`(`https?://\S+` の除去)— URL 正規表現は空白まで貪欲に一致するため、区切りの空白を入れずに URL の直後へ数字を続けると(`https://example.com/page,1400` のように)、その数字も URL の一部として丸ごと除去される(2 の合成実測で確認)。URL に隣接させるだけで任意の数字を検査対象から外せる経路になっていないか。
>
> 5. [直す] `scripts/cat8_ledger.py:328`(`\d+\s*件` の除去)— 起動文 §2(`:35`)が許す場所は「`N 件`」の形だが、実装は文脈を問わず数字+`件` を一律で除去する。実測(scratchpad): `行番号は 1362 件目のようなものだが実際は違う仕様 1362件` を通すと `1362` は 2 箇所とも検出されない。「一覧 N 件 / 読んだ M 件」の専用検査(`:502`)は無傷で機能するとはいえ、`unaccounted_numbers` 側の除去範囲が起動文の記述より広いのではないか。
>
> 6. [直す] `scripts/cat8_ledger.py:302,319-329` — ファイル名の無い裸の範囲表記(例 `1362-1363`)は、直前が数字で塞がれた 2 つ目の数値だけが `(?<![A-Za-z0-9_.\-])` の除外(ハイフンを含む)に該当し検出から漏れる(実測: `1362-1363 の範囲(参照なし)` → 検出されるのは `1362` のみ)。ファイル名を伴わない範囲風の数字列の後半が黙って通る経路になっていないか。
>
> 7. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md:35` / `scripts/cat8_ledger.py:319-329` — 生ログではなくリポジトリ内の他ファイル(スクリプト・README・LICENSE 等)の行番号を指す慣用表現(`scripts/setup_data.sh 40-42行`、`freqai-running.md 71-72行` など)は、起動文 §2 が列挙する 6 種類の置き場のどれにも該当せず、常に止まる。実測: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` 全体で `N行`/`N-M行` 形の生ログ以外への行参照が 161 回出現し(`要素と段` 表の根拠列内にも複数実例)、6 回目の該当セル(:2271, :2293)はいずれもこの型で 7 回目基準に当てると止まる。同時に 3 のバッククォート除外を使えば無条件に検査を回避できてしまう(段 N と同じ二重の問題)。起動文はこの引用の扱いを定めていないが、このまま起動してよいか、それとも生ログへの転記を義務付けるなど扱いを明記すべきか。
>
> 8. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:102`(監査 38 回目への処置)— 「6 回目の節の文をこの規則に当てると 19 か所が止まる(どれも番号を並べた参照)なので、調査班が書き方を合わせれば通る量」という記述を、`台帳の値のまま` の除外条件を含めて実際に再現すると、止まる数(19)自体は一致するが、その内訳のうち少なくとも 10 か所(`:2271`・`:2293`・`:2300`・`:2305`・`:2307`・`:2351`・`:2371`・`:2372`・`:2375`・`:2376`)は「番号を並べた参照」ではなく、段 N の段判定の理由づけの地の文や生ログ以外のファイルの行参照であり、性質が異なる(1・7 と同型)。「どれも番号を並べた参照」という要約は、止まる件数だけを確認し中身を再照合しない目視要約になっていないか(P10)。
>
> 9. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:104`(記録された限界)— 記録済みの限界は「参照の直後に文字で始まる語を挟んで番号を続ける書き方」と「『…』・`` ` ` ``の中に行番号を書いて逃げる書き方」の 2 種のみを名指ししている。2〜7 で示した経路(英字・記号隣接の無制限除外、URL 直後の数字吸収、`N 件` の無条件除外、範囲表記の後半、生ログ以外のファイルの行参照)はいずれもこの記述と別の機構であり、34〜38 回目と同型(「見つかった形だけを記録し、次に別の経路が見つかる」)の再発ではないか。起動前に検査の作りを直すべきか、それとも今回もこれらを「検収のリードの読みで見る」対象として起動してよいか。

**リードの処置**:
1〜7・9: 認める。34〜39 回目の 6 回、**自由な文の中から行の番号を読み当てる**作りを、読み当て方を変えながら直してきたが、どの作りも、読み当てられない書き方(読み落とし)か、正しい書き方を止める書き方(過検出)が残った。原因は、行の番号が自由な文の中にあること。**番号だけを書く欄を別に置いた**(7 回目以降):
- 「要素と段」の表を 7 列にし、7 列目「生ログの行」には `<生ログ>:N` か `<生ログ>:N-M` を空白で区切って並べるだけにする。**この欄は形を丸ごと確かめる**(1 語ずつ厳密な形に当てて、当たらない語があれば止める。注記・読点・カンマ・`段3`・`` `1200` ``・URL などは全部止まる)。値が `印`・`なし` の行はこの欄が空なら止める。3 つの検査(引用・`印` の描画・`なし`)は、この欄だけを読む。6 列目「根拠」は自由な文に戻した(`段3` も他のファイルの行の番号も書いてよい。検査は読まない)。
- 知見の表の「根拠」の欄は、生ログの参照・URL・日付・「取得日」と区切りの記号だけにする(残りの字があれば止める)。説明は「知見」の欄に書く。
- `selftest` を、2 つの欄の形の試し 19 通りに書き直した(39 回目の形 `段3`・`` `1200` ``・URL に続く数字・注記・読点・カンマ・空白・他のファイルの行を含む)。全部通った。合成の 7 回目の節(7 列の表 8 行)で、描画の手を `なし` に引いた行・`印` に引いた描画の手の `text-changed-after-wait`・生ログに無い引用・空の欄・カンマの列挙・6 列目に番号を書いた欄の全部が止まり、正しい行は通ることを確かめた。2〜6 回目の節は 0 件のまま。
- **残る限界**: 7 列目に正しい形で**関係の無い**行を書く(根拠と違う手の行を書く)ことは、形では見分けられない。これは検収でリードが、7 列目の行の手の中身を根拠と突き合わせて読む。
8: 認める(止まる数だけを見て、中身を読まずに「どれも番号を並べた参照」と書いた)。今回の作りでは 6 列目は検査しないので、この 19 か所は止まらない。
直した版(起動文の指紋 `a2d1e56aa3c6`)は監査 40 回目で確かめてから印を記録する。

## 監査 40 回目(対象 = 94bd91a、起動文の指紋 `a2d1e56aa3c6`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件。確かめて問題の無かった点の要点: 6 列・8 列の行、列の中の `|` で列がずれた行は黙って飛ばされずに止まる / `check_scan_report.py` はもとから「要素と段」の表を読まない / `import` は 7 列に対応 / `selftest` 19 通り OK / 2〜6 回目は 0 件):

> 1. [止める] `scripts/cat8_ledger.py:489`・`:517`(`if r7 and "台帳の値のまま" not in ev:` / `if ... val == "なし" and "台帳の値のまま" not in ev:`)— 起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md:35` は「この回に触らない行は、6 列目に `台帳の値のまま(<何>回目の節)` と書き、7 列目は空でよい」と定めているが、コード側の判定は 6 列目(根拠)の**部分一致**(`"台帳の値のまま" not in ev`)であり、6 列目が自由文であるにもかかわらずこの部分一致だけで 7 列目(生ログの行)の形式検査・空欄検査・引用照合(`quotes_in_log`)のすべてを丸ごとスキップする。実測(scratchpad、`docs/DATA/probes/` に一時ログを置いて確認、削除済み): 値 `なし` の行で、6 列目に「…台帳の値のままではなく今回実測した」(=実際には**据え置きではない**という意味の文だが部分文字列としては当たる)と書き、7 列目(生ログの行)を**完全に空欄**にしても `check-elements --round 97` は 0 件で通った。同じ行から「台帳の値のまま」の語だけを除くと正しく 2 件(空欄・一覧根拠なし)で止まることを対照確認済み。値 `印` の行でも同様に、6 列目に「「この文はログに無い」台帳の値のままにはしない今回実測」と書き 7 列目を空にすると、**生ログに一度も現れない捏造引用文**を含んだまま 0 件で通った(`quotes_in_log` も同じ条件でスキップされるため)。この列の分離(6 列目=自由文・7 列目=厳格な形)自体が今回のコミットの新設であり、以前は 1 つの列が評価も兼ねていたため「7 列目が完全に空でも通る」という抜け道は今回のコミットで新たに生じたものと見られる。`cmd_selftest`(19 通り)はこの上位のゲート条件(`"台帳の値のまま" not in ev`)を 1 つも試しておらず、この抜け穴を検出しない。8-016(th2 系、README の再引用)・8-006(freqtrade)は自由文の説明が長くなりがちな候補で、この部分一致に偶然でも当たる文を書く可能性は低くない(P3: 起動文の規則と実装が一致しているかの検査)。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:134`(監査 39 回目処置の「残る限界」)— 記録された限界は「7 列目に正しい形で関係の無い行を書く(根拠と違う手の行を書く)ことは、形では見分けられない」の 1 種類のみだが、指摘 1 は形の話ではなく「7 列目が完全に空でも 6 列目の部分一致で検査自体がスキップされる」という別の機構であり、この記述はカバーしていない。34〜39 回目と同型(「見つかった形だけを記録し、次の回で別の経路が見つかる」)の再発ではないか。

**リードの処置**:
1: 認める(列を分けたときに、「触らない行」の判定を自由な文の部分一致のまま残した)。7 回目以降の「触らない行」は、**6 列目が `台帳の値のまま(…)` だけ(完全一致)で、かつ値と段が台帳と同じ**ときに限る。台帳と違えば止め、「触った行」として 7 列目の検査に回す。6 列目にその語がほかの文と一緒にあれば止める。起動文にも「6 列目を `台帳の値のまま(…)` だけにし、値と段は台帳と同じ」と書いた。合成の 4 行で確かめた: 部分一致の文(止まる。7 列目の空・一覧の根拠なしも止まる)/ `台帳の値のまま` だが段が台帳と違う(止まる)/ 値が台帳と違う(止まる)/ ほんとうに触らない行(通る)。2〜6 回目は 0 件のまま。
2: 認める。記録の限界に足す: 触らない行は、台帳と値・段が同じなら、7 列目の検査を受けない(前の回の根拠をそのまま使う)。その根拠の確かめは、その行を当てた回の検収で済んでいる。
直した版(起動文の指紋は下のコミットのもの)は監査 41 回目で確かめてから印を記録する。
