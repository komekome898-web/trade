# 監査記録: 道具サーベイ 区分 8 の 7 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md`(初版の指紋 `6712ec9368c2`)。6 回目の起動文(監査 31・32 回目を通した版)を元に、範囲・引用の印字・ノートの語の規則を改めた。監査 32 回目の [直す](見出しの回番号)もこの版で直した(「4〜6 回目の差し戻しの対策」)。

## 8 回目以降に回すもの(ここに全部書く)

| 何 | 出所 |
|---|---|
| 8-009・8-010・8-011・8-012・8-014・8-015 の `なし` の当て直し(台帳の `なし` の数: 6・4・3・3・2・6) | 4 回目の検収 §7 の 7、5 回目の検収 §2 |
| 段が `未判別` の行の当て直し(8-002 E5・8-003 E5・8-011 E5・8-012 E5・8-014 E1b/E3b/E4、8-005 の全部。8-016 は 7 回目) | 監査 21・22・30・33 回目 |
| 8-005 `Exegy`(`登録が要る`) | 1 回目 |
| 辿る一覧(dev.to / X の `aiwithjainam` / zenn / quantreo・algorier・fortraders / 1 回目の検索計画 7) | 2 回目の検収 §11 の (4)。1〜6 回目は手付かず |
| 成熟度の枠組みの原典 | `2026-09-24_tools_scan_cat8_run3_prompt.md` の冒頭の表 |
| ~~`akurkar07/OrderBook` の E6~~ → 7 回目の §2 の 5 で当て直す(監査 34 回目の指摘 1) | 同上 |

## 監査 34 回目(対象 = 8347007、起動文の指紋 `6712ec9368c2`)

監査役の出力(逐語。[止める] 1 件・[直す] 2 件・[聞く] 1 件):

> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run7_prompt.md:24` と同 `:43` — §1「この回に回さないもの」は「`akurkar07/OrderBook` の E6」を**この回で手を付けない**ものとして明記しているが、§2 の 5 は「8-004 `akurkar07/OrderBook`: `なし` の E2・E3a・E3b・E4・**E6** を当て直す」と、同じ E6 を**この回でやること**として明記している。§1 のこの一文は 6 回目の起動文(同じ位置に同じ文言)からの持ち越しで、6 回目時点では 8-004 が今回対象外だったための除外だが、7 回目で 8-004 の E6 を対象に含めたのに §1 の除外リストを更新していない。台帳を数え直すと(`docs/DATA/tools_catalog_cat8.tsv`)8-004 の `なし` セルは E2・E3a・E3b・E4・E6 の 5 つ全部で、§2 の 5 の記述はこの 5 つと一致する(§1 の記述だけが古いまま)。調査班がどちらを正として動くかで、E6 を当て直さない(§2 の指示を無視)か、§1 の明記を破るかのどちらかになる。
>
> 2. [直す] `scripts/cat8_ledger.py:277-304`(`quotes_in_log`)/ 起動文 `:35`(引用印字の規則) — scratchpad で合成の報告・生ログを作り実行して確かめた。根拠(知見表の根拠列・要素と段の根拠列)に「20260923_tools_8_run<N>.log:1356,1362」のように**1 つのセルで複数の行番号をカンマ区切り(ファイル名を 2 回目以降省略)で書く**形式 — これは実際に `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2223`(`log:15,23`)・`:2226`(`log:1067(N=107),1186-1223`)・`:2371`(`log:1356,1362`)など、この文書ですでに広く使われている書き方 — を与えると、`quotes_in_log` の正規表現 `(20260923_tools_8_run(\d+)\.log):(\d+)` は最初の「ファイル名:行番号」しか拾わず、2 つ目以降の行番号(「,1362」の部分)を取りこぼす。結果、セル内に引用が複数あると、抽出できなかった行番号に対応する引用が**実際には生ログに存在するのに「無い」と誤検出**される(実測: `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2371` と同じ形の根拠文言・同じ 2 つの引用文を合成データで再現し、正しく生ログに両方置いた状態で `check-elements --round <N>` を打つと、2 つ目の行番号に対応する引用側が「生ログの手の出力に無い」と誤って出た)。7 回目は th2-estore・th2-rpt-viewer・th2-check1 の README を「本文を生ログに印字し直してから引く」よう指示しており(起動文 `:17,40`)、この再引用がまさに複数 README・複数行番号を 1 セルにまとめる形になりやすい。起動文はこの形式上の制約(1 引用につき 1 回「ファイル名:行番号」をフルで書く、など)を指示しておらず、§7 の 2 の検査が正当な引用を誤って止める可能性がある(P10)。
>
> 3. [直す] 起動文 `:36`(「ノートの語は実際のコマンドの語と同じにする」) — `scripts/cat8_ledger.py`・`scripts/check_scan_report.py`・`scripts/cat8_step.py` を確認したが、ノートの文言と実際に打ったコマンドの正規表現・語を突き合わせる機械的検査は無い(grep で該当箇所なし)。この型は 6 回目に 2 回発生し(監査 33 回目の指摘 2・3)、しかもリード自身の検収がノートの文言だけを見て見逃していた(`docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run6.md:41` 「リードの検収…この食い違いを指摘していない」)。起動文はこの規則を文章で追加しただけで(`:36`)、§7 の完了条件にも検収の手順にもこれを機械的に見る手立てが無いままなので、CLAUDE.md P8 の型(規則は書くだけでは機能しない)に当てはまらないか。次回の検収でノートと実コマンドを 1 手ずつ突き合わせる手順を明記しておく必要は無いか。
>
> 4. [聞く] 起動文 `:43` と `docs/DATA/SCAN_2026-09-23_tools_cat8.md:305` — 1 回目の「危険で導入停止」の理由は「ビルド・実行(導入)は行っていない。README・LICENSEからの文書情報のみで§4の他の列を埋めた」であり、以降の回もこの候補には「未着手」で来た。7 回目は初めて「ファイルを読んで検索するだけ(`git clone` は scratchpad の下で、実行しない)」として、README・LICENSE 限定だった調査範囲をリポジトリ全ファイルの取得・検索まで広げる。実行しない点は 1 回目の理由と矛盾しないが、1 回目が調査範囲そのものを README・LICENSE に絞っていた(公開直後・保守者不明という危険信号を理由に)のに対し、7 回目はリポジトリ全体を clone してファイル一覧を検索する — この範囲拡大が「危険で導入停止」の判断(取得すら避けるか、実行さえしなければ取得・検索は安全と見るか)とどう整合するかは、起動文にも 1 回目の記載にも説明が無い。この拡大はリードが確認済みの判断か。

**リードの処置**:
1: 認める(6 回目の文の持ち越し)。§1 の「回さないもの」から `akurkar07/OrderBook` の E6 を消した。8 回目以降の表の同じ行も「7 回目の §2 の 5 で当て直す」に直した。
2: 認める。`quotes_in_log` が、1 つの参照の中の複数の行(`<生ログ>:1356,1362`)と範囲(`<生ログ>:1186-1223`、上限 500 行)を全部拾うようにした。scratchpad で、`:3,6` の 2 つの引用がどちらも通り、生ログに無い引用は止まることを確かめた。6 回目の節への結果は 0 件のまま。
3: 認める。機械では見ない(ノートは自由な文なので語の対応を機械で決めにくい)。**検収の手順に入れる**: リードは、`なし` の根拠に引いた手の全部について、ノートの語と `$` の行の正規表現を並べて読み、要素の名前が日本語と英語でコマンドに入っているかを書く(7 回目の検収 §2 の表に列を足す)。
4: 1 回目の危険信号(公開直後・保守者不明)は、その道具を**動かす**ことの危険で、読むことの危険ではない、とリードは判断する。ただし `git clone` は取得の仕組みを通すので、起動文を「`git clone` もしない。ファイル一覧を `ungh.cc` で取り、生の本文を `curl` で 1 本ずつ取って読むだけ。実行権を付けず、開くのは `grep`・`sed` だけ」に改めた。
直した版は監査 35 回目で確かめてから印を記録する。

## 監査 35 回目(対象 = 09f1fb4、起動文の指紋 `149ad2b0b4a7`)

監査役の出力(逐語。[止める] 2 件・[聞く] 1 件。34 回目の指摘 1・3 の処置は妥当、差分の外の新しい問題は無し):

> 1. [止める] `scripts/cat8_ledger.py:283`(`quotes_in_log` の正規表現 `[\d,\-]+\d`)— 監査 34 回目の指摘 2 が実例として引いた `docs/DATA/SCAN_2026-09-23_tools_cat8.md:2219`(実測。検収の記録では `:2226` と書かれていたが実際の行は 2219)の実際の文言 `log:1067(N=107),1186-1223(検索と個別確認)` をそのまま regex に通すと、`(N=107)` の丸括弧で文字クラスが切れ、`1067` しか拾えず `1186-1223` は完全に落ちる(scratchpad で実測: `re.findall` の結果は `nums='1067'` のみ)。監査 34 回目が「直った」根拠として自分で引用した実例そのものが、実装した正規表現では直っていない。リードの処置欄が確認したのは単純形 `:3,6` のみで、注記付きの実例では再照合していない(P10: 目視・簡易ケースでの確認を「直った」の根拠にしている)。この形式(`N(N=…),lo-hi`)は 6 回目の報告に複数箇所で既に使われており、7 回目の「一覧 N 件 / 読んだ M 件」の書式とも自然に組み合わさるため、7 回目の `check-elements --round 7`(完了条件 §7 の 2)がこの形式の正当な引用を「生ログに無い」と誤って止める(過検出)可能性がある。
>
> 2. [止める] `scripts/cat8_ledger.py:390`(`印` の描画劣化検査)と `:416`(`なし` の一覧存在検査)— どちらも監査 34 回目で直していない旧正規表現 `(20260923_tools_8_run(\d+)\.log):(\d+)` を使い続けている。scratchpad で合成ログ・合成報告を作って実測: 単独参照 `log:10`(描画で `text-changed-after-wait` を含むブロック)を根拠にすると `印` の検査は正しく止まる。ところが同じ参照を先行する別行番号と 1 セルにまとめた `log:4,10` にすると、2 件目の `10` が一切拾われず検査が **無言で通る**(0 件)。`なし` の検査(:416〜437、描画ページを `なし` の根拠にしない規則)でも同様に、単独 `log:10` は正しく「描画したページを `なし` の根拠にしない」で止まるが、`log:4,10` にまとめると同じく無言で通る。7 回目の §2 の 5(8-004: ファイルを 1 本ずつ curl して読む)・2(8-016: 複数 README を生ログに印字し直して引く)は、まさに複数の生ログ行を 1 セルにまとめる引用を誘発する指示であり、この 2 箇所の未修正がそのまま §7 の 2(「0 件」を完了条件とする検査)の見落としに直結する。`tests/` に `cat8_ledger` の単体テストは無い(grep 0 件)ため、この種の回帰を機械的に検出する手段が今は無い。
>
> 3. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run7_prompt.md:32`(指摘 4 への処置)— 監査 34 回目の指摘 4 は severity `[聞く]`(「この拡大はリードが確認済みの判断か」)だったが、処置は「リードは判断する」と自己完結で決め、オーナーへの確認は記載が無い。`危険で導入停止`(1 回目、公開直後・保守者不明)と判定した候補の調査範囲を README/LICENSE 限定からリポジトリ全ファイルの取得・grep 検索に広げる判断であり、実行はしない設計(`git clone` 禁止・`ungh.cc` + `raw.githubusercontent.com` の `curl`・実行権を付けない)自体は整合しているが、「動かすことの危険であって読むことの危険ではない」という線引きの妥当性そのものは、監査役が聞くべき判断として出した問いであり、リード単独の解釈で確定してよいものかは本文に理由が書かれていない。

**リードの処置**:
1・2: 認める(34 回目の直しは 1 か所の、単純な形だけで確かめていた)。生ログの参照を読む関数 `log_refs` を 1 つにまとめ、**3 つの検査(引用・`印` の描画・`なし` の一覧と描画)の全部で同じ関数を使う**ようにした。読む形: `:10` / `:4,10` / `:1186-1223`(範囲は 500 行まで)/ `:1067(N=107),1186-1223(注記)`(括弧の注記を飛ばす)/ 同じ根拠の中の複数の参照。確かめたこと: 報告の実物 2219 行の文言から 39 行(1067 と 1186〜1223)を拾う / 監査役の合成の報告 5 本(`audit35/`)を打ち直し、`:4,10` の形でも `印` の描画(条件 (iii))と `なし` の描画の禁止が止まる / 6 回目の節は 0 件のまま。
3: 答える。1 回目の `危険で導入停止` の理由は、その道具を**動かす**ことの危険(公開直後・保守者不明のコードを実行する)。7 回目にするのは、ファイルの文字を `curl` で取り、実行権を付けずに `grep`・`sed` で読むことだけで、コードを実行する経路が無い。作業はこの一時的な容器の中で、取り返しのつかないこと(CLAUDE.md §0.2 A-14: 実弾・口座・資本・新市場・リスク上限)に当たらない。**リードの判断で進める**。取ったファイルに実行できる形のもの(スクリプト・バイナリ)があっても開くのは文字としてだけ、と起動文に書いてある。
直した版は監査 36 回目で確かめてから印を記録する(起動文は 35 回目から変えていない。変えたのは `scripts/cat8_ledger.py` だけ)。
