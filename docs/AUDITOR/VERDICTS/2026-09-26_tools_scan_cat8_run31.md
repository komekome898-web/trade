# 検収: 道具サーベイ 区分 8 の 31 回目(2026-09-26、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 31 回目の実行(2026-09-26)`(44025 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run31.log`(224,896 バイト)。受け取ったままの形はコミット 7181863。

## 0. 費用と時間

調査班 509,378 トークン・2,177,080 ms(36.3 分)・道具 238 回。

## 1. 受け入れ検査(リードが打ち直した)

- `check_scan_report.py`(31 本): `---- 合計 58 件`(調査班の貼付と一致)
- `check-elements --round 31`: **14 件**(全部「`/tmp` の直下(scratchpad の外)のファイルを使う手」。生ログ 8・38・63・75・100・202・504・526・552・556・566・587・1375・1923 行)。1375 行は scratchpad の道の中の `/tmp/pandas_titles.py` を使う手で、これも当たり。
- `check "" run31.log`: 0 件 / 過去の節から消えた行: 0
- `/root/.m2` 無し、`/root/.gradle` は 2026-03-31 のまま、`/root/.cache/pip` 728M のまま(リードの打ち直し)。

## 2. リードが見つけたこと(監査の前)

1. **生ログに無い `pip` の手が 1 つあり、それが第三者のコードを §6-1 の前に動かした。**証拠(リードの実測):
   - `/tmp/pip-download-aqz_ek8j`(27M、中に pandas の sdist を開いたもの)・`/tmp/pip-unpack-sse80wjj`(`pandas-3.0.6.tar.gz`)・`/tmp/pip-build-env-j24p8q2a`・`/tmp/pip-build-tracker-ie0q6o8k` が 17:13:52 に作られ、build-tracker の最終更新は 17:16:22。
   - scratchpad の `cat8/pipcache/wheels/` の下に、この窓の時刻で、ソースから作った wheel が 18 個ある(`hatchling-1.32.4`・`meson-1.12.1`・`meson_python-0.22.0`・`patchelf-0.19.1.0`・`setuptools-84.0.0`・`scikit_build_core-1.1.0`・`poetry_core-2.5.0`・`flit_core-4.1.0` など)。ソースから wheel を作るのは、その配布物の作りの手順(第三者のコード)を動かすこと。
   - `/usr/lib/python3/dist-packages/setuptools/`・`wheel/` の下に、17:14〜17:15 に新しく作られた `.pyc` が 111 個(作られた時刻 `stat -c %w` で確かめた)。
   - 生ログで 17:13:45 の `pip download --no-deps --no-binary=:none:` の出力は wheel 1 個だけで、ソースから作った跡は無い。17:16:17 の次の手は `curl` で sdist を取り直している。**この間の手は生ログに無い。**報告にも書かれていない。
   - この手は §6-1 の結論(生ログ 484 行、17:17:08)より前。
2. **導入で入った依存(`numpy 2.4.6`・`python-dateutil 2.9.0.post0`・`six 1.17.0`、生ログ 1649〜1668 行)の中身を開いた手が無い。**§6-1 の結論の行(484 行)に並ぶのは pandas だけ。起動文 §2.5「導入・実行で使う配布物の名前と版を全部並べ、その全部について中身を開いた手の行を引く」に反する。
3. `/tmp` の直下に 36 個のファイル・ディレクトリ(上の 4 つの `pip-*` を含む)。
4. **E2 の `印`**: 引いた逐語(`duplicated`「Return boolean Series denoting duplicate rows」、`merge(indicator=True)`)は汎用の表(DataFrame)についてのもので、述語の「時系列・約定・板・足・参照データについて」に届く逐語が無い(24 回目 Prefect・26 回目 Debezium・28 回目 Kafka と同じ当て方)。
5. **E3a の `なし`**: ファイルの表(報告の節の 424 行目から)44 ファイルのうち約 40 ファイルに同じ文「ソフトウェアのメモリ/リソース(ファイル記述子・スレッド)の「漏れ」(leak)の意…」を当てている。当たった行には、メモリ・資源の漏れではないものがある(`web/pandas/about/governance.md:181`「leak into their work with the Project.」、`pandas/tests/io/pytables/test_select.py:105`「extra rows leak through」、`pandas/tests/io/parser/common/test_read_errors.py:58`「leaked a cryptic error」、`pandas/tests/indexes/multi/test_indexing.py:1301`「leak the NaN row」など)。理由の文が当たった行を読んだ結果になっていない。起動文 §2.4「同じ文を何行・何ファイルにも当てた `なし` は受け取られない」。
6. **§4.0「宣伝詐欺の兆候」**: 理由が「X等で…専用に探す検索はこの回では実施していない」で、値は「見当たらない」、印は `推定`。起動文が理由として認めない文(「この回では実施していない」)。
7. **E4・E6 の案 B の記録**: 読んだ範囲は「doc/source 全 216 件の題 + pandas/io・core 直下のファイル名」。起動文 §1 の「関わりうる頁の本文を読む」の頁の道の一覧、§1 の「ディレクトリを要素の語の一覧で機械的に選ぶ(`find <src> -type d` の全部と `grep -i -E` の出力)」が無い(1593 行は `find pandas -maxdepth 1 -type d`)。案 B の条件(読んだ範囲と当たりの行の数が書いてある)を満たすか、リードは「満たさない」と読む。

## 3. 中身の監査(126 回目、owner-auditor)の逐語

> 監査 126 回目。対象: 区分8・31回目(8-034 Pandas)の納品と、リードの検収の下書き(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md`)。
>
> 以下、指摘のみを列挙する(最大25件、severityは[止める]/[直す]/[聞く])。
>
> 1. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md`(受け入れ検査の出力節、44025〜44690行目の範囲内)— 報告本文が「`cat8_ledger.py check-elements` が、この回の生ログに `/tmp` 直下(scratchpad の外)のファイルを使った手が14件あることを検出した…これは誤検出ではなく実際の違反…リードの判断を仰ぐ」と明記して判断を求めているのに、リードの検収下書き `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md` の「§0.1」(1〜7行目、リードが見つけたこと)にも「リードの読みの案」(39〜45行目)にも、この14件への処置が一つも書かれていない。§1(9〜14行目)で「14件(全部『/tmpの直下…』の手」と再確認だけして、判断(受け取る/直す/次回への申し送り)が抜けている。報告が名指しで求めた論点が監査に掛ける下書きから欠落している(O-5「『止める』を単独で退けない」と同じ構造で、ここでは論点そのものが処理待ちのまま次に進もうとしている)。
>
> 2. [直す] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md:41` — 「この回の実測の値は受け取らない」の根拠として引く「2 の 1・2」は、起動文の明文規則 `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:63`(「実行のあとに§6-1の手が1つでもあれば、この回の実測の値はリードが受け取らない」。28回目の処置1で導入された規則、`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run28.md`)と、文言上のトリガーが一致しない。「2 の 1」(§6-1結論=17:17:08より**前**の未開示のpip build、生ログ1013〜1016行相当のギャップ)は「実行のあとの§6-1」ではなく「§6-1結論の前の未開示コマンド実行」であり、「2の2」(依存numpy/dateutil/sixの§6-1が一度も行われていない、生ログ484〜486行に依存の中身を開いた手の引用が無い)も「後回し」ではなく「欠落」である。結論(受け取らない)そのものは28回目の規則の趣旨(§6-1が実行に対して完全に先行していない場合は文書の値だけにする)に沿うと読めるが、適用する条文とトリガーの対応が明記されていない。次回以降に同型の判断を再現する際の手がかりとして、この回のうちに書いておくべきではないか(P2)。
>
> 3. [直す] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md:43` / `docs/DATA/SCAN_2026-09-23_tools_cat8.md:44521` — 「§4.0の`実測`のうち導入・実行によるもの…は『§6-1を済ませる前の導入による値』とする」の列挙(版の実測・導入可否・install所要秒・pip check・最小実行・実行所要秒・当方データ投入・時刻の扱い・規模の見積・外部送信・登録の要否)に「隠れた依存」(44521行、「実際にpip installで入ったのはnumpy 2.4.6・python-dateutil 2.9.0.post0・six 1.17.0」)が含まれていない。この値は§6-1が一度も行われなかった依存(指摘2の「2の2」)の実行時バージョンそのもので、同じ性質の値に見えるが、対象から外れている理由が書かれていない。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run31_prompt.md:15` — 起動文§0項目5は21・22・24・26・28・29・30回目の既知の誤りを名指しするが、26回目のE2・E4(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run26.md`「8-031の扱い」節「E2・E4は案Bの記録(読んだ範囲・当たりの行の数)が無い」)・28回目のE1b・E2・E4・E6(`..._run28.md`「8-032の扱い」節「E2・E4は案Bの記録が無い」)で共通して起きた「案Bの記録(読んだ範囲の本文・ファイル内容まで読み切る前に割り当てが終わる)」という型は、この一覧に無い。31回目のPandas E4・E6(`docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md:33`「案Bの条件…満たさない」)で3回連続の再発になった。E2/E4述語誤当てはめやhead -25打ち切りは名指しで再発防止されているのに、この型だけ起動文の「同じことをしない」対象に一度も挙がっていない。案Bの読む範囲の決め方・1回に割り当てる候補の大きさ自体の見直しは検討されているか(P8)。
>
> 5. [聞く] `docs/AUDITOR/VERDICTS/2026-09-26_tools_scan_cat8_run31.md:41`の段の決定(E1a段4・E1b段2・E3b段4・E5段2)は、証拠区分を`実測`から`一次資料`(docstringの逐語のみ)へ格下げした後も、実行して確かめた挙動(PASS/FAIL/tolerance吸収、mergeの未来値排除など)を根拠の一部に含んだままの判断に見える(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:44383`等の既存記述と同じ段)。証拠区分を実測から一次資料に落とすなら、段の判定(特に段4の「対象のすべてを外から持ち込める」「自動で行う」の充足)も一次資料(docstring)だけで独立に成立するかを一言書いておくと、格下げの一貫性が検査しやすいのではないか(P2寄り、結論への異論ではない)。
>
> ---
>
> 確認して問題が見当たらなかった点(指摘としては数えない):
> - §2の指摘1〜7(pip-download由来の未開示ビルド、依存の§6-1欠落、E2/E3aの述語不一致、宣伝詐欺の兆候の理由不備、案Bの記録不足)は、生ログ(`docs/DATA/probes/20260923_tools_8_run31.log`)と報告本文の該当行を実測で突き合わせ、いずれも記述どおりであることを確認した(E3a「なし」の複写文が実際に governance.md:181・test_indexing.py:1301・test_read_errors.py:58・test_select.py:105 等では「メモリ/リソースの漏れ」ではない意味の"leak"に誤当てされていることも実測で確認)。
> - E3a「なし」→`未判別`、E2→`未判別`への変更方針(24回目Prefect・26回目Debezium・28回目Kafkaと同型の当て方)、E1b段2の「29回目Apache Kafkaの窓集計と同じ当て方」の引用は、該当する過去の検収記録と実際に一致していた。
> - 数値(N=2,657/2,234、E3a 44ファイル/66行、E4 167ファイル/950行、E6 1,603ファイル/101,494行、ダウンロード14,584,635バイト・チェックアウト49,811,352バイト等)は生ログの該当行と一致し、出所不明の数字は見当たらなかった。
> - 「この回の実測の値は受け取らない」という判断自体は、28回目の処置1で導入された規則の趣旨(§6-1が実行に完全に先行していない回は文書の値だけにする)に沿っており、A-18(安全側・棄却側へだけ振れる)の型には当たらない。E1a・E1b・E3b・E5の印(値そのもの)は維持し、E2・E3a・E4・E6だけを未判別に戻す扱いは、過去の回(24・26・28回目)の当て方と整合しており、一律に厳しく振れているわけではないと判断した。

## 4. 処置(リード、監査 126 回目)

1. **直した**。`/tmp` 直下の 14 件の手の判断: 使ったファイルは `curl` で取った公式の頁・PyPI の JSON などの写しで、取った手と中身の要点は生ログに出ている。置き場所が違っても中身は変わらないので、それを根拠にした**文書の値(一次資料)は受け取る**。規則違反(起動文 §2.6)としては記録する。ファイルは `scratchpad/cat8/run31_tmp/` に移した。**32 回目からは、最初の手で `export TMPDIR=<scratchpad の下>` を打たせる**(`pip` の一時ディレクトリ `/tmp/pip-*` もこれで scratchpad の下に入る)。
2. **直した**。当てた条文と引き金の対応(リードの読み): 起動文 §2.5 の文言は「実行のあとに §6-1 の手が 1 つでもあれば、この回の実測の値はリードが受け取らない」。その趣旨は、28 回目の処置 1 のとおり「§6-1 が実行に完全に先行していない回の実測は受け取らない」。この回は (a) §6-1 の結論(484 行、17:17:08)の前に、生ログに無い手が第三者のコード(ソースからの wheel 作り 18 個)を動かした = §6-1 が済む前の実行、(b) 導入・実行で使った依存 3 つ(numpy・python-dateutil・six)の §6-1 が一度も無いまま実行した = §6-1 が欠けたままの実行。どちらも「§6-1 が実行に完全に先行していない」に当たる。**次の起動文では文言を「§6-1 が、導入・実行で使う配布物の全部について、最初の導入・実行の前に済んでいなければ(後回し・欠落・生ログに無い手のどれでも)、この回の実測の値はリードが受け取らない」に直す。**
3. **直した**。「隠れた依存」「依存数」「依存の一覧」のうち、実際に入った依存(numpy 2.4.6・python-dateutil 2.9.0.post0・six 1.17.0)の部分も「§6-1 を済ませる前の導入による値」に入れる。METADATA の `Requires-Dist`(wheel の中身を開いた手、生ログ 423〜483 行)の部分は一次資料として受け取る。
4. **答える・直す**。検討していなかった。案 B の記録が足りない型は 26・28・31 回目の 3 回。原因の読み(リード): 起動文の「関わりうる頁の本文を読む」の「関わりうる」を調査班が決めるので、題を読んだところで止まる。30 回目にソースのディレクトリを語の一覧で機械的に選ばせたのと同じく、**32 回目からは文書の頁も語の一覧で機械的に選ぶ**(頁の道と題に要素の語が入る頁は全部本文を読む。選んだ一覧を出力ごと生ログに出す)。起動文 §0 の 5 の「同じことをしない」一覧にも、この型を名指しで入れる。1 回に割り当てる大きさは、32 回目を Pandas の残りだけにする。
5. **答える**。docstring の逐語だけで段が立つかを見た(リードが生ログを読んだ):
   - **E1a 段 4**: 段 3 の「自動で判定」は「Check that left and right DataFrame are equal.」(生ログ 1773 行)。段 4 の「対象のすべてを外から持ち込める」は、対象(突き合わせる 2 つの出力)が引数「First DataFrame to compare.」「Second DataFrame to compare.」(1783・1785 行)で、道具が作ったものに限らない一般の表。段 5 は (ア) の `rtol`/`atol` はあるが (イ)(結果を保存して次と比べる)の逐語が無いので段 4。**docstring だけで段 4 が立つ。**
   - **E3b 段 4**: 「direction: str = "backward",」(1867 行)と「A "backward" search selects the last row in the right DataFrame whose」(1879 行)で、既定で時点の揃った結合を自動で行う。結合を掛けるデータは引数「left: DataFrame | Series,」「right: DataFrame | Series,」(1854・1855 行)で一般の表。**docstring だけで段 4 が立つ。**
   - **E1b 段 2・E5 段 2**: どちらも「呼ぶと結果を出すが合否を自分で判定しない」で、docstring(1812〜1824 行・1895〜1901 行)だけで立つ。
   この読みは、区分の全体の検収で段の一貫性を見る対象に入れる。

### 8-034 の扱い(台帳に入れる値)

- E1a 印・段 4・一次資料 / E1b 印・段 2・一次資料 / E3b 印・段 4・一次資料 / E5 印・段 2・一次資料
- E2 未判別(対象の逐語が無い)/ E3a 未判別(ファイルの表の理由が当たった行を読んだ結果になっていない)/ E4・E6 未判別(案 B の記録が足りない)
- 状態 `浅い`。**「残り」に数える**(案 B の条件を満たさない)。
- 32 回目 = 8-034 の残りだけ。

### リードの読みの案(監査に掛ける)

- **この回の実測の値は受け取らない**(2 の 1・2。§6-1 の前に第三者のコードが動き、導入した依存の §6-1 も無い)。`実測` の印の要素は、同じ生ログの中の一次資料の逐語(ソースの docstring を `sed -n` で印字した手)が述語に届くものだけ `一次資料` として受け取る: E1a 印・段 4(`assert_frame_equal`、生ログ 1770〜1811 行)/ E1b 印・段 2(`groupby`・`corr`、1812〜1824 行。29 回目の Kafka の窓集計と同じ当て方)/ E3b 印・段 4(`merge_asof` の backward、1851〜1894 行。述語の例「時点の揃った結合」)/ E5 印・段 2(`random_state`、1895〜1901 行)。
- E2 → `未判別`(2 の 4)。E3a → `未判別`(2 の 5)。E4・E6 → `未判別` のまま、案 B の記録は足りないので 8-034 は「残り」に数える(2 の 7)。
- §4.0 の `実測` のうち導入・実行によるもの(版の実測・導入可否・install 所要秒・pip check・最小実行・実行所要秒・当方データ投入・時刻の扱い・規模の見積・外部送信・登録の要否)は「§6-1 を済ませる前の導入による値」とする。宣伝詐欺の兆候は `未確認`(2 の 6)。
- 片付け(リードが行った): `/tmp` 直下の 36 個を `scratchpad/cat8/run31_tmp/` へ移した。`/usr/lib/python3/dist-packages` の下の新しい `.pyc` 111 個を消した(一覧は `scratchpad/cat8/run31_pyc.txt`。消す前に 111 個とも作られた時刻が 17:14〜17:15 で `.pyc` だけであることを確かめた)。ソースから作った wheel 18 個は scratchpad の `pipcache` に残した(証拠)。
- 32 回目 = 8-034 の残り: §6-1 をやり直す(依存も含め、`--only-binary :all:` で取り、ソースから作らない)、最小実行をやり直す、E2 の対象の逐語、E3a のファイルの表のやり直し、E4・E6 の案 B の記録、宣伝詐欺の兆候の投稿の本文。
