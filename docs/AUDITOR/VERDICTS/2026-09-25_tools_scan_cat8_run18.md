# 検収: 道具サーベイ 区分 8 の 18 回目(2026-09-25、リード)

対象: `docs/DATA/SCAN_2026-09-23_tools_cat8.md` の `## 区分8 — 18 回目の実行(2026-09-25)`(35575 行目から)/ 生ログ `docs/DATA/probes/20260923_tools_8_run18.log`(11,608 行・3,033,270 バイト)。届いたままのコミット fc43462。起動文の印 `20260923_tools_survey_cat8_run18_prompt.md@4bc5c0d40b3f`(この回から、ほかの回の起動文を参照しない形)。

## 0. 費用と時間

調査班 678,762 トークン・3,260,749 ms(54.3 分)・道具 283 回。

## 1. 受け入れ検査(調査班の報告)

`check-elements --round 18` 0 件 / `check` 0 件 / `check_scan_report.py` 38 件(K1・K2・K13。前の回からのものと、§4.0 の表の書き直しに伴う同型の複写)。

## 2. リードが見た点と直し

- **理由の表の書き方**: 型の文の当てはめは、行の表とファイルの表では見られなかった(`なし` の 4 要素は全部 (乙) で、行ごとの判定は E1a 5 行・E2 2 行・E3a/E3b 0 行)。「同じ理由を 2 行以上に書きたくなったら決まりにする」は守られた。
- **ただし決まりが語ごとの決まりだった**: `### 決まりの一覧` の多くは `(?i).valid|valid.`・`(?i).range|range.`・`(?i).missing|missing.`・`(?i).test|test.` のように「検索した語の前か後ろに 1 字ある」だけの形で、`(?i)compar\w+`・`(?i)differ\w*` も語そのものを取る。行の前後の字をほとんど見ていない。道具の説明に「止められない」と書いた型(検索した語に、ありふれた前後の字を付けた決まり)に当たる。理由の文には「全707件の当たりを読み、うち25件を無作為抽出して個別に文脈を確認」とあり、1 行ずつ読んではいない。13・14 回目の「語ごとの決まりを先に立てて当てはめる」と同じ型。
- **リードが確かめた範囲**: AlgoNetwork の当たりのうち、ブログの記事(`blog_*.txt`)の行は、概念の一般の説明で、候補の機能の記述ではない(記事が「AlgoNetwork が〜する」と書いていれば別)。候補の機能の記述が出うるのは製品の頁(`faqs.txt`・`_root.txt`・`algonetwork.txt`・`trade-with-algorithms.txt`・`propsubstitute.txt`・`publish-pros.txt`・`white-label.txt`・`ai-landing.txt`・`about-us.txt`・`terms-of-use.txt`・`backtest.txt`・`trading-bot-marketplace.txt`、別の製品の `algobuild.txt`)なので、そこの当たりをリードが全部読んだ(E1a の手 33 行目 20 行、E2 の手 10409 行目 24 行、E3a・E3b の手 2054・2297 行目は 0 行 = 全部ブログ)。
  - **E1a は `なし` が誤り(`なし` 側に振れていた)**: `faqs.txt:73`「you can compare any two strategies like-for-like」、`trade-with-algorithms.txt:113`「Compare win rates, profit factors, and risk metrics openly.」、`_root.txt:97`「Buyers compare like with like」は、2 つ以上の戦略のバックテスト・フォワードテストの出力を並べて差を見せる機能。設計票 §3 の E1a の述語「**2 つ以上の実装(または実装と参照値)の出力を突き合わせて、差を出す機能**」に当たる。決まり `r_compare_prose`(`(?i)compar\w+`)は、これらの行を「比較する編集記事の一般叙述」として取っていた。**`印`・段 2**(並べて見せ、合否は人が決める)にした。
  - E2: 製品の頁の当たりは「publishing criteria (Sortino ratio,」(公開の基準)、「validates it with a complete backtest」(戦略の検証)、「Sort: Win rate ↓」(並べ替え)、「Quant Researcher · Singapore」(「gap」の部分一致)、「legally valid cases」など。データの欠け・重複・順序・範囲の違反を検出する機能ではない。**`なし` を受け取る。**
  - E3a・E3b: 当たりは全部ブログの記事(ルックアヘッド・生存者の偏りの解説)。**`なし` を受け取る。**
  - E6 `印`・段 1: 根拠「Every backtest and live trade is auditable. No cherry-picking.」は、結果を取捨選択せずに全部見せるという主張で、正しさを確かめる機能の文書の記述(段 1)。**受け取る**(監査で、E1a〜E5 と重ならないかを見る)。
- **Oryon**:
  - E6 `印`・段 3: README「Every feature and target in Oryon ships with contract tests...The test infrastructure is part of the public API. Contributions must pass the same contracts.」と、`#[macro_export]` の契約テストのマクロ。公開の API として、利用者が自分で書く実装(Oryon のトレイトを満たす型)に掛けられるので、L-516 の「候補が自分自身を試験する仕組み」には当たらない(報告の問い 1 への答え)。**受け取る。**
  - E1b の段: 調査班は段 4 とした(根拠 `run_features_pipeline_pandas(fp, df)` が pandas の DataFrame を受け付ける)。設計票 §4.1 の E1b の対象は「同じ種類の出力を出す計算に入れる入力(**データと戦略・計算のコード**)」で、外から持ち込めるのはデータだけ(計算は Oryon 自身の特徴量)。「対象の一部だけが外から持ち込めるものは段 3」により **段 3** にした。
- **状態**: 2 候補とも E1a〜E6 に `未判別` が無く、§4.0 の表の `未確認` の理由は、登録が要る・一次資料に数が無い・実装の作業はサーベイの外、など(「時間」ではない)。**`深掘り` を受け取る**(報告の問い 2・3 は監査で見る)。

## 3. 台帳

`import --round 18`: 変わった所 9。`set` で 8-037 E1a を `印`・段 2、8-041 段_E1b を 3。`check` 0 件。`recount`: 深掘り 26 / 残り 11(8-026・8-029・8-030〜8-035・8-038・8-039・8-040)。

## 4. 判定

**一部受け取り**(E1a を直したうえで、2 候補を `深掘り`)。中身の監査 95 回目を通す。

**語ごとの決まりについて**: 道具は足さない(L-102)。次の起動文に「決まりの正規表現は、検索した語の前後の字(その行の文脈)を入れる。検索した語に 1 字足しただけの決まり(`.X|X.`)や、語そのものを取る決まり(`X\w*`)は書かない。その語が出る行がすべて同じ意味なら、決まりにせず (甲) で、ファイルごとに読んで書く」と書く。守られたかは、リードが検収で決まりの一覧を読んで確かめる(この回と同じ手)。

## 5. 中身の監査(95 回目、owner-auditor)の逐語

> 監査 95 回目。指摘は以下のとおり。
>
> 1. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:35954`(AlgoNetwork E1a「印」・段2)、根拠は同 35598 行(知見#1)・検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:18` — E1a の述語は「**2 つ以上の実装(または実装と参照値)の出力を突き合わせて、差を出す機能**」で、設計票 `docs/DATA/surveys/CAT8_DESIGN.md:49` の括弧内の例示(差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較)はいずれも「同じ計算を2通り行い正しさを確かめる」文脈である。今回の根拠(`faqs.txt:73`「compare any two strategies like-for-like」、`trade-with-algorithms.txt:113`「Compare win rates, profit factors, and risk metrics」、`_root.txt:97`「Buyers compare like with like」、原文は生ログで確認済み)は、**性能の異なる 2 つの戦略の成績指標を横並びにして投資判断に使う機能**であり、「正しさの検証のための突き合わせ」ではなく「比較ショッピングのための表示」である。この読みの違いが報告にも検収にも問いとして出ていない。E1a が本来 B(再現ゲート/正しさの照合)の在処を機械で引くための列(設計票 §3 の「E1a・E3a の側にある」の説明)であることも踏まえると、この判定が「区分8の要素」の趣旨(検証・品質)を捉えているか、それとも述語の字面だけを機械的に満たしただけかを確認する必要がある。
>
> 2. [直す] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:35961`(AlgoNetwork E6「印」・段1) — 根拠に `faqs.txt:54`「independent verification through backtests and forward tests」を含め「同じ保証を指す」と処理しているが、**この同じフレーズは 17 回目の検収(監査 92 回目、`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md:46`)で「E1a の述語(突き合わせ)と概念的に近い」と名指しで指摘され、「E1a を扱う回で一緒に決める」として未判別に据え置かれた対象そのもの**。18 回目は E1a を別の根拠(戦略比較)で確定させたが、E6 に残した「independent verification」がなぜ E1a(突き合わせ機能)に当たらず E6 に留まるのかの検討が書かれていない。「auditable, no cherry-picking」(完全性の保証)と「independent verification」(検証行為そのもの)は語感が異なり、単純に「同じ保証」と片付けてよいかは再検討の余地がある。
>
> 3. [聞く] `docs/DATA/SCAN_2026-09-23_tools_cat8.md:36040`(AlgoNetwork 4軸4_向上)・`36088`(Oryon 4軸4_向上)、検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:25` — 両候補とも「実装作業が道具サーベイの範囲を超える」を理由に `未確認` を残しているが、これは起動文 `docs/DATA/delegations/20260923_tools_survey_cat8_run18_prompt.md:58` が許す 2 類型(試した手段と出力を生ログで示したもの/鍵・口座・登録・購入が要ると分かる逐語を引いたもの)のどちらにも文字どおり当たらない。**Oryon は登録・口座接続が不要な候補**(pip install のみ、同ファイル `36061` 行「登録の要否: 不要」)なので、この理由は「実装統合という別種の作業だから測らない」という新しい第三の理由類型であり、§2.5 が禁じた「時間が掛かる」の言い換えになっていないか自明ではない。報告自身が「判断に迷った点と問い」#3(`36376`)でこの点を明示的に問いに残しているにもかかわらず、検収 §2 は「登録が要る・一次資料に数が無い・実装の作業はサーベイの外、など(『時間』ではない)」と一括で「深掘りを受け取る」とし、この理由類型そのものの妥当性を独立に検討せず「報告の問い2・3は監査で見る」と丸投げしている。
>
> 4. [直す] `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:29,33`(台帳 `set`・`深掘り`判定) — 上記 1〜3 の未解決の問い(E1a の述語適合、E6/E1a の境界、4軸4 理由の妥当性、さらに報告自身の「判断に迷った点と問い」#1 の Oryon E6 の自己試験境界)が残ったまま、`import --round 18` → `set` → `recount` で 2 候補とも `深掘り`(状態板の完了に近い区分)へ台帳を進めている。設計票 `docs/DATA/surveys/CAT8_DESIGN.md:29`「`深掘り` = ... E1a〜E6 に `未判別` が1つも無い」は値の有無だけを条件にしており、値を導いた判定根拠に開いた問いが残っていても状態を進められる作りになっている。この単位で「深掘り」に進めてよいか(値は確定していても、根拠の当てはめに未決の問いがある間は `浅い` に留めるべきではないか)を確認する必要がある。
>
> 5. [聞く] 検収 `docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:35`(「語ごとの決まり」への手当て) — 18 回目自身の検収 §2(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run18.md:16`)が、`cat8_classify.py` の決まりの多くが「検索した語の前後に1字ある」だけの語ごとの決まりで、実際には行を読んでいなかったことを認めている。これは 12・13・14・16・17 回目と続いた「型の文の当てはめ」の変種であり、17 回目の検収(`docs/AUDITOR/VERDICTS/2026-09-25_tools_scan_cat8_run17.md:36`)が「量を減らす」で手当てした直後の回に別の型で再発したことになる。今回の手当ては「道具は足さず、次の起動文に書くだけ」(オーナー逐語 L-102 に沿った選択ではあるが)で、守られたかは次回のリードの目視に委ねている。この回は AlgoNetwork の製品頁の当たりをリード自身が全部読んで補ったため実害は抑えられているが(抜き取りで確認済み)、書くだけの再発防止が CLAUDE.md §0.2 A-17(「対策を記録するだけ・規則を増やすだけで終わらせない」)に当たらないか、確認が要る。
>
> ---
>
> **確認して問題が無かった点**(参考までに記載。指摘ではない):
> - Oryon の E1b 段を 4→3 にした判断(`docs/DATA/SCAN_2026-09-23_tools_cat8.md:35987`)は、設計票 §4.1 の E1b の対象「データと戦略・計算のコード」のうち、計算(Oryon 自身の特徴量計算)は道具の外から持ち込めず、データ(pandas DataFrame)だけが持ち込めることを理由にしており、「対象の一部だけが外から持ち込めるものは段3」の規則に忠実に見える。
> - AlgoNetwork のブログ記事(50 ファイル)に、候補自身の機能を一人称で述べる文(「our platform」「Algorier detects/verifies/compares」等)が紛れていないかを `grep -li` で抜き取り確認したが、該当は 0 件だった。リードの「ブログは概念の一般解説で機能記述ではない」という読みを覆す証跡は見つからなかった。
> - E6 の根拠 `algonetwork.txt:285`・`faqs.txt:54`・`Oryon README.md:127` の逐語は、いずれも生ログの当該行と一致しており、切り貼りや文脈の欠落は見当たらなかった。
> - 4 つの受け入れ検査(`check_scan_report.py`・`check-elements`・`check`・`git diff` の削除行数)は、いずれも 0 件または説明済みの誤検出のみで、機械的な体裁は整っている。
>
> 以上、最大 25 件のうち 5 件。

## 6. 処置(リード、監査 95 回目)

1. **直した(オーナーに聞く)**。戦略どうしの成績を並べて比べる機能を E1a に数えるかは、述語の字面(2 つ以上の実装の出力を突き合わせて差を出す)と、設計票が E1a を置いた目的(§3「E1a・E3a の側にある」= 正しさの照合の在処を引く)の間の境目で、ほかの候補(戦略の売り場を持つ道具)にも同じ型が出る。L-516 と同じくオーナーの読みの問いなので、この回の返答でオーナーに聞く。答えが出るまで、AlgoNetwork の E1a を `未判別` に戻した。
2. **直した**。E6 の根拠から「independent verification through backtests and forward tests」を外し、「Every backtest and live trade is auditable. No cherry-picking.」だけを残す(`印`・段 1 のまま)。外した語は E1a と重なりうるので、1 の答えと一緒に扱う。
3. **答える(受け取る)**。§4.0 の「4軸4_向上」は、道具を当方の環境に組み込んで既存の成果が上がるかを測る項目で、読むだけのサーベイの中ではそもそも測れない(時間の問題ではなく、項目の性質による)。受け入れるが、この理由は「サーベイの外(組み込みの作業が要る)」として、ほかの `未確認` の理由と分けて記録する。
4. **直した**。開いた読みの問いがある候補は `深掘り` にしない。AlgoNetwork は 1 の問いがあるので `浅い` に戻した。Oryon の問い 1(E6 の自己試験の境目)は検収 §2 で答えた(README が契約テストを公開の API と書き、利用者が自分の実装に掛けられる)ので、`深掘り` のまま。
5. **答える**。手当ては記録だけではない。毎回の検収で、リードが決まりの一覧を読み、語ごとの決まりに頼った `なし` は、候補の機能が書かれうる頁・ファイル(製品の頁・文書・ソース)の当たりをリードが読んで確かめる。この回はその手で E1a の誤りを見つけた。起動文の一文は、調査班がこの形を避けるための注意で、守られたかどうかを決めるのはリードの読みである。

台帳(`set`): 8-037 の E1a を `未判別`、状態を `浅い`。`check` 0 件。深掘り 25 / 残り 12。

## 7. リードの判断の改め(処置 1・4 を改める)

処置 1 で「オーナーに聞く」としたが、オーナーの逐語「**なんで私の手間ばかり増やす行動ばかりするんですか？**」(`docs/DISCUSSIONS/2026-09-13_worst_day.md` 発言 20)に照らし、設計票の逐語で決められないかを見直した。E1a の述語の文は、括弧の中に例を持つ:「**2 つ以上の実装(または実装と参照値)の出力を突き合わせて、差を出す機能**を持つ(差分の検査・参照実装との一致試験・二重実装の照合・別の道具の結果の取り込みと比較)」(設計票 §3)。例はどれも、同じものを出すはずの 2 つ(実装と参照実装・二重の実装・別の道具の結果)を照らし合わせるもので、設計票 §3 も E1a を「別実装の再現ゲート」の在処を引く列として置いている。戦略どうしの成績の比較は、違う結果を出す 2 つの戦略を並べるもので、どの例にも当たらない。**述語の文と例から決まるので、規則を足さずにリードが決める**(オーナーには聞かない)。

- AlgoNetwork の E1a は **`なし`**。全件の検索(33 行目の手)の当たりのうち、製品の頁の 20 行はリードが読んだ(§2)。戦略の比較の行は上のとおり述語に当たらず、ほかは「different」「difference」などの一般の語。ブログの行は概念の説明(監査 95 回目も、ブログに候補の機能を一人称で述べる文が 0 件であることを確かめた)。
- 開いた問いが無くなったので、状態は **`深掘り`** に戻す(処置 4 を改める)。
- 検収 §2 で「E1a は `なし` が誤り」と書いたのは、リードの読みの誤り(述語の括弧の例を読まずに、字面だけで当てた)。

台帳(`set`): 8-037 の E1a を `なし`・段 `-`、状態を `深掘り`。`check` 0 件。深掘り 26 / 残り 11。
