# 区分 1 の 4 回目の報告 — リードの検収

対象: `docs/DATA/SCAN_2026-09-21_tools.md` の「## 区分1 — 4 回目の実行(2026-09-22)」(1470 行目以降、581 行の追記)
生ログ: `docs/DATA/probes/20260922_tools_1_run4.log`(新規 102 行、43 手)
委任文: `20260922_tools_survey_prompt.md@ce0012c95154`

## 0. 既存の節が無改変か(調査班が「一度消して作り直した」と自己申告したため、リードが確かめた)

`git show HEAD:...` の 1468 行と、現在のファイルの先頭 1468 行を `diff -q` で突き合わせ、**一致**。
1〜3 回目の節は 1 文字も変わっていない。自己申告のとおりだった。

## 1. 調査班が閉じずに残した 2 件

**どちらも当たっている。1 件はリードの起動指定の誤り、1 件は検査の欠陥。**

1. **K11 の 106 件は、渡した生ログが run4 の 1 本だけだったことによる。**3 回目の節の根拠が指す run3.log が
   渡っていないので、行が実在しないと判定されていた。**リードの起動指定の誤り。**
   生ログを 2 本(run3 と run4)渡して打ち直すと **K11 は 0 件**になることを確認した。
2. **K5 の 1 件は検査の欠陥。**`Backtrader` の「install所要秒 2 秒」と「規模の見積の中の 1.3 秒」を、
   同じ単位「秒」で束ねて食い違い扱いにしていた。項目が違うのだから値が違って当然である。
   **§4.0 の表の行は、単位ではなく項目で束ねるように直した。**
   直したあと、本物の食い違い(同じ道具の同じ項目に 130 と 185)はまだ当たることを欠陥入力で確認した
   (検査が無力化していないことの確認。落ちない検査を作った前例が 2 回あるため毎回やる)。

**2 回続けて、調査班が閉じずに残した行から検査の欠陥が出た。**§12 の規則が働いている。

## 2. リードが打ち直した受け入れ検査

生ログ 2 本を渡して、**13 本すべて 0 件**。報告の貼り付けもこの出力に差し替えた。

## 3. 一次資料の抜き取り(4 回目の節の 56 行から無作為 5 行)

| 行 | 主張 | 取り直した結果 |
|---|---|---|
| Backtrader / 最終更新日 | PyPI の最新版の upload_time は 2023-04-19T14:13:18 | **一致** |
| Backtrader / 保守者数 | `info.author = "Daniel Rodriguez"`、`info.maintainer` は空文字 | **一致**(3 回目の誤りの型が直っている) |
| Backtrader / 配布元の一致 | Homepage と Download がどちらも github.com/mementum/backtrader | **一致** |
| bt / 星 | 2989(fork 501) | **一致**(GitHub の検索の端点で確認) |
| Basana / 週DL数 | last_week=47、last_month=406 | **再現できず**。pypistats が 429。生ログ 93 行に code=200 の記録があり、取得自体はされている。誤りとは書かない |

**3 回目に見つけた 2 つの誤りの型(PyPI の maintainer と author の取り違え / 根拠に資料に無い語を書く)は、
4 回目では 1 件も出なかった。**フィールド名を根拠に明記する書き方に変わっている。

## 4. リードが自分で確かめた追加の 1 件

調査班が「`stefan-jansen/zipline-reloaded` だけ GitHub の検索で返らなかった」と書いていたので、リードが打ち直した。

- `repo:stefan-jansen/zipline-reloaded` 単独でも、`in:name user:stefan-jansen` でも **total_count = 0**。
- 一方 `raw.githubusercontent.com` の README は **main も master も HTTP 200**。リポジトリは実在する。
- **この環境の GitHub 検索の端点だけがこのリポジトリを返さない。原因は未確認。**
  同じ問い合わせで `pmorissette/bt` は返るので、権限や scope の一律の制限ではない。

## 5. リードの判定

- **受け取る。**深掘りが 4 件(Basana / Backtrader / PyBroker / bt)完了。A の残り 8 件と B の 6 件は未着手。
- **区分 1 は未完了。**残りの候補 13 件 + 取り直しの残り 4 件。検索計画 6 本は今回も未実行(委任文 §2 のとおり)。
- 中身の誤りは **0 件**(3 回目は 5 件)。再現できなかったのが 1 件。
- 調査班が「原文に無い判断が要った」として 2 件を報告してきた。**どちらも妥当**:
  PySystemtrade を飛ばした判断(PyPI に無く、git clone が §6-6 の一括ダウンロードに当たる)と、
  文書を一度壊して作り直した経緯の自己申告。**黙って決めずに書いてきたことを評価する。**

## 6. 次回に渡すもの

PySystemtrade(第 2 経路のコマンドは節に記載)/ Ziplime / Superalgos / OpenTrader / CryptoSignal / fast-trade /
OctoBot / pybotters / DeviaVir/zenbot / Bot18 / Mendl-Labs/BacktestingCore / Luczinsritter / mlflow。
取り直し: 5 道具のコミット数(この端点は星と fork しか返さない)/ zipline-reloaded の星 / lib-pybroker の週DL数 /
Jesse の実弾プラグインの金額(jesse.trade/pricing が 403)。
