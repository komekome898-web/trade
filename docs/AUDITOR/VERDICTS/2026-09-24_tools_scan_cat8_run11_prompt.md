# 監査記録: 道具サーベイ 区分 8 の 11 回目の起動文

対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md`(初版の指紋 `500d1f954993`)。10 回目の起動文(監査 48〜62 回目を通した版)を元に、範囲を 8-016 の E3a・E3b・E5 のやり直しと、辿る一覧から足した 12 件(8-017〜8-028)の深掘りにした。監査 63 回目の [止める](切った出力での `なし`)と [直す](狭い検索の語)は、起動文の「検索の手の決まり」と `check-elements` の 11 回目以降の検査(コミット 94c62ae)で塞いだ。オーナーの段の読み L-511・L-512 を入れた。

## この回に回さないもの

| 何 | 理由 |
|---|---|
| 8-029〜8-040(12 件) | 12 回目。1 回の範囲を 24 件にしない(L-416「一気に終わらせようとせずに」) |
| freqtrade の §4.0 の表の合わせ直し・8-015 の N の出所の書き直し | 資源化のとき |

## 監査 64 回目(対象 = e96581d、起動文の指紋 `500d1f954993`)

監査役の出力(逐語。[止める] 2 件・[直す] 1 件・[聞く] 2 件):

> 監査 64 回目(道具サーベイ 区分 8・11 回目の起動文)の指摘。対象: `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md`(指紋 500d1f954993)、参照した機械: `scripts/cat8_ledger.py`(コミット 94c62ae 以降)、`scripts/check_scan_report.py`。検査は scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit64/`、検査後 `rm -rf` 済み)で合成の報告・生ログを作り、実際に道具を打って確認した(検査後に削除済み。リポジトリ本体には一切書き込んでいない)。
>
> 1. [止める] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:77`(§7 の 1、`check_scan_report.py` の呼び出し) — 生ログの引数の並びが `run1〜run9.log` + `run11.log` で、**`run10.log` が抜けている**(10 回目の起動文では `run1〜run10.log` の 10 本すべてを渡していたのに、11 回目は 9 本 + 11 回目だけで 10 回目が丸ごと落ちている)。実測: 現状の `docs/DATA/SCAN_2026-09-23_tools_cat8.md` に対して `run10.log` を抜いて `check_scan_report.py` を打つと、K11(実測の根拠)だけで **20260923_tools_8_run10.log を指す「根拠が指す生ログが渡されていない」が約 30 行**発生し、合計 37 件になった(10 回目の節は 11 回目でも書き換えずに残るため)。11 回目の起動文自身が「委任文 §4.0 の表は 10 回目のものを使ってよい」(8-016 の項)と 10 回目の内容の流用を認めており、10 回目の節の実測根拠は今も `run10.log` を指したまま本文に残る。この状態で調査班が §7 の 1 を打てば、着手前から数十件の偽陽性がほぼ確実に出る。「誤検出は閉じずに行と理由を書いて渡す」規則で形式上は通せるが、10 回目の節を一切触っていないのに毎回同じ数十件を書き出す負担を起動文の記述ミスに起因して背負わせることになる。`run10.log` を引数に足さずに 11 回目を起動してよいか。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:52`(§2 の 1、「検索の手の決まり」)と `scripts/cat8_ledger.py` の `ELEM_TERMS_WIDE`・`check-elements --round 11` 以降のコード(588〜600 行付近) — 起動文の本文は「最低でも」の語群を **E3a・E3b・E5 の 3 要素だけ**に書いているが、実装した `ELEM_TERMS_WIDE` は **E1a・E1b・E2・E4・E6 にも**語群を定義し、`check-elements --round 11` はどの要素の `なし` にも同じ強さで語群一致を要求する。実測: 合成の報告(`E1a` の `なし` を `reconcil` の単語だけで検索した体で作成)で `check-elements --round 11` を打つと、「7 列目の手の `$` の行に当たらない語の組がある: cross.?check|比較|compar / diff|差分|一致|mismatch」で止まった。11 回目の §2 の 2 は 8-017〜8-028 の 12 件を E1a〜E6 の全要素で深掘りするため、この不一致は次回以降ではなく**この回のうちに**当たる可能性が高い。起動文の本文に E1a・E1b・E2・E4・E6 の必須語群を明記しないまま、道具だけがそれを要求してよいか(P8 に近い型: 機械は先に直されているが、行動の直前に読む起動文にはその内容が無い)。
>
> 3. [止める] `scripts/cat8_ledger.py` の `CUT_RE`(419 行台) — `\|\s*(?:head|tail)\b` などの語形しか見ておらず、**`sed -n '1,60p'`・`awk 'NR<=60'`・`rg -m`/`--max-count` など同等の出力の間引きを検出しない**。実測: `grep ... | sed -n '1,60p'` で切った検索を `なし` の根拠にした合成の報告を作り、`check-elements --round 11` を打つと、切ったことは一切指摘されずに通過した(生ログに「[出力は…先頭…だけを残した]」の自動切り詰め表示が出ないケースは `TRUNC_RE` でも捕まらない)。監査 63 回目の [止める] は「`head -60` で切ったうえで `なし` と書いた」ことそのものだったが、その再発防止が「`| head`・`| tail`・`grep -m` を付けない」という**文言の列挙**と、それに対応する**同じ 3 パターンだけを見る正規表現**で閉じられており、字面が違う間引き方法は素通りする。11 回目の起動文の本文(50 行、52 行)は「出力を切らない」という一般原則を書きつつ、具体的な禁止語は 3 つしか挙げていない。この状態で 11 回目を起動してよいか、それとも `CUT_RE` に他の間引き手段(`sed -n`・`awk` の行範囲指定・`rg -m`/`--max-count`・`| tail -n +`)も足してから起動すべきか。
>
> 4. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:42`(§2、「オーナーが決めた…段の読み」)・`docs/OWNER_LOG.md` L-511・L-512 — 8-005 E4 を段 4 とする基準の文言「道具から取り出したファイル(生データ・正規化したデータ)を別の道具で使えるなら、そのデータは『外から持ち込める』に数える」は、L-511 の質問文(5 列目の「8-005 Exegy E4 を段 4 に数えるか(3 か 4)」)にもオーナーの逐語「5は4です」にも無く、**L-512 でリードが選択肢 4 の説明として書いた文**(「取り出したファイルを別の道具で再生できるなら…外から持ち込めるので段4」)がそのまま基準として採用されている。オーナーが直接検分したのは「3・4・決めない」という選択肢そのものであって、この基準の文言自体ではない。これを「問いに出さずに当てる」将来の一般規則(L-508・L-509 と同列)として扱ってよいか、それとも 8-005 E4 という単一の事案への回答に限定すべきか。
>
> 5. [聞く] `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run10.md:61`(§3 の表、行 2「8-014 E3a の段 3 か 4 か」) — `docs/DATA/SCAN_2026-09-23_tools_cat8.md:3701` の 10 回目自身の記述(「LLMの判断は…『合否や検出を自分で判定しない』段2寄りとも読める」)と突き合わせると、8-014 E3a の未決の境界は実際には「段 2 か 3 か」であり、L-511 の Q6 もそのとおり「(2 か 3)」で聞いている。検収の表だけが行 1(8-005 E4、正しく「3 か 4」)の文言をそのまま行 2 にも使い回し、「3 か 4」と誤記している(行 1 からのコピペ跡と見られる)。11 回目の起動文が引用する段の読み(「8-014 E3a = 段 3」)自体は L-511 の実際の問いと整合しており実害は無いが、検収記録の誤記は残ったままでよいか。
>
> 以上、[止める] 2 件・[直す] 1 件・[聞く] 2 件。1(`run10.log` の欠落)は現状のまま起動すると着手前から数十件の偽陽性が確実に出るため、**このまま 11 回目を起動すべきではない**(1 を直してから、可能なら 3 も併せて直してから起動するのが望ましい)。

**リードの処置**:
1: 認める(回の番号を機械で置き換えたとき、`run10.log` が `run11.log` に替わって 10 回目の生ログが落ちた)。§7 の 1 の生ログを `run1`〜`run11` の 11 本にした。
2: 認める。§2 の 1 の「検索の手の決まり」の (c) に、`check-elements` が見る全要素(E1a〜E6)の語の組を、道具と同じ中身で書いた。
3: 認める。`CUT_RE` に `less`・`more`・`rg -m`・`--max-count`・`sed -n <数>`・`sed <数>q`・`awk` の `NR` の比較・`| cut -c`・`| fold`・`[:数]`・`islice` を足した。実測(2026-09-24、リード): `| head -60`・`| sed -n '1,60p'`・`| awk 'NR<=60'`・`rg -m 5`・`rg --max-count 3`・`| cut -c1-200`・`| tail -n +2` は止まり、`grep -rIn -iE 'look-?ahead' dir; echo RC=$?`・`grep -c`・`sed -n 's/a/b/p'` は通る。一覧の件数と先頭・末尾を印字する手(起動文が打てと言う `head`・`tail`)を誤って止めないよう、この検査は**その要素の語を含む手**(検索の手)だけに掛けた。2〜10 回目の結果は変わらない(8 回目 5 件、ほか 0 件)。起動文の (a) にも間引く形を並べ、「ここに無い形でも間引けば同じ」と書いた。
4: 認める(リードの選択肢の説明の文を、一般の規則としてオーナーの答えに見せていた = §0.2 A-1 の型)。起動文では、L-511・L-512 の答えを**その 3 つのセルの答え**とし、ほかの候補で同じ形の境界に当たったら段を `未判別` にして `[値・段の問い]` で出し、前例として番号を書く、とした。
5: 認める。10 回目の検収 §3 の表の行 2 を「段 2 か 3 か」に直した。
直した版(起動文の指紋 `2b956877ceae`)は監査 65 回目で確かめてから印を記録する。


## 監査 65 回目(対象 = b9dbb99、起動文の指紋 `2b956877ceae`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件・[聞く] 1 件):

> 監査 65 回目(対象コミット b9dbb99、範囲: 監査 64 回目の 5 件への処置)の指摘。
>
> 参照した機械: `scripts/cat8_ledger.py`(CUT_RE / ELEM_TERMS / ELEM_TERMS_WIDE / check-elements)。検査は scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit65/`)で合成の報告・生ログを作って実際に道具を打って確認した(リポジトリ本体には一切書き込んでいない。使い終わったら `rm -rf` する)。
>
> 1. [止める] `scripts/cat8_ledger.py:437-442`(`CUT_RE`)・`docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:52` — 監査 64 回目の指摘 3 を受けて広げた `CUT_RE` は、列挙した字面(`head`/`tail`/`less`/`more`/`grep -m`/`rg -m`/`--max-count`/`sed -n <数>`/`sed <数>q`/`awk NR`/`cut -c`/`fold`/`[:数]`/`islice`)しか見ていない。scratchpadに合成の報告・生ログを作り実測した: `なし` の 7 列目に `grep -rniE 'reconcil|cross-check|compar|diff|mismatch' <path> | python3 -c "...sys.stdin.read().splitlines()[0:60]"` という、要素の名前(`reconcil`)も `ELEM_TERMS_WIDE` の E1a の 3 組(reconcil / cross-check・compar / diff・mismatch)も全部満たし、かつ実際には出力を先頭 60 行に間引いている1手だけを引かせて `python3 scripts/cat8_ledger.py check-elements report.md --round 11` を打つと、**0 件で通った**(`| head` 系との違いは `[0:60]`(先頭に `0` があるスライス)である点だけ。`[:2000]`(先頭を空にした形)は正しく止まることも別途確認した。パイプの左に付かない `head -c <数>`(バイト数指定)も同様に通る)。これは監査 63・64 回目が閉じたはずの型(切った出力を `なし` の根拠にする)そのものであり、この回はまさに 8-016 の E3a・E3b・E5 を `未判別` からやり直す回・8-017〜8-028 を E1a〜E6 まで深掘りする回である。この抜け道を塞がずに 11 回目を起動してよいか(関連原則 P3: 「なし」を保証する道具が実際にその保証を満たすかは、実行して確かめるまで分からない。書いてあるから止まるは成立しない)。
>
> 2. [直す] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:5`(§2 の 1 (a)、「ここに無い形でも間引けば同じ」) — 起動文の本文はこの一文で「列挙した形以外の間引きも同様に禁止」という広い規範を調査班に課しているが、実際にそれを機械で担保するはずの `check-elements`(§7 の 2)は指摘 1 のとおり列挙された形しか検出しない。§7 の「0 件にする」という完了の形は、この広い規範までは検査していない。本文の主張と §7 の検査の実際のカバー範囲が一致するよう、どちらかを直すべきではないか(直すなら CUT_RE を広げる方が望ましいと考えられる。指摘 1 と対で扱うのがよい)。
>
> 3. [聞く] `scripts/cat8_ledger.py:602-604`(`if re.search(ELEM_TERMS.get(el, "^$"), c, re.I) and CUT_RE.search(c):`) — CUT_RE を掛けるかどうかのゲートは `ELEM_TERMS`(狭い語、例: E3a/E3b は `ルックアヘッド|look-?ahead` だけ)であって `ELEM_TERMS_WIDE`(広い語群、embargo・purge・point-in-time・survivorship 等を含む)ではない。`なし` の根拠として複数の手を 7 列目に引いたとき、狭い語は切っていない手で満たし、広い語群の一部(embargo 等)だけを含む別の手(実際に切っている手)は狭い語を含まないため CUT_RE 自体が掛からない、という組み合わせが理屈上は残る(指摘 1 の実測とは別経路。今回は実際に構成して確かめてはいない)。8-016 の E3a・E3b・E5 のやり直しでこの組み合わせが起きないか、確認済みか。
>
> チェック済みで問題が見当たらなかったもの(参考、指摘には数えない):
> - §7 の 1(`check_scan_report.py` の呼び出し、`run11_prompt.md:77`)は `run1`〜`run11` の 11 本全部を渡しており、監査 64 回目の指摘 1(`run10.log` の欠落)は解消を確認した。
> - §2 の 1 (c)(`run11_prompt.md:52`、全要素の語の組)は `E1a`〜`E6` の 8 要素すべてで `scripts/cat8_ledger.py` の `ELEM_TERMS_WIDE` と字面まで一致しており、監査 64 回目の指摘 2 は解消を確認した。
> - L-511「5 は 34、6 は 3、7 は 4」・L-512「5は4です」の答えを `run11_prompt.md:35` が「8-005 E4 = 段4 / 8-014 E3a = 段3 / 8-014 E1b = 段4」の3セル限定として扱い、一般規則にしていないことを `docs/OWNER_LOG.md` L-511・L-512 の原文および `docs/DATA/tools_catalog_cat8.tsv` の該当セル(8-005 段_E4=4、8-014 段_E3a=3、段_E1b=4)と突き合わせて確認した。監査 64 回目の指摘 4 は解消。
> - `docs/AUDITOR/VERDICTS/2026-09-24_tools_scan_cat8_run10.md:61`(§3 の表、行 2)は「8-014 E3a の段 3 か 4 か」から「段 2 か 3 か」に直っており、`docs/DATA/SCAN_2026-09-23_tools_cat8.md:3701` の記述・L-511 の Q6「(2 か 3)」と整合することを確認した。監査 64 回目の指摘 5 は解消。
>
> 11 回目を起動してよい状態か: 指摘 1(実測で確認した `check-elements` の抜け道)が未解決のため、**このまま起動すべきではない**。

**リードの処置**(1〜3 をまとめて作りを替えた):
**間引く書き方を並べて止める作りをやめ、`なし` の検索を 1 つの道具に決めた**(監査 34〜39 回目で、文から行の番号を読み当てる検査を専用の列に替えたのと同じ教訓: 並べて禁じる作りは、並べていない形で抜ける)。
- `scripts/cat8_search.py` を作った: 検索するファイルの一覧を全部読み、当たった行を全部出す(400 字を超える行は当たった所の前後 200 字と行の長さ)、ファイルごとの当たった行の数、最後に `cat8_search: complete files=N read=N files_with_hits=F hits=H`。読めないファイルがあれば `INCOMPLETE` と出して終了コード 1。
- `check-elements`(11 回目以降)の `なし` の検査を替えた: 7 列目に cat8_search.py の手が 1 つ以上あること / その手の `$` の行が `python3 scripts/cat8_search.py ...` だけで、引用の外にパイプ・`;`・`&&`・リダイレクトが無いこと(`shlex` で字句に分けて見る。`--pattern 'a|b'` の中の `|` は引用の中なので通る)/ 出力に `complete` の終わりの行があること / 生ログで切られていないこと / cat8_search.py の手の `files=` の和が根拠の「読んだ M 件」と同じこと / 7 列目に grep・rg などほかの検索を引いていないこと / 語の組は cat8_search.py の手の `$` の行だけで見ること(指摘 3 の、狭い語を別の手で満たす抜け道を塞ぐ)。並べた形の検査(`CUT_RE`)は外した。
- 実測(2026-09-24、リード。scratchpad と仮の 11 回目の生ログ、打ったあと消した): 形の判定は `--pattern '"'"'reconcil|diff'"'"' --ignore-case` を通し、`| head -1`・`|head`・`> /tmp/o`・`; true`・`bash -c '"'"'...'"'"'` を止めた。報告の行では、読んだ数の合う cat8_search.py の手は通り、`| head -1` を付けた手・`grep ... | python3 -c '"'"'...[0:60]'"'"'` の手・読んだ数が合わない手・終わりの行が無い手(引数の誤りで終わった手)は止まった。2〜10 回目の結果は変わらない(8 回目 5 件、ほか 0 件)。
- 起動文 §2 の 1 の「検索の手の決まり」の (a) を、この道具だけで打つ形に書き替え、(b) の数え合わせを道具の出力の数で行うようにした。
直した版(起動文の指紋 `31691b3fd3ca`)は監査 66 回目で確かめてから印を記録する。


## 監査 66 回目(対象 = 5c18449、起動文の指紋 `31691b3fd3ca`)

監査役の出力(逐語。[止める] 3 件(うち 1 件は [止める]/[直す])・[直す] 2 件):

> 監査 66 回目(対象コミット 5c18449、範囲: 監査 65 回目の 3 件への処置)。
>
> 参照した機械: `scripts/cat8_search.py`・`scripts/cat8_ledger.py`(`check-elements --round 11`)・`scripts/check_scan_report.py`・`scripts/cat8_step.py`。検査は scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit66/`)で合成の報告・生ログを作り、実際に道具を打って確認した(使用後 `rm -rf` 済み。リポジトリ本体は `docs/DATA/probes/20260923_tools_8_run11.log` に一時的に実ファイルを置いて `check-elements` を打ったが、検査後に削除済み。`git status` で他に変更が残っていないことを確認した)。
>
> 1. [止める] `scripts/cat8_ledger.py:602-636`(`check-elements --round 11` の `なし` の検査)・`scripts/check_scan_report.py` — **`なし` の検査は、引いた `cat8_search.py` の手が実際に当たり(hits)を出したかを一切見ていない。** 実測: 「reconcile」という文字どおりの一致を含む本物のヒット(`def reconcile(a, b):`)がある検索(`cat8_search: complete files=5 read=5 files_with_hits=1 hits=1`)を生ログに記録し、そのヒットに一切触れずに `なし`・根拠「一覧 5 件 / 読んだ 5 件」とだけ書いた合成報告を作って `check-elements --round 11` を打つと、**0 件で通った**。起動文 §2 の 1 (b)(監査 65 回目の処置で作った規則)は「当たった行のあるファイルは 1 つも落とさない…cat8_search.py の出力の『ファイルごとの当たった行の数』と終わりの行の `files_with_hits`・`hits` と数え合わせる」と明記しているが、`check-elements` は `files_with_hits=`・`hits=` の値を一度も読んでいない(`grep -n "files_with_hits\|hits=" scripts/cat8_ledger.py` は 0 件)。`check_scan_report.py` 側にも同種の照合は無い。**この抜け道は監査 63〜65 回目が閉じたどの型とも別物で、`なし` の検証で一番肝心な「検索は本当に無かったか」を機械が全く見ていない。**これを塞がずに 11 回目(8-016 の E3a・E3b・E5 のやり直し・8-017〜8-028 の深掘り、大量の `なし` を書く回)を起動してよいか。
>
> 2. [止める] `scripts/cat8_ledger.py:602-636` — **同じ生ログの手(同じ `<file>:<line>`)を 7 列目に 2 回書くと、和が水増しされて `M` の照合をすり抜ける。** 実測: 真の候補ファイルは 4 件だが `--list` を 1 件だけに縮めて検索した手(`cat8_search: complete files=1 read=1 ...`)を作り、報告の 7 列目に `20260923_tools_8_run11.log:1 20260923_tools_8_run11.log:1`(同じ行を空白区切りで 2 回)、根拠を「一覧 2 件 / 読んだ 2 件」(嘘)と書いて `check-elements --round 11` を打つと、**0 件で通った**(`strict_refs` が同じ参照を重複したまま返し、`searched_files` がそのまま 2 倍にカウントされるため)。設問で示唆された型そのものが実際に機械を抜けることを確認した。7 列目の参照に重複除去(unique)が無い。
>
> 3. [止める]/[直す] `scripts/cat8_search.py:26-40`(`data.decode("utf-8", "replace")`) — **UTF-8 以外の符号化のファイルは、当たりが無言で消える。** 実測: `突き合わせ処理` という日本語の一致語を含むファイルを Shift_JIS で書き、これを `--list` に入れて `--pattern '突き合わせ|reconcil'` で検索すると、`cat8_search: complete files=1 read=1 files_with_hits=0 hits=0` — **一致するはずの語が多バイト文字の壊れ(errors="replace")で消え、しかも「壊れて読んだ」という兆候が出力のどこにも出ない**(`unread` に入らず `complete` のまま)。7 列目の検索の語には日本語(突き合わせ・突合・欠損・重複・生存者・再現 等)が必須で、178 件規模のリポジトリ群には非 UTF-8 の README・コメントが混ざり得る。「読めなかった」の判定(`OSError` のみ)に、符号化の失敗による黙った取りこぼしが入っていない。P1「無い/取れない」は全経路を試した結果でなければ言えない、に近い型(検索という手段そのものが特定の符号化のファイルで機能しない)。
>
> 4. [直す] `scripts/cat8_search.py:26-31`・`scripts/cat8_ledger.py`(`なし` の N=M 照合) — **`--list` の中身が真の一次資料の一覧(`git -C <置き場> ls-files` 等)と一致することを、機械は一度も確かめていない。** 実測: 真の候補ファイルは 4 件だが `--list` を 2 件に縮め、根拠を「一覧 2 件 / 読んだ 2 件」(N=M で一致させただけ)にした合成報告は `check-elements --round 11` の E1a の行で無警告(ほかの要素は語の組不足で別に止まった)。起動文は「検索の対象の一覧の件数を `wc -l` で印字する手を打つ」と求めているが、その `wc -l` の数値を `--list` の実際の行数や `cat8_search.py` の `files=` と突き合わせる検査は無い(`grep -n "wc -l\|ls-files" scripts/cat8_ledger.py` は 0 件)。8-016 はリポジトリを検索後すぐ削除する運用なので、あとから検収で取り直して数え合わせることもできない。機械は「N と M が一致している」ことしか見ておらず、「N が本当に全件か」は投稿者の申告を信じるしかない。
>
> 5. [直す] `scripts/cat8_search.py:26-31` — 空の `--list`(0 行)を渡すと `cat8_search: complete files=0 read=0 files_with_hits=0 hits=0` で正常終了し、根拠「一覧 0 件 / 読んだ 0 件」も N=M で通ってしまう(実測)。極端形だが、N>0 を要求する検査が無い。
>
> チェック済みで問題が見当たらなかったもの(参考、指摘には数えない):
> - `cat8_search.py` は読めないファイルを `unread` に入れ `INCOMPLETE`・終了コード 1 にする(黙って落とさない。実測)。
> - 400 字超の長い行は当たった箇所の前後 200 字と行の長さを明示して出す(黙って切らない)。
> - NUL 区切りの一覧は正しく認識される(`b"\0" in raw` で分岐、実測)。
> - `--keep` を小さくして生ログが切られた場合、`TRUNC_RE`(`[出力は…先頭…だけを残した]`)または `SEARCH_DONE_RE` の不在で正しく止まる(監査 63〜65 回目の型は塞がったまま)。
> - `search_cmd_only` はパイプ・`;`・`&&`・リダイレクトを含む手を正しく止め、`--pattern 'a|b'` の引用内の `|` は正しく通す(実測)。
> - 起動文 §2 の 1 (a) の呼び出し形・7 列目の参照形式は `cat8_ledger.py` の実装と字面まで一致していることを確認した(監査 64・65 回目の指摘は解消のまま)。
> - 起動文が「リポジトリごとに打ってよい」と明記しているため、8-016 の 178 件を 1 リポジトリ 1 検索で行う運用そのものは道具の設計(任意の `--list` を何度でも呼べる)と矛盾しない。
>
> 11 回目を起動してよい状態か: **指摘 1・2(実測で確認した具体的な抜け道)が残るため、このまま起動すべきではない。** とくに指摘 1(hits を一切見ない)は、起動文が明記した規則(b)を検査が実装していない、これまでの監査で見つかった型とは別種の欠落であり、優先して塞ぐべきと考えられる。

**リードの処置**(5 件とも認める。道具と検査を作り足した):
1(当たりを見ていない): `なし` の根拠に `当たり F ファイル / H 行` を必須にし、7 列目の cat8_search.py の手の `files_with_hits`・`hits` の和と同じかを見る。さらに、当たった行のあるファイルの道(検索の出力の「ファイルごとの当たった行の数」の字)が、この回の節に全部書かれているかを見る(読んで理由を書かせる)。
2(同じ手の重複): 7 列目の手は重複を除いてから数える。
3(符号化): `scripts/cat8_search.py` は utf-8・utf-8-sig・cp932・euc_jp の順に厳密に読み、どれでも読めなければ「読めなかった」に入れて `INCOMPLETE`・終了コード 1。utf-8 でなく読んだファイルは符号化の名前を出す。NUL を含むファイル(バイナリ)も「読めなかった」に入れる(一覧を作るときに理由を書いて外させる)。
4(一覧が一部だけ): `scripts/cat8_mklist.py` を作った。置き場の全ファイル(git の作業木なら `git ls-files`)から、`--exclude '<道>=<理由>'` の分だけを外し、`--add` の分を足して一覧を書き、除外・足したもの・`--absent`(置き場に無い、除外の種類のファイル)の名前と理由と `cat8_mklist: complete out=<一覧> in_root=R added=A absent=B excluded=X listed=L` を出す。検査は、7 列目の cat8_search.py の手の一覧(終わりの行の `list=`)を作った cat8_mklist.py の手が同じ生ログのそれより前にあるか、`listed=` がその検索の `files=` と同じか、`listed=` の和が根拠の N と同じか、を見る。
5(空の一覧): 検索の道具は一覧が空なら `INCOMPLETE`・終了コード 1。検査は読んだ件数 0 の `なし` を止める。
実測(2026-09-24、リード。scratchpad と仮の 11 回目の生ログ、打ったあと消した): Shift_JIS の「突き合わせ処理」は cp932 として読んで当たった / バイナリを一覧に残すと `INCOMPLETE` / 空の一覧は `INCOMPLETE` / 置き場に無い道の `--exclude` は終了コード 1。報告の行では、当たったファイルを節に書いた正しい行は通り、書いていない行・同じ手を 2 回書いて件数を倍にした行・手で作った一覧の検索の行・`当たり F ファイル / H 行` の無い行は止まった。2〜10 回目の結果は変わらない(8 回目 5 件、ほか 0 件)。起動文 §2 の 1 の (a)(b) をこの 2 つの道具の形に書き替えた。
直した版(起動文の指紋 `24f1c9801dc0`)は監査 67 回目で確かめてから印を記録する。


## 監査 67 回目(対象 = f9cec5e、起動文の指紋 `24f1c9801dc0`)

監査役の出力(逐語。[止める] 4 件・[直す] 2 件・[聞く] 2 件):

> 監査 67 回目(対象コミット f9cec5e、範囲: 監査 66 回目の 5 件への処置)。
>
> 11 回目の起動文・道具(`scripts/cat8_mklist.py`・`scripts/cat8_search.py`・`scripts/cat8_ledger.py check-elements --round 11`)を実測で検査した。scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit67*/`)に合成の報告・生ログを作り、`docs/DATA/probes/20260923_tools_8_run11.log` に一時的に実ファイルを置いて `check-elements` を打ったのち削除した(`git status` で他の変更が残っていないことを確認済み。残っているのは監査の自動追跡が書き換える `docs/AUDITOR/TRACE/2026-09-24_2da9385f.json` のみで、私は触っていない)。
>
> 1. [止める] `scripts/cat8_ledger.py:666-673`(`なし` の「当たり F ファイル / H 行」検査) — **当たったファイルの道が節のどこかに文字列として出現するかだけを見ており、そこに「理由」が実際に書かれているか、値「なし」と矛盾しないかを一切見ていない。** 実測: 検索で本物の当たり(`leak future data`、`files_with_hits=1 hits=1`)を出した生ログを作り、報告の該当行を「値=なし、根拠=一覧2件/読んだ2件/当たり1ファイル/1行」とし、当たったファイルの絶対パスを無関係な文(「なお〈パス〉は関係ない話だが一応ここに書いておく」)として節の別の場所に置いただけで、`check-elements --round 11` は **0 件で通った**。監査66回目の指摘1(当たりを見ていない)は数の一致までは塞いだが、「理由の有無」「値との整合」までは塞がっていない。この回はまさに大量の `なし` を書く回であり、この抜け道が一番使われやすい。(P3、P5)
>
> 2. [止める] `scripts/cat8_ledger.py:645-651`(`mklist_footer` の照合)・`scripts/cat8_mklist.py` 全体 — **`cat8_mklist.py` が作った一覧ファイルは、その後 `cat8_search.py` に渡されるまでの間、手で自由に書き換えられる。書き換えを検出する仕組み(ハッシュ・不変性チェック)が無い。** 実測: 一覧作成後に、当たりを含むファイル(`leak` を含む)を一覧から消し、別のファイルの行を複製して件数(2件)を維持してから検索したところ、`files=`・`listed=` の数合わせはすべて一致し、当たり0件の `なし` が通る状態を再現した。監査65〜66回目が塞いだのは「検索の手の形」「同じ参照の重複」で、「一覧そのものの事後改変」は対象外のまま。
>
> 3. [止める] `scripts/cat8_ledger.py:626-655` — **`check-elements` は報告の7列目に明示的に引用された検索の手だけを見る。** 同じ候補・要素に対して複数回 `cat8_search.py` を打っても、生ログ全体を走査して未引用の呼び出しを探す仕組みが無いため、当たりが出た検索(あるいは1MB超で取れなかったファイルを含む広い検索)を単に引用せず、当たりが出ない別の検索だけを7列目に引けば検出を逃れられる(実測は上記1・2と組み合わせれば同じ帰結になることを確認済み)。「同じ一覧で2回検索して片方だけ引く」という監査の懸念そのもの。
>
> 4. [止める] `scripts/cat8_ledger.py:648-665`(`n_src += mk["listed"]`。`mk["absent"]` は一度も参照されない。`grep -n "skipped\|files_in_tree\|absent" scripts/cat8_ledger.py` で確認)・`scripts/cat8_repo_fetch.sh:96-103`(`[ "$n_out" -eq "$n_present" ]`、取得後の作業木の `git ls-files` は 1MB 超で取らなかったブロブを含まないことをこの不変条件から確認)。**8-016(および §2 の 2 で同じ道具を使う候補)で「1MB超のため取らなかった」ファイルは、`cat8_mklist.py` の `N`(`in_root`/`listed`)に一切現れない。** 実測: 取得ログに `files_in_tree_N_4`・`skipped: c_large.bin`・`skipped: d_large.bin`(2件)を記録し、対応する `--absent` を一切与えずに mklist(`listed=2`)→search(0 hit)→「なし・一覧2件/読んだ2件」の行を書いたところ、`check-elements --round 11` は **0 件で通った**。起動文§2の1本文が定めるNの定義(「一次資料の一覧の全件」)と、機械が実際に検査するNの定義(mklistのlisted)が食い違っている(task 3 にも関連)。
>
> 5. [直す] `scripts/cat8_mklist.py:36-41`(非 git、`os.walk` 経路) — **シンボリックリンクのディレクトリ配下を黙ってたどらない**(`followlinks` 既定 False)。実測: `root/real_sub/linked`(外部ディレクトリへのシンボリックリンク、配下に当たりファイルあり)を作ると、`in_root` にも「除外」「無い」の出力にも一切現れず、理由も付かずに一覧から消えた。「普通の置き場」(サイト取得・アーカイブ展開など、§2の2の候補で使われうる)で起きうる。
>
> 6. [直す] `scripts/cat8_mklist.py:64-66`(`"".join(p + "\n" for p in listed)`)対 `scripts/cat8_search.py:26-28`(NUL/改行の自動判定) — **一覧の書き出しが常に `\n` 区切りで、パス自体に改行を含むファイル名を表現できない。** 実測: git 作業木に改行入りファイル名(`c\nd.py`)を作り、`git ls-files -z` では正しく1件として取れたのに、`cat8_mklist.py --out` が書いた一覧では2行(`.../c` と `d.py`)に分裂した。`cat8_search.py` 自体はNUL区切りに対応できる作りなのに、`cat8_mklist.py` は活かしていない。分裂後は多くの場合ファイルが存在せず `unread`/`INCOMPLETE` になるはずだが、エラーメッセージは真因(改行入りファイル名)を示さず、対象ファイルは検索されないまま終わる。
>
> 7. [聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:41` と同 `:52` — Nの定義が本文内で二重(「一次資料の一覧の全件」と「mklistのlistedの和」)。8-016のように取得ツールが一部ファイルを取らない候補では両者が別の値になる(指摘4で実測)。この回の完了条件(§7の2)は後者しか機械で見ていないことを把握したうえでこの記述にしたか。
>
> 8. [聞く] 起動文 §2 の 2(8-017〜8-028)は GitHub リポジトリを `cat8_repo_fetch.sh` で取ってよいとする一方、§2 の 1 が 8-016 に課す `cat8_fetch_tally.py` によるN・skipped・読んでいない件数の機械集計と、リードの検収時の数え合わせは §2 の 2 には明記が無い。12件のどれかが1MB超のファイルを含んだ場合、指摘4と同じ抜け道が、8-016以上に何の集計手順も無いまま残る。同じ手順を§2の2にも課すか、それとも別の理由で不要と判断したか。
>
> 11 回目を起動してよい状態か: 指摘 1〜4(いずれも実測で確認した、値「なし」を機械の検査が通してしまう具体的な抜け道)が未解決のため、このまま起動すべきではない。とくに指摘 1 は「なし」の意味そのもの(当たりが無い、または当たりに正当な理由がある)を機械が保証していないという最も根の深い型であり、優先して塞ぐべきと考えられる。

**リードの処置**:
1: 一部を機械で塞ぎ、残りは読む側に置く。当たったファイルは節の「どこか」でなく `### 当たりの判定` の表(`| ファイルの道 | 当たった行の数 | 述語に当たらない理由 |`)に、理由の欄を空にせずに書かせ、検査はその表の行があるか・理由が空でないかを見る。**理由が当たっているか(値の `なし` と矛盾しないか)は文の意味の判断なので機械にしない**。リードの検収で表を読み、中身の監査に通す(監査 63 回目は、まさにこの読みで [止める] を出した)。
2: 認める。`cat8_mklist.py` は一覧を NUL 区切りで書き、終わりの行に一覧の `sha=` を出す。`cat8_search.py` も読んだ一覧の `sha=` を出す。検査は、検索の手の一覧の sha が、それを作った cat8_mklist.py の手の sha と同じかを見る。実測: 一覧を作ったあとに書き換えて検索した手は「作ったあとに書き換えられている」で止まった。
3: 認める。11 回目以降、この回の生ログの**すべての** cat8_search.py の手の当たったファイルが `### 当たりの判定` の表に理由つきで無ければ止まる(7 列目に引いていない手も)。実測: 引いていない手の当たりで止まった。
4: 認める。`cat8_mklist.py` は、置き場が `cat8_repo_fetch.sh` で取ったもの(`.git/cat8_missing` と `.git/cat8_tree` がある)なら、取らなかったファイルが全部 `--add`(置き場の同じ道に置いたもの)か `--absent`(理由つき)になっているかを見て、なっていなければ止まる。実測: `th2-net/viewer` で、何も付けないと取らなかった 14 件の名前を出して止まり、14 件を `--absent` にすると `listed=25985` で通った。
5: 認める。置き場の下にシンボリックリンクのディレクトリがあると、その名前を出して止まる(中身を写すか `--add` する)。実測: 止まった。
6: 認める。一覧は NUL 区切りで書く(検索の道具は NUL 区切りを読む)。
7: 答える。起動文で、`なし` の N と M は「除外のあとの件数 = cat8_mklist.py の `listed=` の和」と書いた(これまでの回の決まりと同じ。除外・取らなかったものは cat8_mklist.py の出力に名前と理由)。一覧の全件(除外の前)との関係は 4 の検査が見る。
8: 答える。4 の検査は置き場ごとに掛かるので、§2 の 2 の候補を同じ取得の道具で取ったときも同じく止まる。起動文にそう書いた。
2〜10 回目の結果は変わらない(8 回目 5 件、ほか 0 件)。

**リードの見立て(オーナーに伝える)**: 監査 63〜67 回目の [止める] は、63 回目(調査班が実際に出力を切った)を除き、**調査班がわざと検査をすり抜けようとした場合**の抜け道を、監査役が 1 回ごとに新しく作って見つけたもの。1 回ごとに塞げてはいるが、この型は並べ切れない。機械が見るのは形(道具で打ったか・数が合うか・表があるか)までにし、理由の中身はリードの検収と中身の監査で読む、という線を引いた。
直した版(起動文の指紋 `14abfb7880b6`)は監査 68 回目で確かめてから印を記録する。


## 監査 68 回目(対象 = a048b11、起動文の指紋 `e1cde2de5678`)

監査役の出力(逐語。[止める] 1 件・[直す] 1 件・[聞く] 2 件。67 回目の処置 6 件は実物で成り立つと確かめた):

> 監査 68 回目。対象コミット a048b11。実測は scratchpad(`/tmp/claude-0/-home-user-trade/2da9385f-4fe4-506a-be3d-78153c549662/scratchpad/audit68/`、検査後 `rm -rf` 済み)で合成の報告・生ログ・簡易 git リポジトリ(partial clone を smart-http 相当のローカル bare で再現)を作り、実際に `scripts/cat8_mklist.py`・`scripts/cat8_search.py`・`scripts/cat8_ledger.py check-elements --round 11` を打って確認した。`docs/DATA/probes/20260923_tools_8_run11.log` に一時的に実ファイルを置いたが検査後に削除済み(`git status` は `docs/AUDITOR/TRACE/...json` の自動更新のみ)。
>
> ## 1. 監査 67 回目の処置(a048b11)が実物で成り立つか
>
> **当たりの判定の表・理由欄の要求**: 理由欄を空にした合成報告で `check-elements --round 11` を打つと、テーブル行の欠落と生ログ側の欠落の 2 件が正しく検出された(`docs/AUDITOR/VERDICTS/...` の主張どおり)。
>
> **一覧の sha 照合(改変検知)**: `cat8_mklist.py` で一覧を作った後、同じファイルを改変してから別の `cat8_search.py` を打ち、その手を 7 列目に引いた報告を作ると、「作ったあとに書き換えられている(sha が違う)」「files= が listed= と違う」「読んだ件数が違う」の 3 件が同時に発火した。実物で機能している。
>
> **同じ参照の重複除外**: 同じ `<file>:<line>` を 7 列目に 2 回書いて N/M/当たりを水増しした報告を作ると、`dict.fromkeys` による重複除去のとおり、水増し後の値と実際の和(重複除去後)の不一致がすべて検出された。
>
> **7 列目に引かなかった検索の当たりも見る(監査 67 指摘 3 の閉じ)**: 7 列目に引用していない別の `cat8_search.py` の手(当たり 1 件)を生ログに追加すると、`### 当たりの判定` の表に無いことを理由に正しく止まった。
>
> **シンボリックリンクのディレクトリ**: 非 git の置き場にシンボリックリンクを作ると `cat8_mklist.py` は名前を出して exit 1 で止まった(中身を書き換えたり通したりしない)。
>
> **改行入りファイル名 / NUL 区切り**: git 作業木に改行を含むファイル名を作ると、NUL 区切りの一覧は 1 エントリとして正しく保持され、`cat8_search.py` も 1 ファイルとして正しく検索・報告した。
>
> **1MB 超で取得しなかった blob の会計(監査 67 指摘 4)**: 最初、手作りの git リポジトリ(`git add` して commit したあと working tree だけ `rm`)で試したところ `--absent` が「置き場の下にある」で拒否され、一度は欠陥と誤認しかけた。しかし **これは `cat8_repo_fetch.sh` の実際の手順(`--no-checkout` クローン → `git checkout --pathspec-from-file` で present だけを選択的にチェックアウト)を反映しない偽の再現だった。** `git clone --no-checkout` は最初インデックスが空になり(`git ls-files` 0 件を確認)、選択的チェックアウト後は present のファイルだけが `git ls-files` に載る(skipped の blob は載らない)ことを、実際に `uploadpack.allowFilter=true` を立てたローカル bare リポジトリで partial clone を通して確認した。この正しい条件下では `--absent` は意図どおり動作し(未処理だと拒否・`--absent` を付けると通る)、`--add` 単独でも重複なく機能した。**この項目は監査 67 回目の指摘どおり正しく塞がっている。**(自分の最初の実験設計の誤りに気づいて訂正した、という経緯も含めて報告する)
>
> 以上、監査 67 回目が閉じたと主張する 6 件はすべて実測で確認でき、リードの処置の記述と実物の挙動は一致していた。
>
> ## 2. 機械の外に残る、正直な調査班が誤って「なし」を書いてしまう経路
>
> **2-1. [止める] `scripts/cat8_ledger.py:628-688`(`なし` の 7 列目照合) — 引いた検索の手が、その表の行の「候補(道具名)」に対する検索であることを一切確認していない。** 実測で直接証明した: 私が作った合成報告(`qf-lib` の E1a 行)で、7 列目に引いた `cat8_search.py` の手は `qf-lib` とは無関係な自作テスト用ディレクトリ(`.../scratchpad/audit68/repoA`)を検索したものだったが、`check-elements --round 11` は **0 件で通った**(添付した実測ログのとおり)。機械が見るのは sha・files=・listed= などの数値の内部整合性だけで、「この検索は本当にこの候補を検索したものか」は一度も検査していない。委任文 §2 の 1 (a) は `cat8_step.py --target <名前>` の `<名前>` を候補名にする形を定めているが(`run11_prompt.md:52`)、`--- ... target=... ...` の見出し行はコード上どこにも候補名と突き合わせられていない(`grep -n "target=" scripts/cat8_ledger.py` は budget 期限切れの判定にしか使われていない)。8-016 は 178 件のリポジトリを順に取得・検索・削除するため、同名の一覧ファイル(`list.txt` 等)を使い回しながら数百行に及ぶ生ログの行番号を手で表に転記することになる。8-017〜8-028 の 12 件と合わせ、似た形の生ログの行を候補間で取り違えて引用する(手作業の転記ミス)ことは、悪意ではなく通常の作業量から十分に起こりうる。この場合、機械はどの数値も矛盾なく一致するため検出できず、**間違った候補の「なし」がそのまま確定する。**
>
> **2-2. [聞く] `scripts/cat8_ledger.py:424-433`(`ELEM_TERMS_WIDE`) — E6 の語の組の1つが `test|assert|check`。** これは通常のソフトウェアリポジトリでほぼ必ず大量にヒットする一般語であり、8-017〜8-028(Backtrader・Qlib・Lean・OpenBB Terminal 等、いずれも数千ファイル規模の成熟したリポジトリ)でこの組を使うと、無関係なテストコード・アサーションが数百〜数千件ヒットしうる(10 回目の失敗事例でも `viewer` 1 件だけで 713 件・282 件のヒットが出たと記録されている)。`### 当たりの判定` の理由欄の中身は仕組み上機械で検証しない設計(リードの見立てのとおり)なので、量が多いほど「読まずに定型文を書く」という誘惑が正直な作業でも強まる。11 回目でこの語の組を使う前に、対象リポジトリの規模とヒット数の見込みについて何らかの安全弁(サンプリング確認・スコープを絞る除外基準の追加検討など)を置くか、それとも量の多さは検収の負担として許容するか、決めているか。
>
> **2-3. [直す] `scripts/cat8_mklist.py:87-92` と `### 当たりの判定` 表の行の形式 — パス自体に `|`(パイプ文字)を含むファイルがあると、markdown の表のセル分割(`ln.strip().strip("|").split("|")`)がそのファイルの道を正しく1セルとして復元できない。** 改行入りファイル名は一覧の書き出し側では NUL 区切りで正しく扱われるようになった(監査 67 指摘 6 の修正)が、`### 当たりの判定` の表自体は生の markdown テーブルのままであり、`cat8_ledger.py:511-514` の解析はパス中の `|` をエスケープなしに列区切りとして解釈する。極端な事例(自動生成コーパス等でパスに `|` を含む場合)では、当たりが記載されているのに検査側の解析がずれて「無い」と誤判定しうる(誤検出の方向であり「なし」の見逃しではないが、機械の外での混乱要因として記録する)。
>
> ## 3. 起動文 §2 の 1 (a)(b)・§4 の足した文が道具と食い違わないか
>
> §2 の 1 (a)(b) の記述(一覧は NUL 区切り・sha 突き合わせ・シンボリックリンクで止まる・`--absent`/`--add` の使い分け・当たりの判定表の形・当たり F ファイル/H 行の和)は、実測した限り道具の実装と字面まで一致しており、調査班がその通りに打てば §7 の検査を通過できることを確認した。
>
> **[聞く] `docs/DATA/delegations/20260923_tools_survey_cat8_run11_prompt.md:64`(§4)** — 「候補の一覧と『要素と段』の表は、8-001〜8-040 の 40 行全部を、この回の値で載せる」と定めているが、`scripts/cat8_ledger.py` の `check-elements` は候補の一覧に載った名前とテーブルの整合性(載っている名前どうしの突き合わせ)しか見ておらず、「40 行全部が揃っているか」自体は機械で検査していない(揃っていなくてもテーブル内の行同士が整合していれば 0 件で通る)。これは今回変更された部分ではなく前回までと同じ扱いと見受けられるが、11 回目は E1a〜E6 の全部を書く回で分量が多いため、揃っているかの確認は検収の手作業に委ねられたままでよいか。
>
> ## 11 回目を起動してよい状態か
>
> 監査 67 回目が指摘した 6 件はすべて実物で正しく塞がっていることを確認した。ただし 2-1(候補名と検索対象の紐付けが機械で一切検証されていない、実測で 0 件通過を確認済み)は、178+12 件という規模で「正直な転記ミス」が現実的に起こりうる新種の抜け道であり、このまま 11 回目を起動すべきではない。

**リードの処置**:
2-1(正直な転記の取り違え): 認める。`cat8_mklist.py`・`cat8_search.py` に `--candidate <台帳の番号>` を必須にし、終わりの行に `candidate=8-NNN` を出す。検査は、`なし` の行の候補の台帳の番号と、7 列目の検索の手の番号、その一覧を作った手の番号が同じかを見る。実測(2026-09-24、リード): 8-001 の番号で打った検索を 8-016 の行に引くと「候補 8-001 が、この行の候補の番号 8-016 と違う」で止まり、8-016 の番号の手は通った。
2-2(E6 の `test|assert|check` の当たりの量): 答える。語の組は設計票 §3 の E6 の述語の語から取ったもので、狭めない(狭めると当たるはずの機能を探さない方へ振れる = §0.2 A-18)。当たりが多いのは読む量の問題なので、表に全部書かせたうえで、**リードが検収で理由の欄を抜き取って一次資料と読み合わせ**、中身の監査にも通す(定型文の理由が並んでいれば差し戻す)。
2-3(道の中の `|`): 認める。表の読み取りを「`\|` でない `|`」で分けるようにし、起動文に「道に `|` があれば `\|` と書く」と足した。
3(40 行が揃っているか): 認める。11 回目以降、「要素と段」の表に台帳の全行が 8 要素ずつ無ければ止まる。10 回目の報告を 11 回目と見なして打つと、台帳に足した 24 行が止まる側に出た(もとの 16 行は通る)。
2〜10 回目の結果は変わらない(8 回目 5 件、ほか 0 件)。
直した版(起動文の指紋 `3b00f9d0fd9d`)は監査 69 回目で確かめてから印を記録する。
