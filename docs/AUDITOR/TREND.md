# 監査役の傾向(週次。`IMPROVEMENT.md` §4)

| 日付 | 定義の版 | 事例数 | 捕捉率 | 部分 | 見逃し | 負荷(到達件数/成果物、要旨付き) | $/回 | 備考 |
|---|---|---|---|---|---|---|---|---|
| 2026-09-11 | 2cfb5bd(本番) | 28 + 対照 3 | 46.4%(汚染 2 件除外で 46.2%) | 21.4% | 32.1% | 4.8 / 0 件 | 0.41 | 盲検化後の初回。EVAL_2026-09-11b |

---

## プロセス指標(② 2026-09-14、L-169。定義は `docs/AUDITOR/PROCESS_METRICS.md`)

**値は `python3 scripts/trace_metrics.py --rollup` が書く。人が書かない。**
**「判定: 通す」の件数はここに載せない**(Urbach 2014 の自己申告 98% と同じ性質の数字だから)。

| 日付 | 手の数 | P1 ゴール参照率 | P2 対応表の提示率 | P3 主張と出力の同居率 | P4 逐語の即時貼付率 | P5 道具/手 中央値 | P6 指摘の実物反映率 | P7 read-do を開いた | TRACE |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-14 | 102 | **0.0526**(1/19) | **0.1053**(2/19) | **0.5317**(134/252) | **0.1455**(8/55) | **4**(最大 214) | **0.4107**(23/56、近似 v2) | **0 回** | 2026-09-14_fa7bf0d4.json |

**この行がベースラインである。**Haynes 2009 の「6 指標すべて実施 34.2%」にあたる出発点。

## 回避の測定(⑥。**これが無い版は実装しない**)

強制も回避される。WHO の高濃度カリウム撤去の文書は自ら「**病棟での未使用溶液の再蓄積**」を
意図しない結果として挙げ、麻酔ガスの Pin Index Safety System は「foolproof」を意図した設計なのに
**ピンが取り外されて誤接続された事例**が報告されている。
**だから「強制を入れたから解決した」とは書かない。**

| # | 指標 | 数え方 | いま |
|---|---|---|---|
| R1 | 拒否の迂回 | `PreToolUse` の拒否件数と、その直後 5 手以内に別経路で同じ結果を出した件数 | 未計測(TRACE に拒否が載るのは次の会話から) |
| R2 | 機械そのものの変更 | `.claude/hooks/**` / `settings.json` / `.claude/agents/**` の変更件数と**オーナー指示の有無**。**あわせてリポジトリ外の `~/.claude.json` の `hasTrustDialogAccepted` の値**(2026-09-14 の段 0 で、この 1 行だけでフックが全部飛ぶと**書いたが、誤診だった(2026-09-16、L-177 / I-010)。真因は `settings.json` のスキーマ外の鍵で、旗単独の効果は未測定**。**差分にも指紋の台帳にも pytest にも残らない**) | 2026-09-14: 変更 5 件(すべて L-169 の承認あり)/ 信頼フラグ `true` |
| R3 | 指紋の食い違い | `python3 scripts/verify_gates.py` の食い違い件数 | 2026-09-14: **0 件** |
| R4 | 解除ファイル | **機械で数える**: `trace_metrics.py` が道具の入力に `owner_unlock_` が現れた Write/Edit/Bash を数える。**あわせて `.gitignore` の `!.claude/state/owner_unlock_*` で実物が差分に残る(ただし塞がるのは「作って残したまま」だけで、同じ手で「作って通して消す」と差分には出ない。そちらは TRACE の R4 が数える)**(2026-09-14、35 本目の監査。前版は「0 回」とリードが手で書いており、**「判定: 通す」を指標から外したのと同じ性質の自己申告だった**) | 2026-09-14: **7 回**(`verify_gates.py` の一時ディレクトリでの作成と本文中の言及を含む。**実物の解除は差分に 0 件**) |

## 全称語の検査(L-174 選択ウ、L-182。**2026-09-30 まで表示のみ**)

オーナー逐語: 「**フックの変更してOK**」「**ウ: 2 週間測ってから止めに切り替える**」(L-174)/ 「**研究の段 1 の前に作ってください。**」(L-182)。
機械: `.claude/hooks/universal_claim_notice.sh`(Stop)。引き金の語があり、範囲を切っていない検索のコマンドも「範囲を切った」の明記も無い返答で鳴る。
記録: `.claude/state/universal_claim_hits.jsonl`(リポジトリ外。引き金があった返答を 1 行ずつ、`evidence` の真偽つき)。
数え方(週次、リード): `python3 -c "import json;r=[json.loads(l) for l in open('.claude/state/universal_claim_hits.jsonl')];print(len(r),sum(1 for x in r if not x['evidence']))"` → 引き金あり / 鳴った。本物(実際に範囲を切っていた)かは 1 件ずつ返答を読んで下に書く。
**止めに切り替えるのはオーナーの指示があったときだけ(A-16)。**

| 週 | 引き金あり | 鳴った | 本物 | 誤警報率 |
|---|---|---|---|---|
| 2026-09-16〜09-23 | (未計測) | | | |
| 2026-09-23〜09-30 | (未計測) | | | |

## 撤去条件(A-17 への対策。**先に書く**)

**2026-09-28 までに ④ の閾値を置く**(P5 の分布を測ってから。A-12)。
**2026-12-07(12 週後)までに P1〜P7 のいずれも改善していなければ、①〜⑤ を撤去する。**
週次の定期実行がこの期限を過ぎたら、`docs/OWNER_STATUS.md` の「判断が要る項目」に自動で載せる。
**「効果が無かった」を測って書くところまでが対策である。**
