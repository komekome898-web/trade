# 項目 4 要件の固定 — 統合と答え合わせ(参照実装・統合・旧エンジンとの互換)

資料係(要件の固定)。委任文 `docs/DATA/delegations/20260925_backtest_env_prompt.md`(指紋 `20260925_backtest_env_prompt.md@388d55cdeb32`。`sha256sum` で確認済み)§2・§3「要件と判定の固定」「調査結果の側の選び方」に従う。**このファイル以外は書いていない。**

## 0. 起動前の対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 項目 4 の要件ファイルを固定する | L-454「案B。理由は上限5周といえばあなたは5周以上回すからです。最小で回しますが…」(案 B = 項目 4 は最大 2 周で回す決定。この決定の実行として委任文 §2 項目 4・§3「要件と判定の固定」が具体化したもの) |
| 比較の観点をオーナーの完了の形(信頼性と再現性)を軸に立てる | L-405「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」 |
| 調査結果の側の候補を機械的に(人が足し引きせず)全部抜き出す | L-413「**動かせないからと言って機構を検討せず無視することを避ける**」「**(理由なしでスキップは禁止)**」(委任文 §3「調査結果の側の選び方」がこれを「機械で全部抜き出す」と具体化) |
| 項目 4 の通過の判定は「圧倒」(部品の「同等以上」ではない) | L-431「**最終的に全ての部品を組み合わせた結果「圧倒」されればよい**」+ L-432「**案1 項目13で測る (推奨)**」 |

右が全部埋まっているので、擦り合わせ不要でそのまま作成した。起動文の注記(12 回目の再開、L-451/L-454 は着手を止める指示ではないという説明)のとおり、着手を保留せず作った。

## 1. 委任文 §2 の項目 4 の行(逐語)

委任文 §2 の表、項目番号 4 の行をそのまま引く(1 語も削らない):

> | 4 | 統合と答え合わせ(参照実装・統合・旧エンジンとの互換 = 旧 11 + 13 + 14。通過の判定は圧倒) | `src/bot/bt/reference/`・`tests/bt/item_4/reference/`(旧 `tests/bt/item_11/`)、`src/bot/bt/compat/`・`tests/bt/compat/`。最後の段だけ `src/bot/backtest/` の 3 本、全体 | 【旧 11 正しさの答え合わせ】**核を見ずに別の作業者が書く**遅い参照実装との突き合わせ。性質の試験。正解つきの場面。外部の道具を §4 の規則で scratchpad の venv に入れ、同じ**合成**の場面で同じ数が出るかを比べる **/** 【旧 14 旧エンジンとの互換】L-407「**新しいエンジンはそもそも旧エンジンの完全上位互換になっていないと意味がない**」。旧 `src/bot/backtest/` の全挙動(翌足の始値での taker 執行・指値の厳密な通過・足内の TP/SL と STOP 優先・`wick_invalidation`・`maker_tp`・`max_hold_bars`・`entry_mask`/`entry_sides`・`swap_daily_pct`・`missed_fills`・`compute_metrics` の全指標・`split_data`)を新エンジンの選べる模型として再現する。確認 = 旧の試験 8 本の全場面 + 種つきの乱数の場面での旧と新の突き合わせ(一致しなければ通過にしない)。旧の出力は、旧の実装を消す前に旧で作った旧の出力(golden)のファイル(`tests/bt/compat/golden/`、種と入力つき)として残し、以後は新がそれと一致することを試験で見る(旧のコードは取り込まない)。**最後の段**: 突き合わせが全部一致し、批評家と盲検が通ったあとに限り、`src/bot/backtest/` の 3 本の中身を新エンジンへの呼び口に置き換える。置き換えたあと旧の試験 8 本・旧を import している 12 本の読み込みが変えずに通ることを確かめる。1 つでも落ちたら、`git checkout -- src/bot/backtest/` で置き換えを戻し(旧の 3 本を元に戻す)、[止める] として次の周で直す。戻したことと落ちた試験を報告に書く **/** 【旧 13 統合】§1 の実データ(bitFlyer の約定 + 板 top10 の 1 日、Binance aggTrades の 1 日、FX のイベントティック 1 日、JPX と FX の 1 分足)を新エンジンに通し、実行記録・指標の書き出し・ダッシュボードまで。全試験(`PYTHONPATH=src python -m pytest`)。**走らせる戦略は §4 の「動作確認用の固定の手順」だけ** **項目 4 の中の役(L-447)**: 参照実装(旧 11)は核を読まない別の agent が書く(台本の役 `参照実装:4`。項目 4 の 1 周目の作業者の前に起こし、読めるのは §2 の項目 4 の行と要件のファイルだけ。`src/bot/bt/core/` を開いたら [止める] = 批評家が agent の記録ではなく成果物の形(核の内部の名前・構造の写し)で読む(台本の批評家の文の項目 4 の分岐に配線 = 監査 65-3)。項目 4 の作業者は参照実装を読んでよい。監査 64-4)。旧 14 の「最後の段」の条件(突き合わせが全部一致し、批評家と盲検が通ったあとに限り置き換える。落ちたら戻す)はそのまま項目 4 に当てる |

(§2 の見出し行から持ち物の共通の注記: 「1 項目 = 作業者 1 体 + 場面係 1 体(1 周目の前だけ)+ 資料係 1 体 + 批評家 1 体 + 審査員 6 体」。持ち物には `tests/bt/item_4/` を含む(旧エンジンとの互換の部分だけは `tests/bt/compat/`。§3 場面集の規則 7 の特則)。)

**参照実装を書く役(委任文の指示。要件のファイルに書き残す)**: 参照実装(旧 11 に当たる遅い突き合わせ実装)は、この項目 4 の 1 周目の作業者より前に、**核 `src/bot/bt/core/` の実装を読まない別の agent**(台本の役 `参照実装:4`)が書く。その agent が読めるのは委任文 §2 の項目 4 の行と、この要件のファイルだけである。`src/bot/bt/core/` を開いたら批評家が [止める] にする(agent 自身の自己申告ではなく、成果物の形 = 核の内部の名前・構造の写しが出ていないかで判定する)。**項目 4 の作業者(場面係・資料係(表)・批評家自身を含む他の役)は参照実装を読んでよい。** この分離の目的は、核の実装を見ながら書いた参照実装が核のバグをそのまま模倣してしまう(答え合わせにならない)ことを防ぐためである。

**旧 14 の最後の段の実行条件(委任文の行に既にある条件を、要件としてもここに明記する)**: `src/bot/backtest/` の 3 本(`engine.py`・`metrics.py`・`walk_forward.py`)の中身を新エンジンへの呼び口に置き換えてよいのは、(a) 旧の出力(golden)との突き合わせが全部一致し、(b) この周の批評家の [止める] が 0 件になったとき、**の両方を満たしたときだけ**である。置き換えたら、旧の試験 8 本(`tests/test_backtest.py`・`test_engine_maker_exit.py`・`test_maker_execution.py`・`test_max_hold.py`・`test_qa_pipeline_known_answer.py`・`test_short_margin.py`・`test_tp_sl.py`・`test_wick_stop.py`)を**変えずに**回し、1 つでも落ちたら `git checkout -- src/bot/backtest/` で置き換えを戻す(委任文 §2 の項目 4 の行のとおり)。戻したことと落ちた試験は次の周の [止める] として持ち越し、報告に逐語で書く。

## 2. 比較の観点(観点ごとに 1 行、測れる形)

オーナーの完了の形(逐語、L-405)「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」をどの観点でも軸に置く。旧 11(正しさの答え合わせ)・旧 13(統合)・旧 14(旧エンジンとの互換)の逐語の各要素に対応させた。項目 1〜3 で既に固定した観点(データ層・執行の模型・検証/再現/出力)は再測しない(射程外、下記)。

| # | 観点 | 測り方(測れる形) |
|---|---|---|
| I4-1 | 独立参照実装との突き合わせ | 核 `src/bot/bt/core/` を読まない別の agent が書いた、遅くてよい参照実装(素朴な実装。最適化しない)を持ち、同じ入力に対して核ベースの実装と同じ出力(値の場面: 既知の入力 → 突き合わせが一致)を出すか |
| I4-2 | 性質の試験(不変条件) | 実装の場合分けから入力を作らない敵対的な入力の格子で、数量保存・PnL の恒等式・時系列順序の保存などの不変条件が崩れないかを確かめる性質の試験を持つか |
| I4-3 | 正解つきの場面 | 値の場面(手計算またはエンジンを見ない閉じた式で決めた正解)を、核・参照実装・新エンジン全体のそれぞれの粒度に持つか |
| I4-4 | 外部道具との数値突き合わせ(合成データ) | §4 の安全規則(隔離 venv・導入前検査・外部送信なし)で導入した外部道具に、当方と同じ**合成**の場面を通し、同じ数(または文書化された許容誤差内)が出るか |
| I4-5 | 全資産・複数データ源の統合パイプライン一本化 | §1 の実データ(bitFlyer の約定+板 top10 の 1 日、Binance aggTrades の 1 日、FX のイベントティック 1 日、JPX と FX の 1 分足)を、資産種別ごとに別のコードパスを書かずに 1 本の実行で新エンジンに通し、実行記録・指標の書き出し・ダッシュボードの項目別タブまで到達するか |
| I4-6 | 動作確認用の固定手順の遵守 | 実データを通す実行の戦略が「時刻だけで決まる機械的な手順」か「種つき乱数」に限られ、信号・条件付け・最適化を持ち込んでいないか。その実行の指標書き出し全部に目的 `動作確認` が載り、良し悪しの記述が無いか |
| I4-7 | 全試験通過(既存を壊さない) | `PYTHONPATH=src python -m pytest` が全件通るか。既存の試験を落とさない・消さない・飛ばさないか |
| I4-8 | 翌足始値での taker 執行 | シグナルの発生バーの**次の**バーの始値で taker 約定するか(先読み無し)。種つき乱数の場面と旧の試験 8 本の全場面で、旧エンジンと同じ約定価格・タイミングになるか |
| I4-9 | 指値の厳密な通過 | 指値が価格条件を満たしたときだけ約定し、有利な方向へ値をずらさない(黙って有利な価格で埋めない)か。埋まる/埋まらないの判定が旧エンジンと一致するか |
| I4-10 | 足内の TP/SL と STOP 優先順位 | 1 本の足の中で TP と SL の両方の条件を満たしうる場面で、優先順位(STOP 優先)が旧エンジンと同じ規則で決まるか |
| I4-11 | `wick_invalidation`(構造的ストップ) | `stop_mode="wick_invalidation"` + `stop_window_bars` を渡したとき、ウィック(ヒゲ)によるストップの発火とその後の固定挙動(`wick_level` を凍結する)が旧エンジンと一致するか |
| I4-12 | `maker_tp`(指値イグジット) | `exit_execution="maker_tp"` + `maker_tp_pct` を渡したとき、指値の利確とその通過条件(厳密な通過)・maker 手数料の適用が旧エンジンと一致するか |
| I4-13 | `max_hold_bars`(強制タイムイグジット) | ポジションが `max_hold_bars` 本保持されたら、ちょうど bar `entry_bar + max_hold_bars` の始値で taker 決済されるか。境界(ちょうど N 本目)の挙動が旧エンジンと一致するか |
| I4-14 | `entry_mask` / `entry_sides` | per-bar のブールマスクで新規建玉を抑制でき、`entry_sides`(`both`/`long`/`short`)でシグナル発生バー時点での方向制限が旧エンジンと一致するか |
| I4-15 | `swap_daily_pct`(スワップ/資金調達費用) | 建玉保有中、日割りのスワップ費用が保有バー数に応じて開建玉の PnL に反映されるか(`bar_seconds` に基づく按分)。数値が旧エンジンと一致するか |
| I4-16 | `missed_fills`(未約定カウント) | 約定しなかった指値・ストップの件数が `BacktestResult.missed_fills` として旧エンジンと同じ条件・同じ値で数えられるか |
| I4-17 | `compute_metrics` の全指標一致 | `Metrics` の 12 フィールド(`total_pnl_jpy`・`num_trades`・`win_rate_pct`・`profit_factor`・`sharpe_ratio`・`max_drawdown_pct`・`max_consecutive_losses`・`avg_win_jpy`・`avg_loss_jpy`・`risk_reward_ratio`・`expectancy_per_trade_jpy`・`total_fees_jpy`)が、同じ trade_pnls・equity_curve に対し旧エンジンとビット単位で一致するか |
| I4-18 | `split_data`(暦ではない行数割合の 3 分割) | `train_frac`・`val_frac` による行数ベースのシャッフル無しの時系列 3 分割(OOS が末尾固定)が旧エンジンと同じ境界を返すか(項目 3 の暦日 Train/Val/OOS = C3-1 とは別に、互換の呼び口としてこの挙動そのものを保つか) |
| I4-19 | 旧の出力(golden)の保存と一致 | 旧の実装を消す前に、旧エンジンの出力(種と入力つき)を `tests/bt/compat/golden/` に保存したか。以後、新エンジンの互換の口がその golden と一致することを試験(`tests/bt/compat/`)で恒久的に固定しているか |
| I4-20 | 最後の段の安全な置き換えと復元 | 突き合わせ全一致 + 批評家 [止める] 0 + 盲検通過の**すべて**を満たしたときだけ `src/bot/backtest/` の 3 本を新エンジン呼び口に置き換え、置き換え後に旧の試験 8 本・旧を import する 12 本が変えずに通るか。1 つでも落ちたら `git checkout -- src/bot/backtest/` で戻し、戻したことと落ちた試験を報告に書いているか |

**射程外(この項目では測り直さない)**: データ層の読み込み・時刻正規化(項目 1 の V1〜V7)、注文・約定・遅延・費用・口座の個別の模型そのものの品質(項目 2 の C2-1〜C2-12)、検証・統計手続き・実行記録の形式・ダッシュボードの個別タブの中身(項目 3 の C3-1〜C3-19)は、それぞれの項目で既に固定・判定済みであり、ここでは**それらの部品が 1 本の統合されたパイプラインとして繋がっているか**(I4-5)と、**旧エンジン固有の足バックテストの挙動を新エンジンが選べる模型として再現できているか**(I4-8〜I4-18)だけを見る。個別部品の内部の正しさを再判定すると項目の切り分け(L-447 の 4 項目化)が崩れるため、範囲を分けた理由をここに明記する(A-10 への牽制)。

## 3. 調査結果の側(候補は機械で全部抜き出す。人が足し引きしない)

### 3.0 tsv に該当列があるか(観点群ごとに確認)

`docs/DATA/tools_catalog.tsv` の全 19 列(実測、`head -1 docs/DATA/tools_catalog.tsv | tr '\t' '\n' | cat -n`):
```
 1 番号  2 名前  3 到達  4 足  5 ティック  6 板の待ち行列  7 イベント駆動  8 ベクトル化
 9 市場影響と約定の模型  10 段(機構)  11 段(既定)  12 遅延  13 取り消しの扱い
14 他の区分へ  15 安全側の所見  16 尽きていない理由  17 報告書の最後の記載の行
18 段の表の行  19 遅延(原文)  20 取り消しの扱い(原文)
```

- **I4-1〜I4-4(参照実装・性質の試験・正解つきの場面・外部道具との突き合わせ)**: これは「答え合わせの方法論」であり、道具の機構(板・約定・遅延など)を表す列ではない。**該当する列は無い**。→ §3.1 で SCAN への grep。
- **I4-5〜I4-7(統合パイプライン・動作確認手順・全試験)**: 当方の内部のつなぎ方と規律であり、これも機構の列ではない。**該当する列は無い**。→ §3.2 で SCAN への grep(I4-7 は内部制約のため §3.4 で不実施の理由を書く)。
- **I4-8〜I4-18(旧 14 = 旧エンジンの足バックテストの挙動そのものの再現)**: 委任文 §3「項目 4 の場面(旧 13・14)」に明記された選び方 ——「調査結果の側は、足のバックテストを持つ道具を同じ場面に通す」。これは**列 4(足)に印がある行**という具体の基準そのものである。→ §3.3 で列 4 を機械抽出。

**この節にも道具台帳 §2.1 の「段(機構)」の尺度は元から存在しない(段は市場影響と約定の模型=項目 2 専用の尺度)。** I4-8〜I4-18 のどの観点も、旧エンジンの足バックテスト特有の挙動(翌足始値執行・wick_invalidation 等)であって「指値の埋まり方の高さ」を測る段の物差しの対象ではないため、「段が最も高い機構」という形の順位は付けない(根拠の無い尺度を新設すると A-12 に触れる)。委任文 §3「調査結果の側の選び方」の場合分けに当てはめると、I4-8〜I4-18 は全部**段の無い観点**であり、動かせる候補は原則「全部を再現する」対象になる(場面係の役)。

### 3.1 I4-1〜I4-4: SCAN への grep(語を先に書いてから打った)

事前にノイズを除いた SCAN の作業用コピーを使う(項目 2・3 の資料係と同じ問題: `SCAN_2026-09-21_tools.md` の各回冒頭に挟まる `### 当方の道具立て` 節が当方自身のスクリプト名にノイズマッチするため、行番号を保ったまま空行に置換したもの)。

```
awk '
/^### 当方の道具立て/ {skip=1; print ""; next}
skip && /^### / {skip=0}
{ if (skip) print ""; else print }
' docs/DATA/SCAN_2026-09-21_tools.md > SCAN_clean.md
```
(置換先: `/tmp/claude-0/-home-user-trade/220780c0-d897-5de0-a902-2af69538ba02/scratchpad/bt/item4_shiryo_scan_clean.md`。10,694 行のまま、`wc -l` で元ファイルと一致することを確認済み。)

| # | grep 語(先に決めた) | コマンド | 当たり件数 |
|---|---|---|---|
| I4-1 | `reference implementation`・`golden (test\|file\|output)`・`known.?answer`・`known.?good` | `grep -inE 'reference implementation\|golden (test\|file\|output)\|known.?answer\|known.?good' SCAN_clean.md` | 0 |
| I4-2 | `property.?(based\|test)`・`hypothesis`・片仮名`プロパティベース`・`プロパティテスト`・`ハイポセシス` | `grep -inE 'property.?(based\|test)\|hypothesis' SCAN_clean.md` / `grep -inE 'プロパティベース\|プロパティテスト\|ハイポセシス' SCAN_clean.md` | 2(英語版のみ。片仮名 0 件) |
| I4-3 | I4-1 と同じ語(正解つきの場面 = known-answer) | (I4-1 のコマンドと同じ) | 0 |
| I4-4 | `verified against`・`等価性`・`drop-in replacement`・`API.?compatib`・`cross.?check`・`クロスチェック`・`benchmark against`・`答え合わせ`・`regression test`・`test vector`・`テストベクタ` | `grep -inE 'verified against\|等価性\|drop-in replacement\|API.?compatib' SCAN_clean.md` / `grep -inE 'cross.?check\|クロスチェック\|benchmark against\|答え合わせ' SCAN_clean.md` / `grep -inE 'regression test\|test vector\|テストベクタ' SCAN_clean.md` | 0 / 0 / 0 |

**当たった候補(全部載せる。人が足し引きしていない)**:

I4-2 の 2 件はどちらも `DaniyalMlk/slippage`(候補 105、SCAN 5186・5209 行)の**開発用依存の一覧**「numpy>=1.23(実行時)。開発用は pytest>=7.4 / hypothesis>=6.80 / mypy>=1.8 / ruff>=0.16,<0.17 / scipy>=1.10」(実測、`docs/DATA/probes/20260922_tools_1_run16.log:578`)であり、**この道具自身が性質の試験(不変条件テスト)の機構を持つという記述ではなく、その道具の開発者が `hypothesis` ライブラリを CI 用に使っているという依存関係の事実**である。委任文の観点 I4-2 は「性質の試験を実装が持つか」を問うので、この 1 件は候補として数えない(0 件のまま。開発依存の存在と、道具自身が性質ベースの検証機構を外部に提供することは別の主張であり、混同すると O-6 = 出所の分からない数字・意味の分からない文章 と同じ誤りになる)。

**I4-1・I4-3・I4-4 は候補 0 件、I4-2 も実質 0 件。** これは「この材料(区分 1: バックテスト・シミュレーション道具のサーベイ)の範囲では 0 件」であって「世の中にこの種の機構を持つ道具が存在しない」ではない(CLAUDE.md §5.2「無い・取れない」の規則。この SCAN は道具の機構調査であり、答え合わせの方法論そのものを主題にした調査ではないため、無いことは驚きではない)。I4-1・I4-3・I4-4(参照実装・正解つきの場面・外部道具との数値突き合わせ)は、委任文 §3「動かせない候補の検討と再現」・「調査結果の側の選び方」の対象が本質的に「エンジンの機構」であって「検証方法論」ではないため、候補が無くても当方の実装がこれらの観点を満たすかどうかは場面(値の場面・能力の場面)で直接判定できる(場面集の規則 2「振る舞いを試し、作りの形を試さない」の範囲内)。

### 3.2 I4-5・I4-6: SCAN への grep

| # | grep 語(先に決めた) | コマンド | 当たり件数 |
|---|---|---|---|
| I4-5 | `end.?to.?end`・`pipeline`・`統合テスト`・`integration test`・`複数資産`・`マルチアセット`・`multi-asset` | `grep -inE 'end.?to.?end\|pipeline\|統合テスト\|integration test' SCAN_clean.md` / `grep -inE '複数資産\|マルチアセット\|multi-asset\|asset class' SCAN_clean.md` | 4 / 1 |
| I4-6 | `dry.?run`・`smoke.{0,3}test`・`paper.{0,3}(mode\|trading)`・`動作確認` | `grep -inE 'dry.?run\|smoke.{0,3}test\|paper.{0,3}(mode\|trading)' SCAN_clean.md` / `grep -inE '動作確認' SCAN_clean.md` | 11 / 1(1 件は本文自身の「動作確認」という語であって候補の記述ではない。後述) |

**I4-5 の当たり(候補番号は tsv の名前列と突き合わせて確認した)**:
- **候補 121 `mhallsmoore/qstrader`**(SCAN 599 行)。PyPI description 逐語「a free Python-based open-source modular schedule-driven backtesting framework for long-short equities and ETF based systematic trading strategies」「loosely-coupled collection of modules for carrying out **end-to-end backtests** with realistic trading mechanics」。tsv 上は `到達済み`、足=○、市場影響と約定の模型の列・段の列は空欄(段の機構は確かめていない)。
- **候補 21 `Qlib`**(SCAN 739・754 行)。GitHub 要約「full ML **pipeline** of data processing, model training, back-testing」「the entire chain of quantitative investment: alpha seeking, risk modeling, portfolio optimization, and order execution」。同じ節に「当方に無いもの」として「一気通貫の ML パイプラインという統合された枠組み」と明記。tsv 上は `到達済み`、足=○、段(既定)=1、他の区分へ「区分4・区分5」。
- **候補 93 `punyamodi/binary_market_engine`**(SCAN 4681 行)。「End-to-end algorithmic trading system for binary prediction markets」。**tsv 上は `未到達`**(動かせない候補として扱う。§3「動かせない候補の検討と再現」の対象)。
- 複数資産(`multi-asset`)の語での当たり: **候補 1 `hftbacktest`**(SCAN 212 行前後)「Backtesting of multi-asset and multi-exchange models」。ただし同じ節に「現状 Binance Futures と Bybit のライブ実行のみ対応(Rust限定)で、bitFlyer 等の国内取引所には触れていない」とも明記(マルチアセットを謳うが、bitFlyer FX_BTC_JPY 対応の記述は無い)。tsv 上は `到達済み`、足=○。

**I4-6 の当たり(項目 3 の資料係が C3-15 で見つけたものと同じ集合。ここでも独立に確認した)**:
- **候補 75 `Freqtrade`**(SCAN 300 行)「Dry-run: Run the bot without paying money」「Backtesting: Run a simulation of your buy/sell strategy」。
- **候補 19 `Jesse`**(SCAN 1198 行)「本体(バックテスト・最適化)は上限の記述なし。実弾・ペーパーは license が要る」。**候補 19 は道具台帳 §3 の危険な兆候 11 件の 1 つ**(導入も実行もしない)。
- **候補 8 `OpenTrader`**(SCAN 3823 行)「Paper Trading: Test your strategies without risking real money」。
- **候補 12 `pybotters`(否定側)**「paper や dry-run の模擬は道具の側に無い」(実測)。
- 「動作確認」の直接語の当たり 1 件は本文が自分自身の作業ログの語(調査班の記録)であり、道具の機構の記述ではないため候補に数えない。

**いずれも「実行モードの切替」であって、当方の要求(実データを通す実行の戦略を時刻だけで決まる機械的手順・種つき乱数に限り、指標書き出しに目的ラベル `動作確認`/`研究` を構造的に強制する)とは形が違う**(項目 3 の資料係の C3-15 の記載と同じ判断。場面係が能力の場面として採用するかどうかの判断に委ねる)。

### 3.3 I4-8〜I4-18: 列 4(足)= ○ の行を機械抽出

コマンド(そのまま):
```
awk -F'\t' 'NR>1 && $4=="○" {print $1"\t"$2"\t"$3"\t"$11"\t"$14}' docs/DATA/tools_catalog.tsv
```
(列 1 番号・列 2 名前・列 3 到達・列 11 段(既定)・列 14 他の区分へ、を並べた。列 4 = 足。値は空白・`×`・`○` のいずれかで、`○` のみ抽出。)

抽出結果(55 件、人の取捨てなし):

| 番号 | 名前 | 到達 | 段(既定) | 他の区分へ |
|---|---|---|---|---|
| 1 | Basana | 到達済み | 4・6 | 区分2 |
| 2 | Backtrader | 到達済み | 1 | 区分2 |
| 3 | PySystemtrade | 到達済み | 2 | 区分2 |
| 4 | PyBroker | 到達済み | 1 | 区分2・区分4 |
| 5 | Lean CLI | 到達済み | | |
| 6 | Ziplime | 到達済み | 4 | 区分2・区分3・区分6 |
| 7 | Superalgos | 到達済み | 1 | 区分2 |
| 8 | OpenTrader | 到達済み | 1 | 区分2 |
| 10 | fast-trade | 到達済み | | 区分3・区分4・区分6 |
| 11 | python3(道具名として台帳に記載) | 到達済み | | 区分2・区分6 |
| 13 | DeviaVir/zenbot | 到達済み | 3・4 | 区分2 |
| 15 | Mendl-Labs/BacktestingCore | 到達済み | 6(推定) | |
| 16 | Luczinsritter/event_driven_backtesting_engine | 到達済み | | |
| 18 | zipline-reloaded | 到達済み | 4 | |
| 19 | Jesse | 到達済み | 2 | 区分2・区分6 |
| 20 | VnPy | 到達済み | 2 | 区分2 |
| 21 | Qlib | 到達済み | 1 | 区分4・区分5 |
| 40 | OpenMarket | 到達済み | | |
| 43 | QuantDinger | 到達済み | | 区分2・区分6 |
| 44 | lo2cin4 | 到達済み | | 区分4 |
| 45 | ForexTester | 到達済み | 2 | |
| 46 | MT4裁量トレード練習君プレミアム | 到達済み | | |
| 48 | BacktestingMax | 到達済み | | |
| 50 | AlgoTest | 到達済み | 1 | |
| 51 | QUANTAXIS | 到達済み | 0 | 区分2・区分3・区分5 |
| 52 | QuantConnect | 到達済み | | 区分2 |
| 53 | Rqalpha | 到達済み | 4 | 区分2 |
| 54 | finmarketpy | 到達済み | 該当なし | 区分4 |
| 55 | backtesting.py | 到達済み | | |
| 56 | zvt | 到達済み | | |
| 57 | WonderTrader | 到達済み | 3・4・5 | 区分2 |
| 58 | nautilus_trader | 到達済み | | 区分2 |
| 60 | Hikyuu | 到達済み | 2 | 区分4 |
| 61 | barter-rs | 到達済み | | 区分2 |
| 62 | qf-lib | 到達済み | | 区分3・区分5・区分8 |
| 67 | lumibot | 到達済み | 2 | 区分2 |
| 68 | quanttrader | 到達済み | | 区分2 |
| 69 | gobacktest | 到達済み | | |
| 70 | PineForge | 到達済み | 2・3 | 区分8 |
| 72 | QTradeX | 到達済み | 1 | 区分2 |
| 73 | vectorbt | 到達済み | | |
| 74 | ml-quant-trading | 到達済み | | 区分4 |
| 75 | Freqtrade | 到達済み | | 区分2・区分4 |
| 80 | Hummingbot | 到達済み | | 区分2 |
| 85 | czsc | 到達済み | | 区分4 |
| 86 | analyzingalpha | 到達済み | | |
| 87 | PyTrendFollow | 到達済み | 該当なし | 区分2 |
| 92 | prediction-market-backtester | 到達済み | 0 | 区分8 |
| 94 | mote/backtest | 到達済み | 2 | |
| 107 | sigc | 到達済み | | 区分4 |
| 111 | HKUDS/Vibe-Trading | 到達済み | 0(暗号資産の機関)・1(中国株の機関) | 区分3・区分6 |
| 120 | wbt | 到達済み | | |
| 121 | mhallsmoore/qstrader | 到達済み | | |
| 122 | gbeced/pyalgotrade | 到達済み | | |
| 123 | carlos8f/zenbot | 到達済み | 3・4 | 区分2 |

**このうち道具台帳 §3 の危険な兆候 11 件(44・74・120・41・58・19・112・111・97・51・118)に含まれるもの**: 44(lo2cin4)・74(ml-quant-trading)・120(wbt)・58(nautilus_trader)・19(Jesse)・111(HKUDS/Vibe-Trading)・51(QUANTAXIS)の**7 件**。委任文 §4「道具台帳 §3 の 11 件は導入も実行もしない」により、この 7 件は**動かせない候補**として扱う(検討表は (a) 書き写しの範囲だけで作り、導入・実行はしない)。

**この 55 件のどれを実際に動かし、旧 14 の具体的な挙動(翌足始値執行・`wick_invalidation`・`maker_tp` 等)のどれを再現できるかの判定・実行は、委任文の役割分担どおり場面係の仕事である(この資料係の役割は候補集合を機械的に確定するところまで)。** 段の尺度が無い観点なので、動かせる候補は原則として全部を場面に通し(委任文「段の無い観点」の規則)、「調査結果から明らかに弱いことがわかっている機構」だけを理由つきでスキップしてよい。

### 3.4 内部制約の観点(I4-7・I4-19・I4-20)は SCAN 対象外

I4-7(`pytest` 全件通過)・I4-19(旧の出力 golden の保存手順)・I4-20(最後の段の置き換えと復元の手順)は、**外部の道具の機構と比較する性質の観点ではなく、当方の開発・移行の手続き上の制約**(委任文 §2 の項目 4 の行そのものが定める手順、および CLAUDE.md の試験規律)である。他のどんな道具も「旧 `src/bot/backtest/` という当方固有のファイルを新エンジンの呼び口に安全に置き換える手順」を持ちようがなく、SCAN に語を探しても意味が無いため、grep は打っていない(打たない理由をここに明記することで A-9「説明なく外す」を避ける。§2 の観点表からは外していない)。この 3 観点は「調査結果の側」の欄を持たず、当方の実装がその手順どおりに行われたか(git の操作ログ・試験の実行結果)だけで直接判定する。

## 4. 当方の現状(該当箇所)

現行の唯一の実装は旧エンジン `src/bot/backtest/{engine,metrics,walk_forward}.py`(3 本、計 525 行)。新エンジン側は項目 0(核)・項目 1(データ/ベクトル)・項目 2(執行の模型)・項目 3(検証/再現/出力/ダッシュボード)が着手済みだが、**項目 4 の持ち物 `src/bot/bt/reference/`・`src/bot/bt/compat/`・`tests/bt/item_4/`・`tests/bt/compat/` はすべて未着手**(`find src/bot/bt/reference src/bot/bt/compat`・`find tests/bt/compat` の実行結果はいずれも「該当なし」)。旧を import している 12 本のスクリプトと旧の試験 8 本は実測で確認済み(`grep -rl "bot\.backtest" --include=*.py .` の実行結果、後述)。

| 委任文の要素 | 当方の現状 | ファイル:行 |
|---|---|---|
| 独立参照実装(I4-1) | 無い。`src/bot/bt/reference/` は存在しない。項目 0 の盲検比較で使った外部道具のアダプタ(`tests/bt/battery/item_0/opponents/*.py`、`repro_lean52.py` など)はあるが、これらは「核を読まない別の agent が書いた遅い参照実装」ではなく、外部道具を当方の場面に接続するアダプタである | `src/bot/bt/reference/`(該当なし)。`tests/bt/battery/item_0/opponents/`(性質が異なる既存物) |
| 性質の試験(I4-2) | 項目 0 の核には性質の試験に相当するもの(`tests/bt/battery/item_0/mutant.py`、敵対的格子の試験群 `tests/bt/critic/item_0/test_i0r*_*.py` 多数)があるが、これは核の契約についての試験であり、項目 4(統合された新エンジン全体・旧エンジンとの互換)を対象にした性質の試験ではない | `tests/bt/battery/item_0/mutant.py`。`src/bot/bt/reference/`・`tests/bt/item_4/`(該当なし) |
| 正解つきの場面(I4-3) | 項目 0〜3 それぞれの `tests/bt/battery/item_<N>/` に値の場面・能力の場面があるが、項目 4(統合・答え合わせ・旧互換)専用の場面集は無い | `tests/bt/battery/item_1,2,3/`(既存、項目 4 の対象外)。`tests/bt/battery/item_4/`(該当なし) |
| 外部道具との数値突き合わせ・合成データ(I4-4) | 項目 0〜3 それぞれの盲検比較で外部道具を走らせた実績(`docs/DATA/probes/2026092*_tools_1_run*.log`)はあるが、項目 4 として「統合された新エンジン全体」を外部道具と突き合わせた実績は無い | `tests/bt/battery/item_0/opponents/`(既存、性質が異なる) |
| 全資産・複数データ源の統合パイプライン(I4-5) | 無い。`src/bot/bt/core/`・`data/`・`vector/`・`orders/`・`fill/`・`latency/`・`costs/`・`portfolio/`・`validation/`・`repro/`・`report/` の各パッケージは項目 0〜3 で個別に実装されているが、それらを 1 本の実行として繋ぐ最上位のドライバ(旧エンジンの `run_backtest` に相当する、資産種別を問わず 1 呼び出しで通す口)は `src/bot/bt/` 直下に存在しない(`find src/bot/bt -maxdepth 1 -type f` の結果は `__init__.py` のみ) | `src/bot/bt/`(統合ドライバ該当なし。`__init__.py` のみ) |
| 動作確認用の固定手順の遵守(I4-6) | 無い(統合パイプライン自体が無いため、実データを通す実行という単位も無い) | 同上 |
| 全試験通過(I4-7) | 新エンジンの試験群(`tests/bt/item_0〜3/`・`tests/bt/battery/item_0〜3/`・`tests/bt/critic/item_0/`)と旧エンジンの試験 8 本は現時点でそれぞれ独立に存在し、統合後に全部が同時に通ることは未確認(統合ドライバが無いため検証もできない) | `tests/bt/`(既存の項目別試験群)。統合後の全体試験は未着手 |
| 翌足始値での taker 執行(I4-8) | 旧エンジンに実装済み。`i` バー目のシグナルは `i+1` バーの始値で taker 約定する(先読み防止の設計) | `src/bot/backtest/engine.py:282-291`(コメント「the fill is bar i's open with taker costs」と該当ロジック) |
| 指値の厳密な通過(I4-9) | 旧エンジンに `_PendingLimit` の仕組みがあるが、新エンジン側(`src/bot/bt/orders/`・`fill/`)に旧と同じ厳密通過の互換の口(旧の呼び口と同じ挙動を選べる模型)は無い | `src/bot/backtest/engine.py:128-131`(`class _PendingLimit`)。`src/bot/bt/fill/`(互換の口は未実装) |
| 足内の TP/SL と STOP 優先(I4-10) | 旧エンジンに実装済み(`close_position` の `reason` に `"stop_loss"`/`"take_profit"`/`"wick_stop"` 等の区別があり、優先順位はコード順で決まる) | `src/bot/backtest/engine.py:219`(`stop_mode` 分岐)、`:259,264,290,319,323,327,338`(各 `reason=` の呼び出し) |
| `wick_invalidation`(I4-11) | 旧エンジンに実装済み(`stop_mode="wick_invalidation"` + `stop_window_bars`、`wick_level` を凍結する構造的ストップ) | `src/bot/backtest/engine.py:55-77`(docstring)、`:160-169`(バリデーション)、`:192`(`wick_level` フィールド)、`:219`(分岐) |
| `maker_tp`(I4-12) | 旧エンジンに実装済み(`exit_execution="maker_tp"` + `maker_tp_pct`) | `src/bot/backtest/engine.py:34-37`(docstring)、`:170-176`(バリデーション)、`:305-327`(実行ロジックと `reason="maker_tp"`) |
| `max_hold_bars`(I4-13) | 旧エンジンに実装済み(`entry_bar + max_hold_bars` の始値で強制決済) | `src/bot/backtest/engine.py:18-20`(docstring)、`:158-159`(バリデーション)、`:332-335`(発火条件) |
| `entry_mask`/`entry_sides`(I4-14) | 旧エンジンに実装済み | `src/bot/backtest/engine.py:78-83`(docstring)、`:151-152,177-185`(引数とバリデーション)、`:247-249`(方向制限の適用) |
| `swap_daily_pct`(I4-15) | 旧エンジンに実装済み(`bar_seconds` に基づく日割り按分) | `src/bot/backtest/engine.py:16`(docstring)、`:144`(引数)、`:201`(`swap_per_bar` の計算) |
| `missed_fills`(I4-16) | 旧エンジンに実装済み(`BacktestResult.missed_fills` として返す) | `src/bot/backtest/engine.py:124`(フィールド定義)、`:200`(初期化)、`:364,388`(カウントアップ)、`:398`(返り値) |
| `compute_metrics` の全指標(I4-17) | 旧エンジンに実装済み。12 フィールド全部を持つ | `src/bot/backtest/metrics.py:10-23`(`class Metrics`)、`:29`(`compute_metrics` シグネチャ) |
| `split_data`(I4-18) | 旧エンジンに実装済み(行数割合・シャッフル無し・OOS 末尾固定) | `src/bot/backtest/walk_forward.py:14-32`(`class Splits`・`split_data`) |
| 旧の出力(golden)の保存(I4-19) | 無い。`tests/bt/compat/golden/` は存在しない。旧エンジンはまだ削除されていない(現存する)ので、golden の生成自体はいつでも可能な状態 | `tests/bt/compat/`(該当なし)。`src/bot/backtest/`(現存、削除前) |
| 最後の段の置き換えと復元手順(I4-20) | 無い(統合・答え合わせが未着手のため、置き換えの実行自体がまだできない段階) | `src/bot/backtest/`(現存。3 本とも未変更) |
| 旧を import している 12 本のスクリプト | 実測(`grep -rl "bot\.backtest" --include=*.py . \| grep -v "/src/bot/backtest/" \| grep -v __pycache__` の結果からテスト 8 本と項目 0〜3 のアダプタ 5 本を除いた集合): `scripts/research_basis.py`・`research_anchor.py`・`research_fx.py`・`run_backtest.py`・`research_tournament.py`・`qa/pipeline_known_answer_taker.py`・`research_legacy_elements.py`・`research_mainbot_exits.py`・`research_user_strategies.py`・`research_anchor_v2.py`・`validate_composite.py`・`research_signals.py`(ちょうど 12 本、委任文の記述と一致) | `scripts/`(上記 12 ファイル) |
| 旧の試験 8 本 | 実測(委任文 §4 に列挙された 8 本の存在を `ls` で確認済み): `tests/test_backtest.py`・`test_engine_maker_exit.py`・`test_maker_execution.py`・`test_max_hold.py`・`test_qa_pipeline_known_answer.py`・`test_short_margin.py`・`test_tp_sl.py`・`test_wick_stop.py` | `tests/`(上記 8 ファイル、存在確認済み) |
| ダッシュボードの統合到達点(I4-5 の一部) | **項目 3 の成果で既に着手済み**: 「バックテスト」タブ(`tab-backtest`)と API ルート `/api/backtest/runs`・`/api/backtest/run/<id>` が存在する。ただし、項目 4 の統合パイプライン(実データを新エンジンに通して実際にこの API へ到達するか)は未確認 | `scripts/dashboard.py:211`(`<button id="tab-backtest">`)、`:747,757`(fetch 呼び出し)、`:1413-1417`(`do_GET` のルーティング) |

## 5. 提出前の吟味(委任文 §3「提出前の吟味」。最初に作る役の文)

固定した要件・§2 の観点(I4-1〜I4-20)を読み直し、「非常に厳しい監査役・批評家なら何を [止める] にするか」を観点ごとに列べ、返す前に潰した。

1. **I4-1〜I4-4・I4-5 の一部・I4-6 が「候補 0 件」または弱い候補しかないまま出している。**「調査せずに『無い』と断定している」(O-2)と見えないか。→ **潰した対応**: すべての観点で grep 語を先に書いてから実際にコマンドを打ち、コマンドと出力(当たり件数)をこのファイルに残した(O-3)。I4-2 の「hypothesis」の 2 件がある候補自身の性質試験機構ではなく開発依存の記述であることを個別に確認し、単純に「1 件ヒット」で済ませず、なぜ候補として数えないかを書いた。**この SCAN は区分 1(バックテスト・シミュレーション道具)のサーベイであり、検証方法論(参照実装・golden・property test)を主題にした調査ではない。**したがって「候補 0 件」は「この材料の範囲では 0 件」であって「世の中に存在しない」ではないことを明記した(CLAUDE.md §5.2)。
2. **I4-8〜I4-18 の「調査結果の側」を、列 4(足)= ○ の 55 件全部という粗い集合のまま置いていて、観点ごとに絞り込んでいない。**「機構を検討せず一括りにしている」と見えないか。→ **潰した対応**: 委任文 §3「項目 4 の場面(旧 13・14)」が明示的に「調査結果の側は、足のバックテストを持つ道具を同じ場面に通す」と書いており、旧 14 の観点群(I4-8〜I4-18)は個別の機構ごとに専用の tsv 列を持たない(段の尺度も無い)ため、単一の候補プール(足=○)を全観点で共有するのが委任文の指示どおりであることを §3.0・§3.3 に明記した。**各観点でどの候補が実際にその機構を持つかを判定・再現する作業は場面係の役**(委任文の役割分担)であることを明記し、資料係の役割(候補集合の機械的確定)を超えて先取りしていない。
3. **危険な兆候 11 件のうち 7 件(44・74・120・58・19・111・51)が候補集合(足=○)に混入したまま出している。**「危険な道具を比較対象に入れている」と見えないか。→ **潰した対応**: §3.3 でこの 7 件を名指しし、委任文 §4「道具台帳 §3 の 11 件は導入も実行もしない」に従って「動かせない候補」として扱うことを明記した(検討表は書き写しの範囲だけ、導入・実行はしない)。
4. **I4-5 で見つけた候補(121 QSTrader・21 Qlib)は「end-to-end」「pipeline」という語を持つだけで、bitFlyer FX_BTC_JPY を含む当方と同じ 4 種のデータ源(暗号資産・FX・JPX)を 1 本で統合しているかどうかは未確認のまま出している。**過大評価に見えないか。→ **潰した対応**: 各候補の記述をそのまま逐語で引用し、hftbacktest(候補 1)については「マルチアセットを謳うが bitFlyer 等の国内取引所対応は書かれていない」という限界も併記した。一次資料未読・要約のみという限界を隠さず書いた。
5. **I4-6 の当たり(Freqtrade・Jesse・OpenTrader・pybotters)は項目 3 の資料係が C3-15 で既に見つけた集合と同じであり、「使い回しているだけで自分で調べていない」と見えないか。**→ **潰した対応**: 同じ grep 語・コマンドを自分で実際に打ち直し、当たり件数・行番号を自分の実行結果として確認した上で「項目 3 と同じ集合になった」ことを明記した(独立に確認した事実と、結果が一致した事実を分けて書いている。数値を借りてきて確認せずに転記してはいない)。
6. **I4-7・I4-19・I4-20 に SCAN の grep を打たなかった理由が、単なる手抜きに見えないか。**→ **潰した対応**: これらが「外部道具の機構比較」ではなく当方固有の移行手続き(委任文 §2 の項目 4 の行そのものが定める手順)であることを §3.4 に独立の節として明記し、打たない理由を書くことで A-9(説明なく外す)を避けた。観点表(§2)からは外していない。
7. **§4「当方の現状」で、項目 3 が既に作ったダッシュボードの「バックテスト」タブと API ルートの存在を見落として「無い」と書きかけていないか。**→ **潰した対応**: `scripts/dashboard.py` を実際に grep し、`tab-backtest`・`/api/backtest/runs`・`/api/backtest/run/<id>` が既に存在することを確認して file:line で明記した(A-11「目視・ぱっと見で判定する」への牽制)。ただし「その API に実データの統合実行が実際に到達するか」は別の問題であり、未確認とはっきり書いた(確認できることと確認できないことを混同しない)。
8. **参照実装(旧 11)を書く役の分離規則(核を読まない別 agent)が、要件のファイルの本文に埋もれて見落とされないか。**→ **潰した対応**: §1 に独立した見出し(「参照実装を書く役」)を立て、委任文の逐語(L-447 由来の記述)をそのまま引用し、批評家が何を [止める] にするか(核の内部の名前・構造の写しが成果物に出ていないか)まで明記した。
9. **I4-20(最後の段の置き換え)の実行条件を、要件のファイルの中で 1 か所にしか書いていないと、周回の途中で条件が緩められる(A-6 と同種の事故)危険があるのでは。**→ **潰した対応**: §1(委任文の行の直後)と §2(観点表の I4-20)の 2 か所に、同じ条件(golden 全一致 + 批評家 [止める] 0 + 盲検通過の全部を満たしたときだけ、1 つでも試験が落ちたら `git checkout -- src/bot/backtest/` で戻す)を重複して明記し、周回の途中でこの条件を緩めたり厳しくしたりしないことを確認できるようにした(委任文 §3「要件と判定の固定」の「周回の途中で要件・観点・通過の判定を緩めない、後から厳しくもしない」に対応)。
