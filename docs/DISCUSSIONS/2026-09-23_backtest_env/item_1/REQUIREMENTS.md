# 項目 1「データと時刻」— 要件の固定

資料係(要件の固定)。委任文 `docs/DATA/delegations/20260925_backtest_env_prompt.md`
(指紋 `20260925_backtest_env_prompt.md@388d55cdeb32`。`sha256sum` で確認済み。**2026-09-26 追記で更新、理由は末尾「6. 委任文の版の食い違いの直し」参照**)§3「要件と判定の固定」
「調査結果の側の選び方」に従って書く。**このファイルは一度固定したら書き直さない**
(委任文の版と食い違う箇所が出たときだけ末尾に追記する)。

【リードの注記(12 回目の再開)への対応】起動文に付いていた注記どおり、着手を保留せず作った。
着手前に聞くべき空欄は無かった(下の対応表参照)。

## 0. 起動前の対応表(CLAUDE.md §0.1)

| やろうとすること | オーナーの原文の該当語(逐語) |
|---|---|
| 項目 1 の要件ファイルを固定する | L-454「案B。理由は上限5周といえばあなたは5周以上回すからです。最小で回しますが…」(案 B = 項目 1〜3 は 1 周ずつ最小で回す決定。この決定の実行として委任文 §2 項目 1・§3「要件と判定の固定」が具体化したもの) |
| 比較の観点をオーナーの完了の形(信頼性と再現性)を軸に立てる | L-405「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」 |
| 調査結果の側の候補を機械的に(人が足し引きせず)全部抜き出す | L-413「**動かせないからと言って機構を検討せず無視することを避ける**」「**(理由なしでスキップは禁止)**」(委任文 §3「調査結果の側の選び方」がこれを「機械で全部抜き出す」と具体化) |

右が全部埋まっているので、擦り合わせ不要でそのまま作成した。

## 1. 委任文 §2 項目 1 の行(逐語)

> | 1 | データと時刻(旧 1 + 12) | `src/bot/bt/data/`、`src/bot/bt/vector/` | 【旧 1 データ層】§1 の全資産の読み込み。時刻の単位の正規化(秒・ミリ・マイクロ・ISO)。重複・逆行・欠落・世代間の値の食い違いの検出(黙って結合しない)。sha256 の記録。合成・中間物・封印区間を拒む許可一覧。JPX 個別株の分割・併合・上場廃止・コード変更の調整と、生存者の偏りを避ける銘柄集合の型(データが無いものは §4「無い・取れない」の規則で書く) **/** 【旧 12 足のベクトル化の近道】足の研究を速く回す道。**同じ足で事象駆動の結果と一致することを試験で固定** |

持ち物: `src/bot/bt/data/`、`src/bot/bt/vector/`(+ `tests/bt/item_1/`)。

## 2. 比較の観点(観点ごとに 1 行、測れる形。軸 = オーナーの完了の形「すべてが調査結果以上の信頼性と再現性に優れたもの」L-405)

委任文 §2 の項目 1 の行を、正しさ(信頼性)と再現性で測れる単位に割った。各観点は場面係が
`tests/bt/battery/item_1/` に値の場面 ≥1・能力の場面を置いて判定する(委任文「場面集の規則」1〜3)。

| # | 観点 | 測れる形(合否の基準) |
|---|---|---|
| V1 | 全資産の読み込み(§1 の全資産: bitFlyer/Binance 等の暗号資産、FX、JPX) | 同じ読み込み口に、資産種別の異なる合成データ(暗号資産のティック・FX のイベントティック・JPX の 1 分足の形)を渡したとき、資産ごとに専用のパーサを書かずに共通の内部表現(事象)へ変換できるか。変換後の件数・列が入力と一致するかを場面で判定 |
| V2 | 時刻の単位の正規化(秒・ミリ・マイクロ・ISO) | 秒/ミリ秒/マイクロ秒/ISO8601 文字列の 4 通りで同じ時刻を表す合成データを渡し、全部が同じ UTC int64 ナノ秒(核の契約 = 項目 0)に変換されるか。4 通りの入力が同じ内部値になることを値の場面で判定 |
| V3 | 重複・逆行・欠落・世代間の値の食い違いの検出(黙って結合しない) | 重複行・時刻が逆行する行・欠落(歯抜け)・同一時刻で値が違う世代間データを仕込んだ合成データを渡したとき、黙って結合(サイレントマージ)せず、検出して拒否または明示的な警告を返すか。仕込んだ異常の件数と検出件数が一致するかで判定 |
| V4 | sha256 の記録 | 読み込んだデータファイルごとに sha256 が実行記録(項目 3 の `backtest_runs/<id>/`)に残るか。同じファイルを 2 回読んで同じハッシュが出るか(再現性)で判定 |
| V5 | 合成・中間物・封印区間を拒む許可一覧 | `backtest_data/` 配下の `qa_*`(合成)・`o3c_*`・`phase2_runs/`(研究の中間物)・封印区間(`load_sealed` が守る範囲)を渡したとき、構造で読み込みを拒否するか(CLAUDE.md §2 の「合成の `qa_*`、研究の中間物 `o3c_*`・`phase2_runs/` は市場データとして読まない」を許可一覧として実装しているか)。拒否の実行結果(例外・エラーコード)で判定 |
| V6 | JPX 個別株の分割・併合・上場廃止・コード変更の調整と、生存者の偏りを避ける銘柄集合の型 | 分割・併合・上場廃止・コード変更を仕込んだ合成の JPX 個別株データを渡し、調整後の価格・銘柄集合が正解(場面係が手計算)と一致するか。銘柄集合の型が、上場廃止銘柄を後から除いていない(生存者バイアスを避けている)かを能力の場面で判定。JPX の実ティック・板はこの環境に見当たらない(委任文 §2 項目 2 の行、リード確認済み)ため、**合成データでの判定に限る**(データが無い旨は §4「無い・取れない」の規則で別記) |
| V7(旧 12) | 足のベクトル化の近道。同じ足で事象駆動の結果と一致することを試験で固定 | 同じ入力データを (a) 事象駆動の経路と (b) ベクトル化した近道の両方に通し、足ごとの OHLCV と指標が bit 単位で一致するか。所要時間が (b) < (a) であるか(近道の意味を成すか)を実測で判定 |

**射程外(この項目では測らない)**: 執行の模型(約定・遅延・費用・口座、V の対象外)は項目 2、検証・
再現性の統計的手続き(walk-forward・PBO 等)と実行記録の形式そのものは項目 3 の観点で扱う。ここでは
データ層が「正しい入力を作れるか」だけを見る。

## 3. 調査結果の側(機械的抽出。場面係が動かせる候補の判定・再現を行う入力)

### 3.1 段のある観点 / 段の無い観点の判定

`docs/DATA/tools_catalog.tsv` のヘッダ(実測): `番号 名前 到達 足 ティック 板の待ち行列
イベント駆動 ベクトル化 市場影響と約定の模型 段(機構) 段(既定) 遅延 取り消しの扱い 他の区分へ
安全側の所見 尽きていない理由 報告書の最後の記載の行 段の表の行 遅延(原文) 取り消しの扱い(原文)`
(コマンド: `head -1 docs/DATA/tools_catalog.tsv | tr '\t' '\n' | cat -n`)。

`段(機構)`・`段(既定)` の 2 列は `市場影響と約定の模型`(執行の模型 = 項目 2 の観点)専用で、
データ層の観点(V1〜V6)にも V7(ベクトル化)にも段の定義が無い(TOOLS_CATALOG.md §2.1 の「段」は
「指値の埋まり方の尺度」であり、データ読み込み・時刻正規化・ベクトル化とは無関係)。
**したがって V1〜V7 のどの観点にも「段(機構)が最も高い機構」は存在しない。** 存在しないものを
作らない(CLAUDE.md O-6・O-12)。V7 のみ `ベクトル化` 列に直接の印があるので機械抽出し、
V1〜V6 は該当する列が無いので SCAN への grep で候補を集める。

### 3.2 V7(ベクトル化)= 該当する列がある観点 → 列の印で機械抽出

コマンド:
```
awk -F'\t' 'NR==1{for(i=1;i<=NF;i++)h[i]=$i} NR>1 && $8=="○" {print "----"; for(i=1;i<=NF;i++) print h[i]": "$i}' docs/DATA/tools_catalog.tsv
```
(列 8 = `ベクトル化`。値は空白 98 件・`×` 14 件・`○` 4 件を実測で確認。`○` のみ抽出)

抽出結果(4 件、人の取捨てなし):

| 候補番号 | 名前 | 到達 | 足 | 市場影響と約定の模型 | 段(機構)/段(既定) | 他の区分へ | 報告書の行 |
|---|---|---|---|---|---|---|---|
| 54 | finmarketpy | 到達済み | ○ | ○ | 該当なし/該当なし | 区分4 | 7851 |
| 73 | vectorbt | 到達済み | ○ | (空欄) | (空欄) | (空欄) | 5907 |
| 74 | ml-quant-trading | 到達済み | ○ | (空欄) | (空欄) | 区分4 | 5910 |
| 87 | PyTrendFollow | 到達済み | ○ | ○ | 該当なし/該当なし | 区分2 | 7857 |

**74 `ml-quant-trading` は道具台帳 §3 の危険な兆候 11 件(44・74・120・41・58・19・112・111・97・51・118)
の 1 つ**(委任文 §4「道具台帳 §3 の 11 件は導入も実行もしない」)。場面係はこの候補を
**動かせない候補**として扱い、検討表(§3.4 の規則どおり (a) の書き写しの範囲だけで検討し、
導入も実行もしない)。

### 3.3 V1〜V6 = 該当する列が無い観点 → grep 語を先に書いてから SCAN に当てる

該当列なしの根拠(§3.1 のヘッダ一覧に V1〜V6 の語がどれも現れない)。以下、観点ごとに
**先に語を書き**、その語で `docs/DATA/SCAN_2026-09-21_tools.md`(10,694 行)に当てたコマンドと
当たった候補を全部記録する。人が足し引きしていない(ヒットした行をすべて載せる)。

#### V1 全資産の読み込み
語: `複数資産` / `マルチアセット` / `multi-asset` / `asset class`
```
grep -n "複数資産\|マルチアセット\|multi-asset\|asset class" docs/DATA/SCAN_2026-09-21_tools.md
```
当たり(1 件、他 3 語は 0 件): L212(候補一覧の 1 番 `hftbacktest` の節)
「Backtesting of multi-asset and multi-exchange models」(出典: PyPI JSON description、
2026-09-21 取得)。ただし同じ節に「現状 Binance Futures と Bybit のライブ実行のみ対応
(Rust限定)で、bitFlyer 等の国内取引所には触れていない」(L166〜169 相当、知見の表)とも
明記されており、**マルチアセットを謳うが bitFlyer 対応は書かれていない**。

#### V2 時刻の単位の正規化
語: `正規化` / `タイムゾーン` / `ISO 8601`
```
grep -n "正規化\|タイムゾーン\|ISO 8601" docs/DATA/SCAN_2026-09-21_tools.md
```
当たり(データ層に関係するもの):
- L2788: 候補 12 `pybotters`「投入した時刻の文字列(`exec_date`/`event_date`)を変換せずそのまま
  保持する。UTC / ミリ秒への正規化は道具の側でしない」(実測、`docs/DATA/probes/20260922_tools_1_run*.log`)
- L584: 候補 5 `Backtesting.py`(`backtesting`、`docs/DATA/tools_catalog.tsv` に印なし)
  「pandas.DataFrame(OHLCV+DatetimeIndex)を要求…時刻はpandas Timestampでタイムゾーン任意
  (実測で確認、UTCで問題なく動作)」
- L614: `QSTrader`(台帳に印なし)「時刻: UTCタイムゾーン付きpandas Timestampを要求(実測)」
- L1260: 候補 20 `VnPy`「datetime オブジェクトをそのまま取る。タイムゾーンの既定は確かめていない」(実測)
- L5203: 候補 105 `DaniyalMlk/slippage`「ISO 8601(`io.py` の逐語「Timestamps are ISO 8601.」)」(実測)
- L6303: 候補 61(名前は台帳照合が要る。SCAN 本文で「Barter」)「足の型は正規化された OHLCV」
  (`pub struct Candle { pub close_time: DateTime<Utc>, … }`)

**時刻の単位を明示的に自動正規化する(4 通りの単位を受けて内部で揃える)道具は、この grep では
見つからなかった。** 見つかったのは「pandas の型を要求する(変換は呼び出し側の責任)」
「変換しない」「ISO 8601 に固定」のどれかで、V2 の要件(秒・ミリ・マイクロ・ISO の 4 通りを
受けて自動で揃える)を満たす候補は 0 件。

#### V3 重複・逆行・欠落・世代間の値の食い違いの検出
語: `重複` / `逆行` / `欠落`
```
grep -n "重複\|逆行\|欠落" docs/DATA/SCAN_2026-09-21_tools.md
```
`逆行`: 0 件。`重複`(41 件)・`欠落`(38 件)は実測で全件目視したが、**中身はほぼ全部が
この調査自体の候補台帳の重複整理(「6 要素のどれかに印がある候補は、重複を除いて80件」等)
と `K7 表の項目の欠落 0 件`(報告テンプレートの記入漏れチェック)であり、候補の道具が
「データの重複行・時刻逆行・欠落を検出する」という機能を持つと書いた一次資料の引用は
1 件も無かった。** 近い言及として L518/L549/L912(候補 `freqtrade`)の「bitFlyer は
`fetchOrder`/`fetchOHLCV` の欠落で使えない側」があるが、これは取引所 API の機能欠落であり
時系列データの欠落検出ではない。**V3 を満たす候補は grep 上 0 件。**

#### V4 sha256 の記録
語: `sha256` / `ハッシュ`
```
grep -n "sha256\|ハッシュ" docs/DATA/SCAN_2026-09-21_tools.md
```
`ハッシュ`: 0 件。`sha256`: 3 件(L5712・L10414・L10552)、いずれも候補 86/96/104 の
**配布物(git-lfs ポインタ・照合の本体ファイル)がバイト単位で同じかを調べた調査班の作業記録**
であり、候補の道具が「読み込んだ市場データの sha256 を実行記録に残す」機能を持つという
一次資料の記載ではない。**V4 を満たす候補は grep 上 0 件。**

#### V5 合成・中間物・封印区間を拒む許可一覧
語: `封印` / `許可一覧` / `sealed` / `合成データ` / `中間物`
```
grep -n "封印\|許可一覧\|sealed\|合成データ\|中間物" docs/DATA/SCAN_2026-09-21_tools.md
```
`封印`・`許可一覧`・`中間物`: 0 件。`sealed`: 21 件、全部が当リポジトリ自身の
`src/bot/research/sealed.py`(ファイル一覧・依存グラフの引用)で、候補の道具の機能ではない。
`合成データ`: 27 件、いずれも調査班が候補を動かすときに**自分で用意した**合成入力の記述
(例: 候補ごとの「最小の実行」節)であり、道具側が「合成データ・中間物・封印区間を拒否する
許可一覧」を持つという記載は 0 件。**V5 を満たす候補は grep 上 0 件。**

#### V6 JPX 個別株の分割・併合・上場廃止・コード変更の調整、生存者の偏りを避ける銘柄集合の型
語: `生存者` / `分割` / `併合` / `上場廃止` / `銘柄` / `生存バイアス` / `コーポレートアクション` / `権利落ち` / `銘柄コード変更`
```
grep -n "生存者\|分割\|併合\|上場廃止\|銘柄\|生存バイアス\|コーポレートアクション\|権利落ち\|銘柄コード変更" docs/DATA/SCAN_2026-09-21_tools.md
```
`併合`・`上場廃止`・`生存バイアス`・`コーポレートアクション`・`権利落ち`・`銘柄コード変更`: 0 件。
`分割`(20 件)は全部が調査班の作業(候補一覧の重複分割・非分割ハイフンの字形・Jesse の足内
約定分割・Almgren-Chriss の恒久/一時分割)で、株式分割の調整機能の記載は 0 件。
`生存者`(1 件、L5944): **候補 74 `ml-quant-trading`**「約定・滑り・生存者の偏りの前提を、
報告の必須項目として一覧にしている運用」— これは§3.2 で確認済みの危険候補(道具台帳§3の11件)
であり、**導入も実行もしない。(a) の書き写しの範囲だけで検討表に載せる。**
`銘柄`(26 件)のうち V6 に関係しうるもの:
- L3168〜L3254: 候補 3 `PySystemtrade`「同梱の先物の価格データが 252 銘柄」だが「当方は暗号資産と
  JPX しか持っていない」「先物」であり JPX 個別株のコーポレートアクション調整の記載は無い
- L1302/L1391: 候補 21 `Qlib`「ファイル名が銘柄名になる」単純な csv 取り込みで、分割・併合・
  上場廃止の調整は記載なし

**JPX 個別株の分割・併合・上場廃止・コード変更を調整する機構を持つと一次資料に書かれた候補は
grep 上 0 件。**「生存者の偏り」への言及は危険候補 74(導入不可)の運用上の注意書きが 1 件あるのみ。

### 3.4 検討表への引き継ぎ

V1〜V7 で見つかった候補(54・73・74・87・12・5・7・20・105・61・3・21・その他)は、場面係が
`tests/bt/battery/item_1/opponents/CONSIDERED.md` の検討表(委任文「場面集の規則」6・9)で
1 行ずつ判断する。**74 は道具台帳 §3 の 11 件に該当し導入・実行しない**(委任文 §4)。
V2〜V6 で「grep 上 0 件」と書いた観点は、**候補が無いこと自体は場面係の検討表とは別に
このファイルの§3.3で確認済みの事実**であり、場面係は検討表の各候補についてこの grep 結果を
出発点にしつつ、一次資料((a) SCAN の書き写し、(b) 足りなければ一次資料)を自分でも確認する
(委任文「材料の順」)。

## 4. 当方の現状(旧 `src/bot/backtest/` ほか)の該当箇所

実測(2026-09-25、このファイルの作成時点)。

| 観点 | 該当箇所 | 現状 |
|---|---|---|
| データ読み込み全般 | `src/bot/backtest/engine.py:134`(`def run_backtest(`) | 既に `pd.DataFrame` に整形済みの `candles` を引数で受け取るだけで、**旧 backtest 自身はファイル読み込み・資産種別の判定を一切行わない** |
| 全資産の読み込み(V1) | — | 上記のとおり `engine.py`/`walk_forward.py`/`metrics.py`(計 398+53+74=525行、`wc -l` 実測)のどこにも複数資産の統一読み込み口が無い。各データ取得スクリプト(`scripts/fetch_binance_vision.py`・`scripts/fetch_dukascopy.py`・`scripts/fetch_history.py`・`scripts/build_*.py` 等、`grep -rln "read_csv\|read_parquet" scripts/*.py` で 20 本以上ヒット)がそれぞれ個別に `pandas.read_csv`/`read_parquet` を呼んでおり、共通のデータ層が無い |
| 時刻の正規化(V2) | `src/bot/backtest/walk_forward.py:20`(`def split_data(candles: pd.DataFrame, …)`) | 時刻の単位変換・検証は行わず、渡された `candles` の行順をそのまま `iloc` で分割するだけ(`grep -n "to_datetime\|astype" src/bot/backtest/*.py` はヒット 0 件) |
| 重複・逆行・欠落検出(V3) | — | `src/bot/backtest/` 配下に dedup/monotonic チェックのコードは無い(`grep -n "duplicat" src/bot/backtest/*.py` はヒット 0 件) |
| sha256 の記録(V4) | `scripts/fetch_binance_vision.py:118-124`(`def sha256_of`) | ダウンロード時の**ファイル単体の整合性検証**にのみ使われており、バックテストの実行記録にデータの sha256 を残す機構ではない(`src/bot/backtest/` にはハッシュ記録の呼び出しが無い) |
| 許可一覧(V5) | `src/bot/research/sealed.py:358-372`(`def load_sealed`) | **既存の最も近い実装**。封印区間(`backtest_data/phase2_sealed/`)を env・承認ファイル・トークンの 3 門で拒む仕組みがあるが、これは研究の評価用データセット専用で、`src/bot/backtest/` エンジン自体には接続されていない。CLAUDE.md §2 に「合成の `qa_*`、研究の中間物 `o3c_*`・`phase2_runs/` は市場データとして読まない」という**文書上の規則**はあるが、これを構造で強制するコードは `src/bot/backtest/` に無い |
| JPX 個別株の調整(V6) | — | `grep -rn "上場廃止\|分割併合\|生存者" src/bot/` はヒット 0 件。JPX 側は `src/bot/jpx/`(`kabu_client.py`・`on1_executor.py`)に発注実行の機構はあるが、個別株のコーポレートアクション調整コードは無い |
| ベクトル化(V7、旧12) | — | `src/bot/backtest/engine.py` は for ループでバー単位に進む事象駆動型の実装(`_PendingLimit` クラスなど、`class _PendingLimit:` L128)で、ベクトル化した近道は存在しない |
| 新エンジン側の現況 | `src/bot/bt/core/`(`ls src/bot/bt/` で実測) | 項目 0(核)は実装済み。`src/bot/bt/data/`・`src/bot/bt/vector/`(項目 1 の持ち物)は **まだ存在しない**(`ls src/bot/bt/data/ 2>/dev/null` は空、`src/bot/bt/vector/` も同様)。項目 1 はこのファイルの固定をもって着手前の段階 |

## 5. 返す前の吟味(委任文 §3「提出前の吟味」、L-433。最初に作る役の文)

観点ごとに、「非常に厳しい監査役・批評家なら何を [止める] にするか」を自分で列べ、返す前に潰した。

1. **V1〜V6 に「該当する列が無い」と判定した根拠が薄いのでは** → §3.1 で `tools_catalog.tsv` の
   ヘッダ全 20 列を実際に列挙し、6 要素(足・ティック・板の待ち行列・イベント駆動・ベクトル化・
   市場影響と約定の模型)がいずれもデータ層の観点(全資産読み込み・時刻正規化・重複逆行欠落検出・
   sha256・許可一覧・JPX調整)と一致しないことを確認済み。潰した。
2. **grep 語が少なすぎて見落としがあるのでは** → 各観点で複数の同義語・英語表記を試した
   (例: V1 は「複数資産」「マルチアセット」「multi-asset」「asset class」の 4 語、V6 は
   「生存者」「分割」「併合」「上場廃止」「銘柄」「生存バイアス」「コーポレートアクション」
   「権利落ち」「銘柄コード変更」の 9 語)。それでも見落としが完全に無いとは言い切れないので、
   **場面係が検討表を作る際に、このファイルの語と別の語でも当ててよい**旨をここに明記する
   (このファイル自体は書き直さないが、場面係の追加調査を妨げない)。
3. **「V2〜V6 は候補 0 件」と断定してよいか(O-2 断定癖)** → 断定ではなく「この grep 語では
   0 件」と範囲を明示した(CLAUDE.md §5.2 の「無い・取れない」の規則どおり、どこで・何を試した
   かを書いた)。「絶対に存在しない」とは書いていない。
4. **候補 74(ml-quant-trading)の扱いが V3・V6 の grep 結果に混ざり込み、あたかも「使える候補」
   に見えないか** → §3.2・§3.3 双方で「道具台帳 §3 の危険な兆候 11 件に該当し導入・実行しない」
   ことを明記し、検討表では動かせない候補として (a) の書き写しの範囲だけで扱う指示を書いた。
5. **V7(ベクトル化)の段の話を書かなかったのは説明不足では** → §3.1 で「段(機構)/段(既定)は
   市場影響と約定の模型専用で、V1〜V7 のどの観点にも段の定義が無い」ことを明記し、代わりに
   4 候補全部を機械抽出して列挙した(委任文の「段の無い観点は実装のコードで確かめられる候補を
   全部再現する」の対象として場面係に引き継ぐ)。
6. **「当方の現状」の行が旧 `src/bot/backtest/` 以外(研究スクリプト散在)に広げすぎ、範囲が
   曖昧では** → 各行にファイル:行・実行したコマンドを付け、断定箇所は実測根拠を添えた
   (`wc -l`・`grep -n`・`ls` の実行結果をそのまま使用)。
7. **観点(V1〜V7)が「測れる形」になっているか** → 各観点に、仕込む異常・合成データの形と
   判定基準(件数一致・値一致・所要時間の大小)を書き、場面係がそのまま値の場面/能力の場面に
   落とし込める形にした。
8. **このファイルが委任文の版(指紋 `da84268e88fc`)と一致しているか** → 冒頭で `sha256sum` に
   よる指紋確認を実行し記録した(実測: `da84268e88fc`、委任文記載の指紋と一致)。**2026-09-26 時点では
   委任文の版が進み指紋が変わっている(下記「6.」参照)。この行は作成時点(2026-09-25)の記録として
   そのまま残す。**
