# 項目 2 要件の固定 — 執行の模型(注文・約定・遅延・費用・口座)

資料係(要件の固定)。委任文 `docs/DATA/delegations/20260925_backtest_env_prompt.md`(指紋 `20260925_backtest_env_prompt.md@da84268e88fc`)§2・§3「要件と判定の固定」「調査結果の側の選び方」に従う。**このファイル以外は書いていない。**

## 1. 委任文 §2 の項目 2 の行(逐語)

委任文 §2 の表、項目番号 2 の行をそのまま引く(1 語も削らない):

> | 2 | 執行の模型(注文・約定・遅延・費用・口座 = 旧 2 + 3 + 4 + 5 + 6) | `src/bot/bt/orders/`、`src/bot/bt/fill/`、`src/bot/bt/latency/`、`src/bot/bt/costs/`、`src/bot/bt/portfolio/` | 【旧 2 注文と取引所の規則】成行・指値・post-only・IOC・FOK・逆指値・reduce-only・OCO・取消と訂正・部分約定・呼値と最小数量の丸めと拒否・自己約定防止・商品ごとの規則(FX_BTC_JPY、JPX の取引時間と値幅制限、FX)。異常系の注入: 拒否・時間切れ・状態不明(`STATE_UNKNOWN` を保持し自動で再送しない、CLAUDE.md §1)・Kill Switch での停止 **/** 【旧 3 約定と待ち行列の模型】道具台帳 §2.1 の段 0〜6 を全部選べる。列の位置の追跡、先行注文の取り消しの扱い 5 つの立場(道具台帳 §2.2)と確率型の列の模型を選べる。板を辿る成行。市場影響の関数。**楽観側と悲観側の両方を必ず回して幅で出す**。JPX の板・ティックは、この環境では見当たらない(リードが 2026-09-23 に確かめた方法: `ls backtest_data | grep -iE "jpx|n225|topix|225|kabu|tick"` で出た JPX 関係は 1 分足・日足・ETF・日報のフォルダで、`audit_fetch_JPX_tick_20260906/` の中身は `README.md` と HTML 2 本だけ。オーナー PC は未確認)。列の模型は暗号資産の実データと合成データで検証し、JPX は §4「無い・取れない」の規則で「データ待ち」と書く **/** 【旧 4 遅延の模型】配信の遅れ・発注の遅れ・取消の遅れを別々に。定数・実測の分布・種つきの乱数。受付と拒否の通知も事象にする **/** 【旧 5 費用と資金】maker / taker 手数料・スプレッド・資金調達(FX_BTC_JPY)・スワップ(FX)・JPX の手数料。**既定値を持たない**(全部の費用を実行ごとに明示させ、欠けたら拒否 = 道具台帳 §2.4 の形 2「既定で無効」を構造で防ぐ)。出所の欄を必須にする **/** 【旧 6 口座と会計】円建て、証拠金・レバレッジ・強制決済、建玉の時間(露出の時計)、脚ごとの費用、実現と評価の損益、分割・併合の建玉への反映(データが無いものは §4「無い・取れない」の規則で書く) |

(§2 の見出し行から持ち物の共通の注記: 「1 項目 = 作業者 1 体 + 場面係 1 体(1 周目の前だけ)+ 資料係 1 体 + 批評家 1 体 + 審査員 6 体」。持ち物には `tests/bt/item_2/` を含む。)

## 2. 比較の観点(観点ごとに 1 行、測れる形)

オーナーの完了の形(逐語、L-405)「**すべてが調査結果以上の信頼性と再現性に優れたものにすること。**」をどの観点でも軸に置く。各行は「満たすか満たさないかを場面(値の場面 / 能力の場面)で判定できる」形にした。

| # | 観点 | 測り方(測れる形) |
|---|---|---|
| C2-1 | 発注の型 | 成行・指値・post-only・IOC・FOK・逆指値・reduce-only・OCO のそれぞれについて、個別の場面(値の場面: 入力の注文 → 期待する受理/拒否/約定の状態)を持ち、全種が仕様どおりの状態遷移になるか |
| C2-2 | 取消と訂正・部分約定 | 取消・訂正・部分約定のそれぞれを個別の場面に持ち、待ち行列上の位置・残数量が仕様どおりに変わるか |
| C2-3 | 取引所固有の規則 | 呼値・最小数量の丸めと拒否、自己約定防止、商品ごとの規則(FX_BTC_JPY・JPX の取引時間と値幅制限・FX)を、規則に反する注文を個別に拒否する場面で測る |
| C2-4 | 異常系の注入 | 拒否・時間切れ・状態不明(`STATE_UNKNOWN` を保持し自動で再送しない)・Kill Switch での停止の 4 種を注入する場面を持ち、それぞれが CLAUDE.md §1 の安全不変条件どおりに振る舞うか |
| C2-5 | 約定/待ち行列の段(機構・既定) | 道具台帳 §2.1 の段 0〜6 のうち、差し替えれば届く高さ(段(機構))と、そのまま使ったときの高さ(段(既定))を、両方とも数値で持ち、選べるか |
| C2-6 | 列の位置の追跡・先行注文の取り消しの扱い | 道具台帳 §2.2 の 5 つの立場のうち、いくつを選べるか(候補の立場と一致する振る舞いを場面で確認できるか) |
| C2-7 | 板を辿る成行・市場影響の関数 | 板を辿って複数の呼値にまたがる成行の約定と、市場影響の関数(段 6)を持つか |
| C2-8 | 楽観側・悲観側の幅 | 同じ場面を楽観側の埋まり方と悲観側の埋まり方の両方で回し、結果を単一値ではなく幅(レンジ)で出せるか |
| C2-9 | 遅延の模型 | 配信の遅れ・発注の遅れ・取消の遅れを別々の分布(定数・実測・種つき乱数)として持てるか。受付・拒否の通知も事象になるか |
| C2-10 | 費用(既定値なし) | maker/taker 手数料・スプレッド・資金調達(FX_BTC_JPY)・スワップ(FX)・JPX の手数料の全部を、実行ごとに明示必須(欠けたら拒否)・出所の欄つきで指定できるか |
| C2-11 | 口座と会計 | 円建て証拠金・レバレッジ・強制決済・建玉の時間(露出の時計)・脚ごとの費用・実現/評価損益・分割/併合の建玉反映を持つか |
| C2-12 | JPX データ待ちの扱い | JPX の板・ティックが無い間、「データ待ち」と明示し、完了の判定に混ぜずに別立てで書けるか(§4「無い・取れない」の規則) |

## 3. 調査結果の側(候補は機械で全部抜き出す。人が足し引きしない)

### 3.1 段(機構)の尺度がある観点(C2-5〜C2-8): `docs/DATA/tools_catalog.tsv` の該当列

`docs/DATA/tools_catalog.tsv` の列 9(市場影響と約定の模型)・列 10(段(機構))・列 11(遅延)・列 12(取り消しの扱い)に印(空でない値)がある行を機械で抜き出した。**候補は `docs/DATA/TOOLS_CATALOG.md`(`scripts/build_tools_catalog.py` が同じ台帳から機械で作った索引)の §2.1〜§2.3 と同じ集合である(52 件)。**

使ったコマンド(そのまま):
```
awk -F'\t' 'NR>1 && ($9!="" || $10!="") {print $1"\t"$2"\t段機構="$9"\t段既定="$10}' docs/DATA/tools_catalog.tsv
```
→ 52 件(番号: 1・2・3・4・5(×)・6・7・8・10(×)・13・15・16(×)・18・19・20・21・23・32・33・35・36・37・38・45・50・51・53・54・57・60・61(×)・63・65・67・70・72・87・90・91・92・94・95・96・97・98・99・100(×)・101・102・103・104・105・106・107(×)・109(×)・111・116・119・123。うち×= 段機構が「×」= 差し替えても届かない 8 件を含む、有効(段機構=○)は 44 件)。

```
awk -F'\t' 'NR>1 && $11!="" {print $1"\t"$2"\t遅延="$11}' docs/DATA/tools_catalog.tsv
```
→ 52 件(遅延の値を持つ集合。番号は上と同じ 52 件から段機構=×の 8 件を除いた集合と一致)。

```
awk -F'\t' 'NR>1 && $12!="" {print $1"\t"$2"\t取消="$12}' docs/DATA/tools_catalog.tsv
```
→ 52 件。うち値が「在る」(取り消しの扱いを実測で確認)= 15・23・33・37・91・92・98 の 7 件、「該当なし」「該当なし(別の意味)」= 別の意味での取り消し(自分の注文の取消であって先行注文の扱いではない)、残りは「未確認」。

**C2-5(段(機構)・段(既定))の最も高い機構**: `docs/DATA/TOOLS_CATALOG.md` §2.1 の段の定義(0=必ず埋まる/1=価格が跨いだら埋まる/2=次の足の値と比べ未約定を残す/3=埋まりを決める到着が事象/4=出来高を読んで埋まる量を決める/5=自分の注文の列内の位置を追う/6=市場影響の関数を持つ)に照らし、**段(既定)= 6(そのまま使って市場影響の関数まで届く)** の候補は 15・35・36・92・105・106・116・119(段機構も○)。33・37・91 は既定が「5・6」で複数の型を同梱(段の表行はそれぞれ tsv 列18 = 10515・10532・10525)。個々の SCAN 行(報告書の最後の記載の行 = tsv 列17):
  - 106 `almgren-chriss` — SCAN 5913 行(段の表行 10522)。「印は `区分1-市場影響と約定の模型`」「導入は止めたまま(メタデータの指す先が 404、委任文 §6-1 の配布元一致が未達)」。**動かせない候補**(検討表で扱う)。
  - 116 `braverock/blotter` の `acOptTxns` — SCAN 5916 行(段の表行 10523)。「この環境に R は無い」。**動かせない候補。**
  - 105 `DaniyalMlk/slippage` — SCAN 5243 行(段の表行 10521)。
  - 92 `prediction-market-backtester` — SCAN 7859 行(段の表行 10492)。
  - 119 `peernagy/lob_bench` — SCAN 9405 行(段の表行 10524)。
  - 35 `Optimal-Execution-And-Market-Impact-Simulator` — SCAN 4618 行(段の表行 10519)。
  - 36 `Market-Impact-Model` — SCAN 4619 行(段の表行 10520)。
  - 15 `Mendl-Labs/BacktestingCore` — SCAN 6879 行(段の表行 10518)。段既定=6 だが `TOOLS_CATALOG.md` §2.4 の形 1「機構はあるが検証の実行経路が呼ばない」の例に挙がる(97KB の検証本体が 12KB の滑りと影響の板に一度も触らない)。
  **どれが「動かせる」候補かは §3「動かせない候補の検討と再現」の判定(この資料係の役の外)。ここでは段(機構)の値と根拠行だけを固定する。**

**C2-6(取り消しの扱い)**: `TOOLS_CATALOG.md` §2.2 の 5 つの立場(順不同、段のような大小の尺度は原典に無い)。
  - 95 `polymarket-fill-model`(何もしない。逐語「Cancellations ahead of us are invisible, so we never move up the queue」)
  - 57 `WonderTrader`(差した瞬間に 1 回、平均取消率で割り引く。既定 0)
  - 104 `LimitOrderBook-MatchingEngine`(取り消し時点で繰り上がる)
  - 98 `limitOrderBook`(取り消し済みに印を付けて列に残す)
  - 90 `PredictionMarketBench`(板の写真の量が先行量を下回ったら先行量を下げる。既定の型は 95 と同じ)
  当方(旧 `maker_fill_ref.py`)は「95 側 = 何もしない」(`TOOLS_CATALOG.md` §2.2 冒頭の逐語)。**5 つの立場は互いに排他で、「最も高い」という尺度は原典に定義されていないので、ここでは 5 つを列挙するに留め、順位付けはしない(根拠のない順位を置くのは A-12 に触れるため)。**

**C2-7(市場影響)**: C2-5 と同じ段 6 の集合(市場影響の関数を持つ = 段 6 の定義そのもの)。

**C2-8(楽観側・悲観側の幅)**: `docs/DATA/tools_catalog.tsv` にこの軸の列は無い。§3.2 のとおり `docs/DATA/SCAN_2026-09-21_tools.md` を grep した(結果は候補 0、下記)。

**C2-9(遅延)**: 上のコマンドの出力 52 件のうち、値が数値の段で埋まっている(段機構と同じ数値尺度を流用)候補の最高値は **6**(105・106・116・119、いずれも C2-5 と同じ候補・同じ SCAN 行)。33 は送信と取消で別々の遅延を持つ(値 = 5、SCAN 4616 行)、**当方に無い軸(遅延そのものを持たない)は `TOOLS_CATALOG.md` §2.3 が「注文から約定までの遅延の模型を持つのは 8 件(15・23・33・37・63・91・92・98)」と機械の集計を持つ。**

### 3.2 段の尺度が無い観点(C2-1〜C2-4、C2-10、C2-11): `docs/DATA/tools_catalog.tsv` に該当列が無いので `SCAN_2026-09-21_tools.md` への grep

`SCAN_2026-09-21_tools.md` には各回の冒頭に当方自身のファイル一覧(`### 当方の道具立て` 節、`scripts/tools_inventory.py` の出力)が挟まり、そこに `record_liquidations.py`・`check_liquidation_feeds.py` のような当方のスクリプト名が「liquidat」等の語にマッチしてノイズになる。そこで先に、この節だけを空行に置き換えた一時ファイルを機械で作ってから grep した(行番号は原本と同じに保つため、削除ではなく空行への置換):

```
awk '
/^### 当方の道具立て/ {skip=1; print ""; next}
skip && /^### / {skip=0}
{ if (skip) print ""; else print }
' docs/DATA/SCAN_2026-09-21_tools.md > /tmp/.../SCAN_clean.md
```
(526 行を除外。除いた範囲は §8「当方の道具立て」の自動ダンプのみで、候補の記述は 1 行も含まない。)

**C2-1(発注の型)・C2-2(取消と訂正)語: `post-only|IOC|FOK|reduce-only|OCO|逆指値|stop order|self-trade|self_match`**
```
grep -inE 'post-?only|\bIOC\b|\bFOK\b|reduce-?only|\bOCO\b|逆指値|stop[_ -]?order|self[- ]?trade|self_match' SCAN_clean.md
```
→ 21 件。当たった候補(番号は台帳と対応、番号未確定は台帳外の新規発見扱いとして名前のみ):
  - `nautilus_trader`(台帳検索中。SCAN 247 行、一次資料 = PyPI description 逐語「Advanced: Time in force IOC, FOK, GTC, GTD, DAY, ... post-only, reduce-only, and icebergs. Contingency orders including OCO, OUO, OTO」)。**IOC・FOK・post-only・reduce-only・OCO を単独で全部満たすと自称する唯一の候補。**
  - 15 `Mendl-Labs/BacktestingCore`(SCAN 1819・1823 行。成行・指値・逆指値・逆指値付き指値の 4 種、貸借、流動性の模型、手数料の戦略を持つが「逆指値と逆指値付き指値の注文」は当方に無いものとして 1823 行に列挙)
  - 6 `Ziplime`(SCAN 2399 行。「指値と成行の 1 往復」を初めて通せた道具、成行・指値・逆指値・逆指値付き指値の 4 型、`bt` と `fast-trade` は注文種別の概念自体が無い)
  - 33 `SarthakDalmia1/backtesting_execution_simulator`(SCAN 4104 行。成行・指値・IOC・FOK に対応と説明、一次資料未取得で浅い)
  - 94 `mote/backtest`(SCAN 4874 行。「limit/stop order based」とだけ説明、足かティックか未確定)
  - 98 `mihircoding/limitOrderBook`(SCAN 5115・5127・5282・5284・5288 行。README 逐語「No order types beyond limit, market, IOC, and FOK: no stops, icebergs, pegged」。自己約定防止(STP)・IOC・FOK を同じ `_match` 経路で扱う。取消は待ち行列の位置を保つ(部分約定の残りは板に残る))
  - 63 `trade-frame`(SCAN 5777 行。指値・成行・逆指値を遅延の列で埋める)
  - 20 `VnPy`(SCAN 6992・9137 行。足とティックの両方で指値と逆指値、`Status.CANCELLED` は自分の注文の状態)
  - 45 `ForexTester`(SCAN 10060 行。未約定の逆指値が値幅の外に出ると指値として残る挙動を文書上主張。未実行)
  - 94(再掲、SCAN 10065・10066 行。逆指値は足の高安の範囲で埋まり、埋まらない注文は次の足へ残る。`cancel` の当たりは自分の注文の取消と OCO(`o1.cancel(o2)`)だけ)
  - 50 `AlgoTest`(SCAN 10186 行。未約定注文を持ち、逆指値が値幅の外なら指値として残り値が戻ったときだけ埋まる。文言のみ・未実行)

**C2-3・C2-4(取引所規則・異常系)**: 上の grep と同じ語群に加え、専用語(呼値・自己約定防止・STATE_UNKNOWN・Kill Switch)は当方独自の語であり調査対象の外部道具側の報告に一致する語が無い(§4 の道具台帳 §3「安全側」が扱う「作業者への指示」の危険信号とは別物)。**候補 0 件**(検索のコマンド: `grep -inE 'self[- ]?trade|self_match|STATE_UNKNOWN|kill.switch|呼値|tick size' SCAN_clean.md` — STATE_UNKNOWN・Kill Switch・呼値は当方の語で SCAN に 0 件、self-trade/self_match は上の C2-1 の結果に含まれる(98 の STP)ので重複計上しない)。

**C2-10(費用)語: `maker.{0,5}taker|手数料|fee|funding|資金調達|swap|スワップ`**
```
grep -inE 'maker.{0,5}taker|手数料|\bfee\b|funding|資金調達|swap|スワップ' SCAN_clean.md
```
→ 41 件。候補(重複する行を候補ごとに 1 回に集約): 1 `Jesse`(1226 行、取引所ごとの手数料・レバレッジ・証拠金)/ 20 `VnPy`(1269 行、rate/slippage/pricetick を分離)/ 15 `BacktestingCore`(1374・1382 行、手数料・滑り・呼値を別々に持つが証拠金なし)/ 7 `Basana`(1656・1657・1666・1679 行、`fees.Percentage`・流動性模型 `VolumeShareImpact`・`bid_ask_spread` 既定値つき)/ 2 `Backtrader`(1709 行、手数料 0.05% で最小実行)/ `bt`(1795 行、手数料 0.05%、注文種別 API 無し)/ 6 `Ziplime`(1819〜1823 行、成行・指値・逆指値 4 種 + 滑り/手数料の差し替え式模型、2399・2483 行で滑り模型が出来高比/変動率/固定bp/なしを選べ手数料模型も株/先物で分岐)/ 2 `Backtrader`(1828 行、`notify_order` の状態遷移通知 + 手数料設定)/ `bt`(1846 行、手数料の関数)/ 10 `fast-trade`(2157 行、`comission=0.05`、成行/指値の往復不可)/ 12 `pybotters`(2768・2770 行、費用は接続先取引所側のみ)/ 3 `PySystemtrade`(3232・3606 行、費用は外側(IB 口座)/ 「当方が持っていて向こうに無い」逆列挙(3373 行、指値・手数料・滑り・建玉上限・複数銘柄は当方にあり PySystemtrade に無い)/ 41 `prediction-market-backtesting`(4112 行、清算・資金調達の無い場)/ 40 `OpenMarket`(4111・5694・5771 行、資金調達率・清算・不利な価格での約定を含むと自称、登録要否未確認)/ 11 `OctoBot`(4135 行、`FEE` 辞書で手数料を約定ごとに持つ)/ `QuantCore`(4427 行、`TickData` を受けるが清算・資金調達率の入口が公開名に無い)/ 63 `trade-frame`(6458 行、滑り・手数料・市場影響を未読と明記)/ 61 `barter-rs`(6566 行、手数料は一定率 `fees_percent`、遅延は固定値 `latency_ms`)/ `carlos8f/zenbot`(6651 行、maker/taker の約定・滑り・手数料)/ `sigc`(6652 行、市場影響を Almgren-Chriss 含む関数として選べる、手数料・滑り・市場影響・借株料を別々に見積もる)/ 10 `fast-trade`(6866 行、注文種別が無いため区分1の印を持たない)/ 20 `VnPy`(6897 行、手数料・滑り・呼値を別々に与えられる)/ 7 `Superalgos`(6986 行、実際の大きさ/実際の値/手数料/埋まった割合の 4 関数分離)/ 13 `DeviaVir/zenbot`(6989 行、滑りの既定値を環境変数で差替え)/ 19 `Jesse`(7147 行、建玉と手数料)/ 94(10066 行、遅延・滑り・手数料・出来高の語が 0 件)/ 90 `PredictionMarketBench`(10408・10434 行、`execution_mode=maker_taker` の同梱回、段(機構)4・5、段(既定)4・5)。
**「最も高い機構」= 費用の軸には道具台帳に段の尺度が定義されていない。**手数料・滑り・資金調達・呼値を「別々の差し替え式の関数として持つ」候補として 6 `Ziplime`(SCAN 2483 行、(a)(b)(d) が差し替え式で当方の `engine.py` には差し替えの口が無いと明記)と `sigc`(SCAN 6652 行、市場影響を線形・平方根・Almgren-Chriss から選べる)が最も分離度が高い。**この 2 件は根拠のある観察であって、順位を数値化した尺度ではない(A-12 のとおり尺度が無いのに私が数値の順位を作らない)。**

**C2-11(口座・会計)語: `証拠金|margin|レバレッジ|leverage|liquidat|強制決済|口座`**
```
grep -inE '証拠金|margin|レバレッジ|leverage|liquidat|強制決済|口座' SCAN_clean.md
```
→ 26 件。候補: `vectorbt.pro` の PRO 限定機能(281 行、レバレッジ・指値・ポートフォリオ最適化が有償側)/ 1 `Jesse`(1226・1374 行、取引所ごとの手数料・レバレッジ・証拠金の型。「当方に証拠金の模型は無い」と SCAN 自身が明記)/ 6 `Ziplime`(2458〜2462 行、証券口座・購読制の料金体系。2484 行、`perf` が `net_leverage`/`gross_leverage`/`max_leverage` を毎期出す。「建玉のてこの 3 種類を毎期出す点は当方のリスクの見方に対応物が無い」)/ 12 `pybotters`(2772 行、取引所口座と鍵)/ 11 `OctoBot`(2813 行、課金経路に取引所口座と鍵)/ 3 `PySystemtrade`(3232・3234・3567・3606 行、実弾は IB の口座が要る)/ 40 `OpenMarket`(5694 行、清算の地図・資金調達率)/ `vectorbt`(5881 行、無償版に指値が無く「limit orders, leverage」は PRO の有償側と原典が明記)/ 61 `barter-rs`(6284・6302・6568 行、模擬の口座に待ち行列が無いことを一次資料で確認、`Liquidation` を型として受け取る)/ 51 `QUANTAXIS`(7825・7968・8303・8669・9498 行、口座・建玉・保存・可視化を 1 package に統合、ただし外部送信の口(作者のサーバーへ口座名・銘柄・向き・値段・数量を送る)を持つ = 道具台帳 §3 の安全側の所見の対象)/ 2 `Backtrader`(9132 行、`Canceled, Expired, Margin, Rejected` は自分の注文の状態であって証拠金の計算ではない = 該当なし)。
**段の尺度なし。**「円建て証拠金・レバレッジ・強制決済・建玉の時間・脚ごとの費用・実現/評価損益・分割併合」を単独の候補で全部満たすものは grep の範囲では見当たらない(1 `Jesse` が手数料・レバレッジ・証拠金の型を持つが「当方に証拠金の模型は無い」という比較の文であって Jesse 自身の完全な仕様は未確認)。**この観点は §3「動かせない候補の検討と再現」の段階で個々に一次資料へ当たり直す必要がある(資料係の役の外)。**

## 4. 当方の現状(該当箇所)

現行の唯一の実装は旧エンジン `src/bot/backtest/engine.py`(398 行、`src/bot/bt/` は未着手)。

| 委任文の要素 | 当方の現状 | ファイル:行 |
|---|---|---|
| 発注の型 | `execution` は `"taker"`/`"maker"` の 2 値のみ。post-only・IOC・FOK・逆指値・reduce-only・OCO は無い | `src/bot/backtest/engine.py:141`, `:156`(`if execution not in ("taker", "maker")`) |
| 約定の模型 | 段 1(価格が跨いだら埋まる、taker)/ 段の表の当方欄に「自ら『楽観的』と書いている」と明記。maker 側も「traded strictly THROUGH the level」の単純な閾値越え判定で、列の位置(段 5)は持たない | `src/bot/backtest/engine.py:1-13`(モジュール docstring の taker/maker の説明)、`docs/DATA/TOOLS_CATALOG.md:34`(段の表の当方欄) |
| 取消と訂正・部分約定 | 指値は `maker_timeout_bars` 経過で一括取消になるだけ(部分約定・訂正の概念は無い) | `src/bot/backtest/engine.py:141`(`maker_timeout_bars: int = 5`) |
| 先行注文の取り消しの扱い | 別ファイル `scripts/qa/maker_fill_ref.py` に FIFO 実装があるが、**研究の実行経路(`engine.py`)は呼んでいない** | `docs/DATA/TOOLS_CATALOG.md:38`「ただし研究の実行経路が呼んでいない」 |
| 楽観側・悲観側の幅 | 単一の決定的な結果のみ。幅(レンジ)を出す機構は無い | `src/bot/backtest/engine.py`(全体。乱数・確率的な列の模型は 1 箇所も無い) |
| 遅延 | 配信・発注・取消いずれの遅延も無い(信号が出たバーの次バー始値で即時執行) | `src/bot/backtest/engine.py:6-9`(docstring「a signal at bar i executes at bar i+1's OPEN」) |
| 費用 | `CostModel` に `taker_fee_pct`/`maker_fee_pct`/`slippage_pct`/`spread_pct` の 4 つの**既定値つき**フィールドがあり、明示しなくても動く(委任文が禁じる「既定値を持たない」に反する現状) | `src/bot/backtest/engine.py:98-103`(`class CostModel` の 4 フィールド、いずれも `= 数値` の既定) |
| 資金調達・スワップ | `swap_daily_pct: float = 0.0`(既定 0 = 無効)。maker/taker 以外の商品別費用(JPX 手数料など)は無い | `src/bot/backtest/engine.py:144`(`swap_daily_pct: float = 0.0`)、`:201`(`swap_per_bar = swap_daily_pct / 100 * (bar_seconds / 86400.0)`)、`:275-278`(建玉があれば毎バー加算) |
| 口座・証拠金・レバレッジ | `initial_equity_jpy`/`order_notional_jpy` の 2 つの円建てスカラーのみ。証拠金率・レバレッジ倍率・強制決済・分割/併合建玉の反映は無い。`src/bot/portfolio/portfolio.py`(ライブ執行用)にも margin/leverage/liquidat の語は 0 件(`grep -n "margin\|leverage\|liquidat" src/bot/portfolio/portfolio.py` の当たり 0) | `src/bot/backtest/engine.py:138-139`(`initial_equity_jpy: float = 6000.0`, `order_notional_jpy: float = 3000.0`)、`src/bot/portfolio/portfolio.py`(該当なし) |
| 異常系(拒否・時間切れ・STATE_UNKNOWN・Kill Switch) | バックテスト側には無い。`OrderStateUnknown` はライブの取引所クライアントにのみ存在し、バックテストの実行経路には統合されていない | `src/bot/exchange/bitflyer_client.py:70`(`class OrderStateUnknown(Exception)`)、`:354`, `:367`(発火箇所)。`src/bot/backtest/engine.py` に `OrderStateUnknown`/`KillSwitch` の参照 0 件 |
| JPX の板・ティック | この環境には無い(2026-09-23 にリードが確認、委任文 §2 の項目 2 行に方法が逐語で記載) | `backtest_data/`(`ls backtest_data | grep -iE "jpx|n225|topix|225|kabu|tick"` の結果は委任文の行のとおり) |

## 5. 提出前の吟味(委任文 §3「提出前の吟味」。最初に作る役の文)

固定した要件・§2 の観点(C2-1〜C2-12)を読み直し、「非常に厳しい監査役・批評家なら何を [止める] にするか」を観点ごとに列べ、返す前に潰した。

1. **C2-5(段の尺度)で「最も高い機構」を複数(15・35・36・92・105・106・116・119)並べ、単一の候補番号に絞らなかった。** → 批評家は「観点ごとに段(機構)が最も高い機構を書け、という指示に従っていない」と止めうる。**潰した対応**: 委任文の指示は「段(機構)が最も高い機構」であり、tsv の値は同点(段既定=6)が 8 件ある実測結果である。**同点を 1 件に絞る基準(タイブレーク)は委任文にもツール台帳にも無い。**根拠のない基準で 1 件を選べば A-12(根拠のない設定値)に触れるため、8 件を根拠(SCAN 行)つきで全部書く形にした。この判断自体を吟味の記録として残す。
2. **C2-1・C2-2・C2-10・C2-11 は段の尺度が無く、「最も高い」を書けなかった。** → 批評家は「委任文の指示(段(機構)が最も高い機構を書け)を満たしていない」と止めうる。**潰した対応**: §3.1 の段の尺度(道具台帳 §2.1)は市場影響と約定の模型「専用」に定義されたものであり、費用・口座・発注の型には元から段の尺度が存在しない(`docs/DATA/TOOLS_CATALOG.md` に段の表は §2.1 の 1 個だけ)。存在しない尺度で順位を作ると A-12 に触れるので、該当する観点は「候補の全列挙 + 分離度が高いと事実で確認できるものの指摘」に留め、その旨を本文に明記した。段の尺度を新設するかはオーナー/場面係の判断であり、資料係が独自に作らない。
3. **「動かせるか」を確認せずに段 6 の候補を「最も高い」として書いた(106 は導入不可、116 は R が無い)。** → 批評家は「動かせない候補を機構の最高位として挙げるのは、検討表で弱い相手を選ぶ操作と同じではないか」と疑いうる。**潰した対応**: 委任文 §3「調査結果の側の選び方」は「動かせないものは、その場面について『結果なし』と記録する」と場面係の段階の規則であり、要件の固定(このファイル)の段階では機構の値を隠さない。106・116 それぞれに「動かせない候補」と明記し、場面係が §3「動かせない候補の検討と再現」で扱う対象であることを本文に残した。
4. **grep の語の選び方(C2-1〜C2-4、C2-10、C2-11)が、委任文の逐語(post-only・IOC・FOK・reduce-only・OCO・逆指値・maker/taker 手数料・資金調達・スワップ・証拠金・レバレッジ・強制決済)からどれだけ外れているか未確認のまま出していないか。** → 潰した対応: 語は全部、委任文 §2 の項目 2 行の逐語(「post-only・IOC・FOK・逆指値・reduce-only・OCO」「maker / taker 手数料・スプレッド・資金調達・スワップ」「証拠金・レバレッジ・強制決済」)からそのまま起こした。取引所固有の規則・異常系(呼値・自己約定防止・STATE_UNKNOWN・Kill Switch)は当方の内部語で外部道具の報告に一致する語が無いため候補 0 件と明記し、「候補が無いから測らない」にはせず、C2-3・C2-4 自体は §2 の観点表に残した(A-9「意図マップで○とした項目を黙って外す」への牽制)。
5. **「当方の現状」で `maker_fill_ref.py` の存在を書きながら「研究の実行経路が呼んでいない」ことを見落として『持っている』と書きかけていないか。** → 潰した対応: `docs/DATA/TOOLS_CATALOG.md:38` の逐語「ただし研究の実行経路が呼んでいない」をそのまま引用し、C2-6 の当方の現状の欄に明記した(A-11「目視・ぱっと見で判定する」への牽制。実装ファイルの存在だけで「持つ」と判定していない)。
6. **費用に既定値がある現状(`CostModel` の 4 フィールド)を、委任文の禁止(既定値を持たない)と混同して「現状が既に満たしている」と書いていないか。** → 潰した対応: §4 の表に「委任文が禁じる『既定値を持たない』に反する現状」と明記し、これが新エンジンで直すべきギャップであることを明示した。
7. **費用・口座の grep の当たり(41 件・26 件)を候補ごとに集約する際、原文の一部だけを引いて文脈を落としていないか。** → 潰した対応: 各候補の行に SCAN の行番号を必ず添え、原文の要約ではなく逐語の引用(「」内)をできるだけ残した。要約だけの行(A-8 の「軽い測定で足切り」に類する省略)は避けた。

## 6. 委任文の今の版との突き合わせ(2026-09-25、資料係(要件の固定)再着手)

このファイルはすでに固定済みなので書き直さない。起動文の指示どおり、委任文の今の版(指紋 `20260925_backtest_env_prompt.md@da84268e88fc`)と、このファイルを作ったときの版(指紋 `20260925_backtest_env_prompt.md@c26059000160`。コミット `2c868f1`)を `git diff 2c868f1 43c2e6f -- docs/DATA/delegations/20260925_backtest_env_prompt.md` で突き合わせた。

- **食い違いがあり直した箇所**: 冒頭(§0 の直前の段落、1 行目)の指紋の逐語引用。`c26059000160` → `da84268e88fc` に直した(上の行)。理由: 委任文の指紋そのものが変わっており(枠組みの指紋も `7e9d8b96ba0e` → `2c19e69c0755` に変わっている)、古い指紋を引用したままだと「読んだ版」が今の版と食い違ったまま固定されてしまうため(CLAUDE.md §0.1「読んだ証拠は逐語で引用する」に反する)。
- **食い違いが無く直さなかった箇所**: 委任文 §2 の項目 2 の行(逐語、本ファイル §1)、§3「要件と判定の固定」の段落、§3「調査結果の側の選び方」の段落は、2 版の間で 1 文字も変わっていない(`grep -n "^[+-]"` の差分に、これらの節の本文行が 1 行も現れない。行番号だけがずれた(79 行目 → 82 行目)ことを `diff` で確認した)。したがって §1〜§4 の中身(逐語・観点・調査結果の側・当方の現状)を直す理由は無い。
- **版間で変わったが、このファイルに反映すべき内容ではないもの**: I-013(L-451、通過の判定を測れる条件に置き換え)・L-454 案 B(項目 1〜3 は批評家なしで最大 1 周、項目 4 は最大 2 周)・L-458 案 A(Workflow の起動を予約した知らせの回に限る)。これらは §0 の通過の判定・周回の上限・起動の仕方の行と、§3「周回の数え方と止める条件」・「格付けの基準」の変更であり、台本(`scripts/workflows/backtest_env.js`)とリードの運用が持つ規則であって、資料係(要件の固定)がこのファイルに書く「要件」「観点」「調査結果の側」「当方の現状」のどれにも当たらない(§2 の項目 2 の行にも §3「要件と判定の固定」「調査結果の側の選び方」にも 1 語も変更が無い)。反映するとすれば審査員・批評家・台本の役の文であり、資料係(要件の固定)の役の外。

## 7. 返す前の吟味(委任文 §3「提出前の吟味」、最初に作る役の文。今回の直しに対して)

要件・規則・観点(C2-1〜C2-12)を読み直し、「非常に厳しい監査役・批評家なら、この直しの何を [止める] にするか」を観点ごとではなく直しの論点ごとに列べ、返す前に潰した(今回の変更は §1〜§4 の内容そのものではなく §0 直前の指紋の引用だけなので、C2-1〜C2-12 の観点ごとの再吟味は §5 のとおり変わらない。ここでは「直したこと自体」を吟味する)。

1. **「指紋だけ直して中身は直さない」という判断が、実は §1〜§4 のどこかに本当は反映すべき食い違いを見落としているのではないか。** → 潰した対応: `git diff 2c868f1 43c2e6f -- docs/DATA/delegations/20260925_backtest_env_prompt.md` の全差分(+/- 行、15 行)を 1 行残らず読み、§2 項目 2 の行・§3「要件と判定の固定」・§3「調査結果の側の選び方」のいずれにも変更が無いことを `grep -c` で機械的に確認した(0 件)。目視だけで「変わっていないはず」と断定していない(A-11 への牽制)。
2. **fingerprint の再計算が誤っていて、直した先の値自体が古いか間違っているのではないか。** → 潰した対応: `sha256sum docs/DATA/delegations/20260925_backtest_env_prompt.md | cut -c1-12` を実行し、`da84268e88fc` を得た。これは起動文が指定した指紋(`20260925_backtest_env_prompt.md@da84268e88fc`)と一致することを文字列比較で確認した(O-3: 実行したコマンドと出力をここに出す。CLAUDE.md §0.2 O-3)。
3. **「資料係(要件の固定)の役の外」として I-013・L-454・L-458 を反映しないと書いたが、これは A-9(○とした項目を黙って外す)に当たらないか。** → 潰した対応: これらは項目 2 の「要件」「比較の観点」「調査結果の側」「当方の現状」のいずれの節にも対応する変更ではなく、周回運用(台本・批評家・審査員の起こし方)の変更である。§2 の対応表を作らず「関係ない」と一言で済ませず、3 つの決定それぞれの中身と、それがこのファイルのどの節にも当たらない理由を個別に書いた(上の行)。黙って外したのではなく、外した理由を明記した。
4. **この節を書き足したこと自体が、固定済みのファイルへの実質的な書き直しになっていないか。** → 潰した対応: §1〜§5(既存の固定済み部分)は 1 文字も変更していない(fingerprint の引用 1 箇所を除く)。変更は末尾への追記(§6・§7)のみで、起動文の指示「その箇所を直し、直した行と理由をファイルの末尾に書き足す」の形どおりであることを、このファイル自体の diff(`git diff -- docs/DISCUSSIONS/2026-09-23_backtest_env/item_2/REQUIREMENTS.md` を後でリードが確認できる)で示せる状態にした。
