# 盲検の審査員の出力(項目 0 第 1 周、Workflow の記録から逐語で書き出し)

## 盲検:0#1:current0(agent a286051f4d912aa74)

```json
{
  "choice": "右",
  "reasons": "右(資料_乙)を選ぶ。REQUIREMENTS.md の「比較の観点」V1〜V6 の各項目について、乙はいずれも要求されている性質(単一検証型への統一と単位不正の例外拒否/V1、種類ごとの専用型の1対1列挙と型システムによる強制/V2、構造による将来分の完全遮断/V3、優先順位と通し番号による同時刻順序の完全確定/V4、公開面を数個のAPIに限定し内部状態への経路皆無/V5、約定・遅延・費用・口座を核を書き換えずに差し替え可能な独立部品として持ち、未指定時は明示的に「何もしない」代替になる/V6)を、いずれも肯定形・具体的な仕組みの名指しで満たしていると書いている。丙は同じ6観点のすべてで「機構を持たない」「規則が存在しない」「構造で強制できるか判定できない」という欠如の記述に終始しており、観点が要求する信頼性・再現性の性質をそもそも備えていない側として書かれている。従って見た目の優劣は明確に乙が上。",
  "missing_in_choice": "右(乙)に足りないもの: 「根拠」欄がいずれも「検査を実行し成功(実行コマンドと出力あり)」のように結果の要約だけで、実際のコマンド文字列・実際の出力・対象ファイル:行への言及を一切示していない(左の丙は「該当検索の一致 0 件」のように具体的な検索結果の数値を書いており、乙より検証可能性の粒度がわずかに高い箇所がある)。乙はまた、V2の「専用型の数」やV4の「優先順位の一覧」やV5の「窓口の数」など、自らが主張する具体的な個数・列挙内容そのものを一つも示しておらず、読者が数え直して確認できる材料がない。要するに乙は主張の強さ(肯定的な設計の充実度)では丙に優るが、再現性の核心である「同じコマンドを打てば同じ出力を誰でも確認できる」という逐語的な裏付けを欠いており、この点は丙の方がわずかに(不十分ながら)踏み込んでいる。"
}
```

## 盲検:0#1:current1(agent a3d7e229dd53c8be3)

```json
{
  "choice": "右",
  "reasons": "観点(REQUIREMENTS.md §2 の V1〜V6)ごとに読み比べた結果、信頼性・再現性は右(資料_丙)が優る。\n\n1. 具体性と検証可能性: 丙の各観点は「索引をそのまま使う」「on_candles 1メソッドのみ」「CostModel がエンジンと同じファイルに埋め込み」など、何を指しているか特定できる粒度の記述で、REQUIREMENTS.md §4(当方の現状、ファイル:行つき)の内容と整合している。乙は「単一の検証済み整数型」「専用の型を1対1で用意」「独立した窓口」のように、対象の実体(どのクラス・どのファイルか)を一切特定しない抽象語で書かれており、何を指しているか外部から確認できない。\n\n2. 根拠の中身: 乙の「根拠」欄はすべて「検査を実行し全件成功」「実行コマンドと出力あり」という記述のみで、実際のコマンド文字列も出力も本文に無い(「あり」と書いてあるだけで示していない)。丙の「根拠」欄は「該当検索の一致0件」「処理の繰り返し構造を実測」のように、何を数えた・何を探した結果ゼロだったかという否定的事実の実測に踏み込んでおり、再現(同じ検索をやり直す)の当たりが付けられる。\n\n3. 限界の記載: 丙はV3・V5で「複数種類になったときに同じ強制が効くかはこの実装からは判定できない」「窓口は種類ごとに分かれていない」と、測れない部分・弱い部分を明記している。乙は6観点すべてで「検査を実行し成功」と一様に肯定しており、限界や不確実性の記載が一つも無い。全項目が無条件に成功しているという構成自体が、CLAUDE.md が警戒する「断定癖」「安全側/都合の良い側への一様な結果」のパターンに近く、不自然に整いすぎている。\n\n4. 整合性: 丙の記述はREQUIREMENTS.md §4(現状把握、ファイル:行付きで既に確認済み)とほぼ一致し、既存の実測と矛盾しない。乙が記述する実装(複数事象型・決定的順序・疎結合API・差し替え可能な拡張点)がどのファイルに存在するのか、本資料からもREQUIREMENTS.mdからも裏付けが取れず、出所不明である。\n\n比較の観点(V1〜V6)以外(道具台帳・SCAN報告書の記載等)は今回読んでいないため、それらとの突き合わせはしていない。",
  "missing_in_choice": "右(資料_丙)に足りない点(左の資料_乙と比べて):\n- 丙は現状の欠落を指摘するだけで、V1〜V6のどれについても改善後の姿(何を作ればどう検査が通るか)を示していない。乙は(根拠が薄いとはいえ)各観点で「検査を実行し成功」という目標状態の型・検査の存在を主張しており、目指す完成形の具体像を提示する記述は丙には無い。\n- 丙は各観点の「根拠」で実際に打ったコマンド全文や生の出力(grep結果そのもの等)を本文中に再掲していない(「一致0件」等の要約のみ)。乙も同様に生出力は示していないため差は小さいが、丙側にも一次証拠の埋め込みという点でなお不足がある。\n- V2・V4・V5について、丙は「存在しない」ことの確認に留まり、数値(例えば公開APIの窓口の個数や列挙型の個数)を明示していない箇所がある一方、乙は(検証はできないが)「数が一致することを確認する検査」のように定量的な照合の枠組み自体は言及している。"
}
```

## 盲検:0#1:current2(agent af2a086ef92c36750)

```json
{
  "choice": "左",
  "reasons": "比較の観点は REQUIREMENTS.md §「比較の観点」(V1〜V6、「測れる形」列)のみを基準にした。\n\n左(資料_丙)は、各観点の「根拠」行が一貫して「実測」「コマンドで実測」「一致 0 件」のように、何を測ったか・どういう方法で測ったか・結果の件数までを具体的に書いている(例: V1「単位や型の検証に相当する語が該当箇所に一件も現れないことをコマンドで実測(該当検索の一致 0 件)」、V6「遅延に相当する語が該当箇所に一件も現れないことをコマンドで確認(一致 0 件)」)。これは再実行すれば同じ「0 件」という数値で照合できる、再現性のある書き方になっている。加えて、「固定した 6 観点ごとに 1 節、各節『何ができるか / 既定で何が起きるか / 根拠』の 3 行」という雛形を V1〜V6 の全節で厳密に守っている。\n\n右(資料_乙)は、各観点で「検査一式を実行し全件成功」「実行コマンドと出力あり」「検査を実行し成功」のように、実行した検査の名称・実際のコマンド文字列・実際の出力(たとえば何件中何件成功したか)を一切示さないまま、成功したという結論だけを書いている。特に V1 と V2 では括弧書きで「(実行コマンドと出力あり)」「(実行コマンドと出力: …)」とわざわざ言及しながら、その中身(コマンド文そのものや標準出力)をこの資料に一切提示していない。これは「検査をした」という主張を検証可能な形で裏付けておらず、信頼性・再現性の観点では左より弱い。さらに V4 では「既定で何が起きるか(補足)/ 根拠:」という行を追加しており、資料の雛形が定めた「3 行」の構成をこの節だけ崩している(構成上の一貫性の欠如)。\n\n以上より、示された 2 つの資料そのものの記述の質(検証可能な具体性・雛形の一貫性)で比較すると、左(資料_丙)の方が信頼性・再現性に優れて見える。",
  "missing_in_choice": "左(資料_丙)に、右(資料_乙)と比べて足りない点:\n- 丙は全観点で「機構が無い/存在しない」という否定的な結果のみを報告しており、乙のように「検査を実行して成功した」という能動的な検証(単位変換検査・型の列挙数一致検査・先読み経路の非存在検査・順序再現性検査・公開面の数え上げ検査・代替実装の差し込み検査)に相当するものを何一つ提示していない。否定的事実の実測はあっても、\"どうすれば観点を満たす実装になるか\"を検証した形跡がない。\n- 丙も乙と同様、実際のコマンド文字列や生の標準出力そのものはこの資料内には書かれておらず(「実測」「コマンドで確認」とだけ書かれている)、コマンド行・出力行を本文に引用する点では乙に対する優位性を実証しきれていない(乙が「あると言って出していない」のに対し、丙は「あるとすら言っていない」だけで、資料単体での再現性という点では両者とも同水準の弱さが残る)。\n- V3・V4 では「事象の種類が 1 つしかないため測る対象がない/規則を書く対象がない」と、観点そのものが測定不能であることを認めており、右のような多事象型を前提にした検証済みの解決策(優先順位+通し番号による確定的順序など)を示せていない。"
}
```

## 盲検:0#1:survey0(agent ad1cab67598f6b540)

```json
{
  "choice": "左",
  "reasons": "観点(V1〜V6)に沿って読み比べた結果、信頼性・再現性ともに甲(左)が優れて見える。\n\n- 甲は各観点について「打ったコマンド」と「出力」をそのまま貼っている(例: `awk -F'\\t' ...`、`grep -n \"deterministic event-driven architecture\" docs/DATA/SCAN_2026-09-21_tools.md` とその出力行番号・逐語)。読み手が同じコマンドを再実行すれば同じ結果を得られる、という意味で再現性がある。\n- 甲は根拠を一次資料(README・登録情報の逐語)まで遡り、取得日・URL・SCAN報告書の行番号を明記している。どの主張がどの一次資料のどの文言に基づくかを追跡できる。\n- 甲はV5(戦略APIの疎結合性)について「直接裏付ける逐語が見つからなかった」と正直に書き、19候補のうちどれが最良かを決めきれないと明言している。都合の悪い観点を隠さず「不明」と書く姿勢は信頼性を上げる。\n- 「段(機構)」という既存の尺度がV1〜V6を直接測るものではないと自己点検し、その尺度を安易に流用しない判断根拠まで示している。\n\n対して乙は、全観点について「検査一式を実行し全件成功」「失敗0件」「想定した窓口の数とだけ一致」のように、6観点すべてで一様に強い達成を主張しているが、実際に打ったコマンド・検査の中身・出力・対象ファイル・行番号が一つも示されていない。読み手は乙の主張を検証も再現もできず、記述だけを見ると「型で強制」「公開面を機械的に数え上げ」等、根拠不明のまま結論だけが並んでいる。これはCLAUDE.mdが禁じる「出所の分からない数字・意味の分からない文章」に近い形式であり、信頼性の観点では弱い。",
  "missing_in_choice": "甲(左)に不足している点(乙と比べて):\n- 甲の根拠は複数の異なるOSSプロジェクトのREADME・登録情報の「文言」に留まり、実際にその機能が動く挙動として検証されたものではない(マーケティング文言の存在確認であって、実装・実行結果の確認ではない)。\n- 甲は19個の異なる候補から観点ごとに最良のものをつまみ食いした「寄せ集め」であり、単一の一貫した設計・実装として全観点を満たす候補は示されていない(3.1で自ら明記)。\n- V5(戦略APIの疎結合性)については当方の現状との比較材料はあるが、外部候補側の直接的な裏付けは無いと明言しており、この観点だけは「どちらが優れるか」を判定できていない。\n- 乙が主張している「実行時の既定動作(渡す範囲が先に確定してから呼ばれる、未指定時は無効な代替が使われる等)」のような、コードの実行時の振る舞いに踏み込んだ記述は、甲には無い(甲は文言・静的な型定義止まりで、既定動作については軒並み「未確認」としている)。"
}
```

## 盲検:0#1:survey1(agent aeb79a32571569d19)

```json
{
  "choice": "右",
  "reasons": "観点は REQUIREMENTS.md の「2. 比較の観点」節(V1〜V6)のみを基準にした。\n\n右(甲)を選ぶ理由:\n- 冒頭で「複数の候補を寄せ集めた合成の資料であり、単一の実装の資料ではない。観点によっては裏付けが見つからなかったことも正直に書く」と前提を明示しており、各観点の根拠が「どの一次資料由来か」に対して誠実(V1=ナノ秒分解能の記載、V2=実装コードで型名5種を実測、V3/V4/V6=紹介文・登録情報の文言、と根拠の種類が観点ごとに異なることを隠していない)。\n- V5(戦略APIの疎結合性)について「直接裏付ける文言は見つからなかった」「不明」と明記している。これは REQUIREMENTS.md 3.3 の記述(「この観点は…一次資料の逐語だけでは決めきれない」)と整合しており、実際に見つからなかったものを見つかったと書いていない=再現性・検証可能性の点で信頼できる。\n- 「既定で何が起きるか」を確認できていない箇所は一貫して「未確認」と明記しており、断定していない(O-2 の「断定癖」に該当する記述がない)。\n\n左(乙)に足りないもの:\n- 全観点で「根拠: 〜検査を実行し全件成功」「実行コマンドと出力あり」と書きながら、実際のコマンドや出力そのもの、対象コード・候補名・ファイル:行が一切示されておらず、第三者が同じ結果を再現・検証する手がかりがない(再現性が無い)。\n- あたかも単一の実装が V1〜V6 すべてを満たしているかのように書かれているが、どの候補(あるいは自社実装)のことかが不明であり、REQUIREMENTS.md 3.1〜3.3 が明らかにしている「候補は分かれており、V5 は裏付けが見つからなかった」という実態と矛盾する。特に V5 で「想定した窓口の数とだけ一致することを確認」と断定しているのは、右や REQUIREMENTS.md が正直に「不明」としている点と食い違い、信頼性を損なう。\n- 「何ができるか」の記述が抽象的な理想像の列挙にとどまり、出典(一次資料・ファイル・SCAN 行など)の提示が一切ない。",
  "missing_in_choice": "右(甲)には、左(乙)にある「既定で何が起きるか」の具体的な挙動描写(例外拒否の仕組み、優先順位による順序確定、未指定時に「何もしない」代替が使われる、等)がほとんど無く、大半が「未確認」で止まっている。仮にこれらの具体描写が検証済みであれば有用な情報だが、右はその代わりに正直さを優先しており、検証済みの具体的挙動という情報量そのものは左より少ない。また右は各記述に対応する候補名・ファイル:行・grep コマンドの実出力をこの資料内には直接示しておらず(REQUIREMENTS.md 側には有るが本資料単体では)、O-3(実行コマンドと出力を同じ返答に出す)の水準までは満たしていない。"
}
```

## 盲検:0#1:survey2(agent a82ec14dcc55df850)

```json
{
  "choice": "左",
  "reasons": "観点は REQUIREMENTS.md §2 の「比較の観点」(V1〜V6)のみで判定した。信頼性・再現性の軸で見ると:\n\n- 甲は主張のすべてに出典を明示している。「ナノ秒分解能」「deterministic event-driven architecture」「Tools to prevent look-ahead bias」等の逐語、候補番号・SCAN報告書の行番号・一次資料URLまで辿れる形になっている(REQUIREMENTS.md 3.2 節と対応)。これは第三者が同じ grep やURLを叩けば同じ文言に到達できる=再現性がある。\n- 甲はV5について「直接裏付ける逐語が見つからなかった」「疎結合の保証ではなく一般的な柔軟性の主張に過ぎない」と、分からないことを分からないと明記している(A-18 に沿った振る舞い)。V1・V2・V4・V6も「既定の挙動までは分からない(未確認)」と射程を明記しており、測ったことと測っていないことの境界が読める。\n- 乙は逆に、V1〜V6の全項目で「検査を実行し全件成功」「想定した窓口の数とだけ一致」と、隙のない結果だけが並ぶ。しかしどの節にも、対象が何(どのリポジトリ/どのファイル)で、どのコマンドを打ち、どんな出力が出たかが一切書かれていない。「実行コマンドと出力あり」と書きながら実物が本文に無い節すらある(V2)。CLAUDE.md O-3(機構やデータの挙動を主張するときは実行したコマンドと出力を同じ返答に出す。出せないなら「未確認」と書く)に照らすと、乙の6項目はすべてこの基準を満たしていない。\n- 結果として乙は「何を対象に、どうやって確かめたか」を読者が検証する手段が無く、数値・結論の出所も追えない(O-6 の「出所の分からない文章」に該当しうる)。全項目が綺麗に成功している点も、失敗や未確認が一つも無いという点で、甲のような限界の申告が無く、構造的にA-18(安全側/都合の良い側への偏り)を疑わせる作りになっている。\n\n以上より、信頼性・再現性の観点では出典・逐語・行番号まで遡れて限界も申告している甲(左)を優れていると判定する。",
  "missing_in_choice": "甲(選んだ側)に足りない点: 乙が主張しているような「構造的強制」「全件成功の検査」「窓口数の一致確認」に相当する、実装レベルでの網羅的な検証(実際にテストを書いて動かした結果)が甲には無い。甲は外部候補ツールの紹介文・コードの逐語調査にとどまり、V5は裏付けが見つからず未決着のまま、V1・V2・V4・V6も「既定の挙動は未確認」と留保が残っている。乙が(出典さえ示せば)提供できていたはずの「検査を実際に動かした一次証拠」「6観点すべてに答えを出す網羅性」が甲には欠けている。ただし乙はその出典・実行痕跡そのものを本文に示していないため、この網羅性が実在するかどうか自体を今回の資料だけでは検証できない。"
}
```
