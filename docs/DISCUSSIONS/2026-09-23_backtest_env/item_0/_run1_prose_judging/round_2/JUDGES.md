# 盲検の審査員の出力(項目 0 第 2 周、Workflow の記録から逐語で書き出し)

## 盲検:0#2:current0(agent a440543ae619cc0db)

```json
{
  "choice": "左",
  "reasons": "左(丙)は V1〜V6 のすべてで、右(甲)より信頼性・再現性の高い具体的機構を主張している。\n- V1: 型付き64bit・ナノ秒・UTCで、検査関数を通さない限り事象を作れず、単位混在・範囲外値を例外で弾く(甲は表形式索引をそのまま使い、単位の検出・拒否機構が無い)。\n- V2: 8種以上(10種類)を単一の列挙型と1対1対応の専用型で網羅し、対応が崩れていないかを確かめる試験がある(甲は「足」1種類のみ)。\n- V3: 呼び出し前に境界を確定してから窓口を構築する順序で、戦略が保持した窓口を後から使っても時刻が進まない構造(甲は「足」という1種類についてのみ強制でき、複数種類混在時に同じ強制が効くかは判定不能と自ら書いている)。\n- V4: 時刻→種別優先順位→一意な到着番号の3段階規則を持ち、全事象種別が優先順位表に含まれることを検査する内蔵の表明がある(甲は事象が1種類のため規則そのものが存在しない)。\n- V5: 現在時刻・現在の事象・見える範囲取得・発注・取消の5メソッドだけの専用窓口で、公開面がちょうど5つであることを確かめる試験がある(甲は1メソッドで指図オブジェクトを返すのみ、複数APIに分かれていない)。\n- V6: 約定・遅延・費用・口座の4つが独立した型の約束事として外に出ており、何も渡さなければ既定の空部品が動き、外部試験で差し替え可能性を示している(甲は費用・口座・約定判定が実行主体の関数内にローカル変数・分岐として直書きされ、遅延模型の概念自体が無い)。\n全体として丙は「型・境界確定・試験による表明」という構造的強制を各観点に持ち、甲はREQUIREMENTS.md §4の「当方の現状」とほぼ一致する未整備な実装を記述しているにすぎない。",
  "missing_in_choice": "丙には、主張を裏づける具体的な出典が一切無い。ファイルパス・クラス名・関数名・行番号・試験名のいずれも書かれておらず、「検査関数」「基底クラス」「窓口オブジェクト」「内蔵の表明」「外部の試験」がコード上のどこに実在するのかを指せない。対する甲は、本資料自体には行番号は無いものの、その内容はREQUIREMENTS.md §4で `src/bot/backtest/engine.py:396` や `src/bot/strategy/base.py:46` のようにファイル:行まで既に実測・提示された現状と一致しており、実在するコードに紐づけて検証できる。したがって丙を選ぶ場合、V1〜V6で主張されている機構が実装として本当に存在するのか、まだ構想段階なのかを、この2資料だけからは確認できないという再現性上の不足が残る。"
}
```

## 盲検:0#2:current1(agent ad511d0568df8f5b4)

```json
{
  "choice": "右",
  "reasons": "右(資料_丙)は V1〜V6 の全観点で、検証可能な具体的機構と、それを担保する専用の試験・表明(assertion)まで挙げている。\n- V1: 単一整数型(64bit・ナノ秒・UTC)への統一を「専用の検査関数」が強制し、単位取り違えは例外で弾く、と検査経路まで明記。\n- V2: 8種の事象を列挙型と1対1対応する専用の型で持ち、列挙型と型定義の対応が崩れていないかを確かめる専用の試験がある、と網羅性の崩れを検出する機構まで言及。\n- V3: 境界確定→窓口構築→戦略呼び出しという順序を明記し、「以前受け取った窓口を保持して後から使っても時刻は進まない」という再利用時の抜け道潰しまで書いている。\n- V4: 時刻→事象種別優先順位(決済系→市場状態系→自注文通知系→心拍)→一意到着順番号、という3段階の明文規則を挙げ、全事象種別が優先順位表に含まれることを検査する内蔵の表明がある、と規則の網羅性検証まで示す。\n- V5: 戦略が触れる窓口の公開面が「ちょうど5つ」であることを確かめる専用の試験がある、と疎結合の保証を試験で裏付けている。\n- V6: 約定・遅延・費用・口座の4つを独立インターフェースとして外出しし、既定は何もしない部品で動き、外部で作った試験用部品で差し替えて動かす専用の試験がある、と拡張点の独立性を試験で示している。\n\n左(資料_甲)はどの観点も「何ができるか/既定で何が起きるか」が現状の弱点描写(単位混在を検出しない、事象は足1種類のみ、優先順位の規則自体が存在しない、疎結合は設計方針の明記のみで複数APIに分かれていない、拡張点は核の内部にベタ書き)にとどまり、それを裏付ける専用の検査・試験・表明への言及が一つも無い。根拠欄も「〜のコードで確認」という一般的な確認にとどまり、検証を担保する仕組み(型・例外・専用テスト)の記述が無い。",
  "missing_in_choice": "右(丙)にも、実際にそのコード・試験が動く一次資料(ファイル:行番号や実行ログ)への具体的参照が本文中には無く、記述が設計の主張にとどまっている可能性がある点は、左と同様に本資料だけでは確認できない(「根拠」欄がどちらも「コードで確認」という抽象的な記述で、行番号までは踏み込んでいない)。"
}
```

## 盲検:0#2:current2(agent a7f3ea430f74ee470)

```json
{
  "choice": "左",
  "reasons": "左(資料_丙)を選ぶ。REQUIREMENTS.md の「比較の観点」V1〜V6 それぞれについて、丙は要件の字面をほぼ満たす具体的な仕組みを書いている。\n- V1: 「専用の検査関数」を通さない限り事象を作れない=型強制+検査で単位混在を拒否\n- V2: 8種を「共通の列挙型と1対1対応する専用の型」で表現し、「列挙型と型定義の対応が崩れていないかを確かめる専用の試験」まで用意\n- V3: 「境界の確定→窓口の構築→戦略呼び出し」という順序で、戦略側が以前の窓口を保持しても時刻が進まない構造的な不能化\n- V4: 時刻→種別優先順位→一意な到着順番号の3段階で、かつ「全事象種別が優先順位表に含まれることを検査する内蔵の表明」\n- V5: 窓口オブジェクトの公開面が「ちょうど5つ」であることを確かめる専用の試験\n- V6: 4部品を「それぞれ独立した型の約束事」として外出しし、「何も渡さなければ何もしない既定の部品」で動き、「外部の試験」で差し替え可能性を示す\n\n甲はどの観点でも「1種類の事象(足)しか扱えない」「単位変換・検査の機構が無い」「規則を書く対象が無い」「実行主体の関数内にローカル変数・分岐として直接書かれている」と、機構そのものの不在を繰り返し記述しており、信頼性(検査・表明・専用試験の有無)・再現性(決定的順序の規則・単一時刻型への強制)のいずれの観点でも丙が明確に上回る。",
  "missing_in_choice": "丙(選んだ側)には、各観点の「根拠」が「検査関数の実装を確認」「専用の試験をコードで確認」のように述べられているだけで、対象コードのファイル名・関数名・行番号などの具体的な参照が一切無い。つまり、書かれている仕組み(検査関数・列挙型・窓口オブジェクト・優先順位表・専用試験)が実在するのか、この文書だけでは第三者が追跡・再現して確かめられない。一方、甲は(同じ節の範囲内で見る限り記述の粒度は同等だが)「戦略が受け取る引数の型を定義したコード」「繰り返し処理本体のコード」など、確認対象をやや具体的な処理単位まで絞り込んで書いており、また V3・V4 で「この実装からは判定できない」と限界を自己申告している点で、主張の射程を偽らない透明性がある。丙にはこの種の限界の明記(射程の自己申告)が無く、全観点で高い達成を主張している点が、裏付けの検証可能性という意味では甲に劣る。"
}
```

## 盲検:0#2:survey0(agent ac1eccfd4dfb9c478)

```json
{
  "choice": "右",
  "reasons": "右(資料_乙)は全観点で「候補番号・プロジェクト名・SCAN報告書の行番号・逐語」という追跡可能な出典を明示しており、同じ grep コマンドを打てば誰でも再現・検証できる(例: V1「候補58 nautilus_trader、SCAN:6061、『with nanosecond resolution』」)。加えて、確認できていない点(既定動作・V5 の裏付け)を「未確認」「見つからなかった」と正直に書き分けており、断定と不明の境界が読み手にそのまま伝わる。\n\n左(資料_丙)は「専用の検査関数」「窓口オブジェクト」「専用の試験」「内蔵の表明」など具体性の高い記述をしているが、それらがどのファイル・どの関数・どの行にあるかを一つも示していない。根拠欄はすべて「〜をコードで確認」とだけ書かれ、実行したコマンドも出力も逐語の引用もない。同じ項目の他資料(REQUIREMENTS.md の「当方の現状」表)は当方の記述についてすら全てファイル:行を付けているのに、丙にはそれが一切無い。つまり丙は内容が体系的で読みやすい反面、第三者が独立に検証できる手がかりがゼロで、実在するコードの記述なのか理想化された記述なのかを外形から区別できない。\n\n信頼性・再現性という観点では「誰が読んでも同じ場所を辿って同じ結論に達せられるか」が核心であり、右はそれを満たすが左は満たさない。",
  "missing_in_choice": "右(資料_乙)に丙と比べて足りない点: (1) 一つの統合された実装ではなく、観点ごとに別々の候補を寄せ集めた構成のため、V1〜V6 が単一のシステムとして両立するかは示せていない。(2) 「既定で何が起きるか」がほぼ全観点で「未確認」のままで、実際の挙動までは踏み込めていない。(3) V5(戦略APIの疎結合性)は直接裏付ける逐語が見つからず、根拠なしと明記されている。(4) 引用元がいずれも未導入(実行していない)一次資料の記述に留まり、実測ではなく文書の主張どまりである。"
}
```

## 盲検:0#2:survey1(agent a234e63c1429cb8e7)

```json
{
  "choice": "左",
  "reasons": "比較の観点(V1〜V6)ごとに検証可能性を見ると、乙は各主張の出所を「一次資料の該当記述(調査班による実測記録)」「該当コードの実測記録」と明示しており、これは REQUIREMENTS.md §3.2 に既に記録された grep・SCAN 報告書の行番号(例: SCAN:6061, 7180, 6118/6154, 6151)に遡って裏取りできる。V5 では「直接裏付ける記述が見つからなかった」ことも正直に書いており、事前登録的な誠実さ(A-18「安全側に振れて無いと決めつけない」/O-6「出所の分からない…文章を出さない」)に沿っている。\n\n一方、丙は V1〜V6 のすべてに具体的で完成度の高い記述(64bit ナノ秒の検査関数、10 種類の列挙型、3 段階の決定的順序規則、5 メソッドの専用窓口、4 部品の外出しと既定の no-op 部品)を与えているが、それぞれの根拠が「コードで確認」としか書かれておらず、ファイル名・行番号・リポジトリ名・URL・試験名など、どこの何を確認したのかを一切示していない。REQUIREMENTS.md の「当方の現状」節や乙の記述はどちらもファイル:行または SCAN 行番号で裏付けを示しているのに対し、丙だけがこの慣行を欠いている。しかも丙は 6 観点すべてに「不明・未確認」が一つも無く、要件文の裏返しのように過不足なく符合しており、これは実測ではなく要件から逆算して書かれた(あるいは実装が存在しない)可能性を疑わせる。出典を追えない主張は第三者が再現・検証できず、「信頼性と再現性」という比較軸そのものにおいて丙は評価不能である。したがって、記述の完成度では丙が上に見えても、実際に裏取りできる乙の方が信頼性・再現性で優れている。",
  "missing_in_choice": "乙(左)に足りないもの: (1) 観点ごとに別々の外部候補(候補58/18/62/52など)を寄せ集めただけで、単一の一貫した実装として V1〜V6 を同時に満たす設計を示せていない(丙は核の実装案として整合した1つの設計を提示している)。(2) V5(戦略APIの疎結合性)は「見つからなかった」で止まっており、丙のように具体的な窓口インターフェース案(5メソッド・実行主体を渡さない設計)を提示できていない。(3) 「既定で何が起きるか」がほぼ全観点で「未確認」であり、既定動作の設計そのものを示せていない(丙は例外での弾き方・no-op既定部品など既定動作を具体的に記述している)。(4) 内蔵の検査・試験(型の対応を確かめる試験、公開面を数える試験など)への言及が無く、再現性を機構で保証する仕組みの提案が無い。"
}
```

## 盲検:0#2:survey2(agent a8e4361f0f06dbbf9)

```json
{
  "choice": "左",
  "reasons": "左(資料_丙)は観点(V1〜V6)ごとに比較すると、単一の一貫した実装として書かれている。時刻型の検査関数、10種の事象型を持つ列挙型、境界確定→窓口構築→戦略呼び出しという順序、同時刻事象の3段階の決定的な順序規則、公開面が5つちょうどであることを確かめる専用の試験、4つの拡張点を外部から差し替えて動かす専用の試験など、V1〜V6のすべてで「既定で何が起きるか」が明記され、かつ内蔵の表明・専用の試験によって崩れを検出する機構が各観点に揃っている。これは1つの系として同じ入力から同じ結果が再現する(=再現性)ことを裏付けやすい構造である。\n\n一方、右(資料_乙)は自ら「調査結果の側は、観点ごとに最も高い機構を持つ候補を寄せ集めたもの(1つの実装ではない)」と明記しており、V1はnautilus_trader、V2はzipline-reloaded、V3はqf-lib、V4はnautilus_trader、V6はQuantConnect/Leanと、観点ごとに異なる候補の一次資料の一文を拾い集めた構成になっている。V1〜V4・V6のすべてで「既定で何が起きるか: 未確認」であり、V5に至っては「直接裏付ける記述は見つからなかった」としている。これらは別々のリポジトリの断片的な記述であり、1つの系として動かして再現できることを何一つ示していない(未導入・未確認が大半)。信頼性(主張の裏付けの強さ)・再現性(同じ入力で同じ結果を出せる一つの系であること)のどちらの点でも、右は「観点ごとに最も強そうな一文を集めた」段階に留まり、左が示す「一つの系として全観点が揃い、機構で強制・検査されている」という水準に届いていない。",
  "missing_in_choice": "左(資料_丙)には、各観点の「根拠」欄が「検査関数の実装と…をコードで確認」「列挙型の定義と…をコードで確認」のように、確認した事実そのものを主張するだけで、どのファイルの何行目か、実際にどんなコマンド・grep・テスト実行で確認したかという具体的な引用や出典が一切示されていない。右(資料_乙)は(観点ごとに寄せ集めではあるが)打ったgrepコマンドと一次資料の逐語引用、取得日、ログの行番号までを示しており、主張の追跡可能性という点では左より高い。左を選ぶ場合、この「出典・実測コマンド・行番号の欠如」が右に対して足りない点である。"
}
```
