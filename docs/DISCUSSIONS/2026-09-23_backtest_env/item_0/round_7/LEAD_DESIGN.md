# 項目 0「核」— 第 7 周の戻し(L-436)を受けたリードの根本原因の読みと設計の直し

合意した完了の形(オーナー逐語、L-405): 「**すべてが調査結果以上の信頼性と再現性に優れたものにすること**」。
戻しの規則(オーナー逐語、L-436 の案 1 の文面): 「項目 0 でも、同じ未達の理由が 3 周続いたら一度止めてリードに戻す。未達の確定ではなく、リードが根本原因を読んで設計を直してから続ける(上限はない)。」

## 1. 何が 3 周続いたか(実測。出所は各周の CRITIC.md と ROOTCAUSE.md)

| 周 | 指摘 | 形 | 作業者の直し |
|---|---|---|---|
| 4 | i0-r4-02 | 送った注文を戦略が後の呼び出しから書き換えられる | `OrderRequest.extra` を `freeze` で不変に |
| 5 | i0-r5-01 | `extra` の外の欄(size・side・Enum の要素…)は今も送り手の物のまま渡る | 欄ごとに `as_text`/`as_float`… で組み込みの型に直す。**「正確に組み込みの型ならそのまま保つ」一覧 `_SCALARS` を置く** |
| 7 | i0-r7-02 | `_SCALARS` に入れた `Fraction` は Python の class で `__slots__` の私的な属性に代入できる → 同じ物が路を渡り、送り手が後から書き換えた値を取引所が読む | (この周で戻し) |

## 2. 根本原因(リードの読み)

**設計の向きが逆だった。**3 周とも作業者は「送り手の物のうち、そのまま渡してよい物の一覧」を書き、一覧の外を断った。一覧に載せた物が本当に不変かは、載せた時点で誰も確かめていない(`Fraction` は「不変の値」として一覧に入ったが、Python の不変は慣習で、機構ではない)。**一覧を広げる直しは、一覧の穴を 1 つずつ批評家に見つけてもらう構造**であり、周を重ねても収束しない。第 5 周の直しはこの形をむしろ固定した(`_SCALARS` の新設)。

同じ構造の穴が第 6→7 周の i0-r6-01→i0-r7-01(答えの外の名指し)にもある: 「境界 `b >= 0` だけ検める」「答えの事実 1 つで種類を決める」という**扱う場合の一覧**を書き、一覧の外(負の境界、後ろ向きの答え)を批評家が突いた。作業者の神託の試験は 21,888 の名指しを列べたが、負の境界は列に入れていなかった = **試験の入力の空間を、実装が扱う場合の一覧から作っていた**(批評家の指摘のとおり「実装の規則を写している」)。

## 3. 設計の直し(第 8 周の作業者への指示。`lead_notes` で渡す)

### 3.1 路を渡る物: 「保つ物の一覧」を捨て、「核が作り直した物しか渡らない」にする

- 受け手に届く物は**核が自分で構築した新しい物**だけ。送り手の物の同一性(`is`)を保って渡す道を 1 つも残さない。組み込みの型の値も例外にせず、`int(x)`・`float(x)`・`str(x)`・`bytes(x)`・`complex(x)`・`Decimal(str(x))`・`Fraction(x.numerator, x.denominator)` の形で**作り直す**(`None`・`True`・`False` だけは唯一物)。
- 「正確に組み込みの型なら保つ」の分岐(`_plain_scalar` の `t in _SCALARS: return value`)を消す。一覧 `_SCALARS` は「受け付ける型」の一覧としてだけ残してよいが、**同一性を保つ根拠には使わない**。
- 不変条件を機械で測る試験を作業者が持つ(批評家の probe を写す): **送り手の敵対者** = 送った後に、送った物から到達できる全てのオブジェクト(`__dict__`・`__slots__` の私的な属性・list/dict の中身・`__class__` の差し替え)を書き換え、受け手が読む値が送った時の写しと一致することを、受け付ける全ての型 × 全ての運び手(`PATH_CARRIERS`)× 全ての欄について列べて確かめる。加えて、届いた物のグラフと送った物のグラフに `is` で同じ物が無いこと(`None`/`True`/`False` と小さい整数・短い文字列の interned を除く)を確かめる。
- 「なぜ第 5 周で見つけられなかったか」を ROOTCAUSE に書く。答えは上の §2 と一致するはず(一覧の外を試す試験が無かった)。

### 3.2 答えの外の名指し: 扱う場合を列べず、全ての名指しを 1 つの座標に戻す

- 添字・区間の境界(正・負・None・歩幅の符号)を、**切り詰める前に**配達の列の座標に戻し、その座標で「届いた / まだ / 何も無い / 落とした」を決める。`tuple` の切り詰めは、判定が済んだ後にだけ使う。
- 神託の試験の入力は**実装の場合分けから作らない**。答えの形(前向き・後ろ向き・歩幅つき・`until_ns`・`n`・`history_limit`)× 添字と境界を **[-len-3, len+3] の全格子**で列べ、入力だけから決めた神託と比べる。「独立の神託 6 種で 21,888」のように数を書くときは、**何を列に入れなかったか**を併記する(A-10)。

### 3.3 提出前の吟味への追加(作業者)

- 直した規則ごとに、**その規則の入力の空間を全格子で列べる敵対者の試験**を先に書き、規則を直したあと通す。批評家が見つけるまで待たない(L-433「1 回の指摘で終わらせられるように提出前に吟味しろ」)。

## 4. 場面集の側(監査役(場面)r7-1 の [聞く] 2 件へのリードの答え。**オーナーが L-438 で案 1「条件つきで認める」を選んだ**。§5 の 2 も L-438「案1 族で数え、直した族は 0 から」で承認)

- **br7-1-1**(4 場面の入力が対象自身の p3 の応答から機械で決まる設計は「場面集を 1 周目の前に固定」「期待の値が要件の文から独立」を満たすか): **満たすと読む。ただし条件つき。**固定されているのは「入力を決める規則」(`TYPE_RULE`)と「入力から期待を出す閉じた式」で、対象ごとに変わるのは入力の型の組だけ。期待の値は対象の出力を見ずに入力から式で出る。条件: (a) 規則と式が DEFINITIONS.md に、対象を見る前の文として書かれていること、(b) 場面が対象ごとに選んだ入力を materials に記録し、審査員が読める表の注記に「型の組は対象の持つ型から規則で決めた」と 1 文あること。この設計は新実装に不利な向き(相手の型で測る)なので、偏りの検査(委任文 §3 の監査の観点)には反しない。
- **br7-1-2**(値が不一致でも型を持つと数えるか): **数えない。**「型を持つ」= その型の p3 場面で「正解と一致」。値を誤って運ぶ型で他の観点の入力を組めば、その観点の不一致が型の欠陥か並びの欠陥か分けられなくなる(§5.2「陰性と不明を分ける」)。新実装に有利な向きではない(新実装は全型が一致)。

## 5. 手順の直し(L-437 で合意した 2 点+この戻しで足す 1 点)

1. 場面係は、直しの前に「対象が届けた物」の**正の定義**(1 段落: 何を対象の振る舞いと数え、何を数えないか)を ROOTCAUSE に書き、**その定義だけを監査役に先に通す**。監査役が定義を通すまでコードを直さない。
2. 監査役(場面)への文: `repeat_of` は「同じ欠陥」ではなく「**同じ根本原因の族**(前の指摘と同じ機構の別の形。例: 帰属の穴が dict → 対象の class → 戦略の外の呼び出し、と形を変えたもの)」で付け、**必須**にする(無ければ `null` と明示)。批評家にも同じ文を当てる。
3. 作業者の提出前の吟味に §3.3 を足す。

## 6. 測れていないこと(限界)

- §3.1 の作り直しで速さがどれだけ落ちるかは未測定(第 7 周の作業者の実測: 場所の検めで 1 事象 18.6 → 23.6 マイクロ秒)。要件に速さの行があれば作業者が測って書く。
- 敵対者の試験が「全ての書き換えの道」を尽くしているかは、試験の列の外は測れない。列に入れた道を DEFINITIONS と同じ形で試験のファイルに書く。

## 7. 8 回目の第 8 周、場面係の定義 r8-1-1 が「止めて上げた判断」と監査役(定義)r8-1-1 の [聞く] へのリードの答え(2026-09-24 12:00 UTC。場面係はこの節を読んでから定義 r8-1-2 を書く)

1. **候補 35(権限の分類器の拒否)**: 受け取った。場面係はこれ以上試さない。リードが run の止まった時機に試す。
2. **定義 C を当てたときの通知・時計の組(i0-r7-05)**: 場面にしない。**「場面にしていない観点・側面」に、組ごとに理由「固定した測り方(要件 P0-4 の文)の外。要件は 1 周目の前に固定したもので、周の途中で広げない(委任文 §3「要件と判定の固定」)」を書く。**批評家 i0-r7-05 自身が「P0-4 の固定した測り方の外なので [直す] に留める」と書いている。要件を広げるかはオーナーの判断で、項目 0 の通過のあとに「欠けているもの」の批評家の経路で上げる。
3. **「対象の型」を 1 つの設定に限ることが L-438 の範囲か**: 範囲内。L-438 の案 1 の文面は「型の組は対象の持つ型から規則で決める」で、規則の中身は場面集の定義(監査役が通したもの)。「利用者が公開の手段で 1 つの実行として作れる設定」に限るのは、その規則の中身として認める。
4. **dr8-1-1-3(実行の途中の正当な再設定)**: 「設定」= **1 つの実行の中で、利用者が対象の公開の手段で行った設定の操作の列**(開始前に限らない)。実行の途中に公開の API で購読や型を足すことは、その列の一部として数える(弱めない)。定義 A の「事象を届ける前に決まっている全部」をこの文に置き換える。
5. **dr8-1-1-4(設定ごとの最良を場面ごとに寄せてよいか)**: **設定の違う実行は別の対象(別の行)として扱う。**「対象 × 設定」を 1 つの走らせた候補として survey_results に別々に記録し、表の「調査結果の側」の行は委任文 §3 の規則どおり「場面ごとに走らせた候補の最良」を寄せる(候補の間の選択で、1 つの候補の中の設定の和ではない)。型の組(p1・p5)は候補(= 対象 × 設定)ごとに、その候補の p3 の結果だけから決める。materials に「どの対象のどの設定か」を書く。根本原因 2 が禁じたのは「1 つの候補の型の組を設定の違う実行の和で決める」ことで、候補を設定ごとに分ければ両立する。
6. **dr8-1-1-1(定義 B の対象に ROOTCAUSE*.md を含めるか)**: 含める。ROOTCAUSE には場面の中身を自然文で書かず、場面 id と欄の参照で書く。今の ROOTCAUSE_r8-1.md の該当行(監査役が挙げた 50-52・88-90・96-102 行)を参照の形に直す。
7. **dr8-1-1-2(定義 C を P0-3・P0-6・P0-7 にも当てる)**: 当てる。7 観点全部を事象 8 種 × 見る道 3 種で列べ、DEFINITIONS.md の「場面にしていない観点・側面」を定義から機械で作る(組の抜けは試験が落とす)。

### 7.1 監査役(定義)r8-1-3 の [聞く] 2 件へのリードの答え(12:50 UTC)

8. **dr8-1-3-5(§7 の「止めそうなこと / 潰し方」の表は O-10 に触れないか)**: 触れない。ただし書き方に条件がある。委任文 §3「提出前の吟味」(4) は「非常に厳しい批評家なら何を [止める] にするかを自分で列べ、返す前に潰す」を求めている。これは**指摘の候補を列べて、それぞれに何をしたか(直した / 直さない理由)を書く**ことで、**自分の作業に合否や点数を付けることではない**。O-10 が禁じるのは後者(「潰した = 通る」「指摘は n 件に減った」のような自己採点)。書いてよいのは「候補 → 何をしたか(ファイル:行、コマンドと出力)」の 2 列だけで、「潰した」「問題なし」「合格」のような判定の語を書かない。判定は監査役がする。
9. **dr8-1-3-6(敵対者の試験の表を手で列べた暫定表として扱ってよいか)**: 定義の段では暫定表として認める。ただし表の見出しに「**手で列べた暫定表。直しの段で正の定義 A の文から機械で書き出した物に置き換え、書き出した数と試験の数が等しいことを出力で示す**」と 1 文書く。直しの段でその出力が無ければ監査役(場面)は [止める] にしてよい。

### 7.2 監査役(定義)r8-1-5 の [聞く] 3 件と、作業者 `作る:0#8` の「リードに聞くこと」2 件へのリードの答え(14:10 UTC)

10. **dr8-1-5-1(L-438 (2) の 4 場面の識別を欄の有無に委ねている)**: 場面 id で名指す。DEFINITIONS.md に「L-438 (2) を当てる場面 = p1-merge-by-time・p1-typed-events・p5-same-time-twice・p5-hand-over-order の 4 つ」と id で書き、試験は「`type_plan` の欄を持つ場面の id の集合がこの 4 つと等しい」ことを確かめる(観点と件数だけでは入れ替わりを検知できない)。
11. **dr8-1-5-2(場面係が自分で足した「緩める決定は名指さない物に当てず、義務の側を当てる」の規則で上位の決定の当てる先を読み直した)**: 規則の中身はリードの読みと一致するので、**リードの規則として採る**(オーナーの逐語ではない。経緯 = dr8-1-4-1)。文: 「緩める決定(許可・免除)は、その決定の文が名指す先にだけ当てる。名指されていない先には義務の側の規則を当てる。」場面係はこの規則を自分の規則として書かず、「リードの規則(LEAD_DESIGN §7.2 の 11)」と引く。上位の決定の読み直しは、場面係が単独で行わず、この節のように上げて答えを待つ(dr8-1-4-3 と同じ形)。今回は結果が一致したので直しは要らない。
12. **dr8-1-5-4(B・D・E の全格子 497,664 / 68,812,800 / 103,680 は走らせられるか)**: 走らせる数の規則をリードが決める: **全格子は必ず機械で書き出して数を示す。走らせるのは、升目の数が 100,000 以下なら全部。それを超えるなら、各軸の全部の値 × 他の軸の全部の組(2 軸の全組 = pairwise)以上を決定的に選び、選んだ数と選び方を試験のファイルに書き、「全格子を走らせた」とは書かない。**0・A・C にも同じ規則を当てる。速さは走らせて測り、測った値を書く(未測定と書いたまま置かない)。
13. **作業者の問い 1(速さ 12.3 → 29.4〜30.6 マイクロ秒/事象)**: 要件は 1 周目の前に固定したもので、周の途中で速さの行を足さない。測った値を REPORT に書き、契約 core-10 に「受け手ごとの写し」の費用として記す。速さを要件にするかはオーナーの判断で、項目 12(足のベクトル化の近道)・13(統合)の要件を固定するときにリードが上げる。この周の [止める] の根拠にはならない。
14. **作業者の問い 2(振る舞いの変化 2 つ: 最も古い事象より前へはみ出す負の区間の境界を断る / Decimal の子を受け付ける)**: 契約 core-10 に書いてあれば足りる。後の項目の要件は契約を参照して固定するので、別の周知は要らない。`[-k:]` の代わりが `visible_events(n=k)` であることは契約の同じ箇所に書く。

### 7.3 監査役(場面)r8-1 の [聞く] 3 件へのリードの答え(16:15 UTC)

15. **(判定の基準の部分は §8.2 の 1 で撤回・置き換え。2026-09-24 19:40 UTC)** **br8-1-1(P0-7 の升目 160 のうち 153 が「未決 = 測り方の文が見る道を名指さない」のまま、次の周を起こしてよいか)**: 起こしてよい。ただし「未決」という語は消す。**判定の規則: 場面にするのは、固定した要件 §2 の観点の「測り方」の文が名指す組だけ。測り方の文が名指さない升目は「場面にしていない側面」に、理由「固定した測り方の外(要件は 1 周目の前に固定し、周の途中で広げない = 委任文 §3「要件と判定の固定」、判定バーを開封後に動かさない)」で入れる。**P0-7 の測り方は「各口にダミー実装を差し込み、核のコードを変えずに動くか」で、口 × 事象 × 見る道の全升目を測る文ではない。升目の表は「測っていない範囲」の記録として価値があり、**「欠けているもの」の批評家(台本の最後)に渡す**。P0-7 の 153 升目を測るかは要件を広げる判断 = オーナーの決定で、項目 0 が通ったあとにこの表をそのまま上げる(リードはここで決めない)。監査役の懸念(P0-7 が 4 口だけで通過になる)は正しく、その事実を通過の報告に「測っていない範囲」として書く(§5.2「棄却には射程を書く」の裏返し: 通過にも射程を書く)。
16. **br8-1-2(再現が LEAN の 1 件だけで、6 観点は再現 0 のままか)**: 断定で答えない。確かめ方を決める: (a) 批評家は毎周、検討表の「スキップ(上位互換)」の行から 3 行を選び(番号の小さい順に周ごとにずらす)、含む側の候補の機構の行(一次資料)を自分で読んで「含む」が成り立つかを書く。成り立たない行が 1 つでもあれば [止める](族 = 上位互換の誤用、i0-r4-03 → i0-r5-04 の続き)。(b) リードも run が止まったあとに 3 行を自分で読む(A-2)。(c) 「再現 0 件」自体は不合格の根拠にしない(再現すべき候補が無いことは検討表の行が示す)。
17. **br8-1-3(正の定義の自己監査の輪が収束しているか)**: 実測で答える。定義の監査は r8-1-1〜r8-1-5 の 5 回(8 / 14 / 17 / 20 / 42 分)、[止める] は 1 → 3 → 2 → 2 → 0。そのあとのコードの直し(74 分)は場面集の監査 r8-1 で [止める] 0(初回で通過。第 6 周は 3 回、第 7 周は 1 回かかった)。定義の段に時間が寄り、コードの段の手戻りが消えた = 収束している。ただし定義の文書が 900 行(ROOTCAUSE_r8-1.md)に膨らんだのは A-17(規則を増やすだけ)の形に近い。次の周から: 正の定義は DEFINITIONS.md に規則の文として置き(1 定義 1 段落)、ROOTCAUSE は指摘ごとの根本原因と直しだけにする。規則を足すときは、足す規則が既存の規則のどれで代替できないかを 1 行書く。

### 3.4 第 8 周の批評家 i0-r8-01(注文の口の状態を戦略が API の外から書き換えられる)を受けたリードの設計の追記(17:20 UTC。第 9 周の作業者が読む。読んだ時点が作業の途中なら、そこから当てる)

- これは §2 と同じ構造の再発である: 作業者は「受け手に渡す物を写しにする」を運び手(事象・注文・報告)の一覧に当てたが、**戦略が文脈(`StrategyContext`)から属性をたどって届く核の物**(注文の台帳 `_registry`・出口の箱 `_outbox`・見え方 `OrderView`)は一覧に無かった。「写しにする物の一覧」を広げる直しは、また次の形を批評家に見つけてもらう構造になる。
- 設計: **戦略の側から届く物のグラフと、核が持つ変わりうる物のグラフを、`is` で交わらせない。**戦略に渡す `ctx` は、核の物への参照を属性に持たない(核の物は closure か別の所有者に置き、`ctx` からは呼び出しでしか届かない)。呼び出しが返す物(`order()`・`open_orders()`・`visible_events()` の答え)は毎回作った写し。台帳への書き込みは核の内部の関数だけが行い、その関数は `ctx` から到達できる名前空間に無い。
- 試験(敵対者の格子。一覧ではなく到達性で): `gc.get_referents` で `ctx` から到達できる全オブジェクトを列べ(深さの上限なし、`type`・関数・モジュールを除く)、核が持つ変わりうる物(台帳・箱・列・窓・模型の実体)のどれとも `is` で一致しないこと、および到達した物のうち変わりうる型(dict・list・set・`__dict__` を持つ物・`__slots__` に代入できる物)が 0 件であることを確かめる。加えて「結果を取る前に戦略が到達した物を全て書き換える」敵対者を回し、結果(orders・fills・配達の要約)が書き換えの前と同じであることを確かめる。この試験は場面ではなく作業者の試験(`tests/bt/item_0/`)に置く。
- 同じ根の探しは、属性の名前の一覧ではなく上の到達性の出力で行う。出力に 1 件でも核の物が出たら、その経路を塞ぐ(名前を隠すのではなく、参照を持たない)。

### 7.4 第 9 周の作業者 `作る:0#9` の「リードに聞くこと」3 件へのリードの答え(18:20 UTC)

18. **速さ(32.35 → 39.84 / 43.38 → 54.2 マイクロ秒/事象)**: §7.2 の 13 と同じ。要件に速さの行は無く、周の途中で足さない。測った値を REPORT と契約に記す。速さを要件にするかは項目 12・13 の要件を固定するときにオーナーに上げる。
19. **§3.4 の「毎回作った写し」の読み方(見え方の物は呼ぶたびに新しいが、中の注文・事象は戦略の写しの同じ凍った物)**: **その読み方でよい。**§3.4 が求めるのは「戦略から届く物のグラフに核の変わりうる物が無い」ことで、「呼ぶたびに全部を作り直す」ことではない。中の物が (a) 凍っていて(slot への代入も断る)、(b) 核の物ではなく戦略の側の写しで、(c) 戦略が `object.__setattr__` で変えても戦略自身が後で読む物にしか効かない、の 3 つが到達性の試験で確かめられていれば足りる。件数に比例して遅くする作りは取らない。
20. **振る舞いの変化(口を通らず出口の箱に書いた知らせのうち API の規則に合う物は、API の呼び出しと同じ結果になる)**: 契約 core-11 に書いてあれば足り、別の周知は要らない。ただし条件: 出口の箱は**戦略の側の物**であり(核の物への参照ではない)、核はそれを写して 1 度だけ読み、核の台帳と時刻に対して規則を当てる、が到達性の試験に入っていること。箱が核の物なら §3.4 に反する。

### 7.5 第 9 周の報告の監査 `監査役:0#9` へのリードの答え(19:25 UTC)

21. **[止める](round_9/JUDGES.md が無い)**: リードの落ち度。審査員の出力の書き出し(`export_workflow_judges.py`)を周の終わりに打てていなかった。この回に round_8・round_9 を書き出した。次の起動の台本で、報告の監査の文に審査員の出力の逐語を渡し、「JUDGES.md はリードが周の終わりに書き出す」と書く。
22. **[聞く](i0-r9-02 の [直す] への格下げ)**: 批評家の格付けは委任文 §3「批評家」の基準で決まり、リードは個別に承認しない。(a) は FAILED で止まる経路、(b) は §3.4 の設計の不一致。第 10 周で (b) を必ず閉じる(到達性 0)。鎖は 1 のまま。
23. **[直す](DEFINITIONS.md の「未決」)**: 場面係の第 10 周の直し(i0-r9-03)で消す。第 9 周の判定はやり直さない(表は 32 場面の結果だけに依る)。

## 8. 8 回目の第 10 周の戻し(定義の監査で同じ理由 3 回)を受けたリードの根本原因の読みと設計の直し(2026-09-24 19:40 UTC)

### 8.1 何が起きたか(実測: `battery/AUDIT.md` の 定義:0#r10-1-1〜3 と 監査役(定義):0#r10-1-1〜3)
- 第 10 周の場面集の側の指摘は **[直す] 1 件だけ**(i0-r9-03「未決」の升目を消す = §7.3 の 15 の反映)。それに対し場面係は「正の定義 G」(升目の判断・要件の文の「名指し」を切片に切って機械で判定・上位の決定の「登録簿」・文の持ち主と出所)を 3 版書き、監査役(定義)は版ごとに新しい穴([止める] 1 → 3 → 2)を出し、dr10-1-1-2 → dr10-1-2-1 → dr10-1-3-1 の族で 3 回 = 戻し。
- 根本原因(リードの読み): (1) **定義を先に通す手順を [直す] にも当てた。**経緯を正確に書く: L-437 でオーナーに見せた文面は「対象が届けた物」の正の定義(帰属の族の [止める] への処置)。委任文ではリードがそれを「[止める] があれば」全般に広げた(リードの規則)。台本(`defineThenAudit`)はさらに、指摘の重さを見ずに場面集の側の指摘の全部に当てていた(台本の誤り)。§8.2 の 3 は台本を委任文の文言([止める] のとき)に合わせるもので、族では絞らない(族が新しくても [止める] なら正の定義が要る)。(2) **§7.3 の 15 の答え「測り方の文が名指さない升目」が、場面係に「名指す」の機械の定義を作らせた**。要件の文を切片に切って升目を判定する機械は、要件に無い規則の山(定義 G・登録簿・持ち主)を生む = A-17(規則を増やすだけ)、O-7(用語)。(3) 監査役(定義)は定義の内部の整合を検めるので、規則が増えるほど穴が増える(収束しない構造)。
- 作業者の側は問題なく進んだ(第 10 周: `_StrategySide` に戦略の側の物をまとめ、到達性 0、全試験は「3 failed, 4300 passed, 6 skipped, 1 warning」で、3 failed は場面集の試験 = 場面係の書きかけの ROOTCAUSE が原因と作業者が切り分けた)。

### 8.2 設計の直し(第 11 周から)
1. **升目の判断は機械で 2 値だけ**: 「場面にした」= その升目を `covers` に持つ場面がある / 「測っていない(固定した測り方の外)」= それ以外。**これは §7.3 の 15 の判定の基準(「測り方の文が名指す組」)を撤回して置き換えるもの**である。15 のうち残るのは「升目の表は測っていない範囲の記録で、要件を広げるかはオーナーの判断」の部分だけ。**要件の文を切片に切る「名指し」の判定・登録簿・持ち主・上位の決定の切片は作らない(定義 G は採らない。書きかけの ROOTCAUSE_r10-1.md は「採らなかった案」と見出しを付けて残し、DEFINITIONS には入れない)。**`covers` は場面の宣言で、批評家が「場面の測る物と covers が合わない」と見れば [直す]/[止める] にする(人の読みで検める。機械の解析で検めない)。
2. **升目の表の意味を 1 文で書く**: 「この表は測っていない範囲の記録である。要件を広げるかはオーナーの判断で、項目 0 の通過のあとに『欠けているもの』の批評家の経路で上げる。」
3. **定義を先に通す段は [止める] にだけ当てる**([直す] だけの直しは定義の段を置かず、直して監査に出す)。定義は指摘の族への **1 段落**で、新しい一般規則・手続き・登録簿・用語の表を足さない。足したい規則があれば「リードに聞くこと」に書いて止める。定義の段は 3 回で通らなければリードへ戻す(台本で固定)。
4. 監査役(定義)への文: 検めるのは「その定義が、指摘の族のどの形にも当たる正の規則か」と「固定した規則(要件・場面集の規則 1〜9・正の定義 0〜E)に反しないか」だけ。定義に無い一般規則の追加を求めない。
5. 正の定義 0〜E は **DEFINITIONS.md に 1 定義 1 段落で置いて凍結**する(ROOTCAUSE には根本原因と直しだけ)。

### 8.3 作業者 `作る:0#10` の「リードに聞くこと」への答え
- 到達性 0 の読み: **「文脈から届く物のうち、核の状態(`_StrategySide` を通らずに届く物)が 0 件」でよい。**戦略の側の物(写しの台帳・出口の箱・口・履歴の列・落とした事実)を戦略が届くのは §7.4 の 19・20 のとおり。配達ごとに作り直す作りは取らない。エンジンは `_StrategySide` の中の物を書くだけで、読むのは箱を 1 度写すときだけ、を試験で固定する。
- 落とした事象の事実(16 バイト × 件数)が増え続ける件: 要件の外なので上限を付けない。REPORT と契約に「history_limit は保つ事象の数を抑え、落とした事実の数は抑えない(1 件 16 バイト)」を書く。上限が要るかは項目 12・13 の要件を固定するときに上げる。
- i0-r9-04(集合の反復の順と PYTHONHASHSEED): 項目 8 の要件を固定するときに決める(§7.2 と同じ扱い)。
- 場面集の 3 件の落ちは場面係の書きかけが原因で、第 11 周で場面係が §8.2 の 1 を当てれば消える。

### 8.4 監査 51 回目の [聞く] 4(この手順の変更は A-13 の「枠組み」か)へのリードの答え
- 定義の段の当て方(どの重さの指摘に置くか・何回で戻すか)は、L-437 でオーナーに見せた手順の運用の細目と読む(枠組み = 問いの立て方・族の切り方・検定単位は変えていない)。ただし A-13 の趣旨に沿い、9 回目の起動の前にこの変更をオーナーへの報告に書いて見せる(事後ではなく起動前)。オーナーが違うと言えば戻す。

### 8.5 第 11 周の監査役(場面)r11-1 の [聞く] 2 件と作業者 `作る:0#11` の問いへのリードの答え(20:58 UTC)

24. **br11-1-1(`covers` が 1 回の実行で試した型より広い範囲を宣言できる)**: 宣言を狭める。**`covers` の事象の軸は、その場面の入力(scenes.py の入力の事象と `type_plan` で作る事象)に実際に含まれる型から機械で出す**(手で書いた一覧を持たない)。見る道の軸は場面の宣言のままでよいが、その道を実際に呼ぶ入力であることを批評家が読む。升目の表の「場面にした」= 「その升目の型が入力に含まれ、その道を宣言した場面が 1 回の実行で走った」の意味になる。次の場面集の直し(批評家が場面集の側に指摘を出した周)で当てる。新実装に有利な向き(測った範囲を広く見せる)を消す直しなので、判定バーは動かさない。
25. **br11-1-2(候補 35 を試した記録が無い)**: 事実。リードはまだ試していない(run が走っている間は場面集のファイルに触らない = 指紋が変わる)。項目 0 の通過の報告には、候補 35 を「比べていない候補(権限の分類器の拒否。リードは未確認)」と名前つきで書く。run が止まった時機に、リードが公開の版の置き場所へ移す操作を 1 回試し、コマンドと出力を記録に残す(それが通れば次の周で場面係が走らせる)。
26. **作業者の問い 1(核は `_StrategySide` の中の参照の列を切り出して新しい履歴の列を作る)**: その読みでよい。核が読むのは参照の並びだけで写しの中身は読まず、その列は戦略から届かない(試験あり)。列を核の側へ移す・落とすたびに写し直す、はどちらも取らない。
27. **作業者の問い 2(`__del__`・weakref の呼び戻しは核の手順の中でも走る)**: 要件の外。契約の scope に「対象外」と書けば足りる。核はそこから判断しない、を試験で固定していれば十分。
28. **作業者の問い 4(第 9 周の批評家の試しが第 10 周からの版では名前で口を探せない)**: 批評家への注意として `lead_notes` に入れる必要はない。批評家は自分の試しを作り直す(直りを確かめるのは批評家の仕事で、古い試しが動かないことは直りの証拠ではない)。

### 8.6 第 12 周の定義 r12-1-1 と監査役(定義)r12-1-1 へのリードの答え(22:00 UTC。場面係は定義 r12-1-2/3 でこれを当てる)

29. **§8.5 の 24 を機械の規則として確定する(意味の条件を足さない)**: `covers` の事象の軸は、**その場面を走らせた記録(runner が場面ごと・設定つき対象ごとに残す入力の事象の型)から機械で出す**。scenes.py の手書きの宣言も、「正解がその事象を名指すか」「その道を通さなければ正解にならないか」という意味の読みも、使わない。升目 (e, v, x) を「場面にした」と数えるのは、**この場面集の走った記録のどれか 1 本で、型 e の事象が入力にあり、見る道 v を宣言した場面が走ったとき**。型を対象が選ぶ場面は、走った対象ごとに実際に入った型だけを数える(6 型を宣言しても 1 型しか走らなければ 1 型)。事象を持たない場面(p2-iso-*)は事象の軸のどの値も「場面にした」にしない。見る道の軸は場面の宣言のままで、その道を実際に呼ぶ入力かは批評家が読む(§8.5 の 24 のとおり)。
30. **定義 r12-1-1 の「正解が名指す事象そのもの」「必要性」の条件は採らない**(監査役(定義)r12-1-1 の 1・2・4・5 のとおり、§8.5 の 24 を超える意味の条件で、「名指し」の考え方の復活でもある)。除外の節 (a)〜(h) の一覧も持たない。定義は 29 の 1 段落だけ。p6-place-then-cancel の見る道(読む物)の covers が合うかは批評家の読みに任せる(i0-r11-02 の「人の読みで残る点」)。
31. **作業者へ(i0-r11-01・03 の族 = 外の物のコードが核の手順の中で走る)**: 外の物を dict の鍵にしない、外の class の属性(`__module__`・`__name__` を含む)を読まない、外の物の buffer を参照で持たない(落とした事実の array は戦略に buffer を出さない = memoryview を作れない型か、写しを渡す)。「外の物に核が触れる全ての操作」を、名前の一覧ではなく**核のコードから外の物へ届く全ての呼び出し**(dict の検索・比較・hash・buffer・属性の読み)として格子に列べ、格子は外の物の型の方法を全部差し替える敵対者で回す(§3.3)。

## 9. 9 回目の第 12 周の戻し(定義の段が 3 回で通らなかった)を受けたリードの根本原因の読みと設計の直し(2026-09-24 23:00 UTC)

### 9.1 何が起きたか(実測: run `wf_a29e5ac0-1a4` の各 agent の記録ファイルの最終書き込み時刻 = `stat -c %y <run>/agent-*.jsonl`、リードのコミットの時刻 = `git log --format='%h %ci' e75c931`。開始時刻は残っていないので終了時刻だけを書く)

| 段 | 終了(UTC) | 結果(逐語は `battery/AUDIT.md`) |
|---|---|---|
| 定義 r12-1-1 | 21:38 | 「正解が名指す事象」「必要性」の意味の条件と除外 (a)〜(h) |
| 監査役(定義) r12-1-1 | 21:43 | [止める] 2(§8.5 の 24 を超える意味の条件 / リードの承認前の論点を定義に確定)、[直す] 1、[聞く] 2 |
| 定義 r12-1-2 | 21:55 | `type_plan` の場面の型を「6 型を持つ対象のために scenes.py が作る組」から機械で出す |
| リードの §8.6(29〜31)のコミット e75c931 | 21:55:40 | 「`covers` の事象の軸は走った記録から出す」 |
| 監査役(定義) r12-1-2 | 22:00 | [止める] 1(repeat_of i0-r11-02: §8.6 の 29「走った記録」と正面から食い違う)、[聞く] 1 |
| 定義 r12-1-3 | 22:09 | 29 のとおり「走った記録」で書いた。実測: runner は `type_plan` の 4 場面の `types_1` の欄にしか対象に入った型を残していない(`run_battery.py` 790 行)→ 証拠の道具が残りの場面で `s.input`(宣言)を使った |
| 監査役(定義) r12-1-3 | 22:16 | [止める] 1(repeat_of i0-r11-02: 証拠が定義自身の禁止「宣言を使わない」に反する方法で作られている)、[聞く] 1 |
| 台本 | 22:45 | 定義の段 3 回 → リードへ戻し。作業者 `作る:0#12` は 22:45 に完了(全試験 4765 passed、落ちるのは場面集の側の批評家の試験 13 件だけ) |

生の出力(監査 52 回目の 52-6 で足した。`stat -c '%y %n' <run>/agent-*.jsonl | sort; git log --format='%h %ci' -1 e75c931`。agent の id と役の対応は各 `.meta.json` の description):

```
2026-09-24 20:26:05.023296423 +0000 agent-afcb010dd4b6ec9b2.jsonl
2026-09-24 20:30:59.687313939 +0000 agent-ae7d31b1d437acf90.jsonl
2026-09-24 20:34:22.499325994 +0000 agent-aa35d795cfbccd2de.jsonl
2026-09-24 21:04:45.703434371 +0000 agent-a0a7ed1808c421eb0.jsonl
2026-09-24 21:04:59.095435167 +0000 agent-ada05215ae42ca420.jsonl
2026-09-24 21:05:12.603435970 +0000 agent-a651d746abff1581c.jsonl
2026-09-24 21:05:26.699436808 +0000 agent-a7932d56403306578.jsonl
2026-09-24 21:05:53.643438410 +0000 agent-a2aaeb5db28bf65b6.jsonl
2026-09-24 21:06:26.599440369 +0000 agent-a9ad8290ac8bd7363.jsonl
2026-09-24 21:06:52.447441905 +0000 agent-ad324ddcfb5d6b90e.jsonl
2026-09-24 21:26:44.599512770 +0000 agent-a2ce3ac28b747689a.jsonl
2026-09-24 21:38:38.315555196 +0000 agent-a6addc6afa28ec51f.jsonl
2026-09-24 21:43:57.387574162 +0000 agent-a6e10026a8caaa08f.jsonl
2026-09-24 21:55:46.376326133 +0000 agent-a2ee6d2f3e8409898.jsonl
2026-09-24 22:00:48.007634236 +0000 agent-a1a5dcf7746db2d9b.jsonl
2026-09-24 22:09:57.927666925 +0000 agent-a47b6e40bf3b92934.jsonl
2026-09-24 22:16:12.839689211 +0000 agent-ac98dcfe8142e7755.jsonl
2026-09-24 22:45:27.303793502 +0000 agent-aa34e2a49faee8527.jsonl
e75c931 2026-09-24 21:55:40 +0000
```

リードの根本原因の読み(3 つ。どれもリードの側):
- (a) **リードの答えの時機**: §8.6 は定義 r12-1-2 の記録の最終書き込み(21:55)の直後にコミットされ(21:55:40)、その監査役(22:00)だけが読んだ。第 2 版は読めなかった規則で [止める] になった(監査役 r12-1-2 自身が「時刻は §8.5 の 24〈20:58 UTC〉より後の 22:00 UTC」と書いている)。台本にはリードの答えを待つ段が無く、リードは周の途中で設計を足した。
- (b) **無い記録を根拠にした規則**: §8.6 の 29 は「runner が場面ごと・設定つき対象ごとに残す入力の事象の型」を根拠にしたが、その記録は `type_plan` の 4 場面にしか無い(場面係の実測 `<S>item0_r12-1_scenekeeper_def3_records.txt`: ほかの場面は全レコード `types_1={'null': 53}`)。リードが記録の有無を確かめずに規則を書いた(O-2 の形)。定義の段はコードを変えない段なので、場面係は定義の証拠を作れず、第 3 版は宣言で代えて [止める] になった。定義の段の「コードを変えない」と、証拠に記録の機構が要る規則は両立しない。
- (c) **29 は批評家の指摘より広かった**: i0-r11-02 の批評家の神託(`tests/bt/critic/item_0/test_i0r11_covers_within_scene_input.py`)は「場面の入力(6 型を持つ対象のために runner が作る入力)にその型があるか」で、走った記録ではない(場面係 ROOTCAUSE_r12-1.md §6 の 5)。29 は、入力に無い事象(時計・注文の通知・取消の通知)の升目、凍結した正の定義 C(表は DEFINITIONS.md に「場面の定義と升目の軸から機械で作る」)、L-435(場面集の指紋が同じなら相手の結果を再利用)との食い違いも決めていなかった(場面係の §8 の 1・2・4)。

### 9.2 設計の直し(第 13 周から)

32. **§8.6 の 29 の「走った記録から出す」を取り下げ、次の正の定義 F に替える。**族 i0-r11-02 の定義はリードが書き、監査役(52 回目)を通した版を台本が `args.lead_definitions` で場面係に渡す。定義の段は置かない。この族の鎖は 0 から(L-438)。
    **正の定義 F(升目を「場面にした」と数える物)**: 升目 (e, v, x) を場面 s が「場面にした」と数えるのは、s が見る道と追加の軸の組 (v, x) を宣言し、かつ型 e が s の**入力から機械で出る**ときだけである。市場の事象の型(約定・板の写真・板の差分・足・資金調達・清算)は、scenes.py が s の入力に載せた事象(`events` と `streams` の中身)の型の欄そのもの(`type_plan` の場面は runner が全 6 型を持つ対象のために作る組 = `for_target_types(s, TYPE_ORDER)` の入力の型の欄。型の欄の無い事象と、事象を持たない場面からは型を出さない。adapter が代えた型は数えない)。これは批評家 i0-r11-02 の神託 `_kinds(scene)`(`test_i0r11_covers_within_scene_input.py` 38-49 行)と同じ計算である(監査 52 回目の 52-3 で直した)。時計・注文の通知(受付/拒否/約定)・取消の通知は、s の入力の戦略の頼みがその型を作らせる頼みであるときだけ(時計の頼み → 時計、発注 → 注文の通知、取消 → 取消の通知)。手書きの宣言・adapter が書く値・対象の出力・「正解がその事象を名指すか」「その道を通さなければ正解にならないか」の意味の読みは使わない。判断は機械で 2 値。(v, x) が s の測る物と合うか、s の入力がその道を実際に呼ぶかは批評家が読む(§8.5 の 24)。
33. **記録(表の判断には使わない)**: runner は場面ごと・設定つき対象ごとに、実際に対象に入った入力の事象の型(`types_in`)と、戦略が出した頼み(`requests`)の欄を記録(`survey_results/` と周の materials)に残す。資料係の表の注記に、対象ごとの「場面にしたが、この対象には型が入らなかった升目」を機械で出す(対象ごとの測っていない範囲)。試験: 全記録の `types_in` ⊆ F で出した型(超えれば試験が落ちる = adapter が型を足していない)。
34. **場面集の直し(第 13 周の場面係、定義の段なし)**: scenes.py の `COVERS` の事象の手書きの一覧を消し、(v, x) の宣言だけを残し、`covers_of(s)` を F で計算する関数にする。`grid_c.py`・`gen_definitions.py` は `covers_of` から表を作る。DEFINITIONS.md の正の定義 C の「`covers` は場面の宣言で」の文を F の文に替え、F を正の定義の節に足す(凍結の変更はリードの決定 = この §9。場面係は替えるだけ)。批評家の試験 `test_i0r11_covers_within_scene_input.py` は変えずに通す。p6-place-then-cancel の読む物の covers が合うかは批評家の読み(§8.6 の 30 のまま)。表は DEFINITIONS.md に置き、記録に依らないので場面集の指紋は対象の結果で変わらない(L-435 は保たれる)。
35. **台本と手順**: (a) `args.lead_definitions[項目][指摘 id]` があり、[止める] の指摘の全部に定義があれば、定義の段を置かずにその定義で `場面の直し` へ進む。(b) (採らない。最初の版にあった「監査役は記録の版より後の規則で [止める] にしない」は、監査 52 回目の 52-7・52-8 のとおり監査役の判定を弱める側の規則で、(c) の手続きで足りるので台本と委任文から外した。) (c) リードは、定義の段・直しの段が走っている間は LEAD_DESIGN に規則を足さない。場面係・作業者の問いへの答えは次の周の `lead_notes` で渡す(今回の (a) を機械で防ぐ)。
36. **候補 35(§8.5 の 25)**: run が止まった時機にリードが試した(2026-09-24 22:50〜22:51 UTC)。実際に打ったコマンド(逐語。`C` = clone `<S>/venvs/item_0/_dl/c35`(794fa647)、`S` = リードの作業用の写し `c35_trial`): `rm -rf $S; mkdir -p $S && cp -r $C/. $S/ && mkdir -p $S/src && for f in "$S"/src\ *.py; do b=$(basename "$f"); mv "$f" "$S/src/${b#src }"; done` → src/ に `__init__.py execution.py impact.py metrics.py models.py simulator.py strategies.py visualization.py` の 8 本。`python3 -m venv .venv && .venv/bin/pip install --no-cache-dir -q -r requirements.txt` → `pip rc=1`(requirements.txt の先頭行は '`requirements.txt`' と '```text' = Markdown の囲みつき)。`rm -rf .venv && python3 -m venv --system-site-packages .venv && .venv/bin/pip install --no-cache-dir -q matplotlib` → `pip rc=0`。`MPLBACKEND=Agg timeout 600 .venv/bin/python main.py` → `run rc=1`、`FileExistsError: [Errno 17] File exists: 'outputs'`(公開の版の `outputs` は 1,139 バイトのファイル)。`mv outputs outputs_file_as_published.txt && MPLBACKEND=Agg timeout 600 .venv/bin/python main.py` → `run rc=0`、出力の末尾「Experiment completed successfully. Generated files: outputs/cost_distribution.png / cost_risk_frontier.csv / cost_risk_frontier.png / execution_summary.csv / execution_trajectories.png」、TWAP Mean Cost 52496075.8166 ほか 3 戦略の表。置き換えの操作は権限の分類器に止められなかった。第 13 周で場面係が同じ 3 手で走らせ、検討表の行を「走った」に直す(結果が「対応なし」でも記録に残す)。

### 9.3 作業者 `作る:0#12` の「リードに聞くこと」への答え

37. `__qualname__` を type の C の欄から、`__module__` を class の辞書の反復で取る読み方: **認める**。§8.6 の 31 の「読まない」は「外のコードが走る読み方をしない」の意味で、型の名前を文に書くことは保つ(契約 type_decisions と第 11 周の格子 1 の神託は変えない)。
38. 落とした事実の第 3 の形(核は新しい塊の `list.append` だけ、まとめは戦略の読み出しの中、書き出し中なら新しい配列): **認める**。88.2 バイト/件の実測を契約 history_limit の文に残す。
39. 振る舞いの変化 3 つ(Python で書いた数の class を断る / Sequence の答えは list か tuple / 出口の箱で numpy の数を受ける): 契約 core-14 に書いたことで足りる。項目 11(参照実装)と項目 13 は契約を読む。別の周知は要らない。
40. shown の列の上限: 項目 12・13 の要件を固定するときに決める(要件の候補に残す)。
41. 候補 35 は 36 のとおり。i0-r11-02 の批評家の試験 13 件は 34 で場面係が通す。作業者は第 13 周で、批評家が i0-r11-01・03 の直りを確かめるのを待つ(新しい実装の側の指摘は無い)。

### 9.4 監査 52 回目(委任文 40365240c384)へのリードの処置

- 52-1 [止める] と 52-7・52-8 [聞く]: 監査役の判定を弱める側の規則(記録の版より後の規則で [止める] にしない)は採らない。台本の `VERSION_RULE` と委任文の同じ文を外した。再発防止は 35 (c) の手続き(リードは段の途中で規則を足さない)だけで行う。§0 の行の右の列に、逐語に無い 3 語(リードが定義を書く / 定義の段を置かない / 段の途中で足さない)を「リードの具体化」と明記した。
- 52-2 [聞く]: リードが族の定義を書いて定義の段を置かないのは、A-13 の「枠組み」(監査の当て方)に当たると読む。§8.4 の先例どおり、10 回目の起動の前にオーナーへの報告に書いて見せる(この会話の 2026-09-24 23 時台 UTC の報告と `OWNER_STATUS.md`)。オーナーが退ければ 10 回目は止めて定義の段を戻す。
- 52-3 [聞く]: 実測した。批評家の神託 `_kinds(scene)` は `type_plan` の場面は `for_target_types(s, TYPE_ORDER)`、ほかは場面の入力の `events`・`streams` の `kind` そのもの(38-49 行)。F の「runner の選び方の規則が最初に選ぶ型」はこの神託に無い計算だったので、F を神託と同じ計算(入力の型の欄そのもの)に直した(32)。
- 52-4 [止める]: 記録の命令は事後の言い換えで、for 文として不正だった。実際に打った形(この会話の Bash の呼び出しの逐語)に 36 と記録の末尾を書き直した。
- 52-5 [直す]: 実測した。批評家の文は 2 か所にある: 批評家自身が書いたファイル `round_11/CRITIC.md`(「走る。断りの型も」「の 18 升目は」)と、台本に返した構造化の返り値(journal: 「走り、断りの型が」「のうち 18 升目は」。`grep -c` でそれぞれ 1 / 0)。10 回目の引数の `last_findings` は返り値の逐語で、リードは括弧の注記を先頭に足しただけ。9 回目の記録の写しは CRITIC.md の側。両方とも批評家の語で、リードは書き換えていない。記録にこの出所の違いを書いた。
- 52-6 [直す]: §9.1 に `stat` と `git log` の生の出力を足した。
