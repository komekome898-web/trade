# 盲検の審査員の出力(項目 0 第 4 周、Workflow の記録から逐語で書き出し)

## 盲検:0#4:current0(agent a60a21db70b0603e0)

```json
{
  "choice": "左",
  "reasons": "7 観点を 1 つずつ比べた(場面の数では重み付けしていない)。行 A は 6 観点で B より上、1 観点(P0-2)で同じ。B が上の観点は 1 つも無い。\n- P0-1: A は 3 場面すべて正解と一致。B は p1-one-call-per-event だけ一致で、p1-merge-by-time と p1-typed-events は「対応なし」。A が上。\n- P0-2: A も B も 4 場面すべて正解と一致。同じ。\n- P0-3: A は 11 場面すべて一致。B は p3-bar だけ一致で、残る 10 場面は「対応なし」(足以外の事象型、時計、注文の通知)。A が上。\n- P0-4: A は p4-visible-at-step と p4-received-time が一致。B は p4-received-time が「対応なし」。p4-future-read-attempt は両方とも同じ値で不一致(every_attempt_stopped_by_error=false、future_value_obtained=false)。A が上。\n- P0-5: A は 3 場面すべて一致。B は p5-same-stream-order だけ一致で、p5-same-time-twice と p5-hand-over-order は「対応なし」。A が上。\n- P0-6: A は 3 場面すべて一致。B は 3 場面すべて「対応なし」。A が上。\n- P0-7: A は 5 場面すべて一致。B は fill-model-swap と cost-model-swap だけ一致で、latency-model-swap、cost-per-unit、account-swap は「対応なし」。A が上。\n- 再現: A も B も 32 場面すべて「2 回の実行で同じ」なので、再現では差がつかない。\n- A に「結果なし」は無く、不一致は 1 場面だけで、その場面は B も同じ値の不一致。\nよって A の方が、正しさ(正解との一致と能力の有無)でより正しく、再現では B と同じ。",
  "missing_in_choice": "P0-4 の p4-future-read-attempt(能力の場面)。値は不一致({\"every_attempt_stopped_by_error\": false, \"future_value_obtained\": false})。未来の値 104 は漏れていない。一方で、5 本目の足を時刻か位置で名指して読もうとしたとき、全部の試しが例外で止まってはいない。要件の測り方「実行時エラーか型エラーで止まるか(素通りしたら不合格)」を満たしていない。ほかの 31 場面は正解と一致し、32 場面すべてが「2 回の実行で同じ」。"
}
```

## 盲検:0#4:current1(agent ab684d82e959f4c12)

```json
{
  "choice": "右",
  "reasons": "7 観点を 1 つずつ比べると、行 B はどの観点でも行 A と同じか上で、下回る観点は 1 つもない。場面数の多い P0-3(11 件)を除いて比べても結論は変わらない。\n- P0-1: B は 3/3 が「正解と一致」。A は 1/3 で、p1-merge-by-time(時刻順の併合)と p1-typed-events が「対応なし」。\n- P0-2: A・B とも 4/4 が「正解と一致」で同点。\n- P0-3: B は 11/11。A は p3-bar の 1 件だけが一致で、残る 10 件(約定・板・清算・時計・注文の通知など)は「対応なし」。\n- P0-4: B は 2/3、A は 1/3。A は p4-received-time が「対応なし」。p4-future-read-attempt は両方とも同じ値の「不一致」(every_attempt_stopped_by_error=false、future_value_obtained=false)で同点。\n- P0-5: B は 3/3。A は 1/3 で、p5-same-time-twice と p5-hand-over-order が「対応なし」。\n- P0-6: B は 3/3。A は 0/3 で、発注・取消・約定の通知が全部「対応なし」。\n- P0-7: B は 5/5。A は 2/5 で、遅延模型・1 単位あたりの費用・口座の差し替えが「対応なし」。\n再現は、A・B とも 32/32 場面で「2 回の実行で同じ」で差がない。以上から、正しさ(能力の有無を含む)で B が明らかに上で、再現は同点なので、B を選ぶ。",
  "missing_in_choice": "p4-future-read-attempt(P0-4 の能力の場面)。結果は「不一致」で、値は every_attempt_stopped_by_error=false、future_value_obtained=false。5 本目の足(104)は漏れていないが、5 本目を名指した読み出しが全部は例外で止まっていない。空の結果か切り詰めた結果を返す素通りがあったと読める(試しの中身は表に無いので未確認)。要件の「未来を読もうとするコードが実行時エラーか型エラーで止まる」を満たしていない。行 A も同じ値の不一致で、この場面は差にならない。"
}
```

## 盲検:0#4:current2(agent a507e383a35bb4020)

```json
{
  "choice": "左",
  "reasons": "場面の数に引きずられないよう、観点ごとに比べた。\n正しさ(正解との一致)\n・P0-1: A は 3/3、B は 1/3(p1-merge-by-time と p1-typed-events は対応なし)→ A が上\n・P0-2: 両者とも 4/4 → 同じ\n・P0-3: A は 11/11、B は 1/11(p3-bar だけ一致、ほかは対応なし)→ A が上\n・P0-4: A は 2/3、B は 1/3。p4-future-read-attempt は両者とも同じ値で不一致({every_attempt_stopped_by_error: false, future_value_obtained: false})。B はさらに p4-received-time が対応なし → A が上\n・P0-5: A は 3/3、B は 1/3 → A が上\n・P0-6: A は 3/3、B は 0/3(全部対応なし)→ A が上\n・P0-7: A は 5/5、B は 2/5(遅延模型の差し替え・数量単位の費用・口座の差し替えが対応なし)→ A が上\n7 観点のうち A が上なのが 6、同じなのが 1(P0-2)。B が上の観点は無い。B が A に勝つ場面も 1 つも無い。B の足りない分はすべて「対応なし」(能力が無い)で、A の不一致は 1 件だけ。その 1 件は B も同じ値で不一致なので、差にはならない。\n再現(2 回の実行の一致): 両者とも全観点 32/32 で同じ。差は無い。\n以上から、正しさでは A が観点ごとに優勢か同等で、再現は同等。よって A(左)の方が正しく、再現性は同じ水準と判断する。",
  "missing_in_choice": "P0-4 の p4-future-read-attempt だけ(1 場面)。T0 + 4 日の時点で戦略が 5 本目の足(T0 + 5 日、終値 104)を時刻や位置で名指して読もうとしたとき、A では例外で止まらない読み出しが残っていた(every_attempt_stopped_by_error = false)。104 は漏れていない(future_value_obtained = false)が、要件は「未来を読もうとすると例外で止まる」ことで、空の結果や切り詰めた結果を返して素通りさせる形になっている。ほかの 31 場面は正解と一致し、2 回の実行でも同じ結果になった。"
}
```

## 盲検:0#4:survey0(agent a03d35916cafe5e31)

```json
{
  "choice": "右",
  "reasons": "再現の欄では差がつかない。行 A も行 B も 32 場面すべてが「2 回の実行で同じ」である。差があるのは正しさの欄の 1 場面だけで、P0-4 の p4-future-read-attempt(能力の場面)である。行 B は正解と一致した。行 A は不一致で、出力は {\"every_attempt_stopped_by_error\": false, \"future_value_obtained\": false} だった。つまり A は未来の値 104 を漏らしてはいないが、5 本目を名指した読み出しの少なくとも 1 つが例外で止まらずに素通りした。定義はこの場面の正解を「空の結果・切り詰めた結果・値を返すのは素通りで、正解ではない」としており、P0-4 が求める「構造で(読もうとすると例外で)止める」能力が A には無い。P0-1・P0-2・P0-3・P0-5・P0-6・P0-7 の 6 観点は、A・B とも全場面が一致・再現で同じなので、どれも引き分けである。場面の多い P0-3(11 件)に引きずられずに観点ごとに比べても、B が上回るのは P0-4 の 1 観点で、A が上回る観点は無い。以上から、B の方がより正しく、再現性は A と同じと判断した。",
  "missing_in_choice": "なし。行 B は 32 場面すべてで正解と一致し、再現も 2 回の実行で同じである。ただし表の注記によると、p6-place-then-cancel では道具の 1 つが結果なし(例外 AttributeError・TypeError)で、この場面の行は残りの道具のうち最も良い結果が載っている。その道具が A と B のどちらのものかは表からは分からない。"
}
```

## 盲検:0#4:survey1(agent a22cd77adc7cb2d36)

```json
{
  "choice": "左",
  "reasons": "7 つの観点を 1 つずつ比べると、6 つの観点(P0-1・P0-2・P0-3・P0-5・P0-6・P0-7)では A と B に差がありません。どちらも全場面で「正解と一致」、再現も「2 回の実行で同じ」です。\n\n差が出たのは P0-4(戦略が「受け取れた時刻 ≤ 今」の事象しか見られないこと)の 1 観点だけです。\n- A: 3 場面とも「正解と一致」。\n- B: p4-future-read-attempt(能力の場面)が「不一致」。値は every_attempt_stopped_by_error=false、future_value_obtained=false でした。未来の値 104 は漏れていません。ただ、5 本目を名指した読み出しの少なくとも 1 つが例外で止まらず、空か切り詰めた結果を返して素通りしています。要件の測り方「実行時エラーか型エラーで止まるか(素通りしたら不合格)」を満たさないので、未来の読み出しを構造で止める能力が B には無いと表から読めます。\n\n再現は両者とも 32 場面すべてで「2 回の実行で同じ」で、差はありません。B の不一致も 2 回とも同じ結果です。\n\n以上から、観点単位で見ても A は B に対して 1 観点で勝ち、ほかは引き分けです。場面数の多い P0-3(11 場面)は両者同点なので、判断には影響していません。\n\n補足: 表の注記によると、p6-place-then-cancel では動かせた道具の 1 件が「結果なし」(AttributeError・TypeError)で、その行は残りの道具の最も良い結果で埋められています。注記は A と B のどちらかを書いていないので、この件をどちらか一方の減点には使えません。",
  "missing_in_choice": "表の上では、A に足りない場面はありません。32 場面すべてが「正解と一致」かつ「2 回の実行で同じ」です。確かめられていない点が 2 つあります。\n- p6-place-then-cancel の注記にある「動かせた道具のうち 1 件は結果なし(AttributeError・TypeError)」が A 側のことだった場合、その道具では A も結果が出ていません。どちらの対象かは表からは確かめられません。\n- DEFINITIONS.md は P0-2 の拒否の側(秒・ミリ秒・ISO 文字列の時刻をそのまま渡したとき、止めるか黙って受けるか)を場面にしていません。この点は A についても未測定です。"
}
```

## 盲検:0#4:survey2(agent a91a3a683fd7a6e45)

```json
{
  "choice": "右",
  "reasons": "7 つの観点のうち 6 つ(P0-1・P0-2・P0-3・P0-5・P0-6・P0-7)は、全場面で A と B が同じ結果でした。正しさはどちらも「正解と一致」、再現はどちらも「2 回の実行で同じ」です。差が出たのは P0-4(戦略が「受け取れた時刻 ≤ 今」の事象しか見られないこと)の場面 p4-future-read-attempt だけです。B は正解と一致しました。A は不一致で、出力は {\"every_attempt_stopped_by_error\": false, \"future_value_obtained\": false} でした。A は未来の値 104 を漏らしてはいませんが、5 本目の足を名指した読み出しの中に、例外で止まらずに通ったもの(空・切り詰め・値の返却のどれか)があります。場面の定義は、要件の測り方(「素通りしたら不合格」)に従い、これを正解としていません。つまり A は、未来を構造で封じる能力を満たしていません。この場面の再現は、A も B も「2 回の実行で同じ」で差はありません。再現は両者とも全 32 場面で 2 回一致しており、差はありません。観点ごとに比べると、B が上の観点が 1 つ(P0-4)、A が上の観点は 0 です。場面の数には関係なく、B がより正しく、再現性は同等です。なお、表の注記にある p6-place-then-cancel の「結果なし」は道具 1 件の話です。その行は両対象とも「正解と一致」なので、比較には影響しません。",
  "missing_in_choice": "表の上では、B(右)が正解と一致しなかった場面はありません(32/32 が一致、32/32 が 2 回の実行で同じ)。場面の定義に書かれた「場面にしていない側面」、つまり P0-2 の拒否の側(秒の整数や小数の時刻を渡したとき、止めるか黙って受けるか)は、B についても測られていません。"
}
```
