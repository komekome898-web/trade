# 比較の表(項目 0、第 14 周)

A と B は名前を伏せた 2 つの対象。場面の定義は `tests/bt/battery/item_0/DEFINITIONS.md`。
各セルは 2 つの欄: 正しさ(正解と一致 / 対応なし / 不一致(値)/ 結果なし)と、再現(2 回の実行で同じ / 2 回で違う(値)/ 結果なし)。
良い順: 正解と一致 > 対応なし > 不一致 > 結果なし。正しさが同じなら「2 回の実行で同じ」が上。

注記: 型の組は対象の持つ型から規則で決めた

注記: 行 A の対象: 場面にしたが、この対象には型が入らなかった升目: 無し。頼みの記録が無く入ったかを決められない升目: 無し。

注記: 行 B に寄せた設定つき対象 54 件のうち 18 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-1 足/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 足/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 9 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 4 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-1 足/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 4 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 4 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 4 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 4 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 足/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 2 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-1 足/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 足/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 1 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-1 足/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 足/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 1 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の写真/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-1 足/戦略の呼び出しに届く物、P0-3 板の写真/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 足/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-4 足/戦略が対象の公開の手段で読む物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の写真/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物、P0-5 足/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 1 件: 場面にしたが、この対象には型が入らなかった升目: P0-1 約定/戦略の呼び出しに届く物、P0-1 板の差分/戦略の呼び出しに届く物、P0-3 約定/戦略の呼び出しに届く物、P0-3 板の差分/戦略の呼び出しに届く物、P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物、P0-4 約定/戦略の呼び出しに届く物、P0-5 約定/戦略の呼び出しに届く物、P0-5 板の差分/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 1 件: 場面にしたが、この対象には型が入らなかった升目: P0-3 資金調達/戦略の呼び出しに届く物、P0-3 清算/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 行 B に寄せた設定つき対象 54 件のうち 1 件: 場面にしたが、この対象には型が入らなかった升目: P0-3 資金調達/戦略の呼び出しに届く物。頼みの記録が無く入ったかを決められない升目: P0-3 時計/戦略の呼び出しに届く物、P0-3 注文の受付の通知/戦略の呼び出しに届く物、P0-3 注文の受付の通知/発注の呼び出しがその場で返す物、P0-3 注文の拒否の通知/戦略の呼び出しに届く物、P0-3 注文の拒否の通知/発注の呼び出しがその場で返す物、P0-3 注文の約定の通知/戦略の呼び出しに届く物、P0-3 注文の約定の通知/発注の呼び出しがその場で返す物、P0-6 注文の約定の通知/戦略が対象の公開の手段で読む物、P0-7 注文の約定の通知/戦略の呼び出しに届く物/約定模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/遅延模型、P0-7 注文の約定の通知/戦略の呼び出しに届く物/費用。

注記: 調査結果の側の候補 52 件のうち、この行に寄せたのは動かせた 37 件と、動かせなかった 15 件のうちその機構を一次資料どおりに書き直して走らせた 1 件の結果(計 38 件。設定の違いを別に数えると 54 対象)。残りの 14 件(理由の内訳: 一括の取得(数百 MB 以上) 1 件、動かなかった 2 件、危険(§6-1) 3 件、危険(台帳 §3 の 11 件) 4 件、外部の場 1 件、導入前の検査を通せない・依存が repository の外 1 件、鍵・登録 2 件)はこの行に入っていない(候補ごとの欠け。場面集の検討表で扱う)。

注記: 場面 p2-iso-utc: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p2-iso-offset: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p3-bar: この行に寄せた対象のうち 1 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 TypeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 22 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p3-clock-timer: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p3-notice-accepted: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p3-notice-rejected: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p3-notice-filled: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p4-visible-at-step: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p4-received-time: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p4-future-read-attempt: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p6-place-then-cancel: この行に寄せた対象のうち 16 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(3) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p6-cancel-notice: この行に寄せた対象のうち 14 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p6-fill-seen-by-strategy: この行に寄せた対象のうち 16 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(3) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p7-fill-model-swap: この行に寄せた対象のうち 18 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 採点の前の出所の検めで退けられた結果(対象が届けたことを示せない。例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(2) 出たもの: 採点の前の出所の検めで退けられた結果(対象が届けたことを示せない。例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(3) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(4) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(5) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(17) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(18) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p7-latency-model-swap: この行に寄せた対象のうち 13 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(2) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(3) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p7-cost-model-swap: この行に寄せた対象のうち 16 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(3) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p7-cost-per-unit: この行に寄せた対象のうち 16 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(3) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

注記: 場面 p7-account-swap: この行に寄せた対象のうち 16 件は結果なし(試したこと: 場面の定義どおりに対象の公開の口で走らせた。(1) 出たもの: 例外 CustomException。実測の時間: その対象を全場面に 2 回ずつ通した所要 65 秒(時間の打ち切りではない)/(2) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 218 秒(時間の打ち切りではない)/(3) 出たもの: 例外 AttributeError。実測の時間: その対象を全場面に 2 回ずつ通した所要 215 秒(時間の打ち切りではない)/(4) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(5) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 2 秒(時間の打ち切りではない)/(6) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(7) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(8) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(9) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(10) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(11) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(12) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(13) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(14) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない)/(15) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 1 秒(時間の打ち切りではない)/(16) 出たもの: この場面の機構を実装していない旨を返し、採点できる結果が無い(例外ではない)。実測の時間: その対象を全場面に 2 回ずつ通した所要 0 秒(時間の打ち切りではない))。この場面の行は残りの対象の最も良い結果 = 正解と一致。

## 観点ごとの「正解と一致」の数

| 対象 | P0-1 | P0-2 | P0-3 | P0-4 | P0-5 | P0-6 | P0-7 | 計 |
|---|---|---|---|---|---|---|---|---|
| A | 3 / 3 | 4 / 4 | 11 / 11 | 3 / 3 | 3 / 3 | 3 / 3 | 5 / 5 | 32 / 32 |
| B | 3 / 3 | 4 / 4 | 10 / 11 | 3 / 3 | 3 / 3 | 3 / 3 | 5 / 5 | 31 / 32 |

## 観点ごとの「2 回の実行で同じ」の数

| 対象 | P0-1 | P0-2 | P0-3 | P0-4 | P0-5 | P0-6 | P0-7 | 計 |
|---|---|---|---|---|---|---|---|---|
| A | 3 / 3 | 4 / 4 | 11 / 11 | 3 / 3 | 3 / 3 | 3 / 3 | 5 / 5 | 32 / 32 |
| B | 3 / 3 | 4 / 4 | 11 / 11 | 3 / 3 | 3 / 3 | 3 / 3 | 5 / 5 | 32 / 32 |

## P0-1 核が事象駆動(型を持つ事象を時刻順に流す)であること

場面 3 件: p1-merge-by-time(値)・p1-one-call-per-event(値)・p1-typed-events(能力)

| 対象 | p1-merge-by-time | p1-one-call-per-event | p1-typed-events |
|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-2 時刻が UTC の int64 ナノ秒で表されること

場面 4 件: p2-iso-utc(値)・p2-iso-offset(値)・p2-event-time-exact(値)・p2-one-ns-apart(能力)

| 対象 | p2-iso-utc | p2-iso-offset | p2-event-time-exact | p2-one-ns-apart |
|---|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-3 8 種の事象型(約定・板の写真・板の差分・足・資金調達・清算・時計・注文の受付/拒否/約定の通知)を扱えること

場面 11 件: p3-trade(能力)・p3-book_snapshot(能力)・p3-book_delta(能力)・p3-bar(能力)・p3-funding(能力)・p3-liquidation(能力)・p3-mixed-one-run(値)・p3-clock-timer(能力)・p3-notice-accepted(能力)・p3-notice-rejected(能力)・p3-notice-filled(能力)

| 対象 | p3-trade | p3-book_snapshot | p3-book_delta | p3-bar | p3-funding | p3-liquidation | p3-mixed-one-run | p3-clock-timer | p3-notice-accepted | p3-notice-rejected | p3-notice-filled |
|---|---|---|---|---|---|---|---|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 対応なし<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-4 戦略が「受け取れた時刻 ≤ 今」の事象しか見られないこと(構造で)

場面 3 件: p4-visible-at-step(値)・p4-received-time(値)・p4-future-read-attempt(能力)

| 対象 | p4-visible-at-step | p4-received-time | p4-future-read-attempt |
|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-5 同時刻の事象の並びが決定的で、規則に従うこと

場面 3 件: p5-same-time-twice(値)・p5-hand-over-order(能力)・p5-same-stream-order(値)

| 対象 | p5-same-time-twice | p5-hand-over-order | p5-same-stream-order |
|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-6 戦略の API(事象ごとの呼び出し・発注・取消)

場面 3 件: p6-place-then-cancel(値)・p6-cancel-notice(能力)・p6-fill-seen-by-strategy(能力)

| 対象 | p6-place-then-cancel | p6-cancel-notice | p6-fill-seen-by-strategy |
|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

## P0-7 他項目が核を書き換えずに差し込める口(約定模型・遅延模型・費用・口座)

場面 5 件: p7-fill-model-swap(能力)・p7-latency-model-swap(能力)・p7-cost-model-swap(能力)・p7-cost-per-unit(値)・p7-account-swap(能力)

| 対象 | p7-fill-model-swap | p7-latency-model-swap | p7-cost-model-swap | p7-cost-per-unit | p7-account-swap |
|---|---|---|---|---|---|
| A | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |
| B | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ | 正しさ: 正解と一致<br>再現: 2 回の実行で同じ |

