# 文書整理の案(2026-09-12、L-108 の順序 ③。オーナー承認待ち)

**状態: 提案。実行していない。** 棚卸し(委任、読み取りのみ。要点は §0)の上で、リードが「削除・統合・移動・分割・追加」の一覧を作り、
監査役の指摘 12 件(`docs/AUDITOR/VERDICTS/2026-09-12_docs_reorg_plan.md`)を反映した。**初版は対象ファイルを読まずに棚卸し表だけで書いており、監査役が 4 件の事実誤認を止めた**(D4 の参照先、F4 の P10、D10 の固有節、D11 の事実 5 件)。本版は対象の各ファイルを読んで直した。
オーナーが見るのは **§1 の削除一覧**が中心。承認された行だけ実行する。

判断基準はオーナーの 6 項目(L-105): 長すぎる / 多すぎる / あるのに読まない / 重複している / 不要 / 足りていない。
制約: CLAUDE.md §5.1 の生存指定物(データ・コード・手順・事故の記録・議論の記録・`STRATEGY_IDEAS.md`・
`DATA_CONSUMPTION_LOG.md`)と、追記専用の台帳(`OWNER_LOG.md`・`INCIDENTS.md`・`DISCUSSIONS/`)は削除しない。

## 0. 棚卸しの要点(測った数)

| 量 | 値 |
|---|---|
| `docs/` のファイル | 203(うち .md 130、約 34,150 行) |
| うち生成物(スクリプトの出力) | JSON 67 本 ≈ 57 MB、`*_TABLES.md` 15 本 = 20,090 行 |
| `docs/INDEX.md` に載っている文書 | 約 12 本。**載っていない .md が 60 本以上**(AUDITOR/ 67 ファイル、DATA_* 系、PHASE2_SPEC 等) |
| 400 行超の手書き文書 | 6 本(K1 RESULT 2,813 / OPERATIONS 896 / K1 PREREG 824 ×2 / OWNER_PROCEDURES 523 / K1 AUDIT_TRIAGE 404) |
| 被参照 0〜1 件の文書 | 約 10 本(DATA_QA_CHECKLIST 経由でしか参照されない一度きりの納品物 6 本を含む) |

「読まない」の根拠は被参照数(他の文書からの言及回数)だけであり、実際に読まれたかは測れない。

## 1. 削除(11 本 + スキル 1 つ)。**この表を承認してください**

| # | ファイル | 種類 | 理由 | 残す情報とその行き先 |
|---|---|---|---|---|
| D1 | `docs/DATA_QA_CHECKLIST.md`(24 行) | 手順 | 全項目「済」(2026-09-06)。完了したチェックリスト | 「データ QA 完了 2026-09-06(11 項目)」の 1 行を `DATA.md` の冒頭へ |
| D2 | `docs/DATA_QA_TRIAGE.md`(145 行) | 記録 | 2026-09-05 の品質点検の生記録。D1 の根拠。以後参照なし | 現在も効く品質の注記(欠測の既知区間)は `DATA.md` の該当行の備考へ |
| D3 | `docs/SINGLE_SOURCE_AUDIT.md`(74 行) | 記録 | 2026-09-05 の直読み棚卸し。修正済み | 無し(修正はコードに入っている) |
| D4 | `docs/DATA_GOVERNANCE_PLAN.md`(75 行) | 手順 | 2026-09-05 の設計書(§2 設計、§4 スナップショットのバイト完全一致)。実装は `fetch_all.bat` の `retention_snapshot.py` / `verify_snapshots.py` / `intake_ledger.py`。**運用文書にこの仕組みの説明は無い**(初版で挙げた `OPERATIONS.md` §6.5 は「ログを Claude に見せる手順」で無関係。監査役の指摘で訂正) | §2 の設計の要点と §4 の「バイト完全一致の検証」を **`DATA.md` 冒頭の「守る仕組み」節**(新設、D8 の §0 と合わせて 1 表)へ |
| D5 | `docs/DATA_PLAN_2026-08-23.md`(69 行) | 手順 | 最古の収集計画(Round 17)。全捨て前の研究に紐づく。**相場についての断定が 3 行残っている**(「往復 0.55% のコスト壁で戦略空間が閉じている」「DVOL は時計の情報を超えないと実証済み」等)= 2026-09-08 の全捨てで消すべきだった行(KA-10 の型)。他の削除候補 10 本を同じ語で検索したが、相場の断定は無かった | 無し。§5.1 により断定は引用しない |
| D6 | `docs/CONSTANTS_TODO.md`(53 行) | 手順 | 「定数は単位ごとに測り直す」(§5.1)に置き換わった。計画としての役目は終わり | 無し(`config/constants.yaml` の `status: discarded` が真実) |
| D7 | `docs/DATA_LOSS_REGISTER.md`(53 行、表 16 行) | 状態 | 別台帳にする理由が無い | **`DATA.md` に「損失・欠損(事象単位、追記専用)」節を新設し、表を列そのまま(何が / 期間 / 原因 / 復旧可否 / 記録日)で移す**。資産単位の行には混ぜない(行の単位が違う。監査役の指摘) |
| D8 | `docs/DATA_PERISHABILITY.md`(86 行) | 状態 | 「消える」軸の台帳。別台帳は二重管理になる | **`DATA.md` に「保持期間(資産単位)」節を新設し、§1・§1.5 の表を列そのまま(資産 / 保持期間 / 出所の印 / 判定日)で移す**。§0 の「守る仕組み」表は D4 と同じ節へ |
| D9 | `docs/PHASE2_SPEC.md`(156 行) | 手順 | `research-protocol` スキルと同じ規律(封印・反復と停止・監査・判定)を再定義。**確認済み(grep)**: SPEC には L-043(意図マップ)・L-050(出荷前検査)の語が無く、スキルにはある = スキルの方が新しい | オーナー合意(2026-09-05)の事実、§10 開始条件、§11 未決事項の決着(5 項目)を `research-protocol` §0 に「フェーズ 2 の前提(2026-09-05 合意)」として原文のまま移す |
| D10 | `docs/PHASE2_TEMPLATES.md`(182 行) | 手順 | §1〜§4・§8(雛形、INTENT_MAP)はスキルと K1 の実物で二重。**§5 エッジ推移・§6 条件分析・§7 調達票とリード盲点監査は固有**(オーナー提案・指摘 2026-09-06 に由来し、スキルにも `DELEGATION.md` にも同じ詳細は無い。監査役の指摘) | **§5・§6・§7 は全文を `research-protocol` に節として移す**(削らない)。§1〜§4・§8 は見出し一覧だけ |
| D11 | `docs/NOTE_CRYPTO_CFD_2026-08-20.md`(24 行) | 記録 | 被参照 0。2026-08-20 の仕様確認 | **事実 5 件を全部移す**(Lightning FX 廃止と後継 / 商品コード継続 / SFD 廃止と資金調達率 8 時間ごと・上限 ±0.375% / 手数料 0%・レバ 2 倍 / 最小 0.001 BTC)→ `OPERATIONS.md` §1 に「商品仕様の確認(2026-08-20 実測)」として。**「戦略への影響」節(「シグナル系の結論に変更なし」)は §5.1 の全捨てで無効なので移さない** |
| D12 | `.claude/skills/tournament/`(56 行) | 手順 | 一度も呼ばれた記録が無い。前提の「チャンピオン戦略」は全捨てで消えた | 無し(必要になったら書き直す方が早い) |

**削除しないもの(検討したが残す)**: `DELEGATION.md`(オーナー承認の統治文書。§3 で修正)、
`docs/legacy/*`(一次資料)、`docs/AUDITOR/before/*`・`answers/*`(監査役の検査集合。週次の改良が使う)、
`docs/PHASE2/K1/*`(終了した研究の記録。§4 で扱う)、`POSTMORTEM_2026-09-04_tp_precursor.md`(事故の記録。§2 で移動)。

## 2. 移動(場所を正す。内容は変えない)

| # | 何を | どこへ | 理由 |
|---|---|---|---|
| M1 | `docs/PHASE2/**/*.json`(67 本、57 MB)と `*_TABLES.md`(15 本) | `results/PHASE2/...`(同じ相対パス。git 管理は継続) | 生成物は `docs/` の文書ではない。`docs/` の重さの 95% がこれ。スクリプトの出力先と `RESULT.md` の参照を書き換える(委任) |
| M2 | `docs/DATA_SOURCES/*.md`(5 本) | `docs/DATA/surveys/` | 調達調査は `docs/DATA/`(probes / SCAN)と同じ場所に置く。INDEX の「記録」に載る |
| M3 | `docs/POSTMORTEM_2026-09-04_tp_precursor.md` | `docs/DISCUSSIONS/2026-09-04_postmortem_tp_precursor.md` | 記録は `DISCUSSIONS/` に集める |
| ~~M4~~ | `docs/PHASE2/K1/PREREG.md.tmpl` | **取り下げ** | `preflight_prereg.py` は事前登録のパスから `.md.tmpl` を導出する(`with_suffix`)ので、雛形は事前登録の隣に置くのが設計。参照は `render_prereg.py`・`preflight_prereg.py`・`tests/test_preflight_prereg.py` の 3 箇所(監査役の指摘)。動かさない |

## 3. 直す(足りていない・矛盾)

| # | 何を | どう |
|---|---|---|
| F1 | `docs/INDEX.md` | 4 種の索引を**全文書**について書き直す(AUDITOR/、DATA/、PHASE2/、legacy/ を含む)。新しい文書を作る前に読む文書なので、抜けがあると意味が無い |
| F2 | `docs/DELEGATION.md` | 「週次要約」(削除済み文書)への言及 4 箇所を「`OWNER_STATUS.md` の判断が要る項目」に置き換える |
| F3 | `docs/PHASE2/K1/PREREG.md` | 冒頭に 3 行の注記だけ追加: 「L-056 以降の決定(帰無・MDE・バーを作らない)を反映していない。実態は `RESULT.md`。事後の書き換えはしない」。本文は触らない |
| F4 | `docs/OWNER_PROCEDURES.md`(523 行) | **状態板が「完了」と言う手順だけ**を「完了(日付、L 番号)。手順は git 履歴」に畳む。現時点で該当は **P8 §1(到達確認、完了 2026-09-08)のみ**。P8 §2 は記録中、P9 は状態板に完了の記載が無い、**P10 は期限 2026-09-23 で進行中**(初版は P10 を完了扱いにしていた。監査役の指摘で訂正)。→ 削減は約 60 行。P 番号は保つ |
| F5 | `docs/OPERATIONS.md`(896 行) | JPX 側(§5.1 ON1・§5.2 ETF 板寄せ、計 231 行)を `docs/OPERATIONS_JPX.md` に分ける。読者が違う(暗号資産の運用者 / JPX の実弾ジョブ)。本体 ≈ 660 行 |
| F6 | `docs/PHASE2/K1/AUDIT_TRIAGE.md` + `_2.md` | 1 本 `AUDIT_TRIAGE.md`(1 回目・2 回目の 2 節)に統合 |
| F7 | 増やさない規則(`INDEX.md` 冒頭に 3 行)**+ 読む導線** | (i) 一度きりの納品物(計画・チェックリスト・監査)は `docs/` 直下に置かず、`DISCUSSIONS/` か `AUDITOR/VERDICTS/` に日付つきで。(ii) 生成物は `docs/` に書かない。(iii) 「状態」は台帳 4 本以外に作らない。**規則を書くだけでは働かない**(L-104 でオーナーが却下した型、監査役の指摘)ので、既存の `Write` フック(表示のみ)を拡張し、`docs/` 直下に新しいファイルを作ろうとした瞬間にこの 3 行を表示する |

## 4. 分けない(検討したが見送り)

- `docs/PHASE2/K1/RESULT.md`(2,813 行): K1 は終了(L-118)。入口は `HANDOFF.md`(終了と再開条件を明記済み)で、
  部ごとに分けても読む人は増えない。参照 28 箇所を壊す危険の方が大きい。**触らない。**
- `docs/AUDITOR/`(67 ファイル): 監査役の検査集合と測定記録。週次の改良ループ(L-116)の入力で、構造は
  `IMPROVEMENT.md` が決めている。INDEX に載せる(F1)だけ。

## 5. 実行の順序と量

1. オーナー承認(§1 の行ごとに yes / no。§2〜§3 は異論が無ければ進める)
2. D1〜D12(残す情報を先に移してから削除。1 行ずつ移し先を確認。リード)
3. M2・M3・F2〜F6(移動・矛盾の修正・畳み込み。リード)
4. **F1・F7(索引の全面書き直しと導線)は削除・移動が終わってから最後に**(削除予定の文書を索引に載せないため。監査役の指摘で順序を入れ替え)
5. M1(生成物の移動。スクリプト 20 本の出力先とテストの書き換え。委任 → 監査役)

結果の見込み(数え直し): `docs/` 直下の .md は **24 本 → 12 本**(削除 11 + 移動 1)、`docs/` の容量 61 MB → **約 4 MB**、
索引の網羅率 12/130 → **全件**。行数: 削除 11 本の合計 941 行 + F4 の約 60 行から、移設で増える分(D4・D7・D8・D9・D10・D11 で約 140 行)を差し引き、**正味 約 850 行減**(生成物を除く)。

## 6. この案が根本的に解決しないこと

「あるのに読まない」は、文書の数を減らしても**読む導線が無ければ再発する**。導線はフック
(SessionStart の状態板・共有経路の表示、PreToolUse の規則表示)と INDEX の F7 で作るが、
それでも読まないなら文書の問題ではなくリードの問題(I-006)。ここでは文書の側だけを直す。
